---
context: default
phase: "debug: yt-live-drops-buffer-underrun"
task: null
total_tasks: null
status: paused
last_updated: 2026-07-11T03:17:59.067Z
---

# Paused: debug session `yt-live-drops-buffer-underrun`

> This is a `/gsd:debug` session pause, not a phase. The live record is
> `.planning/debug/yt-live-drops-buffer-underrun.md` (status `awaiting_human_verify`).
> Branch: **`fix/yt-live-drops-buffer-underrun`** (2 commits, not pushed, no PR).

<current_state>
YouTube live-stream drops investigated across two rounds. **Two distinct root causes**:

- **Round 1 — FIXED & VERIFIED.** Adaptive buffer growth (30s→60s→120s) was a silent no-op
  for live sessions: no reference to the running hlsdemux2 element was retained, so growth
  only moved a UI counter + staged for a URI rebind that live radio never performs. Fix
  retains the element and re-applies via `_apply_growth_to_live_hlsdemux2`. User confirmed:
  **buffer now reaches 100% (it never did before).** Commit `9b49182e`.
- **Round 2 — INSTRUMENTATION SHIPPED, PENDING REAL-WORLD VERIFY.** Residual sudden 100%→0%
  drains with 108–172s recovery are **segment-supply stalls inside hlsdemux2's own HTTP
  fetch/retry** — primarily YouTube-CDN-side, not player-fixable at the source. All 511
  production underruns logged `cause_hint=unknown` because the only cause-writer was wired
  to top-level `message::error` and hlsdemux2 retries never escalate to a fatal error. R2
  added `message-forward=True` + a `message::element` handler logging `hls_segment_fetch_issue`
  and setting `cause_hint=segment_retry`. Commit `3a5f3b41`.

Working tree clean apart from this handoff. 60 regression tests total, all green.
</current_state>

<completed_work>

- Round 1 root-cause + fix — Done (commit `9b49182e`, 24 tests)
- Round 1 real-world verification (buffer hits 100%) — Done
- Round 2 root-cause + observability instrumentation — Done (commit `3a5f3b41`, 36 tests)
- Split intermingled player.py diff into two atomic commits — Done
</completed_work>

<remaining_work>

- **USER ACTION:** Run the fixed build on a lofi live station (repeat offenders: station ids
  1851 / 3300 / 2093 / 3461). On the next rebuffer, inspect
  `~/.local/share/musicstreamer/buffer-events.log` around the `buffer_underrun` line.
- **Round 3 (gated on the above):**
  - If `cause_hint=segment_retry` + `hls_segment_fetch_issue` lines appear → build a
    stall-duration watchdog / forced reconnect-or-re-seek to cap the 100+s passive waits.
  - If a long stall STILL shows `cause_hint=unknown` with NO `hls_segment_fetch_issue` →
    new evidence of a silent sub-Bin stall; reopen with `/gsd:debug continue yt-live-drops-buffer-underrun`.
- Decide whether to push branch `fix/yt-live-drops-buffer-underrun` / open a PR (currently neither).
</remaining_work>

<decisions_made>

- Split the mixed player.py diff into two atomic commits (R1 buffer-growth, R2 instrumentation)
  via full-file backup → reduce to R1-only → commit → restore → commit R2. (User requested.)
- Deferred the stall-duration watchdog to Round 3 — insufficient evidence yet that a forced
  re-seek helps; ship instrumentation first. (User chose "wait for evidence".)
- Concluded the residual drains are primarily env/YouTube-CDN-side; the player's only lever
  is faster forced recovery, not a bigger buffer.
</decisions_made>

<blockers>
- Round 3 fix is blocked on a real user listening session to confirm `cause_hint=segment_retry`
  is the true driver. Round 2 instrumentation exists precisely to gather that evidence.
</blockers>

## Required Reading (in order)
1. `.planning/debug/yt-live-drops-buffer-underrun.md` — full evidence log, both rounds, resolution
2. Memory `yt-live-drops-two-root-causes.md` — condensed two-cause summary + Round 3 gate
3. Memory `hlsdemux2-owns-independent-segment-buffer.md` — why playbin3.buffer-duration ≠ hlsdemux2 buffer

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: treating live-stream drops as a buffer-*size* problem → No buffer covers a
  172s upstream gap. Attribute the cause (segment_retry vs unknown vs network) FIRST via the
  log, THEN choose a lever. Bigger buffers are not the residual fix.
- ANTI-PATTERN: "adaptive" growth that only updates a counter/UI without a handle to the live
  GStreamer element → verify the property actually reaches the running element (the Round 1 bug).

## Infrastructure State
- Branch `fix/yt-live-drops-buffer-underrun`: 2 commits ahead of `main`, NOT pushed, no PR.
- Evidence source (read-only): `~/.local/share/musicstreamer/buffer-events.log`.
- Debug session file status: `awaiting_human_verify`.

<context>
The investigation is genuinely done for now — the ball is in the user's court to run a real
session. Do not build the Round 3 watchdog speculatively; it was deliberately deferred pending
log evidence. Resume is evidence-driven, not time-driven.
</context>

<next_action>
Start with: wait for the user to report a real fixed-build listening session, then read
`~/.local/share/musicstreamer/buffer-events.log` for `cause_hint=segment_retry` /
`hls_segment_fetch_issue` around a `buffer_underrun`. Branch decision: build the Round 3
stall-duration watchdog if confirmed, else `/gsd:debug continue yt-live-drops-buffer-underrun`.
</next_action>
