---
phase: 97-resolve-station-url-duplication-between-the-top-level-standa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_repo.py
  - tests/test_edit_station_dialog.py
  - tests/test_url_helpers.py
  - tests/test_aa_siblings.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07]
must_haves:
  truths:
    - "All Phase-97 Wave-0 tests from 97-VALIDATION.md exist and currently FAIL (RED) because the production code is not yet written"
    - "Every implementation task in Waves 2-3 has a pre-existing automated test to turn GREEN"
  artifacts:
    - path: "tests/test_repo.py"
      provides: "5 canonical_stream_id migration/setter/FK tests (RED)"
      contains: "test_canonical_stream_id_migration_idempotent"
    - path: "tests/test_edit_station_dialog.py"
      provides: "6 canonical dialog tests (RED) + drift-guard for url_edit removal"
      contains: "test_url_edit_widget_does_not_exist"
    - path: "tests/test_url_helpers.py"
      provides: "canonical_url FK-resolution + position-1 fallback test (RED)"
      contains: "test_canonical_url_resolves_fk_with_position1_fallback"
    - path: "tests/test_aa_siblings.py"
      provides: "AA sibling detection keyed off canonical_url test (RED)"
      contains: "canonical"
  key_links:
    - from: "tests/test_repo.py"
      to: "musicstreamer.repo.Repo.set_canonical_stream"
      via: "method call asserting round-trip"
      pattern: "set_canonical_stream"
    - from: "tests/test_edit_station_dialog.py"
      to: "edit_station_dialog._COL_CANONICAL / _canonical_row"
      via: "dialog attribute reads"
      pattern: "_canonical_row|_COL_CANONICAL"
---

<objective>
Create all Phase-97 Wave-0 test stubs from 97-VALIDATION.md so every downstream
implementation task (Waves 2-3) has a pre-existing automated test to turn GREEN.
This plan is Nyquist scaffolding: the tests assert the FINAL intended behavior of
canonical_stream_id (DB), the canonical marker UI, and the canonical_url accessor,
and therefore MUST fail (RED) at the end of this plan because production code is
not yet written.

Purpose: Establish the feedback substrate (RED tests) before any production change,
so each later task can be validated in <90s and no 3 consecutive tasks lack an
automated verify (Nyquist sampling continuity).
Output: New/extended test functions in 4 test files, all currently failing.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-CONTEXT.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-VALIDATION.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Contracts the tests assert against. These do NOT exist yet — that is why the tests are RED. -->

Repo (musicstreamer/repo.py) — to be added in Plan 02:
  def set_canonical_stream(self, station_id: int, stream_id: Optional[int]) -> None
  # ALTER: stations.canonical_stream_id INTEGER REFERENCES station_streams(id) ON DELETE SET NULL
  # backfill: canonical_stream_id := position-1 stream per station

Station (musicstreamer/models.py) — to be added in Plan 02:
  canonical_stream_id: Optional[int] = None
  @property
  def canonical_url(self) -> str   # FK lookup -> position-1 fallback -> "" 

EditStationDialog (musicstreamer/ui_qt/edit_station_dialog.py) — to be added in Plan 03:
  self._canonical_row: int           # tracked canonical row index, pinned across reorder
  _COL_CANONICAL = 6                 # trailing column (RESEARCH Open-Q1 recommendation)
  # url_edit removed entirely (D-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 repo tests for canonical_stream_id (migration, backfill, setter, FK)</name>
  <read_first>
    - tests/test_repo.py (the preferred_stream_id test block, lines 873-971 — exact template)
    - musicstreamer/repo.py (set_preferred_stream at 814-820; migration ALTER at 282-295; db_init signature)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (tests/test_repo.py section — mirrored test bodies)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <files>tests/test_repo.py</files>
  <action>
    Add 5 test functions mirroring the existing preferred_stream_id test block (tests/test_repo.py:873-971):
    (1) test_canonical_stream_id_migration_idempotent — call db_init(repo.con) twice more; PRAGMA table_info('stations'); assert column canonical_stream_id present, type == "INTEGER", notnull index-3 == 0 (nullable), dflt_value index-4 is None. Mirror test_preferred_stream_id_migration_idempotent exactly.
    (2) test_canonical_stream_id_default_none_on_fresh_station — repo.create_station() with NO streams; assert repo.get_station(sid).canonical_stream_id is None (no streams => backfill EXISTS-guard skips it).
    (3) test_canonical_stream_id_backfill_defaults_position1 — create_station, insert_stream twice (s1 first => position 1, s2 second); call db_init(repo.con) to run backfill; assert repo.get_station(sid).canonical_stream_id == s1_id (ORDER BY position ASC, id ASC picks first-inserted).
    (4) test_set_canonical_stream_round_trip — seed two-stream station; repo.set_canonical_stream(sid, second_id); assert both repo.list_stations() and repo.get_station(sid) report canonical_stream_id == second_id (mirror the two preferred-stream round-trip tests).
    (5) test_canonical_stream_id_on_delete_set_null_when_stream_deleted — seed station, set canonical to a stream, repo.delete_stream(that stream_id) (or prune_streams to remove it) with PRAGMA foreign_keys ON (fixture already sets it); assert repo.get_station(sid).canonical_stream_id is None (ON DELETE SET NULL fired).
    Use the existing `repo` fixture and `_seed_two_stream_station` helper if present; if no two-stream helper exists, add a local one mirroring the preferred-stream test's seeding. Do NOT add production code — these MUST be RED.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py -k "canonical" --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_canonical" tests/test_repo.py` returns 5
    - `.venv/bin/python -m pytest tests/test_repo.py -k canonical --co -q` collects exactly 5 tests (collection succeeds, no ImportError)
    - Running `.venv/bin/python -m pytest tests/test_repo.py -k canonical -x` FAILS (RED — set_canonical_stream / column not yet implemented)
  </acceptance_criteria>
  <done>5 canonical repo tests exist, collect cleanly, and fail because production code is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 dialog tests (url_edit drift-guard, canonical marker, live read, save, reorder, auto-row, dirty-state)</name>
  <read_first>
    - tests/test_edit_station_dialog.py (test_is_dirty_after_url_edit at 885-888; test_stream_table_populated_and_add at 134-183; test_save_calls_repo_correctly at 214-229; the `dialog` and `station` fixtures)
    - musicstreamer/ui_qt/edit_station_dialog.py (column constants 278-283; _add_stream_row 1102-1137; streams_table init 544-574; _snapshot_form_state 752-762)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (tests/test_edit_station_dialog.py section — replacement bodies)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <files>tests/test_edit_station_dialog.py</files>
  <action>
    Add NEW test functions (do not yet touch the ~14 existing url_edit tests — that mechanical update happens in Plan 03 where the widget is removed, to avoid breaking currently-green tests in this RED-only plan). Import _COL_URL and _COL_CANONICAL from musicstreamer.ui_qt.edit_station_dialog at module top (the _COL_CANONICAL import is what makes these RED until Plan 03 defines it).
    Add:
    (1) test_url_edit_widget_does_not_exist — assert not hasattr(dialog, "url_edit") (D-01 drift-guard).
    (2) test_canonical_marker_defaults_to_row_0 — open dialog on a 2-stream station with canonical_stream_id None/unset; assert dialog._canonical_row == 0.
    (3) test_metadata_reads_canonical_cell_live — set canonical row URL cell text via table.item(dialog._canonical_row, _COL_URL).setText("http://twitch.tv/x"); assert dialog._get_canonical_url_live() == "http://twitch.tv/x" (D-02 live unsaved read).
    (4) test_save_persists_canonical_stream_id — drive a save; assert repo.set_canonical_stream was called once with the canonical stream's id (use FakeRepo/MagicMock per existing test_save_calls_repo_correctly pattern).
    (5) test_canonical_marker_stays_pinned_after_reorder — 2-stream station, canonical on row 0; trigger _on_move_down (or _swap_rows + move handler) so the canonical stream's content moves to row 1; assert dialog._canonical_row == 1 AND the same stream_id (Qt.UserRole on _COL_URL item) is still canonical (D-04 pinned-by-content, not positional).
    (6) test_auto_create_primary_row_on_empty_station — open dialog on a station with zero streams; assert dialog.streams_table.rowCount() == 1 and dialog._canonical_row == 0 (D-03).
    (7) test_dirty_state_captures_canonical_url_not_url_edit — assert _snapshot_form_state() contains key "canonical_url" and NOT key "url" (Pitfall 7).
    These MUST be RED (module import of _COL_CANONICAL fails / attrs absent). Use the existing dialog/station/FakeRepo fixtures.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_edit_station_dialog.py -k "canonical or url_edit_widget or auto_create_primary or dirty_state_captures" --co -q ; echo "expect collection error or RED — production not written"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_edit_station_dialog.py` increased by exactly 7 vs pre-task baseline
    - The 7 new test names are present: grep for test_url_edit_widget_does_not_exist, test_canonical_marker_defaults_to_row_0, test_metadata_reads_canonical_cell_live, test_save_persists_canonical_stream_id, test_canonical_marker_stays_pinned_after_reorder, test_auto_create_primary_row_on_empty_station, test_dirty_state_captures_canonical_url_not_url_edit
    - The 7 tests FAIL or error on collection (RED) because _COL_CANONICAL / _canonical_row / _get_canonical_url_live do not exist yet
  </acceptance_criteria>
  <done>7 new dialog tests exist and are RED; existing url_edit tests left untouched (updated in Plan 03).</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 canonical_url accessor + AA sibling tests</name>
  <read_first>
    - tests/test_url_helpers.py (existing find_aa_siblings / suggest_similar tests for fixture shape)
    - tests/test_aa_siblings.py (existing AA sibling detection tests + how Station/StationStream are built)
    - musicstreamer/models.py (Station/StationStream dataclasses, lines 13-48 — to know field names: id, position, url, streams)
    - musicstreamer/url_helpers.py (find_aa_siblings 213-216; suggest_similar 354-358)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (models.py canonical_url property body)
  </read_first>
  <files>tests/test_url_helpers.py, tests/test_aa_siblings.py</files>
  <action>
    In tests/test_url_helpers.py add test_canonical_url_resolves_fk_with_position1_fallback covering all four resolution branches of the planned Station.canonical_url property:
      (a) canonical_stream_id set + matching stream present => returns that stream's url (even when it is NOT position 1);
      (b) canonical_stream_id None => returns position-1 stream url (sorted by position, id);
      (c) canonical_stream_id set but stale (no matching stream id) => falls back to position-1 url;
      (d) no streams => returns "".
    Build Station/StationStream dataclasses directly (no DB) per TESTING.md "do not mock model dataclasses".
    In tests/test_aa_siblings.py add a test asserting AA sibling detection keys off the canonical stream URL, not streams[0]: build a station whose canonical_stream_id points at a NON-first stream that is an AA URL while streams[0] is a non-AA URL; assert find_aa_siblings (or the sibling matcher under test) matches via the canonical URL. Name it so `-k canonical` selects it.
    These MUST be RED — Station.canonical_url does not exist yet and find_aa_siblings still reads streams[0].
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_url_helpers.py tests/test_aa_siblings.py -k canonical --co -q ; echo "expect RED"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_url_helpers.py contains def test_canonical_url_resolves_fk_with_position1_fallback (grep returns 1)
    - tests/test_aa_siblings.py contains at least 1 new test matching `-k canonical`
    - Both fail (RED): Station has no canonical_url property and find_aa_siblings reads streams[0]
  </acceptance_criteria>
  <done>canonical_url accessor test (4 branches) + AA-canonical sibling test exist and are RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds test code; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | New test fixtures (DB via tmp_path) | accept | Tests use pytest tmp_path ephemeral SQLite; no production data, no network, no secrets. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (97-RESEARCH.md: "This phase installs nothing"). No legitimacy gate required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_repo.py tests/test_edit_station_dialog.py tests/test_url_helpers.py tests/test_aa_siblings.py -k canonical --co -q` collects the new tests without ImportError on the test modules themselves (RED at run-time is expected and correct).
- The whole point: running these tests now FAILS. They turn GREEN incrementally across Plans 02-03.
</verification>

<success_criteria>
- All 13 Wave-0 test functions from 97-VALIDATION.md exist across the 4 files.
- Each new test fails (RED) because the production code is not yet written.
- No production source file is modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-01-SUMMARY.md` when done
</output>
