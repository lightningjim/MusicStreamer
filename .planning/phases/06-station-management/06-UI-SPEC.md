---
phase: 6
slug: station-management
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-21
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for Phase 6: Station Management.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

**Note:** This project is a GTK4/libadwaita Python desktop app. There is no web
framework, no shadcn, no Tailwind. The design system is GNOME HIG / Adwaita.
Token values below map to GTK/Adwaita equivalents, not CSS variables.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (GTK4 + libadwaita) |
| Preset | not applicable |
| Component library | libadwaita 1.x (Adw.* widgets) |
| Icon library | GNOME symbolic icons (freedesktop icon theme) |
| Font | system default (Cantarell on GNOME) |

---

## Spacing Scale

Follows the existing codebase convention (margins observed in edit_dialog.py and
main_window.py). All values are multiples of 4.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, tight row padding |
| sm | 8px | Panel margin-start/end, row internal spacing |
| md | 12px | Dialog content margins (exception: matches existing edit_dialog.py content-area margins — codebase compatibility) |
| lg | 16px | Separator breathing room |
| xl | 24px | Major dialog section gaps |
| 2xl | 32px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Station art preview size: 128x128px (matches existing `station_pic.set_size_request(128, 128)`)
- `Gtk.Spinner` in art preview slot: 32x32px centered within the 128x128 slot

---

## Typography

GTK4/Pango — no custom font scale is applied. All sizes are GNOME system defaults.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / entry label | system (approx 14px) | Regular (400) | 1.5 (system) |
| Dialog title (HeaderBar) | system (approx 15px) | Semibold (600) | 1.2 (system) |
| Section label (grid label, xalign=0) | system (approx 14px) | Regular (400) | 1.4 |
| Confirmation dialog heading | system (approx 16px) | Semibold (600) | 1.2 |

Source: GNOME HIG defaults; existing code does not override font size or weight anywhere.

---

## Color

GTK4/Adwaita — colors are resolved by the system theme. No hex values are hardcoded.

| Role | Adwaita Token | Usage |
|------|---------------|-------|
| Dominant (60%) | `@window_bg_color` | Window background, dialog background |
| Secondary (30%) | `@card_bg_color` | Form rows, art preview containers |
| Accent (10%) | `@accent_color` | Save Station button (`suggested-action` CSS class) |
| Destructive | `@destructive_color` | Delete button (`destructive-action` CSS class) |

Accent reserved for: **Save Station button only.**
Destructive reserved for: **Delete button only** (inside EditStationDialog footer area).

---

## Widget & Interaction Contract

### EditStationDialog — New Controls (source: CONTEXT.md decisions)

**Primary Visual Anchor**
- The station name entry (`Gtk.Entry` for the Name field) is the focal point of
  EditStationDialog. It is the top-most form field and the primary identifier for
  the station. It receives focus on dialog open.

**ICY Metadata Toggle**
- Widget: `Adw.SwitchRow`
- Label: `"Disable ICY metadata"`
- Placement: append after the Tags row in the existing form grid, above the
  `Gtk.Separator` that precedes the art section
- State: initialized from `station.icy_disabled`; saved on Save Station press

**YouTube Thumbnail Fetch**
- Auto-fetch trigger: URL entry `focus-out` event (only when URL contains
  `youtube.com` or `youtu.be`)
- Manual button: `Gtk.Button(label="Fetch from URL")`, placed below
  `station_art_btn` in the arts grid (row 3, col 0)
- Fetch-in-progress state: replace `station_pic` with a `Gtk.Spinner`
  (32x32px, centered in the 128x128 slot) while fetch is running
- On success: spinner removed, station_pic updated with downloaded thumbnail
- On failure: spinner removed, station_pic reverts to previous state; no
  error toast — silent no-op (per Claude's Discretion: error handling)

**Delete Button**
- Widget: `Gtk.Button(label="Delete Station")`
- CSS class: `destructive-action`
- Placement: header bar, packed-start (left side), opposite Save Station button
- Source: Claude's Discretion (exact placement); destructive-action at top-left
  is the GNOME HIG pattern for irreversible header actions

### Delete Confirmation Flow (source: CONTEXT.md)

1. User clicks Delete Station
2. If station is currently playing: show `Adw.MessageDialog`
   - Heading: `"Cannot Delete Station"`
   - Body: `"Stop playback before deleting this station."`
   - Button: OK (default)
3. If station is NOT playing: show `Adw.MessageDialog`
   - Heading: `"Delete [Station Name]?"`
   - Body: `"This station will be permanently removed."`
   - Buttons: Keep Station (default) / Delete (destructive)
4. On Delete confirmed: close dialog, remove station from list

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (save) | "Save Station" |
| Delete action button | "Delete Station" |
| Delete confirmation heading (blocked) | "Cannot Delete Station" |
| Delete confirmation body (blocked) | "Stop playback before deleting this station." |
| Delete confirmation heading (allowed) | "Delete [Station Name]?" |
| Delete confirmation body (allowed) | "This station will be permanently removed." |
| Delete confirmation buttons | Keep Station / Delete |
| Fetch button | "Fetch from URL" |
| ICY toggle label | "Disable ICY metadata" |
| Error state (fetch failure) | silent no-op — no copy shown |

Empty states: not applicable to this phase (EditStationDialog always opens on
an existing station; no list-empty scenario introduced here).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — GTK4 app, no web registry |
| third-party | none | not applicable |

No third-party component registries are used. All widgets are stdlib GTK4/Adwaita.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
