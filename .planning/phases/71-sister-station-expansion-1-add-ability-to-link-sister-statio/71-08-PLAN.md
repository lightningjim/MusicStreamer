---
phase: 71
plan: 08
type: execute
wave: 5
depends_on: [71-01, 71-02, 71-03, 71-04, 71-05, 71-06, 71-07]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
decisions_covered:
  - D-01
  - D-02
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
must_haves:
  truths:
    - "PROJECT.md Key Decisions table gains Phase 71 entries (one per material decision, following the same format as existing Phase 70 / Phase 68 rows)"
    - "ROADMAP.md Phase 71 entry has the **Goal:** line populated and the Plans list reflects the 9 plans created"
    - "REQUIREMENTS.md gains a SIB-01 (or equivalent) traceability row referencing Phase 71 plans (rolling-milestone convention)"
    - "All 22 tests across tests/test_station_siblings.py + tests/test_add_sibling_dialog.py + the sibling/richtext extensions in 4 other test files are GREEN"
    - "Full suite remains GREEN; T-40-04 RichText baseline is locked at 3"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table updated with Phase 71 entries"
      contains: "[Phase 71"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 71 entry finalized with Goal + Plans listing"
      contains: "71-00-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability row for Phase 71"
      contains: "SIB-"
  key_links:
    - from: ".planning/ROADMAP.md Phase 71 entry"
      to: ".planning/phases/71-*/71-NN-PLAN.md files"
      via: "explicit Plans list with file links"
      pattern: "71-\\d\\d-PLAN\\.md"
---

<objective>
Wave 5 — docs polish. Finalize Phase 71 documentation: append decision rows to `.planning/PROJECT.md`'s Key Decisions table, populate `.planning/ROADMAP.md`'s Phase 71 entry (Goal + Plans list), and add a SIB-01 traceability row to `.planning/REQUIREMENTS.md`.

Purpose: Cross-doc traceability per project convention (Phase 70 / Phase 68 / Phase 63 precedent). Closes Phase 71's documentation surface area before /gsd-transition.

Output: 3 doc files updated. No code changes. No new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-CONTEXT.md

<interfaces>
<!-- Existing PROJECT.md Key Decisions format (around lines 165-213, full format documented) -->

Each row in the Key Decisions table is `| Decision | Rationale | Outcome |`. Existing Phase 70 entry has individual rows for each major Phase 70 decision; Phase 68 has rows for the LIVE badge / Z-suffix decisions; Phase 63 has rows for the versioning convention. Phase 71 should add 4-6 high-signal rows covering: symmetric join table choice, ZIP carries siblings by name (not id), per-chip × affordance, merged 'Also on:' line (no separate Sisters row).

<!-- Existing ROADMAP.md Phase 71 entry (lines 596-604) -->

```
### Phase 71: Sister station expansion: ...

**Goal:** [To be planned]
**Requirements**: TBD
**Depends on:** Phase 70
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd-plan-phase 71 to break down)
```

After Plan 71-08, this becomes:

```
### Phase 71: Sister station expansion: ...

**Goal:** Ship manual user-curated sibling-station linking as a first-class in-app concept...
**Requirements**: [SIB-01]
**Depends on:** Phase 70
**Plans:** 9 plans

Plans:
- [ ] 71-00-PLAN.md — Wave 0 RED contract (22 RED tests across 6 test files; fixture mirroring per project convention)
- [ ] 71-01-PLAN.md — Wave 1 station_siblings schema + Repo.add/remove/list_sibling_link CRUD
- [ ] 71-02-PLAN.md — Wave 1 url_helpers.find_manual_siblings + merge_siblings pure helpers
- [ ] 71-03-PLAN.md — Wave 3 EditStationDialog chip row + + Add sibling + sibling_toast Signal
- [ ] 71-04-PLAN.md — Wave 2 AddSiblingDialog new file (two-step picker modal)
- [ ] 71-05-PLAN.md — Wave 3 NowPlayingPanel._refresh_siblings merge-helper swap
- [ ] 71-06-PLAN.md — Wave 4 MainWindow signal wiring (sibling_toast → show_toast)
- [ ] 71-07-PLAN.md — Wave 2 settings_export ZIP round-trip forward-compat
- [ ] 71-08-PLAN.md — Wave 5 docs polish (PROJECT.md decisions + ROADMAP + REQUIREMENTS traceability)
```

<!-- Existing REQUIREMENTS.md format -->

REQUIREMENTS.md tracks traceability with codes like `SIB-NN` for sibling-related requirements. Phase 71 introduces SIB-01 (manual sibling linking via GUI replaces hand-DB-edits) as the rolling-milestone traceability anchor.
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown documentation only; no executable surface | N/A |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | docs-only changes | accept | No threats from Markdown updates. |

</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update PROJECT.md Key Decisions table with Phase 71 entries</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full Key Decisions table around lines 165-213; particularly the Phase 70 / Phase 68 / Phase 63 row formatting examples)
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-CONTEXT.md §"Implementation Decisions" (D-01 through D-15)
  </read_first>
  <action>
    Open `.planning/PROJECT.md` and locate the Key Decisions table (the markdown table beginning with `| Decision | Rationale | Outcome |`). Append the following rows at the end of the table (immediately before the `## Versioning` section header). Use the same `[Phase 71]: ` prefix convention as existing Phase 70 / Phase 68 rows.

    Add these high-signal rows (do NOT mechanically dump all 15 decisions — pick the ones that carry the most weight for future cross-referencing):

    1. `| [Phase 71]: Symmetric join table for sibling links | station_siblings(a_id, b_id) with CHECK(a_id < b_id) + UNIQUE — single-row representation of an undirected edge avoids dual-write + dedup-on-read. ON DELETE CASCADE auto-cleans link rows when a partner station is deleted (D-05, D-08) | ✓ Good — Phase 71 |`
    2. `| [Phase 71]: ZIP siblings exported by station NAME, not ID | Names survive cross-machine sync where the same station may have different IDs; unresolved names silently drop on import (D-07) | ✓ Good — Phase 71 |`
    3. `| [Phase 71]: Merged 'Also on:' line | Manual links + AA URL-derived siblings render in one unified row; dedup by station_id with AA winning on collision (D-01, D-03) | ✓ Good — Phase 71 |`
    4. `| [Phase 71]: Per-chip × on manual chips only; AA chips show plain text | The presence/absence of the × button is the sole visual distinction between manual and auto siblings — no tint, no italic, no opacity change (D-14, D-15) | ✓ Good — Phase 71 |`
    5. `| [Phase 71]: AddSiblingDialog dismiss button labelled "Don't Link" (not "Cancel") | Names the outcome explicitly; contrasts unambiguously with the primary "Link Station" CTA (UI-SPEC Open Decision 16) | ✓ Good — Phase 71 |`
    6. `| [Phase 71]: Removed EditStationDialog's _sibling_label QLabel | The chip row uses Qt widget composition (QPushButton chips) instead of HTML; T-40-04 RichText baseline drops 4 → 3 across musicstreamer/ (Pitfall 1 + the new test_richtext_baseline_unchanged_by_phase_71 drift-guard) | ✓ Good — Phase 71 |`

    Format precisely: each row is one Markdown line with leading and trailing `|`, three columns separated by `|`. No blank lines between rows. Match the indentation/spacing convention of the existing rows.

    Do NOT modify any other section of PROJECT.md.
  </action>
  <verify>
    <automated>grep -c "\[Phase 71\]" .planning/PROJECT.md</automated>
  </verify>
  <done>
    PROJECT.md Key Decisions table has 6 new `[Phase 71]:` rows. No other sections modified.
  </done>
  <validation_map>
    71-VALIDATION.md does not enumerate doc-update rows; this task closes CONTEXT D-01..D-15 by writing them into the persistent decision log.
  </validation_map>
  <acceptance_criteria>
    - `grep -c "\[Phase 71\]" .planning/PROJECT.md` returns at least 6.
    - `git diff .planning/PROJECT.md | grep -c "^-"` returns 0 (purely additive, no deletions).
    - The added rows all begin with `| [Phase 71]:` and end with `| ✓ Good — Phase 71 |` so they're greppable post-phase.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md Phase 71 entry with Goal + Plans listing</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 596-604 for the existing Phase 71 entry; lines 580-595 for the Phase 70 finalized format as a template)
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-CONTEXT.md §"Phase Boundary" — paragraph 1 is the Goal source
  </read_first>
  <action>
    Open `.planning/ROADMAP.md` and locate the Phase 71 entry (around line 596 — header `### Phase 71: Sister station expansion: ...`).

    Replace the `**Goal:** [To be planned]` line with:
    ```
    **Goal:** Ship manual user-curated sibling-station linking as a first-class in-app concept that replaces hand-DB edits. Augments (does not replace) Phase 51's AA URL-derived cross-network sibling auto-detection. Handles two real cases: (1) AA name-mismatch overrides where channel-key auto-detection fails (e.g., DI.fm "Classical Relaxation" ↔ RadioTunes "Relaxing Classical"); (2) same-provider variants like SomaFM's 2× Drone Zone or 3× Groove Salad. UI: '+ Add sibling' button + two-step picker (provider → station) in EditStationDialog; per-chip × on manual links; AA chips plain text. Storage: symmetric `station_siblings(a_id, b_id)` join table with `CHECK(a_id < b_id)`, `UNIQUE`, `ON DELETE CASCADE`. ZIP export carries siblings by name. NowPlaying 'Also on:' line shows the merged AA+manual list.
    ```

    Replace the `**Requirements**: TBD` line with:
    ```
    **Requirements**: [SIB-01]
    ```

    Replace the `**Plans:** 0 plans` line with:
    ```
    **Plans:** 9 plans
    ```

    Replace the entire `Plans:` block (the `- [ ] TBD (run /gsd-plan-phase 71 to break down)` line) with:
    ```
    Plans:
    - [ ] 71-00-PLAN.md — Wave 0 RED contract (22 RED tests across 6 test files; fixture mirroring per project convention)
    - [ ] 71-01-PLAN.md — Wave 1 station_siblings schema + Repo.add/remove/list_sibling_link CRUD
    - [ ] 71-02-PLAN.md — Wave 1 url_helpers.find_manual_siblings + merge_siblings pure helpers
    - [ ] 71-03-PLAN.md — Wave 3 EditStationDialog chip row + + Add sibling + sibling_toast Signal
    - [ ] 71-04-PLAN.md — Wave 2 AddSiblingDialog new file (two-step picker modal)
    - [ ] 71-05-PLAN.md — Wave 3 NowPlayingPanel._refresh_siblings merge-helper swap
    - [ ] 71-06-PLAN.md — Wave 4 MainWindow signal wiring (sibling_toast → show_toast)
    - [ ] 71-07-PLAN.md — Wave 2 settings_export ZIP round-trip forward-compat
    - [ ] 71-08-PLAN.md — Wave 5 docs polish (PROJECT.md decisions + ROADMAP + REQUIREMENTS traceability)
    ```

    Do NOT modify any other phase entry.
  </action>
  <verify>
    <automated>grep -c "71-0\d-PLAN\.md\|71-08-PLAN\.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    ROADMAP.md Phase 71 entry has populated Goal, Requirements [SIB-01], 9 plans listed.
    No other phase entries modified.
  </done>
  <acceptance_criteria>
    - `grep -c "71-00-PLAN\.md\|71-01-PLAN\.md\|71-02-PLAN\.md\|71-03-PLAN\.md\|71-04-PLAN\.md\|71-05-PLAN\.md\|71-06-PLAN\.md\|71-07-PLAN\.md\|71-08-PLAN\.md" .planning/ROADMAP.md` returns at least 9.
    - `grep -c "To be planned" .planning/ROADMAP.md` is unchanged from baseline minus 1 (the Phase 71 placeholder is now populated; Phase 73-75 still have "To be planned" goals).
    - `grep -c "9 plans" .planning/ROADMAP.md | head -1` ≥ 1 (new Phase 71 count).
    - `grep -A1 "### Phase 71:" .planning/ROADMAP.md | grep -c "To be planned"` returns 0 (Phase 71 entry no longer shows the placeholder).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add SIB-01 traceability row to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — review existing format for traceability rows; particularly any v2.1 phase that's added rows like HRES-01 from Phase 70, LIVE-01 from Phase 68, or VER-01 from Phase 63)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Find the section that lists v2.1 phase traceability rows (the most recent additions from Phase 70 / Phase 68 / etc. — typically near the end of the file or in a "Phase mapping" table).

    Add a new SIB-01 row mirroring the existing precedent format. Suggested content:

    ```
    | SIB-01 | Manual sibling-station linking via GUI replaces hand-DB-edits. User adds links from EditStationDialog '+ Add sibling' button → two-step picker (provider → station). Per-chip × unlinks. Merges with AA URL-derived siblings in the 'Also on:' line on both EditStationDialog and NowPlayingPanel. ZIP export carries siblings by name for cross-machine sync. | Phase 71 | shipped 2026-05-12+ |
    ```

    Adjust the exact column count and separator format to match the existing REQUIREMENTS.md table — read the file to confirm column shape; if the file uses a different format (e.g., bullet list rather than table, or a different column set), adapt the SIB-01 row to that format.

    If REQUIREMENTS.md does not have a matching section to append to, add a minimal "## Phase 71" section near the bottom with the SIB-01 description.

    Do NOT modify other rows.
  </action>
  <verify>
    <automated>grep -c "SIB-01\|Sister station\|manual sibling" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md has at least one new row/entry referencing SIB-01 or Phase 71.
  </done>
  <acceptance_criteria>
    - `grep -c "SIB-01" .planning/REQUIREMENTS.md` returns at least 1.
    - `grep -c "Phase 71" .planning/REQUIREMENTS.md` returns at least 1 (mentions the phase).
    - `git diff .planning/REQUIREMENTS.md | grep -c "^-"` returns 0 (purely additive).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final verification — full suite green + T-40-04 baseline locked at 3</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - All Phase 71 plans (71-00 through 71-08) and their SUMMARY.md files if present
  </read_first>
  <action>
    Run the final verification commands and capture results for the SUMMARY:

    1. Full test suite must be GREEN:
       `uv run --with pytest --with pytest-qt pytest tests/ -x --tb=short -q`
       — Expected: 0 failures, 0 errors. ≥420 tests pass (399 existing + ≥22 new Phase 71).

    2. T-40-04 RichText baseline locked at 3:
       `grep -rc "setTextFormat(Qt\.RichText)" musicstreamer/ | awk -F: '$2>0 {s+=$2} END {print s}'`
       — Expected: 3.

    3. Phase 71 plan files list complete (9 plan files):
       `ls .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-*-PLAN.md | wc -l`
       — Expected: 9 (71-00 through 71-08).

    4. Phase 71 test count:
       `pytest tests/test_station_siblings.py tests/test_add_sibling_dialog.py --collect-only -q 2>&1 | tail -3`
       — Expected: 22+ tests collected, 22+ pass.

    5. CONTEXT decisions all covered:
       `grep -c "D-0[1-9]\|D-1[0-5]" .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-CONTEXT.md`
       — Expected: ≥15 (one per locked decision).

    No file modifications in this task. If any check fails, return to the failing plan to fix.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/ -x --tb=line -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 4 verification checks pass:
    1. Full suite GREEN (0 failures).
    2. T-40-04 baseline == 3.
    3. 9 Phase 71 plan files exist.
    4. Phase 71 tests all GREEN.
    5. All 15 CONTEXT decisions traced.
  </done>
  <acceptance_criteria>
    - Final `pytest tests/ -x` exit code is 0.
    - Whole-package RichText baseline: exactly 3.
    - Phase 71 plan files: exactly 9.
    - Phase 71 test files have ≥22 passing tests.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Final Phase 71 verification gate
uv run --with pytest --with pytest-qt pytest tests/ -x --tb=short -q

# T-40-04 baseline locked
grep -rc "setTextFormat(Qt\.RichText)" musicstreamer/ | awk -F: '$2>0 {s+=$2} END {print s}'
# Expected: 3

# Doc updates verified
grep -c "\[Phase 71\]" .planning/PROJECT.md           # ≥6
grep -c "71-0\d-PLAN\.md\|71-08-PLAN\.md" .planning/ROADMAP.md   # ≥9
grep -c "SIB-01\|Phase 71" .planning/REQUIREMENTS.md  # ≥2
```
</verification>

<success_criteria>
- PROJECT.md Key Decisions: 6+ new [Phase 71] rows.
- ROADMAP.md Phase 71 entry: Goal populated, Requirements [SIB-01], 9 plans listed.
- REQUIREMENTS.md: SIB-01 row added.
- Full suite GREEN.
- T-40-04 RichText baseline locked at 3.
- All 9 Phase 71 plans complete; all 22+ Wave 0 RED tests now GREEN.
- Phase 71 is ready for /gsd-verify-work + /gsd-transition.
</success_criteria>

<output>
After completion, create `.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-08-SUMMARY.md` with: number of new [Phase 71] rows in PROJECT.md, number of plans listed in ROADMAP.md Phase 71 entry, REQUIREMENTS.md SIB-01 placement, final test count, T-40-04 baseline confirmation, and a one-paragraph phase-completion summary suitable for the Phase History section.
</output>
