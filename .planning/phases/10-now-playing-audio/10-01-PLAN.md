---
phase: 10-now-playing-audio
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - musicstreamer/player.py
  - tests/test_player_volume.py
autonomous: true
requirements:
  - AUDIO-01

must_haves:
  truths:
    - "Player.set_volume(0.8) sets GStreamer pipeline volume to 0.8"
    - "Player.set_volume(1.5) clamps to 1.0"
    - "Player.set_volume(-0.5) clamps to 0.0"
    - "Player stores volume for mpv subprocess launch"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "set_volume method and _volume instance variable"
      contains: "def set_volume"
    - path: "tests/test_player_volume.py"
      provides: "Unit tests for volume clamping and property set"
      contains: "def test_set_volume"
  key_links:
    - from: "musicstreamer/player.py"
      to: "GStreamer playbin3"
      via: "_pipeline.set_property('volume', clamped)"
      pattern: "set_property.*volume"
    - from: "musicstreamer/player.py"
      to: "mpv subprocess"
      via: "--volume={int} arg in Popen call"
      pattern: "--volume="
---

<objective>
Add `Player.set_volume(float)` method with clamping and mpv volume passthrough using TDD.

Purpose: AUDIO-01 requires volume control. The Player method is the foundation -- UI slider (Plan 02) calls this.
Output: `set_volume` method on Player, `_volume` instance var, mpv `--volume` arg, passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-now-playing-audio/10-RESEARCH.md
@.planning/phases/10-now-playing-audio/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD Player.set_volume with clamping and mpv --volume arg</name>
  <files>musicstreamer/player.py, tests/test_player_volume.py</files>
  <read_first>
    - musicstreamer/player.py (current Player class -- see __init__, _play_youtube, play, stop methods)
    - tests/test_player_tag.py (existing test pattern for Player -- uses unittest.mock for GStreamer)
    - .planning/phases/10-now-playing-audio/10-RESEARCH.md (Pattern 3 and Pattern 4 code examples)
  </read_first>
  <behavior>
    - test_set_volume_normal: Player().set_volume(0.8) calls _pipeline.set_property("volume", 0.8) and stores _volume = 0.8
    - test_set_volume_clamps_high: Player().set_volume(1.5) calls _pipeline.set_property("volume", 1.0)
    - test_set_volume_clamps_low: Player().set_volume(-0.5) calls _pipeline.set_property("volume", 0.0)
    - test_set_volume_stores_for_mpv: After set_volume(0.6), _volume attribute equals 0.6
  </behavior>
  <action>
RED phase:
1. Create `tests/test_player_volume.py` with these tests:
   - Use `unittest.mock.MagicMock` and `patch` to mock `gi.repository.Gst`
   - Create Player instance, replace `_pipeline` with MagicMock
   - `test_set_volume_normal`: call `p.set_volume(0.8)`, assert `p._pipeline.set_property.assert_called_with("volume", 0.8)`
   - `test_set_volume_clamps_high`: call `p.set_volume(1.5)`, assert `p._pipeline.set_property.assert_called_with("volume", 1.0)`
   - `test_set_volume_clamps_low`: call `p.set_volume(-0.5)`, assert `p._pipeline.set_property.assert_called_with("volume", 0.0)`
   - `test_set_volume_stores_for_mpv`: call `p.set_volume(0.6)`, assert `p._volume == 0.6`
2. Run tests -- must FAIL (Player has no set_volume method yet)

GREEN phase:
3. In `musicstreamer/player.py`, add to `Player.__init__`:
   ```python
   self._volume = 1.0
   ```
4. Add method to Player class:
   ```python
   def set_volume(self, value: float):
       clamped = max(0.0, min(1.0, value))
       self._volume = clamped
       self._pipeline.set_property("volume", clamped)
   ```
5. In `_play_youtube`, add `--volume={int(self._volume * 100)}` to the mpv Popen args list, between `"--really-quiet"` and the url:
   ```python
   self._yt_proc = subprocess.Popen(
       ["mpv", "--no-video", "--really-quiet", f"--volume={int(self._volume * 100)}", url],
       stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
   )
   ```
6. Run tests -- must PASS

Per D-03/D-04: Volume slider wiring happens in Plan 02. This plan only provides the Player API.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && pytest tests/test_player_volume.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/player.py contains `def set_volume(self, value: float):`
    - musicstreamer/player.py contains `self._volume = 1.0` in __init__
    - musicstreamer/player.py contains `self._pipeline.set_property("volume", clamped)`
    - musicstreamer/player.py _play_youtube contains `f"--volume={int(self._volume * 100)}"`
    - tests/test_player_volume.py contains `def test_set_volume_normal`
    - tests/test_player_volume.py contains `def test_set_volume_clamps_high`
    - tests/test_player_volume.py contains `def test_set_volume_clamps_low`
    - tests/test_player_volume.py exits 0
    - pytest tests/ -x -q exits 0 (full suite still green)
  </acceptance_criteria>
  <done>Player.set_volume clamps 0.0-1.0, sets GStreamer volume property, stores _volume for mpv, all 4 tests pass, full suite green</done>
</task>

</tasks>

<verification>
pytest tests/test_player_volume.py -x -q
pytest tests/ -x -q
</verification>

<success_criteria>
- Player.set_volume(float) exists and clamps to [0.0, 1.0]
- GStreamer pipeline volume property is set on every call
- mpv subprocess receives --volume arg derived from stored _volume
- 4 new tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/10-now-playing-audio/10-01-SUMMARY.md`
</output>
