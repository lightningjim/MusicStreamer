---
phase: 86-linux-flatpak-build
plan: 04
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - tools/linux-flatpak/build.sh
  - .github/workflows/linux-flatpak.yml
  - tests/test_packaging_linux_spec.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PKG-LIN-FP-02, PKG-LIN-FP-04, PKG-LIN-FP-10]
must_haves:
  truths:
    - "build.sh runs flatpak-builder, the FP-10 validators as a hard pre-flight gate, then flatpak build-bundle --gpg-sign"
    - "build.sh fails fast when GPG_KEY_ID is unset unless SKIP_SIGN=1 (mirrors Phase 85 exit-code discipline)"
    - "CI is workflow_dispatch-only, runs privileged for FUSE, imports the signing key into an ephemeral GNUPGHOME, uploads the .flatpak artifact, and scrubs GNUPGHOME"
    - "REQUIREMENTS.md gains the new signing row (D-08) and the FP-04 :ro-mount reconciliation note (D-05)"
  artifacts:
    - path: "tools/linux-flatpak/build.sh"
      provides: "Flatpak build+validate+sign+bundle driver (mirrors tools/linux-build/build.sh)"
      contains: "flatpak build-bundle"
    - path: ".github/workflows/linux-flatpak.yml"
      provides: "workflow_dispatch-only CI building + signing the .flatpak (no auto-publish)"
    - path: "tests/test_packaging_linux_spec.py"
      provides: "Source-text drift-guards for the Flatpak build.sh GPG/validator discipline"
  key_links:
    - from: ".github/workflows/linux-flatpak.yml"
      to: "tools/linux-flatpak/build.sh"
      via: "build step invocation"
      pattern: "tools/linux-flatpak/build.sh"
    - from: "tools/linux-flatpak/build.sh"
      to: "io.github.kcreasey.MusicStreamer.yaml"
      via: "flatpak-builder manifest arg"
      pattern: "io.github.kcreasey.MusicStreamer.yaml"
---

<objective>
Author the Flatpak build driver (build.sh), the workflow_dispatch-only CI workflow, and the build-script drift-guards — mirroring Phase 85's AppImage discipline (D-06/D-07/D-08). Add the new GPG signing requirement row (D-08) and the FP-04 `:ro`-mount reconciliation note (D-05) to REQUIREMENTS.md. Depends on Plan 01 (manifest must exist for the build to consume).

Purpose: Produce a signed `MusicStreamer-<version>.flatpak` (FP-02 install target) via a reproducible local + CI build, with the FP-10 validators enforced as a hard pre-flight gate (D-15), reusing Phase 85's GPG fail-fast + ephemeral-GNUPGHOME + secrets discipline.
Output: tools/linux-flatpak/build.sh, .github/workflows/linux-flatpak.yml, Flatpak build-script guards in tests/test_packaging_linux_spec.py, and the two REQUIREMENTS.md edits.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-linux-flatpak-build/86-CONTEXT.md
@.planning/phases/86-linux-flatpak-build/86-RESEARCH.md
@.planning/phases/86-linux-flatpak-build/86-PATTERNS.md
@.planning/phases/85-linux-common-appimage-build/85-CONTEXT.md

# Plan 01 output (build input):
@io.github.kcreasey.MusicStreamer.yaml

<interfaces>
<!-- Phase 85 analogs to mirror verbatim except for the noted differences (PATTERNS.md lines 27-157). -->

tools/linux-build/build.sh (the analog):
  - GPG fail-fast (lines 82-85): if [[ -z "${GPG_KEY_ID:-}" && "${SKIP_SIGN:-0}" != "1" ]]; then echo "BUILD_FAIL reason=gpg_key_unset ..." >&2; exit 5; fi
  - Repo-root/artifacts layout (lines 87-93): HERE / ARTIFACTS / REPO_ROOT / mkdir -p ARTIFACTS
  - Sign step (lines 390-398): command -v gpg2 || command -v gpg; BUILD_FAIL reason=signing_failed (exit 6); SIGN_OK / SIGN_SKIPPED
  - Exit codes: 0=ok 1=env-missing 2=build-failed 5=gpg_key_unset 6=signing_failed
  - Diagnostic prefixes: BUILD_FAIL reason= / BUILD_OK / SIGN_OK / SIGN_SKIPPED

.github/workflows/linux-appimage.yml (the analog):
  - on: workflow_dispatch only; permissions: contents: read
  - Validate signing secrets (lines 42-52): LINUX_SIGNING_KEY / LINUX_SIGNING_KEY_ID; WORKFLOW_FAIL if missing
  - Ephemeral GNUPGHOME import (lines 54-73): mktemp -d, allow-loopback-pinentry, pinentry-mode loopback, gpg --batch --import, GPG_KEY_ID=...>>GITHUB_ENV
  - Upload artifact (lines 121-130): actions/upload-artifact@v4, if-no-files-found: error, retention-days: 30
  - Scrub GNUPGHOME (lines 131-137): if: always(), rm -rf GNUPGHOME

Flatpak-specific differences (PATTERNS.md lines 70-74, 149-157, Critical Note #1):
  - Build cmd: flatpak-builder --user --repo=flatpak-repo --force-clean build-dir io.github.kcreasey.MusicStreamer.yaml
  - Pre-flight gate: appstreamcli validate + desktop-file-validate (HARD gate, fail build on error — D-15)
  - Sign+bundle: flatpak build-bundle --gpg-sign="$GPG_KEY_ID" --gpg-homedir="$GNUPGHOME" flatpak-repo "MusicStreamer-${VERSION}.flatpak" io.github.kcreasey.MusicStreamer
  - Signing is INLINE in the bundle (no .sig sidecar — Critical Note #1); artifact path tools/linux-flatpak/artifacts/MusicStreamer-*.flatpak
  - CI: container options: --privileged (FUSE/OSTree — Pitfall 9); no GLIBC scan; no smoke step (no desktop session — D-07)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md — add signing row (D-08) and FP-04 :ro reconciliation note (D-05)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §"Linux Packaging — Flatpak (PKG-LIN-FP)" (the PKG-LIN-FP-01..10 list at lines 26-37 and the traceability table at lines 165-174)
    - .planning/phases/86-linux-flatpak-build/86-CONTEXT.md D-05 (reconcile :ro mount) and D-08 (new signing row, mirror how Phase 85 added PKG-LIN-APP-10)
  </read_first>
  <action>
    Add a new requirement row PKG-LIN-FP-11 after PKG-LIN-FP-10: "The sideload `.flatpak` is GPG-signed via `flatpak build-bundle --gpg-sign=$GPG_KEY_ID` (reusing `secrets.LINUX_SIGNING_KEY` / `secrets.LINUX_SIGNING_KEY_ID`); build.sh fails fast if `GPG_KEY_ID` unset unless `SKIP_SIGN=1` (mirrors Phase 85 PKG-LIN-APP-10 / D-08)." Add the matching traceability table row: PKG-LIN-FP-11 | Phase 86 | Pending.
    Amend PKG-LIN-FP-04's text to enumerate the narrow :ro path as an APPROVED EXCEPTION (D-05 reconciliation): append "; plus the single narrow `--filesystem=~/.local/share/musicstreamer:ro` path (D-01 approved addition for first-launch detection — NOT broad filesystem access)." This makes the requirement text match the manifest + the drift-guard allow-list so traceability is consistent.
  </action>
  <verify>
    <automated>bash -c 'grep -q "PKG-LIN-FP-11" .planning/REQUIREMENTS.md && grep -q "musicstreamer:ro" .planning/REQUIREMENTS.md && echo REQS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains a PKG-LIN-FP-11 requirement bullet AND a PKG-LIN-FP-11 traceability table row
    - PKG-LIN-FP-04 text now enumerates `--filesystem=~/.local/share/musicstreamer:ro` as an approved exception (D-05)
    - `grep -c "musicstreamer:ro" .planning/REQUIREMENTS.md` >= 1
  </acceptance_criteria>
  <done>PKG-LIN-FP-11 signing row added with traceability entry; FP-04 reconciled to enumerate the narrow :ro mount (D-05/D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Flatpak build.sh driver (build + hard validator pre-flight + sign + bundle)</name>
  <files>tools/linux-flatpak/build.sh</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`tools/linux-flatpak/build.sh` (GPG fail-fast, layout, sign, exit codes, key differences — lines ~27-74) and §Shared Patterns (GPG Signing Discipline, BUILD_FAIL/BUILD_OK prefix — lines ~479-495) and §Critical Implementation Notes #1 (inline signing, no .sig sidecar)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Architecture Patterns Pattern 6 (build+sign flow, lines ~363-390) and Pitfall 8 (passphrase-less key)
    - tools/linux-build/build.sh (the verbatim analog — read the GPG fail-fast block ~82-93 and sign block ~390-398, and the VERSION derivation)
  </read_first>
  <action>
    Create tools/linux-flatpak/build.sh (executable, set -euo pipefail). Copy verbatim from tools/linux-build/build.sh: the exit-code header convention (0/1/2/5/6), the GPG fail-fast guard (BUILD_FAIL reason=gpg_key_unset / exit 5, with the SKIP_SIGN=1 local escape hatch), the HERE/ARTIFACTS/REPO_ROOT layout (ARTIFACTS = tools/linux-flatpak/artifacts), and the BUILD_FAIL/BUILD_OK/SIGN_OK/SIGN_SKIPPED diagnostic prefixes.
    Replace the AppImage-specific body with: (1) build via flatpak-builder --user --repo=flatpak-repo --force-clean build-dir io.github.kcreasey.MusicStreamer.yaml (run from REPO_ROOT so the manifest path resolves); exit 2 with BUILD_FAIL reason=builder_failed on non-zero. (2) HARD pre-flight validator gate (D-15) sequenced BEFORE build-bundle: run appstreamcli validate on tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml and desktop-file-validate on tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop; on either non-zero, exit with BUILD_FAIL reason=validator_failed (build FAILS on validator error — not skip). (3) Sign+bundle inline (Critical Note #1 — no .sig sidecar): when SKIP_SIGN != 1, flatpak build-bundle --gpg-sign="$GPG_KEY_ID" --gpg-homedir="${GNUPGHOME:-$HOME/.gnupg}" flatpak-repo "${ARTIFACTS}/MusicStreamer-${VERSION}.flatpak" io.github.kcreasey.MusicStreamer, emit SIGN_OK; when SKIP_SIGN=1, run flatpak build-bundle WITHOUT --gpg-sign and emit SIGN_SKIPPED (local-iteration mode). Derive VERSION using the same method Phase 85's build.sh uses (match it exactly after reading that file).
    Document in a comment that CI imports a passphrase-less key (Pitfall 8, mirrors Phase 85 D-16). Do NOT add a .sig sidecar step.
  </action>
  <verify>
    <automated>bash -c 'set -e; bash -n tools/linux-flatpak/build.sh; grep -q "flatpak build-bundle" tools/linux-flatpak/build.sh; grep -q "appstreamcli validate" tools/linux-flatpak/build.sh; grep -q "desktop-file-validate" tools/linux-flatpak/build.sh; grep -q "BUILD_FAIL reason=gpg_key_unset" tools/linux-flatpak/build.sh; echo BUILDSH_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tools/linux-flatpak/build.sh` exits 0 (valid shell syntax)
    - build.sh contains `flatpak build-bundle`, `appstreamcli validate`, and `desktop-file-validate`
    - build.sh contains `BUILD_FAIL reason=gpg_key_unset` and `exit 5` (Phase 85 fail-fast discipline)
    - build.sh contains a `SKIP_SIGN` non-comment line (local-iteration escape hatch) and `BUILD_FAIL reason=validator_failed`
    - build.sh does NOT add a `.sig` sidecar (inline signing — Critical Note #1)
  </acceptance_criteria>
  <done>build.sh builds via flatpak-builder, hard-gates on the two validators, signs inline via flatpak build-bundle --gpg-sign, fails fast on unset GPG_KEY_ID, supports SKIP_SIGN=1 local iteration, reuses Phase 85 prefixes/exit codes.</done>
</task>

<task type="auto">
  <name>Task 3: CI workflow + build-script drift-guards</name>
  <files>.github/workflows/linux-flatpak.yml, tests/test_packaging_linux_spec.py</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`.github/workflows/linux-flatpak.yml` (trigger, secrets validation, GPG import, artifact upload, GNUPGHOME scrub, key differences — lines ~78-157) and §CI Secrets + Ephemeral GNUPGHOME (lines ~485-489)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md Pitfall 9 (privileged container/FUSE) and §Architecture Patterns Pattern 6 (flatpak-github-actions container)
    - .github/workflows/linux-appimage.yml (the verbatim analog — copy secrets/GNUPGHOME/scrub steps)
    - tests/test_packaging_linux_spec.py (read the build.sh guard idioms: path constants, scope=module fixture, _strip_comments_sh — lines 25-79, 187-209)
  </read_first>
  <action>
    Create .github/workflows/linux-flatpak.yml. Name it "Build signed Flatpak (org.kde.Platform//6.8)". Trigger: on: workflow_dispatch only (D-07 — no push/release trigger; no auto-publish). permissions: contents: read. Copy verbatim from linux-appimage.yml: the "Validate signing secrets" fail-fast step (LINUX_SIGNING_KEY / LINUX_SIGNING_KEY_ID, WORKFLOW_FAIL if missing), the ephemeral GNUPGHOME import step (mktemp -d, allow-loopback-pinentry, pinentry-mode loopback, gpg --batch --import, write GPG_KEY_ID to GITHUB_ENV), and the "if: always()" GNUPGHOME scrub step.
    Flatpak-specific changes: job runs with container options: --privileged (FUSE/OSTree — Pitfall 9); prerequisite install step does sudo apt-get install -y flatpak-builder + flatpak install --user -y flathub org.kde.Platform//6.8 org.kde.Sdk//6.8 io.qt.PySide.BaseApp//6.8 org.freedesktop.Platform.ffmpeg-full//24.08 org.freedesktop.Sdk.Extension.node20 (OR use the official flatpak/flatpak-github-actions container which preinstalls flatpak-builder + FUSE — pick one and note why). The build step invokes tools/linux-flatpak/build.sh (which carries the hard validator gate + sign). NO GLIBC scan step; NO smoke/launch step (no desktop session — CI success = bundle builds + validators pass, D-07). Upload artifact: actions/upload-artifact@v4, name MusicStreamer-Flatpak, path tools/linux-flatpak/artifacts/MusicStreamer-*.flatpak (no .sig — inline signing), if-no-files-found: error, retention-days: 30.
    Append Flatpak build.sh drift-guards to tests/test_packaging_linux_spec.py using the existing build.sh-guard idiom (path constant _FLATPAK_BUILD_SH, scope=module fixture, _strip_comments_sh for negative gates): test_flatpak_build_gpg_sign (assert "flatpak build-bundle" and "--gpg-sign" present in source); test_flatpak_build_fail_fast_gpg (assert "BUILD_FAIL reason=gpg_key_unset" and "exit 5" present, and "SKIP_SIGN" appears in a non-comment line via _strip_comments_sh); test_flatpak_build_validator_gate (assert both "appstreamcli validate" and "desktop-file-validate" present — the hard pre-flight per D-15); test_flatpak_ci_workflow_dispatch_only (read .github/workflows/linux-flatpak.yml; assert "workflow_dispatch" present and assert no "push:" / "on:\n  push" auto-trigger, and assert "--privileged" present).
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_packaging_linux_spec.py -k "flatpak" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_packaging_linux_spec.py -k flatpak -x exits 0`
    - linux-flatpak.yml contains `workflow_dispatch`, `--privileged`, and invokes `tools/linux-flatpak/build.sh`
    - linux-flatpak.yml has NO push/release auto-trigger (workflow_dispatch-only, D-07) and uploads `MusicStreamer-*.flatpak` with `if-no-files-found: error`
    - test_flatpak_build_validator_gate asserts BOTH validators present in build.sh (D-15 hard gate)
    - `uv run --with pytest pytest tests/test_packaging_linux_spec.py -x` green (no AppImage-guard regression)
  </acceptance_criteria>
  <done>workflow_dispatch-only privileged CI authored mirroring Phase 85 (secrets fail-fast, ephemeral GNUPGHOME, scrub, artifact upload, no auto-publish, no smoke); Flatpak build.sh + CI drift-guards added and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI secrets → signing key material | LINUX_SIGNING_KEY enters an ephemeral GNUPGHOME, used once, scrubbed |
| build artifact → user download | The .flatpak crosses the network to users; signature is the integrity anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-10 | Tampering | Unsigned/tampered .flatpak in transit | mitigate | flatpak build-bundle --gpg-sign embeds a signature; build.sh fails fast (exit 5) if GPG_KEY_ID unset unless SKIP_SIGN=1; test_flatpak_build_gpg_sign asserts the sign step exists (D-08/PKG-LIN-FP-11). |
| T-86-11 | Information Disclosure | Signing key leaking from CI | mitigate | Ephemeral GNUPGHOME via mktemp -d, "if: always()" scrub step removes it after the job (copied verbatim from Phase 85 D-16). |
| T-86-12 | Tampering | Invalid metainfo/.desktop shipped (broken trust signals) | mitigate | build.sh hard pre-flight gate runs appstreamcli validate + desktop-file-validate and FAILS the build on error (D-15); test_flatpak_build_validator_gate asserts both are present. |
| T-86-SC | Tampering | flatpak-github-actions / apt packages in CI | accept | Official flatpak-org action + Ubuntu apt packages (RESEARCH.md Package Legitimacy Audit — no [SUS]/[SLOP], no human gate required). |
</threat_model>

<verification>
- `uv run --with pytest pytest tests/test_packaging_linux_spec.py -x` green.
- `bash -n tools/linux-flatpak/build.sh` clean; build.sh carries the full validate+sign+fail-fast discipline.
- CI is workflow_dispatch-only, privileged, no auto-publish.
- REQUIREMENTS.md carries PKG-LIN-FP-11 and the FP-04 :ro reconciliation.
</verification>

<success_criteria>
- FP-02 (install target produced by the build), FP-10 (validators enforced at build time), and the new FP-11 signing requirement are materially satisfied by the build driver + CI.
- The actual `flatpak install --user` + GNOME Software launch (FP-02/SC1) is the human-verified portion (Plan 05 / must_haves SC1).
</success_criteria>

<output>
Create `.planning/phases/86-linux-flatpak-build/86-04-SUMMARY.md` when done
</output>
