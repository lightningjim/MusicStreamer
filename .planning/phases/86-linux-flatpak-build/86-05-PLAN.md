---
phase: 86-linux-flatpak-build
plan: 05
type: execute
wave: 3
depends_on: ["86-01", "86-02", "86-03", "86-04"]
files_modified:
  - .planning/phases/86-linux-flatpak-build/86-VERIFICATION.md
autonomous: false
requirements: [PKG-LIN-FP-02, PKG-LIN-FP-05, PKG-LIN-FP-07, PKG-LIN-FP-08]
must_haves:
  truths:
    - "[human-verified] flatpak install --user succeeds, app appears in GNOME Software, flatpak run launches (SC1)"
    - "[human-verified] AAC streams (DI.fm, AudioAddict, SomaFM AAC) play audibly inside the sandbox via ffmpeg-full (SC2)"
    - "[human-verified] GBS.FM login completes with no namespace error and cookies persist across full quit+relaunch (SC3)"
    - "[human-verified] MPRIS2 name visible via busctl/playerctl from outside the sandbox AND OS media keys control playback (SC4)"
    - "[human-verified] first launch with existing ~/.local/share/musicstreamer/ offers the import wizard (SC5 functional half)"
  artifacts:
    - path: ".planning/phases/86-linux-flatpak-build/86-VERIFICATION.md"
      provides: "Empirical evidence bundle (audible + Wayland screenshot + D-Bus/terminal transcript per capability, D-09)"
  key_links:
    - from: "86-VERIFICATION.md"
      to: "org.mpris.MediaPlayer2.MusicStreamer"
      via: "busctl --user / playerctl introspection transcript"
      pattern: "org.mpris.MediaPlayer2.MusicStreamer"
---

<objective>
Build, install, and run the signed `.flatpak` on the native dev-rig Wayland GNOME host, then capture the empirical evidence bundle (audible + Wayland screenshot + D-Bus/terminal transcript) proving the four in-sandbox capabilities that no automated test can cover: install/launch (SC1), AAC audio (SC2), GBS.FM login persistence (SC3), and MPRIS2 media-key control (SC4) — plus the functional first-launch import offer (SC5). Depends on all prior plans: the manifest (01), the wizard (02), the green drift-guards (03), and the build driver (04).

Purpose: Satisfy PKG-LIN-FP-02/05/07/08 functional acceptance via the D-09..D-12 evidence protocol. Single-host Wayland GNOME UAT (D-11) — Flatpak's bundled org.kde.Platform//6.8 makes the cross-distro matrix moot.
Output: 86-VERIFICATION.md evidence bundle with embedded transcripts + screenshot references per capability.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-linux-flatpak-build/86-CONTEXT.md
@.planning/phases/86-linux-flatpak-build/86-RESEARCH.md
@.planning/phases/86-linux-flatpak-build/86-VALIDATION.md
@io.github.kcreasey.MusicStreamer.yaml
@tools/linux-flatpak/build.sh

<interfaces>
<!-- Verification protocols (CONTEXT.md D-09..D-12, VALIDATION.md Manual-Only table). -->

D-09 evidence bundle per capability: audible confirmation + Wayland screenshot + terminal/D-Bus transcript.
D-10 MPRIS2: busctl --user list | grep mpris ; playerctl --player=MusicStreamer status ; + OS media-key press test (play/pause/next reach sandbox playback).
D-11: native dev-rig Wayland GNOME host (DPR=1.0), single-host, no cross-distro matrix.
D-12 GBS.FM: log in via QtWebEngine subprocess → fully quit → relaunch → still logged in (cookies persisted) AND no namespace/sandbox error in the subprocess.
RESEARCH.md verification commands: gst-inspect-1.0 avdec_aac (from flatpak run ... bash) confirms the ffmpeg-full AAC decoder resolved (Open Question 2 / A7 — if it fails, uncomment the GST_PLUGIN_PATH fallback in the manifest per Plan 01 Task 2, rebuild, retest).
AAC test tiers (SC2): DI.fm, AudioAddict, SomaFM AAC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + install the signed .flatpak locally and confirm AAC decoder resolution</name>
  <files>.planning/phases/86-linux-flatpak-build/86-VERIFICATION.md</files>
  <read_first>
    - tools/linux-flatpak/build.sh (the driver to invoke — read its exit codes and SKIP_SIGN flag)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Architecture Patterns Pattern 6 (install command: flatpak install --user --no-gpg-verify for local test) and Pattern 3 (gst-inspect-1.0 avdec_aac verification) and Open Question 2/A7 (GST_PLUGIN_PATH fallback)
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §Pattern 2 (node /app/bin copy) for the node-presence check
  </read_first>
  <action>
    Run tools/linux-flatpak/build.sh (use GPG_KEY_ID if a local signing key exists, else SKIP_SIGN=1 for local iteration) to produce tools/linux-flatpak/artifacts/MusicStreamer-<version>.flatpak. Capture the BUILD_OK / SIGN_OK|SIGN_SKIPPED transcript. If the build fails on the validator gate or builder step, record the BUILD_FAIL reason and fix the upstream artifact (Plan 01) before continuing — do not bypass the gate.
    Install locally: flatpak install --user --no-gpg-verify <path>.flatpak (--no-gpg-verify is acceptable for the local self-built bundle per RESEARCH.md Pattern 6). Confirm the AAC decoder resolves INSIDE the sandbox: flatpak run --command=bash io.github.kcreasey.MusicStreamer -c "gst-inspect-1.0 avdec_aac" exits 0 and prints the element. Also confirm node is present: flatpak run --command=bash io.github.kcreasey.MusicStreamer -c "which node" returns /app/bin/node (Pitfall 3 / Pattern 2). If avdec_aac does NOT resolve, uncomment the `--env=GST_PLUGIN_PATH=/app/lib/ffmpeg` fallback in the manifest (Plan 01 Task 2 left it commented), rebuild, retest, and note the resolution of Open Question 2/A7 in 86-VERIFICATION.md.
    Create 86-VERIFICATION.md and record the build transcript, install transcript, gst-inspect avdec_aac output, and which node output as the first evidence section.
  </action>
  <verify>
    <automated>bash -c 'flatpak run --command=bash io.github.kcreasey.MusicStreamer -c "gst-inspect-1.0 avdec_aac >/dev/null 2>&1 && which node" 2>/dev/null | grep -q "/app/bin/node" && echo SANDBOX_DECODER_NODE_OK || echo "MANUAL — record gst-inspect avdec_aac + which node transcript in 86-VERIFICATION.md"'</automated>
  </verify>
  <acceptance_criteria>
    - tools/linux-flatpak/artifacts/MusicStreamer-*.flatpak produced (build.sh exited 0)
    - `flatpak install --user --no-gpg-verify` succeeds (transcript captured)
    - `gst-inspect-1.0 avdec_aac` resolves inside the sandbox (transcript captured); if it required the GST_PLUGIN_PATH fallback, Open Question 2/A7 resolution is documented
    - `which node` inside the sandbox returns `/app/bin/node` (node20 runtime-copy confirmed)
    - 86-VERIFICATION.md has a build+install+decoder evidence section
  </acceptance_criteria>
  <done>Signed (or SKIP_SIGN) bundle built + installed locally; AAC decoder + node binary confirmed resolvable inside the sandbox; first evidence section written; any GST_PLUGIN_PATH fallback resolution recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT evidence bundle — install/launch, AAC audio, GBS.FM login persistence, MPRIS2 (SC1/SC2/SC3/SC4/SC5)</name>
  <files>.planning/phases/86-linux-flatpak-build/86-VERIFICATION.md</files>
  <action>
    Claude has already built + installed the bundle and confirmed the AAC decoder + node resolve inside the sandbox (Task 1). This checkpoint is the human-verified functional UAT that automation cannot cover: audible audio, GNOME Software listing, interactive GBS.FM login, OS media-key control, and the first-launch import offer. The human performs each step in <how-to-verify> and records the D-09 evidence (audible note + Wayland screenshot ref + terminal/D-Bus transcript) into 86-VERIFICATION.md, one section per success criterion (SC1-SC5).
  </action>
  <what-built>
    A signed sideload .flatpak installed on the native dev-rig Wayland GNOME host. This checkpoint captures the human-verified empirical evidence (D-09..D-12) that automated tests cannot cover: audible audio, GNOME Software listing, interactive GBS.FM login, OS media-key control, and the first-launch import offer.
  </what-built>
  <how-to-verify>
    Perform each and record audible-confirmation note + Wayland screenshot + terminal/D-Bus transcript into 86-VERIFICATION.md (D-09 bundle):
    1. SC1 install/launch: confirm `io.github.kcreasey.MusicStreamer` appears in GNOME Software; `flatpak run io.github.kcreasey.MusicStreamer` launches the GUI. Screenshot the GNOME Software entry + the running app.
    2. SC2 AAC audio: play a DI.fm, an AudioAddict, and a SomaFM AAC tier stream inside the sandbox; confirm each is AUDIBLE via PipeWire. Screenshot Now Playing per tier; note audible=yes.
    3. SC3 GBS.FM login (D-12): log in via the in-app QtWebEngine subprocess; confirm NO namespace/sandbox error in the subprocess stderr; fully quit the app; relaunch; confirm still logged in (cookies persisted in sandbox data). Capture the subprocess transcript showing no `namespace: not permitted` error.
    4. SC4 MPRIS2 (D-10): from OUTSIDE the sandbox while playing, run `busctl --user list | grep mpris` (expect `org.mpris.MediaPlayer2.MusicStreamer`, the SHORT name) and `playerctl --player=MusicStreamer status`; then press OS media keys (play/pause/next) and confirm they reach sandbox playback. Capture both transcripts.
    5. SC5 first-launch import (functional half): on a host with existing `~/.local/share/musicstreamer/musicstreamer.sqlite3`, confirm first launch OFFERS the import wizard; dismiss it; relaunch; confirm it does NOT re-offer (offer-once, D-03). The original ~/.local/share/musicstreamer/ remains intact (copy-don't-delete, D-02).
  </how-to-verify>
  <verify>
    <human-check>All five evidence sections (SC1-SC5) recorded in 86-VERIFICATION.md with audible note + screenshot ref + transcript per D-09; MPRIS2 transcript shows the short name; GBS.FM transcript shows no namespace error and persistence across relaunch.</human-check>
  </verify>
  <done>86-VERIFICATION.md contains the five-capability evidence bundle and the human has typed "approved".</done>
  <resume-signal>Type "approved" once all five evidence sections (audible note + screenshot ref + transcript) are recorded in 86-VERIFICATION.md, or describe which capability failed (e.g. "SC4 busctl shows long name" or "SC3 namespace error").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox → host session bus (MPRIS2) | UAT confirms ONLY the granted --own-name appears on the host bus, not broad session-bus access |
| QtWebEngine subprocess → outer Flatpak sandbox | UAT confirms the inner-sandbox-disabled subprocess stays constrained by the outer sandbox (no namespace error, no escape) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-13 | Elevation of Privilege | QtWebEngine subprocess sandbox-in-sandbox (SC3) | accept | QTWEBENGINE_DISABLE_SANDBOX=1 disables only the inner Chromium sandbox; UAT (D-12) confirms the subprocess runs with no namespace error AND remains within the outer Flatpak sandbox (acceptable per FP-05). Evidence transcript captures the absence of `namespace: not permitted`. |
| T-86-14 | Information Disclosure | MPRIS2 over-exposure (SC4) | mitigate | UAT (D-10) confirms `busctl --user list` shows ONLY `org.mpris.MediaPlayer2.MusicStreamer`, proving the manifest's --own-name (not broad session-bus) is what's on the bus — the deny-list drift-guard (Plan 03) is the static enforcement; this UAT is the functional confirmation. |
| T-86-15 | Tampering | Host data mutated by import (SC5) | mitigate | UAT confirms the original ~/.local/share/musicstreamer/ is intact after import (copy-don't-delete, D-02; the :ro mount enforces it). |
</threat_model>

<verification>
- 86-VERIFICATION.md contains all five evidence sections (SC1-SC5) with audible note + screenshot ref + transcript per D-09.
- MPRIS2 transcript shows the short name org.mpris.MediaPlayer2.MusicStreamer (D-10).
- GBS.FM transcript shows no namespace error and persistence across relaunch (D-12).
- AAC audible across DI.fm + AudioAddict + SomaFM tiers (SC2).
</verification>

<success_criteria>
- PKG-LIN-FP-02 (install/launch), FP-05 (GBS.FM login persistence), FP-07 (audible AAC), FP-08 (MPRIS2 media-key control) functionally verified with embedded evidence — the human-verified completion of Phase 86's five ROADMAP success criteria.
</success_criteria>

<output>
Create `.planning/phases/86-linux-flatpak-build/86-05-SUMMARY.md` when done
</output>
