---
phase: 72.3
slug: make-station-tile-and-album-art-responsive-to-window-screen
status: passed
verified_at: 2026-05-19
must_haves_verified: 11/11
tests_green: 174/174 (phase + sibling suites)
---

# Phase 72.3 — Verification

**Phase:** 72.3 — Make station tile and album art responsive to window/screen size
**Status:** ✓ PASSED
**Verified:** 2026-05-19

## Phase Goal

Replace the fixed 180×180 station logo and 160×160 album cover on the now-playing panel with a three-tier responsive scheme (140 / 180 / 240) driven by `NowPlayingPanel.width()` via the existing Phase 72.1 `resizeEvent` override. Logo and cover are equal-sized at every tier (D-05 — cover bumps 160→180 at the medium-tier default). Tier change is instant; no persistence, no animation, no hysteresis. Behavior holds in both compact and expanded modes.

## Must-Have Verification

| # | Must-Have | Status | Evidence |
|---|-----------|--------|----------|
| 1 | All five legacy literals source from central tier | ✓ | `now_playing_panel.py:1576, 1608, 1614` read `n = self._current_art_tier or 180`; lines 350 + 757 are construction-time medium-tier defaults (180/180). |
| 2 | `setFixedSize` updates via `_apply_art_tier` cached-diff orchestrator | ✓ | `now_playing_panel.py:1499-1511` — cached-diff guard at L1500. |
| 3 | Phase 72.1's `resizeEvent` EXTENDED, picker reflow runs first | ✓ | Single override at L1170-1185; ordering super → `_reflow_stream_picker_row` → `_apply_art_tier`. |
| 4 | Logo and cover both report 140/180/240 at matching widths | ✓ | Tests TBD-01/02/03 + TBD-06 GREEN. |
| 5 | YouTube 16:9 letterboxing preserved | ✓ | `_load_scaled_pixmap` at L213 unchanged (`KeepAspectRatio + SmoothTransformation`); TBD-10 GREEN. |
| 6 | No `repo.set_setting` calls for tier state (D-09) | ✓ | TBD-12 GREEN. |
| 7 | No `lambda` in tier wiring (QA-05) | ✓ | TBD-13 GREEN. |
| 8 | Compact and expanded modes return same tier at same width | ✓ | TBD-11 GREEN. |
| 9 | 13 tests in `test_phase72_3_responsive_art.py` pass | ✓ | 13/13 GREEN. |
| 10 | Phase 72/72.1 sibling test suites still pass | ✓ | 174 passed (test_phase72_3 + test_phase72_1 + test_phase72_now_playing + test_now_playing_panel). |
| 11 | LAYOUT-03 registered in REQUIREMENTS.md | ✓ | L61 definition + L183 traceability row + L227 rollup + L241 footer. |

## Implementation Surface

- **Modified:** `musicstreamer/ui_qt/now_playing_panel.py` — added `_current_art_tier`, `_last_cover_path` sentinels; added `_current_art_tier_size()` (pure tier predicate, thresholds 700/1100) and `_apply_art_tier()` (cached-diff orchestrator with cover-slot state branching); refactored three render call sites to read the central tier; extended Phase 72.1 resizeEvent override; bumped `cover_label` construction-time default 160→180 (D-05).
- **Created:** `tests/test_phase72_3_responsive_art.py` — 13 tests covering tier truth table, thresholds, equality invariant, pixmap dimensions, idempotence, cover-art replay, YouTube 16:9 letterbox, compact/expanded parity, no-persistence + no-lambda negative controls.
- **Updated:** `tests/test_now_playing_panel.py` — 3 stale `QSize(160, 160)` asserts updated to `QSize(180, 180)`; letterbox expected pixmap for 320×180 source 160×90 → 180×101 (commit `508b618`).
- **Registered:** `LAYOUT-03` in `.planning/REQUIREMENTS.md` under v2.1 milestone.

## Plans Executed

- ✓ 72.3-01 — Wave 0 TDD-RED scaffold (13 tests + LAYOUT-03 registration)
- ✓ 72.3-02 — Wave 1 helpers + sentinels + cover_label default bump
- ✓ 72.3-03 — Wave 2 render-site refactor + Phase 72.1 resizeEvent extension + _last_cover_path tracking

## Deferred / Out-of-Scope (Acceptable)

- Sidebar station-tile icons (`STATION_ICON_SIZE = 32`) — explicitly excluded per D-01.
- Recently-Played QListView icons — same constant; out of scope.
- Continuous proportional sizing / fill-and-cap — rejected this phase; could revisit if tiered feels too steppy.
- Hysteresis dead-band on tier thresholds — ship without; observe; same as Phase 72.1's deferral.
- iTunes URL upgrade `160x160 → 600x600` for sharper tier-240 covers — separate cover-art-quality phase.
- Animation / fade on tier change — instant resize matches 72/72.1 precedent.

## Known Out-of-Scope Failures (Pre-existing)

- `tests/test_headless_entry.py::test_headless_smoke_wires_without_error` — pre-existing flaky / environment issue.
- `tests/test_main_window_integration.py::test_hamburger_menu_actions` — documented carry-over from Phase 74/76 (per PROJECT.md).
- `ModuleNotFoundError: 'gi'` on `tests/test_player_*.py`, `tests/test_twitch_*.py`, etc. — environment gap (PyGObject/GStreamer Python bindings not installed in worktree `.venv`); pre-existing, unrelated to Phase 72.3.

## Conclusion

Phase 72.3 delivers its full scope. The now-playing logo and album cover are responsive to panel width across three tiers (140/180/240), driven by `NowPlayingPanel.width()` through the extended Phase 72.1 `resizeEvent` override. All locked decisions (D-01..D-07) honored. All 13 Nyquist validation tests pass. Sibling suites unaffected. Ready to ship.
