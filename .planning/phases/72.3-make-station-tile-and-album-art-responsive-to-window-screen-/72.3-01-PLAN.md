---
phase: 72.3
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_phase72_3_responsive_art.py
autonomous: true
requirements: [LAYOUT-03]
tags: [layout, pyside6, ui-qt, tdd-red, scaffold, layout-03, phase-72-3]
must_haves:
  truths:
    - "tests/test_phase72_3_responsive_art.py exists and is collected by pytest (13 tests total)."
    - "Every test in the new file is RED at this wave — production code (the _apply_art_tier orchestrator and _current_art_tier_size pure helper) does NOT exist yet, so every test that asserts panel._current_art_tier or panel._current_art_tier_size() raises AttributeError. Tests that only assert label sizes (e.g., test_medium_tier_at_default) ALSO fail because the cover_label is still 160x160 (Plan 02 ships the 180x180 default)."
    - "REQUIREMENTS.md ### Layout (LAYOUT) section gains a LAYOUT-03 row continuing the LAYOUT-01 / LAYOUT-02 cluster."
    - "REQUIREMENTS.md Traceability table gains a row mapping LAYOUT-03 -> Phase 72.3, status Pending."
    - "REQUIREMENTS.md coverage rollup (Total / Pending counts) updates accordingly."
    - "Test idiom is the live Phase 72.1 pattern: FakePlayer + simple FakeRepo + _make_panel(qtbot) factory + panel.show() + qtbot.waitExposed(panel) before any panel.resize(w, h)."
    - "Decisions D-01..D-07 are encoded in the test names: D-02 (single source of truth read by all sites) = test_pixmap_dimensions_track_tier; D-03 (three render sites refactored) = test_cover_art_ready_after_resize_matches_tier; D-04 (tier values 140/180/240) = test_narrow_tier_at_floor / test_medium_tier_at_default / test_wide_tier_at_widescreen; D-05 (logo==cover at every tier) = test_logo_equals_cover_at_every_tier; D-06 (panel-width trigger via resizeEvent) = test_compact_and_expanded_modes_same_tier; D-07 (no lambdas) = test_no_lambda_in_tier_wiring."
    - "Threshold lock (700, 1100) is encoded by test_narrow_to_medium_threshold (599/699 -> 140; 700/701 -> 180) and test_medium_to_wide_threshold (1099 -> 180; 1100 -> 240)."
    - "Negative tests assert NO repo.set_setting for tier keys (D-09 inheritance from Phase 72) and NO lambda on tier-helper source lines (QA-05)."
  artifacts:
    - path: "tests/test_phase72_3_responsive_art.py"
      provides: "TDD-RED scaffold of 13 tests covering the LAYOUT-03 truth table, thresholds, idempotence, replay, letterbox, mode parity, and two negative grep-style assertions."
      contains: "def test_narrow_tier_at_floor"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAYOUT-03 requirement row + traceability row + updated coverage rollup numbers."
      contains: "LAYOUT-03"
  key_links:
    - from: "tests/test_phase72_3_responsive_art.py"
      to: "musicstreamer/ui_qt/now_playing_panel.py"
      via: "imports NowPlayingPanel; references panel._current_art_tier and panel._current_art_tier_size() (both will exist after Plan 02)"
      pattern: "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"
    - from: ".planning/REQUIREMENTS.md (LAYOUT-03 row)"
      to: ".planning/REQUIREMENTS.md (Traceability table row)"
      via: "REQ-ID string match"
      pattern: "LAYOUT-03"
---

<objective>
Wave 0 TDD-RED scaffold for Phase 72.3 (responsive logo + cover-art tier). Lands two artifacts:

1. **REQUIREMENTS.md update** — register `LAYOUT-03` as a fresh requirement code in the `### Layout (LAYOUT)` section. LAYOUT-01 stays owned by Phase 72 (compact-mode toggle, Complete); LAYOUT-02 stays owned by Phase 72.1 (multi-stream picker reflow, Pending); LAYOUT-03 owns the responsive logo/cover tier scheme.
2. **Test file scaffold** — `tests/test_phase72_3_responsive_art.py` with the 13 tests enumerated in `72.3-VALIDATION.md` §Per-Task Verification Map (TBD-01..TBD-13) PLUS the W0 collect-only check (TBD-W0-01). Tests are written against the contract that Plan 02 (helpers + cover default bump) and Plan 03 (render-site refactor + resizeEvent extension + cover-state tracking) will implement.

Purpose: Lock the contract in pytest. Every test must be RED before Plan 02 begins. As Plans 02 and 03 land, tests flip GREEN in groups: Plan 02's helper landings flip TBD-01..TBD-08 (label-size assertions); Plan 03's render-site refactor + resizeEvent extension flips TBD-09..TBD-13. The scaffold also writes the negative-grep tests (TBD-12 no-repo-set-setting and TBD-13 no-lambda) — both will be GREEN-by-coincidence at Wave 0 but encode the invariants that prevent regression in Plans 02/03.

Output: REQUIREMENTS.md gains LAYOUT-03 and updated traceability. Test file exists with all 13 tests collected; running `pytest tests/test_phase72_3_responsive_art.py --collect-only` returns 13 items; running `pytest tests/test_phase72_3_responsive_art.py -x` exits non-zero (RED state expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-CONTEXT.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-VALIDATION.md
@tests/test_phase72_1_stream_picker_reflow.py
@tests/test_phase72_now_playing_panel.py
@tests/_fake_player.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register LAYOUT-03 in REQUIREMENTS.md (cluster continuation + traceability row)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — locate `### Layout (LAYOUT)` section near LAYOUT-01 and LAYOUT-02 rows; locate Traceability table; locate Coverage rollup at the bottom)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md §Phase Requirements (recommends the exact LAYOUT-03 description text)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-CONTEXT.md §canonical_refs (cluster continuation rationale)
  </read_first>
  <action>
    Three additive edits to .planning/REQUIREMENTS.md (no deletions, no renumbering of other rows):

    (1) In `### Layout (LAYOUT)` section, immediately after the LAYOUT-02 row, append a new `- [ ] **LAYOUT-03**:` bullet using the description from 72.3-RESEARCH.md §Phase Requirements. The body must mention: width-driven trigger on NowPlayingPanel.width(); three tiers 140/180/240; logo equals cover at every tier (D-05); recomputed on every resizeEvent with `setFixedSize` + pixmap rescale firing only on tier change (cached diff); no SQLite persistence (D-09 inheritance from Phase 72); works in both compact and expanded modes; cite Phase 72.3 as the owning phase.

    (2) In the Traceability table (the markdown table near the bottom with `| Requirement | Phase | Status |` columns), append a row `| LAYOUT-03 | Phase 72.3 | Pending |` immediately after the existing `| LAYOUT-02 | Phase 72.1 | Pending |` row.

    (3) In the Coverage rollup block (the section starting `**Coverage:**` near the bottom), update the totals so v2.1 requirements increments by one (61 -> 62), the Mapped count increments by one, the Pending count increments by one, and the Pending parenthetical list of in-flight items gains `LAYOUT-03 -- Phase 72.3 responsive logo/cover tier`. Add a dated footer line at the very bottom matching the existing footer format: `*Last updated: 2026-05-19 -- LAYOUT-03 (Phase 72.3 responsive logo + cover-art tier) added during `/gsd:plan-phase 72.3`. Fresh-code precedent matches Phase 72.1 / LAYOUT-02 -- follow-up phases get their own requirement code.*`

    All updates per the cluster pattern Phase 79 / BUG-11 established (fresh-code precedent already cited in REQUIREMENTS.md's LAYOUT-02 footer note).
  </action>
  <verify>
    <automated>grep -c "LAYOUT-03" .planning/REQUIREMENTS.md  # must be >= 3 (section row + traceability row + footer note); and: grep -n "^### Layout (LAYOUT)" .planning/REQUIREMENTS.md (section still present)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LAYOUT-03" .planning/REQUIREMENTS.md` returns >= 3.
    - `grep -c "LAYOUT-02" .planning/REQUIREMENTS.md` is unchanged from pre-edit (LAYOUT-02 row preserved verbatim).
    - The new LAYOUT-03 row appears AFTER LAYOUT-02 inside `### Layout (LAYOUT)` (grep -n shows LAYOUT-03 line number > LAYOUT-02 line number).
    - The Traceability table contains a row matching `| LAYOUT-03 | Phase 72.3 | Pending |` (literal substring grep).
    - The Coverage rollup numbers are incremented by exactly 1 (Total and Mapped and Pending each + 1; Complete unchanged).
    - No other row in REQUIREMENTS.md is modified (`git diff --stat .planning/REQUIREMENTS.md` shows only additions, zero deletions).
  </acceptance_criteria>
  <done>LAYOUT-03 is registered as the owning requirement code for Phase 72.3 with full traceability and updated coverage rollup. Downstream plans (02, 03) reference `requirements: [LAYOUT-03]` in their frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_phase72_3_responsive_art.py with 13 RED tests + scaffolding fixtures</name>
  <files>tests/test_phase72_3_responsive_art.py</files>
  <read_first>
    - tests/test_phase72_1_stream_picker_reflow.py (sibling-phase analog — read the FakeRepo at lines 75-120, the _make_panel factory at lines 123-142 INCLUDING the `panel.show() + qtbot.waitExposed(panel)` lines 140-141 which are load-bearing per Pitfall 6 of 72.3-RESEARCH, the resize-then-assert idiom at lines 225-239, the no-repo-set-setting negative test at lines 510-541, and the no-lambda source-grep test at lines 544-571)
    - tests/test_phase72_now_playing_panel.py (alternate simpler FakeRepo at lines 44-72 — Phase 72.3 does NOT need streams_by_station so either FakeRepo shape works; PATTERNS.md §B recommends the simpler shape)
    - tests/_fake_player.py (the shared FakePlayer — imported unchanged)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §`tests/test_phase72_3_responsive_art.py` sections A through F (verbatim code excerpts for fixture, resize idiom, negative tests, and the 16:9 letterbox test skeleton — Plan 01 follows these to the letter, with one deviation noted below)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-VALIDATION.md §Per-Task Verification Map (13 test names — every test in the new file MUST use exactly the name listed)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md §Threshold Derivation (locks 700 and 1100; tests must use 599/699/700/701 for the narrow boundary and 1099/1100 for the wide boundary) and §Open Question 3 (aspect-ratio rounding tolerance: assert `abs(pix.width()/pix.height() - 16/9) < 0.01`)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md §Tier State Truth Table (the canonical truth table tests must encode)
    - musicstreamer/ui_qt/now_playing_panel.py lines 195-213 (`_load_scaled_pixmap` — tests load this transitively when binding a station) and lines 290-350 (`__init__` around the existing logo `setFixedSize(180, 180)` at line 339)
  </read_first>
  <action>
    Create `tests/test_phase72_3_responsive_art.py` mirroring the Phase 72.1 file structure (header, fixtures, then 13 named tests in order).

    Imports block (top of file, mirror 72.1 lines 60-72): `from __future__ import annotations`; `import inspect`; `from typing import Any, Optional`; `import pytest`; `from PySide6.QtCore import QSize, Qt`; `from PySide6.QtGui import QPixmap`; `from musicstreamer.ui_qt import icons_rc  # noqa: F401  side-effect: registers :/icons`; `from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel`; `from musicstreamer.models import Station, StationStream`; `from tests._fake_player import FakePlayer`. Do NOT import any helper that does not exist on `main` today; this file is independent of Plan 02/03 code.

    Fixture block: copy the simpler FakeRepo from `tests/test_phase72_now_playing_panel.py:44-72` verbatim (no `streams_by_station` kwarg — tier behavior is stream-count-independent per RESEARCH §Wave 0 Gaps). Then a `_make_panel(qtbot, settings: Optional[dict] = None) -> NowPlayingPanel` factory following PATTERNS.md §A — instantiate FakeRepo + NowPlayingPanel(FakePlayer(), repo), call `qtbot.addWidget(panel)`, then `panel.show()` followed by `qtbot.waitExposed(panel)` (BOTH are load-bearing — without them Wayland-style resize events don't dispatch and tier assertions become flaky per Pitfall 6 of 72.3-RESEARCH). Add a small helper `_make_bound_station(art_path: Optional[str] = None) -> Station` that constructs a minimal Station with no streams; binding is optional — most tests run on an unbound panel.

    Then write each of the 13 tests in the exact order and with the exact name from `72.3-VALIDATION.md` §Per-Task Verification Map. The contract for each test is:

    1. `test_narrow_tier_at_floor` — `panel.resize(600, 600)`; assert `panel.logo_label.size().width() == 140`, `panel.logo_label.size().height() == 140`, `panel.cover_label.size().width() == 140`, `panel.cover_label.size().height() == 140`, AND `panel._current_art_tier == 140`. Uses width 600 (in [560, 700) per truth table -> narrow).

    2. `test_medium_tier_at_default` — `panel.resize(800, 600)`; assert all four label dims == 180 AND `panel._current_art_tier == 180`.

    3. `test_wide_tier_at_widescreen` — `panel.resize(1400, 800)`; assert all four label dims == 240 AND `panel._current_art_tier == 240`.

    4. `test_narrow_to_medium_threshold` — call resize at FOUR widths in order: 599, 699, 700, 701. After each, assert `panel._current_art_tier_size()` returns 140 at 599 and 699, returns 180 at 700 and 701. This pins the inclusive lower edge at 700 per the UI-SPEC `< 700 -> 140; 700 <= width < 1100 -> 180` predicate.

    5. `test_medium_to_wide_threshold` — call resize at THREE widths: 1099, 1100, 1500. Assert `_current_art_tier_size()` returns 180 at 1099 and 240 at 1100 and 240 at 1500.

    6. `test_logo_equals_cover_at_every_tier` — loop over widths `(600, 800, 1200)`; after each resize assert `panel.logo_label.size() == panel.cover_label.size()` AND both are square (`size.width() == size.height()`). Encodes D-05.

    7. `test_pixmap_dimensions_track_tier` — bind a station with no station_art_path (so the fallback icon renders). Resize to 600, then 800, then 1400. After each resize, assert that the logo_label's current pixmap (via `panel.logo_label.pixmap()`) has `.width() <= tier_value` AND `.height() <= tier_value` (KeepAspectRatio means a 16:9 fallback fits inside the box but is not larger than tier; SVG fallback at audio-x-generic renders as a square so .width() == .height() == tier exactly).

    8. `test_within_band_resize_is_noop` — `panel.resize(800, 600)`; capture `cached_tier = panel._current_art_tier`. Then `panel.resize(900, 600)`, then `panel.resize(950, 600)`, then `panel.resize(1050, 600)` — all within the medium band [700, 1100). Assert `panel._current_art_tier == 180` at each step (tier value stays). The idempotence claim is verified by the cache value not bumping; pixmap-thrash avoidance is implementation-side, the test pins the OUTPUT contract.

    9. `test_cover_art_ready_after_resize_matches_tier` — `panel.resize(1400, 800)` so tier=240. Build a real PNG fixture inline using PIL or QPixmap: `src = QPixmap(300, 300); src.fill(Qt.blue); art_path = tmp_path / "fake_cover.png"; src.save(str(art_path))`. Then call `panel._set_cover_pixmap(str(art_path))` directly (the rescale path Plan 03 refactors). Assert `panel.cover_label.pixmap().size() == QSize(240, 240)` (1:1 source scaled to 240x240). Use `tmp_path` fixture from pytest. Encodes D-03 cover-art rescale through tier helper.

    10. `test_youtube_16_9_letterbox_at_every_tier` (uses `tmp_path`) — Build a 16:9 source: `src = QPixmap(1280, 720); src.fill(Qt.red); art_path = tmp_path / "yt_16_9.png"; src.save(str(art_path))`. Construct a Station with `station_art_path = str(art_path)`. For each tier width in `[(600, 140), (800, 180), (1400, 240)]`: resize the panel, bind the station, call `_show_station_logo()` directly, and assert that the resulting `panel.logo_label.pixmap()` size has width == tier (i.e., 140/180/240) AND `abs(pix.width() / pix.height() - 16/9) < 0.01` (per RESEARCH Open Question 3 rounding tolerance). The 16:9 letterboxes inside the square label; height is approximately tier * 9/16.

    11. `test_compact_and_expanded_modes_same_tier` — instantiate two panels at the SAME width (e.g., 1200). Panel A's panel.width() is 1200 (expanded analog); Panel B's panel.width() is also 1200 (compact analog). Assert `panel_a._current_art_tier == panel_b._current_art_tier == 180`. Per UI-SPEC: the trigger is `self.width()` not mode, so equal width -> equal tier regardless of compact state.

    12. `test_no_repo_set_setting_for_tier_state` — follow PATTERNS.md §D verbatim, swap forbidden_fragments to `("tier", "art_size", "logo_size", "cover_size")`. Snapshot `panel._repo._settings` keys before, sweep widths `(560, 800, 1200, 1600, 600, 1100)`, snapshot after, assert no new key contains any forbidden fragment. Encodes D-09 no-persistence.

    13. `test_no_lambda_in_tier_wiring` — follow PATTERNS.md §E verbatim, swap targets to `("_current_art_tier_size", "_apply_art_tier", "resizeEvent")`. `inspect.getsource(NowPlayingPanel)` and assert no line containing any target string also contains `lambda`. Encodes QA-05.

    Every test uses the `_make_panel(qtbot)` factory (so `panel.show() + qtbot.waitExposed` runs every time). Tests 9 and 10 also take the `tmp_path` pytest fixture. No `qtbot.wait()` / no synthetic `QResizeEvent` (Pitfall 7) — use the public `panel.resize(w, h)` API only.

    All tests are RED at this wave because (a) `panel._current_art_tier` attribute does not exist (Plan 02 adds it), so tests 1-8, 11 hit AttributeError; (b) `panel._current_art_tier_size()` method does not exist (Plan 02 adds it), so tests 4-5 hit AttributeError; (c) the `cover_label` is still 160x160 at construction (Plan 02 bumps the default to 180), so the equal-size invariant in test 6 fails at the medium band; (d) `_set_cover_pixmap` still hard-codes QSize(160, 160) (Plan 03 refactors), so test 9 fails. Tests 12 and 13 may pass GREEN-by-coincidence at Wave 0 — that is acceptable; they encode the invariant going forward.

    Do NOT inline a FakePlayer subclass — import `FakePlayer` from `tests._fake_player` (per the project INFRA-01 / D-17 source-grep drift-guard at `tests/test_fake_player_no_inline.py`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase72_3_responsive_art.py --collect-only -q 2>&1 | tail -5  # must show 13 items collected; and: uv run pytest tests/test_phase72_3_responsive_art.py -x 2>&1 | tail -5  # must exit non-zero (RED)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_phase72_3_responsive_art.py` exists.
    - `uv run pytest tests/test_phase72_3_responsive_art.py --collect-only -q` reports exactly 13 collected test items.
    - `grep -c "^def test_" tests/test_phase72_3_responsive_art.py` returns 13.
    - All 13 test names appear in the file (grep each from 72.3-VALIDATION.md TBD-01..TBD-13).
    - `grep -c "panel.show()" tests/test_phase72_3_responsive_art.py` >= 1 AND `grep -c "waitExposed" tests/test_phase72_3_responsive_art.py` >= 1 (fixture has the load-bearing show + waitExposed prelude per Pitfall 6).
    - `grep -c "^class FakePlayer" tests/test_phase72_3_responsive_art.py == 0` AND `grep -c "from tests._fake_player import FakePlayer" tests/test_phase72_3_responsive_art.py == 1` (INFRA-01 / D-17 drift-guard satisfied — no inline FakePlayer; canonical import only).
    - `grep -c "QApplication.sendEvent" tests/test_phase72_3_responsive_art.py == 0` (no synthetic resize events — Pitfall 7).
    - `grep -c "lambda" tests/test_phase72_3_responsive_art.py == 0` (test file itself follows QA-05; the `targets` tuple in test 13 lists symbol NAMES as bare strings, not lambdas).
    - `uv run pytest tests/test_phase72_3_responsive_art.py -x` exits non-zero AND the first failure cites an AttributeError or assertion error tied to `_current_art_tier` / `_current_art_tier_size` / cover_label size (RED state confirmed).
  </acceptance_criteria>
  <done>13 RED tests collected by pytest. Plan 02 can begin; as it lands `_current_art_tier_size` + `_apply_art_tier` + the cover_label default bump, tests 1-8 and 11 flip GREEN. Plan 03's render-site refactor flips tests 9 and 10. Tests 12 and 13 are GREEN-by-coincidence and stay GREEN as invariant guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qt platform -> NowPlayingPanel.resizeEvent | Resize events are platform-dispatched; no remote injection surface in a single-user desktop app. |
| Repo (SQLite) -> Station.station_art_path | Repo-supplied station_art_path is validated by the Phase 47.x station-edit flow before reaching the renderer; no new write paths in 72.3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.3-01 | Information Disclosure (residual V12) | _load_scaled_pixmap on station_art_path | accept-inherited | Phase 72.3 introduces no new file-resolution path; the existing _load_scaled_pixmap null-fallback at line 213 of now_playing_panel.py handles missing/invalid paths by rendering :/icons/audio-x-generic-symbolic.svg. RESEARCH §Security Domain confirms "this phase introduces no new threat surface." |
| T-72.3-02 | Denial of Service | Tier 240 pixmap upscale from 160x160 iTunes source | accept | Qt's SmoothTransformation is O(target_size) not O(source_size), so a 240x240 upscale costs the same as a downscale. No DoS vector. Documented as a quality-not-security concern (mild blur on iTunes covers at tier 240; out-of-scope mitigation in a future cover-art-quality phase). |

Phase 72.3 is a pure UI-sizing phase with no new threat surface. All mitigations inherit from existing code paths.
</threat_model>

<verification>
- `git status` shows two modified/new files: `.planning/REQUIREMENTS.md` and `tests/test_phase72_3_responsive_art.py`. No edits to `musicstreamer/` source files (Plan 02 owns those).
- `uv run pytest tests/test_phase72_3_responsive_art.py --collect-only -q` reports 13 collected tests.
- `uv run pytest tests/test_phase72_3_responsive_art.py -x` exits non-zero (RED state).
- `uv run pytest tests/ -x -k "not phase72_3"` continues to exit 0 (no collateral damage to the existing suite from the REQUIREMENTS.md edit or the new file).
- REQUIREMENTS.md coverage rollup numbers are incremented by exactly one (Total + 1, Mapped + 1, Pending + 1, Complete unchanged).
</verification>

<success_criteria>
- LAYOUT-03 is registered in REQUIREMENTS.md `### Layout (LAYOUT)` section, in the Traceability table, and in the Coverage rollup footer.
- `tests/test_phase72_3_responsive_art.py` exists and is collected by pytest with exactly 13 test items.
- Every test name in the file matches the corresponding TBD-01..TBD-13 row in `72.3-VALIDATION.md`.
- Fixture uses `panel.show() + qtbot.waitExposed(panel)` (Pitfall 6 mitigation).
- No inline FakePlayer; FakePlayer is imported from `tests._fake_player` (INFRA-01 drift-guard compatible).
- All 13 tests are in RED state at Wave 0 end (production code does not exist yet).
- `git diff --stat .planning/REQUIREMENTS.md` shows additions only (no deletions).
</success_criteria>

<output>
Create `.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-01-SUMMARY.md` when done. Include:
- LAYOUT-03 row text added to REQUIREMENTS.md (verbatim).
- Confirmation of 13 collected tests + RED state (paste pytest collect-only output tail).
- Note any test names that flipped GREEN-by-coincidence at Wave 0 (tests 12 / 13 are expected GREEN; flag if any others did).
</output>
