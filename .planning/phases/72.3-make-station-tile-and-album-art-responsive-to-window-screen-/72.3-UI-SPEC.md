---
phase: 72.3
slug: make-station-tile-and-album-art-responsive-to-window-screen
status: draft
shadcn_initialized: false
preset: not applicable
created: 2026-05-19
parent_ui_spec: .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md
sibling_ui_spec: .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md
---

# Phase 72.3 — UI Design Contract (Delta Supplement)

> This is a **delta** UI-SPEC layered on top of Phase 72's parent contract and Phase 72.1's sibling delta. Read those first. Sections marked **Inherited** carry forward unchanged; only the **Delta** sections below describe new visual contract for Phase 72.3.
>
> **Scope:** ONE behavior — replace the two fixed `setFixedSize` calls on `logo_label` (180×180 at `now_playing_panel.py:339`) and `cover_label` (160×160 at `now_playing_panel.py:745`) with a three-tier responsive scheme (140 / 180 / 240) driven by the now-playing panel's own width via the existing `resizeEvent` override. Both labels render at the **same** N value at every tier. The three pixmap re-render call sites (lines 1468, 1495, 1499) read the central tier instead of hardcoded `QSize`. No new colors, no new copy, no new layout containers.

---

## Inheritance From Phase 72 / 72.1 UI-SPECs

The following sections of the parent / sibling UI-SPECs apply unchanged. Do NOT re-litigate.

| Parent / Sibling Section | Status for 72.3 | Notes |
|--------------------------|-----------------|-------|
| Design System (PySide6 6.10+, palette-driven, system font) | Inherited from Phase 72 | No stack change |
| Spacing Scale (4 / 8 / 16 / 24 / 32 / 48 / 64) | Inherited from Phase 72 | Outer `QHBoxLayout(margins=16, spacing=24)` unchanged |
| Typography (system default, no new text) | Inherited from Phase 72 | No labels, headings, or copy introduced |
| Color (60/30/10 split; `palette(...)` tokens; no hardcoded hex) | Inherited from Phase 72 | Logo/cover are image labels; no chrome color changes |
| Copywriting Contract (no new copy, no toasts on layout changes) | Inherited from Phase 72 / 72.1 | Silent tier change (matches the no-toast precedent) |
| Z-order contract (toast > peek > now-playing) | Inherited from Phase 72 | Labels remain in NowPlayingPanel's outer plane |
| Animation precedent (instant, no transitions) | Inherited from Phase 72 / 72.1 D-06 | Tier change is INSTANT; matches "no animation" precedent |
| `resizeEvent` override hook on NowPlayingPanel | Inherited from Phase 72.1 D-05 | 72.3 extends the existing override with one additional call (see Interaction Contract) |
| Width-driven layout decisions, not mode-coupled | Inherited from Phase 72.1 D-01 | Tier follows panel width regardless of compact-mode state |
| No SQLite persistence for layout state | Inherited from Phase 72 D-09 | Tier is recomputed every resize; no `Repo.set_setting` call |
| Bound-method overrides/connects (QA-05) | Inherited project-wide | `resizeEvent` extension + tier helper are bound methods, no lambdas |
| Registry Safety (Qt-native; n/a) | Inherited from Phase 72 | No registries, no third-party blocks |

---

## Design System (Delta)

No changes. This phase adds no widgets, no icons, no fonts, no color tokens, no QSS rules. It modifies two `setFixedSize(...)` integer literals and three `QSize(...)` integer literals so all five values are sourced from one central tier helper.

---

## Spacing Scale (Delta)

The outer three-column `QHBoxLayout` retains its existing 16px content margins and 24px spacing (`now_playing_panel.py:332-333`). The phase introduces new **square art tier dimensions** which are not part of the general spacing scale but are declared here as prescriptive constants.

### Art tier dimensions (prescriptive, single source of truth)

| Tier | Logo size | Cover size | Logo = Cover invariant (D-05) | Source |
|------|-----------|------------|-------------------------------|--------|
| Narrow (S) | 140×140 px | 140×140 px | YES | CONTEXT D-04 + D-05 |
| Medium (M) — default | 180×180 px | 180×180 px | YES | CONTEXT D-04 + D-05 (cover bumps 160→180) |
| Wide (L) | 240×240 px | 240×240 px | YES | CONTEXT D-04 + D-05 |

All three tier values are multiples of 4 (140 = 4×35, 180 = 4×45, 240 = 4×60), preserving the project's 4px-base invariant.

### Threshold widths (planner-discretion within recommended bands)

Decision points where the panel's own width transitions between tiers. CONTEXT recommends:

| Threshold | Recommended panel width | Rationale |
|-----------|-------------------------|-----------|
| Narrow → Medium | ~700 px | Between the 560 px `setMinimumWidth` floor and the typical default. Aligns with Phase 72 UAT-04's "560–700 px wide" overlap-range observation |
| Medium → Wide | ~1100 px | Leaves room for two 240-px squares + center column without crowding |

Planner finalizes exact values (Claude's Discretion per CONTEXT). Both thresholds must satisfy: `560 < narrow→medium < medium→wide`. Thresholds may be derived from widget minimums (preferred — option (a) from CONTEXT) or hand-tuned constants.

### Tier invariants (executor must enforce)

| Invariant | Rule |
|-----------|------|
| Equality | At any tier, `logo_label.size().width() == cover_label.size().width()` AND `.height()` is the same `N`. Both labels are square. |
| Single source of truth | All five legacy literals (lines 339, 745, 1468, 1495, 1499) read from one helper (e.g., `_current_art_tier_size() -> int`) or the cached `self._current_art_tier` integer. No call site computes its own N. |
| Multiples of 4 | The three tier values are 140 / 180 / 240. No other tier values may be introduced without amending this contract. |
| Tier-change minimum | `setFixedSize` + pixmap rescale fire **only** when the computed tier differs from `self._current_art_tier` (cached). Continuous drag within a band is a no-op. |

Exceptions: none — every dimension this phase introduces is a multiple of 4.

---

## Typography (Delta)

No new text. The logo and cover labels are image-only; both already use `Qt.AlignCenter` and `setScaledContents(False)`. No tooltip, no overlay label, no fallback text is introduced. Existing system-font inheritance applies to any future text adjacent to these labels but is untouched here.

---

## Color (Delta)

No new tokens. The labels are `QLabel` widgets carrying pixmaps; their background is `palette(window)` (theme-dependent) and is never overridden. The empty / fallback / placeholder state continues to show `_FALLBACK_ICON` via `_load_scaled_pixmap` at every tier — fallback rendering is identical at 140 / 180 / 240 except for the scale (Qt scales the fallback pixmap with `Qt.KeepAspectRatio + Qt.SmoothTransformation`).

No border, no drop-shadow, no outline, no checkered placeholder is introduced. The label "empty state" is the existing fallback `audio-x-generic` icon, scaled to the current tier.

---

## Copywriting Contract (Delta)

No new copy. No tooltips, no toasts, no labels, no error messages, no destructive confirmations are introduced.

| Element | Copy | Source |
|---------|------|--------|
| Tier-change notification | n/a — silent | Matches Phase 72 / 72.1 D-Discretion ("toast on layout change: silent") |
| Empty-art label text | n/a — no text overlay; fallback icon scales with tier | Existing `_FALLBACK_ICON` path in `_load_scaled_pixmap` |
| Threshold-crossing toast | n/a — silent | Same precedent as the picker reparent in 72.1 |

---

## Interaction Contract (Delta — the heart of this phase)

This is the only substantive section. Everything below is new visual / interaction contract for Phase 72.3.

### Tier State Truth Table

The panel's width (read inside `resizeEvent`) maps to exactly one tier. Both labels and all three rescale call sites read the same tier value.

| Panel width range | Tier | `logo_label.setFixedSize` | `cover_label.setFixedSize` | `_load_scaled_pixmap` size at lines 1468 / 1495 / 1499 |
|-------------------|------|---------------------------|----------------------------|---------------------------------------------------------|
| 560 ≤ width < narrow→medium threshold (~700) | Narrow (S) | `(140, 140)` | `(140, 140)` | `QSize(140, 140)` |
| narrow→medium ≤ width < medium→wide (~1100) | Medium (M) — default | `(180, 180)` | `(180, 180)` | `QSize(180, 180)` |
| width ≥ medium→wide threshold | Wide (L) | `(240, 240)` | `(240, 240)` | `QSize(240, 240)` |

**Invariants enforced by this table:**
- Logo size always equals cover size at the same instant (D-05).
- Tier values are exactly `{140, 180, 240}`; no continuous or interpolated values.
- The trigger metric is `NowPlayingPanel.width()` — NOT MainWindow width, NOT screen geometry, NOT splitter handle position (D-06).
- The 560-px minimum width from `now_playing_panel.py:326` is unchanged; the narrow tier (140) is sized to fit comfortably alongside the center column at that floor.

### Resize → Tier Transition Contract

| Property | Value | Source |
|----------|-------|--------|
| Trigger event | `NowPlayingPanel.resizeEvent(QResizeEvent)` (override — already exists from Phase 72.1) | CONTEXT D-06 + Phase 72.1 D-05 |
| Tier recompute frequency | Every resize event (cheap — three integer comparisons) | Implementation budget |
| `setFixedSize` + pixmap rescale frequency | Only when tier value changes (cached `self._current_art_tier` comparison) | CONTEXT D-Claude-Discretion "Tier change detection" |
| Animation | None — instant | CONTEXT inherits Phase 72 / 72.1 D-06 |
| Flicker target | ≤ 1 frame at 60Hz (≤ 16 ms) between old and new label size being painted | Implementation budget — `setFixedSize` + `setPixmap` are synchronous |
| Aspect-ratio handling at every tier | `Qt.KeepAspectRatio + Qt.SmoothTransformation` (existing `_load_scaled_pixmap` line 213) | CONTEXT — unchanged |
| YouTube 16:9 logo behavior | Letterboxes inside the (now variable) square box at all three tiers — CONTAIN-style fit preserved | CONTEXT — explicit invariant |
| Native 1:1 cover behavior | Fills the square at all three tiers — preserved | CONTEXT — explicit invariant |
| Repaint scope | NowPlayingPanel labels only — splitter, MainWindow, ToastOverlay, peek overlay unaffected | Self-contained in NowPlayingPanel |
| Z-order change | None — labels remain in the outer `QHBoxLayout` at the same z-plane | RESEARCH §Architecture |
| Hysteresis dead-band | None (ship without; observe; only add if real flicker reported) | CONTEXT Deferred Ideas |
| Mid-resize coalescing / debounce | None (Wayland coalesces resize events) | CONTEXT Deferred Ideas + Phase 72.1 precedent |

### Initial-Bind Contract

| Trigger | Tier value |
|---------|------------|
| `NowPlayingPanel.__init__` (before any data is bound) | `self._current_art_tier = None` (sentinel); labels constructed with the **medium tier default (180×180)** so off-layout test instantiation gets predictable sizes. |
| First `resizeEvent` after the splitter assigns initial sizes | `_apply_art_tier()` runs; if `self.width()` selects a non-medium tier, labels resize from 180 → tier value (one-shot at startup). |
| Cover-art-ready signal received before first resize | `_set_cover_pixmap` reads the current tier (180 default if `_current_art_tier` is still `None`) and rescales accordingly. |

### Co-existence With Phase 72.1's resizeEvent

Phase 72.1 has shipped (`resizeEvent` override at `now_playing_panel.py:1149` calls `self._reflow_stream_picker_row()`). Phase 72.3 **extends** the same override with one additional bound-method call:

```python
def resizeEvent(self, event):  # noqa: N802 (Qt override)
    super().resizeEvent(event)
    self._reflow_stream_picker_row()  # Phase 72.1 / LAYOUT-02
    self._apply_art_tier()             # Phase 72.3 / NEW
```

| Property | Value |
|----------|-------|
| Override count per class | Exactly ONE `resizeEvent` override on `NowPlayingPanel` (Qt convention) |
| Ordering of helpers | `super().resizeEvent(event)` FIRST (mandatory per Pitfall 9 in 72.1-RESEARCH); then `_reflow_stream_picker_row()`; then `_apply_art_tier()`. The two helpers are independent — neither reads state mutated by the other. |
| Bound-method invariant | Both calls are bound methods on `self`; no lambdas (QA-05). |
| Recursion guard | Neither helper calls `self.resize(...)` or `self.setGeometry(...)` (would cause infinite recursion). Both only mutate child widgets. |

### Compact-Mode / Expanded-Mode Cooperation

Tier follows panel width regardless of compact-mode state. Truth table:

| Mode | Panel width source | Tier behavior |
|------|--------------------|---------------|
| Expanded (left column visible) | Splitter assigns NowPlayingPanel width based on user's splitter drag + window width | Tier picked from `self.width()` per the truth table above |
| Compact (left column hidden, Phase 72) | NowPlayingPanel inherits the full freed width (splitter handles auto-hide per Phase 72 RESEARCH A1) | Tier still picked from `self.width()` — typically larger, so tier may step up to L |
| Compact + peek overlay open (Phase 72 hover-peek) | Peek overlay is parented to `centralWidget()` and floats over; NowPlayingPanel's own width is unchanged | Tier does NOT recompute on peek open/close (peek doesn't trigger NowPlayingPanel.resizeEvent) |

### Per-Render-Site Source-of-Truth Mapping

Three pixmap-render call sites must be refactored to read the tier (CONTEXT D-03). After refactor, each site reads the same tier integer:

| Site | Before (current code) | After (this contract) | Why |
|------|-----------------------|------------------------|-----|
| `_set_cover_pixmap` line 1468 | `pix.scaled(QSize(160, 160), Qt.KeepAspectRatio, Qt.SmoothTransformation)` | `pix.scaled(QSize(N, N), Qt.KeepAspectRatio, Qt.SmoothTransformation)` where N is the current tier | Cover-art replay after tier change must produce a pixmap matching the new label size |
| `_show_station_logo` line 1495 | `_load_scaled_pixmap(path, QSize(180, 180))` | `_load_scaled_pixmap(path, QSize(N, N))` where N is the current tier | Logo render must match tier-resized label |
| `_show_station_logo_in_cover_slot` line 1499 | `_load_scaled_pixmap(path, QSize(160, 160))` | `_load_scaled_pixmap(path, QSize(N, N))` where N is the current tier | Logo-in-cover-fallback must also track tier |

Helper-function shape: planner picks pattern (a) `_current_art_tier_size() -> int` (pure, reads `self.width()`, returns 140/180/240) + `_apply_art_tier()` (orchestrator that diffs against `self._current_art_tier`, calls `setFixedSize` + re-renders), or (b) one combined method. CONTEXT recommends (a) for testability.

### Focus / Tab-Order / Accessibility Contract

| Concern | Behavior |
|---------|----------|
| Keyboard focus | Labels are not focusable (`QLabel` default). Tier changes do not steal focus from any widget. |
| Tab order | Unchanged. `logo_label` and `cover_label` are skipped by tab navigation (non-focusable). |
| Screen reader / accessible name | Unchanged — labels carry no `setAccessibleName`. Existing accessibility surface is preserved. |
| High-contrast | Pixmap rendering quality is identical at all three tiers (Qt's `SmoothTransformation` honors system render settings). |
| Color-blind safety | n/a — labels carry imagery, not state-distinguishing color. |
| Touch | Out of scope — deployment is Wayland desktop with mouse (per memory `project_deployment_target.md`). |

### Visual Edges (explicit clarifications)

CONTEXT noted that pixel-only changes can still create visual edges. Each is addressed explicitly here:

| Edge | Decision |
|------|----------|
| No-art / empty-cover placeholder appearance | Existing `_FALLBACK_ICON` (audio-x-generic) scales to the tier via `Qt.KeepAspectRatio + Qt.SmoothTransformation`. No border, no checkered pattern, no "No art" text overlay is introduced. The label background remains `palette(window)` (theme-driven). |
| Logo + cover vertical alignment at the medium tier (cover bumps 160 → 180) | Both labels are equal-sized at 180×180; the outer `QHBoxLayout` with `Qt.AlignVCenter` on `center` (the middle column) centers all three columns vertically. The +20 px cover bump produces **balanced** framing (no asymmetric "logo bigger than cover" appearance). |
| Center column squeeze at the wide tier (240×240 squares) | The center column uses `outer.addLayout(center, 1)` — it stretches with available width. At the wide tier threshold (~1100 px), the center column has ≥ 1100 − 16×2 − 24×2 − 240×2 = 540 px of working width. No clipping of ICY label, elapsed timer, or control row expected. Planner verifies the exact threshold satisfies this floor. |
| Tier change while cover-art fetch is in flight | The cover-art fetch token (`self._cover_fetch_token` at line 1455) is unaffected by tier changes. When the in-flight fetch returns, `_set_cover_pixmap` reads the **current** tier and rescales correctly. |
| Tier change while the fallback `_show_station_logo_in_cover_slot` is showing | The cover label is showing the station logo at the old tier size. On tier change, `_apply_art_tier` re-invokes the fallback render path so the logo-in-cover is rescaled to the new tier. |
| Wayland coalesced resize during continuous drag | Tier-change check (`if new_tier != self._current_art_tier`) is the single guard against pixmap thrash during drag-within-band. Wayland's resize coalescing further reduces event volume. |

### Out-of-Scope Visual Decisions (explicit)

The following visual / interaction surfaces are explicitly NOT introduced by this phase. Adding any of them would expand scope.

- **No continuous proportional sizing** (e.g., `size = clamp(pane_width × 0.3, min, max)`). Tiered breakpoints only. (CONTEXT D-04)
- **No fill-and-cap sizing** (label grows to fill its column). Fixed tier values only. (CONTEXT Deferred Ideas)
- **No tier-change animation** or fade. Instant resize. (CONTEXT inherits Phase 72 / 72.1)
- **No hysteresis dead-band** for threshold flicker suppression. (CONTEXT Deferred Ideas)
- **No persisted "user prefers tier X" setting.** Pane width is authoritative. (CONTEXT Deferred Ideas)
- **No per-display-geometry tier memory.** Dynamic width-based tier is the trigger. (CONTEXT Deferred Ideas)
- **No tier-change toast** ("Art resized to fit window"). Silent. (CONTEXT Deferred Ideas)
- **No symmetric-asymmetric scaling** preserving the legacy 180/160 logo-bigger-than-cover. Equal at every tier. (CONTEXT D-05)
- **No changes to `STATION_ICON_SIZE`** in `_theme.py`. Sidebar tiles and Recently-Played icons stay at 32×32. (CONTEXT D-01)
- **No changes to `_load_scaled_pixmap` signature or aspect-ratio logic.** Helper already accepts arbitrary `QSize`. (CONTEXT canonical_refs)
- **No changes to the outer `QHBoxLayout` margins (16) or spacing (24).** Only the two `setFixedSize` calls and three `QSize` literals move. (CONTEXT canonical_refs)
- **No changes to `setMinimumWidth(560)`** on NowPlayingPanel or MainWindow. (CONTEXT canonical_refs)
- **No changes to `setScaledContents(False)` on labels.** Pixmap is pre-scaled by `_load_scaled_pixmap`, not by Qt auto-scaling. (CONTEXT canonical_refs)
- **No new placeholder border / outline / shadow** on empty cover state. (Visual edges decision above)
- **No keyboard shortcut to force a tier.** Layout is fully resize-driven. (Inherited)

---

## Theme & A11y Integration

| Concern | Decision |
|---------|----------|
| Theme compatibility | Labels carry pixmaps, not chrome. The eight Phase 66 themes (System default, Vaporwave, Overrun, GBS.FM, GBS.FM After Dark, Dark, Light, Custom) all show the same image content; only the surrounding panel background (`palette(window)`) differs by theme. |
| Accent color (Phase 19) | Not consumed by labels. No selection / focus / highlight state exists. |
| Keyboard navigation | Labels are non-focusable; tab order unchanged. |
| Screen reader | No new accessible names introduced; existing pixmap-only labels carry no text content. |
| High-contrast | Pixmap render quality identical at all three tiers. |
| Touch | Out of scope (Wayland desktop). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | n/a — Qt-native phase | not applicable |
| Third-party | none | not applicable |

This phase uses only stock PySide6 widgets (`QLabel`, `QPixmap`, `QSize`). No SVG assets, no new icons, no `icons.qrc` change, no `icons_rc.py` regeneration, no remote registries.

---

## File Inventory (artifacts this contract creates or modifies)

| File | New / Modified | Visual responsibility |
|------|----------------|------------------------|
| `musicstreamer/ui_qt/now_playing_panel.py` | Modified | (1) `self.logo_label.setFixedSize(180, 180)` at line 339 → tier-driven initial value (default 180 if pre-resize). (2) `self.cover_label.setFixedSize(160, 160)` at line 745 → tier-driven initial value (equal to logo per D-05). (3) `QSize(160, 160)` in `_set_cover_pixmap` at line 1468 → `QSize(N, N)`. (4) `QSize(180, 180)` in `_show_station_logo` at line 1495 → `QSize(N, N)`. (5) `QSize(160, 160)` in `_show_station_logo_in_cover_slot` at line 1499 → `QSize(N, N)`. (6) New `self._current_art_tier: int | None = None` initialized in `__init__`. (7) New `_current_art_tier_size(self) -> int` pure helper (returns 140/180/240 from `self.width()`). (8) New `_apply_art_tier(self)` orchestrator (diffs cached tier, calls `setFixedSize` + re-renders both labels). (9) Extend existing `resizeEvent` at line 1149 with `self._apply_art_tier()` after `self._reflow_stream_picker_row()`. |
| `tests/test_phase72_3_responsive_art.py` | **New** | pytest-qt coverage of the three-width → three-tier truth table; tier-change idempotence within a band; YouTube 16:9 letterboxing at each tier; cover-art-replay-after-resize matches new tier; equal-size invariant at every tier; compact + expanded mode honour same tier given same panel width; negative-grep assertion that no `repo.set_setting` call references any tier key. Test idiom matches `tests/test_phase72_1_stream_picker_reflow.py` (`qtbot.resize(panel, QSize(w, h))` / `widget.resize(w, h); qtbot.wait(0)`). |

No SVG changes, no QSS changes, no theme additions, no `_theme.py` changes, no `icons.qrc` updates.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (no new copy; silent tier change matches 72/72.1 precedent)
- [ ] Dimension 2 Visuals: PASS (tier truth table, three-tier values, single-source-of-truth contract, visual-edge clarifications all declared; animation/z-order/flicker budget explicit)
- [ ] Dimension 3 Color: PASS (no new tokens; labels carry pixmaps only; fallback icon scales with tier)
- [ ] Dimension 4 Typography: PASS (no new text introduced)
- [ ] Dimension 5 Spacing: PASS (tier values 140/180/240 all multiples of 4; outer layout margins/spacing unchanged)
- [ ] Dimension 6 Registry Safety: PASS (n/a — Qt-native, no registries)

**Approval:** pending
