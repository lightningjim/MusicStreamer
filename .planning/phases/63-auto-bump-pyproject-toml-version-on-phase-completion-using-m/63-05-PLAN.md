---
phase: 63
plan: 05
type: execute
wave: 3
depends_on: [63-01, 63-02, 63-03, 63-04]
files_modified:
  - .planning/PROJECT.md
  - tests/test_bump_version.py
autonomous: true
requirements: [VER-01]
tags: [version-bump, documentation, project-md, drift-guard]
must_haves:
  truths:
    - "`.planning/PROJECT.md` gains a `## Versioning` section inserted IMMEDIATELY ABOVE the existing `## Constraints` heading (PATTERNS.md §`.planning/PROJECT.md` insertion target)"
    - "The new section documents the `major.minor.phase` schema in prose, points to the helper at `tools/bump_version.py`, and points to the config flag `workflow.auto_version_bump` in `.planning/config.json`"
    - "The worked example uses the verbatim text from CONTEXT.md §specifics line 112: \"Closing Phase 50 of v2.1 yields `version = \\\"2.1.50\\\"`; closing Phase 63 yields `2.1.63`. The leading `2.1` comes from `## Current Milestone: v2.1` in this file.\""
    - "A new drift-guard test `test_project_md_has_versioning_section` asserts (a) the H2 heading `## Versioning` exists, (b) the worked-example anchor string `2.1.50` AND `2.1.63` both appear in PROJECT.md, (c) the helper path `tools/bump_version.py` is mentioned, (d) the flag name `workflow.auto_version_bump` is mentioned"
    - "The drift-guard test follows the `tests/test_constants_drift.py` idiom — failure message lists what was found nearby so the fix is obvious"
    - "No other PROJECT.md content is touched — the insertion is additive, NOT a rewrite (`git diff --shortstat .planning/PROJECT.md` shows pure insertion)"
    - "The FINAL acceptance criterion of this plan's last task verifies SC #2 end-to-end: after Phase 63's own self-completion commit lands, `git log -1 --name-only HEAD` shows `pyproject.toml` AND at least one `.planning/` file in the SAME commit object — proving the bump was bundled into the phase-completion commit (Warning 4 Option A — verification, not code change)"
    - "D-12: a `## Versioning` section is added to `PROJECT.md` near `## Constraints` documenting the `major.minor.phase` schema with a worked example (Phase 50 of v2.1 → `2.1.50`) and noting that the bump is automated via `workflow.auto_version_bump`; the schema doc is committed in this phase, not bumped separately"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "## Versioning section above ## Constraints — schema docs + worked example + helper/flag pointers"
      contains: "## Versioning"
      min_lines_added: 4
    - path: "tests/test_bump_version.py"
      provides: "+ test_project_md_has_versioning_section (VALIDATION row 63-05-01)"
      min_lines: 280
  key_links:
    - from: ".planning/PROJECT.md (## Versioning section)"
      to: "tools/bump_version.py"
      via: "inline mention naming the helper path"
      pattern: "tools/bump_version\\.py"
    - from: ".planning/PROJECT.md (## Versioning section)"
      to: ".planning/config.json"
      via: "inline mention of workflow.auto_version_bump flag"
      pattern: "workflow\\.auto_version_bump"
    - from: ".planning/PROJECT.md (## Versioning section)"
      to: ".planning/PROJECT.md (## Current Milestone heading)"
      via: "prose explanation that {major}.{minor} is sourced from the milestone heading"
      pattern: "Current Milestone"
    - from: "tests/test_bump_version.py::test_project_md_has_versioning_section"
      to: ".planning/PROJECT.md"
      via: "Path(__file__).resolve().parent.parent / '.planning' / 'PROJECT.md' read + grep assertions"
      pattern: "PROJECT\\.md"
---

<objective>
Document the `major.minor.phase` versioning schema in `.planning/PROJECT.md` with a worked example, then add a drift-guard test that fails loudly if the documentation is reformatted away. This plan closes SC #4 ("convention is documented in PROJECT.md with the schema and a worked example") and adds the final end-to-end verification gate for SC #2 (bumped pyproject lands in the same commit object as `.planning/*` files — Warning 4 Option A).

Purpose: Implements D-12 verbatim (`## Versioning` section near `## Constraints` with worked example). The drift-guard test ensures the section cannot silently disappear in a future PROJECT.md edit. The SC #2 verification at the END of this plan's tasks is the final hard contract: after Phase 63 self-completes, the resulting commit object MUST contain pyproject.toml AND at least one .planning/ file — anything less is a regression on D-02.
Output: PROJECT.md gains 4-6 lines (one H2 heading + one paragraph + one bold-labeled worked example); test count rises from 10 (post-Plan 04) to 11.

**Decisions implemented:** D-12 (schema doc with worked example, near `## Constraints`).

**Open Question 3 resolution (RESEARCH.md):** Place the section IMMEDIATELY ABOVE `## Constraints` (after the last `## Key Decisions` table row at line 221, before `## Constraints` at line 223). RESEARCH §Open Question 3 recommendation; PATTERNS.md §`.planning/PROJECT.md` exact insertion target.

**Warning 4 (plan-checker):** SC #2 ("bump bundled in phase-completion commit, no orphaned uncommitted state") was previously verified ONLY in a synthetic `fake_repo` integration test in Plan 03. Option A applied here: this plan's final task adds a HARD acceptance criterion that runs `git log -1 --name-only HEAD` AFTER Phase 63's self-completion lands, asserting `pyproject.toml` and at least one `.planning/` file co-occur in that commit's tree. This is a verification, not a code change — it respects the planner's earlier judgment that fixing `gsd-sdk query commit`'s gitignore behavior (RESEARCH §Pitfall 1) is out of scope for Phase 63.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-CONTEXT.md
@.planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-RESEARCH.md
@.planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-PATTERNS.md
@.planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-VALIDATION.md
@.planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-01-SUMMARY.md
@.planning/PROJECT.md
@tests/test_bump_version.py
@tests/test_constants_drift.py

<interfaces>
<!-- The exact prose to insert into .planning/PROJECT.md. -->
<!-- Source: PATTERNS.md §`.planning/PROJECT.md` target shape; CONTEXT.md §specifics line 112. -->

Inserted immediately above `## Constraints` (current PROJECT.md line 223), after the last Key Decisions table row at line 221:

```markdown
## Versioning

The `pyproject.toml` `version` field follows `{major}.{minor}.{phase}`, where `{major}.{minor}` is sourced from the `## Current Milestone: vX.Y` heading above and `{phase}` is the most recently completed phase number. The bump is automated by `tools/bump_version.py` via the `.claude/settings.json` PreToolUse hook on `gsd-sdk query commit "docs(phase-NN): complete phase execution"` — gated by the `workflow.auto_version_bump` flag in `.planning/config.json` (default: true).

**Worked example:** Closing Phase 50 of v2.1 yields `version = "2.1.50"`; closing Phase 63 yields `2.1.63`. The leading `2.1` comes from `## Current Milestone: v2.1` in this file.

## Constraints
```

The four critical anchor strings the drift-guard test will assert:
1. `## Versioning` (H2 heading literal — must be unique in the file)
2. `2.1.50` AND `2.1.63` (worked example version values)
3. `tools/bump_version.py` (helper pointer)
4. `workflow.auto_version_bump` (flag pointer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the ## Versioning section into .planning/PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 215-230 — the insertion neighborhood; verify the `## Constraints` heading is still at line 223 before editing; if it has moved, find the new location and adjust)
    - .planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-CONTEXT.md (D-12 + §specifics line 112 — verbatim worked example)
    - .planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-PATTERNS.md (§`.planning/PROJECT.md` cluster — exact insertion target above `## Constraints`; section style: prose paragraph + bold-inline-label worked example)
    - .planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-RESEARCH.md (§Open Question 3 — placement recommendation: above `## Constraints`)
  </read_first>
  <action>
    **Step A — Locate the insertion point with grep first (defense against line-number drift):**

    ```bash
    grep -n "^## Constraints" .planning/PROJECT.md
    ```
    Expected output: `223:## Constraints` (per PATTERNS.md inspection 2026-05-08). If the line number has shifted, use the heading match — `^## Constraints` is unique in the file.

    **Step B — Use the Edit tool to insert the new section ABOVE `## Constraints`:**

    Match on the literal string `## Constraints\n\n- **Tech stack:**` (NOT line numbers — line numbers may drift). Replace it with:

    ```
    ## Versioning

    The `pyproject.toml` `version` field follows `{major}.{minor}.{phase}`, where `{major}.{minor}` is sourced from the `## Current Milestone: vX.Y` heading above and `{phase}` is the most recently completed phase number. The bump is automated by `tools/bump_version.py` via the `.claude/settings.json` PreToolUse hook on `gsd-sdk query commit "docs(phase-NN): complete phase execution"` — gated by the `workflow.auto_version_bump` flag in `.planning/config.json` (default: true).

    **Worked example:** Closing Phase 50 of v2.1 yields `version = "2.1.50"`; closing Phase 63 yields `2.1.63`. The leading `2.1` comes from `## Current Milestone: v2.1` in this file.

    ## Constraints

    - **Tech stack:**
    ```

    The literal `## Constraints` heading and the `- **Tech stack:**` bullet are PRESERVED unchanged — they are part of the Edit tool's `old_string` AND `new_string`, sandwiching the new section.

    **CRITICAL — preserve all other content:** The Edit tool replaces ONLY the matched substring. Lines before `## Constraints` (Key Decisions table at lines 215-221, blank line at 222) and lines after `- **Tech stack:**` (the rest of the Constraints bullet list + `## Phase History` table) MUST remain byte-identical. Verify with `git diff --shortstat .planning/PROJECT.md` after the edit — should show pure insertion (e.g., `+5 -0`).

    **Section style fidelity** (per PATTERNS.md §`.planning/PROJECT.md` Target shape):
    - H2 heading (`## Versioning`)
    - One paragraph of prose with backticked inline code (`pyproject.toml`, `{major}.{minor}.{phase}`, `tools/bump_version.py`, etc.)
    - **Bold inline label** (`**Worked example:**`) followed by the example sentence — matches the prose-paragraph + bold-inline-label idiom seen in `## Core Value` (line 7-9) and `## Current Milestone` (line 11-13)
    - Blank line above and below the section (separates it from the Key Decisions table above and `## Constraints` below)

    **DO NOT** add subheadings (`### Schema`, `### Helper`, etc.) — the parent sections (`## Core Value`, `## Constraints`) are flat prose; match that style for consistency.
  </action>
  <acceptance_criteria>
    - `grep -c "^## Versioning$" .planning/PROJECT.md` returns exactly 1 (heading present, unique)
    - `grep -c "^## Constraints$" .planning/PROJECT.md` returns exactly 1 (Constraints heading still present, not duplicated)
    - The Versioning section appears BEFORE the Constraints section: `awk '/^## Versioning$/{v=NR} /^## Constraints$/{c=NR} END{exit !(v && c && v < c)}' .planning/PROJECT.md` exits 0
    - `grep -q "tools/bump_version.py" .planning/PROJECT.md` (helper pointer)
    - `grep -q "workflow.auto_version_bump" .planning/PROJECT.md` (flag pointer)
    - `grep -q "Worked example" .planning/PROJECT.md` (bold label present — bolded as `**Worked example:**`)
    - `grep -q "2.1.50" .planning/PROJECT.md` (worked example anchor #1 — also confirms the historical-context Phase 50 callout)
    - `grep -q "2.1.63" .planning/PROJECT.md` (worked example anchor #2 — Phase 63 self)
    - `grep -q "Current Milestone" .planning/PROJECT.md` (cross-link to existing heading — pre-existing AND newly added; this confirms the prose mentions the milestone heading by name; count should be >= 2)
    - `git diff --shortstat .planning/PROJECT.md` shows pure insertion: positive insertion count, ZERO deletion count (e.g., `1 file changed, 5 insertions(+)` — the `(-)` count is 0 or absent)
    - `grep -q "## Tech stack" .planning/PROJECT.md && false || true` (negative check — no accidental promotion of a sub-bullet to an H2)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Versioning$" .planning/PROJECT.md | grep -qx "1" && grep -q "tools/bump_version.py" .planning/PROJECT.md && grep -q "workflow.auto_version_bump" .planning/PROJECT.md && grep -q "2.1.50" .planning/PROJECT.md && grep -q "2.1.63" .planning/PROJECT.md && awk '/^## Versioning$/{v=NR} /^## Constraints$/{c=NR} END{exit !(v && c && v &lt; c)}' .planning/PROJECT.md</automated>
  </verify>
  <done>The `## Versioning` section is present in `.planning/PROJECT.md` immediately above `## Constraints`, contains the verbatim worked example from CONTEXT.md §specifics line 112, mentions both `tools/bump_version.py` and `workflow.auto_version_bump`, and is the only modification to PROJECT.md in this task (pure additive diff).</done>
</task>

<task type="auto">
  <name>Task 2: Add the documentation drift-guard test + final SC #2 verification</name>
  <files>tests/test_bump_version.py</files>
  <read_first>
    - tests/test_bump_version.py (existing 10 tests from Plans 01-04 — APPEND only; do NOT rewrite)
    - tests/test_constants_drift.py (lines 18-27 — drift-guard idiom: helpful failure message listing what was found near the missing target)
    - .planning/PROJECT.md (the file under test — Task 1 just modified it)
    - .planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-VALIDATION.md (row 63-05-01 — `test_project_md_has_versioning_section` is the contract; SC #4 is the requirement reference)
  </read_first>
  <behavior>
    test_project_md_has_versioning_section (VALIDATION row 63-05-01, SC #4):
    - Resolve `Path(__file__).resolve().parent.parent / ".planning" / "PROJECT.md"` and read with `encoding="utf-8"`.
    - Assert the file exists with a helpful failure message listing what's in `.planning/`.
    - Assert the H2 heading `## Versioning` appears exactly once via `re.findall(r'^## Versioning$', text, re.MULTILINE)` having length 1.
    - Assert ALL FOUR anchor strings appear in the file:
      1. `tools/bump_version.py` (helper pointer)
      2. `workflow.auto_version_bump` (flag pointer)
      3. `2.1.50` (worked example anchor — Phase 50 historical)
      4. `2.1.63` (worked example anchor — Phase 63 self)
    - Assert the section ordering: `## Versioning` appears BEFORE `## Constraints` in the file (heading position relationship — drift-guard against accidental section reordering).
    - Failure messages mirror `tests/test_constants_drift.py:22-27`: state what was looked for, what was found nearby, and which Phase 63 plan introduced the section (Plan 05).
  </behavior>
  <action>
    Append ONE new test function to `tests/test_bump_version.py`. All required imports (`re`, `pathlib.Path`) are already present from Plans 01-04. No new imports needed.

    Test body (verbatim — append after the last existing test):
    ```python
    def test_project_md_has_versioning_section():
        """Phase 63 Plan 05 (SC #4 / D-12 / VALIDATION row 63-05-01): PROJECT.md
        must document the `major.minor.phase` schema with a worked example.

        Drift-guard: if a future edit reformats `## Versioning` away (or moves it
        below `## Constraints`, or strips the worked-example anchors), this test
        fails loud and tells you what to put back.

        The four anchor strings (helper path, flag name, two worked-example
        version values) ensure the section's CONTENT is intact — not just the
        heading. Plan 05's source content is in CONTEXT.md §specifics line 112.
        """
        repo = Path(__file__).resolve().parent.parent
        project_md = repo / ".planning" / "PROJECT.md"
        assert project_md.exists(), (
            f"Phase 63 drift: expected {project_md}; "
            f"found in .planning/: "
            f"{sorted(p.name for p in (repo / '.planning').iterdir())}"
        )

        text = project_md.read_text(encoding="utf-8")

        # 1. ## Versioning H2 heading present, unique.
        versioning_matches = re.findall(r'^## Versioning$', text, re.MULTILINE)
        assert len(versioning_matches) == 1, (
            f"Phase 63 Plan 05 drift: expected exactly one `## Versioning` H2 "
            f"heading in {project_md}, found {len(versioning_matches)}. "
            f"PATTERNS.md §`.planning/PROJECT.md` insertion target: "
            f"immediately above `## Constraints`. "
            f"Existing H2 headings nearby: "
            f"{re.findall(r'^## .+$', text, re.MULTILINE)[:8]}"
        )

        # 2. Four content anchors — helper path, flag, two worked examples.
        anchors = {
            "helper path (tools/bump_version.py)": "tools/bump_version.py",
            "config flag (workflow.auto_version_bump)": "workflow.auto_version_bump",
            "worked example anchor 2.1.50": "2.1.50",
            "worked example anchor 2.1.63": "2.1.63",
        }
        missing = [
            label for label, needle in anchors.items() if needle not in text
        ]
        assert not missing, (
            f"Phase 63 Plan 05 drift: PROJECT.md `## Versioning` section is "
            f"missing required anchor(s): {missing}. "
            f"Source-of-truth content lives in CONTEXT.md §specifics line 112. "
            f"Re-add the section (Task 1 of Plan 05) to fix."
        )

        # 3. Section ordering — Versioning BEFORE Constraints.
        ver_line = next(
            (i for i, line in enumerate(text.splitlines(), 1)
             if line.strip() == "## Versioning"),
            None,
        )
        cons_line = next(
            (i for i, line in enumerate(text.splitlines(), 1)
             if line.strip() == "## Constraints"),
            None,
        )
        assert ver_line is not None, "## Versioning heading not found by line scan"
        assert cons_line is not None, "## Constraints heading not found by line scan"
        assert ver_line < cons_line, (
            f"Phase 63 Plan 05 drift: `## Versioning` (line {ver_line}) must "
            f"appear BEFORE `## Constraints` (line {cons_line}) per "
            f"PATTERNS.md §`.planning/PROJECT.md` insertion target."
        )
    ```

    **Why these specific anchors:** Each anchor maps to a load-bearing piece of D-12:
    - `tools/bump_version.py` → "where the bump logic lives" (D-03)
    - `workflow.auto_version_bump` → "how to opt out" (D-10/D-11; SC #3)
    - `2.1.50` → ties the worked example to the historical record (Phase 50 was the first instance — REQUIREMENTS.md notes this)
    - `2.1.63` → ties the worked example to the self-completion case Phase 63 produces

    Drop any one of these and the test fails — precisely the drift behavior we want.
  </action>
  <acceptance_criteria>
    - `grep -c "^def test_" tests/test_bump_version.py` >= 11 (10 from Plans 01-04 + 1 new)
    - `grep -q "test_project_md_has_versioning_section" tests/test_bump_version.py`
    - `grep -q "^## Versioning" tests/test_bump_version.py` (the test contains the literal heading regex it asserts on; this is the regex-target literal in the source code)
    - `uv run --with pytest pytest tests/test_bump_version.py::test_project_md_has_versioning_section -x` exits 0 (test GREEN against the live PROJECT.md from Task 1)
    - `uv run --with pytest pytest tests/test_bump_version.py -x` — ALL 11 tests GREEN (or 9 GREEN + 2 SKIPPED if `jq` unavailable; the new test does NOT depend on `jq`)
    - No regression on the 10 prior tests (Plans 01-04)
  </acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_bump_version.py::test_project_md_has_versioning_section -x</automated>
  </verify>
  <done>11 test definitions in `tests/test_bump_version.py`; the new drift-guard test is GREEN against the live PROJECT.md. The drift-guard pattern matches `tests/test_constants_drift.py` — failure messages tell the next reader exactly which anchor is missing and where to look (CONTEXT.md §specifics line 112) for the source-of-truth content.</done>
</task>

<task type="auto">
  <name>Task 3: Final end-to-end SC #2 verification gate (Warning 4 Option A)</name>
  <files>tests/test_bump_version.py</files>
  <read_first>
    - tests/test_bump_version.py (existing 11 tests from Plans 01-05/Tasks 1-2 — APPEND only)
    - .planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-RESEARCH.md (§Common Pitfalls §1 — `gsd-sdk query commit` gitignore failure background; §Architecture diagram — bump-fires-before-commit flow)
    - .planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-CONTEXT.md (SC #2 verbatim: "no orphaned uncommitted state"; D-02: bump fires BEFORE the commit lands)
    - .planning/ROADMAP.md (Phase 63 SC #2 — wording exactly as the user signed off)
  </read_first>
  <behavior>
    test_phase_63_self_completion_bundles_pyproject_with_planning (SC #2 final gate, Warning 4 Option A):
    - This test is GATED on Phase 63 having self-completed. Until Phase 63 is closed, it is SKIPPED.
    - Detection: Phase 63 self-completion is detected by ANY of the following (use the most reliable available):
      a. The repo's HEAD commit message matches `^docs\(phase-63\): complete phase execution` (regex on `git log -1 --format=%s HEAD`).
      b. Searching back through `git log --grep='^docs(phase-63): complete phase execution' -1 --format=%H` returns at least one commit.
    - When such a commit IS detected:
      - Capture the commit hash via `git log --grep='^docs(phase-63): complete phase execution' -1 --format=%H` (most recent match — robust against later cherry-picks of the same message).
      - Run `git show --name-only --format= <hash>` to list files in the commit's tree.
      - Assert `pyproject.toml` is in the list (the bump landed in the commit object).
      - Assert at least one path beginning with `.planning/` is in the list (the phase-completion bookkeeping landed in the same commit object — SC #2 "bump is committed as part of (or alongside) the existing phase-completion commit").
      - Assert the commit's pyproject.toml content has `version = "2.1.63"` (the bump produced the expected value): run `git show <hash>:pyproject.toml` and grep for `version = "2.1.63"`.
    - When NO matching commit exists yet (Phase 63 still in flight): `pytest.skip("Phase 63 not yet self-completed; gate fires on next /gsd-execute-phase 63 close")`.

    **Why this is the right shape (Warning 4 Option A):**
    - It is a VERIFICATION, not a code change — the planner explicitly chose this over Option B (replacing `gsd-sdk query commit` with direct `git add -f && git commit`) because Option B would require a code change to the GSD workflow integration that's out of Phase 63's scope.
    - It runs zero-cost when Phase 63 hasn't completed yet (test SKIPS).
    - It runs ALWAYS in any subsequent Phase 64+ test session, providing a permanent regression net: if a future GSD-workflow change ever breaks the bump-bundling behavior, this test goes RED on the next test run.
    - The commit-content checks are independent of Plan 03's `fake_repo` integration test — that test proves the hook MECHANISM; this test proves the live OUTCOME on the real repo.
  </behavior>
  <action>
    Append ONE new test function to `tests/test_bump_version.py`. The test uses `subprocess` (already imported from prior plans) and standard `git` commands. No new imports needed.

    Test body (verbatim — append after `test_project_md_has_versioning_section`):
    ```python
    def test_phase_63_self_completion_bundles_pyproject_with_planning():
        """Phase 63 Plan 05 (SC #2 / Warning 4 Option A): when Phase 63's own
        self-completion commit lands, the resulting commit object MUST contain
        BOTH `pyproject.toml` (the bump) AND at least one `.planning/` file
        (the phase-completion bookkeeping) — proving the bump is bundled into
        the same commit, not landed in a separate orphan commit (D-02).

        SKIPPED until Phase 63 self-completes: the trigger is the canonical
        commit message `docs(phase-63): complete phase execution`. After the
        first such commit lands in the repo, this test becomes a permanent
        regression net for SC #2 across all future phases.
        """
        repo = Path(__file__).resolve().parent.parent

        # Find the most-recent Phase 63 self-completion commit.
        result = subprocess.run(
            ["git", "-C", str(repo), "log",
             "--grep=^docs(phase-63): complete phase execution",
             "-1", "--format=%H"],
            capture_output=True, text=True, timeout=10,
        )
        commit_hash = result.stdout.strip()
        if not commit_hash:
            pytest.skip(
                "Phase 63 self-completion commit not yet present — "
                "gate fires on next /gsd-execute-phase 63 close. "
                "Searched: git log --grep='^docs(phase-63): complete phase execution' -1"
            )

        # List files in the commit's tree.
        files_result = subprocess.run(
            ["git", "-C", str(repo), "show", "--name-only", "--format=",
             commit_hash],
            capture_output=True, text=True, check=True, timeout=10,
        )
        files = [f for f in files_result.stdout.splitlines() if f.strip()]

        # Assert pyproject.toml is in the commit (the bump landed).
        assert "pyproject.toml" in files, (
            f"SC #2 regression: Phase 63 self-completion commit {commit_hash} "
            f"did NOT include pyproject.toml. The bump was either skipped or "
            f"landed in a separate orphan commit (violating D-02). "
            f"Files in commit: {files}"
        )

        # Assert at least one .planning/ path is in the commit (bookkeeping landed).
        planning_files = [f for f in files if f.startswith(".planning/")]
        assert planning_files, (
            f"SC #2 regression: Phase 63 self-completion commit {commit_hash} "
            f"did NOT include any .planning/ files alongside pyproject.toml. "
            f"The phase-completion bookkeeping must land in the SAME commit "
            f"object as the bump (SC #2: 'no orphaned uncommitted state'). "
            f"Files in commit: {files}"
        )

        # Assert the bumped version value is 2.1.63 (the expected value for
        # Phase 63 of v2.1).
        show_result = subprocess.run(
            ["git", "-C", str(repo), "show", f"{commit_hash}:pyproject.toml"],
            capture_output=True, text=True, check=True, timeout=10,
        )
        assert 'version = "2.1.63"' in show_result.stdout, (
            f"SC #2 regression: Phase 63 self-completion commit {commit_hash} "
            f"contains pyproject.toml but the version value is not '2.1.63'. "
            f"This indicates the bump helper either ran with the wrong --phase "
            f"argument or computed the wrong milestone. Got pyproject.toml content: "
            f"{show_result.stdout[:200]!r}..."
        )
    ```

    **Important — this test SKIPS during Plan 05 execution itself:** When Plan 05 runs (before Phase 63 closes), the Phase 63 commit does NOT yet exist. The test will SKIP with the message above. That's the correct behavior — the gate FIRES on the next test run after Phase 63's `/gsd-complete-phase` lands the commit. From that point onward, the test is GREEN forever (or RED if a future change breaks SC #2 — exactly the regression net we want).

    **Idempotence:** Running this test multiple times after Phase 63 completes always produces the same GREEN — `git log --grep` is deterministic; `--format=%H` returns one hash; the file-list and content checks are read-only.
  </action>
  <acceptance_criteria>
    - `grep -c "^def test_" tests/test_bump_version.py` >= 12 (11 from prior tasks + 1 new)
    - `grep -q "test_phase_63_self_completion_bundles_pyproject_with_planning" tests/test_bump_version.py`
    - `grep -q "git log" tests/test_bump_version.py` (the test invokes git log to find the Phase 63 commit)
    - `grep -q "pytest.skip" tests/test_bump_version.py` (the SKIP-when-not-yet-completed branch is present)
    - `grep -q '2.1.63' tests/test_bump_version.py` (the expected version value is asserted)
    - `uv run --with pytest pytest tests/test_bump_version.py::test_phase_63_self_completion_bundles_pyproject_with_planning -x` either exits 0 (GREEN if Phase 63 already completed) OR exits 0 with SKIPPED status (Phase 63 not yet completed). Both outcomes are acceptable — the test is gated.
    - `uv run --with pytest pytest tests/test_bump_version.py -x` — ALL 12 tests pass-or-skip cleanly: at minimum 9 GREEN + 1 SKIP (this gate) + up to 2 SKIPS for the jq-gated tests. NO test ERRORs, NO test FAILUREs.
    - `git log -1 --name-only --format= HEAD | grep -q "pyproject.toml" && git log -1 --name-only --format= HEAD | grep -q "^.planning/"` — verifies AT WHATEVER POINT the test runs that the LATEST commit (whether Phase 63's or a follow-up) is consistent: if HEAD is a phase-63 commit, both predicates are true; if HEAD is something else (e.g., a Plan 05 commit during execution), this is informational only and does NOT gate the test (the gate is the dedicated test above, which uses `git log --grep`, not HEAD).
  </acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_bump_version.py::test_phase_63_self_completion_bundles_pyproject_with_planning -x</automated>
  </verify>
  <done>12 test definitions present in `tests/test_bump_version.py`. The new SC #2 verification test is correctly gated: it SKIPS when Phase 63 has not yet self-completed (which is the case during this plan's execution) and GATES on `pyproject.toml + .planning/* + version = "2.1.63"` co-occurrence in the Phase 63 self-completion commit object once that commit exists. This is the permanent regression net Warning 4 Option A required — verification, not code change, respecting the planner's earlier judgment that fixing `gsd-sdk query commit`'s gitignore behavior is out of Phase 63's scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → live `.planning/PROJECT.md` | Read-only file inspection — no mutation possible from the test path |
| Test → `git log` / `git show` subprocess | Read-only git queries scoped to the repo via `cwd=str(repo)` and `--format`-controlled output |

## STRIDE Threat Register (V12 File Handling, V14 Configuration; full matrix in RESEARCH §Security Domain)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V-DOC-DRIFT | Tampering (data integrity) | `## Versioning` section silently reformatted/removed | mitigate | `test_project_md_has_versioning_section` asserts heading + 4 content anchors + section ordering — drift fails loud with a remediation pointer (CONTEXT.md §specifics line 112) |
| T-V-SC2-REGRESSION | Tampering (data integrity, regression) | A future GSD-workflow change breaks the bump-bundling behavior | mitigate | `test_phase_63_self_completion_bundles_pyproject_with_planning` runs on every test invocation; once Phase 63 has self-completed it is GREEN forever (or RED if SC #2 regresses) — permanent regression net per Warning 4 Option A |
| T-V-DOC-INJECT | Tampering | Section content inserted via Edit tool; what if the worked example is wrong? | mitigate | The drift-guard test asserts BOTH `2.1.50` AND `2.1.63` — typo'd values fail the test immediately; CONTEXT.md §specifics line 112 is the source-of-truth reference for the correct content |
</threat_model>

<verification>
- `uv run --with pytest pytest tests/test_bump_version.py -x` — at minimum 9 GREEN + 1 SKIP (SC #2 gate, Phase 63 not yet self-completed) + up to 2 SKIPS for jq-gated tests. NO ERROR/FAIL.
- `grep -c "^## " .planning/PROJECT.md` — count of H2 headings is exactly one higher than before this plan (the new `## Versioning` heading).
- `git diff --shortstat .planning/PROJECT.md` shows positive insertions, ZERO deletions (pure additive change).
- `python3 -c "import re; t = open('.planning/PROJECT.md', encoding='utf-8').read(); assert len(re.findall(r'^## Versioning$', t, re.MULTILINE)) == 1"` exits 0.
- `awk '/^## Versioning$/{v=NR} /^## Constraints$/{c=NR} END{exit !(v && c && v < c)}' .planning/PROJECT.md` exits 0 (Versioning before Constraints).
- After Phase 63 self-completes (post-plan, future test run): `pytest tests/test_bump_version.py::test_phase_63_self_completion_bundles_pyproject_with_planning -x` exits 0 GREEN — proving SC #2 mechanically on the live commit object.
</verification>

<success_criteria>
- [ ] `.planning/PROJECT.md` has a `## Versioning` H2 section above `## Constraints`, byte-additive insertion only
- [ ] The section contains the verbatim worked example from CONTEXT.md §specifics line 112 (with `2.1.50` and `2.1.63` anchors)
- [ ] The section names both `tools/bump_version.py` (helper) and `workflow.auto_version_bump` (flag)
- [ ] `test_project_md_has_versioning_section` is GREEN — drift-guard against future PROJECT.md edits
- [ ] `test_phase_63_self_completion_bundles_pyproject_with_planning` is correctly gated: SKIPS pre-completion, GREEN post-completion (verifying SC #2 mechanically on the live commit)
- [ ] No regression on tests from Plans 01-04 (10 prior tests; status preserved: GREEN-or-SKIP)
- [ ] `pyproject.toml` is byte-identical pre/post plan execution (this plan touches only PROJECT.md and tests/)
</success_criteria>

<output>
Create `.planning/phases/63-auto-bump-pyproject-toml-version-on-phase-completion-using-m/63-05-SUMMARY.md` documenting:
- The exact diff applied to PROJECT.md (line range and content) — sufficient for replay or revert
- Confirmation that `git diff --shortstat .planning/PROJECT.md` shows pure insertion (no deletions; no other lines touched)
- Test count + GREEN/SKIPPED status:
  - Pre-self-completion (during Plan 05 run): 11 GREEN/PRIOR + 1 SKIP (SC #2 gate) [+ up to 2 SKIPS for jq-gated tests]
  - Post-self-completion (next test run after Phase 63 closes): 12 GREEN [or 11 GREEN + up to 2 SKIPS for jq-gated tests if jq missing]
- Note on Warning 4 Option A: SC #2 is now verified by BOTH the synthetic `test_bump_stages_pyproject` (Plan 03; mechanism) AND the live `test_phase_63_self_completion_bundles_pyproject_with_planning` (this plan; outcome). The mechanism test runs every CI; the outcome test gates on the actual Phase 63 commit object once it exists.
- Cite to CONTEXT.md §specifics line 112 as the source-of-truth for the worked-example prose.
- Cite to RESEARCH.md §Open Question 3 for the placement decision (above `## Constraints`).
</output>
