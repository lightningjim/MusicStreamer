---
phase: 80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe
plan: 04
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - tests/test_db_connect_is_sole_connection_factory.py
autonomous: true
requirements: [BUG-10]
requirements_addressed: [BUG-10]

must_haves:
  truths:
    - "A source-grep test asserts exactly one `sqlite3.connect(` callsite in `musicstreamer/` (production tree), located inside `musicstreamer/repo.py`."
    - "Tests/ tree is intentionally excluded from the gate (D-12) so the negative-proof test in Plan 80-03 can use raw `sqlite3.connect(\":memory:\")` legally."
    - "Pattern also matches the alternative `sqlite3.dbapi2.connect(` spelling (planner-discretion belt-and-suspenders per RESEARCH knowledge gap #4)."
    - "D-09: defense-in-depth — this source-grep gate is the static-analysis half of the drift-guard; Plan 80-01's runtime read-back is the dynamic half."
    - "D-12: source-grep test scope is production tree only (`musicstreamer/**/*.py`); `tests/` is intentionally allowed so Plan 80-03's D-15 negative test can use raw `sqlite3.connect(\":memory:\")` legally."
  artifacts:
    - path: tests/test_db_connect_is_sole_connection_factory.py
      provides: "Source-grep drift-guard asserting db_connect is the sole production sqlite3.connect callsite"
      contains: "sqlite3"
  key_links:
    - from: "tests/test_db_connect_is_sole_connection_factory.py"
      to: "musicstreamer/ production tree"
      via: "pathlib.rglob('*.py') + re.compile match"
      pattern: "rglob"
    - from: "tests/test_db_connect_is_sole_connection_factory.py"
      to: "musicstreamer/repo.py"
      via: "assertion on the unique hit's filename"
      pattern: "repo.py"
---

<objective>
Create `tests/test_db_connect_is_sole_connection_factory.py` — a pure-Python source-grep drift-guard that walks `musicstreamer/**/*.py` (production tree only, excludes `tests/`), matches the regex `sqlite3(\.dbapi2)?\.connect\(`, and asserts:

1. **Exactly one** callsite exists in the production tree.
2. **That one callsite lives in `musicstreamer/repo.py`** (specifically inside `db_connect()`).

This is D-09 / D-12 closure: the static-analysis half of the drift-guard. Plan 80-01 owns the runtime half (the post-SET read-back inside `db_connect`); this plan owns the structural half (no production file outside `repo.py` may open a SQLite connection bypassing the factory).

Purpose: Without this gate, a future commit could add `sqlite3.connect(...)` inside `aa_import.py` or `gbs_api.py` or anywhere else in production code and silently bypass `db_connect()`'s PRAGMA + drift-guard surface. Memory `feedback_gstreamer_mock_blind_spot.md` is the canonical precedent: "add source-level grep gates to ban legacy `playbin` 1.x signals."

Output: One new file `tests/test_db_connect_is_sole_connection_factory.py` ships two green tests; the existing single legal callsite at `musicstreamer/repo.py:18` (inside `db_connect`'s body, post-Plan-80-01) is the sole match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-01-SUMMARY.md
@tests/test_packaging_spec.py
@musicstreamer/repo.py

<interfaces>
<!-- Key existing source-grep precedent extracted from current source. -->

From tests/test_packaging_spec.py (verified shape — Phase 65 source-grep gate precedent to mirror):
```python
from __future__ import annotations
from pathlib import Path
import pytest

_SPEC = (
    Path(__file__).resolve().parent.parent
    / "packaging" / "windows" / "MusicStreamer.spec"
)

@pytest.fixture(scope="module")
def spec_source() -> str:
    assert _SPEC.is_file(), f"expected MusicStreamer.spec at {_SPEC}"
    return _SPEC.read_text(encoding="utf-8")

def test_spec_imports_copy_metadata(spec_source: str) -> None:
    assert "copy_metadata" in spec_source, (...)
```

From musicstreamer/repo.py (post-Plan-80-01, line ~18): the single legal production callsite of `sqlite3.connect(...)` — inside `db_connect()`. After Plan 80-01, the function has a docstring + global + read-back; the line `con = sqlite3.connect(paths.db_path())` is unchanged.

Verified inventory (RESEARCH § "Verified `sqlite3.connect(` callsite inventory"): production tree contains exactly one `sqlite3.connect(` hit at `musicstreamer/repo.py`. Test tree contains six legitimate hits across `tests/test_repo.py`, `tests/test_station_siblings.py`, `tests/test_settings_export.py`, `tests/test_theme.py`, `tests/test_main_window_integration.py`, `tests/test_main_window_soma.py` — all of which the gate intentionally allows by scoping to `musicstreamer/` only.

Memory `feedback_gstreamer_mock_blind_spot.md`: "pipeline mocks pass through any `pipeline.emit(...)` call; add source-level grep gates to ban legacy `playbin` 1.x signals on playbin3 code paths." This is the canonical project precedent for this kind of gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_db_connect_is_sole_connection_factory.py — source-grep gate over musicstreamer/**/*.py</name>
  <files>tests/test_db_connect_is_sole_connection_factory.py</files>
  <read_first>
    - tests/test_packaging_spec.py (verify the exact shape — module-scope path constant, module-scope fixture returning source content, individual test functions asserting substring/regex matches with grep-friendly failure messages)
    - musicstreamer/repo.py (post-Plan-80-01 — verify the single legal production callsite of `sqlite3.connect(...)` is at the expected line inside `db_connect()`; the gate's positive expectation is "exactly one hit, in repo.py")
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md (D-09 — defense-in-depth runtime + source-grep gates; D-12 — production tree only, allow `tests/`)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md (Code Examples section "Source-grep gate — target shape" at lines 528-568; § "Verified `sqlite3.connect(` callsite inventory" — the gate's expected output)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (test_db_connect_is_sole_connection_factory section — `pathlib.rglob` walk strategy, regex `re.compile(r"sqlite3(\.dbapi2)?\.connect\(")`, module-scope fixture returning `dict[Path, list[int]]`)
  </read_first>
  <behavior>
    After this task, the new file `tests/test_db_connect_is_sole_connection_factory.py` contains exactly two test functions, both GREEN:

    1. `test_only_one_sqlite_connect_callsite_in_production` — asserts the sum of regex matches across all `*.py` files in the `musicstreamer/` package equals exactly 1.

    2. `test_sole_sqlite_connect_callsite_lives_in_repo_py` — asserts the single file containing the match is `musicstreamer/repo.py` (filename `.name == "repo.py"`).

    Both tests use a module-scope fixture that walks `musicstreamer/**/*.py` once and returns `dict[Path, list[int]]` mapping file paths to lists of line numbers where matches occurred.

    The regex matches BOTH `sqlite3.connect(` and `sqlite3.dbapi2.connect(` (planner-discretion belt-and-suspenders per RESEARCH knowledge gap #4).

    The gate's scope is `musicstreamer/` ONLY — the `tests/` tree is intentionally excluded so the negative-proof test in Plan 80-03 (`test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing`) can use raw `sqlite3.connect(":memory:")` legally (D-12).

    Both tests emit grep-friendly failure messages that name the offending file(s) and line number(s) so a future maintainer who triggers the gate can diagnose the bypass in one read.
  </behavior>
  <action>
    Create the new file `tests/test_db_connect_is_sole_connection_factory.py`.

    File-level imports: `from __future__ import annotations`, `import re`, `from pathlib import Path`, `import pytest`.

    Module-level constants:
    - `_MUSICSTREAMER_PKG = Path(__file__).resolve().parent.parent / "musicstreamer"` (the production package root, located relative to `tests/`).
    - `_PATTERN = re.compile(r"sqlite3(\.dbapi2)?\.connect\(")` (matches both `sqlite3.connect(` and `sqlite3.dbapi2.connect(` per RESEARCH knowledge gap #4 — belt-and-suspenders, two characters of regex).

    File-level docstring citing Phase 80 / BUG-10 / D-09 / D-12; reference memory `feedback_gstreamer_mock_blind_spot.md` as the canonical project precedent for source-level grep gates; explain that the runtime drift-guard inside `db_connect()` (Plan 80-01) covers the case where the PRAGMA SET line is silently dropped, while this static gate covers the case where a NEW production file opens its own `sqlite3.connect(...)` and never touches `db_connect`.

    Define a module-scope fixture `production_callsites() -> dict[Path, list[int]]`:
    - Decorator: `@pytest.fixture(scope="module")`.
    - Body: iterate `_MUSICSTREAMER_PKG.rglob("*.py")`. For each `.py` file, read it as UTF-8, split into lines, run `_PATTERN.search` on each line, collect matching line numbers (1-based). Skip files with no matches.
    - Return: `dict[Path, list[int]]` where keys are absolute file paths and values are lists of 1-based line numbers.
    - Assert (inside the fixture, before return): `_MUSICSTREAMER_PKG.is_dir()` so a missing production tree fails loudly instead of silently producing an empty dict (mirrors `tests/test_packaging_spec.py`'s `assert _SPEC.is_file()`).

    Test 1: `test_only_one_sqlite_connect_callsite_in_production(production_callsites)`:
    - Compute `total = sum(len(v) for v in production_callsites.values())`.
    - Assert `total == 1` with a grep-friendly failure message: report the total count and the full `{file: [line numbers]}` mapping. Cite Phase 80 / BUG-10 / D-09 / D-12 in the assertion message. Reference the runtime drift-guard at `repo.py` and the WARN string `PRAGMA foreign_keys is OFF after SET — drift detected` so a maintainer reading the CI failure understands the full invariant.

    Test 2: `test_sole_sqlite_connect_callsite_lives_in_repo_py(production_callsites)`:
    - `files = list(production_callsites.keys())`.
    - First assert `len(files) == 1` (defensive — covered by test 1 too, but makes the second assertion safe to index).
    - Then assert `files[0].name == "repo.py"` with a grep-friendly failure message: report the actual file path and instruct the maintainer to either move the callsite into `db_connect()` inside `repo.py` or refactor the new code to call `db_connect()` instead. Cite the canonical-refs note from CONTEXT.md `<canonical_refs>`.

    Do NOT add a third test that asserts the callsite is INSIDE the `db_connect` function body (CONTEXT does not require this; line-range detection is over-engineered for the current scope). The two tests above are sufficient.

    Do NOT use `git grep` subprocess (RESEARCH knowledge gap #3 recommendation — pure Python avoids the git dependency in the test env).

    Do NOT add a `# noqa: db-connect-bypass` allow-list mechanism (CONTEXT D-12 explicitly rejected this — tests/ is already excluded by scope; production code has zero legitimate reason to bypass).

    Verify after writing: `uv run pytest tests/test_db_connect_is_sole_connection_factory.py -x` exits 0. The gate's expected output is the single line at `musicstreamer/repo.py` inside `db_connect()` — Plan 80-01 did NOT add any new `sqlite3.connect(` callsites, so the gate should be GREEN immediately.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db_connect_is_sole_connection_factory.py -x</automated>
    <automated>uv run pytest tests/test_db_connect_is_sole_connection_factory.py::test_only_one_sqlite_connect_callsite_in_production -x</automated>
    <automated>uv run pytest tests/test_db_connect_is_sole_connection_factory.py::test_sole_sqlite_connect_callsite_lives_in_repo_py -x</automated>
    <source-grep>grep -c "^def test_" tests/test_db_connect_is_sole_connection_factory.py | grep -q "^2$"</source-grep>
    <source-grep>grep -c "sqlite3(\\\\.dbapi2)?\\\\.connect" tests/test_db_connect_is_sole_connection_factory.py | grep -q "^[1-9]"</source-grep>
    <source-grep>grep -c "rglob" tests/test_db_connect_is_sole_connection_factory.py | grep -q "^[1-9]"</source-grep>
    <inventory-check>python -c "import re,pathlib; pkg=pathlib.Path('musicstreamer'); pat=re.compile(r'sqlite3(\\.dbapi2)?\\.connect\\('); total=sum(1 for f in pkg.rglob('*.py') for line in f.read_text(encoding='utf-8').splitlines() if pat.search(line)); assert total == 1, f'expected 1 production hit, found {total}'"</inventory-check>
  </verify>
  <acceptance_criteria>
    - `tests/test_db_connect_is_sole_connection_factory.py` exists and contains exactly 2 functions starting with `def test_` (grep matches exactly 2).
    - Both tests exit 0 individually and the omnibus file run exits 0.
    - The file imports `re`, `pathlib.Path`, and `pytest` — `grep -c "^import re$" tests/test_db_connect_is_sole_connection_factory.py >= 1` and similarly for `from pathlib import Path` and `import pytest`.
    - The regex pattern in the file matches the dbapi2 alternative spelling — `grep -c "dbapi2" tests/test_db_connect_is_sole_connection_factory.py >= 1`.
    - The file does NOT shell out to `git grep` — `grep -c "git grep" tests/test_db_connect_is_sole_connection_factory.py` outputs `0`.
    - The file does NOT include any `# noqa: db-connect-bypass` allow-list — `grep -c "noqa: db-connect-bypass" tests/test_db_connect_is_sole_connection_factory.py` outputs `0`.
    - Inventory invariant: the standalone Python one-liner counts exactly 1 production hit. If a future commit adds a second hit anywhere in `musicstreamer/`, the gate fails loudly.
    - Pre-existing tests still pass: `uv run pytest tests/` (full suite — though this plan only needs to confirm the new file passes; full-suite gate is in Plan 80-03's wider regression net and the phase-completion gate).
  </acceptance_criteria>
  <done>
    `tests/test_db_connect_is_sole_connection_factory.py` exists with 2 green source-grep tests; the gate matches both `sqlite3.connect(` and `sqlite3.dbapi2.connect(`; scope is `musicstreamer/` only; the sole legal production callsite remains the line inside `db_connect()` in `musicstreamer/repo.py`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production tree (`musicstreamer/`) | The gate enforces: every connection-opening callsite within this boundary MUST go through `db_connect()` so the PRAGMA + drift-guard chain is unbroken. |
| Test tree (`tests/`) | Intentionally outside the gate — bypasses here are legal (D-12) because the negative-proof test in Plan 80-03 requires raw `sqlite3.connect(":memory:")`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-11 | Tampering | Future commit adds `sqlite3.connect(` to a new production file | mitigate | This gate fails loudly with a grep-friendly message naming the offending file + line. CI catches the bypass before it merges. Complementary to the runtime drift-guard in Plan 80-01 (which catches the orthogonal failure mode of "PRAGMA SET line removed from existing factory"). |
| T-80-12 | Tampering | Bypass via alternative `sqlite3.dbapi2.connect` spelling | mitigate | Regex matches both spellings (`sqlite3(\.dbapi2)?\.connect\(`). RESEARCH knowledge gap #4 — belt-and-suspenders, two characters of regex; zero false-positive risk in this codebase. |
| T-80-13 | Tampering | Bypass via `# noqa: db-connect-bypass` annotation | mitigate | No allow-list mechanism exists. CONTEXT D-12 rejected it explicitly; the gate has no escape hatch in production scope. |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs zero new packages. Pure stdlib (`re`, `pathlib`) + pytest already present. |
</threat_model>

<verification>
- `uv run pytest tests/test_db_connect_is_sole_connection_factory.py -x` exits 0.
- Standalone Python inventory check confirms the production tree has exactly one `sqlite3.connect(` callsite.
- The file does not use `git grep` subprocess; pure-Python `pathlib.rglob` walk only.
- The regex matches the `dbapi2` alternative spelling.
- The gate does NOT touch `tests/` — running `python -c "import re,pathlib; pat=re.compile(r'sqlite3(\\.dbapi2)?\\.connect\\('); print(sum(1 for f in pathlib.Path('tests').rglob('*.py') for line in f.read_text(encoding='utf-8').splitlines() if pat.search(line)))"` outputs a number > 0 (RESEARCH inventory says 6 hits in tests/), and the gate ignores all of them.
</verification>

<success_criteria>
- Two green tests asserting the sole-factory invariant: count==1 in production, and the file is `repo.py`.
- `tests/` tree explicitly excluded (D-12).
- Pure-Python implementation; no `git grep` subprocess dependency.
- Failure messages are grep-friendly and cite Phase 80 / BUG-10 / D-09 / D-12.
- The `dbapi2` alternative spelling is covered.
</success_criteria>

<output>
Create `.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-04-SUMMARY.md` when done.
</output>
