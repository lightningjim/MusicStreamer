---
phase: 80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe
plan: 02
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - musicstreamer/__main__.py
autonomous: true
requirements: [BUG-10]
requirements_addressed: [BUG-10]

must_haves:
  truths:
    - "`sweep_orphans(con)` runs at app start on the same `con` produced by `db_connect()`, immediately after `db_init(con)` and BEFORE `repo = Repo(con)`."
    - "The `musicstreamer.repo` logger is escalated to INFO in `__main__.main` so the sweep_orphans INFO line and the drift-guard WARN line reach the user's log."
    - "Global root logger stays at WARNING — per-logger escalation pattern (Phase 62 D-NN precedent) is preserved."
    - "D-02: `sweep_orphans(con)` is called from `__main__._run_gui` immediately after `db_init(con)`, on the SAME `con` object — no second connection is opened."
    - "D-03: `sweep_orphans(con)` runs every app start, unconditionally (no `user_version` gate, no settings flag, no first-run-only marker)."
  artifacts:
    - path: musicstreamer/__main__.py
      provides: "Wiring of sweep_orphans into _run_gui startup sequence + per-logger INFO escalation for musicstreamer.repo"
      contains: "sweep_orphans(con)"
    - path: musicstreamer/__main__.py
      provides: "Per-logger INFO escalation for musicstreamer.repo"
      contains: "musicstreamer.repo"
  key_links:
    - from: "musicstreamer/__main__.py::_run_gui"
      to: "musicstreamer/repo.py::sweep_orphans"
      via: "import + call site between db_init(con) and repo = Repo(con)"
      pattern: "sweep_orphans\\(con\\)"
    - from: "musicstreamer/__main__.py::main"
      to: "musicstreamer.repo logger"
      via: "logging.getLogger(...).setLevel(logging.INFO)"
      pattern: "musicstreamer.repo"
---

<objective>
Wire Plan 80-01's surface into the application entry. Two tiny edits in `musicstreamer/__main__.py`:
1. In `_run_gui`, extend the existing `from musicstreamer.repo import ...` to include `sweep_orphans`, and insert a single `sweep_orphans(con)` call immediately after `db_init(con)` and BEFORE `repo = Repo(con)` (same `con` object — no new connection per D-02).
2. In `main`, add `logging.getLogger("musicstreamer.repo").setLevel(logging.INFO)` to the existing per-logger INFO escalation block so the sweep_orphans INFO line and the drift-guard WARN line surface at default verbosity without bumping the global level (Phase 62 D-NN precedent).

Purpose: Without these two lines, Plan 80-01's `sweep_orphans` is dead code at startup and its INFO line never reaches the user's log. Both edits are mechanical and follow established in-file precedents (Phase 62 player, Phase 79 yt_import).

Output: `musicstreamer/__main__.py` calls `sweep_orphans(con)` on every GUI startup; the `musicstreamer.repo` logger is escalated alongside `musicstreamer.player`, `musicstreamer.soma_import`, and `musicstreamer.yt_import`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-01-SUMMARY.md
@musicstreamer/__main__.py
@musicstreamer/repo.py

<interfaces>
<!-- Key existing __main__.py shape extracted from current source. -->

From musicstreamer/__main__.py:194 (import line — extend to add sweep_orphans):
```python
from musicstreamer.repo import Repo, db_connect, db_init
```

From musicstreamer/__main__.py:208-210 (insertion point — `sweep_orphans(con)` goes BETWEEN db_init and Repo):
```python
con = db_connect()
db_init(con)
repo = Repo(con)
```

From musicstreamer/__main__.py:241-250 (per-logger INFO escalation block — add one more line at the end):
```python
def main(argv: list[str] | None = None) -> int:
    logging.basicConfig(level=logging.WARNING)
    logging.getLogger("musicstreamer.player").setLevel(logging.INFO)
    logging.getLogger("musicstreamer.soma_import").setLevel(logging.INFO)
    logging.getLogger("musicstreamer.yt_import").setLevel(logging.INFO)
```

Pitfall reminder (RESEARCH Pitfall 5 / Landmine 3): Do NOT relocate the `db_connect`/`db_init`/`repo` block — the existing Phase 66 / THEME-01 comment block at lines 202-207 documents why these three lines were hoisted to here from later in the function. Insert ONE new line between two existing lines; do not rewrite the surrounding block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire sweep_orphans(con) into _run_gui startup sequence</name>
  <files>musicstreamer/__main__.py</files>
  <read_first>
    - musicstreamer/__main__.py (verify current shape; especially line 194 import block and the line range 208-212 for the db lifecycle and theme-apply sequence — the Phase 66 hoist comment block at 202-207 documents why these lines live here and MUST NOT be reordered)
    - musicstreamer/repo.py (confirm Plan 80-01 already landed `sweep_orphans` — `grep -c "^def sweep_orphans" musicstreamer/repo.py` must equal 1; this task imports that symbol)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md (D-02 — sweep called from `_run_gui` immediately after `db_init(con)`, on the same connection)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (Analog 1 in the `__main__.py` section — exact insertion site between `db_init(con)` and `repo = Repo(con)`)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-01-SUMMARY.md (verify Plan 01 landed all five new symbols in repo.py)
  </read_first>
  <behavior>
    - After this task, every GUI startup of MusicStreamer calls `sweep_orphans(con)` exactly once on the connection returned by `db_connect()`, immediately after `db_init(con)` and before `repo = Repo(con)`.
    - The same `con` object is reused across `db_connect()`, `db_init(con)`, `sweep_orphans(con)`, and `Repo(con)` — no new SQLite connections opened (D-02 invariant).
    - The Phase 66 / THEME-01 ordering at lines 202-212 of `__main__.py` is preserved: `db_connect` → `db_init` → `sweep_orphans` (NEW) → `Repo` → `theme.apply_theme_palette`. The Phase 66 comment block at lines 202-207 is left untouched.
    - On a steady-state DB with no orphans, the call is a sub-millisecond no-op and emits no log line (D-04).
    - On a DB with orphans (Phase 74 F-07-03 Synphaera-shape failure), the call emits one INFO line `sweep_orphans: station_streams=N station_siblings=M` and the orphan rows are gone post-startup.
  </behavior>
  <action>
    Edit the import line at `musicstreamer/__main__.py:194` to add `sweep_orphans` to the existing `from musicstreamer.repo import ...` tuple. New form: `from musicstreamer.repo import Repo, db_connect, db_init, sweep_orphans`. Preserve alphabetical / existing ordering convention if any; simplest correct form is appending `, sweep_orphans` at the end.

    Insert a single new line `sweep_orphans(con)` between the existing `db_init(con)` line (currently 209) and the existing `repo = Repo(con)` line (currently 210). Add an inline trailing comment in the established Phase-X-rationale style: `# Phase 80 / BUG-10 D-02: heal orphans left by manual sqlite3-shell DELETEs.` (or split to a leading comment line — match the file's local style at the insertion site).

    Do NOT relocate, reorder, or rewrite the surrounding lines. The Phase 66 hoist comment block at lines 202-207 stays verbatim. The `theme.apply_theme_palette(app, repo)` call at line 212 stays verbatim. Only ONE new line is added between two existing lines; the import line gets ONE new symbol added.

    Do NOT add the per-logger INFO escalation in this task — that lands in Task 2 of this same plan (separate function, separate concern).

    After the edit, smoke-test that the module still imports cleanly: `python -c "from musicstreamer import __main__"` should exit 0.
  </action>
  <verify>
    <automated>uv run pytest tests/ -k "main_window or run_gui or __main__" -x --no-header --tb=short</automated>
    <source-grep>grep -c "from musicstreamer.repo import .*sweep_orphans" musicstreamer/__main__.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "^    sweep_orphans(con)" musicstreamer/__main__.py | grep -q "^1$"</source-grep>
    <ordering-check>python -c "import re,pathlib; s=pathlib.Path('musicstreamer/__main__.py').read_text(); i_init=s.find('db_init(con)'); i_sweep=s.find('sweep_orphans(con)'); i_repo=s.find('repo = Repo(con)'); assert 0 < i_init < i_sweep < i_repo, f'ordering wrong: init={i_init} sweep={i_sweep} repo={i_repo}'"</ordering-check>
    <import-check>python -c "from musicstreamer import __main__; from musicstreamer.repo import sweep_orphans; assert callable(sweep_orphans)"</import-check>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/__main__.py` import line (around line 194) contains `sweep_orphans` in the `from musicstreamer.repo import ...` tuple (grep matches at least once).
    - `musicstreamer/__main__.py` contains exactly one occurrence of `sweep_orphans(con)` indented at function-body level (4-space indent) — this is the call site inside `_run_gui`.
    - File-position ordering: `db_init(con)` appears BEFORE `sweep_orphans(con)` which appears BEFORE `repo = Repo(con)` in the source (the Python `find` ordering check above asserts this exactly).
    - `python -c "from musicstreamer import __main__"` exits 0 — module imports without error.
    - Any pre-existing tests matching `main_window`, `run_gui`, or `__main__` continue to pass.
    - The Phase 66 / THEME-01 comment block at the original position (around lines 202-207) is unchanged — `grep -c "Phase 66 / THEME-01: theme palette FIRST" musicstreamer/__main__.py` still equals 1.
  </acceptance_criteria>
  <done>
    `_run_gui` imports `sweep_orphans` and calls it on `con` between `db_init` and `Repo` construction; existing ordering and Phase 66 comment block are untouched; module imports cleanly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Escalate musicstreamer.repo logger to INFO in main()</name>
  <files>musicstreamer/__main__.py</files>
  <read_first>
    - musicstreamer/__main__.py (lines 241-250 — the existing per-logger INFO escalation block; add one more line matching this exact shape)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md (Logger config section in `<domain>` — Phase 80 adds `musicstreamer.repo` next to existing `musicstreamer.player` / `musicstreamer.soma_import` lines; global level stays WARN; per Phase 62 D-09 precedent the INFO line never reaches the user log without this escalation)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (Analog 2 in the `__main__.py` section — per-logger INFO escalation pattern with Phase-X-rationale comment style)
  </read_first>
  <behavior>
    - After this task, `__main__.main()` adds `musicstreamer.repo` to the list of module loggers escalated to `logging.INFO` (alongside `musicstreamer.player`, `musicstreamer.soma_import`, `musicstreamer.yt_import`).
    - The global root logger remains at `logging.WARNING` — only specific MusicStreamer module loggers are surfaced at INFO (Phase 62 D-NN precedent — avoids surfacing `gi`, `PySide6`, `urllib3` chatter at INFO).
    - At default verbosity, the `sweep_orphans` INFO line (when N>0) reaches stderr; the drift-guard WARN line (always, at WARN) also reaches stderr.
    - On a steady-state startup with no orphans, no INFO line is emitted (D-04 silent on N=0) so the escalation produces no new noise.
  </behavior>
  <action>
    In `musicstreamer/__main__.py::main` (currently lines 241-250), append one new line to the per-logger INFO escalation block, AFTER the existing `logging.getLogger("musicstreamer.yt_import").setLevel(logging.INFO)` line. The new line is `logging.getLogger("musicstreamer.repo").setLevel(logging.INFO)`.

    Add a one- or two-line Phase-X-rationale comment ABOVE the new line in the style established by the surrounding lines. Example shape: `# Phase 80 / BUG-10: surface sweep_orphans INFO line + PRAGMA drift WARN line without bumping the global level. sweep_orphans is silent on N=0 (D-04) so steady-state output is unchanged.` Match the wrap and comment depth of the existing Phase-62 and Phase-79 comment blocks at lines 243-250.

    Do NOT change `logging.basicConfig(level=logging.WARNING)` at line 242 — global level stays WARN. Do NOT add a new escalation for any logger other than `musicstreamer.repo`.

    Do NOT modify `_run_gui` in this task — Task 1 handled that. Do NOT create or modify any test file — Plans 80-03 and 80-04 own the tests.
  </action>
  <verify>
    <automated>uv run pytest tests/ -k "logger or logging or main_window" -x --no-header --tb=short</automated>
    <source-grep>grep -c 'logging.getLogger("musicstreamer.repo").setLevel(logging.INFO)' musicstreamer/__main__.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c 'logging.basicConfig(level=logging.WARNING)' musicstreamer/__main__.py | grep -q "^1$"</source-grep>
    <ordering-check>python -c "import pathlib; s=pathlib.Path('musicstreamer/__main__.py').read_text(); i_player=s.find('logging.getLogger(\"musicstreamer.player\")'); i_repo=s.find('logging.getLogger(\"musicstreamer.repo\")'); assert 0 < i_player < i_repo, f'repo escalation must be after player escalation: player={i_player} repo={i_repo}'"</ordering-check>
    <runtime-check>python -c "import logging; from musicstreamer import __main__ as m; m.main.__code__; assert 'musicstreamer.repo' in pathlib.Path('musicstreamer/__main__.py').read_text()" || python -c "import pathlib; assert 'musicstreamer.repo' in pathlib.Path('musicstreamer/__main__.py').read_text()"</runtime-check>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/__main__.py` contains the literal string `logging.getLogger("musicstreamer.repo").setLevel(logging.INFO)` exactly once.
    - The new escalation line appears AFTER `logging.getLogger("musicstreamer.player").setLevel(logging.INFO)` in source order (the Python ordering check above asserts this).
    - `logging.basicConfig(level=logging.WARNING)` is still present exactly once — global level is unchanged.
    - `python -c "from musicstreamer import __main__"` exits 0 — module imports cleanly.
    - Tests touching logging or main-window startup continue to pass.
  </acceptance_criteria>
  <done>
    `musicstreamer.repo` logger is escalated to INFO inside `main()` alongside the three existing per-logger escalations; global level remains WARN; no other behavior changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `__main__._run_gui` startup sequence | All edits happen inside the trust zone of the single MusicStreamer process; no external input crosses here. |
| Log surface (stderr at default verbosity) | Per-logger INFO escalation widens what reaches stderr; format args are integer rowcounts and a hard-coded WARN string from Plan 80-01 — no user-controlled data flows into the new log lines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-06 | Tampering | `_run_gui` startup ordering | mitigate | Insert exactly one line between two existing lines; do NOT relocate the surrounding block. The Phase 66 / THEME-01 comment at lines 202-207 documents the load-bearing ordering of `db_connect` → `db_init` → `Repo` → `theme.apply_theme_palette` (the new `sweep_orphans` slot is between `db_init` and `Repo` per CONTEXT D-02). The Python ordering source-check in the acceptance criteria asserts the insertion is in the correct slot. |
| T-80-07 | Information disclosure (log volume) | Per-logger INFO escalation | accept | `sweep_orphans` is silent on N=0 (D-04) so steady-state log volume is unchanged. The drift-guard WARN line is throttled to once-per-session by `_pragma_drift_logged` (Plan 80-01). Risk is bounded and observable. |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs zero new packages. |
</threat_model>

<verification>
After both tasks land:
- `python -c "from musicstreamer import __main__"` exits 0.
- `grep -c "sweep_orphans" musicstreamer/__main__.py` >= 2 (one in the import tuple, one at the call site).
- `grep -c 'logging.getLogger("musicstreamer.repo")' musicstreamer/__main__.py` equals 1.
- Source-ordering check: `db_init(con)` precedes `sweep_orphans(con)` precedes `repo = Repo(con)` in `__main__.py`.
- Any test touching `main_window`, `__main__`, or logging continues to pass.
- Manual smoke (developer runs `uv run musicstreamer`): app launches as before; no new noise at default verbosity unless N>0 orphans exist.
</verification>

<success_criteria>
- `musicstreamer/__main__.py` calls `sweep_orphans(con)` on every GUI startup, exactly once, on the same `con` as `db_init`.
- `musicstreamer.repo` logger is escalated to INFO alongside the three existing per-logger escalations.
- Phase 66 / THEME-01 comment block and ordering are preserved verbatim.
- Global root logger level stays WARN; no new chatter at default verbosity.
</success_criteria>

<output>
Create `.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-02-SUMMARY.md` when done.
</output>
