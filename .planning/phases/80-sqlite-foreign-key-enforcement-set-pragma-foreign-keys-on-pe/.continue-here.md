---
context: phase
phase: 80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe
workflow: /gsd-discuss-phase 80 --chain
status: in_progress
progress: 2 of 4 gray areas resolved
last_updated: 2026-05-18T04:22:53.354Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assume db_connect() is missing the PRAGMA without checking | Phase 80's ROADMAP goal reads as if PRAGMA needs to be added. It does NOT — `repo.py:18-21` already sets `PRAGMA foreign_keys = ON` on every connection, since the original Phase 01-01 commit `59fc516`. The Phase 74 F-07-03 orphans came from a manual sqlite3-shell DELETE that bypassed db_connect, not a code bug. | advisory | Already verified this session via `git log -L 17,21:musicstreamer/repo.py`. Resuming agent should treat (1) as "lock in via regression test + docstring," not "add the PRAGMA." |
| Sweep favorites as a "soft orphan" | The ROADMAP mentions "favorites" alongside the FK child tables, but favorites has NO FK to stations — it uses TEXT station_name and intentionally persists track titles after a station is deleted (v1.3 FAVES-01..04). Sweeping would silently change documented v1.3 behavior. | advisory | Decision already locked: tables swept = station_streams + station_siblings ONLY. Favorites stays out. |

<current_state>
Mid `/gsd-discuss-phase 80 --chain`. Two of four gray areas fully resolved with 7 sub-decisions locked. Area 3 "Drift-guard mechanism" was announced; no question has been asked yet. Resume should immediately ask the first Area-3 question. After Area 3 + Area 4 + final wrap, the workflow writes CONTEXT.md and DISCUSSION-LOG.md, deletes the checkpoint, commits, and auto-advances to `/gsd-plan-phase 80 --auto` via the chain.md flow.

Checkpoint file is on disk at `.planning/phases/80-.../80-DISCUSS-CHECKPOINT.json` (mirrors the in-memory accumulator).
</current_state>

<completed_work>
- Loaded phase context: PROJECT.md, ROADMAP Phase 80 entry, REQUIREMENTS BUG-10, STATE.md, prior CONTEXT.md (Phase 79)
- Inspected `musicstreamer/repo.py` (db_connect, db_init, FK schemas, delete_station call sites)
- Confirmed ground-truth: PRAGMA already set in db_connect since Phase 01-01
- Confirmed FK child tables at risk: `station_streams`, `station_siblings` only
- Presented 4 gray areas — all selected by user
- **Area 1 (Orphan-sweep policy + location) — RESOLVED:**
  - Sweep action: delete orphans + INFO-log per-table counts when N>0
  - Sweep home: separate `sweep_orphans(con)` function called from `__main__._run_gui` after `db_init()`
  - Run cadence: every app start (no user_version gate, no settings flag)
  - N=0 logging: silent (INFO line only when N>0)
- **Area 2 (Which tables to sweep) — RESOLVED:**
  - Tables swept: `station_streams` + `station_siblings` ONLY (favorites excluded — no FK, v1.3 FAVES behavior; provider FK excluded — SET NULL, not CASCADE)
  - station_siblings strategy: single DELETE handles both a_id and b_id atomically
  - Provider FK orphans: out of scope
</completed_work>

<remaining_work>
- **Area 3 (Drift-guard mechanism) — NOT STARTED.** Ask ~3-4 questions:
  - Q1 (recommended): Mechanism — once-per-session log inside db_connect() if PRAGMA==OFF, OR a source-grep test that bans `sqlite3.connect(...)` outside `repo.db_connect()` (mirrors `tests/test_packaging_spec.py` shape per `feedback_gstreamer_mock_blind_spot.md` memory), OR both. Lead with "both" as the recommended option.
  - Q2: Log level if PRAGMA==OFF — INFO, WARN, or ERROR? (Recommend WARN — "drift detected" deserves more than INFO but isn't fatal.)
  - Q3: Log throttle — once-per-session vs once-per-connection?
  - Q4: Source-grep gate scope — ban `sqlite3.connect` everywhere except `musicstreamer/repo.py:db_connect`, OR also allow it in `tests/` (production-only ban)?
- **Area 4 (Regression test surface) — NOT STARTED.** Ask ~3-4 questions:
  - Q1: Minimum test (insert station+stream, Repo.delete_station, assert streams gone) — yes/required.
  - Q2: Also test station_siblings cascade for both a_id and b_id? (Recommend yes — symmetry coverage.)
  - Q3: Negative test that raw `sqlite3.connect()` WITHOUT PRAGMA leaks orphans (proves the PRAGMA is what matters)? (Recommend yes — locks intent into the test name.)
  - Q4: Docstring placement — top-of-module in repo.py, on db_connect() function, or both?
- Final "any more gray areas?" wrap (default mode terminal step)
- Write `80-CONTEXT.md` (lazy-load `workflows/discuss-phase/templates/context.md`)
- Write `80-DISCUSSION-LOG.md` (lazy-load `workflows/discuss-phase/templates/discussion-log.md`)
- Delete `80-DISCUSS-CHECKPOINT.json` after CONTEXT.md write
- Commit `docs(80): capture phase context`
- Update STATE.md via `state.record-session`
- **Auto-advance:** `--chain` triggers `Skill(gsd-plan-phase, '80 --auto')` per `workflows/discuss-phase/modes/chain.md` step 5
</remaining_work>

<decisions_made>
- **GROUND-TRUTH:** `db_connect()` already sets `PRAGMA foreign_keys = ON` (repo.py:18-21, since Phase 01-01 commit `59fc516`). Phase 80 is mostly: lock in existing PRAGMA via regression test + docstring, clean up pre-existing orphans (Synphaera-style), drift-guard future bypasses.
- **FK surface:** Only `station_streams` and `station_siblings` are real cascade-leak tables. Favorites has no FK (text-based soft reference, v1.3 FAVES design). `stations.provider_id` is SET NULL not CASCADE.
- **Sweep:** Delete + INFO-log per-table counts when N>0; silent on N=0; lives in a separate `sweep_orphans(con)` function called from `__main__._run_gui` after `db_init()`; runs every app start (sub-millisecond when N=0).
- **station_siblings sweep:** single DELETE with `a_id NOT IN (...) OR b_id NOT IN (...)` — one atomic statement, one log line.
- **Out of scope:** favorites soft-sweep, provider FK orphans, generic PRAGMA foreign_key_list introspection, user_version gating, settings flag.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state for `/gsd-resume-work`
2. `.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-DISCUSS-CHECKPOINT.json` — locked decisions from Areas 1 & 2
3. `.planning/ROADMAP.md` (Phase 80 entry) — BUG-10 deliverables (5 sub-items)
4. `.planning/REQUIREMENTS.md` (BUG-10 row) — full requirement text
5. `musicstreamer/repo.py:1-205` — db_connect + db_init + FK schemas (already inspected this session)
6. `.planning/phases/79-.../79-CONTEXT.md` — shape parity for the CONTEXT.md to be written
7. `~/.claude/get-shit-done/workflows/discuss-phase.md` — parent workflow (default mode + chain mode lazy-loaded)

## Critical Anti-Patterns (do NOT repeat these)

- **Do not "add" PRAGMA foreign_keys = ON to db_connect()** — it's already there since the original Phase 01-01 commit. Treat (1) in the ROADMAP as "lock in via regression test + docstring," not "add it."
- **Do not sweep favorites** — no FK; v1.3 FAVES-01..04 intentionally persists track titles after station delete. Sweeping would silently change documented behavior.
- **Do not gate sweep_orphans() by user_version** — once bumped, future drift would leak silently forever. Run every app start; it's sub-millisecond when N=0.

## Infrastructure State

- Git: branch `main`, only `uv.lock` modified (pre-existing, not introduced this session)
- Phase directory created: `.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/`
- Checkpoint file: `80-DISCUSS-CHECKPOINT.json` (Areas 1 & 2 decisions persisted)
- No background processes
- No human actions pending

<context>
Working a /gsd-discuss-phase 80 --chain session. The phase looked routine from the ROADMAP text, but a quick `git log -L 17,21:musicstreamer/repo.py` revealed the PRAGMA was always there — the Phase 74 Synphaera orphans came from a manual sqlite3-shell DELETE, not the app. That changes the framing: the phase is mostly tightening (regression test, sweep for pre-existing orphans, drift-guard, docstring), not "add the PRAGMA."

Took the default discuss-mode path — one question at a time, ~4 questions per area, then "more or next?" wrap. User has been consistently picking the recommended option, all 7 sub-decisions so far align with the project's established patterns (focused tiny modules, log-only-when-interesting, self-healing-with-INFO-log, source-grep drift guards).

The next question to ask (Area 3, Q1) should lead with "both — once-per-session log inside db_connect() + source-grep test banning sqlite3.connect outside repo.db_connect" as the recommended option. This is the strongest match for the user's `feedback_gstreamer_mock_blind_spot.md` memory ("pipeline mocks pass through any pipeline.emit(...) call; add source-level grep gates").
</context>

<next_action>
Re-enter `/gsd-discuss-phase 80 --chain` workflow at Area 3 (Drift-guard mechanism). Ask the first question — recommend "both: once-per-session WARN log inside db_connect() if PRAGMA==OFF after the SET, AND a source-grep drift-guard test (`tests/test_db_connect_is_sole_connection_factory.py` or similar) that bans `sqlite3.connect(...)` outside `musicstreamer/repo.py`."
</next_action>
