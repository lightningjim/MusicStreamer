---
phase: 43-gstreamer-windows-spike
plan: 03
type: execute
wave: 3
depends_on: ["43-02"]
files_modified:
  - .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md
autonomous: false
requirements: [PKG-06]

must_haves:
  truths:
    - "43-SPIKE-FINDINGS.md exists with exact GStreamer version, DLL list, plugin list, gio/modules contents, env vars, and PATH/CWD gotchas (D-11)"
    - "Findings doc contains only redacted evidence — no raw listen keys or unredacted URLs"
    - "spike-findings-43 skill persisted to .claude/skills/ via /gsd-spike-wrap-up (D-13)"
    - "ROADMAP Phase 43 marked complete; Phase 44 + Phase 43.1 unblocked"
  artifacts:
    - path: ".planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md"
      provides: "Canonical Windows GStreamer bundling bill-of-materials Phase 44 copies as-is into its plan context"
      contains: "GStreamer 1.28.2"
      min_lines: 80
    - path: ".claude/skills/spike-findings-43-*/SKILL.md"
      provides: "Project-local skill auto-loaded in future Windows packaging conversations"
      contains: "GStreamer"
  key_links:
    - from: "43-02-SUMMARY.md + scratch paste files (plugin list, DLL dir)"
      to: "43-SPIKE-FINDINGS.md DLL + Plugin bill-of-materials tables"
      via: "Harvest the paste-back data; cross-reference with RESEARCH's expected-shape tables; produce the canonical version"
      pattern: "Bill-of-Materials"
    - from: "43-SPIKE-FINDINGS.md"
      to: ".claude/skills/spike-findings-43-*/SKILL.md"
      via: "/gsd-spike-wrap-up reads findings and emits the skill file"
      pattern: "spike-findings-43"
    - from: "43-SPIKE-FINDINGS.md"
      to: "Phase 44 PKG-01 plan context"
      via: "Phase 44 ROADMAP entry inherits this doc as first-class canonical ref"
      pattern: "Phase 44"
---

<objective>
Harvest the Wave 2 paste-back evidence into `43-SPIKE-FINDINGS.md` (D-11 deliverable) and persist a project-local skill via `/gsd-spike-wrap-up` (D-13). This is the phase's lasting value — Phase 44's installer plan reads this doc as its canonical reference, and Phase 43.1 (SMTC) depends on the "confirmed Windows runtime" this doc represents.

Purpose: The .spec/hook/smoke files are reference artifacts for Phase 44 to extend; the findings doc is the structured knowledge Phase 44 (PKG-01) and Phase 43.1 inherit without re-running the spike.

Output: A committed findings doc + a project-local skill. ROADMAP Phase 43 flips to complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Autonomous: false.** Requires one paste-back (`gst-inspect-1.0.exe --print-all` against the bundled registry + bundle `dir /b`) that Wave 2 should have already captured, plus the user running the `/gsd-spike-wrap-up` slash command at the end.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-gstreamer-windows-spike/43-CONTEXT.md
@.planning/phases/43-gstreamer-windows-spike/43-RESEARCH.md
@.planning/phases/43-gstreamer-windows-spike/43-VALIDATION.md
@.planning/phases/43-gstreamer-windows-spike/43-01-SUMMARY.md
@.planning/phases/43-gstreamer-windows-spike/43-02-SUMMARY.md
@.planning/phases/43-gstreamer-windows-spike/43-spike.spec
@.planning/phases/43-gstreamer-windows-spike/runtime_hook.py
@.planning/phases/35-backend-isolation/35-SPIKE-MPV.md

<interfaces>
<!-- 43-SPIKE-FINDINGS.md canonical structure per D-11 + precedent from 35-SPIKE-MPV.md. -->
<!-- Minimum sections (planner-enforced): -->

## Findings doc skeleton

```markdown
# Phase 43 — GStreamer Windows Spike Findings

**Status:** COMPLETE (or PARTIAL if Wave 2 escalated)
**Closed:** <date>
**Iterations to green:** N
**Audibility:** audible | silent-with-VM-note

## Canonical Pin

| Item | Version |
|------|---------|
| GStreamer MSVC Windows runtime | 1.28.2 |
| GStreamer MSVC Windows devel | _(bundled in 1.28.2 Complete installer — no separate package)_ |
| PyInstaller | <as-pinned> |
| pyinstaller-hooks-contrib | <as-pinned> |
| PyGObject | <resolves A7: 3.5x> |
| Python | <resolves VM question> |

## Required DLLs (bundle root `_internal/`)
<table from paste-back `dir /b` filtered to .dll, cross-ref RESEARCH §"DLL Bill-of-Materials" for Role column>

## Required Plugins (`_internal/gst_plugins/` — subdir name resolves A5)
<table from paste-back gst-inspect output, cross-ref RESEARCH §"Plugin Bill-of-Materials" for Elements column>

## GIO Modules (`_internal/gio/modules/`)
<table; libgiognutls.dll is the must-have per D-06>

## Required Environment Variables (runtime hook)
<table: name | value | set by | why>
Entries: GST_PLUGIN_PATH, GST_PLUGIN_SYSTEM_PATH, GST_REGISTRY, GST_REGISTRY_FORK (stock contrib rthook); GIO_EXTRA_MODULES, GI_TYPELIB_PATH, GST_PLUGIN_SCANNER (custom runtime_hook.py).

## PATH / CWD Gotchas Discovered
<bullet list from iteration log — any A5/A7/A10 resolutions, sink selection, VM audio config notes, subdir-naming surprises>

## .spec Delta From RESEARCH Skeleton
<list every edit made in Wave 2 iterations, with the SPIKE_FAIL reason that prompted it>

## Evidence
- Final `SPIKE_OK` line (redacted): `<line>`
- `SPIKE_DIAG ... has_default_database=True` (redacted)
- Iteration commit log: `git log --oneline 43-spike.spec runtime_hook.py`

## Unresolved / Deferred to Phase 44
<any RESEARCH failure mode not hit but still worth flagging; or Wave-2 escalation items; or Windows 10 note per D-02>

## Artifact References (for Phase 44)
- `43-spike.spec` — copy as starting point for PKG-01 production spec
- `runtime_hook.py` — unchanged template
- `smoke_test.py` — Phase 44 smoke suite extends this (HLS, yt-dlp, Twitch, cover-art, ICY — RESEARCH §"User Constraints" Deferred Ideas list)
```

## Redaction rule for findings doc
Every URL reproduced in this doc MUST be redacted at the query string boundary (`?<redacted>`). No raw `artifacts/smoke.log` content gets committed — only SPIKE_OK/DIAG lines filtered and individually verified for redaction. T-43-01 mitigation applies to the committed doc too, not just chat paste-back.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harvest 43-SPIKE-FINDINGS.md from paste-back evidence</name>
  <files>
    .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md
  </files>
  <read_first>
    43-02-SUMMARY.md — iteration history, final delta summary, pointer to scratch files with plugin + DLL pastes.
    43-RESEARCH.md §"DLL Bill-of-Materials" (lines ~782-815) — expected-shape template.
    43-RESEARCH.md §"Plugin Bill-of-Materials" (lines ~817-843) — expected-shape template.
    43-RESEARCH.md §"Assumptions Log" (lines ~991-1005) — items A5/A7/A10 MUST have resolutions noted in findings.
    43-RESEARCH.md §"Open Questions" (lines ~1008-1030) — all five need closure notes in findings.
    .planning/phases/35-backend-isolation/35-SPIKE-MPV.md — precedent structure for findings docs in this project.
    43-CONTEXT.md §D-11 — canonical deliverable spec.
  </read_first>
  <action>
    Create `43-SPIKE-FINDINGS.md` using the skeleton in `<interfaces>` above. Each section MUST be populated from Wave 2 paste-back evidence (captured in `43-02-SUMMARY.md` + scratch files), cross-referenced against RESEARCH's expected-shape tables.

    **Concrete harvesting steps:**

    1. **Canonical Pin table** — fill from the final green build's env. `GSTREAMER_ROOT` version comes from paste-back `gst-inspect-1.0.exe --version`. PyInstaller/hooks-contrib/PyGObject versions from build.log's pip install output (user either already pasted or pasted back on request). Python version from the first `python --version` line of build.log.

    2. **Required DLLs table** — the user's bundle `dir /b` output, filtered to `.dll` entries in `dist/spike/_internal/`. For each DLL, copy the Role column from RESEARCH §"DLL Bill-of-Materials" where the name matches. Flag any DLL that appears in the bundle but is NOT in the RESEARCH table (new data).

    3. **Required Plugins table** — rows from `gst-inspect-1.0.exe --print-all` output. One row per plugin `.dll` in `_internal/gst_plugins/` (or `gst-plugins/` — record the actual subdir name, which resolves A5). Cross-ref RESEARCH §"Plugin Bill-of-Materials" for Elements column.

    4. **GIO Modules table** — paste-back `dir dist\spike\_internal\gio\modules\`. `libgiognutls.dll` MUST be present — this is the SC-2 evidence.

    5. **Environment Variables table** — both stock contrib rthook (GST_PLUGIN_PATH, GST_PLUGIN_SYSTEM_PATH, GST_REGISTRY, GST_REGISTRY_FORK) AND the three custom rthook vars (GIO_EXTRA_MODULES, GI_TYPELIB_PATH, GST_PLUGIN_SCANNER). "Set by" column distinguishes stock vs custom.

    6. **PATH / CWD Gotchas** — bullets from 43-02-SUMMARY.md iteration log. At minimum:
       - A5 resolution: `gst_plugins` vs `gst-plugins` subdir (whichever the hook actually produced).
       - A7 resolution: `girepository-1.0` vs `girepository-2.0` (which PyGObject version resolved).
       - A10 resolution: VM audio device present/absent + whether `wasapi2sink` auto-selected or fallback needed.
       - Any "never run build from a shell with system GStreamer on PATH" reminder (RESEARCH anti-pattern).
       - Any registry-write-path note (onedir bundle vs Program Files).

    7. **.spec Delta** — walk `git log --oneline -- 43-spike.spec runtime_hook.py` for Wave 2 commits. One bullet per iteration: "Iter N: SPIKE_FAIL reason=X → added/changed Y in {.spec|runtime_hook}".

    8. **Evidence** — copy the FINAL iteration's `SPIKE_OK` line + `SPIKE_DIAG ... has_default_database=True` line from the paste. Apply redaction: any URL MUST be `https://<host>/<path>?<redacted>`. Also include the short git log of Wave 2 commits.

    9. **Unresolved / Deferred** — at minimum:
       - Windows 10 validation (D-02 — explicitly out of scope, flag for Phase 44 to decide).
       - HLS / yt-dlp / Twitch / cover-art / ICY-tag coverage (RESEARCH §"User Constraints" Deferred Ideas).
       - Anything Wave 2 escalated past (if the spike closed PARTIAL, enumerate each).
       - `gschemas.compiled` regeneration if bundle had an issue that required `glib-compile-schemas.exe` (RESEARCH §"Known Failure Modes" #3).
       - Any RESEARCH-open-question not conclusively resolved (the five in §"Open Questions").

    10. **Artifact References** — explicit paths Phase 44 copies. Call out that `43-spike.spec` is the STARTING point; Phase 44's canonical spec lives elsewhere per D-12.

    **Redaction gate:** Before saving, grep the draft for `?` in any URL context. Every hit MUST be followed by `<redacted>`. No AA listen key substrings (check for the user's known key if 43-02-SUMMARY exposes it — though it shouldn't).

    Implements: PKG-06 (per D-11). Consumed downstream by PKG-01 (Phase 44) and Phase 43.1.
    Traceability: closes 43-VALIDATION.md row 43-03-XX (findings doc exists).
  </action>
  <verify>
    <automated>
    test -f .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -q 'GStreamer 1.28' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -q 'libgiognutls' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -q 'GIO_EXTRA_MODULES' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -q 'GI_TYPELIB_PATH' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -q 'GST_PLUGIN_SCANNER' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -q 'Phase 44' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && grep -qE 'has_default_database\s*=\s*True' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md \
      && [ "$(wc -l < .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md)" -ge 80 ] \
      && ! grep -Ei 'listen[_-]?key=[A-Za-z0-9]{8,}' .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md
    </automated>
  </verify>
  <done>
    Findings doc exists (≥80 lines), has all mandatory sections populated from paste-back evidence, includes the libgiognutls + TLS-backend proof, references Phase 44 as the downstream consumer, and passes the redaction negative-grep (no raw listen-key substrings).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Persist spike-findings skill via /gsd-spike-wrap-up</name>
  <files>.claude/skills/spike-findings-43-*/SKILL.md</files>
  <read_first>
    D-13 in 43-CONTEXT.md — "Run `/gsd-spike-wrap-up` at phase close to persist findings as a project-local skill."
    43-SPIKE-FINDINGS.md (produced in Task 1) — input material for the skill.
  </read_first>
  <what-built>
    `43-SPIKE-FINDINGS.md` with the canonical GStreamer Windows bundling bill-of-materials, produced in Task 1.
  </what-built>
  <how-to-verify>
    **User runs `/gsd-spike-wrap-up` in a fresh Claude session** (or in this one if budget allows). The slash command reads `43-SPIKE-FINDINGS.md` and writes `.claude/skills/spike-findings-43-gstreamer-windows/SKILL.md` (or similar — the command picks the exact slug).

    After it runs:
    1. Confirm `ls .claude/skills/spike-findings-43-*/SKILL.md` finds a single file.
    2. Confirm the SKILL.md has the frontmatter + summary that future Windows packaging conversations will auto-load.
    3. Commit the new skill file with message `skill(phase-43): persist GStreamer Windows bundling findings`.

    Then update the ROADMAP:
    4. Flip `[ ] **Phase 43: GStreamer Windows Spike**` to `[x]` with a completion date.
    5. Fill in the placeholder plan count: `**Plans:** 3 plans` (matching this plan set).
    6. Add the plan list under Phase 43 pointing at `43-01-PLAN.md`, `43-02-PLAN.md`, `43-03-PLAN.md`, all checked.
    7. Commit as `docs(phase-43): mark spike complete + unblock phases 44 and 43.1`.
  </how-to-verify>
  <action>
    Execute the wrap-up protocol documented in `<how-to-verify>`: user runs `/gsd-spike-wrap-up` (ideally in a fresh session). The command reads `43-SPIKE-FINDINGS.md` and writes `.claude/skills/spike-findings-43-*/SKILL.md`. Claude then (1) confirms the skill file exists, (2) commits it as `skill(phase-43): persist GStreamer Windows bundling findings`, (3) flips ROADMAP Phase 43 entry to `[x]` with completion date + plan list (43-01/02/03 all checked), (4) commits the ROADMAP update as `docs(phase-43): mark spike complete + unblock phases 44 and 43.1`.
  </action>
  <verify>
    <automated>ls .claude/skills/spike-findings-43-*/SKILL.md 2>/dev/null | head -1 | grep -q SKILL.md && grep -Eq '\[x\] \*\*Phase 43:' .planning/ROADMAP.md</automated>
  </verify>
  <done>`.claude/skills/spike-findings-43-*/SKILL.md` exists and is committed; ROADMAP shows Phase 43 as complete with a date and the three plan files checked; Phase 44 + Phase 43.1 are documented as unblocked. OR (fallback per D-13) skill persistence failed — findings doc still committed, phase closes without the skill, documented as a follow-up.</done>
  <resume-signal>
    User replies with one of:
    - `wrap-up done` (and `ls` output confirming `.claude/skills/spike-findings-43-*/SKILL.md` exists)
    - `wrap-up failed — <reason>` (then diagnose; the skill is optional-nice per D-13, so non-blocking for Phase 43 close)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paste-back scratch → committed findings doc | Scratch files in `artifacts/` (gitignored) may contain raw smoke.log contents including unredacted URLs or error paths leaking paths with the listen key. The findings doc commits to git, so every line copied forward must be re-verified redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | `43-SPIKE-FINDINGS.md` + `.claude/skills/spike-findings-43-*/SKILL.md` | mitigate | Task 1 action explicitly filters paste-back evidence to only the SPIKE_OK/SPIKE_DIAG lines (which are already URL-redacted by `smoke_test.py`). Before save, grep enforces `?<redacted>` on all URL strings and a negative-grep rejects any `listen_key=[A-Za-z0-9]{8,}` substring (acceptance criterion). The skill file is derived from the findings doc, so redaction is transitive. Raw `artifacts/*.log` files stay in `artifacts/` (gitignored) and never get copy-pasted wholesale. |
</threat_model>

<verification>
- `43-SPIKE-FINDINGS.md` exists (≥ 80 lines) with all mandatory sections populated.
- `has_default_database=True` evidence present (SC-2 proof).
- `GStreamer 1.28.2` + the three custom env vars named.
- Phase 44 referenced as downstream consumer.
- Redaction negative-grep passes (no listen key substring in committed doc).
- `.claude/skills/spike-findings-43-*/SKILL.md` exists after user runs `/gsd-spike-wrap-up`.
- ROADMAP flipped to `[x] Phase 43` with completion date.
</verification>

<success_criteria>
Phase 43 closes green when:
1. `43-SPIKE-FINDINGS.md` is committed to git with redacted evidence matching Wave 2's green iteration.
2. `/gsd-spike-wrap-up` has persisted the project-local skill.
3. ROADMAP is updated to reflect completion; STATE.md shows Phase 43 done; Phase 44 + Phase 43.1 are unblocked.

Partial-close path (if Wave 2 escalated): findings doc is produced with `Status: PARTIAL`, documenting exactly where the spike stuck, and Phase 44 inherits the unresolved items as its first sub-task.
</success_criteria>

<output>
After completion, create `.planning/phases/43-gstreamer-windows-spike/43-03-SUMMARY.md` with: findings doc line count + section inventory, skill file path, ROADMAP commit SHA, confirmation that Phase 44 and 43.1 are now unblocked, any items carried forward into Phase 44 as deferred-from-spike.
</output>
