---
phase: 43-gstreamer-windows-spike
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - .planning/phases/43-gstreamer-windows-spike/43-spike.spec
  - .planning/phases/43-gstreamer-windows-spike/runtime_hook.py
  - .planning/phases/43-gstreamer-windows-spike/smoke_test.py
autonomous: false
requirements: [PKG-06]

must_haves:
  truths:
    - "User's clean Win11 VM runs build.ps1 successfully and produces artifacts/smoke.log"
    - "smoke.log shows SPIKE_OK audio_sample_received=True on the final iteration"
    - "smoke.log shows SPIKE_DIAG ... has_default_database=True (libgiognutls.dll loaded — SC-2 pass)"
    - "User confirms 'audible' (or 'silent' with SPIKE_OK, which still passes at the GStreamer layer per VALIDATION.md)"
    - "Iteration count ≤5; if 5th still fails, task escalates to scope revisit"
  artifacts:
    - path: ".planning/phases/43-gstreamer-windows-spike/43-spike.spec"
      provides: "Revised .spec reflecting any Tree()/binaries/hooksconfig fixes from paste-back diagnosis"
      contains: "Tree("
    - path: ".planning/phases/43-gstreamer-windows-spike/runtime_hook.py"
      provides: "Revised runtime_hook.py reflecting any env-var fixes (e.g., girepository-1.0 → 2.0 path if PyGObject 3.52+)"
      contains: "GIO_EXTRA_MODULES"
    - path: ".planning/phases/43-gstreamer-windows-spike/smoke_test.py"
      provides: "Revised smoke_test.py only if a pipeline-side bug surfaces (e.g., explicit audio-sink override per failure mode #8)"
      contains: "SPIKE_OK"
  key_links:
    - from: "User paste-back of artifacts/smoke.log"
      to: "Claude's fix diff on .spec / runtime_hook.py"
      via: "Grep SPIKE_FAIL reason=... against RESEARCH §Known Failure Modes row; apply targeted fix; commit; emit re-run instructions"
      pattern: "SPIKE_FAIL|SPIKE_OK"
    - from: "SPIKE_OK + libgiognutls loaded"
      to: "ROADMAP Phase 43 SC-1 + SC-2 pass"
      via: "Green iteration triggers Wave 3 (findings + wrap-up)"
      pattern: "has_default_database=True"
---

<objective>
Iterate on the `.spec` and `runtime_hook.py` based on the user's VM paste-back until the smoke test emits `SPIKE_OK` with `has_default_database=True` and the user confirms `audible` (or `silent` — still a pass at the GStreamer layer per 43-VALIDATION.md §"What 'Pass' Means for Audible Playback").

Purpose: Wave 1 landed the broad skeleton; Wave 2 closes the empirical gaps the research flagged as MEDIUM confidence — exact DLL names, typelib version (girepository-1.0 vs -2.0 per A7), gst-plugins vs gst_plugins subdir naming (A5), audio sink selection on the VM (A10), and any TLS/scanner/typelib misses not caught by the contrib hook.

Output: Green iteration log in chat history + committed spec/hook/smoke revisions. Sets up Wave 3 to harvest the findings doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Autonomous: false.** This plan requires the user to execute `build.ps1` on the Windows 11 VM and paste back `artifacts/smoke.log`. Claude cannot run PyInstaller on Linux.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-gstreamer-windows-spike/43-CONTEXT.md
@.planning/phases/43-gstreamer-windows-spike/43-RESEARCH.md
@.planning/phases/43-gstreamer-windows-spike/43-VALIDATION.md
@.planning/phases/43-gstreamer-windows-spike/43-01-SUMMARY.md
@.planning/phases/43-gstreamer-windows-spike/43-spike.spec
@.planning/phases/43-gstreamer-windows-spike/runtime_hook.py
@.planning/phases/43-gstreamer-windows-spike/smoke_test.py
@.planning/phases/43-gstreamer-windows-spike/build.ps1

<interfaces>
<!-- The key diagnostic table: grep SPIKE_FAIL reason=... → look up fix. -->
<!-- All ten rows live in 43-RESEARCH.md §"Known Failure Modes" (lines ~844-894). Do NOT re-derive — match the reason string, apply the fix. -->

SPIKE_FAIL marker grammar in smoke.log:
  - `SPIKE_FAIL step=<stage> error=<msg>` — setup failures (exit 1)
  - `SPIKE_FAIL step=pipeline errors=[...]` — runtime failures (exit 2)
  - `SPIKE_FAIL step=timeout warnings=[...]` — no audio in 8s (exit 3)

Known-failure → fix map (from RESEARCH §"Known Failure Modes"):
  #1 "TLS/SSL support not available" → verify `_internal/gio/modules/libgiognutls.dll` + `libgnutls-30.dll` at `_internal/` root; re-check GIO_EXTRA_MODULES path.
  #2 "plugin scanner helper not found" → confirm `_internal/gst-plugin-scanner.exe` + GST_PLUGIN_SCANNER env var.
  #3 "g_settings_schema_source_new_from_directory" → verify `share/glib-2.0/schemas/gschemas.compiled` in bundle; re-run `glib-compile-schemas.exe` if missing.
  #4 `no element "playbin3"` → check `_internal/gst_plugins/` vs `gst-plugins/` subdir name (A5 resolution on iter-1 paste). Fix Tree() prefix in .spec.
  #5 Registry write failure → onedir is writable; if still fails, re-check GST_REGISTRY path from stock rthook.
  #6 `import gi` ImportError → PyGObject MSVC ABI mismatch; confirm pip install not MSYS2.
  #7 Missing `Gst-1.0.typelib` → verify girepository version; adjust `typelib_tree` prefix in .spec (1.0 vs 2.0) + `GI_TYPELIB_PATH` in rthook (A7).
  #8 `wasapi2sink: Could not get the default audio device` → VM audio config. Fallback: explicit `autoaudiosink` override in smoke_test.py (this is the only justified smoke_test.py edit in Wave 2).
  #9 `g_type_init` deprecation spam → ignorable; add `GLIB_SILENT=1` in build.ps1 only if paste-back is getting crowded.
  #10 `Could not create resource` — `libsoup-3.0-0.dll` not found → add explicit `binaries=` entry in .spec for `bin/libsoup-3.0-0.dll`.

Exact DLL + plugin filename patterns surface on iteration 1 via `dir` paste; RESEARCH §"DLL Bill-of-Materials" + §"Plugin Bill-of-Materials" are the expected shape, not ground truth.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Iteration loop — paste-back + targeted fix</name>
  <files>
    .planning/phases/43-gstreamer-windows-spike/43-spike.spec
    .planning/phases/43-gstreamer-windows-spike/runtime_hook.py
    .planning/phases/43-gstreamer-windows-spike/smoke_test.py
  </files>
  <read_first>
    43-RESEARCH.md §"Known Failure Modes" (lines ~844-894) — the ten-row fix table.
    43-RESEARCH.md §"Assumptions Log" items A5 (gst-plugins subdir) and A7 (girepository version) — these resolve on iteration 1.
    43-VALIDATION.md §"Manual-Only Verifications" — the SC-2 `has_default_database=True` gate + user "audible/silent" word contract.
    43-CONTEXT.md §Specifics bullet 3: "Target ≤5 iterations before the spike is considered 'stuck' and needs a scope revisit."
  </read_first>
  <what-built>
    Four committed artifacts from Wave 1: `43-spike.spec`, `runtime_hook.py`, `smoke_test.py`, `build.ps1`. They compile but have not yet been exercised on a Windows VM — several assumptions (A5, A7, A10) resolve only through paste-back.
  </what-built>
  <how-to-verify>
    **Per-iteration protocol (max 5 iterations):**

    **User steps (each iteration):**
    1. Revert Win11 VM to the clean snapshot (no system GStreamer on PATH — D-01).
    2. `cd` into the phase directory on the VM (pull latest from git first).
    3. Run `.\build.ps1`.
    4. Paste the FULL content of `artifacts\smoke.log` back into the chat.
    5. Also paste one of the following:
       - `dir dist\spike\_internal\gio\modules` (to confirm libgiognutls.dll present)
       - `dir dist\spike\_internal | findstr girepository` (resolves A7 — 1.0 vs 2.0)
       - `dir dist\spike\_internal\gst_plugins` OR `dir dist\spike\_internal\gst-plugins` (resolves A5 — hyphen vs underscore)
    6. State one word: `audible` or `silent` (whether host speakers produced sound for ≥5s).

    **Claude's diagnosis step (after each paste):**
    1. Grep paste for `SPIKE_OK` or `SPIKE_FAIL`.
    2. If `SPIKE_OK audio_sample_received=True` AND `has_default_database=True`:
       - If user said `audible` → exit the loop, both SC-1 + SC-2 green. Proceed to post-loop step.
       - If user said `silent` → 43-VALIDATION.md §"What 'Pass' Means" says this still passes at the GStreamer layer. Record the VM-audio-config note for the findings doc, exit the loop.
    3. If `SPIKE_FAIL reason=<R>`:
       - Match `<R>` against the RESEARCH §"Known Failure Modes" table (10 rows in `<interfaces>` above).
       - Apply the SPECIFIC fix to `.spec` (most common) or `runtime_hook.py` (env var fix) or `smoke_test.py` (ONLY for failure #8 audio-sink override — otherwise leave smoke_test untouched).
       - Commit with message `spike(43-02): iter-NN fix <short reason>`.
       - Emit re-run instructions to the user for iteration N+1.
    4. If the paste shows a reason NOT in the RESEARCH table:
       - Diagnose directly from the GStreamer error text; if unclear, ask the user for `artifacts\build.log` too.
       - Log a new row in an `iteration-log.md` scratchpad for Wave 3's findings.

    **Post-loop step (after green iteration):**
    1. Ask the user to run: `& "C:\spike-gst\devel\1.0\msvc_x86_64\bin\gst-inspect-1.0.exe" --print-all 2>&1 | Out-File -Encoding UTF8 artifacts\gst-plugins.txt` (against the bundled registry: `$env:GST_REGISTRY = "dist\spike\_internal\registry.bin"` first).
    2. User pastes `artifacts\gst-plugins.txt` (plugin list) and `dir dist\spike\_internal /b` (DLL list). These feed Wave 3's findings tables.
    3. Claude commits the final green `.spec` + `runtime_hook.py` + any `smoke_test.py` changes, and leaves a `43-02-SUMMARY.md` stub with the iteration count and the DLL/plugin paste saved to a scratch file Wave 3 reads.

    **Escalation trigger:** If iteration 5 still emits `SPIKE_FAIL` for a reason not covered by RESEARCH's ten failure modes, STOP the loop. Summarize the iteration history and ask the user whether to (a) extend the budget, (b) scope down (e.g., swap AA HTTPS for a public ShoutCast URL per D-08 if the failure looks listen-key-auth-related), or (c) defer the unresolved issue to Phase 44 per D-14 and close the spike with partial findings.

    **What the user confirms verbally at the end:**
    - "Green iteration — audible" (or "silent"), OR
    - "Escalate — stuck on iteration 5"
  </how-to-verify>
  <action>
    Execute the iteration protocol documented in `<how-to-verify>`. Per iteration: user pastes `artifacts\smoke.log` + a targeted dir listing + audibility word; Claude greps SPIKE_OK/SPIKE_FAIL, matches failure reason against RESEARCH §"Known Failure Modes" (10 rows enumerated in `<interfaces>`), applies targeted fix to `.spec` / `runtime_hook.py` / `smoke_test.py`, commits as `spike(43-02): iter-NN fix <reason>`, emits re-run instructions. Exit on green (SPIKE_OK + has_default_database=True + audibility word). Escalate after iteration 5. **Preserve the `?<redacted>` line in `smoke_test.py` on every edit — T-43-01 mitigation, non-negotiable.** After green, collect post-loop plugin + DLL dumps for Wave 3.
  </action>
  <verify>
    <automated>grep -q '?&lt;redacted&gt;' .planning/phases/43-gstreamer-windows-spike/smoke_test.py</automated>
  </verify>
  <done>Green iteration achieved: final smoke.log paste contains `SPIKE_OK audio_sample_received=True` and `SPIKE_DIAG ... has_default_database=True`; user stated `audible` or `silent`; post-loop plugin + DLL dumps captured for Wave 3; iteration count ≤5. OR user invoked the "escalate" path and the spike closes PARTIAL with documented stuck point.</done>
  <resume-signal>
    User pastes final `artifacts\smoke.log` showing `SPIKE_OK audio_sample_received=True` + `has_default_database=True`, confirms `audible` or `silent`, pastes the post-loop `gst-inspect` plugin dump + bundle `dir /b`, and states "green" — OR says "escalate" with iteration history, which pivots to a scope-revisit decision checkpoint.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's AA listen key → chat paste-back | `artifacts/smoke.log` contains stdout from `smoke_test.py` which logs the redacted URL; a raw listen key only reaches the chat if the redaction regressed in Wave 2 edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | `smoke_test.py` + paste-back chat messages | mitigate | Wave 1 landed the `?<redacted>` URL redaction. Wave 2 edits to `smoke_test.py` (permitted only for failure mode #8 audio-sink override) MUST preserve lines 693-ish (`url.split("?", 1)[0] + "?<redacted>"`) — the fix diff touches bus-message or audio-sink code paths only, not the URL-emit path. Verified by a grep check on every commit: `grep -q '?<redacted>' smoke_test.py` must pass before commit. `artifacts/smoke.log` is already gitignored (Wave 1 Task 3), so paste-back is the only exposure surface; redaction is the single point of defense. |
</threat_model>

<verification>
- Final `artifacts/smoke.log` paste shows `SPIKE_OK audio_sample_received=True` with non-empty `duration_s` ≥ 5.
- Final paste shows `SPIKE_DIAG ... has_default_database=True` (libgiognutls.dll loaded — SC-2 pass).
- User confirmed `audible` OR `silent`-with-VM-note (both count per 43-VALIDATION.md).
- Iteration count ≤ 5; each iteration committed as `spike(43-02): iter-NN fix <reason>` so git log shows the progression.
- Every `smoke_test.py` edit (if any) left the `?<redacted>` line intact — grep passes on final file.
- Post-loop plugin + DLL pastes captured for Wave 3.
</verification>

<success_criteria>
Plan complete when the spike achieves a green iteration: SPIKE_OK + has_default_database=True + user audibility word + final plugin/DLL dump captured. Wave 3 consumes this to write `43-SPIKE-FINDINGS.md`.

Fallback completion: user elects "escalate" on iteration 5; spike closes with partial findings documenting exactly where it stuck — Phase 44 inherits the unresolved work.
</success_criteria>

<output>
After completion, create `.planning/phases/43-gstreamer-windows-spike/43-02-SUMMARY.md` with: iteration count, final green `.spec` + `runtime_hook.py` delta summary, paste-back evidence (SPIKE_OK line + libgiognutls confirmation + audibility word), any RESEARCH-table failure mode actually hit + its fix, any NEW failure mode hit that RESEARCH didn't cover (flag for findings doc), A5/A7 resolution (gst-plugins subdir name + girepository version), pointer to scratch files containing the plugin + DLL dumps for Wave 3.
</output>
