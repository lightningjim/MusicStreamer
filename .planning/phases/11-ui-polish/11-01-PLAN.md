---
phase: 11-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/__main__.py
  - musicstreamer/ui/main_window.py
  - musicstreamer/ui/station_row.py
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Now-playing panel displays with rounded corners (12px radius)"
    - "Now-playing panel has a subtle gradient background using @card_bg_color"
    - "Now-playing panel has increased internal whitespace (16px vertical, 24px horizontal)"
    - "Station list rows have visibly more vertical padding than before"
  artifacts:
    - path: "musicstreamer/__main__.py"
      provides: "Gtk.CssProvider loaded at app startup"
      contains: "CssProvider"
    - path: "musicstreamer/ui/main_window.py"
      provides: "Panel CSS class and increased margins"
      contains: "now-playing-panel"
    - path: "musicstreamer/ui/station_row.py"
      provides: "Station row CSS class"
      contains: "station-list-row"
  key_links:
    - from: "musicstreamer/__main__.py"
      to: "musicstreamer/ui/main_window.py"
      via: "CSS provider targets .now-playing-panel and .station-list-row classes"
      pattern: "now-playing-panel"
    - from: "musicstreamer/__main__.py"
      to: "musicstreamer/ui/station_row.py"
      via: "CSS provider targets .station-list-row class added to Adw.ActionRow"
      pattern: "station-list-row"
---

<objective>
Add visual polish to the MusicStreamer UI: rounded corners and gradient background on the now-playing panel, increased panel whitespace, and bumped station row padding.

Purpose: Final visual refinement for v1.2 milestone — softens the interface without structural changes.
Output: Three modified Python files; CSS-only visual changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-polish/11-CONTEXT.md
@.planning/phases/11-ui-polish/11-RESEARCH.md
@.planning/phases/11-ui-polish/11-UI-SPEC.md

@musicstreamer/__main__.py
@musicstreamer/ui/main_window.py
@musicstreamer/ui/station_row.py

<interfaces>
<!-- No new interfaces — CSS class names are the contracts between files -->

From musicstreamer/__main__.py (current):
- App.do_activate creates MainWindow and calls win.present()
- CSS provider must be loaded AFTER win = MainWindow(...) and BEFORE win.present()

From musicstreamer/ui/main_window.py (current):
- panel = Gtk.Box at L39, margins at L40-43 (4/4/8/8)
- _make_action_row at L452 creates Adw.ActionRow for ExpanderRow children

From musicstreamer/ui/station_row.py (current):
- row = Adw.ActionRow at L21, set as child at L42
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS provider, panel gradient+corners, panel margins</name>
  <files>musicstreamer/__main__.py, musicstreamer/ui/main_window.py</files>
  <read_first>
    - musicstreamer/__main__.py (current do_activate flow — CSS must be inserted between win construction and win.present)
    - musicstreamer/ui/main_window.py (panel construction at L38-46 — current margin values to replace)
    - .planning/phases/11-ui-polish/11-UI-SPEC.md (exact CSS rules and margin values)
  </read_first>
  <action>
**In `musicstreamer/__main__.py`** (per D-06: CSS provider at app startup):

1. Add imports at top: `from gi.repository import Gtk, Gdk` (Gdk needed for Display)
2. Define a module-level CSS string constant named `_APP_CSS` containing exactly:

```css
.now-playing-panel {
    background: linear-gradient(
        to bottom,
        shade(@card_bg_color, 1.04),
        shade(@card_bg_color, 0.97)
    );
    border-radius: 12px;
}

.station-list-row {
    padding-top: 4px;
    padding-bottom: 4px;
}
```

3. In `do_activate`, after `win = MainWindow(self, repo)` and BEFORE `win.present()`, add:

```python
css_provider = Gtk.CssProvider()
css_provider.load_from_string(_APP_CSS)
Gtk.StyleContext.add_provider_for_display(
    Gdk.Display.get_default(),
    css_provider,
    Gtk.STYLE_PROVIDER_PRIORITY_APPLICATION,
)
```

**In `musicstreamer/ui/main_window.py`** (per D-02, D-03, UI-01, UI-04):

1. Change panel margins at L40-43:
   - `panel.set_margin_top(16)` (was 4)
   - `panel.set_margin_bottom(16)` (was 4)
   - `panel.set_margin_start(24)` (was 8)
   - `panel.set_margin_end(24)` (was 8)

2. Add CSS class to panel AFTER the panel box is created (after L45):
   - `panel.add_css_class("now-playing-panel")`

3. In `_make_action_row` method (L452), add CSS class to the ActionRow after it is created:
   - `ar.add_css_class("station-list-row")` (after `ar.set_activatable(True)` at L462)
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
# Check __main__.py has CssProvider and _APP_CSS
with open('musicstreamer/__main__.py') as f: src = f.read()
assert 'CssProvider' in src, 'Missing CssProvider'
assert '_APP_CSS' in src, 'Missing _APP_CSS constant'
assert 'load_from_string' in src, 'Missing load_from_string'
assert 'STYLE_PROVIDER_PRIORITY_APPLICATION' in src, 'Missing priority'
assert 'now-playing-panel' in src, 'Missing CSS class in CSS string'
assert 'station-list-row' in src, 'Missing station-list-row in CSS string'
assert 'border-radius: 12px' in src, 'Missing border-radius'
assert 'linear-gradient' in src, 'Missing gradient'
assert 'shade(@card_bg_color' in src, 'Missing shade function'
# Check main_window.py has panel class and updated margins
with open('musicstreamer/ui/main_window.py') as f: mw = f.read()
assert 'now-playing-panel' in mw, 'Missing panel CSS class'
assert 'set_margin_top(16)' in mw, 'Panel margin_top not 16'
assert 'set_margin_bottom(16)' in mw, 'Panel margin_bottom not 16'
assert 'set_margin_start(24)' in mw, 'Panel margin_start not 24'
assert 'set_margin_end(24)' in mw, 'Panel margin_end not 24'
assert 'station-list-row' in mw, 'Missing station-list-row in _make_action_row'
print('All checks passed')
" && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/__main__.py contains `_APP_CSS` string with `.now-playing-panel` and `.station-list-row` rules
    - musicstreamer/__main__.py contains `Gtk.CssProvider()` and `load_from_string(_APP_CSS)`
    - musicstreamer/__main__.py contains `Gtk.StyleContext.add_provider_for_display` with `STYLE_PROVIDER_PRIORITY_APPLICATION`
    - musicstreamer/__main__.py CSS provider is loaded between `win = MainWindow(...)` and `win.present()`
    - musicstreamer/ui/main_window.py contains `panel.set_margin_top(16)`, `set_margin_bottom(16)`, `set_margin_start(24)`, `set_margin_end(24)`
    - musicstreamer/ui/main_window.py contains `panel.add_css_class("now-playing-panel")`
    - musicstreamer/ui/main_window.py `_make_action_row` contains `ar.add_css_class("station-list-row")`
    - `python3 -m pytest tests/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>CSS provider loaded at startup with gradient+radius+padding rules; panel has increased margins and CSS class; _make_action_row ActionRows have station-list-row class</done>
</task>

<task type="auto">
  <name>Task 2: Station row CSS class on StationRow and verify</name>
  <files>musicstreamer/ui/station_row.py</files>
  <read_first>
    - musicstreamer/ui/station_row.py (Adw.ActionRow at L21 — add CSS class after construction)
  </read_first>
  <action>
**In `musicstreamer/ui/station_row.py`** (per D-07, UI-03):

Add the `.station-list-row` CSS class to the `Adw.ActionRow` in `StationRow.__init__`. After the ActionRow is created at L21-24 (the `row = Adw.ActionRow(...)` block), add:

```python
row.add_css_class("station-list-row")
```

Place it after `row.set_activatable(False)` at L25, before the art prefix logic at L27.

This ensures both StationRow (used in Recently Played and flat mode) and _make_action_row (used in grouped ExpanderRow mode) have the same padding class.
  </action>
  <verify>
    <automated>python3 -c "
with open('musicstreamer/ui/station_row.py') as f: src = f.read()
assert 'station-list-row' in src, 'Missing station-list-row CSS class'
assert 'add_css_class' in src, 'Missing add_css_class call'
print('StationRow check passed')
" && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/ui/station_row.py contains `row.add_css_class("station-list-row")`
    - The add_css_class call is on the `row` (Adw.ActionRow) variable, not `self`
    - `python3 -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>All Adw.ActionRow instances in both StationRow and _make_action_row have the station-list-row CSS class for consistent padding</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of UI polish</name>
  <files>none — visual inspection only</files>
  <action>
Human visually verifies all four UI polish requirements at runtime. Run `python3 -m musicstreamer` and inspect:

1. Now-playing panel: rounded corners (UI-01), gradient background (UI-02), increased whitespace (UI-04)
2. Station list rows: increased vertical padding (UI-03)
3. No regressions in search, filters, station editing, playback
  </action>
  <what-built>
    Rounded corners and subtle gradient on the now-playing panel, increased panel whitespace (16px vertical / 24px horizontal margins), and bumped vertical padding on all station list rows. All via CSS provider loaded at app startup.
  </what-built>
  <how-to-verify>
    1. Run the app: `python3 -m musicstreamer`
    2. Check the now-playing panel area (top of window):
       - Should have visible rounded corners (12px radius)
       - Should have a subtle gradient background (slightly lighter at top, slightly darker at bottom) — distinct from the flat window background
       - Should have noticeably more internal whitespace than before (margins bumped from 4/8 to 16/24)
    3. Click a station to play — verify the panel looks correct with content (logo, title, cover art)
    4. Check station list rows (expand a provider group):
       - Rows should have slightly more vertical padding than before (4px bump)
       - Both grouped rows (inside ExpanderRow) and Recently Played rows should have the same padding
    5. Verify nothing is broken: search, filter chips, station editing still work
  </how-to-verify>
  <verify>Visual inspection — no automated check for rendering</verify>
  <done>User confirms all four UI polish changes are visible and no regressions exist</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -x -q` passes (no regressions)
- `python3 -c "..."` inline checks confirm CSS strings, class names, and margin values in all 3 files
- Visual inspection confirms rounded corners, gradient, whitespace, and row padding
</verification>

<success_criteria>
- Now-playing panel has 12px rounded corners visible at runtime
- Now-playing panel has @card_bg_color gradient background (not flat window color)
- Now-playing panel has 16px top/bottom and 24px start/end margins
- Station list rows have 4px additional vertical padding from CSS
- Existing test suite passes without regression
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-polish/11-01-SUMMARY.md`
</output>
