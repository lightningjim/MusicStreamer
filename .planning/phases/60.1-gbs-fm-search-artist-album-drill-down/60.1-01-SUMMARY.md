---
phase: 60.1-gbs-fm-search-artist-album-drill-down
plan: 01
subsystem: testing
tags: [phase60.1, gbs-fm, drill-down, tdd-red, fixture-capture, multi-word, html-parser]

requires:
  - phase: 60-gbs-fm-integration
    provides: gbs_api.search + _ArtistAlbumParser + GBSSearchDialog scaffold
provides:
  - Captured multi-word search fixture (search_multiword_p1.html, 17.5 KB, foo+fighters)
  - 6 RED parser tests in tests/test_gbs_api.py pinning _ArtistPageParser, _AlbumPageParser, fetch_artist_songs, fetch_album_songs, multi-word search contracts
  - 7 RED dialog tests in tests/test_gbs_search_dialog.py pinning drill-down click → worker → render flow, Back-button restore, snapshot guard, in-flight Back-disable, new-search abandon, auth-expired toast
affects: [60.1-02, 60.1-03, 60.1-04]

tech-stack:
  added: []
  patterns:
    - "Fixture-first capture (D-03): capture live HTML before authoring root-cause-confirming test"
    - "RED-by-AttributeError: tests reference unimplemented symbols (_ArtistPageParser, fetch_artist_songs) so collection / execution fails until Plan 02 lands"
    - "RED-by-ImportError: dialog tests import unimplemented worker classes — entire file fails to collect until Plan 03 lands"

key-files:
  created:
    - tests/fixtures/gbs/search_multiword_p1.html
  modified:
    - scripts/gbs_capture_fixtures.sh
    - tests/test_gbs_api.py
    - tests/test_gbs_search_dialog.py

key-decisions:
  - "Did not wrap an extra cookie-expiry hard guard around the existing capture script for this plan — the existing script lacks it (Phase 60-11 added it to a different ad-hoc wrapper); plan 01 only adds one new curl line."
  - "test_search_returns_artist_links_multiword authored with optimistic assertion (>= 1) per CONTEXT.md D-03 / RESEARCH §Wave 0 Gaps — Plan 02 will relax to == 0 if hypothesis 1 holds after fixture inspection."
  - "Did NOT delete D-07 Shape-4 tests in this plan (plan 03 deletes them in the same commit as dialog GREEN per CONTEXT.md D-07)."

patterns-established:
  - "Multi-word fixture captures via curl-only invocation (not full bash script) to avoid re-capturing destructive fixture #10"
  - "_GbsArtistWorker / _GbsAlbumWorker stubbed in dialog_logged_in fixture via monkeypatch to prevent test-time thread spawn"

requirements-completed: [GBS-01e]

duration: 11min
completed: 2026-05-06
---

# Phase 60.1 Plan 01: Wave 0 (TDD-RED) — multi-word fixture + 13 failing tests

**Multi-word foo+fighters search response captured (17.5 KB valid HTML), 6 RED parser tests authored against tests/test_gbs_api.py, and 7 RED dialog tests authored against tests/test_gbs_search_dialog.py — Phase 60.1 contracts pinned end-to-end before any production code is written.**

## Performance

- **Duration:** ~11 min (subagent partial; orchestrator finalized commits + summary inline after org usage cap)
- **Started:** 2026-05-06T13:37Z
- **Completed:** 2026-05-06T13:53Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Captured `tests/fixtures/gbs/search_multiword_p1.html` via cookied curl (BEFORE the 2026-05-17 cookie expiry; HTML doctype confirmed; csrftoken grep returned 0 — no leaked session cookies)
- Extended `scripts/gbs_capture_fixtures.sh` with re-runnable `curl /search?query=foo+fighters&page=1` capture line (entry #16)
- Authored 6 RED parser tests pinning multi-word search, `_ArtistPageParser` (artist page), `_AlbumPageParser` (album page + D-02 belt-and-suspenders), `fetch_artist_songs`, `fetch_album_songs`
- Authored 7 RED dialog tests pinning the drill-down click → worker → render flow, Back-button state restore, Pitfall 8 snapshot guard, Pitfall 9 in-flight Back-disable, Pitfall 10 new-search abandon, and auth-expired toast path
- Confirmed RED state matches plan: 5 parser tests fail with `AttributeError`, dialog suite fails at collection with `ImportError: _GbsArtistWorker`

## Task Commits

Each task was committed atomically:

1. **Task 1: Capture multi-word search fixture (foo+fighters)** — `16e6b7e` (test)
2. **Task 2: Author 6 RED parser tests in tests/test_gbs_api.py** — `d22db41` (test)
3. **Task 3: Author 7 RED dialog tests in tests/test_gbs_search_dialog.py** — `f06338b` (test)

## Files Created/Modified
- `tests/fixtures/gbs/search_multiword_p1.html` — captured /search?query=foo+fighters&page=1 (live network)
- `scripts/gbs_capture_fixtures.sh` — appended fixture #16 (multi-word search capture line)
- `tests/test_gbs_api.py` — 170 LOC added (6 new RED tests + section header + import re-uses existing fixtures)
- `tests/test_gbs_search_dialog.py` — 257 LOC added (7 new RED tests + 2 new monkeypatch lines on `_GbsArtistWorker.start` / `_GbsAlbumWorker.start` in `dialog_logged_in` fixture + extended import statement)

## Decisions Made

- **Did NOT add a cookie-expiry hard guard to scripts/gbs_capture_fixtures.sh.** The existing repo script does not have one; Phase 60-11 added the `[ "$(date +%Y%m%d)" -ge "20260517" ]` guard to a different ad-hoc wrapper, not this script. This plan's scope is one new capture line only — no script-wide policy change.
- **Optimistic assertion in `test_search_returns_artist_links_multiword`** (`len(links) >= 1`) authored per CONTEXT.md D-03 / RESEARCH §Wave 0 Gaps. Plan 02 may relax to `== 0` if fixture inspection shows hypothesis 1 (server omits panel) holds.
- **Did NOT delete D-07 Shape-4 tests** (`test_artist_click_kicks_free_text_search`, `test_album_click_kicks_free_text_search`) in this plan. Plan 03 deletes them in the same commit as the dialog GREEN per CONTEXT.md D-07.

## Deviations from Plan

None — plan executed as written. (Subagent stopped at the org's monthly usage cap after producing all 3 task outputs but before committing or writing SUMMARY; orchestrator finalized commits inline with no content changes — diffs match plan-specified additions.)

## Issues Encountered

- **`test_search_returns_artist_links_multiword` PASSED on first run.** This is informative for Plan 02: when the captured `search_multiword_p1.html` is fed to the existing `_ArtistAlbumParser`, it does yield non-empty `artist_links` ≥ 1. That suggests RESEARCH §Pitfall 1 hypothesis 1 (server omits the panel for multi-word) does NOT hold for the foo+fighters fixture. Plan 02 should re-confirm the user's reported Issue A reproduces against live production (not just this fixture) and document the divergence in its own SUMMARY. The RED contract still holds for the OTHER 5 parser tests; the multi-word test is functionally GREEN already, which means D-04 may simplify to "no-op + document" rather than a parser change.

## Next Phase Readiness

- Plan 02 (Wave 1, parser tier) can begin: it must add `_ArtistPageParser`, `_AlbumPageParser`, `fetch_artist_songs`, `fetch_album_songs` to `musicstreamer/gbs_api.py` and triage the multi-word finding above.
- Plan 03 (Wave 2, dialog tier) is blocked by Plan 02 (test_gbs_search_dialog.py cannot collect until `_GbsArtistWorker` / `_GbsAlbumWorker` exist — though those classes are added by Plan 03, not Plan 02). The plan's depends_on chain (03 depends on 01+02) preserves the wave order.
- Plan 04 (Wave 3, manual UAT) waits on 02+03.

---
*Phase: 60.1-gbs-fm-search-artist-album-drill-down*
*Plan: 01*
*Completed: 2026-05-06*
