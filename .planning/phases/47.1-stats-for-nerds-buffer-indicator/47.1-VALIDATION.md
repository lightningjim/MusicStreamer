---
phase: 47.1
slug: stats-for-nerds-buffer-indicator
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-18
---

# Phase 47.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x + pytest-qt 4.x |
| **Config file** | `pyproject.toml` (pytest.ini_options) |
| **Quick run command** | `python -m pytest tests/test_player_failover.py tests/test_now_playing_panel.py -q --tb=line` |
| **Full suite command** | `python -m pytest -q --tb=short` |
| **Estimated runtime** | ~5 seconds quick, ~10 seconds full |

---

## Sampling Rate

- **After every task commit:** Run quick command on the single changed test file
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green (excluding known pre-existing env failures: missing `yt_dlp`, missing `gi`, `QtTest=None`)
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

> Populated by the planner using RESEARCH §7 "Per-Decision Verification Map" (16 observables).
> Planner maps each task's acceptance criteria to the matching D-01..D-14 observable.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {pending planner population} | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_now_playing_panel.py` — new test file for stats widget visibility and slot updates (if no widget test exists yet for this panel)
- [ ] Confirm pytest-qt fixtures (`qtbot`, `QTBOT_FIXTURE_ARGS`) resolve in the existing test env

*Note: existing infrastructure (pytest-qt, repo mocking patterns in `conftest.py`) covers the bulk of phase requirements. Researcher flagged no Wave 0 framework installs needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Buffer percent updates live during playback of a real HTTP stream | D-12, D-14 | Requires a running GStreamer pipeline with a real network source — not reproducible in unit tests without heavy integration scaffolding | Play any AudioAddict or RadioBrowser station, enable "Stats for Nerds" via hamburger menu, observe that the bar fills toward 100% during initial buffering and stays at 100% during steady-state playback |
| Visual QA: bar + label align and fit the center column without wrapping at typical window widths | D-01, D-02, D-07 | Subjective visual check | Resize main window; confirm the row stays on a single line at 1200px default and at the minimum window width |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
