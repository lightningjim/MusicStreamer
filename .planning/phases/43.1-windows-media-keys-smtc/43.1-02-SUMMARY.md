---
phase: 43.1-windows-media-keys-smtc
plan: 02
subsystem: media-keys
tags: [winrt, smtc, pywinrt, windows, pyproject, optional-dependencies, tdd]

# Dependency graph
requires:
  - phase: 41-platform-media-keys
    provides: "MediaKeysBackend abstract class, create() factory, NoOpMediaKeysBackend fallback, smtc.py stub"
provides:
  - "[project.optional-dependencies].windows group in pyproject.toml with four winrt-Windows.* packages (D-05)"
  - "Linux-importable smtc.py skeleton with deferred winrt import guard (D-06)"
  - "create_windows_backend raises ImportError with install hint when winrt absent (D-07)"
  - "tests/test_media_keys_smtc.py with 4 Wave-0 assertions + shared test structure for Plans 03/04/05"
  - "Phase 41 scaffold Test 8 updated from NotImplementedError to ImportError"
affects:
  - 43.1-03-PLAN
  - 43.1-04-PLAN
  - 43.1-05-PLAN

# Tech tracking
tech-stack:
  added:
    - "winrt-Windows.Media.Playback (declared, not installed on Linux)"
    - "winrt-Windows.Media (declared)"
    - "winrt-Windows.Storage.Streams (declared)"
    - "winrt-Windows.Foundation (declared)"
  patterns:
    - "Deferred winrt import inside function body (D-06) -- module stays Linux-importable"
    - "ImportError propagates to factory except-Exception branch -> NoOpMediaKeysBackend (D-07)"
    - "tomllib stdlib used for pyproject.toml assertion in tests (Python 3.11+)"

key-files:
  created:
    - tests/test_media_keys_smtc.py
  modified:
    - pyproject.toml
    - musicstreamer/media_keys/smtc.py
    - tests/test_media_keys_scaffold.py
    - uv.lock

key-decisions:
  - "TDD applied per plan: RED commits precede GREEN commits for both tasks"
  - "pyproject.toml [windows] group uses no version pins -- winrt-runtime pulled as transitive dep"
  - "smtc.py _INSTALL_HINT is a fixed string constant (no sys.path / env leakage per T-43.1-03)"
  - "Second raise ImportError after deferred import succeeds surfaces unfinished state on Windows until Plan 03 lands WindowsMediaKeysBackend"

patterns-established:
  - "Pattern: deferred winrt import + ImportError re-raise -- copy to WindowsMediaKeysBackend.__init__ in Plan 03"
  - "Pattern: test_factory_falls_back_to_noop_on_linux -- monkeypatch sys.platform='win32' + clear cached modules to exercise factory dispatch"

requirements-completed: [MEDIA-03]

# Metrics
duration: 3min
completed: 2026-04-21
---

# Phase 43.1 Plan 02: SMTC Scaffold and ImportError Fallback Summary

**[windows] optional-dep group declared in pyproject.toml, smtc.py deferred-import skeleton replacing NotImplementedError stub, and 4 Wave-0 unit tests confirming Linux importability and NoOp factory fallback**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-21T22:46:16Z
- **Completed:** 2026-04-21T22:49:00Z
- **Tasks:** 2
- **Files modified:** 5 (pyproject.toml, smtc.py, test_media_keys_smtc.py, test_media_keys_scaffold.py, uv.lock)

## Accomplishments

- `pyproject.toml` gains `[project.optional-dependencies].windows` with four winrt-Windows.* packages (D-05); existing `test` group untouched
- `smtc.py` stub replaced: module scope is pure Python (D-06), `create_windows_backend` does deferred winrt import that raises `ImportError` with install hint when wheels absent (D-07)
- `tests/test_media_keys_smtc.py` created with 4 Wave-0 tests (pyproject assertion, module import guard, ImportError sentinel, factory NoOp end-to-end)
- Phase 41 scaffold Test 8 updated from `NotImplementedError` to `ImportError`; all 17 scaffold + smtc tests green

## Task Commits

1. **Task 1 RED: [windows] optional-deps test** - `f3a2e37` (test)
2. **Task 1 chore: uv.lock update** - `63afa49` (chore)
3. **Task 2 RED: Wave-0 ImportError tests** - `f949e81` (test)
4. **Task 2 GREEN: smtc.py skeleton + scaffold Test 8** - `36343d3` (feat)

## Files Created/Modified

- `pyproject.toml` - Added `[project.optional-dependencies].windows` with 4 winrt-Windows.* packages
- `musicstreamer/media_keys/smtc.py` - Replaced NotImplementedError stub with deferred-import skeleton; function-body winrt imports; ImportError with _INSTALL_HINT
- `tests/test_media_keys_smtc.py` - New Wave-0 test file: pyproject assertion, Linux import guard, ImportError sentinel, factory NoOp fallback
- `tests/test_media_keys_scaffold.py` - Test 8 updated: NotImplementedError -> ImportError
- `uv.lock` - Regenerated after pyproject.toml change

## Decisions Made

- TDD applied for both tasks: RED commits with failing tests precede GREEN commits with implementation
- `_INSTALL_HINT` is a fixed string constant (no sys.path or env data leaked per threat T-43.1-03)
- uv.lock committed to reflect pyproject.toml change (uv updated it automatically during test run)
- The second `raise ImportError` after the deferred import succeeds (when winrt IS installed) ensures Plan 03 landing is explicit and loud rather than silently returning a broken backend

## Deviations from Plan

None - plan executed exactly as written. The test/RED + feat/GREEN TDD commit structure was applied per plan's `tdd="true"` attribute on both tasks.

## Issues Encountered

- `uv run` updated `uv.lock` during the first test run; committed separately as `chore` to avoid polluting the RED test commit
- Factory smoke test via raw Python `-c` flag fails with system PySide6's `os.add_dll_directory` error on Linux; pytest-based `test_factory_falls_back_to_noop_on_linux` covers the same path correctly via uv's venv PySide6

## Known Stubs

`create_windows_backend` raises `ImportError("WindowsMediaKeysBackend pending...")` as the final line -- intentional pending-stub signal for Plan 43.1-03 which will replace it with `return WindowsMediaKeysBackend(player, repo)`. The stub is load-bearing for Wave 1 correctness: on a Windows machine with winrt wheels installed, the factory still falls back to NoOp rather than returning a broken backend.

## Next Phase Readiness

- Plan 03 can now subclass `MediaKeysBackend` and add `WindowsMediaKeysBackend` to smtc.py -- the import guard and factory plumbing are in place
- `test_media_keys_smtc.py` has the mock-winrt fixture structure ready to extend
- Win11 VM `pip install -e ".[windows]"` command is documented in plan frontmatter `user_setup`

## Threat Flags

None - no new network endpoints, auth paths, file access patterns, or trust boundary changes introduced. `_INSTALL_HINT` is a fixed string (T-43.1-03 mitigation confirmed).

## Self-Check: PASSED

- `tests/test_media_keys_smtc.py` exists: FOUND
- `musicstreamer/media_keys/smtc.py` modified: FOUND
- `tests/test_media_keys_scaffold.py` modified: FOUND
- `pyproject.toml` modified: FOUND
- Commits f3a2e37, 63afa49, f949e81, 36343d3: all in git log

---
*Phase: 43.1-windows-media-keys-smtc*
*Completed: 2026-04-21*
