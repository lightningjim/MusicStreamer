---
phase: 43.1
plan: 06
status: pending
created: 2026-04-21
---

# Phase 43.1 — Windows UAT (SMTC)

> **This UAT runs on the Win11 VM used during the Phase 43 GStreamer spike.** Linux CI cannot validate SMTC — real winrt wheels, real media-key HID, and the Windows media overlay are all OS-owned.
>
> Sign off every row below before marking Phase 43.1 complete. Paste diagnostic output verbatim — it resolves RESEARCH.md open questions (Pitfall #3 async deadlock, Assumption A1 enum casing, A6 thumbnail=None).

## Environment Snapshot

Captured on Win11 VM:

```
python --version    # expected: Python 3.12.x (conda-forge)
conda info --envs   # expected: env used in Phase 43 spike is active
pip list | findstr winrt
```

**Paste actual output here:**

```
TBD — fill in from Win11 VM
```

## Enum Casing Diagnostic (Research Open Question 2 / Assumption A1)

Command:
```powershell
python -c "from winrt.windows.media import SystemMediaTransportControlsButton; print(list(SystemMediaTransportControlsButton))"
```

**Paste actual output here:**

```
TBD — fill in from Win11 VM
```

**Assumption check:** Does output use `UPPER_SNAKE_CASE` (PLAY, PAUSE, STOP, NEXT, PREVIOUS)? `smtc.py` hardcodes these names — if live output differs, file a follow-up plan before continuing with UAT.

- [ ] Enum casing is UPPER_SNAKE_CASE (smtc.py assumptions hold)
- [ ] Enum casing differs → file follow-up + pause UAT

## UAT Checklist

All UAT is performed on a clean Win11 VM session after `pip install -e ".[windows]"` in the conda-forge env.

| # | Behavior | Requirement | Method | Pass/Fail | Notes |
|---|----------|-------------|--------|-----------|-------|
| UAT-1 | App launches without crash after `pip install -e ".[windows]"`; no stderr traceback from `media_keys.create()` | MEDIA-03 | `python -m musicstreamer` | ☐ | |
| UAT-2 | Pressing keyboard media-play key pauses playback; pressing again resumes | MEDIA-04 / SC #3 | Start SomaFM HTTPS station, press media-play twice, confirm audio pause+resume | ☐ | |
| UAT-3 | Pressing keyboard media-stop key stops playback | MEDIA-04 / SC #3 | Press media-stop during playback | ☐ | |
| UAT-4 | SMTC overlay (Win+V) shows station name + ICY track title + cover art thumbnail | MEDIA-05 / SC #4 | Win+V opens media panel, confirm MusicStreamer session appears with all three fields | ☐ | |
| UAT-5 | Overlay play/pause/stop buttons trigger Player state changes in-app | MEDIA-04 / SC #3 | Click overlay buttons, verify app Player transitions | ☐ | |
| UAT-6 | ICY title update mid-stream refreshes the overlay title | MEDIA-05 / SC #4 | Wait 2-5 min or skip to a fresh station; confirm overlay title changes to new ICY value without app restart | ☐ | |
| UAT-7 | No Pitfall #3 deadlock — overlay updates promptly when title changes (store_async().get() does not block Qt main thread) | Pitfall #3 research gate | Observe that overlay updates within ~1s of ICY change; no UI freeze | ☐ | |
| UAT-8 | Closing app normally clears MusicStreamer SMTC session within 30s | T-43.1-01 / D-09 | Close via X button, open another SMTC-aware app (Spotify/etc.), confirm MusicStreamer session gone from overlay | ☐ | |
| UAT-9 | D-07 fallback: fresh conda env WITHOUT `[windows]` extras launches app, logs install-hint WARNING, playback still works (just no media keys) | D-07 / SC #5 | Create new conda env from scratch (no `pip install .[windows]`), launch app, check stderr for install-hint warning, play station | ☐ | |
| UAT-10 | Thumbnail clear to None works (no raise on empty-station publish) | Assumption A6 | Stop playback (Player emits blank state), confirm overlay returns to app-icon state without error | ☐ | |

## Pitfall-In-The-Wild Notes

Record anything unexpected observed during UAT — even if the checklist row was "pass". Examples: slow overlay updates, warning messages at startup, unexpected threading behavior.

```
TBD
```

## Sign-Off

- [ ] All UAT-1 through UAT-10 pass OR deviations are documented with follow-up plan numbers
- [ ] Environment snapshot captured
- [ ] Enum casing verified UPPER_SNAKE_CASE
- [ ] Pitfall #3 (store_async deadlock) empirically resolved

**Signed off by:** TBD
**Date:** TBD
