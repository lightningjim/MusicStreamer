---
phase: 43.1-windows-media-keys-smtc
plan: 03
subsystem: media-keys
tags: [winrt, smtc, pywinrt, windows, tdd, qt-signals, threadpool]

# Dependency graph
requires:
  - phase: 43.1-windows-media-keys-smtc/43.1-02
    provides: "smtc.py deferred-import skeleton, [windows] optional-deps group, 4 Wave-0 tests"
  - phase: 41-platform-media-keys
    provides: "MediaKeysBackend abstract class, Signal surface, NoOpMediaKeysBackend, factory"
provides:
  - "WindowsMediaKeysBackend(MediaKeysBackend) class in smtc.py with __init__, _on_button_pressed, _apply_playback_state"
  - "command_manager.is_enabled=False (Pitfall #1 mitigated)"
  - "button_pressed token stored as self._bp_token (Pitfall #4 mitigated)"
  - "try/except wrapping _on_button_pressed body (T-43.1-04 mitigated)"
  - "create_windows_backend returns WindowsMediaKeysBackend(player, repo) on winrt-enabled systems"
  - "mock_winrt_modules pytest fixture + _build_winrt_stubs() helper for Linux unit tests"
  - "14 tests green (4 Plan-02 + 10 Plan-03) in tests/test_media_keys_smtc.py"
affects:
  - 43.1-04-PLAN
  - 43.1-05-PLAN
  - 43.1-06-PLAN

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Deferred winrt imports inside __init__ body (D-06) -- module stays Linux-importable"
    - "command_manager.is_enabled=False before add_button_pressed subscription (Pitfall #1)"
    - "EventRegistrationToken stored on self._bp_token for remove_button_pressed at shutdown (Pitfall #4)"
    - "try/except Exception wrapping entire winrt callback body to prevent threadpool poisoning (T-43.1-04)"
    - "Dict-based state map for Literal->enum translation (_apply_playback_state)"
    - "monkeypatch.setitem(sys.modules) pattern for stub injection; monkeypatch cleanup is automatic"
    - "Enum-ref caching: self._button_enum / self._status_enum / self._type_enum stored in __init__"

key-files:
  created:
    - .planning/phases/43.1-windows-media-keys-smtc/43.1-03-SUMMARY.md
  modified:
    - musicstreamer/media_keys/smtc.py
    - tests/test_media_keys_smtc.py

key-decisions:
  - "Dict lookup chosen over match statement for _apply_playback_state state mapping (Claude's Discretion per CONTEXT)"
  - "Enum class references cached on self at __init__ time to avoid re-import overhead on each button callback"
  - "publish_metadata and shutdown not stubbed -- inherited NotImplementedError from base class; Plan 04/05 override"
  - "TDD applied: RED commit (995d6c1) precedes GREEN commit (560c31e)"

patterns-established:
  - "Pattern: mock_winrt_modules fixture via _build_winrt_stubs() -- copy for Plans 04/05 test extensions"
  - "Pattern: import WindowsMediaKeysBackend fresh inside each test (after mock_winrt_modules installs stubs)"
  - "Pattern: MagicMock(side_effect=factory_fn) for MediaPlayer class so each call returns fresh instance"

requirements-completed: [MEDIA-03, MEDIA-04]

# Metrics
duration: 15min
completed: 2026-04-21
---

# Phase 43.1 Plan 03: WindowsMediaKeysBackend Class Summary

**WindowsMediaKeysBackend wired to SMTC via MediaPlayer conduit: button routing (play/pause/stop), state mapping (playing/paused/stopped->MediaPlaybackStatus), Pitfall #1 + #4 + T-43.1-04 mitigations, 14 unit tests green on Linux with mocked winrt**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-21T23:00:00Z
- **Completed:** 2026-04-21T23:15:00Z
- **Tasks:** 2 (TDD: RED + GREEN commits per task)
- **Files modified:** 2 (smtc.py, tests/test_media_keys_smtc.py)

## Accomplishments

- `WindowsMediaKeysBackend(MediaKeysBackend)` class implemented in `smtc.py` with full `__init__`, `_on_button_pressed`, `_apply_playback_state`
- All three mandatory pitfall mitigations in place: `command_manager.is_enabled=False` (Pitfall #1), `self._bp_token` storage (Pitfall #4), `try/except` wrapping callback body (T-43.1-04)
- `create_windows_backend` now returns `WindowsMediaKeysBackend(player, repo)` -- transitional `raise ImportError` removed
- Shared `mock_winrt_modules` fixture + `_build_winrt_stubs()` helper added to test file for Plans 03/04/05 to consume
- 10 new tests cover init configuration, PLAY/PAUSE/STOP routing, unknown button logging, exception swallowing, and all three state-map paths

## Task Commits

1. **Task 1+2 RED: mock_winrt_modules fixture + 10 failing tests** - `995d6c1` (test)
2. **Task 2 GREEN: WindowsMediaKeysBackend implementation** - `560c31e` (feat)

## Files Created/Modified

- `musicstreamer/media_keys/smtc.py` - Added `WindowsMediaKeysBackend` class (109 lines); `create_windows_backend` now returns instance
- `tests/test_media_keys_smtc.py` - Added `_build_winrt_stubs()`, `mock_winrt_modules` fixture, 10 new Plan-03 tests (268 lines added)

## Decisions Made

- Dict lookup chosen over `match` statement for `_apply_playback_state` -- cleaner than a 3-arm match, no Python version constraints
- Enum class refs cached on `self` (`self._button_enum`, `self._status_enum`, `self._type_enum`) so `_on_button_pressed` avoids re-importing on every keystroke
- `publish_metadata` and `shutdown` not stubbed on `WindowsMediaKeysBackend` -- base class `NotImplementedError` surfaces loudly if called; Plans 04/05 override them

## Deviations from Plan

None - plan executed exactly as written. Both Pitfall mitigations and T-43.1-04 requirement implemented verbatim from plan spec.

## Issues Encountered

- Worktree path issue: initial edits accidentally went to main repo instead of worktree. Detected early, corrected by writing directly to worktree paths. No plan impact.
- Pre-existing `test_linux_mpris_backend_constructs` failure in `test_media_keys_mpris2.py` (D-Bus not available in worktree env). Confirmed pre-existing before changes via `git stash`. Out of scope per deviation rules.
- Pre-existing `test_cookies.py` collection error (no `gi` module in uv venv). Also out of scope.

## Known Stubs

`publish_metadata` -- not yet implemented on `WindowsMediaKeysBackend`; inherits `NotImplementedError` from `MediaKeysBackend`. Plan 04 overrides this. Intentional stub; does not prevent this plan's goal (button routing + state mapping).

`shutdown` -- not yet implemented; inherits `NotImplementedError`. Plan 05 overrides this. Intentional stub.

## Threat Flags

None - no new network endpoints, auth paths, or file access patterns introduced. `_on_button_pressed` try/except (T-43.1-04) mitigates the only STRIDE threat in this plan's threat register.

## Next Phase Readiness

- Plan 04 (publish_metadata) can now override `publish_metadata` on `WindowsMediaKeysBackend` -- class exists and is wired
- `mock_winrt_modules` fixture is ready to extend; Plan 04 tests add metadata-publish assertions
- Plan 05 (shutdown) overrides `shutdown`, calls `remove_button_pressed(self._bp_token)` -- token stored and ready
- Win11 VM UAT (Plan 06) can now test the full button -> signal -> Player flow

## Self-Check: PASSED

- `musicstreamer/media_keys/smtc.py` exists and contains `WindowsMediaKeysBackend`: FOUND (line 50)
- `tests/test_media_keys_smtc.py` contains `mock_winrt_modules` fixture: FOUND (line 118 equivalent)
- `command_manager.is_enabled = False` present: FOUND (line 88)
- `self._bp_token = ` present: FOUND (line 99)
- `return WindowsMediaKeysBackend(player, repo)` present: FOUND (line 163)
- 0 occurrences of "WindowsMediaKeysBackend pending": CONFIRMED
- Commits 995d6c1 (RED), 560c31e (GREEN): both in git log

---
*Phase: 43.1-windows-media-keys-smtc*
*Completed: 2026-04-21*
