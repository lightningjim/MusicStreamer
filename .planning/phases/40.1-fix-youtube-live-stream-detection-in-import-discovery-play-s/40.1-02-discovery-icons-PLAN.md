---
phase: 40.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/ui_qt/discovery_dialog.py
  - tests/test_discovery_dialog.py
autonomous: true
requirements:
  - REGRESSION-DISCOVERY-ICONS  # D-03, D-04, D-05, D-06
tags: [qt, discovery, icons, ui]

must_haves:
  truths:
    - "Discovery results table renders play buttons with an icon and no text"
    - "Clicking play swaps icon from media-playback-start to media-playback-stop"
    - "Clicking stop swaps icon back to media-playback-start"
    - "Save column continues to render a text 'Save' button"
    - "Each play button has accessibleName 'Play preview' or 'Stop preview'"
  artifacts:
    - path: "musicstreamer/ui_qt/discovery_dialog.py"
      provides: "Icon-only play toggle buttons in results table"
      contains: "QIcon.fromTheme(\"media-playback-start-symbolic\""
    - path: "tests/test_discovery_dialog.py"
      provides: "Regression test asserting play buttons use icons not text"
      contains: "test_play_button_uses_icon"
  key_links:
    - from: "_on_search_finished"
      to: "QPushButton.setIcon"
      via: "icon toggle at construction"
      pattern: "setIcon\\(QIcon\\.fromTheme"
    - from: "_on_play_row"
      to: "QPushButton.setIcon (swap)"
      via: "state transition on click"
      pattern: "media-playback-(start|stop)-symbolic"
---

<objective>
Replace the "Play"/"Stop" text buttons in the Discovery results table with icon-only toggle buttons that swap between `media-playback-start-symbolic` and `media-playback-stop-symbolic` SVG resources per D-03/D-04/D-05. Keep the Save column as a text button (D-06).

Purpose: Restore icon-based preview UX (UI-06 polish) matching the rest of the Qt port.
Output: Icon toggle buttons + regression test asserting icon state transitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md
@.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md
@musicstreamer/ui_qt/discovery_dialog.py
@musicstreamer/icons.qrc
@tests/test_discovery_dialog.py

<interfaces>
Available SVG resources (from icons.qrc, verified):
- `:/icons/media-playback-start-symbolic.svg`
- `:/icons/media-playback-stop-symbolic.svg`

Existing state in DiscoveryDialog:
- `self._previewing_row: int = -1` — tracks which row is currently previewing
- `self._play_buttons: list[QPushButton]` — per-row button list, indexed by row
- `self._make_play_slot(row_idx)` — closure factory for connecting button clicks (QA-05 compliant, no self-capturing lambda)

Established theme-with-fallback pattern (Phase 36, verified in now_playing_panel.py):
```python
QIcon.fromTheme(
    "media-playback-start-symbolic",
    QIcon(":/icons/media-playback-start-symbolic.svg"),
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression test for icon-only play buttons</name>
  <files>tests/test_discovery_dialog.py</files>
  <read_first>
    - tests/test_discovery_dialog.py (fixtures, existing construction pattern)
    - musicstreamer/ui_qt/discovery_dialog.py (find self._play_buttons, _on_search_finished, _on_play_row)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md (Fix 2 section — exact toggle logic)
  </read_first>
  <behavior>
    Test 1 — test_play_button_uses_icon:
      - Construct DiscoveryDialog with qtbot
      - Monkeypatch radio_browser.search to return 1 fake station dict
      - Trigger search completion
      - Assert dialog._play_buttons[0].text() == "" (no text)
      - Assert not dialog._play_buttons[0].icon().isNull() (icon set)
      - Assert dialog._play_buttons[0].accessibleName() == "Play preview"
    Test 2 — test_play_button_toggles_icon_on_click:
      - Same setup, after search completion
      - Record initial icon cacheKey: key0 = dialog._play_buttons[0].icon().cacheKey()
      - Click the play button (qtbot.mouseClick) OR call _on_play_row(0) directly
      - Assert dialog._previewing_row == 0
      - Assert dialog._play_buttons[0].icon().cacheKey() != key0  (icon changed)
      - Assert dialog._play_buttons[0].accessibleName() == "Stop preview"
      - Click again → previewing_row == -1 and accessibleName back to "Play preview"
    Both tests MUST fail against current code (uses text not icon).
  </behavior>
  <action>
    Append two test functions to tests/test_discovery_dialog.py.
    Use the existing qtbot/DiscoveryDialog construction pattern already in the file.
    Mock radio_browser.search via monkeypatch to return a minimal list like:
      [{"name": "Test Station", "url": "https://example.com/stream", "url_resolved": "https://example.com/stream", "tags": "jazz", "countrycode": "US", "bitrate": 128}]
    For the search trigger, either call dialog._on_search_finished(results) directly (if public-enough) or set the search field and click Search then qtbot.waitUntil for row population.
    To prevent actual playback during toggle test, monkeypatch dialog._player.play and dialog._player.stop to no-op Mock objects.
    Run both tests and confirm they fail against unfixed code.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_discovery_dialog.py::test_play_button_uses_icon tests/test_discovery_dialog.py::test_play_button_toggles_icon_on_click -x -q</automated>
  </verify>
  <done>Two new tests exist; both FAIL against HEAD; test file imports unchanged except for additions.</done>
  <acceptance_criteria>
    - `grep -n "def test_play_button_uses_icon" tests/test_discovery_dialog.py` matches
    - `grep -n "def test_play_button_toggles_icon_on_click" tests/test_discovery_dialog.py` matches
    - `grep -n "accessibleName" tests/test_discovery_dialog.py` has at least 2 new occurrences
    - Running both tests against current code produces assertion failures (not errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace text play buttons with icon toggle</name>
  <files>musicstreamer/ui_qt/discovery_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/discovery_dialog.py (_on_search_finished around line 314, _on_play_row around line 413)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md (D-03..D-06)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md (Pattern 2 — exact code)
  </read_first>
  <action>
    In musicstreamer/ui_qt/discovery_dialog.py:

    1. Add module-level icon helpers (near top, after imports):
       ```python
       def _play_icon() -> QIcon:
           return QIcon.fromTheme(
               "media-playback-start-symbolic",
               QIcon(":/icons/media-playback-start-symbolic.svg"),
           )

       def _stop_icon() -> QIcon:
           return QIcon.fromTheme(
               "media-playback-stop-symbolic",
               QIcon(":/icons/media-playback-stop-symbolic.svg"),
           )
       ```

    2. In `_on_search_finished`, find where each row's play button is created. Replace:
         `play_btn = QPushButton("Play", ...)`
       with:
         ```python
         play_btn = QPushButton(self._results_table)
         play_btn.setIcon(_play_icon())
         play_btn.setAccessibleName("Play preview")
         play_btn.setToolTip("Play preview")
         ```
       Do NOT set any text. Keep the rest of the per-row wiring (setIndexWidget, _make_play_slot connection) intact.

    3. In `_on_play_row`, replace all `.setText("Play")` / `.setText("Stop")` calls with icon + accessibleName updates.

       Transition logic (per D-03 and Pattern 2 in RESEARCH.md):
       - If `self._previewing_row == row_index` (user clicked currently-previewing row → stop):
           `self._player.stop()`
           `self._previewing_row = -1`
           if `row_index < len(self._play_buttons)`:
               `self._play_buttons[row_index].setIcon(_play_icon())`
               `self._play_buttons[row_index].setAccessibleName("Play preview")`
               `self._play_buttons[row_index].setToolTip("Play preview")`
           return

       - Else (starting preview on a new row):
           if `0 <= self._previewing_row < len(self._play_buttons)`:
               # Reset the previously-previewing button to play icon
               `self._play_buttons[self._previewing_row].setIcon(_play_icon())`
               `self._play_buttons[self._previewing_row].setAccessibleName("Play preview")`
               `self._play_buttons[self._previewing_row].setToolTip("Play preview")`
           # Kick off play (preserve existing play logic — temp station, etc.)
           ... existing play logic ...
           `self._previewing_row = row_index`
           if `row_index < len(self._play_buttons)`:
               `self._play_buttons[row_index].setIcon(_stop_icon())`
               `self._play_buttons[row_index].setAccessibleName("Stop preview")`
               `self._play_buttons[row_index].setToolTip("Stop preview")`

    4. Save column is untouched — per D-06, keep the text "Save" button exactly as-is.

    5. Ensure QIcon is imported from PySide6.QtGui at the top of the file.

    Do NOT modify icons.qrc — the `-symbolic` resources already exist (verified in RESEARCH.md). No icons_rc.py regeneration needed.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_discovery_dialog.py -x -q</automated>
  </verify>
  <done>Both new tests pass; existing discovery tests still pass; no text on play buttons; Save column unchanged.</done>
  <acceptance_criteria>
    - `grep -n "QPushButton(\"Play\"" musicstreamer/ui_qt/discovery_dialog.py` returns no matches
    - `grep -n "setText(\"Play\")" musicstreamer/ui_qt/discovery_dialog.py` returns no matches
    - `grep -n "setText(\"Stop\")" musicstreamer/ui_qt/discovery_dialog.py` returns no matches
    - `grep -n "media-playback-start-symbolic" musicstreamer/ui_qt/discovery_dialog.py` has ≥1 match
    - `grep -n "media-playback-stop-symbolic" musicstreamer/ui_qt/discovery_dialog.py` has ≥1 match
    - `grep -n "QPushButton(\"Save\"" musicstreamer/ui_qt/discovery_dialog.py` still has ≥1 match (Save unchanged)
    - pytest tests/test_discovery_dialog.py exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new boundaries. Icon swap is local UI state.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-DIS-01 | Tampering | Icon resource path | accept | Resources bundled via icons.qrc; paths are compile-time constants in the code. No user-controlled data reaches QIcon.fromTheme. |
</threat_model>

<verification>
- Both regression tests pass
- Existing tests/test_discovery_dialog.py tests still pass
- Manual: open Discovery, search, confirm play/stop icon swap on click
</verification>

<success_criteria>
- Play buttons render as icons only (no text)
- Icon toggles between start/stop on click
- Save column unchanged
- All discovery_dialog tests green
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-02-SUMMARY.md`.
</output>
