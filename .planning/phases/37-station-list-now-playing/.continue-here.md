---
context: phase
phase: 37-station-list-now-playing
plan: 37-02
total_plans: 4
status: paused_mid_wave_2
last_updated: 2026-04-12T00:52:18.724Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered at pause time, not from failure, but they must be
> addressed before continuing Wave 2 or starting Wave 3.

- [ ] **A 37-02 background executor agent was still running when pause was invoked.** It may have kept committing after the pause. Before doing ANY new work, run `git log --oneline 76ecfac..HEAD` to see what actually landed. Do not assume the last commit you remember from the pause notes is HEAD.
- [ ] **Unexpected 37-03 test commit appeared at HEAD without 37-03 being launched.** Commit `63b2de3 test(37-03): add failing tests for ToastOverlay widget` exists but Plan 37-03 was never spawned. Read that commit before launching 37-03 to understand what was authored — it may be usable as TDD-red tests the 37-03 plan can make green.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Background-agent scope bleed | A background executor agent may have committed work outside its assigned plan (Plan 37-02 committed a `test(37-03)` file). Parallel executors can drift into adjacent plan work if they read the phase directory. | advisory | When spawning parallel executors, explicitly prohibit writing files belonging to other plans in the same phase. Check HEAD vs expected commits before assuming plan boundaries held. |

<current_state>
**Phase 37 Wave 2 is partially executed.** Wave 1 (Plan 37-01) completed cleanly — StationListPanel + StationTreeModel landed with 18 new tests, full suite at 284 passing, UI-01 marked complete in REQUIREMENTS.md.

Wave 2 has two plans: 37-02 (NowPlayingPanel) and 37-03 (ToastOverlay). They are independent (zero file overlap). The orchestrator launched 37-02 as a background agent with `run_in_background: true`, then attempted to launch 37-03 as a foreground agent. The user rejected the 37-03 spawn and immediately invoked `/gsd-pause-work`.

At pause time, git HEAD shows two commits from 37-02's background agent:
- `1b99d85 feat(37-02): add media-playback Adwaita icons`
- `63b2de3 test(37-03): add failing tests for ToastOverlay widget`  ← UNEXPECTED

The second commit is surprising because 37-03 was never launched. Most likely the 37-02 background agent read the phase directory and scope-bled into 37-03 test authoring. Less likely: a different Claude process. The background agent a3db1d65634d99411 may still be running — it will continue committing until done or killed.

**Wave 3 (Plan 37-04, MainWindow integration) has not started.** It depends on both 37-02 and 37-03.
</current_state>

<completed_work>

### Phase 35 (Backend Isolation) — Complete
All 6 plans shipped, 272 tests passing, mpv dropped via Plan 35-06 (yt-dlp library with EJS solver). PORT-01, PORT-02, PORT-05, PORT-06, PORT-09, QA-02, RUNTIME-01 complete. PKG-05 retired.

### Phase 36 (Qt Scaffold + GTK Cutover) — Complete
4 plans shipped, 266 tests, ~4150 LOC of GTK code deleted. PORT-03, PORT-04, PORT-07, PORT-08, QA-01, QA-04 complete. `musicstreamer/ui/` gone, `ui_qt/` is the only UI package.

### Phase 37 Plan 37-01 — Complete (Wave 1)
- New `musicstreamer/ui_qt/station_tree_model.py` — `StationTreeModel(QAbstractItemModel)` with provider grouping
- New `musicstreamer/ui_qt/station_list_panel.py` — `StationListPanel(QWidget)` with recently-played section above the main tree
- Added `musicstreamer/ui_qt/icons/audio-x-generic-symbolic.svg` for station fallback
- New `tests/test_station_tree_model.py` (8 tests), new `tests/test_station_list_panel.py` (10 tests)
- 284 tests passing (266 baseline + 18 new)
- UI-01 marked complete in REQUIREMENTS.md
- Commits: `4340792`, `4dcdddd`, `76ecfac`, `f95d2ad`

### Phase 37 Plan 37-02 (Wave 2, in-flight at pause)
- `1b99d85 feat(37-02): add media-playback Adwaita icons` — at minimum this landed (start/pause/stop symbolic SVGs added to `icons.qrc`)
- Anything else from the 37-02 background agent is unknown — check git log after the pause

### Seeds audit
- SEEDs 001, 002, 003, 004 marked shipped (delivered in v1.4/v1.5)
- SEED-007 planted (In-app parametric EQ with AutoEQ profile import — post-v2.0 idea)
- SEEDs 005, 006, 007 remain dormant
</completed_work>

<remaining_work>

### Phase 37
- **Verify 37-02 state** — run `git log --oneline 76ecfac..HEAD` and `QT_QPA_PLATFORM=offscreen pytest -q` to see what actually landed from the background agent. Expected: `now_playing_panel.py`, `tests/test_now_playing_panel.py`, maybe icons.qrc + icons_rc.py regeneration. Mark UI-02 and UI-14 complete if 37-02 finished.
- **Reconcile 37-03 unexpected commit** — read `git show 63b2de3` to see what test file was authored. Decide whether to keep and have 37-03 make those tests green, OR revert and do 37-03 from scratch.
- **Launch Plan 37-03** — `ToastOverlay` frameless widget + `tests/test_toast_overlay.py`. Requirement UI-12.
- **Launch Plan 37-04** — MainWindow integration + FakePlayer test double + full-suite gate + QA-05 lifetime audit. Depends on 37-02 and 37-03.
- **Phase 37 verification** — spawn gsd-verifier against the 5 ROADMAP success criteria and all 4 REQ IDs.

### Remaining v2.0 milestone (post-37)
- Phase 38: Filter Strip + Favorites (UI-03, UI-04)
- Phase 39: Core Dialogs (UI-05, UI-06, UI-07, UI-13)
- Phase 40: Auth Dialogs + Accent (UI-08, UI-09, UI-10, UI-11)
- Phase 41: Platform Media Keys — QtDBus MPRIS2 + Windows SMTC (MEDIA-01..05)
- Phase 42: Settings Export/Import (SYNC-01..05)
- Phase 43: GStreamer Windows Spike (PKG-06)
- Phase 44: Windows Packaging + Installer (PKG-01..04, QA-03, QA-05)
</remaining_work>

<decisions_made>

- Plan 37-02 launched as background agent (run_in_background=true) to parallelize Wave 2 with 37-03
- Plan 37-03 spawn rejected by user → immediate /gsd-pause-work invocation
- Handoff notes the unexpected 63b2de3 commit prominently because scope bleed is an edge case future agents must reconcile

### Carried phase 37 context decisions (from 37-CONTEXT.md)
- D-01: QTreeView + QAbstractItemModel (NOT QTreeWidget, NOT custom)
- D-05: 3-column horizontal now-playing panel — [logo | info+controls | cover art]
- D-07: Phase 37 control row has ONLY play/pause, stop, volume slider. Star/edit/stream-picker deferred to 38/39.
- D-09: Custom ToastOverlay(QWidget) with QPropertyAnimation, NOT QStatusBar.showMessage
- D-16: YouTube thumbnails via QPixmap.scaled + Qt.KeepAspectRatio in fixed 160×160 slot
- D-19: Reuse musicstreamer/cover_art.py (pure Python, zero changes)
- QA-05: No WA_DeleteOnClose, no self-capturing lambdas, construct/destroy lifetime test in Plan 37-04
</decisions_made>

<blockers>
- None technical. The only action on resume is the state-reconciliation work described in the BLOCKING CONSTRAINTS section.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured snapshot of state at pause time
2. `.planning/phases/37-station-list-now-playing/37-CONTEXT.md` — locked decisions D-01..D-24
3. `.planning/phases/37-station-list-now-playing/37-UI-SPEC.md` — typography, spacing, copywriting (all pinned)
4. `.planning/phases/37-station-list-now-playing/37-02-PLAN.md` — NowPlayingPanel plan
5. `.planning/phases/37-station-list-now-playing/37-03-PLAN.md` — ToastOverlay plan
6. `.planning/phases/37-station-list-now-playing/37-04-PLAN.md` — integration plan
7. `.planning/phases/37-station-list-now-playing/37-01-SUMMARY.md` — Wave 1 result
8. `git show 63b2de3` — the unexpected test commit
9. `git log --oneline 76ecfac..HEAD` — see everything that landed since Plan 37-01 ended

## Infrastructure State

- **Background agent a3db1d65634d99411 (Plan 37-02 executor)** — status unknown at pause time. May still be running. Can be resumed via `SendMessage to: 'a3db1d65634d99411'` or terminated. Prefer reading git log + filesystem state over interrogating the agent.
- **Test suite baseline:** 284 passing as of end of Wave 1 (Plan 37-01). Whatever the 37-02 agent committed is on top of that.
- **Git HEAD at pause:** `63b2de3 test(37-03): add failing tests for ToastOverlay widget`
- **Uncommitted working tree:** unknown at pause — run `git status` on resume.
- **Active branch:** `main`

<context>
The pipeline that led here:
1. Phase 35 Plan 35-06 (drop mpv) — user-requested amendment after the original spike decision was superseded. Landed clean.
2. Phase 35 close-out — 272 tests, 6/6 REQ IDs.
3. Phase 36 discuss/plan/execute/verify auto-chain — landed clean on first pass, 266 tests, 6/6 REQ IDs, 4150 LOC of GTK code deleted.
4. Seed audit — 4 seeds retired as shipped, SEED-007 planted for post-v2.0 EQ/AutoEQ work.
5. Phase 37 discuss-chain — user chose defaults across 5 gray areas ("port v1.5 faithfully").
6. UI-SPEC approved 6/6 first pass.
7. Research + planning + check — 1 blocker (missing VALIDATION.md, created inline) + 3 minor warnings (folded into executor prompts instead of burning a revision cycle).
8. Wave 1 (37-01) shipped clean — 284 tests.
9. Wave 2 started: 37-02 launched in background, 37-03 foreground spawn rejected, /gsd-pause-work invoked.
</context>

<next_action>
1. **FIRST:** `git log --oneline 76ecfac..HEAD` — see everything that landed after Plan 37-01. Do not skip this step.
2. **SECOND:** `git show 63b2de3` — read the unexpected 37-03 test commit. Evaluate: is it legitimate TDD-red tests that Plan 37-03 can later make green? Or scope bleed that should be reverted?
3. **THIRD:** `QT_QPA_PLATFORM=offscreen .venv/bin/pytest -q` — see current test count. Expected floor ~284 (Wave 1 baseline), expected ceiling ~300+ depending on how far 37-02 got.
4. **FOURTH:** If 37-02 is done (`now_playing_panel.py` exists with all specified tests passing), mark UI-02 and UI-14 complete in REQUIREMENTS.md and advance to 37-03. If 37-02 is partial, decide whether to resume the background agent via SendMessage or re-spawn a fresh executor with an update prompt that documents what already landed.
5. **FIFTH:** Launch Plan 37-03 (ToastOverlay) — if the 63b2de3 test file is usable, tell the executor "a failing test file already exists at tests/test_toast_overlay.py; your job is to implement musicstreamer/ui_qt/toast.py such that those tests pass."
6. **SIXTH:** Launch Plan 37-04 (MainWindow integration).
7. **SEVENTH:** gsd-verifier + phase close-out + auto-advance to Phase 38 if the user wants.
</next_action>
</content>
</invoke>
