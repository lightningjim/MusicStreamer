---
phase: 37-station-list-now-playing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/ui_qt/station_tree_model.py
  - musicstreamer/ui_qt/station_list_panel.py
  - musicstreamer/ui_qt/icons/audio-x-generic-symbolic.svg
  - musicstreamer/ui_qt/icons/NOTICE.md
  - musicstreamer/ui_qt/icons.qrc
  - musicstreamer/ui_qt/icons_rc.py
  - tests/test_station_tree_model.py
  - tests/test_station_list_panel.py
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Provider-grouped station tree renders from repo.list_stations()"
    - "Provider rows are not selectable; station rows are selectable"
    - "Recently-played top-3 stations render above the tree"
    - "Per-row 32x32 station logos resolve via QPixmapCache with audio-x-generic-symbolic.svg fallback"
    - "Clicking a station emits station_activated(Station)"
  artifacts:
    - path: "musicstreamer/ui_qt/station_tree_model.py"
      provides: "StationTreeModel(QAbstractItemModel) two-level tree"
    - path: "musicstreamer/ui_qt/station_list_panel.py"
      provides: "StationListPanel widget with Recently Played + QTreeView"
    - path: "musicstreamer/ui_qt/icons/audio-x-generic-symbolic.svg"
      provides: "Adwaita station fallback icon"
    - path: "tests/test_station_tree_model.py"
      provides: "Model row/parent/flags coverage"
  key_links:
    - from: "StationListPanel.tree.clicked"
      to: "station_activated signal"
      via: "_on_activated → model.station_for_index"
      pattern: "station_activated\\.emit"
    - from: "StationTreeModel.data DecorationRole"
      to: "QPixmapCache"
      via: "_icon_for_station"
      pattern: "QPixmapCache\\.(find|insert)"
---

<objective>
Build the Phase 37 station list left panel: a custom QAbstractItemModel that exposes provider-grouped stations (with per-provider count suffix) plus a top-of-panel Recently Played section. This is the first widget plan of Phase 37 — it produces the data-side of the UI loop and a Qt-native click-to-play signal that Plan 37-04 wires to the Player.

Purpose: UI-01 — provider groups, recently-played, per-row logos, click-to-play.
Output: StationTreeModel + StationListPanel modules, the audio-x-generic-symbolic icon registered through icons.qrc, and unit tests covering model semantics + panel signal emission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-station-list-now-playing/37-CONTEXT.md
@.planning/phases/37-station-list-now-playing/37-UI-SPEC.md
@.planning/phases/37-station-list-now-playing/37-RESEARCH.md
@musicstreamer/ui_qt/main_window.py
@musicstreamer/ui_qt/icons.qrc
@musicstreamer/repo.py
@musicstreamer/models.py

<interfaces>
From musicstreamer/models.py:
```python
@dataclass
class Station:
    id: int
    name: str
    provider_name: str | None
    station_art_path: str | None
    last_played_at: str | None
    icy_disabled: bool
    streams: list[StationStream]
```

From musicstreamer/repo.py:
```python
class Repo:
    def list_stations(self) -> list[Station]: ...
    def list_recently_played(self, n: int = 3) -> list[Station]: ...
```

Already-bundled icon resource path: `:/icons/audio-x-generic-symbolic.svg` (added this plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audio-x-generic-symbolic.svg + register through icons.qrc</name>
  <read_first>musicstreamer/ui_qt/icons.qrc, musicstreamer/ui_qt/icons/LICENSE</read_first>
  <files>musicstreamer/ui_qt/icons/audio-x-generic-symbolic.svg, musicstreamer/ui_qt/icons/NOTICE.md, musicstreamer/ui_qt/icons.qrc, musicstreamer/ui_qt/icons_rc.py</files>
  <action>
1. Download or copy the verbatim Adwaita symbolic SVG `audio-x-generic-symbolic.svg` (CC-BY-SA 3.0, GNOME Adwaita Icon Theme) into `musicstreamer/ui_qt/icons/audio-x-generic-symbolic.svg`. Source: https://gitlab.gnome.org/GNOME/adwaita-icon-theme/-/raw/master/Adwaita/symbolic/mimetypes/audio-x-generic-symbolic.svg
2. Create `musicstreamer/ui_qt/icons/NOTICE.md` with the CC-BY-SA 3.0 attribution for the bundled Adwaita symbolic icons. List ALL bundled icons by name (current: app-icon.svg, open-menu-symbolic.svg, audio-x-generic-symbolic.svg) and credit "GNOME Adwaita Icon Theme — CC-BY-SA 3.0 — https://gitlab.gnome.org/GNOME/adwaita-icon-theme". This is the file Plan 37-02 will append three more entries to.
3. Update `musicstreamer/ui_qt/icons.qrc` to add `<file alias="audio-x-generic-symbolic.svg">icons/audio-x-generic-symbolic.svg</file>` inside the `<qresource prefix="/icons">` block (preserve the existing alias attribute pattern from Phase 36 D-01).
4. Regenerate `musicstreamer/ui_qt/icons_rc.py` via `.venv/bin/pyside6-rcc musicstreamer/ui_qt/icons.qrc -o musicstreamer/ui_qt/icons_rc.py`.
  </action>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen .venv/bin/python -c "from musicstreamer.ui_qt import icons_rc; from PySide6.QtGui import QIcon; assert not QIcon(':/icons/audio-x-generic-symbolic.svg').isNull(), 'fallback icon failed to load from resource'"</automated>
  </verify>
  <acceptance_criteria>
- `grep -q 'audio-x-generic-symbolic.svg' musicstreamer/ui_qt/icons.qrc`
- `grep -q 'CC-BY-SA 3.0' musicstreamer/ui_qt/icons/NOTICE.md`
- `grep -q 'audio-x-generic-symbolic' musicstreamer/ui_qt/icons/NOTICE.md`
- `test -f musicstreamer/ui_qt/icons/audio-x-generic-symbolic.svg`
- `grep -q 'audio_x_generic_symbolic' musicstreamer/ui_qt/icons_rc.py` (rcc-mangled name)
  </acceptance_criteria>
  <done>QIcon(":/icons/audio-x-generic-symbolic.svg") returns a non-null icon at runtime; NOTICE.md lists all bundled icons under CC-BY-SA 3.0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StationTreeModel + tests</name>
  <read_first>musicstreamer/models.py, musicstreamer/repo.py, .planning/phases/37-station-list-now-playing/37-RESEARCH.md (Code Examples §1)</read_first>
  <files>musicstreamer/ui_qt/station_tree_model.py, tests/test_station_tree_model.py</files>
  <behavior>
- Test 1: Empty stations list → rowCount(QModelIndex()) == 0
- Test 2: Three stations across two providers → rowCount(root) == 2 provider rows; each provider's rowCount equals its station count
- Test 3: Provider node label includes count suffix `(N)` (e.g. "SomaFM (2)")
- Test 4: Top-level provider rows: `model.parent(model.index(0, 0)).isValid() is False` (Pitfall §4 — prevents infinite recursion)
- Test 5: `flags()` for a station index includes `Qt.ItemIsSelectable`; `flags()` for a provider index does NOT include `Qt.ItemIsSelectable` (only `Qt.ItemIsEnabled`)
- Test 6: `data(station_index, Qt.DecorationRole)` returns a non-null `QIcon`; `data(station_index, Qt.DisplayRole)` returns the station name
- Test 7: `station_for_index(station_index)` returns the underlying `Station`; `station_for_index(provider_index)` returns `None`
- Test 8: `columnCount() == 1`
- Test 9: `_icon_for_station` falls back to `audio-x-generic-symbolic.svg` resource when `station.station_art_path` is None — returned QIcon must be non-null
  </behavior>
  <action>
Create `musicstreamer/ui_qt/station_tree_model.py` implementing `StationTreeModel(QAbstractItemModel)` per RESEARCH.md Code Examples §1 verbatim. Required surface:

- `_TreeNode` dataclass with fields `kind` ("root"|"provider"|"station"), `label`, `parent`, `children`, `station`
- `StationTreeModel.__init__(stations: list[Station], parent=None)`
- `FALLBACK_ICON = ":/icons/audio-x-generic-symbolic.svg"` class constant
- `refresh(stations)` using `beginResetModel()` / `endResetModel()`
- `_populate(stations)` groups by `station.provider_name or "Ungrouped"`, appends `(N)` suffix to each provider label per D-04
- `station_for_index(index) -> Station | None`
- Overrides: `columnCount` (always 1), `rowCount`, `index`, `parent`, `flags`, `data`
- `flags()`: station rows return `Qt.ItemIsEnabled | Qt.ItemIsSelectable`; provider rows return `Qt.ItemIsEnabled` only (D-03 — provider headers expand/collapse but never trigger play)
- `data()` roles:
  - `Qt.DisplayRole` → node.label
  - `Qt.DecorationRole` (station only) → `_icon_for_station(node.station)`
  - `Qt.FontRole` (provider only) → `QFont` with `setBold(True)` and `setPointSize(13)` (UI-SPEC: 13pt DemiBold for provider group headers)
- `_icon_for_station(station)`:
  - Use `QPixmapCache.find(key, pix)` two-arg form (Pitfall §5)
  - Key format: `f"station-logo:{path}"`
  - On miss: `QPixmap(path)`; if `pix.isNull()` substitute `QPixmap(self.FALLBACK_ICON)`; scale to 32x32 with `Qt.KeepAspectRatio | Qt.SmoothTransformation`; insert into cache
  - Return `QIcon(pix)`

Write `tests/test_station_tree_model.py` covering all 9 behaviors above. Use a tiny `make_station(id, name, provider, art=None)` helper that constructs `Station` instances with empty `streams=[]`. Ensure `from musicstreamer.ui_qt import icons_rc` is imported at module top so the resource prefix is registered before icon lookups in tests.
  </action>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen .venv/bin/pytest tests/test_station_tree_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `grep -q 'class StationTreeModel' musicstreamer/ui_qt/station_tree_model.py`
- `grep -q 'FALLBACK_ICON.*audio-x-generic-symbolic' musicstreamer/ui_qt/station_tree_model.py`
- `grep -q 'QPixmapCache' musicstreamer/ui_qt/station_tree_model.py`
- `grep -q 'Qt.ItemIsSelectable' musicstreamer/ui_qt/station_tree_model.py`
- `grep -q 'beginResetModel' musicstreamer/ui_qt/station_tree_model.py`
- `grep -q 'station_for_index' tests/test_station_tree_model.py`
- `grep -q 'parent.*isValid.*False' tests/test_station_tree_model.py` (Pitfall §4 explicit assertion)
  </acceptance_criteria>
  <done>All 9 model tests pass under offscreen Qt; provider rows are non-selectable; top-level parent() returns invalid QModelIndex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StationListPanel (Recently Played + tree) + tests</name>
  <read_first>musicstreamer/ui_qt/station_tree_model.py, .planning/phases/37-station-list-now-playing/37-RESEARCH.md (Code Examples §2, §4), .planning/phases/37-station-list-now-playing/37-UI-SPEC.md (Layout Contracts → Recently-Played Section)</read_first>
  <files>musicstreamer/ui_qt/station_list_panel.py, tests/test_station_list_panel.py</files>
  <behavior>
- Test 1: Construct StationListPanel with a fake repo (returns 3 stations, 2 providers); panel.minimumWidth() == 280
- Test 2: panel exposes `station_activated = Signal(Station)`
- Test 3: panel has child `tree` (QTreeView) and child `recent_view` (QListView), separated by a QFrame.HLine
- Test 4: tree.headerHidden is True, tree.rootIsDecorated is False, tree.iconSize == QSize(32, 32), tree.indentation == 16
- Test 5: tree.selectionMode == QAbstractItemView.SingleSelection
- Test 6: All provider groups are expanded after construction (`tree.isExpanded(model.index(0, 0))` is True)
- Test 7: Triggering `tree.clicked.emit(station_index)` programmatically emits `station_activated` exactly once with the correct Station instance (use `qtbot.waitSignal`)
- Test 8: Triggering `tree.clicked.emit(provider_index)` does NOT emit `station_activated` (provider rows have no Station payload)
- Test 9: Recently Played view populates 3 rows when `repo.list_recently_played(3)` returns 3 stations; clicking a recently-played row also emits `station_activated`
  </behavior>
  <action>
Create `musicstreamer/ui_qt/station_list_panel.py` implementing `StationListPanel(QWidget)` per RESEARCH.md Code Examples §2 + §4. Required surface:

- Class signal: `station_activated = Signal(Station)` (use `from musicstreamer.models import Station`)
- `__init__(self, repo: Repo, parent: QWidget | None = None)`:
  - `self.setMinimumWidth(280)` (UI-SPEC component inventory)
  - Outer `QVBoxLayout`, `setContentsMargins(0, 8, 0, 0)`, `setSpacing(0)`
  - Recently Played header: `QLabel("Recently Played")` with `setContentsMargins(16, 0, 16, 4)`. Apply 9pt Normal QFont per UI-SPEC Typography (Label role).
  - `RecentlyPlayedView`: a `QListView` backed by a `QStandardItemModel`. Populate from `repo.list_recently_played(3)`. Each row: 32x32 station logo (use the same `_icon_for_station` helper logic — extract to a small `_load_station_icon(station)` module-level helper, or duplicate the QPixmapCache idiom inline; prefer extraction). Set `view.setIconSize(QSize(32, 32))`. Set the item's `Qt.UserRole` data to the `Station` instance for click retrieval. Set view max height to 160px per UI-SPEC (`view.setMaximumHeight(160)`). Use `view.clicked.connect(self._on_recent_clicked)`.
  - Separator: `QFrame()` with `setFrameShape(QFrame.HLine)`, `setFrameShadow(QFrame.Sunken)`
  - Main tree: `QTreeView()` with `StationTreeModel(self._repo.list_stations())` as model. Configure exactly per RESEARCH.md §2:
    - `setHeaderHidden(True)`
    - `setRootIsDecorated(False)`
    - `setIndentation(16)`
    - `setUniformRowHeights(True)`
    - `setIconSize(QSize(32, 32))`
    - `setSelectionBehavior(QAbstractItemView.SelectRows)`
    - `setSelectionMode(QAbstractItemView.SingleSelection)`
    - `expandAll()`
    - `clicked.connect(self._on_tree_activated)` (BOUND METHOD — not lambda — per Pitfall §2 / QA-05)
    - `doubleClicked.connect(self._on_tree_activated)`
    - Add to layout with `stretch=1`
- `_on_tree_activated(self, index)`: resolve `station = self.model.station_for_index(index)`; if not None, emit `self.station_activated.emit(station)`
- `_on_recent_clicked(self, index)`: pull `station = index.data(Qt.UserRole)`; if not None, emit `self.station_activated.emit(station)`

QA-05 note: prefer bound methods for ALL signal connections in this file. Do NOT capture `self` in lambdas.

Write `tests/test_station_list_panel.py` covering all 9 behaviors above. Build a tiny `FakeRepo` with `list_stations()` and `list_recently_played(n)` methods returning hand-built Station lists. Use `qtbot.waitSignal(panel.station_activated, timeout=500)` to assert click-to-play emission.
  </action>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen .venv/bin/pytest tests/test_station_list_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `grep -q 'class StationListPanel' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'station_activated = Signal(Station)' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'setMinimumWidth(280)' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'setIconSize(QSize(32, 32))' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'setIndentation(16)' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'expandAll' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'list_recently_played(3)' musicstreamer/ui_qt/station_list_panel.py`
- `grep -q 'QFrame.HLine' musicstreamer/ui_qt/station_list_panel.py`
- `grep -nq 'lambda.*self' musicstreamer/ui_qt/station_list_panel.py && exit 1 || exit 0` (QA-05: no self-capturing lambdas)
- `grep -q 'waitSignal.*station_activated' tests/test_station_list_panel.py`
  </acceptance_criteria>
  <done>StationListPanel renders the recently-played section + provider tree + click-to-play signal with all bound-method connections; 9 panel tests pass.</done>
</task>

</tasks>

<verification>
- `QT_QPA_PLATFORM=offscreen .venv/bin/pytest tests/test_station_tree_model.py tests/test_station_list_panel.py -q` — all green
- `QT_QPA_PLATFORM=offscreen .venv/bin/pytest -q` — full suite ≥ 266 baseline + new model + panel tests
- New audio-x-generic-symbolic.svg loads from `:/icons/` resource
</verification>

<success_criteria>
- StationTreeModel correctly groups stations by provider with `(N)` count suffix
- Provider rows are not selectable; station rows are
- StationListPanel exposes `station_activated(Station)` signal that fires on click of either tree row or recently-played row
- audio-x-generic-symbolic.svg ships in icons.qrc and NOTICE.md
- Zero self-capturing lambdas in station_list_panel.py (QA-05)
</success_criteria>

<output>
After completion, create `.planning/phases/37-station-list-now-playing/37-01-SUMMARY.md`
</output>
