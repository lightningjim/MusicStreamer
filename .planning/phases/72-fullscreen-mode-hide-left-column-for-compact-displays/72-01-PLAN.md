---
phase: 72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase72_assumptions.py
autonomous: true
requirements:
  - LAYOUT-01
must_haves:
  truths:
    - "Calling station_panel.hide() on the live QSplitter auto-hides the adjacent handle in the project's PySide6 version (RESEARCH A1)"
    - "Reparenting StationListPanel into a QFrame and back into the QSplitter at index 0 preserves filter/search/scroll state with no crash (RESEARCH A2)"
    - "Both Wave 0 assumptions are pinned by automated tests so a future Qt or pattern regression is caught"
  artifacts:
    - path: "tests/test_phase72_assumptions.py"
      provides: "Wave 0 spike tests for A1 (handle auto-hide) and A2 (reparent safety)"
      contains: "def test_splitter_handle_autohides_when_child_hidden"
  key_links:
    - from: "tests/test_phase72_assumptions.py"
      to: "musicstreamer.ui_qt.main_window.MainWindow"
      via: "pytest-qt qtbot.addWidget(MainWindow(fake_player, fake_repo))"
      pattern: "MainWindow\\(fake_player"
---

<objective>
Wave 0 spike: lock the two RESEARCH assumptions (A1, A2) with automated pytest-qt tests
BEFORE any production code changes for Phase 72. These tests prove the load-bearing Qt
behaviors the rest of the phase depends on and become regression locks for future
PySide6 upgrades.

Purpose: Phase 72 RESEARCH §Assumptions Log marks A1 (QSplitter auto-hides handle when
a child is hidden) as LOW risk but explicitly silent in Qt 6 docs, and A2 (StationListPanel
has no parent-assumption code that would break under reparenting) as MEDIUM risk based on
a grep audit alone. Both must be verified by spike tests before Plans 02–05 commit code
that depends on these behaviors.

Output: tests/test_phase72_assumptions.py with 2 passing automated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-CONTEXT.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-RESEARCH.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-PATTERNS.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tests/test_main_window_integration.py (existing FakeRepo + fixture pattern):

```python
class FakeRepo:
    def __init__(self, stations=None, recent=None, ...):
        self._settings: dict[str, str] = {}
    def get_setting(self, key: str, default: str = "") -> str: ...
    def set_setting(self, key: str, value: str) -> None: ...
```

From musicstreamer/ui_qt/main_window.py (lines 269-287):

```python
self._splitter = QSplitter(Qt.Horizontal, self)
self._splitter.setChildrenCollapsible(False)
self.station_panel = StationListPanel(repo, parent=self._splitter)
self.station_panel.setMinimumWidth(280)
self.now_playing = NowPlayingPanel(player, repo, parent=self._splitter)
self.now_playing.setMinimumWidth(560)
self._splitter.addWidget(self.station_panel)
self._splitter.addWidget(self.now_playing)
self._splitter.setSizes([360, 840])
self.setCentralWidget(self._splitter)
```

QSplitter.handle(int) returns a QSplitterHandle widget. handle(0) is the leading edge
(not interactive); handle(1) is the divider between widget 0 and widget 1.

From tests/conftest.py: QT_QPA_PLATFORM=offscreen is set at module import time.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 spike test A1 — QSplitter auto-hides adjacent handle when child is hidden</name>
  <files>tests/test_phase72_assumptions.py</files>
  <read_first>
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-RESEARCH.md (§Assumptions Log A1; §Pattern 1; §Pitfall 1)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-VALIDATION.md (Wave 0 Requirements; Sampling Rate)
    - tests/test_main_window_integration.py (lines 80-220 for FakeRepo + MainWindow fixture pattern, lines 1186-1215 for toggle-test shape)
    - tests/conftest.py (QT_QPA_PLATFORM=offscreen setup)
    - musicstreamer/ui_qt/main_window.py (lines 269-310 — splitter construction)
  </read_first>
  <behavior>
    - Construct a MainWindow with FakeRepo + FakePlayer fixtures (offscreen).
    - Capture initial sizes via main_window._splitter.sizes() — expect both children to have non-zero width.
    - Verify main_window._splitter.handle(1).isVisible() is True before any hide call.
    - Call main_window.station_panel.hide() directly (no through-the-button call yet — production toggle slot does not exist).
    - Assert main_window.station_panel.isHidden() is True.
    - Assert main_window._splitter.handle(1).isVisible() is False — A1 confirmed (Qt auto-hides handle when adjacent child is hidden).
    - On the failure path, the assertion message MUST cite RESEARCH A1 and quote the forum source so the next maintainer understands the regression scope.
  </behavior>
  <action>
    Create tests/test_phase72_assumptions.py with `from __future__ import annotations` and the standard pytest-qt header. Import FakeRepo and FakePlayer from tests/test_main_window_integration.py (or copy minimal definitions if cross-test import is not the project pattern — verify via existing imports in tests/ before deciding). Add module-level fixtures fake_player and fake_repo. Define test_splitter_handle_autohides_when_child_hidden(qtbot, fake_player, fake_repo) that constructs MainWindow, calls qtbot.addWidget(w), waits with qtbot.waitExposed(w), captures handle(1).isVisible() pre-hide, calls w.station_panel.hide(), then asserts handle(1).isVisible() is False. Include an explanatory docstring referencing RESEARCH A1 and the forum.qt.io/topic/45377 citation. Per QA-05, no lambdas. Per CONVENTIONS.md, snake_case test name + plain docstring on first line + thread/context details in subsequent lines.
  </action>
  <verify>
    <automated>pytest tests/test_phase72_assumptions.py::test_splitter_handle_autohides_when_child_hidden -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_phase72_assumptions.py exists and is importable.
    - Test passes against current PySide6 version on dev box (offscreen platform).
    - On test failure, the assertion message references RESEARCH A1.
    - grep gate: `grep -E '^\s*(lambda|QShortcut|setShortcut|QKeySequence)' tests/test_phase72_assumptions.py` returns zero matches (no lambda usage; spike does not register a shortcut).
    - Source assertion: `grep -c "station_panel.hide()" tests/test_phase72_assumptions.py` returns ≥ 1.
  </acceptance_criteria>
  <done>
    pytest exits 0 for test_splitter_handle_autohides_when_child_hidden; the test acts as a permanent lock so a future PySide6 upgrade that removes auto-hide behavior fails this test immediately.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 spike test A2 — StationListPanel reparent round-trip preserves state with no crash</name>
  <files>tests/test_phase72_assumptions.py</files>
  <read_first>
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-RESEARCH.md (§Assumptions Log A2; §Pattern 4 reparent; §Pitfall 6 insertWidget vs addWidget)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-PATTERNS.md (§Pattern Assignments §3 reparent class skeleton)
    - musicstreamer/ui_qt/station_list_panel.py (verify zero self.parent() / self.window() / topLevelWidget() calls — re-confirm RESEARCH audit)
    - tests/test_main_window_integration.py (any existing test that interacts with station_panel filter/search APIs — find via grep for station_panel.search or station_panel.filter)
  </read_first>
  <behavior>
    - Construct MainWindow + qtbot.addWidget(w).
    - If StationListPanel exposes a search/filter API (verify via grep on station_list_panel.py public methods before writing test), set a non-default state on the panel — e.g., set a search query or click a filter chip — and capture the state.
    - Construct a bare QFrame container parented to w.centralWidget() (mirroring the StationListPeekOverlay parent strategy from PATTERNS §3).
    - Reparent station_panel INTO the QFrame using a QVBoxLayout: container_layout.addWidget(station_panel). This implicitly setParent's the panel into the container.
    - Assert station_panel.parent() is the container (panel successfully reparented).
    - Reparent station_panel BACK into the splitter via w._splitter.insertWidget(0, w.station_panel) — Pitfall 6 critical: insertWidget(0,…), NOT addWidget which appends at end and swaps visual order.
    - Assert w._splitter.indexOf(w.station_panel) == 0 (still left child).
    - Assert the previously-set state survives the round-trip (search query / filter chip selection is intact).
    - No exceptions raised during the round-trip.
    - If StationListPanel exposes no introspectable filter/search state, fall back to asserting (a) panel reparent did not crash, (b) panel.parent() is correct after each step, and (c) panel is still alive and responsive (panel.isWidgetType() True). Document the fallback in the test docstring.
  </behavior>
  <action>
    Add test_station_panel_reparent_round_trip_preserves_state(qtbot, fake_player, fake_repo) to the same tests/test_phase72_assumptions.py file. Build a small QFrame + QVBoxLayout container parented to w.centralWidget(). Capture station_panel state (search/filter or fallback to identity check). Reparent station_panel into the container via layout.addWidget. Reparent back via w._splitter.insertWidget(0, w.station_panel). Assert indexOf returns 0, parent() chain is correct, and state survives. Docstring quotes RESEARCH A2 + Pitfall 6 + the doc.qt.io/qt-6/qsplitter.html source. Use QVBoxLayout setContentsMargins(0, 0, 0, 0) to mirror the StationListPeekOverlay PATTERNS §3 layout config. Per QA-05, no lambdas.
  </action>
  <verify>
    <automated>pytest tests/test_phase72_assumptions.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_station_panel_reparent_round_trip_preserves_state passes on dev box.
    - Test exercises BOTH the into-container reparent AND the back-to-splitter insertWidget(0, …) reparent (key Pitfall 6 lock).
    - grep gate: `grep -c "insertWidget(0" tests/test_phase72_assumptions.py` returns ≥ 1 (proves Pitfall 6 path is exercised).
    - grep gate: `grep -E "addWidget\(.*station_panel" tests/test_phase72_assumptions.py` returns matches ONLY inside the into-container step (not in the back-to-splitter step). The back-to-splitter step uses insertWidget(0, …).
    - No new pytest warnings introduced (cmp `pytest -W error tests/test_phase72_assumptions.py`).
  </acceptance_criteria>
  <done>
    Both Wave 0 spike tests pass; A1 + A2 are now regression-locked. Plans 02–05 may safely depend on these behaviors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | No new I/O, network, file, IPC, or auth surface — pure intra-process Qt widget state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | (n/a) | Wave 0 spike tests | accept | No new attack surface — tests run offscreen, exercise existing Qt widgets only. |

Phase 72 introduces no new attack surface (UI-state-only; no new I/O, no new auth, no new external surfaces). Per RESEARCH §Security Domain, V2–V6 do not apply; V5 keyboard input is constrained to QKeySequence(bool) payload.
</threat_model>

<verification>
After both tasks complete:
- `pytest tests/test_phase72_assumptions.py -x -v` exits 0
- `pytest tests/` full suite still green (no regression from new test file)
- grep gate (negative): `grep -E '^\s*(lambda |QShortcut|setShortcut|QKeySequence|set_setting\("compact)' tests/test_phase72_assumptions.py` returns zero matches (no lambdas, no premature production shortcut/setting writes in spike tests)
</verification>

<success_criteria>
- A1 spike test green: QSplitter.handle(1).isVisible() flips False after station_panel.hide() — locked.
- A2 spike test green: station_panel round-trips between QFrame container and splitter index 0 with no crash and no state loss — locked.
- Plans 02–05 may safely depend on A1 (no explicit handle.hide() call needed) and A2 (reparent is the recommended pattern, not two instances).
</success_criteria>

<output>
After completion, create `.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-01-SUMMARY.md` documenting:
- A1 result: handle auto-hide observed in PySide6 X.Y.Z (capture actual version from `python -c "import PySide6; print(PySide6.__version__)"`)
- A2 result: round-trip behavior + any unexpected state-preservation findings
- Any deltas vs RESEARCH expectations
</output>
