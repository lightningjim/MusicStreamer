---
phase: 999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02
plan: 01
subsystem: testing
tags: [cookies, tempfile, yt-dlp, contextmanager, testing, tdd, python]

# Dependency graph
requires:
  - phase: v1.5/23-fix-youtube-stream-playback-broken-on-cli-and-app
    provides: "Original FIX-02 temp-copy pattern (mkstemp + shutil.copy2 + unlink) on the deprecated mpv subprocess path"
  - phase: 35-audio-pipeline-youtube-library-api
    provides: "yt_dlp library-API read sites (yt_import.scan_playlist + Player._youtube_resolve_worker) where FIX-02 was lost"
provides:
  - "musicstreamer/cookie_utils.py shared helper module (is_cookie_file_corrupted + temp_cookies_copy @contextmanager)"
  - "FakeYDLSaveCookies test fixture simulating yt-dlp save_cookies() side effect on __exit__"
  - "9 new tests in tests/test_cookies.py: 2 green (helper), 7 RED awaiting Plans 02/03"
affects:
  - "999.7-02 (yt_import wiring — turns 4 RED tests green)"
  - "999.7-03 (Player wiring + cookies_cleared Signal — turns 3 RED tests green)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "@contextlib.contextmanager with try/finally unlink for per-call tempfile copies (cross-platform via mkstemp, Windows-safe for v2.0)"
    - "Forgive-cleanup posture for cookie predicates: swallow OSError, return safe default (mirrors constants.clear_cookies)"
    - "Fake-class test fixtures that mimic real library side effects (save_cookies on __exit__) to assert byte-equality protection"

key-files:
  created:
    - "musicstreamer/cookie_utils.py"
  modified:
    - "tests/test_cookies.py"

key-decisions:
  - "Helper lives at package root (musicstreamer/cookie_utils.py), not inside a sub-package — minimal import overhead, mirrors constants.py placement"
  - "Corruption predicate reads first 512 bytes (not full file) and scans first 4 non-blank lines for marker substring — O(1) memory, advisory-only"
  - "temp_cookies_copy yields None (not raises) on absent canonical or copy2 OSError — callers fallback to no-cookies operation per D-06"
  - "FakeYDLSaveCookies writes marker header on __exit__ (not __init__) — matches real yt_dlp.YoutubeDL.__exit__ calling save_cookies()"

patterns-established:
  - "Pattern: shared helper module for cross-call-site infrastructure (cookie_utils.py will be imported by both yt_import and player in Plans 02/03)"
  - "Pattern: RED tests that specify behavior contracts across multiple future plans (this plan writes 9 tests; Plans 02 and 03 each turn a subset green)"

requirements-completed: [FIX-02-a, FIX-02-b, FIX-02-c, FIX-02-d, FIX-02-e, NEW-a, NEW-b, NEW-c, NEW-d]

# Metrics
duration: ~6min
completed: 2026-04-24
---

# Phase 999.7 Plan 01: cookie_utils helper + 9 failing tests Summary

**Shared cookie_utils.py helper (is_cookie_file_corrupted + temp_cookies_copy @contextmanager) and 9 RED tests that drive FIX-02 restoration and corruption auto-recovery in Plans 02/03.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-24T19:30:00Z
- **Completed:** 2026-04-24T19:36:01Z
- **Tasks:** 1
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- Shipped `musicstreamer/cookie_utils.py` as a fully implemented shared helper — no stubs. `is_cookie_file_corrupted(path)` detects the yt-dlp save header in first 4 lines; `temp_cookies_copy()` @contextmanager provides per-call tempfile copies with guaranteed unlink in finally.
- Added `FakeYDLSaveCookies` fixture class that writes the yt-dlp marker header into `opts['cookiefile']` on `__exit__` — provides a real side effect so byte-equality tests can prove the canonical cookies.txt is NOT the file being overwritten.
- Wrote 9 new tests in `tests/test_cookies.py` covering FIX-02-a..e (temp-copy, byte-unchanged, unlink, copy-failure fallback) + NEW-a..d (corruption predicate + auto-clear at both read sites). 2 pass immediately (helper correctness), 7 fail RED (awaiting Plans 02/03).
- All 8 pre-existing cookie tests continue to pass unchanged.

## Task Commits

1. **Task 1: RED — Create cookie_utils.py + 9 failing tests** — `a974e5f` (test)

## Files Created/Modified

- `musicstreamer/cookie_utils.py` (created, 82 lines) — `_YTDLP_MARKER` constant, `is_cookie_file_corrupted(path)` predicate, `temp_cookies_copy()` @contextmanager using tempfile.mkstemp + shutil.copy2 + os.unlink in try/finally.
- `tests/test_cookies.py` (modified, +243 lines) — added `_tempfile` import, `FakeYDLSaveCookies` shared fixture, 9 new test functions in two sectioned blocks (temp-copy protection, corruption detection + auto-clear).

## Test State (RED/GREEN breakdown)

**Green (helper is self-contained) — 2 tests:**
- `test_corruption_marker_detected` (NEW-a) ✓
- `test_clean_netscape_not_corrupted` (NEW-b) ✓

**RED — awaiting Plan 02 (yt_import wiring) — 4 tests:**
- `test_ytdlp_uses_temp_cookie_copy` (FIX-02-a) ✗ — canonical path still passed to yt-dlp
- `test_canonical_cookies_unchanged_by_ytdlp` (FIX-02-b) ✗ — marker written into canonical
- `test_temp_cookie_unlinked_after_call` (FIX-02-d) ✗ — not using temp_cookies_copy
- `test_copy_failure_omits_cookiefile` (FIX-02-e) ✗ — copy2 fallback not wired
- `test_scan_playlist_auto_clears_corrupted` (NEW-c) ✗ — `toast_callback` kwarg not yet added

**RED — awaiting Plan 03 (Player wiring + Signal) — 2 tests:**
- `test_youtube_resolve_uses_temp_cookie_copy` (FIX-02-c) ✗ — Player still uses canonical path
- `test_youtube_resolve_auto_clears_corrupted` (NEW-d) ✗ — `Player.cookies_cleared` Signal not yet defined

**Pre-existing tests (still green) — 8 tests:**
- `test_cookie_path_resolves_under_root`
- `test_cookie_path_constant_via_constants_getattr`
- `test_ytdlp_passes_cookiefile_when_file_exists`
- `test_ytdlp_omits_cookiefile_when_absent`
- `test_youtube_resolve_passes_cookiefile_when_present`
- `test_youtube_resolve_omits_cookiefile_when_absent`
- `test_clear_removes_cookies_file`
- `test_clear_returns_false_when_absent`

Final pytest result: **10 passed, 7 failed** — exactly matching the plan's success_criteria.

## TDD Gate Compliance

This plan is a pure RED-gate plan (`type: tdd`, wave 1). The commit `a974e5f` is the `test(...)` RED commit. Plans 999.7-02 and 999.7-03 provide the `feat(...)` GREEN commits that turn the 7 RED tests green at their respective read sites.

No refactor commit is needed in this plan — the helper module was implemented correctly on the first pass and verified by the 2 helper-only tests.

## Decisions Made

- Followed the plan's `<action>` section verbatim — both the `cookie_utils.py` module contents and the 9 test bodies are exact copies of the plan specification. No deviations.
- Kept corruption detection as a pure substring scan (no parsing) — predicate is advisory (triggers deletion, not code branching), so trust-boundary threats are limited to the canonical file the app already owns.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. All acceptance criteria passed on first run:
- File `musicstreamer/cookie_utils.py` contains all required signatures and idioms (`is_cookie_file_corrupted`, `@contextlib.contextmanager`, `temp_cookies_copy`, `tempfile.mkstemp(prefix="ms-cookies-", suffix=".txt")`, `shutil.copy2(canonical, tmp_path)`, `os.unlink(tmp_path)`, `_YTDLP_MARKER = "generated by yt-dlp"`).
- `tests/test_cookies.py` contains `FakeYDLSaveCookies` and all 9 new test function signatures.
- Helper-only tests pass in isolation: `uv run --with pytest pytest tests/test_cookies.py::test_corruption_marker_detected -x` exits 0; same for `test_clean_netscape_not_corrupted`.
- Plan-02-dependent test `test_ytdlp_uses_temp_cookie_copy` fails RED as required.
- 8 pre-existing tests continue to pass.

## User Setup Required

None — this plan adds internal helper code and tests only.

## Next Phase Readiness

- Plan 999.7-02 can now import `from musicstreamer.cookie_utils import is_cookie_file_corrupted, temp_cookies_copy` in `musicstreamer/yt_import.py`, wrap `scan_playlist`'s yt_dlp call with the context manager, and add the corruption check + `toast_callback` kwarg. Expected to turn 5 RED tests green (FIX-02-a, FIX-02-b, FIX-02-d, FIX-02-e, NEW-c).
- Plan 999.7-03 can wire the same helper into `musicstreamer/player.py::_youtube_resolve_worker` and add the `cookies_cleared` Signal. Expected to turn 2 RED tests green (FIX-02-c, NEW-d).
- After both plans land: all 9 new tests green, all 8 pre-existing tests green, FIX-02 restored + corruption auto-recovery live at both yt-dlp library-API read sites.

## Self-Check: PASSED

- FOUND: /home/kcreasey/OneDrive/Projects/MusicStreamer/musicstreamer/cookie_utils.py
- FOUND: /home/kcreasey/OneDrive/Projects/MusicStreamer/tests/test_cookies.py (modified)
- FOUND commit: a974e5f

---
*Phase: 999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02*
*Completed: 2026-04-24*
