---
phase: 83-at-start-of-playing-a-station-randomly-select-and-play-one-o
plan: 03
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - musicstreamer/player.py
  - tests/test_player.py
autonomous: true
requirements: []
tags: [phase-83, player, somafm, preroll, about-to-finish, playbin3, queued-signal, drift-guard, qt-glib-threading]
must_haves:
  truths:
    - "D-11: `Player.play(station)` consults `station_prerolls` ONLY when `station.provider_name == 'SomaFM'` (CamelCase literal; per Phase 74 D-02); non-SomaFM stations bypass the preroll codepath even if `station.prerolls` has rows (synthetic)"
    - "D-12: a global per-Player `_last_preroll_played_at: Optional[float]` (time.monotonic) gates the preroll. When the throttle window (>10 min) has NOT elapsed, preroll is suppressed and play proceeds directly to `_try_next_stream`. Timestamp is updated at preroll START, not handoff"
    - "D-05: when preroll fires, playbin3 URI is set to the preroll URL via the existing `_set_uri` path; a one-shot handler is connected to playbin3's `about-to-finish` signal; `_streams_queue` is built (Phase 82 head-of-queue logic intact) but `_try_next_stream` is NOT called yet — the about-to-finish slot calls it"
    - "D-06: `_streams_queue` content equals what Phase 82 produced (preferred-first + order_streams) — preroll URLs NEVER enter the queue"
    - "D-05 + qt-glib-bus-threading Rule 2: the streaming-thread `about-to-finish` callback ONLY emits a queued Signal; the main-thread slot does the `disconnect(handler_id)` + flag clear + `_try_next_stream()` call. New class-level Signal `_preroll_about_to_finish_requested = Signal()` wired with `Qt.ConnectionType.QueuedConnection` in `__init__`"
    - "D-07: while `_preroll_in_flight is True`, `_on_gst_tag` early-returns BEFORE emitting `title_changed`; the m4a title tag is suppressed (Now Playing keeps showing the station name through the preroll)"
    - "D-09: if the preroll bus errors before about-to-finish fires, `_handle_gst_error_recovery` (guarded by `_preroll_in_flight` at the top of its body) disconnects the preroll handler, clears the flag, and routes to `_try_next_stream()` — NO retry of a different preroll"
    - "D-03 + D-13: when a SomaFM station has 0 stored prerolls AND `prerolls_fetched_at IS NULL`, a daemon backfill thread is spawned (single-flight via `self._backfill_in_flight: set[int]`); play proceeds straight to the stream WITHOUT waiting on the fetch"
    - "D-04: when a SomaFM station has 0 stored prerolls AND `prerolls_fetched_at IS NOT NULL`, NO backfill is scheduled and play proceeds straight to the stream (the fetch happened earlier and returned empty — legitimately-empty channels like Seven Inch Soul)"
    - "D-08: Pause / Stop / Resume during a preroll behave exactly as today — `Player.pause` / `Player.stop` set pipeline state without consulting `_preroll_in_flight`; pausing during a preroll does NOT clear the flag and does NOT invoke `_try_next_stream`"
    - "D-10: failover via the existing `_streams_queue` advance path is preserved after preroll handoff — once `_on_preroll_about_to_finish` runs and the station stream is playing, a subsequent bus error advances to `_streams_queue[1]` exactly as today; the preroll handler does NOT regress Phase 82 failover"
    - "Edge case (live-spike Q3 RESOLVED): a malformed preroll (e.g. 0-byte response) that reaches EOS without firing about-to-finish is bridged by a `message::eos` bus handler `_on_gst_eos_during_preroll`; when `_preroll_in_flight is True`, the handler emits the existing `_try_next_stream_requested` queued Signal (same fall-through path D-09 uses for bus errors); when False, the handler returns immediately and does not alter today's EOS semantics"
    - "Backfill worker uses thread-local `Repo(db_connect())` (Pattern 4); never shares Player's repo handle (Player has no repo handle today per player.py:422 Pitfall 9 comment)"
    - "Single-flight backfill guard (T-83-10): `self._backfill_in_flight: set[int]` tracks station_ids whose backfill is in flight; second concurrent click on the same station does NOT spawn a duplicate thread"
    - "D-14 source-grep drift-guard: a pytest reads `musicstreamer/player.py`, filters non-comment lines, asserts BOTH the literal `'\"SomaFM\"'` AND `'_last_preroll_played_at'` appear in the non-comment text (Phase 51/55/61/63/81/82 precedent; non-comment filter is Pitfall 8 mitigation)"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "preroll gate in Player.play; _start_preroll + _on_preroll_about_to_finish_callback (streaming thread) + _on_preroll_about_to_finish (main thread) slot; _on_gst_eos_during_preroll bus-eos handler + bus.connect('message::eos', ...) wiring; _preroll_about_to_finish_requested Signal; _preroll_in_flight + _last_preroll_played_at + _preroll_handler_id + _backfill_in_flight fields; _on_gst_tag early-return guard; _handle_gst_error_recovery preroll-aware branch; _preroll_backfill_worker daemon thread"
      contains: "_last_preroll_played_at"
    - path: "tests/test_player.py"
      provides: "10 behavioral tests (7 D-XX-mapped per VALIDATION.md + D-08 pause-during-preroll + D-10 streams-queue failover after preroll handoff + preroll-EOS-without-about-to-finish edge case from live-spike Q3 RESOLVED) + 1 source-grep drift-guard pinning '\"SomaFM\"' and '_last_preroll_played_at' = 13 total"
      contains: "test_phase_83_preroll_drift_guard"
  key_links:
    - from: "musicstreamer/player.py:Player.play"
      to: "station.prerolls + station.prerolls_fetched_at"
      via: "provider_name == 'SomaFM' gate + throttle gate + random.choice"
      pattern: "\"SomaFM\""
    - from: "musicstreamer/player.py:_on_preroll_about_to_finish_callback (streaming thread)"
      to: "_preroll_about_to_finish_requested.emit() (queued → main)"
      via: "Qt.ConnectionType.QueuedConnection (Pattern 1; Phase 43.1 f1333ed precedent)"
      pattern: "_preroll_about_to_finish_requested"
    - from: "musicstreamer/player.py:_preroll_backfill_worker (daemon thread)"
      to: "Repo(db_connect()).insert_preroll + Repo.set_prerolls_fetched_at"
      via: "thread-local connection; close in finally (Pattern 4; soma_import.py:398-402 precedent)"
      pattern: "_preroll_backfill_worker"
---

<objective>
Player layer for Phase 83. This is the heart of the phase — the SomaFM preroll gate inside `Player.play`, the playbin3 `about-to-finish` wiring for gapless transition, the metadata suppression during preroll, the failover-on-preroll-error path, and the lazy on-demand backfill worker. All five sub-features land in `musicstreamer/player.py`; tests land in `tests/test_player.py`.

Purpose: Plans 83-01 (data foundation) and 83-02 (importer capture) are inert without this layer — they only populate `station_prerolls` rows; this plan is what causes the preroll to actually play. The integration point is the single `Player.play(station)` funnel (mirrors Phase 82 D-03's "fix at the Player layer" principle).

Per-test alignment: every behavioral test in this plan maps 1:1 to a row in VALIDATION.md's per-decision test map (D-03, D-05, D-06, D-07, D-09, D-11, D-12-window, D-12-timestamp, D-13). The drift-guard test (D-14) closes the source-level loop.

Output: ~90-line block in `musicstreamer/player.py` (3 new Signal/fields, 1 new gate block in `Player.play`, 4 new methods including `_on_gst_eos_during_preroll` per live-spike Q3 RESOLVED, 1 new `bus.connect('message::eos', ...)` wire, 2 surgical edits in existing methods, 1 new daemon-worker method) + ~280-line appendix in `tests/test_player.py` (13 tests + factory helpers: 7 D-XX-mapped behavioral + 3 added per checker WARNING-4/-5 covering D-08 pause / D-10 streams-queue failover after handoff / malformed-preroll EOS + 2 ancillary behavioral + 1 source-grep drift-guard).

Wave 2 — depends on Plan 83-01 (`Station.prerolls`, `Station.prerolls_fetched_at` fields must exist; `Repo.insert_preroll` and `Repo.set_prerolls_fetched_at` for the backfill worker). Runs in parallel with Plan 83-02 (no file overlap; 83-03 owns player.py + test_player.py while 83-02 owns soma_import.py + test_soma_import.py).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-CONTEXT.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-VALIDATION.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-01-PLAN.md
@.planning/phases/82-twitch-only-station-still-tries-to-play-youtube-stream-first/82-02-PLAN.md
@musicstreamer/player.py
@musicstreamer/models.py
@musicstreamer/soma_import.py
@musicstreamer/repo.py
@tests/test_player.py
@.claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md

<interfaces>
<!-- Authoritative analog map: 83-PATTERNS.md §"musicstreamer/player.py" -->
<!-- Authoritative pattern: 83-RESEARCH.md §"Pattern 1/2/3/4" + §"Operation 1" + §"Common Pitfalls" -->

From musicstreamer/player.py:34-56 (existing imports — `threading` and `time` are ALREADY imported; `random` is NOT — verify with `grep -n '^import random' musicstreamer/player.py` and ADD `import random` to the stdlib block if absent. Also need `from musicstreamer.repo import db_connect, Repo` for the backfill worker — Player currently imports neither (per player.py:422 comment "Player does NOT self-connect — no repo handle — Pitfall 9"; this remains true for reads — Station carries prerolls eagerly; but the backfill WORKER needs db_connect + Repo for its thread-local connection. Pitfall 6 option 3a hybrid).

From musicstreamer/player.py:259-263 (existing class-level `_try_next_stream_requested = Signal()` — exact mirror for the new `_preroll_about_to_finish_requested = Signal()`; both MUST be class-level per the Pitfall 4 comment at player.py:240).

From musicstreamer/player.py:404-406 (existing `_try_next_stream_requested.connect(self._try_next_stream, Qt.ConnectionType.QueuedConnection)` — exact mirror for the new wiring of `_preroll_about_to_finish_requested.connect(self._on_preroll_about_to_finish, Qt.ConnectionType.QueuedConnection)` inside `__init__`).

From musicstreamer/player.py:434-456 (Runtime state field band in `__init__` — new fields land in this band):
```
self._volume: float = 1.0
self._streams_queue: list = []
self._current_stream: StationStream | None = None
self._current_station_name: str = ""
self._is_first_attempt: bool = True
self._twitch_resolve_attempts: int = 0
self._recovery_in_flight: bool = False  # gap-05: coalesce cascading bus errors per URL
self._last_buffer_percent: int = -1  # 47.1 D-14: sentinel so first real 0-100 always emits
# ... (Phase 62/70/78 fields follow)
```

Phase 83 new fields go in this band, ideally alongside `_recovery_in_flight` since both are bool guards:
```
# Phase 83 D-12/D-13/D-07/Pitfall 6 — SomaFM preroll state.
self._preroll_in_flight: bool = False
self._last_preroll_played_at: Optional[float] = None     # D-12 throttle (monotonic clock; resets per launch)
self._preroll_handler_id: int = 0                        # 0 = no handler connected
self._backfill_in_flight: set[int] = set()               # D-13 single-flight guard (T-83-10)
```

From musicstreamer/player.py:505-547 (current `Player.play` body — Phase 82 head-of-queue logic intact; preroll gate lands BETWEEN `self._streams_queue = queue` and `self._try_next_stream()` — that is, AFTER the queue is built but BEFORE the queue starts playing). Verbatim baseline ends at:
```
self._streams_queue = queue
self._try_next_stream()
```

The new shape (per RESEARCH §Pitfall 5 + §Pattern 2 + D-11 + D-12 + D-13):
```
self._streams_queue = queue
# Phase 83 D-11/D-12/D-13 — SomaFM preroll gate.
if (
    station.provider_name == "SomaFM"
    and (self._last_preroll_played_at is None
         or time.monotonic() - self._last_preroll_played_at > 600)
):
    urls = list(getattr(station, "prerolls", []) or [])
    if urls:
        # D-05 happy path: pick random URL, set URI, attach about-to-finish handler.
        preroll_url = random.choice(urls)
        self._start_preroll(preroll_url)
        return                                          # _try_next_stream fires from the about-to-finish slot
    elif (
        getattr(station, "prerolls_fetched_at", None) is None
        and station.id not in self._backfill_in_flight
    ):
        # D-13: kick lazy backfill (non-blocking); play proceeds to stream below.
        self._backfill_in_flight.add(station.id)
        threading.Thread(
            target=self._preroll_backfill_worker,
            args=(station.id, station.name),
            daemon=True,
        ).start()
    # else (D-04 / Pitfall 5): fetched, genuinely-empty SomaFM channel — silent skip.
self._try_next_stream()
```

Three new Player methods land alongside the existing `_try_next_stream` cluster (~player.py:972+):

1. `_start_preroll(self, preroll_url: str) -> None` (main thread):
   - Sets `self._preroll_in_flight = True`.
   - Sets `self._last_preroll_played_at = time.monotonic()` (D-12 — at preroll START, not handoff; per RESEARCH Anti-Pattern explicit).
   - `self._preroll_handler_id = self._pipeline.connect("about-to-finish", self._on_preroll_about_to_finish_callback)` — one-shot handler; `handler_id` retained for clean disconnect in the slot.
   - `self._set_uri(preroll_url)` — uses the existing main-thread URI-set path; preroll URL is direct HTTPS m4a so souphttpsrc handles it without special elements.

2. `_on_preroll_about_to_finish_callback(self, pipeline) -> None` (GStreamer streaming thread):
   - Body is EXACTLY ONE LINE: `self._preroll_about_to_finish_requested.emit()`.
   - Anti-Pattern: do NOT touch playbin3 properties, do NOT call any Qt API from this callback (RESEARCH §"Anti-Patterns to Avoid" + qt-glib-bus-threading Rule 2 + Phase 43.1 commit f1333ed precedent).

3. `_on_preroll_about_to_finish(self) -> None` (main thread; wired via QueuedConnection):
   - Guard + disconnect the one-shot handler: `if self._preroll_handler_id: try: self._pipeline.disconnect(self._preroll_handler_id) except (TypeError, RuntimeError): pass; self._preroll_handler_id = 0`.
   - `self._preroll_in_flight = False` (D-07 metadata gate releases here per Pitfall 2 timing reasoning).
   - `self._try_next_stream()` — plays `_streams_queue[0]` through the existing YT/Twitch resolve path if needed.

Two surgical edits in existing methods:

A. `_on_gst_tag` (~player.py:713-722) — add ONE early-return BETWEEN the existing `if not found: return` and the `title = _fix_icy_encoding(value)` line:
```
if self._preroll_in_flight:
    return
```
Cross-thread plain bool read; atomic in CPython (RESEARCH §Pattern 2 comment block — verbatim shape).

B. `_handle_gst_error_recovery` (~player.py:682-704) — add a guarded branch at the TOP, AFTER the `if self._recovery_in_flight: return` short-circuit but BEFORE the Twitch branch:
```
if self._preroll_in_flight:
    if self._preroll_handler_id:
        try:
            self._pipeline.disconnect(self._preroll_handler_id)
        except (TypeError, RuntimeError):
            pass
        self._preroll_handler_id = 0
    self._preroll_in_flight = False
    self._try_next_stream()
    QTimer.singleShot(0, self._clear_recovery_guard)
    return
```
D-09 — no retry of a different preroll; user experience is "slightly faster intro than expected."

One new daemon-worker method `_preroll_backfill_worker(self, station_id: int, station_name: str) -> None` lands at the bottom of the Player class (~end of file):
```
def _preroll_backfill_worker(self, station_id: int, station_name: str) -> None:
    """D-13 daemon backfill. Fetches SomaFM channels.json off the main
    thread, matches the channel by title (Pitfall 3 option 1), inserts
    station_prerolls rows + sets prerolls_fetched_at via a thread-local
    Repo (Pattern 4). Silent on all failures (D-04). Single-flight via
    self._backfill_in_flight (T-83-10) — discarded in finally so a later
    retry can fire on a fresh play."""
    from musicstreamer.soma_import import fetch_channels
    from musicstreamer.repo import db_connect, Repo
    try:
        channels = fetch_channels()
        match = next((c for c in channels if c.get("title") == station_name), None)
        prerolls = (match or {}).get("preroll_urls", []) or []
        if len(prerolls) > 50:
            prerolls = prerolls[:50]                                   # mirrors importer cap (T-83-02)
        con = db_connect()
        try:
            repo = Repo(con)
            for pos, url in enumerate(prerolls, start=1):
                try:
                    repo.insert_preroll(station_id, url, pos)
                except ValueError:
                    # URL-scheme rejection from Plan 83-01 — skip and keep going.
                    continue
            # D-04: mark fetched regardless of preroll count.
            repo.set_prerolls_fetched_at(station_id, int(time.time()))
        finally:
            con.close()
    except Exception as exc:  # noqa: BLE001 — D-04 silent failure path
        _log.warning("Phase 83 preroll backfill failed for station %d (%r): %s",
                     station_id, station_name, exc)
    finally:
        self._backfill_in_flight.discard(station_id)
```

Note: `_log` is the existing module-level logger (per Phase 62 / `musicstreamer.player`). No new logger.

From tests/test_player.py — existing test infrastructure (fixtures, `make_player`-style helpers, mock Gst pipeline). Phase 82's behavioral suite at ~tests/test_player.py:589-765 is the exact tone mirror. Use `with patch.object(p, "_set_uri"):` to suppress real I/O; assert on `_streams_queue` / `_current_stream` / `p._set_uri.call_args[0][0]` / `p._pipeline.connect.call_args` — NOT on `pipeline.emit(...)` (MEMORY `feedback_gstreamer_mock_blind_spot.md`).

From tests/test_player.py drift-guard idiom at ~tests/test_player.py:746-764 (Phase 82 `test_preferred_stream_id_drift_guard`). Verbatim template for Phase 83's `test_phase_83_preroll_drift_guard`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Player.play preroll gate + new Signal + new fields + 3 new methods (_start_preroll, callback, slot)</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    - musicstreamer/player.py lines 1-60 (imports — confirm `threading`, `time` already imported; check `random` presence)
    - musicstreamer/player.py lines 239-285 (class-level Signal band — new `_preroll_about_to_finish_requested = Signal()` lands here)
    - musicstreamer/player.py lines 290-460 (`__init__` — new `connect` wiring + new instance fields land here)
    - musicstreamer/player.py lines 325-340 (existing bus.connect block — message::error / message::tag / message::buffering / message::state-changed wiring; new `message::eos` connect lands HERE, alongside the other bus handlers, per live-spike Q3 RESOLVED + qt-glib-bus-threading Rule 2)
    - musicstreamer/player.py lines 505-548 (`Player.play` body + `Player.play_stream` — preroll gate lands between `self._streams_queue = queue` and `self._try_next_stream()`; play_stream is NOT modified)
    - musicstreamer/player.py lines 972-1020 (`_try_next_stream` body — reference only; new methods land in proximity)
    - musicstreamer/player.py lines 1021-1034 (`_set_uri` — preroll URL flows through this existing path; no change to _set_uri itself)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md §"Pattern 1" + §"Operation 1" + §"Anti-Patterns to Avoid" + §"Common Pitfalls" 1, 2, 5
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md §"musicstreamer/player.py" (preroll cluster)
    - .claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md (Rule 2 — streaming-thread callbacks may only emit Signals)
    - .planning/phases/82-twitch-only-station-still-tries-to-play-youtube-stream-first/82-02-PLAN.md (tone calibration — minimal-diff edits to Player.play)
  </read_first>
  <behavior>
    - When `station.provider_name != "SomaFM"`: preroll codepath is bypassed entirely; `_set_uri` is invoked with the queue head's URL (Phase 82 head-of-queue logic intact); `_preroll_in_flight` stays False; `p._pipeline.connect("about-to-finish", ...)` is NOT called.
    - When `station.provider_name == "SomaFM"` AND `station.prerolls` is non-empty AND throttle window expired: `random.choice` picks ONE URL; `_set_uri` is called with that URL; `_pipeline.connect("about-to-finish", callback)` is called and the returned handler id is stored in `_preroll_handler_id`; `_preroll_in_flight` becomes True; `_last_preroll_played_at` becomes a `time.monotonic()` reading; `_streams_queue` content equals Phase 82's queue (preroll URLs NEVER enter it); `_try_next_stream` is NOT called from `play` (it will be called from the about-to-finish slot).
    - When `station.provider_name == "SomaFM"` AND throttle window NOT expired: preroll path skipped; `_set_uri` is called with the queue head's URL; `_last_preroll_played_at` is unchanged.
    - When `station.provider_name == "SomaFM"` AND `station.prerolls == []` AND `station.prerolls_fetched_at is None` AND `station.id not in _backfill_in_flight`: `threading.Thread` is spawned with `target=self._preroll_backfill_worker` and `daemon=True`; `_backfill_in_flight.add(station.id)` runs before the spawn; `_set_uri` is called with the queue head's URL (play does NOT block on the backfill).
    - When `station.provider_name == "SomaFM"` AND `station.prerolls == []` AND `station.prerolls_fetched_at is not None`: NO Thread spawn; `_set_uri` is called with the queue head's URL.
    - When the about-to-finish callback fires on the streaming thread: ONLY `_preroll_about_to_finish_requested.emit()` is invoked — no other side effects from that callback.
    - When `_on_preroll_about_to_finish` runs on the main thread (via queued Signal): `_pipeline.disconnect(_preroll_handler_id)` is attempted (with try/except for double-disconnect safety); `_preroll_in_flight` becomes False; `_preroll_handler_id` becomes 0; `_try_next_stream()` runs.
    - Edge case (live-spike Q3 RESOLVED — malformed preroll, e.g. 0-byte response): if a `message::eos` arrives on the bus while `_preroll_in_flight is True` (i.e. before about-to-finish fired), the new `_on_gst_eos_during_preroll` bus handler emits `_try_next_stream_requested` (the same queued Signal D-09's bus-error path uses); play falls through to `_streams_queue[0]` on the main thread. When `_preroll_in_flight is False`, the handler returns immediately and the existing EOS semantics are unchanged (current player has no normal-path EOS — live-spike confirmed about-to-finish fires before EOS on the happy path).
  </behavior>
  <action>
    Per D-05, D-06, D-11, D-12, D-13, Pitfall 1, Pitfall 5, Pattern 1, Pattern 2, and RESEARCH Operation 1.

    Five distinct edits in `musicstreamer/player.py`:

    **Edit 1 — Imports (top of file):** If `random` is not in the stdlib import block, add `import random` (alphabetical position). `threading` and `time` are already present per the existing import band — verify and skip if so.

    **Edit 2 — Class-level Signal (the band at lines 259-285):** ADD a new class-level Signal alongside `_try_next_stream_requested` and `_playbin_playing_state_reached`:

    ```
    # Phase 83 D-05 / Pattern 1 — playbin3 about-to-finish fires on the GStreamer
    # streaming thread. Marshal to main via queued Signal so the disconnect +
    # _try_next_stream() call runs on the QThread (per qt-glib-bus-threading
    # Rule 2; Phase 43.1 fix commit f1333ed).
    _preroll_about_to_finish_requested = Signal()
    ```

    **Edit 3 — __init__ Signal wiring (the band at lines 400-417):** ADD the queued-connection wire alongside the existing `_try_next_stream_requested.connect(...)`:

    ```
    # Phase 83 D-05 / Pattern 1 — queue streaming-thread → main for preroll handoff.
    self._preroll_about_to_finish_requested.connect(
        self._on_preroll_about_to_finish, Qt.ConnectionType.QueuedConnection
    )
    ```

    **Edit 4 — __init__ instance fields (the runtime-state band at lines 434-456):** ADD four new fields, ideally adjacent to `_recovery_in_flight` for thematic grouping:

    ```
    # Phase 83 D-12 / D-13 / D-07 / T-83-10 — SomaFM preroll state.
    self._preroll_in_flight: bool = False
    self._last_preroll_played_at: Optional[float] = None
    self._preroll_handler_id: int = 0
    self._backfill_in_flight: set[int] = set()
    ```

    Note: `Optional` is already imported on player.py line 41 (`from typing import Callable, Optional`). `set[int]` is PEP 585 generic syntax; the file uses `from __future__ import annotations` at line 34, so it is safe at runtime.

    **Edit 5 — Player.play preroll gate (the queue-build tail at lines 545-547):** Locate the current 2-line block:

    ```
    self._streams_queue = queue
    self._try_next_stream()
    ```

    REPLACE with the gate block (the EXACT shape — copy carefully):

    ```
    self._streams_queue = queue
    # Phase 83 D-11 / D-12 / D-13 — SomaFM preroll gate.
    # The literal "SomaFM" is the drift-guard pin (Phase 74 D-02 CamelCase
    # convention; Phase 83 D-14 test pins this literal in non-comment lines).
    if (
        station.provider_name == "SomaFM"
        and (self._last_preroll_played_at is None
             or time.monotonic() - self._last_preroll_played_at > 600)
    ):
        urls = list(getattr(station, "prerolls", []) or [])
        if urls:
            preroll_url = random.choice(urls)
            self._start_preroll(preroll_url)
            return                       # _on_preroll_about_to_finish triggers _try_next_stream
        elif (
            getattr(station, "prerolls_fetched_at", None) is None
            and station.id not in self._backfill_in_flight
        ):
            # D-13 lazy backfill; non-blocking. Worker discards station.id from
            # _backfill_in_flight in its finally clause (T-83-10 single-flight).
            self._backfill_in_flight.add(station.id)
            threading.Thread(
                target=self._preroll_backfill_worker,
                args=(station.id, station.name),
                daemon=True,
            ).start()
        # else (D-04 / Pitfall 5): fetched, genuinely-empty channel — skip silently.
    self._try_next_stream()
    ```

    **Edit 6 — Three new methods** placed alongside the existing `_try_next_stream` / `_set_uri` cluster (~player.py:972-1034 band):

    ```
    def _start_preroll(self, preroll_url: str) -> None:
        """Phase 83 D-05 / D-12. Main-thread call. Wires playbin3 to play
        `preroll_url`, attaches a one-shot about-to-finish handler that
        schedules the station-stream handoff via queued Signal.
        _last_preroll_played_at is set HERE (preroll START) not in the
        about-to-finish slot — D-12 explicit anti-pattern is "update on
        handoff" (rapid replay would let a second preroll start)."""
        self._preroll_in_flight = True
        self._last_preroll_played_at = time.monotonic()
        self._preroll_handler_id = self._pipeline.connect(
            "about-to-finish", self._on_preroll_about_to_finish_callback
        )
        self._set_uri(preroll_url)

    def _on_preroll_about_to_finish_callback(self, pipeline) -> None:
        """Phase 83 D-05 / Pattern 1 — GStreamer streaming-thread callback.
        ONLY emits the queued Signal. NEVER call set_property or any Qt API
        from this body (qt-glib-bus-threading Rule 2; Phase 43.1 f1333ed)."""
        self._preroll_about_to_finish_requested.emit()

    def _on_preroll_about_to_finish(self) -> None:
        """Phase 83 D-05 — main-thread slot wired via QueuedConnection.
        Disconnects the one-shot handler, clears the in-flight flag, and
        kicks _try_next_stream() to play _streams_queue[0]."""
        if self._preroll_handler_id:
            try:
                self._pipeline.disconnect(self._preroll_handler_id)
            except (TypeError, RuntimeError):
                pass  # already disconnected (e.g. bus-error path took it)
            self._preroll_handler_id = 0
        self._preroll_in_flight = False
        self._try_next_stream()
    ```

    **Edit 7 — Bus EOS handler wiring (live-spike Q3 RESOLVED — malformed-preroll edge case):**

    Locate the existing bus-handler setup band at roughly player.py:334-337 (the cluster of `bus.connect("message::error", self._on_gst_error)`, `bus.connect("message::tag", self._on_gst_tag)`, `bus.connect("message::buffering", self._on_gst_buffering)`, `bus.connect("message::state-changed", self._on_gst_state_changed)`). Append ONE new line at the END of that cluster (after the `message::state-changed` line, before the trailing `# NOTE: no sync-message handler is registered ...` comment):

    ```
    bus.connect("message::eos", self._on_gst_eos_during_preroll)  # Phase 83 — malformed-preroll EOS bridge (live-spike Q3 RESOLVED)
    ```

    The handler runs on the GstBusLoopThread (same async-handler thread as `_on_gst_tag` etc.) — it may ONLY emit Qt Signals or read atomic state. The body matches that constraint by design (see Edit 8).

    **Edit 8 — `_on_gst_eos_during_preroll` helper method:**

    Add a small new helper method ALONGSIDE the preroll cluster (next to `_on_preroll_about_to_finish` from Edit 6, ~player.py:972-1034 band). Body:

    ```
    def _on_gst_eos_during_preroll(self, bus, msg) -> None:
        """Phase 83 — malformed-preroll EOS bridge (live-spike Q3 RESOLVED).

        On the GstBusLoopThread. Bridges the (rare) case where a preroll
        reaches EOS WITHOUT firing about-to-finish first — e.g. a 0-byte
        response, a truncated m4a, or a non-decodable container. The
        live-spike (2026-05-22 Linux GStreamer 1.28.2) confirms this path
        is NOT reached on the normal happy path (about-to-finish fires at
        +7.849s before EOS), so this handler is defense-in-depth only.

        When `_preroll_in_flight is False`, returns immediately so that
        today's EOS semantics (i.e. none on the streaming/looping path)
        are unchanged. When True, emits the existing
        `_try_next_stream_requested` queued Signal — the SAME fall-through
        Signal D-09's bus-error path uses — which marshals to main, runs
        `_try_next_stream()`, and advances to `_streams_queue[0]`.

        Plain-bool cross-thread read of `_preroll_in_flight` is atomic in
        CPython (mirrors `_on_gst_tag`'s guard read; Pattern 2).
        """
        if not self._preroll_in_flight:
            return
        # Same fall-through path as D-09 bus-error. `_try_next_stream_requested`
        # is the existing class-level Signal wired with QueuedConnection at
        # player.py:404-406 (Phase 82 / 43.1 pattern); the slot on main does
        # the disconnect + flag clear + _try_next_stream() invocation via
        # `_handle_gst_error_recovery`'s D-09 branch (added in Task 2) OR
        # directly via `_try_next_stream`. The handler does NOT itself touch
        # the pipeline, the handler-id, or the flag (those are main-thread
        # concerns; qt-glib-bus-threading Rule 2).
        self._try_next_stream_requested.emit()
    ```

    Hard constraints:
    - Do NOT mutate `_streams_queue` from any preroll codepath (D-06).
    - Do NOT call `self._pipeline.set_property("uri", ...)` from `_on_preroll_about_to_finish_callback` — that's the streaming thread; use the queued Signal (Pitfall 1; Anti-Pattern explicit).
    - Do NOT update `_last_preroll_played_at` in the about-to-finish slot — D-12 says preroll START, not handoff (Anti-Pattern explicit).
    - Do NOT remove or rewrite the Phase 82 head-of-queue logic in `Player.play` — the preroll gate is APPENDED to it; the queue is still built (D-06).
    - Do NOT modify `Player.play_stream` (D-08 / Phase 82 D-04 — direct-play path is unaffected).
    - Do NOT modify `_set_uri` itself — the existing URI-set path handles preroll URLs unchanged.
    - The `return` after `_start_preroll(preroll_url)` is load-bearing — without it, `_try_next_stream()` would be called immediately and clobber the preroll URI.
    - The EOS handler `_on_gst_eos_during_preroll` MUST be a no-op when `_preroll_in_flight is False` — DO NOT add any "interesting" behavior to the False branch. Today's player has no normal-path EOS (live-spike Q3 RESOLVED confirms); this handler exists solely as a malformed-preroll bridge.
    - The EOS handler runs on the GstBusLoopThread — it may ONLY emit Qt Signals; do NOT call `_try_next_stream` directly, do NOT touch `_pipeline`, do NOT touch `_preroll_handler_id` or `_preroll_in_flight` from this body. The queued Signal pattern (`_try_next_stream_requested.emit()`) is the existing main-thread bridge.
  </action>
  <verify>
    <automated>uv run pytest tests/test_player.py -x -q --co 2>&1 | tail -5  # smoke: test collection succeeds after Edit 1-8</automated>
  </verify>
  <done>
    - `grep -c '_preroll_about_to_finish_requested = Signal' musicstreamer/player.py` outputs `1`
    - `grep -c '_preroll_about_to_finish_requested.connect' musicstreamer/player.py` outputs `1`
    - `grep -c '_last_preroll_played_at' musicstreamer/player.py` outputs at least `2` (one in __init__ field declaration; one in the play gate)
    - `grep -c '_preroll_in_flight' musicstreamer/player.py` outputs at least `3` (field declaration + start_preroll + slot; more in subsequent tasks)
    - `grep -c '_preroll_handler_id' musicstreamer/player.py` outputs at least `4` (field + start_preroll + slot + about-to-finish slot's disconnect)
    - `grep -c '"SomaFM"' musicstreamer/player.py` outputs at least `1` (the gate literal; this MUST be in non-comment text per D-14 drift-guard)
    - `grep -c 'random.choice' musicstreamer/player.py` outputs `1`
    - `grep -c 'def _start_preroll\|def _on_preroll_about_to_finish_callback\|def _on_preroll_about_to_finish' musicstreamer/player.py` outputs `3`
    - `grep -c 'def _on_gst_eos_during_preroll' musicstreamer/player.py` outputs `1` (live-spike Q3 RESOLVED — malformed-preroll EOS bridge)
    - `grep -c 'bus.connect("message::eos"' musicstreamer/player.py` outputs `1` (the new EOS wiring inside the bus-handler cluster at player.py:325-340)
    - `grep -c 'def play_stream' musicstreamer/player.py` outputs exactly `1` (signature intact; body unchanged)
    - `grep -c 'order_streams' musicstreamer/player.py` is unchanged from pre-edit count (Phase 82 head-of-queue logic untouched)
    - `python -c "from musicstreamer.player import Player; p = Player.__new__(Player); print('OK')"` exits 0 (module imports cleanly)
    - `uv run pytest tests/test_player.py -x -q` exits 0 (existing tests still pass; new tests land in Task 4)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _on_gst_tag preroll-suppress early-return + _handle_gst_error_recovery preroll-aware branch</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    - musicstreamer/player.py lines 682-704 (`_handle_gst_error_recovery` body — new guarded branch lands at the TOP, AFTER the `_recovery_in_flight` short-circuit, BEFORE the Twitch branch)
    - musicstreamer/player.py lines 713-722 (`_on_gst_tag` body — new early-return lands BETWEEN `if not found: return` and `title = _fix_icy_encoding(value)`)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md §"Pattern 2" + §"Pitfall 2" + §"Operation 1" surrounding text
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md §"Bus-handler choke-point pattern" + §"Failover-aware error recovery"
  </read_first>
  <behavior>
    - When `_preroll_in_flight is True` AND `_on_gst_tag` is invoked with a TITLE tag: the early-return fires; `title_changed` is NOT emitted (Now Playing keeps showing the station name through the preroll). When `_preroll_in_flight is False`, the existing behavior is preserved (title is decoded and emitted).
    - When `_preroll_in_flight is True` AND `_handle_gst_error_recovery` is invoked (preroll URL 404 / network timeout / bus error): the preroll handler is disconnected with try/except safety; `_preroll_in_flight` flips to False; `_preroll_handler_id` resets to 0; `_try_next_stream()` runs (advances to `_streams_queue[0]` — the station's actual first stream); `QTimer.singleShot(0, self._clear_recovery_guard)` runs; the method returns early WITHOUT executing the existing Twitch / generic-failover branches. When `_preroll_in_flight is False`, the existing recovery behavior is preserved (no regression).
  </behavior>
  <action>
    Per D-07, D-09, and RESEARCH §Pattern 2 + §Pitfall 2.

    Two surgical inserts in existing methods.

    **Edit A — `_on_gst_tag` early-return (D-07 metadata suppression):**

    Locate the existing body (at roughly player.py:713-722). After the existing line `if not found: return` and BEFORE the existing `self._cancel_timers_requested.emit()` line OR before the existing `title = _fix_icy_encoding(value)` line (depending on the exact current order — `read_first` confirms), INSERT:

    ```
    # Phase 83 D-07 — suppress preroll's m4a title tag so Now Playing keeps
    # showing the station name through the ~5s ID. Set on main in Player.play
    # (before set_uri to the preroll URL) and cleared in _on_preroll_about_to_finish
    # (also main). This read is cross-thread; Python bool read is atomic. Worst
    # case (Pitfall 2 — m4a tag arrives between disconnect and flag clear) is
    # one frame of preroll title leaked, ~30ms, acceptable per D-07's
    # "no UI flicker" intent (not "zero leak").
    if self._preroll_in_flight:
        return
    ```

    Per RESEARCH §Pattern 2 verbatim. Read the existing `_on_gst_tag` body carefully — the early-return MUST precede `title = _fix_icy_encoding(value)` (otherwise title gets decoded and the `title_changed.emit(title)` fires before the guard). If `_cancel_timers_requested.emit()` is also in the body, the guard placement is a judgment call — but per Pattern 2 verbatim shape, the order is `if not found: return` → `if _preroll_in_flight: return` → `title = ...` → `title_changed.emit(title)`. Replicate that exact ordering; the existing `_cancel_timers_requested.emit()` should remain in its current position relative to the OTHER lines (do not move it).

    **Edit B — `_handle_gst_error_recovery` preroll-aware branch (D-09):**

    Locate the existing body (at roughly player.py:682-704). After the existing `if self._recovery_in_flight: return` short-circuit + the `self._recovery_in_flight = True` line + the `self._cancel_timers()` line (i.e. all the pre-conditions are set up), but BEFORE the existing Twitch branch (`if self._current_stream and "twitch.tv" in self._current_stream.url:`), INSERT:

    ```
    # Phase 83 D-09 — preroll error path. Don't retry a different preroll;
    # immediately advance to the station's actual stream. No second preroll
    # selection — user experience is "slightly faster intro than expected."
    if self._preroll_in_flight:
        if self._preroll_handler_id:
            try:
                self._pipeline.disconnect(self._preroll_handler_id)
            except (TypeError, RuntimeError):
                pass
            self._preroll_handler_id = 0
        self._preroll_in_flight = False
        self._try_next_stream()
        QTimer.singleShot(0, self._clear_recovery_guard)
        return
    ```

    Per PATTERNS.md §"Failover-aware error recovery" verbatim.

    Hard constraints:
    - Do NOT modify the existing Twitch branch or the trailing `self._try_next_stream()` + `QTimer.singleShot(0, self._clear_recovery_guard)` in `_handle_gst_error_recovery` — the preroll branch is a guarded short-circuit added at the TOP that returns before the existing branches execute.
    - Do NOT move `_cancel_timers_requested.emit()` in `_on_gst_tag` — its placement is independent of the new guard (covers a separate concern: cancel-failover-timer-when-audio-arrives).
    - The `try/except (TypeError, RuntimeError)` around `_pipeline.disconnect` is load-bearing — `_handle_gst_error_recovery` can race with `_on_preroll_about_to_finish` (both clear the handler); double-disconnect is benign but the exception types come from GObject internals and must be swallowed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_player.py -x -q --co 2>&1 | tail -5  # smoke: collection still succeeds</automated>
  </verify>
  <done>
    - `grep -c 'if self._preroll_in_flight:' musicstreamer/player.py` outputs at least `2` (one in `_on_gst_tag`, one in `_handle_gst_error_recovery`; plus possibly the slot's clear)
    - The `_handle_gst_error_recovery` branch is structurally correct via inspection: `_recovery_in_flight` short-circuit → preroll branch → Twitch branch → generic `_try_next_stream`. Verify via `git diff musicstreamer/player.py`.
    - The `_on_gst_tag` guard is BEFORE `_fix_icy_encoding(value)`. Verify via inspection of the diff.
    - `uv run pytest tests/test_player.py -x -q` exits 0 (existing tests still pass; behavioral tests for these guards land in Task 4)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: _preroll_backfill_worker daemon thread + Repo + db_connect imports (Pattern 4 thread-local)</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    - musicstreamer/player.py top of file (imports — verify current state; the worker uses lazy imports of db_connect/Repo/fetch_channels inside the function body OR adds them at the top — see action notes for the decision rationale)
    - musicstreamer/player.py end of class Player (the last method before any module-level code; this is where `_preroll_backfill_worker` lands)
    - musicstreamer/soma_import.py lines 398-402 (Phase 74 thread-local Repo precedent — exact shape mirror for the backfill writer's `con = db_connect(); try: ...; finally: con.close()` block)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md §"Pattern 4: Thread-local Repo for DB Writes from Worker Threads" + §"Pitfall 3" + §"Pitfall 6"
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md §"Daemon-thread worker pattern" + §"Thread-local Repo(db_connect()) for worker writes"
    - musicstreamer/soma_import.py top of file (existing `fetch_channels` import surface)
  </read_first>
  <behavior>
    - When invoked with a `station_id` whose backfill is in-flight, the worker fetches the SomaFM channels.json via the existing `soma_import.fetch_channels()` (off the main thread; daemon).
    - Matches the upstream channel by `title == station_name` (Pitfall 3 option 1 — simpler; renamed stations silently never get a preroll, but D-04 marker semantics cap the damage).
    - Opens its OWN `Repo(db_connect())` connection — does NOT reuse the main-thread Repo (SQLite connections are not thread-safe; project precedent at soma_import.py:398-402).
    - Inserts each preroll URL via `repo.insert_preroll`; if `insert_preroll` raises `ValueError` (URL-scheme rejection from Plan 83-01), the worker catches the exception locally and continues to the next URL (graceful — never aborts the whole backfill).
    - Caps the URL list at 50 entries before the insert loop (mirrors the importer cap in Plan 83-02; double-defense per T-83-02).
    - Calls `repo.set_prerolls_fetched_at(station_id, int(time.time()))` regardless of URL count — even if 0 URLs were found, the timestamp is set so subsequent plays of that station do NOT re-trigger backfill (D-04).
    - On any other exception (network failure, JSON parse error, etc.), logs `_log.warning(...)` and exits silently (D-04 silent-failure path; T-83-08 informational).
    - In a `finally` block: `self._backfill_in_flight.discard(station_id)` runs unconditionally so a later play of the same station can retry (T-83-10 single-flight invariant — the set tracks IN-FLIGHT, not "ever attempted").
  </behavior>
  <action>
    Per D-03, D-04, D-13, RESEARCH §Pattern 4, §Pitfall 3, §Pitfall 6, and PATTERNS.md analogs.

    **Imports** — DECISION: lazy-import inside the worker body. Rationale: keeps Player's top-of-file imports clean (preserves the project invariant "Player has no top-level Repo dep — Pitfall 9 comment at player.py:422"); deferred imports only fire when backfill is actually triggered (negligible cost on first-call). The worker body opens with:

    ```
    from musicstreamer.soma_import import fetch_channels
    from musicstreamer.repo import db_connect, Repo
    ```

    These two lines land inside the worker method, NOT at the top of the file. (Phase 82 follows the same lazy-import pattern for `Repo` in the worker thread; verify by grepping `^from musicstreamer.repo import` in player.py — the top of file should remain repo-free.)

    **Method placement:** Append `_preroll_backfill_worker` at the bottom of the `class Player` body, after the existing `_twitch_resolve_worker` cluster (~player.py:1161+). The exact placement is flexible — anywhere among the per-stream worker / resolver methods is fine; thematic grouping suggests adjacency to `_youtube_resolve_worker` (player.py:1051) and `_twitch_resolve_worker` (player.py:1161).

    **Method body** (verbatim shape per RESEARCH §Pattern 4):

    ```
    def _preroll_backfill_worker(self, station_id: int, station_name: str) -> None:
        """Phase 83 D-13 daemon backfill worker (Pattern 4 thread-local Repo).

        Fetches SomaFM channels.json off the main thread, matches the
        upstream channel by title (Pitfall 3 option 1), opens its own
        Repo via db_connect() (Pattern 4 — never share the main-thread
        Repo across threads), inserts station_prerolls rows + sets
        prerolls_fetched_at. Silent on all failures (D-04). Single-flight
        via self._backfill_in_flight (T-83-10) — discarded in finally so a
        later play retries on a fresh attempt.

        Per Pitfall 3: matches by title because Phase 74 does not persist
        the SomaFM slug. A user-renamed station silently never gets a
        preroll (acceptable per D-04 silent failure caps).
        """
        from musicstreamer.soma_import import fetch_channels
        from musicstreamer.repo import db_connect, Repo
        try:
            channels = fetch_channels()
            match = next(
                (c for c in channels if c.get("title") == station_name), None
            )
            preroll_urls = list((match or {}).get("preroll_urls", []) or [])
            if len(preroll_urls) > 50:
                preroll_urls = preroll_urls[:50]   # T-83-02 double-defense
            con = db_connect()
            try:
                repo = Repo(con)
                for pos, url in enumerate(preroll_urls, start=1):
                    try:
                        repo.insert_preroll(station_id, url, pos)
                    except ValueError:
                        # URL-scheme rejection from Plan 83-01; skip and continue.
                        continue
                # D-04: mark fetched regardless of count (legitimately-empty
                # channels must not re-trigger backfill on every Play).
                repo.set_prerolls_fetched_at(station_id, int(time.time()))
            finally:
                con.close()
        except Exception as exc:  # noqa: BLE001 — D-04 silent failure path
            _log.warning(
                "Phase 83 preroll backfill failed for station %d (%r): %s",
                station_id, station_name, exc,
            )
        finally:
            self._backfill_in_flight.discard(station_id)
    ```

    Hard constraints:
    - Do NOT share Player's would-be Repo handle (Player doesn't have one — Pitfall 9 invariant). Open the connection inside the worker; close it in `finally`.
    - Do NOT mutate `_streams_queue`, `_current_stream`, `_set_uri`, or ANY Qt UI object from this worker — it runs on a non-Qt daemon thread.
    - Do NOT emit Signals from this worker except as a defensive future addition (none required for this phase — the user's next play picks up the new prerolls naturally).
    - Do NOT raise out of the worker — daemon-thread exceptions are silently lost AND would leave `_backfill_in_flight` non-empty if not for the `finally: discard()` block. The `try/except Exception` catches everything per D-04.
    - The `self._backfill_in_flight.discard(station_id)` MUST be in `finally`, NOT in the `except` block — runs even on success, error, AND if `fetch_channels` itself raises. Without this, a SINGLE failed backfill blocks all future retries for that station_id until app restart.
    - `_log` is the existing module-level logger (per Phase 62). No new logger.
    - `time` is already imported (player.py:39).
  </action>
  <verify>
    <automated>uv run pytest tests/test_player.py -x -q --co 2>&1 | tail -5  # smoke: collection still succeeds; module still imports cleanly</automated>
  </verify>
  <done>
    - `grep -c 'def _preroll_backfill_worker' musicstreamer/player.py` outputs `1`
    - `grep -c '_backfill_in_flight.discard' musicstreamer/player.py` outputs `1` (the finally clause)
    - `grep -c 'from musicstreamer.repo import db_connect, Repo' musicstreamer/player.py` outputs `1` (lazy import inside worker)
    - `grep -c '^from musicstreamer.repo import' musicstreamer/player.py` outputs `0` (no top-level Repo import — preserves the player.py:422 invariant)
    - `grep -c 'repo.insert_preroll' musicstreamer/player.py` outputs `1`
    - `grep -c 'repo.set_prerolls_fetched_at' musicstreamer/player.py` outputs `1`
    - `python -c "from musicstreamer.player import Player; print('OK')"` exits 0
    - `uv run pytest tests/test_player.py -x -q` exits 0 (existing tests pass; new behavioral tests land in Task 4)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: tests/test_player.py appendix — 7 behavioral tests + 1 source-grep drift-guard</name>
  <files>tests/test_player.py</files>
  <read_first>
    - tests/test_player.py (existing test surface; particularly Phase 82's behavioral suite at ~lines 589-765 — exact tone mirror including `_make_player_mock` / `_make_stream_ph82` / `_make_station_ph82` factories + 7 tests + drift-guard)
    - musicstreamer/player.py (full Player class after Tasks 1-3)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-VALIDATION.md (per-decision test map — test NAMES MUST MATCH the verify-command rows verbatim)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md §"tests/test_player.py" (factory + assertion patterns; MEMORY blind-spot reminder)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md §"Pattern 3: Source-Grep Drift-Guard" + §"Pitfall 8"
  </read_first>
  <behavior>
    Each behavioral test below maps 1:1 to a row in VALIDATION.md's per-decision test map. Function NAMES MUST MATCH the VALIDATION.md rows so the verify-work step locates them.

    - `test_preroll_sets_uri_and_connects_handler` (D-05): SomaFM station with 2 prerolls + throttle expired → `p._set_uri.call_args[0][0]` equals one of the preroll URLs (the random pick); `p._pipeline.connect.call_args` first positional equals `"about-to-finish"`; `p._preroll_in_flight is True`; `p._last_preroll_played_at is not None`; `p._streams_queue == station.streams` (equal-but-as-order_streams-returned); `_try_next_stream` is NOT called from `play` itself (assert via either `with patch.object(p, "_try_next_stream") as m: p.play(station); assert m.call_count == 0`, OR — equivalently — by asserting `p._current_stream is None` AFTER `play` returns, because `_try_next_stream` is what advances `_current_stream`).
    - `test_throttle_window_suppresses_preroll` (D-12 window): SomaFM station with prerolls; set `p._last_preroll_played_at = time.monotonic()` BEFORE the play call (i.e. throttle window NOT elapsed); `p._set_uri.call_args[0][0]` equals the queue head's stream URL (NOT a preroll URL); `p._pipeline.connect` is NOT called with `"about-to-finish"` (use `assert call("about-to-finish", ANY) not in p._pipeline.connect.call_args_list` or similar).
    - `test_throttle_timestamp_set_on_start` (D-12 timestamp): SomaFM station with prerolls + throttle expired; monkeypatch `random.choice` to a deterministic pick AND patch `time.monotonic` to a fixed value (e.g. 1000.0); after `play`, assert `p._last_preroll_played_at == 1000.0` — proves the timestamp is set when `_start_preroll` runs (preroll START), NOT in the about-to-finish slot (which never runs in this test).
    - `test_preroll_does_not_pollute_streams_queue` (D-06): same setup as D-05; after `play`, assert `p._streams_queue == list(order_streams(station.streams))` (i.e. preroll URLs never enter the queue); assert `len(p._streams_queue) == len(station.streams)` (exact count match).
    - `test_preroll_backfill_scheduled_when_unfetched` (D-03 / D-13): SomaFM station with `station.prerolls == []` AND `station.prerolls_fetched_at is None` AND `station.id not in p._backfill_in_flight`; `monkeypatch.setattr("musicstreamer.player.threading.Thread", MockThread)` where `MockThread` records construction args; after `play`, assert `MockThread` was constructed with `target=p._preroll_backfill_worker` AND `daemon=True` AND `args=(station.id, station.name)`; assert `station.id in p._backfill_in_flight`; assert `_set_uri` was called with the stream URL (play proceeds — non-blocking; D-13).
    - `test_backfill_non_blocking` (D-13): same as above; additionally assert `_set_uri` is called BEFORE `MockThread.start()` would have returned (proves play does not block on the worker — use `MockThread.start = MagicMock(side_effect=lambda: time.sleep(0.5))` and bound the test runtime via `qtbot.wait(50)` if needed; OR more simply, assert `p._set_uri.call_count == 1` immediately after `play` returns synchronously, which is the same proof).
    - `test_non_somafm_provider_bypasses_preroll` (D-11): NON-SomaFM station (e.g. `provider_name="DI.fm"` or `provider_name="GBS.FM"`) with SYNTHETIC `station.prerolls = ["https://example.com/x.m4a"]`; after `play`, assert `p._set_uri.call_args[0][0]` equals the stream URL (NOT the synthetic preroll URL); assert `p._pipeline.connect` was NOT called with `"about-to-finish"`; assert `p._preroll_in_flight is False`. Proves the provider gate is doing its job.
    - `test_title_tag_suppressed_during_preroll` (D-07): SomaFM station with prerolls + throttle expired; call `p.play(station)` to enter preroll state; then DIRECTLY invoke `p._on_gst_tag(mock_bus, mock_msg)` where `mock_msg.parse_tag()` returns a taglist whose `.get_string(Gst.TAG_TITLE)` returns `(True, "BeatBlenderID1")`; use `qtbot.waitSignal(p.title_changed, timeout=100, raising=False)` and assert it TIMED OUT (signal NOT emitted) — OR simpler, `with patch.object(p, "title_changed") as m: p._on_gst_tag(...); assert m.emit.call_count == 0`. Then SET `p._preroll_in_flight = False` and re-invoke `_on_gst_tag` with the same taglist; assert `title_changed.emit` IS called this time (proves the guard is gated, not unconditional).
    - `test_preroll_bus_error_advances_to_stream` (D-09): SomaFM station; call `p.play(station)` to enter preroll state; spy/patch `p._try_next_stream` and `p._clear_recovery_guard`; directly invoke `p._handle_gst_error_recovery()`; assert `p._preroll_in_flight is False` AFTER; assert `p._preroll_handler_id == 0` AFTER; assert `p._try_next_stream.called`; assert NO retry-of-a-different-preroll (i.e. `p._pipeline.connect` was NOT called a second time with `"about-to-finish"` during the recovery).
    - `test_preroll_in_flight_pause_does_not_clear_flag` (D-08 — pause during preroll behaves as today): SomaFM station with prerolls + throttle expired; `p.play(station)` enters preroll state (`p._preroll_in_flight is True`, `_set_uri` called with a preroll URL). Then call `p.pause()`. Assert: (a) `p._preroll_in_flight is True` (flag NOT cleared by pause), (b) the mocked pipeline's `set_state` was called with `Gst.State.PAUSED` (or whatever the existing `Player.pause` already does — assert behavioral parity with the pre-Phase-83 baseline), (c) `_try_next_stream` is NOT invoked from `pause` (use `with patch.object(p, '_try_next_stream') as m: p.pause(); assert m.call_count == 0`). Proves Pause / Stop / Resume during preroll do not regress today's semantics — the preroll flag is independent of pipeline transport state.
    - `test_streams_queue_failover_after_preroll_handoff` (D-10 — failover advances `_streams_queue` normally after handoff): SomaFM station with 2 prerolls AND 2 streams (so `_streams_queue == [stream_0, stream_1]` after `Player.play` builds it). Enter preroll state via `p.play(station)`. Directly invoke `p._on_preroll_about_to_finish()` (the main-thread slot) to simulate the about-to-finish handoff — at this point `_preroll_in_flight is False`, `_try_next_stream` has run, `_current_stream == stream_0`. Now simulate a bus error for the station stream by calling `p._handle_gst_error_recovery()` (with `_preroll_in_flight is False` so the D-09 preroll branch is NOT taken — the test confirms post-preroll path uses pre-Phase-83 failover). Assert `p._try_next_stream` was invoked AND `p._current_stream` advances to `stream_1` (or `_streams_queue` is drained as today). Proves Phase 82's head-of-queue failover behaves exactly as before once the preroll handoff completes — the preroll handler does NOT regress Phase 82 failover.
    - `test_preroll_eos_without_about_to_finish_advances_to_stream` (live-spike Q3 RESOLVED — malformed-preroll EOS bridge): SomaFM station with prerolls; call `p.play(station)` to enter preroll state. Construct a mock bus message with `msg.type == Gst.MessageType.EOS` (or the project's existing EOS mock idiom — inspect `_on_gst_state_changed` tests for pattern). Spy/patch `p._try_next_stream_requested.emit` (the existing class-level Signal). Directly invoke `p._on_gst_eos_during_preroll(mock_bus, mock_msg)`. Assert: (a) `p._try_next_stream_requested.emit` was called exactly once (the bridge fired the SAME Signal D-09 uses), (b) `p._preroll_in_flight is True` (the streaming-thread handler does NOT touch the flag — main-thread slot does that). Then SET `p._preroll_in_flight = False` and re-invoke the EOS handler with the same mock_msg; assert `emit.call_count` is unchanged (still 1) — proves the False branch is a no-op (no regression of today's EOS-less normal path). Maps to live-spike Q3 RESOLVED.
    - `test_phase_83_preroll_drift_guard` (D-14): source-grep test mirroring tests/test_player.py:746-764 (Phase 82 idiom). Reads `musicstreamer/player.py`, strips comment-only lines, asserts BOTH `'"SomaFM"'` AND `'_last_preroll_played_at'` appear in the non-comment text. Non-comment filter is REQUIRED (Pitfall 8 — a comment-only literal would satisfy the assert even after the gate code is removed).
  </behavior>
  <action>
    Per D-03, D-05, D-06, D-07, D-09, D-11, D-12, D-13, D-14, RESEARCH §Pattern 3, §Pitfall 8, and PATTERNS.md test analogs.

    Append a Phase 83 section to `tests/test_player.py` (no new file — appendix style mirrors Phase 82 at lines 589-765). Reuse the existing fixtures/helpers — `make_player`, `make_stream`, `make_station_with_streams` (or whatever the file's current convention is); inspect the file BEFORE adding helpers.

    **Factory helpers (if Phase 82's factories don't already cover Phase 83's needs):** Create a `_make_station_ph83(streams, *, provider_name="SomaFM", prerolls=None, prerolls_fetched_at=None)` helper that builds a Station with the new fields. Default `provider_name="SomaFM"` because most Phase 83 tests use it; the bypass test overrides to e.g. `"DI.fm"`. The helper MUST set `provider_name=...` to the exact string the test wants — do NOT hardcode `"SomaFM"` outside the factory (use the parameter so the bypass test can override it).

    **Test names MUST MATCH** the VALIDATION.md verify-command rows verbatim. Mapping (VALIDATION.md row → test function name):
    - D-05 → `test_preroll_sets_uri_and_connects_handler`
    - D-12 (window) → `test_throttle_window_suppresses_preroll`
    - D-12 (timestamp) → `test_throttle_timestamp_set_on_start`
    - D-06 → `test_preroll_does_not_pollute_streams_queue`
    - D-03 → `test_preroll_backfill_scheduled_when_unfetched`
    - D-13 → `test_backfill_non_blocking`
    - D-11 → `test_non_somafm_provider_bypasses_preroll`
    - D-07 → `test_title_tag_suppressed_during_preroll`
    - D-09 → `test_preroll_bus_error_advances_to_stream`
    - D-08 (pause during preroll) → `test_preroll_in_flight_pause_does_not_clear_flag` (WARNING-4 added per checker)
    - D-10 (streams-queue failover after preroll handoff) → `test_streams_queue_failover_after_preroll_handoff` (WARNING-4 added per checker)
    - Live-spike Q3 RESOLVED (malformed-preroll EOS bridge) → `test_preroll_eos_without_about_to_finish_advances_to_stream` (WARNING-5 added per checker)
    - D-14 → `test_phase_83_preroll_drift_guard`

    Implementation notes:
    - All behavioral tests use `with patch.object(p, "_set_uri"):` to suppress real I/O — same idiom as Phase 82's tests at tests/test_player.py:633-647. Do NOT assert on `pipeline.emit(...)` (MEMORY `feedback_gstreamer_mock_blind_spot.md` — pipeline mocks pass through any `emit` call without firing handlers).
    - For pipeline.connect assertions: `_make_player_mock` mocks `_pipeline` as a `MagicMock`; `p._pipeline.connect.call_args_list` is iterable; assert e.g. `("about-to-finish", ANY) in [tuple(c.args) for c in p._pipeline.connect.call_args_list]` OR similar idiom.
    - For the throttle window test (D-12 window): set `p._last_preroll_played_at = time.monotonic()` (i.e. RIGHT NOW) BEFORE `play(station)`. The gate `time.monotonic() - self._last_preroll_played_at > 600` evaluates to False → preroll suppressed. Mathematically the elapsed delta is microseconds, never > 600.
    - For D-12 timestamp: patch `time.monotonic` on the `musicstreamer.player` module (the `import time` is module-level, so `monkeypatch.setattr("musicstreamer.player.time", FakeTime)` works; OR use `monkeypatch.setattr("musicstreamer.player.time.monotonic", lambda: 1000.0)` if the test runner permits attribute-on-module patching — both are valid; pick whichever Phase 82 uses for consistency).
    - For D-03/D-13 backfill scheduling: `monkeypatch.setattr("musicstreamer.player.threading.Thread", MockThread)` where MockThread is a class that records init kwargs and has a no-op `start()`. Assert via `MockThread.last_kwargs` or similar.
    - For D-07 title suppression: prefer the direct invocation pattern (`p._on_gst_tag(mock_bus, mock_msg)`) over qtbot.waitSignal — fastpath, no event loop dependency. The mock_msg needs `.parse_tag()` returning a mock taglist with `.get_string(Gst.TAG_TITLE) -> (True, "BeatBlenderID1")`. Existing player tests likely already have a helper for this — inspect and reuse.
    - For D-14 drift-guard:

    ```
    def test_phase_83_preroll_drift_guard():
        """Phase 83 D-11 / D-14 drift-guard (Phase 51/55/61/63/81/82 precedent).
        Pins both the provider-gate literal '"SomaFM"' and the throttle-state
        token '_last_preroll_played_at' in non-comment text of player.py.
        Non-comment filter is REQUIRED (Pitfall 8 — a comment-only literal
        would satisfy the assert even after the gate code is removed)."""
        from pathlib import Path
        import re
        source = (
            Path(__file__).resolve().parent.parent / "musicstreamer" / "player.py"
        ).read_text()
        non_comments = "\n".join(
            re.sub(r"^\s*#.*$", "", ln) for ln in source.splitlines()
        )
        assert '"SomaFM"' in non_comments, (
            "Phase 83 D-11: SomaFM provider-gate literal must remain in Player. "
            "Do not remove silently."
        )
        assert "_last_preroll_played_at" in non_comments, (
            "Phase 83 D-12: throttle-state token must remain in Player. "
            "Do not remove silently."
        )
    ```

    The `re.sub` form strips comment-trailing portions of partially-commented lines, slightly stronger than `ln.lstrip().startswith("#")` (which only excludes whole-comment lines). Either form is acceptable per Phase 82 precedent; the regex form matches MEMORY `feedback_gstreamer_mock_blind_spot.md` closer (the memory anchor mentions header-prose self-invalidation; the regex form prevents `# something "SomaFM" something` from passing). Use whichever the executor finds cleaner — both are Pitfall 8-compliant.

    Hard constraints:
    - All test names exactly match VALIDATION.md mapping above.
    - Do NOT use `pytest.fail` placeholders — every test must have a real assertion path (per `feedback_gstreamer_mock_blind_spot.md` lesson: explicit tests over silent-passing ones).
    - Do NOT modify existing Phase 47/74/82 tests in test_player.py — append-only.
    - Do NOT skip any test conditionally on platform — every test runs on Linux + Windows alike.
  </action>
  <verify>
    <automated>uv run pytest tests/test_player.py -k "preroll or phase_83 or streams_queue_failover_after_preroll_handoff" -x -q</automated>
  </verify>
  <done>
    - All 13 new tests pass under the verify command (10 from the original plan + D-08 pause-during-preroll + D-10 streams-queue-failover-after-handoff + malformed-preroll EOS bridge per checker WARNING-4/-5)
    - `grep -c 'def test_.*preroll\|def test_phase_83\|def test_streams_queue_failover_after_preroll_handoff' tests/test_player.py` outputs at least `13`
    - Specific new tests resolve: `uv run pytest tests/test_player.py::test_preroll_in_flight_pause_does_not_clear_flag tests/test_player.py::test_streams_queue_failover_after_preroll_handoff tests/test_player.py::test_preroll_eos_without_about_to_finish_advances_to_stream -x -q` exits 0
    - Drift-guard specifically: `uv run pytest tests/test_player.py::test_phase_83_preroll_drift_guard -x -q` exits 0
    - Negative-grep sanity: `grep -v '^[[:space:]]*#' musicstreamer/player.py | grep -c '"SomaFM"'` outputs at least `1` (the gate literal is in non-comment text)
    - Negative-grep sanity: `grep -v '^[[:space:]]*#' musicstreamer/player.py | grep -c '_last_preroll_played_at'` outputs at least `2` (field declaration + the gate's `time.monotonic() - ...` arithmetic)
    - Quick run: `uv run pytest tests/test_player.py tests/test_soma_import.py tests/test_repo.py -q` exits 0 (no regressions across the three quick-run files)
    - Full suite: `uv run pytest -q --tb=short` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `station.prerolls` (DB-sourced) → `Player._set_uri` → playbin3 | Plan 83-01's `Repo.insert_preroll` already validates URL schemes at the persistence boundary (ASVS V5 + parameterized SQL). Player reads from the validated table and feeds URLs to playbin3's `souphttpsrc`. No new external input surface. |
| GStreamer streaming thread → Qt main thread | Pre-existing trust posture: the streaming thread is project-internal but lacks a Qt event loop. The new `about-to-finish` callback respects this boundary via the class-level queued Signal pattern (Pattern 1; Phase 43.1 commit f1333ed precedent). |
| Daemon backfill worker → SQLite | Thread-local `Repo(db_connect())` via Pattern 4 (soma_import.py:398-402 precedent). Connection is owned by the worker for its lifetime; closed in `finally`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-10 | Denial of Service | concurrent backfill threads (user rapidly clicks Play on many SomaFM stations with `prerolls_fetched_at IS NULL`) | mitigate | `self._backfill_in_flight: set[int]` of station_ids gates `threading.Thread.start` calls in `Player.play`. Second call for an in-flight station_id is a no-op. Worker's `finally: self._backfill_in_flight.discard(station_id)` ensures a failed backfill can be retried on a later play. (RESEARCH §"Security Domain" DoS pattern; verified by D-03/D-13 behavioral test.) |
| T-83-11 | Tampering | preroll URL with non-HTTP(S) scheme reaches playbin3 | transfer to T-83-01 | All preroll URLs come from `station.prerolls` (DB-sourced) which Plan 83-01's `Repo.insert_preroll` validated at insert time. The backfill worker also catches `ValueError` per-URL and skips bad entries. Defense in depth: Player itself does not re-validate, relying on the upstream gate. Documented in plan invariant. |
| T-83-12 | Information Disclosure | preroll's m4a TAG title leaks to Now Playing UI | mitigate | `_on_gst_tag` early-returns when `_preroll_in_flight is True` (D-07). Pitfall 2 acceptable-edge-case: ~30ms one-frame leak if the m4a tag arrives between handler disconnect and flag clear — acceptable per D-07's "no UI flicker" intent (not "zero leak"). |
| T-83-13 | Tampering | streaming-thread callback corrupts pipeline by setting properties from the wrong thread | mitigate | `_on_preroll_about_to_finish_callback` body is EXACTLY ONE LINE: `self._preroll_about_to_finish_requested.emit()`. No property writes; no Qt API calls. (qt-glib-bus-threading Rule 2; Phase 43.1 f1333ed precedent; verified by source-grep drift-guard.) |
| T-83-14 | Tampering | hostile m4a content triggers a decoder bug | accept | Existing trust posture — gstreamer plugins (Linux apt + Windows conda-forge per Phase 69) are widely audited; SomaFM is a trusted source per RESEARCH §"Security Domain". Worst case (pipeline crash) is covered by `_on_gst_error` / `_handle_gst_error_recovery` (which now includes D-09 preroll-aware silent skip — added by this plan). |
| T-83-15 | Repudiation | silent backfill failures leave no audit trail | accept | D-04 silent-failure semantics. Backfill failures emit `_log.warning` to the project's existing logger (`musicstreamer.player` INFO level per Phase 62 + `_log.warning` always logged). User-visible UAT signal: a SomaFM station that never plays a preroll after multiple plays + waits has likely been renamed (Pitfall 3) — documented in 83-CONTEXT.md `<specifics>`. |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (player.py + test_player.py edits; stdlib + project-internal imports only). |
</threat_model>

<verification>
- `uv run pytest tests/test_player.py -k "preroll or phase_83 or streams_queue_failover_after_preroll_handoff" -x -q` — all 13 new tests pass (7 D-XX-mapped behavioral + D-08 pause-during-preroll + D-10 streams-queue-failover-after-handoff + malformed-preroll EOS bridge per checker WARNING-4/-5 + 2 ancillary behavioral + 1 drift-guard)
- `uv run pytest tests/test_player.py -x -q` — full Player suite green (Phase 47/62/74/82 regressions caught)
- Quick run: `uv run pytest tests/test_player.py tests/test_soma_import.py tests/test_repo.py -q` exits 0
- Full suite: `uv run pytest -q --tb=short` exits 0
- Source-grep invariants:
  - `grep -c '_preroll_about_to_finish_requested = Signal' musicstreamer/player.py` outputs `1`
  - `grep -c 'def _start_preroll\|def _on_preroll_about_to_finish_callback\|def _on_preroll_about_to_finish\|def _preroll_backfill_worker\|def _on_gst_eos_during_preroll' musicstreamer/player.py` outputs `5`
  - `grep -c 'bus.connect("message::eos"' musicstreamer/player.py` outputs `1` (live-spike Q3 RESOLVED — malformed-preroll EOS bridge)
  - `grep -v '^[[:space:]]*#' musicstreamer/player.py | grep -c '"SomaFM"'` outputs at least `1` (non-comment text — drift-guard pin)
  - `grep -v '^[[:space:]]*#' musicstreamer/player.py | grep -c '_last_preroll_played_at'` outputs at least `2` (non-comment text — drift-guard pin)
  - `grep -c '^from musicstreamer.repo import' musicstreamer/player.py` outputs `0` (Pitfall 9 invariant — no top-level Repo import in Player)
  - `grep -c 'def play_stream' musicstreamer/player.py` outputs `1` (D-08 — direct-play path untouched)
- Threading discipline (qt-glib-bus-threading Rule 2): visually verify via `git diff` that `_on_preroll_about_to_finish_callback` body contains EXACTLY ONE statement (`self._preroll_about_to_finish_requested.emit()`) — no property writes, no Qt API calls from the streaming thread.
- Visual diff sanity: `Player.play` queue-build retains Phase 82 head-of-queue logic; preroll gate is APPENDED, not a rewrite.
</verification>

<success_criteria>
- `Player.play(station)` honors the SomaFM provider gate (D-11) + 10-min throttle (D-12) + random preroll selection (D-05) + non-blocking lazy backfill (D-03/D-13) + single-flight backfill guard (T-83-10)
- `_streams_queue` content unchanged by preroll path (D-06); Phase 82 head-of-queue logic intact
- About-to-finish handoff uses class-level queued Signal + QueuedConnection (qt-glib-bus-threading Rule 2)
- `_on_gst_tag` suppresses TAG title during `_preroll_in_flight` (D-07)
- `_handle_gst_error_recovery` silently routes to `_try_next_stream` on preroll error (D-09 — no retry of a different preroll)
- Backfill worker uses thread-local `Repo(db_connect())` (Pattern 4); single-flight via `_backfill_in_flight.discard` in `finally`
- 13 new tests in tests/test_player.py pass (10 original + D-08 pause-during-preroll + D-10 streams-queue-failover-after-handoff + malformed-preroll EOS bridge); source-grep drift-guard pins `"SomaFM"` AND `_last_preroll_played_at` in non-comment text
- D-08: `Player.pause` / `Player.stop` during preroll do NOT clear `_preroll_in_flight` and do NOT invoke `_try_next_stream` (behavioral test pinned)
- D-10: failover via `_streams_queue` advance behaves exactly as Phase 82 after preroll handoff completes (behavioral test pinned)
- Live-spike Q3 RESOLVED: malformed-preroll EOS (no about-to-finish) is bridged via `bus.connect("message::eos", _on_gst_eos_during_preroll)` + queued `_try_next_stream_requested.emit()`; False branch is a no-op
- `Player.play_stream` unchanged (D-08 / Phase 82 D-04 direct-play path)
- Full suite green; no regressions in Phase 47/62/74/82 tests
</success_criteria>

<output>
Create `.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-03-SUMMARY.md` when done. Record:
- Exact line range in player.py of the new class-level Signal + __init__ wiring + 4 instance fields
- Exact line range of the new preroll gate inside `Player.play` (the `if station.provider_name == "SomaFM" and ...` block)
- File:line of each new method (`_start_preroll`, `_on_preroll_about_to_finish_callback`, `_on_preroll_about_to_finish`, `_preroll_backfill_worker`)
- File:line of the `_on_gst_tag` guard insertion
- File:line of the `_handle_gst_error_recovery` branch insertion
- File:line of the new `bus.connect("message::eos", self._on_gst_eos_during_preroll)` wire inside the bus-handler setup band at player.py:325-340
- File:line of the new `_on_gst_eos_during_preroll` helper method (live-spike Q3 RESOLVED bridge)
- Confirmation that `play_stream` was NOT modified (D-08)
- Confirmation that top-of-file imports do NOT add a top-level `from musicstreamer.repo import` (Pitfall 9 invariant)
- Drift-guard test name (`test_phase_83_preroll_drift_guard`) and exact non-comment grep expectation
- Test count (13 new tests in tests/test_player.py — 7 behavioral named per VALIDATION.md + D-08 pause-during-preroll + D-10 streams-queue-failover-after-handoff + malformed-preroll EOS bridge (WARNING-4/-5 per checker) + 2 ancillary behavioral + 1 drift-guard)
- Note any factory helper additions (e.g. `_make_station_ph83`)
- Recommend running the manual UAT items from VALIDATION.md §"Manual-Only Verifications" (Beat Blender preroll, Seven Inch Soul no-preroll, 10-min throttle) on Linux Wayland; record outcome in `83-HUMAN-UAT.md` per Phase 82 precedent
</output>
</content>
