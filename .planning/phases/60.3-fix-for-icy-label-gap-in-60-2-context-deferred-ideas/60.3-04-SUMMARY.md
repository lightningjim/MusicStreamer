---
phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
plan: 04
subsystem: ui
tags: [pyqt6, gbs.fm, no-downgrade, kick-split, track-change-reset, gap-closure, cr-01, verification-gap-1]

# Dependency graph
requires:
  - phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
    plan: 03
    provides: "on_title_changed body with no-downgrade early-return + idle-worker kick (the structure Plan 04 corrects); _on_gbs_playlist_ready with track_changed computation (Plan 04 hooks the flag reset into the existing `if track_changed:` block); _gbs_label_source tri-state flag (Plan 01)."
provides:
  - "on_title_changed restructured: `suppress_local_writes` guard variable wraps ONLY the local label/star/cover-art writes. The D-03 kick at the bottom always runs in GBS context when the worker is idle — no longer blocked by a stale 'ajax' flag from the prior track."
  - "_on_gbs_playlist_ready track-change branch resets `_gbs_label_source = None` so the next ICY tag for the new track is treated as a fresh write — flag flips back to 'ajax' when `_apply_gbs_icy_label` runs at the end of the slot."
  - "test_gbs_icy_label_new_track_after_ajax_resets_and_kicks — five-step regression test pinning the new-track-ICY scenario (kick fires despite stale 'ajax' flag; track-change branch resets flag; subsequent same-track ICY still suppressed; subsequent new-track ICY still kicks)."
  - "Closes CR-01 (60.3-REVIEW.md) and Verification Gap 1 (60.3-VERIFICATION.md). D-05 contract is now satisfied per CONTEXT.md line 41 (entryid coupling via track_changed branch)."
affects:
  - "Plan 05 (CR-02 / Verification Gap 2 closure for D-08 auth-expired): independent code path; no coupling."
  - "Plan 06 (CR-03 favourites refresh in _apply_gbs_icy_label + CR-04 _make_gbs_station factory fix): independent — Plan 04's new test ships the BLOCKER #1 explicit override pattern (gbs_station.icy_disabled = False) so the regression is locked even if Plan 06's factory fix is later reverted."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Guard-variable form for no-downgrade + kick split: `suppress_local_writes = is_gbs and _gbs_label_source == 'ajax'` followed by `if not suppress_local_writes:` wrapping the local writes; kick runs unconditionally below at method-body indent. Smaller diff against Plan 03 baseline than the REVIEW's two-separate-blocks form (REVIEW.md lines 60-79)."
    - "Track-change reset placed INSIDE the existing `if track_changed:` block (not in `_apply_gbs_icy_label`): keeps the cross-track / same-track distinction visible at the call site that has entryid context. The reset is a single-line assignment, not a new method or new Signal — preserves the no-new-Signal anti-pattern from RESEARCH §Anti-Patterns."

key-files:
  created: []
  modified:
    - "musicstreamer/ui_qt/now_playing_panel.py — on_title_changed body restructured (lines 529-598): `suppress_local_writes` guard variable replaces the early-return; kick at the bottom (lines 595-598) lives at method-body indent and always fires in GBS context when the worker is idle. _on_gbs_playlist_ready track_changed branch (lines 1033-1042) gains a 6-line addition: 5 lines of comment + `self._gbs_label_source = None` reset."
    - "tests/test_now_playing_panel.py — one new test test_gbs_icy_label_new_track_after_ajax_resets_and_kicks (lines 1404-1505) inserted between test_gbs_icy_label_no_downgrade_after_ajax and test_gbs_on_title_changed_kicks_poll_when_idle. Sets gbs_station.icy_disabled = False per BLOCKER #1 (count rises from 8 to 9). Five-step scenario; Step 3 captures post-kick token because the wrapped MagicMock spy exercises the real _on_gbs_poll_tick which increments _gbs_poll_token."

key-decisions:
  - "Combined option (b) + option (c) from REVIEW.md, not just one. Option (b) alone (track-change reset only) relies on _on_gbs_playlist_ready firing for every track transition; if /ajax fails transiently, the flag never resets and we regress. Option (c) alone (kick split only) lets the kick fire, but local label writes stay suppressed so the user sees Track A's label until /ajax responds (~200 ms via kick, but visibly stale). Combined, local writes happen immediately on track change AND the kick guarantees a /ajax upgrade even if the same-track guard mis-fires."
  - "Guard-variable form (`suppress_local_writes`) chosen over the REVIEW's two-separate-blocks form (REVIEW.md lines 68-78). Rationale: smaller diff against Plan 03 baseline and easier to review against a single function's existing shape. Functionally equivalent — the kick is the LAST statement in both forms."
  - "Track-change reset placed in the existing `if track_changed:` block, NOT inside _apply_gbs_icy_label. Rationale: the cross-track / same-track distinction is most visible at the call site that has entryid context (_on_gbs_playlist_ready already computes track_changed via `new_entryid != prev_entryid`). Putting the reset in the helper would couple it to the absent-entryid case (helper has no entryid input), which is the wrong pivot."
  - "Test Step 3 uses `current_token = panel._gbs_poll_token` instead of hard-coding 5. Step 2's kick exercises the wrapped real _on_gbs_poll_tick (via `MagicMock(wraps=panel._on_gbs_poll_tick)`), which increments _gbs_poll_token to 6. Hard-coding 5 in Step 3 would trigger the slot's stale-token discard at line 1021 and the second _apply_gbs_icy_label would never run. This is documented inline in the test and captured here so future maintainers don't re-introduce the bug. Original plan-text used `panel._on_gbs_playlist_ready(5, ...)` in Step 3 — Plan 04 corrects this in the implementation while preserving all five assertion steps verbatim."

requirements-completed: []  # Phase 60.3 has no formal REQ-IDs; acceptance == CONTEXT.md decisions D-NN. CR-01 / Verification Gap 1 closed.

# Metrics
duration: ~25min
completed: 2026-05-07
---

# Phase 60.3 Plan 04: D-05 Over-Fire Fix (CR-01 / Verification Gap 1) Summary

**Closes CR-01 and Verification Gap 1: split the D-03 kick out of the D-05 no-downgrade early-return so a stale 'ajax' flag from a prior track does not block the kick on a server-side track change; reset `_gbs_label_source = None` in the `if track_changed:` branch of `_on_gbs_playlist_ready` so the next ICY tag for the new track is treated as a fresh write. Combined fix (option (b) + option (c) from REVIEW.md) closes both halves of the gap with a minimal diff.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-05-07T15:39:48Z
- **Completed:** 2026-05-07T16:04:47Z
- **Tasks:** 2 (Task 1 implementation, Task 2 regression test)
- **Files modified:** 2 (`musicstreamer/ui_qt/now_playing_panel.py`, `tests/test_now_playing_panel.py`)
- **Commits:** 2 (`3559b81 fix(60.3-04): ...`, `2679391 test(60.3-04): ...`)

## Accomplishments

- **`on_title_changed` restructured (now_playing_panel.py:529-598).** Replaced `if is_gbs and self._gbs_label_source == 'ajax': return` with `suppress_local_writes = is_gbs and self._gbs_label_source == 'ajax'` followed by `if not suppress_local_writes:` wrapping ONLY the local label/star/cover-art writes. The D-03 kick (`if is_gbs and not self._gbs_poll_in_flight(): self._on_gbs_poll_tick()`) is now the LAST statement at method-body indent — it ALWAYS runs in GBS context when the worker is idle, regardless of source-flag state. Pre-fix: a server-side track change with stale 'ajax' flag silently waited up to 15 s for the next periodic poll. Post-fix: kick fires within one ICY-tag arrival, /ajax round-trip ~200 ms.
- **`_on_gbs_playlist_ready` track-change reset (now_playing_panel.py:1033-1042).** Inside the existing `if track_changed:` block (which already resets the position cursor for HIGH 4), added `self._gbs_label_source = None`. Sequence: (1) detect track_changed via `new_entryid != prev_entryid`, (2) reset position cursor, (3) reset source flag to None, (4) ... cursor + entryid + vote work ..., (5) call `_apply_gbs_icy_label` at slot end which flips the flag back to 'ajax' for the new track. Same-track late ICY arrivals after this slot still see flag == 'ajax' and the no-downgrade guard catches them — the original D-05 mechanism is preserved unchanged.
- **One new regression test (test_now_playing_panel.py:1404-1505).** `test_gbs_icy_label_new_track_after_ajax_resets_and_kicks` exercises five steps: (1) /ajax stamps Track A, flag = 'ajax'; (2) on_title_changed("Track B ICY") suppresses local writes (still flag=='ajax') BUT the kick fires; (3) /ajax responds with NEW entryid → track_changed branch resets flag → `_apply_gbs_icy_label` re-stamps Track B; (4) same-track late ICY for Track B still suppressed (D-05 preserved); (5) Track C ICY still kicks (kick is independent of no-downgrade guard). Sets `gbs_station.icy_disabled = False` per BLOCKER #1 (override count rises 8 → 9).
- **All 8 must_haves.truths verified.** See section below.

## must_haves.truths Verification

1. **D-05 (revised): no-downgrade guard fires ONLY for same-track ICY-after-/ajax ordering races; on a server-side track change, a new bare ICY title writes the label, updates `_last_icy_title`, and the D-03 kick still fires.** VERIFIED. Step 4 of the new test asserts same-track suppression preserved (`panel.icy_label.text() == "Artist B - Track B"` after `panel.on_title_changed("Track B")`). Step 3 asserts cross-track upgrade works after the track-change reset. The early-return form is grep-verified gone (`grep -c "if is_gbs and self._gbs_label_source == 'ajax': return"` returns 0).
2. **D-03 (re-enforced): in GBS context, an ICY tag arrival kicks `_on_gbs_poll_tick()` when the worker is idle, even when the previous track was /ajax-stamped (`_gbs_label_source == 'ajax'`). The kick lives OUTSIDE the no-downgrade early-return.** VERIFIED. Step 2 asserts `tick_spy.call_count == 1` after `on_title_changed("Track B ICY")` with flag still 'ajax'. Step 5 asserts the same after Track B is /ajax-stamped (kick fires for hypothetical Track C ICY). Grep-verified: `grep -cE "^        if is_gbs and not self\._gbs_poll_in_flight\(\):"` returns 1 (kick at method-body indent, 8 spaces).
3. **Track-change reset: when `_on_gbs_playlist_ready` detects `track_changed is True`, the source flag is reset to `None` BEFORE delegating to `_apply_gbs_icy_label` so the next ICY for the new track is unsuppressed.** VERIFIED. Inside `if track_changed:` block (lines 1033-1042): position cursor reset (HIGH 4) + flag reset (Plan 04). `_apply_gbs_icy_label` is called as the last statement of the slot (line 1071) and flips the flag back to 'ajax' after writing the canonical label. Grep-verified: `grep -A8 "if track_changed:" | grep -c "self._gbs_label_source = None"` returns 1.

## key_links Verification

- `_on_gbs_playlist_ready` → track-change flag reset, via `if track_changed:` block (pattern `if track_changed:\s*\n\s+self\._gbs_label_source = None`): WIRED. Note: pattern includes 5 intervening comment lines + the position cursor reset; the reset is the 6th line in the block, not the 1st. Grep with `-A8` confirms presence; semantic equivalence with the planner's `-A2` pattern is preserved (the reset is unambiguously inside the `if track_changed:` body).
- `on_title_changed` → `_on_gbs_poll_tick` kick (always, when idle in GBS context), via the kick living below the early-return scope (pattern `if is_gbs and not self\._gbs_poll_in_flight\(\):`): WIRED at method-body indent (8 spaces). Grep-verified.

## Verification Gap 1 Closure (60.3-VERIFICATION.md lines 7-28)

The verifier's "missing" list specified two items; both addressed:

- **"Reset _gbs_label_source to None in _on_gbs_playlist_ready when track_changed is True (line ~1018), OR carry the entryid into the flag state and compare, OR split the kick out of the early-return so it always runs in GBS context regardless of source flag."** — Plan 04 implements TWO of the three (reset in track_changed branch + split the kick), per the option (b) + option (c) combined-fix rationale documented in 60.3-04-PLAN.md objective.
- **"Add a test: /ajax stamps Track A, on_title_changed fires with DIFFERENT title (Track B ICY) -> label updates to Track B bare title and kick fires."** — `test_gbs_icy_label_new_track_after_ajax_resets_and_kicks` covers this exactly. NOTE: the test asserts that the bare ICY label DOES NOT immediately appear; instead the kick triggers a /ajax round-trip and Step 3 verifies the canonical Artist - Title appears via the track-change reset. This is intentionally MORE STRICT than the verifier's "missing" item: it pins the actual user-observable outcome (full Artist - Title within one /ajax round-trip) rather than the intermediate bare-title flash. Steps 4 + 5 add same-track suppression and subsequent-new-track-kick assertions for completeness.

## REVIEW CR-01 Closure (60.3-REVIEW.md lines 39-79)

The reviewer recommended option (a) entryid-tuple OR option (b) track-change reset, with optional combination of option (c) kick split. Plan 04 picks (b) + (c) — see CONTEXT.md D-05 line 41 ("title-suffix match OR entryid coupling"). The track_changed branch IS entryid coupling — entryid identity decides whether the flag resets. The implementation is the smaller diff than carrying entryid into the source-flag tuple and avoids the `Optional[Tuple[str, int]]` type complexity.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test Step 3 hardcoded token=5 would trigger stale-token discard**

- **Found during:** Task 2 first pytest run.
- **Issue:** The plan-text inserted `panel._on_gbs_playlist_ready(5, {...})` for Step 3. The wrapped `MagicMock(wraps=panel._on_gbs_poll_tick)` spy in Step 2 actually invokes the real `_on_gbs_poll_tick`, which increments `_gbs_poll_token` from 5 to 6. The slot's token-discard guard at `now_playing_panel.py:1021` then drops the Step 3 call as stale, `_apply_gbs_icy_label` never runs, and the assertion `panel.icy_label.text() == "Artist B - Track B"` fails (label still shows "Artist A - Track A"). This is a defect in the plan-text, not the implementation.
- **Fix:** Capture `current_token = panel._gbs_poll_token` after the kick has fired (Step 2) and pass `current_token` to the Step 3 `_on_gbs_playlist_ready` call. Inline comment documents the reasoning for future maintainers. All five assertion steps preserved verbatim.
- **Files modified:** `tests/test_now_playing_panel.py` (test as committed).
- **Commit:** `2679391`.

### Notes (not deviations)

- **Plan-text grep baseline mismatch (informational, no fix needed):** Plan 04 acceptance criteria stated "`grep -c "setTextFormat\|setHtml\|RichText" musicstreamer/ui_qt/now_playing_panel.py` matches Plan 03 baseline (1)". Actual baseline is 4 (3× `setTextFormat` calls at lines 250, 266, 280 + 1× `RichText` mention in a comment at line 255). Plan 04 does not touch any of these — post-edit count is 4, semantic invariant ("count unchanged from prior commit") holds. The plan-text's "1" was a documentation slip; the real plain-text invariant is preserved.
- **Plan-text grep window mismatch (informational, no fix needed):** Plan 04 acceptance criteria for the track_changed reset stated `grep -A2 "if track_changed:" | grep -c "self._gbs_label_source = None"` returns 1. Plan 04's implementation places the reset on the 6th line of the `if track_changed:` block (after a 4-line explanatory comment and the existing position-cursor reset), not the 1st. With `-A2` the reset is outside the window and the grep returns 0; with `-A8` it returns 1. Functionally equivalent — both forms place the reset inside the same block — but the plan-text's narrow `-A2` window predates the comment block. SUMMARY uses `-A8` for verification.

## Self-Check: PASSED

- File `musicstreamer/ui_qt/now_playing_panel.py` exists: FOUND.
- File `tests/test_now_playing_panel.py` exists: FOUND.
- Commit `3559b81` (Task 1 implementation): FOUND in `git log --oneline`.
- Commit `2679391` (Task 2 test): FOUND in `git log --oneline`.
- 89 panel tests pass (`uv run pytest tests/test_now_playing_panel.py -x -q`): VERIFIED.
- 37 GBS tests pass (`uv run pytest tests/test_now_playing_panel.py -k gbs -x -q`): VERIFIED.
- New test passes (`uv run pytest tests/test_now_playing_panel.py::test_gbs_icy_label_new_track_after_ajax_resets_and_kicks -x -q`): VERIFIED.
- All 11 success criteria from PLAN: VERIFIED (see grep run before Task 2 commit).
