---
phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
verified: 2026-05-07T17:30:00Z
status: human_needed
score: 8/8 D-NN must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/8
  gaps_closed:
    - "D-05 (CR-01 / Gap 1): no-downgrade guard over-fires on new-track ICY arrivals"
    - "D-08 (CR-02 / Gap 2): auth-expired fallback does not relax bridge gate when cookies persist"
  gaps_remaining: []
  regressions: []
  review_findings_closed:
    - "CR-01 (Critical) — closed by Plan 04 (kick split + track-change flag reset)"
    - "CR-02 (Critical) — closed by Plan 05 (_gbs_ajax_disabled flag, preserves Phase 60 D-04)"
    - "CR-03 (Critical) — closed by Plan 06 (_refresh_star_display shared helper)"
    - "CR-04 (Critical) — closed by Plan 06 (_make_gbs_station factory icy_disabled=False default)"
    - "WR-01 (Warning) — closed by Plan 06 (provider guard in _apply_gbs_icy_label)"
    - "WR-02 (Warning) — closed by Plan 05 (asymmetric lifecycle reset; same-station rebind preserves)"
    - "WR-03 (Warning) — closed by Plan 06 (getattr at on_title_changed + _apply_gbs_icy_label)"
    - "WR-04 (Warning) — closed by Plan 06 (docstring note on bare-vs-full favourite-row distinctness)"
    - "IN-01 (Info) — closed by Plan 05 (apology comment replaced with assertions)"
    - "IN-02 (Info) — closed by Plan 06 (symbol refs replace stale line numbers)"
human_verification:
  - test: "Live GBS.FM stream — first-bind bridge-window UX"
    expected: "Bind a freshly-authenticated GBS.FM station. Within ~5 s the label briefly shows the bare ICY title with the star button greyed out, then upgrades to the full `Artist - Title` and the star button becomes enabled. No cover-art request is issued during the bridge."
    why_human: "Real /ajax round-trip timing (~200–500 ms) and GStreamer ICY-tag parse latency cannot be reproduced deterministically in unit tests; the bridge-window UX is sub-second visual state."
  - test: "Live GBS.FM stream — track-change label upgrade (CR-01 / Gap 1 closure)"
    expected: "While bound to a GBS.FM station that has already been /ajax-stamped, wait for a server-side track change. Within ~1 s of the new ICY tag arriving, an immediate /ajax kick fires (D-03) and the label upgrades to the new track's full `Artist - Title`. The user does NOT see the previous track's label persist for 15 s."
    why_human: "Requires live GStreamer ICY-tag sequencing across a real track transition; the kick + /ajax round-trip timing is observable only with a live authenticated GBS session."
  - test: "Live GBS.FM stream — auth-expired fallback UX (CR-02 / Gap 2 closure)"
    expected: "While bound to a GBS.FM station, let the session expire mid-run (or simulate by removing/invalidating cookies while the app is running). After the next poll fails with auth_expired, the bare ICY title continues to update from `on_title_changed`, the star button stays enabled (favourites can still be saved against bare titles), and cover-art lookup fires against the bare title."
    why_human: "Requires a real GBS session expiry event; cannot simulate the cookie-still-on-disk-but-stale state in unit tests without bypassing _is_gbs_logged_in()."
  - test: "Live GBS.FM stream — star icon refresh on /ajax upgrade (CR-03 closure)"
    expected: "Favourite a GBS.FM track under its full `Artist - Title` key while bound to the station. Trigger a re-bind (or wait for the next /ajax poll). When the bare ICY arrives, the star may briefly show non-starred (bare key not in favourites). When /ajax stamps the canonical title, the star icon flips to starred and the tooltip reads 'Remove track from favorites'."
    why_human: "Requires the icon to be visually inspected during the bridge transition; unit tests assert the underlying `setChecked(True)` / `setIcon` calls, but the actual rendered icon and tooltip text are user-observable only on the running app."
---

# Phase 60.3: Fix for ICY Label Gap Verification Report (Re-Verification After Gap Closure)

**Phase Goal:** When a GBS.FM station is the bound provider, `now_playing_panel.icy_label` shows the full `Artist - Title` (sourced from the existing `/ajax` poll) — bringing GBS.FM to parity with SomaFM-style ICY streams. Bridge window: bare ICY title is shown briefly before `/ajax` stamps; star/cover-art are gated during the bridge to avoid partial-title favorites and redundant iTunes lookups. Auth-expired / not-logged-in falls back to bare-ICY-as-truth.

**Verified:** 2026-05-07T17:30:00Z
**Status:** human_needed (all 8 D-NN must-haves verified in code; 4 live-stream behaviours require manual UAT)
**Re-verification:** Yes — second pass after gap-closure cycle (Plans 60.3-04, 60.3-05, 60.3-06)

## Re-Verification Summary

The previous VERIFICATION.md (`6f17c1d` review + `a9e18f9` verification, 2026-05-07T15:30Z) returned `gaps_found` with 6/8 D-NN truths verified and 2 BLOCKER gaps:

- **Gap 1 (D-05 / CR-01):** No-downgrade guard over-fires on new-track ICY arrivals.
- **Gap 2 (D-08 / CR-02):** Auth-expired bridge gate does not relax when cookies persist on disk.

Three gap-closure plans were planned and executed (commits `3559b81`, `2679391`, `6dad27d`, `5928bfb`, `d923665`, `8d61b9c`, `ef71b9e`, `2df2627`, `7dcdedc` — HEAD `7dcdedc`). All four BLOCKER (CR-01..CR-04), four WARNING (WR-01..WR-04), and two INFO (IN-01, IN-02) findings from `60.3-REVIEW.md` are now closed in code, and both VERIFICATION blocker gaps are resolved.

This re-verification re-evaluated all 8 D-NN truths from scratch (not just the previously-failed two), confirmed each REVIEW finding closure against the actual code state, and ran the full test suite.

## Goal Achievement

### Observable Truths (D-01..D-08)

| #   | D-NN | Truth                                                                                                              | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                  |
| --- | ---- | ------------------------------------------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | D-01 | `/ajax` response upgrades `icy_label` from bare ICY to full `Artist - Title`                                      | ✓ VERIFIED  | `_apply_gbs_icy_label` at `now_playing_panel.py:1203-1267` is invoked from `_on_gbs_playlist_ready:1138`. Stamps `icy_label.setText(icy_title)` (line 1255) + flips `_gbs_label_source = 'ajax'` (line 1257). Tests `test_gbs_icy_label_upgrades_on_ajax_after_icy`, `test_gbs_ajax_stamps_label_first_no_prior_icy`, `test_gbs_playlist_populates_from_mock_state` all green. |
| 2   | D-02 | ICY title-only acts as transient bridge; `on_title_changed` continues writing `icy_label` in GBS context           | ✓ VERIFIED  | `on_title_changed:570-577` writes `icy_label`, `_last_icy_title`, then sets `_gbs_label_source = 'icy'` when in GBS context. Last-writer-wins semantics preserved.                                                                                                                                                                                            |
| 3   | D-03 | ICY tag in GBS context kicks `_on_gbs_poll_tick()` when worker is idle                                            | ✓ VERIFIED  | Kick at `on_title_changed:607-608` lives at method-body indent (8 spaces) — OUTSIDE the `if not suppress_local_writes:` block. Plan 04 split the kick out of the early-return so a stale `'ajax'` flag never blocks it. Tests `test_gbs_on_title_changed_kicks_poll_when_idle`, `test_gbs_icy_label_new_track_after_ajax_resets_and_kicks` Step 2 green.       |
| 4   | D-04 | Kick is skipped when `_GbsPollWorker` is in flight                                                                  | ✓ VERIFIED  | `_gbs_poll_in_flight()` predicate at `now_playing_panel.py:1032-1046`. Read at line 607 (`if is_gbs and not self._gbs_poll_in_flight(): self._on_gbs_poll_tick()`). Test `test_gbs_on_title_changed_skips_kick_when_worker_in_flight` green.                                                                                                              |
| 5   | D-05 | No-downgrade guard prevents bare ICY from overwriting /ajax-stamped label for the same track                      | ✓ VERIFIED  | **Previously FAILED — now closed by Plan 04.** Guard at `on_title_changed:569-570` uses `suppress_local_writes` variable (NOT early-return), so D-03 kick still fires. Track-change reset at `_on_gbs_playlist_ready:1089-1097` clears flag to `None` when entryid changes. Tests `test_gbs_icy_label_no_downgrade_after_ajax` (same-track preserved) + `test_gbs_icy_label_new_track_after_ajax_resets_and_kicks` (cross-track unblocked) both green. The literal early-return form `if is_gbs and self._gbs_label_source == 'ajax': return` is grep-verified absent (count = 0). |
| 6   | D-06 | `_last_icy_title`, favorites key, cover-art string, AND star display all driven by same full `Artist - Title` after /ajax stamps | ✓ VERIFIED  | **Previously verified with WARNING — star display warning closed by Plan 06.** `_apply_gbs_icy_label` now calls `_refresh_star_display(icy_title)` at line 1261 (after `_update_star_enabled()`). Shared helper at line 715-740 is the single source of truth for star icon/checked/tooltip; called from both `on_title_changed:582` and `_apply_gbs_icy_label:1261`. Tests `test_gbs_apply_icy_label_refreshes_star_icon_when_favorited` and `test_gbs_apply_icy_label_refreshes_star_icon_when_not_favorited` green. |
| 7   | D-07 | During bridge window (logged-in + /ajax pending), `star_btn` disabled and cover-art suppressed; both come alive when /ajax stamps | ✓ VERIFIED  | `_update_star_enabled` bridge predicate at lines 699-707; `on_title_changed` `in_bridge_window` at lines 587-592. Both gates now include `and not self._gbs_ajax_disabled` conjunct (Plan 05). `_apply_gbs_icy_label` calls `_update_star_enabled()` and `_fetch_cover_art_async` after stamping. Test `test_gbs_star_disabled_during_bridge_window` green. |
| 8   | D-08 | Auth-expired / not-logged-in fallback: bare ICY is truth, star/cover-art remain enabled                            | ✓ VERIFIED  | **Previously PARTIAL (logged-out only) — now full closure by Plan 05.** Logged-out path: `_is_gbs_logged_in()` returns False, gate relaxes (test `test_gbs_logged_out_icy_writes_normally` green). Auth-expired path: `_on_gbs_playlist_error('auth_expired')` sets `self._gbs_ajax_disabled = True` at line 1165 alongside `_gbs_label_source = 'icy'`. Bridge predicate's `and not self._gbs_ajax_disabled` conjunct relaxes the gate even though cookies persist on disk (Phase 60 D-04 invariant preserved). Test `test_gbs_auth_expired_relaxes_bridge_gate` now asserts `panel.star_btn.isEnabled() is True` and `panel._gbs_ajax_disabled is True` (the 13-line apology comment at IN-01 was replaced with real assertions by Plan 05). |

**Score:** 8/8 truths verified. All previously-failing items (D-05, D-08) and previously-warning item (D-06 star display) are now fully verified.

### Required Artifacts

| Artifact                                                | Expected                                                                          | Status      | Details                                                                                                                                                              |
| ------------------------------------------------------- | --------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `_gbs_label_source: Optional[str] = None` declared in `__init__`                   | ✓ VERIFIED  | Line 437                                                                                                                                                             |
| `musicstreamer/ui_qt/now_playing_panel.py`              | **`_gbs_ajax_disabled: bool = False` declared in `__init__` (NEW — Plan 05)**        | ✓ VERIFIED  | Line 446 — adjacent to `_gbs_label_source` per Plan 01 placement convention                                                                                          |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `bind_station` resets `_gbs_ajax_disabled = False` (NEW — Plan 05)                | ✓ VERIFIED  | Line 524                                                                                                                                                             |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `_refresh_gbs_visibility` resets both flags on leaving-GBS branch                  | ✓ VERIFIED  | `_gbs_label_source = None` at lines 1003 (entry) + 1024 (leave); `_gbs_ajax_disabled = False` at line 1029 (leave only — WR-02 invariant: NOT reset on `should_show=True`) |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `_gbs_poll_in_flight()` predicate                                                  | ✓ VERIFIED  | Lines 1032-1046; docstring rewritten with symbol refs (IN-02 closure)                                                                                                |
| `musicstreamer/ui_qt/now_playing_panel.py`              | **`_refresh_star_display(title)` shared helper (NEW — Plan 06)**                  | ✓ VERIFIED  | Lines 715-740. Called from both `on_title_changed:582` and `_apply_gbs_icy_label:1261`                                                                              |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `_apply_gbs_icy_label(icy_title)` helper                                          | ✓ VERIFIED  | Lines 1203-1267 — now includes WR-01 provider guard (line 1250), WR-03 getattr (line 1253), WR-04 docstring note, CR-03 `_refresh_star_display` call (line 1261)    |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `on_title_changed` GBS branch with kick split out of early-return + WR-03 getattr | ✓ VERIFIED  | Lines 543-608 — `suppress_local_writes` guard variable (line 569); kick at method-body indent (line 607); WR-03 getattr at line 548                                  |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `_update_star_enabled` bridge gate with `and not _gbs_ajax_disabled` conjunct      | ✓ VERIFIED  | Lines 699-707; docstring at lines 681-697 documents both D-08 paths                                                                                                  |
| `musicstreamer/ui_qt/now_playing_panel.py`              | `_on_gbs_playlist_error` auth_expired branch sets both flags                       | ✓ VERIFIED  | Lines 1149-1165: `_gbs_label_source = 'icy'` (line 1157) + `_gbs_ajax_disabled = True` (line 1165)                                                                  |
| `tests/test_now_playing_panel.py`                       | `_make_gbs_station` factory sets `s.icy_disabled = False` (NEW — Plan 06)         | ✓ VERIFIED  | Line 969 (factory body); docstring at lines 951-961 documents the BLOCKER #1 invariant                                                                                |
| `tests/test_now_playing_panel.py`                       | 16 GBS-cluster tests covering 60.3 (12 from Plans 01-03 + 4 from Plans 04-06)      | ✓ VERIFIED  | All located via grep: lines 1017, 1255, 1307, 1344, 1395, 1429, 1540, 1571, 1603, 1640, 1681, 1707, 1763, 1797, 1830, 1888                                          |

### Key Link Verification

| From                                  | To                                                              | Via                                                                | Status     | Details                                                                                                                                  |
| ------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `_on_gbs_playlist_ready`              | `_apply_gbs_icy_label`                                          | direct call as last statement                                      | ✓ WIRED    | Line 1138                                                                                                                                |
| `_on_gbs_playlist_ready` track_changed | `_gbs_label_source = None` reset (Plan 04 / CR-01)               | inside existing `if track_changed:` block                          | ✓ WIRED    | Lines 1089-1097 (5 comment lines + assignment)                                                                                            |
| `_apply_gbs_icy_label`                | `_gbs_label_source = 'ajax'`                                    | assignment after icy_label/setText                                 | ✓ WIRED    | Line 1257                                                                                                                                |
| `_apply_gbs_icy_label`                | `_refresh_star_display(icy_title)` (Plan 06 / CR-03)            | call after `_update_star_enabled()`                                | ✓ WIRED    | Line 1261                                                                                                                                |
| `_apply_gbs_icy_label`                | WR-01 provider guard                                            | early-return when `provider_name != "GBS.FM"`                      | ✓ WIRED    | Line 1250                                                                                                                                |
| `on_title_changed`                    | `_refresh_star_display(title)` (Plan 06 / CR-03)                | call inside `if not suppress_local_writes:` block                  | ✓ WIRED    | Line 582                                                                                                                                 |
| `on_title_changed`                    | suppress-vs-kick split (Plan 04 / CR-01)                        | `suppress_local_writes` variable + kick at method-body indent      | ✓ WIRED    | Lines 569 (assign), 570 (guard), 607-608 (kick at 8-space indent)                                                                        |
| `on_title_changed`                    | `_on_gbs_poll_tick()` kick (always, when idle in GBS context)    | kick lives below early-return scope, runs unconditionally          | ✓ WIRED    | Line 607-608                                                                                                                             |
| `_on_gbs_playlist_error('auth_expired')` | `_gbs_label_source = 'icy'` + `_gbs_ajax_disabled = True`     | both writes inside token-discard guard                             | ✓ WIRED    | Lines 1149-1165 (token guard line 1149; flag flips lines 1157, 1165)                                                                    |
| `_update_star_enabled` bridge gate    | `_gbs_ajax_disabled` conjunct (Plan 05 / CR-02)                 | `and not self._gbs_ajax_disabled` in predicate                     | ✓ WIRED    | Line 704                                                                                                                                 |
| `on_title_changed` cover-art bridge   | `_gbs_ajax_disabled` conjunct (Plan 05 / CR-02)                 | `and not self._gbs_ajax_disabled` in `in_bridge_window` predicate  | ✓ WIRED    | Line 590                                                                                                                                 |
| `bind_station`                        | `_gbs_ajax_disabled = False` reset (Plan 05 / WR-02 corollary)  | direct assignment after `star_btn.setChecked(False)`               | ✓ WIRED    | Line 524                                                                                                                                 |
| `_refresh_gbs_visibility` (leaving-GBS) | `_gbs_ajax_disabled = False` reset (Plan 05 / WR-02 corollary) | inside `if not should_show:` body, after `_gbs_label_source = None` | ✓ WIRED    | Line 1029. NOT present in `should_show=True` branch (deliberate WR-02 preservation invariant — verified by absence)                       |

### Data-Flow Trace (Level 4)

| Artifact                | Data Variable                    | Source                                         | Produces Real Data                                  | Status       |
| ----------------------- | -------------------------------- | ---------------------------------------------- | --------------------------------------------------- | ------------ |
| `icy_label` (QLabel)    | `icy_title` from `state["icy_title"]` | `gbs_api.fetch_active_playlist` via `_fold_ajax_events` | Yes (gbs.fm /ajax metadata event delivers full `Artist - Title`) | ✓ FLOWING    |
| `_last_icy_title`        | same                             | `_apply_gbs_icy_label` or `on_title_changed`   | Yes                                                 | ✓ FLOWING    |
| `star_btn.isChecked()`  | `is_favorited()` call            | repo favourites store                          | **NOW correctly refreshed on /ajax upgrade** via `_refresh_star_display(canonical_title)` (Plan 06 / CR-03) | ✓ FLOWING (no longer HOLLOW) |
| `star_btn.setIcon(...)` | derived from `is_fav` boolean    | `_refresh_star_display` (lines 734-737)        | Yes — both bare-title and canonical-title paths refresh icon | ✓ FLOWING    |

### Behavioral Spot-Checks

| Behavior                                                | Command                                                                                                          | Result                                | Status |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------ |
| All panel tests pass                                    | `uv run pytest tests/test_now_playing_panel.py -x -q`                                                           | 94 passed, 1 deprecation warning      | ✓ PASS |
| GBS cluster all green                                   | `uv run pytest tests/test_now_playing_panel.py -k gbs -x -q`                                                    | 42 passed, 52 deselected               | ✓ PASS |
| All 16 60.3-related tests pass                          | `uv run pytest tests/test_now_playing_panel.py::<16 named tests> -v`                                             | 16 passed                             | ✓ PASS |
| Module imports cleanly                                  | `uv run python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"`                          | exit 0                                | ✓ PASS |
| Stale early-return form is absent (CR-01 grep gate)     | `grep -c "if is_gbs and self._gbs_label_source == 'ajax': return" musicstreamer/ui_qt/now_playing_panel.py`     | 0                                     | ✓ PASS |
| `suppress_local_writes` guard variable present          | `grep -c "suppress_local_writes" musicstreamer/ui_qt/now_playing_panel.py`                                       | 2 (1 assign + 1 read)                 | ✓ PASS |
| Track-change reset present in `if track_changed:` block | `grep -A8 "if track_changed:" musicstreamer/ui_qt/now_playing_panel.py \| grep -c "self._gbs_label_source = None"` | 1                                     | ✓ PASS |
| `_gbs_ajax_disabled` flag declared + 6 sites            | `grep -c "_gbs_ajax_disabled" musicstreamer/ui_qt/now_playing_panel.py`                                          | 14 (counts comments + assignments + reads) | ✓ PASS |
| Bridge predicates have new conjunct (both sites)        | `grep -c "and not self._gbs_ajax_disabled" musicstreamer/ui_qt/now_playing_panel.py`                              | 2                                     | ✓ PASS |
| `_refresh_star_display` helper extracted (Plan 06)      | `grep -c "_refresh_star_display" musicstreamer/ui_qt/now_playing_panel.py`                                       | 14 (1 def + 2 call sites + comments)  | ✓ PASS |
| WR-03 getattr at slot sites (3 sites: bind_station, on_title_changed, _apply_gbs_icy_label) | `grep -c 'getattr(self._station, "icy_disabled", False)' musicstreamer/ui_qt/now_playing_panel.py` | 3 | ✓ PASS |
| IN-02 stale line refs gone in `_gbs_poll_in_flight` docstring | `awk '/def _gbs_poll_in_flight/,/return self._gbs_poll_worker/' musicstreamer/ui_qt/now_playing_panel.py \| grep -c "line ~404\|line ~928\|line 941"` | 0 | ✓ PASS |
| `_make_gbs_station` factory default (CR-04)             | `awk '/def _make_gbs_station/,/return s/' tests/test_now_playing_panel.py \| grep -c "s.icy_disabled = False"`   | 1                                     | ✓ PASS |
| `test_gbs_auth_expired_relaxes_bridge_gate` apology comment removed (IN-01) | `grep -c "the gate may still close" tests/test_now_playing_panel.py` | 0 | ✓ PASS |
| `test_gbs_auth_expired_relaxes_bridge_gate` has the missing assertion (IN-01) | `grep -c "panel.star_btn.isEnabled() is True" tests/test_now_playing_panel.py` | 2 (auth_expired test + WR-02 preservation test) | ✓ PASS |
| Phase 60 D-04 invariant preserved (no cookie deletion added) | `git log --all -p -- musicstreamer/ui_qt/now_playing_panel.py \| grep -E "os\.remove.*cookies\|os\.unlink.*cookies"` | no matches in Plan 04/05/06 commits | ✓ PASS |

### Requirements Coverage

Phase 60.3 has no formal REQ-IDs in `REQUIREMENTS.md`; the acceptance contract is `60.3-CONTEXT.md` decisions D-01 through D-08 (verified above) plus `60.3-REVIEW.md` findings CR-01..CR-04, WR-01..WR-04, IN-01..IN-02 (all closed below) plus the previous `60.3-VERIFICATION.md` Gap 1 + Gap 2 (both closed).

### REVIEW Findings Closure (60.3-REVIEW.md)

| Finding | Severity | Closed By | Verification Site                                                                                                                                                |
| ------- | -------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CR-01: D-05 over-fires on track change                                | Critical | Plan 04   | `now_playing_panel.py:569` (`suppress_local_writes` guard variable replaces early-return); `:1097` (track-change flag reset); `test_gbs_icy_label_new_track_after_ajax_resets_and_kicks` regression-lock |
| CR-02: D-08 auth-expired bridge gate not relaxing                     | Critical | Plan 05   | `now_playing_panel.py:446` (`_gbs_ajax_disabled` declared); `:1165` (set on auth_expired); `:704` + `:590` (read in both bridge predicates); `test_gbs_auth_expired_relaxes_bridge_gate` upgraded with assertions |
| CR-03: `_apply_gbs_icy_label` does not refresh star display           | Critical | Plan 06   | `now_playing_panel.py:715-740` (`_refresh_star_display` helper); `:1261` (called from helper); `test_gbs_apply_icy_label_refreshes_star_icon_when_favorited` + `_when_not_favorited` |
| CR-04: `_make_gbs_station` truthy MagicMock invalidates coverage      | Critical | Plan 06   | `tests/test_now_playing_panel.py:969` (factory `s.icy_disabled = False`); `test_gbs_playlist_populates_from_mock_state` now asserts `panel.icy_label.text()`, `_gbs_label_source`, `_last_icy_title` |
| WR-01: `_apply_gbs_icy_label` lacks provider guard                    | Warning  | Plan 06   | `now_playing_panel.py:1250` (`if self._station is None or self._station.provider_name != "GBS.FM": return`)                                                    |
| WR-02: source-flag wipe in `_refresh_gbs_visibility` clobbers `'icy'` | Warning  | Plan 05   | Asymmetric reset semantics — flag reset only in `should_show=False` branch (line 1029); `should_show=True` branch (line 999-1010) deliberately untouched. `test_gbs_refresh_gbs_visibility_preserves_ajax_disabled_on_rebind` + `_clears_*_on_fresh_station` + `_clears_*_on_leaving_gbs` lock the three lifecycle paths. |
| WR-03: direct `self._station.icy_disabled` access at slot sites       | Warning  | Plan 06   | `getattr(self._station, "icy_disabled", False)` at three sites: `bind_station:513`, `on_title_changed:548`, `_apply_gbs_icy_label:1253`                       |
| WR-04: bare-vs-full favourite-row distinctness undocumented           | Warning  | Plan 06   | `_apply_gbs_icy_label` docstring lines 1227-1233 documents the invariant + cites CONTEXT.md Deferred Ideas                                                    |
| IN-01: 13-line apology comment in test                                | Info     | Plan 05   | Replaced with two assertions (`panel._gbs_ajax_disabled is True`, `panel.star_btn.isEnabled() is True`) at `tests/test_now_playing_panel.py:1672-1678`         |
| IN-02: hard-coded line numbers in `_gbs_poll_in_flight` docstring     | Info     | Plan 06   | Docstring at `now_playing_panel.py:1033-1046` now references symbols (`_on_gbs_playlist_ready`, `_on_gbs_poll_tick`, "token-discard guard")                    |

**All 10 REVIEW findings closed.**

### Previous VERIFICATION.md Gap Closure

| Gap   | Description                                                                            | Closed By | Verification Site                                                                                                                                            |
| ----- | -------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Gap 1 | D-05 no-downgrade guard over-fires on new-track ICY arrivals (= CR-01)                  | Plan 04   | Same as CR-01 above. The guard now only suppresses LOCAL writes; the kick at line 607 always fires in GBS context when worker idle. Track-change branch at line 1089-1097 resets the flag so the next ICY for a new track is treated as a fresh write. |
| Gap 2 | D-08 auth-expired does not relax bridge gate when cookies persist (= CR-02)             | Plan 05   | Same as CR-02 above. Phase 60 D-04 (cookie-file existence as login signal) preserved — no cookie deletion. New `_gbs_ajax_disabled` panel-local flag distinguishes "cookies on disk" from "/ajax doesn't work." |

**Both BLOCKER gaps closed.**

### Anti-Patterns Found

| File                                       | Line | Pattern                                                            | Severity | Impact                                                                                            |
| ------------------------------------------ | ---- | ------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------- |
| —                                          | —    | None found                                                         | —        | All previously-flagged anti-patterns (BLOCKER + WARNING rows from prior verification) are closed. |

The previous verification flagged 5 anti-patterns (3 BLOCKER, 2 WARNING). All are now resolved:

- **(prev BLOCKER) lines 542-543** "No-downgrade guard returns before kick" — removed by Plan 04 (kick split out of early-return).
- **(prev BLOCKER) lines 668-675** "Bridge-gate `_is_gbs_logged_in()` reads only file existence" — closed by Plan 05 (additional `and not self._gbs_ajax_disabled` conjunct).
- **(prev WARNING) lines 1118-1152** "`_apply_gbs_icy_label` does not refresh star icon" — closed by Plan 06 (`_refresh_star_display` shared helper).
- **(prev WARNING) tests:1024** "`_make_gbs_station()` without `icy_disabled = False`" — closed by Plan 06 (factory default).
- **(prev WARNING) tests:1532-1544** "13-line prose comment replacing missing assertion" — closed by Plan 05 (assertions added).

### Human Verification Required

Four behaviours must be validated against a live GBS.FM stream because they cannot be deterministically reproduced in unit tests. Three are inherited from the previous verification (live stream timing, bridge UX, auth-expired UX); one is new (CR-03 star icon visual refresh). All four behaviours are also captured in `60.3-VALIDATION.md § Manual-Only Verifications`.

#### 1. Live GBS.FM First-Bind Bridge-Window UX

**Test:** Bind a freshly-authenticated GBS.FM station and observe the label from the moment of binding.
**Expected:** Within ~5 s the label briefly shows the bare ICY title with the star button greyed out, then upgrades to the full `Artist - Title` and the star button becomes enabled. No cover-art request is issued during the bridge.
**Why human:** Real /ajax round-trip timing (~200–500 ms) and GStreamer ICY-tag parse latency cannot be reproduced deterministically in unit tests; the bridge-window UX is sub-second visual state.

#### 2. Live GBS.FM Track-Change Label Upgrade (CR-01 / Gap 1 Closure)

**Test:** While bound to a GBS.FM station that has already been /ajax-stamped (label shows full Artist - Title), wait for a server-side track change.
**Expected:** Within ~1 s of the new ICY tag arriving, an immediate /ajax kick fires (D-03) and the label upgrades to the new track's full `Artist - Title`. The user does NOT see the previous track's label persist for 15 s.
**Why human:** Requires live GStreamer ICY-tag sequencing across a real track transition; the kick + /ajax round-trip timing is observable only with a live authenticated GBS session.

#### 3. Live GBS.FM Auth-Expired Fallback UX (CR-02 / Gap 2 Closure)

**Test:** While bound to a GBS.FM station, let the session expire mid-run (or simulate by removing/invalidating cookies while the app is running).
**Expected:** After the next poll fails with auth_expired, the bare ICY title continues to update from `on_title_changed`, the star button stays enabled (favourites can still be saved against bare titles), and cover-art lookup fires against the bare title.
**Why human:** Requires a real GBS session expiry event; cannot simulate the cookie-still-on-disk-but-stale state in unit tests without bypassing `_is_gbs_logged_in()`.

#### 4. Live GBS.FM Star Icon Refresh on /ajax Upgrade (CR-03 Closure)

**Test:** Favourite a GBS.FM track under its full `Artist - Title` key while bound to the station. Trigger a re-bind (or wait for the next /ajax poll).
**Expected:** When the bare ICY arrives, the star may briefly show non-starred (bare key not in favourites). When /ajax stamps the canonical title, the star icon flips to starred and the tooltip reads "Remove track from favorites".
**Why human:** Requires the icon to be visually inspected during the bridge transition; unit tests assert the underlying `setChecked(True)` / `setIcon` calls, but the actual rendered icon and tooltip text are user-observable only on the running app.

## Gaps Summary

**No gaps remain.** All 8 D-NN truths verified, all 10 REVIEW findings closed, both VERIFICATION blocker gaps resolved.

The phase is functionally complete. Status `human_needed` reflects only the live-stream behaviours that are intrinsically untestable offline — these are documented in `60.3-VALIDATION.md § Manual-Only Verifications` and are appropriate UAT items rather than implementation gaps.

## Decision-Tree Justification

Per the verifier's Step 9 decision tree (most restrictive first):

1. Any failed truth, missing artifact, broken link, or blocker anti-pattern? **No.**
2. Any human-verification items? **Yes (4).** → status: `human_needed`.
3. (Skipped — branch 2 fired.)

`passed` is reserved for phases where the human-verification section is empty. Phase 60.3 has irreducible live-stream UAT items, so `human_needed` is the correct terminal status.

---

_Verified: 2026-05-07T17:30:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — second pass after gap-closure cycle (Plans 60.3-04, 60.3-05, 60.3-06)_
_HEAD at verification: 7dcdedc_
