---
status: partial
phase: 86.1-sc5-failure-followup-from-phase-86
source: [86.1-VERIFICATION.md]
started: 2026-06-05T02:40:40Z
updated: 2026-06-05T02:40:40Z
---

## Current Test

[awaiting human testing — requires a real Flatpak build + sandbox runtime with Qt display]

## Tests

### 1. First sandboxed launch — import wizard appears over main window
expected: On first launch inside the Flatpak sandbox with existing `~/.local/share/musicstreamer/` host data, the FlatpakImportWizard is offered (appears over the main window after `window.show()`). This is the SC5 functional path that failed in Phase 86.
result: [pending]

### 2. Offer-once on relaunch
expected: After the wizard is dismissed by any path (Import, Skip, X, Esc, Cancel), `write_offered_flag()` is called so the wizard is NOT offered again on subsequent sandboxed launches.
result: [pending]

### 3. Native Linux non-offer + no silent secret copy
expected: Running natively (not sandboxed) with legacy data, `is_sandboxed()` returns False so the wizard is never offered. Confirm `run_migration()` does NOT silently copy host secrets (`cookies.txt`, `twitch-token.txt`) into the sandbox — copy only happens via explicit consent through the wizard.

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
