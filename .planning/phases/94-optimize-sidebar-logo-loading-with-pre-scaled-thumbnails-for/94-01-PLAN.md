---
phase: 94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_art_paths.py
  - tests/test_station_thumb_async.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "Encodes D-01, D-02, D-03, D-04, D-05, D-06 as executable RED test contracts per the VALIDATION.md per-decision map (Wave 0 scaffolding)"
    - "Running the quick test command discovers 9 new test functions (5 in test_art_paths.py, 4 in test_station_thumb_async.py)"
    - "All 9 new tests FAIL or ERROR (RED) because the implementation symbols do not yet exist"
    - "Existing tests in test_art_paths.py and test_station_icon_integration.py still pass unchanged"
  artifacts:
    - path: "tests/test_station_thumb_async.py"
      provides: "New integration test file for async thumb generation, dataChanged delivery, dedup, and now_playing drift-guard"
      contains: "def test_thumb_landing_emits_datachanged"
    - path: "tests/test_art_paths.py"
      provides: "Extended unit tests for thumb path derivation, freshness, 96px size, miss/stale behavior"
      contains: "def test_thumb_path_derivation"
  key_links:
    - from: "tests/test_art_paths.py"
      to: "musicstreamer.ui_qt._art_paths"
      via: "import of _thumb_path_for, _is_thumb_fresh, _generate_thumb"
      pattern: "from musicstreamer.ui_qt._art_paths import"
    - from: "tests/test_station_thumb_async.py"
      to: "musicstreamer.ui_qt.station_tree_model.StationTreeModel"
      via: "model instantiation + dataChanged capture"
      pattern: "StationTreeModel"
---

<objective>
Establish the Wave 0 test scaffolding for Phase 94 BEFORE any implementation. Per VALIDATION.md, this phase requires 9 new tests across 2 files that encode the locked decisions D-01..D-06 as executable contracts. All 9 tests must exist and FAIL/ERROR (RED) — they reference implementation symbols (`_thumb_path_for`, `_is_thumb_fresh`, `_generate_thumb`, the `on_thumb_needed` param, `StationTreeModel._request_thumb`/`_on_thumb_landing`) that Plans 02 and 03 will create.

Purpose: Lock the behavioral contract for every decision so implementation plans turn RED → GREEN with no ambiguity. This is the Nyquist sampling floor — no implementation task may run without a failing test it must satisfy.
Output: Extended `tests/test_art_paths.py` (+5 tests) and new `tests/test_station_thumb_async.py` (+4 tests), all RED.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-CONTEXT.md
@.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-VALIDATION.md
@.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-PATTERNS.md

<interfaces>
<!-- Symbols the tests will import/exercise. These do NOT exist yet — that is why the tests are RED. Plans 02/03 create them. -->

Planned in _art_paths.py (Plan 02):
- THUMB_FILENAME = "station_art.thumb.png"
- _thumb_path_for(abs_source_path: str) -> str   # dirname(source)/THUMB_FILENAME
- _is_thumb_fresh(source_path: str, thumb_path: str) -> bool   # os.stat mtime: thumb >= source
- _generate_thumb(source_path: str, thumb_path: str, station_id: int, callback) -> None
    # callback signature: callback(station_id: int, source_path: str, thumb_path: str | None)
- load_station_icon(station, size=STATION_ICON_SIZE, on_thumb_needed=None) -> QIcon
    # on_thumb_needed: callable(station_id: int, source_abs_path: str, thumb_abs_path: str)

Planned in station_tree_model.py (Plan 03):
- StationTreeModel._thumb_landing = Signal(int, str, str)   # (station_id, source_abs_path, thumb_abs_path)
- StationTreeModel._in_flight_thumbs: set[int]
- StationTreeModel._request_thumb(station_id, source_path, thumb_path)  # main-thread, dedup guard
- StationTreeModel._on_thumb_landing(station_id, source_path, thumb_path)  # main-thread slot
- StationTreeModel.index_for_station_id(station_id) -> QModelIndex

Existing reusable test fixtures (replicate, do not modify originals):
- tests/test_art_paths.py: _isolate_pixmap_cache (autouse), tmp_data_dir, _write_logo
- tests/test_station_icon_integration.py lines 39-51: _make_station; lines 77-87: fixtures
- tests/test_gbs_marquee.py lines 229-236: _get_qapp(); QTest.qWait delivery pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/test_art_paths.py with 5 RED unit tests (D-02, D-04, D-05, D-06)</name>
  <files>tests/test_art_paths.py</files>
  <read_first>
    - tests/test_art_paths.py (the file being modified — read fixtures _isolate_pixmap_cache, tmp_data_dir, helper _write_logo, and an existing test like test_relative_station_art_path_resolves_via_abs_art_path for parameter shape)
    - musicstreamer/ui_qt/_art_paths.py (current load_station_icon + abs_art_path — the symbols under test)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-VALIDATION.md (exact test names in the Per-Task Verification Map)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-PATTERNS.md (test_art_paths.py extend section, lines 283-333)
  </read_first>
  <action>
    Append 5 new test functions to tests/test_art_paths.py, each accepting tmp_data_dir (and qtbot where a QObject/QPixmap is built). Add imports `import time` and the new-helper imports from musicstreamer.ui_qt._art_paths: `_thumb_path_for`, `_is_thumb_fresh`, `_generate_thumb` (these imports will fail until Plan 02 — that is the intended RED state).

    Required test names (use EXACTLY these — VALIDATION.md depends on them):
    - test_thumb_path_derivation (D-05): assert _thumb_path_for of an abs source ending in assets/{id}/station_art.png returns the sibling path ending in assets/{id}/station_art.thumb.png (os.path.dirname(source)/THUMB_FILENAME). Verify the constant THUMB_FILENAME == "station_art.thumb.png".
    - test_thumb_is_96px (D-04): call _generate_thumb synchronously-ish — invoke it with a captured callback, wait briefly for the daemon thread, then load the written PNG via QImage and assert max(width, height) <= 96 and aspect ratio preserved (use a non-square source via _write_logo width/height so the assertion is meaningful). Use a 200x100 source.
    - test_generate_thumb_writes_png (D-02 disk write): _write_logo a source, call _generate_thumb, wait for callback, assert the thumb file exists at the derived path, is a valid non-null PNG (QImage(thumb).isNull() is False), and the callback received (station_id, source_path, thumb_path) with a non-None thumb_path.
    - test_thumb_freshness_check (D-06): write source then thumb (thumb newer) → _is_thumb_fresh True; make source newer than thumb (use os.utime to bump source mtime forward, or time.sleep + rewrite source) → _is_thumb_fresh False; missing thumb → False; missing source → False.
    - test_thumb_missing_returns_fallback (D-02 lazy gen): call load_station_icon(station, on_thumb_needed=spy) for a station whose source logo exists but has NO thumb on disk. Assert the returned QIcon is non-null AND the spy was called once with (station.id, source_abs_path, thumb_abs_path). Use a simple list-append spy as on_thumb_needed.

    For daemon-thread waits, capture results via a threading.Event or a list the callback appends to, then poll with a short bounded loop (e.g. up to ~2s) rather than a fixed long sleep — daemon thread + file write is fast. Do NOT construct QPixmap off the main thread inside the test worker; the worker is _generate_thumb itself which uses QImage.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_art_paths.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The 5 named test functions exist in tests/test_art_paths.py (grep -c confirms each name once).
    - Running the file shows the 5 new tests as FAILED or ERROR (collection/import error on the missing _art_paths helpers counts as RED) while pre-existing tests in the file are unaffected by the edit (no pre-existing test renamed or deleted).
    - Behavior assertion: test_thumb_missing_returns_fallback asserts the on_thumb_needed spy is called with exactly (station.id, source_abs_path, thumb_abs_path).
    - Source assertion: `grep -c 'def test_thumb_path_derivation\|def test_thumb_is_96px\|def test_generate_thumb_writes_png\|def test_thumb_freshness_check\|def test_thumb_missing_returns_fallback' tests/test_art_paths.py` returns 5.
  </acceptance_criteria>
  <done>5 new RED tests appended to tests/test_art_paths.py covering D-02/D-04/D-05/D-06; existing tests untouched; failures are due to missing Plan-02 symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_station_thumb_async.py with 4 RED tests (D-01, D-03)</name>
  <files>tests/test_station_thumb_async.py</files>
  <read_first>
    - tests/test_gbs_marquee.py lines 229-350 (_get_qapp helper + QTest.qWait daemon-thread Signal delivery pattern — the analog)
    - tests/test_station_icon_integration.py lines 39-51 and 77-87 (_make_station factory + _isolate_pixmap_cache/tmp_data_dir fixtures to replicate)
    - musicstreamer/ui_qt/station_tree_model.py (StationTreeModel.__init__, _populate, data DecorationRole at ~line 163, index/createIndex shape — the symbols under test)
    - musicstreamer/ui_qt/now_playing_panel.py (the _load_scaled_pixmap function — drift-guard target; confirm it does NOT and must not reference the thumb)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-PATTERNS.md (test_station_thumb_async.py new-file section, lines 336-411)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-VALIDATION.md (exact test names)
  </read_first>
  <action>
    Create new file tests/test_station_thumb_async.py. Copy verbatim: the _get_qapp() helper (test_gbs_marquee.py 229-236), the _isolate_pixmap_cache autouse fixture and tmp_data_dir fixture, and the _make_station factory (from test_station_icon_integration.py) so Station objects have all required fields. Set/rely on QT_QPA_PLATFORM=offscreen (conftest handles it; do not re-set). Import QTest from PySide6.QtTest, StationTreeModel from musicstreamer.ui_qt.station_tree_model, and read source via importlib/inspect or plain file read for the drift-guard.

    Required test names (EXACT):
    - test_thumb_landing_emits_datachanged (D-03 async repaint): build a StationTreeModel with one station whose source logo exists on disk (via _write_logo under tmp_data_dir) but no thumb. Connect a listener to model.dataChanged that appends (top_left, bottom_right, roles). Trigger the DecorationRole path by calling model.data(station_index, Qt.DecorationRole) (which must invoke on_thumb_needed → _request_thumb → worker). QTest.qWait(500) to allow daemon write + QueuedConnection delivery. Assert exactly one dataChanged emission, top_left == bottom_right (single row), and Qt.DecorationRole in roles.
    - test_in_flight_dedup (D-03 dedup): call model.data(station_index, Qt.DecorationRole) multiple times in rapid succession (simulating fast-scroll repaints) BEFORE the worker finishes. Assert the station_id is added to model._in_flight_thumbs once and that only one worker/one landing occurs (e.g. patch _generate_thumb with a spy that counts calls; assert call count == 1 for that station across N rapid data() calls). After qWait, assert _in_flight_thumbs no longer contains the id (discarded in the landing slot).
    - test_now_playing_panel_does_not_use_thumb (D-01 drift-guard): read the source of musicstreamer/ui_qt/now_playing_panel.py and assert the substring "station_art.thumb" does NOT appear, and that _load_scaled_pixmap exists. This guards that the full-res Now Playing path is never rewired to the sidebar thumbnail.
    - test_index_for_station_id_roundtrip (D-03 coordination): build a model with ≥2 providers each with ≥1 station; assert model.index_for_station_id(known_id).isValid() and that station_for_index(that index).id == known_id; assert index_for_station_id(unknown_id) returns an invalid index.

    Use a list-append + threading.Event or QTest.qWait for delivery synchronization. Never construct QPixmap on a non-main thread in the test. The drift-guard test reads a file, so it passes immediately once now_playing is correct — but the other 3 will be RED until Plan 03 adds _request_thumb/_on_thumb_landing/index_for_station_id/_in_flight_thumbs/_thumb_landing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_thumb_async.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_station_thumb_async.py exists with the 4 named test functions.
    - Source assertion: `grep -c 'def test_thumb_landing_emits_datachanged\|def test_in_flight_dedup\|def test_now_playing_panel_does_not_use_thumb\|def test_index_for_station_id_roundtrip' tests/test_station_thumb_async.py` returns 4.
    - Behavior: test_now_playing_panel_does_not_use_thumb passes NOW (proves D-01 baseline). The other 3 are RED (AttributeError/missing symbol on StationTreeModel) until Plan 03.
    - The file imports _get_qapp, QTest, and StationTreeModel and uses QT_QPA_PLATFORM=offscreen via conftest (no hard-coded display).
  </acceptance_criteria>
  <done>New tests/test_station_thumb_async.py with 4 tests; drift-guard GREEN, async/dedup/index tests RED pending Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importer → disk (assets/{id}/station_art.png) | PNG bytes are downloaded by aa/soma/gbs importers and written to disk BEFORE render. At render time the logo is already-trusted local content, not user-supplied. |
| disk → Qt image decoder | The only external input this phase touches is the on-disk PNG that the (future) worker will decode via QImage. Tests in this plan write their own fixture PNGs only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | Test fixture file writes under tmp_data_dir | accept | Tests write only into pytest tmp_path; no production paths touched. paths._root_override is monkeypatched per-test. |
| T-94-02 | Denial of Service | Daemon thread spawned in tests | mitigate | Tests use bounded qWait/poll; daemon threads (daemon=True) die with the test process. No unbounded loops. |
| T-94-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — phase adds zero external dependencies (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks in this plan. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_art_paths.py tests/test_station_thumb_async.py -q 2>&1 | tail -30` shows 9 new tests, 8 RED (missing impl) + 1 GREEN (now_playing drift-guard).
- `.venv/bin/python -m pytest tests/test_station_icon_integration.py -q` still passes (no regression to existing integration tests).
- No test uses system python3 (MEMORY.md): all commands invoke `.venv/bin/python -m pytest`.
</verification>

<success_criteria>
- 9 new test functions exist with the exact names in VALIDATION.md's Per-Task Verification Map.
- 8 of 9 are RED (reference Plan-02/Plan-03 symbols that do not exist yet); the now_playing drift-guard is GREEN.
- Existing tests in test_art_paths.py and test_station_icon_integration.py are unmodified and pass.
- Test commands use `.venv/bin/python -m pytest`, scoped to phase files (full suite >600s is NOT run here).
</success_criteria>

<output>
Create `.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-01-SUMMARY.md` when done.
</output>
