---
phase: 69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check_bundle_plugins.py
  - packaging/windows/build.ps1
  - packaging/windows/README.md
  - tests/test_packaging_spec.py
  - .planning/codebase/CONCERNS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - WIN-05
tags:
  - windows
  - packaging
  - gstreamer
  - bundling
  - drift-guard
must_haves:
  truths:
    - "Running `python tools/check_bundle_plugins.py --bundle dist/MusicStreamer/_internal` against a bundle containing both `gst_plugins/gstlibav.dll` and `gst_plugins/gstaudioparsers.dll` exits 0 with a one-line OK message; same command against a bundle missing either DLL exits 10 with a per-DLL stderr line naming the conda-forge package that supplies it"
    - "`packaging/windows/build.ps1` invokes `python ..\\..\\tools\\check_bundle_plugins.py --bundle ..\\..\\dist\\MusicStreamer\\_internal` AFTER the existing post-bundle dist-info assertion (line 283) and BEFORE the smoke-test block (line 285), branching on `$LASTEXITCODE` with a `Write-Host \"BUILD_FAIL reason=plugin_missing hint=...\" -ForegroundColor Red` + `exit 10` failure block"
    - "`packaging/windows/build.ps1:4-6` exit-code header lists `10=post-bundle plugin-presence guard fail (Phase 69)` as the new code"
    - "`packaging/windows/README.md` conda recipe block (the fenced PowerShell `conda create -n musicstreamer-build` block) lists `gst-libav gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly` alongside the existing `python=3.12 pygobject gstreamer=1.28 pyinstaller \"pyinstaller-hooks-contrib>=2026.2\"` packages"
    - "`uv run pytest tests/test_packaging_spec.py -x` exits 0 with the two new drift-guard tests passing: `test_readme_conda_recipe_lists_every_required_plugin_package` and `test_build_ps1_invokes_plugin_guard_with_exit_10`"
    - "`.planning/codebase/CONCERNS.md` GStreamer Windows Plugin Availability concern (lines 55–59) `Fix approach:` line no longer claims `Phase 44 bundling confirmed gst-libav is present in conda-forge build` and instead documents Phase 69's reconciliation"
    - "`.planning/REQUIREMENTS.md` contains a new `WIN-05` row in the Windows Polish (WIN) section AND in the Traceability table, with Coverage tally bumped to `20 total / 20 mapped / 18 complete / 2 pending (WIN-02, WIN-05)`"
  artifacts:
    - path: "tools/check_bundle_plugins.py"
      provides: "Source-of-truth REQUIRED_PLUGIN_DLLS dict + CLI guard for build.ps1 + import target for drift-guard pytest"
      contains: "REQUIRED_PLUGIN_DLLS"
      exports: ["REQUIRED_PLUGIN_DLLS", "main"]
      min_lines: 50
    - path: "packaging/windows/build.ps1"
      provides: "PowerShell invocation of check_bundle_plugins.py with exit 10 failure block + updated exit-code header"
      contains: "POST-BUNDLE PLUGIN GUARD"
    - path: "packaging/windows/README.md"
      provides: "Conda recipe with explicit gst-plugins-* + gst-libav packages"
      contains: "gst-libav"
    - path: "tests/test_packaging_spec.py"
      provides: "Two new drift-guard test functions covering README recipe ↔ REQUIRED_PLUGIN_DLLS parity AND build.ps1 ↔ exit-code 10 invocation parity"
      contains: "test_readme_conda_recipe_lists_every_required_plugin_package"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Reconciled GStreamer Windows Plugin Availability fix-approach line per DOC-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WIN-05 row in Windows Polish + Traceability per DOC-04"
      contains: "WIN-05"
  key_links:
    - from: "tests/test_packaging_spec.py::test_readme_conda_recipe_lists_every_required_plugin_package"
      to: "tools/check_bundle_plugins.py::REQUIRED_PLUGIN_DLLS"
      via: "from tools.check_bundle_plugins import REQUIRED_PLUGIN_DLLS"
      pattern: "from tools\\.check_bundle_plugins import REQUIRED_PLUGIN_DLLS"
    - from: "tests/test_packaging_spec.py::test_readme_conda_recipe_lists_every_required_plugin_package"
      to: "packaging/windows/README.md conda recipe block"
      via: "regex match on `conda create -n musicstreamer-build` fenced block"
      pattern: "conda create -n musicstreamer-build"
    - from: "packaging/windows/build.ps1 step 4b"
      to: "tools/check_bundle_plugins.py"
      via: "Invoke-Native { python ..\\..\\tools\\check_bundle_plugins.py --bundle ..\\..\\dist\\MusicStreamer\\_internal }"
      pattern: "python \\.\\.\\\\\\.\\.\\\\tools\\\\check_bundle_plugins\\.py"
    - from: "tools/check_bundle_plugins.py::REQUIRED_PLUGIN_DLLS"
      to: "packaging/windows/README.md conda packages"
      via: "Drift-guard pytest enforces every (_, package) tuple value appears in the conda recipe block"
      pattern: "gst-libav|gst-plugins-good"
---

<objective>
Land the Phase 69 packaging+bundle fix on Linux dev: create the single-source-of-truth Python guard (`tools/check_bundle_plugins.py`), wire it into `packaging/windows/build.ps1` as a new post-bundle step 4b (exit code 10), update `packaging/windows/README.md` so the conda recipe explicitly carries every plugin package the guard requires, add two new drift-guard tests to `tests/test_packaging_spec.py` (README↔required-list parity + build.ps1↔invocation parity), reconcile the stale `.planning/codebase/CONCERNS.md` claim per DOC-01, and add the new `WIN-05` requirement row to `.planning/REQUIREMENTS.md` per DOC-04. All six file edits are coupled — the test imports the tool, the tool's required list must align with the README recipe, the build.ps1 invocation pattern is locked by the test — so they ship as one atomic wave.

Purpose: A fresh Win11 build host following the production README must produce a bundle that contains `gstlibav.dll` (provides `avdec_aac` for AAC decode) and `gstaudioparsers.dll` (provides `aacparse` for ADTS framing) in `dist/MusicStreamer/_internal/gst_plugins/`. Without both DLLs, AAC streams fail at runtime with the generic "Playback error" toast — the empirical Phase 56 F2 finding this phase exists to fix. The build-time guard catches future regressions of this exact failure mode (e.g. a maintainer dropping `gst-libav` from the recipe during a future cleanup), and the Linux dev drift-guard pytest catches recipe-vs-required-list divergence before any Windows build is even attempted.

Output: Six file edits, all green on `uv run pytest -x` on Linux dev. Plan 02 (Win11 VM UAT) consumes this plan's output and proves end-to-end AAC playback on the operator's VM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 69 inputs (mandatory)
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md

# Project skill (Windows packaging spike findings, auto-loaded per CLAUDE.md routing)
@.claude/skills/spike-findings-musicstreamer/SKILL.md

# Smoking-gun evidence (motivation; do not modify)
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md

# Analog source files for new Python guard
@tools/check_subprocess_guard.py
@tools/check_spec_entry.py

# Analog source file for new build.ps1 step + WR-01 discipline
@packaging/windows/build.ps1

# Analog source file for new drift-guard tests
@tests/test_packaging_spec.py
@tests/test_constants_drift.py

# Edit targets
@packaging/windows/README.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Single-source-of-truth dict that the new tool exports and the new drift-guard test imports.
     Per CONTEXT G-02 + RESEARCH "Plugin → conda-forge package map", the corrected mapping is:
       - avdec_aac → gst-libav (NOT in gst-plugins-bad as CONTEXT-DG-01 originally stated)
       - aacparse → gst-plugins-good (NOT in gst-plugins-bad as CONTEXT-DG-01 originally stated)
     faad is NOT viable on conda-forge win-64 (GPL excluded) — do NOT add it to REQUIRED_PLUGIN_DLLS. -->

From tools/check_bundle_plugins.py (NEW — to be created by Task 1):
```
REQUIRED_PLUGIN_DLLS: dict[str, tuple[str, str]] = {
    "gstlibav.dll": ("avdec_aac", "gst-libav"),
    "gstaudioparsers.dll": ("aacparse", "gst-plugins-good"),
}
```

From packaging/windows/build.ps1 (existing — to be extended by Task 2):
```
function Invoke-Native { ... }   # line 39–49 — wrap python invocations
```

From tests/test_packaging_spec.py (existing — to be extended by Task 3):
```
@pytest.fixture(scope="module")
def build_ps1_source() -> str: ...   # line 52–55, already exists
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/check_bundle_plugins.py source-of-truth helper</name>
  <files>tools/check_bundle_plugins.py</files>
  <read_first>
    - tools/check_subprocess_guard.py (the primary analog — same exit-code convention, same `Path(__file__).resolve().parent.parent` idiom; clone the docstring + import triplet + main() shape)
    - tools/check_spec_entry.py (secondary analog — proves the pattern generalizes)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md (G-01 / G-02 / G-03 decisions + Claude's Discretion default `REQUIRED_PLUGIN_DLLS = {"gstlibav.dll": ("avdec_aac", "gst-libav"), "gstaudioparsers.dll": ("aacparse", "gst-plugins-good")}`)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md (Pattern 2 full code shape lines 333–414; "Plugin → conda-forge package map" lines 137–149 confirms aacparse ships in gst-plugins-good NOT gst-plugins-bad; Pitfall 1 confirms the bundled subdir is `gst_plugins/` underscore NOT `gst-plugins/` hyphen)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md (the analog mapping table + verbatim Python excerpts)
  </read_first>
  <action>
Create the new file `tools/check_bundle_plugins.py` as a single-source-of-truth Python guard mirroring the structural shape of `tools/check_subprocess_guard.py`. Required structure (concrete identifiers and behavior — no inline implementation here, see `<read_first>` references for the verbatim analog code patterns):

1. Module docstring describing the purpose (Phase 69 G-01/G-02 AAC plugin-presence guard) and an `Exit codes:` block listing exactly two codes: `0` for clean (all required plugin DLLs present) and `10` for plugin missing (matches `packaging/windows/build.ps1` exit code convention per G-04). State that the file is callable as `python tools/check_bundle_plugins.py --bundle <path>` and that `REQUIRED_PLUGIN_DLLS` is the single source of truth imported by `tests/test_packaging_spec.py`'s drift-guard pytest.

2. Imports: `from __future__ import annotations`, `import argparse`, `import sys`, `from pathlib import Path` (RESEARCH Pattern 2 shape; argparse is the one deviation from `check_subprocess_guard.py` since the new tool takes `--bundle` arg).

3. Module-level constant — this is the literal dict shape, place it AFTER the imports with a one-line comment citing Phase 69 G-02:
   `REQUIRED_PLUGIN_DLLS: dict[str, tuple[str, str]] = {`
   `    "gstlibav.dll": ("avdec_aac", "gst-libav"),`
   `    "gstaudioparsers.dll": ("aacparse", "gst-plugins-good"),`
   `}`
   The values are `(element_name, conda_forge_package)` tuples. Do NOT add `faad` — conda-forge does not ship `gstfaad.dll` in win-64 gst-plugins-bad (GPL licensing, confirmed in RESEARCH).

4. `def main(argv: list[str] | None = None) -> int:` — argparse parses `--bundle` with default `Path("dist/MusicStreamer/_internal")` (the PyInstaller `_internal/` path; build.ps1 passes the explicit Windows path). Compute `plugins_dir = args.bundle / "gst_plugins"` (underscore — RESEARCH Pitfall 1; cite the spike-findings rationale in a brief inline comment).

5. If `plugins_dir.is_dir()` is False → print `f"PHASE-69 FAIL: bundle plugins dir not found at {plugins_dir}"` to stderr and `return 10`.

6. Iterate `REQUIRED_PLUGIN_DLLS.items()`; collect a `missing: list[str]` of formatted lines `"  {dll_name} (provides {element}, ships in conda-forge package {package})"` for each DLL not present in `plugins_dir`.

7. If `missing` is non-empty → print a header `"PHASE-69 FAIL: required GStreamer plugin DLL(s) missing from bundle:"` to stderr, print each missing line to stderr, then print a `"Fix: add the named conda-forge package(s) to packaging/windows/README.md conda recipe, recreate the conda env, and rebuild."` hint to stderr, and `return 10`.

8. On success: print `f"PHASE-69 OK: all {len(REQUIRED_PLUGIN_DLLS)} required plugin DLL(s) present in {plugins_dir}"` to stdout and `return 0`.

9. `if __name__ == "__main__": sys.exit(main())` guard at file end (RESEARCH Pattern 2 shape — `return int` from main, exit in `__main__` block).

Add type hints throughout (`from __future__ import annotations` covers PEP 604 union syntax). File should be ~60–80 lines including docstring. Do NOT add a logging module — use plain `print(..., file=sys.stderr)` matching the analog.
  </action>
  <verify>
    <automated>uv run python -c "from tools.check_bundle_plugins import REQUIRED_PLUGIN_DLLS; assert REQUIRED_PLUGIN_DLLS == {'gstlibav.dll': ('avdec_aac', 'gst-libav'), 'gstaudioparsers.dll': ('aacparse', 'gst-plugins-good')}, REQUIRED_PLUGIN_DLLS; import tools.check_bundle_plugins as m; assert m.main(['--bundle', '/nonexistent/path']) == 10; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/check_bundle_plugins.py` exists and is importable.
    - Module exports `REQUIRED_PLUGIN_DLLS` as a `dict[str, tuple[str, str]]` with EXACTLY the two keys `"gstlibav.dll"` and `"gstaudioparsers.dll"` mapping to `("avdec_aac", "gst-libav")` and `("aacparse", "gst-plugins-good")` respectively.
    - `python tools/check_bundle_plugins.py --bundle /nonexistent/path` exits 10 and writes a `PHASE-69 FAIL: bundle plugins dir not found` line to stderr.
    - File contains the literal string `"Exit codes:"` in its module docstring (mirrors `check_subprocess_guard.py:8`) AND the literal `10` in that block (documents the exit code per G-04).
    - File contains the literal `gst_plugins` (underscore form, NOT `gst-plugins` hyphen) — Pitfall 1 lock; grep `grep -c "gst_plugins" tools/check_bundle_plugins.py` returns ≥1.
    - File contains NO reference to `faad` (negative drift-guard: `grep -c "faad" tools/check_bundle_plugins.py` returns 0).
    - File ends with `sys.exit(main())` inside a `if __name__ == "__main__":` guard (RESEARCH Pattern 2 shape).
  </acceptance_criteria>
  <done>The new `tools/check_bundle_plugins.py` is committed with the exact `REQUIRED_PLUGIN_DLLS` dict shape, exits 10 on a missing-bundle path and 0 on a present-bundle path, and exposes `REQUIRED_PLUGIN_DLLS` for import by the new drift-guard pytest in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Wire packaging/windows/build.ps1 with new step 4b post-bundle plugin guard + exit code 10 header</name>
  <files>packaging/windows/build.ps1</files>
  <read_first>
    - packaging/windows/build.ps1 — read in full (current state). Pay special attention to lines 4–6 (exit-code header — extend), lines 18–27 (WR-01 rationale block — DO NOT modify), lines 39–49 (`Invoke-Native` wrapper — reuse verbatim), lines 111–121 (PKG-03 invocation — primary structural analog for the new block), lines 200–283 (post-bundle dist-info assertion — secondary structural analog; the `# DO NOT REMOVE without first updating both:` rationale block at lines 200–202 must be cloned). Confirm the line numbers above — file may have shifted since the planning snapshot.
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md (build.ps1 section lines 101–148: exact pattern to copy verbatim — `=== POST-BUNDLE PLUGIN GUARD: ...` header, `Invoke-Native { python ..\\..\\tools\\check_bundle_plugins.py --bundle ..\\..\\dist\\MusicStreamer\\_internal 2>&1 | Out-Host }`, `if ($LASTEXITCODE -ne 0) { Write-Host \"BUILD_FAIL reason=plugin_missing hint=...\" -ForegroundColor Red; exit 10 }`, `Write-Host \"POST-BUNDLE PLUGIN GUARD OK\"`)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md (Example 2 lines 587–598 — full canonical shape; Example 3 lines 602–608 — exact updated exit-code header form; Pitfall 4 lines 537–545 — Invoke-Native requirement)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md (G-01, G-04 — exit code 10 spec; "BUILD_FAIL reason=plugin_missing" required form)
  </read_first>
  <action>
Two surgical edits to `packaging/windows/build.ps1`:

**Edit A — Exit-code header (lines 4–6).** Extend the existing `# Exit codes: ...` comment block to add `10=post-bundle plugin-presence guard fail (Phase 69)` as a fourth wrapped line. The result must read (per RESEARCH Example 3, RESEARCH lines 605–609 verbatim):
- Line 4: `# Exit codes: 0=ok, 1=env missing, 2=pyinstaller failed, 3=smoke test failed,`
- Line 5: `#             4=PKG-03 guard fail, 5=version parse fail, 6=iscc fail, 7=spec entry guard fail,`
- Line 6: `#             8=pre-bundle clean fail, 9=post-bundle dist-info assertion fail,`
- Line 7 (NEW): `#             10=post-bundle plugin-presence guard fail (Phase 69)`

Preserve the `Phase 44 MusicStreamer Windows build driver.` line and the `Adapted from .planning/...` line above the exit-codes block exactly as-is. Append-only edit; existing exit codes 0–9 keep their meanings.

**Edit B — New step 4b after line 283 (after `Write-Host "POST-BUNDLE ASSERTION OK -- dist-info singleton: ..."`).** Insert a new block AFTER the `POST-BUNDLE ASSERTION OK` Write-Host line (currently at line 283) and BEFORE the `# --- 5. Smoke test` block (currently at line 285). The new block has this exact shape, indented at the same 4-space level as steps 3a/3b/3c/4a (inside the `try { Push-Location ... }` block):

```
# --- 4b. Post-bundle plugin-presence guard (Phase 69 / G-01 / WIN-05) ---
# Validates that AAC-required GStreamer plugin DLLs landed in the bundle.
# Without this, a future docs-drift regression (e.g. a maintainer drops
# gst-libav from the conda recipe in packaging/windows/README.md) would
# silently produce a bundle that fails AAC playback at runtime with only
# the generic "Playback error" toast as a signal — the empirical Phase 56
# F2 finding (56-05-UAT-LOG.md).
#
# Required plugin list is single-sourced from tools/check_bundle_plugins.py
# REQUIRED_PLUGIN_DLLS dict (also imported by
# tests/test_packaging_spec.py P-01 drift-guard pytest).
#
# DO NOT REMOVE without first updating both:
#   - tests/test_packaging_spec.py (P-01 drift-guard test)
#   - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-01-PLAN.md (this plan's rationale)
Write-Host "=== POST-BUNDLE PLUGIN GUARD: python tools/check_bundle_plugins.py (Phase 69 / WIN-05) ==="
Invoke-Native { python ..\..\tools\check_bundle_plugins.py --bundle ..\..\dist\MusicStreamer\_internal 2>&1 | Out-Host }
if ($LASTEXITCODE -ne 0) {
    Write-Host "BUILD_FAIL reason=plugin_missing hint='see tools/check_bundle_plugins.py output above; add the named conda-forge package to packaging/windows/README.md conda recipe'" -ForegroundColor Red
    exit 10
}
Write-Host "POST-BUNDLE PLUGIN GUARD OK"
```

Critical constraints (RESEARCH Pitfall 4 + Phase 65 WR-01):
- Use the existing `Invoke-Native` wrapper (defined at lines 39–49); DO NOT introduce a new wrapper or call `python` directly. PS 5.1 stderr-trap mitigation per Pitfall 4.
- The diagnostic line MUST use `Write-Host ... -ForegroundColor Red` (NOT `Write-Error`). Phase 65 WR-01 — terminating-error escalation under `$ErrorActionPreference = "Stop"` would bypass `exit 10`. Documented at lines 18–27 of build.ps1.
- The `exit 10` line MUST appear inside the same `if ($LASTEXITCODE -ne 0) { ... }` block, immediately after the `Write-Host BUILD_FAIL ... -ForegroundColor Red` line. The drift-guard test in Task 3 enforces 120-char-before / 400-char-after adjacency.
- The `reason=plugin_missing` literal MUST match exactly (no rephrasing) — the drift-guard test asserts this exact substring.
- Path strings use Windows backslash form `..\..\tools\check_bundle_plugins.py` and `..\..\dist\MusicStreamer\_internal` because build.ps1 runs from `packaging/windows/`.

Verify ONLY the two edits land; do not touch any other line. After edit, run `grep -c "exit 10" packaging/windows/build.ps1` and confirm it returns ≥2 (one in the header comment, one in the new failure block).
  </action>
  <verify>
    <automated>grep -v "^#" packaging/windows/build.ps1 | grep -c "POST-BUNDLE PLUGIN GUARD" | grep -q "^[2-9]" && grep -q "10=post-bundle plugin-presence guard fail (Phase 69)" packaging/windows/build.ps1 && grep -q "BUILD_FAIL reason=plugin_missing" packaging/windows/build.ps1 && echo "build.ps1 edits OK"</automated>
  </verify>
  <acceptance_criteria>
    - `packaging/windows/build.ps1` contains the literal `10=post-bundle plugin-presence guard fail (Phase 69)` in the header comment block (lines 4–7 region).
    - `packaging/windows/build.ps1` contains the literal `=== POST-BUNDLE PLUGIN GUARD:` AND `python ..\..\tools\check_bundle_plugins.py` AND `BUILD_FAIL reason=plugin_missing` AND `exit 10` AND `POST-BUNDLE PLUGIN GUARD OK` (all in step 4b).
    - The new block sits AFTER the existing `POST-BUNDLE ASSERTION OK` Write-Host line AND BEFORE the `# --- 5. Smoke test` header. Confirm with `grep -n "POST-BUNDLE ASSERTION OK\|POST-BUNDLE PLUGIN GUARD\|--- 5. Smoke test" packaging/windows/build.ps1` — the three line numbers must be strictly increasing in that order.
    - Total occurrences of `exit 10` in the file is ≥2 (header + failure block).
    - The literal `Invoke-Native` appears at least 3 times more than the pre-edit baseline (existing PKG-03 + spec-entry + dist-info + the new step 4b — confirm step 4b uses the wrapper).
    - WR-01 discipline preserved: the BUILD_FAIL line uses `Write-Host ... -ForegroundColor Red` (NOT `Write-Error`) — already locked by Task 3's drift-guard test.
  </acceptance_criteria>
  <done>The PowerShell driver wires the new Python guard with the correct exit code, WR-01-compliant diagnostic emission, and the new exit code is documented in the header. Re-running `build.ps1` on Windows (covered by Plan 02 UAT) will exercise step 4b after PyInstaller produces the bundle.</done>
</task>

<task type="auto">
  <name>Task 3: Extend packaging/windows/README.md conda recipe + tests/test_packaging_spec.py with two new drift-guard tests</name>
  <files>packaging/windows/README.md, tests/test_packaging_spec.py</files>
  <read_first>
    - packaging/windows/README.md — read in full (current state). Pay attention to lines 14–28 (the fenced PowerShell `conda create -n musicstreamer-build` block — edit target).
    - tests/test_packaging_spec.py — read in full (existing fixtures + test patterns). Particularly the `_BUILD_PS1` module-level path + `build_ps1_source` fixture at lines 38–55 (clone-target for new `_README` + `readme_source` fixture), and the `test_build_ps1_post_bundle_dist_info_assertion_present` function at lines 295–401 (clone-target for the new `test_build_ps1_invokes_plugin_guard_with_exit_10` — specifically the WR-01 Write-Host adjacency check at lines 380–393 and the `fail_reasons` enumeration at lines 368–374).
    - tests/test_constants_drift.py:52–68 (`test_dev_launch_script_app_id_matches_constants`) — cross-file literal-parity analog for the new README↔required-list drift guard.
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md (Pattern 3 lines 416–469 — full canonical test shape for `test_readme_conda_recipe_lists_every_required_plugin_package`; Pitfall 5 lines 547–555 — regex must anchor on the fenced block close ``` ``` `` to avoid over-matching markdown prose)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md (`tests/test_packaging_spec.py` section lines 211–322 — the verbatim five-element clone checklist for `test_build_ps1_invokes_plugin_guard_with_exit_10`)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md (P-01, P-02 — static drift-guard pytest only; Claude's Discretion: extend `tests/test_packaging_spec.py` rather than adding a new file)
  </read_first>
  <action>
This task ships two file edits as ONE atomic commit because the drift-guard test imports from a file that this task also creates content in. Either edit alone would cause CI to red on the other side.

**Edit A — packaging/windows/README.md fenced conda recipe (lines 14–28).** Replace the existing PowerShell fenced block:
```
conda create -n musicstreamer-build -c conda-forge `
    python=3.12 pygobject gstreamer=1.28 pyinstaller `
    "pyinstaller-hooks-contrib>=2026.2"
conda activate musicstreamer-build
```

With the new expanded recipe (per RESEARCH lines 178–187 — researcher recommends the full explicit set, not just `gst-libav`, to make the recipe reproducible on a fresh build host per DG-02 confirmation):
```
conda create -n musicstreamer-build -c conda-forge `
    python=3.12 pygobject gstreamer=1.28 `
    gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly `
    gst-libav `
    pyinstaller "pyinstaller-hooks-contrib>=2026.2"
conda activate musicstreamer-build
# AAC playback requires gst-libav (Phase 69 — provides avdec_aac in gstlibav.dll).
# aacparse ships with gst-plugins-good's audioparsers plugin (gstaudioparsers.dll).
```

Constraints:
- Keep the leading list-item `1. **Miniforge / conda-forge environment** — create once on the build VM:` header line unchanged.
- Keep the fence language as ` ```powershell` (the regex in the drift-guard test anchors on the conda command literal AND the closing fence).
- Keep PowerShell line-continuation backticks at end of each split line.
- The two trailing comment lines starting with `# AAC playback requires gst-libav` and `# aacparse ships with gst-plugins-good` are valid PowerShell comments and must live INSIDE the fenced block (they are the human-readable rationale linking back to Phase 69; do not move them to markdown prose outside the fence).
- Keep the existing prose paragraph immediately AFTER the fenced block (starting `The conda-forge GStreamer package ships the MSVC build with...`) unchanged.

**Edit B — tests/test_packaging_spec.py** — Add THREE additions to the existing file (do not modify existing tests):

B.1 — Add a new module-level path constant + fixture for `README.md`, placed immediately AFTER the existing `_BUILD_PS1` constant + `build_ps1_source` fixture (around line 55, before the first `def test_spec_imports_copy_metadata` function). The shape mirrors the existing fixtures verbatim:
```
_README = (
    Path(__file__).resolve().parent.parent
    / "packaging"
    / "windows"
    / "README.md"
)


@pytest.fixture(scope="module")
def readme_source() -> str:
    assert _README.is_file(), f"expected README.md at {_README}"
    return _README.read_text(encoding="utf-8")
```

B.2 — Append a new test function `test_readme_conda_recipe_lists_every_required_plugin_package(readme_source: str) -> None:` AT THE END of the file. Body per RESEARCH Pattern 3 (lines 422–468) — verbatim shape:
   - Docstring cites `Phase 69 / P-01` and explains that the README conda recipe must mention every conda-forge package referenced in `tools/check_bundle_plugins.py REQUIRED_PLUGIN_DLLS` values.
   - `from tools.check_bundle_plugins import REQUIRED_PLUGIN_DLLS` (inside the test body to keep the import lazy).
   - `import re` (inside the test body).
   - Regex match: `block_match = re.search(r"conda create -n musicstreamer-build[^\n]*\n((?:[^\n]*\n)+?)```", readme_source)`. Per RESEARCH Pitfall 5, this regex terminates the match at the closing fence so comment lines outside fenced blocks can't match. Assert `block_match` is truthy with a message instructing future maintainers to update both this test and `tools/check_bundle_plugins.py` if the env name is renamed.
   - Extract `recipe_block = block_match.group(0)`.
   - `required_packages = {pkg for (_, pkg) in REQUIRED_PLUGIN_DLLS.values()}`.
   - `missing = [pkg for pkg in required_packages if pkg not in recipe_block]`.
   - `assert not missing` with a failure message naming `Phase 69 / P-01 drift-guard FAIL` and listing the missing packages.

B.3 — Append a SECOND new test function `test_build_ps1_invokes_plugin_guard_with_exit_10(build_ps1_source: str) -> None:` AT THE END of the file (after B.2). Body per PATTERNS lines 297–316 — clone the five-element shape from `test_build_ps1_post_bundle_dist_info_assertion_present`:
   - Docstring cites `Phase 69 / G-01 / WIN-05` and explains the drift-guard catches accidental removal of step 4b or accidental swap from Write-Host to Write-Error.
   - Rationale-tag check: `assert "POST-BUNDLE PLUGIN GUARD" in build_ps1_source` with a message instructing the maintainer to keep the step header literal stable.
   - Invocation-substring check: `assert "python ..\\..\\tools\\check_bundle_plugins.py" in build_ps1_source` (note the double-backslash for Python source).
   - BUILD_FAIL substring check: `assert "BUILD_FAIL reason=plugin_missing" in build_ps1_source`.
   - Exit-code substring check: `assert "exit 10" in build_ps1_source` AND `assert "10=post-bundle plugin-presence guard fail" in build_ps1_source` (covers BOTH the failure block AND the header documentation).
   - WR-01 Write-Host adjacency check — clone PATTERNS pattern at lines 304–315 verbatim. Locate `BUILD_FAIL reason=plugin_missing` in `build_ps1_source`, slice 120 chars before and 50 after, assert `"Write-Host" in before` with a message explaining the WR-01 rationale (Write-Error escalates to terminating error under `$ErrorActionPreference = "Stop"` and skips `exit 10`). Then slice 400 chars after the same index, assert `"exit 10" in after` with a message explaining the failure block adjacency requirement.

Test function names must match EXACTLY: `test_readme_conda_recipe_lists_every_required_plugin_package` and `test_build_ps1_invokes_plugin_guard_with_exit_10`. The Per-Task Verification Map in `69-VALIDATION.md` will be filled with these names.

Do NOT touch any of the existing 4 tests in the file. Do NOT touch the existing module-level constants (`_SPEC`, `_BUILD_PS1`) or fixtures (`spec_source`, `build_ps1_source`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_packaging_spec.py::test_readme_conda_recipe_lists_every_required_plugin_package tests/test_packaging_spec.py::test_build_ps1_invokes_plugin_guard_with_exit_10 -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `packaging/windows/README.md` conda recipe fenced PowerShell block contains all of: `gstreamer=1.28`, `gst-plugins-base`, `gst-plugins-good`, `gst-plugins-bad`, `gst-plugins-ugly`, `gst-libav`, `pyinstaller`, `"pyinstaller-hooks-contrib>=2026.2"`. Confirm with `grep -A 10 "conda create -n musicstreamer-build" packaging/windows/README.md | grep -c "gst-libav"` returns ≥1 (and same for each of the other four `gst-plugins-*` packages).
    - `packaging/windows/README.md` contains the literal `# AAC playback requires gst-libav (Phase 69` inside the fenced PowerShell block.
    - `tests/test_packaging_spec.py` defines `_README` module-level constant, `readme_source` fixture, and two new test functions named exactly `test_readme_conda_recipe_lists_every_required_plugin_package` and `test_build_ps1_invokes_plugin_guard_with_exit_10`.
    - `uv run pytest tests/test_packaging_spec.py -x` exits 0 — all existing tests + the two new tests pass.
    - `uv run pytest tests/test_packaging_spec.py::test_readme_conda_recipe_lists_every_required_plugin_package -x` exits 0.
    - `uv run pytest tests/test_packaging_spec.py::test_build_ps1_invokes_plugin_guard_with_exit_10 -x` exits 0.
    - Negative drift-guard: temporarily editing `tools/check_bundle_plugins.py REQUIRED_PLUGIN_DLLS` to add a fake entry like `"gstfake.dll": ("fake_element", "gst-fake-package")` would cause `test_readme_conda_recipe_lists_every_required_plugin_package` to fail. (This is a thought-experiment criterion — do NOT actually make the edit; just confirm the test logic would fire.)
  </acceptance_criteria>
  <done>Both edits ship in one commit; the two new drift-guard tests pass on Linux dev CI; future drift between the README recipe and `REQUIRED_PLUGIN_DLLS` will fail the pytest before any Windows build is attempted, and future drift between the documented exit code 10 / step 4b shape and the actual `build.ps1` will fail the second pytest the moment a maintainer touches the script.</done>
</task>

<task type="auto">
  <name>Task 4: Reconcile documentation — CONCERNS.md GStreamer Windows Plugin Availability fix + REQUIREMENTS.md WIN-05 row</name>
  <files>.planning/codebase/CONCERNS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (in full — confirm the `**GStreamer Windows Plugin Availability:**` concern entry is still at the lines 55–59 region the planner snapshot captured; the file uses a `- Issue / - Files / - Impact / - Fix approach` four-line shape per concern entry, preserve that structure)
    - .planning/REQUIREMENTS.md (in full — confirm the Windows Polish (WIN) section structure at lines 26–33, the Traceability table at lines 83–103, and the Coverage tally at lines 105–110)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md (DOC-01, DOC-04 — the operative reconciliation directives)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md (CONCERNS.md section lines 326–343 — exact replacement-text shape; REQUIREMENTS.md section lines 346–393 — the two edit sites + Coverage tally update)
  </read_first>
  <action>
Two surgical doc edits as one atomic commit.

**Edit A — .planning/codebase/CONCERNS.md, GStreamer Windows Plugin Availability section (lines 55–59 region).** Locate the concern entry whose header is `**GStreamer Windows Plugin Availability:**`. Preserve the four bullet lines `- Issue:`, `- Files:`, `- Impact:` exactly as they are. Replace ONLY the `- Fix approach:` line (currently line 59 in the planner snapshot, but the file may have shifted — anchor on the literal substring `Phase 44 bundling confirmed gst-libav is present in conda-forge build`) with this exact replacement text (per CONTEXT DOC-01 + PATTERNS lines 337–340):

`- Fix approach: Phase 69 confirmed gst-libav was missing from the conda recipe shipped through v2.0–v2.1.0; the production conda-forge gstreamer meta-package does not pull in any gst-plugins-* subpackages. README recipe now explicitly lists gst-libav gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly; a post-bundle plugin-presence guard (tools/check_bundle_plugins.py invoked from build.ps1 exit code 10) prevents future regressions, and a drift-guard pytest (tests/test_packaging_spec.py::test_readme_conda_recipe_lists_every_required_plugin_package) catches recipe↔required-list divergence on Linux dev CI before any Windows build is attempted.`

Critical constraints:
- DO NOT modify any other concern entry in the file.
- DO NOT delete the `- Issue / - Files / - Impact` bullets — only the `- Fix approach:` line is replaced.
- The replacement line MUST start with `- Fix approach:` and MUST NOT contain the literal substring `Phase 44 bundling confirmed gst-libav is present` after the edit (the drift-guard line in the test below enforces this).

**Edit B — .planning/REQUIREMENTS.md** — Three sub-edits, all in this single file:

B.1 — Windows Polish (WIN) section, append a new checkbox row at the END of the existing four-item list (after `- [x] **WIN-04**: ...` line):

`- [ ] **WIN-05**: AAC-encoded streams play on Windows — DI.fm AAC tier + SomaFM HE-AAC fixtures verified post-bundle-fix *(Phase 69)*`

(Leave the checkbox unchecked `[ ]` — Plan 02 UAT or `/gsd-complete-phase` flips it to `[x]`. The `*(Phase 69)*` italic-annotated phase reference matches the convention used at e.g. BUG-08 line 23.)

B.2 — Traceability table, append a new row AFTER the existing `| THEME-01 | Phase 66 | Complete |` row:

`| WIN-05 | Phase 69 | Pending |`

(Status `Pending` matches the existing `| WIN-02 | Phase 56 | Pending |` row at line 93.)

B.3 — Coverage tally (lines 105–110 region — anchor on `**Coverage:**`). Update the four lines from:
```
- v2.1 requirements: 19 total
- Mapped to phases: 19 ✓
- Unmapped: 0 ✓
- Complete: 18
- Pending: 1 (WIN-02 — SMTC Start Menu shortcut with AUMID)
```
to:
```
- v2.1 requirements: 20 total
- Mapped to phases: 20 ✓
- Unmapped: 0 ✓
- Complete: 18
- Pending: 2 (WIN-02, WIN-05)
```

(The `(WIN-02 — SMTC Start Menu shortcut with AUMID)` annotation in the pending line is replaced by the bare `(WIN-02, WIN-05)` enumeration per PATTERNS lines 386–392; the prior verbose annotation was a one-off.)

DO NOT modify any other section of REQUIREMENTS.md (Out of Scope table, Future Requirements, etc.).
  </action>
  <verify>
    <automated>grep -q "Phase 69 confirmed gst-libav was missing" .planning/codebase/CONCERNS.md && ! grep -q "Phase 44 bundling confirmed gst-libav is present" .planning/codebase/CONCERNS.md && grep -q "WIN-05" .planning/REQUIREMENTS.md && grep -q "| WIN-05 | Phase 69 | Pending |" .planning/REQUIREMENTS.md && grep -q "v2.1 requirements: 20 total" .planning/REQUIREMENTS.md && grep -q "Pending: 2 (WIN-02, WIN-05)" .planning/REQUIREMENTS.md && echo "doc reconciliation OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/CONCERNS.md` no longer contains the literal substring `Phase 44 bundling confirmed gst-libav is present` (negative drift-guard).
    - `.planning/codebase/CONCERNS.md` contains the literal `Phase 69 confirmed gst-libav was missing from the conda recipe shipped through v2.0–v2.1.0` inside the GStreamer Windows Plugin Availability concern entry's `- Fix approach:` bullet.
    - `.planning/codebase/CONCERNS.md` preserves the `- Issue:` / `- Files:` / `- Impact:` bullets immediately above the new `- Fix approach:` line (concern entry structure preserved).
    - `.planning/REQUIREMENTS.md` Windows Polish section contains `- [ ] **WIN-05**: AAC-encoded streams play on Windows — DI.fm AAC tier + SomaFM HE-AAC fixtures verified post-bundle-fix *(Phase 69)*` as the last bullet of the WIN list.
    - `.planning/REQUIREMENTS.md` Traceability table contains `| WIN-05 | Phase 69 | Pending |` as the final row of the table.
    - `.planning/REQUIREMENTS.md` Coverage tally reads `v2.1 requirements: 20 total`, `Mapped to phases: 20 ✓`, `Complete: 18`, `Pending: 2 (WIN-02, WIN-05)`.
    - The full Linux test suite continues to pass: `uv run pytest -x` exits 0 (sanity check — no doc edits should regress code).
  </acceptance_criteria>
  <done>The documentation drift surfaced during context-load is reconciled: CONCERNS.md no longer makes the stale "gst-libav is present" claim; REQUIREMENTS.md now tracks WIN-05 with Pending status pending Plan 02 UAT; Coverage tally accurately reflects 20 requirements / 2 pending. The full test suite continues to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Conda-forge package repository → build VM | Operator-driven `conda create / conda env update` pulls untrusted bytes from anaconda.org into the build environment. Adding `gst-libav` brings statically-linked FFmpeg into the bundle. |
| Build VM filesystem → bundled `dist/MusicStreamer/_internal/gst_plugins/*.dll` | PyInstaller copies plugin DLLs from the conda env into the bundle. The new G-01 guard validates only file presence, not content. |
| Inno Setup installer → end-user Windows host | Installer ships the bundle as a per-user install. Out of scope for Phase 69 (no installer changes). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | conda-forge `gst-libav` package (supply-chain) | accept | conda-forge gst-libav 1.28.2 ships LGPL-2.1-or-later FFmpeg statically linked. Same trust model as the existing `gstreamer=1.28` pin (which already ships LGPL GStreamer wholesale). Pinning to major.minor (`=1.28`) via conda's `run_exports` from `gstreamer=1.28` constrains the build family. Pinning to a SHA-256 hash is not standard practice for conda-forge packages in this project and would be a Phase-N follow-up if a CVE surfaces. **Documented in CONCERNS.md (existing entry) — not a Phase 69 mitigation target.** |
| T-69-02 | Spoofing | Build-time guard validates file presence, not authenticity (a malicious actor with write access to the build VM could substitute a renamed-but-empty `gstlibav.dll` and bypass the guard) | accept | The build VM is the operator's single-user Win11 box; threat model is a personal-use app. File-on-disk presence is the same trust level the existing post-bundle dist-info assertion uses (verifies a file exists + has a Version: line, not its signature). A SHA-256 manifest check is a YAGNI extension per RESEARCH Open Question 3 (recommendation: file-presence only). Phase 69's guard explicitly closes the **silent-failure** mode (DLL missing), not the **active-attack** mode. |
| T-69-03 | Information Disclosure / Repudiation | UAT-LOG.md records AAC fixture URLs (DI.fm + SomaFM) for post-fix attestation | accept | URLs are public radio stream endpoints with no credential surface (D-15 DI.fm cookies are sent via the AudioAddict API, not embedded in the URL). UAT-LOG.md commits to `.planning/phases/69-*/` and is mirrored to QNAP→GitHub (operator's project context: QNAP pushes are effectively public). No PII or auth tokens in scope. |
</threat_model>

<verification>
After ALL four tasks complete:

1. Full Linux test suite green: `uv run pytest -x` exits 0 (no regressions to the existing 399 tests + the 2 new drift-guard tests = 401 total).
2. `tools/check_bundle_plugins.py` import and exit-code smoke test passes: `uv run python -c "from tools.check_bundle_plugins import REQUIRED_PLUGIN_DLLS; print(REQUIRED_PLUGIN_DLLS)"` prints the exact two-key dict.
3. Static guard cohesion: every conda-forge package in `REQUIRED_PLUGIN_DLLS` values appears in `packaging/windows/README.md`'s conda recipe block (verified by the new `test_readme_conda_recipe_lists_every_required_plugin_package` test).
4. Build.ps1 exit code 10 cohesion: the failure block's WR-01 discipline (Write-Host adjacency + exit 10 within 400 chars) is locked by `test_build_ps1_invokes_plugin_guard_with_exit_10`.
5. Documentation reconciliation: CONCERNS.md no longer contains the stale Phase 44 claim; REQUIREMENTS.md carries WIN-05 with Pending status.

Plan 02 (Win11 VM UAT) is the SHIP gate — it proves the bundle change actually makes AAC streams play on the operator's VM. Plan 01 is the development gate — it proves the code-level work is correct on Linux dev CI.
</verification>

<success_criteria>
- `tools/check_bundle_plugins.py` exists and `REQUIRED_PLUGIN_DLLS` matches the exact spec (`{"gstlibav.dll": ("avdec_aac", "gst-libav"), "gstaudioparsers.dll": ("aacparse", "gst-plugins-good")}`).
- `packaging/windows/build.ps1` invokes the new tool with `exit 10` and WR-01-compliant Write-Host diagnostic, and the new exit code is documented in the header comment.
- `packaging/windows/README.md` conda recipe lists `gst-libav gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly` alongside the existing `gstreamer=1.28` pin.
- `tests/test_packaging_spec.py` ships two new drift-guard tests (`test_readme_conda_recipe_lists_every_required_plugin_package` and `test_build_ps1_invokes_plugin_guard_with_exit_10`), both green.
- `.planning/codebase/CONCERNS.md` GStreamer Windows Plugin Availability concern is reconciled per DOC-01.
- `.planning/REQUIREMENTS.md` carries the WIN-05 row in both Windows Polish + Traceability sections, with Coverage tally updated to 20 total / 2 pending.
- `uv run pytest -x` exits 0 (full Linux test suite passes).
</success_criteria>

<output>
After completion, create `.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-01-SUMMARY.md` documenting:
- The exact `REQUIRED_PLUGIN_DLLS` dict shipped (single source of truth).
- The exit code 10 documentation locked in `build.ps1:5–7` header + step 4b block.
- The new conda recipe form in README.md (with the inline `# AAC playback requires gst-libav (Phase 69)` rationale).
- The two new test function names + their assertion summaries.
- The CONCERNS.md fix-approach line replacement (anchor literal `Phase 69 confirmed gst-libav was missing` for future grep discovery).
- The REQUIREMENTS.md Coverage tally bump (19→20 total; Pending 1→2).
- Confirmation that no app-side Python code in `musicstreamer/` was touched (boundary lock per D-02).
- A pointer to Plan 02 (`.planning/phases/69-*/69-02-PLAN.md`) for the operator UAT cycle.
</output>
