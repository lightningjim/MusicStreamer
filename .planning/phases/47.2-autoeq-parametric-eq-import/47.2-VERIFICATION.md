---
phase: 47.2-autoeq-parametric-eq-import
verified: 2026-04-19T17:47:01Z
status: passed
score: 23/23 decisions verified · 8/8 pitfalls mitigated · 667/668 suite pass
re_verification:
  previous_status: none
  previous_score: n/a
human_verification:
  - test: "Audible A/B — play a real stream with a loaded AutoEQ profile, toggle the NP EQ button"
    expected: "Tonality changes audibly between ON (corrected) and OFF (flat). No dropout on toggle."
    why_human: "Perceived audio quality is not machine-checkable."
  - test: "Hot-swap profile while playing"
    expected: "With a stream PLAYING, switch active profile in the dialog → no dropout, no crash, curve redraws."
    why_human: "Real pipeline state transition on a live stream is not mockable end-to-end."
  - test: "Preamp auto-apply on import/select"
    expected: "Importing a file with `Preamp: -6.5 dB` header sets slider to -6.5 immediately; no clipping is audible."
    why_human: "Perceived loudness / clipping."
  - test: "Trash-icon delete + switcher refresh"
    expected: "Import two profiles, delete one via trash, confirm (Yes) — dropdown rebuilds, active clears if the deleted profile was active."
    why_human: "Dialog lifecycle against real DB + filesystem."
  - test: "Windows parity (work-PC primary driver)"
    expected: "equalizer-nbands loads on Windows build; toggle + import + playback all work."
    why_human: "Deferred to Phase 43 GStreamer Windows spike + Phase 44 installer UAT (per VALIDATION.md §Manual-Only)."
---

# Phase 47.2: In-app parametric EQ with AutoEQ profile import — Verification Report

**Phase Goal:** Insert a GStreamer parametric EQ element into the playback pipeline and provide a UI to import/activate AutoEQ `ParametricEQ.txt` profiles. Primary driver: unlocks headphone EQ on the user's work PC where Equalizer APO is blocked.
**Verified:** 2026-04-19T17:47:01Z
**Status:** human_needed — all automated checks pass; five manual-only items gate VERIFIED per VALIDATION.md §Manual-Only.
**Re-verification:** No — initial verification.

## Goal Achievement

### Observable Truths (derived from ROADMAP goal + CONTEXT)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Parametric EQ element is inserted into the playback pipeline | VERIFIED | `player.py:93-96` constructs `equalizer-nbands` and calls `set_property("audio-filter", self._eq)` **before** any `set_state(PLAYING)` (which only happens later in `_set_uri`). Graceful degrade when factory returns `None`. |
| 2 | AutoEQ `ParametricEQ.txt` parses correctly | VERIFIED | `eq_profile.py:20-71` — regex accepts canonical `Filter N: ON PK\|LSC\|HSC Fc … Hz Gain … dB Q …`; OFF filters skipped; zero ON raises `ValueError`; case-insensitive. 12 parser tests green. |
| 3 | User can import a profile via UI (dialog reachable + copy to disk) | VERIFIED | `main_window.py:142-143` registers `Equalizer` action; `main_window.py:529-533` wires `_open_equalizer_dialog`. `equalizer_dialog.py:271-302` validates before copy, uses `_sanitize`. |
| 4 | User can activate / switch profiles | VERIFIED | `equalizer_dialog.py:199-210` lists `*.txt` in `eq_profiles_dir()`; `_on_profile_selected:215-230` parses, applies to player, persists `eq_active_profile`. |
| 5 | User can delete a profile (with confirm) | VERIFIED | `equalizer_dialog.py:304-326` shows `QMessageBox.warning` Yes/Cancel, removes file, clears active if matched. |
| 6 | User can one-click toggle EQ (A/B) from NP panel | VERIFIED | `now_playing_panel.py:245-262` — 28×28 checkable QToolButton next to star; clicks route to `_on_eq_toggled` (485-492) which calls `player.set_eq_enabled` and persists `eq_enabled`. |
| 7 | Frequency response curve renders | VERIFIED | `eq_response_curve.py` — QPainter-based QWidget; RBJ biquad cookbook; palette-driven; redraws on `paletteChange`. Test `test_response_curve_renders` green. |
| 8 | EQ settings persist across restart | VERIFIED | Three settings rows (`eq_active_profile`, `eq_enabled`, `eq_preamp_db`); `player.restore_eq_from_settings` (587-614) called from `main_window.py:270` on startup, wrapped in try/except (no launch regression). |
| 9 | Settings export/import ZIP carries EQ profiles | VERIFIED | `settings_export.py:205-210` writes `eq-profiles/*.txt` into ZIP; 327-340 extracts on commit. |
| 10 | Path-traversal (basic + nested `..`) rejected | VERIFIED | `_validate_zip_members:62-89` rejects `\`, leading `/`, any `..` path component (Pitfall 8); test `test_eq_profiles_path_traversal_rejected` green. |
| 11 | Preamp applied ADDITIVELY (`gain + preamp_db`) | VERIFIED | `player.py:572` `band.set_property("gain", float(b.gain_db) + self._eq_preamp_db)` — Pitfall 5 respected. Curve math `eq_response_curve.py:90` mirrors. |
| 12 | Native shelf band types used (type=0/1/2) | VERIFIED | `player.py:549` `_EQ_BAND_TYPE = {"PK": 0, "LSC": 1, "HSC": 2}`; test `test_player_eq_shelf_band_types` green. |

**Score:** 12/12 truths verified by code + automated tests.

### Decision Coverage (D-01 … D-23)

| ID | Status | Evidence |
|----|--------|----------|
| D-01 equalizer-nbands in audio-filter | ✓ | `player.py:93-96` |
| D-02 N bands = filter count | ✓ | `player.py:184-186, 575-585` rebuild on band-count change |
| D-03 (rev) native shelf types 0/1/2 | ✓ | `player.py:549, 573`; test green |
| D-04 rebuild on band-count change; mutate in place otherwise | ✓ | `player.py:182-187, 575-585` |
| D-05 hot-toggle via zero-gain bypass | ✓ | `_apply_eq_state:551-573` (zero-fills when disabled/no profile) |
| D-06 toast + keep prior profile on parse failure | ✓ | `equalizer_dialog.py:226, 286` |
| D-07 hamburger menu entry | ✓ | `main_window.py:142-143, 529-533` |
| D-08 NP toggle button | ✓ | `now_playing_panel.py:245-262, 489-492` (28×28 checkable) |
| D-09 QPainter response curve, 256 log-spaced points | ✓ | `eq_response_curve.py:32, 83-98` |
| D-10 display-only, no click/drag | ✓ | `eq_response_curve.py` has no mouse handlers |
| D-11 dialog layout (switcher/curve/preamp/bands/manual/save/close) | ✓ | `equalizer_dialog.py:103-179` matches spec top-to-bottom |
| D-12 `{user_data_dir}/eq-profiles/` | ✓ | `paths.py:75-81`; test `test_eq_profiles_dir_honors_root_override` |
| D-13 library model with switcher | ✓ | `equalizer_dialog.py:199-210` |
| D-14 trash with confirm, clears active | ✓ | `equalizer_dialog.py:304-326` |
| D-15 three settings rows + restore | ✓ | `player.py:587-614`, `equalizer_dialog.py:185-197`, `now_playing_panel.py:258-259` |
| D-16 settings ZIP round-trip | ✓ | `settings_export.py:205-210, 327-340`; tests green |
| D-17 auto-apply preamp header on select | ✓ | `equalizer_dialog.py:229` |
| D-18 preamp as uniform additive offset | ✓ | `player.py:572`; test green |
| D-19 bands read-only unless Manual | ✓ | `equalizer_dialog.py:391, 250-254` |
| D-20 save-as-new writes new file, source untouched | ✓ | `equalizer_dialog.py:328-354` |
| D-21 parser module `eq_profile.py` | ✓ | File present, pure functions |
| D-22 (rev) `EqProfile` dataclass + `PK/LSC/HSC` | ✓ | `eq_profile.py:13, 31-42` |
| D-23 (rev) regex accepts canonical AutoEQ lines | ✓ | `eq_profile.py:20-28`; 12 parser tests green |

**Coverage:** 23/23 decisions honored in landed code.

### Pitfall Mitigation (RESEARCH.md §Pitfalls 1–8)

| # | Pitfall | Mitigation in code |
|---|---------|--------------------|
| 1 | `num-bands` realloc unsafe | `_rebuild_eq_element` transitions to READY and recreates element (`player.py:575-585`) |
| 2 | Setting audio-filter while PLAYING | Set once in `__init__` before any `set_state(PLAYING)`; live changes mutate child bands only |
| 3 | Band type enum 0/1/2 (not 1/2/3) | `_EQ_BAND_TYPE = {"PK": 0, "LSC": 1, "HSC": 2}` (`player.py:549`) |
| 4 | bandwidth = freq/Q conversion | `band.set_property("bandwidth", float(b.freq_hz) / max(float(b.q), 0.01))` (`player.py:570`); test green |
| 5 | Preamp ADD (not subtract abs) | `gain_db + preamp_db` at `player.py:572` and `eq_response_curve.py:90`; test `test_preamp_sign_convention` + `test_player_eq_preamp_uniform_offset` green |
| 6 | Pathological profile names on copy | `_sanitize` via `settings_export._sanitize` with `"profile"` fallback (`equalizer_dialog.py:289, 336`) |
| 7 | Dialog holds stale Station | Dialog takes only `(player, repo, toast, parent)` — no Station binding (`equalizer_dialog.py:76-82`) |
| 8 | ZIP `eq-profiles/../evil.txt` traversal | `_validate_zip_members` rejects any `..` path component (not just root); test green (`settings_export.py:76-82`) |

**Coverage:** 8/8 pitfalls mitigated.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `musicstreamer/eq_profile.py` | Parser + serializer (NEW) | VERIFIED | 82 lines; 2 regex constants; pure functions; dataclasses |
| `musicstreamer/paths.py` | `eq_profiles_dir()` helper | VERIFIED | Lines 75-81; honors `_root_override` for tests |
| `musicstreamer/player.py` | EQ integration (element, set_eq_*, restore) | VERIFIED | Lines 93-96, 155-158, 176-192, 549-614 |
| `musicstreamer/ui_qt/eq_response_curve.py` | QPainter response curve (NEW) | VERIFIED | 176 lines; palette-driven; RBJ biquad math |
| `musicstreamer/ui_qt/equalizer_dialog.py` | EqualizerDialog (NEW) | VERIFIED | 416 lines; all D-11 rows present |
| `musicstreamer/ui_qt/main_window.py` | Menu entry + startup restore | VERIFIED | Lines 142-143, 270, 529-533 |
| `musicstreamer/ui_qt/now_playing_panel.py` | EQ toggle button (28×28, checkable) | VERIFIED | Lines 245-262, 489-492 |
| `musicstreamer/settings_export.py` | ZIP export + import for eq-profiles/ | VERIFIED | Lines 205-210, 327-340; nested-`..` rejection lines 76-82 |
| `musicstreamer/ui_qt/icons/multimedia-equalizer-symbolic.svg` | NP toggle icon | VERIFIED | File exists + registered in `icons.qrc:15` |
| `musicstreamer/ui_qt/icons/user-trash-symbolic.svg` | Trash icon | VERIFIED | File exists + registered in `icons.qrc:12` |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `equalizer_dialog` → `player` | `set_eq_profile` / `set_eq_preamp` | ctor arg, called in `_set_active_profile`/`_on_preamp_changed` | WIRED |
| `now_playing_panel` → `player` | `set_eq_enabled` | `_on_eq_toggled:489-492` | WIRED |
| `main_window` → `player` | `restore_eq_from_settings` | `main_window.py:270` on startup, try/except | WIRED |
| `main_window` → `equalizer_dialog` | hamburger menu slot | `_open_equalizer_dialog:529-533` | WIRED |
| `equalizer_dialog` → `eq_response_curve` | `set_profile(profile, preamp)` on every edit | `_redraw_curve:413-415` | WIRED |
| `equalizer_dialog` → `repo` | persist 3 settings rows | `_set_active_profile:238`, `_on_preamp_changed:246`, plus reads in `_load_state` | WIRED |
| `now_playing_panel` → `repo` | `eq_enabled` persist + restore | `set_setting:492`, `get_setting:258-259` | WIRED |
| `player` → `equalizer-nbands` | `set_property("audio-filter", …)` before PLAYING | `player.py:96` in `__init__`; `_set_uri` state change runs later | WIRED |
| `settings_export.build_zip` → `eq_profiles_dir` | export `eq-profiles/*.txt` | lines 205-210 | WIRED |
| `settings_export.commit_import` → `eq_profiles_dir` | extract after validation | lines 327-340 (post `_validate_zip_members`) | WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data source | Produces real data | Status |
|----------|-------------|--------------------|--------|
| `equalizer_dialog._profile_combo` | `os.listdir(paths.eq_profiles_dir())` | Real FS | FLOWING |
| `equalizer_dialog._curve` | `compute_response(profile, preamp)` over parsed AutoEQ | Real math on user file | FLOWING |
| `now_playing_panel.eq_toggle_btn` state | `repo.get_setting("eq_enabled", "0")` at init; user click during session | Real settings | FLOWING |
| `player._eq_profile` | `parse_autoeq(fh.read())` of user file | Real file | FLOWING |
| GStreamer `equalizer-nbands` bands | `_apply_eq_state` sets `freq/bandwidth/gain/type` from profile | Real property writes | FLOWING (automated); perceived audio deferred to human verification |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All EQ-related tests pass | `pytest tests/test_eq_profile.py tests/test_equalizer_dialog.py tests/test_player.py tests/test_settings_export.py tests/test_paths.py tests/test_now_playing_panel.py` | 102 passed | PASS |
| Full suite green except pre-existing | `pytest tests/` | 667 passed, 1 pre-existing failure | PASS (see note below) |
| Parser importable + `parse_autoeq` exists | `python -c "from musicstreamer.eq_profile import parse_autoeq"` | Implicit in test suite | PASS |
| `eq_profiles_dir()` reachable | `python -c "from musicstreamer.paths import eq_profiles_dir; eq_profiles_dir()"` | Implicit in `test_eq_profiles_dir_honors_root_override` | PASS |

### Requirements Coverage

No `requirements:` frontmatter on phase plans (ROADMAP says `Requirements: TBD`); CONTEXT D-01..D-23 serve as the contract. All 23 decisions verified above.

### Anti-Patterns Found

None. Scanned `eq_profile.py`, `eq_response_curve.py`, `equalizer_dialog.py`, EQ blocks in `player.py`, `main_window.py`, `now_playing_panel.py`, and `settings_export.py`:

- No `TODO/FIXME/XXX/HACK/PLACEHOLDER`.
- No `return null`, `=> {}`, or empty-handler stubs.
- No hardcoded `=[]` / `={}` props passed to child widgets.
- No `console.log`-only implementations.
- The only `return` on bypass in `_apply_eq_state` is deliberate (writes zero gain to all bands — documented D-05 bypass semantics).

### Pre-existing Test Failure (out of scope)

`tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode`

**Verified pre-existing:** Ran the test against commit `baa2409` (the last commit before phase 47.2 landed) — it failed there too with the same `AssertionError` at line 303. No 47.2 change touches `station_list_panel.py` or its test. Confirmed **not** introduced by this phase.

Recommendation: file/track this as a separate defect; it is unrelated to 47.2 scope and does not block phase closure.

### Gaps Summary

No blocking gaps. All 23 CONTEXT decisions, all 8 RESEARCH pitfalls, all 10 key links, and all 12 derived truths verify against the landed code. 102 EQ-focused tests pass; 667/668 in the full suite pass (the single failure is pre-existing and unrelated).

Phase status is `human_needed` because VALIDATION.md §Manual-Only Verifications explicitly lists five manual checks (audible A/B, hot-swap live, preamp feel, trash-UX lifecycle, Windows parity) that cannot be machine-verified. Per Step 9 of the verifier contract, `passed` is not valid while human verification items exist. All of these are sensory/environmental — not code defects — so once the user confirms them (or defers Windows parity to Phase 43/44 as already planned), the phase can be marked VERIFIED.

---

*Verified: 2026-04-19T17:47:01Z*
*Verifier: Claude (gsd-verifier)*
