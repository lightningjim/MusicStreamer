---
phase: 65
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - musicstreamer/__version__.py
autonomous: true
requirements: [VER-02]
tags: [versioning, cleanup, single-source-of-truth]
must_haves:
  truths:
    - "musicstreamer/__version__.py is deleted (D-06) — single source of truth for the version string is now pyproject.toml [project].version, read at runtime via importlib.metadata"
    - "Zero remaining importers of musicstreamer.__version__ in the post-deletion tree (D-06a grep gate clean) — no broken imports introduced"
    - "build.ps1 still passes (it reads from pyproject.toml directly via regex per RESEARCH Q6) — no Windows-build regression"
    - "The full test suite is GREEN after the deletion — including all 6 Plan 65-01 tests + 4 Plan 65-02 tests + every pre-existing test"
  artifacts:
    - path: "musicstreamer/__version__.py"
      provides: "DELETED — file no longer exists"
      contains: "DELETED"
  key_links:
    - from: "Single source of truth"
      to: "pyproject.toml [project].version"
      via: "importlib.metadata.version('musicstreamer') (Plan 65-01) + copy_metadata in PyInstaller bundle (Plan 65-02)"
      pattern: "importlib\\.metadata|copy_metadata"
---

<objective>
Delete `musicstreamer/__version__.py`. The file is a stale literal mirror (`__version__ = "2.0.0"`); its own docstring forecasts this exact deletion ("Future About dialog / hamburger menu footer (runtime read)"). Once Plan 65-01 wires the runtime read site and Plan 65-02 wires the bundle-side metadata shipping, the mirror has zero remaining purpose.

Run the D-06a grep gate one final time AGAINST THE POST-WAVE-1 TREE (i.e., after Plans 01 and 02 have committed their changes) to prove zero importers exist before the `git rm`. This is a belt-and-braces guard — RESEARCH Q5 verified the gate is clean today, and Plan 65-01's Task 1 ran the gate before any code edit, but a final pre-deletion run guarantees no Plan 01 / Plan 02 edit accidentally introduced a new importer between research time and execution time. The gate is dirt-cheap and surfaces drift loudly.

Purpose: Closes the internal promise of Phase 65 — "a single literal in `pyproject.toml`, read everywhere via `importlib.metadata`. No second source can drift."

Output: A single-file deletion (one `git rm` operation) plus a post-deletion full-suite green confirmation. This is intentionally a tiny consolidation plan in its own wave — it depends on both 65-01 (production code uses importlib.metadata, not `__version__.py`) and 65-02 (bundle ships dist-info, not `__version__.py`) to be safe. Splitting it out makes the dependency on Plans 01 + 02 explicit and gives the executor a clean atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-CONTEXT.md
@.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-RESEARCH.md
@.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-PATTERNS.md
@.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-VALIDATION.md

# Prior plan summaries — required reading for this plan because it depends on their work
@.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-01-SUMMARY.md
@.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-02-SUMMARY.md

# File this plan deletes — read before deleting (sanity-check the contents match expectations)
@musicstreamer/__version__.py

<interfaces>
<!-- D-06a grep gate command (run before deletion + after deletion to lock the contract). -->

CONTEXT D-06a / RESEARCH Q5 — pre-deletion grep gate (must exit with status 1 = zero hits):
```bash
git grep -nl "from musicstreamer\.__version__\|musicstreamer/__version__\|__version__\.py\|musicstreamer\.__version__" -- ':!.planning' ':!.claude/worktrees' ':!.venv'
```

The four patterns (in order):
1. `from musicstreamer.__version__` — direct `from ... import` style
2. `musicstreamer/__version__` — string-path reference (e.g. in build scripts)
3. `__version__.py` — bare filename reference
4. `musicstreamer.__version__` — module-path attribute access (e.g. `import musicstreamer; musicstreamer.__version__`)

Excluded paths (`':!...'`):
- `.planning/` — planning documents reference the file by name (CONTEXT.md, RESEARCH.md, PATTERNS.md). These references are about the file, not importers of the symbol. Always excluded.
- `.claude/worktrees` — git worktrees attached to other phases; not part of the live tree.
- `.venv` — virtualenv site-packages (will contain dist-info / `.pyc` references).

Pre-existing acceptable hits (RESEARCH Q5 verified these do NOT match the gate's regex):
- `packaging/windows/build.ps1:85` — `PyInstaller.__version__` (third-party, unrelated)
- `.claude/skills/spike-findings-musicstreamer/sources/43-gstreamer-windows-spike/build.ps1:72` — `gi.__version__` / `PyInstaller.__version__` (third-party diagnostic, unrelated)

Both match `__version__` (the bare token) but NOT any of the four regex patterns above (which all require either `musicstreamer.__version__`, `from musicstreamer.__version__`, `musicstreamer/__version__`, or `__version__.py`). The gate is correctly scoped.

RESEARCH Q6 confirms `packaging/windows/build.ps1:141-148` reads version from `pyproject.toml` directly via regex, NOT from `__version__.py`:
```powershell
$pyproject = Get-Content "..\..\pyproject.toml" -Raw
if ($pyproject -match '(?ms)^\[project\].*?^version\s*=\s*"([^"]+)"') {
    $appVersion = $matches[1]
}
```
Deletion is therefore safe for the Windows build pipeline.

Current `musicstreamer/__version__.py` content (verified at planning time):
```python
"""Single source of truth for the application version.
... (10 lines of docstring)
"""
__version__ = "2.0.0"
```
The file is stale (pyproject.toml has `2.1.63+`), and the docstring at line 6 explicitly forecasts Phase 65: "Future About dialog / hamburger menu footer (runtime read)".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-run D-06a grep gate against post-Wave-1 tree, then `git rm musicstreamer/__version__.py`</name>
  <files>musicstreamer/__version__.py</files>
  <behavior>
    - Pre-deletion: D-06a grep gate exits with status 1 (zero matches) — proving Plans 65-01 and 65-02 did NOT introduce a new `__version__` importer. (RESEARCH Q5 verified this is clean today; this final re-run is a belt-and-braces guard for the post-Wave-1 tree.)
    - Deletion: `musicstreamer/__version__.py` no longer exists in the working tree (`git rm` records the deletion).
    - Post-deletion: D-06a grep gate STILL exits with status 1 (no remaining references to a now-deleted file), AND `tests/test_main_window_integration.py` + `tests/test_main_run_gui_ordering.py` + `tests/test_version.py` + `tests/test_packaging_spec.py` all stay GREEN, AND the full suite stays GREEN.
  </behavior>
  <action>
    Step 1 — Read `musicstreamer/__version__.py` once and confirm contents match the planning expectation (`__version__ = "2.0.0"` literal + the forecasting docstring). If the contents have drifted (e.g., someone hand-edited the literal between Phase 65 research and execution), STOP and surface the discrepancy in the SUMMARY rather than blindly deleting.

    Step 2 — Re-run the D-06a grep gate against the current (post-Wave-1) tree:

    ```bash
    git grep -nl "from musicstreamer\.__version__\|musicstreamer/__version__\|__version__\.py\|musicstreamer\.__version__" -- ':!.planning' ':!.claude/worktrees' ':!.venv' && exit 1 || echo "GREP_GATE_OK no remaining importers — safe to delete"
    ```

    Expected output: `GREP_GATE_OK no remaining importers — safe to delete`. If ANY hit appears, STOP and surface it in the SUMMARY:
    - If the hit is in production code (`musicstreamer/**/*.py`), the offending importer was introduced by Plans 65-01 or 65-02 by mistake — fix the importer to use `importlib.metadata.version("musicstreamer")` instead, then re-run the gate.
    - If the hit is in a test file (`tests/**/*.py`), the test must be updated to use `importlib.metadata.version` (the same shape Plan 65-01 wires).
    - If the hit is in `packaging/` outside the excluded paths, evaluate whether the reference is a string-path (acceptable) or an importer (must be fixed).
    - If the hit is in `.planning/` despite the exclusion (shouldn't happen — gate excludes `.planning`), check the grep flags and re-run.

    Step 3 — Delete the file:

    ```bash
    git rm musicstreamer/__version__.py
    ```

    This stages the deletion. Do NOT use `rm` followed by `git add` — `git rm` is the canonical idiom and produces a cleaner commit history.

    Step 4 — Re-run the D-06a grep gate POST-deletion to confirm no dangling references:

    ```bash
    git grep -nl "from musicstreamer\.__version__\|musicstreamer/__version__\|__version__\.py\|musicstreamer\.__version__" -- ':!.planning' ':!.claude/worktrees' ':!.venv' && exit 1 || echo "GREP_GATE_OK post-deletion — file removed cleanly"
    ```

    Expected: same `GREP_GATE_OK` output. (The file no longer exists, so the second pattern `musicstreamer/__version__` cannot match itself.)

    Step 5 — Run the full test suite to confirm zero regression:

    ```bash
    uv run pytest -x
    ```

    Expected: full suite GREEN. If any test fails, the most likely cause is a test that imported `musicstreamer.__version__` and slipped past the grep gate (perhaps via dynamic import or a string that the gate missed). Fix or update the test, then re-run.

    Constraints:
    - Do NOT touch any other file. This plan is a SINGLE deletion + verification.
    - Do NOT replace `__version__.py` with a placeholder/shim. CONTEXT D-06 mandates outright deletion; the runtime-read mechanism (Plan 65-01) supersedes the file's purpose entirely.
    - Do NOT modify `packaging/windows/build.ps1` — RESEARCH Q6 confirms it reads from `pyproject.toml` directly; no edit needed.
    - Do NOT modify `pyproject.toml` — Phase 63's auto-bump is the single write site; Phase 65 is read-only with respect to it.
    - If the grep gate fires and surfaces an importer that Plans 01 or 02 should have removed but didn't, the correct response is to fix the importer (not silently leave `__version__.py` in place). Phase 65's whole point is single-source-of-truth; leaving a mirror in place would defeat the phase.
  </action>
  <verify>
    <automated>! test -f musicstreamer/__version__.py &amp;&amp; (git grep -nl "from musicstreamer\.__version__\|musicstreamer/__version__\|__version__\.py\|musicstreamer\.__version__" -- ':!.planning' ':!.claude/worktrees' ':!.venv'; test $? -eq 1) &amp;&amp; uv run pytest -x</automated>
  </verify>
  <done>
    - `musicstreamer/__version__.py` no longer exists in the working tree (`test -f musicstreamer/__version__.py` returns false).
    - The deletion is staged for commit (`git status` shows `deleted: musicstreamer/__version__.py`).
    - D-06a grep gate exits with status 1 (zero matches) post-deletion (VER-02-G GREEN).
    - Full test suite (`uv run pytest -x`) GREEN — including all Plan 65-01 + Plan 65-02 tests.
    - `packaging/windows/build.ps1` is UNCHANGED (per RESEARCH Q6).
    - `pyproject.toml` is UNCHANGED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-deletion source tree → grep gate | The grep gate operates on the entire git-tracked source tree (excluding `.planning`, `.claude/worktrees`, `.venv`). The tree is developer-controlled; no untrusted input crosses this boundary. |
| Pre-deletion source tree → post-deletion source tree | `git rm` removes a single file. No untrusted input, no network access, no code execution. The deletion is atomic at the git index level. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-05 | Denial of Service | accidental deletion of an actively-imported file | mitigate | D-06a grep gate runs BEFORE deletion (Step 2) AND AFTER deletion (Step 4). The pre-deletion gate proves zero importers exist; the post-deletion gate confirms no dangling references remain. Belt-and-braces — the same gate runs in Plan 65-01 Task 1, which means by the time this plan runs, the gate has already been verified clean three times (research, Plan 01, Plan 03). |
| T-65-06 | Tampering | a malicious / well-meaning future contributor restoring `__version__.py` and re-introducing drift | accept | The risk is inherent to any deleted file in a long-lived repo. Mitigation: future Phase 65 audit + the SUMMARY artifact records the rationale. If the file is ever resurrected, future Phase XX should re-run the D-06a grep gate to confirm no regression. Not blocking for this plan. |

**Block on: high — no high-severity threats identified for Phase 65 Plan 03.** All threats either mitigated by the existing grep gate (T-65-05) or accepted (T-65-06 — future audit problem, not Phase 65's). No new attack surface introduced; one less file in the source tree means one fewer drift target.
</threat_model>

<verification>
After the single task completes, the post-deletion verification chain is:

1. `! test -f musicstreamer/__version__.py` — file is gone.
2. `git grep -nl "from musicstreamer\.__version__\|musicstreamer/__version__\|__version__\.py\|musicstreamer\.__version__" -- ':!.planning' ':!.claude/worktrees' ':!.venv'; test $? -eq 1` — D-06a grep gate post-deletion (VER-02-G).
3. `uv run pytest -x` — full suite GREEN.

Manual UAT items deferred to `/gsd-verify-work`:
- VER-02-I: Kyle launches `uv run python -m musicstreamer` on his Linux Wayland rig, opens the hamburger menu, confirms the last entry reads `v{version}` (e.g. `v2.1.65`) and is greyed out.
- VER-02-J: Kyle runs `packaging/windows/build.ps1` on the Win11 VM, installs the resulting .exe, launches, opens the hamburger menu, confirms the same `v{version}` matches the dev value and that the bundle did NOT raise `PackageNotFoundError` at startup (proves Plan 65-02's `copy_metadata` edit works end-to-end).
</verification>

<success_criteria>
- `musicstreamer/__version__.py` is deleted (file no longer exists in working tree; deletion staged for commit).
- D-06a grep gate exits with status 1 both before AND after the deletion.
- VER-02-G is now GREEN.
- Full test suite (`uv run pytest -x`) GREEN.
- `packaging/windows/build.ps1` is UNCHANGED (deletion did not break the Windows build script — RESEARCH Q6 invariant).
- `pyproject.toml` is UNCHANGED (Phase 63 auto-bump invariant).
- All 6 Plan 65-01 tests + 4 Plan 65-02 tests + every pre-existing test still GREEN.
- Single source of truth invariant established: `pyproject.toml [project].version` is the only literal version write site in the repo; everywhere else reads via `importlib.metadata`.
</success_criteria>

<output>
After completion, create `.planning/phases/65-show-current-version-in-app-unsure-yet-if-either-in-the-hamb/65-03-SUMMARY.md` with:
- Confirmation that `musicstreamer/__version__.py` has been deleted (`git rm` output captured).
- Pre-deletion D-06a gate result: GREP_GATE_OK.
- Post-deletion D-06a gate result: GREP_GATE_OK.
- Full test suite result: number of passing tests + zero failures.
- Confirmation that `packaging/windows/build.ps1` and `pyproject.toml` were NOT modified.
- Status of all VER-02 sub-checks:
  - VER-02-A: GREEN (Plan 01)
  - VER-02-B: GREEN (Plan 01)
  - VER-02-C: GREEN (Plan 01)
  - VER-02-D: GREEN (Plan 01)
  - VER-02-E: GREEN (Plan 01)
  - VER-02-F: GREEN (Plan 01)
  - VER-02-G: GREEN (Plan 03 — this plan)
  - VER-02-H: GREEN (Plan 02)
  - VER-02-I: PENDING — manual UAT (deferred to `/gsd-verify-work`)
  - VER-02-J: PENDING — manual Win11 VM UAT (deferred to `/gsd-verify-work`)
- Note: phase complete pending manual UAT items VER-02-I and VER-02-J.
</output>
