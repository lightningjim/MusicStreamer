---
phase: 57-windows-audio-glitch-test-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md
autonomous: false
requirements: [WIN-03]
tags: [windows, audio, gstreamer, diagnostic, win11-vm, manual-uat, win-03]

user_setup:
  - service: windows-11-vm
    why: "WIN-03 fix shape (Option A re-apply property vs Option B explicit volume element) is data-driven from three readbacks per CONTEXT D-04. Sink identity, playbin3.volume persistence across NULL→PLAYING, and slider mid-stream effect cannot be observed without a real Win11 host running the conda-forge GStreamer 1.28.x bundle (Phase 43 spike rig). Linux mocks would beg the question."
    env_vars: []
    dashboard_config:
      - task: "Power on the existing Win11 VM (Phase 43 / 44 / 56-03 spike rig). Confirm Inno Setup 6.3+ + Miniforge env are available. Same VM used in Phase 56-03 (env named `spike` per 56-03 readiness table)."
        location: "Win11 VM"
      - task: "Have a fresh installer artifact available — either build via `./packaging/windows/build.ps1` (produces `dist/installer/MusicStreamer-2.1.57-win64-setup.exe`) or reuse an existing installer if pause/resume + volume code paths are unchanged from the build artifact's source (acceptable per the Phase 56-03 readiness precedent — `WIN-03` wiring is also unchanged from `main` until Plans 03/04 land)."
        location: "Win11 VM PowerShell"
      - task: "Have access to one playable HTTP stream (e.g., SomaFM Drone Zone) for the diagnostic — DI.fm premium not required for WIN-03 readbacks (different surface from Phase 56)."
        location: "Win11 VM (network)"

must_haves:
  truths:
    - "READBACK 1 captured: pipeline.get_property('audio-sink') returns the actual sink element name (e.g., wasapi2sink, directsoundsink) on the running install (D-04 step 1)"
    - "READBACK 2 captured: pipeline.get_property('volume') value before AND after a NULL→PLAYING cycle, with slider at 0.5 — disambiguates Option A (property resets to 1.0 → re-apply needed) vs Option B (property retains 0.5 but sink ignores it → explicit volume element needed) (D-04 step 2)"
    - "READBACK 3 captured: slider mid-stream effect (100% → 0% → 50%) — confirms whether write reaches the audio path at all (D-04 step 3)"
    - "DIAGNOSTIC ARTIFACT exists: 57-DIAGNOSTIC-LOG.md captures all three readbacks in the 56-03-DIAGNOSTIC-LOG.md format (header + pre-flight table + per-step PRE blocks + decision section) (D-05)"
    - "FIX-SHAPE DECISION recorded: Option A vs Option B vs hybrid, with rationale tying back to which readback's evidence drove it (D-06)"
    - "GLITCH HYPOTHESIS recorded: which sink ships and what smoothing approach composes with the chosen volume fix (Claude's discretion — input for Plan 57-04)"
  artifacts:
    - path: ".planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md"
      provides: "Three D-04 readbacks + D-06 fix-shape decision + glitch hypothesis"
      contains: "D-06 Fix-Shape Selection"
  key_links:
    - from: "57-DIAGNOSTIC-LOG.md::D-06 decision"
      to: "57-03-PLAN.md (volume fix)"
      via: "Option A vs Option B branch selector"
      pattern: "Option [AB]"
    - from: "57-DIAGNOSTIC-LOG.md::Readback 1 sink identity"
      to: "57-04-PLAN.md (glitch fix)"
      via: "Sink-mediated smoothing approach"
      pattern: "wasapi2sink|directsoundsink|autoaudiosink"
---

<objective>
Run a Win11 VM diagnostic session that captures the three D-04 readbacks (sink identity, `playbin3.volume` persistence across NULL→PLAYING, slider mid-stream effect) and writes them to `57-DIAGNOSTIC-LOG.md`. The output of this plan is a written decision: ship Option A (one-line re-apply) vs Option B (explicit `volume` element in `Gst.Bin` chained with EQ) vs hybrid for WIN-03 volume, plus a glitch-fix hypothesis tied to the actual sink identity.

Purpose: Per CONTEXT D-01, the Linux side of the volume wiring is correct (`set_volume` → `playbin3.volume` → slider connects via `now_playing_panel.py:330`); the Windows-only failure means the cause is environmental (sink-selection, property-loss-on-NULL, or sink-internal-volume drift), NOT a Linux code bug. Phase 56-03 set the diagnose-first cadence for Windows-only bugs with correct-looking Linux code; this plan applies the same cadence verbatim. No production code changes. Speculative refactoring is explicitly forbidden — Plans 57-03 and 57-04 branch on this log's evidence.

Output: `57-DIAGNOSTIC-LOG.md` next to `57-CONTEXT.md`, populated with three readback blocks + a D-06 fix-shape decision section. Plans 57-03 and 57-04 cannot execute until this log records a decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-PATTERNS.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md
@musicstreamer/player.py
@.claude/skills/spike-findings-musicstreamer/SKILL.md
@.claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md

<interfaces>
<!-- Reference points the diagnostic must inspect (READ-ONLY for this plan; production untouched) -->

<!-- musicstreamer/player.py:218-220 — set_volume signature -->
def set_volume(self, value: float) -> None:
    self._volume = max(0.0, min(1.0, value))
    self._pipeline.set_property("volume", self._volume)

<!-- musicstreamer/player.py:485-490 — _set_uri funnel (the NULL→PLAYING site) -->
def _set_uri(self, uri: str) -> None:
    uri = aa_normalize_stream_url(uri)  # WIN-01 / D-01 / D-03 invariant
    self._pipeline.set_state(Gst.State.NULL)
    self._pipeline.get_state(Gst.CLOCK_TIME_NONE)
    self._pipeline.set_property("uri", uri)
    self._pipeline.set_state(Gst.State.PLAYING)

<!-- musicstreamer/player.py:113-121 — Phase 47.2 audio-filter slot (single-element; EQ already occupies it) -->
self._eq = Gst.ElementFactory.make("equalizer-nbands", "eq")
if self._eq is not None:
    self._eq.set_property("num-bands", 10)
    self._pipeline.set_property("audio-filter", self._eq)

<!-- 56-03 reference structure — every section header in 57-DIAGNOSTIC-LOG.md mirrors a section in 56-03-DIAGNOSTIC-LOG.md (D-05 + PATTERNS.md "57-DIAGNOSTIC-LOG.md" block). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 57-DIAGNOSTIC-LOG.md skeleton (header + pre-flight table + section stubs)</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (entire file — copy section structure: header lines 1-9, pre-flight table lines 11-22, per-step PRE block lines 26-79, decision section lines 117-141)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-PATTERNS.md (the `57-DIAGNOSTIC-LOG.md` block at the bottom — has the EXACT section headers to use)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md (D-04 step list, D-05 artifact location, D-06 fix-shape options)
  </read_first>
  <action>
    Create `.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md` as a SKELETON with the literal section headers below, leaving the readback content for Task 2 / Task 3 / Task 4 to fill in. Headers and structure copy 56-03-DIAGNOSTIC-LOG.md verbatim with the labels swapped from `D-08 Step N` to `D-04 Step N` and the WIN-02 / SMTC subject swapped to WIN-03 / audio.

    Literal file contents (write exactly this skeleton; later tasks fill in the `_TBD_` placeholders):

    ```markdown
    # Phase 57 / WIN-03 — Win11 VM Audio Diagnostic Log

    **Started:** _TBD: yyyy-mm-dd_
    **Driver:** Linux orchestrator (interactive paste-back mode); user executes PowerShell + Python on Win11 VM
    **Goal:** Capture the three D-04 readbacks (sink identity + `playbin3.volume` persistence across NULL→PLAYING + slider mid-stream effect), so Plan 57-03 knows whether to ship Option A (re-apply property), Option B (explicit `volume` element chained with EQ inside a `Gst.Bin`), or a hybrid; and so Plan 57-04 picks a glitch-smoothing approach that composes with the chosen volume fix.

    ---

    ## Pre-flight: VM environment readiness

    | Check | Result | Notes |
    |-------|--------|-------|
    | Win11 22H2+ | _TBD_ | Same VM used in Phase 43 / 44 / 56-03 spike rig |
    | Conda env active | _TBD_ | env name (Phase 56-03 used `spike`) |
    | Fresh installer artifact | _TBD_ | path on VM, build date, byte size |
    | One playable HTTP stream available | _TBD_ | e.g., SomaFM Drone Zone (no DI.fm premium needed for WIN-03 — different surface from Phase 56) |
    | Two PowerShell windows ready | self-managed | one for app, one for python introspection |

    **Status:** _TBD: READY / BLOCKED_

    ---

    ## D-04 Step 1: Audio Sink Identity (PRE-FIX)

    **Method (interactive Python REPL on the VM, with the app launched and playing a stream — see Task 2 of Plan 57-02 for the exact snippet):**

    Read back the running pipeline's selected audio sink. Capture the GStreamer element factory name (e.g., `wasapi2sink`, `directsoundsink`, `autoaudiosink`).

    **Output:**
    ```
    _TBD: paste the readback verbatim_
    ```

    **Outcome classification:**
    - **wasapi2sink** — GStreamer 1.28.x default on Windows; honors `playbin3.volume` natively (per Phase 43 spike findings)
    - **directsoundsink** — legacy fallback; volume behavior known-quirky
    - **autoaudiosink** wrapping one of the above — same final element decides
    - **other** — flag for D-09-style follow-up

    **Implication for Plan 57-04 (glitch fix):**
    - _TBD: which sink → which smoothing wrapper shape (sink-volume mute window vs `playbin3.volume`-side fade vs `volume`-element ramp)._

    ---

    ## D-04 Step 2: `playbin3.volume` Persistence Across NULL→PLAYING (PRE-FIX)

    **Method:** Set volume to 0.5 via the slider. Read back `pipeline.get_property("volume")` (expected 0.5). Press Pause (NULL). Press Resume (rebuild → PLAYING). Read back `pipeline.get_property("volume")` again. Compare.

    **Output:**

    | Read time | `pipeline.get_property("volume")` | Audible level (subjective) |
    |-----------|-----------------------------------|----------------------------|
    | After slider → 0.5, before pause | _TBD_ | _TBD_ |
    | After Resume (post-rebuild) | _TBD_ | _TBD_ |

    **Outcome classification:**
    - **A — property resets to 1.0:** `playbin3.volume` is being lost on NULL→PLAYING rebuild. **Option A** wins (one-line re-apply at end of `_set_uri`).
    - **B — property stays 0.5 but audio is full-volume:** Sink ignores `playbin3.volume`. **Option B** wins (explicit `volume` GstElement chained with EQ in a `Gst.Bin`, occupying the single `audio-filter` slot).
    - **C — property stays 0.5 and audio is half-volume:** Bug not reproducible on this run. **Re-test** with a longer pause hold; if still not reproducible, escalate as "intermittent — defer".

    **Implication for D-06:**
    - _TBD: which classification fired._

    ---

    ## D-04 Step 3: Slider Mid-Stream Effect (PRE-FIX)

    **Method:** While playing, move the slider 100% → 0% → 50%. Confirm whether the audible level changes immediately at each move.

    **Output:**

    | Slider move | Audible response | `pipeline.get_property("volume")` |
    |-------------|------------------|----------------------------------|
    | 100% → 0% | _TBD: silent / unchanged / partial_ | _TBD_ |
    | 0% → 50% | _TBD: half-loud / silent / unchanged_ | _TBD_ |

    **Outcome classification:**
    - **Always responsive:** bug is "post-rebuild only" — Step 2 will be the deciding readback for D-06.
    - **Always unresponsive:** property writes never reach the audio path — strengthens Option B (the sink isn't honoring `playbin3.volume`).
    - **Partially responsive:** flag and follow up; rare.

    **Implication for D-06:**
    - _TBD: confirm or contradict the prior from Step 2._

    ---

    ## D-06 Fix-Shape Selection + Glitch Hypothesis

    **Decision:** _TBD: Option A / Option B / hybrid_

    **D-06 classification:** _TBD: which evidence path picked it_

    **Rationale (one sentence):** _TBD_

    **Cross-reference table:**

    | D-06 candidate | Hypothesis | Status | Evidence |
    |---------------|-----------|--------|----------|
    | Option A | `playbin3.volume` resets on NULL→PLAYING (Win-only) | _TBD: confirmed / ruled out_ | Step 2 readback |
    | Option B | Sink ignores `playbin3.volume` entirely | _TBD: confirmed / ruled out_ | Step 2 + Step 3 readbacks |
    | Hybrid | Both: re-apply AND own the element | _TBD: warranted? unlikely_ | (only if Step 2 = A and Step 3 = "always unresponsive") |

    **Glitch-fix hypothesis (Claude's discretion, sink-mediated):**

    Sink identified in Step 1 = _TBD_. Most likely smoothing approach: _TBD_.
    Approach must compose with the chosen volume fix without double-writing the volume property:
    - If Option A ships: smoothing wrapper writes to `playbin3.volume`.
    - If Option B ships: smoothing wrapper writes to the new `volume` GstElement (NOT to `playbin3.volume`, which would be unconnected).

    Plan 57-04 picks the exact ramp shape (Phase 52 EQ ramp template at `musicstreamer/player.py:160-163, 683-685, 746-786`).

    ---

    ## Sign-off

    - **Diagnostic complete:** _TBD: yyyy-mm-dd_
    - **D-06 decision recorded:** _TBD: Option A / Option B / hybrid_
    - **Plan 57-03 unblocked:** _TBD: yes / no_
    - **Plan 57-04 unblocked:** _TBD: yes / no_
    ```

    DO NOT fill in `_TBD_` slots in this task — Tasks 2/3/4 own that work after running on the VM.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md &amp;&amp; grep -q "Phase 57 / WIN-03" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md &amp;&amp; grep -c "## D-04 Step" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md | grep -q "^3$" &amp;&amp; grep -q "## D-06 Fix-Shape Selection" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md`
    - Contains "Phase 57 / WIN-03" header
    - Contains exactly 3 sections matching `## D-04 Step` (steps 1, 2, 3)
    - Contains "## D-06 Fix-Shape Selection" decision section
    - Contains "## Pre-flight" readiness table
    - All `_TBD_` placeholders are clearly marked (Task 4 turns them into real readbacks)
  </acceptance_criteria>
  <done>
    Skeleton file exists with header + pre-flight + 3 D-04 step sections + D-06 decision section + sign-off — all `_TBD_`-stubbed for fill-in by Task 4.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run D-04 readbacks on Win11 VM (interactive)</name>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md (skeleton from Task 1 — the placeholders show what to capture)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md (D-04 method for each step)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (interactive paste-back style — orchestrator hands user one PowerShell/Python snippet at a time, user pastes output back)
  </read_first>
  <files>none — interactive VM session; output is captured into chat for downstream tasks</files>
  <action>
    <!-- INTERACTIVE CHECKPOINT — human runs commands on Win11 VM and pastes output back. -->
    <!-- What was built before this checkpoint: -->
    Task 1 created the skeleton file. This task runs the three D-04 readbacks live on the Win11 VM. No code is written here — the deliverable is captured stdout that Task 4 will paste into the log.


    <!-- Steps the human follows: -->
    The user (Kyle) executes the following on the Win11 VM, paste-back style. Orchestrator/Claude provides one snippet at a time and waits for stdout. The exact snippets:

    **Pre-flight check** (run in PowerShell on the VM):

    ```powershell
    # Confirm OS, env, installer presence
    [System.Environment]::OSVersion.VersionString
    conda info --envs | Select-String "spike|musicstreamer"
    Get-ChildItem "$env:USERPROFILE\musicstreamer\dist\installer\*.exe" -ErrorAction SilentlyContinue | Select-Object Name, Length, LastWriteTime
    ```

    Expected: Win11 22H2+, env active (or equivalent), one or more `MusicStreamer-*-win64-setup.exe` artifacts present. Paste the stdout into the orchestrator chat for the pre-flight table.

    **Step 1 — Sink identity:**

    Launch the app via the Start Menu shortcut (NOT `python -m musicstreamer` per Phase 43.1 / 56-03 launch-discipline rule — though for THIS readback the launch path doesn't affect sink selection, the consistency matters for Plan 57-05 UAT). Start playing any HTTP stream (e.g., SomaFM Drone Zone). Then in a SECOND PowerShell window:

    ```powershell
    # Attach to the running MusicStreamer python process and read the sink.
    # Run from the repo root in the conda env that built the installer.
    cd $env:USERPROFILE\musicstreamer
    conda activate spike  # or whatever env name was active in Phase 56-03 — adjust if different
    python -c "
    import sys, time, os
    # Find the running MusicStreamer pid:
    import subprocess
    out = subprocess.check_output(['powershell', '-Command', 'Get-Process MusicStreamer | Select-Object -ExpandProperty Id'], text=True).strip()
    print('MusicStreamer pid(s):', out)
    "
    ```

    The above prints the PID. Then to read the sink, the most reliable approach is to add a TEMPORARY one-shot diagnostic print directly inside the running app via a debug REPL — but since attaching to a GStreamer pipeline in a foreign process is non-trivial, use this self-contained variant instead:

    ```powershell
    # Self-contained: build a minimal playbin3 in this Python process,
    # set state to PLAYING with the same conda env GStreamer plugins,
    # and read the sink. This mirrors what the running app sees because
    # both processes share the conda-forge gstreamer env.
    python -c "
    import gi
    gi.require_version('Gst', '1.0')
    from gi.repository import Gst
    Gst.init(None)
    p = Gst.ElementFactory.make('playbin3', 'p')
    p.set_property('uri', 'http://ice1.somafm.com/dronezone-128-mp3')
    p.set_state(Gst.State.PLAYING)
    p.get_state(Gst.CLOCK_TIME_NONE)
    sink = p.get_property('audio-sink')
    print('audio-sink object:', sink)
    print('audio-sink factory name:', sink.get_factory().get_name() if sink and sink.get_factory() else 'unknown')
    p.set_state(Gst.State.NULL)
    "
    ```

    Paste full stdout. Capture the `factory name` line (e.g., `wasapi2sink`).

    **Step 2 — `playbin3.volume` persistence across NULL→PLAYING:**

    In the SAME Python REPL session as Step 1 (or a fresh one — re-create `p` and `Gst.init` as above):

    ```powershell
    python -c "
    import gi
    gi.require_version('Gst', '1.0')
    from gi.repository import Gst
    Gst.init(None)
    p = Gst.ElementFactory.make('playbin3', 'p')
    p.set_property('uri', 'http://ice1.somafm.com/dronezone-128-mp3')
    p.set_state(Gst.State.PLAYING)
    p.get_state(Gst.CLOCK_TIME_NONE)

    # Step 2a: set volume to 0.5, read back
    p.set_property('volume', 0.5)
    print('after set 0.5, volume =', p.get_property('volume'))

    # Simulate the pause/resume rebuild that Player._set_uri does
    p.set_state(Gst.State.NULL)
    p.get_state(Gst.CLOCK_TIME_NONE)
    p.set_property('uri', 'http://ice1.somafm.com/dronezone-128-mp3')
    p.set_state(Gst.State.PLAYING)
    p.get_state(Gst.CLOCK_TIME_NONE)

    # Step 2b: read volume after the rebuild
    print('after NULL->PLAYING rebuild, volume =', p.get_property('volume'))

    p.set_state(Gst.State.NULL)
    "
    ```

    Paste full stdout. Critical lines:
    - `after set 0.5, volume = ...`
    - `after NULL->PLAYING rebuild, volume = ...`

    Also note (subjectively, by ear during the second PLAYING state): is the audio level half or full?

    **Step 3 — Slider mid-stream:**

    Switch to the actual MusicStreamer app (still running from Step 1's launch — if it crashed during Step 2's parallel session, relaunch via the Start Menu shortcut and start a stream). With audio playing:

    1. Move the volume slider to 100%. Listen.
    2. Move the volume slider to 0%. Listen.
    3. Move the volume slider to 50%. Listen.

    Report verbally / in the chat:
    - "100%: full volume" / "100%: silent" / "100%: half"
    - "0%: silent" / "0%: full volume" / "0%: half"
    - "50%: half" / "50%: silent" / "50%: full"

    Optionally, if a Python REPL is still attached to the running app's pipeline (advanced — skip if not feasible), read `pipeline.get_property("volume")` at each slider position to corroborate.
  </action>
  <verify>
    <automated>echo 'Interactive VM checkpoint — see action block for human-run commands; this verify gate is human-attested via the resume signal.'</automated>
  </verify>
  <done>
    Human pastes back required output and signals completion.
    Paste back the stdout for the pre-flight check, Step 1, Step 2, and the verbal Step 3 outcomes. Type "diagnostic captured" when all three are in the chat. Orchestrator will then dispatch Task 3 (analyst classification) and Task 4 (write to log).
  </done>
</task>

<task type="auto">
  <name>Task 3: Classify outcomes against D-06 cross-reference table</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md (D-06 Option A vs Option B criteria; Claude's Discretion section on glitch fix)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-PATTERNS.md (Option A literal patch shape at line 80; Option B `Gst.Bin` chaining caveat at line 113; Phase 52 EQ ramp template lines 160-186)
    - the Task 2 paste-back outputs from the chat (the orchestrator will have the readback strings)
  </read_first>
  <action>
    Apply the D-06 cross-reference table to the Task 2 readbacks. Reasoning steps (no file edits yet — Task 4 commits the analysis to the log):

    1. **Step 1 sink classification.** Map the factory name to the D-04 step 1 outcome categories. Most-expected case: `wasapi2sink` (1.28.x default on Windows). If anything else, note it for Plan 57-04's glitch-fix shape.

    2. **Step 2 NULL→PLAYING readback classification.** Compare the two `volume =` lines:
       - If `0.5 → 1.0` (or 0.5 → anything ≠ 0.5): **Outcome A** — property is reset on rebuild → Option A wins.
       - If `0.5 → 0.5` AND user reported "audio still full-volume after the rebuild": **Outcome B** — sink ignores property → Option B wins.
       - If `0.5 → 0.5` AND user reported "audio went half-volume": **Outcome C** — bug didn't reproduce on this run. Document, escalate ("re-test on a longer hold" or "defer if intermittent").

    3. **Step 3 slider classification.** Compare to Step 2:
       - If Step 2 = Outcome A AND Step 3 = "always responsive": consistent with Option A (post-rebuild only bug).
       - If Step 2 = Outcome B AND Step 3 = "always unresponsive": consistent with Option B (property never reaches sink).
       - Other combinations: flag as anomalous, document, may indicate hybrid.

    4. **Glitch-fix hypothesis.** Combine Step 1 sink identity with the chosen Option:
       - **wasapi2sink + Option A**: Smoothing wrapper writes to `playbin3.volume` (single property surface). Phase 52 EQ ramp template applies — QTimer-driven 8-tick fade from current → 0 → NULL/PLAYING bracket → 0 → current.
       - **wasapi2sink + Option B**: Smoothing wrapper writes to the new `volume` GstElement. Same Phase 52 ramp shape, just a different write target.
       - **directsoundsink + either**: Legacy sink — note Phase 43 spike findings if available; otherwise default to the same ramp shape with a longer interval.
       - **autoaudiosink wrapping X**: treat the wrapped sink as the deciding factor.

    5. Compose the rationale sentence: "Step 2 readback shows X→Y, classifying as Outcome [A|B|C]; sink is `<sink>`; therefore Plan 57-03 ships Option [A|B|hybrid] and Plan 57-04 wraps `<target>` with a Phase 52-style ramp."

    6. Plan 57-04 will read this hypothesis as input but is free to refine it (Claude's discretion per CONTEXT). The analysis here just sets the prior.

    NO file edits in this task — Task 4 commits the analysis. The orchestrator carries the analyst output forward as inline reasoning.
  </action>
  <verify>
    <automated>echo "Task 3 is reasoning-only; verification gate is the Task 4 acceptance check that the log has a non-TBD D-06 decision line."</automated>
  </verify>
  <acceptance_criteria>
    - Outcome classification (A / B / C) chosen for Step 2 with explicit citation of the readback values.
    - Step 3 slider behavior cross-referenced against Step 2 — consistent / inconsistent flagged.
    - D-06 decision line composed: "Option A" or "Option B" or "hybrid" or (rare) "C — defer / re-test".
    - Glitch-fix hypothesis composed: sink identity + smoothing target + Phase 52 ramp template citation.
  </acceptance_criteria>
  <done>
    A complete D-06 fix-shape decision (one of: Option A / Option B / hybrid / defer) and glitch-fix hypothesis exist as orchestrator-side reasoning, ready to be written to disk by Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fill in 57-DIAGNOSTIC-LOG.md with all readbacks + decision + hypothesis</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md (the skeleton from Task 1 — replace `_TBD_` placeholders, do not restructure)
    - Task 2 paste-back outputs (live in chat)
    - Task 3 analyst classification (live in chat)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (reference for the prose tone in the "Implication" and "Verdict" lines — terse, evidence-citing)
  </read_first>
  <action>
    Replace every `_TBD_` placeholder in `.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md` with concrete values from Tasks 2 + 3.

    Specifically:
    - **Header `Started:` line** — set to today's date (yyyy-mm-dd).
    - **Pre-flight table** — populate from the pre-flight check output (OS version, conda env, installer artifact path/size/date, stream name, etc.).
    - **D-04 Step 1 Output block** — paste the Step 1 stdout verbatim (factory name + sink object). If the readback came from a self-contained REPL rather than the running app, note that in the "Method" line.
    - **D-04 Step 1 Implication for Plan 57-04** — write the smoothing-wrapper hypothesis from Task 3.
    - **D-04 Step 2 Output table** — fill the `_TBD_` cells with the volume readback values from Task 2 + the subjective audible-level observation.
    - **D-04 Step 2 Implication for D-06** — write "Outcome A confirmed" / "Outcome B confirmed" / "Outcome C — bug did not reproduce" with one-line evidence citation.
    - **D-04 Step 3 Output table** — fill the `_TBD_` cells with the verbal slider observations from Task 2.
    - **D-04 Step 3 Implication for D-06** — write the consistency check from Task 3 step 3.
    - **D-06 Fix-Shape Selection section**:
      - `Decision:` set to Option A / Option B / hybrid / defer (per Task 3 step 5).
      - `D-06 classification:` cite which step's readback drove the call.
      - `Rationale:` one-sentence prose from Task 3 step 5.
      - `Cross-reference table:` mark each candidate ✅ confirmed / ❌ ruled out / ⚠️ hybrid-warranted with one-line evidence each.
      - `Glitch-fix hypothesis:` paste Task 3 step 4 output (sink + smoothing target + Phase 52 template citation).
    - **Sign-off section**:
      - `Diagnostic complete:` today's date.
      - `D-06 decision recorded:` Option A / Option B / hybrid / defer.
      - `Plan 57-03 unblocked:` yes (unless decision is "defer" — in that case, mark as no and note the deferral).
      - `Plan 57-04 unblocked:` yes (Plan 57-04 needs both the volume decision and the sink identity to choose its smoothing target).

    Do NOT add new sections or change existing headers. The log is a structured artifact; downstream plans grep specific phrases (`grep -q "Decision:" 57-DIAGNOSTIC-LOG.md` and `grep -q "Option [AB]" 57-DIAGNOSTIC-LOG.md`).

    Keep the prose tone terse and evidence-cited (mirror 56-03-DIAGNOSTIC-LOG.md).
  </action>
  <verify>
    <automated>! grep -q "_TBD_" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md &amp;&amp; grep -E -q "Decision:\s*(Option A|Option B|hybrid|defer)" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md &amp;&amp; grep -q "Plan 57-03 unblocked:" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md &amp;&amp; grep -q "Plan 57-04 unblocked:" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md &amp;&amp; grep -E -q "Glitch-fix hypothesis" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "_TBD_" 57-DIAGNOSTIC-LOG.md` returns 1 (no remaining placeholders).
    - `grep -E "Decision:\s*(Option A|Option B|hybrid|defer)"` matches exactly once.
    - `grep "Plan 57-03 unblocked:"` and `grep "Plan 57-04 unblocked:"` both match.
    - `grep "Glitch-fix hypothesis"` matches.
    - `grep "audio-sink factory name:"` (or equivalent step 1 readback evidence) appears in the Step 1 output block.
    - `grep -E "after set 0.5, volume = "` and `grep -E "after NULL->PLAYING rebuild, volume = "` both appear in the Step 2 output block (or equivalent prose if the diagnostic was run differently).
  </acceptance_criteria>
  <done>
    `57-DIAGNOSTIC-LOG.md` is fully populated. Plan 57-03 and Plan 57-04 can read it as input. The phase's diagnose-first half is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Win11 VM ↔ Linux orchestrator | The user pastes verbatim stdout from the VM into the chat. Orchestrator treats the paste as user-attested data. No automated execution of VM-side code from the orchestrator side this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Information Disclosure | 57-DIAGNOSTIC-LOG.md | accept | Log records GStreamer property values + sink identity. No PII, no credentials, no DI.fm premium key (unlike Phase 56-05). Public stream URL (SomaFM) is the only network reference. |
| T-57-02-02 | Tampering | Diagnostic readback values | accept | Plans 57-03 and 57-04 grep for "Option A" / "Option B" decision. A future contributor manually editing the log to flip the decision is a workflow-discipline issue, not a security issue, and is caught by the existence of the readback evidence (a flipped decision without changing the readbacks would be visibly inconsistent). |
| T-57-02-03 | Denial of Service | Win11 VM Python REPL | accept | The diagnostic snippets create transient `playbin3` pipelines that play public radio. They tear down on REPL exit. No persistence, no external resource consumption beyond network bandwidth for the stream. |

**Phase-level note:** Diagnostic plan reads back GStreamer property values on a VM — no production data, no PII, no new external boundary. No high-severity threats.
</threat_model>

<verification>
- `test -f .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md` exits 0
- `! grep -q "_TBD_" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md` (no placeholders remain)
- `grep -E "Decision:\s*(Option A|Option B|hybrid|defer)" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md` matches
- `grep "Glitch-fix hypothesis" .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md` matches
- `git diff musicstreamer/` is empty (no production code change this plan)
- `git diff tests/` is empty (no test code change this plan)
</verification>

<success_criteria>
- All three D-04 readbacks recorded with verbatim stdout in 57-DIAGNOSTIC-LOG.md.
- D-06 decision (Option A / Option B / hybrid / defer) recorded with rationale tied to readback evidence.
- Glitch-fix hypothesis recorded as input for Plan 57-04 (sink identity + smoothing target + Phase 52 template citation).
- Plans 57-03 and 57-04 are unblocked (or, in the rare "defer" case, are explicitly blocked with a deferral note).
- No production code or test code edited.
</success_criteria>

<output>
After completion, create `.planning/phases/57-windows-audio-glitch-test-fix/57-02-SUMMARY.md` documenting:
- The D-06 decision (Option A / Option B / hybrid / defer) — the headline.
- Sink identity from Step 1.
- One-sentence rationale tying the decision to the Step 2 readback values.
- Glitch-fix hypothesis (target + Phase 52 template).
- Confirmation that 57-DIAGNOSTIC-LOG.md is committed and 57-03 / 57-04 are unblocked.
- Note for Plan 57-03: which Option-block of the dual-option action to follow.
- Note for Plan 57-04: which smoothing target to write to (avoid double-write with Plan 57-03's volume fix).
</output>
