---
phase: 57-windows-audio-glitch-test-fix
plan: 05
type: execute
wave: 4
depends_on: ["57-01", "57-03", "57-04"]
files_modified:
  - .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md
autonomous: false
requirements: [WIN-03, WIN-04]
tags: [uat, win11-vm, linux-ci, audio, pause-resume, volume-slider, async-mock, smtc-thumbnail]

user_setup:
  - service: windows-11-vm
    why: "Final perceptual UAT for WIN-03 (ROADMAP SC #1 no audible pop on pause/resume; ROADMAP SC #2 volume slider takes effect on Windows). Both criteria are perceptual — Linux CI structural guards (Plans 57-03 and 57-04) cannot judge audibility."
    env_vars: []
    dashboard_config:
      - task: "Power on the existing Win11 VM (Phase 43 / 44 / 56-03 / 57-02 spike rig). Same VM as 57-02; conda env `spike` activated."
        location: "Win11 VM"
      - task: "Build the freshly-rebased installer with Plans 57-03 + 57-04 baked in: ./packaging/windows/build.ps1 (produces dist/installer/MusicStreamer-2.1.57-win64-setup.exe). The installer MUST contain the bus-message STATE_CHANGED handler (Plan 57-03) AND the pause-volume ramp (Plan 57-04). Verify by checking the build host's git log includes both commits before running the build."
        location: "Linux build host"
      - task: "Have one playable HTTP stream available (e.g., SomaFM Drone Zone http://ice1.somafm.com/dronezone-128-mp3) — same canonical stream used in 57-02 diagnostic. DI.fm premium not required for WIN-03 UAT."
        location: "Win11 VM (network)"
      - task: "Force-uninstall any prior MusicStreamer install per Phase 56-05 D-08 step 4 procedure: Remove-Item %LOCALAPPDATA%\\Programs\\MusicStreamer; Remove-Item %APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\MusicStreamer.lnk. Test-Path on both must return False before installing the new build."
        location: "Win11 VM PowerShell"

must_haves:
  truths:
    - "ROADMAP SC #1 verified on Win11 VM: pressing Pause then Resume on a playing SomaFM stream produces NO audible pop, gap, or restart artifact (perceptual; user attestation captured in 57-05-UAT-LOG.md)"
    - "ROADMAP SC #2 verified on Win11 VM: moving the volume slider mid-stream (100% -> 50% -> 0% -> 50% sweep) changes the audible playback volume immediately AND a pause/resume cycle preserves the slider's last value post-resume — matches Linux behavior (cross-platform per D-11)"
    - "ROADMAP SC #3 verified on Linux CI: tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream passes — referenced via 57-01-SUMMARY.md (already shipped 2026-05-02; no re-run required for this UAT, only an attestation that 57-01 closure is preserved on the rebased branch)"
    - "ROADMAP SC #4 verified on Linux CI: full pytest suite produces no NEW failures vs. base — the 10 pre-existing failures called out in 57-01-SUMMARY.md remain (mpris2, station_list_panel, twitch_auth) but are not introduced by Phase 57"
    - "57-05-UAT-LOG.md exists with PASS/FAIL attestations for all 4 ROADMAP success criteria + screenshots/transcripts for the perceptual SCs (#1 and #2)"
  artifacts:
    - path: ".planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md"
      provides: "Final UAT attestations for WIN-03 (SC #1 + #2 perceptual on VM) and WIN-04 (SC #3 + #4 referenced from 57-01 + Linux suite re-run)"
      contains: "## SC #1: Pause/Resume audible glitch"
  key_links:
    - from: "57-05 UAT outcome"
      to: "phase 57 completion gate"
      via: "All 4 ROADMAP SCs attested -> /gsd-verify-work green-lights phase complete"
      pattern: "SC #1.*PASS|SC #2.*PASS|SC #3.*PASS|SC #4.*PASS"
    - from: "57-05-UAT-LOG.md SC #3"
      to: "57-01-SUMMARY.md"
      via: "WIN-04 already shipped 2026-05-02 — UAT references the existing CI evidence rather than re-running"
      pattern: "57-01-SUMMARY.md"
---

<objective>
Final UAT for Phase 57 covering all four ROADMAP success criteria. Two halves:

1. **WIN-03 (perceptual, Win11 VM):** SC #1 (no audible pop on pause/resume) and SC #2 (volume slider takes effect immediately AND survives pause/resume). Both require human ear / hand on the running app — Linux CI structural guards (Plans 57-03 + 57-04) cannot judge audibility.

2. **WIN-04 (CI, Linux):** SC #3 (test_thumbnail_from_in_memory_stream passes) and SC #4 (full suite no new failures). SC #3 is ALREADY SHIPPED via Plan 57-01 (commit `c1c783c`, 2026-05-02 — see 57-01-SUMMARY.md); this UAT only re-attests on the rebased branch (which now also contains Plans 57-03 + 57-04) that the AsyncMock fix is preserved and the pre-existing 10 failures haven't grown.

Per `<rescope_constraints>` constraint #5, this plan's `requirements` field includes BOTH WIN-03 AND WIN-04 because the UAT verifies all 4 ROADMAP SCs across both requirement IDs. SC #3 attestation references 57-01's existing CI evidence rather than re-shipping the fix.

Purpose: closes Phase 57. Without these attestations, neither WIN-03 nor WIN-04 can be considered shipped on the rebased branch — the automated tests in Plans 57-03 and 57-04 prove the structural invariants on Linux CI but cannot prove the real Win11 wasapi2sink produces no audible pop or that the slider takes effect mid-stream.
Output: `.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` with PASS/FAIL attestations + observations for all 4 ROADMAP SCs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-01-SUMMARY.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-02-SUMMARY.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-03-PLAN.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-04-PLAN.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-PLAN.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md
@.claude/skills/spike-findings-musicstreamer/SKILL.md
@.claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md

<interfaces>
ROADMAP success criteria the UAT attests against (all 4 must PASS):

- SC #1 (WIN-03 / perceptual / VM): Pressing Pause and then Resume on a playing stream on Windows produces no audible pop, gap, or restart artifact.
- SC #2 (WIN-03 / perceptual / VM): Moving the volume slider on Windows changes the playback volume immediately (matches Linux behavior), AND the slider's value is preserved across pause/resume.
- SC #3 (WIN-04 / CI / Linux): test_thumbnail_from_in_memory_stream passes — store_async is awaited correctly via AsyncMock, no MagicMock not awaitable error.
- SC #4 (WIN-04 / CI / Linux): Full test suite passes with no new failures on the fix branch.

Phase 56-05 UAT log format precedent: each SC has a header section with `**Status:** PASS / FAIL`, attestation text, screenshots/transcripts, and a verbatim observation block. Mirror that shape exactly. See `.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` for the literal template.

WIN-04 attestation shortcut: 57-01-SUMMARY.md already documents that `test_thumbnail_from_in_memory_stream` passes (commit `c1c783c`) and that the full pytest suite has 10 pre-existing failures unrelated to Phase 57. SC #3 attestation here is "57-01's fix is preserved on the post-rebase branch" + a fresh `pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` run. SC #4 attestation is a full `pytest -x` (or `pytest --tb=no -q`) run vs. the pre-existing-failure list from 57-01-SUMMARY.md.

Win11 VM UAT cadence (mirrors Phase 56 D-11 + Phase 57-02 task 2):
1. Pre-UAT: build host pytest -x is GREEN (only the documented 10 pre-existing failures); commit hashes for Plans 57-01 + 57-03 + 57-04 are present on the rebased branch; build.ps1 produces fresh installer; force-uninstall on VM; install fresh.
2. SC #1 perceptual gate: Start a stream, hit Pause, wait 2s, hit Play. Listen for pop. If no pop -> PASS. If pop -> FAIL with audio recording attached.
3. SC #2 perceptual gate part a: Start a stream at 100%. Sweep slider 100% -> 50% -> 0% -> 50% mid-stream. Each move should produce immediate audible level change. PASS / FAIL.
4. SC #2 perceptual gate part b: Set slider to 0.5. Hit Pause. Wait 2s. Hit Play. Audible level on resume should match the slider position (half-volume), NOT 100%. PASS / FAIL.

Linux CI cadence (SC #3 + #4):
1. Re-run `pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` on the rebased branch — exits 0.
2. Run `pytest --tb=no -q` on the rebased branch; capture failure list. Diff against the pre-existing failure list from 57-01-SUMMARY.md "Issues Encountered" table. New failures = FAIL; same list = PASS.

Pre-existing failure list (carry-forward from 57-01-SUMMARY.md):
- tests/test_media_keys_mpris2.py (7 tests)
- tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode
- tests/test_station_list_panel.py::test_refresh_recent_updates_list
- tests/test_twitch_auth.py::test_play_twitch_sets_plugin_option_when_token_present

Total: 10 pre-existing. Phase 57 must not GROW this list.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-UAT — branch readiness + Linux CI re-attestation (SC #3 + #4)</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-01-SUMMARY.md (carry-forward 10 pre-existing failures table; AsyncMock fix shipped 2026-05-02; commit c1c783c)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-03-PLAN.md (verify gates + acceptance criteria — confirm bus-message handler tests pass)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-04-PLAN.md (verify gates + acceptance criteria — confirm pause-ramp tests pass)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md (UAT log template — copy header + per-SC section structure)
  </read_first>
  <what-built>
    Pre-UAT verification that the rebased fix branch contains all three Phase 57 commits (Plans 57-01, 57-03, 57-04), that Linux CI is green, and that the SC #3 + SC #4 attestations have been recorded into 57-05-UAT-LOG.md (since SC #3 + #4 are CI-attestable, not perceptual).
  </what-built>
  <how-to-verify>
    On the Linux build host:

    1. Confirm rebased branch state:
       - `git log --oneline -20` shows commits for Plans 57-01 (test fix), 57-03 (bus-message handler + 4 tests), 57-04 (pause-volume ramp + 3 tests).
       - `grep -c "message::state-changed" musicstreamer/player.py` returns 1 (Plan 57-03 acceptance gate).
       - `grep -c "_pause_volume_ramp_timer" musicstreamer/player.py` returns >= 5 (Plan 57-04 acceptance gate).
       - `grep -q "DataWriter.return_value.store_async = AsyncMock" tests/test_media_keys_smtc.py` exits 0 (Plan 57-01 acceptance gate).

    2. Re-attest SC #3 (WIN-04 store_async test passes):
       - `cd /home/kcreasey/OneDrive/Projects/MusicStreamer && PYTHONPATH=. uv run pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` exits 0.
       - Capture exit code + last 5 lines of output for the UAT log.

    3. Re-attest SC #4 (WIN-04 full suite no new failures):
       - `cd /home/kcreasey/OneDrive/Projects/MusicStreamer && PYTHONPATH=. uv run pytest --tb=no -q 2>&1 | tail -50` — capture the failure list.
       - Diff against the 10 pre-existing failures from 57-01-SUMMARY.md "Issues Encountered" table. Confirm exactly the same set; no new failures introduced by Plans 57-03 or 57-04.
       - Capture failure count + per-file breakdown for the UAT log.

    4. Create the SKELETON of `.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` with the following sections (mirror 56-05-UAT-LOG.md structure):
       - Header: `# Phase 57 UAT Log` + Started date + VM/host info.
       - `## Pre-UAT readiness` table: commit hash, grep gate results, build status.
       - `## SC #3: WIN-04 AsyncMock store_async test passes` — fill in PASS attestation with the rerun output (already-passing-gate referenced from 57-01-SUMMARY.md).
       - `## SC #4: Linux full suite no new failures` — fill in PASS attestation with the failure list diff (must equal the 10 pre-existing).
       - `## SC #1: WIN-03 pause/resume audible glitch (Win11 VM)` — placeholder marked PENDING (Tasks 2-3 fill in).
       - `## SC #2: WIN-03 volume slider takes effect (Win11 VM)` — placeholder marked PENDING (Tasks 2-3 fill in).
       - `## Phase 57 readiness summary` — placeholder marked PENDING (Task 4 fills in).

    5. Linux build host runs `./packaging/windows/build.ps1` (assumes the build host has the PowerShell environment from the Phase 43 spike rig OR runs the equivalent build via the Windows VM's `build.ps1` directly — Kyle has done this before for 57-02). Confirm `dist/installer/MusicStreamer-2.1.57-win64-setup.exe` is fresh (mtime newer than the latest 57-03 / 57-04 commit). The build artifact MUST contain Plans 57-03 + 57-04 commits.

    6. Transfer the fresh installer to the Win11 VM. Force-uninstall any prior install:
       ```powershell
       Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Programs\MusicStreamer" -ErrorAction SilentlyContinue
       Remove-Item -Force "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk" -ErrorAction SilentlyContinue
       Test-Path "$env:LOCALAPPDATA\Programs\MusicStreamer"  # must print False
       Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk"  # must print False
       ```

    7. Run the freshly-built installer on the VM. Leave the post-install Run checkbox UNCHECKED.

    Pre-UAT readiness check confirmation: `Get-StartApps | Where-Object Name -like 'MusicStreamer*'` shows AppID = `org.lightningjim.MusicStreamer`.
  </how-to-verify>
  <resume-signal>
    Reply "READY" with: (a) commit hash of the rebased branch HEAD, (b) confirmation that all three Plan acceptance grep gates pass, (c) the 4 captured pieces of evidence: SC #3 pytest exit code, SC #4 failure list count + diff verdict, build artifact mtime, post-install Get-StartApps AppID. The UAT log skeleton at `57-05-UAT-LOG.md` should already have SC #3 and SC #4 sections marked PASS with attestation text.
    OR reply "BLOCKED: <reason>" if any of the gates fail.
  </resume-signal>
  <action>
    Linux build host: verify the three Plan acceptance grep gates (above), run the targeted SC #3 pytest, run the full pytest suite for SC #4, diff against 57-01-SUMMARY.md's pre-existing failure list, run `./packaging/windows/build.ps1`. Create 57-05-UAT-LOG.md with sections for all 4 SCs; fill in SC #3 and SC #4 with PASS attestations + captured output + failure diff. Mark SC #1 and SC #2 as PENDING. Transfer fresh installer to VM. Win11 VM: force-uninstall + install fresh + leave Run checkbox unchecked + confirm AppID via Get-StartApps. The UAT log file MUST exist at task completion (skeleton + SC #3 / #4 attested).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && PYTHONPATH=. uv run pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x 2>&1 | tail -5 && grep -c "message::state-changed" musicstreamer/player.py | grep -q '^1$' && grep -c "_pause_volume_ramp_timer" musicstreamer/player.py | grep -q '^[5-9]$\|^[1-9][0-9]$' && grep -q "DataWriter.return_value.store_async = AsyncMock" tests/test_media_keys_smtc.py && test -f .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/kcreasey/OneDrive/Projects/MusicStreamer && PYTHONPATH=. uv run pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` exits 0 (SC #3 re-attestation; preserves Plan 57-01).
    - `grep -c "message::state-changed" musicstreamer/player.py` returns 1 (Plan 57-03 acceptance gate carried forward).
    - `grep -c "_pause_volume_ramp_timer" musicstreamer/player.py` returns >= 5 (Plan 57-04 acceptance gate carried forward).
    - `grep -q "DataWriter.return_value.store_async = AsyncMock" tests/test_media_keys_smtc.py` exits 0 (Plan 57-01 acceptance gate carried forward).
    - `test -f .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0 (UAT log skeleton exists).
    - `grep -q "## SC #3" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
    - `grep -q "## SC #4" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
    - `grep -q "PASS" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0 (at least SC #3 + SC #4 marked PASS).
    - `grep -q "PENDING" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0 (SC #1 + SC #2 still pending VM verification).
    - Pre-existing failure count: SC #4 attestation in the UAT log explicitly states "10 pre-existing failures, no new ones introduced by Phase 57" with the per-file breakdown matching 57-01-SUMMARY.md.
    - Win11 VM is in a clean state ready for SC #1 + SC #2 perceptual UAT (Task 2).
  </acceptance_criteria>
  <done>
    All 4 grep gates pass on the rebased branch. SC #3 and SC #4 attested PASS in 57-05-UAT-LOG.md with the captured pytest output and failure-list diff. Fresh build artifact transferred to VM and installed cleanly. Get-StartApps confirms AppID. SC #1 and SC #2 sections in the UAT log marked PENDING for Tasks 2-3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC #1 UAT — Pause/Resume audible glitch on Windows</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md (D-11 cross-platform scope; D-15 ramp template — verifies the smoothing wrapper landed)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-04-PLAN.md (the structural-guard tests confirm the ramp wrapper is wired; this UAT is the perceptual gate that the wrapper actually masks the pop)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md (Step 1 sink = wasapi2sink — same sink the UAT exercises)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md (perceptual attestation format — copy verbatim shape: status badge + steps + observation block + screenshots if applicable)
  </read_first>
  <what-built>
    WIN-03 audible-glitch fix (Plan 57-04 pause-volume ramp wrapper composed with Plan 57-03's bus-message re-apply). This UAT verifies the ROADMAP SC #1 attestation: pause/resume produces no audible pop on Windows.
  </what-built>
  <how-to-verify>
    On the Win11 VM, with the freshly-installed app launched via Start Menu shortcut (post-Task-1 state):

    1. In the station list, navigate to a SomaFM HTTP stream (e.g., Drone Zone — `http://ice1.somafm.com/dronezone-128-mp3`). DI.fm is not required (and DI.fm uses HTTP fallback per Phase 56 anyway, which is unrelated to the WIN-03 surface).

    2. Click Play. Wait for audible audio + ICY title display (~3-10s buffer).

    3. **Test 1 — Single pause/resume cycle:**
       - Press Pause. Listen carefully for ~1s. Should hear: a smooth fade-out across ~40ms (the Plan 57-04 ramp), then silence.
       - Wait 2 seconds.
       - Press Play. Listen carefully for the resume. Should hear: the audio coming back at the slider's volume (no pop, no click, no jarring restart artifact). Plan 57-04's ramp made the fade-out smooth; Plan 57-03's bus-message handler re-applies self._volume on PLAYING-arrival so resume audio comes back at the right level.
       - Attestation: PASS = no audible pop / gap / restart artifact. FAIL = pop heard.

    4. **Test 2 — Rapid pause/resume:**
       - Press Pause. Immediately press Play (within ~500ms). This stresses the composition: the pause-volume ramp may still be in flight when Play fires. The ramp is supposed to complete to NULL (D-15) then the new play() call rebuilds the pipeline.
       - Listen for any audible glitch. PASS = clean rapid cycle. FAIL = pop, freeze, or stuck-at-half-volume.

    5. **Test 3 — Pause/resume across stream switch:**
       - Press Pause on the current SomaFM stream.
       - In the station list, click a DIFFERENT station (e.g., another SomaFM channel). This triggers play() with a new station while the previous pause may still be settling.
       - Listen for any audible glitch on the new station start. PASS = clean transition. FAIL = pop or stuck-at-half-volume.

    6. **Negative-side check (composition with Plan 57-03):** While playing, observe whether the audible volume "snaps" momentarily on each play attempt — Plan 57-03's re-apply happens on PLAYING-arrival; if the bus-message handler fires too aggressively (e.g., re-applies on every internal element transition rather than top-level) the audible volume could jitter. PASS = stable volume during steady-state playback. FAIL = audible jitter without slider movement.

    7. Append the SC #1 attestation to `57-05-UAT-LOG.md` under `## SC #1: WIN-03 pause/resume audible glitch (Win11 VM)`. Mark `**Status:** PASS` if all three tests pass and the negative check is clean. Mark `**Status:** FAIL` and capture an audio recording (Win+G or any screen recorder with audio) if any test fails. Include verbatim observations for each test.

    User attestation: state explicitly whether each of Test 1, Test 2, Test 3, and the negative check passed. If any fail, the SC #1 status is FAIL and Phase 57 cannot ship.
  </how-to-verify>
  <resume-signal>
    Reply "SC #1 PASS" with verbatim observations for Tests 1-3 + negative check (e.g., "Test 1: smooth fade-out, no pop on resume. Test 2: rapid cycle clean. Test 3: stream switch clean. Negative check: stable volume.") OR "SC #1 FAIL: <observation> (recording attached at <path>)".
  </resume-signal>
  <action>VM: Run the three pause/resume tests + negative check above. Append SC #1 attestation to 57-05-UAT-LOG.md with PASS/FAIL status, per-test observations, and screen-recording attachment if FAIL.</action>
  <verify>
    <automated>grep -q "## SC #1" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md && (grep -A 3 "## SC #1" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -E "PASS|FAIL")</automated>
  </verify>
  <acceptance_criteria>
    - SC #1 section exists in 57-05-UAT-LOG.md.
    - SC #1 status is explicitly PASS or FAIL (not PENDING) — `grep -A 3 "## SC #1" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -E "PASS|FAIL"` returns at least one match.
    - All three test scenarios + negative check are documented in the UAT log with verbatim observations.
    - If FAIL: a screen recording with audio is attached or referenced from the UAT log.
  </acceptance_criteria>
  <done>
    SC #1 is attested PASS or FAIL in 57-05-UAT-LOG.md with full observation set. If PASS, proceed to Task 3. If FAIL, the phase is BLOCKED — return to Plan 57-04 to refine the ramp shape (cycle count, tick interval, or ramp curve) and re-run this UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC #2 UAT — Volume slider takes effect on Windows + survives pause/resume</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md (D-11 cross-platform scope; D-12 hook site; D-13 single mechanism Option A; D-14 regression scope)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-DIAGNOSTIC-LOG.md (Step 2 readback `0.5 -> 1.0` reset across NULL->PLAYING — this UAT verifies the fix masks that reset; Step 3 — slider always responsive mid-stream baseline)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-03-PLAN.md (the bus-message handler re-applies self._volume on PLAYING-arrival; this UAT is the perceptual gate that the re-apply is audible)
  </read_first>
  <what-built>
    WIN-03 volume-slider fix (Plan 57-03 bus-message STATE_CHANGED handler re-applying self._volume on every PLAYING transition). This UAT verifies ROADMAP SC #2 attestation: slider takes effect immediately AND survives pause/resume on Windows (parity with Linux).
  </what-built>
  <how-to-verify>
    On the Win11 VM, with the freshly-installed app launched and a SomaFM stream playing (post-Task-2 state OR start a fresh stream):

    1. **Test 1 — Mid-stream slider sweep (D-04 Step 3 baseline + post-fix verification):**
       - Set volume slider to 100%. Confirm audible at full level.
       - Move slider to 50%. Confirm audible level drops to ~half.
       - Move slider to 0%. Confirm silence.
       - Move slider back to 50%. Confirm audible level returns to ~half.
       - Attestation: PASS = each move produces an immediate audible level change (no lag, no stuck-at-old-value). FAIL = any slider position fails to take effect.
       - This was already PASS-confirmed in 57-02 diagnostic Step 3 PRE-fix; this re-test confirms the fix did not BREAK steady-state behavior.

    2. **Test 2 — Slider survives pause/resume (the WIN-03 D-12 + D-15 composition gate):**
       - Set slider to 50% (so half-volume audible).
       - Press Pause. Wait 2s.
       - Press Play. Wait for audible audio (~3-10s buffer).
       - Listen carefully: the resumed audio should be at HALF volume (matching the slider's 50%), NOT 100%. The slider position itself should remain at 50% throughout (visually unchanged).
       - PASS = audible level on resume matches slider at 50%. FAIL = audible level jumps to 100% (the pre-fix bug).
       - This is the literal user-visible bug closure: the diagnostic Step 2 readback `0.5 -> 1.0` is invisible to users; what users see is "I set the slider to half, paused, resumed, and now it's loud". Plan 57-03's bus-message handler closes this surface.

    3. **Test 3 — Slider survives buffer-drop auto-rebuffer (the in-session disclosure surface from 57-02):**
       - Set slider to 30% (low audible volume).
       - Wait for the playback to encounter a natural buffer drop (or simulate by briefly disabling the VM's network adapter for ~5 seconds, then re-enabling — playbin3 will auto-pause when the buffer drops below threshold, then auto-resume when refilled). Note this is the GStreamer-internal PAUSED->PLAYING path that Plan 57-03's bus-message handler covers (which would otherwise miss it if the hook had been at the tail of `_set_uri` per the original D-06 Option A).
       - When playback resumes, listen: audible level should be at 30% (matching the slider), NOT 100%.
       - PASS = audible level on auto-resume matches slider at 30%. FAIL = audible level jumps to 100% on every buffer-recovery.
       - If reproducing the buffer-drop is unreliable on the VM's network, this test can be marked SKIPPED with rationale "buffer-drop not reliably reproducible on VM Wi-Fi during UAT window — Test 1 and Test 2 cover the explicit user-visible cases". Test 3 is best-effort.

    4. **Test 4 — Slider survives station switch:**
       - Set slider to 75%.
       - Click a different SomaFM station in the list.
       - When the new station starts playing (~3-10s), listen: audible level should be at 75%, not 100%.
       - PASS = audible level on station switch matches slider at 75%. FAIL = audible level jumps to 100%.

    5. Append SC #2 attestation to 57-05-UAT-LOG.md under `## SC #2: WIN-03 volume slider takes effect (Win11 VM)`. Mark `**Status:** PASS` if all four tests pass (Test 3 may be SKIPPED). Mark `**Status:** FAIL` if any required test fails. Include verbatim observations for each test.

    User attestation: state explicitly whether each of Test 1, Test 2, Test 3 (or SKIPPED), Test 4 passed. If any required test fails, the SC #2 status is FAIL and Phase 57 cannot ship.
  </how-to-verify>
  <resume-signal>
    Reply "SC #2 PASS" with verbatim observations for Tests 1-4 (or "Test 3: SKIPPED, rationale: ...") OR "SC #2 FAIL: <observation> (recording attached at <path>)".
  </resume-signal>
  <action>VM: Run the four slider tests above. Append SC #2 attestation to 57-05-UAT-LOG.md with PASS/FAIL/SKIPPED-Test-3 status, per-test observations, and screen-recording attachment if FAIL.</action>
  <verify>
    <automated>grep -q "## SC #2" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md && (grep -A 3 "## SC #2" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -E "PASS|FAIL")</automated>
  </verify>
  <acceptance_criteria>
    - SC #2 section exists in 57-05-UAT-LOG.md.
    - SC #2 status is explicitly PASS or FAIL (not PENDING) — `grep -A 3 "## SC #2" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -E "PASS|FAIL"` returns at least one match.
    - All four test scenarios are documented in the UAT log with verbatim observations (Test 3 may be SKIPPED with rationale).
    - If FAIL: a screen recording with audio is attached or referenced from the UAT log.
  </acceptance_criteria>
  <done>
    SC #2 is attested PASS or FAIL in 57-05-UAT-LOG.md with full observation set. If PASS, proceed to Task 4. If FAIL, the phase is BLOCKED — return to Plan 57-03 to refine the bus-message handler (e.g., a missed transition path) or to Plan 57-04 to refine the composition with the ramp wrapper, and re-run this UAT.
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase 57 closure summary in 57-05-UAT-LOG.md</name>
  <files>.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md (post-Tasks-1-2-3 — all 4 SCs filled in)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md (closure-summary section format precedent — see "Phase 56 readiness summary" or equivalent at the end of that file)
    - .planning/ROADMAP.md (Phase 57 entry; success criteria)
  </read_first>
  <action>
    Append a final `## Phase 57 readiness summary` section to `.planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` with:

    1. A summary table mapping each ROADMAP SC to its attestation status:

       | SC | Requirement | Verification | Status | Evidence |
       |----|-------------|--------------|--------|----------|
       | #1 | WIN-03 | Win11 VM perceptual | PASS / FAIL | Task 2 attestation |
       | #2 | WIN-03 | Win11 VM perceptual | PASS / FAIL | Task 3 attestation |
       | #3 | WIN-04 | Linux CI pytest | PASS | 57-01-SUMMARY.md (commit c1c783c) + Task 1 re-attestation |
       | #4 | WIN-04 | Linux CI pytest | PASS | Task 1 attestation (10 pre-existing failures unchanged) |

    2. A "Phase 57 ships if" checklist:
       - [ ] All 4 SCs are PASS.
       - [ ] No new failures introduced by Plans 57-03 or 57-04 (Task 1 SC #4 attestation).
       - [ ] No regression of Plan 57-01's WIN-04 fix (Task 1 SC #3 attestation).

    3. A "Composition contract verified" subsection confirming D-12 + D-15:
       - Plan 57-03's bus-message re-apply fires on every PLAYING transition (NULL->PLAYING + PAUSED->PLAYING).
       - Plan 57-04's pause-volume ramp wraps the pause()->NULL transition without double-writing playbin3.volume.
       - Composition observed perceptually: pause is a smooth fade, resume comes back at slider position (no half-volume tail, no full-volume jump).

    4. A "Pre-existing failures carry-forward" subsection listing the 10 failures from 57-01-SUMMARY.md and explicitly stating "Phase 57 does NOT close any of these; they are tracked separately and remain out of scope."

    5. A "Next steps" line: "Phase 57 closed — Phase 58 already shipped (PLS auto-resolve); next active phase is TBD per ROADMAP.md." Or, if any SC is FAIL: "Phase 57 BLOCKED on SC #N FAIL — see Task <N> for re-plan trigger."

    Sign and date the section: `*Phase 57 UAT complete: YYYY-MM-DD*`.
  </action>
  <verify>
    <automated>grep -q "## Phase 57 readiness summary" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md && grep -q "Composition contract verified" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md && grep -q "Pre-existing failures carry-forward" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md && grep -q "Phase 57 UAT complete:" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Phase 57 readiness summary" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
    - `grep -q "Composition contract verified" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
    - `grep -q "Pre-existing failures carry-forward" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
    - `grep -q "Phase 57 UAT complete:" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
    - The summary table maps all 4 ROADMAP SCs to their PASS/FAIL status with attestation references.
    - The "Phase 57 ships if" checklist is present and all checkboxes are checked (if all SCs are PASS) — if any is FAIL, the corresponding checkbox is unchecked and the "Next steps" line names the re-plan trigger.
  </acceptance_criteria>
  <done>
    Phase 57 readiness summary added to UAT log. All 4 ROADMAP SCs mapped to PASS/FAIL. Composition contract attested. Pre-existing failures carry-forward documented. The UAT log is the canonical artifact for phase closure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host -> Win11 VM | Installer artifact transferred from Linux build host to Win11 VM. Risk: stale or wrong-branch installer. Mitigation: Task 1 grep gates verify all three Plan acceptance gates pass on the rebased branch BEFORE the build runs. |
| Human attestation -> UAT log | Perceptual SCs (#1, #2) rely on human ear / hand. Risk: missed audible glitch or biased attestation. Mitigation: explicit per-test observation block in the UAT log (no single PASS/FAIL summary without verbatim per-test text); FAIL requires a screen recording with audio. |
| 57-01 carry-forward attestation | SC #3 attestation references 57-01-SUMMARY.md rather than re-running. Risk: rebase silently broke 57-01's AsyncMock fix. Mitigation: Task 1 re-runs the targeted test on the rebased branch (`pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x`) and includes the exit code in the attestation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05-01 | Tampering | Stale installer (missing Plans 57-03 / 57-04 commits) is shipped to the VM, leading to false PASS attestations because the new code paths aren't running | mitigate | Task 1 acceptance gate `grep -c "message::state-changed" musicstreamer/player.py` returns 1 + `grep -c "_pause_volume_ramp_timer" musicstreamer/player.py` >= 5 are checked on the LINUX BUILD HOST source tree (not the installed VM binary). The source-tree gates verify the build input is correct; build.ps1 packages from that source tree. The "fresh installer mtime newer than 57-04 commit" check in Task 1 step 5 is the integrity link. |
| T-57-05-02 | Repudiation | Perceptual UAT attestation is subjective and could be inconsistent across re-runs | accept | Single-developer UAT (Kyle); the same ear runs all four SC re-tests. If a re-run is needed (e.g., after a Plan 57-04 ramp-shape tweak), the same person re-attests. PASS/FAIL has explicit per-test observation criteria (Test 1: "smooth fade-out, no pop"; Test 3: "audible level matches slider at 30%") that reduce subjective drift. |
| T-57-05-03 | Information Disclosure | Screen recordings attached to FAIL attestations capture VM screen + audio, potentially showing other VM windows | accept | Single-user VM, no PII or credentials displayed in the streaming UI. Recordings are local files on the build host; not committed to the repo. |
| T-57-05-04 | Denial of Service | VM Wi-Fi instability during Test 3 (buffer-drop reproduction) blocks the SC #2 attestation | mitigate | Test 3 is explicitly marked "best-effort, can be SKIPPED with rationale". Tests 1, 2, 4 are required and cover the user-visible cases. SKIPPED Test 3 + PASS Tests 1/2/4 = SC #2 PASS. |
| T-57-05-05 | Information Disclosure | UAT log captures the canonical SomaFM stream URL — public-facing, non-secret | accept | http://ice1.somafm.com/dronezone-128-mp3 is publicly known; same URL used in 57-02 diagnostic. |
</threat_model>

<verification>
- `test -f .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0 (UAT log exists).
- `grep -c "## SC #" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` returns 4 (one section per ROADMAP SC).
- `grep -q "## SC #1.*PASS\|## SC #1.*FAIL" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md || grep -B 0 -A 10 "## SC #1" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -qE "PASS|FAIL"` (status explicitly PASS or FAIL, not PENDING).
- `grep -B 0 -A 10 "## SC #2" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -qE "PASS|FAIL"`.
- `grep -B 0 -A 5 "## SC #3" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -q "PASS"` (SC #3 attested PASS via 57-01 carry-forward).
- `grep -B 0 -A 5 "## SC #4" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md | grep -q "PASS"` (SC #4 attested PASS — 10 pre-existing failures unchanged).
- `grep -q "## Phase 57 readiness summary" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
- `grep -q "Composition contract verified" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
- `grep -q "Phase 57 UAT complete:" .planning/phases/57-windows-audio-glitch-test-fix/57-05-UAT-LOG.md` exits 0.
- `cd /home/kcreasey/OneDrive/Projects/MusicStreamer && PYTHONPATH=. uv run pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` exits 0 (SC #3 re-attestation; Plan 57-01 preserved).
</verification>

<success_criteria>
- All four ROADMAP SCs (#1, #2, #3, #4) attested PASS in 57-05-UAT-LOG.md.
- SC #1 (no audible pop on pause/resume): perceptual VM attestation with three test scenarios + negative check.
- SC #2 (volume slider takes effect on Windows): perceptual VM attestation with four test scenarios (mid-stream sweep + pause/resume preserve + buffer-drop survival + station switch).
- SC #3 (test_thumbnail_from_in_memory_stream passes): Linux CI re-attestation referencing 57-01's existing fix (commit c1c783c, 2026-05-02) — already shipped, preserved on rebased branch.
- SC #4 (full suite no new failures): Linux CI attestation that the 10 pre-existing failures from 57-01-SUMMARY.md are unchanged; no new failures introduced by Plans 57-03 or 57-04.
- Composition contract D-12 + D-15 verified perceptually: pause is a smooth fade, resume comes back at slider position.
- D-13 invariant carried through the UAT: no `_volume_element` reference anywhere; the single write surface is `playbin3.volume`.
- Phase 57 readiness summary section in 57-05-UAT-LOG.md is the canonical closure artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/57-windows-audio-glitch-test-fix/57-05-SUMMARY.md` capturing:
- Final UAT verdict for each of the 4 ROADMAP SCs.
- Reference to 57-05-UAT-LOG.md as the canonical attestation artifact.
- Pre-existing failures carry-forward from 57-01-SUMMARY.md (no new failures introduced).
- Cross-reference to Plan 57-01's WIN-04 closure (already shipped 2026-05-02; SC #3 verified preserved on rebased branch).
- Cross-reference to Plan 57-03's bus-message STATE_CHANGED handler (D-12 + D-13 + D-14).
- Cross-reference to Plan 57-04's pause-volume ramp wrapper (D-15) + composition contract verification.
- A "Phase 57 closure" line: "ROADMAP Phase 57 closed YYYY-MM-DD — WIN-03 + WIN-04 both shipped; ROADMAP entry can be marked [x] complete."
</output>
