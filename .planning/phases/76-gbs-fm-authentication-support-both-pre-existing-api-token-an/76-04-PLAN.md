---
phase: 76
plan: 04
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - tests/test_accounts_dialog.py
autonomous: true
requirements:
  - GBS-AUTH-01

must_haves:
  truths:
    - "`TestAccountsDialogGBS` class is extended with tests covering: 2-state status enumeration, secondary import button, subprocess launch, finished-handler success/failure/invalid-Netscape, disconnect-Yes/No."
    - "Tests use the existing `_mock_proc_with_stderr` helper at `tests/test_accounts_dialog.py:67` and mirror the `TestAccountsDialogOAuthFinished` test shape at lines 196-251."
    - "Tests are RED before Plan 76-03 merges, GREEN after."
    - "Existing tests in `TestAccountsDialogGBS` that asserted the old `\"Import GBS.FM Cookies...\"` text or the old CookieImportDialog-on-connect behavior are migrated, NOT left as broken legacy tests."
  artifacts:
    - path: "tests/test_accounts_dialog.py"
      provides: "RED → GREEN test contract for Plan 76-03 deliverables (GBS subprocess flow)"
      contains: "class TestAccountsDialogGBS"
  key_links:
    - from: "tests/test_accounts_dialog.py::TestAccountsDialogGBS"
      to: "musicstreamer.ui_qt.accounts_dialog.AccountsDialog GBS methods (Plan 76-03 output)"
      via: "MagicMock spec=QProcess + _mock_proc_with_stderr helper"
      pattern: "_mock_proc_with_stderr"
---

<objective>
Extend the existing `TestAccountsDialogGBS` class in `tests/test_accounts_dialog.py` with RED test stubs covering every behavior contract Plan 76-03 ships. Migrate any existing tests in `TestAccountsDialogGBS` that assert against the OLD GBS behavior (e.g. assertions on `"Import GBS.FM Cookies..."` button text, or the CookieImportDialog-on-connect path) so the file is consistent with Plan 76-03's new surface.

Purpose: Wave 2 contract pins for the AccountsDialog GBS subprocess wiring. Mirror the existing `TestAccountsDialogOAuthFinished` test shape verbatim so the test architecture is consistent across providers.

Output: An extended `tests/test_accounts_dialog.py::TestAccountsDialogGBS` class with ≥ 9 new tests + the migration of any pre-existing tests that hardcoded the old GBS connect-branch behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-CONTEXT.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md

# Mirror sources:
@tests/test_accounts_dialog.py
@musicstreamer/ui_qt/accounts_dialog.py

# Plan 76-03 produces what these tests assert against:
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-03-SUMMARY.md
</context>

<interfaces>
<!-- Verbatim shapes the new tests mirror, cited from the actual test file. -->

Existing test helpers / classes (verified via grep):

- `tests/test_accounts_dialog.py:67` — `def _mock_proc_with_stderr(stderr_bytes: bytes = b"", stdout_bytes: bytes = b"")` — the shared mock helper. Plan 76-04 reuses for GBS finished-handler tests.
- `tests/test_accounts_dialog.py:86` — `class TestAccountsDialogStatus` — existing status tests; do NOT modify directly but the GBS-specific status checks live in `TestAccountsDialogGBS`.
- `tests/test_accounts_dialog.py:155` — `class TestAccountsDialogConnect` — the Twitch subprocess launch tests; mirror shape for GBS.
- `tests/test_accounts_dialog.py:196` — `class TestAccountsDialogOAuthFinished` — the Twitch finished-handler tests; mirror shape for GBS. Plan 76-04 reuses `_mock_proc_with_stderr` + the `QProcess.ExitStatus.NormalExit` exit-status enum.
- `tests/test_accounts_dialog.py:886` — `class TestAccountsDialogGBS` — Plan 76-04 EXTENDS this class.

Test infrastructure conventions in this file (mirror these):

- Tests construct `AccountsDialog` via `_make_dialog(qtbot, ...)` fixture (verify pattern in `TestAccountsDialogStatus` setUp). If the existing class uses a different fixture, use whichever pattern `TestAccountsDialogGBS` already uses.
- `MagicMock(spec=QProcess)` for finished-handler tests; `_mock_proc_with_stderr` for stderr-bytes + stdout-bytes wiring.
- Bound-method assertions: `assert dialog._gbs_import_btn.clicked.receivers() ...` is NOT idiomatic; instead test the BEHAVIOR (click triggers handler) via `qtbot.mouseClick` or direct `dialog._on_gbs_import_clicked()` invocation.
- For dialog-modal tests (`QMessageBox.question` mocks): `monkeypatch.setattr(QMessageBox, "question", lambda *a, **k: QMessageBox.StandardButton.Yes)` — mirror existing pattern at any of the disconnect tests in the file.
- For `CookieImportDialog` invocation tests: `monkeypatch.setattr(<module>, "CookieImportDialog", MagicMock())` then assert `MagicMock.call_args.kwargs` matches the GBS parameterization.

Plan 76-03 surface that this plan asserts against:

- `AccountsDialog._gbs_login_proc: QProcess | None` — attribute initialized to None in `__init__`.
- `AccountsDialog._gbs_import_btn: QPushButton` — text `"Import cookies file…"`, visible only when not connected.
- `AccountsDialog._on_gbs_action_clicked()` — connect branch calls `_launch_gbs_login_subprocess`; disconnect branch deletes cookies file on Yes.
- `AccountsDialog._on_gbs_import_clicked()` — opens `CookieImportDialog` with `target_label="GBS.FM"`, `cookies_path=paths.gbs_cookies_path`, `validator=gbs_api._validate_gbs_cookies`, `oauth_mode=None`.
- `AccountsDialog._launch_gbs_login_subprocess()` — `QProcess.start(sys.executable, ["-m", "musicstreamer.oauth_helper", "--mode", "gbs"])`.
- `AccountsDialog._on_gbs_login_finished(exit_code, exit_status)` — on success: validates Netscape, writes cookies file with 0o600, logs `provider="gbs"`, toasts, updates status. On failure: delegates to `_classify_and_show_failure(provider="gbs", ...)`.

Valid Netscape fixture for the success-path test (use this verbatim):

```text
# Netscape HTTP Cookie File
.gbs.fm\tTRUE\t/\tTRUE\t1799999999\tsessionid\ttest_sessionid_value
.gbs.fm\tTRUE\t/\tFALSE\t1799999999\tcsrftoken\ttest_csrftoken_value
```

(Tab-delimited per Netscape format. Fields: domain, includeSubdomains, path, secure, expiry, name, value. Existing `_validate_gbs_cookies` accepts this shape — it requires sessionid + csrftoken + gbs.fm domain match. Plan 76-02 Task 2's `test_gbs_flush_produces_valid_netscape` round-trip already exercises this validator.)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate existing GBS tests to new contract + add 2-state status enumeration tests</name>
  <files>tests/test_accounts_dialog.py</files>
  <read_first>
    - tests/test_accounts_dialog.py:886-1020 (the existing `TestAccountsDialogGBS` class — read in full to identify which existing tests need migration)
    - musicstreamer/ui_qt/accounts_dialog.py (Plan 76-03's output — `_gbs_import_btn`, `_update_status` GBS branch, button text changes)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md (§File 4 - tests/test_accounts_dialog.py — mapping to TestAccountsDialogOAuthFinished and existing TestAccountsDialogGBS)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§Test Strategy Group B lines 782-795 — RED test list)
  </read_first>
  <behavior>
    - Existing tests in `TestAccountsDialogGBS` that asserted `_gbs_action_btn.text() == "Import GBS.FM Cookies..."` are migrated to assert `"Connect to GBS.FM…"` (U+2026).
    - Existing tests that asserted `_on_gbs_action_clicked` opens `CookieImportDialog` are migrated to assert it now calls `_launch_gbs_login_subprocess()`.
    - New test: `test_gbs_status_shows_connected_when_cookies_present` — fixture creates a temp gbs-cookies.txt file (or monkeypatches `_is_gbs_connected` to return True); call `dialog._update_status()`; assert `dialog._gbs_status_label.text() == "Connected"` (or `"Connected (cookies)"` per Plan 76-03's discretion choice — accept either), `dialog._gbs_action_btn.text() == "Disconnect"`, `dialog._gbs_import_btn.isVisible() == False`.
    - New test: `test_gbs_status_shows_not_connected_when_no_cookies` — monkeypatch `_is_gbs_connected` to return False; call `_update_status`; assert label=`"Not connected"`, button text=`"Connect to GBS.FM…"`, import button visible.
    - New test: `test_gbs_import_button_exists` — `dialog._gbs_import_btn` is a QPushButton with text `"Import cookies file…"` (U+2026), bound to `_on_gbs_import_clicked`.
  </behavior>
  <action>
    Two-part task:

    **Part A: Migrate existing tests.** Identify each test in `TestAccountsDialogGBS` (currently lines 886-1020) that asserts against the OLD GBS connect-branch behavior. For each:

    - If the test asserts `_gbs_action_btn.text() == "Import GBS.FM Cookies..."` → migrate to assert `"Connect to GBS.FM…"`.
    - If the test asserts the connect-click opens `CookieImportDialog` directly → migrate to assert it now calls `_launch_gbs_login_subprocess()`. The CookieImportDialog flow MOVES to a separate `test_gbs_import_button_opens_cookieimportdialog` test (Task 2).
    - If the test was asserting against a NotConnected state's button text being `"Import GBS.FM Cookies..."` → update the expected text to `"Connect to GBS.FM…"`.

    Document each migrated test in the commit message with `# Migrated for Phase 76 D-03 collapse` comments above the affected test functions. Per project memory `feedback_mirror_decisions_cite_source.md`: name the original test purpose and cite the line range BEFORE the migration so the audit trail is clear.

    **Part B: Add 3 new tests** to `TestAccountsDialogGBS`:

    1. `test_gbs_status_shows_connected_when_cookies_present` — mock `_is_gbs_connected` to return True; call `_update_status`; assert label = `"Connected"` (OR `"Connected (cookies)"` — Plan 76-03 picks one; accept both with `dialog._gbs_status_label.text() in {"Connected", "Connected (cookies)"}`); assert primary button text = `"Disconnect"`; assert import button hidden (`isVisible() == False`).
    2. `test_gbs_status_shows_not_connected_when_no_cookies` — mock `_is_gbs_connected` to return False; call `_update_status`; assert label = `"Not connected"`; primary button = `"Connect to GBS.FM…"`; import button visible (`isVisible() == True`).
    3. `test_gbs_import_button_exists_with_correct_text_and_handler` — assert `dialog._gbs_import_btn` is a `QPushButton` instance, text contains `"Import cookies file"`, contains U+2026 ellipsis (single character — NOT three-dot variant).

    Use the existing class's setup pattern (whatever fixture/setUp `TestAccountsDialogGBS` already employs). Do NOT introduce new fixture machinery in this task.
  </action>
  <acceptance_criteria>
    - Source assertion: existing GBS tests no longer assert the old text — `grep -A 3 "def test_" tests/test_accounts_dialog.py | grep -c "Import GBS.FM Cookies..."` returns 0 inside test bodies (note: some pre-existing tests may have used this; ALL must be migrated).
    - Source assertion: NEW tests `test_gbs_status_shows_connected_when_cookies_present`, `test_gbs_status_shows_not_connected_when_no_cookies`, `test_gbs_import_button_exists_with_correct_text_and_handler` exist — `grep -c "def test_gbs_status_shows_connected\|def test_gbs_status_shows_not_connected\|def test_gbs_import_button_exists" tests/test_accounts_dialog.py` returns 3.
    - Source assertion: at least one test asserts `"Connect to GBS.FM…"` — `grep -c "Connect to GBS.FM…" tests/test_accounts_dialog.py` returns ≥ 1.
    - Source assertion: at least one test asserts `_gbs_import_btn.isVisible` — `grep -c "_gbs_import_btn\.isVisible\|_gbs_import_btn.*[vV]isible" tests/test_accounts_dialog.py` returns ≥ 2 (one for each branch).
    - Behavior (RED before 76-03 merges): collection still succeeds — `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS --collect-only -q` exits 0.
    - Behavior (GREEN after 76-03 merges): `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS -x -q -k "status_shows or import_button_exists"` exits 0.
    - Behavior: tests outside the migrated set still pass — `python -m pytest tests/test_accounts_dialog.py -x -q --deselect tests/test_accounts_dialog.py::TestAccountsDialogGBS` exits 0 (no cross-class regression).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS --collect-only -q</automated>
  </verify>
  <done>Existing OLD-GBS-behavior tests migrated to assert against new text/behavior; 3 new status-and-import-button tests added; existing non-GBS tests unaffected; commit message documents each migrated test with line range citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add subprocess-launch + disconnect-flow + import-button-opens-dialog tests</name>
  <files>tests/test_accounts_dialog.py</files>
  <read_first>
    - tests/test_accounts_dialog.py:155-195 (existing `TestAccountsDialogConnect` — the Twitch subprocess-launch test shape Plan 76-04 mirrors for GBS)
    - tests/test_accounts_dialog.py:110-154 (existing `TestAccountsDialogDisconnect` — Twitch disconnect-Yes/No pattern Plan 76-04 mirrors for GBS)
    - tests/test_accounts_dialog.py:67 (the shared `_mock_proc_with_stderr` helper)
    - musicstreamer/ui_qt/accounts_dialog.py (Plan 76-03's `_launch_gbs_login_subprocess` + `_on_gbs_action_clicked` + `_on_gbs_import_clicked`)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§Test Strategy Group B lines 782-795 — `test_gbs_action_launches_subprocess_when_not_connected`, `test_gbs_disconnect_clears_cookies_with_yes`, `test_gbs_disconnect_no_op_with_no`, `test_gbs_import_button_opens_cookieimportdialog`)
  </read_first>
  <behavior>
    - `test_gbs_action_launches_subprocess_when_not_connected` — mock `_is_gbs_connected` to return False; mock `QProcess` to a MagicMock that captures `.start(...)` calls. Trigger `dialog._on_gbs_action_clicked()`. Assert exactly one `QProcess.start` call with `sys.executable` as first arg and `["-m", "musicstreamer.oauth_helper", "--mode", "gbs"]` as the args list. Mirror the existing Twitch launch test shape.
    - `test_gbs_disconnect_clears_cookies_with_yes` — mock `_is_gbs_connected` to return True; monkeypatch `QMessageBox.question` to return `Yes`; monkeypatch `os.remove` to a MagicMock. Trigger `_on_gbs_action_clicked()`. Assert `os.remove` called exactly once with `paths.gbs_cookies_path()`; assert `_update_status` was triggered (label flipped to `"Not connected"` after mock state changes — or simply assert `_update_status` mock was called).
    - `test_gbs_disconnect_no_op_with_no` — mock `_is_gbs_connected` to return True; monkeypatch `QMessageBox.question` to return `No`; monkeypatch `os.remove` to a MagicMock. Trigger `_on_gbs_action_clicked()`. Assert `os.remove` NOT called. Assert NO toast fired (no state change).
    - `test_gbs_disconnect_tolerates_oserror` — Phase 60 HIGH 2 regression guard. Mock `_is_gbs_connected` True; `QMessageBox.question` Yes; `os.remove` raises `PermissionError` (a subclass of `OSError`). Trigger `_on_gbs_action_clicked()`. Assert no exception propagates out of the handler; assert `_update_status` still called.
    - `test_gbs_import_button_opens_cookieimportdialog` — monkeypatch the `CookieImportDialog` import target to a MagicMock. Trigger `dialog._on_gbs_import_clicked()`. Assert exactly one call to the mock; assert call kwargs include `target_label="GBS.FM"`, `cookies_path=paths.gbs_cookies_path`, `validator=gbs_api._validate_gbs_cookies`, `oauth_mode=None`. Assert `dlg.exec()` was called on the constructed instance.
  </behavior>
  <action>
    Add 5 new tests to `TestAccountsDialogGBS` per RESEARCH §Test Strategy Group B lines 782-795. Per project memory `feedback_mirror_decisions_cite_source.md`: each test that mirrors a Twitch counterpart adds a `# Mirror: tests/test_accounts_dialog.py:LL-LL` comment above the function definition naming the source.

    Implementation guidance:

    1. **`test_gbs_action_launches_subprocess_when_not_connected`** (mirror `TestAccountsDialogConnect` Twitch shape — typically uses `monkeypatch.setattr(<module>, "QProcess", MagicMock())` then triggers the click handler and inspects `MagicMock.return_value.start.call_args`). Argv list MUST be exactly `["-m", "musicstreamer.oauth_helper", "--mode", "gbs"]`.

    2. **`test_gbs_disconnect_clears_cookies_with_yes`** (mirror `TestAccountsDialogDisconnect` shape). Use `monkeypatch.setattr(<paths_or_os_module>, "remove", MagicMock())` to capture the call; use `monkeypatch.setattr(QMessageBox, "question", lambda *a, **k: QMessageBox.StandardButton.Yes)`.

    3. **`test_gbs_disconnect_no_op_with_no`** — same scaffold as #2 but `lambda *a, **k: QMessageBox.StandardButton.No`. Assert `os.remove` NOT called.

    4. **`test_gbs_disconnect_tolerates_oserror`** — `os.remove` mock with `side_effect = PermissionError("read-only fs")`. Wrap the handler call in `try ... except OSError: pytest.fail("OSError should be swallowed")` OR simply call it and assert no exception escapes. Assert subsequent `_update_status` still fires.

    5. **`test_gbs_import_button_opens_cookieimportdialog`** — monkeypatch `musicstreamer.ui_qt.accounts_dialog.CookieImportDialog` (or whatever the import path resolves to inside `_on_gbs_import_clicked`'s deferred import — likely `musicstreamer.ui_qt.cookie_import_dialog.CookieImportDialog` at the module level; use `monkeypatch.setattr` accordingly). Inspect `MagicMock.call_args.kwargs` for the four required kwargs (`target_label`, `cookies_path`, `validator`, `oauth_mode`). Note: deferred imports mean the test must patch at the target module BEFORE the handler runs.

    All 5 tests defer `from musicstreamer.ui_qt.accounts_dialog import AccountsDialog` to inside the function body (mirror Plan 76-02 discipline).

    Do NOT add tests that exercise the real QProcess subprocess (live oauth_helper invocation); those are manual UAT (Plan 76-05).
  </action>
  <acceptance_criteria>
    - Source assertion: 5 new test names exist in the file — `grep -c "def test_gbs_action_launches_subprocess\|def test_gbs_disconnect_clears_cookies\|def test_gbs_disconnect_no_op\|def test_gbs_disconnect_tolerates_oserror\|def test_gbs_import_button_opens_cookieimportdialog" tests/test_accounts_dialog.py` returns 5.
    - Source assertion: subprocess argv check — `grep -A 25 "def test_gbs_action_launches_subprocess" tests/test_accounts_dialog.py | grep -cE "\"--mode\".*\"gbs\""` returns ≥ 1.
    - Source assertion: disconnect-Yes path includes `os.remove` assertion — `grep -A 25 "def test_gbs_disconnect_clears_cookies_with_yes" tests/test_accounts_dialog.py | grep -cE "os\.remove|remove\.call"` returns ≥ 1.
    - Source assertion: disconnect-No path asserts NO remove — `grep -A 25 "def test_gbs_disconnect_no_op_with_no" tests/test_accounts_dialog.py | grep -cE "not_called|0|assert.*not"` returns ≥ 1.
    - Source assertion: OSError tolerance test uses `PermissionError` or `OSError` — `grep -A 25 "def test_gbs_disconnect_tolerates_oserror" tests/test_accounts_dialog.py | grep -cE "PermissionError|OSError"` returns ≥ 1.
    - Source assertion: import-button test asserts the 4 kwargs — `grep -A 25 "def test_gbs_import_button_opens_cookieimportdialog" tests/test_accounts_dialog.py | grep -cE "target_label.*GBS|cookies_path|validator|oauth_mode"` returns ≥ 3.
    - Behavior (RED before 76-03 fully merges): collection succeeds — `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS --collect-only -q` exits 0.
    - Behavior (GREEN after 76-03 merges): `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS -x -q -k "launches_subprocess or disconnect or import_button_opens"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS --collect-only -q</automated>
  </verify>
  <done>5 new subprocess + disconnect + import-button tests added with Mirror-cite comments; collection succeeds standalone; tests GREEN after Plan 76-03 Tasks 2 + 3 merge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add finished-handler success/failure/invalid-Netscape tests</name>
  <files>tests/test_accounts_dialog.py</files>
  <read_first>
    - tests/test_accounts_dialog.py:196-251 (`TestAccountsDialogOAuthFinished` — the Twitch finished-handler shape Plan 76-04 mirrors)
    - tests/test_accounts_dialog.py:67 (`_mock_proc_with_stderr` helper)
    - tests/test_accounts_dialog.py:254-381 (`TestAccountsDialogStderrParsing` — additional finished-handler edge cases — read for reference, don't necessarily mirror all of them)
    - tests/test_accounts_dialog.py:454-508 (`TestAccountsDialogFailureDialogPlainText` — failure dialog category coverage)
    - musicstreamer/ui_qt/accounts_dialog.py (Plan 76-03 Task 3's `_on_gbs_login_finished` + the extracted `_classify_and_show_failure`)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§Test Strategy Group B lines 782-795 — `test_gbs_login_finished_writes_cookies_on_success`, `test_gbs_login_finished_failure_dialog_for_each_category`, `test_gbs_login_finished_invalidates_bad_netscape`)
  </read_first>
  <behavior>
    - `test_gbs_login_finished_writes_cookies_on_success` — fixture mocks `proc.readAllStandardOutput()` to return the valid Netscape fixture (the verbatim block in this plan's `<interfaces>` section), mocks `proc.readAllStandardError()` to return `b'{"category":"Success","detail":"","provider":"gbs","ts":1.0}\n'`, mocks `proc.exitCode()` → 0. Set `dialog._gbs_login_proc = mock_proc`. Patch `open`, `os.makedirs`, `os.chmod` to MagicMocks (or use `tmp_path` fixture for real file). Call `dialog._on_gbs_login_finished(exit_code=0, exit_status=QProcess.ExitStatus.NormalExit)`. Assert: `open(paths.gbs_cookies_path(), "w", ...)` called once; written content equals the Netscape fixture (no `.strip()` applied — preserves leading/trailing newlines); `os.chmod(paths.gbs_cookies_path(), 0o600)` called; `dialog._toast_callback` invoked with a "GBS.FM logged in." (or equivalent) string; `dialog._update_status` invoked.
    - `test_gbs_login_finished_does_not_strip_netscape` — variant assertion of the previous: the written content has the EXACT bytes from stdout, including any leading newlines. Acts as the T-76-D2 anti-pitfall regression guard from RESEARCH line 709.
    - `test_gbs_login_finished_logs_provider_gbs_event` — capture the OAuthLogger event written on success. Assert `event["provider"] == "gbs"`, `event["category"] == "Success"`. Approach: monkeypatch `_get_oauth_logger` to return a MagicMock; inspect `mock.log_event.call_args.args[0]`.
    - `test_gbs_login_finished_invalidates_bad_netscape` — mock `proc.readAllStandardOutput()` to return `b"garbage text not netscape format"`, exit_code=0, stderr Success event. Call `_on_gbs_login_finished(...)`. Assert: `open(...)` NOT called for the cookies file (or assert `os.chmod` not called); assert `_classify_and_show_failure` called with `provider="gbs"`; assert failure dialog shown with `InvalidTokenResponse`-like category. (Acceptance: the empty / invalid-output branch routes through the same classification path as `_on_oauth_finished` empty-stdout branch.)
    - `test_gbs_login_finished_failure_dialog_for_each_category` — parametrize over `["LoginTimeout", "WindowClosedBeforeLogin", "InvalidTokenResponse", "SubprocessCrash"]`. For each: mock stderr containing the JSON event with that category and `provider="gbs"`, exit_code=1, empty stdout. Call `_on_gbs_login_finished(...)`. Assert `_classify_and_show_failure` invoked with the right category in `last_event` AND `provider="gbs"`. Plan 76-03's extracted helper routes through `_show_failure_dialog(category, detail)` — the existing dialog tests at `TestAccountsDialogFailureDialogPlainText` cover the PlainText invariant, no need to re-test that here.
    - `test_gbs_login_finished_no_strip_anti_pitfall` (regression guard, separate from the writes test) — source-level: `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "netscape_text.*\.strip\(\)"` returns 0. This test asserts the grep gate at runtime via `inspect.getsource(_on_gbs_login_finished)` to belt-and-brace the source-level acceptance check.
    - `test_gbs_login_finished_no_provider_twitch_hardcode` — source-level: similar to above. Inspect source; assert no `"provider": "twitch"` literal appears inside `_on_gbs_login_finished` body (T-76-D4 mitigation regression guard).
  </behavior>
  <action>
    Add 6 new tests + 1 parametrized test (≈ 4 parameters) to `TestAccountsDialogGBS` per RESEARCH §Test Strategy Group B lines 782-795. Per project memory `feedback_mirror_decisions_cite_source.md`: each test that mirrors `TestAccountsDialogOAuthFinished` adds a `# Mirror: tests/test_accounts_dialog.py:LL-LL` comment.

    Implementation guidance:

    1. **`test_gbs_login_finished_writes_cookies_on_success`** — use `tmp_path` fixture to set `paths.gbs_cookies_path()` to a tmp file (monkeypatch `paths.gbs_cookies_path`); construct a real `_mock_proc_with_stderr(stderr_bytes=success_json_bytes, stdout_bytes=netscape_fixture_bytes)`; set `dialog._gbs_login_proc = mock_proc`; call `_on_gbs_login_finished(0, QProcess.ExitStatus.NormalExit)`; assert the tmp file exists with content matching the fixture; assert `os.stat(path).st_mode & 0o777 == 0o600` (or use `stat.S_IMODE`).

    2. **`test_gbs_login_finished_does_not_strip_netscape`** — variant: feed stdout with leading newlines (`b"\n\n# Netscape HTTP Cookie File\n.gbs.fm\t..."`); assert written content includes the leading newlines.

    3. **`test_gbs_login_finished_logs_provider_gbs_event`** — monkeypatch `dialog._get_oauth_logger` to return a `MagicMock()`; trigger success path; assert `mock.log_event.call_args[0][0]["provider"] == "gbs"`.

    4. **`test_gbs_login_finished_invalidates_bad_netscape`** — feed stdout `b"garbage"`; trigger; assert NO `os.chmod` call AND `_classify_and_show_failure` invoked.

    5. **`test_gbs_login_finished_failure_dialog_for_each_category`** — `@pytest.mark.parametrize("category", ["LoginTimeout", "WindowClosedBeforeLogin", "InvalidTokenResponse", "SubprocessCrash"])`. For each: feed stderr JSON event with that category and `provider="gbs"`, exit_code=1; trigger; assert `_classify_and_show_failure` called with the category preserved.

    6. **`test_gbs_login_finished_no_strip_anti_pitfall`** — source inspection: `import inspect; from musicstreamer.ui_qt.accounts_dialog import AccountsDialog; src = inspect.getsource(AccountsDialog._on_gbs_login_finished); assert "netscape_text.strip()" not in src; assert ".strip()" not in src.split("netscape_text")[1].split("\n")[0]` (or simpler: regex check that no `.strip()` follows `netscape_text` on the same line).

    7. **`test_gbs_login_finished_no_provider_twitch_hardcode`** — source inspection: `src = inspect.getsource(AccountsDialog._on_gbs_login_finished); assert '"provider": "twitch"' not in src` AND assert `'"provider": "gbs"'` appears at least once. This locks T-76-D4.

    For tests 1-5, use `_mock_proc_with_stderr` from the existing helper at line 67 — it already produces a MagicMock with `readAllStandardError()` and `readAllStandardOutput()` returning configurable bytes wrappers.

    For the failure-category parametrization: the test does NOT need to validate that the failure dialog actually appears (that's Phase 999.3's existing test coverage); it only validates that the GBS flow correctly funnels the event into the helper with `provider="gbs"`.
  </action>
  <acceptance_criteria>
    - Source assertion: 6+ new test names — `grep -c "def test_gbs_login_finished" tests/test_accounts_dialog.py` returns ≥ 6.
    - Source assertion: parametrized category test exists — `grep -B 2 "def test_gbs_login_finished_failure_dialog_for_each_category" tests/test_accounts_dialog.py | grep -c "parametrize"` returns 1.
    - Source assertion: 0o600 assertion — `grep -A 25 "def test_gbs_login_finished_writes_cookies_on_success" tests/test_accounts_dialog.py | grep -cE "0o600|st_mode"` returns ≥ 1.
    - Source assertion: provider-gbs logger event assertion — `grep -A 15 "def test_gbs_login_finished_logs_provider_gbs_event" tests/test_accounts_dialog.py | grep -cE "provider.*gbs"` returns ≥ 1.
    - Source assertion: bad-Netscape gate — `grep -A 15 "def test_gbs_login_finished_invalidates_bad_netscape" tests/test_accounts_dialog.py | grep -cE "garbage|invalid|not_called|_classify"` returns ≥ 1.
    - Source assertion: anti-pitfall guards run via `inspect.getsource` — `grep -A 15 "def test_gbs_login_finished_no_strip_anti_pitfall\|def test_gbs_login_finished_no_provider_twitch_hardcode" tests/test_accounts_dialog.py | grep -c "inspect.getsource"` returns ≥ 2.
    - Behavior (RED before 76-03 merges): collection succeeds.
    - Behavior (GREEN after 76-03 merges): `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS -x -q -k "login_finished"` exits 0.
    - Behavior: Twitch finished-handler tests still pass — `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished -x -q` exits 0.
    - Behavior: Full test_accounts_dialog.py pass after Plan 76-03 — `python -m pytest tests/test_accounts_dialog.py -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS --collect-only -q && python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished -x -q</automated>
  </verify>
  <done>6+ finished-handler tests + 1 parametrized category test added; anti-pitfall regression guards via `inspect.getsource` lock the no-strip and no-twitch-hardcode invariants; collection passes; Twitch tests still pass; commit message cites the four mirror-source line ranges (Twitch finished-handler test class, stderr parsing test class, failure-dialog test class, _mock_proc helper).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → file system | Tests writing real cookies files use `tmp_path` fixture to isolate from `~/.local/share/musicstreamer/gbs-cookies.txt`. |
| Test → `QMessageBox` modal | Tests mock `QMessageBox.question` via `monkeypatch.setattr` to inject Yes/No without blocking the test runner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-T3 | Tampering | Test pollutes real cookies file on a developer's machine | mitigate | All file-write tests use `tmp_path` + `monkeypatch.setattr(paths, "gbs_cookies_path", lambda: tmp_path / "gbs-cookies.txt")`. Source-grep: `grep -c "tmp_path" tests/test_accounts_dialog.py` returns ≥ 1 for new tests. |
| T-76-T4 | Information Disclosure | Synthetic Netscape fixture leaks real session IDs | mitigate | Netscape fixture uses `test_sessionid_value` / `test_csrftoken_value` literals (NOT base64 / hex blobs). Source-grep: `grep -E "test_sessionid_value|test_csrftoken_value" tests/test_accounts_dialog.py` returns ≥ 1 match each. |
| T-76-T5 | Tampering | Existing migrated GBS tests assert against stale text and silently pass against new code | mitigate | Task 1 acceptance gate runs `grep` for the OLD text — `"Import GBS.FM Cookies..."` MUST return 0 matches in the entire test file. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No new package installs. |
</threat_model>

<verification>
After all 3 tasks land + Plan 76-03 fully merges:

```bash
# Full TestAccountsDialogGBS pass:
python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS -x -q

# Twitch regression guard:
python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished \
    tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing \
    tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText \
    tests/test_accounts_dialog.py::TestAccountsDialogRetry -x -q

# Anti-pitfall guards:
test "$(grep -c 'Import GBS.FM Cookies...' tests/test_accounts_dialog.py)" -eq 0
test "$(grep -c 'def test_gbs_login_finished' tests/test_accounts_dialog.py)" -ge 6
test "$(grep -c 'inspect.getsource' tests/test_accounts_dialog.py)" -ge 2
```
</verification>

<success_criteria>
- [ ] Existing GBS tests migrated to assert new contract (no `"Import GBS.FM Cookies..."` text remains in test bodies).
- [ ] 3 status/import-button tests added (Task 1).
- [ ] 5 subprocess/disconnect tests added (Task 2).
- [ ] 6+ finished-handler tests + 1 parametrized failure-category test added (Task 3).
- [ ] Anti-pitfall regression guards via `inspect.getsource` lock the no-strip and no-twitch-hardcode invariants.
- [ ] Existing Twitch tests still pass after Plan 76-03 + this plan merge.
- [ ] All tests use `tmp_path` for file writes; synthetic cookie values only.
- [ ] Commit messages cite specific source line ranges per test (`# Mirror: tests/test_accounts_dialog.py:LL-LL` comments above each cloned-shape test).
</success_criteria>

<output>
Create `.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-04-SUMMARY.md` when done.
</output>
