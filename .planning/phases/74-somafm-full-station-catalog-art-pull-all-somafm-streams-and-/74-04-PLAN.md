---
phase: 74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-
plan: 04
type: execute
wave: 3
depends_on: [74-02, 74-03]
files_modified:
  - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md
autonomous: false
requirements:
  - SOMA-01
  - SOMA-02
  - SOMA-03
  - SOMA-04
  - SOMA-05
  - SOMA-06
  - SOMA-07
  - SOMA-08
  - SOMA-09
  - SOMA-10
  - SOMA-11
  - SOMA-12
  - SOMA-17

must_haves:
  truths:
    - "D-04: Manual re-import only — user clicks the hamburger 'Import SomaFM' action and SomaFM stations appear in the library; no auto-import on app start."
    - "D-06: Toast sequence verified live — 'Importing SomaFM…' fires immediately; ~5 sec later (per D-08 implicit threshold) the final 'SomaFM import: N stations added' toast appears where N matches the live channel count (46 at research time)."
    - "D-08: Total import wall-clock under 5 seconds on a typical home connection (channels.json ~50 KB + 4 PLS GETs/channel + parallel 8-way logo download)."
    - "D-11: Logos render in the station tree / Now Playing panel — user visually confirms images appear for at least 3 stations (gracefully degrading if some logo URLs fail)."
    - "Playback smoke test: the user can click one imported SomaFM station and hear audio within 5 seconds (validates the stream-URL resolution from Plan 02's _resolve_pls actually produced playable ICE URLs)."
    - "D-05 re-import idempotence: clicking 'Import SomaFM' a SECOND time produces toast 'SomaFM import: no changes' (inserted=0 because all stream URLs now match existing rows)."
  artifacts:
    - path: ".planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md"
      provides: "User-confirmed UAT findings — pass/fail per checklist row, plus any deferred items for follow-up phases"
  key_links:
    - from: "User click"
      to: "MainWindow._on_soma_import_clicked → _SomaImportWorker → soma_import.fetch_channels + import_stations → repo writes → station tree refresh"
      via: "Live SomaFM API (api.somafm.com:443)"
      pattern: "user observable: ~46 new stations + N×20 streams in the library"
---

<objective>
Phase 74 / Wave 3: User-Acceptance Test against the LIVE SomaFM catalog. This plan is non-autonomous — it pauses for the user to run the import twice on their actual app + confirm the observable invariants from CONTEXT D-04, D-05, D-06, D-08, D-11.

Purpose: Wave 2 turns the entire Wave-0 RED suite GREEN (all 17 tests + 2 drift-guards). But the test suite mocks `urlopen` everywhere — the live SomaFM API is never touched in CI. This plan closes the gap with a single ~5-minute human-verify checkpoint against the real catalog. Per CONTEXT D-08, the implicit performance budget is "<5 sec for ~40 channels"; if the live import takes longer, the planner can revisit progress-bar UX in a follow-up phase.

Output: a populated `74-04-UAT-LOG.md` with pass/fail per checklist row + any deferred items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-02-SUMMARY.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — scaffold 74-04-UAT-LOG.md with checklist rows</name>
  <files>.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md (`<decisions>` D-04 through D-15)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-02-SUMMARY.md (confirms Plan 02 shipped the soma_import module)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-03-SUMMARY.md (confirms Plan 03 shipped the GUI wiring)
  </read_first>
  <action>
Create `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md` as a NEW Markdown file. Structure:

1. **Frontmatter header** with `phase: 74`, `plan: 04`, `status: pending-user`, `created: <today>`.
2. **Top section** — three sentences explaining: (a) Wave 2 turned all 17+2 RED tests GREEN against mocked urlopen, (b) this plan verifies the same invariants against the LIVE api.somafm.com:443 catalog, (c) failures here surface as Phase 74.1 gap-closure items.
3. **Pre-test fresh-library step** — instruct the user how to either run on a fresh dev DB (preferred) or document existing SomaFM rows in the library (via `sqlite3 ~/.local/share/musicstreamer/library.db "SELECT count(*) FROM stations WHERE provider_name='SomaFM'"`) so D-05 dedup can be observed.
4. **Pass/fail checklist** with the following rows, each formatted as `- [ ] UAT-NN: <description>` with a blank line after each for user notes:

   - UAT-01 (D-06 click toast): user clicks hamburger → "Import SomaFM"; immediately a toast appears with exactly the text `Importing SomaFM…`.
   - UAT-02 (D-08 wall-clock): the import completes (final toast appears) within 5 seconds. Record the observed elapsed time in seconds.
   - UAT-03 (D-06 success toast): the final toast text is `SomaFM import: N stations added` where N is at least 40 (the live catalog had 46 channels at research time; tolerate ±5 for catalog drift).
   - UAT-04 (D-04 manual only): close the app and reopen it; no SomaFM import fires on startup (no `Importing SomaFM…` toast appears).
   - UAT-05 (D-11 logos render): scroll the station tree to the SomaFM-imported rows; at least 3 of them show non-placeholder station art.
   - UAT-06 (playback smoke): click on a SomaFM station (e.g., Groove Salad if imported); audio plays within 5 seconds with no error toast.
   - UAT-07 (D-05 re-import idempotence): with stations from UAT-03 still present, click hamburger → "Import SomaFM" a second time; final toast is exactly `SomaFM import: no changes` (inserted=0).
   - UAT-08 (D-13 cover_art_source default): open EditStationDialog on an imported SomaFM station; confirm the per-station `cover_art_source` is `auto` (NOT empty, NOT `itunes_only`). If the dialog does not expose this field as a visible widget yet, run `sqlite3 ~/.local/share/musicstreamer/library.db "SELECT cover_art_source FROM stations WHERE provider_name='SomaFM' LIMIT 5"` and verify all rows show `auto`.
   - UAT-09 (D-02 provider name pin): run `sqlite3 ~/.local/share/musicstreamer/library.db "SELECT DISTINCT provider_name FROM stations WHERE name LIKE '%Groove Salad%' OR name LIKE '%Drone Zone%'"` and verify the SomaFM-imported rows show exactly `SomaFM` (CamelCase, no space, no period).
   - UAT-10 (D-03 stream count): run `sqlite3 ~/.local/share/musicstreamer/library.db "SELECT s.name, count(ss.id) FROM stations s JOIN station_streams ss ON ss.station_id=s.id WHERE s.provider_name='SomaFM' GROUP BY s.id ORDER BY count(ss.id) DESC LIMIT 5"` and verify each SomaFM station has exactly 20 stream rows (4 tiers × 5 ICE relays). If any station has <20 streams, record which one + the actual count for follow-up triage.

5. **"Findings" section** — empty Markdown subsection where the user records any unexpected behavior, regressions, or items to defer to Phase 74.1.

6. **"Sign-off" line** at the bottom: `- [ ] All UAT rows pass OR all failures documented as Phase 74.1 gap-closure items.`

The file lands in the phase directory so /gsd-verify-work picks it up.
  </action>
  <verify>
    <automated>test -f .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md &amp;&amp; grep -c "^- \[ \] UAT-" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md | grep -E "^10$"</automated>
  </verify>
  <acceptance_criteria>
    - File `74-04-UAT-LOG.md` exists with status `pending-user` in frontmatter.
    - Exactly 10 `- [ ] UAT-NN:` checklist rows present.
    - Each UAT row maps to at least one CONTEXT D-NN by citation in the row text.
    - The Findings section header exists (empty body OK at this stage).
    - The Sign-off line exists at the bottom.
  </acceptance_criteria>
  <done>UAT-LOG scaffold landed; ready for user-driven verification step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: USER runs live SomaFM import + populates UAT-LOG</name>
  <files>.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md (the scaffold landed by Task 1)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-02-SUMMARY.md
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-03-SUMMARY.md
  </read_first>
  <action>
HUMAN-VERIFY checkpoint — Claude pauses and surfaces the UAT script to the user. Claude MUST NOT execute the import on the user's behalf; instead it presents the checklist and waits for user-supplied results.

What Claude does:
  1. Restate the UAT-LOG checklist (10 rows) verbatim to the user.
  2. Surface the live-launch command `uv run --with PySide6 python -m musicstreamer` (or whatever the project's standard dev launch is — check pyproject.toml `[project.scripts]` first).
  3. Ask the user to walk the checklist, record pass/fail per row inside `74-04-UAT-LOG.md`, paste any Findings, and then return the literal string `approved` (or a list of failed UATs).
  4. On `approved` with no failed rows: Claude updates the UAT-LOG `status` frontmatter from `pending-user` to `approved`, ticks the sign-off checkbox, and proceeds to Task 1's SUMMARY emission.
  5. On `approved` with failed rows: Claude updates the UAT-LOG `status` to `gap-closure-needed`, leaves Findings as the user wrote them, and returns `## GAP CLOSURE NEEDED` to the orchestrator so `/gsd-plan-phase 74 --gaps` can pick it up.

What Claude does NOT do:
  - Run the app — the user runs the app.
  - Click any UI element — only the user touches the GUI.
  - Edit pass/fail rows on behalf of the user — only the user can flip a `[ ]` to `[x]`.
  - Decide whether a failed UAT is severe enough to block phase close — the user decides.

What-built recap (surface to user verbatim):
  Wave 2 GREEN tier — `musicstreamer/soma_import.py` + main_window hamburger handlers + __main__.py logger registration. All 17 + 2 Wave-0 RED tests are now GREEN against mocked urlopen. This checkpoint verifies the same behaviors against the LIVE api.somafm.com:443 catalog and live audio playback.

How-to-verify recap (surface to user verbatim):
  Pre-flight:
    1. Confirm app builds + launches: `uv run --with PySide6 python -m musicstreamer` (or your normal dev launch command).
    2. Optionally back up your library DB before the test: `cp ~/.local/share/musicstreamer/library.db ~/.local/share/musicstreamer/library.db.pre-phase74`.
    3. Open `74-04-UAT-LOG.md` in your editor — populate it as you go.

  Then walk the checklist:
    - UAT-01 to UAT-03: click hamburger → "Import SomaFM"; observe both toasts + wall-clock elapsed. Mark pass/fail + record the elapsed seconds.
    - UAT-04: close and reopen the app; verify no auto-import fires.
    - UAT-05: scroll the station tree to SomaFM rows; confirm at least 3 logos render.
    - UAT-06: click a SomaFM station; confirm audio plays within 5 seconds.
    - UAT-07: click "Import SomaFM" again; verify the toast is `SomaFM import: no changes`.
    - UAT-08, UAT-09, UAT-10: run the three sqlite3 queries on your library DB; record the output verbatim under each row.

  Any UAT failures: write a "Findings" entry describing what you observed. The orchestrator can route gap-closure to a follow-up phase or commit-back-to-this-phase via `/gsd-plan-phase 74 --gaps`.

Resume-signal: User types `approved` after marking all 10 UAT rows + the sign-off checkbox. If any UAT row failed, the user pastes the Findings block alongside the `approved` (or types a list of failed UAT IDs) so the orchestrator can decide between (a) opening a Phase 74.1 gap-closure phase or (b) deferring the finding to a backlog phase.
  </action>
  <verify>
    <automated>grep -E "^status: (approved|gap-closure-needed)" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - User has returned to the conversation with the literal string `approved` (with or without a Findings list).
    - `74-04-UAT-LOG.md` frontmatter `status` field is one of `approved` or `gap-closure-needed`.
    - Sign-off checkbox at the bottom of the UAT-LOG is ticked (`- [x] All UAT rows pass OR all failures documented as Phase 74.1 gap-closure items.`).
    - Findings section is present (may be empty if every row passed).
  </acceptance_criteria>
  <done>User walked the 10-row UAT checklist live; UAT-LOG status flipped from pending-user to approved (or gap-closure-needed); sign-off checkbox ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live network → user's app + DB | The UAT exercises the full production path: api.somafm.com:443 → fetch_channels → _resolve_pls (each PLS fetched live) → ThreadPoolExecutor logo downloads → SQLite writes. The mocked CI cannot verify rate limiting, SomaFM TOS compliance, or actual playback. |
| User input → UAT-LOG | The user records observations in plain Markdown; no automation parses this beyond /gsd-verify-work's frontmatter check. Failed rows become inputs to a potential Phase 74.1 gap-closure phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | I (SomaFM ban) | Live HTTP to api.somafm.com | mitigate (inherited) | Plan 02's _USER_AGENT is verified by Wave-0 source-grep gates; the UAT empirically confirms SomaFM does not 429/403 us on a real client. |
| T-74-08 (new) | D (live network failure during UAT) | api.somafm.com reachability | accept | If api.somafm.com is unreachable during the UAT window, the user marks UAT-01..UAT-03 as `pending — retry when net stable`; the import is purely manual so there is no urgency. |
| T-74-09 (new) | T (library DB pollution from a flawed import) | SQLite library.db | accept | The user is instructed to back up library.db before running UAT, mitigating accidental data loss. D-05 dedup means a re-import on a partially-imported library is safe. |
</threat_model>

<verification>
1. `74-04-UAT-LOG.md` exists with 10 checklist rows.
2. After Task 2 returns from the human-verify checkpoint, all 10 rows have either `[x]` (pass) OR a Findings entry referencing the row.
3. The Sign-off line is checked.
4. If any UAT row failed: the orchestrator emits `## GAP CLOSURE NEEDED` and routes to `/gsd-plan-phase 74 --gaps` for follow-up.
</verification>

<success_criteria>
- [ ] UAT-LOG scaffold landed via Task 1.
- [ ] User completes Task 2 by walking the 10 checklist rows on the live app.
- [ ] All 10 rows either pass OR each failure is recorded in Findings with a clear repro step.
- [ ] Sign-off line checked.
- [ ] Phase 74 transitions from "Wave 2 GREEN" to "live-verified" — closes SOMA-01..SOMA-17 unless UAT surfaces a regression.
</success_criteria>

<output>
After completion, create `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-SUMMARY.md` describing UAT outcome + any deferred items.
</output>
