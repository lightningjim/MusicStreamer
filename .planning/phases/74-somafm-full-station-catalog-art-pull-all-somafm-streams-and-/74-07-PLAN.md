---
phase: 74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-
plan: "07"
type: execute
wave: 5
depends_on: ["74-05", "74-06"]
files_modified:
  - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md
autonomous: false
gap_closure: true
requirements: [SOMA-02, SOMA-11]

must_haves:
  truths:
    - "Live re-import of SomaFM with existing SomaFM rows in the DB produces exactly the toast 'SomaFM import: no changes' (closes UAT-07)"
    - "Live import populates Synphaera Radio's MP3-highest tier with bitrate_kbps=256, NOT 128 (closes Finding F-01)"
    - "All previously-passing UAT rows (UAT-01..06, UAT-08..10) still pass — no regression in the broader import path"
  artifacts:
    - path: ".planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md"
      provides: "Live UAT addendum recording the user's PASS/FAIL on the two gap-closure verifications"
  key_links:
    - from: "running app (hamburger 'Import SomaFM' on second click)"
      to: "show_toast('SomaFM import: no changes')"
      via: "_SomaImportWorker.import_finished signal → _on_soma_import_done(0, N)"
      pattern: "user sees toast text"
    - from: "DB row for Synphaera Radio's -256-mp3 stream"
      to: "stored bitrate_kbps=256"
      via: "_bitrate_from_url override at fetch_channels streams.append site"
      pattern: "sqlite3 query result"
---

<objective>
Re-verify the two gap closures (G-01 + G-02) against the live application + live `api.somafm.com` catalog. Plans 74-05 and 74-06 have already turned the automated test suite GREEN (15 soma_import tests + 8 main_window_soma tests + drift-guards all passing). This plan confirms the same fixes hold against real-world data:

1. **G-01 closure (UAT-07 re-do):** With SomaFM stations already in the library, clicking "Import SomaFM" a second time produces the `SomaFM import: no changes` toast.
2. **G-02 closure (UAT Finding F-01 re-do):** A live import populates Synphaera Radio's MP3-highest tier with `bitrate_kbps=256` (the actual stream bitrate encoded in the URL slug), not the old hardcoded `128`.

Purpose: Close out Phase 74 by promoting both VERIFICATION.md gaps from FAILED to VERIFIED via live user UAT.
Output: `74-07-UAT-LOG.md` with the user's PASS/FAIL checkmarks on each row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-VERIFICATION.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-05-PLAN.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-06-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 74-07-UAT-LOG.md skeleton</name>
  <files>.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-04-UAT-LOG.md (UAT format precedent — mirror this shape)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-VERIFICATION.md (gap definitions + DB schema notes: stations.provider_id is FK to providers.id; no provider_name column — JOIN required)
  </read_first>
  <action>
    Create the file `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` with the structure described below. Mirror the formatting of `74-04-UAT-LOG.md` (frontmatter + title + intro + pre-test + numbered UAT rows + sign-off).

    Frontmatter fields: phase=74, plan="07", status=gap-closure-uat-pending, created=2026-05-14, addresses_gaps list with "G-01 (SOMA-11 / UAT-07)" and "G-02 (SOMA-02 / UAT Finding F-01)".

    Title: `# Phase 74 Plan 07: Gap-Closure UAT Re-Verification`.

    Intro: state that Plans 74-05 and 74-06 closed both VERIFICATION.md blockers via automated tests; this plan re-runs the two previously-failing live-UAT rows against the running app + live api.somafm.com to confirm the fixes hold end-to-end.

    Pre-test section: record DB path `~/.local/share/musicstreamer/musicstreamer.sqlite3` and the schema note that `stations.provider_id` is FK to `providers.id` with no `provider_name` column on `stations` (so SomaFM queries need the JOIN). Recommend an optional backup `cp ~/.local/share/musicstreamer/musicstreamer.sqlite3 ~/.local/share/musicstreamer/musicstreamer.sqlite3.pre-phase74-07`.

    Three UAT rows, each starting with `- [ ]` so the user flips to `- [x]` on PASS. Row content:

    Row 1 — `UAT-07-RETEST` (G-01 closure / SOMA-11). Steps: confirm SomaFM rows exist via the JOIN query `sqlite3 ~/.local/share/musicstreamer/musicstreamer.sqlite3 "SELECT count(*) FROM stations s JOIN providers p ON s.provider_id=p.id WHERE p.name='SomaFM'"` (expect ≥ 1 — if 0, click Import SomaFM once first and wait for the success toast). Then click hamburger menu → "Import SomaFM" a second time. Within ~5 s a toast appears with EXACTLY the text `SomaFM import: no changes`. Fields to fill: recorded toast text, pre-click SomaFM count.

    Row 2 — `UAT-F01-RETEST` (G-02 closure / SOMA-02). Steps: confirm Synphaera Radio's MP3-highest tier rows store `bitrate_kbps=256`. NOTE for the user: if the existing Phase 74.0 import already populated Synphaera with bitrate=128 rows, those rows survive Plan 74-05 (the parser only affects NEW imports — D-05 dedup means re-import is a no-op on URL match). To re-verify against the live parser, the user must first delete the Synphaera rows then re-import. Suggested cleanup command: `sqlite3 ~/.local/share/musicstreamer/musicstreamer.sqlite3 "DELETE FROM stations WHERE name LIKE '%Synphaera%'"` (after backup). Then click Import SomaFM and wait for the success toast. Verification SQL to paste into the document as a fenced bash block:

    ```bash
    sqlite3 ~/.local/share/musicstreamer/musicstreamer.sqlite3 \
      "SELECT s.name, ss.url, ss.bitrate_kbps, ss.codec, ss.quality
       FROM stations s
       JOIN providers p ON s.provider_id=p.id
       JOIN station_streams ss ON ss.station_id=s.id
       WHERE p.name='SomaFM'
         AND s.name LIKE '%Synphaera%'
         AND ss.url LIKE '%-256-mp3%'"
    ```

    Expected: at least one row where `bitrate_kbps = 256`. Field to fill: paste the verbatim SQL output.

    Row 3 — `UAT-REGRESSION` (no regression on previously-passing rows). User spot-checks UAT-01 (click toast `Importing SomaFM…` appears within 1 s of the click) and UAT-06 (a SomaFM station — for example Synphaera Radio or Groove Salad — plays audio within 5 s with no error toast). Fields to fill: PASS/FAIL for each spot-check.

    Sign-off: `- [ ] All three rows pass → Phase 74 gap closure is COMPLETE. If any row fails, the failure routes back to a Phase 74.2 gap closure cycle.`

    Leave a final blank `## Findings` heading at the end so the user has somewhere to note unexpected observations (mirror 74-04-UAT-LOG.md "Findings" section pattern).
  </action>
  <verify>
    <automated>test -f .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md &amp;&amp; grep -c "^- \[ \] UAT" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` exists.
    - `grep -c "^- \[ \] UAT" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` returns `3` (three unchecked UAT rows ready for user fill-in).
    - `grep -F "UAT-07-RETEST" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` returns at least 1 match.
    - `grep -F "UAT-F01-RETEST" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` returns at least 1 match.
    - `grep -F "synphaera-256-mp3" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` OR `grep -F "ss.url LIKE '%-256-mp3%'" .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-UAT-LOG.md` returns at least 1 match (the verification SQL block landed).
    - Frontmatter contains `addresses_gaps:` and lists both `G-01` and `G-02`.
  </acceptance_criteria>
  <done>
    The UAT skeleton file exists with three unchecked UAT rows (UAT-07-RETEST, UAT-F01-RETEST, UAT-REGRESSION), correctly-formed SQL verification blocks, and a sign-off line.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — walk the three rows and record results</name>
  <what-built>The UAT log skeleton from Task 1. Plans 74-05 and 74-06 closed both VERIFICATION.md blockers via code changes + automated tests. Now we re-run the two failing UAT rows against the running app to confirm end-to-end behavior.</what-built>
  <how-to-verify>
    Walk the three rows in `74-07-UAT-LOG.md` against the live application:

    1. **UAT-07-RETEST (re-import toast):**
       - Confirm SomaFM rows already exist in the DB (run the count JOIN query in the file; expect ≥ 1). If zero, click Import SomaFM once first and wait for the success toast before proceeding.
       - Click hamburger menu → "Import SomaFM" a SECOND time.
       - Expected: within ~5 s, a toast appears with EXACTLY the text `SomaFM import: no changes`.
       - Record the toast text in the file. Flip `- [ ]` to `- [x]` if PASS; otherwise write `FAIL: <what you saw>`.

    2. **UAT-F01-RETEST (Synphaera 256 kbps bitrate):**
       - Read the cleanup note in Row 2. If existing Synphaera rows already have `bitrate_kbps=128`, run the suggested DELETE command (after the backup recommended at the top of the file), then click Import SomaFM and wait for the success toast.
       - Run the verification SQL from Row 2 and paste the verbatim output into the file.
       - Expected: at least one row where `bitrate_kbps = 256`.
       - Flip `- [ ]` to `- [x]` if PASS; otherwise write `FAIL: <what you saw>`.

    3. **UAT-REGRESSION (spot-check):**
       - UAT-01 spot-check: when you clicked Import SomaFM for Row 1 or Row 2, the `Importing SomaFM…` toast appeared within 1 s. Record PASS/FAIL.
       - UAT-06 spot-check: click a SomaFM station (Synphaera Radio or Groove Salad) and confirm audio plays within ~5 s with no error toast. Record PASS/FAIL.
       - Flip the row to `- [x]` if both spot-checks PASS.

    4. Flip the final `- [ ]` sign-off line if all three rows passed.

    Reply with "approved" if all three rows passed. If any row failed, reply with the failure details so we can triage whether a follow-on gap closure cycle (Phase 74.2) is needed.
  </how-to-verify>
  <resume-signal>Type "approved" if all three UAT rows pass and the sign-off line is flipped; otherwise describe the failure(s).</resume-signal>
</task>

</tasks>

<verification>
1. File-exists gate (Task 1 acceptance criteria): the UAT log skeleton lands at the expected path with the three unchecked rows and SQL block.

2. User UAT gate (Task 2, blocking): user reports "approved" after confirming all three rows pass; OR the user reports specific failure(s) that route to a follow-on gap closure cycle.

3. (Implicit) Re-verification of VERIFICATION.md: when the user approves Task 2, Phase 74 status can move from `gaps_found` to `verified` — handled by `/gsd-verify-phase` after this plan completes.
</verification>

<success_criteria>
- Plan 74-07 produces a complete `74-07-UAT-LOG.md` with three rows flipped to PASS by the user.
- The two VERIFICATION.md gaps (G-01 + G-02) are confirmed closed against live conditions, not just the mocked test suite.
- No regression appears in the previously-passing UAT rows (UAT-01..06, UAT-08..10).
- Phase 74 can be marked complete via `/gsd-verify-phase 74`.
</success_criteria>

<output>
After Task 2 user approval, create `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-07-SUMMARY.md` per the standard summary template. Include:
- The UAT row outcomes (verbatim user-filled values)
- Confirmation that both VERIFICATION.md gaps are now closed against live data
- Reference to the closed gaps (G-01, G-02) and the requirements they unblocked (SOMA-02, SOMA-11)
- A note that Phase 74 is ready for `/gsd-verify-phase` re-run
</output>
