---
phase: 74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_soma_import.py
  - tests/test_main_window_soma.py
  - tests/fixtures/soma_channels_3ch.json
  - tests/fixtures/soma_channels_with_dedup_hit.json
  - tests/test_constants_drift.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SOMA-01
  - SOMA-02
  - SOMA-03
  - SOMA-04
  - SOMA-05
  - SOMA-06
  - SOMA-07
  - SOMA-08
  - SOMA-09
  - SOMA-10
  - SOMA-11
  - SOMA-12
  - SOMA-13
  - SOMA-14
  - SOMA-15
  - SOMA-16
  - SOMA-17

must_haves:
  truths:
    - "D-02: Every inserted SomaFM station has provider_name = 'SomaFM' (CamelCase, no space, no period); pinned by a Wave-0 RED test."
    - "D-03: The 4-tier × 5-relay scheme (mp3-highest → hi/MP3/128; aac-highest → hi2/AAC/128; aacp-high → med/AAC/64; aacp-low → low/AAC/32; position = tier_base*10 + relay_index) is pinned by an RED unit test that asserts the (codec, quality, bitrate_kbps, position) tuple set."
    - "D-05 + D-09: Re-import full no-op on URL match — fixture soma_channels_with_dedup_hit.json + stub repo asserts inserted=0, skipped=1 after the second pass; insert_station is never called."
    - "D-06: 'Import SomaFM' menu entry exists in MainWindow._menu; toast strings 'Importing SomaFM…' (click), 'SomaFM import: {N} stations added' or 'SomaFM import: no changes' (finished), 'SomaFM import failed: <truncated>' (error) — pinned verbatim by Wave-0 qtbot tests."
    - "D-07 + SYNC-05: Click sets MainWindow._soma_import_worker to a non-None _SomaImportWorker; cleared in done/error slots — pinned by retention test."
    - "D-11 + D-15: Logo HTTP failure is non-fatal (station + streams stay inserted, update_station_art NOT called); per-channel exception inside the catalog loop increments skipped and continues — both pinned by RED tests."
    - "D-14: Network error / JSON parse error from fetch_channels → worker emits error signal → toast 'SomaFM import failed: <reason>' (truncated at 80 chars + '…')."
    - "D-16 + Test 17: __main__.py registers musicstreamer.soma_import logger at INFO level — pinned by drift-guard source-grep."
    - "T-74-01 mitigation: source-grep test asserts the literal 'MusicStreamer/' AND 'https://github.com/lightningjim/MusicStreamer' appear in soma_import.py (UA presence gate)."
    - "T-74-02 mitigation: source-grep test asserts no STORED codec field equals 'AAC+' (only 'MP3' or 'AAC' values appear in _TIER_BY_FORMAT_QUALITY)."
    - "T-74-03 mitigation: fixture-driven test asserts inserted URLs equal direct ice*.somafm.com URLs (NOT .pls URLs); D-05 dedup correctness hinges on this."
    - "T-74-04 mitigation: logo-failure test asserts station survives; update_station_art is NOT called."
    - "T-74-05 mitigation: per-channel exception test asserts skipped += 1 and import continues."
    - "REQUIREMENTS.md registers SOMA-01..SOMA-17 with the Coverage block bumped from 38/38/20/18 to 55/55/20/35; the Traceability table gains rows mapping each ID to Phase 74."
    - "STATION-ART-04 is NOT claimed closed in this plan (CONTEXT specifics note that Phase 74 is a strict subset of the broader ART-04 note, not a closure)."
  artifacts:
    - path: "tests/test_soma_import.py"
      provides: "RED unit tests for soma_import (tests 1–9 + 14–15 from RESEARCH validation matrix)"
      min_lines: 250
    - path: "tests/test_main_window_soma.py"
      provides: "RED qtbot tests for MainWindow Soma wiring (tests 10–12 + 16)"
      min_lines: 150
    - path: "tests/fixtures/soma_channels_3ch.json"
      provides: "3-channel canonical fixture for fetch/insert/multi-tier tests"
    - path: "tests/fixtures/soma_channels_with_dedup_hit.json"
      provides: "Fixture whose first channel's resolved ICE URL collides with a stubbed station_exists_by_url to test D-05 skip"
    - path: "tests/test_constants_drift.py"
      provides: "Extended with test_soma_import_logger_registered AND test_soma_nn_requirements_registered (RESEARCH tests 13 + 17)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SOMA-01..SOMA-17 rows in the requirement list + Traceability table + Coverage block bump"
  key_links:
    - from: "tests/test_soma_import.py"
      to: "musicstreamer.soma_import"
      via: "import + monkeypatch on urllib.request.urlopen + _resolve_pls"
      pattern: "patch.*musicstreamer\\.soma_import\\."
    - from: "tests/test_main_window_soma.py"
      to: "musicstreamer.ui_qt.main_window"
      via: "MainWindow construction with _FakePlayer + _FakeRepo doubles"
      pattern: "MainWindow\\(_FakePlayer\\(\\)"
    - from: "tests/test_constants_drift.py"
      to: ".planning/REQUIREMENTS.md + musicstreamer/__main__.py"
      via: "Path-based read of source files + literal-substring assertions"
      pattern: "Path\\(__file__\\)\\.parent\\.parent"
---

<objective>
Phase 74 / Wave 0: Land the SomaFM bulk-importer RED contract — five new/extended test files plus REQUIREMENTS.md registration — so Plans 02 + 03 implement against a fixed spec rather than improvising.

Purpose: Encode CONTEXT D-01..D-16 + RESEARCH validation tests 1–17 + STRIDE mitigations T-74-01..T-74-05 as failing pytest tests BEFORE the production module exists. This is the executable spec the GREEN plans satisfy.

Output: 4 NEW test/fixture files + 1 extended test file + REQUIREMENTS.md with 17 new SOMA-NN rows. Coverage block bumped from 38/38/20/18 to 55/55/20/35. Drift-guard for the logger registration (D-16) lands in test_constants_drift.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-VALIDATION.md

@tests/test_aa_import.py
@tests/test_main_window_gbs.py
@tests/test_cover_art_mb.py
@tests/test_constants_drift.py

<interfaces>
<!-- The soma_import.py module these tests will exercise is created in Plan 02. -->
<!-- Tests must reference its planned public API + module-level constant names. -->

Planned `musicstreamer/soma_import.py` public surface (Plan 02 will create):
- `_API_URL: str = "https://api.somafm.com/channels.json"`
- `_USER_AGENT: str` containing literals `"MusicStreamer/"` and `"https://github.com/lightningjim/MusicStreamer"`
- `_TIER_BY_FORMAT_QUALITY: dict[tuple[str,str], dict]` keyed by (format, quality); values carry quality / tier_base / codec / bitrate_kbps
- `def fetch_channels(timeout: int = 15) -> list[dict]` — returns channel dicts `{id, title, description, image_url, streams: [{url, quality, position, codec, bitrate_kbps}, ...]}`
- `def import_stations(channels: list[dict], repo, on_progress=None) -> tuple[int, int]` — returns (inserted, skipped)
- `def _resolve_pls(pls_url: str, timeout: int = 10) -> list[str]` — returns ALL File= entries (5 ICE relay URLs); on failure returns `[pls_url]` fallback

Planned `musicstreamer/ui_qt/main_window.py` additions (Plan 03 will create):
- Module-scope class `_SomaImportWorker(QThread)` with `finished = Signal(int, int)` (inserted, skipped) + `error = Signal(str)`
- `MainWindow.__init__` adds menu action via `act_soma_import = self._menu.addAction("Import SomaFM")` and connects `triggered` to `self._on_soma_import_clicked` (bound method, NOT lambda)
- `MainWindow._on_soma_import_clicked(self) -> None`
- `MainWindow._on_soma_import_done(self, inserted: int, skipped: int) -> None`
- `MainWindow._on_soma_import_error(self, msg: str) -> None`
- Instance attribute `self._soma_import_worker: Optional[QThread]` initialized to `None`

Planned `musicstreamer/__main__.py` modification (Plan 03):
- After line 235, add `logging.getLogger("musicstreamer.soma_import").setLevel(logging.INFO)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register SOMA-01..SOMA-17 requirements in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the entire file — must see existing ART-MB-NN block as the formatting template, AND the Coverage block at lines 152-157 verbatim)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md (sections `<decisions>` D-01..D-16 + `<specifics>` lines 171–175 confirming `ART-04` NOT `STATION-ART-04` is the parking-lot ID and Phase 74 does NOT close it)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md (section `Open Questions` item 4 — planner picks 8–10 requirement IDs; this plan picks 17 to map 1:1 with the 17 validation tests for maximum traceability)
  </read_first>
  <action>
Insert a new SomaFM Catalog Import (SOMA) section in REQUIREMENTS.md positioned between the existing Versioning Convention (VER) section and the Future Requirements section. Mirror the Markdown formatting of the ART-MB block (header level `###`, list bullets `- [ ] **SOMA-NN**: …`).

Register exactly seventeen requirement IDs SOMA-01..SOMA-17:

- SOMA-01: Provider name pin — every imported SomaFM station has provider_name = "SomaFM" (CamelCase, no space, no period). Maps to CONTEXT D-02.
- SOMA-02: 4-tier × 5-relay stream scheme — each channel produces 20 stream rows whose (codec, quality, bitrate_kbps) tuples follow D-03's LOCKED map: (MP3, hi, 128) × 5 + (AAC, hi2, 128) × 5 + (AAC, med, 64) × 5 + (AAC, low, 32) × 5. Maps to CONTEXT D-03.
- SOMA-03: Position numbering — within each tier, position = tier_base * 10 + relay_index (tier_base = {hi:1, hi2:2, med:3, low:4}; relay_index = 1..5). Maps to CONTEXT D-03.
- SOMA-04: Codec normalization — format "aacp" stores codec field as "AAC" (NOT "AAC+", NOT "aacp"). Maps to CONTEXT D-03 + Phase 69 WIN-05 closure + stream_ordering._CODEC_RANK.
- SOMA-05: PLS resolution before store — stored stream URLs are the direct ice*.somafm.com URLs returned by playlist_parser.parse_playlist, NOT the .pls URLs from the API response. Maps to T-74-03 mitigation.
- SOMA-06: Dedup-by-URL skip — if ANY of a fetched channel's resolved stream URLs match an existing station's stream URL (any provider), the whole channel is skipped. Maps to CONTEXT D-05 + D-09.
- SOMA-07: Full no-op on re-import — running the import a second time on an already-imported library results in inserted=0, skipped=N; no station/stream/logo updates fire. Maps to CONTEXT D-05.
- SOMA-08: Logo failure non-fatal — when the per-channel image GET fails, the station + streams stay inserted and update_station_art is NOT called. Maps to CONTEXT D-11 + T-74-04 mitigation.
- SOMA-09: Per-channel exception isolation — a malformed channel dict (KeyError, etc.) inside the catalog loop increments skipped and the import continues. Maps to CONTEXT D-15 + T-74-05 mitigation.
- SOMA-10: Hamburger entry — MainWindow._menu contains a top-level "Import SomaFM" action wired to a bound method (no lambda). Maps to CONTEXT D-06 + QA-05.
- SOMA-11: Toast verbatim strings — click toast "Importing SomaFM…" (U+2026), finished toast "SomaFM import: {N} stations added" or "SomaFM import: no changes", error toast "SomaFM import failed: {truncated}" where truncation is `msg[:80] + "…"` when len(msg) > 80. Maps to CONTEXT D-06 + D-14.
- SOMA-12: Worker retention — clicking the menu action sets MainWindow._soma_import_worker to a non-None _SomaImportWorker; both _on_soma_import_done and _on_soma_import_error reset it to None. Maps to CONTEXT D-07 + Phase 60 SYNC-05.
- SOMA-13: User-Agent literal — outbound HTTP requests carry the literal UA "MusicStreamer/{version} (https://github.com/lightningjim/MusicStreamer)". Maps to CONTEXT Discretion + Phase 73 protocol convention + T-74-01 mitigation.
- SOMA-14: Source-grep gate for UA — the literal "MusicStreamer/" AND "https://github.com/lightningjim/MusicStreamer" both appear in musicstreamer/soma_import.py source.
- SOMA-15: Source-grep gate against AAC+ literal — no STORED codec value equals "AAC+" inside _TIER_BY_FORMAT_QUALITY in musicstreamer/soma_import.py.
- SOMA-16: Logger registration — musicstreamer/__main__.py registers musicstreamer.soma_import at logging.INFO. Maps to CONTEXT D-16.
- SOMA-17: Network failure is a clean toast — fetch_channels raising urllib.error.URLError, urllib.error.HTTPError, ValueError, or json.JSONDecodeError surfaces as the error toast (D-14 full abort), NOT as a partial import. Maps to CONTEXT D-14.

Also update the Traceability table at REQUIREMENTS.md:111-150: add seventeen new rows mapping `SOMA-01 ... SOMA-17` to `Phase 74` with status `Pending`. Place them after the existing ART-MB-16 row.

Update the Coverage block at REQUIREMENTS.md:152-157 verbatim. New numbers:
- "v2.1 requirements: 55 total" (38 + 17)
- "Mapped to phases: 55 ✓"
- "Unmapped: 0 ✓"
- "Complete: 20"
- "Pending: 35 (WIN-02 — SMTC Start Menu shortcut with AUMID; WIN-05 — AAC Win11 UAT; SOMA-01..SOMA-17 — Phase 74 in flight)"

Update the trailing "*Last updated:*" line to reflect 2026-05-14 and the SOMA-NN block addition.

Do NOT add a `STATION-ART-04` row — CONTEXT.md `<specifics>` line 175 explicitly states Phase 74 is a strict subset of `ART-04` (the parking-lot note in `.planning/notes/2026-04-03-station-art-fetching-beyond-youtube.md`) and does NOT close it.
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*SOMA-" .planning/REQUIREMENTS.md | grep -E "^17$" &amp;&amp; grep -c "| SOMA-" .planning/REQUIREMENTS.md | grep -E "^17$" &amp;&amp; grep -E "^- v2.1 requirements: 55 total" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- Pending: 35 " .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains exactly 17 `- [ ] **SOMA-` checklist rows.
    - REQUIREMENTS.md Traceability table contains 17 `| SOMA-NN | Phase 74 | Pending |`-shaped rows.
    - Coverage block line reads `- v2.1 requirements: 55 total`.
    - Coverage block line reads `- Mapped to phases: 55 ✓`.
    - Coverage block line reads `- Pending: 35 ` (count bumped by exactly 17 from 18).
    - `grep -c "STATION-ART" .planning/REQUIREMENTS.md` returns 0 (this plan does NOT close ART-04).
  </acceptance_criteria>
  <done>SOMA-01..SOMA-17 registered; Coverage rolled 38→55; Traceability rows added; ART-04 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create canonical 3-channel + dedup-hit JSON fixtures</name>
  <files>tests/fixtures/soma_channels_3ch.json, tests/fixtures/soma_channels_with_dedup_hit.json</files>
  <read_first>
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md (section "Code Examples → Operation 1" — verbatim live-probed shape of channels.json including the 4-playlist invariant)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md (section `tests/fixtures/soma_channels_3ch.json` — the literal JSON template)
    - tests/fixtures/ (directory listing — confirm JSON fixtures go alongside the existing mb_recording_search_*.json files)
  </read_first>
  <action>
Create two NEW JSON fixtures in `tests/fixtures/`. Both wrap the channels list inside a top-level `{"channels": [...]}` object — this matches the live SomaFM API contract verified 2026-05-13 (RESEARCH "Operation 1").

`tests/fixtures/soma_channels_3ch.json`: three discrete channels named "Groove Salad" (id `groovesalad`), "Drone Zone" (id `dronezone`), "Boot Liquor" (id `bootliquor`). Each channel carries the full 12-field shape from RESEARCH §"Operation 1" — `id`, `title`, `description`, `dj`, `djmail`, `genre`, `image` (e.g. `https://api.somafm.com/img/groovesalad120.png`), `largeimage`, `xlimage`, `twitter`, `updated`, `listeners`, `lastPlaying`. Each channel's `playlists` array contains exactly four entries in fixed order: `{url: ".../<id>256.pls", format: "mp3", quality: "highest"}`, `{url: ".../<id>130.pls", format: "aac", quality: "highest"}`, `{url: ".../<id>64.pls", format: "aacp", quality: "high"}`, `{url: ".../<id>32.pls", format: "aacp", quality: "low"}`. Use realistic PLS URLs under the `api.somafm.com` host. Omit the `preroll` field (RESEARCH note: ignored by importer).

`tests/fixtures/soma_channels_with_dedup_hit.json`: one channel (id `groovesalad`, title "Groove Salad") with the same 4-playlist shape. This fixture exists to be paired with a stubbed `repo.station_exists_by_url` returning True for the FIRST resolved ICE URL, so the test asserts the whole channel is skipped.

Both files end with a trailing newline. UTF-8 encoded.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; p=pathlib.Path('tests/fixtures'); a=json.loads((p/'soma_channels_3ch.json').read_text()); b=json.loads((p/'soma_channels_with_dedup_hit.json').read_text()); assert len(a['channels'])==3 and all(len(c['playlists'])==4 for c in a['channels']), 'a shape wrong'; assert len(b['channels'])==1 and len(b['channels'][0]['playlists'])==4, 'b shape wrong'; ids=[c['id'] for c in a['channels']]; assert ids==['groovesalad','dronezone','bootliquor'], f'unexpected ids {ids}'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; assert len(json.load(open('tests/fixtures/soma_channels_3ch.json'))['channels'])==3"` exits 0.
    - Every channel in `soma_channels_3ch.json` has exactly 4 entries in `playlists` with format/quality pairs `(mp3,highest)`, `(aac,highest)`, `(aacp,high)`, `(aacp,low)` in that order.
    - `soma_channels_with_dedup_hit.json` parses as JSON with one channel and 4 playlists.
    - Both files load via `json.load` without UnicodeDecodeError.
  </acceptance_criteria>
  <done>Two JSON fixtures land in tests/fixtures/, parseable and shape-correct.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_soma_import.py (RED — RESEARCH tests 1–9 + 14–15)</name>
  <files>tests/test_soma_import.py</files>
  <read_first>
    - tests/test_aa_import.py (read lines 1–120 for helpers `_urlopen_factory` + `_make_http_error`, lines 195–470 for mocked-urlopen + patch+_resolve_pls + assert call_count test patterns, lines 274–414 for multi-stream insert + dedup + logo-failure templates)
    - tests/test_cover_art_mb.py (lines 340–360 — `test_user_agent_string_literals_present` source-grep template for the UA gate)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md (section "tests/test_soma_import.py" — verbatim adaptation notes from each AA test pattern; lift the `_urlopen_factory` + `_make_http_error` helpers verbatim)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md (section "Phase Requirements → Test Map" — the 17-test validation matrix; this task implements tests 1–9 + 14–15)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md (`<decisions>` D-02, D-03, D-05, D-09, D-11, D-15)
  </read_first>
  <action>
Create `tests/test_soma_import.py` as a NEW pytest module. It MUST import `from musicstreamer import soma_import` — this import will fail at collection time until Plan 02 ships, which is the RED state. Tests assume Plan 02's planned public surface from this plan's `<interfaces>` block.

Lift verbatim from tests/test_aa_import.py: `_urlopen_factory(data: bytes, content_type: str = ...)` and `_make_http_error(code: int)`. Helper module path differs (`musicstreamer.soma_import` vs `musicstreamer.aa_import`).

Implement exactly these eleven test functions (mapping to RESEARCH tests #1, #2, #3, #4, #5, #6, #7, #8, #9, #14, #15):

1. `test_fetch_channels_parses_canonical_blob` — monkeypatch urllib.request.urlopen to return the soma_channels_3ch.json bytes; monkeypatch `_resolve_pls` to deterministically rewrite each .pls URL to a list of 5 `ice{i}.somafm.com/<id>-<bitrate>-<codec>` URLs in order; call `soma_import.fetch_channels()`; assert returned list length == 3 and each channel dict has keys `id`, `title`, `description`, `image_url`, `streams`.

2. `test_fetch_channels_maps_four_tiers_twenty_streams_per_channel` — same patches as test 1; assert the first channel's `streams` list contains exactly 20 entries; build a multiset of `(codec, quality, bitrate_kbps)` tuples and assert it equals `{('MP3','hi',128): 5, ('AAC','hi2',128): 5, ('AAC','med',64): 5, ('AAC','low',32): 5}`. (Maps SOMA-02 + SOMA-04.)

3. `test_fetch_channels_position_numbering_tier_base_times_ten` — same patches; for the first channel, assert that for each quality tier the five positions are `{tier_base*10 + 1, tier_base*10 + 2, ..., tier_base*10 + 5}` with `tier_base = {hi:1, hi2:2, med:3, low:4}`. (Maps SOMA-03.)

4. `test_aacp_codec_maps_to_AAC_not_aacplus` — same patches; collect all codec values across all streams of the first channel; assert no codec is the literal string `"AAC+"`; assert codec set is exactly `{"MP3", "AAC"}`. (Maps SOMA-04 + T-74-02.)

5. `test_resolve_pls_returns_all_five_direct_urls` — patch only `urllib.request.urlopen` (NOT `_resolve_pls`) with a content_type `"audio/x-scpls"` and a body containing five `File1=...` through `File5=...` ICE URLs (use the RESEARCH §"Operation 2" example body verbatim). Call `soma_import._resolve_pls("https://api.somafm.com/groovesalad.pls")` and assert the return is a list of 5 strings each starting with `https://ice` and none equal to the input .pls URL. (Maps SOMA-05 + T-74-03.)

6. `test_import_skips_when_url_exists` — load `soma_channels_with_dedup_hit.json` into channel-dict form using the same fetch_channels patches; instantiate a MagicMock repo whose `station_exists_by_url` returns True; call `soma_import.import_stations(channels, repo)`; assert `(inserted, skipped) == (0, 1)`; assert `repo.insert_station.assert_not_called()`. (Maps SOMA-06 + SOMA-07.)

7. `test_import_three_channels_full_path_creates_stations_and_streams` — load `soma_channels_3ch.json`; MagicMock repo with `station_exists_by_url=False`, `insert_station` returning incrementing ints (42, 43, 44), `list_streams` returning `[MagicMock(id=N)]`; call `import_stations`; assert `(inserted, skipped) == (3, 0)`; assert `repo.insert_station.call_count == 3`; assert `repo.insert_stream.call_count == 57` (3 channels × 19 streams; first stream per channel comes via insert_station auto-creation then update_stream). Each `insert_station` call has `provider_name="SomaFM"` and tags `""` (SOMA-01 / D-02 verbatim). (Maps SOMA-01 + general flow.)

8. `test_logo_failure_is_non_fatal` — load a one-channel fixture inline; MagicMock repo with `insert_station` returning 99 + `list_streams` returning `[MagicMock(id=200)]`; patch the logo `urllib.request.urlopen` to raise `Exception("network error")`; also patch `musicstreamer.soma_import.db_connect` + `musicstreamer.soma_import.Repo` (mirror tests/test_aa_import.py:385–414); call `import_stations`; assert `(inserted, skipped) == (1, 0)` AND `mock_thread_repo.update_station_art.assert_not_called()`. (Maps SOMA-08 + T-74-04.)

9. `test_per_channel_exception_skips_only_that_channel` — three channels in the fixture; make the second channel raise inside the body of the loop by giving it `playlists=None` (or an unexpected shape); MagicMock repo; assert `(inserted, skipped) == (2, 1)`; assert `repo.insert_station.call_count == 2`. (Maps SOMA-09 + T-74-05.)

10. `test_no_aacplus_codec_literal_in_source` — read `musicstreamer/soma_import.py` via `importlib.resources.files("musicstreamer").joinpath("soma_import.py").read_text(encoding="utf-8")`; strip Python comments (lines starting with `#` after lstrip) with `[l for l in lines if not l.lstrip().startswith("#")]` then rejoin; assert `'"AAC+"'` NOT in stripped source. (Maps SOMA-15 + T-74-02 — comment-strip is mandatory per `<critical_rules>` Nyquist hygiene.)

11. `test_user_agent_literal_present_in_source` — read soma_import.py the same way; assert `"MusicStreamer/"` substring present AND `"https://github.com/lightningjim/MusicStreamer"` substring present. (Maps SOMA-13 + SOMA-14 + T-74-01.)

Frame the `<filename, function-pattern, expected substring>` of each source-grep test inside the test itself; do NOT extract into a helper.

All eleven tests RED-fail on first run: `from musicstreamer import soma_import` is an `ImportError` until Plan 02 lands.

Test module header lists each function with the matching `SOMA-NN` + `T-74-NN` references in its docstring for traceability.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_soma_import.py --collect-only 2>&amp;1 | grep -E "ImportError.*soma_import|11 tests collected|9 errors" &amp;&amp; grep -c "^def test_" tests/test_soma_import.py | grep -E "^11$"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_soma_import.py contains exactly 11 `def test_` functions named per the action list.
    - Module-level `from musicstreamer import soma_import` (or equivalent) is present so the file collection RED-fails until Plan 02 lands.
    - Helpers `_urlopen_factory` and `_make_http_error` are present (lifted verbatim from tests/test_aa_import.py with module-path rename).
    - Test 7 asserts `repo.insert_station.call_count == 3` and `insert_stream.call_count == 57` (3 × 19).
    - Test 10 strips Python comment lines before checking for `"AAC+"` to avoid self-invalidating grep gate (project memory `feedback_gstreamer_mock_blind_spot.md`).
    - Running `uv run --with pytest pytest tests/test_soma_import.py --collect-only` yields a clear ImportError reference to `musicstreamer.soma_import`.
  </acceptance_criteria>
  <done>tests/test_soma_import.py exists with 11 RED tests; collection fails ONLY on the missing musicstreamer.soma_import import.</done>
</task>

<task type="auto">
  <name>Task 4: Create tests/test_main_window_soma.py (RED — RESEARCH tests 10–12 + 16)</name>
  <files>tests/test_main_window_soma.py</files>
  <read_first>
    - tests/test_main_window_gbs.py (read the ENTIRE 242-line file — `_FakePlayer` + `_FakeRepo` doubles at lines 25–117, `main_window` qtbot fixture at lines 123–139, `_find_action` helper, menu-entry/worker-start/toast tests at 146–227, and the lambda-ban source-grep test at 230–242)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md (section "tests/test_main_window_soma.py" — verbatim adaptation notes; SomaFM toast verbatim strings)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md (`<decisions>` D-06, D-07, D-14 — toast verbatim, worker retention, error format)
  </read_first>
  <action>
Create `tests/test_main_window_soma.py` as a NEW pytest-qt module. It MUST import the planned `_SomaImportWorker` class from `musicstreamer.ui_qt.main_window` — that import will RED-fail until Plan 03 lands.

Lift verbatim from tests/test_main_window_gbs.py: the `_FakePlayer` QObject double (lines 25–94 — includes all eight Signals + the no-op methods), the `_FakeRepo` class (lines 96–117), the `main_window(qtbot)` fixture, and the `_find_action(window, text)` helper. Module path rename only.

Implement exactly these six test functions (mapping to RESEARCH tests #10, #11, #12 (×3 toast cases), #16):

1. `test_import_soma_menu_entry_exists` — assert `_find_action(main_window, "Import SomaFM")` returns a non-None QAction AND `action.isEnabled()` is True. (Maps SOMA-10 + D-06.)

2. `test_import_soma_triggers_worker_start_and_retains_reference` — monkeypatch `_SomaImportWorker.__init__` + `_SomaImportWorker.start` so `started["flag"]` flips on start; call `main_window._on_soma_import_clicked()`; assert `started["flag"] is True` AND `main_window._soma_import_worker is not None`. (Maps SOMA-12 + SYNC-05.)

3. `test_import_soma_done_inserted_emits_added_toast` — monkeypatch `show_toast` to capture into a dict; monkeypatch `_refresh_station_list` to a no-op; set `main_window._soma_import_worker = MagicMock()`; call `main_window._on_soma_import_done(5, 0)`; assert captured text is the literal `"SomaFM import: 5 stations added"`; assert `main_window._soma_import_worker is None` after. (Maps SOMA-11a.)

4. `test_import_soma_done_zero_inserted_emits_no_changes_toast` — same pattern; call `_on_soma_import_done(0, 46)`; assert captured text is the literal `"SomaFM import: no changes"`. (Maps SOMA-11b.)

5. `test_import_soma_error_truncates_message_at_80_chars` — capture toast text; call `_on_soma_import_error("x" * 200)`; assert the captured text starts with `"SomaFM import failed: "` AND contains the trailing `"…"` (U+2026) AND total length is exactly `len("SomaFM import failed: ") + 80 + len("…")`. Also call `_on_soma_import_error("short")`; assert exactly `"SomaFM import failed: short"` (no `"…"`). Both calls clear `_soma_import_worker` to None. (Maps SOMA-11c + D-14.)

6. `test_no_self_capturing_lambda_in_soma_action` — open `musicstreamer/ui_qt/main_window.py`, run `re.findall(r"act_soma_import\.triggered\.connect\(([^)]+)\)", src)`; assert matches non-empty; for each match assert `"lambda"` NOT in match AND match.strip() starts with `"self."`. (Maps SOMA-10 + QA-05 — mirrors tests/test_main_window_gbs.py:230–242 verbatim with rename.)

The click handler verifies the literal click toast in a separate function `test_import_soma_click_emits_importing_toast`: capture toast, call `main_window._on_soma_import_clicked()` (monkeypatch `_SomaImportWorker.start` to a no-op so the thread doesn't actually run), assert captured text is the literal `"Importing SomaFM…"` (U+2026 ellipsis at the end).

Total: 7 test functions. Each has a docstring citing the SOMA-NN it pins.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_main_window_soma.py --collect-only 2>&amp;1 | grep -E "ImportError|7 tests collected" &amp;&amp; grep -c "^def test_" tests/test_main_window_soma.py | grep -E "^7$"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_main_window_soma.py contains exactly 7 `def test_` functions matching the action list (six numbered + one extra `test_import_soma_click_emits_importing_toast`).
    - Module imports `_SomaImportWorker` from `musicstreamer.ui_qt.main_window` so collection RED-fails until Plan 03 lands.
    - `_FakePlayer` and `_FakeRepo` doubles are present (lifted verbatim from tests/test_main_window_gbs.py — same Signal list, same no-op methods).
    - The lambda-ban test uses regex `r"act_soma_import\.triggered\.connect\(([^)]+)\)"` exactly.
    - Toast assertions match the literal Unicode strings character-for-character (U+2026 horizontal ellipsis, no ASCII three-dot fallback).
  </acceptance_criteria>
  <done>tests/test_main_window_soma.py exists with 7 RED tests; collection fails ONLY on the missing `_SomaImportWorker` import.</done>
</task>

<task type="auto">
  <name>Task 5: Extend tests/test_constants_drift.py with two new drift-guards</name>
  <files>tests/test_constants_drift.py</files>
  <read_first>
    - tests/test_constants_drift.py (read the entire 108-line file — see the existing source-grep test pattern at lines 82–108, the `pkg_root = Path(__file__).parent.parent / "musicstreamer"` idiom, and how the file already structures `EXPECTED_*_COUNT` constants)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md (section "tests/test_constants_drift.py (MODIFY)" — the two new tests verbatim)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md (tests #13 and #17 — soma_import logger registration + SOMA-NN requirement IDs registration)
  </read_first>
  <action>
Append two new test functions to `tests/test_constants_drift.py`. Do NOT remove or rename anything existing. Mirror the existing module style (Path-based reads, literal-substring `assert needle in text` assertions with descriptive failure messages).

1. `test_soma_import_logger_registered` — Read `musicstreamer/__main__.py` via `Path(__file__).parent.parent / "musicstreamer" / "__main__.py"`. Assert the LITERAL substring `'logging.getLogger("musicstreamer.soma_import").setLevel(logging.INFO)'` (double-quoted exactly as Python source emits it) appears in the read text. Failure message references D-16. (Maps SOMA-16 + RESEARCH test #17.)

2. `test_soma_nn_requirements_registered` — Read `.planning/REQUIREMENTS.md` via `Path(__file__).parent.parent / ".planning" / "REQUIREMENTS.md"`. For each of `["SOMA-01", "SOMA-02", "SOMA-03", "SOMA-04", "SOMA-05", "SOMA-06", "SOMA-07", "SOMA-08", "SOMA-09", "SOMA-10", "SOMA-11", "SOMA-12", "SOMA-13", "SOMA-14", "SOMA-15", "SOMA-16", "SOMA-17"]`, assert the ID literal appears in the file with a clear per-ID failure message (e.g. `f"REQUIREMENTS.md missing {req_id} after Phase 74"`). (Maps RESEARCH test #13.)

Both tests run as plain functions (no pytest fixtures). The first RED-fails until Plan 03 modifies `__main__.py`. The second goes GREEN as soon as Task 1 (this plan) lands — verifies that Plan 01 itself completed correctly without requiring downstream plans.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_constants_drift.py::test_soma_nn_requirements_registered -x 2>&amp;1 | grep -E "1 passed" &amp;&amp; uv run --with pytest pytest tests/test_constants_drift.py::test_soma_import_logger_registered -x 2>&amp;1 | grep -E "AssertionError|1 failed"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_constants_drift.py contains both new functions `test_soma_import_logger_registered` and `test_soma_nn_requirements_registered`.
    - `pytest tests/test_constants_drift.py::test_soma_nn_requirements_registered -x` PASSES (because Task 1 already registered SOMA-01..SOMA-17).
    - `pytest tests/test_constants_drift.py::test_soma_import_logger_registered -x` FAILS with an AssertionError citing D-16 (because __main__.py is not yet modified — that lands in Plan 03).
    - Existing test functions in test_constants_drift.py are unchanged (no rename, no delete).
  </acceptance_criteria>
  <done>Two drift-guards appended; SOMA-NN drift PASSES now, soma_import-logger drift RED-pending until Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → REQUIREMENTS.md | Local edit; no external input — but downstream automation (drift-guards in tests/test_constants_drift.py) consumes the file. A typo in the ID literal silently breaks Plans 02/03's grep-based verification. |
| disk → test fixtures | JSON fixtures are read by pytest with `json.load` — a malformed shape passes JSON parse but breaks tests with confusing KeyError stacks at the import-stations call site. |
| Python source → grep gate | Comment-bearing source files (`musicstreamer/soma_import.py`) get scanned by `test_no_aacplus_codec_literal_in_source`. If the test fails to strip `#`-prefixed lines, comments can self-invalidate the gate (project memory `feedback_gstreamer_mock_blind_spot.md`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | I (Information disclosure / fingerprinting) | `soma_import.fetch_channels` outbound HTTP | mitigate | Wave 0 test `test_user_agent_literal_present_in_source` (Task 3, test #11) asserts the literal `"MusicStreamer/"` AND `"https://github.com/lightningjim/MusicStreamer"` appear in `musicstreamer/soma_import.py` source. Plan 02 must satisfy this gate. |
| T-74-02 | T (Tampering / data integrity) | `_TIER_BY_FORMAT_QUALITY` codec field | mitigate | Wave 0 test `test_aacp_codec_maps_to_AAC_not_aacplus` (Task 3, test #4) + `test_no_aacplus_codec_literal_in_source` (Task 3, test #10, with mandatory comment-stripping per `<critical_rules>`) double-gate the literal `"AAC+"` from appearing as a stored codec value — both behavioral assertion AND source-grep gate. Aligns with Phase 47 gap-07 + stream_ordering._CODEC_RANK guarantees. |
| T-74-03 | T (Tampering / data integrity) | Stored `station_streams.url` values | mitigate | Wave 0 test `test_resolve_pls_returns_all_five_direct_urls` (Task 3, test #5) asserts ALL returned URLs start with `https://ice` (NOT `.pls`). Plan 02 must implement `_resolve_pls` via `playlist_parser.parse_playlist`, NOT store the input PLS URL on error. D-05 dedup correctness depends on this. |
| T-74-04 | D (Denial-of-service / cascading failure) | Logo download phase | mitigate | Wave 0 test `test_logo_failure_is_non_fatal` (Task 3, test #8) asserts station + streams survive a raised `urllib.error.URLError` and `update_station_art` is NOT called. Plan 02 must wrap the logo body in `try/except`. |
| T-74-05 | D (Denial-of-service / cascading failure) | Per-channel iteration in `import_stations` | mitigate | Wave 0 test `test_per_channel_exception_skips_only_that_channel` (Task 3, test #9) asserts one bad channel only increments `skipped`. Plan 02 MUST add a per-channel `try/except` wrapper inside the loop body (RESEARCH Pitfall 2 — AA does NOT have this; Phase 74 deviates from AA here). |
</threat_model>

<verification>
1. `grep -c "^- \[ \] \*\*SOMA-" .planning/REQUIREMENTS.md` returns `17`.
2. `python -c "import json; assert len(json.load(open('tests/fixtures/soma_channels_3ch.json'))['channels'])==3 and len(json.load(open('tests/fixtures/soma_channels_with_dedup_hit.json'))['channels'])==1"` exits 0.
3. `uv run --with pytest pytest tests/test_soma_import.py --collect-only` reports `ImportError: cannot import name 'soma_import' from 'musicstreamer'` (or equivalent), confirming 11 tests would collect once Plan 02 ships.
4. `uv run --with pytest pytest tests/test_main_window_soma.py --collect-only` reports `ImportError` on `_SomaImportWorker`, confirming 7 tests would collect once Plan 03 ships.
5. `uv run --with pytest pytest tests/test_constants_drift.py::test_soma_nn_requirements_registered -x` PASSES.
6. `uv run --with pytest pytest tests/test_constants_drift.py::test_soma_import_logger_registered -x` FAILS (expected RED for Plan 03).
7. Full test suite: existing pre-Phase-74 tests do not regress. Wave-0 RED count: 11 (soma_import) + 7 (main_window_soma) + 1 (logger drift) = **19 RED tests pending Plans 02/03**, plus 1 new GREEN (`test_soma_nn_requirements_registered`).
</verification>

<success_criteria>
- [ ] REQUIREMENTS.md has 17 new SOMA-NN rows + Traceability table updated + Coverage block bumped 38→55 / 18→35.
- [ ] tests/fixtures/soma_channels_3ch.json + soma_channels_with_dedup_hit.json exist with shape verified.
- [ ] tests/test_soma_import.py exists with 11 test functions; collection RED-fails on missing musicstreamer.soma_import.
- [ ] tests/test_main_window_soma.py exists with 7 test functions; collection RED-fails on missing `_SomaImportWorker`.
- [ ] tests/test_constants_drift.py extended with 2 new functions; `test_soma_nn_requirements_registered` passes immediately; `test_soma_import_logger_registered` red-pends Plan 03.
- [ ] STATION-ART-04 is NOT introduced in REQUIREMENTS.md (CONTEXT specifics line 175 — only `ART-04` exists, and Phase 74 does NOT close it).
- [ ] No regression to existing test suite (`uv run --with pytest pytest -x` baseline unchanged except for the 19 newly added RED tests + 1 new GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-01-SUMMARY.md`.
</output>
