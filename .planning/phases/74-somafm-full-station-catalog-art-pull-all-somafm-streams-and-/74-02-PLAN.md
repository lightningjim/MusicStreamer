---
phase: 74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-
plan: 02
type: execute
wave: 2
depends_on: [74-01]
files_modified:
  - musicstreamer/soma_import.py
autonomous: true
requirements:
  - SOMA-01
  - SOMA-02
  - SOMA-03
  - SOMA-04
  - SOMA-05
  - SOMA-06
  - SOMA-07
  - SOMA-08
  - SOMA-09
  - SOMA-13
  - SOMA-14
  - SOMA-15
  - SOMA-17

must_haves:
  truths:
    - "D-01 + D-10: Each SomaFM channel inserts atomically — station row + ALL stream rows + queued logo target — or not at all (channel-level skip on dedup or per-channel exception)."
    - "D-02: insert_station is called with provider_name='SomaFM' (CamelCase literal, no space, no period) for every imported channel."
    - "D-03: fetch_channels expands each of the 4 (format, quality) tiers into 5 ICE-relay streams via _resolve_pls, producing 20 stream dicts per channel; position = tier_base*10 + relay_index."
    - "D-03 codec mapping: format 'mp3' → 'MP3', format 'aac' → 'AAC', format 'aacp' → 'AAC' (never 'AAC+'; never 'aacp' as a stored codec value)."
    - "D-05 + D-09: import_stations checks repo.station_exists_by_url for ANY of the channel's 20 stream URLs; on match the channel is skipped, no station/stream/logo write fires."
    - "D-10 ordering: insert_station first, then update_stream on the auto-created first-stream row to backfill codec/quality/bitrate metadata, then insert_stream for the remaining 19 streams, then queue (station_id, image_url) for the logo phase."
    - "D-11: Logo download phase uses ThreadPoolExecutor(max_workers=8); the inner _download_logo body swallows ALL exceptions — station + streams stay inserted, update_station_art is skipped on failure."
    - "D-14: fetch_channels raises ValueError('no_channels') on empty catalog; urllib/json exceptions propagate to the worker layer."
    - "D-15 — Phase 74 DEVIATES from AA: the `for ch in channels:` loop body inside import_stations is wrapped in try/except so a malformed channel only increments skipped and the import continues. AA's aa_import.py has no such wrapper; Phase 74 must ADD it (RESEARCH Pitfall 2)."
    - "T-74-01: outbound urllib.request.Request carries User-Agent header literal 'MusicStreamer/{importlib.metadata.version(\"musicstreamer\")} (https://github.com/lightningjim/MusicStreamer)' — derived at module load."
    - "T-74-02: _TIER_BY_FORMAT_QUALITY dict values use codec literal 'MP3' or 'AAC' only; the string 'AAC+' does NOT appear as a stored codec value in soma_import.py."
    - "T-74-03: stored station_streams.url values are direct ice*.somafm.com URLs returned by playlist_parser.parse_playlist, NOT the .pls input URLs (dedup-by-URL correctness)."
  artifacts:
    - path: "musicstreamer/soma_import.py"
      provides: "Public API fetch_channels + import_stations + module-level _TIER_BY_FORMAT_QUALITY + _USER_AGENT + _resolve_pls helper"
      min_lines: 240
      contains:
        - "def fetch_channels"
        - "def import_stations"
        - "def _resolve_pls"
        - "_TIER_BY_FORMAT_QUALITY"
        - "MusicStreamer/"
        - "https://github.com/lightningjim/MusicStreamer"
        - "provider_name=\"SomaFM\""
  key_links:
    - from: "musicstreamer/soma_import.py"
      to: "musicstreamer.playlist_parser.parse_playlist"
      via: "import inside _resolve_pls"
      pattern: "from musicstreamer\\.playlist_parser import parse_playlist"
    - from: "musicstreamer/soma_import.py"
      to: "musicstreamer.assets.copy_asset_for_station"
      via: "logo persistence path"
      pattern: "from musicstreamer\\.assets import copy_asset_for_station"
    - from: "musicstreamer/soma_import.py"
      to: "musicstreamer.repo.Repo + db_connect"
      via: "per-thread Repo for logo update_station_art"
      pattern: "Repo\\(db_connect\\(\\)\\)"
---

<objective>
Phase 74 / Wave 2 GREEN tier 1 of 2: Create `musicstreamer/soma_import.py` so the Wave-0 RED test suite (tests/test_soma_import.py, 11 tests) and the SOMA-NN drift-guards turn GREEN.

Purpose: Land the public-API client + bulk importer that mirrors `aa_import.py` verbatim where possible (PLS resolution, logo download, dedup-by-URL, atomic per-channel insert) while DEVIATING in two places: (a) no `listen_key` / no `_fetch_image_map` (SomaFM is public + has inline image URLs), and (b) ADD a per-channel try/except wrapper inside the catalog loop (D-15 — AA does not have this; Pitfall 2).

Output: a single new file `musicstreamer/soma_import.py` that turns tests 1–9 + 14–15 in tests/test_soma_import.py from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-RESEARCH.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-01-PLAN.md
@.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-01-SUMMARY.md

@musicstreamer/aa_import.py
@musicstreamer/cover_art_mb.py
@musicstreamer/playlist_parser.py
@musicstreamer/assets.py
@musicstreamer/repo.py
@tests/test_soma_import.py

<interfaces>
Existing primitives this plan calls (no signature changes — DO NOT edit these files):

From `musicstreamer/repo.py` (Repo methods):
- `Repo.insert_station(name: str, url: str, provider_name: str = "", tags: str = "", ...) -> int` — returns the new station_id; auto-creates ONE station_streams row at `position=1` with the given url
- `Repo.list_streams(station_id: int) -> list[StationStream]` — used to grab the auto-created stream row's id
- `Repo.update_stream(stream_id: int, url: str, label: str, quality: str, position: int, stream_type: str, codec: str, *, bitrate_kbps: int = 0) -> None`
- `Repo.insert_stream(station_id: int, url: str, *, label: str = "", quality: str = "", position: int = 0, stream_type: str = "shoutcast", codec: str = "", bitrate_kbps: int = 0) -> int`
- `Repo.station_exists_by_url(url: str) -> bool`
- `Repo.update_station_art(station_id: int, art_path: str) -> None`

From `musicstreamer/playlist_parser.py`:
- `parse_playlist(body: str, content_type: str = "", url_hint: str = "") -> list[dict]` — returns list of `{"url": str, ...}` entries; PLS bodies yield ALL File= entries in file order.

From `musicstreamer/assets.py`:
- `copy_asset_for_station(station_id: int, src_path: str, name: str) -> str` — returns relative path used by `update_station_art`.

From `musicstreamer/repo.py`:
- `db_connect() -> sqlite3.Connection` — used inside `_download_logo` to construct a per-thread `Repo` (SQLite forbids cross-thread connection sharing).

From `importlib.metadata`:
- `version("musicstreamer")` — used to interpolate the running MusicStreamer version into the User-Agent string at module load. Wrap in try/except to fall back to `"0.0.0"` (mirrors cover_art_mb.py:68-83).

This plan's `_resolve_pls(pls_url, timeout=10) -> list[str]` MUST return ALL File= entries (D-03 LOCKED: 5 ICE relays per tier). Mirror aa_import.py:23–47 verbatim with one change: return ALL entries, not just the first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create musicstreamer/soma_import.py module shell + module-level constants + UA + _resolve_pls + fetch_channels</name>
  <files>musicstreamer/soma_import.py</files>
  <read_first>
    - musicstreamer/aa_import.py (read the ENTIRE file — primary analog; this task lifts lines 1–186 with the documented deviations)
    - musicstreamer/cover_art_mb.py (lines 60–95 — `_USER_AGENT` construction via importlib.metadata, the literal Phase 74 SOMA-13/14 reuses verbatim)
    - musicstreamer/playlist_parser.py (read entire file to understand the `parse_playlist` content-type sniff + return shape)
    - tests/test_soma_import.py (just landed in Plan 01 — read the 11 test functions; tests 1–5 + 10–11 in particular constrain this task's contract)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md (sections "Module-level tier constants pattern", "User-Agent constant pattern", "PLS resolution helper pattern", "Catalog fetch pattern")
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md (`<decisions>` D-03 — full 4-tier × 5-relay LOCKED mapping including position formula)
  </read_first>
  <action>
Create `musicstreamer/soma_import.py` as a NEW module. File must contain, in this order:

1. **Module docstring + imports.** Docstring lists the public API (`fetch_channels`, `import_stations`). Imports: `json`, `logging`, `os`, `tempfile`, `urllib.error`, `urllib.request`, `from concurrent.futures import ThreadPoolExecutor, as_completed`, `from importlib.metadata import version as _pkg_version`, `from musicstreamer.assets import copy_asset_for_station`, `from musicstreamer.repo import db_connect, Repo`. Do NOT import `re` (no AA-CDN URL templating). Do NOT import `playlist_parser` at module top — import it inside `_resolve_pls` (mirror aa_import.py:39).

2. **Module-level constants.**
   - `_log = logging.getLogger(__name__)` (mirrors aa_import.py:20).
   - `_API_URL = "https://api.somafm.com/channels.json"` (RESEARCH Open Question 5 LOCKED).
   - `_TIMEOUT_S = 15` (AA convention).
   - `_USER_AGENT` constructed exactly like cover_art_mb.py:68–83 — wrap `_pkg_version("musicstreamer")` in try/except defaulting to `"0.0.0"`, then build the f-string `f"MusicStreamer/{_MS_VERSION} (https://github.com/lightningjim/MusicStreamer)"`. Both literals `"MusicStreamer/"` and `"https://github.com/lightningjim/MusicStreamer"` MUST appear in source (SOMA-14 source-grep gate).
   - `_TIER_BY_FORMAT_QUALITY` — dict keyed by `(format, quality)` tuples; values are dicts with keys `quality`, `tier_base`, `codec`, `bitrate_kbps`. EXACT contents per CONTEXT D-03 LOCKED:
     - `("mp3",  "highest"): {"quality": "hi",  "tier_base": 1, "codec": "MP3", "bitrate_kbps": 128}`
     - `("aac",  "highest"): {"quality": "hi2", "tier_base": 2, "codec": "AAC", "bitrate_kbps": 128}`
     - `("aacp", "high"):    {"quality": "med", "tier_base": 3, "codec": "AAC", "bitrate_kbps": 64}`
     - `("aacp", "low"):     {"quality": "low", "tier_base": 4, "codec": "AAC", "bitrate_kbps": 32}`
   - Inline comment block above the dict cites `Phase 74 D-03 LOCKED 2026-05-13` + tier→quality table + position formula. Comment lines do NOT contain the literal `"AAC+"` (SOMA-15 source-grep gate works on stripped source, but keep it clean regardless).

3. **`_resolve_pls(pls_url: str, timeout: int = 10) -> list[str]`** — lift aa_import.py:23–47 verbatim with one behavioral change: keep returning `[e["url"] for e in entries]` (the FULL list of File= entries, not just the first). On `Exception`, fall back to `[pls_url]` (matches AA). Pass through the same `Request(pls_url, headers={"User-Agent": _USER_AGENT})` shape so the UA goes out on PLS GETs too. `parse_playlist` import remains inside the function body.

4. **`fetch_channels(timeout: int = _TIMEOUT_S) -> list[dict]`** — public API. Build `urllib.request.Request(_API_URL, headers={"User-Agent": _USER_AGENT})`; call `urlopen(req, timeout=timeout)`; parse `json.loads(resp.read())`; extract `data.get("channels", [])`. Raise `ValueError("no_channels")` if empty (D-14 catalog-empty signal).

   For each channel dict `ch` in the input list, wrap the body in `try/except Exception as exc:` (D-15 per-channel best-effort — Pitfall 2). On exception: `_log.warning("Skipping SomaFM channel %s: %s", ch.get("id"), exc); continue`. Inside the try body:
   - Build `streams: list[dict] = []` by iterating `ch.get("playlists", [])`.
   - For each playlist entry `pl`, look up `tier_meta = _TIER_BY_FORMAT_QUALITY.get((pl.get("format"), pl.get("quality")))`. If `tier_meta is None`, `continue` (silently skip unknown tiers per Pitfall 6).
   - Call `relay_urls = _resolve_pls(pl["url"])`. For `relay_index, relay_url in enumerate(relay_urls, start=1)`, append `{"url": relay_url, "quality": tier_meta["quality"], "position": tier_meta["tier_base"] * 10 + relay_index, "codec": tier_meta["codec"], "bitrate_kbps": tier_meta["bitrate_kbps"]}` to streams.
   - If `streams` is empty after the loop, `continue` (drop the channel).
   - Append to `out`: `{"id": ch["id"], "title": ch["title"], "description": ch.get("description", ""), "image_url": ch.get("image"), "streams": streams}`. Use `ch.get("image")` NOT `ch.get("xlimage")` (RESEARCH "Alternatives Considered" — 120px to match AA logo dimensionality).

   Return `out`.

NEVER inline a fenced code block here; the production source goes in the file, not the plan. Comments inside the source file should reference CONTEXT D-NN anchors.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_soma_import.py::test_fetch_channels_parses_canonical_blob tests/test_soma_import.py::test_fetch_channels_maps_four_tiers_twenty_streams_per_channel tests/test_soma_import.py::test_fetch_channels_position_numbering_tier_base_times_ten tests/test_soma_import.py::test_aacp_codec_maps_to_AAC_not_aacplus tests/test_soma_import.py::test_resolve_pls_returns_all_five_direct_urls tests/test_soma_import.py::test_no_aacplus_codec_literal_in_source tests/test_soma_import.py::test_user_agent_literal_present_in_source -x</automated>
  </verify>
  <acceptance_criteria>
    - File `musicstreamer/soma_import.py` exists with a top-level module docstring.
    - `grep -c "_TIER_BY_FORMAT_QUALITY" musicstreamer/soma_import.py` ≥ 2 (one definition + at least one use).
    - `grep "MusicStreamer/" musicstreamer/soma_import.py` returns at least one match.
    - `grep "https://github.com/lightningjim/MusicStreamer" musicstreamer/soma_import.py` returns at least one match.
    - When Python comment lines are stripped (`grep -v '^\s*#' musicstreamer/soma_import.py | grep '"AAC+"'`) the result is EMPTY (zero matches).
    - Wave-0 tests 1, 2, 3, 4, 5, 10, 11 (the seven listed in `<verify>`) all PASS.
    - `from musicstreamer.playlist_parser import parse_playlist` appears inside `_resolve_pls`, not at module top.
  </acceptance_criteria>
  <done>Module shell + constants + UA + _resolve_pls + fetch_channels GREEN; 7 of 11 Wave-0 soma_import tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement import_stations + _download_logos in soma_import.py</name>
  <files>musicstreamer/soma_import.py</files>
  <read_first>
    - musicstreamer/soma_import.py (the file Task 1 just created — confirm the module-level constants and helper are present)
    - musicstreamer/aa_import.py (lines 189–290 — `import_stations_multi` is the primary analog; this task lifts it almost verbatim with the documented deviations)
    - tests/test_soma_import.py (tests 6, 7, 8, 9 — the four tests this task must turn GREEN)
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-PATTERNS.md (section "Importer pattern (dedup-by-URL + atomic per-channel insert)" + section "Per-channel try/except wrap (D-15 — new for Phase 74)")
    - .planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-CONTEXT.md (`<decisions>` D-02, D-05, D-09, D-10, D-11, D-15)
  </read_first>
  <action>
Append two new top-level functions to `musicstreamer/soma_import.py`:

1. **`import_stations(channels: list[dict], repo, on_progress=None) -> tuple[int, int]`** — public API. Initialize `imported = 0`, `skipped = 0`, `logo_targets: list[tuple[int, str]] = []`. Iterate `for ch in channels:`. Wrap the ENTIRE loop body in `try/except Exception as exc:` so a malformed channel only increments `skipped` and the loop continues (D-15 — explicit deviation from aa_import.py:189–251 which lacks this wrapper; this is RESEARCH Pitfall 2 mitigation and the SOLE place Phase 74 differs structurally from AA).

   Inside the try body:
   - If `not ch.get("streams")`: `skipped += 1; _log.warning("Skipping SomaFM channel with no streams: %s", ch.get("title", "<unnamed>")); continue`.
   - Check `any_exists = any(repo.station_exists_by_url(s["url"]) for s in ch["streams"])`. If True: `skipped += 1` and skip to `on_progress`.
   - Else:
     - `first_url = ch["streams"][0]["url"]`.
     - `station_id = repo.insert_station(name=ch["title"], url=first_url, provider_name="SomaFM", tags="")` — provider_name literal `"SomaFM"` per CONTEXT D-02.
     - For each stream `s in ch["streams"]`:
       - If `s["url"] == first_url`: get `streams = repo.list_streams(station_id)`; if streams non-empty, call `repo.update_stream(streams[0].id, s["url"], "", s["quality"], s["position"], "shoutcast", s["codec"], bitrate_kbps=s["bitrate_kbps"])` — backfills codec/quality/bitrate on the auto-created row.
       - Else: `repo.insert_stream(station_id, s["url"], label="", quality=s["quality"], position=s["position"], stream_type="shoutcast", codec=s["codec"], bitrate_kbps=s["bitrate_kbps"])`.
     - `imported += 1`.
     - If `ch.get("image_url")`: `logo_targets.append((station_id, ch["image_url"]))`.

   After the try body (still inside `for ch`), call `if on_progress: on_progress(imported, skipped)`.

   In the `except` branch: `_log.warning("SomaFM channel %s import skipped: %s", ch.get("id"), exc); skipped += 1` (note we are not re-running `on_progress` from the except — matches AA loop position; the outer `if on_progress` call lives at the bottom of the `for` body so it fires after both success and exception paths only if it lives there in your refactor; safer to place `if on_progress: on_progress(imported, skipped)` INSIDE both branches of the try/except OR as the last statement of the outer for-body. Use the second placement.).

   After the loop: call internal `_download_logos(logo_targets)` (D-11 best-effort). Return `(imported, skipped)`.

2. **`_download_logos(logo_targets: list[tuple[int, str]]) -> None`** — internal. If `not logo_targets`: return. Otherwise define a nested function `def _download_logo(station_id: int, image_url: str) -> None:` that lifts aa_import.py:244–262 verbatim — outer try/except wraps the urlopen + tempfile + copy_asset_for_station + per-thread `Repo(db_connect())` + `update_station_art` + finally-unlink dance; bare `except Exception: pass` swallows all failures (D-11 logo failure non-fatal).
   - The outbound `urllib.request.urlopen(image_url, timeout=15)` MUST be wrapped in `urllib.request.Request(image_url, headers={"User-Agent": _USER_AGENT})` so logo GETs carry the same UA as catalog/PLS GETs.

   Use `with ThreadPoolExecutor(max_workers=8) as executor:` + `as_completed(...)` to dispatch ~46 logo downloads concurrently. Match aa_import.py:266–271 pattern; drop the `on_logo_progress` callback path entirely (D-08: no progress bar).

After both functions exist, the public surface of `musicstreamer/soma_import.py` is exactly: `fetch_channels`, `import_stations`, `_resolve_pls` (private but module-scope so tests can patch it). All other helpers (`_download_logos`, `_download_logo`) are private.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_soma_import.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 11 tests in `tests/test_soma_import.py` PASS (RED → GREEN).
    - `grep -c "def import_stations" musicstreamer/soma_import.py` returns 1.
    - `grep -c "def _download_logos" musicstreamer/soma_import.py` returns 1.
    - `grep "provider_name=\"SomaFM\"" musicstreamer/soma_import.py` returns at least one match (CONTEXT D-02 literal).
    - `grep -c "ThreadPoolExecutor" musicstreamer/soma_import.py` returns 1 (logo download concurrency).
    - The per-channel try/except wrapper exists inside the `for ch in channels:` loop body of `import_stations` (RESEARCH Pitfall 2 / D-15). Verify via: `python -c "import ast,sys; t=ast.parse(open('musicstreamer/soma_import.py').read()); imp=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='import_stations'][0]; loops=[n for n in ast.walk(imp) if isinstance(n,ast.For)]; print(any(any(isinstance(c,ast.Try) for c in ast.walk(l)) for l in loops))"` prints `True`.
    - `uv run --with pytest pytest tests/test_aa_import.py -x` continues to pass (no regression to existing AA importer).
  </acceptance_criteria>
  <done>11/11 tests in test_soma_import.py GREEN; import_stations + _download_logos land; per-channel try/except verified via AST scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → soma_import.fetch_channels | Untrusted JSON from api.somafm.com:443 enters the Python process. Malformed JSON / unexpected schema must not crash the whole import (D-14 outer fence + D-15 per-channel fence). |
| network → _resolve_pls | Untrusted PLS body parsed by playlist_parser.parse_playlist. A malformed body returns empty list and the helper falls back to `[pls_url]` (mirrors AA). |
| network → _download_logo | Untrusted bytes written to tempfile and then to assets/<sid>/. `copy_asset_for_station` is the trust boundary — it controls the destination path so a maliciously-named image URL cannot escape the assets dir. |
| Python source → grep gate (T-74-02) | Stored codec values in `_TIER_BY_FORMAT_QUALITY` must NOT include `"AAC+"`. Wave-0 test #10 strips comment lines before checking, so comments may use any wording. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | I (Information disclosure / SomaFM may ban us) | outbound HTTPS to api.somafm.com + ice*.somafm.com + img.somafm.com | mitigate | Module-level `_USER_AGENT` constant built from `importlib.metadata.version("musicstreamer")` (mirrors cover_art_mb.py:68-83). All three outbound HTTP sites (`fetch_channels`, `_resolve_pls`, `_download_logo`) wrap the URL in `urllib.request.Request(url, headers={"User-Agent": _USER_AGENT})`. Wave-0 source-grep test #11 (SOMA-13 / SOMA-14) blocks the gate. |
| T-74-02 | T (codec literal regression — Phase 47 gap-07 history) | `_TIER_BY_FORMAT_QUALITY` codec field | mitigate | The dict maps `("aacp", "high") → "AAC"` and `("aacp", "low") → "AAC"` (NOT `"AAC+"`). Wave-0 behavioral test #4 + source-grep test #10 (with mandatory comment-strip) double-gate this. Phase 69 WIN-05 closure already verified HE-AAC decodes via the same aacparse + avdec_aac chain. |
| T-74-03 | T (PLS-stored-as-stream → unplayable + dedup-broken) | `_resolve_pls` return value used by `fetch_channels` | mitigate | `_resolve_pls` returns the LIST of `parse_playlist`-extracted File= URLs; only on full exception does it fall back to `[pls_url]` (matches AA semantics). Wave-0 test #5 asserts all 5 returned URLs start with `https://ice` and none equal the input .pls URL. |
| T-74-04 | D (logo failure cascades to station insert loss) | `_download_logo` inner body | mitigate | Outer `try/except Exception: pass` swallows ALL failures including network, filesystem, and SQLite errors. Wave-0 test #8 asserts station survives + update_station_art is NOT called when urlopen raises. |
| T-74-05 | D (cross-channel exception aborts entire batch — RESEARCH Pitfall 2) | `for ch in channels:` loop in `import_stations` | mitigate | Explicit `try/except Exception as exc: skipped += 1; _log.warning(...)` wrapper inside the loop body. Wave-0 test #9 asserts one bad channel only increments skipped. AST-scan acceptance criterion verifies the wrapper exists at the loop level. |
</threat_model>

<verification>
1. `uv run --with pytest pytest tests/test_soma_import.py -x` reports 11 passed, 0 failed.
2. `uv run --with pytest pytest tests/test_aa_import.py -x` continues to pass (no regression to AA importer — the two modules share no code, but both import from playlist_parser/assets/repo and we did not modify those).
3. `grep -v '^\s*#' musicstreamer/soma_import.py | grep '"AAC+"'` returns empty (SOMA-15 source-grep gate, comment-stripped per project memory `feedback_gstreamer_mock_blind_spot.md`).
4. AST scan confirms per-channel try/except inside `import_stations`'s for-loop body (D-15 / RESEARCH Pitfall 2 mitigation).
5. Module surface: `python -c "from musicstreamer import soma_import; print(sorted(n for n in dir(soma_import) if not n.startswith('__')))"` lists at least `_API_URL`, `_TIER_BY_FORMAT_QUALITY`, `_USER_AGENT`, `_resolve_pls`, `fetch_channels`, `import_stations`.
</verification>

<success_criteria>
- [ ] musicstreamer/soma_import.py exists; ~240 lines.
- [ ] tests/test_soma_import.py: 11/11 GREEN.
- [ ] tests/test_aa_import.py: no regression.
- [ ] _TIER_BY_FORMAT_QUALITY maps exactly 4 tier tuples per D-03; codec values are only `"MP3"` and `"AAC"`.
- [ ] _USER_AGENT module constant contains both literals required by SOMA-14.
- [ ] Per-channel try/except wrapper exists inside the catalog loop (AST scan verifies; D-15 / Pitfall 2).
- [ ] ThreadPoolExecutor(max_workers=8) drives the logo phase (RESEARCH "Alternatives Considered" — keeps real-world import under D-08's implicit 5-sec threshold).
- [ ] No new external dependencies (only stdlib + already-in-tree musicstreamer modules).
</success_criteria>

<output>
After completion, create `.planning/phases/74-somafm-full-station-catalog-art-pull-all-somafm-streams-and-/74-02-SUMMARY.md`.
</output>
