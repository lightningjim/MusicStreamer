---
phase: 999.9-youtube-playback-broken-no-video-formats-found-regression
plan: 01
subsystem: media-playback
tags: [yt-dlp, youtube, hls, ejs, player_client, regression-fix]

# Dependency graph
requires:
  - phase: 35
    provides: yt-dlp library API path with EJS PO-token solver (Plan 35-06)
  - phase: 999.7
    provides: cookie_utils.temp_cookies_copy invariant + corruption auto-clear (FIX-02)
provides:
  - Restored YouTube live playback via pinned player_client="web"
  - Removed dead extractor_args["youtubepot-jsruntime"] namespace (was silently ignored by yt-dlp 2026.03.17)
  - Audit evidence for which clients work in current yt-dlp (web/web_safari/mweb succeed; tv fails; ios/android skip cookies)
affects: [yt-dlp upgrade procedure, future YouTube extraction regressions, OneDrive Linux sync exclusion review]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Probe-matrix-first diagnosis for upstream library API drift (one-pass terminal probe + DECISION footer drives conditional fix)"

key-files:
  created:
    - tests/integration/yt_player_client_audit.txt
  modified:
    - musicstreamer/player.py
    - tests/test_cookies.py

key-decisions:
  - "Phase 999.9: yt-dlp 2026.03.17 dropped extractor_args[\"youtubepot-jsruntime\"] namespace; pinned player_client=web instead (Branch A). Bundled yt_dlp_ejs handles JS challenges without --remote-components (probe row 7 confirmed)."
  - "Phase 999.9: Updated tests/test_cookies.py to assert the corrected contract (player_client=web, no dead namespace, no remote_components) — existing test edits per Rule 1, no new automated regression tests added."

patterns-established:
  - "Probe matrix audit file (tests/integration/yt_player_client_audit.txt) with DECISION footer block — reusable template for future yt-dlp/extractor regressions"

requirements-completed: []  # Phase has no formal REQ-IDs (regression hotfix); coverage anchored by D-01..D-07

# Metrics
duration: ~4min
completed: 2026-04-24
---

# Phase 999.9 Plan 01: YouTube Playback Regression Fix Summary

**Pinned yt-dlp player_client="web" and removed the dead extractor_args["youtubepot-jsruntime"] namespace; YouTube live HLS resolution restored (pending in-app UAT).**

## Performance

- **Duration:** ~4 min (probe + edit + tests + commits)
- **Started:** 2026-04-24T23:36:37Z
- **Completed:** 2026-04-24T23:40:50Z (Tasks 1-2; Task 3 awaiting human UAT)
- **Tasks:** 2 of 3 executed (Task 3 = `checkpoint:human-verify`)
- **Files modified:** 3 (1 created + 2 modified)

## Accomplishments

- **Probe matrix executed:** 7 rows captured against `https://www.youtube.com/watch?v=jfKfPfyJRdk` (LoFi Girl). web/web_safari/mweb returned valid HLS URLs; tv failed with "No video formats found"; ios/android skipped (no cookie support); control row (no `--remote-components`) succeeded — proving bundled yt_dlp_ejs is sufficient.
- **Branch A fix applied:** `musicstreamer/player.py::_youtube_resolve_worker` now pins `extractor_args={"youtube":{"player_client":["web"]}}`. Dead `youtubepot-jsruntime` namespace removed from both opts dict and module docstring.
- **Phase 999.7 invariant preserved:** `with cookie_utils.temp_cookies_copy() as cookiefile:` block (now at lines 503-505) unchanged; `musicstreamer/cookie_utils.py` unchanged (`git diff --quiet` succeeded).
- **Regression suite passes:** `uv run pytest tests/test_cookies.py tests/test_player.py -x` → 29 passed.

## Task Commits

1. **Task 1: yt-dlp probe matrix audit** — `3afc3fd` (test)
2. **Task 2: Branch A fix (pin player_client=web)** — `0fcff6d` (fix)
3. **Task 3: In-app UAT** — PENDING (`checkpoint:human-verify`, awaiting user)

**Plan metadata commit:** pending (will follow this SUMMARY.md)

## Files Created/Modified

- `tests/integration/yt_player_client_audit.txt` — Probe matrix audit (7 rows + DECISION=A footer)
- `musicstreamer/player.py` — Pinned player_client=web in `_youtube_resolve_worker` opts; updated module docstring; dead namespace removed
- `tests/test_cookies.py` — Updated 2 existing tests to assert corrected contract (player_client=web, no `youtubepot-jsruntime`, no `remote_components`)

## Decisions Made

- **Branch A over Branch C:** Probe row 7 succeeded → bundled `yt_dlp_ejs` is sufficient; no top-level `remote_components` needed. Cleanest fix is just pinning the proven-working `web` client.
- **Pinned `web`:** Default client, succeeded in BOTH row 1 (with `--remote-components`) AND row 7 (without). web_safari and mweb also work but `web` is the most stable choice and matches what already worked under the corrected config.
- **Cookies-incompatible clients (`ios`, `android`) treated as informational, not failure:** yt-dlp explicitly warns "Skipping client X since it does not support cookies" then errors. These rows do not refute the fix; they just confirm those clients require a cookie-less path.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated tests/test_cookies.py to match corrected contract**
- **Found during:** Task 2 verification (`uv run pytest tests/test_cookies.py tests/test_player.py -x`)
- **Issue:** Two existing tests (`test_youtube_resolve_passes_cookiefile_when_present`, `test_youtube_resolve_omits_cookiefile_when_absent`) asserted `extractor_args["youtubepot-jsruntime"]["remote_components"] == ["ejs:github"]` — codifying a contract that was always false (yt-dlp 2026.03.17 silently ignored that namespace). After removing the dead config, both tests failed.
- **Fix:** Updated assertions to check `extractor_args["youtube"]["player_client"] == ["web"]`, absence of `youtubepot-jsruntime`, and absence of top-level `remote_components`. This is editing existing tests to track the corrected reality, NOT adding new automated regression tests (CONTEXT D-04 forbids the latter).
- **Files modified:** tests/test_cookies.py (lines 152-160, 184-189)
- **Verification:** `uv run pytest tests/test_cookies.py tests/test_player.py -x` → 29 passed
- **Committed in:** 0fcff6d (Task 2 commit)

**2. [Rule 2 - Missing Critical] Removed `youtubepot-jsruntime` reference from module docstring**
- **Found during:** Task 2 acceptance check (`grep -c youtubepot-jsruntime musicstreamer/player.py` returned 2 instead of 0)
- **Issue:** The plan's acceptance criterion ("string `youtubepot-jsruntime` does NOT appear anywhere in `musicstreamer/player.py`") was violated by a stale module docstring at line 15 referencing the dead namespace.
- **Fix:** Updated docstring to describe the corrected Branch A configuration (player_client=web, bundled yt_dlp_ejs, no remote_components).
- **Files modified:** musicstreamer/player.py (lines 13-21)
- **Verification:** `grep -c youtubepot-jsruntime musicstreamer/player.py` → 0
- **Committed in:** 0fcff6d (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical)
**Impact on plan:** Both fixes were essential to satisfy the acceptance criteria as written. No scope creep — both changes track the same single fix (the dead namespace removal) into adjacent surfaces (docstring, existing test assertions).

## Issues Encountered

None beyond the deviations above. The probe matrix executed cleanly; row 4 (ios) and row 5 (android) showed expected client-skip warnings that the decision logic table accounts for.

## User Setup Required

**Manual UAT (Task 3) required before this plan can be marked verified.**

Steps for the user (from PLAN.md Task 3 `<how-to-verify>`):

1. Capture pre-UAT cookies sha256 (already captured: `1a7774201a9fa0d3435375c0c3e99d8914433a816743c0f163dcd25cd45b90cb` at 2026-04-24T23:40:50Z; note the canonical file may have rotated since the probe due to OneDrive sync — re-capture before UAT).
2. Launch app: `bash run_local.sh`
3. Click the LoFi Girl YouTube live station in the station list.
4. Within ~10 seconds, expect audible playback. NO "Playback error" or "YouTube playback temporarily broken" toast should appear.
5. Re-capture cookies sha256 — must match step 1's hash (Phase 999.7 byte-identity invariant).
6. (Optional) Click a non-YouTube station to confirm no regression on the non-YouTube path.

Type "approved" if playback works AND cookies sha256 unchanged. Otherwise capture terminal output + before/after sha256 and re-open the diagnostic.

## Next Phase Readiness

- **YouTube live HLS resolution restored at the library level** — terminal probe proves the new opts dict resolves valid `.m3u8` URLs.
- **Phase 999.7 cookie protection invariant intact** — verified via grep (`with cookie_utils.temp_cookies_copy() as cookiefile` present at line 503; `cookie_utils.py` unchanged).
- **In-app UAT pending** — Task 3 will close out the phase. Until then, the Phase 999.9 ROADMAP entry is "executing", not "completed".
- **PROJECT.md Key Decisions candidate:** `[Phase 999.9]: yt-dlp 2026.03.17 silently dropped extractor_args["youtubepot-jsruntime"]; pinned player_client=web in musicstreamer/player.py::_youtube_resolve_worker. Bundled yt_dlp_ejs handles JS challenges without --remote-components (probe row 7 confirmed). Phase 999.7 cookie invariant preserved.`
- **No automated YouTube regression tests added** (per CONTEXT D-04). Existing test_cookies.py assertions updated to track corrected contract.

## Self-Check: PASSED

- FOUND: tests/integration/yt_player_client_audit.txt
- FOUND: musicstreamer/player.py
- FOUND: tests/test_cookies.py
- FOUND: .planning/phases/999.9-youtube-playback-broken-no-video-formats-found-regression/999.9-01-SUMMARY.md
- FOUND COMMIT: 3afc3fd (Task 1 — probe matrix audit)
- FOUND COMMIT: 0fcff6d (Task 2 — Branch A fix)
- Phase 999.7 invariants verified: `with cookie_utils.temp_cookies_copy() as cookiefile` present at musicstreamer/player.py:505; `git diff --quiet musicstreamer/cookie_utils.py` exits 0.
- `grep -c youtubepot-jsruntime musicstreamer/player.py` returns 0.
- Regression suite: 29/29 passed.

---
*Phase: 999.9-youtube-playback-broken-no-video-formats-found-regression*
*Completed: 2026-04-24 (Tasks 1-2; Task 3 = UAT pending)*
