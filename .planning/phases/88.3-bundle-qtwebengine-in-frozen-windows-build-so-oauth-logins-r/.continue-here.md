---
context: phase
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-13T01:37:59.148Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-executing completed waves | Waves 1–2 (plans 88.3-02/03/04) are merged on `main`. Re-running `/gsd:execute-phase 88.3` correctly skips them (SUMMARYs exist) — but do NOT manually re-run or revert them. Only 88.3-05 (VM UAT) remains. | advisory | `execute-phase` discovery skips `has_summary: true`; trust it. |
| pip PySide6 in the conda build env | Leftover pip `PySide6-Essentials/Addons` in `musicstreamer-build` shadowed conda's Qt → `QtCore` load failed ("specified procedure could not be found"). Broke the main bundle this session. | advisory | Build env stays conda-PySide6 only. Fix if seen: `pip uninstall -y PySide6 PySide6-Essentials PySide6-Addons shiboken6 && conda install -y -c conda-forge --force-reinstall pyside6=6.10.1`. See memory `build-env-must-stay-pip-pyside6-free`. |
| Wrong interpreter for local tests | System `python3` lacks PySide6 → 55 false collection errors; full Qt GUI suite times out >540s. | advisory | Use `.venv/bin/python -m pytest` and scope to packaging-spec + oauth tests, not the whole suite. |

<current_state>
Phase 88.3 (B1: bundle QtWebEngine via an isolated oauth_helper.exe) is at its FINAL plan, 88.3-05 — the human-gated Win11 VM UAT. Waves 1 and 2 are complete and merged on `main`:
- 88.3-02 reverted conda-side WebEngine + re-pointed drift-guards to B1 invariants
- 88.3-03 rewired the frozen launch to the separate `oauth_helper.exe` + platform-aware Twitch UA
- 88.3-04 added the isolated-pip helper build + two-artifact Inno installer
- A stale Phase-88.2 test (`test_launcher_frozen_dispatch.py`) was updated to the B1 contract (post-merge gate catch).

During the VM UAT we hit — and FIXED — a conda-env contamination that blocked the main bundle (pip PySide6 removed). The main bundle now builds green and the two-artifact build + installer were produced. We are now blocked on the helper login step.

**Active blocker:** the installed `oauth_helper.exe` exits 2 — `DLL load failed while importing QtWebEngineWidgets: The specified procedure could not be found`. The helper bundle is COMPLETE (all `.pyd` bindings + `Qt6WebEngine*.dll` + `QtWebEngineProcess.exe` present), so this is a DLL-resolution/ABI issue, not a collection gap. Strong hypothesis: the reproductions were run FROM the `(musicstreamer-build)` conda shell, so conda's `Qt6Core.dll` on PATH shadows the helper's bundled pip Qt.
</current_state>

<completed_work>

Completed Tasks:
- Wave 1 — 88.3-02 revert conda WebEngine + B1 drift-guards — Done (39154a97)
- Wave 1 — 88.3-03 B1 launch target + Twitch platform UA — Done (688083a3)
- Post-merge — fix stale test_launcher_frozen_dispatch.py to B1 contract — Done (6fcc9156)
- Wave 2 — 88.3-04 isolated helper build + two-artifact installer — Done (13938369)
- 88.3-05 pre-build gate — 4 helper PyPI packages verified legit on pypi.org — Done
- Env fix — pip PySide6 removed from musicstreamer-build; ENV QtCore OK; main bundle builds — Done
- 88.3-05 Task 1 — two-artifact VM build green; oauth_helper.exe + QtWebEngineProcess.exe present; installer produced — Done
</completed_work>

<remaining_work>

- 88.3-05 final gate — resolve the helper `QtWebEngineWidgets` DLL-load (exit=2), then run GBS.FM / Twitch / Google logins from the INSTALLED build
- Record UAT-10 results in `88.3-UAT.md` mapping G6-T6 / G6-T7 / G6-T8
- Resume `/gsd:execute-phase 88.3` → phase verification (gsd-verifier) + completion (update_roadmap, close 88.3)
</remaining_work>

<decisions_made>

- B1 isolated-helper architecture validated — mixing pip WebEngine into the conda env breaks QtCore (proven by this session's contamination failure)
- conda `musicstreamer-build` must stay pip-PySide6-free (saved to memory `build-env-must-stay-pip-pyside6-free`)
- Local pytest must use `.venv/bin/python`, scoped to phase-relevant tests
</decisions_made>

<blockers>
- Helper `oauth_helper.exe` exits 2: `DLL load failed while importing QtWebEngineWidgets: The specified procedure could not be found`. Bundle is complete. Hypothesis: conda Qt on PATH (ran from conda shell) shadows the helper's pip Qt. DECISIVE TEST PENDING (see next_action).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state, exact resume commands, blocker detail
2. `.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-05-PLAN.md` — the VM UAT plan (3 tasks, G6-T6/T7/T8)
3. `.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-UAT.md` — where login results get recorded
4. `.planning/spikes/001-isolated-webengine-helper/README.md` — B1 runbook + the "Stage C PASS — no PATH mitigation needed" finding (validated in a CLEAN env)
5. Project memory `build-env-must-stay-pip-pyside6-free` — the env-contamination fix

## Infrastructure State
- Win11 VM at `Z:\musicstreamer` shares THIS repo (OneDrive/network mount). VM build dropped untracked `packaging/windows/.venv-oauth-helper/` and `packaging/windows/build-helper/` here — build artifacts, should be gitignored, do NOT commit.
- conda `musicstreamer-build` env: now clean (conda-forge PySide6 6.10.1 only, ICU 75, qt6-main 6.10.1). `from PySide6.QtCore import QCoreApplication` succeeds.
- Installed app path: `%LOCALAPPDATA%\Programs\MusicStreamer`; helper at `…\oauth_helper\oauth_helper.exe`.
- Helper provider Python: python.org 3.12 at `C:\Users\kcreasey\AppData\Local\Programs\Python\Python312\python.exe` (passed via `-HelperPythonExe`).

<context>
The phase CODE is done and merged; what remains is purely the operator VM UAT plus resolving an environment/bundle DLL-resolution issue the UAT surfaced (its job). We already solved one PATH-shadowing failure (the main bundle, via conda env cleanup). The helper exit=2 is the same error family and almost certainly the same root: conda Qt on PATH shadowing the helper's bundled pip Qt — because the helper was launched from the conda-activated shell. Confirm with a sanitized-PATH run; if conda is also on the GLOBAL PATH, the installed app inherits it and the helper needs startup PATH-hardening (an 88.3-04 gap-closure fix) before the UAT can pass.
</context>

<next_action>
Start with: on the VM, run the helper with a sanitized PATH to confirm/refute conda-Qt shadowing —
```powershell
$saved=$env:PATH
$env:PATH='C:\Windows\System32;C:\Windows;C:\Windows\System32\Wbem'
& "$env:LOCALAPPDATA\Programs\MusicStreamer\oauth_helper\oauth_helper.exe" --mode gbs
echo "exit=$LASTEXITCODE"
$env:PATH=$saved
```
If the GBS window opens (or no exit=2) → confirmed shadowing. Then in a FRESH (no-conda) PowerShell: `$env:PATH -split ';' | Select-String 'conda|miniforge|miniconda|Library\\bin'`. If empty → launch the installed app from the Start Menu (NOT the conda shell) and run the 3 logins. If conda IS on the global PATH (or sanitized-PATH still fails) → harden the helper startup PATH in `oauth_helper_standalone.spec`/`oauth_helper.py` (88.3-04 gap-closure), rebuild, retest. Record results in `88.3-UAT.md`, then resume `/gsd:execute-phase 88.3` for verification + completion.
</next_action>
