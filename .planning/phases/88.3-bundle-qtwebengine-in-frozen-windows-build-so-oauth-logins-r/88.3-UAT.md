---
status: diagnosed
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
source: [88.3-01-SUMMARY.md]
started: 2026-06-11T01:12:20Z
updated: 2026-06-11T01:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing paused — Test 1 failed (QtWebEngineWidgets DLL load failure in build env); Tests 2-4 blocked on a working build. Live VM diagnostic in progress to fork conda-Qt-conflict vs missing-dependency.]

## Tests

### 1. Frozen build bundles QtWebEngine (build smoke)
expected: build.ps1 reaches BUILD_OK with WEBENGINE GUARD OK (no -Skip flags); QtWebEngineProcess.exe present in dist\MusicStreamer\_internal\PySide6\
result: issue
reported: "Blocked. Still not working. PySide6-Addons==6.10.1 already installed (pyside6 6.10.1). `python -c \"from PySide6.QtWebEngineWidgets import QWebEngineView\"` raises: ImportError: DLL load failed while importing QtWebEngineWidgets: The specified module could not be found."
severity: blocker

### 2. GBS.FM in-app login (G6-T6 / UAT-10)
expected: Installed frozen exe → AccountsDialog → GBS.FM login button opens the QtWebEngine login window; authentication completes; GBS.FM audio plays. No exit=2 "Login helper crashed unexpectedly"; cookie-import fallback does NOT trigger.
result: blocked
blocked_by: release-build
reason: "Cannot build a working frozen exe until the WebEngine DLL load failure in the build env is resolved (Test 1)."

### 3. Twitch in-app login (G6-T7 / UAT-10b)
expected: Installed frozen exe → AccountsDialog → Connect Twitch opens the Twitch QtWebEngine login window from the frozen exe. No exit=2 crash.
result: blocked
blocked_by: release-build
reason: "Depends on a working frozen build (Test 1)."

### 4. Google/YouTube in-app login (G6-T8 / UAT-10c)
expected: Installed frozen exe → CookieImportDialog → Google/YouTube login button opens the Google QtWebEngine login window. If login is cancelled/fails, _on_google_process_error surfaces a warning pointing to the File/Paste tabs (graceful, no silent dead-end).
result: blocked
blocked_by: release-build
reason: "Depends on a working frozen build (Test 1)."

## Summary

total: 4
passed: 0
issues: 1
pending: 0
skipped: 0
blocked: 3

## Gaps

- truth: "musicstreamer-build env has PySide6.QtWebEngineWidgets importable (PySide6-Addons pinned to the installed pyside6 version)"
  status: failed
  reason: "User reported: PySide6-Addons==6.10.1 installed and pinned to pyside6 6.10.1, but `from PySide6.QtWebEngineWidgets import QWebEngineView` raises 'ImportError: DLL load failed while importing QtWebEngineWidgets: The specified module could not be found.' The version pin matched, so this is a missing dependency DLL (loader cannot locate a transitive dependency of Qt6WebEngineCore.dll), not an ICU symbol-version mismatch. Strong hypothesis: a conda-forge Qt6 on the env PATH shadows/conflicts with the pip PySide6 Qt DLLs (RESEARCH Pitfall 2 escalation path), or a WebEngine-only Qt dependency (Qt6Quick/Qml/Positioning/WebChannel/Pdf) is absent."
  severity: blocker
  test: 1
  root_cause: "CONFIRMED via live VM diagnostic: the musicstreamer-build env has TWO incompatible PySide6 stacks layered together — conda-forge `pyside6` 6.10.1 + `qt6-main` 6.10.1 (the GStreamer-compatible base) AND pip `pyside6-essentials` + `pyside6-addons` 6.10.1 (installed to add WebEngine). The pip Addons WebEngine DLLs ARE present in site-packages/PySide6 (Qt6WebEngineCore.dll, QtWebEngineProcess.exe, Qt6Quick/WebChannel/Positioning/Pdf all present — files not missing). But `where Qt6Core.dll` resolves to conda's `...\\Library\\bin\\Qt6Core.dll` (qt6-main, on PATH), while pip-Addons' Qt6WebEngineCore.dll was built against pip-PySide6's OWN Qt6Core — so the loader feeds a conda-built Qt6Core/Gui/Quick to a pip-built WebEngine DLL → a transitive export is unresolvable → 'DLL load failed ... module could not be found'. Classic never-mix-conda-and-pip-PySide6 hazard. All-pip is NOT an option: spike-findings-musicstreamer mandates conda-forge for GStreamer (pip PySide6 ICU differs from conda-forge GStreamer ICU — would break audio). Fix must source WebEngine from conda-forge to match the existing conda pyside6+qt6-main."
  artifacts:
    - path: "(VM) musicstreamer-build conda env"
      issue: "pip pyside6-essentials/pyside6-addons 6.10.1 layered over conda-forge pyside6 6.10.1 + qt6-main; pip WebEngine DLLs load conda Qt6Core → DLL load failure"
    - path: "packaging/windows/README.md"
      issue: "Plan-01 precondition recommends `pip install PySide6-Addons==<pyside6 ver>` — this is the WRONG recipe for a conda-forge env; it produces the mixed-stack failure. Must be replaced with the conda-forge WebEngine recipe."
    - path: "packaging/windows/build.ps1"
      issue: "step-0 preflight + step-4e guard logic correct, but the documented remediation hint points at pip Addons (wrong for conda env)."
  missing:
    - "Decide architecture: conda-forge cannot provide PySide6 WebEngine bindings at any version (see verified findings below). Must EITHER isolate the oauth_helper into its own pip-PySide6 artifact, OR drop QtWebEngine for a system-browser OAuth flow."
    - "Replace the Plan-01 README/build.ps1 pip-Addons precondition (wrong for a conda-forge single-bundle env) with the chosen architecture's recipe"
    - "Reconsider the Plan-01 spec hiddenimports + step-4e guard: they assume WebEngine lives in the same bundle as the conda main app, which is not achievable"
  verified_findings:
    - "conda-meta pyside6-6.10.1 manifest contains NO 'WebEngine' entries → conda-forge pyside6 ships zero WebEngine Python bindings"
    - "conda search qt6-webengine → only 6.10.2 / 6.11.0 / 6.11.1 (no 6.10.1); and it provides only Qt C++ libs, not PySide6 bindings"
    - "Therefore the ONLY source of PySide6.QtWebEngineWidgets is pip PySide6-Addons, which is ABI-incompatible with conda Qt/GStreamer in one process (spike ICU constraint + observed DLL load failure)"
  resolution_options:
    - "B1 (isolated helper bundle): build oauth_helper as a SEPARATE PyInstaller artifact from a pip-PySide6-Addons env (no GStreamer); main conda bundle launches the helper exe. oauth_helper is already a separate subprocess, so this is a launch-target change + a 2nd build, not a main-app rewrite. Keeps the existing webview cookie-capture logins."
    - "B6 (system-browser OAuth): drop QtWebEngine entirely; use the OS default browser + loopback redirect. Eliminates 130MB Chromium + the whole conflict, but may not support GBS.FM's cookie-based login and is a larger login-flow rework."
  chosen_path: "B1 (isolated helper bundle) — to be SPIKED first (user decision 2026-06-11). Spike goal: prove a standalone PyInstaller oauth_helper exe, built from an isolated pip-PySide6-Addons env (no conda Qt, no GStreamer), opens a QtWebEngine login window on the Win11 VM. If the spike passes, plan the integration (main conda exe launches the helper exe; installer ships both) as 88.3 gap closure. The committed 88.3-01 source changes (same-bundle spec hiddenimports + step-4e guard) will need rework under B1."
  debug_session: "remote-only (Windows VM); diagnosed via live operator commands — conda list, where.exe Qt6Core.dll, conda-meta manifest read, and conda search qt6-webengine were decisive"
