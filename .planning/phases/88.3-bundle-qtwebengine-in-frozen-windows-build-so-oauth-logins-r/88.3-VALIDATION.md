---
phase: 88.3
slug: bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-10
---

# Phase 88.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 88.3-RESEARCH.md ## Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x |
| **Config file** | pyproject.toml `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run --with pytest pytest tests/test_packaging_webengine_spec.py -x` |
| **Full suite command** | `uv run --with pytest pytest` |
| **Estimated runtime** | ~5–15 seconds (source-text drift guards; no build invoked) |

---

## Sampling Rate

- **After every task commit:** `uv run --with pytest pytest tests/test_packaging_webengine_spec.py tests/test_packaging_winrt_spec.py -x`
- **After every plan wave:** `uv run --with pytest pytest`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| G6-T1 | spec | — | spec has WebEngine hiddenimports | — | N/A | unit (source-text) | `pytest tests/test_packaging_webengine_spec.py::test_spec_webengine_hiddenimports -x` | ❌ W0 | ⬜ pending |
| G6-T2 | spec | — | spec wires WebEngine into Analysis | — | N/A | unit (source-text) | `pytest tests/test_packaging_webengine_spec.py::test_spec_wires_webengine_into_analysis -x` | ❌ W0 | ⬜ pending |
| G6-T3 | build.ps1 | — | build.ps1 has --check-webengine guard (step-4e) | — | N/A | unit (source-text) | `pytest tests/test_packaging_webengine_spec.py::test_build_ps1_webengine_guard_present -x` | ❌ W0 | ⬜ pending |
| G6-T4 | build.ps1 | — | build.ps1 exit 13 documented | — | N/A | unit (source-text) | `pytest tests/test_packaging_webengine_spec.py::test_build_ps1_webengine_guard_exit_13 -x` | ❌ W0 | ⬜ pending |
| G6-T5 | __main__.py | — | __main__.py has --check-webengine + _run_check_webengine | — | N/A | unit (source-text) | `pytest tests/test_packaging_webengine_spec.py::test_main_py_check_webengine -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*(Wave/Plan/Task IDs are finalized by the planner; this map fixes the requirement→test binding. The drift-guard tests are source-text assertions over the spec / build.ps1 / __main__.py — they do not invoke a Windows build.)*

---

## Wave 0 Requirements

- [ ] `tests/test_packaging_webengine_spec.py` — new drift-guard file covering G6-T1 through G6-T5 (mirror of `tests/test_packaging_winrt_spec.py` structure)

*Test framework itself already exists (pytest configured in pyproject.toml); only the new test file is missing.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GBS.FM QtWebEngine login window opens + auth completes from frozen exe | G6 / test 6 (VER-02-J / UAT-10) | Requires a real frozen Windows build on a VM + interactive OAuth | AccountsDialog → GBS.FM login button → authenticate → audio plays; cookie-import fallback must NOT trigger |
| Twitch QtWebEngine login window opens from frozen exe | G6 / test 7 (UAT-10b) | Same frozen-bundle + interactive-auth constraint | AccountsDialog → Connect Twitch → Twitch login window opens |
| Google/YouTube QtWebEngine login window opens from frozen exe | G6 / test 8 (UAT-10c / CR-01) | Same constraint; also exercises graceful error path | CookieImportDialog → Google/YouTube login → window opens; on failure `_on_google_process_error` points to File/Paste tabs (no silent dead-end) |

*These three are the phase's reason-for-being; the automated source-text guards prevent silent regression of the bundling fix but cannot replace VM UAT.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the new webengine spec test file)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
