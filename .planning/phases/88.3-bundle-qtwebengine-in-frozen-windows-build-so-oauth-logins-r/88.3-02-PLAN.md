---
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
plan: 02
type: execute
wave: 2
depends_on: ["88.3-01"]
files_modified: []
autonomous: false
requirements: [G6-T6, G6-T7, G6-T8]
user_setup:
  - service: "Windows build VM (musicstreamer-build conda env)"
    why: "QtWebEngine is not in the conda-forge pyside6 package; PySide6-Addons must be pip-installed (pinned) before the frozen build can bundle it. The build + interactive OAuth UAT can only run on the Win11 VM."
    dashboard_config:
      - task: "Activate musicstreamer-build, read installed pyside6 version, pip install PySide6-Addons==<that version>, verify QWebEngineView imports"
        location: "Win11 VM PowerShell, musicstreamer-build env"

must_haves:
  truths:
    - "musicstreamer-build env has PySide6.QtWebEngineWidgets importable (PySide6-Addons pinned to the installed conda-forge pyside6 version)"
    - "build.ps1 completes WITHOUT -SkipWebEngineGuard: preflight passes, step-4e WEBENGINE GUARD OK, installer produced"
    - "dist/MusicStreamer/_internal/PySide6/QtWebEngineProcess.exe exists in the frozen bundle"
    - "From the installed frozen exe, GBS.FM in-app login opens the QtWebEngine window and authentication completes; cookie-import fallback does NOT trigger (G6-T6)"
    - "From the frozen exe, Twitch login opens the QtWebEngine window (G6-T7)"
    - "From the frozen exe, Google/YouTube login opens the QtWebEngine window; on failure _on_google_process_error points to File/Paste tabs, no silent dead-end (G6-T8)"
  artifacts:
    - path: "(VM) dist/installer/MusicStreamer-<version>-win64-setup.exe"
      provides: "Frozen installer with QtWebEngine bundled"
    - path: "(VM) dist/MusicStreamer/_internal/PySide6/QtWebEngineProcess.exe"
      provides: "Chromium renderer subprocess proving the WebEngine hook fired"
  key_links:
    - from: "musicstreamer-build env"
      to: "PySide6.QtWebEngineWidgets"
      via: "pip install PySide6-Addons==<pinned pyside6 version>"
      pattern: "PySide6-Addons=="
    - from: "build.ps1 step-4e"
      to: "frozen QtWebEngineProcess.exe"
      via: "--check-webengine exits 0 → WebEngine bundled"
      pattern: "WEBENGINE GUARD OK"
---

<objective>
Provision QtWebEngine in the Windows build env, run the full frozen build through
build.ps1 (with the new step-4e guard active), confirm QtWebEngineProcess.exe landed,
then run the three interactive OAuth UAT smokes that are this phase's reason-for-being.

Purpose: fix_shape part 1 (env provisioning) + end-to-end proof that G6 is closed —
GBS.FM, Twitch, and Google in-app logins open the QtWebEngine window from the frozen
exe instead of crashing exit=2.

Output: A clean build.ps1 run (no -Skip flags), a QtWebEngineProcess.exe assertion,
and three human-verified login smokes. This plan is Windows-VM-bound and NOT autonomous.

Depends on 88.3-01 (the spec hiddenimports + step-4e guard + preflight must already be
in the source tree before this build can succeed).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-RESEARCH.md
@.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-VALIDATION.md
@.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision PySide6-Addons (pinned) in musicstreamer-build + run build.ps1 + assert QtWebEngineProcess.exe</name>
  <files>(no tracked source files — VM env provisioning + build run only)</files>
  <read_first>
    - .planning/phases/88.3-.../88.3-RESEARCH.md §Architecture Patterns Pattern 3 (env provisioning, ICU pin) + §Standard Stack installation steps + Pitfall 2/3
    - packaging/windows/README.md Build prerequisites (the Phase 88.3 PySide6-Addons note added in Plan 01)
    - packaging/windows/build.ps1 step-0 preflight + step-4e WEBENGINE GUARD (added in Plan 01)
  </read_first>
  <what-built>
    Plan 01 added the WebEngine hiddenimports to MusicStreamer.spec, the --check-webengine guard to
    __main__.py, the build.ps1 step-4e guard + preflight, and the README precondition. This step
    provisions the build env and runs the real Windows build to confirm the WebEngine runtime is
    actually collected into the bundle.
  </what-built>
  <action>
    HUMAN-ACTION checkpoint — no CLI/API substitute exists (Windows VM build + interactive conda env
    on hardware the Linux dev host cannot reach). On the Win11 VM with musicstreamer-build activated,
    perform the steps in <how-to-verify>: pin-install PySide6-Addons to the installed pyside6 version,
    verify QWebEngineView imports in the build env, run build.ps1 with NO -Skip flags, assert
    QtWebEngineProcess.exe is present in the bundle, then install via the produced setup.exe.
  </action>
  <how-to-verify>
    On the Win11 VM, with the musicstreamer-build conda env activated:

    1. Read the installed pyside6 version and pin PySide6-Addons to it (do NOT use an unpinned
       install — RESEARCH Pitfall 2 ICU ABI hazard):
         $pyside_ver = python -c "from PySide6 import __version__; print(__version__)"
         pip install "PySide6-Addons==$pyside_ver"
    2. Verify the binding imports in the build env (this is what the diagnostic found missing):
         python -c "from PySide6.QtWebEngineWidgets import QWebEngineView; print('WebEngine OK')"
       Expect: "WebEngine OK". If instead you get a Qt6Core.dll / "DLL load failed" error, the pin
       did not match — STOP and report (ICU ABI mismatch per Pitfall 2).
    3. From packaging\windows, run the full build with NO skip flags:
         .\build.ps1
       Expect: preflight "PREFLIGHT WebEngine OK" (does NOT exit 1); step-4c SMTC SMOKE GUARD OK;
       step-4d OAUTH HELPER GUARD OK; step-4e "WEBENGINE GUARD OK" (does NOT BUILD_FAIL / exit 13);
       "BUILD_OK installer='...MusicStreamer-<version>-win64-setup.exe'".
    4. Assert the Chromium subprocess landed in the bundle (RESEARCH Pitfall 5):
         Test-Path dist\MusicStreamer\_internal\PySide6\QtWebEngineProcess.exe
       Expect: True. If False, the hook did not fire from hiddenimports alone — report so Plan 01's
       spec can be revised to add explicit Tree() collection (RESEARCH OQ1 / A2 fallback).
    5. Install via the produced setup.exe over the prior install (per the consolidated 88-03 VM flow).
  </how-to-verify>
  <verify>
    <human-check>build.ps1 reaches BUILD_OK with WEBENGINE GUARD OK (no -Skip flags); Test-Path dist\MusicStreamer\_internal\PySide6\QtWebEngineProcess.exe returns True</human-check>
  </verify>
  <done>PySide6-Addons pinned + QWebEngineView imports in the build env; clean build.ps1 run with step-4e guard active; QtWebEngineProcess.exe bundled; installer produced and installed.</done>
  <resume-signal>
    Report: pyside6 version + PySide6-Addons pinned version, build.ps1 reached BUILD_OK with
    WEBENGINE GUARD OK (yes/no), QtWebEngineProcess.exe present (yes/no), installer path. If any
    step failed, paste the BUILD_FAIL line / exit code. Type "build-ok" to proceed to the login UAT,
    or describe the failure.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — GBS.FM / Twitch / Google in-app QtWebEngine login smokes (G6-T6/T7/T8)</name>
  <files>(no tracked source files — interactive UAT on the installed frozen exe)</files>
  <read_first>
    - .planning/phases/88.3-.../88.3-VALIDATION.md ## Manual-Only Verifications (the three UAT rows)
    - .planning/phases/88-.../88-UAT.md ## Gaps G6 (tests 6/7/8 expected behavior)
  </read_first>
  <what-built>
    The frozen build from Task 1 now bundles QtWebEngine. These three logins previously crashed
    "Login helper crashed unexpectedly" exit=2 (oauth_helper.py:62-71 module-level import →
    sys.exit(2)). They should now open the real QtWebEngine login window.
  </what-built>
  <action>
    HUMAN-VERIFY checkpoint — interactive OAuth flows requiring real credentials and a rendered
    QtWebEngine window cannot be automated. Launch the installed frozen exe via the Start-Menu
    shortcut and exercise the three login flows in <how-to-verify>.
  </action>
  <how-to-verify>
    Launch MusicStreamer via the Start-Menu shortcut (the installed frozen exe, not the dev tree):

    1. (G6-T6 / UAT-10) AccountsDialog → GBS.FM login button → the QtWebEngine login window opens →
       authenticate → GBS.FM audio plays. The cookie-import fallback must NOT trigger. No exit=2 /
       "Login helper crashed unexpectedly".
    2. (G6-T7 / UAT-10b) AccountsDialog → Connect Twitch → the Twitch QtWebEngine login window opens
       from the frozen exe (no exit=2).
    3. (G6-T8 / UAT-10c) CookieImportDialog → Google/YouTube login button → the Google QtWebEngine
       login window opens. If the login is cancelled/fails, confirm _on_google_process_error surfaces
       a warning pointing at the File/Paste tabs (CR-01) — a graceful path, not a silent dead-end.
  </how-to-verify>
  <verify>
    <human-check>All three login windows (GBS.FM, Twitch, Google) open from the frozen exe; GBS.FM authenticates and audio plays; Google failure path points to File/Paste tabs</human-check>
  </verify>
  <done>G6-T6/T7/T8 human-verified: all three QtWebEngine login windows open from the frozen exe; GBS.FM auth completes + plays; no exit=2 crash; Google error path graceful.</done>
  <resume-signal>
    Report pass/fail per login (GBS.FM, Twitch, Google). For any failure, capture the dialog text and
    whether a Chromium subprocess appears in Task Manager when the window should be open. Type
    "approved" if all three open and GBS.FM authenticates + plays, or describe issues (these feed a
    gap-closure pass if any remain).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip wheel ↔ conda-forge Qt env | PySide6-Addons pip wheel mixed into conda-forge pyside6 (ICU ABI) |
| WebEngine subprocess ↔ third-party OAuth pages | Chromium renders gbs.fm / twitch / google login over isolated process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.3-02 | Denial of Service | ICU ABI mismatch from unpinned PySide6-Addons crashing Qt6Core.dll | mitigate | Task 1 step 1 pins PySide6-Addons to the exact installed pyside6 version; step 2 verifies the import before any build, catching a mismatch early |
| T-88.3-03 | Information Disclosure | OAuth cookies handled by the WebEngine helper subprocess | accept | Existing oauth_helper mitigations unchanged (T-76-01 domain-boundary cookie check, T-76-02 detail field never a cookie value); this phase adds no new attack surface (RESEARCH Security Domain) |
| T-88.3-SC | Tampering | pip install PySide6-Addons on the VM | accept | RESEARCH Package Legitimacy Audit: PySide6-Addons [OK] (official Qt wheel, slopcheck 2 OK) — no blocking legitimacy checkpoint required |
</threat_model>

<verification>
- build.ps1 reaches BUILD_OK with WEBENGINE GUARD OK and no -Skip flags
- dist/MusicStreamer/_internal/PySide6/QtWebEngineProcess.exe present
- GBS.FM / Twitch / Google login windows all open from the installed frozen exe (human-verified)
</verification>

<success_criteria>
- PySide6-Addons provisioned pinned; QWebEngineView imports in the build env
- Full build.ps1 run is clean with the WebEngine guard active
- QtWebEngineProcess.exe bundled (hook fired)
- G6-T6/T7/T8 human UAT smokes pass: all three logins open; GBS.FM authenticates + plays; Google error path graceful
</success_criteria>

<output>
Create `.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-02-SUMMARY.md` when done.
Do NOT git-add or commit anything under .planning/ — it is gitignored. This plan modifies no
tracked source files (VM build + manual UAT only), so there is nothing to commit; record the VM
results in the SUMMARY.
</output>
