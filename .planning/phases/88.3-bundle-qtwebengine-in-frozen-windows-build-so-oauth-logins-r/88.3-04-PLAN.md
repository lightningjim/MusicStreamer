---
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
plan: "04"
type: execute
wave: 2
depends_on: ["88.3-02"]
files_modified:
  - packaging/windows/oauth-helper-requirements.txt
  - packaging/windows/oauth_helper_standalone.spec
  - packaging/windows/build.ps1
  - packaging/windows/MusicStreamer.iss
  - packaging/windows/README.md
  - tests/test_packaging_helper_build_spec.py
autonomous: true
requirements: [G6-T6, G6-T7, G6-T8]
must_haves:
  truths:
    - "A 2nd PyInstaller build produces a standalone oauth_helper.exe from an isolated pip PySide6 6.10.1 env (no conda, no GStreamer)"
    - "build.ps1 builds the helper exe in addition to the conda main bundle and asserts QtWebEngineProcess.exe + Qt6WebEngineCore.dll are present in it"
    - "The Inno installer ships BOTH artifacts, installing the helper to a LOCAL path under {app}\\oauth_helper\\"
    - "packaging/windows/README.md documents the conda-free Python 3.12 build prereq (replacing the old wrong pip-Addons-into-conda precondition)"
  artifacts:
    - path: "packaging/windows/oauth-helper-requirements.txt"
      provides: "Pinned isolated helper env (PySide6-Essentials+Addons==6.10.1, pyinstaller, hooks-contrib)"
      contains: "PySide6-Addons==6.10.1"
    - path: "packaging/windows/oauth_helper_standalone.spec"
      provides: "PyInstaller spec freezing musicstreamer/oauth_helper.py with WebEngine hiddenimports"
      contains: "PySide6.QtWebEngineCore"
    - path: "packaging/windows/build.ps1"
      provides: "Second build step + helper-bundle WebEngine assertion (new exit code)"
      contains: "oauth_helper"
    - path: "packaging/windows/MusicStreamer.iss"
      provides: "Two-artifact installer; helper under {app}\\oauth_helper\\"
      contains: "oauth_helper"
    - path: "tests/test_packaging_helper_build_spec.py"
      provides: "Drift-guards: helper spec has WebEngine, requirements pinned, build.ps1 builds+asserts helper, Inno ships helper"
      contains: "def test_"
  key_links:
    - from: "packaging/windows/build.ps1"
      to: "packaging/windows/oauth_helper_standalone.spec"
      via: "second pyinstaller invocation against an isolated venv built from oauth-helper-requirements.txt"
      pattern: "oauth_helper_standalone.spec"
    - from: "packaging/windows/MusicStreamer.iss"
      to: "dist\\oauth_helper"
      via: "[Files] Source row installing the helper bundle to {app}\\oauth_helper"
      pattern: "oauth_helper"
---

<objective>
Add the second (isolated-pip) PyInstaller build to the Windows packaging and
ship both artifacts. Promote the spike-proven recipe into packaging/windows/:
an isolated requirements.txt (PySide6 6.10.1 + pyinstaller + hooks-contrib), a
standalone spec that freezes musicstreamer/oauth_helper.py with the WebEngine
hiddenimports, a build.ps1 step that builds the helper from a conda-free Python
3.12 venv and asserts QtWebEngineProcess.exe + Qt6WebEngineCore.dll are present,
an Inno installer that ships BOTH bundles (helper to {app}\oauth_helper\), and a
README that documents the conda-free Python 3.12 prereq.

Purpose: produce the WebEngine-carrying helper artifact that 88.3-03's launch
path targets, installed to a local path the Chromium sandbox accepts. Output:
two-artifact Windows build + installer + drift-guards.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.4.10/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.4.10/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Spike-proven recipe to COPY FROM (these are the source of truth for this plan):
@.planning/spikes/001-isolated-webengine-helper/README.md
@.planning/spikes/001-isolated-webengine-helper/requirements.txt
@.planning/spikes/001-isolated-webengine-helper/oauth_helper_standalone.spec
@.planning/spikes/001-isolated-webengine-helper/build-helper.ps1
@.planning/spikes/MANIFEST.md

# Project skills — Windows PS 5.1 / PyInstaller / conda gotchas:
@.claude/skills/spike-findings-musicstreamer/SKILL.md

<interfaces>
<!-- Proven spike artifacts to promote, and the build.ps1/iss insertion points. -->

Spike requirements.txt (copy, adjust comment to packaging context):
  PySide6-Essentials==6.10.1 / PySide6-Addons==6.10.1 / pyinstaller==6.19.0 / pyinstaller-hooks-contrib==2026.2

Spike oauth_helper_standalone.spec — freezes REPO_ROOT/musicstreamer/oauth_helper.py with
  hiddenimports=["PySide6.QtWebEngineWidgets","PySide6.QtWebEngineCore","PySide6.QtNetwork"],
  excludes gi/gst/numpy/etc, name="oauth_helper", COLLECT name="oauth_helper".
  NOTE: spike spec computes REPO_ROOT via Path(SPECPATH).resolve().parents[2] (3 levels up from
  .planning/spikes/001-.../). In packaging/windows/ the spec is 2 levels up from repo root, so the
  promoted spec MUST recompute REPO_ROOT = Path(SPECPATH).resolve().parents[1].
  Spike spec uses console=True for VM JSON visibility; the production helper can keep console=True
  (it is a child process; no taskbar window flash concern beyond what 88.2 already accepts) OR set
  console=False — keep console=True to preserve the spike's stderr-event observability.

Spike build-helper.ps1 — the proven driver: isolated venv (-PythonExe override for a clean
  conda-forge python provider OR py -3.12), pip install -r requirements, pyinstaller build,
  Assert-WebEngine (QtWebEngineProcess.exe + Qt6WebEngineCore.dll under _internal\PySide6\),
  exit codes 20/21/22/23/24. The CONDA guard (exit 20 on $env:CONDA_PREFIX) is load-bearing:
  the helper build must NOT run in the conda env.

build.ps1 (post-88.3-02 revert) — surviving exit codes 0-12; step-4d OAUTH HELPER GUARD ends
  ~line 384; step 6 Inno compile follows. The new helper-build step inserts AFTER the conda
  main-bundle build (step 4 region) and BEFORE step 6 Inno compile, so dist\oauth_helper exists
  when iscc runs. Map the spike's exit-22 (missing WebEngine) to this project's guard convention:
  use a NEW exit code 14 (the next free code after 13 was retired in 88.3-02). Document 14 in the
  exit-codes header.

MusicStreamer.iss [Files] section (line 58-61) currently ships only dist\MusicStreamer\* to {app}.
  Add a SECOND Source row: dist\oauth_helper\* → {app}\oauth_helper (recursesubdirs createallsubdirs).
  Helper installs LOCAL (under {app}) — Chromium sandbox blocks network-path launch (spike VM run 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the isolated requirements + standalone spec into packaging/windows/</name>
  <files>packaging/windows/oauth-helper-requirements.txt, packaging/windows/oauth_helper_standalone.spec, tests/test_packaging_helper_build_spec.py</files>
  <read_first>
    - .planning/spikes/001-isolated-webengine-helper/requirements.txt (copy source)
    - .planning/spikes/001-isolated-webengine-helper/oauth_helper_standalone.spec (copy source — note the parents[2] → parents[1] path change)
    - packaging/windows/MusicStreamer.spec (sibling spec, for layout/style consistency)
    - tests/test_packaging_webengine_spec.py (post-88.3-02 — the drift-guard idiom to mirror)
  </read_first>
  <behavior>
    - test_helper_requirements_pinned: oauth-helper-requirements.txt contains the exact pins "PySide6-Essentials==6.10.1", "PySide6-Addons==6.10.1", "pyinstaller==6.19.0", "pyinstaller-hooks-contrib==2026.2".
    - test_helper_spec_has_webengine_hiddenimports: comment-stripped oauth_helper_standalone.spec contains "PySide6.QtWebEngineWidgets" AND "PySide6.QtWebEngineCore" (the helper IS the WebEngine carrier — opposite of the conda spec's B1 invariant).
    - test_helper_spec_freezes_oauth_helper: the spec references musicstreamer/oauth_helper.py as its entry and names the EXE/COLLECT "oauth_helper".
    - test_helper_spec_repo_root_two_levels: the spec computes REPO_ROOT with parents[1] (packaging/windows is 2 levels below repo root), not parents[2].
  </behavior>
  <action>
    Copy .planning/spikes/001-isolated-webengine-helper/requirements.txt to
    packaging/windows/oauth-helper-requirements.txt, keeping the four exact pins; rewrite
    the header comment to describe this as the production isolated-helper build env (pip-only,
    Qt-isolated, no conda/GStreamer; pinned to 6.10.1 for the QNetworkCookie contract parity
    with the conda main app).

    Copy .planning/spikes/001-isolated-webengine-helper/oauth_helper_standalone.spec to
    packaging/windows/oauth_helper_standalone.spec. CHANGE the REPO_ROOT computation from
    Path(SPECPATH).resolve().parents[2] to parents[1] (packaging/windows/ is two levels
    below repo root, not three). Keep HELPER_SRC = REPO_ROOT/musicstreamer/oauth_helper.py,
    the three WebEngine/Network hiddenimports, the gi/gst/numpy excludes, name="oauth_helper",
    and console=True (preserves the stderr JSON-event observability). Update the header comment
    to reference 88.3-04 and spike 001 as the proven recipe.

    Create tests/test_packaging_helper_build_spec.py with the four tests in <behavior>,
    mirroring the test_packaging_webengine_spec.py fixture + _strip_comments idiom.
  </action>
  <acceptance_criteria>
    - `grep -c 'PySide6-Addons==6.10.1' packaging/windows/oauth-helper-requirements.txt` returns 1
    - `grep -c 'PySide6-Essentials==6.10.1\|pyinstaller==6.19.0\|pyinstaller-hooks-contrib==2026.2' packaging/windows/oauth-helper-requirements.txt` returns 3
    - `grep -v '^[[:space:]]*#' packaging/windows/oauth_helper_standalone.spec | grep -c QtWebEngineCore` returns >= 1
    - `grep -c 'parents\[1\]' packaging/windows/oauth_helper_standalone.spec` returns >= 1
    - `grep -c 'parents\[2\]' packaging/windows/oauth_helper_standalone.spec` returns 0
    - `grep -c 'musicstreamer.*oauth_helper.py\|oauth_helper.py' packaging/windows/oauth_helper_standalone.spec` returns >= 1
    - `python -c "import ast; ast.parse(open('packaging/windows/oauth_helper_standalone.spec').read())"` exits 0
    - `uv run --with pytest pytest tests/test_packaging_helper_build_spec.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/MusicStreamer && uv run --with pytest pytest tests/test_packaging_helper_build_spec.py -x</automated>
  </verify>
  <done>The isolated requirements.txt and standalone spec live in packaging/windows/, pinned to 6.10.1, freezing oauth_helper.py with WebEngine hiddenimports, with REPO_ROOT corrected to parents[1]. Drift-guards green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build.ps1 second build + WebEngine assertion; Inno two-artifact; README prereq</name>
  <files>packaging/windows/build.ps1, packaging/windows/MusicStreamer.iss, packaging/windows/README.md, tests/test_packaging_helper_build_spec.py</files>
  <read_first>
    - packaging/windows/build.ps1 (post-88.3-02 — insertion point: after the conda main build / step-4d OAUTH HELPER GUARD ~line 384, before step 6 Inno ~line 418)
    - .planning/spikes/001-isolated-webengine-helper/build-helper.ps1 (proven driver: isolated venv, -PythonExe, Assert-WebEngine, exit 22)
    - packaging/windows/MusicStreamer.iss ([Files] section lines 58-61 — add second Source row)
    - packaging/windows/README.md (Build prerequisites lines 40-65 — replace the wrong pip-Addons-into-conda precondition)
    - .claude/skills/spike-findings-musicstreamer/SKILL.md (PS 5.1 native-stderr trap via Invoke-Native, ASCII-only .ps1, exit-code guard convention)
  </read_first>
  <behavior>
    - test_build_ps1_builds_helper: build.ps1 references "oauth_helper_standalone.spec" and "oauth-helper-requirements.txt" (it builds the second artifact).
    - test_build_ps1_helper_isolated_venv: build.ps1 creates an isolated venv NOT in the conda env (references a venv path + does not run the helper pyinstaller under $env:CONDA_PREFIX) and supports a conda-free Python 3.12 provider (a -HelperPythonExe param or equivalent, mirroring the spike's -PythonExe).
    - test_build_ps1_asserts_helper_webengine: build.ps1 asserts QtWebEngineProcess.exe and Qt6WebEngineCore.dll exist in dist\oauth_helper and exits 14 + documents "14=" in the exit-codes header (the spike's exit-22 signature mapped to this project's convention).
    - test_iss_ships_helper: MusicStreamer.iss [Files] contains a Source row for dist\oauth_helper installing to {app}\oauth_helper.
    - test_readme_documents_conda_free_python: README contains "conda-free Python 3.12" (or equivalent) AND no longer recommends `pip install PySide6-Addons` into the conda env as the WebEngine precondition.
  </behavior>
  <action>
    In packaging/windows/build.ps1: append "14=oauth_helper.exe WebEngine missing from isolated
    helper bundle (Phase 88.3 / B1 / G6)" to the exit-codes header. Add a HELPER BUILD step
    AFTER the conda main bundle build + step-4d guard and BEFORE step 6 Inno compile. The step
    must: (a) create an isolated venv using a conda-free Python 3.12 — add a `-HelperPythonExe`
    param (mirroring the spike's -PythonExe) so a clean conda-forge python+pip env or python.org
    3.12 can provide it; (b) `pip install -r oauth-helper-requirements.txt` into that venv;
    (c) run `<venv>\Scripts\python.exe -m PyInstaller oauth_helper_standalone.spec --noconfirm
    --distpath ..\..\dist --workpath build-helper`; (d) Assert QtWebEngineProcess.exe AND
    Qt6WebEngineCore.dll exist under dist\oauth_helper\_internal\PySide6\ (fall back to the flat
    PySide6\ layout like the spike's Assert-WebEngine), and on failure `Write-Host
    "BUILD_FAIL reason=helper_webengine_missing ..." -ForegroundColor Red; exit 14` (WR-01:
    Write-Host not Write-Error). Use Invoke-Native around pip/pyinstaller (PS 5.1 native-stderr
    trap). ASCII-only. Do NOT run the helper pyinstaller under the conda env — the venv python
    is the whole isolation point.

    In packaging/windows/MusicStreamer.iss: add a second [Files] Source row:
    `Source: "..\..\dist\oauth_helper\*"; DestDir: "{app}\oauth_helper"; Flags: ignoreversion
    recursesubdirs createallsubdirs` directly after the existing MusicStreamer\* row. This makes
    the helper a LOCAL sibling of MusicStreamer.exe (Chromium sandbox requires a local path), at
    exactly the path 88.3-03's launcher resolves ({app}\oauth_helper\oauth_helper.exe).

    In packaging/windows/README.md: REPLACE the Phase 88.3 PySide6-Addons-into-conda precondition
    (lines ~40-65) with a B1 section: the OAuth helper is a SEPARATE PyInstaller artifact built
    from an isolated pip env (oauth-helper-requirements.txt) using a CONDA-FREE Python 3.12
    (python.org `winget install -e --id Python.Python.3.12`, OR a clean conda-forge
    `conda create -n helper-iso -c conda-forge python=3.12 pip` used only as the venv provider
    via -HelperPythonExe). Document that the helper installs to {app}\oauth_helper\ and must run
    from a local path (Chromium sandbox). Remove the claim that `conda install qt6-webengine` or
    `pip install PySide6-Addons` into the conda env is needed.

    Add the five tests in <behavior> to tests/test_packaging_helper_build_spec.py.
  </action>
  <acceptance_criteria>
    - `grep -c 'oauth_helper_standalone.spec' packaging/windows/build.ps1` returns >= 1
    - `grep -c 'oauth-helper-requirements.txt' packaging/windows/build.ps1` returns >= 1
    - `grep -c 'QtWebEngineProcess.exe' packaging/windows/build.ps1` returns >= 1
    - `grep -c 'exit 14' packaging/windows/build.ps1` returns >= 1 && `grep -c '14=' packaging/windows/build.ps1` returns >= 1
    - `grep -c 'HelperPythonExe' packaging/windows/build.ps1` returns >= 1
    - `grep -c 'Write-Error' packaging/windows/build.ps1` returns 0 (WR-01 — no terminating-error escalation)
    - `grep -c 'oauth_helper' packaging/windows/MusicStreamer.iss` returns >= 1
    - `grep -c 'conda-free Python 3.12\|conda-free python 3.12' packaging/windows/README.md` returns >= 1
    - `grep -c 'pip install .*PySide6-Addons.*conda\|conda install qt6-webengine' packaging/windows/README.md` returns 0
    - `python -c "import re,sys; s=open('packaging/windows/build.ps1','rb').read(); sys.exit(0 if all(b<128 for b in s) else 1)"` exits 0 (ASCII-only .ps1)
    - `uv run --with pytest pytest tests/test_packaging_helper_build_spec.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kcreasey/Documents/Projects/MusicStreamer && uv run --with pytest pytest tests/test_packaging_helper_build_spec.py -x</automated>
  </verify>
  <done>build.ps1 builds the isolated helper from a conda-free Python 3.12 venv and asserts QtWebEngineProcess.exe + Qt6WebEngineCore.dll (exit 14); MusicStreamer.iss ships both artifacts with the helper under {app}\oauth_helper\; README documents the conda-free Python 3.12 prereq and drops the wrong pip-Addons-into-conda precondition. Drift-guards green, .ps1 ASCII-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → isolated helper venv | four pip packages enter the build; supply-chain surface |
| build env → two shipped artifacts | the installer carries both the conda exe and the WebEngine helper exe |
| installer → {app}\oauth_helper\ | the helper lands on local disk where Chromium's sandbox will spawn QtWebEngineProcess.exe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.3-04-01 | Tampering | helper bundle silent WebEngine drop | mitigate | build.ps1 Assert step exits 14 if QtWebEngineProcess.exe / Qt6WebEngineCore.dll are missing — the build fails loudly instead of shipping a helper that crashes at login |
| T-88.3-04-02 | Tampering | isolated venv contaminated by conda Qt | mitigate | the helper build uses a conda-free Python 3.12 venv (-HelperPythonExe); ABI-self-consistent pip Qt is the entire B1 fix (spike Stage C) |
| T-88.3-04-03 | DoS | ICU/ABI mismatch if PySide6 unpinned | mitigate | oauth-helper-requirements.txt pins PySide6 6.10.1 (matches the cookie contract) |
| T-88.3-04-SC | Tampering | npm/pip/cargo installs | mitigate | this plan only DECLARES the four [ASSUMED] PyPI packages in oauth-helper-requirements.txt (no install runs Linux-side); the actual install happens in 88.3-05's VM build, gated by a blocking-human package-legitimacy checkpoint that verifies all four on pypi.org before the build env installs them |
</threat_model>

<verification>
- `grep -c 'oauth_helper_standalone.spec' packaging/windows/build.ps1` returns >= 1
- `grep -c 'QtWebEngineProcess.exe' packaging/windows/build.ps1` returns >= 1 and `grep -c 'exit 14' packaging/windows/build.ps1` returns >= 1
- `grep -c 'oauth_helper' packaging/windows/MusicStreamer.iss` returns >= 1
- `grep -c 'conda-free Python 3.12' packaging/windows/README.md` returns >= 1
- `uv run --with pytest pytest tests/test_packaging_helper_build_spec.py tests/test_packaging_webengine_spec.py -x` exits 0
</verification>

<success_criteria>
- A 2nd PyInstaller build produces oauth_helper.exe from an isolated pip 6.10.1 env
- build.ps1 asserts WebEngine binaries in the helper bundle (exit 14)
- Inno ships both artifacts; helper under {app}\oauth_helper\ (local path)
- README documents the conda-free Python 3.12 prereq
- The four [ASSUMED] packages are declared pinned here; their install is gated by 88.3-05's legitimacy checkpoint
</success_criteria>

<output>
Create `.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-04-SUMMARY.md` when done
</output>
