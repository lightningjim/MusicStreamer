---
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
plan: "05"
type: execute
wave: 3
depends_on: ["88.3-03", "88.3-04"]
files_modified: []
autonomous: false
requirements: [G6-T6, G6-T7, G6-T8]
must_haves:
  truths:
    - "The two-artifact frozen build (conda main exe + isolated oauth_helper.exe) builds green on the Win11 VM"
    - "From the Inno-installed build, GBS.FM, Twitch, and Google/YouTube in-app logins open the QtWebEngine login window (no exit=2, no DLL-load failure)"
    - "Twitch login completes (no longer rejected on UA) after the platform-aware _CHROME_UA fix"
    - "{app}\\oauth_helper\\...\\QtWebEngineProcess.exe exists in the installed build, and the cookie-import fallback does NOT trigger for GBS"
  artifacts:
    - path: ".planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-UAT.md"
      provides: "Recorded UAT-10 results mapping G6-T6/T7/T8"
  key_links:
    - from: "installed {app}\\MusicStreamer.exe"
      to: "{app}\\oauth_helper\\oauth_helper.exe"
      via: "QProcess.start of the separate helper exe on login"
      pattern: "oauth_helper"
---

<objective>
Close Phase 88.3 G6 with a Win11 VM UAT of the two-artifact B1 build. Build both
artifacts on the VM, install via Inno, and verify GBS.FM + Twitch + Google/YouTube
in-app logins open the QtWebEngine login window from the INSTALLED build with no
exit=2 crash and no DLL-load failure — the exact failures that defined G6. This
is the human-judgement gate the spike's Stage B foreshadowed; it confirms the
integration (conda exe launches the separate helper) works end-to-end and that
the Twitch UA fix lands.

Purpose: prove logins open from the installed two-artifact frozen build. Output:
recorded UAT results mapping G6-T6/T7/T8.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.4.10/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.4.10/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# What this UAT verifies + the build/run runbook + the prior G6 diagnosis:
@.planning/spikes/001-isolated-webengine-helper/README.md
@.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-UAT.md
@.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-04-PLAN.md
@.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-03-PLAN.md

# Project skills — VM build gotchas (conda deactivate, ASCII .ps1, exit-code map):
@.claude/skills/spike-findings-musicstreamer/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this is the pre-install package-legitimacy gate for the isolated helper env (the four PyPI packages declared in 88.3-04's oauth-helper-requirements.txt are installed for the FIRST time during Task 1's VM build).</what-built>
  <how-to-verify>
    88.3-04 declared four PyPI packages NOT in the conda recipe (treated as [ASSUMED] — RESEARCH.md has no Package Legitimacy Audit table). Before build.ps1 installs them into the isolated helper venv (Task 1), verify each on pypi.org:
      1. PySide6-Essentials 6.10.1 -> https://pypi.org/project/PySide6-Essentials/6.10.1/ (official Qt for Python)
      2. PySide6-Addons 6.10.1 -> https://pypi.org/project/PySide6-Addons/6.10.1/ (official Qt for Python addons; ships WebEngine)
      3. pyinstaller 6.19.0 -> https://pypi.org/project/pyinstaller/6.19.0/
      4. pyinstaller-hooks-contrib 2026.2 -> https://pypi.org/project/pyinstaller-hooks-contrib/2026.2/
    These are the same Qt-for-Python / PyInstaller families already trusted in the conda main build; the spike installed exactly these and produced a working WebEngine bundle on Win11.
  </how-to-verify>
  <resume-signal>Type "approved" once each package is confirmed legitimate on pypi.org, or describe any concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Build both artifacts on the Win11 VM</name>
  <files></files>
  <read_first>
    - packaging/windows/build.ps1 (post-88.3-04 — the two-build driver; -HelperPythonExe param; exit codes incl. 14)
    - packaging/windows/README.md (post-88.3-04 — conda-free Python 3.12 prereq)
    - .planning/spikes/001-isolated-webengine-helper/README.md ("Prereqs" + "Investigation Trail": conda deactivate, network-path sandbox, conda-free Python 3.12)
  </read_first>
  <action>
    On the Win11 VM, in the conda `musicstreamer-build` env (for the main bundle) AND with a
    conda-free Python 3.12 available for the helper venv (per README), run
    `packaging\windows\build.ps1 -HelperPythonExe "<path to conda-free python.exe>"`. The build
    must: produce the conda main bundle (dist\MusicStreamer\), build the isolated helper
    (dist\oauth_helper\) from the pip-only 6.10.1 venv, assert QtWebEngineProcess.exe +
    Qt6WebEngineCore.dll in the helper bundle (exit 14 on failure), and compile the Inno
    installer shipping BOTH artifacts. Confirm a clean exit 0 (BUILD_OK step=done) and that
    dist\oauth_helper\oauth_helper.exe and dist\oauth_helper\_internal\PySide6\QtWebEngineProcess.exe
    both exist. If the helper build reports exit 20 (conda env active), run `conda deactivate`
    for the helper venv provider or pass an explicit -HelperPythonExe per the spike's VM-run-1 finding.
  </action>
  <verify>
    <automated>MISSING — Windows-VM-only build; verified by operator running build.ps1 to BUILD_OK + the file-existence checks below. No Linux automation possible (the frozen exe is not runnable on Linux).</automated>
    <human-check>build.ps1 exits 0 with BUILD_OK step=done; dist\oauth_helper\oauth_helper.exe exists; dist\oauth_helper\_internal\PySide6\QtWebEngineProcess.exe exists; dist\installer\MusicStreamer-<ver>-win64-setup.exe was produced.</human-check>
  </verify>
  <done>Both artifacts build green on the VM; the helper bundle contains QtWebEngineProcess.exe + Qt6WebEngineCore.dll; the Inno installer was produced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The two-artifact B1 build: conda MusicStreamer.exe + a SEPARATE isolated oauth_helper.exe, shipped by one Inno installer. The conda exe launches the helper exe (no self-re-exec); the helper carries its own pip-PySide6 WebEngine; the Twitch UA is now Windows-appropriate.</what-built>
  <how-to-verify>
    1. Install the freshly built installer (dist\installer\MusicStreamer-<ver>-win64-setup.exe) on the Win11 VM. Launch via the Start Menu shortcut.
    2. Confirm the helper landed locally: {app}\oauth_helper\oauth_helper.exe and {app}\oauth_helper\_internal\PySide6\QtWebEngineProcess.exe both exist ({app} = %LOCALAPPDATA%\Programs\MusicStreamer).
    3. GBS.FM login (G6-T6 / UAT-10): AccountsDialog → GBS.FM login button → the QtWebEngine login window OPENS (no "Login helper crashed unexpectedly", no exit=2, no DLL-load failure). Complete login; GBS.FM audio plays. The cookie-import (File/Paste) fallback must NOT auto-trigger.
    4. Twitch login (G6-T7 / UAT-10b): AccountsDialog → Connect Twitch → the Twitch QtWebEngine login window opens AND Twitch accepts the browser (NO "Your browser is not currently supported" — this is the UA fix landing). Complete login.
    5. Google/YouTube login (G6-T8 / UAT-10c): CookieImportDialog → Google/YouTube login button → the Google QtWebEngine login window opens (no exit=2). If you cancel, the graceful warning points to the File/Paste tabs (no silent dead-end).
    6. Across all three: no helper-crash toast, no DLL-load failure in any window.
    Record each result in 88.3-UAT.md (pass/issue) mapping G6-T6/T7/T8.
  </how-to-verify>
  <resume-signal>Type "approved" if all three logins open and Twitch is accepted (no exit=2, no DLL-load failure, GBS cookie-fallback did not trigger), or describe the failure (window/exit code/UA message) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed conda exe → installed helper exe | the spawned binary must be the local {app}\oauth_helper\oauth_helper.exe, not a PATH-resolved or network-path binary |
| helper WebEngine → OAuth provider servers (GBS/Twitch/Google) | login pages run in the bundled Chromium; cookies/tokens captured per the existing oauth_helper contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.3-05-01 | DoS | helper exit=2 / DLL-load failure at login | mitigate | UAT explicitly verifies the G6 failure signatures are gone; a regression blocks the phase gate |
| T-88.3-05-02 | Tampering | helper spawned from a network/UNC path | mitigate | Inno installs the helper to local {app}\oauth_helper\ (88.3-04); the launcher resolves it relative to the local main exe (88.3-03) — Chromium sandbox never sees a network path |
| T-88.3-05-SC | Tampering | npm/pip/cargo installs | mitigate | the isolated helper venv's four [ASSUMED] PyPI packages are installed for the first time during Task 1's VM build; the blocking-human legitimacy checkpoint (first task) verifies all four on pypi.org before that install runs |
</threat_model>

<verification>
- build.ps1 exits 0 (BUILD_OK step=done) producing both bundles + the installer
- {app}\oauth_helper\...\QtWebEngineProcess.exe exists in the installed build
- GBS.FM, Twitch, Google/YouTube login windows all open from the installed build (no exit=2, no DLL-load failure)
- Twitch is accepted (no "browser not supported"); GBS cookie-import fallback does NOT trigger
- Results recorded in 88.3-UAT.md mapping G6-T6/T7/T8
</verification>

<success_criteria>
- Two-artifact build green on the VM; helper bundle carries QtWebEngineProcess.exe
- All three in-app logins open from the installed build with no G6 failure signatures
- Twitch login accepted after the UA fix
- G6-T6/T7/T8 recorded pass in 88.3-UAT.md
</success_criteria>

<output>
Create `.planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-05-SUMMARY.md` when done
</output>
