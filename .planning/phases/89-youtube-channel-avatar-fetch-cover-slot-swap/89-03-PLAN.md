---
phase: 89-youtube-channel-avatar-fetch-cover-slot-swap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/cover_art.py
  - tests/test_cover_art_avatar.py
  - tests/test_constants_drift.py
autonomous: true
requirements: [ART-AVATAR-07, ART-AVATAR-09, ART-AVATAR-10]
must_haves:
  truths:
    - "cover_art.py defines _mb_caa_lookup then _channel_avatar_lookup in that source order"
    - "fetch_cover_art routes its MB-CAA call through _mb_caa_lookup (precedence preserved)"
    - "_channel_avatar_lookup is synchronous, reads channel_avatar_path, never raises, never touches Qt"
    - "Phase 71 RichText baseline count (3) is unchanged by Phase 89"
  artifacts:
    - path: "musicstreamer/cover_art.py"
      provides: "named precedence wrappers for drift-guard"
      contains: "def _mb_caa_lookup"
    - path: "tests/test_cover_art_avatar.py"
      provides: "source-grep precedence drift-guard"
      contains: "def test_mb_caa_runs_before_channel_avatar"
    - path: "tests/test_constants_drift.py"
      provides: "Phase 89 RichText parity guard"
      contains: "def test_richtext_baseline_unchanged_by_phase_89"
  key_links:
    - from: "musicstreamer/cover_art.py fetch_cover_art"
      to: "_mb_caa_lookup"
      via: "replaces inline _cover_art_mb.fetch_mb_cover call"
      pattern: "_mb_caa_lookup\\("
    - from: "tests/test_cover_art_avatar.py"
      to: "cover_art.py source order"
      via: "src.find positions, mb_pos < avatar_pos"
      pattern: "mb_pos < avatar_pos"
---

<objective>
Refactor `cover_art.py` to expose named `_mb_caa_lookup` and `_channel_avatar_lookup` functions (in that source order) so the precedence drift-guard (ART-AVATAR-09) is structurally stable, and add both source-grep drift-guards: `test_mb_caa_runs_before_channel_avatar` (ART-AVATAR-07/09) and `test_richtext_baseline_unchanged_by_phase_89` (ART-AVATAR-10).

Purpose: ART-AVATAR-07 locks precedence `ICY → iTunes → MB-CAA → channel-avatar → placeholder`; the project convention enforces this with a source-grep gate over the named functions, not behavioral mocks. The channel-avatar must NEVER short-circuit Phase 73 MB-CAA coverage.
Output: named wrappers in cover_art.py between `_split_artist_title` and `fetch_cover_art`, the new drift-guard test file, and the appended Phase 71 parity test.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-RESEARCH.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-PATTERNS.md

<interfaces>
<!-- Verified from codebase. Use directly — no exploration. -->

cover_art.py current structure (verified L1-227):
  _split_artist_title   ~L131
  fetch_cover_art       ~L147  — calls _cover_art_mb.fetch_mb_cover INLINE at ~L197 (mb_only) and ~L224 (auto)
  (no _mb_caa_lookup / _channel_avatar_lookup exist yet)
  _itunes_attempt never raises out (D-20 contract — wrappers must preserve this)

Target order between _split_artist_title (L131) and fetch_cover_art (L147):
  _split_artist_title
  _mb_caa_lookup          (NEW — thin wrapper over _cover_art_mb.fetch_mb_cover)
  _channel_avatar_lookup  (NEW — synchronous avatar reader, see Pitfall 7)
  fetch_cover_art         (updated: call _mb_caa_lookup, not _cover_art_mb.fetch_mb_cover directly)

tests/test_constants_drift.py:
  EXPECTED_RICHTEXT_COUNT = 3            # before L82
  def test_richtext_baseline_unchanged_by_phase_71()   # L82-108 — exact analog to mirror
  Path already imported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add named _mb_caa_lookup / _channel_avatar_lookup wrappers + precedence drift-guard</name>
  <files>musicstreamer/cover_art.py, tests/test_cover_art_avatar.py</files>
  <read_first>
    - musicstreamer/cover_art.py (full structure L41-227: _split_artist_title L131, fetch_cover_art L147, the two inline _cover_art_mb.fetch_mb_cover call sites ~L197 and ~L224, the _itunes_attempt never-raise contract)
    - 89-PATTERNS.md §`musicstreamer/cover_art.py` (named-wrapper pattern, _channel_avatar_lookup synchronous body reading channel_avatar_path via paths.data_dir(), inline call replacement, never-raise contract)
    - 89-RESEARCH.md §Pattern 2 + Pitfall 7 (avatar fires only when ICY empty/disabled; _channel_avatar_lookup is a synchronous path-returner, no thread, no QPixmap) + Research Question 2 (exact placement) + Research Question 5 (drift-guard test) + Open Question 2 (real stub reading channel_avatar_path)
    - tests/test_gbs_marquee_drift_guard.py (source-grep drift-guard style: Path.read_text + positional assertion)
  </read_first>
  <behavior>
    - cover_art.py source has "def _mb_caa_lookup" at an index strictly less than "def _channel_avatar_lookup".
    - fetch_cover_art's MB-CAA dispatch goes through _mb_caa_lookup (no remaining bare inline _cover_art_mb.fetch_mb_cover in the dispatch paths).
    - _channel_avatar_lookup(None, cb) calls cb(None); with a station lacking channel_avatar_path calls cb(None); with a stored relative path that exists on disk calls cb(abs_path); never raises.
    - Existing fetch_cover_art behavior (iTunes → MB-CAA precedence) is unchanged for ICY-enabled stations.
  </behavior>
  <action>
    Insert two functions in cover_art.py BETWEEN `_split_artist_title` (~L131) and `fetch_cover_art` (~L147), in this exact order so `mb_pos < avatar_pos` holds: (1) `def _mb_caa_lookup(artist, title, callback) -> None` — thin wrapper that calls `_cover_art_mb.fetch_mb_cover(artist, title, callback)`; (2) `def _channel_avatar_lookup(station, callback) -> None` — synchronous: if station is None or has no `channel_avatar_path`, call `callback(None)`; else build `abs_path = os.path.join(paths.data_dir(), rel)` and `callback(abs_path if os.path.exists(abs_path) else None)`. NO thread launch, NO QPixmap (Pitfall 7 / WR-04 — must never raise; wrap in try/except if needed so a Qt slot caller can't crash). Then replace the inline `_cover_art_mb.fetch_mb_cover(...)` calls in fetch_cover_art's `mb_only` (~L197) and `auto` (~L224) branches with `_mb_caa_lookup(...)`. Do NOT change iTunes routing or the precedence order. Create tests/test_cover_art_avatar.py with `test_mb_caa_runs_before_channel_avatar` (read cover_art.py source, assert both `def _mb_caa_lookup` and `def _channel_avatar_lookup` present and `mb_pos < avatar_pos`) plus the field-filter-logic unit tests from 89-PATTERNS.md (avatar_uncropped preferred, non-square rejected, None dims allowed). NOTE (RESEARCH.md Q2, RESOLVED): `_channel_avatar_lookup` is positioned for the ART-AVATAR-09 precedence drift-guard and is a real synchronous stub reading `station.channel_avatar_path` — it is deliberately NOT wired into `fetch_cover_art`'s `auto` dispatch chain. The live ICY-disabled avatar swap is triggered by `bind_station` in now_playing_panel (89-04), not through `fetch_cover_art`. The absence of an `auto`-mode dispatch call to `_channel_avatar_lookup` is intentional, not a gap.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cover_art_avatar.py tests/test_cover_art.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_cover_art_avatar.py::test_mb_caa_runs_before_channel_avatar -x` passes.
    - `grep -n "def _mb_caa_lookup\|def _channel_avatar_lookup" musicstreamer/cover_art.py` shows _mb_caa_lookup first.
    - `grep -c "_mb_caa_lookup(" musicstreamer/cover_art.py` >= 3 (definition + 2 dispatch call sites); no bare `_cover_art_mb.fetch_mb_cover(` remains inside fetch_cover_art dispatch branches.
    - `_channel_avatar_lookup` is a real synchronous stub (reads `station.channel_avatar_path`, never raises) positioned for ART-AVATAR-09 drift-guarding; it is intentionally NOT called from `fetch_cover_art`'s `auto` dispatch chain (per RESEARCH.md Q2, RESOLVED). The live ICY-disabled trigger is `bind_station` in now_playing_panel (89-04) — so no `auto`-mode dispatch grep check is expected for `_channel_avatar_lookup`.
    - `.venv/bin/python -m pytest tests/test_cover_art.py -x` still green (no precedence regression).
  </acceptance_criteria>
  <done>Named wrappers exist in the locked order, fetch_cover_art routes MB-CAA through _mb_caa_lookup, _channel_avatar_lookup is a safe synchronous reader, precedence drift-guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 71 RichText parity drift-guard for Phase 89</name>
  <files>tests/test_constants_drift.py</files>
  <read_first>
    - tests/test_constants_drift.py (EXPECTED_RICHTEXT_COUNT before L82; test_richtext_baseline_unchanged_by_phase_71 L82-108 — the exact analog to mirror; Path already imported)
    - 89-RESEARCH.md §Research Question 4 (the verbatim new-test body) and 89-PATTERNS.md §`tests/test_constants_drift.py`
  </read_first>
  <action>
    Append `def test_richtext_baseline_unchanged_by_phase_89()` to tests/test_constants_drift.py, mirroring test_richtext_baseline_unchanged_by_phase_71 exactly: rglob musicstreamer/*.py, count occurrences of the needle `setTextFormat(Qt.RichText)`, assert `count == EXPECTED_RICHTEXT_COUNT` (3), with a UnicodeDecodeError-skip guard and a Phase-89-specific assertion message. No new imports (Path already present). This is a guard task — no production code changes; it re-asserts that Phase 89's circular-avatar render path adds no new RichText labels (ART-AVATAR-10).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_constants_drift.py::test_richtext_baseline_unchanged_by_phase_89 -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_richtext_baseline_unchanged_by_phase_89" tests/test_constants_drift.py` matches.
    - `.venv/bin/python -m pytest tests/test_constants_drift.py -x` passes (both phase_71 and phase_89 baseline tests green at count 3).
  </acceptance_criteria>
  <done>Phase 89 RichText parity guard appended and green; baseline count remains 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cover-resolver dispatch | precedence ordering is a correctness/coverage invariant — drift here silently degrades MB-CAA coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-07 | Tampering | precedence reorder regressing MB-CAA coverage | mitigate | Source-grep drift-guard test_mb_caa_runs_before_channel_avatar pins _mb_caa_lookup before _channel_avatar_lookup (ART-AVATAR-07/09) |
| T-89-08 | Denial of Service | _channel_avatar_lookup raising into a Qt callback chain | mitigate | _channel_avatar_lookup is synchronous, never raises (WR-04 contract); no thread, no QPixmap (Pitfall 7) |
| T-89-09 | Tampering | unnoticed RichText sibling-render regression | mitigate | test_richtext_baseline_unchanged_by_phase_89 pins the count at 3 (ART-AVATAR-10) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_cover_art_avatar.py tests/test_cover_art.py tests/test_constants_drift.py -x` green.
</verification>

<success_criteria>
- Precedence order is structurally pinned: _mb_caa_lookup before _channel_avatar_lookup.
- fetch_cover_art routes MB-CAA through the named wrapper; no behavioral regression.
- Phase 71 RichText baseline preserved by an explicit Phase 89 guard.
</success_criteria>

<output>
Create `.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-03-SUMMARY.md` when done.
</output>
