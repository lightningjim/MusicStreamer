---
phase: 89-youtube-channel-avatar-fetch-cover-slot-swap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/yt_import.py
  - tests/test_yt_import_library.py
autonomous: true
requirements: [ART-AVATAR-03]
must_haves:
  truths:
    - "fetch_channel_avatar selects the avatar_uncropped thumbnail and downloads its bytes"
    - "Non-square avatar entries (width != height both present) are rejected"
    - "A YouTube video URL resolves to its channel before re-fetching the avatar"
    - "A per-provider fetcher registry exposes youtube and accepts future twitch registration"
  artifacts:
    - path: "musicstreamer/yt_import.py"
      provides: "fetch_channel_avatar + per-provider registry"
      contains: "def fetch_channel_avatar"
  key_links:
    - from: "musicstreamer/yt_import.py fetch_channel_avatar"
      to: "yt_dlp channel thumbnails[]"
      via: "extract_info(download=False) without extract_flat"
      pattern: "avatar_uncropped"
    - from: "musicstreamer/yt_import.py register_avatar_fetcher"
      to: "_AVATAR_FETCHERS registry"
      via: "module-load registration of youtube fetcher"
      pattern: "register_avatar_fetcher\\(\"youtube\""
---

<objective>
Add `yt_import.fetch_channel_avatar(channel_url) -> bytes` (ART-AVATAR-03) and the per-provider fetcher registry (D-04) so Phase 89b registers Twitch with zero dialog/cover-slot rework.

Purpose: This is the network-fetch service the dialog worker (Plan 05) calls. The field-filter semantics are subtle (only `avatar_uncropped` reliably matches; `None != None` must NOT reject) and are the core of ART-AVATAR-03.
Output: `fetch_channel_avatar`, video-URL→channel-URL two-step resolution, the `register_avatar_fetcher`/`get_avatar_fetcher` registry with youtube pre-registered, and unit tests for the filter logic.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-RESEARCH.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-PATTERNS.md

<interfaces>
<!-- Verified from codebase. Use directly — no exploration. -->

yt_import.py (L11-19 imports already present: logging, os, re, typing.Callable/Optional, yt_dlp,
              constants/cookie_utils/paths/yt_dlp_opts, runtime_check.NodeRuntime):
  def is_yt_playlist_url(url: str) -> bool          # L24 — covers playlist + @handle tabs, NOT bare video URLs
  def scan_playlist(...)                            # L43 — opts structure + cookie_utils.temp_cookies_copy guard to copy

scan_playlist opts to copy (OMIT extract_flat for avatar fetch):
  quiet=True, no_warnings=True, skip_download=True,
  js_runtimes=yt_dlp_opts.build_js_runtimes(node_runtime),
  remote_components={"ejs:github"}

cookie guard (MUST wrap YoutubeDL): with cookie_utils.temp_cookies_copy() as cookiefile: ...
</interfaces>

# Spike landmine relevant here: this function runs ON A WORKER THREAD (Plan 05).
# It must NOT touch any Qt widget and must NOT use QTimer.singleShot — results
# marshal back via queued Signal in the dialog. fetch_channel_avatar is pure
# network/data; keep it Qt-free.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fetch_channel_avatar with avatar_uncropped field filter</name>
  <files>musicstreamer/yt_import.py, tests/test_yt_import_library.py</files>
  <read_first>
    - musicstreamer/yt_import.py (scan_playlist L43-121 — opts dict, build_js_runtimes, cookie_utils.temp_cookies_copy guard, DownloadError handling; is_yt_playlist_url L24)
    - 89-PATTERNS.md §`musicstreamer/yt_import.py` (opts pattern, cookie-guard pattern, avatar field-filter pattern, anti-pattern: never pass extract_flat)
    - 89-RESEARCH.md §Pattern 1 + Pitfall 1 (only avatar_uncropped has a stable id), Pitfall 2 (None != None == False — do NOT reject null dims), Pitfall 3 (extract_flat suppresses thumbnails), Open Question 3 (video URL → channel_url two-step)
    - tests/test_yt_import_library.py (existing test conventions, how yt_dlp is mocked/avoided)
  </read_first>
  <behavior>
    - Given thumbnails [{id:"0",url:cropped,width:200,height:200},{id:"avatar_uncropped",url:uncropped}], the selected entry is the avatar_uncropped one.
    - An entry with width=200,height=150 (both present, unequal) is rejected (raises ValueError "not square").
    - An entry with width=None,height=None is NOT rejected (None != None is False) — uncropped is square by construction.
    - When no avatar/avatar_uncropped entry exists, raises ValueError "No channel avatar found".
    - For a video URL, channel_url is read from the info dict and the avatar is re-resolved from the channel (two-step).
  </behavior>
  <action>
    Add `def fetch_channel_avatar(channel_url: str) -> bytes` to yt_import.py. Build opts by copying scan_playlist's keys but OMIT `extract_flat` (Pitfall 3). Wrap `yt_dlp.YoutubeDL` in `cookie_utils.temp_cookies_copy()` exactly like scan_playlist. Call `ydl.extract_info(url, download=False)`. If the resolved info is a video (no avatar in its own thumbnails) read `info.get("channel_url")` (fallback `uploader_url`) and re-run extract_info on that channel URL (Open Question 3 two-step). Filter: `avatar_entry = next(t for t in thumbnails if t.get("id")=="avatar_uncropped", None) or next(t for t in thumbnails if t.get("id")=="avatar", None)`; raise ValueError if None. Reject ONLY when both width and height are present and unequal: `if w is not None and h is not None and w != h: raise ValueError(...)` (Pitfall 2 — never use truthiness on dims). Download via `urllib.request.urlopen(url, timeout=10).read()` and return bytes. Structure the filter logic so it is unit-testable WITHOUT a live network call (extract the entry-selection + square-check into testable form or test the predicate directly per 89-PATTERNS.md test examples). Add `import urllib.request` (module or function scope per codebase style). Tests go in tests/test_yt_import_library.py under `-k avatar`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_yt_import_library.py -k avatar -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def fetch_channel_avatar" musicstreamer/yt_import.py` matches; body contains `avatar_uncropped` and `extract_flat` does NOT appear in the avatar opts.
    - `grep -n "w is not None and h is not None and w != h\|w is not None" musicstreamer/yt_import.py` confirms the null-safe square guard.
    - `.venv/bin/python -m pytest tests/test_yt_import_library.py -k avatar -x` passes: prefers avatar_uncropped, rejects non-square, allows None dims, raises on missing.
  </acceptance_criteria>
  <done>fetch_channel_avatar selects avatar_uncropped, rejects only genuinely non-square entries, handles video→channel two-step, returns downloaded bytes; avatar tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-provider avatar fetcher registry (D-04)</name>
  <files>musicstreamer/yt_import.py, tests/test_yt_import_library.py</files>
  <read_first>
    - musicstreamer/yt_import.py (top-of-module + the new fetch_channel_avatar from Task 1; Callable/Optional already imported L14)
    - 89-PATTERNS.md §`musicstreamer/yt_import.py` "Provider registry pattern (D-04)"
    - 89-RESEARCH.md §Pattern 6 (per-provider fetch registry) — keep it minimal; YT-gated activation now (D-10), structured so 89b adds Twitch by registering only
  </read_first>
  <behavior>
    - get_avatar_fetcher("youtube") returns the fetch_channel_avatar callable after module import.
    - get_avatar_fetcher("twitch") returns None (not registered this phase).
    - register_avatar_fetcher("twitch", fn) then get_avatar_fetcher("twitch") returns fn (proves 89b extensibility).
  </behavior>
  <action>
    Add the minimal registry to yt_import.py after fetch_channel_avatar: module-level `_AVATAR_FETCHERS: dict[str, Callable[[str], bytes]] = {}`, `def register_avatar_fetcher(provider, fetcher) -> None`, `def get_avatar_fetcher(provider) -> Optional[Callable[[str], bytes]]`. Register YouTube at module load: `register_avatar_fetcher("youtube", fetch_channel_avatar)`. This is the D-04 hook Phase 89b uses to add Twitch with zero dialog/cover-slot rework. Add registry tests (youtube present, twitch absent then registerable) to tests/test_yt_import_library.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_yt_import_library.py -k avatar -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "register_avatar_fetcher(\"youtube\"" musicstreamer/yt_import.py` matches (youtube pre-registered).
    - `grep -n "def register_avatar_fetcher\|def get_avatar_fetcher\|_AVATAR_FETCHERS" musicstreamer/yt_import.py` shows all three registry members.
    - `.venv/bin/python -m pytest tests/test_yt_import_library.py -k avatar -x` passes: youtube resolves, twitch is None then registerable.
  </acceptance_criteria>
  <done>The provider registry exposes youtube and accepts runtime registration (89b-ready); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → process | yt-dlp returns thumbnail URLs and avatar bytes from untrusted YouTube CDN |
| yt-dlp → cookies file | extract_info may write cookies on exit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Tampering | malformed/oversized avatar bytes from CDN | mitigate (partial here) | Square-dimension reject filter rejects mismatched entries; raw bytes are validated as a real image via QPixmap.isNull() at the render boundary (Plan 04/05) before any disk write is rendered — bytes are never exec'd/eval'd |
| T-89-05 | Information Disclosure | yt-dlp writing to canonical cookies.txt | mitigate | fetch_channel_avatar wraps YoutubeDL in cookie_utils.temp_cookies_copy() (same as scan_playlist) so the canonical cookie file is never mutated |
| T-89-06 | Denial of Service | network hang on avatar download | mitigate | urllib.request.urlopen(url, timeout=10) bounds the download; runs on a worker thread (Plan 05) so the Qt main thread never blocks |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_yt_import_library.py -k avatar -x` green (filter + registry).
- No `extract_flat` key in the avatar opts (grep gate above).
</verification>

<success_criteria>
- fetch_channel_avatar reliably selects avatar_uncropped and rejects only genuinely non-square entries.
- Video URLs resolve to their channel before avatar fetch.
- Provider registry is in place with youtube registered; twitch slot open for 89b.
</success_criteria>

<output>
Create `.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-02-SUMMARY.md` when done.
</output>
