---
phase: 89-youtube-channel-avatar-fetch-cover-slot-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/models.py
  - musicstreamer/repo.py
  - musicstreamer/assets.py
  - tests/test_repo.py
  - tests/test_assets_avatar.py
autonomous: true
requirements: [ART-AVATAR-08]
must_haves:
  truths:
    - "Station dataclass carries channel_avatar_path through every read mapper"
    - "repo.update_channel_avatar_path writes the column without touching other fields"
    - "assets.write_channel_avatar persists PNG bytes atomically and returns a data_dir-relative path"
  artifacts:
    - path: "musicstreamer/models.py"
      provides: "channel_avatar_path field on Station"
      contains: "channel_avatar_path"
    - path: "musicstreamer/repo.py"
      provides: "channel_avatar_path in 4 Station mappers + update_channel_avatar_path write method"
      contains: "def update_channel_avatar_path"
    - path: "musicstreamer/assets.py"
      provides: "atomic avatar PNG writer"
      contains: "def write_channel_avatar"
  key_links:
    - from: "musicstreamer/repo.py"
      to: "stations.channel_avatar_path column (89a)"
      via: "UPDATE statement + row mapper read"
      pattern: "channel_avatar_path"
    - from: "musicstreamer/assets.py"
      to: "paths.channel_avatars_dir()"
      via: "tempfile.mkstemp + os.replace"
      pattern: "os\\.replace"
---

<objective>
Thread the Phase 89a `channel_avatar_path` column through the model/read/write layer (D-13) and add an atomic avatar PNG writer (D-12). This is the foundation that the fetch service (Plan 02) and the now-playing render path (Plan 04) consume.

Purpose: The DB column exists (89a) but is not wired into `Station`, the 4 row mappers, or any write boundary. Without this plumbing nothing downstream can persist or read an avatar.
Output: `channel_avatar_path` on `Station`, in all 4 read mappers, a dedicated `update_channel_avatar_path` write method, and `assets.write_channel_avatar` atomic writer — all unit-tested.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-RESEARCH.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-PATTERNS.md

<interfaces>
<!-- Verified from codebase. Use directly — no exploration. -->

models.py Station dataclass (L27+), last fields:
  cover_art_source: Literal["auto","itunes_only","mb_only"] = "auto"   # L36 Phase 73
  prerolls_fetched_at: Optional[int] = None                            # L42 Phase 83

repo.py — 4 Station(...) constructor call sites that need the new kwarg:
  list_stations           ~L556
  get_station             ~L595
  list_recently_played    ~L704
  list_favorite_stations  ~L819
repo.py write-method template (do NOT extend update_station — Pitfall 5):
  def update_station_art(self, station_id, art_path) -> None  ~L839
  (cover_art_source keyword-default precedent at ~L565)

assets.py (L1-4 imports: os, shutil, from musicstreamer import paths):
  def ensure_dirs()                                  # L7 — already creates channel_avatars_dir (89a)
  def copy_asset_for_station(...) -> str             # L13 — uses shutil.copy2 (NON-atomic; do NOT replicate)

paths.py (89a, already present — no changes):
  def channel_avatars_dir() -> str                   # L103
  def data_dir() -> str
  _root_override: str | None = None                  # L25 — test isolation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread channel_avatar_path through models + repo read/write</name>
  <files>musicstreamer/models.py, musicstreamer/repo.py, tests/test_repo.py</files>
  <read_first>
    - musicstreamer/models.py (Station dataclass L27-42 — field ordering + Phase 73/83 comment style)
    - musicstreamer/repo.py (4 Station constructors at ~L556, ~L595, ~L704, ~L819; cover_art_source keyword pattern at ~L565; update_station_art at ~L839; update_station signature ~L617 — confirm you do NOT touch it)
    - 89-PATTERNS.md §`musicstreamer/repo.py` (the cover_art_source keyword pattern and update_station_art write-method template) and §`musicstreamer/models.py`
    - tests/test_repo.py (existing repo test conventions, fixture/connection setup, _root_override usage)
  </read_first>
  <behavior>
    - A station round-tripped through repo with no avatar set reads back channel_avatar_path == None.
    - After update_channel_avatar_path(station_id, "assets/channel-avatars/12.png"), get_station(id).channel_avatar_path == "assets/channel-avatars/12.png".
    - update_channel_avatar_path(station_id, None) clears the column back to None.
    - Calling the existing update_station path (without the avatar kwarg) does NOT reset channel_avatar_path (Pitfall 5 — dedicated method, not update_station).
  </behavior>
  <action>
    Add `channel_avatar_path: Optional[str] = None  # Phase 89 D-13` to the `Station` dataclass in models.py immediately after `prerolls_fetched_at` (~L42), matching the existing trailing-comment style. Add `channel_avatar_path=r["channel_avatar_path"],  # Phase 89 D-13` to all 4 `Station(...)` constructor call sites in repo.py (`list_stations` ~L556, `get_station` ~L595, `list_recently_played` ~L704, `list_favorite_stations` ~L819) — no `or` default, `None` is the correct sentinel. Add a dedicated write method `def update_channel_avatar_path(self, station_id: int, path: Optional[str]) -> None` mirroring `update_station_art` (~L839): single `UPDATE stations SET channel_avatar_path = ? WHERE id = ?` + `self.con.commit()`. Do NOT add the column to `update_station` (silent-reset footgun per RESEARCH.md Pitfall 5). Add the round-trip + clear + no-reset tests to tests/test_repo.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "channel_avatar_path" musicstreamer/models.py` shows the new field on Station.
    - `grep -c "channel_avatar_path=r\[" musicstreamer/repo.py` returns 4 (all mappers wired).
    - `grep -n "def update_channel_avatar_path" musicstreamer/repo.py` matches; `def update_station` body does NOT reference channel_avatar_path.
    - `.venv/bin/python -m pytest tests/test_repo.py -x` passes including the new round-trip/clear/no-reset tests.
  </acceptance_criteria>
  <done>Station carries channel_avatar_path; all 4 mappers read it; update_channel_avatar_path writes/clears it in isolation; repo tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add assets.write_channel_avatar atomic PNG writer</name>
  <files>musicstreamer/assets.py, tests/test_assets_avatar.py</files>
  <read_first>
    - musicstreamer/assets.py (L1-28 — imports, ensure_dirs, copy_asset_for_station shutil.copy2 NON-atomic pattern to NOT replicate)
    - musicstreamer/paths.py (channel_avatars_dir L103, data_dir, _root_override L25 test isolation)
    - 89-PATTERNS.md §`musicstreamer/assets.py` (the tempfile.mkstemp + os.replace atomic-write pattern; relpath-to-data_dir return value) and §Shared Patterns "Atomic file write" + "_root_override test isolation"
    - 89-RESEARCH.md §Pattern 5 (atomic overwrite) + Assumptions A2 (os.replace same-filesystem requirement → mkstemp dir=dst_dir)
  </read_first>
  <behavior>
    - write_channel_avatar(12, b"<pngbytes>") creates assets/channel-avatars/12.png under the _root_override and returns "assets/channel-avatars/12.png" (relative to data_dir).
    - A second write_channel_avatar(12, b"<newbytes>") overwrites atomically — final file contains newbytes, no .tmp file remains in the dir.
    - On simulated write failure no partial/temp file is left behind (temp cleaned up, exception re-raised).
  </behavior>
  <action>
    Add `def write_channel_avatar(station_id: int, data: bytes) -> str` to assets.py implementing D-12 atomic overwrite: resolve `dst_dir = paths.channel_avatars_dir()`, `os.makedirs(dst_dir, exist_ok=True)`, `dst = os.path.join(dst_dir, f"{station_id}.png")`, write via `tempfile.mkstemp(dir=dst_dir, suffix=".png.tmp")` then `os.write`/`os.close`/`os.replace(tmp, dst)`; on exception close fd, unlink the temp, re-raise. Return `os.path.relpath(dst, paths.data_dir())`. Add `import tempfile` (module-level, alongside existing os/shutil). Station id is an int from SQLite (no traversal risk — threat T-89-02). Write the three tests in tests/test_assets_avatar.py using `paths._root_override = str(tmp_path)` set/reset.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_assets_avatar.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def write_channel_avatar" musicstreamer/assets.py` matches and the body contains `os.replace` and `tempfile.mkstemp`.
    - `.venv/bin/python -m pytest tests/test_assets_avatar.py -x` passes: write returns the relative path, overwrite leaves no `.tmp`, failure cleans up.
    - No `.png.tmp` file remains in channel_avatars_dir after any test.
  </acceptance_criteria>
  <done>write_channel_avatar persists PNG bytes atomically via mkstemp+os.replace, returns a data_dir-relative path, cleans up on failure; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process → filesystem | avatar bytes (from network in Plan 02) are written to disk here; station-id-derived filename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Denial of Service | assets.write_channel_avatar PNG write | mitigate | Atomic write via tempfile.mkstemp(dir=dst_dir) + os.replace — interrupted write never leaves a corrupt destination file; temp cleaned on failure (Task 2) |
| T-89-02 | Tampering | path construction from station_id | mitigate | station_id is an int from SQLite, formatted as f"{station_id}.png"; path built only via paths.channel_avatars_dir() — no user-controlled string, no traversal |
| T-89-03 | Tampering | update_station silent column reset | mitigate | channel_avatar_path written ONLY via dedicated update_channel_avatar_path; update_station never touches the column (Pitfall 5) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_repo.py tests/test_assets_avatar.py tests/test_paths.py -x` green.
- Per-wave scoped run (89-VALIDATION.md) passes for the touched modules.
</verification>

<success_criteria>
- channel_avatar_path round-trips through repo (read + write) in isolation.
- assets.write_channel_avatar writes atomically and returns a data_dir-relative path.
- No regression in existing repo/paths tests.
</success_criteria>

<output>
Create `.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-01-SUMMARY.md` when done.
</output>
