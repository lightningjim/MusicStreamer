---
phase: 72.4
plan: 03
type: execute
wave: 2
depends_on: ["72.4-02"]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: false
requirements: ["LAYOUT-04"]
tags: [pyside6, qt-layout, reflow-predicate, resize-event-extension]

must_haves:
  truths:
    - "self._controls_row3: QHBoxLayout exists, has setSpacing(8), is appended to `center` immediately after self._controls_row2 (between line 577 and the `_stats_widget` block at line 582)"
    - "self._reflow_volume_cluster() exists with defensive guards (hasattr volume_slider / compact_mode_toggle_btn / _controls_row3; panel_width <= 0 early-return), reads include_picker = not self.stream_combo.isHidden(), computes should_wrap via self._row1_min_width(include_picker=..., include_volume_cluster=False), dispatches to controls / _controls_row2 / _controls_row3 per UI-SPEC §Target-Row Selection, and toggles volume's size policy via _set_volume_size_policy(expanding=should_wrap)"
    - "resizeEvent on NowPlayingPanel calls self._reflow_volume_cluster() BETWEEN self._reflow_stream_picker_row() and self._apply_art_tier()"
    - "All 16 RESEARCH §Validation Architecture rows in tests/test_phase72_4_volume_cluster_reflow.py now exit 0 (full GREEN)"
    - "test_no_lambda_in_reflow_wiring source-grep negative is GREEN across the entire reflow helper set (72.1 + 72.4)"
    - "test_no_addstretch_in_wrap_path source-grep negative is GREEN"
    - "test_no_repo_set_setting_for_cluster_wrap_state is GREEN (D-09 inheritance: no SQLite persistence of layout state)"
    - "tests/test_phase72_1_stream_picker_reflow.py still exits 0 (no regression on picker reflow)"
    - "tests/test_now_playing_panel.py and tests/test_phase72_3_*.py still exit 0 (no regression on broader panel suite or art-tier)"
    - "Width 560 (the user's reproducer) on multi-stream station → picker on row 2, cluster on row 3, all widgets non-overlapping per cluster invariants"
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "self._controls_row3 layout container, self._reflow_volume_cluster predicate, resizeEvent extension"
      contains: "_reflow_volume_cluster"
  key_links:
    - from: "resizeEvent"
      to: "_reflow_volume_cluster"
      via: "third call between _reflow_stream_picker_row and _apply_art_tier"
      pattern: "self\\._reflow_volume_cluster\\(\\)"
    - from: "_reflow_volume_cluster"
      to: "_row1_min_width"
      via: "self._row1_min_width(include_picker=not self.stream_combo.isHidden(), include_volume_cluster=False)"
      pattern: "_row1_min_width\\(include_picker=not self\\.stream_combo\\.isHidden\\(\\), include_volume_cluster=False\\)"
    - from: "_reflow_volume_cluster"
      to: "_move_volume_cluster_to"
      via: "dispatch to controls / _controls_row2 / _controls_row3 per target-row selection"
      pattern: "self\\._move_volume_cluster_to\\("
    - from: "_reflow_volume_cluster"
      to: "_set_volume_size_policy"
      via: "self._set_volume_size_policy(expanding=should_wrap)"
      pattern: "_set_volume_size_policy\\(expanding="
    - from: "center QVBoxLayout"
      to: "_controls_row3"
      via: "center.addLayout(self._controls_row3) after center.addLayout(self._controls_row2)"
      pattern: "center\\.addLayout\\(self\\._controls_row3\\)"
---

<objective>
Land the final wiring that makes the 72.4 volume-cluster reflow live: construct `self._controls_row3` in `__init__`, add the `_reflow_volume_cluster` predicate, and extend the `resizeEvent` override to call it between the existing 72.1 picker reflow and the 72.3 art-tier reflow. After this plan, every Plan 01 RED test turns GREEN, the user's 560px reproducer no longer overlaps, and the LAYOUT-04 contract is fully delivered.

Purpose: close LAYOUT-04 (Phase 72.4) by wiring the helpers from Plan 02 into the resize-driven layout state machine. Both wrap tiers (Phase 72.1 picker, Phase 72.4 cluster) now fire independently per UI-SPEC §State Truth Table.

Output: three surgical edits to ONE file (`musicstreamer/ui_qt/now_playing_panel.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-VALIDATION.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-01-PLAN.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-02-PLAN.md
@musicstreamer/ui_qt/now_playing_panel.py
@tests/test_phase72_4_volume_cluster_reflow.py

<interfaces>
<!-- Existing symbols this plan reads / extends. After Plan 02 lands: -->

From musicstreamer/ui_qt/now_playing_panel.py (post-Plan-02 state):
  - self.controls: QHBoxLayout  (row 1, line 455 area)
  - self._controls_row2: QHBoxLayout  (row 2, line 575-577, Phase 72.1)
  - self._row1_min_cache: dict[tuple[bool, bool], int]  (line 297 area, from Plan 02 Task 1)
  - self._volume_cluster_row1_index: int  (after volume_slider addWidget, from Plan 02 Task 1)
  - self._row1_min_width(include_picker, include_volume_cluster) -> int  (from Plan 02 Task 2)
  - self._move_volume_cluster_to(target_layout) -> None  (from Plan 02 Task 3)
  - self._set_volume_size_policy(expanding) -> None  (from Plan 02 Task 3)
  - self.stream_combo.isHidden()  (single-source-of-truth for stream count; set by _populate_stream_picker via setVisible(len > 1))
  - resizeEvent(event) at line 1170-1185 — currently calls super, _reflow_stream_picker_row, _apply_art_tier
  - center: QVBoxLayout  (the local variable in __init__ that owns controls + _controls_row2 + future _controls_row3)

New symbols this plan introduces:
  - self._controls_row3: QHBoxLayout  (constructed in __init__ after _controls_row2)
  - self._reflow_volume_cluster() -> None  (method, placed adjacent to _reflow_stream_picker_row)

Test surface (from Plan 01, currently RED for cluster-membership tests):
  - tests/test_phase72_4_volume_cluster_reflow.py — all 16 RESEARCH §Validation Architecture rows
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct self._controls_row3 in __init__ between _controls_row2 and _stats_widget</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
  </read_first>
  <behavior>
    - In NowPlayingPanel.__init__, IMMEDIATELY AFTER the existing `center.addLayout(self._controls_row2)` call (currently line 577) and IMMEDIATELY BEFORE the `self._stats_widget = self._build_stats_widget()` block (currently line 582), insert three lines:
      1. `self._controls_row3 = QHBoxLayout()`
      2. `self._controls_row3.setSpacing(8)`  # mirrors row 1 and row 2 — see PATTERNS §Pattern 3
      3. `center.addLayout(self._controls_row3)`
    - A comment block above these three lines cites Phase 72.4 / LAYOUT-04 + CONTEXT D-04 (multi-stream wrap target) + UI-SPEC §"Row 3 Visual Contract" (zero-item collapse, no setContentsMargins, inherits center margins). Mirror the prose shape of the Phase 72.1 `_controls_row2` comment at line 572-574.
    - No `setContentsMargins` call on `_controls_row3` (inherits `center`). PATTERNS §Pattern 3 Landmine: "Insertion ORDER inside `center` is load-bearing: if `_controls_row3` is added AFTER `_stats_widget` (or worse, after `_gbs_playlist_widget` at line 591), the visual row order on screen breaks."
  </behavior>
  <action>
    Apply a single Edit inserting the three lines + comment block at the exact insertion site (between line 577 and 582 of the post-Plan-02 source). After landing, run a Python import smoke test (`uv run python -c 'from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel'`) to confirm no syntax error, then run the 72.1 regression suite — `_controls_row3` is empty so 72.1 picker behavior is unaffected. The Plan 01 RED tests now stop crashing with `AttributeError: '_controls_row3'` but still fail on the missing `_reflow_volume_cluster` (Task 2) — partial progress.
  </action>
  <verify>
    <automated>grep -q 'self\._controls_row3 = QHBoxLayout()' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'self\._controls_row3\.setSpacing(8)' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'center\.addLayout(self\._controls_row3)' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; uv run python -c 'from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel' &amp;&amp; uv run python -c "import inspect; from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; src = inspect.getsource(NowPlayingPanel.__init__); r2 = src.find('center.addLayout(self._controls_row2)'); r3 = src.find('center.addLayout(self._controls_row3)'); stats = src.find('_stats_widget = self._build_stats_widget'); assert 0 &lt;= r2 &lt; r3 &lt; stats, f'order violation: r2={r2} r3={r3} stats={stats}'" &amp;&amp; uv run pytest tests/test_phase72_1_stream_picker_reflow.py tests/test_now_playing_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All three insertion lines grep cleanly: `grep -q 'self\._controls_row3 = QHBoxLayout()'`, `grep -q 'self\._controls_row3\.setSpacing(8)'`, `grep -q 'center\.addLayout(self\._controls_row3)'` succeed.
    - The inspect.getsource ordering gate confirms `center.addLayout(self._controls_row2)` precedes `center.addLayout(self._controls_row3)` precedes `_stats_widget = self._build_stats_widget` in `NowPlayingPanel.__init__`.
    - `setContentsMargins` is NOT called on `_controls_row3` — `grep -q '_controls_row3.setContentsMargins' musicstreamer/ui_qt/now_playing_panel.py` returns no match.
    - `uv run python -c 'from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel'` succeeds.
    - `uv run pytest tests/test_phase72_1_stream_picker_reflow.py tests/test_now_playing_panel.py -x -q` exits 0 (no regression).
  </acceptance_criteria>
  <done>self._controls_row3 is constructed empty between row 2 and stats; no margins/contents tweaks; visual order is row1 → row2 → row3 → stats → gbs_playlist; 72.1 tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _reflow_volume_cluster predicate + wire into resizeEvent</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py
    tests/test_phase72_4_volume_cluster_reflow.py
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
  </read_first>
  <behavior>
    - Add `_reflow_volume_cluster(self) -> None` as a new method placed IMMEDIATELY AFTER `_reflow_stream_picker_row` (currently ends around line 1432). Method body matches RESEARCH §"New _reflow_volume_cluster predicate" (lines 869-922 of RESEARCH.md) and PATTERNS §Pattern 7 sketch:
        * Defensive guards (Pitfalls 5, 6 from RESEARCH): `if not hasattr(self, "volume_slider"): return`; `if not hasattr(self, "compact_mode_toggle_btn"): return`; `if not hasattr(self, "_controls_row3"): return`; then `panel_width = self.width(); if panel_width <= 0: return`.
        * Compute threshold: `include_picker = not self.stream_combo.isHidden()` (per PATTERNS §Pattern 7 Landmine + RESEARCH Pitfall 4: use `isHidden()` NOT `isVisible()` — intended-visibility semantics); `threshold = self._row1_min_width(include_picker=include_picker, include_volume_cluster=False)`; `should_wrap = panel_width < threshold`.
        * Target-row selection per UI-SPEC §Target-Row Selection table:
          - `if not should_wrap:` → `target = self.controls`
          - `elif self.stream_combo.isHidden():` (single-stream narrow per D-07) → `target = self._controls_row2`
          - `else:` (multi-stream very narrow per D-04) → `target = self._controls_row3`
        * Dispatch: `self._move_volume_cluster_to(target)` then `self._set_volume_size_policy(expanding=should_wrap)`.
        * Method docstring cites D-01..D-14, RESEARCH Pitfalls 4-6, and PATTERNS §Pattern 7 Landmine "No `return` after the single-stream-hidden branch — unlike 72.1's predicate, 72.4 must always fall through to the width-compare + dispatch."
        * No `lambda` anywhere; no nested closures; bound-method dispatch only (QA-05).
    - Extend `resizeEvent(self, event)` (currently lines 1170-1185) to call `self._reflow_volume_cluster()` BETWEEN the existing `self._reflow_stream_picker_row()` call and the existing `self._apply_art_tier()` call. Per RESEARCH §Pattern 4 + UI-SPEC §"Co-existence With Phase 72.1's and Phase 72.3's resizeEvent" + PATTERNS §Pattern 8:
        * `super().resizeEvent(event)` remains FIRST (mandatory per 72.1 Pitfall 9).
        * `self._reflow_stream_picker_row()` — Phase 72.1 / LAYOUT-02 (unchanged).
        * `self._reflow_volume_cluster()` — Phase 72.4 / LAYOUT-04 — NEW.
        * `self._apply_art_tier()` — Phase 72.3 / LAYOUT-03 (unchanged).
        * The resizeEvent docstring is updated to add a "Phase 72.4 / LAYOUT-04: volume-cluster reparent on very-narrow widths (NEW)" line.
        * No additional `super().resizeEvent()` call (PATTERNS §Pattern 8 Landmine "No double-dispatch").
    - After this task, the Plan 01 RED test file is fully GREEN, including all six-state truth table rows, signal-survival round-trip, idempotency, widen-back, station-change rehome, CR-01 echo, cluster-as-unit, order, compact-toggle size invariant, and the three negative-assertion source-greps.
  </behavior>
  <action>
    Two surgical Edits: (a) insert the new `_reflow_volume_cluster` method body immediately after `_reflow_stream_picker_row`'s closing line (~1432). The body is taken from RESEARCH §"New _reflow_volume_cluster predicate" (lines 869-922 of RESEARCH.md) — quote the docstring's invariant claims verbatim. (b) extend the `resizeEvent` body with the one-line `self._reflow_volume_cluster()` call between the existing two helper calls; update the docstring header to add the LAYOUT-04 line per UI-SPEC §"Co-existence With Phase 72.1's and Phase 72.3's resizeEvent" code block. After landing, the full LAYOUT-04 acceptance gate fires: focused 72.4 tests, 72.1 regression, 72.3 regression, broader now-playing-panel suite. The full suite (`uv run pytest tests/ -x`) is the phase-gate.
  </action>
  <verify>
    <automated>grep -q 'def _reflow_volume_cluster(self) -&gt; None' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'self\._reflow_volume_cluster()' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; uv run python -c "import inspect; from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; src = inspect.getsource(NowPlayingPanel.resizeEvent); p = src.find('_reflow_stream_picker_row()'); v = src.find('_reflow_volume_cluster()'); a = src.find('_apply_art_tier()'); assert 0 &lt; p &lt; v &lt; a, f'resizeEvent order broken: picker={p} volume={v} art={a}'" &amp;&amp; uv run python -c "import inspect; from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; src = inspect.getsource(NowPlayingPanel._reflow_volume_cluster); assert 'lambda' not in src; assert 'isHidden()' in src; assert 'include_volume_cluster=False' in src; assert 'should_wrap' in src" &amp;&amp; uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q &amp;&amp; uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q &amp;&amp; uv run pytest tests/test_now_playing_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def _reflow_volume_cluster(self) -&gt; None'` succeeds.
    - `grep -q 'self\._reflow_volume_cluster()'` succeeds (the resizeEvent call site).
    - inspect.getsource ordering gate on `NowPlayingPanel.resizeEvent` confirms `_reflow_stream_picker_row()` precedes `_reflow_volume_cluster()` precedes `_apply_art_tier()`.
    - inspect.getsource on `_reflow_volume_cluster` shows no `lambda`, uses `isHidden()` (not `isVisible()`), uses `include_volume_cluster=False` in the threshold call, computes `should_wrap`.
    - `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q` exits 0 — FULL GREEN, all 16 RESEARCH rows pass including the three negative source-greps.
    - `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` exits 0 (no Phase 72.1 regression).
    - `uv run pytest tests/test_now_playing_panel.py -x -q` exits 0 (no broader now-playing-panel regression).
    - The full reflow helper set has no lambdas — inspect-getsource over `(_reflow_stream_picker_row, _reflow_volume_cluster, _move_stream_picker_to, _move_volume_cluster_to, _set_picker_size_policy, _set_volume_size_policy, _row1_min_width)` confirms none contain `lambda`.
  </acceptance_criteria>
  <done>_reflow_volume_cluster predicate exists with defensive guards, isHidden-based stream-count branching, three-target dispatch (controls / _controls_row2 / _controls_row3), and is wired into resizeEvent between the 72.1 picker reflow and the 72.3 art-tier reflow. All Plan 01 RED tests GREEN; no regression on Phase 72.1 or broader now-playing-panel suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — 560px reproducer on the user's small lower display</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <action>Pause for the user to perform the live UAT on their small lower display (the original reproducer surface). Do NOT modify code in this task; the implementation is fully landed by Tasks 1 and 2. The executor's only job is to surface the &lt;how-to-verify&gt; steps below, wait for the &lt;resume-signal&gt;, and record the outcome in the SUMMARY. If the user reports a regression, do NOT attempt an in-task hotfix — open a gap-closure plan via /gsd:plan-phase 72.4 --gaps.</action>
  <what-built>
    - Phase 72.4 / LAYOUT-04 fully landed: the volume slider + compact-mode toggle wrap as a unit to row 3 (multi-stream very-narrow) or row 2 (single-stream narrow) when the now-playing panel narrows past the second threshold.
    - Both wrap tiers (Phase 72.1 picker, Phase 72.4 cluster) fire independently per the six-state truth table.
    - Volume slider's `setFixedWidth(120)` ↔ `Expanding` round-trip uses the canonical Qt 6 three-call sequence (Pitfall 1 mitigation).
    - Compact-toggle stays 28×28 fixed in every state (D-11 invariant).
    - Width-driven, not mode-coupled (D-12).
    - All 174 pre-existing pytest tests still green plus the new 16-row 72.4 test file fully green.
    - No SQLite persistence; layout state recomputed every resize.
    - Verified locally (deployment target = Linux Wayland / GNOME Shell / DPR=1.0 per memory `project_deployment_target.md`).
  </what-built>
  <how-to-verify>
    1. Launch the app on the user's lower display (the original reproducer surface from CONTEXT.md "Reproducer anchor"). Confirm the sidebar is EXPANDED (compact OFF — Phase 72 compact-toggle in unchecked state).
    2. Resize the main window so the right pane reaches roughly the 560px minimum. The right pane minimum is enforced by `main_window.py:310 setMinimumWidth(560)`.
    3. Visual no-overlap check (PATTERNS §VALIDATION.md Manual-Only row 2):
        - Volume slider does NOT visually overlap the compact-toggle button.
        - Compact-toggle is fully visible on row 3 (multi-stream station) or row 2 (single-stream station — try with a non-AA, non-multi station like a SomaFM single).
        - Transport+actions cluster on row 1 (play / stop / edit / star / eq) fits without truncation.
    4. Volume-drag-across-resize continuity (VALIDATION row 1):
        - Begin dragging the volume slider thumb.
        - While dragging, widen the panel back past the threshold so the cluster returns to row 1.
        - Slider should remain grabbed; volume should keep updating; no Python tracebacks in the terminal.
        - Release; volume should be at the final position.
    5. Compact-toggle icon flip across reparent (VALIDATION row 3):
        - In the very-narrow state with cluster on row 3, click the compact-toggle button.
        - Sidebar should hide; icon should flip to `sidebar-show-symbolic`.
        - Click again; sidebar reappears; icon flips back to `sidebar-hide-symbolic`; tooltip cycles correctly.
    6. Splitter-drag flicker check (VALIDATION row 4):
        - Drag the main-window splitter slowly across both thresholds (picker threshold and volume-cluster threshold).
        - No visual flicker, no layout-reshuffle artifacts, no transient overlap.
        - Drag rapidly back and forth several times. No accumulated layout corruption; no ghost widgets at old positions.
    7. Confirm `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py tests/test_phase72_1_stream_picker_reflow.py tests/test_phase72_3_*.py tests/test_now_playing_panel.py -x -q` exits 0 (full sampling-rate wave check per VALIDATION.md).
  </how-to-verify>
  <resume-signal>Type "approved" once steps 1-7 confirm the reproducer is closed and no regression observed; or describe issues encountered (which step, what you saw vs expected). If issues block GREEN, name the failing test or visual symptom so /gsd-verify-work can route to a gap-closure plan.</resume-signal>
  <verify>
    <human-check>User confirms steps 1-7 of &lt;how-to-verify&gt; pass on the live lower-display reproducer. The full automated suite (`uv run pytest tests/test_phase72_4_volume_cluster_reflow.py tests/test_phase72_1_stream_picker_reflow.py tests/test_phase72_3_*.py tests/test_now_playing_panel.py -x -q`) MUST exit 0 before this checkpoint is presented.</human-check>
  </verify>
  <done>User has typed "approved" (or equivalent affirmative); no visual overlap at 560px right pane; volume drag survives resize-across-threshold; compact-toggle icon flips correctly on reparent; no splitter-drag flicker. The phase reproducer (CONTEXT.md "Reproducer anchor") is closed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary. Phase 72.4 Plan 03 is a pure intra-class Qt-method addition + override extension, no I/O, no untrusted input. All state mutations are layout-only; no SQLite persistence (D-09 inheritance from Phase 72).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.4-06 | Tampering | _reflow_volume_cluster — using `isVisible()` instead of `isHidden()` would silently misclassify single-stream as multi-stream in pre-show test contexts (RESEARCH Pitfall 4 — 72.1 burned a plan on this) | mitigate | Acceptance criterion source-greps for `isHidden()`; `test_narrow_single_stream_cluster_in_row_2` from Plan 01 directly exercises this branch and would FAIL if `isVisible()` is used. |
| T-72.4-07 | Tampering | resizeEvent order — call order `picker → cluster → art-tier` is documented (UI-SPEC §"Co-existence"); a reversed order does not break correctness (the cluster's target-row selection reads `isHidden()` which is layout-agnostic) but violates the documented convention | mitigate | Acceptance criterion inspect.getsource gates the relative offsets of the three calls. |
| T-72.4-08 | Tampering | resizeEvent recursion — accidentally calling `self.resize(...)` or `self.setGeometry(...)` inside any helper would cause infinite Qt recursion | accept | None of the helpers in Plan 02 or Plan 03 reach the resize/setGeometry APIs — verified by inspect.getsource. The 72.1 / 72.3 precedent established the discipline; this plan extends it. |
| T-72.4-09 | DoS | Resize event spam during splitter drag could over-trigger reflow | accept | Reflow cost is ~3µs per tick (three idempotent helpers, cached threshold, comparison). Wayland coalesces resize events. RESEARCH §"Security Domain" notes this is not a concern. |
| T-72.4-SC | Tampering | npm/pip/cargo installs | accept | No new packages. |

</threat_model>

<verification>
- `grep -q 'self\._controls_row3 = QHBoxLayout()'` succeeds.
- inspect.getsource gate on `NowPlayingPanel.__init__` confirms `_controls_row2` add precedes `_controls_row3` add precedes `_stats_widget` init.
- `grep -q 'def _reflow_volume_cluster(self) -&gt; None'` succeeds.
- inspect.getsource gate on `NowPlayingPanel.resizeEvent` confirms picker call precedes volume call precedes art-tier call.
- inspect.getsource gate on `_reflow_volume_cluster` confirms `lambda` absent, `isHidden()` present, `include_volume_cluster=False` present.
- `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q` exits 0 (full GREEN — closes LAYOUT-04).
- `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` exits 0 (no Phase 72.1 regression).
- `uv run pytest tests/test_now_playing_panel.py -x -q` exits 0 (no broader now-playing regression).
- Full-suite green: `uv run pytest tests/ -x -q` exits 0 (phase gate per VALIDATION.md).
- UAT on the user's lower display closes the original reproducer (CONTEXT.md "Reproducer anchor": sidebar expanded, ~560px right pane, no overlap on row 1).
</verification>

<success_criteria>
1. `self._controls_row3` constructed in `__init__` between `_controls_row2` and `_stats_widget` with `setSpacing(8)`, no `setContentsMargins`.
2. `_reflow_volume_cluster` predicate implemented with all defensive guards, `isHidden()`-based stream-count branching, three-target dispatch, no lambda, no addStretch.
3. `resizeEvent` calls `_reflow_volume_cluster()` BETWEEN picker reflow and art-tier reflow; super-call remains FIRST.
4. All 16 RESEARCH §Validation Architecture rows GREEN in `tests/test_phase72_4_volume_cluster_reflow.py`.
5. Phase 72.1 and Phase 72.3 test files exit 0 (no regression).
6. Full pytest suite exits 0 (phase gate).
7. UAT on lower display confirms: no overlap at 560px right pane, multi-stream → 3 rows visible, single-stream → 2 rows visible, volume drag survives resize, compact-toggle icon flips correctly across reparent, no splitter-drag flicker.
8. User signs off on Task 3 checkpoint with "approved".
</success_criteria>

<output>
Create `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-03-SUMMARY.md` when done. Record:
- Final diff stat on now_playing_panel.py (lines added across Plans 02 + 03).
- Pytest count: 72.4 file tests passing / total; 72.1 regression file tests; broader now-playing-panel tests.
- UAT outcome: user approval status + any visual notes from the live test on the lower display.
- Confirmation that the resizeEvent docstring is updated to include the Phase 72.4 / LAYOUT-04 line.
- Any deviations from RESEARCH §"New _reflow_volume_cluster predicate" pattern (none expected).
- LAYOUT-04 status update for REQUIREMENTS.md traceability table: should flip from `Pending` to `Complete` once phase.complete fires (NOT part of this plan; phase-complete tool handles it).
</output>
</content>
</invoke>