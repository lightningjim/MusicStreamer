---
context: phase
phase: 72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid
task: 0
total_tasks: 6
status: paused
last_updated: 2026-05-20T02:35:32.429Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| setFixedWidth ↔ setSizePolicy direct swap | Calling `setSizePolicy(Expanding, Fixed)` AFTER `setFixedWidth(120)` does NOT release the width clamp. `setFixedWidth` pins `min == max == 120` directly via `setMinimumWidth/setMaximumWidth`; size policy alone won't grow the widget. | blocking | Wave 1 / Plan 02 Task 3 MUST implement the canonical Qt 6 THREE-CALL escape sequence: (1) `self.volume_slider.setMinimumWidth(0)` (2) `self.volume_slider.setMaximumWidth(16777215)` [QWIDGETSIZE_MAX] (3) `self.volume_slider.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Fixed)`. To go BACK to Fixed-120, `setFixedWidth(120)` alone is sufficient. See `72.4-RESEARCH.md` §Pattern 3 / Pitfall 1. |
| FakeRepo set_setting_calls field | Original plan draft instructed the test FakeRepo to record into a `self.set_setting_calls: list` field, then asserted on it in negative-1. But the analog FakeRepo at tests/test_phase72_1_stream_picker_reflow.py:75-120 has NO such field — it writes directly into `self._settings: dict`. Plan-checker B2 caught this; fix already applied. | advisory | Plan 01 already shipped the correct verbatim-mirror form. Wave 1+ executors do not regress this — Plan 02 doesn't touch the test file. |
| Worktree HEAD on protected ref | If a worktree agent's HEAD ends up on main/master/etc., self-recovery via `git update-ref` destroys concurrent commits. | advisory | Each `<worktree_branch_check>` block in the executor prompts asserts the branch matches `^worktree-agent-[A-Za-z0-9._/-]+$` and refuses to commit otherwise. Wave 0 executor passed this check. |

_All three are advisory at this resume point — the blocking one (three-call round-trip) is enforced structurally by Plan 02's verify chain (which greps for `setMinimumWidth(0)`, `setMaximumWidth(16777215)`, and `setFixedWidth(120)` in `_set_volume_size_policy`)._

<current_state>
Mid auto-chain pipeline launched by `/gsd-discuss-phase 72.4 --chain`. The chain has progressed: discuss → ui-phase (UI-SPEC.md, 6/6 pass) → research → patterns → validation → planner → plan-checker (round 1 found 2 blockers + 4 warnings; fixes applied; round 2 PASS) → execute-phase Wave 0 (Plan 01 RED scaffold + LAYOUT-04 registration, merged via worktree-agent-adbd640667392da8b, plan marked complete in ROADMAP/STATE).

**Pause point:** Immediately before spawning the Wave 1 executor for plan 72.4-02. The Agent() spawn was rejected by the user mid-call. No partial state on Plan 02 — clean restart from the top of Wave 1.

**Auto-chain flag is still ACTIVE** (`gsd-sdk query check auto-mode --pick auto_chain_active` returns `true`). Resuming will continue the chain.
</current_state>

<completed_work>

**Discuss / Design / Plan (round-1):**
- 72.4-CONTEXT.md (4 locked decisions D-A..D-D) — commit `b9047f3`
- 72.4-UI-SPEC.md (6/6 dimensions PASS) — commit `6448d73`
- 72.4-RESEARCH.md (8 patterns, 11 pitfalls, 16-test envelope, LAYOUT-04 row text) — commit `f94c8b1`
- 72.4-VALIDATION.md (Nyquist Dimension 8) — commit `2416e29`
- 72.4-PATTERNS.md (analog code excerpts) — commit `6bafbde`
- 72.4-01/02/03-PLAN.md (3 plans, 3 waves) — commit `e112617`
- Plan-checker round 1 fixes (B1, B2, W1, W2, W4) — commit `c4fd3e6`
- Plan-checker round 2: `## PLAN VERIFIED`

**Wave 0 / Plan 72.4-01 (TDD-RED scaffold + LAYOUT-04 registration):**
- Task 1: REQUIREMENTS.md LAYOUT-04 row + traceability row + coverage 62→63 / 41→42 — commit `a7f1136`
- Task 2: tests/test_phase72_4_volume_cluster_reflow.py with 16 test functions (17 collected with parametrize), FakeRepo verbatim from analog (NO `set_setting_calls` field), `_assert_volume_cluster_in_layout` helper verbatim from RESEARCH §Code Examples — commit `62e836b`
- SUMMARY.md — commit `5e5f203`
- Merge worktree-agent-adbd640667392da8b → main — commit `36e4ea6`
- Roadmap+state marked complete — commit `18cb6af`

**RED-state confirmation:** `uv run --extra test pytest tests/test_phase72_4_volume_cluster_reflow.py -x` exits non-zero at `test_wide_multi_stream_cluster_in_row_1` with `AttributeError: 'NowPlayingPanel' object has no attribute '_controls_row3'` — the genuine missing Plan 02 symbol.

**Regression sweep:** 153 passed in tests/test_phase72_1_stream_picker_reflow.py + tests/test_now_playing_panel.py (no regression).
</completed_work>

<remaining_work>

**Wave 1 / Plan 72.4-02 (helpers + threshold + size-policy round-trip):**
- Task 1: Initialize `self._row1_min_cache: dict[tuple[bool, bool], int] = {}` at line ~297 (where `_cached_row1_min` was); capture `self._volume_cluster_row1_index = self.controls.indexOf(self.volume_slider)` immediately after `self.controls.addWidget(self.volume_slider)` at line 546 and BEFORE `self.controls.addWidget(self.compact_mode_toggle_btn)` at line 566 (mirrors `self._picker_row1_index` at line 509).
- Task 2: Generalize `_picker_row_min_width()` → `_row1_min_width(include_picker: bool, include_volume_cluster: bool) -> int`. REMOVE the old name entirely (no shim). Update the single 72.1 caller at line 1425 area to `self._row1_min_width(include_picker=True, include_volume_cluster=True)`. Preserve the CR-01 layout-agnostic add-back for ALL THREE wrap-eligible widgets (stream_combo, volume_slider, compact_mode_toggle_btn) per RESEARCH §Pattern 1.
- Task 3: Add `_move_volume_cluster_to(target_layout: QHBoxLayout) -> None` and `_set_volume_size_policy(expanding: bool) -> None`. The size-policy helper MUST use the canonical Qt 6 three-call escape sequence (see Anti-Patterns table). The cluster reparent uses idempotent `indexOf(...) >= 0` early-return per 72.1 Pattern 2.
- SUMMARY.md, merge worktree → main, mark plan complete in ROADMAP/STATE.

**Wave 2 / Plan 72.4-03 (row 3 + reflow predicate + resizeEvent extension):**
- Task 1: Add `self._controls_row3 = QHBoxLayout()` with `setSpacing(8)`; append via `center.addLayout(self._controls_row3)` immediately after the existing `_controls_row2` at line 577 and before `_stats_widget` at line 583.
- Task 2: Add `_reflow_volume_cluster()` predicate (defensive guards; `panel_width <= 0` early-return; dispatches to controls / `_controls_row2` / `_controls_row3` per UI-SPEC §Target-Row Selection; toggles size policy via `_set_volume_size_policy(expanding=should_wrap)`). Wire into the existing `resizeEvent` override BETWEEN `self._reflow_stream_picker_row()` and `self._apply_art_tier()`.
- Task 3: **HUMAN UAT CHECKPOINT** (autonomous: false) — user manually verifies on the small lower display per `<how-to-verify>`: visual no-overlap at 560px, volume drag continuity across reparent, compact-toggle icon flip across reparent, no splitter-drag flicker.

**Verify + close out:**
- gsd-verifier on phase 72.4 (must_haves + LAYOUT-04 coverage).
- Mark phase 72.4 complete in ROADMAP.md + STATE.md.
</remaining_work>

<decisions_made>

- Worktree isolation (`isolation=worktree`) for each executor; merge via `git merge --no-ff worktree-agent-*` after each wave. Matches recent precedent (commit `03afa8b` for plan 72.3-03).
- Wave 0 RED scaffold is genuinely RED with AttributeError (NOT pytest.fail placeholders) — the missing symbols ARE the contract.
- FakeRepo verbatim mirror — no `set_setting_calls` field; negative-1 uses `_settings`-keys-diff idiom (plan-checker B2 fix).
- Coverage assertions locked: `Pending: 42` AND `v2.1 requirements: 63 total` (positive); `! Pending: 41 (` AND `! 62 total` (negative). Plan-checker W1 fix.
- Plan 02's three-call setFixedWidth↔Expanding round-trip is the load-bearing landmine — without it, volume slider stays clamped at 120px on the wrap row and the membership/widen-back tests fail. See Anti-Patterns table.
- LAYOUT-04 registered as continuation of LAYOUT-01 (Phase 72) / LAYOUT-02 (Phase 72.1) / LAYOUT-03 (Phase 72.3).
</decisions_made>

<blockers>
- None at the pause point. Wave 0 cleanly closed; Wave 1 not yet started.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state mirror of this file.
2. `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md` — locked user decisions (D-01..D-14, plus four discussed gray areas D-A..D-D).
3. `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md` — six-state truth table, target-row selection, transition contract.
4. `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md` — Pattern 1 (threshold generalization), Pattern 2 (cluster reparent), Pattern 3 (Qt 6 three-call round-trip — load-bearing), Pattern 4-8, Pitfalls 1-11, 16-test envelope.
5. `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md` — analog code excerpts with `now_playing_panel.py:line` citations.
6. `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-02-PLAN.md` — the plan to execute next.
7. `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-01-SUMMARY.md` — what Wave 0 actually landed (sanity check before Wave 1).
8. `musicstreamer/ui_qt/now_playing_panel.py` — the file Plans 02 and 03 modify (key ranges: 280-300 fields, 454-577 control row, 1170-1185 resizeEvent, 1260-1432 existing reflow helpers).
9. `tests/test_phase72_4_volume_cluster_reflow.py` — the RED scaffold (must turn GREEN after Plans 02+03).
10. `tests/test_phase72_1_stream_picker_reflow.py` — must stay GREEN throughout (no regression).

## Infrastructure State

- Git worktree `worktree-agent-adbd640667392da8b` is locked by Claude Code runtime PID 16132 (already merged to main; safe to ignore — runtime cleans up).
- `uv.lock` has a pre-existing modification (predates this session); leave untouched.
- Auto-chain flag persists: `gsd-sdk query check auto-mode --pick auto_chain_active` returns `true`. Resume will continue the chain.
- HEAD on `main` at commit `18cb6af`.

## Pre-Execution Critique Required

Not applicable — Plans 02 and 03 already passed plan-checker round 2 (`## PLAN VERIFIED`). No additional design critique needed; proceed to execution.

<context>
The pause came at a clean wave boundary. Wave 0 is fully landed (merge commit `36e4ea6`, roadmap-marked `18cb6af`). The Agent() spawn for Wave 1 / Plan 72.4-02 was about to dispatch but the user interrupted before the executor started, so no partial worktree state on Plan 02.

Resuming should re-issue the same Wave 1 executor prompt (the one that was rejected). The prompt is reconstructable from the orchestrator's prior message — files_to_read, success_criteria, and the critical-landmine awareness block (three-call round-trip) are documented in `72.4-02-PLAN.md` itself, so a fresh orchestrator can rebuild it directly from the plan.

After Wave 1 merges back, proceed to Wave 2 / Plan 72.4-03. Wave 2's Task 3 is `autonomous: false` — the chain will STOP there for the user to manually verify on the small lower display. That's the natural terminator for the auto-chain.
</context>

<next_action>
Run `/gsd-resume-work` OR `/gsd-execute-phase 72.4`. The orchestrator should:

1. Detect Wave 0 already complete (plan 72.4-01 has SUMMARY.md, ROADMAP marked complete).
2. Skip to Wave 1 (Plan 72.4-02).
3. Spawn `gsd-executor` with `isolation=worktree` using the Wave-1 prompt that was rejected. Files to pass: `72.4-02-PLAN.md`, `72.4-CONTEXT.md`, `72.4-RESEARCH.md` (Pattern 3 is critical), `72.4-UI-SPEC.md`, `72.4-PATTERNS.md`, `72.4-01-SUMMARY.md`, `now_playing_panel.py`, `tests/test_phase72_4_volume_cluster_reflow.py` (the RED scaffold), `tests/test_phase72_1_stream_picker_reflow.py` (must stay GREEN).
4. After Wave 1 executor returns: merge worktree, mark plan complete, advance to Wave 2.
5. Wave 2 / Plan 72.4-03 Task 3 will halt at the autonomous: false UAT checkpoint and return control to the user.

**Critical landmine reminder for Wave 1:** The three-call setFixedWidth↔Expanding round-trip MUST appear in `_set_volume_size_policy`. Without it, `test_widen_back_restores_cluster_to_row_1` and the membership tests will fail in Wave 2. See Anti-Patterns table above for the canonical sequence.
</next_action>
