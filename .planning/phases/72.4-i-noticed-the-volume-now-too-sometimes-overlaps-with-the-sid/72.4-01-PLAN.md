---
phase: 72.4
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_phase72_4_volume_cluster_reflow.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: ["LAYOUT-04"]
tags: [pytest-qt, layout-reflow, tdd-red, requirements-registry]

must_haves:
  truths:
    - "tests/test_phase72_4_volume_cluster_reflow.py exists and is committed as TDD-RED (every behavior test FAILS today against the unchanged NowPlayingPanel)"
    - "REQUIREMENTS.md registers LAYOUT-04 in the Layout section + Traceability table + Coverage summary"
    - "Test file imports the existing FakePlayer from tests/_fake_player.py and builds its own FakeRepo / _make_panel / _make_multi_stream_station / _make_single_stream_station factories mirroring tests/test_phase72_1_stream_picker_reflow.py:75-182"
    - "Test file declares _assert_volume_cluster_in_layout(panel, expected_layout, msg) helper that asserts BOTH cluster widgets are in expected_layout, neither in any other layout, and volume_slider's index < compact_mode_toggle_btn's index"
    - "All 12 behavior tests + 3 negatives map 1:1 to RESEARCH §Validation Architecture rows (a)-(m) + negative-1/2/3"
    - "Tests fail today because _reflow_volume_cluster / _move_volume_cluster_to / _set_volume_size_policy / _row1_min_width / _volume_cluster_row1_index / _controls_row3 / _row1_min_cache do not yet exist on NowPlayingPanel"
  artifacts:
    - path: "tests/test_phase72_4_volume_cluster_reflow.py"
      provides: "TDD-RED scaffold for Phase 72.4 / LAYOUT-04 across the six-state truth table + signal survival + idempotency + widen-back + cluster-as-unit + order + compact-toggle-size invariant + 3 negatives"
      contains_classes_or_functions: ["FakeRepo", "_make_panel", "_make_multi_stream_station", "_make_single_stream_station", "_assert_volume_cluster_in_layout", "test_wide_multi_stream_cluster_in_row_1", "test_mid_narrow_multi_stream_cluster_stays_row_1", "test_very_narrow_multi_stream_cluster_in_row_3", "test_wide_single_stream_cluster_in_row_1", "test_narrow_single_stream_cluster_in_row_2", "test_widen_back_restores_cluster_to_row_1", "test_reflow_idempotent", "test_signals_survive_round_trip", "test_multi_to_single_mid_very_narrow_rehomes_cluster", "test_multi_to_multi_mid_very_narrow_keeps_cluster_on_row_3", "test_cluster_as_unit_invariant", "test_cluster_left_to_right_order", "test_compact_toggle_size_invariant", "test_no_repo_set_setting_for_cluster_wrap_state", "test_no_lambda_in_reflow_wiring", "test_no_addstretch_in_wrap_path"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAYOUT-04 row, traceability row, coverage bump 62→63 / 41→42, dated 2026-05-19 update marker"
      contains: "**LAYOUT-04**"
  key_links:
    - from: "tests/test_phase72_4_volume_cluster_reflow.py"
      to: "tests/_fake_player.py"
      via: "from tests._fake_player import FakePlayer"
      pattern: "from tests\\._fake_player import FakePlayer"
    - from: "tests/test_phase72_4_volume_cluster_reflow.py"
      to: "musicstreamer/ui_qt/now_playing_panel.py"
      via: "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"
      pattern: "from musicstreamer\\.ui_qt\\.now_playing_panel import NowPlayingPanel"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 72.4"
      via: "LAYOUT-04 row trailing *(Phase 72.4)* citation marker"
      pattern: "LAYOUT-04.*Phase 72\\.4"
---

<objective>
Land the TDD-RED scaffold for Phase 72.4 / LAYOUT-04: a new pytest-qt test file covering the full state space of the volume-cluster reflow (six-state truth table + signal survival + idempotency + widen-back + station-change rehome + cluster-as-unit + order + compact-toggle-size invariant + three negative assertions), plus the LAYOUT-04 requirement row in REQUIREMENTS.md.

Purpose: encode the locked behavioral contract from CONTEXT.md D-01..D-14, UI-SPEC §State Truth Table, and RESEARCH §Validation Architecture as failing tests that Plans 02 and 03 will turn green. Failures today MUST be `AttributeError: 'NowPlayingPanel' object has no attribute '_reflow_volume_cluster'` (or sibling symbols) — that IS the RED state. NO pytest.fail placeholders, NO `@pytest.mark.skip` markers.

Output: one new test file (~400 lines) and a four-block edit to REQUIREMENTS.md (LAYOUT row + traceability row + coverage line bump + dated update marker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-VALIDATION.md
@tests/test_phase72_1_stream_picker_reflow.py
@tests/_fake_player.py
@musicstreamer/ui_qt/now_playing_panel.py

<interfaces>
<!-- Key existing symbols the executor will import / mirror. -->

From tests/test_phase72_1_stream_picker_reflow.py (the canonical analog):
  - FakeRepo (class, ~lines 75-120): exposes streams_by_station, list_streams, set_setting, etc.
  - _make_panel(qtbot, settings=None, streams_by_station=None) -> NowPlayingPanel (~lines 123-142):
    constructs FakePlayer + FakeRepo + NowPlayingPanel, calls panel.show() + qtbot.waitExposed(panel).
  - _make_multi_stream_station(n: int) -> Station (~lines 145-173)
  - _make_single_stream_station() -> Station (~lines 175-189)
  - _assert_stream_combo_in_layout(panel, expected_layout, msg) (~lines 191-217)
  - test_no_lambda_in_reflow_wiring (~lines 544-572): source-grep idiom for QA-05; targets tuple includes
    "_reflow_stream_picker_row", "_move_stream_picker_to", "_set_picker_size_policy", "_picker_row_min_width".
  - test_no_repo_set_setting_for_wrap_state (~lines 510-541): forbidden-fragments idiom.

From musicstreamer/ui_qt/now_playing_panel.py (the target file — current state):
  - class NowPlayingPanel(QWidget)
  - self.controls: QHBoxLayout  (line 455 area, row 1)
  - self._controls_row2: QHBoxLayout  (line 575, row 2 — Phase 72.1)
  - self.stream_combo: QComboBox  (line 498)
  - self.volume_slider: QSlider  (line 542-546, setFixedWidth(120))
  - self.compact_mode_toggle_btn: QToolButton  (line 554-566, setFixedSize(28, 28))
  - self._picker_row1_index: int  (line 509)
  - self._cached_row1_min: Optional[int]  (line 297 — to be REPLACED by Plan 02 with _row1_min_cache dict)
  - self._reflow_stream_picker_row()  (line 1385)
  - self._move_stream_picker_to(target_layout)  (line 1338)
  - self._set_picker_size_policy(expanding)  (line 1369)
  - self._picker_row_min_width()  (line 1260 — to be GENERALIZED by Plan 02 to _row1_min_width(include_picker, include_volume_cluster))
  - self._apply_art_tier()  (Phase 72.3)
  - resizeEvent(event)  (line 1170)
  - volume signals (line 774-775): valueChanged.connect(self._on_volume_changed_live), sliderReleased.connect(self._on_volume_released)
  - compact-toggle signal (line 565): toggled.connect(self._on_compact_btn_toggled)

Symbols this plan asserts must come into existence in Plans 02/03 (RED today):
  - panel._reflow_volume_cluster
  - panel._move_volume_cluster_to
  - panel._set_volume_size_policy
  - panel._row1_min_width(include_picker: bool, include_volume_cluster: bool) -> int
  - panel._row1_min_cache  (dict[tuple[bool, bool], int])
  - panel._volume_cluster_row1_index  (int)
  - panel._controls_row3  (QHBoxLayout)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register LAYOUT-04 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
  </read_first>
  <behavior>
    - The Layout section gains a fourth `**LAYOUT-04**` bullet immediately after the existing LAYOUT-03 bullet (currently REQUIREMENTS.md line 61).
    - The Traceability table gains a `| LAYOUT-04 | Phase 72.4 | Pending |` row immediately after the existing LAYOUT-03 row (currently line 183).
    - The Coverage summary "Pending: 41 (...)" line at line 227 becomes "Pending: 42 (... ; LAYOUT-04 — Phase 72.4 volume-cluster reflow into row 2/row 3 when narrow ...)"; the v2.1 requirements total bumps from 62 to 63 wherever the "62" appears in the same coverage region.
    - A dated update marker line is appended after the existing dated lines, citing LAYOUT-04 + Phase 72.4 + fresh-code precedent (mirrors RESEARCH §`<phase_requirements>` recommendation and the line-241 LAYOUT-03 marker shape).
    - No other Layout requirements are touched; LAYOUT-01..LAYOUT-03 prose stays byte-identical.
  </behavior>
  <action>
    Apply the four edits exactly as specified in PATTERNS.md §Pattern R1 / R2 / R3 and RESEARCH.md §`<phase_requirements>`. The LAYOUT-04 bullet text MUST be pasted VERBATIM from RESEARCH §`<phase_requirements>` (lines 79-81 of 72.4-RESEARCH.md — do NOT paraphrase; PATTERNS §R1 calls this out as a load-bearing contract surface). The dated marker line cites date 2026-05-19 (today per environment), LAYOUT-04, and "follow-up phases get their own requirement code" — mirror the shape of the existing line 241 dated marker. Update the "Pending: 41" → "Pending: 42" and the totals counter on the same coverage line; append `; LAYOUT-04 — Phase 72.4 volume-cluster reflow into row 2/row 3 when narrow` to the pending-fragments enumeration before the closing parenthesis. Do NOT renumber or reorder LAYOUT-01..LAYOUT-03. Use single Edit calls per change region to minimize diff surface.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*LAYOUT-04\*\*' .planning/REQUIREMENTS.md | grep -qx 1 &amp;&amp; grep -q '| LAYOUT-04 | Phase 72\.4 | Pending |' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Pending: 42' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'v2\.1 requirements: 63 total' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'LAYOUT-04 — Phase 72\.4 volume-cluster reflow' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'Pending: 41 (' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'v2\.1 requirements: 62 total' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*LAYOUT-04\*\*' .planning/REQUIREMENTS.md` returns exactly `1`.
    - `grep -q '| LAYOUT-04 | Phase 72\.4 | Pending |' .planning/REQUIREMENTS.md` succeeds.
    - `grep -q 'Pending: 42' .planning/REQUIREMENTS.md` succeeds AND `grep -q 'Pending: 41 ('` no longer succeeds (the coverage line was bumped, not duplicated).
    - `grep -q '2026-05-19.*LAYOUT-04' .planning/REQUIREMENTS.md` succeeds (dated marker landed).
    - The full LAYOUT-04 bullet text exactly matches RESEARCH §`<phase_requirements>` — no paraphrasing.
    - LAYOUT-01, LAYOUT-02, LAYOUT-03 bullets and traceability rows are byte-identical to pre-edit.
  </acceptance_criteria>
  <done>REQUIREMENTS.md registers LAYOUT-04 in the Layout section, traceability table, and Coverage summary; the dated update marker cites Phase 72.4; the LAYOUT-04 row's prose is the verbatim text from RESEARCH §`<phase_requirements>`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TDD-RED test scaffold tests/test_phase72_4_volume_cluster_reflow.py</name>
  <files>tests/test_phase72_4_volume_cluster_reflow.py</files>
  <read_first>
    tests/test_phase72_1_stream_picker_reflow.py
    tests/_fake_player.py
    musicstreamer/ui_qt/now_playing_panel.py
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-VALIDATION.md
  </read_first>
  <behavior>
    Author a new pytest-qt file. Top-to-bottom structure (mirrors PATTERNS §T1 / §T2 and the analog file's outline):

    1. Module docstring citing Phase 72.4 / LAYOUT-04 + CONTEXT D-01..D-14 + UI-SPEC §State Truth Table.

    2. Imports: `from __future__ import annotations`; stdlib `inspect`; PySide6 — `QSize` from QtCore; `QHBoxLayout`, `QSizePolicy`, `QSlider`, `QToolButton`, `QWidget` from QtWidgets. Project: `from musicstreamer.ui_qt import icons_rc` (noqa F401, side-effect), `NowPlayingPanel`, `Station`, `StationStream`, `from tests._fake_player import FakePlayer`.

    3. `class FakeRepo:` — mirror tests/test_phase72_1_stream_picker_reflow.py:75-120 VERBATIM. Analog's `set_setting` writes directly into `self._settings` (a `dict[str, Any]`); analog has NO `set_setting_calls` field. Do NOT introduce a `set_setting_calls` list — match the analog. Negative-1 (below) uses the `_settings`-keys-diff idiom from analog lines 510-541, NOT a `set_setting_calls` field. Reuse-by-copy is the established pattern per the analog comment at lines 53-57.

    4. `def _make_panel(qtbot, settings=None, streams_by_station=None) -> NowPlayingPanel:` — mirror analog lines 123-142 INCLUDING the `panel.show()` + `qtbot.waitExposed(panel)` calls. Non-negotiable per PATTERNS §T1 Landmines (`panel.width()` is 0 before exposure → the `panel_width <= 0` defensive guard early-returns and reparent is never tested).

    5. `def _make_multi_stream_station(n: int = 3) -> Station:` — mirror analog lines 145-173. Returns station with n StationStream rows.

    6. `def _make_single_stream_station() -> Station:` — mirror analog lines 175-189.

    7. `def _assert_volume_cluster_in_layout(panel: NowPlayingPanel, expected_layout: QHBoxLayout, msg: str = "") -> None:` — verbatim from RESEARCH §"Layout-membership assertion helper for tests" (Code Examples block lines 930-966). Three-part assertion: both widgets in expected_layout, neither in any of the other two layouts (controls / row2 / row3), and `vol_idx < comp_idx` (D-02 order).

    8. Behavior tests — one function per RESEARCH §Validation Architecture row (a) through (m):

       Pattern for each: `station = _make_*_stream_station(...)`, `panel = _make_panel(qtbot, streams_by_station=...)`, `panel.bind_station(station)`, `panel.resize(W, H)`, then 1-3 assertions.

       (a) `test_wide_multi_stream_cluster_in_row_1` — resize(1600, 800), assert cluster in `panel.controls`, assert `panel.volume_slider.sizePolicy().horizontalPolicy() == QSizePolicy.Fixed`, assert `panel.volume_slider.width() == 120 or panel.volume_slider.maximumWidth() == 120` (fixed clamp), assert `panel.compact_mode_toggle_btn.size() == QSize(28, 28)`.

       (b) `test_mid_narrow_multi_stream_cluster_stays_row_1` — resize ~720x800 (above volume threshold, below picker threshold per UI-SPEC tick T1). Assert picker in `panel._controls_row2` (existing 72.1 invariant — DO NOT regress) AND cluster in `panel.controls`. Picker assertion uses `panel._controls_row2.indexOf(panel.stream_combo) >= 0` (do not call the 72.1 helper to avoid coupling).

       (c) `test_very_narrow_multi_stream_cluster_in_row_3` — resize(560, 800), assert picker in `panel._controls_row2`, cluster in `panel._controls_row3`, `panel.volume_slider.sizePolicy().horizontalPolicy() == QSizePolicy.Expanding`.

       (d) `test_wide_single_stream_cluster_in_row_1` — resize(1600, 800), single-stream station, assert `panel.stream_combo.isHidden()` is True, cluster in `panel.controls`, volume Fixed.

       (e) `test_narrow_single_stream_cluster_in_row_2` — resize(560, 800), single-stream, assert picker hidden, cluster in `panel._controls_row2`, volume Expanding, row 3 is empty (`panel._controls_row3.count() == 0`).

       (f) `test_widen_back_restores_cluster_to_row_1` — start multi-stream wide(1600), then narrow(560) — cluster lands in row 3; then wide(1600) again — cluster MUST return to `panel.controls` at `panel.controls.indexOf(panel.volume_slider) == panel._volume_cluster_row1_index` AND `panel.controls.indexOf(panel.compact_mode_toggle_btn) == panel._volume_cluster_row1_index + 1`. Volume slider's `maximumWidth() == 120` (Fixed clamp restored) and `sizePolicy().horizontalPolicy() == QSizePolicy.Fixed`.

       (g) `test_reflow_idempotent` — resize to a stable width, snapshot `(controls.indexOf(vol), controls.indexOf(comp), row2.indexOf(vol), row3.indexOf(vol), volume_slider.sizePolicy())`, call `panel._reflow_volume_cluster()` twice, assert post-state tuple equals pre-state tuple bit-for-bit.

       (h) `test_signals_survive_round_trip` — wide(1600) → narrow(560) → wide(1600). After each transition: assert `_assert_volume_cluster_in_layout(panel, expected, ...)`, append-to-list test observer connected to `volume_slider.valueChanged` BEFORE the transitions, fire `panel.volume_slider.setValue(42)` at the end, assert observer received 42 (signal still alive). Same idea for `sliderReleased` via `panel.volume_slider.sliderReleased.emit()` (and assert append-list received it). Same for `compact_mode_toggle_btn.toggled` — connect observer, call `panel.compact_mode_toggle_btn.click()`, assert observer received the toggled state. Volume value AND toggle's `isChecked()` state preserved across the round-trip (set both BEFORE the wide→narrow transition, assert AFTER the narrow→wide transition).

       (i) `test_multi_to_single_mid_very_narrow_rehomes_cluster` — start multi-stream narrow(560), cluster in row 3; bind_station(_make_single_stream_station()); `panel.resize(560, 800)` (force a resize tick); assert cluster now in `panel._controls_row2`, row 3 empty.

       (j) `test_multi_to_multi_mid_very_narrow_keeps_cluster_on_row_3` — CR-01 echo: start multi-stream narrow(560), cluster in row 3; bind_station(_make_multi_stream_station(2)); `panel.resize(560, 800)`; assert cluster still in `panel._controls_row3` (does NOT regress to row 1 via stale threshold).

       (k) `test_cluster_as_unit_invariant` — sweep widths [1600, 900, 720, 620, 580, 560] for both stream counts; at each tick assert volume's layout-of-residence == compact-toggle's layout-of-residence (cluster-as-unit). Use parametrize over (stream_count, widths).

       (l) `test_cluster_left_to_right_order` — at every wrap target (controls / row2 / row3), assert `target.indexOf(volume_slider) < target.indexOf(compact_mode_toggle_btn)`.

       (m) `test_compact_toggle_size_invariant` — sweep widths wide→narrow→wide; at each tick assert `panel.compact_mode_toggle_btn.size() == QSize(28, 28)`.

    9. Negative-assertion tests:

       negative-1: `test_no_repo_set_setting_for_cluster_wrap_state(qtbot)` — VERBATIM idiom from analog `test_no_repo_set_setting_for_wrap_state` at tests/test_phase72_1_stream_picker_reflow.py:510-541. Capture `keys_before = set(fake_repo._settings.keys())` BEFORE binding a station; sweep widths through both thresholds (e.g., `for w in (1600, 560, 1600, 560, 800): panel.resize(w, 800)`); compute `keys_after = set(fake_repo._settings.keys())` and `new_keys = keys_after - keys_before`. Forbidden fragments specific to 72.4: `("volume_cluster", "row3", "wrap_state", "cluster_wrap", "volume_wrap")`. Assert `not offending`. Do NOT use a `set_setting_calls` field — the analog's FakeRepo does not have one, and Plan 01 must mirror it verbatim per behavior point 3.

       negative-2: `test_no_lambda_in_reflow_wiring()` — pure-source-grep, no qtbot. Use `inspect.getsource(NowPlayingPanel)`; targets = `("_reflow_volume_cluster", "_move_volume_cluster_to", "_set_volume_size_policy", "_row1_min_width")` + the existing 72.1 targets (`"_reflow_stream_picker_row", "_move_stream_picker_to", "_set_picker_size_policy"`). For each method, extract its source via `inspect.getsource(getattr(NowPlayingPanel, name))`, assert `"lambda" not in source`. NOTE: today these methods don't exist — use a `getattr(NowPlayingPanel, name, None)` guard so the test FAILS with a CLEAR `AssertionError: _reflow_volume_cluster does not exist on NowPlayingPanel (Wave 1+)` rather than crashing with `AttributeError`. (This negative test is asymmetric — it's RED today because the methods don't exist; it should turn GREEN when methods exist AND contain no lambda.)

       negative-3: `test_no_addstretch_in_wrap_path()` — pure-source-grep on `inspect.getsource(NowPlayingPanel._move_volume_cluster_to)`; assert `"addStretch" not in source`. D-11 enforcement. Same getattr guard pattern.

    10. Each test references the relevant CONTEXT decision IDs in a docstring or trailing comment (e.g., `# per D-04, D-12, UI-SPEC truth table row 3`).

    11. Tests MUST NOT use `pytest.fail(...)` or `@pytest.mark.skip(...)` or `xfail`. The RED state is genuine `AttributeError` / `AssertionError` raised when the test executes against today's NowPlayingPanel.

    12. Width samples use safety-margin constants, not hardcoded thresholds, per PATTERNS §T2 Landmine "Threshold values are derived, not hardcoded": `_W_WIDE = 1600`, `_W_MID = 720`, `_W_VERY_NARROW = 560`. Per UI-SPEC §Wrap-Tier Cascade, these correspond to the three tick points in the cascade diagram.
  </behavior>
  <action>
    Create the new file from scratch. Mirror analog naming and section order. Reuse FakePlayer via import. Copy FakeRepo / _make_panel / _make_*_stream_station BODY-VERBATIM from tests/test_phase72_1_stream_picker_reflow.py:75-189 (per PATTERNS §T1 "Same in 72.4: Copy these blocks verbatim into the new file"). Author the new `_assert_volume_cluster_in_layout` helper verbatim from RESEARCH §"Layout-membership assertion helper for tests" (Code Examples block at lines 930-966). Author the 13 behavior tests + 3 negative tests per the behavior table above. Reference RESEARCH §Pattern 1 / 2 / 3 / 4 (cache, cluster reparent, size-policy round-trip, resizeEvent extension) and PATTERNS §T2 sample-point table inline where it clarifies a specific assertion. Do NOT touch musicstreamer/ui_qt/now_playing_panel.py — this is the RED scaffold; Plans 02 and 03 do the implementation. Do NOT add `@pytest.mark.xfail`. The failure mode IS the contract — each method that does not yet exist produces an `AttributeError: 'NowPlayingPanel' object has no attribute '_reflow_volume_cluster'` (or sibling), which IS the RED.
  </action>
  <verify>
    <automated>test -f tests/test_phase72_4_volume_cluster_reflow.py &amp;&amp; grep -c '^def test_' tests/test_phase72_4_volume_cluster_reflow.py | awk '$1 >= 16 {exit 0} {exit 1}' &amp;&amp; grep -q 'def _assert_volume_cluster_in_layout' tests/test_phase72_4_volume_cluster_reflow.py &amp;&amp; grep -q 'from tests\._fake_player import FakePlayer' tests/test_phase72_4_volume_cluster_reflow.py &amp;&amp; grep -q 'def _make_panel' tests/test_phase72_4_volume_cluster_reflow.py &amp;&amp; ! grep -q 'pytest\.fail\|pytest\.mark\.skip\|pytest\.mark\.xfail' tests/test_phase72_4_volume_cluster_reflow.py &amp;&amp; uv run pytest tests/test_phase72_4_volume_cluster_reflow.py --collect-only -q 2>&amp;1 | grep -q 'test_widen_back_restores_cluster_to_row_1'</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_phase72_4_volume_cluster_reflow.py` exists.
    - At least 16 `^def test_` lines (13 behavior + 3 negatives).
    - File defines `FakeRepo`, `_make_panel`, `_make_multi_stream_station`, `_make_single_stream_station`, `_assert_volume_cluster_in_layout` (verifiable via `grep -q 'def _assert_volume_cluster_in_layout'` and `grep -q 'class FakeRepo'`).
    - File imports `FakePlayer` from `tests._fake_player` (no local duplicate FakePlayer).
    - File contains NO `pytest.fail`, NO `pytest.mark.skip`, NO `pytest.mark.xfail`.
    - `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py --collect-only -q` succeeds (collection passes — no syntax errors).
    - `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x` exits NON-ZERO today (RED state). The first failure is an `AttributeError` for a missing 72.4 symbol or an `AssertionError` for a 72.4 invariant — NOT a syntax error or collection error.
    - `_make_panel` calls `panel.show()` and `qtbot.waitExposed(panel)` (greppable invariant: `grep -q 'qtbot.waitExposed' tests/test_phase72_4_volume_cluster_reflow.py`).
    - Width samples use named constants `_W_WIDE`, `_W_MID`, `_W_VERY_NARROW`; no hardcoded threshold-derivation constants like `672` appear in test bodies.
  </acceptance_criteria>
  <done>tests/test_phase72_4_volume_cluster_reflow.py exists, collects clean, runs to a genuine RED with AttributeError on the missing 72.4 symbols, contains 13+3 tests covering RESEARCH §Validation Architecture rows (a)-(m) + 3 negatives, and matches the analog test file's structure for FakeRepo / _make_panel / _make_*_stream_station / layout-assertion helper.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary. Phase 72.4 is a pure intra-panel Qt-layout change with no I/O, no untrusted input, no new APIs. The volume slider's integer value (0..100) and the compact-toggle's boolean checked state are Qt-internal widget properties — never crossing a process or network boundary. Local files modified:
- `tests/test_phase72_4_volume_cluster_reflow.py` (new, dev-tree only)
- `.planning/REQUIREMENTS.md` (docs, dev-tree only)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.4-01 | Tampering | tests/test_phase72_4_volume_cluster_reflow.py — accidental introduction of a `pytest.mark.xfail` or `pytest.fail("not implemented")` placeholder would silently mask the RED→GREEN gate | mitigate | Acceptance criterion forbids `pytest.fail`, `pytest.mark.skip`, `pytest.mark.xfail` (greppable). Verify step pipelines a `! grep -q` guard for those tokens. |
| T-72.4-02 | Tampering | .planning/REQUIREMENTS.md — paraphrased LAYOUT-04 text would drift from RESEARCH's contract surface | mitigate | Acceptance criterion requires byte-identical paste from RESEARCH §`<phase_requirements>` (PATTERNS §R1 Landmine 1). |
| T-72.4-SC | Tampering | npm/pip/cargo installs | accept | No new packages — `tests/_fake_player.py` and `pytest-qt` are already installed; no `uv pip install` performed by this plan. |

</threat_model>

<verification>
- `grep -c '^- \[ \] \*\*LAYOUT-04\*\*' .planning/REQUIREMENTS.md` returns `1`.
- `grep -q '| LAYOUT-04 | Phase 72\.4 | Pending |' .planning/REQUIREMENTS.md` succeeds.
- `grep -q 'Pending: 42' .planning/REQUIREMENTS.md` succeeds and no `Pending: 41 (` remains.
- `test -f tests/test_phase72_4_volume_cluster_reflow.py` succeeds.
- `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py --collect-only -q` returns exit 0 (collection-clean).
- `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x 2>&1 | tail -20` shows at least one test failure with `AttributeError` referencing `_reflow_volume_cluster`, `_volume_cluster_row1_index`, `_controls_row3`, `_row1_min_width`, `_move_volume_cluster_to`, or `_set_volume_size_policy` (the genuine RED state).
- `! grep -q 'pytest\.fail\|pytest\.mark\.skip\|pytest\.mark\.xfail' tests/test_phase72_4_volume_cluster_reflow.py` succeeds (no placeholders).
</verification>

<success_criteria>
1. REQUIREMENTS.md contains LAYOUT-04 row (verbatim text from RESEARCH §`<phase_requirements>`), traceability table row, Coverage line bumped 62→63 and 41→42, dated update marker citing Phase 72.4.
2. tests/test_phase72_4_volume_cluster_reflow.py exists with 13 behavior tests + 3 negative tests covering all 16 RESEARCH §Validation Architecture rows.
3. Running `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x` today exits non-zero with AttributeError or AssertionError citing a missing 72.4 symbol (genuine RED, NOT a syntax / collection error).
4. Existing tests are not modified (`tests/test_phase72_1_stream_picker_reflow.py` and `tests/test_now_playing_panel.py` untouched): `uv run pytest tests/test_phase72_1_stream_picker_reflow.py tests/test_now_playing_panel.py -x` exits 0.
5. Both Task 1 and Task 2 commit atomically with messages following the format `test(72.4-01): <subject>` and `docs(72.4-01): <subject>`.
</success_criteria>

<output>
Create `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-01-SUMMARY.md` when done. Record:
- Final RED-state observation (which AttributeError fires first on `pytest -x`).
- Count of `^def test_` in the new file.
- Confirmation that LAYOUT-04 text in REQUIREMENTS.md is byte-identical to RESEARCH §`<phase_requirements>` block.
- Any analog-test divergences (if FakeRepo body needed adaptation, document why).
</output>
</content>
</invoke>