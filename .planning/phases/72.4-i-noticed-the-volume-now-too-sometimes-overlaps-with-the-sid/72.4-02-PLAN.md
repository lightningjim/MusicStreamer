---
phase: 72.4
plan: 02
type: execute
wave: 1
depends_on: ["72.4-01"]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: true
requirements: ["LAYOUT-04"]
tags: [pyside6, qt-layout, threshold-cache, size-policy-round-trip]

must_haves:
  truths:
    - "self._row1_min_cache: dict[tuple[bool, bool], int] = {} is initialized in __init__ where self._cached_row1_min was previously declared (line 297 area)"
    - "self._volume_cluster_row1_index: int is captured AFTER self.controls.addWidget(self.volume_slider) at line 546 and BEFORE self.controls.addWidget(self.compact_mode_toggle_btn) at line 566 (analogous to self._picker_row1_index at line 509)"
    - "self._row1_min_width(include_picker: bool, include_volume_cluster: bool) -> int exists, returns the layout-agnostic threshold per Pattern 1, and is cached on the tuple key"
    - "self._move_volume_cluster_to(target_layout: QHBoxLayout) -> None exists, walks both cluster widgets in D-02 left-to-right order, uses indexOf() for idempotent early-return, and inserts at _volume_cluster_row1_index / _volume_cluster_row1_index + 1 on row-1 return"
    - "self._set_volume_size_policy(expanding: bool) -> None exists and implements the canonical three-call setFixedWidth↔Expanding round-trip from RESEARCH Pattern 3"
    - "_picker_row_min_width and _cached_row1_min are gone from the source file; the single existing caller (line 1425 area) now invokes self._row1_min_width(include_picker=True, include_volume_cluster=True)"
    - "_populate_stream_picker's invalidation hook (was self._cached_row1_min = None at line 1213) now calls self._row1_min_cache.clear()"
    - "The CR-01 layout-agnostic invariant is preserved for ALL THREE widgets (picker, volume_slider, compact_mode_toggle_btn): include_X=True with X not in row 1 adds X's min-width contribution explicitly"
    - "Tests TestThreshold + TestSizePolicyRoundTrip (subset of the test file from Plan 01) turn GREEN; all 12 existing Phase 72.1 tests stay GREEN"
    - "No lambda introduced in any new method (QA-05); test_no_lambda_in_reflow_wiring still RED on Plan 03 helpers but no false-positive on the new helpers"
    - "Compact-toggle is NEVER size-mutated by _set_volume_size_policy (D-11)"
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "Helper trio (_row1_min_width, _move_volume_cluster_to, _set_volume_size_policy), the _row1_min_cache dict field, the _volume_cluster_row1_index capture, and updated caller for the 72.1 picker reflow"
      contains: "_row1_min_width"
  key_links:
    - from: "_row1_min_width"
      to: "_row1_min_cache"
      via: "dict[tuple[bool, bool], int] lookup keyed on (include_picker, include_volume_cluster)"
      pattern: "self\\._row1_min_cache\\[cache_key\\]"
    - from: "_populate_stream_picker"
      to: "_row1_min_cache"
      via: ".clear() invalidation hook (was = None)"
      pattern: "self\\._row1_min_cache\\.clear\\(\\)"
    - from: "_reflow_stream_picker_row"
      to: "_row1_min_width"
      via: "call self._row1_min_width(include_picker=True, include_volume_cluster=True)"
      pattern: "_row1_min_width\\(include_picker=True"
    - from: "_move_volume_cluster_to row-1 return"
      to: "_volume_cluster_row1_index"
      via: "insertWidget(self._volume_cluster_row1_index, self.volume_slider) then insertWidget(self._volume_cluster_row1_index + 1, self.compact_mode_toggle_btn)"
      pattern: "insertWidget\\(self\\._volume_cluster_row1_index"
---

<objective>
Land the helper trio + cache generalization that makes the 72.4 reflow predicate possible WITHOUT yet wiring it. After this plan:
- `self._row1_min_cache` (dict) replaces `self._cached_row1_min` (Optional[int]).
- `_picker_row_min_width()` is REPLACED by `_row1_min_width(include_picker, include_volume_cluster)` — generalized per RESEARCH §Pattern 1. The single 72.1 caller is updated; the layout-agnostic CR-01 invariant is preserved for ALL THREE row-1 wrap-eligible widgets.
- `_move_volume_cluster_to(target_layout)` exists per RESEARCH §Pattern 2 / PATTERNS §Pattern 5 — cluster-as-unit, D-02 left-to-right insert order, idempotent indexOf early-return across all THREE candidate layouts (controls / _controls_row2 / _controls_row3).
- `_set_volume_size_policy(expanding)` exists per RESEARCH §Pattern 3 / PATTERNS §Pattern 6 — canonical Qt 6 three-call setFixedWidth ↔ Expanding round-trip.
- `_volume_cluster_row1_index` is captured at construction time (line 546→554 region, mirroring `_picker_row1_index` at line 509).

NOT YET added in this plan: `_reflow_volume_cluster` predicate, `_controls_row3` layout, and the resizeEvent extension call — those land in Plan 03 (Wave 2) so this plan stays under the 30%-context-per-task budget.

Purpose: turn the THRESHOLD + SIZE-POLICY subset of the Plan 01 RED test file GREEN, leaving the membership / wave / station-change subset RED for Plan 03.

Output: surgical edits to ONE file (`musicstreamer/ui_qt/now_playing_panel.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-VALIDATION.md
@.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-01-PLAN.md
@musicstreamer/ui_qt/now_playing_panel.py
@tests/test_phase72_4_volume_cluster_reflow.py

<interfaces>
<!-- Existing symbols that this plan reads / replaces / extends. -->

From musicstreamer/ui_qt/now_playing_panel.py (CURRENT STATE, pre-edit):
  - self._cached_row1_min: Optional[int] at line 297 — TO BE REPLACED by self._row1_min_cache: dict[tuple[bool, bool], int] = {}
  - self._picker_row1_index: int at line 509 — UNCHANGED (Phase 72.1 contract)
  - self.volume_slider construction at lines 542-546 — UNCHANGED in body; _volume_cluster_row1_index capture INSERTED immediately after line 546
  - self.compact_mode_toggle_btn construction at lines 554-566 — UNCHANGED
  - self._controls_row2 at lines 575-577 — UNCHANGED (Plan 03 inserts _controls_row3 AFTER this)
  - self.controls (row 1 QHBoxLayout, line 455 area) — UNCHANGED
  - _populate_stream_picker hook at line 1213 (`self._cached_row1_min = None`) — REPLACED with `self._row1_min_cache.clear()`
  - _picker_row_min_width() at lines 1260-1336 — REMOVED; replaced by _row1_min_width(include_picker, include_volume_cluster)
  - _reflow_stream_picker_row line 1425 (`threshold = self._picker_row_min_width()`) — UPDATED to `self._row1_min_width(include_picker=True, include_volume_cluster=True)`
  - _move_stream_picker_to at lines 1338-1367 — UNCHANGED (72.1 contract)
  - _set_picker_size_policy at lines 1369-1383 — UNCHANGED (72.1 contract)
  - resizeEvent at lines 1170-1185 — UNCHANGED in this plan (Plan 03 extends with _reflow_volume_cluster call)

New symbols this plan introduces:
  - self._row1_min_cache: dict[tuple[bool, bool], int] (init line 297 area)
  - self._volume_cluster_row1_index: int (init between lines 546 and 554)
  - self._row1_min_width(include_picker: bool, include_volume_cluster: bool) -> int  (method)
  - self._move_volume_cluster_to(target_layout: QHBoxLayout) -> None  (method)
  - self._set_volume_size_policy(expanding: bool) -> None  (method)

NOT in this plan (Plan 03 owns these):
  - self._controls_row3: QHBoxLayout
  - self._reflow_volume_cluster method
  - resizeEvent extension call to _reflow_volume_cluster

The QSizePolicy and QHBoxLayout imports are already present (used by 72.1's _set_picker_size_policy / _controls_row2). QSlider, QToolButton already imported. QWIDGETSIZE_MAX may need importing from PySide6.QtWidgets, OR use the verified literal 16777215 per RESEARCH Pattern 3 — see Task 3 action.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename cache to dict + capture _volume_cluster_row1_index + update _populate_stream_picker invalidation hook</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-CONTEXT.md
  </read_first>
  <behavior>
    - In NowPlayingPanel.__init__ around line 295-297: replace the `self._cached_row1_min: Optional[int] = None` declaration with `self._row1_min_cache: dict[tuple[bool, bool], int] = {}`. The pre-existing comment block (Phase 72.1 / LAYOUT-02 lazy cache note) is updated to cite both LAYOUT-02 and LAYOUT-04 and to describe the new tuple-key cache shape (per PATTERNS §Pattern 1).
    - Immediately AFTER `self.controls.addWidget(self.volume_slider)` (currently line 546) and BEFORE the compact-toggle's `self.compact_mode_toggle_btn = QToolButton(self)` (currently line 554), insert `self._volume_cluster_row1_index: int = self.controls.indexOf(self.volume_slider)` with a comment-block citing Phase 72.4 / LAYOUT-04 + Pattern 6 (mirrors the comment shape at lines 502-509 for `_picker_row1_index`).
    - In `_populate_stream_picker` at line 1213, replace `self._cached_row1_min = None` with `self._row1_min_cache.clear()` and update the surrounding comment to cite both 72.1 and 72.4 invalidation semantics (per PATTERNS §Pattern 1 Landmine 1 — "missed rename here re-introduces CR-01").
    - Do NOT yet add `_controls_row3`. Do NOT yet add `_reflow_volume_cluster`. Do NOT yet touch resizeEvent.
    - Do NOT yet remove `_picker_row_min_width` — that happens in Task 2 in the same plan.
  </behavior>
  <action>
    Apply three surgical Edit calls per RESEARCH §"Construction-time additions to __init__" (lines 790-826 of RESEARCH.md) and PATTERNS §Pattern 1 / Pattern 2. Make each edit small and reviewable: (a) cache-field rename at line 297 area; (b) _volume_cluster_row1_index capture between lines 546 and 554; (c) _populate_stream_picker hook update at line 1213. After edits, run a quick grep gate `grep -c '_cached_row1_min' musicstreamer/ui_qt/now_playing_panel.py` — must equal 0 (per PATTERNS §Pattern 1 Landmine 1). The full type annotation `dict[tuple[bool, bool], int]` works on Python 3.9+ via PEP 585; the file already uses `Optional[int]` so the `from __future__ import annotations` / typing patterns in use are compatible — verify by reading the file's top imports if uncertain.
  </action>
  <verify>
    <automated>grep -c 'self\._cached_row1_min' musicstreamer/ui_qt/now_playing_panel.py | grep -qx 0 &amp;&amp; grep -c 'self\._row1_min_cache' musicstreamer/ui_qt/now_playing_panel.py | awk '$1 >= 2 {exit 0} {exit 1}' &amp;&amp; grep -q 'self\._volume_cluster_row1_index: int = self\.controls\.indexOf(self\.volume_slider)' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'self\._row1_min_cache\.clear()' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; uv run python -c 'from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel' &amp;&amp; uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'self\._cached_row1_min' musicstreamer/ui_qt/now_playing_panel.py` returns `0` (the old field name is gone — Plan 01 landmine 1).
    - `grep -c 'self\._row1_min_cache' musicstreamer/ui_qt/now_playing_panel.py` is at least 2 (the field declaration + the `.clear()` in _populate_stream_picker; more when Task 2 lands but baseline at Task 1 close is 2).
    - `grep -q 'self\._volume_cluster_row1_index: int = self\.controls\.indexOf(self\.volume_slider)'` succeeds.
    - `grep -q 'self\._row1_min_cache\.clear()'` succeeds.
    - `uv run python -c 'from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel'` succeeds (no syntax / import error).
    - `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` exits 0 — note: the existing 72.1 caller still calls `self._picker_row_min_width()` at this point, which still exists; Task 2 in this plan replaces that. Until Task 2, 72.1 tests CONTINUE to pass.
  </acceptance_criteria>
  <done>The cache field is renamed/reshaped, the cluster row-1 index is captured at construction, _populate_stream_picker's invalidation hook calls .clear(), and 72.1 tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace _picker_row_min_width with generalized _row1_min_width(include_picker, include_volume_cluster) + update 72.1 caller</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
  </read_first>
  <behavior>
    - Replace the `_picker_row_min_width(self) -> int` method body (currently lines 1260-1336) with `_row1_min_width(self, include_picker: bool, include_volume_cluster: bool) -> int` whose body matches RESEARCH §Pattern 1 (lines 240-336 of RESEARCH.md) verbatim in shape: tuple-key cache lookup; walk `self.controls` with per-widget exclusion branches (stream_combo when not include_picker; volume_slider when not include_volume_cluster; compact_mode_toggle_btn when not include_volume_cluster); preserve the CR-01 layout-agnostic add-back for ALL THREE widgets if currently-not-in-row-1 AND included; outer-chrome walk identical to the 72.1 body; 16px safety buffer preserved; widget_count + spacing math identical.
    - Remove the old `_picker_row_min_width` symbol entirely (no shim).
    - Update the single existing caller in `_reflow_stream_picker_row` (currently `threshold = self._picker_row_min_width()` at line 1425 area) to `threshold = self._row1_min_width(include_picker=True, include_volume_cluster=True)` — semantically identical to the pre-change call (row 1 with everything, mirrors PATTERNS §Pattern 4 "All callers update" guidance).
    - The CR-01 invariant from Phase 72.1 commit at lines 1305-1312 MUST be preserved and EXTENDED to include all three wrap-eligible widgets (stream_combo, volume_slider, compact_mode_toggle_btn). Per PATTERNS §Pattern 4 Landmine: "Phase 72.1 burned a whole compute-from-scratch revision when this was missed for the picker (sample (b) failure at 560px). 72.4 has THREE such widgets and three corresponding `if not X_in_row1: total += min` add-backs."
    - No behavior change for the 72.1 picker reflow (Plan 01 RED tests for picker-on-row-1 / picker-on-row-2 must stay GREEN; the new `_row1_min_width(True, True)` returns the same threshold as the old `_picker_row_min_width()` did).
    - Does NOT introduce `_reflow_volume_cluster` or call the new helper for the volume-cluster threshold yet (Plan 03 wires that).
  </behavior>
  <action>
    Apply a single Edit replacing the method definition and a separate Edit at the call site. The method body is non-trivial (~80 lines) — copy the structure from RESEARCH §Pattern 1 (lines 243-335 of RESEARCH.md) and substitute matching variable names from the existing 72.1 helper where clearer (e.g., the existing body uses `layout = self.controls` and `outer = self.layout()` — keep those names). Method docstring cites both Phase 72.1 / LAYOUT-02 and Phase 72.4 / LAYOUT-04 with the four threshold variants documented (per the RESEARCH docstring at lines 248-260). After landing, run the focused 72.1 test suite to confirm no regression and the 72.4 Threshold subset of tests turns GREEN.
  </action>
  <verify>
    <automated>grep -c '_picker_row_min_width' musicstreamer/ui_qt/now_playing_panel.py | grep -qx 0 &amp;&amp; grep -q 'def _row1_min_width(self, include_picker: bool, include_volume_cluster: bool) -&gt; int' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'self\._row1_min_width(include_picker=True, include_volume_cluster=True)' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q &amp;&amp; uv run pytest tests/test_phase72_4_volume_cluster_reflow.py::test_widen_back_restores_cluster_to_row_1 -x -q || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_picker_row_min_width' musicstreamer/ui_qt/now_playing_panel.py` returns `0` (old name is gone — no shim).
    - `grep -q 'def _row1_min_width(self, include_picker: bool, include_volume_cluster: bool) -&gt; int'` succeeds.
    - `grep -q 'self\._row1_min_width(include_picker=True, include_volume_cluster=True)'` succeeds (the 72.1 picker reflow now calls the generalized helper).
    - `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` exits 0 (no Phase 72.1 regression — CR-01 layout-agnostic invariant preserved).
    - Source-grep confirms three CR-01 add-back branches in `_row1_min_width`: one for stream_combo, one for volume_slider, one for compact_mode_toggle_btn — `grep -c 'if include_picker and not picker_in_row1' musicstreamer/ui_qt/now_playing_panel.py` ≥ 1 AND `grep -c 'if include_volume_cluster and not volume_in_row1' musicstreamer/ui_qt/now_playing_panel.py` ≥ 1 AND `grep -c 'if include_volume_cluster and not compact_in_row1' musicstreamer/ui_qt/now_playing_panel.py` ≥ 1.
    - `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q 2>&1 | grep -E 'AttributeError|AssertionError'` still fires for the membership-tests (test_very_narrow_multi_stream_cluster_in_row_3 etc.) because Plan 03 hasn't wired the reflow yet. This is EXPECTED.
  </acceptance_criteria>
  <done>The threshold helper is generalized to `_row1_min_width(include_picker, include_volume_cluster)`, the old `_picker_row_min_width` symbol is gone, the existing 72.1 caller is updated, and the CR-01 layout-agnostic invariant is preserved for all three wrap-eligible widgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add _move_volume_cluster_to and _set_volume_size_policy helpers</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-RESEARCH.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-PATTERNS.md
    .planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-UI-SPEC.md
  </read_first>
  <behavior>
    - Add `_move_volume_cluster_to(self, target_layout: QHBoxLayout) -> None` as a new method placed IMMEDIATELY AFTER `_move_stream_picker_to` (currently ends around line 1367). Method body matches RESEARCH §Pattern 2 (lines 359-422 of RESEARCH.md) and PATTERNS §Pattern 5 sketch:
        * membership-probe via volume_slider's indexOf across all three layouts (controls, _controls_row2, _controls_row3) — **but in this plan _controls_row3 does not yet exist, so the helper must defensively check `hasattr(self, '_controls_row3')` before referencing it. If the attribute is absent (pre-Plan-03 state), treat row 3 as never-containing-the-cluster (membership probe returns False for row 3).**
        * Idempotent early-returns: target == controls and current_in_row1; target == _controls_row2 and current_in_row2; target == _controls_row3 and current_in_row3.
        * Removal of BOTH cluster members from current layout (cluster-as-unit invariant D-03), then target-specific insert: on row-1 return, `insertWidget(self._volume_cluster_row1_index, self.volume_slider)` then `insertWidget(self._volume_cluster_row1_index + 1, self.compact_mode_toggle_btn)`; on row-2/row-3, `addWidget(volume_slider)` then `addWidget(compact_mode_toggle_btn)` (D-02 left-to-right order).
        * No `addStretch(1)` anywhere in this method (D-11 enforcement; the test_no_addstretch_in_wrap_path negative test asserts this).
        * No lambda anywhere.
    - Add `_set_volume_size_policy(self, expanding: bool) -> None` as a new method placed IMMEDIATELY AFTER `_set_picker_size_policy` (currently ends around line 1383). Method body matches RESEARCH §Pattern 3 (lines 446-486 of RESEARCH.md) and PATTERNS §Pattern 6 sketch — the canonical Qt 6 three-call sequence:
        * `expanding=True` branch: `self.volume_slider.setMinimumWidth(0)`; `self.volume_slider.setMaximumWidth(16777215)` (QWIDGETSIZE_MAX literal, per RESEARCH §Pattern 3 Critical note 1 — the literal is verified safe); `self.volume_slider.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Fixed)`.
        * `expanding=False` branch: `self.volume_slider.setFixedWidth(120)`; `self.volume_slider.setSizePolicy(QSizePolicy.Fixed, QSizePolicy.Fixed)` (belt-and-braces per RESEARCH §Pattern 3 Critical note 4).
        * Compact-toggle is NEVER mutated (D-11). The method body ONLY touches volume_slider.
        * No lambda anywhere.
        * The `16777215` literal carries an inline comment "QWIDGETSIZE_MAX = 2^24 - 1 per Qt 6 QWidget docs (RESEARCH §Pattern 3 Critical note 1)" — readers should know what the magic number means.
    - Does NOT yet add `_reflow_volume_cluster` or wire `resizeEvent` — Plan 03 does that.
    - Does NOT yet construct `self._controls_row3` — Plan 03 does that.
  </behavior>
  <action>
    Insert the two methods at their respective positions (after the 72.1 picker helpers). Body content is taken from RESEARCH §Pattern 2 and §Pattern 3 — quote the docstrings verbatim where they cite the Qt 6 invariant (the `setFixedWidth` three-call sequence is THE 72.4-specific landmine per PATTERNS §Pattern 6, and the docstring should pin the contract). For `_move_volume_cluster_to`, the defensive `hasattr(self, '_controls_row3')` check at the top of the membership-probe section MUST be present so this method does not crash when invoked from a hypothetical test before Plan 03 lands; PATTERNS §Pattern 5 Landmine "Cluster-as-unit invariant means a single membership probe is sufficient" — the probe is via volume_slider. After landing, run a focused source-grep gate to confirm no `addStretch` in `_move_volume_cluster_to` and no `lambda` in either new helper.
  </action>
  <verify>
    <automated>grep -q 'def _move_volume_cluster_to(self, target_layout: QHBoxLayout) -&gt; None' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'def _set_volume_size_policy(self, expanding: bool) -&gt; None' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q '16777215' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'insertWidget(self\._volume_cluster_row1_index, self\.volume_slider)' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; grep -q 'insertWidget(self\._volume_cluster_row1_index + 1, self\.compact_mode_toggle_btn)' musicstreamer/ui_qt/now_playing_panel.py &amp;&amp; uv run python -c "import inspect; from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; src = inspect.getsource(NowPlayingPanel._move_volume_cluster_to); assert 'addStretch' not in src; assert 'lambda' not in src" &amp;&amp; uv run python -c "import inspect; from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; src = inspect.getsource(NowPlayingPanel._set_volume_size_policy); assert 'lambda' not in src; assert 'setFixedWidth(120)' in src; assert 'setMinimumWidth(0)' in src; assert 'setMaximumWidth(16777215)' in src" &amp;&amp; uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def _move_volume_cluster_to(self, target_layout: QHBoxLayout) -&gt; None'` succeeds.
    - `grep -q 'def _set_volume_size_policy(self, expanding: bool) -&gt; None'` succeeds.
    - `grep -q '16777215'` succeeds (QWIDGETSIZE_MAX literal present).
    - `grep -q 'insertWidget(self\._volume_cluster_row1_index, self\.volume_slider)'` AND `grep -q 'insertWidget(self\._volume_cluster_row1_index + 1, self\.compact_mode_toggle_btn)'` both succeed (PATTERNS §Pattern 2 off-by-one landmine).
    - Python-level inspect.getsource gates: `'addStretch' not in source` for `_move_volume_cluster_to` (D-11), and `'lambda' not in source` for both new methods (QA-05).
    - Source for `_set_volume_size_policy` contains all three round-trip elements: `setMinimumWidth(0)`, `setMaximumWidth(16777215)`, `setFixedWidth(120)`.
    - `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` exits 0 (no 72.1 regression).
    - The Plan 01 RED test file STILL has failures for the membership / wave / station-change tests because `_reflow_volume_cluster` and `_controls_row3` don't exist yet — Plan 03 lands those. This is EXPECTED.
  </acceptance_criteria>
  <done>The two new helpers exist with correct three-call round-trip semantics, the cluster-as-unit reparent walks both widgets in D-02 order with the off-by-one insert correctly captured, no addStretch is added on the wrap path, no lambda is introduced, and 72.1 regression suite stays green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundary. Phase 72.4 Plan 02 is a pure intra-class Qt-method addition with no I/O, no untrusted input, no network. The volume slider integer (0..100) and compact-toggle bool are Qt widget-internal. No external services.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.4-03 | Tampering | _set_volume_size_policy — silent regression to two-call setSizePolicy-only (Pitfall 1) leaves volume pinned at 120px on wrap row | mitigate | Acceptance criterion source-greps for ALL THREE calls (`setMinimumWidth(0)`, `setMaximumWidth(16777215)`, `setFixedWidth(120)`); Plan 03 turns the test_very_narrow_multi_stream_cluster_in_row_3 GREEN which functionally verifies the round-trip works (assert sizePolicy().horizontalPolicy() == Expanding implies the slider can grow). |
| T-72.4-04 | Tampering | _move_volume_cluster_to — D-02 off-by-one insertWidget reversal puts compact-toggle to the LEFT of volume on row 1 (PATTERNS §Pattern 5 Landmine) | mitigate | Acceptance criterion source-greps for the EXACT insert order (`insertWidget(self._volume_cluster_row1_index, self.volume_slider)` BEFORE `insertWidget(self._volume_cluster_row1_index + 1, self.compact_mode_toggle_btn)`); Plan 03's `test_cluster_left_to_right_order` and `test_widen_back_restores_cluster_to_row_1` will assert behavior. |
| T-72.4-05 | Tampering | _row1_min_width — missed CR-01 add-back for any of the three widgets re-introduces the same family of stale-threshold bug 72.1 burned a plan on | mitigate | Acceptance criterion source-greps for THREE distinct `if include_X and not X_in_row1:` branches; Plan 01's `test_multi_to_multi_mid_very_narrow_keeps_cluster_on_row_3` (CR-01 echo) will FAIL if any branch is omitted (Plan 03 wires the predicate that exercises this). |
| T-72.4-SC | Tampering | npm/pip/cargo installs | accept | No new packages. PySide6 6.11.0 already vendored. |

</threat_model>

<verification>
- `grep -c '_cached_row1_min' musicstreamer/ui_qt/now_playing_panel.py` returns 0.
- `grep -c '_picker_row_min_width' musicstreamer/ui_qt/now_playing_panel.py` returns 0 (no shim).
- `grep -q 'def _row1_min_width(self, include_picker: bool, include_volume_cluster: bool)'` succeeds.
- `grep -q 'def _move_volume_cluster_to'` succeeds.
- `grep -q 'def _set_volume_size_policy'` succeeds.
- `grep -q '16777215'` succeeds (QWIDGETSIZE_MAX literal).
- `inspect.getsource(NowPlayingPanel._move_volume_cluster_to)` contains no `addStretch` and no `lambda`.
- `inspect.getsource(NowPlayingPanel._set_volume_size_policy)` contains `setMinimumWidth(0)`, `setMaximumWidth(16777215)`, `setFixedWidth(120)`, and no `lambda`.
- `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` exits 0 (no Phase 72.1 regression — CR-01 layout-agnostic invariant preserved).
- `uv run pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q` STILL fails some tests at this point — but tests that exercise the cluster-row1-index or membership across known layouts (controls / row2 only) can already pass partial assertions. Plan 03 closes the rest.
</verification>

<success_criteria>
1. Cache field renamed/reshaped (`_cached_row1_min` → `_row1_min_cache: dict[tuple[bool, bool], int]`); zero remaining references to the old name.
2. `_volume_cluster_row1_index` captured at construction time between the volume_slider addWidget and compact_mode_toggle_btn addWidget (mirroring `_picker_row1_index` at line 509).
3. `_picker_row_min_width` REPLACED (no shim) by `_row1_min_width(include_picker, include_volume_cluster)`; existing 72.1 caller updated to `_row1_min_width(True, True)`.
4. CR-01 layout-agnostic invariant preserved with THREE add-back branches (one per wrap-eligible widget).
5. `_move_volume_cluster_to` exists with cluster-as-unit reparent, D-02 left-to-right order, off-by-one-correct insertWidget on row-1 return, no addStretch, no lambda.
6. `_set_volume_size_policy` exists with canonical Qt 6 three-call round-trip (setMinimumWidth(0); setMaximumWidth(16777215); setSizePolicy(Expanding, Fixed)) and explicit belt-and-braces (Fixed, Fixed) on return path.
7. `uv run pytest tests/test_phase72_1_stream_picker_reflow.py -x -q` and `uv run pytest tests/test_now_playing_panel.py -x -q` both exit 0 (no regression).
8. Plan 01 RED test file's threshold + size-policy-round-trip subset (whichever tests exercise the helpers WITHOUT requiring the not-yet-wired reflow predicate) shows green — the membership / station-change / widen-back / idempotency tests stay RED for Plan 03.
</success_criteria>

<output>
Create `.planning/phases/72.4-i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid/72.4-02-SUMMARY.md` when done. Record:
- Diff stat of musicstreamer/ui_qt/now_playing_panel.py (lines added/removed).
- Confirmation: `grep -c '_cached_row1_min'` and `grep -c '_picker_row_min_width'` both return 0.
- Which Plan 01 tests turned GREEN with this plan (expected: threshold + size-policy round-trip; possibly the order/size-invariant negative for compact-toggle).
- Which Plan 01 tests remain RED (expected: membership / wave / widen-back / station-change / idempotency — Plan 03 closes them).
- Any deviations from RESEARCH §Pattern 1/2/3 (none expected).
</output>
</content>
</invoke>