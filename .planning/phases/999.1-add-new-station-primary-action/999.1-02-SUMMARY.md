---
phase: 999.1-add-new-station-primary-action
plan: 02
subsystem: ui
tags: [qt, pyside6, station-list-panel, signals, tdd]

requires:
  - phase: 999.1-00
    provides: Wave-0 RED tests for "+" button and select_station
provides:
  - "StationListPanel.new_station_requested = Signal() — zero-arg panel-level signal"
  - "Right-aligned '+' QToolButton in new top-level header row (visible in both Stations and Favorites modes)"
  - "StationListPanel.select_station(station_id) public method with source→proxy index mapping"
affects: [999.1-03-PLAN]

tech-stack:
  added: []
  patterns:
    - "Bound-slot signal re-emission (QA-05 — no self-capturing lambda)"
    - "Proxy-model index mapping via mapFromSource before setCurrentIndex (Pitfall #2 guard)"

key-files:
  created: []
  modified:
    - musicstreamer/ui_qt/station_list_panel.py

key-decisions:
  - "Header row placed at top-level layout (index 0) not inside Stations page — New Station entry point stays visible when user is on Favorites"
  - "Inline source-model walk in select_station rather than adding index_for_station_id to StationTreeModel — keeps model public surface minimal per plan Pitfall #2 guidance"
  - "Used setFixedSize(24, 24) on QToolButton with '+' text instead of themed QIcon — simpler, consistent across platforms, matches existing clear-btn pattern that falls back to text"

patterns-established:
  - "Panel-level signals re-emitted from private button slots (_on_new_station_btn_clicked → new_station_requested.emit())"
  - "Public navigation helpers on StationListPanel guard visibility by switching _stack to index 0 before acting"

requirements-completed: [D-02, D-07]

duration: 3m 8s
completed: 2026-04-24
---

# Phase 999.1 Plan 02: StationListPanel "+" button + select_station Summary

**Adds a right-aligned "+" QToolButton header row emitting `new_station_requested`, plus a `select_station(station_id)` helper that maps source-model indexes through `StationFilterProxyModel` before calling `setCurrentIndex`.**

## Performance

- **Duration:** 3m 8s
- **Started:** 2026-04-24T13:12:09Z
- **Completed:** 2026-04-24T13:15:17Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- `new_station_requested = Signal()` declared on `StationListPanel` (zero-arg).
- New top-level header row with right-aligned `_new_station_btn` (QToolButton, tooltip "New Station", 24×24 fixed).
- Button click wired via bound slot `_on_new_station_btn_clicked` (QA-05 compliant, no lambda).
- `select_station(station_id)` public method: walks source model, maps via `self._proxy.mapFromSource`, calls `setCurrentIndex` + `scrollTo`.
- Switches to Stations page first if currently on Favorites so selection is visible.
- All 3 Plan-02-scoped Wave-0 tests turn GREEN; no new regressions introduced.

## Task Commits

1. **Task 02-01: Add "+" QToolButton + new_station_requested signal** — `f2840ad` (feat)
2. **Task 02-02: Implement select_station with proxy mapping** — `0add519` (feat)

Plan metadata commit will follow this SUMMARY write.

_No explicit RED commits this plan — the Wave-0 stubs committed in Plan 999.1-00 served as the RED state. Plan 02 delivered the GREEN transition._

## Files Created/Modified
- `musicstreamer/ui_qt/station_list_panel.py`
  - Imports: added `QToolButton`
  - Line 89: added `new_station_requested = Signal()` class attribute
  - Lines 99–119: new header row (QWidget + QHBoxLayout + stretch + QToolButton), `layout.insertWidget(0, header_row)`
  - Lines 322–348: new `select_station(station_id)` public method near `refresh_model`
  - Lines 486–488: new bound slot `_on_new_station_btn_clicked`

## Decisions Made
- Header row placed at top-level layout (above segmented control) rather than inside `stations_page` so "+" is reachable in Favorites mode as well. Aligns with D-02 intent ("header row" + entry-point parity with hamburger menu).
- `select_station` guards visibility by switching `_stack` to index 0 when user is on Favorites (Pitfall #3); no-op when station id is not found.
- Walked source model in `select_station` rather than extending `StationTreeModel` API — plan explicitly preferred inline walk to keep model surface minimal.

## Deviations from Plan

None — plan executed exactly as written.

One environment-level adjustment was needed to run the pytest-qt `mouseClick` helper: the project's PySide6 install (`PySide6==6.9.2` via system package) lacked `QtTest`. Installed matching-version addon via `pip install --user --break-system-packages "PySide6-Addons==6.9.2" "shiboken6==6.9.2"`. This is a local dev-env fix, not a code change — no tracked files affected.

## Issues Encountered
- `tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode` fails on `main` — asserts `panel._search_box.isVisibleTo(panel)` but the filter strip starts collapsed via `self._filter_strip.setVisible(False)`. Confirmed failing on `fb5a733` before Plan 02 edits (out of scope — logged in `deferred-items.md`).

## Verification

- **Plan-02 tests (all GREEN):**
  - `test_new_station_button_exists_and_right_aligned` — PASS
  - `test_new_station_button_emits_signal` — PASS
  - `test_select_station_by_id_sets_current_index` — PASS
- **Full file regression:** `pytest tests/test_station_list_panel.py` — 18 passed, 1 failed (pre-existing, out of scope).
- **Proxy-mapping guard check:** `grep -B 2 -A 8 "setCurrentIndex" musicstreamer/ui_qt/station_list_panel.py` shows `mapFromSource` on the line immediately above `setCurrentIndex` — Pitfall #2 handled.
- **Single-file diff:** `git diff --stat HEAD~2 HEAD` (Plan 02 commits only) shows `musicstreamer/ui_qt/station_list_panel.py` exclusively.

## Next Phase Readiness
- Plan 03 can now wire `MainWindow`'s `_on_new_station_clicked` slot to both the hamburger menu action and `panel.new_station_requested`.
- Post-save focus (D-07) can call `self._station_list_panel.select_station(new_station_id)` after `refresh_model()` — selection will render through the proxy.
- Plan 02 and Plan 01 touch disjoint files (`station_list_panel.py` vs `edit_station_dialog.py`); no merge conflicts expected.

## Self-Check: PASSED

- File exists: `musicstreamer/ui_qt/station_list_panel.py` — FOUND
- Commit exists: `f2840ad` — FOUND
- Commit exists: `0add519` — FOUND

---
*Phase: 999.1-add-new-station-primary-action*
*Completed: 2026-04-24*
