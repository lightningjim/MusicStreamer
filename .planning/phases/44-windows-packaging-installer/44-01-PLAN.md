---
phase: 44
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_single_instance.py
  - tests/test_runtime_check.py
  - tests/test_pkg03_compliance.py
  - tests/test_spec_hidden_imports.py
  - tests/ui_qt/__init__.py
  - tests/ui_qt/test_main_window_node_indicator.py
  - tests/ui_qt/test_missing_node_dialog.py
  - tools/__init__.py
  - tools/check_subprocess_guard.py
  - tools/check_spec_entry.py
autonomous: true
requirements: [PKG-03, PKG-04, QA-03]
must_haves:
  truths:
    - "Wave 0 test scaffolds exist for single_instance, runtime_check, and Node-indicator UI behaviors"
    - "PKG-03 compliance is enforced as a Python test (cross-platform, runs on Linux CI too)"
    - ".spec hidden-imports drift is detectable via an automated AST/text test"
    - "Tooling helpers (check_subprocess_guard.py, check_spec_entry.py) exist as standalone Python scripts callable from build.ps1"
  artifacts:
    - path: "tests/test_single_instance.py"
      provides: "PKG-04 QLocalServer round-trip + activation forwarding tests"
      min_lines: 40
    - path: "tests/test_runtime_check.py"
      provides: "RUNTIME-01 Node detection (available/absent/win32 node.exe preference) tests"
      min_lines: 40
    - path: "tests/test_pkg03_compliance.py"
      provides: "PKG-03 grep regression test (no bare subprocess.* outside subprocess_utils.py)"
      min_lines: 20
    - path: "tests/test_spec_hidden_imports.py"
      provides: "PKG-01 .spec content guard (asserts required hiddenimports + entry point)"
      min_lines: 25
    - path: "tests/ui_qt/test_main_window_node_indicator.py"
      provides: "RUNTIME-01 hamburger menu conditional QAction visibility tests"
      min_lines: 30
    - path: "tests/ui_qt/test_missing_node_dialog.py"
      provides: "RUNTIME-01 startup dialog button wiring tests"
      min_lines: 25
    - path: "tools/check_subprocess_guard.py"
      provides: "Build-time PKG-03 guard callable from build.ps1 + as standalone tool"
      min_lines: 30
    - path: "tools/check_spec_entry.py"
      provides: "Build-time .spec entry-point guard"
      min_lines: 20
  key_links:
    - from: "tools/check_subprocess_guard.py"
      to: "musicstreamer/"
      via: "Path.rglob over musicstreamer/, regex search for subprocess.{Popen,run,call}"
      pattern: "rglob.*\\.py.*subprocess\\\\."
    - from: "tests/test_pkg03_compliance.py"
      to: "musicstreamer/"
      via: "Same grep logic as the build-time guard"
      pattern: "subprocess\\\\.(Popen|run|call)"
    - from: "tests/test_single_instance.py"
      to: "musicstreamer.single_instance"
      via: "import + monkeypatched SERVER_NAME + qtbot.waitSignal(activate_requested)"
      pattern: "from musicstreamer import single_instance"
---

<objective>
Land all Wave 0 test scaffolds + build-time guard tools needed by Plans 02-05. These are
RED tests at the moment Plan 02 lands the modules they import — but the test files,
fixtures, and tooling skeletons must exist FIRST so executors of later plans can write
to them and have a single feedback loop.

Purpose: enforces Nyquist-compliance (every later task has an `<automated>` verify command)
and locks the contracts (server name constants, NodeRuntime dataclass shape, hamburger
QAction text, .spec hiddenimports list) before any production code is written.

Output: 7 new test files + 2 new tools + 1 new test package (`tests/ui_qt/`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-windows-packaging-installer/44-CONTEXT.md
@.planning/phases/44-windows-packaging-installer/44-RESEARCH.md
@.planning/phases/44-windows-packaging-installer/44-PATTERNS.md
@.planning/phases/44-windows-packaging-installer/44-VALIDATION.md

<interfaces>
<!-- Contracts the test files lock in for Plan 02. Plan 02 MUST conform to these. -->

# musicstreamer/single_instance.py — landed by Plan 02
SERVER_NAME: str = "org.lightningjim.MusicStreamer.single-instance"  # D-08
_CONNECT_TIMEOUT_MS: int = 500

class SingleInstanceServer(QObject):
    activate_requested = Signal()
    def close(self) -> None: ...

def acquire_or_forward() -> Optional[SingleInstanceServer]: ...
def raise_and_focus(window) -> None: ...

# musicstreamer/runtime_check.py — landed by Plan 02
NODEJS_INSTALL_URL: str = "https://nodejs.org/en/download"

@dataclass(frozen=True)
class NodeRuntime:
    available: bool
    path: Optional[str]

def _which_node() -> Optional[str]: ...   # win32 prefers node.exe
def check_node() -> NodeRuntime: ...
def show_missing_node_dialog(parent) -> None: ...

# musicstreamer/ui_qt/main_window.py — edited by Plan 03
# MainWindow.__init__ adds keyword-only kwarg `node_runtime: NodeRuntime | None = None`
# When node_runtime is not None and not node_runtime.available:
#   self._menu adds separator + QAction with text "⚠ Node.js: Missing (click to install)"
</interfaces>

<analogs>
<!-- Test files mirror these existing patterns -->
@tests/test_media_keys_scaffold.py   # qtbot + monkeypatch + sys.platform idiom (lines 1-13, 67-82)
@tests/test_main_window_integration.py  # main_window factory + qtbot.addWidget pattern
@musicstreamer/subprocess_utils.py   # The single legitimate subprocess.* call site (excluded by guard)
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create build-time tooling (check_subprocess_guard.py + check_spec_entry.py + test_pkg03_compliance.py + test_spec_hidden_imports.py)</name>
  <files>tools/__init__.py, tools/check_subprocess_guard.py, tools/check_spec_entry.py, tests/test_pkg03_compliance.py, tests/test_spec_hidden_imports.py</files>
  <read_first>
    - .planning/phases/44-windows-packaging-installer/44-RESEARCH.md (Pattern 5 build.ps1 lines 762-775; Pattern 4 .spec diff)
    - .planning/phases/44-windows-packaging-installer/44-PATTERNS.md (test_pkg03_compliance.py skeleton lines 478-498)
    - musicstreamer/subprocess_utils.py (the one legitimate subprocess.* call site — guard MUST exclude it)
  </read_first>
  <behavior>
    - tools/check_subprocess_guard.py: walks musicstreamer/, finds bare subprocess.{Popen,run,call} regex hits, EXCLUDES subprocess_utils.py and any path under tests/. Returns exit 0 on zero hits, exit 4 on hits (matches build.ps1 exit code D-22 / RESEARCH §Pattern 5). Prints "PKG-03 OK" or "PKG-03 FAIL: <path>:<lineno>: <line>" for each offender. Also skips lines that start with `#` (comments). Callable as `python tools/check_subprocess_guard.py`.
    - tools/check_spec_entry.py: reads packaging/windows/MusicStreamer.spec as text, asserts it contains `"../../musicstreamer/__main__.py"` (the entry point per RESEARCH §Pattern 4). Exit 0 on found, exit 7 on missing. If the .spec file does not exist yet, exit 0 with message "spec not yet created — guard inactive" (so Wave 1 doesn't block on Wave 1 plan ordering).
    - tests/test_pkg03_compliance.py::test_no_raw_subprocess_in_musicstreamer: pure-Python grep over musicstreamer/, asserts zero offenders (excluding subprocess_utils.py). Pattern: `\bsubprocess\.(Popen|run|call)\b`. Skip comment lines (line.strip().startswith("#")). Currently MUST PASS because Plan 35-06 retired all bare subprocess uses.
    - tests/test_spec_hidden_imports.py: parses packaging/windows/MusicStreamer.spec as text. If file does not exist yet, mark test as `pytest.skip("spec not yet created — Wave 1 Plan 04 dependency")`. When file exists, assert each of these strings appears verbatim: `"../../musicstreamer/__main__.py"`, `"PySide6.QtNetwork"`, `"PySide6.QtSvg"`, `"winrt.windows.media"`, `"winrt.windows.media.playback"`, `name="MusicStreamer"`, `console=False`, `upx=False`, `icon="icons/MusicStreamer.ico"`.
  </behavior>
  <action>
    Create tools/__init__.py as empty file (makes tools/ a package — keeps imports tidy though scripts run via `python tools/check_*.py`).

    Create tools/check_subprocess_guard.py per RESEARCH §Pattern 5 lines 762-775 ported from PowerShell to Python. Use pathlib.Path.resolve().parent.parent / "musicstreamer" relative to __file__ so it works from any CWD. Regex: `re.compile(r"\\bsubprocess\\.(Popen|run|call)\\b")`. Skip subprocess_utils.py by name. Skip comment lines (`stripped.startswith("#")`). Print results, sys.exit(0) on clean, sys.exit(4) on offenders. Match build.ps1's exit-code convention (D-22).

    Create tools/check_spec_entry.py: reads `packaging/windows/MusicStreamer.spec` (path relative to repo root). If missing, print "spec not yet created — guard inactive" and sys.exit(0). If present, search for the literal string `"../../musicstreamer/__main__.py"` (the entry-point reference per RESEARCH §Pattern 4 line 680). On match: sys.exit(0). On no match: print error and sys.exit(7).

    Create tests/test_pkg03_compliance.py per PATTERNS.md lines 478-498: pure-Python regex grep over musicstreamer/*.py and subdirs, excluding subprocess_utils.py and comment lines. assert not offenders with formatted error. Use Path(__file__).resolve().parent.parent / "musicstreamer" — repo-root anchored.

    Create tests/test_spec_hidden_imports.py with one test function `test_spec_contains_required_imports()`. If the .spec file does not exist (Path.exists() False), `pytest.skip(...)`. When it exists, read full text and assert each required string appears (one assert per required substring, with descriptive message naming the missing string). Required strings list above in <behavior>.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python tools/check_subprocess_guard.py && python tools/check_spec_entry.py && pytest tests/test_pkg03_compliance.py tests/test_spec_hidden_imports.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def test_' tests/test_pkg03_compliance.py` returns ≥ 1
    - `grep -c '^def test_' tests/test_spec_hidden_imports.py` returns ≥ 1
    - `grep -q 'subprocess_utils.py' tools/check_subprocess_guard.py` (the exclusion is present)
    - `grep -q 'sys.exit(4)' tools/check_subprocess_guard.py` (exit code matches D-22 / build.ps1)
    - `grep -q 'sys.exit(7)' tools/check_spec_entry.py`
    - `python tools/check_subprocess_guard.py` exits 0 on current codebase (Plan 35-06 retired all bare subprocess)
    - `pytest tests/test_pkg03_compliance.py -x -q` passes
    - `pytest tests/test_spec_hidden_imports.py -x -q` passes (skipped because Plan 04 hasn't landed .spec yet)
  </acceptance_criteria>
  <done>All four files exist; guards return exit 0 on the current tree; pytest runs both new test files (one passes, one skipped pending Plan 04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds for single_instance + runtime_check + UI Node-indicator</name>
  <files>tests/test_single_instance.py, tests/test_runtime_check.py, tests/ui_qt/__init__.py, tests/ui_qt/test_main_window_node_indicator.py, tests/ui_qt/test_missing_node_dialog.py</files>
  <read_first>
    - .planning/phases/44-windows-packaging-installer/44-PATTERNS.md (test patterns lines 391-470; analogs section)
    - .planning/phases/44-windows-packaging-installer/44-RESEARCH.md (Code Examples 1-4 lines 988-1089)
    - tests/test_media_keys_scaffold.py (canonical qtbot + monkeypatch + sys.platform idiom)
    - tests/test_main_window_integration.py (main_window factory pattern, fixture conventions)
  </read_first>
  <behavior>
    - tests/test_single_instance.py: 2 test functions. (a) test_first_instance_acquires_server: monkeypatches SERVER_NAME to "test-mstream-single-inst-a", asserts acquire_or_forward() returns non-None, calls server.close(). (b) test_second_instance_forwards_and_first_sees_activate: monkeypatches SERVER_NAME to "test-mstream-single-inst-b", first acquire returns server, qtbot.waitSignal(first.activate_requested, timeout=1000) wraps a second acquire that returns None. Each test uses a UNIQUE SERVER_NAME so parallel test runs don't collide on the named pipe / unix socket.
    - tests/test_runtime_check.py: 3 test functions. (a) test_check_node_available: monkeypatches _which_node to return "/usr/bin/node", asserts NodeRuntime(available=True, path="/usr/bin/node"). (b) test_check_node_absent: monkeypatches _which_node to return None, asserts NodeRuntime(available=False, path=None). (c) test_which_node_prefers_exe_on_windows: monkeypatches sys.platform to "win32", uses unittest.mock.patch on shutil.which to return "C:\\Program Files\\nodejs\\node.exe" only when called with "node.exe", asserts _which_node() returns that path AND mock_which.assert_called_with("node.exe").
    - tests/ui_qt/test_main_window_node_indicator.py: 2 test functions checking conditional QAction. (a) test_hamburger_indicator_absent_when_node_available: constructs MainWindow with node_runtime=NodeRuntime(True, "/usr/bin/node"), asserts no menu action text contains "Node.js". (b) test_hamburger_indicator_present_when_node_missing: constructs MainWindow with node_runtime=NodeRuntime(False, None), asserts at least one menu action text contains "Node.js: Missing".
    - tests/ui_qt/test_missing_node_dialog.py: 1 test function test_dialog_has_open_and_ok_buttons: calls runtime_check.show_missing_node_dialog(parent=None) with QMessageBox.exec monkeypatched to return immediately (avoid blocking); asserts dialog has buttons whose text contains "Open nodejs.org" and "OK". Use QtBot to manage the QMessageBox lifecycle.
  </behavior>
  <action>
    Create tests/test_single_instance.py per PATTERNS.md lines 396-429. Module docstring per PATTERNS.md lines 396-401. Imports: pytest + `from musicstreamer import single_instance`. Both tests follow the qtbot fixture + monkeypatch.setattr(single_instance, "SERVER_NAME", ...) pattern from tests/test_media_keys_scaffold.py:30-36. Per-test unique SERVER_NAME mandatory.

    Create tests/test_runtime_check.py per PATTERNS.md lines 440-468. Imports: `from unittest.mock import patch`, `import sys`, `from musicstreamer import runtime_check`. The win32 test uses `monkeypatch.setattr(sys, "platform", "win32")` then `with patch("shutil.which") as mock_which:` — exact pattern from RESEARCH §Code Example 3 lines 1057-1067.

    Create tests/ui_qt/__init__.py as empty file (Python package marker).

    Create tests/ui_qt/test_main_window_node_indicator.py per RESEARCH §Code Example 4 lines 1077-1089. Use the existing main_window_factory fixture from conftest.py if present; otherwise construct MainWindow directly with FakePlayer + Repo from tests/test_main_window_integration.py pattern. Assertion checks `[a.text() for a in window._menu.actions()]` (the existing _menu attribute on MainWindow).

    Create tests/ui_qt/test_missing_node_dialog.py: import runtime_check + QMessageBox. Use `monkeypatch.setattr("PySide6.QtWidgets.QMessageBox.exec", lambda self: 0)` to short-circuit the modal exec; then call show_missing_node_dialog(parent=None) and inspect the dialog's buttons. Pattern: capture the dialog by patching QMessageBox.__init__ to record the instance, OR inspect via mocking — either approach acceptable. The simplest: monkeypatch `runtime_check.QMessageBox` to a FakeBox class that records addButton calls and exec invocation. Assert FakeBox saw at least one "Open nodejs.org" addButton + one "OK" addButton.

    All 4 test files: follow PATTERNS.md "Module Docstring Convention" — single triple-quoted docstring naming the phase/req it covers (e.g., `"""Tests for musicstreamer.single_instance (Phase 44, PKG-04)."""`).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && pytest tests/test_single_instance.py tests/test_runtime_check.py tests/ui_qt/test_main_window_node_indicator.py tests/ui_qt/test_missing_node_dialog.py --collect-only -q 2>&1 | grep -E '^tests/.*::test_' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def test_' tests/test_single_instance.py` returns ≥ 2
    - `grep -c '^def test_' tests/test_runtime_check.py` returns ≥ 3
    - `grep -c '^def test_' tests/ui_qt/test_main_window_node_indicator.py` returns ≥ 2
    - `grep -c '^def test_' tests/ui_qt/test_missing_node_dialog.py` returns ≥ 1
    - `grep -q 'SERVER_NAME' tests/test_single_instance.py` AND `grep -q 'monkeypatch.setattr(single_instance' tests/test_single_instance.py`
    - `grep -q 'qtbot.waitSignal' tests/test_single_instance.py`
    - `grep -q 'NodeRuntime(available=True' tests/test_runtime_check.py` AND `grep -q 'node.exe' tests/test_runtime_check.py`
    - `grep -q 'Node.js: Missing' tests/ui_qt/test_main_window_node_indicator.py`
    - `pytest tests/test_single_instance.py tests/test_runtime_check.py tests/ui_qt/test_main_window_node_indicator.py tests/ui_qt/test_missing_node_dialog.py --collect-only -q` reports ≥ 8 collected tests
    - Tests are RED at this point (modules don't exist) — that's expected. Plan 02 turns them GREEN.
  </acceptance_criteria>
  <done>All 5 files exist; pytest collects ≥ 8 tests across them; tests fail with ImportError on `from musicstreamer import single_instance` / `runtime_check` (expected RED — Plan 02 lands the imports).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-tool → repo | Tooling reads repo source files; no write back. Read-only is the only privilege. |
| test → repo | Tests read source files for grep guards; no production code paths exercised here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | T (Tampering) | tools/check_subprocess_guard.py exclusion list bypass | mitigate | Exclude only by literal `Name == "subprocess_utils.py"` (not by glob). Reviewed during code review when adding new exclusions. |
| T-44-01-02 | I (Info Disclosure) | Test file paths leak in failure messages | accept | Local-only tooling; no remote reporting. Acceptable for personal-scale build. |
</threat_model>

<verification>
- `pytest tests/test_pkg03_compliance.py tests/test_spec_hidden_imports.py -q` — 1 PASS + 1 SKIP (until Plan 04)
- `pytest tests/test_single_instance.py tests/test_runtime_check.py tests/ui_qt/test_main_window_node_indicator.py tests/ui_qt/test_missing_node_dialog.py --collect-only -q` — collects ≥ 8 tests
- `python tools/check_subprocess_guard.py` exits 0 on current tree
- `python tools/check_spec_entry.py` exits 0 (spec not yet created — guard inactive)
- Existing test suite still green: `pytest -q` — 399+ tests pass (no regression)
</verification>

<success_criteria>
- 7 new test files + 2 new tools + 1 new test package marker exist
- All Wave 0 stubs from 44-VALIDATION.md "Wave 0 Requirements" are landed
- nyquist_compliant prerequisites met for downstream plans
- No production code changes — pure scaffolding
</success_criteria>

<output>
After completion, create `.planning/phases/44-windows-packaging-installer/44-01-SUMMARY.md` per the standard SUMMARY template.
</output>
