---
phase: 44
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - musicstreamer/__main__.py
  - musicstreamer/ui_qt/main_window.py
  - tests/test_main_window_integration.py
autonomous: true
requirements: [PKG-04, RUNTIME-01, QA-03]
must_haves:
  truths:
    - "GUI entry path order is: _set_windows_aumid → Gst.init → migration → QApplication → single_instance.acquire_or_forward → runtime_check.check_node → MainWindow → server.activate_requested wiring → window.show → app.exec"
    - "Second instance returns immediately (sys.exit 0 via return) — never constructs MainWindow"
    - "When Node.js is missing: startup dialog displays once, hamburger menu shows '⚠ Node.js: Missing (click to install)' QAction, and YT play failures show 'Install Node.js for YouTube playback' toast"
    - "When Node.js is present: no warning surfaces in any of the three places"
    - "MainWindow constructor accepts node_runtime as keyword-only kwarg (defaults to None for back-compat with existing callers)"
  artifacts:
    - path: "musicstreamer/__main__.py"
      provides: "Single-instance + Node.js wiring in _run_gui"
      contains: "single_instance.acquire_or_forward"
    - path: "musicstreamer/ui_qt/main_window.py"
      provides: "MainWindow accepts node_runtime kwarg; conditional hamburger QAction; YT-fail toast branch"
      contains: "node_runtime"
    - path: "tests/test_main_window_integration.py"
      provides: "Extended integration test for YT-fail toast when Node missing"
      contains: "node_runtime"
  key_links:
    - from: "musicstreamer/__main__.py::_run_gui"
      to: "musicstreamer.single_instance.acquire_or_forward"
      via: "lazy import inside _run_gui after QApplication construction"
      pattern: "single_instance\\.acquire_or_forward\\(\\)"
    - from: "musicstreamer/__main__.py::_run_gui"
      to: "musicstreamer.runtime_check.check_node"
      via: "lazy import after single_instance, before MainWindow"
      pattern: "runtime_check\\.check_node\\(\\)"
    - from: "musicstreamer/__main__.py::_run_gui"
      to: "musicstreamer.single_instance.raise_and_focus"
      via: "server.activate_requested signal connected via parameter-only lambda capturing window"
      pattern: "server\\.activate_requested\\.connect"
    - from: "musicstreamer/ui_qt/main_window.py::MainWindow.__init__"
      to: "node_runtime kwarg → self._node_runtime → conditional self._menu.addAction"
      via: "if node_runtime is not None and not node_runtime.available branch after Group 3 menu items"
      pattern: "Node\\.js: Missing"
    - from: "musicstreamer/ui_qt/main_window.py::_on_playback_error"
      to: "self.show_toast('Install Node.js for YouTube playback')"
      via: "early-return branch when node_runtime missing AND 'YouTube resolve failed' in message"
      pattern: "Install Node.js for YouTube playback"
---

<objective>
Wire the Plan 02 modules into the application entry point and main window. Three coordinated
edits: __main__.py (D-10 single-instance + D-11/D-12 Node detection), main_window.py
(D-13 hamburger indicator + YT-fail toast branch), and an integration test extension to
prove the toast fires.

Purpose: makes single-instance + Node.js detection live at runtime; turns Plan 01's
hamburger-indicator widget tests GREEN.

Output: 3 file edits, no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/44-windows-packaging-installer/44-CONTEXT.md
@.planning/phases/44-windows-packaging-installer/44-RESEARCH.md
@.planning/phases/44-windows-packaging-installer/44-PATTERNS.md

<interfaces>
<!-- Modules landed by Plan 02 — these are the imports __main__.py and MainWindow now consume -->

# musicstreamer/single_instance.py
SERVER_NAME: str = "org.lightningjim.MusicStreamer.single-instance"
class SingleInstanceServer(QObject):
    activate_requested: Signal  # no args
    def close(self) -> None: ...
def acquire_or_forward() -> Optional[SingleInstanceServer]: ...
def raise_and_focus(window) -> None: ...

# musicstreamer/runtime_check.py
@dataclass(frozen=True)
class NodeRuntime:
    available: bool
    path: Optional[str]
def check_node() -> NodeRuntime: ...
def show_missing_node_dialog(parent) -> None: ...

# musicstreamer/__main__.py — current _run_gui (lines 128-156)
# def _run_gui(argv): _set_windows_aumid; Gst.init; migration; QApplication;
#   if win32: setStyle Fusion + _apply_windows_palette; db_connect/Player/Repo;
#   MainWindow(player, repo); window.show(); return app.exec()

# musicstreamer/ui_qt/main_window.py — current MainWindow.__init__ signature (line 103)
# def __init__(self, player, repo, parent: QWidget | None = None) -> None
# Existing _on_playback_error (lines 328-331)
# Existing _menu attribute populated lines 121-166 with addSeparator + addAction calls
</interfaces>

<analogs>
@musicstreamer/__main__.py            # _run_gui body to extend (lines 128-156); lazy-import pattern (lines 133-141)
@musicstreamer/ui_qt/main_window.py   # constructor lines 100-228; hamburger menu lines 119-170; _on_playback_error lines 328-331
@tests/test_main_window_integration.py  # existing FakePlayer fixture + qtbot pattern; extend in place
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire single_instance + runtime_check into __main__.py::_run_gui</name>
  <files>musicstreamer/__main__.py</files>
  <read_first>
    - musicstreamer/__main__.py (current _run_gui lines 128-156 — extend in place, do not refactor)
    - .planning/phases/44-windows-packaging-installer/44-PATTERNS.md (__main__.py section lines 181-232 — exact insertion points)
    - .planning/phases/44-windows-packaging-installer/44-RESEARCH.md (Pattern 2 wiring block lines 504-531; Pattern 3 wiring lines 631-668)
    - musicstreamer/single_instance.py (Plan 02 output — interface for acquire_or_forward + raise_and_focus)
    - musicstreamer/runtime_check.py (Plan 02 output — interface for check_node + show_missing_node_dialog + NodeRuntime)
  </read_first>
  <behavior>
    - _run_gui body order (D-10 explicit): _set_windows_aumid() → Gst.init(None) → migration.run_migration() → imports → QApplication construction → setApplicationName + setDesktopFileName → win32 Fusion+palette branch → [NEW] single_instance.acquire_or_forward → if None return 0 → [NEW] runtime_check.check_node → if not available show_missing_node_dialog(parent=None) → db_connect+Player+Repo → MainWindow(player, repo, node_runtime=node_runtime) → server.activate_requested.connect(lambda: single_instance.raise_and_focus(window)) → window.show() → return app.exec().
    - Lazy imports for single_instance and runtime_check (matches existing __main__.py:133-141 lazy-import idiom — keeps _run_smoke path cheap).
    - Single-instance check MUST run AFTER QApplication construction (D-10 / Pitfall: QLocalServer needs the event loop).
    - Node.js detection MUST run AFTER single-instance (so a second instance never shows the dialog) and BEFORE MainWindow construction (so node_runtime can be passed as kwarg).
    - The activate_requested → raise_and_focus connection is the single permitted parameter-only lambda (captures `window`, not `self` since this is module-scope code, not a method).
    - DO NOT modify _run_smoke, _apply_windows_palette, _set_windows_aumid, or main() — extend _run_gui only.
  </behavior>
  <action>
    Edit musicstreamer/__main__.py::_run_gui (lines 128-156). Apply the exact insertion sequence from PATTERNS.md lines 187-228:

    AFTER the `if sys.platform == "win32":` block (line 145-147 region), BEFORE `con = db_connect()` (line 149):

    ```python
    # D-10: single-instance BEFORE MainWindow construction (after QApplication)
    from musicstreamer import single_instance  # lazy
    server = single_instance.acquire_or_forward()
    if server is None:
        return 0  # second instance forwarded — exit cleanly

    # D-11/D-12: Node.js detection BEFORE window.show()
    from musicstreamer import runtime_check  # lazy
    node_runtime = runtime_check.check_node()
    if not node_runtime.available:
        runtime_check.show_missing_node_dialog(parent=None)
    ```

    Then change the existing `MainWindow(player, repo)` call (line 154) to `MainWindow(player, repo, node_runtime=node_runtime)`.

    Then immediately AFTER MainWindow construction, BEFORE `window.show()`, add:

    ```python
    server.activate_requested.connect(  # parameter-only lambda — captures `window`, not self
        lambda: single_instance.raise_and_focus(window)
    )
    ```

    Verify final order exactly matches PATTERNS.md lines 188-227. No other edits to __main__.py — preserve _run_smoke, _apply_windows_palette, _set_windows_aumid, main(), DEFAULT_SMOKE_URL untouched.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -m py_compile musicstreamer/__main__.py && grep -q 'single_instance.acquire_or_forward' musicstreamer/__main__.py && grep -q 'runtime_check.check_node' musicstreamer/__main__.py && grep -q 'node_runtime=node_runtime' musicstreamer/__main__.py && grep -q 'server.activate_requested.connect' musicstreamer/__main__.py && pytest tests/test_headless_entry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile musicstreamer/__main__.py` exits 0
    - `grep -c 'single_instance' musicstreamer/__main__.py` returns ≥ 2 (one import + one call)
    - `grep -c 'runtime_check' musicstreamer/__main__.py` returns ≥ 2
    - `grep -q 'if server is None:' musicstreamer/__main__.py` AND `grep -q 'return 0' musicstreamer/__main__.py` (second-instance early-exit branch)
    - `grep -q 'show_missing_node_dialog(parent=None)' musicstreamer/__main__.py`
    - `grep -q 'MainWindow(player, repo, node_runtime=node_runtime)' musicstreamer/__main__.py`
    - `grep -q 'server.activate_requested.connect' musicstreamer/__main__.py`
    - `grep -q 'lambda: single_instance.raise_and_focus(window)' musicstreamer/__main__.py`
    - Order check: line number of `acquire_or_forward` < line number of `check_node` < line number of `MainWindow(player, repo` (verify with grep -n)
    - Existing tests/test_headless_entry.py still passes (smoke path unchanged)
    - `_run_smoke` body unchanged — `git diff musicstreamer/__main__.py` shows no edits between line 17-64
  </acceptance_criteria>
  <done>__main__.py wires single_instance + runtime_check in correct order; smoke path untouched; tests/test_headless_entry.py still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add node_runtime kwarg to MainWindow + hamburger indicator + YT-fail toast branch + extend integration test</name>
  <files>musicstreamer/ui_qt/main_window.py, tests/test_main_window_integration.py</files>
  <read_first>
    - musicstreamer/ui_qt/main_window.py (constructor lines 100-228; menu lines 119-170; _on_playback_error lines 328-331)
    - .planning/phases/44-windows-packaging-installer/44-PATTERNS.md (main_window.py section lines 236-302)
    - .planning/phases/44-windows-packaging-installer/44-RESEARCH.md (lines 633-669 — MainWindow integration block)
    - tests/test_main_window_integration.py (existing FakePlayer + main_window construction patterns to extend)
    - tests/ui_qt/test_main_window_node_indicator.py (Plan 01 output — defines the contract for the menu QAction text)
    - tests/ui_qt/test_missing_node_dialog.py (Plan 01 output — already passing from Plan 02; no new wiring needed here)
  </read_first>
  <behavior>
    - MainWindow.__init__ signature (line 103) gains a keyword-only `node_runtime=None` parameter inserted before `parent` (per PATTERNS.md line 247-254). Default None preserves back-compat with existing test callers.
    - `self._node_runtime = node_runtime` stored immediately after `self._player = player; self._repo = repo` (around line 106-107).
    - Hamburger menu insertion AFTER existing Group 3 (export/import settings, line 166) AND AFTER worker reference retention (around line 170 — preserve menu order). Block per PATTERNS.md lines 263-271:
      ```python
      if self._node_runtime is not None and not self._node_runtime.available:
          self._menu.addSeparator()
          self._act_node_missing = self._menu.addAction(
              "⚠ Node.js: Missing (click to install)"
          )
          self._act_node_missing.triggered.connect(self._on_node_install_clicked)
      ```
      The "⚠" escape literal matches existing copywriting convention (`…` for ellipsis). The QAction text MUST contain "Node.js: Missing" exactly (Plan 01 test asserts on this substring).
    - New bound-method handler `_on_node_install_clicked(self)` per PATTERNS.md lines 277-282: lazy-imports QDesktopServices + QUrl, opens "https://nodejs.org/en/download". Place near other `_on_*` handlers (e.g., after `_on_playback_error` block).
    - _on_playback_error (current lines 328-331) gains an early-return branch per PATTERNS.md lines 287-302:
      ```python
      def _on_playback_error(self, message: str) -> None:
          if (
              self._node_runtime is not None
              and not self._node_runtime.available
              and "YouTube resolve failed" in message
          ):
              self.show_toast("Install Node.js for YouTube playback")
              return
          truncated = message[:80] + "…" if len(message) > 80 else message
          self.show_toast(f"Playback error: {truncated}")
      ```
      The Node-missing branch MUST run BEFORE the existing truncation logic; uses early `return` to keep existing path untouched for non-YT errors.
    - Tests/test_main_window_integration.py: add ONE new test `test_yt_fail_toast_when_node_missing` that constructs MainWindow with `node_runtime=NodeRuntime(False, None)`, captures show_toast calls (mock or attribute-spy), emits FakePlayer.playback_error.emit("YouTube resolve failed: ..."), asserts toast was called with "Install Node.js for YouTube playback". Mirror the existing FakePlayer + qtbot conventions in that file. Do NOT modify existing tests in this file.
  </behavior>
  <action>
    Edit musicstreamer/ui_qt/main_window.py:

    (a) Constructor signature (line 103). Replace:
    ```python
    def __init__(self, player, repo, parent: QWidget | None = None) -> None:
    ```
    with:
    ```python
    def __init__(
        self,
        player,
        repo,
        *,
        node_runtime=None,
        parent: QWidget | None = None,
    ) -> None:
    ```

    (b) Constructor body. Insert `self._node_runtime = node_runtime` immediately after the existing `self._player = player; self._repo = repo` lines (around 106-107).

    (c) Hamburger menu insertion. After the existing Group 3 export/import settings + the worker-retention lines (around line 170 — find the exact line by reading the file), insert the conditional Node-missing block from PATTERNS.md lines 263-271 (uses `⚠` Unicode escape literal, matching existing `…` convention). The QAction TEXT MUST be exactly `"⚠ Node.js: Missing (click to install)"`.

    (d) Add `_on_node_install_clicked(self)` bound-method handler per PATTERNS.md lines 277-282. Place near other `_on_*` slots (after _on_playback_error). Lazy-imports QDesktopServices + QUrl.

    (e) Replace _on_playback_error body (lines 328-331) with the extended version per PATTERNS.md lines 288-300 — early-return branch on (node_runtime missing AND "YouTube resolve failed" in message) → show_toast("Install Node.js for YouTube playback"); else fall through to existing truncation behavior. Preserve the exact existing truncation logic.

    Edit tests/test_main_window_integration.py:

    (f) Add one new test function `test_yt_fail_toast_when_node_missing(qtbot, tmp_path)`. Import NodeRuntime: `from musicstreamer.runtime_check import NodeRuntime`. Construct MainWindow with `node_runtime=NodeRuntime(available=False, path=None)` (mirror the existing main_window construction in this file). Use a mock or attribute-spy on `window.show_toast` to record calls (e.g., `window.show_toast = mock.Mock()`). Emit `player.playback_error.emit("YouTube resolve failed: foo")` via the existing FakePlayer pattern. Assert `window.show_toast.assert_called_with("Install Node.js for YouTube playback")`. Do NOT modify any existing test in the file.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -m py_compile musicstreamer/ui_qt/main_window.py && pytest tests/ui_qt/test_main_window_node_indicator.py tests/ui_qt/test_missing_node_dialog.py tests/test_main_window_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile musicstreamer/ui_qt/main_window.py` exits 0
    - `grep -q 'node_runtime=None' musicstreamer/ui_qt/main_window.py` (constructor signature)
    - `grep -q 'self._node_runtime = node_runtime' musicstreamer/ui_qt/main_window.py`
    - `grep -q 'Node.js: Missing (click to install)' musicstreamer/ui_qt/main_window.py`
    - `grep -q '_on_node_install_clicked' musicstreamer/ui_qt/main_window.py`
    - `grep -q 'Install Node.js for YouTube playback' musicstreamer/ui_qt/main_window.py`
    - `grep -q 'YouTube resolve failed' musicstreamer/ui_qt/main_window.py` (Node-fail branch in _on_playback_error)
    - `grep -q 'nodejs.org' musicstreamer/ui_qt/main_window.py`
    - `pytest tests/ui_qt/test_main_window_node_indicator.py -x -q` — both Plan 01 hamburger tests now PASS
    - `pytest tests/test_main_window_integration.py -x -q` — all tests including the new YT-fail toast test PASS
    - All previously-passing tests in tests/test_main_window_integration.py still PASS (no regression)
    - The conditional menu block runs AFTER existing Group 3 menu items: line number of "Node.js: Missing" addAction > line number of last existing addAction in the menu setup
  </acceptance_criteria>
  <done>MainWindow accepts node_runtime kwarg; hamburger indicator + YT-fail toast wired; new integration test passes; no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| second instance → first instance | Bytes from second invocation reach first instance via QLocalSocket; trust boundary already mitigated in Plan 02 |
| user click → external browser | _on_node_install_clicked launches user's default browser with hardcoded NODEJS_INSTALL_URL |
| Player error → UI toast | playback_error message string crosses into show_toast — formatted, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | T (Tampering) | Wiring order regression: someone reorders to put MainWindow before single_instance | mitigate | Acceptance criteria pin line-order check; comments in __main__.py mark D-10 ordering as REQUIRED. |
| T-44-03-02 | I (Information Disclosure) | Player error messages with credentials accidentally leak to toast | mitigate | Pre-existing truncation logic (80 char cap) limits leak surface; this plan does not weaken it (early-return branch only fires for "YouTube resolve failed" prefix which carries no secrets). |
| T-44-03-03 | S (Spoofing) | Synthetic playback_error("YouTube resolve failed") triggers toast even when YT is fine | accept | Heuristic per Assumption A4 — only triggers when Node is already known missing AND Player emits this prefix; false-positive impact is a slightly off-target toast (no security impact). |
</threat_model>

<verification>
- `pytest tests/ui_qt/test_main_window_node_indicator.py tests/ui_qt/test_missing_node_dialog.py tests/test_single_instance.py tests/test_runtime_check.py tests/test_main_window_integration.py tests/test_headless_entry.py -x -q` — all pass
- `pytest -q` full suite — 399 existing + ~9 new tests all green, zero regressions
- `python tools/check_subprocess_guard.py` exits 0
- Manual headless smoke (Linux): `python -m musicstreamer --smoke 'https://ice1.somafm.com/groovesalad-128-mp3'` still works (smoke path uses _run_smoke, not _run_gui)
- Manual GUI launch (Linux): `python -m musicstreamer` opens a window; Node.js is present on dev box, so no warning surfaces
</verification>

<success_criteria>
- 3 file edits land cleanly
- All Plan 01 widget tests + Plan 02 unit tests now pass
- One new integration test (test_yt_fail_toast_when_node_missing) passes
- No regression in existing 399 tests + headless entry test
- Wiring order in __main__.py honors D-10 (single_instance after QApplication, before MainWindow; node detection between)
</success_criteria>

<output>
After completion, create `.planning/phases/44-windows-packaging-installer/44-03-SUMMARY.md` per the standard SUMMARY template.
</output>
