---
phase: 82-twitch-only-station-still-tries-to-play-youtube-stream-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/models.py
  - musicstreamer/repo.py
  - tests/test_repo.py
  - tests/test_stream_picker.py
  - tests/test_now_playing_panel.py
autonomous: true
requirements: []
tags: [phase-82, schema, repo, station, preferred-stream]
must_haves:
  truths:
    - "D-01: stations table carries a `preferred_stream_id INTEGER NULL REFERENCES streams(id) ON DELETE SET NULL` column after db_init"
    - "D-01: Station dataclass exposes `preferred_stream_id: Optional[int]` so all four Repo Station-builders propagate it through `SELECT s.*`"
    - "D-08: schema migration uses the `db_init()` try/except ALTER TABLE idiom (Phase 73 cover_art_source template) — NOT PRAGMA user_version"
    - "D-02: `Repo.set_preferred_stream(station_id, stream_id)` round-trips (set non-None -> get_station returns it; set None -> get_station returns None)"
    - "FakeRepo regression-shield: pre-existing UI suites (tests/test_stream_picker.py, tests/test_now_playing_panel.py) keep passing because their inline FakeRepo gains a `set_preferred_stream` no-op"
  artifacts:
    - path: "musicstreamer/models.py"
      provides: "preferred_stream_id field on Station dataclass"
      contains: "preferred_stream_id: Optional[int] = None"
    - path: "musicstreamer/repo.py"
      provides: "ALTER TABLE migration + Station propagation + set_preferred_stream setter"
      contains: "ALTER TABLE stations ADD COLUMN preferred_stream_id"
    - path: "tests/test_repo.py"
      provides: "migration idempotency + Repo round-trip tests for preferred_stream_id"
      contains: "test_preferred_stream_id_migration_idempotent"
  key_links:
    - from: "musicstreamer/repo.py:db_init"
      to: "stations.preferred_stream_id column"
      via: "try/except sqlite3.OperationalError ALTER TABLE"
      pattern: "ALTER TABLE stations ADD COLUMN preferred_stream_id"
    - from: "Repo.list_stations / get_station / list_recently_played / list_favorite_stations"
      to: "Station(preferred_stream_id=r['preferred_stream_id'])"
      via: "SELECT s.* wildcard auto-picks the new column"
      pattern: "preferred_stream_id=r\\[\"preferred_stream_id\"\\]"
---

<objective>
DB + Repo layer for Phase 82. Adds a single nullable FK column on `stations`, threads it through the `Station` dataclass and all four `Station`-constructing Repo methods, and provides the `set_preferred_stream(station_id, stream_id)` setter that the UI hookup (Plan 82-03) will call. Also seeds `set_preferred_stream` no-ops into the two inline `FakeRepo` classes used by the existing UI test suite so pre-existing tests do not regress with AttributeError once Plan 82-03 inserts the new repo call into `_on_stream_selected`.

Purpose: Plans 82-02 (Player layer) and 82-03 (UI persistence) both require the new column and the new Repo method to exist. This is the foundation wave.

Output: schema migration + dataclass field + four repo SELECT propagation sites + one new setter + idempotency tests + round-trip tests + FakeRepo no-op shields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-twitch-only-station-still-tries-to-play-youtube-stream-first/82-CONTEXT.md
@.planning/phases/82-twitch-only-station-still-tries-to-play-youtube-stream-first/82-RESEARCH.md
@.planning/phases/82-twitch-only-station-still-tries-to-play-youtube-stream-first/82-VALIDATION.md
@musicstreamer/models.py
@musicstreamer/repo.py
@tests/test_repo.py
@tests/test_stream_picker.py
@tests/test_now_playing_panel.py

<interfaces>
<!-- Key contracts the executor must mirror exactly -->

From musicstreamer/models.py (Station dataclass — append-after-last-field rule per Pitfall 3):
```python
@dataclass
class Station:
    id: int
    name: str
    provider_id: Optional[int]
    provider_name: Optional[str]
    tags: str
    station_art_path: Optional[str]
    album_fallback_path: Optional[str]
    icy_disabled: bool = False
    cover_art_source: Literal["auto", "itunes_only", "mb_only"] = "auto"  # Phase 73 D-01/D-05
    streams: List[StationStream] = field(default_factory=list)
    last_played_at: Optional[str] = None
    is_favorite: bool = False
    # Phase 82 D-01: appended LAST with keyword default so existing positional construction stays valid
```

From musicstreamer/repo.py:254-267 (Phase 73 cover_art_source migration template — copy this shape verbatim, only change column SQL):
```python
try:
    con.execute(
        "ALTER TABLE stations ADD COLUMN cover_art_source TEXT NOT NULL DEFAULT 'auto'"
    )
    con.commit()
except sqlite3.OperationalError:
    pass  # column already exists — idempotent
```

From musicstreamer/repo.py:447 / 483 / 570 / 682 (the four Station-builders):
- list_stations() at line 435 — Station(...) at line 447
- get_station() at line 471 — Station(...) at line 483
- list_recently_played() at line 557 — Station(...) at line 570
- list_favorite_stations() at line 671 — Station(...) at line 682

All four use `SELECT s.*, p.name AS provider_name FROM stations s LEFT JOIN providers ...` so the new column auto-selects; the constructor calls need one new kwarg each.

From musicstreamer/repo.py:550 (update_last_played — shape reference for the new setter):
```python
def update_last_played(self, station_id: int):
    self.con.execute(
        "UPDATE stations SET last_played_at = strftime('%Y-%m-%dT%H:%M:%f', 'now') WHERE id = ?",
        (station_id,),
    )
    self.con.commit()
```

From tests/test_repo.py:228 (Phase 73 migration test template) and tests/test_repo.py:860 (drift-guard idiom):
- `test_cover_art_source_migration_idempotent` — exact shape for the new `test_preferred_stream_id_migration_idempotent` (PRAGMA table_info row tuple = (cid, name, type, notnull, dflt_value, pk); index 2 type, 3 NOT NULL, 4 default).
- `test_collate_nocase_drift_guard` — exact shape for any source-grep drift-guard (not required in this plan; Plan 82-02 owns the player.py drift-guard, Plan 82-03 owns the now_playing_panel.py drift-guard).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preferred_stream_id column migration + Station dataclass field</name>
  <files>musicstreamer/models.py, musicstreamer/repo.py, tests/test_repo.py</files>
  <read_first>
    - musicstreamer/repo.py lines 254-267 (Phase 73 cover_art_source ALTER template — copy verbatim, swap column SQL)
    - musicstreamer/repo.py lines 195-252 (legacy URL-column rebuild block — Phase 82 ALTER MUST land AFTER this per Pitfall 2)
    - musicstreamer/models.py lines 26-39 (Station dataclass — append-after-last-field rule)
    - tests/test_repo.py lines 228-252 (test_cover_art_source_migration_idempotent — exact template)
  </read_first>
  <behavior>
    - Test 1 (test_preferred_stream_id_migration_idempotent): db_init twice does not raise; PRAGMA table_info('stations') reports a column named `preferred_stream_id` with type `INTEGER`, notnull=0 (nullable), dflt_value=None.
    - Test 2 (test_preferred_stream_id_default_none_on_fresh_station): `repo.create_station()` followed by `repo.get_station(sid)` returns `preferred_stream_id is None`.
    - Test 3 (test_preferred_stream_id_survives_full_db_init_cycle): inserting a station, manually `UPDATE stations SET preferred_stream_id = <id>`, then re-running `db_init(repo.con)`, leaves the value intact.
  </behavior>
  <action>
    Per D-01 and D-08:
    1. In `musicstreamer/models.py`, append ONE line to the `Station` dataclass AFTER the `is_favorite: bool = False` field (the last field today). The exact line to add (keep the trailing comment for self-documentation):
       `preferred_stream_id: Optional[int] = None  # Phase 82 D-01: per-station sticky preferred stream`
       Do NOT touch any other field, do NOT reorder, do NOT change `Literal[...]` for `cover_art_source`.
    2. In `musicstreamer/repo.py`, after the Phase 73 `cover_art_source` ALTER block (currently ending at line 267 with `pass  # column already exists — idempotent; existing rows backfilled via DEFAULT`), insert a new try/except block. This MUST land AFTER the legacy URL-column rebuild block at lines 195-252 (Pitfall 2 — the rebuild's CREATE TABLE stations_new does not carry dynamically-added columns; placing the ALTER after it ensures the column lands on the rebuilt table). The exact SQL literal to use:
       `ALTER TABLE stations ADD COLUMN preferred_stream_id INTEGER REFERENCES streams(id) ON DELETE SET NULL`
       Wrap in `try: con.execute(...); con.commit() except sqlite3.OperationalError: pass  # column already exists` — same idiom as the cover_art_source block above. NOTE: do NOT add a `NOT NULL` constraint and do NOT add `DEFAULT` — the column is nullable, NULL by default (D-01 + D-08 "no backfill").
    3. In `tests/test_repo.py`, append three new tests near the existing `test_cover_art_source_migration_idempotent` (line 228) using the same `repo` fixture pattern:
       - `test_preferred_stream_id_migration_idempotent(repo)`: call `db_init(repo.con)` twice, then assert PRAGMA table_info('stations') has a row where name == "preferred_stream_id", type == "INTEGER", notnull == 0, dflt_value is None.
       - `test_preferred_stream_id_default_none_on_fresh_station(repo)`: `sid = repo.create_station(); assert repo.get_station(sid).preferred_stream_id is None`.
       - `test_preferred_stream_id_survives_db_init_replay(repo)`: insert station, `repo.con.execute("UPDATE stations SET preferred_stream_id = ? WHERE id = ?", (some_int, sid))`, commit, call `db_init(repo.con)` again, assert `repo.get_station(sid).preferred_stream_id == some_int`. (Note: Task 2 will wire `r["preferred_stream_id"]` into `get_station` — this test passes only after Task 2 also lands. If executor lands Task 1 alone, this test stays RED until Task 2 — acceptable per multi-task plan ordering.)

    Per D-08: do NOT add `PRAGMA user_version` to db_init. CONTEXT.md said "user_version bump" but RESEARCH.md (RQ2) confirmed the project does not use that mechanism — use the try/except ALTER idiom that matches Phase 47 / Phase 73 precedent.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py -k "preferred_stream_id_migration_idempotent or preferred_stream_id_default_none" -x -q</automated>
  </verify>
  <done>
    - `grep -c 'ALTER TABLE stations ADD COLUMN preferred_stream_id' musicstreamer/repo.py` outputs exactly `1`
    - `grep -c 'preferred_stream_id: Optional\[int\] = None' musicstreamer/models.py` outputs exactly `1`
    - Both new migration tests in tests/test_repo.py pass (PRAGMA assertions confirm column type INTEGER, nullable, no default)
    - No `PRAGMA user_version` introduced in musicstreamer/repo.py (verify: `grep -c user_version musicstreamer/repo.py` outputs `0`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread preferred_stream_id through Repo Station-builders + add set_preferred_stream setter + FakeRepo no-op shields</name>
  <files>musicstreamer/repo.py, tests/test_repo.py, tests/test_stream_picker.py, tests/test_now_playing_panel.py</files>
  <read_first>
    - musicstreamer/repo.py lines 435-462 (list_stations Station-builder)
    - musicstreamer/repo.py lines 471-496 (get_station Station-builder)
    - musicstreamer/repo.py lines 557-585 (list_recently_played Station-builder)
    - musicstreamer/repo.py lines 671-697 (list_favorite_stations Station-builder)
    - musicstreamer/repo.py lines 550-555 (update_last_played — shape reference for the new setter)
    - tests/test_stream_picker.py lines 27-50 (inline FakeRepo definition — receives the new no-op)
    - tests/test_now_playing_panel.py lines 38-80 (inline FakeRepo definition — receives the new no-op)
  </read_first>
  <behavior>
    - Test 1 (test_set_preferred_stream_round_trips_via_list_stations): insert a 2-stream station, `repo.set_preferred_stream(sid, twitch_stream_id)`, then `repo.list_stations()` returns the station with `preferred_stream_id == twitch_stream_id`.
    - Test 2 (test_set_preferred_stream_round_trips_via_get_station): same as above but via `get_station(sid)`.
    - Test 3 (test_set_preferred_stream_round_trips_via_list_recently_played): set the value, call `repo.update_last_played(sid)`, assert `list_recently_played()` returns the station with the right `preferred_stream_id`.
    - Test 4 (test_set_preferred_stream_round_trips_via_list_favorite_stations): set the value, favorite the station, assert `list_favorite_stations()` returns it with the right `preferred_stream_id`.
    - Test 5 (test_set_preferred_stream_clears_to_none): set non-None, then `set_preferred_stream(sid, None)`, assert `get_station(sid).preferred_stream_id is None` (D-02 — user re-picks default must be persistable as NULL).
    - Test 6 (test_set_preferred_stream_on_delete_set_null): set the value, then `repo.delete_stream(twitch_stream_id)` (or equivalent), assert `get_station(sid).preferred_stream_id is None` (D-01 FK ON DELETE SET NULL self-heal). NOTE: requires `PRAGMA foreign_keys = ON` — already enforced by the `repo` fixture in test_repo.py.
  </behavior>
  <action>
    Per D-01 / D-02:
    1. In `musicstreamer/repo.py`, modify the four `Station(...)` constructions to pass `preferred_stream_id=r["preferred_stream_id"]`. Exact insertion points (insert ONE line each, immediately after the `is_favorite=...` kwarg line; the `SELECT s.*` clause already pulls the column so `r["preferred_stream_id"]` resolves to either an int or None per D-01):
       - line 447 `list_stations()` Station() — append `preferred_stream_id=r["preferred_stream_id"],` after `is_favorite=bool(r["is_favorite"]),`
       - line 483 `get_station()` Station() — same
       - line 570 `list_recently_played()` Station() — same
       - line 682 `list_favorite_stations()` Station() — same
    2. In `musicstreamer/repo.py`, add a new method `set_preferred_stream` immediately AFTER `update_last_played` (currently at line 550). Match `update_last_played`'s shape exactly. The full method body:
       ```python
       def set_preferred_stream(self, station_id: int, stream_id: Optional[int]) -> None:
           """Phase 82 D-02: persist the user's stream pick. None clears the pick."""
           self.con.execute(
               "UPDATE stations SET preferred_stream_id = ? WHERE id = ?",
               (stream_id, station_id),
           )
           self.con.commit()
       ```
       The `Optional[int]` type hint matters: D-02 says selection is set on every `_on_stream_selected` invocation, INCLUDING when the user picks back the default, so `None` is a valid clear-to-default value.
    3. In `tests/test_stream_picker.py`, add a no-op `set_preferred_stream` method to the inline FakeRepo class (line 27). Exact insertion (place adjacent to other FakeRepo methods):
       ```python
       def set_preferred_stream(self, station_id: int, stream_id) -> None:
           """Phase 82 no-op shield — prevents AttributeError once Plan 82-03 wires the call into _on_stream_selected. Plan 82-03 adds a behavioral test elsewhere."""
           pass
       ```
    4. In `tests/test_now_playing_panel.py`, add the SAME no-op `set_preferred_stream` method to the inline FakeRepo class at line 38. (Same body as above. Note: tests/test_now_playing_panel.py has a SECOND FakeRepo-shaped block at line 3036 with its own `list_streams` — inspect it; if it is a separate class definition, add the no-op there too. If it is a method on the same class, no second edit needed.)
    5. In `tests/test_repo.py`, append the six round-trip tests described in <behavior> above. Use the fixture pattern at the top of test_repo.py (`repo(tmp_path)`); seed multi-stream stations via `repo.insert_station(...)` + `repo.insert_stream(...)` per the existing patterns in that file.

    Per D-04: do NOT modify `Player.play_stream(...)`. This plan touches Repo only.
    Per D-07: do NOT add UI surface. This plan does not touch ui_qt/.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py -k preferred_stream -x -q</automated>
  </verify>
  <done>
    - `grep -c 'preferred_stream_id=r\["preferred_stream_id"\]' musicstreamer/repo.py` outputs exactly `4` (the four Station-builders)
    - `grep -c 'def set_preferred_stream' musicstreamer/repo.py` outputs exactly `1`
    - `grep -c 'def set_preferred_stream' tests/test_stream_picker.py tests/test_now_playing_panel.py` shows at least one match in each file
    - All six new `tests/test_repo.py` round-trip tests pass under `uv run pytest tests/test_repo.py -k preferred_stream -x -q`
    - Pre-existing `tests/test_stream_picker.py` and `tests/test_now_playing_panel.py` suites still pass (no AttributeError regressions): `uv run pytest tests/test_stream_picker.py tests/test_now_playing_panel.py -x -q` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user UI → SQLite | user clicks a stream in a UI-populated dropdown; the integer stream id is written as an FK to stations.preferred_stream_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-00 | n/a | DB + Repo layer | accept | No new external input surface. The `preferred_stream_id` write path accepts only an integer id originally returned by the DB itself (Plan 82-03's `_on_stream_selected` reads `self.stream_combo.itemData(index)` which was set from a DB-backed stream record). All Repo writes use parameterized SQL (`UPDATE stations SET preferred_stream_id = ? WHERE id = ?`). No file I/O, no network, no auth changes. SQLite type affinity + parameterization prevent injection. |
</threat_model>

<verification>
- `uv run pytest tests/test_repo.py -k preferred_stream -x -q` — all 9 new tests pass (3 migration/default + 6 round-trip)
- `uv run pytest tests/test_repo.py -x -q` — full Repo suite remains green (regression net)
- `uv run pytest tests/test_stream_picker.py tests/test_now_playing_panel.py -x -q` — pre-existing UI suites pass (FakeRepo no-ops prevent any future AttributeError regression once Plan 82-03 lands)
- Full suite: `uv run pytest -q --tb=short` — no new failures introduced by schema migration or dataclass field
</verification>

<success_criteria>
- DB migration is idempotent across multiple `db_init()` invocations (PRAGMA table_info assertions)
- `preferred_stream_id` column is INTEGER, nullable, no default, with `ON DELETE SET NULL` FK to `streams(id)`
- Station dataclass propagates the new field through all four read paths (list_stations, get_station, list_recently_played, list_favorite_stations)
- `Repo.set_preferred_stream(station_id, stream_id)` round-trips for both integer and None values
- Pre-existing UI test suites are unaffected (FakeRepo no-ops absorb future Plan 82-03 call)
</success_criteria>

<output>
Create `.planning/phases/82-twitch-only-station-still-tries-to-play-youtube-stream-first/82-01-SUMMARY.md` when done. Record:
- Confirmation that the ALTER TABLE block lands AFTER the legacy URL-column rebuild block (Pitfall 2 mitigated)
- Confirmation that no `PRAGMA user_version` was introduced
- The exact 4 line numbers in repo.py where `preferred_stream_id=r["preferred_stream_id"]` was added (for Plan 82-02's reference)
- Test counts (new tests added in test_repo.py; FakeRepo no-ops in the two UI test files)
</output>
</content>
</invoke>