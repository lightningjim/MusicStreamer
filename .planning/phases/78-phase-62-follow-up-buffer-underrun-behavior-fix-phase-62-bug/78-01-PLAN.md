---
phase: 78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/paths.py
  - musicstreamer/buffer_log.py
  - musicstreamer/__main__.py
  - tests/test_paths.py
  - tests/test_buffer_events_log.py
autonomous: true
requirements:
  - BUG-09
must_haves:
  truths:
    - "A new file `musicstreamer/buffer_log.py` exists with an idempotent `install_buffer_events_handler()` function."
    - "`paths.buffer_events_log_path()` returns `{data_dir()}/buffer-events.log` and honors `_root_override`."
    - "Calling `install_buffer_events_handler()` attaches exactly one `RotatingFileHandler(maxBytes=1_048_576, backupCount=3, encoding='utf-8')` to the `musicstreamer.player` logger pointing at `paths.buffer_events_log_path()`; a second call is a no-op."
    - "`_run_gui` calls `install_buffer_events_handler()` AFTER `migration.run_migration()` returns; `main()` is NOT modified."
    - "`musicstreamer/__main__.py:222` (`logging.basicConfig(level=logging.WARNING)`) is byte-identical to its pre-phase form; per-logger `setLevel(logging.INFO)` for `musicstreamer.player` is retained."
    - "An INFO record on the `musicstreamer.player` logger reaches BOTH the new file sink AND the existing stderr handler (`propagate=True` path)."
    - "At >1MB of writes, `buffer-events.log.1` is produced; `buffer-events.log.4` is never produced (backupCount=3 cap)."
  artifacts:
    - path: "musicstreamer/buffer_log.py"
      provides: "install_buffer_events_handler() — idempotent RotatingFileHandler attach on musicstreamer.player logger"
      exports: ["install_buffer_events_handler"]
    - path: "musicstreamer/paths.py"
      provides: "buffer_events_log_path() helper returning {data_dir}/buffer-events.log"
      contains: "def buffer_events_log_path()"
    - path: "musicstreamer/__main__.py"
      provides: "Boot-time install call inside _run_gui, AFTER migration.run_migration()"
      contains: "install_buffer_events_handler"
    - path: "tests/test_buffer_events_log.py"
      provides: "Handler-attach + rotation + idempotency + propagate-sanity unit tests"
      min_lines: 60
    - path: "tests/test_paths.py"
      provides: "buffer_events_log_path purity + override tests (additive)"
      contains: "test_buffer_events_log_path"
  key_links:
    - from: "musicstreamer/__main__.py::_run_gui"
      to: "musicstreamer.buffer_log.install_buffer_events_handler"
      via: "import + call AFTER migration.run_migration()"
      pattern: "install_buffer_events_handler\\(\\)"
    - from: "musicstreamer/buffer_log.py"
      to: "musicstreamer.player logger"
      via: "logging.getLogger('musicstreamer.player').addHandler(RotatingFileHandler(...))"
      pattern: "getLogger\\(['\"]musicstreamer\\.player['\"]\\)"
    - from: "musicstreamer/buffer_log.py"
      to: "musicstreamer.paths.buffer_events_log_path"
      via: "import + call to resolve sink path"
      pattern: "buffer_events_log_path\\(\\)"
---

<objective>
Ship the file-sink half of Phase 78 Commit A: add a `RotatingFileHandler` to the `musicstreamer.player` logger so every Phase 62 `buffer_underrun ...` INFO line ALSO lands at `~/.local/share/musicstreamer/buffer-events.log` regardless of launch context (terminal vs `.desktop`). The handler attaches to the named logger only — root logger / `basicConfig(WARNING)` stays untouched (Phase 62 Pitfall 5). The install function is idempotent and is called inside `_run_gui` AFTER `migration.run_migration()` so DATA_DIR is guaranteed to exist (Phase 78 Pitfall 1).

Purpose: enable a ~1-week real-world harvest of `cycle_close` events to drive the post-harvest behavior fix (Commit B, planned in a separate pass per D-01).
Output: new helper module + path helper + boot wiring + 5 new unit tests + 2 new path tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-CONTEXT.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-RESEARCH.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md
@.planning/phases/62-audio-buffer-underrun-resilience-intermittent-dropouts-stutt/62-CONTEXT.md
@musicstreamer/oauth_log.py
@musicstreamer/paths.py
@musicstreamer/__main__.py
@tests/test_oauth_log.py
@tests/test_paths.py
@.claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From musicstreamer/paths.py (pure module; no I/O on import; `_root_override` test hook):
- `_root_override: str | None`  (test monkeypatch hook; set to a string to redirect every accessor)
- `def _root() -> str:`         (internal; resolves `_root_override` -> override, else `platformdirs.user_data_dir("musicstreamer")`)
- `def data_dir() -> str:`      (alias of `_root()`)
- `def oauth_log_path() -> str: return os.path.join(_root(), "oauth.log")`  (sibling template)
- All existing helpers use `os.path.join(_root(), "<filename>")` shape; module never calls `os.makedirs`.

From musicstreamer/oauth_log.py (project-canonical RotatingFileHandler pattern):
- `from logging.handlers import RotatingFileHandler`
- Handler shape: `RotatingFileHandler(log_path, maxBytes=64*1024, backupCount=2, encoding="utf-8")`
- Formatter: `logging.Formatter("%(message)s")`
- Attach: `logger.addHandler(handler)`
- KEY DIFFERENCE FROM oauth_log: buffer_log keeps `propagate=True` (default, do NOT set False); maxBytes=1_048_576; backupCount=3; no 0o600 chmod (D-03 diagnostic, not credentials); module-level install function (not a class).

From musicstreamer/__main__.py (boot ordering — load-bearing):
- Line 222 (within `main()`): `logging.basicConfig(level=logging.WARNING)`  -- INVARIANT, byte-identical (Pitfall 5)
- Line 226 (within `main()`): `logging.getLogger("musicstreamer.player").setLevel(logging.INFO)`  -- per-logger escalation, keep in place
- `def _run_gui(argv: list[str]) -> int:` starts at line 163
- Line 176-177 (within `_run_gui`): `from musicstreamer import migration; migration.run_migration()`  -- DATA_DIR is GUARANTEED to exist immediately AFTER this returns
- The install call goes immediately AFTER `migration.run_migration()` returns, before the `QApplication` import block at line 179.

From tests/test_paths.py (existing path-test patterns):
- Autouse fixture `_reset_root_override` at lines 10-16 resets `paths._root_override = None` after each test.
- `test_oauth_log_path_honors_root_override(monkeypatch, tmp_path)` — sibling test template (lines 59-62).
- `test_oauth_log_path_does_not_create_file(monkeypatch, tmp_path)` — purity test template (lines 65-69).
- `test_root_override_redirects_all_accessors` (line ~30) — multi-accessor assertion (add buffer_events_log_path to its assertion list).
- `test_paths_do_no_io_on_import` (line ~50) — call-the-accessor-without-creating-a-file batch (add `paths.buffer_events_log_path()` to its call list).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `buffer_events_log_path()` helper to musicstreamer/paths.py + new tests in tests/test_paths.py</name>
  <files>musicstreamer/paths.py, tests/test_paths.py</files>
  <read_first>
    - musicstreamer/paths.py (full file, 96 lines — read the existing oauth_log_path / gbs_cookies_path / cookies_path helpers and the _root_override test hook for shape)
    - tests/test_paths.py (full file — read the `_reset_root_override` autouse fixture, `test_root_override_redirects_all_accessors`, `test_paths_do_no_io_on_import`, and the `test_oauth_log_path_honors_root_override` / `test_oauth_log_path_does_not_create_file` sibling pair)
    - musicstreamer/oauth_log.py (sibling helper context for D-03 contrast — diagnostic file, no 0o600)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md (§ paths.py section + §S-2 + §S-6)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md (B-78A-06)
  </read_first>
  <action>
    Add a single new helper `buffer_events_log_path() -> str` to `musicstreamer/paths.py` immediately AFTER the existing `oauth_log_path()` definition (the established sibling slot at line ~66). The helper returns `os.path.join(_root(), "buffer-events.log")` and includes a one-line docstring tagged `Phase 78 / BUG-09 D-03` describing it as the size-rotated buffer-event diagnostic log path. Per D-03 and §S-2: the helper is PURE — it does NOT create the directory, does NOT touch disk, and does NOT chmod (this is diagnostic data, not credentials — explicit departure from `oauth_log.py`'s 0o600 wrap that lives in `OAuthLogger.log_event`, not in `paths.py`). The helper takes no arguments. It must honor `_root_override` automatically by going through `_root()`.

    In `tests/test_paths.py`, add THREE additions in this order:
    (1) Inside `test_root_override_redirects_all_accessors`, add one assertion line alongside the existing `paths.oauth_log_path()` line: `assert paths.buffer_events_log_path() == os.path.join(root, "buffer-events.log")`.
    (2) Inside `test_paths_do_no_io_on_import`, add `paths.buffer_events_log_path()` to the batch of accessor calls (alongside the existing `paths.oauth_log_path()` call) so the no-IO invariant is exercised against the new helper.
    (3) Add two NEW sibling test functions modeled verbatim on `test_oauth_log_path_honors_root_override` and `test_oauth_log_path_does_not_create_file` (§ paths.py section of PATTERNS.md gives the exact shape):
      - `test_buffer_events_log_path` (the umbrella name referenced in VALIDATION.md B-78A-06) — assert the path resolves under `_root_override` (`monkeypatch.setattr(paths, "_root_override", str(tmp_path))`) and that the helper returns `os.path.join(str(tmp_path), "buffer-events.log")`.
      - `test_buffer_events_log_path_does_not_create_file` — same monkeypatch, assert `os.path.exists(result) is False` after calling the helper.

    Do NOT modify `_root_override`, `_root()`, or any other accessor. Do NOT add `os.makedirs` anywhere. Do NOT modify the constants.py PEP 562 shim — RESEARCH.md notes it's not needed unless code imports `BUFFER_EVENTS_LOG_PATH` from constants, and Plans 78-01/02/03 do not.
  </action>
  <acceptance_criteria>
    - B-78A-06: `uv run pytest tests/test_paths.py::test_buffer_events_log_path -x` exits 0 AND the docstring of the new helper mentions `Phase 78 / BUG-09 D-03`.
    - `uv run pytest tests/test_paths.py::test_buffer_events_log_path_does_not_create_file -x` exits 0.
    - `uv run pytest tests/test_paths.py::test_root_override_redirects_all_accessors -x` exits 0 (now includes a `buffer_events_log_path` assertion).
    - `uv run pytest tests/test_paths.py::test_paths_do_no_io_on_import -x` exits 0 (now also exercises the new helper).
    - `uv run pytest tests/test_paths.py -x` exits 0 (full file green).
    - `grep -n "buffer_events_log_path" musicstreamer/paths.py` returns exactly 1 definition line (single helper, no duplicates).
    - `grep -n "os.makedirs\|os.chmod" musicstreamer/paths.py` returns no NEW occurrences attributable to this task (purity invariant from §S-2).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_paths.py -x</automated>
  </verify>
  <done>The path helper `buffer_events_log_path()` lives in `musicstreamer/paths.py` next to its `oauth_log_path` sibling; four `test_paths.py` test cases pass; the helper is pure and creates no files.</done>
</task>

<task type="auto">
  <name>Task 2: Create musicstreamer/buffer_log.py with idempotent install_buffer_events_handler() + tests/test_buffer_events_log.py</name>
  <files>musicstreamer/buffer_log.py, tests/test_buffer_events_log.py</files>
  <read_first>
    - musicstreamer/oauth_log.py (full file — exact RotatingFileHandler attach shape; note `propagate=False` there which must NOT be mirrored here)
    - musicstreamer/paths.py (after Task 1 — for `buffer_events_log_path()` symbol)
    - tests/test_oauth_log.py (full file — pattern template; especially the `tmp_path` + rotation assertions at lines 178-203 and the attribute-introspection idiom)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-RESEARCH.md (Pattern 1, Pitfalls 6 + 7, Anti-Patterns section)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md (§ buffer_log.py section + §S-1 + §S-6 + § test_buffer_events_log.py section)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md (B-78A-01..B-78A-05)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Create a new file `musicstreamer/buffer_log.py` containing a single public function `install_buffer_events_handler() -> None`. The function MUST:

    (a) Resolve the file path via `from musicstreamer import paths; path = paths.buffer_events_log_path()` (no string literal duplication). Imports at the top: `from __future__ import annotations`, `import logging`, `from logging.handlers import RotatingFileHandler`, `from musicstreamer import paths` (mirror `oauth_log.py:1-12` shape).

    (b) Grab the named logger via `log = logging.getLogger("musicstreamer.player")` — the SAME logger Phase 62 declared at `player.py:79-81`. Do NOT create a new logger name; do NOT change the logger's level (it's already INFO via `__main__.py:226`).

    (c) Implement idempotency BEFORE adding the handler (RESEARCH Pitfall 7 / §S-1): iterate `log.handlers`, and if any entry is a `RotatingFileHandler` whose `baseFilename == path`, return early without adding a second handler.

    (d) Construct the handler with EXACT D-02 parameters: `RotatingFileHandler(path, maxBytes=1_048_576, backupCount=3, encoding="utf-8")`. Do NOT pass `delay=True` (RESEARCH recommends Option A — install post-migration — over Option B; install timing is already after migration). Do NOT mirror `oauth_log.py`'s `propagate=False` — leave the default `propagate=True` so the existing stderr handler from `basicConfig(WARNING)` keeps receiving the record (Pitfall 5 invariant; cross-checked at Task 3).

    (e) Set the formatter via `handler.setFormatter(logging.Formatter("%(asctime)s %(message)s"))` (RESEARCH Open Question 1 / Pattern 1 recommendation: include `%(asctime)s` so each file line is independently date-stampable on top of the in-line `start_ts=%.3f`). The structured `buffer_underrun ...` payload that grep targets is identical to stderr regardless of formatter choice.

    (f) Attach: `log.addHandler(handler)`. Do NOT call `os.chmod(path, 0o600)` — D-03 explicitly waives the credential-grade permissions; diagnostic data wants `cat`/`grep`-able default perms. Do NOT call `os.makedirs(...)` — directory existence is the caller's responsibility (Task 3 guarantees this by installing post-migration).

    Module docstring tags: `Phase 78 / BUG-09 Commit A`, mentions D-02 (rotation parameters), D-03 (path source), and Pitfall 7 (idempotency).

    Create a new test file `tests/test_buffer_events_log.py` with these test functions (PATTERN: `tests/test_oauth_log.py` + RESEARCH Pitfall 6 attribute-introspection idiom; map each test to a VALIDATION behavior ID):

    - `test_handler_attached_to_player_logger(tmp_path, monkeypatch)` (B-78A-01): monkeypatch `paths._root_override` to `str(tmp_path)`, call `install_buffer_events_handler()`, assert exactly one `RotatingFileHandler` is on `musicstreamer.player`'s `.handlers`, and that its `baseFilename == str(tmp_path / "buffer-events.log")`, `maxBytes == 1_048_576`, `backupCount == 3`.

    - `test_emit_writes_line_to_file(tmp_path, monkeypatch)` (B-78A-02): same setup, then `logging.getLogger("musicstreamer.player").info("buffer_underrun start_ts=1.0 url=%r outcome=recovered", "http://x/")`, assert the file at `tmp_path / "buffer-events.log"` exists and contains the substring `buffer_underrun` (single emit → single line).

    - `test_rotation_at_1mb(tmp_path, monkeypatch)` (B-78A-03 part A): same setup, then write enough INFO records to exceed 1MB (e.g. 600 iterations of ~2KB payload — mirror the `test_log_rotation_at_64kb` shape from `test_oauth_log.py`). Assert `os.path.exists(str(tmp_path / "buffer-events.log.1"))`.

    - `test_never_creates_backup_4(tmp_path, monkeypatch)` (B-78A-03 part B): same setup, then write enough INFO records (~3000 × ~2KB) to exceed 4× the 1MB rollover budget. Assert `os.path.exists(str(tmp_path / "buffer-events.log.4")) is False` — `backupCount=3` caps at `.3`.

    - `test_install_is_idempotent(tmp_path, monkeypatch)` (B-78A-04): same setup, call `install_buffer_events_handler()` TWICE, assert the count of `RotatingFileHandler` instances on the `musicstreamer.player` logger is exactly 1 (Pitfall 7).

    - `test_record_reaches_both_sinks(tmp_path, monkeypatch, capsys)` (B-78A-05): same setup, install handler, emit one INFO record on `musicstreamer.player`, then assert (a) the file at `tmp_path / "buffer-events.log"` has the substring `buffer_underrun`, AND (b) the propagate=True path means the record also reached the root logger — verify by `propagate=True` attribute introspection: `assert logging.getLogger("musicstreamer.player").propagate is True`. Do NOT depend on `capsys`/`caplog` to capture stderr from `basicConfig` (it's set up before pytest starts in production; the propagate=True attribute check is the contract this test enforces — RESEARCH A2 says live-Python-verified, the unit test pins the configuration).

    Add a per-test (function-scoped) fixture `_clean_player_handlers` (autouse=True at file scope or applied to each test via parameter) that snapshots `logging.getLogger("musicstreamer.player").handlers` at setup and restores the original list at teardown — the `musicstreamer.player` logger is process-global so leaks between tests will compound. Pattern shape: mirror `_reset_root_override` in `tests/test_paths.py:10-16`.

    Do NOT touch `~/.local/share/musicstreamer/` from any test (Pitfall 6 invariant). Every test uses `monkeypatch.setattr(paths, "_root_override", str(tmp_path))` to redirect.
  </action>
  <acceptance_criteria>
    - B-78A-01: `uv run pytest tests/test_buffer_events_log.py::test_handler_attached_to_player_logger -x` exits 0.
    - B-78A-02: `uv run pytest tests/test_buffer_events_log.py::test_emit_writes_line_to_file -x` exits 0.
    - B-78A-03: `uv run pytest tests/test_buffer_events_log.py::test_rotation_at_1mb tests/test_buffer_events_log.py::test_never_creates_backup_4 -x` exits 0.
    - B-78A-04: `uv run pytest tests/test_buffer_events_log.py::test_install_is_idempotent -x` exits 0.
    - B-78A-05: `uv run pytest tests/test_buffer_events_log.py::test_record_reaches_both_sinks -x` exits 0.
    - `uv run pytest tests/test_buffer_events_log.py -x` exits 0 (entire new file green).
    - `grep -c "propagate" musicstreamer/buffer_log.py` returns 0 OR any occurrence is in a comment (do NOT set `propagate=False` — that would defeat Pitfall-5 stderr parity).
    - `grep -c "0o600" musicstreamer/buffer_log.py` returns 0 (D-03 — no credential-grade chmod).
    - `grep -c "os.makedirs" musicstreamer/buffer_log.py` returns 0 (purity — caller owns directory creation per §S-2).
    - `grep -nE "maxBytes=1_048_576|backupCount=3" musicstreamer/buffer_log.py` returns BOTH parameters on the construction line (D-02 lock).
    - `grep -nE "musicstreamer\\.player" musicstreamer/buffer_log.py` returns the explicit logger name (D-02 — scoped to player only, not `musicstreamer.*`).
    - `uv run pytest tests/test_oauth_log.py -x` still exits 0 (no regression in sibling handler test patterns).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_buffer_events_log.py tests/test_oauth_log.py -x</automated>
  </verify>
  <done>`musicstreamer/buffer_log.py` exports an idempotent `install_buffer_events_handler()` that attaches a single `RotatingFileHandler(maxBytes=1_048_576, backupCount=3, encoding='utf-8')` to the `musicstreamer.player` logger pointing at `paths.buffer_events_log_path()`; 6 tests in `tests/test_buffer_events_log.py` pass; the handler does NOT chmod the file, does NOT create directories, and leaves `propagate=True`.</done>
</task>

<task type="auto">
  <name>Task 3: Wire install_buffer_events_handler() into _run_gui AFTER migration.run_migration()</name>
  <files>musicstreamer/__main__.py</files>
  <read_first>
    - musicstreamer/__main__.py (lines 160-240 — the `_run_gui` body containing the `migration.run_migration()` call at line 176-177, the `QApplication` import block at line 179, AND the `main()` body at line 230-239 containing `basicConfig(level=logging.WARNING)` at line 231 and the per-logger `setLevel(INFO)` at line 235)
    - musicstreamer/buffer_log.py (after Task 2 — confirm `install_buffer_events_handler` is the entry point)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-RESEARCH.md (Pitfall 1, Pitfall 2 (Pitfall 5 regression), Open Question 4 Option B)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md (§ __main__.py section + §S-8 + §S-9)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md (B-78A-13)
    - tests/test_main_window_underrun.py (existing drift-guard `test_main_module_sets_player_logger_to_info` — read its source-grep regex so the modification does not accidentally trip the guard)
  </read_first>
  <action>
    Edit `musicstreamer/__main__.py` to add a single install-call block inside `_run_gui`, immediately AFTER `migration.run_migration()` at line 177 and BEFORE the `from PySide6.QtWidgets import QApplication` at line 179.

    The block consists of:
    1. A lazy import line: `from musicstreamer.buffer_log import install_buffer_events_handler`.
    2. The call: `install_buffer_events_handler()`.
    3. A leading comment block (~3 lines) tagging the change as `Phase 78 / BUG-09 Commit A`, noting (a) why the call lives in `_run_gui` not `main()` (Pitfall 1 — DATA_DIR must exist), (b) that the install is idempotent so re-entry is safe, and (c) that the named-logger attach preserves Pitfall 5 (`basicConfig(WARNING)` global stays untouched).

    DO NOT change:
    - `logging.basicConfig(level=logging.WARNING)` at line 231 — must remain byte-identical (S-8 / Pitfall 5 / B-78A-13 drift-guard).
    - The per-logger `setLevel(logging.INFO)` block at lines 235-239 — these are filesystem-independent and stay where they are.
    - The order of `migration.run_migration()` vs other `_run_gui` operations — the install call inserts AFTER migration; nothing else moves.
    - Any other module's imports or boot-time setup.

    DO NOT add the install call inside `main()` — Pitfall 1 explicitly forbids this (DATA_DIR is created by `migration.run_migration()` which runs INSIDE `_run_gui`, not in `main()`).

    DO NOT touch `tests/test_main_window_underrun.py::test_main_module_sets_player_logger_to_info` — the source-grep drift-guard already covers the Pitfall 5 invariant and MUST remain green after this change.
  </action>
  <acceptance_criteria>
    - B-78A-13: `uv run pytest tests/test_main_window_underrun.py::test_main_module_sets_player_logger_to_info -x` exits 0 (Pitfall 5 drift-guard still green — handler install runs INSIDE `_run_gui` AFTER `migration.run_migration()` returns, NOT in `main()`, and `basicConfig(level=logging.WARNING)` at `__main__.py:222`/`__main__.py:231` is byte-identical).
    - `grep -nE "install_buffer_events_handler" musicstreamer/__main__.py` returns exactly one call line inside `_run_gui` (between the `migration.run_migration()` line and the `from PySide6.QtWidgets import QApplication` import line).
    - `grep -nE "basicConfig\\(level=logging\\.WARNING\\)" musicstreamer/__main__.py` returns the line still present and unchanged.
    - `grep -nE "getLogger\\(['\"]musicstreamer\\.player['\"]\\)\\.setLevel\\(logging\\.INFO\\)" musicstreamer/__main__.py` returns the line still present and unchanged.
    - `grep -nE "basicConfig\\(level=logging\\.INFO\\)" musicstreamer/__main__.py` returns 0 matches (S-8 invariant — `basicConfig` was NOT bumped to INFO).
    - `grep -nE "install_buffer_events_handler" musicstreamer/__main__.py` returns NO line inside `def main(`'s body (Pitfall 1 — install belongs in `_run_gui`, not `main`).
    - `uv run pytest tests/test_main_window_underrun.py -x` exits 0 (entire file still green — sanity check; no regression in Phase 62 integration tests).
    - `uv run pytest tests/ -q` is not requested as a per-task gate; wave-level full suite gate runs after Plan 78-02 and 78-03 also land.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_main_window_underrun.py::test_main_module_sets_player_logger_to_info tests/test_main_window_underrun.py -x</automated>
  </verify>
  <done>`musicstreamer/__main__.py::_run_gui` calls `install_buffer_events_handler()` immediately after `migration.run_migration()` returns. The per-logger `setLevel(INFO)` and the global `basicConfig(WARNING)` are both unchanged. Pitfall 5 drift-guard test green; Pitfall 1 (handler install after migration) is enforced by the call-site location.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stream metadata (station_name, url) → log record | Phase 62 T-62-01 mitigation locked: `%r` quoting at `player.py:927-934` neutralizes log-injection control chars/quotes/newlines. Phase 78 ADDS a file sink but does NOT change the formatter prefix on the record itself — payload is byte-identical to Phase 62. |
| handler install → file system | Single deterministic path resolved via `paths.buffer_events_log_path()`; no user input flows into the path string. |
| filesystem → log file rotation | Stdlib `RotatingFileHandler` serializes writes via `logging.Handler.lock` (`threading.RLock`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78A-DoS-disk | Denial of Service | `buffer_log.RotatingFileHandler` | mitigate | `maxBytes=1_048_576, backupCount=3` caps total disk usage at ~4MB (CONTEXT.md D-02). Asserted by `test_never_creates_backup_4`. |
| T-78A-DoS-fd | Denial of Service | `install_buffer_events_handler` | mitigate | Idempotent install (RESEARCH Pitfall 7) — second call returns early when an existing `RotatingFileHandler` with the same `baseFilename` is found. Asserted by `test_install_is_idempotent`. |
| T-78A-Pitfall5 | Tampering (regression) | `__main__.py` logging config | mitigate | Source-grep drift-guard `test_main_window_underrun.py::test_main_module_sets_player_logger_to_info` enforces `basicConfig(level=logging.WARNING)` is present AND `getLogger("musicstreamer.player").setLevel(logging.INFO)` is present. Plan touches `_run_gui` only; never `basicConfig`. |
| T-78A-Pitfall1 | Information Disclosure / DoS | `install_buffer_events_handler` boot order | mitigate | Install call lands inside `_run_gui` AFTER `migration.run_migration()` so DATA_DIR exists; opening the file eagerly cannot raise `FileNotFoundError`. Enforced by code-location grep gate in Task 3 acceptance criteria. |
| T-78A-LogInject | Tampering | Record payload `station_name`/`url` fields | accept | Phase 62 T-62-01 lock at `player.py:927-934` already neutralizes via `%r` formatting; no new untrusted input is added by Phase 78. |
| T-78A-SC | Tampering | Supply chain (no new dependencies) | accept | RESEARCH §Package Legitimacy Audit explicitly states "Commit A installs no external packages. Audit is not required for this commit." Zero new third-party deps. |
</threat_model>

<verification>
Run the full per-task verification commands listed under each task's `<verify>` block, then the Plan-78-01 wave-level command:

```
uv run pytest tests/test_paths.py tests/test_buffer_events_log.py tests/test_main_window_underrun.py tests/test_oauth_log.py -q
```

Expected: 0 failures. Pitfall-5 drift-guard remains green. The new path helper, install function, and call-site change introduce no regressions to Phase 62 integration tests or Phase 999.3 OAuth-log tests.
</verification>

<success_criteria>
- All 5 file-sink behavior IDs from VALIDATION.md (B-78A-01..B-78A-05) covered by passing automated tests.
- Path-helper behavior ID B-78A-06 covered by passing automated tests.
- Pitfall-5 regression-lock B-78A-13 still green.
- `musicstreamer/buffer_log.py` exists and is importable; `install_buffer_events_handler` is its only public symbol.
- `paths.buffer_events_log_path()` returns the expected path AND remains pure (creates no files, no directories).
- `__main__._run_gui` calls the install function exactly once, AFTER `migration.run_migration()` returns.
- `__main__.py:222`/`__main__.py:231` `basicConfig(level=logging.WARNING)` is byte-identical to its pre-phase form.
- No new third-party dependencies added (verified by checking `pyproject.toml` is unchanged in this plan's commit).
</success_criteria>

<output>
Create `.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-01-SUMMARY.md` when done.
</output>
