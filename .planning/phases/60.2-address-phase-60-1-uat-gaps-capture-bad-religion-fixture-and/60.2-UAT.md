# Phase 60.2: Address Phase 60.1 UAT Gaps — UAT Log

**Phase:** 60.2
**Requirement:** GBS-01e (refines)
**Date:** 2026-05-06
**Tester:** Kyle Creasey
**Deployment target:** Linux Wayland, DPR=1.0 (per project memory)

---

## Test-Suite Pre-Gate

Phase gate per VALIDATION.md: full suite must show only the 11 pre-existing baseline failures + 1 C-level abort already documented in `60.1-UAT.md` — ZERO new regressions.

**Phase 60.2's own test suites (must be 100% green):**

```
$ uv run pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py --no-header -q
73 passed, 1 warning in 0.58s
```

73/73 GREEN across the full Phase 60.2 surface:
- 42 in `tests/test_gbs_api.py` (Phase 60.1 baseline + Plan 02 GREEN deltas)
- 31 in `tests/test_gbs_search_dialog.py` (Phase 60.1 baseline + Plan 01 RED tests now ALL GREEN)

**Full project test suite:**

```
$ uv run pytest --no-header --tb=no -q --deselect tests/test_import_dialog_qt.py::test_yt_scan_passes_through
11 failed, 1096 passed, 1 skipped, 1 deselected, 5 warnings in 10.17s

FAILED tests/test_edit_station_dialog.py::test_logo_status_clears_after_3s
FAILED tests/test_media_keys_mpris2.py::test_linux_mpris_backend_constructs
FAILED tests/test_media_keys_mpris2.py::test_linux_mpris_backend_publish_metadata
FAILED tests/test_media_keys_mpris2.py::test_linux_mpris_backend_publish_metadata_none
FAILED tests/test_media_keys_mpris2.py::test_linux_mpris_backend_set_playback_state
FAILED tests/test_media_keys_mpris2.py::test_linux_mpris_backend_slot_play_pause_emits_signal
FAILED tests/test_media_keys_mpris2.py::test_linux_mpris_backend_shutdown_idempotent
FAILED tests/test_media_keys_mpris2.py::test_xesam_title_passthrough_verbatim
FAILED tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode
FAILED tests/test_station_list_panel.py::test_refresh_recent_updates_list
FAILED tests/test_twitch_auth.py::test_play_twitch_sets_plugin_option_when_token_present
```

**Baseline accounting (per `60.1-UAT.md` lines 43-55):** Exactly 11 pre-existing failures + 1 C-level abort that pre-date Phase 60.1 — all 12 reproduce on `c3d9164` (the pre-phase HEAD). Phase 60.2 introduces ZERO new regressions:

| # | Test | Pre-existing (in 60.1-UAT)? |
|---|------|-----------------------------|
| 1 | `test_edit_station_dialog.py::test_logo_status_clears_after_3s` | ✅ baseline (timing-flaky) |
| 2 | `test_media_keys_mpris2.py::test_linux_mpris_backend_constructs` | ✅ baseline |
| 3 | `test_media_keys_mpris2.py::test_linux_mpris_backend_publish_metadata` | ✅ baseline |
| 4 | `test_media_keys_mpris2.py::test_linux_mpris_backend_publish_metadata_none` | ✅ baseline |
| 5 | `test_media_keys_mpris2.py::test_linux_mpris_backend_set_playback_state` | ✅ baseline |
| 6 | `test_media_keys_mpris2.py::test_linux_mpris_backend_slot_play_pause_emits_signal` | ✅ baseline |
| 7 | `test_media_keys_mpris2.py::test_linux_mpris_backend_shutdown_idempotent` | ✅ baseline |
| 8 | `test_media_keys_mpris2.py::test_xesam_title_passthrough_verbatim` | ✅ baseline |
| 9 | `test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode` | ✅ baseline |
| 10 | `test_station_list_panel.py::test_refresh_recent_updates_list` | ✅ baseline |
| 11 | `test_twitch_auth.py::test_play_twitch_sets_plugin_option_when_token_present` | ✅ baseline |
| (deselected) | `test_import_dialog_qt.py::test_yt_scan_passes_through` | ✅ baseline (C-level abort) |

**SC4-style verification commands:**

```
$ uv run pytest tests/test_gbs_search_dialog.py -k "drill or back_button or pre_drill or section_header or empty_album or clear_table_clears_spans or album_drill_is_flat" --no-header
13 passed, 18 deselected, 1 warning in 0.26s

$ uv run pytest tests/test_gbs_api.py -k "artist_page or album_page or fetch_artist or fetch_album or response_shape" --no-header
12 passed, 30 deselected, 1 warning in 0.17s

$ uv run pytest tests/test_gbs_search_dialog.py::test_no_self_capturing_lambdas_in_dialog --no-header
1 passed, 1 warning in 0.10s
```

Status: [x] PASS  [ ] FAIL — Phase 60.2 introduces zero new regressions; all phase-scope tests GREEN.

---

## ROADMAP Success Criteria

### SC1: Bad-religion zero-panel response addressed (per TRIAGE outcome)

From CONTEXT.md D-04: "Phase 60.2 commits to triage + ship whatever fix the triage warrants."

TRIAGE outcome: **D-06 (client-side parser fix)** — confirmed via `60.2-01-TRIAGE.md` §"Decision". H2 (singular vs plural discriminator-text edge case) was CONFIRMED via direct parser invocation across all 5 captured fixtures; H1/H3/H4 were REFUTED. Plan 02 shipped a 2-line fix at `gbs_api.py:542-548` (`startswith("artists")` → `startswith("artist")` and same for albums) — `bad religion` should now return 1 artist + 1 album panel.

Test steps (live):
1. Launch the app: `uv run python -m musicstreamer`
2. Open Search GBS.FM dialog (hamburger → Search GBS.FM…)
3. Enter the query: `bad religion`
4. Press Enter; observe the search results.
5. **Per D-06 (parser fix shipped in Plan 02):** confirm the response NOW shows Artist:/Album: panels populated with at least one Bad Religion entry each — singular labels (`Artist:` / `Album:`) since each panel has one hit. Prior to Plan 02, this query showed zero artist + zero album panels (the originally-reported defect from 60.1-UAT.md SC1).
6. Click the `Bad Religion` artist link → confirm drill-down view loads with the artist's catalog.
7. Capture observed behavior in Notes.

Result: [X] PASS  [ ] FAIL  [ ] N/A (would only apply if TRIAGE had routed D-05; not the case here)

Notes: _________________________________________

### SC2: Album section-headers in artist drill-down

From CONTEXT.md D-01..D-03: "Render album grouping in the artist drill-down via section-header rows in the existing QTableView. Section-header is f'{album} ({n} songs)'."

Test steps (live):
1. Continue from SC1 (or fresh search).
2. Search for a multi-album artist: `testament` (single-word; matches the captured `artist_4803.html` fixture — 14 albums).
3. Click the Testament entry in the Artist: panel.
4. Confirm: drill-down view loads. Verify visually:
   - Bold album-name section-header rows appear between groups (e.g., `Brotherhood of the Snake (8 songs)`, `The New Order (10 songs)`).
   - Headers span the entire table width (4 columns merged into one).
   - Headers have NO Add! button.
   - Clicking a header does nothing (non-selectable).
   - Clicking an Add! button on a song row still adds that song to the playlist (toast confirms).
5. Click Back; confirm search results restore (no stale span on the search-results table — Pitfall 1+9 mitigation).
6. Test the empty-album D-11 path (defensive — Testament fixture has zero pre-first-albumTitle song rows in practice, so this may not manifest visually).

Result: [X] PASS  [ ] FAIL

Notes: _________________________________________

### SC3: Single-word click path no regression (Phase 60.1 SC3 baseline)

From Phase 60.1 ROADMAP: "The single-word click path that already works in Phase 60-11 continues to work — no regression on the simple case."

Test steps (live):
1. Search a single-word query: `test` (matches the Phase 60-11 baseline fixture).
2. In the Artist: panel, click `Testament` (or any single-word artist).
3. Confirm: drill-down view loads — `Viewing artist: Testament` breadcrumb; rows show Testament's catalog.
4. **NEW Phase 60.2 check:** verify album section-header rows now group the songs (e.g., `Brotherhood of the Snake (8 songs)`, `The New Order (10 songs)`) — this is the Phase 60.2 enhancement on top of the 60.1 baseline.
5. Click Back; confirm search results restore (page label `Page 1 of N`, prev/next pagination buttons restored, no stale spans on the search-results table).
6. Click any album in the Album: panel; confirm album drill-down works AND stays flat (no section headers — Pitfall 6).

Result: [X] PASS  [ ] FAIL

Notes: _________________________________________

### SC4: Existing tests updated; new tests cover new contract

From ROADMAP: "Existing tests are either updated to match the new contract or marked superseded; new tests cover the new contract."

Verification commands (output captured in Test-Suite Pre-Gate above):
- `uv run pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py --no-header -q` → `73 passed` ✓
- `uv run pytest tests/test_gbs_search_dialog.py -k "drill or back_button or pre_drill or section_header or empty_album or clear_table_clears_spans or album_drill_is_flat" --no-header` → `13 passed` ✓
- `uv run pytest tests/test_gbs_api.py -k "artist_page or album_page or fetch_artist or fetch_album or response_shape" --no-header` → `12 passed` ✓
- `uv run pytest tests/test_gbs_search_dialog.py::test_no_self_capturing_lambdas_in_dialog --no-header` → `1 passed` ✓

The 9 NEW tests authored in Plan 01 (3 parser + 6 dialog including 1 parametrize over 5 fixtures) all turn GREEN after Plan 02's parser tier (`11a8cdf`, `7875522`) and Plan 03's dialog tier (`2552e1c`, `71615c5`) landed. Phase 60.1's baseline tests remain GREEN (zero regression).

Result: [X] PASS  [ ] FAIL

Notes: _________________________________________

---

## Additional Manual Validations (UI-SPEC contract — Phase 60.1 still binding + Phase 60.2 deltas)

### Phase 60.2 UI-SPEC Layout Deltas (NEW)

- [X] Album section-header rows visible between groups in artist drill (D-01) — bold album-name rows separate the per-album song lists
- [X] Header text format is `"{album} ({n} songs)"` (D-03) — e.g., `"Brotherhood of the Snake (8 songs)"`
- [X] Header rows are visually bold but not selectable/editable (D-02) — clicking a header does nothing; cursor doesn't change
- [X] Headers span all 4 columns (one merged cell across the table width)
- [X] Empty-album group renders without a header (D-11) — defensive; may not manifest in Testament fixture
- [X] Add! buttons appear ONLY on song rows, NOT on section-header rows (Pitfall 3)
- [X] Add! click on song under a section-header submits the CORRECT song (verify by toast text matching the song clicked, not a neighbor — Pitfall 3 mitigation)
- [X] No `setStyleSheet` or design-system intrusion (FLAG-01 from Phase 60.1 UI-SPEC) — section-header styling uses bold QFont + Qt.ItemFlag.ItemIsEnabled only

### Phase 60.1 invariants (regression spot-check)

- [X] Phase 60.1 Layout Deltas 1-5 still PASS (drill chrome shows back button + breadcrumb; search chrome restores on Back)
- [X] Phase 60.1 Pitfalls 8/9/10 still PASS (snapshot single-shot; Back disabled during fetch; new search abandons drill)
- [X] Pitfall 1+9: drill artist A → click Back → search again → first row of new search has NO stale span (visual confirmation — `_clear_table` now calls `clearSpans()` BEFORE `removeRows`)
- [X] Pitfall 6: drill into an album (not artist) → flat song list, NO section-headers (album drill stays flat)
- [X] Plain-text invariant T-40-04 still holds: section-header text is plain UTF-8 via QStandardItem default (no Pango/HTML markup)

---

## Sign-Off

- [X] All 4 ROADMAP success criteria met (or N/A documented per TRIAGE)
- [X] Full test suite green for Phase 60.2's own scope (73/73 in test_gbs_api.py + test_gbs_search_dialog.py); pre-existing baseline failures elsewhere documented as out-of-scope
- [X] Phase 60.1 UI-SPEC + Pitfalls still PASS
- [X] Phase 60.2 UI-SPEC Layout Deltas (album section-headers) verified visually

**UAT signed off:** 5/6/2026 by Kyle Creasey

**Outcome:** [X] PASS — Phase 60.2 ready for /gsd-complete-phase
             [ ] FAIL — open Phase 60.3 to address gaps

---

## Deviations / Open Issues

_Record any pre-existing baseline failures, any deferred items, any post-fix follow-ups discovered during UAT._

_Pre-existing baseline failures (out of scope for this phase):_ 11 unit-test failures + 1 C-level abort across `test_media_keys_mpris2`, `test_station_list_panel`, `test_twitch_auth`, `test_edit_station_dialog`, `test_import_dialog_qt`. All reproducible on the pre-phase HEAD; not introduced by Phase 60.2. Tracked separately for a future cleanup phase. Same baseline as `60.1-UAT.md`.
