---
phase: 72.1
plan: 03
type: execute
wave: 2
depends_on: [72.1-02]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: true
requirements: [LAYOUT-02]
tags: [layout, pyside6, ui-qt, extension-hook, station-change, tdd-green, layout-02]
must_haves:
  truths:
    - "When _populate_stream_picker fires for a single-stream station, stream_combo is re-homed to row 1 (self.controls) regardless of prior row 2 state — closes the multi→single mid-narrow transition (D-Discretion / sample f)."
    - "_populate_stream_picker invalidates the threshold cache (self._cached_row1_min = None) so any future row 1 widget addition self-corrects on the next reflow tick (Pitfall 5 partial-mitigation; per-phase scope per CONTEXT D-02)."
    - "Phase 72 compact-mode toggle still works while picker is in row 2 (Ctrl+B toggles independent of row 1/2 reparent state) — locks the cross-phase invariant (sample g)."
    - "Picker's QPalette color roles are byte-identical before and after a wide→narrow→wide round-trip — Qt palette inheritance survives setParent without setStyleSheet pollution (sample h)."
    - "Tests 6, 7, 8 from Plan 01's scaffold now pass (sample points f, g, h)."
    - "Tests 9 and 10 (the two negative-assertion tests) remain GREEN — Plan 03 introduces no setSetting writes and no lambdas."
    - "All 10 tests in tests/test_phase72_1_stream_picker_reflow.py pass."
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "Extension hook inside _populate_stream_picker (cache invalidation + multi→single re-home) — completes LAYOUT-02 production code"
      contains: "self._cached_row1_min = None"
      contains_also: "self._move_stream_picker_to(self.controls)"
  key_links:
    - from: "_populate_stream_picker (post-Plan-03)"
      to: "_move_stream_picker_to + _set_picker_size_policy"
      via: "direct method call on multi→single transition"
      pattern: "if not self.stream_combo.isVisible"
---

<objective>
Wave 2 GREEN closure for Phase 72.1 / LAYOUT-02. Lands the final extension hook inside `_populate_stream_picker` and turns the last three Plan 01 sample points GREEN (f: multi→single mid-narrow re-home; g: compact-mode independence; h: palette survives reparent).

Single edit point: `_populate_stream_picker` (now_playing_panel.py:1114-1135). Two lines added at the END of the method (after line 1135's `setVisible(len(streams) > 1)`):
1. `self._cached_row1_min = None` — invalidate the threshold cache populated by Plan 02's `_picker_row_min_width()`. The widget count in row 1 hasn't changed (still 9), so this is mostly defensive (Pitfall 5 in 72.1-RESEARCH; future Phase may add a row-1 widget without invalidating, but this phase's scope is the picker only). Cheap belt-and-suspenders.
2. Conditional re-home: `if not self.stream_combo.isVisible():` → `self._move_stream_picker_to(self.controls)` + `self._set_picker_size_policy(expanding=False)`. This is the CONTEXT Claude's Discretion item: when station change makes the picker single-stream while it was in row 2, force it back to row 1 (its default home) so layout state stays consistent with the visibility state.

When the picker IS visible (multi-stream station), we defer to the next resizeEvent tick — resize is sampled often enough that the picker reaches the correct row within one frame, and `_populate_stream_picker` is not the right hook to read `panel.width()` (it runs on station bind, not on resize).

Plan 03 does NOT touch any of the five new methods added by Plan 02 (resizeEvent / _reflow_stream_picker_row / _picker_row_min_width / _move_stream_picker_to / _set_picker_size_policy). It only modifies the existing `_populate_stream_picker` and adds two NEW tests that exercise behaviors not covered by Plan 01's scaffold:
- sample (g) compact-mode independence — uses MainWindow integration fixtures
- sample (h) palette preservation — uses QPalette snapshotting

Sample (f) is closed by the extension hook + the existing Plan 01 test scaffold (test_multi_to_single_mid_narrow_rehomes_picker, which already calls `panel.bind_station(single_station)` and asserts re-home).

Purpose: Closes LAYOUT-02 production code. All 10 tests GREEN. Phase 72 + Phase 72.1 regression suites green. Ready for Plan 04 manual UAT.

Output: now_playing_panel.py with a single 4-line addition to `_populate_stream_picker`. All 10 LAYOUT-02 sample tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-CONTEXT.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-01-PLAN.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-02-PLAN.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-LEARNINGS.md

<interfaces>
<!-- Post-Plan-02 state of now_playing_panel.py. Confirm before editing. -->

NowPlayingPanel attributes (post-Plan-02):
- self.controls: QHBoxLayout         # row 1, instance attribute since Plan 02 Task 1
- self._controls_row2: QHBoxLayout   # row 2, added by Plan 02 Task 2
- self._cached_row1_min: Optional[int] = None  # threshold cache, populated by _picker_row_min_width()
- self._picker_row1_index: int       # picker's original row-1 position, captured at __init__
- self.stream_combo: QComboBox       # the widget that reparents

NowPlayingPanel methods (post-Plan-02):
- def resizeEvent(self, event) — Qt override, calls _reflow_stream_picker_row()
- def _picker_row_min_width(self) -> int — lazy-cached threshold compute
- def _move_stream_picker_to(self, target_layout: QHBoxLayout) -> None — idempotent reparent
- def _set_picker_size_policy(self, expanding: bool) -> None — toggle QSizePolicy
- def _reflow_stream_picker_row(self) -> None — main predicate, called from resizeEvent + (post-Plan-03) _populate_stream_picker

Method to extend in Plan 03 (now_playing_panel.py:1114-1135 — DO NOT change the existing body, only APPEND two new lines at the end):
```
def _populate_stream_picker(self, station) -> None:
    """Populate stream picker combo for the bound station (D-19, D-20)."""
    streams = self._repo.list_streams(station.id)
    self._streams = streams
    self.stream_combo.blockSignals(True)
    self.stream_combo.clear()
    for s in streams:
        # ... existing label build (lines 1121-1132) ...
        self.stream_combo.addItem(label, userData=s.id)
    self.stream_combo.blockSignals(False)
    self.stream_combo.setVisible(len(streams) > 1)
    # <-- Plan 03 inserts cache-invalidation + multi→single re-home HERE
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _populate_stream_picker with cache invalidation + multi→single re-home</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <behavior>
    - Test 6 (test_multi_to_single_mid_narrow_rehomes_picker): GREEN — after binding a multi-stream station, resizing narrow (picker in row 2), then binding a single-stream station, the picker is back in self.controls AND stream_combo.isVisible() is False.
    - Cache invalidation: after _populate_stream_picker fires, self._cached_row1_min is None (next _picker_row_min_width() call recomputes from current row 1 contents).
    - Idempotency: calling _populate_stream_picker twice with the same single-stream station does NOT cause the picker to oscillate between rows or break visibility state.
    - Plan 02's tests 1-5 + Plan 01's tests 9-10 STAY GREEN — Plan 03 must not regress them.
    - No new lambdas; no new set_setting writes.
  </behavior>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (lines 1114-1158 — post-Plan-02 state of _populate_stream_picker through _sync_stream_picker; lines 200-260 — Plan 02's new methods cluster for cross-reference)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-CONTEXT.md (§Claude's Discretion — "Handling the multi-stream → single-stream transition mid-narrow" — the canonical spec for the re-home)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (§Code Examples lines 539-566 — the verbatim recipe for the extension hook; §Pattern E — single-source-of-truth visibility invariant; §Pitfall 5 — cache invalidation hook)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md (§Pattern E — _populate_stream_picker line 1135 is the canonical visibility writer; reparent helper only owns layout, not visibility)
  </read_first>
  <action>
Append two scoped additions to the END of `_populate_stream_picker` in `musicstreamer/ui_qt/now_playing_panel.py`. Current end of method is line 1135 (`self.stream_combo.setVisible(len(streams) > 1)`). Insert IMMEDIATELY AFTER that line, BEFORE the next method definition (`def _on_stream_selected` at line 1137).

The inserted block (5 lines: 2 code + 2 comments + 1 conditional body):

  ```
  # Phase 72.1 / LAYOUT-02: invalidate threshold cache (defensive — row-1 widget
  # count is unchanged by this phase's scope, but future widget additions would
  # otherwise be stale; Pitfall 5). _reflow_stream_picker_row will recompute
  # on the next resizeEvent or the immediate call below.
  self._cached_row1_min = None
  # Phase 72.1 / LAYOUT-02 / CONTEXT D-Discretion: when station change made
  # the picker hidden (single-stream), force re-home to row 1 (its default
  # home) so layout state stays consistent with visibility state. Multi-stream
  # case defers to the next resizeEvent tick (resize is sampled often enough).
  if not self.stream_combo.isVisible():
      self._move_stream_picker_to(self.controls)
      self._set_picker_size_policy(expanding=False)
  ```

Do NOT modify any line above 1135 inside `_populate_stream_picker` — the existing label build, blockSignals dance, clear, addItem loop, and setVisible write are all untouched.

Do NOT call `self._reflow_stream_picker_row()` from the multi-stream branch — `_populate_stream_picker` does not know the panel's current width with reliable timing (Pitfall 1 in 72.1-RESEARCH — sizeHint may be stale on first paint). The next resizeEvent dispatches naturally; Wayland coalesces so the picker reaches its correct row within one frame.

Do NOT call `self.stream_combo.setVisible(...)` from the new block — Pattern E invariant: line 1135 (still the same line, just above the new block) is the SINGLE writer of stream_combo.setVisible. The new block only owns layout membership + size policy.

Do NOT call `self._repo.set_setting(...)` from the new block — D-09 inheritance; test 9 locks.

Do NOT introduce a lambda — all calls are bound methods on `self`. Test 10 locks.

This is the FINAL edit to production code for LAYOUT-02. Task 2 adds two new tests (sample g + sample h) to the existing test file; no further production code changes follow in Plan 03.
  </action>
  <verify>
    <automated>pytest tests/test_phase72_1_stream_picker_reflow.py::test_multi_to_single_mid_narrow_rehomes_picker -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 72.1 / LAYOUT-02: invalidate threshold cache" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "if not self.stream_combo.isVisible():" musicstreamer/ui_qt/now_playing_panel.py` >= 1 (one inside _populate_stream_picker; Plan 02's _reflow_stream_picker_row also has one — total may be 2)
    - The new `self._cached_row1_min = None` assignment is inside `_populate_stream_picker` (not in a helper). Verify by extracting the method body: `python3 -c "import ast, sys; tree=ast.parse(open('musicstreamer/ui_qt/now_playing_panel.py').read()); [print('OK') for cls in ast.walk(tree) if isinstance(cls, ast.ClassDef) and cls.name=='NowPlayingPanel' for m in cls.body if isinstance(m, ast.FunctionDef) and m.name=='_populate_stream_picker' for stmt in ast.walk(m) if isinstance(stmt, ast.Assign) and any(isinstance(t, ast.Attribute) and t.attr=='_cached_row1_min' for t in stmt.targets)]"` prints "OK" at least once.
    - Body re-home call inside _populate_stream_picker: grep verifies `self._move_stream_picker_to(self.controls)` appears at least twice in the file (once in Plan 02's `_reflow_stream_picker_row`, once new in `_populate_stream_picker`). `grep -c "self._move_stream_picker_to(self.controls)" musicstreamer/ui_qt/now_playing_panel.py` >= 2
    - No lambdas in the modified `_populate_stream_picker`. Method-body grep: `python3 -c "import ast; tree=ast.parse(open('musicstreamer/ui_qt/now_playing_panel.py').read()); print('FAIL' if any(isinstance(n, ast.Lambda) for cls in ast.walk(tree) if isinstance(cls, ast.ClassDef) and cls.name=='NowPlayingPanel' for m in cls.body if isinstance(m, ast.FunctionDef) and m.name=='_populate_stream_picker' for n in ast.walk(m)) else 'OK')"` prints "OK"
    - No set_setting writes in the modified `_populate_stream_picker`: same AST walk approach, search for `Call` nodes whose `func.attr == 'set_setting'` — should be 0.
    - Test 6 GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py::test_multi_to_single_mid_narrow_rehomes_picker -x` exit 0
    - Plan 02 tests stay GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py::test_wide_multi_stream_picker_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_multi_stream_picker_in_row_2 tests/test_phase72_1_stream_picker_reflow.py::test_wide_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_signal_survives_round_trip -x` exit 0
    - Negative-1 + Negative-2 stay GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py::test_no_repo_set_setting_for_wrap_state tests/test_phase72_1_stream_picker_reflow.py::test_no_lambda_in_reflow_wiring -x` exit 0
    - Phase 72 regression suite green: `pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py tests/test_phase72_peek_overlay.py -x` exit 0
  </acceptance_criteria>
  <done>
    `_populate_stream_picker` re-homes the picker to row 1 when a station change hides it (single-stream). Cache invalidates on every populate. Sample point (f) GREEN; Plan 02's sample points + negatives stay GREEN; Phase 72 regression green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify sample (g) compact-mode independence and sample (h) palette preservation pass against Plan 01's test scaffold</name>
  <files>tests/test_phase72_1_stream_picker_reflow.py</files>
  <read_first>
    - tests/test_phase72_1_stream_picker_reflow.py (post-Plan-01 state — confirm tests 7 and 8 already exist as test_compact_mode_and_picker_wrap_independent and test_palette_survives_reparent; if Plan 01 deferred them or stubbed them incorrectly, fix in this task; otherwise verify-only)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-VALIDATION.md (Per-Task Verification Map — test 7 = sample g, test 8 = sample h; both already in Plan 01's scaffold)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md (§Color (Delta) — "The picker uses its existing palette in both row 1 and row 2 positions. The reparent operation does NOT alter QSizePolicy color, palette, stylesheet, or any visual property other than horizontal stretch." — palette test asserts this.)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (Secondary Sources line — Qt 6 QWidget setParent palette propagation; §Pattern 2 Critical note on focus + palette preservation)
    - tests/test_phase72_integration.py (lines 75-90 — MainWindow fixture pattern with FakePlayer + FakeRepo + _make_station + qtbot.waitExposed; mirror for compact-mode integration test)
    - tests/test_main_window_integration.py (lines 31-90 — MainWindow FakePlayer + FakeRepo; line 197 — _make_station; reuse via import as test_phase72_integration.py does)
  </read_first>
  <action>
This task is **verification-first**. Plan 01 already created `test_compact_mode_and_picker_wrap_independent` and `test_palette_survives_reparent` as part of the TDD-RED scaffold. After Plan 02 + Plan 03 Task 1 land, these two tests SHOULD pass with no test-file changes — they were written to lock the intended behavior, and that behavior is now implemented.

**Step 1 — Run sample (g) and sample (h):**

  ```
  pytest tests/test_phase72_1_stream_picker_reflow.py::test_compact_mode_and_picker_wrap_independent -x
  pytest tests/test_phase72_1_stream_picker_reflow.py::test_palette_survives_reparent -x
  ```

Expected: both pass after Plan 03 Task 1 lands.

**Step 2 — If either test fails, diagnose and fix THE TEST (not the production code).** Production code is locked by Plans 01-02-03's other 8 tests; this task is the test-author's responsibility to make sample (g)/(h) executable on the as-implemented system.

Acceptable test-only fixes (no production code change):
- **Sample (g) failure modes:**
  - The MainWindow fixture in Plan 01 may have under-stubbed FakeRepo (e.g., missing `list_streams` for the multi-stream station; missing `list_sibling_links`; missing `list_recently_played`). Compare to `tests/test_main_window_integration.py:87-194` (the full FakeRepo) and add what's missing as a per-test override OR import the integration FakeRepo directly via `from tests.test_main_window_integration import FakeRepo as MWFakeRepo, FakePlayer as MWFakePlayer, _make_station as mw_make_station` (mirrors `tests/test_phase72_compact_toggle.py:41`).
  - The MainWindow's compact toggle uses `Ctrl+B` shortcut + `window._compact_shortcut.activated.emit()` per `tests/test_phase72_compact_toggle.py:114`. Use the same activation idiom OR `window.now_playing.compact_mode_toggle_btn.click()`. Either is acceptable per UI-SPEC.
  - When constructing the multi-stream station inside the MainWindow path, you may need to override `FakeRepo._streams_by_station` or extend the integration FakeRepo's `list_streams` to return a non-empty list. The cleanest path is to subclass MWFakeRepo locally in the test or use `monkeypatch.setattr(type(fake_repo), 'list_streams', lambda self, sid: [...])`.
- **Sample (h) failure modes:**
  - `QPalette.color(role)` comparison: ensure you compare `QColor.name()` or `QColor.rgba()` rather than direct `QColor` identity (Qt may return different QColor instances even for the same RGB). Adjust assertion to use `.name()` or `.getRgb()`.
  - `panel.stream_combo.palette()` may return a propagated palette that gets re-resolved on reparent — assert role-by-role rather than full palette equality.

**Step 3 — If neither test fails, no test changes needed.** Document under deviations in Plan 03's SUMMARY: "sample (g) and sample (h) passed without test edits."

**Step 4 — Run the full 10-test suite to confirm the LAYOUT-02 closure:**

  ```
  pytest tests/test_phase72_1_stream_picker_reflow.py -x
  ```

All 10 tests MUST be GREEN. If any test other than (g)/(h) regresses, that's a Plan 02 / Plan 03 Task 1 bug — fix in the appropriate prior task, not here.

**Step 5 — Run the Phase 72 + adjacent regression suite:**

  ```
  pytest tests/test_phase72_1_stream_picker_reflow.py tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py tests/test_phase72_peek_overlay.py tests/test_now_playing_panel.py tests/test_main_window_integration.py -x
  ```

Must exit 0 (or unchanged from pre-plan deferred-items.md baseline). Pre-existing failures (FakePlayer drift, MPRIS DBus, AA quality combo) are accepted per Phase 77 deferred-items.md docket.
  </action>
  <verify>
    <automated>pytest tests/test_phase72_1_stream_picker_reflow.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Sample (g) GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py::test_compact_mode_and_picker_wrap_independent -x` exit 0
    - Sample (h) GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py::test_palette_survives_reparent -x` exit 0
    - ALL 10 tests GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py -x` exit 0 (full file passes)
    - Test-count unchanged from Plan 01: `grep -c "^def test_" tests/test_phase72_1_stream_picker_reflow.py` == 10 (Plan 03 must not add or remove tests; only optionally tune (g) and (h) fixture wiring)
    - Test names unchanged from Plan 01: the same 10 names listed in 72.1-VALIDATION.md Per-Task Verification Map are still present.
    - Phase 72 regression green: `pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py tests/test_phase72_peek_overlay.py -x` exit 0
    - Adjacent suite: `pytest tests/test_now_playing_panel.py tests/test_main_window_integration.py -x` exit unchanged from pre-plan baseline (pre-existing failures accepted per Phase 77 deferred-items.md)
    - If any test in the file was modified (fixture tuning per Step 2), document in SUMMARY: which test, what was changed, why no production code change was needed.
  </acceptance_criteria>
  <done>
    All 10 sample/negative tests in tests/test_phase72_1_stream_picker_reflow.py pass. LAYOUT-02 production code is complete. Phase 72.1 ready for Plan 04 manual UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Plan 03 adds 5 lines inside `_populate_stream_picker` calling already-mitigated methods from Plan 02. No new event handlers, no new I/O, no new user-input parsing. |

## STRIDE Threat Register

Per 72.1-RESEARCH §Security Domain. ASVS V2-V13 NOT APPLICABLE.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.1-01 | Tampering | Untrusted stream metadata rendered as rich text in QComboBox items | accept | NOT INTRODUCED by Plan 03 — the existing `_populate_stream_picker` body lines 1118-1135 are untouched; Plan 03 appends only post-setVisible logic that operates on layout membership. Still QComboBox plain-text default. |
| T-72.1-02 | Tampering | Reflow helper re-entry during _populate_stream_picker → _move_stream_picker_to | mitigate | `_move_stream_picker_to` (added by Plan 02) is idempotent — same-state target is a no-op. Calling from inside `_populate_stream_picker` then again from the next `resizeEvent` is harmless (second call sees target state and exits). |
| T-72.1-03 | DoS | Resize-event spam when bind_station fires (which calls _populate_stream_picker which now triggers reparent on single-stream) | accept | Reparent cost ~1µs (single removeWidget + single insertWidget + single setSizePolicy). bind_station fires once per user station selection, not on every frame. Not a DoS surface. |

No `high` severity findings — block-on-high gate is trivially satisfied. Plan 03 introduces zero new threat surface.
</threat_model>

<verification>
**Plan 03 success means:**
1. All 10 LAYOUT-02 sample/negative tests GREEN: `pytest tests/test_phase72_1_stream_picker_reflow.py -x` exit 0.
2. Phase 72 regression green: `pytest tests/test_phase72_*.py -x` exit 0.
3. Adjacent suite green: `pytest tests/test_now_playing_panel.py tests/test_main_window_integration.py -x` exit unchanged from pre-plan baseline.
4. `python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"` exits 0.

**Sampling rate:** Per-task acceptance criteria are the verification gates. After Task 1, run sample (f); after Task 2, run all 10 sample/negative tests.
</verification>

<success_criteria>
- [x] `_populate_stream_picker` body gains exactly 5 lines (cache invalidation + multi→single re-home block) appended after line 1135.
- [x] Cache invalidation: `self._cached_row1_min = None` inside `_populate_stream_picker` (verified by AST walk).
- [x] Multi→single re-home: `if not self.stream_combo.isVisible():` followed by `_move_stream_picker_to(self.controls)` + `_set_picker_size_policy(expanding=False)`.
- [x] Pattern E invariant preserved: only line 1135 writes `stream_combo.setVisible(...)`; Plan 03 does NOT add another writer.
- [x] D-09 invariant preserved: no `set_setting` calls added.
- [x] QA-05 invariant preserved: no lambdas added.
- [x] Sample (f) GREEN: test_multi_to_single_mid_narrow_rehomes_picker passes.
- [x] Sample (g) GREEN: test_compact_mode_and_picker_wrap_independent passes (test-only fixture tuning permitted per Task 2 Step 2).
- [x] Sample (h) GREEN: test_palette_survives_reparent passes (test-only fixture tuning permitted per Task 2 Step 2).
- [x] All 10 tests in tests/test_phase72_1_stream_picker_reflow.py GREEN.
- [x] Phase 72 + adjacent regression green.
- [x] LAYOUT-02 production code is complete — ready for Plan 04 manual UAT.
</success_criteria>

<output>
Create `.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-03-SUMMARY.md` when done. Note: which 3 sample points turned GREEN (f, g, h); whether sample (g) or (h) required test-only fixture tuning per Task 2 Step 2 (and what was tuned); confirmation that all 10 LAYOUT-02 tests pass.
</output>
</content>
</invoke>