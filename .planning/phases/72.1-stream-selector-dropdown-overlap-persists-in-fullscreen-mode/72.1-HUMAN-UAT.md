---
status: complete
phase: 72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode
source: [72.1-VERIFICATION.md]
started: 2026-05-16T23:55:00Z
updated: 2026-05-17T00:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Bug reproduction at 560–700 px window width with a multi-stream station selected
expected: Drag a multi-stream station's window to ~600 px right-pane width on Wayland GNOME Shell at DPR=1.0; the stream picker should move to a second row below the transport controls with no overlap. Resize the window wider until the picker returns to row 1 alongside the other controls.
result: pass

### 2. Phase 72 regression: compact toggle still works while picker is on row 2
expected: With a multi-stream station selected at narrow width (picker on row 2), pressing Ctrl+B (or clicking the compact-toggle button) should hide/show the left column. The picker should stay on row 2 regardless of compact state. Toggling compact off should restore the left column without disturbing the picker's row-2 position.
result: pass
note: User observed "This still brings the stream selector back to row 1 (though I don't feel like this is an issue)." Actual behavior is consistent with spec D-01 (width-responsive trigger, not mode-coupled): toggling compact off widens the right pane, so the picker correctly reflows to row 1. The test expectation overstated the contract — the invariant is "width drives wrap," not "picker pins to row 2 once moved."

### 3. Phase 72 regression: hover-peek overlay still works while picker is on row 2
expected: With a multi-stream station selected at narrow width in compact mode (picker on row 2, station list hidden), hovering the left edge of the window should trigger the peek overlay after ~280 ms dwell. The overlay should open without disturbing the picker's row-2 position underneath. Moving the cursor out of the overlay should close it.
result: pass
### 4. No visible flicker at the threshold boundary when dragging splitter through the wrap point
expected: Drag the splitter slowly through the threshold width (~944 px panel width based on threshold compute). The picker should transition between row 1 and row 2 without visible flicker or oscillation. If flicker IS observed, file a deferred-item note for a hysteresis follow-up (per CONTEXT.md `<deferred>` — hysteresis was deliberately deferred until real flicker is observed).
result: pass

### 5. REQUIREMENTS.md status flip (orchestrator-owned, not a real human-verify item)
expected: LAYOUT-02 row in `.planning/REQUIREMENTS.md` should be marked `[x]` Complete with completion date; Traceability table row for LAYOUT-02 should read "Complete". This is handled automatically by `update_roadmap` when phase.complete is invoked after this UAT is approved.
result: skipped
reason: Orchestrator-owned; LAYOUT-02 flips automatically on phase.complete invocation. Not a human-verify item.

## Summary

total: 5
passed: 4
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps

[none]
