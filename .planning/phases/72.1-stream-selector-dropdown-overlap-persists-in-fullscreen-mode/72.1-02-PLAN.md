---
phase: 72.1
plan: 02
type: execute
wave: 1
depends_on: [72.1-01]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: true
requirements: [LAYOUT-02]
tags: [layout, pyside6, ui-qt, resize-event, tdd-green, reflow, layout-02]
must_haves:
  truths:
    - "A multi-stream station bound to NowPlayingPanel with panel width >= threshold has stream_combo in row 1 (controls QHBoxLayout)."
    - "A multi-stream station bound to NowPlayingPanel with panel width < threshold has stream_combo in row 2 (controls_row2 QHBoxLayout), with horizontal QSizePolicy == Expanding."
    - "A single-stream station bound to NowPlayingPanel keeps stream_combo in row 1 at ALL panel widths (stream_combo.isVisible() is False; D-02)."
    - "Resizing the panel through the threshold in BOTH directions causes the picker to reparent without losing its currentIndex, item model, or currentIndexChanged signal connections."
    - "NowPlayingPanel.resizeEvent always calls super().resizeEvent(event) first (Qt convention; Pitfall 9)."
    - "_reflow_stream_picker_row is idempotent — calling it when state is already correct is a no-op (Pattern 2 early-return)."
    - "controls and _controls_row2 are both instance attributes (self.controls, self._controls_row2) so tests + future plans can address them by name."
    - "Tests 1, 2, 3, 4, 5, 9 from Plan 01's scaffold now pass (sample points a, b, c, d, e + negative-1 by accident)."
    - "Plan 01's existing tests 6, 7, 8, 10 may still fail — those are Plan 03's deliverable."
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "controls_row2 QHBoxLayout + resizeEvent override + _reflow_stream_picker_row + _picker_row_min_width cache + _move_stream_picker_to helper + _set_picker_size_policy helper + QSizePolicy import"
      contains: "def resizeEvent"
      contains_also: "def _reflow_stream_picker_row"
      contains_also_2: "self._controls_row2 = QHBoxLayout()"
  key_links:
    - from: "NowPlayingPanel.resizeEvent"
      to: "NowPlayingPanel._reflow_stream_picker_row"
      via: "direct method call"
      pattern: "self._reflow_stream_picker_row\\(\\)"
    - from: "NowPlayingPanel._reflow_stream_picker_row"
      to: "NowPlayingPanel._picker_row_min_width"
      via: "threshold cache read"
      pattern: "self._picker_row_min_width\\(\\)"
    - from: "NowPlayingPanel._reflow_stream_picker_row"
      to: "self.stream_combo + self.controls + self._controls_row2"
      via: "removeWidget + insertWidget / addWidget + setSizePolicy"
      pattern: "removeWidget\\(self.stream_combo\\)"
---

<objective>
Wave 1 GREEN tier for Phase 72.1 / LAYOUT-02. Lands the core reflow mechanism inside `musicstreamer/ui_qt/now_playing_panel.py`. Single-file edit; no MainWindow / Repo / Player changes per 72.1-RESEARCH §Architectural Responsibility Map.

Plan scope (all six edits in one atomic plan because they form an indivisible mechanism):
1. **Add QSizePolicy to the existing import block** (line 35-48; QSizePolicy is not currently imported in now_playing_panel.py — verified by grep).
2. **Promote local `controls` to instance attribute** — change `controls = QHBoxLayout()` at line 440 to `self.controls = QHBoxLayout()` and update all 9 in-method references (controls.setSpacing, controls.addWidget × N, controls.addStretch, center.addLayout(controls)) accordingly. Pure rename; behavior unchanged.
3. **Add `self._controls_row2 = QHBoxLayout()` + `self._controls_row2.setSpacing(8)` + `center.addLayout(self._controls_row2)`** at line 549 (immediately after the existing `center.addLayout(self.controls)` at line 548, BEFORE `self._stats_widget = self._build_stats_widget()` at line 553). Mirrors row 1's construction style + spacing exactly.
4. **Initialize threshold cache slot** — `self._cached_row1_min: Optional[int] = None` set near top of `__init__` (early state initialization, before any layout construction). Used by `_picker_row_min_width()` and invalidated by `_populate_stream_picker` (Plan 03's deliverable). Add the slot here so Plan 03 only needs to add the invalidation hook.
5. **Snapshot picker's original row-1 index** — after `self.controls.addWidget(self.stream_combo)` at line 488, capture `self._picker_row1_index = self.controls.indexOf(self.stream_combo)` so the row-1-return path uses `insertWidget(<original_index>, ...)` not `addWidget(...)` per PATTERNS Pattern B / Pitfall 6 ("addWidget would append at index 1 and silently swap visual order"). Hardcoding the index is forbidden — must be captured at construction time.
6. **Add the four new methods** in this exact order, placed adjacent to existing layout/sizing helpers in the panel (search for `_populate_stream_picker` at line 1114; place the new methods immediately above it for spatial cohesion):
   - `def _picker_row_min_width(self) -> int` — lazy-cached threshold compute per 72.1-RESEARCH Pattern 4 (with the `+16` safety buffer per Open Question 4).
   - `def _move_stream_picker_to(self, target_layout: QHBoxLayout) -> None` — idempotent reparent helper. Row-1-return uses `self.controls.insertWidget(self._picker_row1_index, self.stream_combo)`; row-2 path uses `self._controls_row2.addWidget(self.stream_combo)`.
   - `def _set_picker_size_policy(self, expanding: bool) -> None` — toggle between `QSizePolicy.Expanding`/`QSizePolicy.Preferred` horizontal × `QSizePolicy.Preferred` vertical.
   - `def _reflow_stream_picker_row(self) -> None` — the main reflow predicate (early-return on hidden/zero-size, compute threshold, dispatch to `_move_stream_picker_to` + `_set_picker_size_policy`).
   - `def resizeEvent(self, event)` Qt override — calls `super().resizeEvent(event)` then `self._reflow_stream_picker_row()`. Place this method BEFORE the four helpers (Qt overrides conventionally live near top of widget class — search for an analogous spot or place immediately after `__init__` ends; if no override exists yet, place at the natural location for Qt-API methods).

Purpose: Closes sample points (a), (b), (c), (d), (e) from 72.1-VALIDATION. Plan 03 closes (f), (g), (h) + the two negative-assertion tests via an extension hook in `_populate_stream_picker`.

Output: All Plan 01 tests except (f), (g), (h) and the QA-05 lint pass. Phase 72 regression suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-CONTEXT.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-01-PLAN.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-LEARNINGS.md

<interfaces>
<!-- Key existing identifiers in now_playing_panel.py the executor must respect. -->

Line 35-48 (existing import block — append QSizePolicy alphabetically between QProgressBar and QPushButton):
  QComboBox, QFormLayout, QHBoxLayout, QLabel, QListWidget, QListWidgetItem, QProgressBar, QPushButton, QSlider, QToolButton, QVBoxLayout, QWidget

Line 215: class NowPlayingPanel(QWidget)
Line 283: def __init__(self, player, repo, parent: Optional[QWidget] = None) -> None
Line 440: controls = QHBoxLayout()       # CHANGE: self.controls = QHBoxLayout()
Line 441: controls.setSpacing(8)         # CHANGE: self.controls.setSpacing(8)
Line 469: controls.addWidget(self.stop_btn)    # CHANGE: self.controls.addWidget(...)
Line 481: controls.addWidget(self.edit_btn)    # CHANGE: self.controls.addWidget(...)
Line 484-488: stream_combo construction:
  self.stream_combo = QComboBox(self)
  self.stream_combo.setMinimumWidth(140)
  self.stream_combo.setVisible(False)
  self.stream_combo.currentIndexChanged.connect(self._on_stream_selected)
  controls.addWidget(self.stream_combo)    # CHANGE: self.controls.addWidget(...)
Line 500: controls.addWidget(self.star_btn)
Line 519: controls.addWidget(self.eq_toggle_btn)
Line 525: controls.addWidget(self.volume_slider)
Line 545: controls.addWidget(self.compact_mode_toggle_btn)
Line 547: controls.addStretch(1)
Line 548: center.addLayout(controls)            # CHANGE: center.addLayout(self.controls)
Line 553: self._stats_widget = self._build_stats_widget()  # INSERT new row-2 lines BEFORE this

Line 1114: def _populate_stream_picker(self, station) -> None  # Plan 03 extends this; Plan 02 does NOT touch
Line 1135: self.stream_combo.setVisible(len(streams) > 1)  # the single-source-of-truth visibility writer (Pattern E)
Line 1137: def _on_stream_selected(self, index: int) -> None  # untouched by reparent (signals survive)
Line 1149: def _sync_stream_picker(self, active_stream) -> None  # untouched

Plan 01 RED-state contracts the new methods must satisfy:
- panel.controls  (instance attribute, type QHBoxLayout, after Task 1)
- panel._controls_row2  (instance attribute, type QHBoxLayout, after Task 2)
- panel.stream_combo  (unchanged from line 484; just moves between layouts)
- panel.resizeEvent fires _reflow_stream_picker_row on every resize
- panel.stream_combo.sizePolicy().horizontalPolicy() ∈ {QSizePolicy.Preferred, QSizePolicy.Expanding}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote `controls` to instance attribute + add QSizePolicy import + initialize threshold cache + snapshot picker index</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (lines 26-50 — import block; lines 280-300 — __init__ start; lines 440-548 — controls construction; lines 1114-1135 — _populate_stream_picker for grep reference)
    - musicstreamer/ui_qt/favorites_view.py (line 27 + line 183 — QSizePolicy import example + use site per PATTERNS Pattern C)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md (§Pattern A — insertion neighborhood; §Pattern B — Pitfall 6 about insertWidget vs addWidget)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (§Pattern 4 — threshold cache shape with `_cached_row1_min`)
  </read_first>
  <action>
Apply four scoped edits to `musicstreamer/ui_qt/now_playing_panel.py`. Each edit is independent and verifiable.

**Edit A — Import QSizePolicy.** In the `from PySide6.QtWidgets import (` block at lines 35-48, insert `QSizePolicy,` alphabetically (between `QProgressBar,` at line 42 and `QPushButton,` at line 43). Do not reorder other imports.

**Edit B — Snake-case-rename local `controls` to `self.controls`.** In `__init__`, change line 440 from `controls = QHBoxLayout()` to `self.controls = QHBoxLayout()`. Then update every `controls.<call>` reference inside `__init__` to `self.controls.<call>`. There are exactly TEN in-body call-sites to update (verified by `grep -nE "^\s*controls\." musicstreamer/ui_qt/now_playing_panel.py` at lines 441, 455, 469, 481, 488, 500, 519, 525, 545, 547) plus the `center.addLayout(controls)` at line 548 → `center.addLayout(self.controls)`. **The grep enumeration in the acceptance criterion is authoritative — do NOT trust the line numbers alone; verify the post-edit `wc -l` returns 0 before moving on.** Do NOT modify any line that does not contain the bare token `controls` (e.g., a comment containing the word "controls" stays unchanged). Use grep `grep -n "controls" musicstreamer/ui_qt/now_playing_panel.py | grep -v "controls_row2\|control row\|control_row\|controls panel\|control panel\|now-playing controls\|now_playing.*controls\|peek_overlay\|_compact"` to enumerate the lines.

**Edit C — Initialize threshold cache slot.** Inside `NowPlayingPanel.__init__`, at the top of the method body (after `super().__init__(parent)` and the existing instance-attribute initialization block — locate by reading lines 283-340 and find the cluster of `self.* = None` / `self.* = []` / `self._station = None` style assignments), add the line:

  `self._cached_row1_min: Optional[int] = None  # Phase 72.1 / LAYOUT-02: lazy cache for _picker_row_min_width(); invalidated by _populate_stream_picker (Plan 03 hook).`

Place it adjacent to other `self._<state> = None` lines. Plan 03 adds the invalidation hook inside `_populate_stream_picker`; Plan 02 only needs the slot to exist with `None`.

**Edit D — Snapshot picker's row-1 index.** Immediately after `self.controls.addWidget(self.stream_combo)` at line 488 (post-Edit-B), insert:

  `self._picker_row1_index: int = self.controls.indexOf(self.stream_combo)  # Phase 72.1 / LAYOUT-02: pinned original index for insertWidget on row-1-return path (Pattern B / Pitfall 6 — addWidget would silently swap visual order with sibling widgets added after this point).`

This must be captured AFTER `addWidget` (otherwise `indexOf` returns -1) and BEFORE any subsequent row-1 widget is added (currently star_btn at line 500; capturing at line 489 satisfies this).

Do not touch `_populate_stream_picker`, `_on_stream_selected`, `_sync_stream_picker`, `_controls_row2` (Task 2's deliverable), `resizeEvent`, or any new method (Task 3's deliverables).
  </action>
  <verify>
    <automated>grep -c "self.controls = QHBoxLayout()" musicstreamer/ui_qt/now_playing_panel.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "self.controls = QHBoxLayout()" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "^    QSizePolicy,$" musicstreamer/ui_qt/now_playing_panel.py` == 1 (alphabetical position in import block)
    - `grep -c "self._cached_row1_min" musicstreamer/ui_qt/now_playing_panel.py` >= 1 (init slot present; Plan 03 adds more)
    - `grep -c "self._picker_row1_index" musicstreamer/ui_qt/now_playing_panel.py` >= 1 (snapshot present; will also be referenced by Task 3)
    - `grep -c "self.controls.addWidget(self.stream_combo)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "center.addLayout(self.controls)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -nE "^(\s*)controls\.\w" musicstreamer/ui_qt/now_playing_panel.py | grep -v "^.*self\.controls\." | wc -l` == 0 (no surviving local-name `controls.<call>` references; everything is `self.controls.<call>`)
    - `grep -c "^(\s*)controls = QHBoxLayout()" musicstreamer/ui_qt/now_playing_panel.py` == 0 (no surviving bare local)
    - Phase 72 regression: `pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py -x` exit 0 (the rename must not break Phase 72's panel/integration tests — they used `panel.controls` only through layout-walking, which Edit B preserves)
    - Python imports clean: `python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"` exits 0
  </acceptance_criteria>
  <done>
    NowPlayingPanel exposes `self.controls`, `self._cached_row1_min` (None), `self._picker_row1_index` (int), and the `QSizePolicy` import lands cleanly. No behavior change; pure scaffolding for Tasks 2-3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Construct `controls_row2` QHBoxLayout under `center`</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (lines 547-554 — the insertion neighborhood post-Task-1; controls.addStretch + center.addLayout + comment block + _stats_widget construction)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md (§Pattern A — exact 3-line construction recipe)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md (§Row 2 Visual Contract table — spacing 8, no setContentsMargins, no setVisible)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (§Pattern 1 lines 205-227 — Qt auto-collapses zero-item QHBoxLayout to zero height; no explicit hide needed)
  </read_first>
  <action>
In `NowPlayingPanel.__init__`, IMMEDIATELY AFTER the line `center.addLayout(self.controls)` (which was line 548 pre-Task-1, now still line 548 post-Edit-B), insert three lines BEFORE the existing line `# Phase 47.1 stats widget (D-07: last center-column item; D-08: always constructed)` (line 550) and BEFORE `self._stats_widget = self._build_stats_widget()` (line 553).

The inserted block (4 lines including a leading comment):

  ```
  # Phase 72.1 / LAYOUT-02: row 2 for the stream picker. Empty by default;
  # _reflow_stream_picker_row reparents stream_combo here when panel is narrow
  # AND multi-stream (D-01, D-02, D-05). Qt collapses a zero-item QHBoxLayout
  # to zero height automatically — no explicit hide/show needed (Pattern 1).
  self._controls_row2 = QHBoxLayout()
  self._controls_row2.setSpacing(8)  # match self.controls.setSpacing(8) at line 441
  center.addLayout(self._controls_row2)
  ```

CRITICAL ordering: this block MUST sit BETWEEN `center.addLayout(self.controls)` and `self._stats_widget = self._build_stats_widget()`. If a future contributor inserts between row 2 and stats, the visual order breaks. Add a comment marker on the LAST line of the inserted block: `# END Phase 72.1 row-2 insertion` is unnecessary — the comment header makes the intent obvious. No marker needed.

Do NOT call `setContentsMargins` on row 2 — inherit `center`'s margins per UI-SPEC §Spacing Scale (Delta) row 4.
Do NOT call `setVisible(True)` / `setVisible(False)` on row 2 — Qt auto-collapses zero-item layouts (UI-SPEC §Row 2 Visual Contract last row).
Do NOT wrap row 2 in a container QWidget — adds widget class for no gain (72.1-RESEARCH §Anti-Patterns to Avoid).
Do NOT call `addStretch` on row 2 — the picker uses Expanding sizePolicy to fill (Task 3); a stretch would defeat the stretch policy.
  </action>
  <verify>
    <automated>grep -c "self._controls_row2 = QHBoxLayout()" musicstreamer/ui_qt/now_playing_panel.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "self._controls_row2 = QHBoxLayout()" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "self._controls_row2.setSpacing(8)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "center.addLayout(self._controls_row2)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - Line ordering verified — the line containing `center.addLayout(self._controls_row2)` MUST appear AFTER the line containing `center.addLayout(self.controls)` and BEFORE the line containing `self._stats_widget = self._build_stats_widget()`. Verify via: `python3 -c "import re; s=open('musicstreamer/ui_qt/now_playing_panel.py').read(); a=s.find('center.addLayout(self.controls)'); b=s.find('center.addLayout(self._controls_row2)'); c=s.find('self._stats_widget = self._build_stats_widget()'); assert a < b < c, f'Ordering wrong: a={a}, b={b}, c={c}'"` exits 0
    - No `setContentsMargins` call on `_controls_row2`: `grep -c "_controls_row2.setContentsMargins" musicstreamer/ui_qt/now_playing_panel.py` == 0
    - No `setVisible` call on `_controls_row2`: `grep -c "_controls_row2.setVisible" musicstreamer/ui_qt/now_playing_panel.py` == 0
    - No `addStretch` on `_controls_row2`: `grep -c "_controls_row2.addStretch" musicstreamer/ui_qt/now_playing_panel.py` == 0
    - `python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"` exits 0 (no syntax / import error)
    - Phase 72 regression: `pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py -x` exit 0 (empty `_controls_row2` should NOT change any existing Phase 72 behavior — visual stats widget still adjacent to controls per UI-SPEC)
  </acceptance_criteria>
  <done>
    NowPlayingPanel.__init__ constructs both row 1 (existing `self.controls`) and row 2 (new `self._controls_row2`) under `center`. Row 2 starts empty, collapses to zero height. Plan 03 will populate row 2 when the picker moves there.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add resizeEvent override + the four reflow helpers</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <behavior>
    - Test 1 (test_wide_multi_stream_picker_in_row_1): GREEN — at panel.resize(1200, 800), stream_combo is in self.controls.
    - Test 2 (test_narrow_multi_stream_picker_in_row_2): GREEN — at panel.resize(560, 800), stream_combo is in self._controls_row2 with QSizePolicy.Expanding horizontal.
    - Test 3 (test_wide_single_stream_picker_hidden_in_row_1): GREEN — at panel.resize(1200, 800), single-stream picker stays in self.controls, isVisible() is False.
    - Test 4 (test_narrow_single_stream_picker_hidden_in_row_1): GREEN — at panel.resize(560, 800), single-stream picker stays in self.controls (D-02: single-stream never reparents).
    - Test 5 (test_signal_survives_round_trip): GREEN — currentIndexChanged.connect survives wide→narrow→wide round-trip; currentIndex preserved; itemData preserved.
    - Tests 6, 7, 8 (sample points f, g, h): may still FAIL — Plan 03 closes them via _populate_stream_picker extension hook + compact-mode integration + palette test.
    - Test 9 (test_no_repo_set_setting_for_wrap_state): GREEN — no production code added in this plan writes wrap-state settings (Plan 02 reaffirms the invariant).
    - Test 10 (test_no_lambda_in_reflow_wiring): GREEN — Plan 02's new methods use bound methods only (no lambdas in resizeEvent / _reflow_stream_picker_row / _move_stream_picker_to / _set_picker_size_policy / _picker_row_min_width).
  </behavior>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (post-Task-1+Task-2 state — see new lines from Tasks 1/2 to confirm insertion point; lines 1110-1160 — _populate_stream_picker, _on_stream_selected, _sync_stream_picker cluster for adjacent placement)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (§Pattern 4 lines 305-358 — _picker_row_min_width recipe with +16 safety buffer; §Pattern 5 lines 360-391 — resizeEvent recipe with `super().resizeEvent(event)` first + `# noqa: N802` marker; §Code Examples lines 570-598 — _reflow_stream_picker_row recipe; §Pitfall 3 lines 456-463 — `hasattr(self, 'stream_combo')` guard for early-resize-during-__init__; §Pitfall 9 lines 523-531 — super().resizeEvent must be first; §Open Question 5 lines 700-703 — `if panel_width <= 0: return` early-out)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md (§Pattern B Pitfall 6 — row-1-return uses insertWidget(<index>, ...); §Pattern C — setSizePolicy(Expanding/Preferred, Preferred); §Pattern D — first resizeEvent in codebase)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-CONTEXT.md (§D-01 width-only trigger; §D-02 single-stream stays one-row; §D-05 reparent on QResizeEvent; §D-06 instant no animation; §D-07 Expanding on row 2; §D-08 Preferred on row 1 + 140px floor preserved)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-LEARNINGS.md (Lesson "pytest-qt synthetic events can mask receiver-identity bugs" — Plan 02's resizeEvent must respond to REAL panel.resize() calls, not just synthetic QResizeEvent injection; Surprise "Qt creates a placeholder slot when a QSplitter child is reparented OUT" — Pitfall 4 in 72.1-RESEARCH explains why the helper must be idempotent)
  </read_first>
  <action>
Add FIVE new methods to `NowPlayingPanel` class in `musicstreamer/ui_qt/now_playing_panel.py`. Place them adjacent to `_populate_stream_picker` (currently line 1114) — the natural neighborhood for stream-picker-related state management. Recommended order: `resizeEvent` first (Qt override, top of cluster), then the four helpers in dependency order (`_picker_row_min_width`, `_move_stream_picker_to`, `_set_picker_size_policy`, `_reflow_stream_picker_row`).

**Method 1 — `resizeEvent` Qt override.** Place IMMEDIATELY BEFORE `def _populate_stream_picker(self, station)` at line 1114. Signature: `def resizeEvent(self, event):  # noqa: N802` (the `# noqa: N802` is mandatory — Qt method names are camelCase, project lint expects snake_case; this is the documented Qt-override exception per 72.1-RESEARCH §Pattern 5).

Body — three lines + docstring:
- Docstring: 1-2 lines citing Phase 72.1 / LAYOUT-02 + D-05 + the load-bearing `super().resizeEvent(event)` first per Pitfall 9.
- Line 1: `super().resizeEvent(event)` — MANDATORY first, per Qt convention + Pitfall 9. The base implementation invalidates layout and updates internal geometry.
- Line 2: `self._reflow_stream_picker_row()` — direct call (NOT signal/slot, NOT `QTimer.singleShot(0, ...)`).

Do NOT call `self.resize()` or `self.setGeometry()` anywhere in resizeEvent — infinite recursion per Qt docs (RESEARCH §Pattern 5 Critical).

**Method 2 — `_picker_row_min_width(self) -> int`.** Place after `_sync_stream_picker` (line 1149) or wherever the cluster reads cleanest. Lazy cache + threshold compute per 72.1-RESEARCH §Pattern 4. Behavior:
- If `self._cached_row1_min is not None`, return it (cache hit).
- Else walk `self.controls.itemAt(i)` for i in range(`self.controls.count()`); for each item, extract widget via `.widget()`; skip when widget is None (stretch item); for each widget, accumulate `max(w.minimumWidth(), w.minimumSizeHint().width())`.
- Add inter-widget spacing: `spacing × max(0, widget_count - 1)` where `spacing = self.controls.spacing()` (the call returns 8 because Task 1 preserved `self.controls.setSpacing(8)`; Pitfall 2 guard not needed because the explicit setSpacing is present).
- Add layout margins: `margins.left() + margins.right()` where `margins = self.controls.contentsMargins()`.
- Add `+16` safety buffer per 72.1-RESEARCH Open Question 4 (splitter handle + rounding).
- Store result in `self._cached_row1_min` and return.

Method docstring: cite Phase 72.1 / LAYOUT-02 + Pattern 4 + Open Question 4 (+16 buffer); note that cache is invalidated by Plan 03's _populate_stream_picker extension.

**Method 3 — `_move_stream_picker_to(self, target_layout: QHBoxLayout) -> None`.** Place after `_picker_row_min_width`. Idempotent reparent helper per 72.1-RESEARCH §Pattern 2 + PATTERNS Pattern B Pitfall 6. Behavior:
- Compute `current_in_row1 = self.controls.indexOf(self.stream_combo) >= 0`
- Compute `current_in_row2 = self._controls_row2.indexOf(self.stream_combo) >= 0`
- If `target_layout is self.controls` and `current_in_row1`: return (idempotent no-op).
- If `target_layout is self._controls_row2` and `current_in_row2`: return (idempotent no-op).
- Remove from whichever layout currently owns it (call `removeWidget(self.stream_combo)` on the owning layout).
- Add to target — CRITICAL: target_layout-specific:
  - If `target_layout is self.controls`: `self.controls.insertWidget(self._picker_row1_index, self.stream_combo)` — uses the index captured at __init__ time per Pattern B Pitfall 6.
  - If `target_layout is self._controls_row2`: `self._controls_row2.addWidget(self.stream_combo)` — row 2 only ever has the picker, so addWidget is safe (no sibling ordering to preserve).

Method docstring: cite Pattern 2 (signals + currentIndex + model survive setParent per Qt) + Pitfall 6 (insertWidget(index, ...) on row-1-return path).

**Method 4 — `_set_picker_size_policy(self, expanding: bool) -> None`.** Place after `_move_stream_picker_to`. Per 72.1-RESEARCH §Pattern 3 + CONTEXT D-07 / D-08. Behavior:
- If `expanding`: `self.stream_combo.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Preferred)` (D-07).
- Else: `self.stream_combo.setSizePolicy(QSizePolicy.Preferred, QSizePolicy.Preferred)` (D-08 — explicit reset; cannot rely on Qt-class default per Assumption A1).
- Do NOT call `setMinimumWidth(0)` or `setMinimumWidth(140)` — the 140px floor from line 485 persists across setSizePolicy changes (RESEARCH §Pattern 3 Critical).
- Do NOT call `updateGeometry()` — setSizePolicy invokes it internally (RESEARCH §Pattern 3 Critical).

**Method 5 — `_reflow_stream_picker_row(self) -> None`.** Place after `_set_picker_size_policy`. The main predicate per 72.1-RESEARCH §Code Examples lines 573-598. Behavior:
- Defensive guards (Pitfall 3, Pitfall 5 from RESEARCH, plus Open Question 5 zero-size guard):
  - `if not hasattr(self, 'stream_combo'):` → return  (Pitfall 3 — handles spurious resize during __init__ before stream_combo exists)
  - `if not hasattr(self, '_controls_row2'):` → return  (defensive — same scenario)
  - `panel_width = self.width()`
  - `if panel_width <= 0:` → return  (Open Question 5 — Qt's spurious zero-size resize during teardown / first-show)
- D-02 single-stream branch (matches 72.1-RESEARCH §Code Examples lines 583-588):
  - `if not self.stream_combo.isVisible():` → `self._move_stream_picker_to(self.controls)` + `self._set_picker_size_policy(expanding=False)` + return
- Width-driven branch:
  - `threshold = self._picker_row_min_width()`
  - `should_wrap = panel_width < threshold`
  - If `should_wrap`: `self._move_stream_picker_to(self._controls_row2)` + `self._set_picker_size_policy(expanding=True)`
  - Else: `self._move_stream_picker_to(self.controls)` + `self._set_picker_size_policy(expanding=False)`

Method docstring: cite D-01 (width-responsive), D-02 (single-stream stays row 1), D-05 (reparent on resize), D-06 (instant), Pattern 2 (idempotent — _move_stream_picker_to handles same-state no-op).

**No lambdas anywhere.** No signal/slot wiring is added by Plan 02 — `resizeEvent` is a Qt override, not a connected slot. Test 10 (`test_no_lambda_in_reflow_wiring`) inspects source for `lambda` on lines containing the five new method names; Plan 02's implementation MUST be bound-method-pure (QA-05).

**No `setVisible(...)` calls on `self.stream_combo` from any of the five new methods.** Per Pattern E + 72.1-RESEARCH §Anti-Patterns / Shared Patterns, `_populate_stream_picker` (line 1135) is the single source of truth for picker visibility. The reflow helper inspects `isVisible()` but never writes to it.

**No `repo.set_setting` calls in any of the five new methods.** Per 72.1-RESEARCH §Pattern (No SQLite persistence) + D-09 inheritance. Test 9 locks this.
  </action>
  <verify>
    <automated>pytest tests/test_phase72_1_stream_picker_reflow.py::test_wide_multi_stream_picker_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_multi_stream_picker_in_row_2 tests/test_phase72_1_stream_picker_reflow.py::test_wide_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_signal_survives_round_trip tests/test_phase72_1_stream_picker_reflow.py::test_no_repo_set_setting_for_wrap_state tests/test_phase72_1_stream_picker_reflow.py::test_no_lambda_in_reflow_wiring -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^    def resizeEvent\(self, event\)" musicstreamer/ui_qt/now_playing_panel.py | wc -l` == 1 (exactly one resizeEvent override)
    - `grep -c "# noqa: N802" musicstreamer/ui_qt/now_playing_panel.py` >= 1 (Qt-override marker present on resizeEvent line)
    - `grep -c "super().resizeEvent(event)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "def _reflow_stream_picker_row(self)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "def _picker_row_min_width(self)" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "def _move_stream_picker_to(self" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "def _set_picker_size_policy(self" musicstreamer/ui_qt/now_playing_panel.py` == 1
    - `grep -c "self.controls.insertWidget(self._picker_row1_index" musicstreamer/ui_qt/now_playing_panel.py` == 1 (row-1-return path uses insertWidget per Pattern B Pitfall 6)
    - `grep -c "self._controls_row2.addWidget(self.stream_combo)" musicstreamer/ui_qt/now_playing_panel.py` == 1 (row-2 path uses addWidget)
    - `grep -c "QSizePolicy.Expanding" musicstreamer/ui_qt/now_playing_panel.py` >= 1 (D-07)
    - `grep -c "QSizePolicy.Preferred" musicstreamer/ui_qt/now_playing_panel.py` >= 1 (D-08; also potentially in other Phase 72.1 code)
    - Negative-grep: no lambdas in any new method. Run:
      `grep -nE "(resizeEvent|_reflow_stream_picker_row|_picker_row_min_width|_move_stream_picker_to|_set_picker_size_policy).*lambda" musicstreamer/ui_qt/now_playing_panel.py | wc -l` == 0
    - Negative-grep: no setSetting writes in any new method body. Inspect the body of each new method (any line between `def <method>` and the next `def` or class end) for `set_setting(` — should be 0.
    - Negative-grep: no setVisible writes on stream_combo from new methods. Inspect each new method body for `stream_combo.setVisible(` — should be 0 (only the existing line 1135 may write visibility).
    - Plan 01 sample tests 1-5 + negatives pass: `pytest tests/test_phase72_1_stream_picker_reflow.py::test_wide_multi_stream_picker_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_multi_stream_picker_in_row_2 tests/test_phase72_1_stream_picker_reflow.py::test_wide_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_signal_survives_round_trip tests/test_phase72_1_stream_picker_reflow.py::test_no_repo_set_setting_for_wrap_state tests/test_phase72_1_stream_picker_reflow.py::test_no_lambda_in_reflow_wiring -x` exits 0
    - Phase 72 regression suite green: `pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py tests/test_phase72_peek_overlay.py -x` exits 0
    - Adjacent suite green: `pytest tests/test_now_playing_panel.py tests/test_main_window_integration.py -x` exits 0 (or unchanged from pre-plan deferred-items.md baseline if pre-existing failures are noted)
  </acceptance_criteria>
  <done>
    NowPlayingPanel has a working `resizeEvent` override + four reflow helpers. Multi-stream panels reparent the picker on width transitions; single-stream panels never reparent. Signals + currentIndex + itemData survive. Plan 01 sample points (a)-(e) + negative-1 + negative-2 pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Plan 02 is layout-only inside a single widget class. No new input parsing, no network I/O, no DB writes, no IPC, no event-loop reentry points beyond Qt's own resize dispatcher. |

## STRIDE Threat Register

Per 72.1-RESEARCH §Security Domain (cited as source — terse threat model approved by phase planning context). ASVS V2-V13 NOT APPLICABLE.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.1-01 | Tampering | Untrusted stream metadata rendered as rich text in QComboBox items | accept | NOT INTRODUCED by Plan 02 — `_populate_stream_picker` (line 1133's `addItem(label, userData=s.id)`) is untouched. Plan 02 only moves the widget between layouts; rendering is still QComboBox plain-text default. Monitored. |
| T-72.1-02 | Tampering | Reflow helper re-entry during resize event handler causing stale state | mitigate | `_move_stream_picker_to` is idempotent (early-return when widget is already in target layout per Pattern 2). `_reflow_stream_picker_row` guards `panel_width <= 0` (Open Question 5 — covers Qt's spurious zero-size resize during teardown). `hasattr(self, 'stream_combo')` + `hasattr(self, '_controls_row2')` early-returns cover Pitfall 3 (resize-during-__init__). |
| T-72.1-03 | DoS | Resize-event spam from user dragging splitter floods UI thread | mitigate | Threshold compute is lazy-cached in `self._cached_row1_min` (initialized None in Task 1; populated on first call to `_picker_row_min_width`). Subsequent resize ticks read the cache + a single arithmetic comparison + an idempotent reparent (no-op when state already correct). Per-call cost ~1µs per RESEARCH §Pattern 4. Wayland coalesces resize events (project_deployment_target memory). |

No `high` severity findings — block-on-high gate is trivially satisfied.
</threat_model>

<verification>
**Plan 02 success means:**
1. Sample points (a), (b), (c), (d), (e) GREEN:
   `pytest tests/test_phase72_1_stream_picker_reflow.py::test_wide_multi_stream_picker_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_multi_stream_picker_in_row_2 tests/test_phase72_1_stream_picker_reflow.py::test_wide_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_narrow_single_stream_picker_hidden_in_row_1 tests/test_phase72_1_stream_picker_reflow.py::test_signal_survives_round_trip -x` exit 0
2. Negative-1 + Negative-2 GREEN (carry forward from Plan 01):
   `pytest tests/test_phase72_1_stream_picker_reflow.py::test_no_repo_set_setting_for_wrap_state tests/test_phase72_1_stream_picker_reflow.py::test_no_lambda_in_reflow_wiring -x` exit 0
3. Sample points (f), (g), (h) may still RED — Plan 03 closes them.
4. Phase 72 + adjacent regression green: `pytest tests/test_phase72_*.py tests/test_now_playing_panel.py tests/test_main_window_integration.py -x` exit 0 (or unchanged from pre-plan baseline if pre-existing failures are noted in deferred-items.md).
5. `python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"` exits 0.

**Sampling rate:** After every task commit, run `pytest tests/test_phase72_1_stream_picker_reflow.py -x` to track GREEN-count progression. After all three Tasks land, run the Phase 72 regression suite.
</verification>

<success_criteria>
- [x] QSizePolicy imported (`grep -c "^    QSizePolicy,$" now_playing_panel.py` == 1)
- [x] `self.controls` exposed (`grep -c "self.controls = QHBoxLayout()" now_playing_panel.py` == 1)
- [x] `self._controls_row2` constructed adjacent to row 1 (verified by line-order check)
- [x] `self._picker_row1_index` snapshot captured at construction time
- [x] `self._cached_row1_min` slot initialized to None
- [x] `resizeEvent` override exists with `super().resizeEvent(event)` as first body line
- [x] Five new methods present: resizeEvent, _picker_row_min_width, _move_stream_picker_to, _set_picker_size_policy, _reflow_stream_picker_row
- [x] Row-1-return uses `insertWidget(self._picker_row1_index, ...)` (Pattern B Pitfall 6)
- [x] No lambdas in any new method (QA-05; test 10 locks)
- [x] No `setVisible` writes on stream_combo from any new method (Pattern E single-source-of-truth invariant)
- [x] No `set_setting` writes from any new method (D-09 inheritance; test 9 locks)
- [x] Plan 01 sample points (a)-(e) + negative-1 + negative-2 pass
- [x] Phase 72 + adjacent regression green
</success_criteria>

<output>
Create `.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-02-SUMMARY.md` when done. Note in the summary: which 5 sample points + 2 negatives turned GREEN; which 3 sample points remain RED for Plan 03; any deviations from this PLAN body (e.g., method placement details).
</output>
</content>
</invoke>