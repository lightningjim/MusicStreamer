---
phase: 88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g
plan: 02
type: execute
wave: 2
depends_on: ["88.2-01"]
files_modified:
  - musicstreamer/ui_qt/accounts_dialog.py
  - packaging/windows/build.ps1
  - tests/test_accounts_dialog.py
  - tests/test_packaging_oauth_launcher_spec.py
autonomous: true
requirements: [VER-02-J]

must_haves:
  truths:
    # Linux-verifiable NOW (phase closes on these)
    - "On QProcess FailedToStart for the GBS login, the errorOccurred handler resets self._gbs_login_proc = None (WR-03) BEFORE opening any dialog"
    - "On GBS FailedToStart, a visible WARNING is logged and the CookieImportDialog File/Paste fallback is surfaced (D-02)"
    - "The GBS errorOccurred handler checks error == FailedToStart specifically, so a Crashed error does not double-trigger the cookie-import fallback"
    - "On Twitch FailedToStart, the errorOccurred handler resets self._oauth_proc = None, logs, and updates status (no cookie-import fallback for Twitch)"
    - "build.ps1 contains the OAUTH HELPER GUARD invoking --oauth-helper --self-test, failing with documented exit 12, with a SkipOauthGuard bypass switch"
    - "tests/test_packaging_oauth_launcher_spec.py and the new errorOccurred cases in tests/test_accounts_dialog.py pass; full uv run pytest stays green"
  artifacts:
    - path: "musicstreamer/ui_qt/accounts_dialog.py"
      provides: "_on_gbs_login_error + _on_oauth_login_error errorOccurred handlers; module-level logging"
      contains: "errorOccurred"
    - path: "packaging/windows/build.ps1"
      provides: "OAUTH HELPER GUARD block (exit 12) + SkipOauthGuard param + exit-code header entry"
      contains: "exit 12"
    - path: "tests/test_packaging_oauth_launcher_spec.py"
      provides: "D-05 source-text drift-guards for build.ps1 + __main__.py"
      contains: "oauth_helper_entrypoint_unreachable"
  key_links:
    - from: "musicstreamer/ui_qt/accounts_dialog.py _on_gbs_login_error"
      to: "self._on_gbs_import_clicked (CookieImportDialog)"
      via: "errorOccurred FailedToStart → guard reset → fallback"
      pattern: "_on_gbs_import_clicked"
    - from: "packaging/windows/build.ps1 OAUTH HELPER GUARD"
      to: "MusicStreamer.exe --oauth-helper --self-test"
      via: "Invoke-Native + $LASTEXITCODE → exit 12"
      pattern: "--oauth-helper --self-test"
  vm_deferred:
    # NOT phase-blocking — verified in consolidated 88-03 Win11 VM session per D-06/D-07
    - "frozen MusicStreamer.exe --oauth-helper --self-test exits 0 on the Windows build VM; build.ps1 OAUTH HELPER GUARD passes (exit 12 only on regression) — 88-03"
    - "GBS.FM in-app login opens, authenticates, audio plays on the frozen exe (UAT-10) — 88-03"
    - "Twitch + Google login also open from the frozen exe — 88-03"
---

<objective>
Add the launch-failure UX and regression guards on top of the Plan 01 launcher fix:
the GBS login QProcess `errorOccurred` handler (D-02/D-03) that resets the WR-03 re-entrancy
guard, logs visibly, and surfaces the existing cookie-import fallback on FailedToStart; a
Twitch `errorOccurred` handler (reset + log, no fallback); the `build.ps1` D-05 runtime guard
(`--oauth-helper --self-test`, exit 12) with a `SkipOauthGuard` bypass; and the D-05 source-text
drift-guard that locks both `build.ps1` and `__main__.py` in CI.

Purpose: Ensure a failed login start is never a silent dead-end (D-02/D-03), and that the
frozen-exe helper entrypoint can never silently regress out of the build (D-05). Mirrors the
88.1 two-layer guard (Linux static test + build.ps1 runtime smoke with a new exit code).

Output: errorOccurred handlers in accounts_dialog.py, the build.ps1 exit-12 guard block, new
errorOccurred unit cases in test_accounts_dialog.py, and tests/test_packaging_oauth_launcher_spec.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-CONTEXT.md
@.planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-RESEARCH.md
@.planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-PATTERNS.md
@.planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-VALIDATION.md
@.planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

musicstreamer/ui_qt/accounts_dialog.py (confirmed):
  # imports at top (line 19-41): import sys; from PySide6.QtCore import QProcess, Qt;
  #   from musicstreamer import constants, paths
  #   NOTE: there is NO module-level `import logging` yet — add `import logging` + `_log = logging.getLogger(__name__)`.
  def _launch_oauth_subprocess(self):            # Twitch; sets self._oauth_proc, finished.connect(self._on_oauth_finished)
  def _launch_gbs_login_subprocess(self):        # GBS; WR-03 guard `if self._gbs_login_proc is not None: return`; finished.connect(self._on_gbs_login_finished)
  def _on_gbs_import_clicked(self):              # opens CookieImportDialog(File/Paste) for GBS — the D-02 fallback target, already wired
  def _on_oauth_finished(self, exit_code, exit_status):   # line ~483; sets self._oauth_proc = None (capture-then-clear pattern)
  def _on_gbs_login_finished(self, ...):         # line ~539; `proc = self._gbs_login_proc; self._gbs_login_proc = None` (capture-then-clear)
  def _update_status(self):                      # line ~220; restores button/label state
  # QProcess error enum: from PySide6.QtCore import QProcess; QProcess.ProcessError.FailedToStart

packaging/windows/build.ps1 (confirmed):
  # Exit-code header lines 4-9 (extend): ...10=plugin-presence(Phase 69), 11=smtc backend not loaded (Phase 88.1 / WIN-02)
  # param block lines 12-19: [switch]$SkipSmtcGuard = $false  (the bypass-switch template)
  # SMTC SMOKE GUARD block lines 325-337 (the exact template to copy/adapt):
  #   if (-not $SkipSmtcGuard) {
  #     Write-Host "=== SMTC SMOKE GUARD: ... ==="
  #     Invoke-Native { & "..\..\dist\MusicStreamer\MusicStreamer.exe" --check-mediakeys 2>&1 | Out-Host }
  #     if ($LASTEXITCODE -ne 0) { Write-Host "BUILD_FAIL reason=smtc_backend_not_loaded ..." -ForegroundColor Red; exit 11 }
  #     Write-Host "SMTC SMOKE GUARD OK"
  #   } else { Write-Host "SMTC SMOKE GUARD skipped (-SkipSmtcGuard)" }
  # The new oauth guard block goes immediately AFTER the SMTC guard block (after line 337), before the step-5 smoke section.
  # WR-01 discipline: Write-Host (NOT Write-Error) + exit N, inside Invoke-Native + $LASTEXITCODE check.

tests/test_packaging_winrt_spec.py (confirmed analog — entire file): module-scope fixtures
  read build.ps1 / spec text once; assert presence of guard string + "exit 11" + WR-01 ordering.
  _strip_comments() helper for negative-assertion gates (grep-gate hygiene).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add errorOccurred handlers (GBS + Twitch) with WR-03 reset, visible log, cookie-import fallback</name>
  <files>musicstreamer/ui_qt/accounts_dialog.py, tests/test_accounts_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/accounts_dialog.py lines 379-424 (_launch_oauth_subprocess, _launch_gbs_login_subprocess, _on_gbs_import_clicked) and lines 539-546 (_on_gbs_login_finished capture-then-clear ordering — the WR-03 model the handler must mirror)
    - musicstreamer/ui_qt/accounts_dialog.py lines 19-41 (imports — confirm NO `import logging` yet; add it)
    - .planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-RESEARCH.md §"Pattern 3: QProcess errorOccurred" + Pitfall 2 + Pitfall 6 (FailedToStart-specific check; finished NOT emitted on FailedToStart)
    - .planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-PATTERNS.md §accounts_dialog.py "New errorOccurred handler pattern"
    - tests/test_accounts_dialog.py (the existing file the new cases append to — match its qtbot/fixture conventions)
  </read_first>
  <behavior>
    - Test A (GBS FailedToStart resets guard first): construct the dialog, set self._gbs_login_proc to a stub QProcess, invoke _on_gbs_login_error(QProcess.ProcessError.FailedToStart); assert self._gbs_login_proc is None afterward AND that the cookie-import fallback path (_on_gbs_import_clicked / CookieImportDialog) was invoked. Mock/patch _on_gbs_import_clicked to record the call so no real modal opens.
    - Test B (GBS visible log on FailedToStart): assert a WARNING-level log line is emitted for the GBS launch failure (use caplog at WARNING).
    - Test C (Crashed does NOT trigger cookie-import fallback): invoke _on_gbs_login_error(QProcess.ProcessError.Crashed); assert _on_gbs_import_clicked was NOT called (FailedToStart-specific gate, Pitfall 6).
    - Test D (Twitch FailedToStart resets guard, no fallback): set self._oauth_proc to a stub, invoke _on_oauth_login_error(QProcess.ProcessError.FailedToStart); assert self._oauth_proc is None and _update_status ran, and no CookieImportDialog fallback is opened.
  </behavior>
  <action>
    Add `import logging` to accounts_dialog.py module imports and `_log = logging.getLogger(__name__)` at module scope. Add two handler methods. `_on_gbs_login_error(self, error: QProcess.ProcessError) -> None`: log WARNING via _log (message names the GBS login subprocess + the error); gate on `if error == QProcess.ProcessError.FailedToStart:` — inside the gate, reset `self._gbs_login_proc = None` FIRST (WR-03; finished is NOT emitted on FailedToStart so it would otherwise never clear), then `self._update_status()`, then `self._on_gbs_import_clicked()` (D-02 fallback). For non-FailedToStart errors, log WARNING only and let the existing finished-path handle UX. `_on_oauth_login_error(self, error: QProcess.ProcessError) -> None`: on FailedToStart, log WARNING, reset `self._oauth_proc = None`, call `self._update_status()` — no cookie-import fallback (Twitch has none; GBS is the D-02 anchor). Wire both: in _launch_gbs_login_subprocess add `self._gbs_login_proc.errorOccurred.connect(self._on_gbs_login_error)` at QProcess construction; in _launch_oauth_subprocess add `self._oauth_proc.errorOccurred.connect(self._on_oauth_login_error)`. Mirror the capture-then-clear ordering from _on_gbs_login_finished (clear the guard field before any dialog open so re-entrant clicks see None). Append the four behavior tests to tests/test_accounts_dialog.py using the file's existing fixture/qtbot conventions; patch _on_gbs_import_clicked so no real modal opens.
  </action>
  <verify>
    <automated>uv run pytest tests/test_accounts_dialog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/ui_qt/accounts_dialog.py contains "errorOccurred.connect" for both _gbs_login_proc and _oauth_proc
    - accounts_dialog.py _on_gbs_login_error sets `self._gbs_login_proc = None` before calling `self._on_gbs_import_clicked()`
    - accounts_dialog.py _on_gbs_login_error gates the cookie-import fallback on `error == QProcess.ProcessError.FailedToStart`
    - accounts_dialog.py contains module-level `import logging` and `_log = logging.getLogger(__name__)`
    - accounts_dialog.py _on_oauth_login_error sets `self._oauth_proc = None` and does NOT open CookieImportDialog
    - tests/test_accounts_dialog.py exits 0 (four new errorOccurred cases pass; `-k error` selects them)
  </acceptance_criteria>
  <done>GBS FailedToStart resets the WR-03 guard, logs at WARNING, and surfaces the cookie-import fallback; Crashed does not double-trigger; Twitch resets + logs with no fallback; all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add build.ps1 OAUTH HELPER GUARD (exit 12) + SkipOauthGuard + source-text drift-guard</name>
  <files>packaging/windows/build.ps1, tests/test_packaging_oauth_launcher_spec.py</files>
  <read_first>
    - packaging/windows/build.ps1 lines 4-9 (exit-code header — append exit 12 entry), lines 12-19 (param block — add SkipOauthGuard mirroring SkipSmtcGuard), lines 322-337 (SMTC SMOKE GUARD block — the exact copy/adapt template; WR-01 Write-Host+exit ordering)
    - tests/test_packaging_winrt_spec.py (entire file — the drift-guard structural analog: module-scope source fixtures, presence assertions, WR-01 ordering check, _strip_comments for negative gates)
    - .planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-PATTERNS.md §build.ps1 and §test_packaging_oauth_launcher_spec.py (the five drift-guard assertions)
    - .planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-RESEARCH.md §"Pattern 4" (the illustrative PowerShell guard block, exit 12)
  </read_first>
  <action>
    In packaging/windows/build.ps1: (1) append to the exit-code header comment a line `12=oauth helper entrypoint unreachable in frozen bundle (Phase 88.2 / D-05)`. (2) Add `[switch]$SkipOauthGuard = $false` to the param block with an inline comment citing Phase 88.2 D-05 / WR-02 (mirror the SkipSmtcGuard comment). (3) Insert a new guard block immediately AFTER the SMTC SMOKE GUARD block (after line 337) and before the step-5 smoke section: `if (-not $SkipOauthGuard) { Write-Host "=== OAUTH HELPER GUARD: assert frozen exe can dispatch --oauth-helper (Phase 88.2 / D-05) ===" ; Invoke-Native { & "..\..\dist\MusicStreamer\MusicStreamer.exe" --oauth-helper --self-test 2>&1 | Out-Host } ; if ($LASTEXITCODE -ne 0) { Write-Host "BUILD_FAIL reason=oauth_helper_entrypoint_unreachable hint='MusicStreamer.exe --oauth-helper --self-test returned non-zero; __main__.py dispatch did not reach _run_oauth_helper'" -ForegroundColor Red ; exit 12 } ; Write-Host "OAUTH HELPER GUARD OK" } else { Write-Host "OAUTH HELPER GUARD skipped (-SkipOauthGuard)" }`. Preserve WR-01: Write-Host (never Write-Error) before exit 12, inside the Invoke-Native + $LASTEXITCODE pattern.
    Create tests/test_packaging_oauth_launcher_spec.py mirroring test_packaging_winrt_spec.py: module-scope fixtures reading build.ps1 and __main__.py text once; a `_strip_comments` helper for negative gates. Write five tests: (a) build.ps1 contains "OAUTH HELPER GUARD" + "--oauth-helper" + "BUILD_FAIL reason=oauth_helper_entrypoint_unreachable"; (b) build.ps1 contains "exit 12" + "12=oauth", with WR-01 ordering (Write-Host precedes the BUILD_FAIL marker, exit 12 follows it); (c) build.ps1 contains "SkipOauthGuard"; (d) __main__.py (comment-stripped) contains "--oauth-helper" and "_run_oauth_helper"; (e) __main__.py contains "--self-test". Use grep-gate hygiene (strip comment lines before any negative/count assertion).
  </action>
  <verify>
    <automated>uv run pytest tests/test_packaging_oauth_launcher_spec.py -x</automated>
  </verify>
  <acceptance_criteria>
    - packaging/windows/build.ps1 contains "OAUTH HELPER GUARD"
    - packaging/windows/build.ps1 contains "--oauth-helper --self-test"
    - packaging/windows/build.ps1 contains "exit 12" and "12=oauth" in the header
    - packaging/windows/build.ps1 contains "SkipOauthGuard"
    - packaging/windows/build.ps1 has Write-Host before "BUILD_FAIL reason=oauth_helper_entrypoint_unreachable" and "exit 12" after it (WR-01)
    - tests/test_packaging_oauth_launcher_spec.py exits 0 (five assertions)
  </acceptance_criteria>
  <done>build.ps1 carries the oauth-helper smoke guard with documented exit 12 and a SkipOauthGuard bypass; the drift-guard locks both build.ps1 and __main__.py source in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failed-launch subprocess → UI fallback | a FailedToStart QProcess error drives the GBS cookie-import fallback dialog |
| build VM → frozen exe self-test | `build.ps1` invokes `MusicStreamer.exe --oauth-helper --self-test` and branches on its exit code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88.2-04 | Denial of Service | WR-03 re-entrancy guard on `_gbs_login_proc` / `_oauth_proc` | mitigate | `errorOccurred` handler resets the guard to `None` on FailedToStart so a failed start can never permanently wedge the login button (the exact failure the guard would otherwise cause, since `finished` is not emitted) |
| T-88.2-05 | Tampering | `--oauth-helper --self-test` build-time entrypoint | accept | self-test returns 0 and performs no file/network/shell action; it only proves the dispatch arm is reachable. Exit code is the sole contract (`console=False` build) — no stdout parsing that could be spoofed |
| T-88.2-06 | Spoofing | `errorOccurred` Crashed vs FailedToStart | mitigate | Fallback is gated on `error == FailedToStart` specifically so a Crashed error cannot spuriously open the cookie-import dialog or double-trigger UX (Pitfall 6) |
| T-88.2-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (RESEARCH.md §Package Legitimacy Audit: not applicable — zero new dependencies); slopcheck N/A |
</threat_model>

<verification>
- `uv run pytest tests/test_accounts_dialog.py -k error -x` — four errorOccurred cases green
- `uv run pytest tests/test_packaging_oauth_launcher_spec.py -x` — five drift-guard assertions green
- `uv run pytest` — full suite green (Linux-side)
- Manual/VM-deferred (88-03): `MusicStreamer.exe --oauth-helper --self-test` exits 0 and the build.ps1 OAUTH HELPER GUARD passes; UAT-10 GBS.FM login opens/authenticates/plays; Twitch + Google open from the frozen exe.
</verification>

<success_criteria>
- GBS `errorOccurred` on FailedToStart resets the WR-03 guard first, logs at WARNING, and surfaces the cookie-import fallback (D-02/D-03); Crashed does not double-trigger.
- Twitch `errorOccurred` on FailedToStart resets the guard + logs (no fallback).
- `build.ps1` carries the OAUTH HELPER GUARD (exit 12) with a `SkipOauthGuard` bypass and documented exit-code header entry.
- `tests/test_packaging_oauth_launcher_spec.py` + the new `test_accounts_dialog.py` cases pass; full `uv run pytest` green.
</success_criteria>

<output>
Create `.planning/phases/88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g/88.2-02-SUMMARY.md` when done
</output>
