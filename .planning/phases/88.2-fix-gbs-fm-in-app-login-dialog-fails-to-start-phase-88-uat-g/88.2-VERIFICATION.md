---
phase: 88.2-fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g
verified: 2026-06-09T00:00:00Z
status: human_needed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "On a Win11 VM with the frozen MusicStreamer.exe bundle, run: MusicStreamer.exe --oauth-helper --self-test"
    expected: "Process exits 0 with no window opened. build.ps1 OAUTH HELPER GUARD passes (exits 0 at step 4d, not 12)."
    why_human: "Requires the PyInstaller-frozen Windows exe — cannot be validated on Linux CI. This is the D-05 runtime guard contract."
  - test: "On the Win11 VM, open AccountsDialog and click the GBS.FM login button."
    expected: "The login window opens (QtWebEngine), the user can authenticate, and GBS.FM audio plays (UAT-10 golden path)."
    why_human: "Requires frozen Windows build + real browser interaction + audio hardware. Core UAT-10 re-test for VER-02-J."
  - test: "On the Win11 VM, open AccountsDialog and click Connect Twitch."
    expected: "Twitch login window opens from the frozen exe (QtWebEngine)."
    why_human: "Requires frozen Windows build. Free consequence of the shared launcher fix, verified interactively."
  - test: "On the Win11 VM, open CookieImportDialog and click the Google/YouTube login button."
    expected: "Google login window opens from the frozen exe (QtWebEngine)."
    why_human: "Requires frozen Windows build. Third call site (CR-01 fix), verified interactively."
---

# Phase 88.2: Fix GBS.FM In-App Login Dialog Fails to Start — Verification Report

**Phase Goal:** Fix the GBS.FM in-app login so it actually starts on the bundled (PyInstaller-frozen) Windows build, unblocking playback (Phase 88 UAT gap G3 / UAT-10 / VER-02-J). The shared subprocess-launch mechanism (also used by Twitch + Google login) is repaired so all three logins work on the frozen build; GBS.FM is the verification anchor. Phase closes on the Linux/CI side; the frozen-Windows UAT-10 interactive re-test + build.ps1 runtime guard are deferred to the consolidated 88-03 Win11 VM session.
**Verified:** 2026-06-09
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Linux-verifiable — phase closes on these)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | When `sys.frozen` is True, `_make_oauth_launch_args` chooses the `--oauth-helper` argv form (not `-m module`) | VERIFIED | `subprocess_utils.py:53-54` — `if getattr(sys, "frozen", False): return sys.executable, ["--oauth-helper", "--mode", mode]`; test 1 passes |
| 2 | When not frozen (dev), the launcher chooses the unchanged `-m musicstreamer.oauth_helper` form (T-40-05 preserved) | VERIFIED | `subprocess_utils.py:55` — `return sys.executable, ["-m", "musicstreamer.oauth_helper", "--mode", mode]`; test 2 passes |
| 3 | All three call sites (Twitch, GBS, Google) launch via the shared `_make_oauth_launch_args` helper — no inlined `-m` string remains | VERIFIED | `accounts_dialog.py:43` module-level import; `:394` Twitch call; `:420` GBS call. `cookie_import_dialog.py:313` lazy import; `:314` Google call. `grep -rn '"-m", "musicstreamer.oauth_helper"' musicstreamer/ui_qt/` returns zero hits. |
| 4 | `MusicStreamer --oauth-helper --self-test` returns 0 without constructing any QApplication/QCoreApplication | VERIFIED | `__main__.py:65` — `if "--self-test" in argv: return 0`; test 3 passes (pure Python, no Qt import) |
| 5 | `__main__.main()` dispatches `--oauth-helper` to `_run_oauth_helper` before `_run_gui`; `--oauth-helper` is stripped before `oauth_helper.main()` parses argv | VERIFIED | `__main__.py:390-391` — `if args.oauth_helper: return _run_oauth_helper(remaining)` precedes check_mediakeys (line 392) and smoke (line 394). `:70` strips `--oauth-helper` from argv. Test 4 confirms stripping. |
| 6 | `tests/test_launcher_frozen_dispatch.py` passes (4 tests); `tests/test_packaging_oauth_launcher_spec.py` passes (5 tests) | VERIFIED | `uv run pytest tests/test_launcher_frozen_dispatch.py tests/test_packaging_oauth_launcher_spec.py -v` — 9 passed, 0 failed |
| 7 | On GBS `FailedToStart`, the `errorOccurred` handler resets `self._gbs_login_proc = None` (WR-03) BEFORE opening any dialog | VERIFIED | `accounts_dialog.py:645-652` — gate on `FailedToStart`, then `self._gbs_login_proc = None` (line 648) before `_update_status()` and `_on_gbs_import_clicked()`. `TestAccountsDialogErrorOccurred` Test A asserts this ordering. |
| 8 | On GBS `FailedToStart`, a visible WARNING is logged and the `CookieImportDialog` File/Paste fallback is surfaced (D-03, D-02) | VERIFIED | `accounts_dialog.py:641-652` — `_log.warning(...)` fires for all error types; `_on_gbs_import_clicked()` called inside FailedToStart gate. Test B asserts WARNING log; Test A asserts fallback call. |
| 9 | The GBS `errorOccurred` handler checks `error == FailedToStart` specifically; a Crashed error does not double-trigger the cookie-import fallback (Pitfall 6) | VERIFIED | `accounts_dialog.py:645` — `if error == QProcess.ProcessError.FailedToStart:`. Test C invokes with `Crashed` and asserts `_on_gbs_import_clicked` was NOT called. |
| 10 | On Twitch `FailedToStart`, the `errorOccurred` handler resets `self._oauth_proc = None`, logs, and updates status (no cookie-import fallback for Twitch); visible user warning is surfaced (WR-02) | VERIFIED | `accounts_dialog.py:654-677` — WARNING logged, `self._oauth_proc = None` on FailedToStart, `_update_status()`, and `QMessageBox.warning(...)` for user visibility. Test D asserts reset and no fallback. |
| 11 | `build.ps1` contains the OAUTH HELPER GUARD invoking `--oauth-helper --self-test`, failing with documented exit 12, with `SkipOauthGuard` bypass switch | VERIFIED | `build.ps1:9` — header documents `12=oauth helper entrypoint unreachable`; `:22` — `[switch]$SkipOauthGuard`; `:344-365` — full guard block with `Invoke-Native`, `LASTEXITCODE` check, WR-01 `Write-Host -ForegroundColor Red` before `exit 12`. |
| 12 | CR-01 fix: `cookie_import_dialog._on_google_process_error` wired; `errorOccurred` connected in `_on_google_login`; `import sys` dead import removed (WR-01) | VERIFIED | `cookie_import_dialog.py:308` — `process.errorOccurred.connect(self._on_google_process_error)`; `:340-369` — handler resets `_google_process = None` on FailedToStart, restores button, shows warning. `grep -n "import sys" cookie_import_dialog.py` returns zero hits. |

**Score:** 12/12 truths verified

---

### VM-Deferred Truths (NOT gaps — per D-06/D-07 and plan `vm_deferred` frontmatter)

These truths are explicitly deferred to the consolidated 88-03 Win11 VM session. They are classified as human verification items, not gaps.

| # | Item | Deferred To |
|---|------|------------|
| 1 | Frozen `MusicStreamer.exe --oauth-helper --self-test` exits 0 on the Windows build VM; build.ps1 OAUTH HELPER GUARD passes | Phase 88-03 VM session |
| 2 | GBS.FM in-app login opens, authenticates, audio plays on the frozen exe (UAT-10 / VER-02-J) | Phase 88-03 VM session |
| 3 | Twitch login opens from the frozen exe | Phase 88-03 VM session |
| 4 | Google/YouTube login opens from the frozen exe | Phase 88-03 VM session |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `musicstreamer/subprocess_utils.py` | `_make_oauth_launch_args(mode)` frozen/non-frozen branch | VERIFIED | `def _make_oauth_launch_args` at line 34; both branches implemented correctly |
| `musicstreamer/__main__.py` | `--oauth-helper` argv dispatch + `_run_oauth_helper` with `--self-test` short-circuit | VERIFIED | `_run_oauth_helper` at line 48; dispatch at line 390; `--self-test` short-circuit at line 65 |
| `musicstreamer/ui_qt/accounts_dialog.py` | `_on_gbs_login_error` + `_on_oauth_login_error` errorOccurred handlers; module-level logging | VERIFIED | `import logging` line 22; `_log` line 45; handlers at lines 627, 654; both wired via `errorOccurred.connect` at lines 389, 415 |
| `musicstreamer/ui_qt/cookie_import_dialog.py` | `_on_google_process_error` handler; `errorOccurred` connected; dead `import sys` removed | VERIFIED | Handler at line 340; `errorOccurred.connect` at line 308; `import sys` absent |
| `packaging/windows/build.ps1` | OAUTH HELPER GUARD block (exit 12) + `SkipOauthGuard` param + exit-code header entry | VERIFIED | Header line 9; param line 22; guard block lines 344-365 |
| `tests/test_launcher_frozen_dispatch.py` | D-04 frozen/non-frozen launch-shape unit tests (4 tests) | VERIFIED | 4 tests collected and passed |
| `tests/test_packaging_oauth_launcher_spec.py` | D-05 source-text drift-guards for build.ps1 + `__main__.py` (5 tests) | VERIFIED | 5 tests collected and passed; `oauth_helper_entrypoint_unreachable` string present |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `accounts_dialog.py` | `subprocess_utils._make_oauth_launch_args` | `from musicstreamer.subprocess_utils import _make_oauth_launch_args` + `QProcess.start(program, args)` | WIRED | Module-level import line 43; used at lines 394 (Twitch) and 420 (GBS) |
| `cookie_import_dialog.py` | `subprocess_utils._make_oauth_launch_args` | Lazy import + `QProcess.start(program, args)` | WIRED | Function-scope lazy import line 313; used line 314 |
| `__main__.py` | `musicstreamer.oauth_helper.main` | `_run_oauth_helper` dispatch arm | WIRED | Lazy import line 71; dispatch condition line 390 |
| `accounts_dialog._on_gbs_login_error` | `_on_gbs_import_clicked` (CookieImportDialog fallback) | `errorOccurred` FailedToStart → guard reset → fallback | WIRED | Lines 645-652; guard reset at line 648 before fallback at line 652 |
| `build.ps1` OAUTH HELPER GUARD | `MusicStreamer.exe --oauth-helper --self-test` | `Invoke-Native { & "..\..\dist\MusicStreamer\MusicStreamer.exe" --oauth-helper --self-test }` + `$LASTEXITCODE` → exit 12 | WIRED | Lines 353-361; `--oauth-helper --self-test` literal at line 356 |

---

### Data-Flow Trace (Level 4)

Not applicable for this phase. Phase produces subprocess launch logic, error handlers, and a build script guard — not data-rendering components. The critical data flow is argv-to-QProcess-to-frozen-exe, verified via unit tests (Level 3) and source-text drift-guards.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `_run_oauth_helper` self-test returns 0 | `uv run pytest tests/test_launcher_frozen_dispatch.py::test_run_oauth_helper_self_test_returns_0 -v` | PASSED | PASS |
| Frozen branch uses `--oauth-helper` form | `uv run pytest tests/test_launcher_frozen_dispatch.py::test_frozen_branch_uses_exe_argv_dispatch -v` | PASSED | PASS |
| Non-frozen branch uses `-m` form | `uv run pytest tests/test_launcher_frozen_dispatch.py::test_non_frozen_branch_uses_module_form -v` | PASSED | PASS |
| `--oauth-helper` stripped before forwarding | `uv run pytest tests/test_launcher_frozen_dispatch.py::test_run_oauth_helper_strips_oauth_helper_flag -v` | PASSED | PASS |
| build.ps1 contains guard block | `uv run pytest tests/test_packaging_oauth_launcher_spec.py -v` | 5 PASSED | PASS |
| No inlined `-m musicstreamer.oauth_helper` in ui_qt/ | `grep -rn '"-m", "musicstreamer.oauth_helper"' musicstreamer/ui_qt/` | zero hits | PASS |

Full suite (Linux-runnable): 9/9 tests pass. Qt-dependent tests (`test_accounts_dialog.py`, `test_cookie_import_dialog.py`) cannot run locally (pre-existing: `PySide6.QtCore` absent in uv venv; system-installed only). Those are CI-verified — source correctness confirmed by direct code inspection.

---

### Probe Execution

No probe scripts declared for this phase. Phase does not follow a probe-based verification model.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| VER-02-J | 88.2-01-PLAN.md, 88.2-02-PLAN.md | Win11 VM packaging UAT golden path — GBS.FM login+playback | PARTIAL (Linux CI side VERIFIED; Win11 UAT interactive portion pending 88-03) | All Linux-side fixes implemented and tested. GBS login subprocess now uses frozen-safe launcher. errorOccurred handlers prevent silent dead-end. Frozen VM UAT deferred per D-06/D-07. |

---

### Anti-Patterns Found

None. Scan of all 7 phase-modified files (`subprocess_utils.py`, `__main__.py`, `accounts_dialog.py`, `cookie_import_dialog.py`, `build.ps1`, `test_launcher_frozen_dispatch.py`, `test_packaging_oauth_launcher_spec.py`) found zero debt markers (`TBD`, `FIXME`, `XXX`), no stub returns, no placeholder implementations.

---

### Human Verification Required

#### 1. OAUTH HELPER GUARD — Win11 VM Runtime Check

**Test:** On a Win11 VM with the PyInstaller-frozen `MusicStreamer.exe` bundle, run: `MusicStreamer.exe --oauth-helper --self-test` (or run `build.ps1` and observe step 4d)
**Expected:** Process exits 0 with no window opened; build.ps1 step 4d prints "OAUTH HELPER GUARD OK"
**Why human:** Requires the PyInstaller-frozen Windows exe — cannot be validated on Linux CI. This is the D-05 runtime guard contract that proves the `__main__.py` dispatch arm is reachable in the frozen bundle.

#### 2. GBS.FM In-App Login — UAT-10 Golden Path (VER-02-J)

**Test:** On the Win11 VM, open AccountsDialog from the frozen exe and click the GBS.FM login button.
**Expected:** The login window opens (QtWebEngine), the user can authenticate, and GBS.FM audio plays (UAT-10 golden path). The `_on_gbs_login_error` fallback to CookieImportDialog should NOT trigger on a working bundle.
**Why human:** Requires frozen Windows build + real QtWebEngine browser interaction + audio hardware. Core UAT-10 re-test for VER-02-J.

#### 3. Twitch Login — Frozen-Build Launch Verification

**Test:** On the Win11 VM, open AccountsDialog and click "Connect Twitch."
**Expected:** Twitch login window opens from the frozen exe.
**Why human:** Requires frozen Windows build. Free consequence of the shared launcher fix.

#### 4. Google/YouTube Login — Frozen-Build Launch Verification (CR-01 Fix)

**Test:** On the Win11 VM, open CookieImportDialog and click the Google/YouTube login button.
**Expected:** Google login window opens from the frozen exe. If it fails, `_on_google_process_error` should restore the button and show a warning dialog (not a silent dead-end).
**Why human:** Requires frozen Windows build. Third call site (CR-01 fix).

---

### Code Review Fixes Verification

The 88.2-REVIEW.md identified one BLOCKER and three warnings. All are confirmed fixed:

| Finding | Status | Evidence |
|---------|--------|----------|
| CR-01 BLOCKER: `cookie_import_dialog._on_google_login` had no `errorOccurred` handler | FIXED | `cookie_import_dialog.py:308` wires `errorOccurred.connect(self._on_google_process_error)`; handler at lines 340-369 resets guard, restores button, shows warning |
| WR-01: Dead `import sys` in `cookie_import_dialog` | FIXED | `grep -n "import sys" cookie_import_dialog.py` returns zero hits |
| WR-02: Twitch `errorOccurred` handler left no user-visible signal | FIXED | `accounts_dialog.py:673-677` — `QMessageBox.warning(...)` surfaces "Could not start the Twitch login window" on FailedToStart |
| WR-03: Forwarded Qt flags could break `oauth_helper` strict `parse_args` | INFO (accepted) | Review classified as Warning, not blocking. Current call sites only pass `["--oauth-helper", "--mode", m]`, making the issue non-triggerable today. No fix required to close phase. |
| IN-01: Strip `--oauth-helper` is dead in real dispatch path | INFO | Harmless belt-and-braces; no action needed |
| IN-02: `importlib.reload` in tests may cause module identity churn | INFO | No test failures observed; no action needed to close phase |

---

### Gaps Summary

No gaps. All 12 Linux-verifiable must-haves are confirmed in the codebase with direct source evidence and passing tests. The four remaining items are VM-deferred human verification items per the phase's D-06/D-07 completion boundary — they are intentional by design (mirroring Phase 88.1's model) and must not be treated as blocking gaps.

---

_Verified: 2026-06-09_
_Verifier: Claude (gsd-verifier)_
