---
phase: 88.2
slug: fix-gbs-fm-in-app-login-dialog-fails-to-start-phase-88-uat-g
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-09
---

# Phase 88.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Sourced from 88.2-RESEARCH.md § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (uv run pytest) |
| **Config file** | `pyproject.toml` [tool.pytest.ini_options] |
| **Quick run command** | `uv run pytest tests/test_launcher_frozen_dispatch.py tests/test_packaging_oauth_launcher_spec.py -x` |
| **Full suite command** | `uv run pytest` |
| **Estimated runtime** | ~3 s (quick) / full suite per existing CI |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_launcher_frozen_dispatch.py tests/test_packaging_oauth_launcher_spec.py -x`
- **After every plan wave:** Run `uv run pytest`
- **Before `/gsd:verify-work`:** Full suite must be green (Linux-side)
- **Max feedback latency:** ~10 seconds (quick run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (D-04) | tbd | 0/1 | VER-02-J | — | Frozen branch returns `--oauth-helper` form; non-frozen returns `-m` form | unit (monkeypatch sys.frozen/sys.executable) | `uv run pytest tests/test_launcher_frozen_dispatch.py -x` | ❌ W0 | ⬜ pending |
| (D-05 static) | tbd | 1 | VER-02-J | — | `build.ps1` contains oauth-helper guard + exit 12; `__main__.py` dispatches `--oauth-helper` | source-text drift-guard | `uv run pytest tests/test_packaging_oauth_launcher_spec.py -x` | ❌ W0 | ⬜ pending |
| (D-02/D-03) | tbd | 1 | VER-02-J | — | `errorOccurred`/FailedToStart resets WR-03 guard to None, logs visibly, surfaces cookie-import fallback | unit | `uv run pytest tests/test_accounts_dialog.py -k error_occurred -x` | ❌ W0 new cases | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_launcher_frozen_dispatch.py` — D-04 frozen/non-frozen launch-shape assertions (monkeypatch `sys.frozen` + `sys.executable`)
- [ ] `tests/test_packaging_oauth_launcher_spec.py` — D-05 `build.ps1` + `__main__.py` source-text drift-guards (oauth-helper dispatch present, exit code 12 documented)
- [ ] New `errorOccurred` cases in `tests/test_accounts_dialog.py` — D-02/D-03 WR-03 guard reset + visible log + cookie-import fallback

*Existing `tests/test_accounts_dialog.py`, `tests/test_packaging_winrt_spec.py`, and `tests/test_packaging_spec.py` are present — no framework setup needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Frozen exe exits 0 on `--oauth-helper --self-test` (D-05 runtime guard) | VER-02-J | Requires the PyInstaller-frozen Windows build; executor runs on Linux | Run `build.ps1` smoke step on Win11 VM; assert exit 0, fail build with exit 12 otherwise — **88-03 VM session** |
| GBS.FM login window opens, authenticates, audio plays (UAT-10) | VER-02-J | Interactive QtWebEngine login + live GBS.FM auth | Open GBS.FM station on frozen build, log in via in-app dialog, confirm audio — **88-03 VM session** |
| Twitch + Google login also open from the frozen exe (D-01 free consequence) | VER-02-J | Same frozen-build interactive requirement | Open each login from frozen build — **88-03 VM session** |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (VM-deferred items explicitly listed as Manual-Only)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
