# Phase 999.3 — Human UAT (Plan 03)

**Status:** Awaiting Kyle's sign-off
**Wave 1 code merged:** commits `c1c65cf`, `12e6ab9`, `2948d49` (Plan 01) + `8019f64`, `3c43fa5`, `103b1c7` (Plan 02)
**Task 1 automated suite:** ✓ 725 passed (run at `084ab48` + merges, pre-existing MPRIS/SMTC/station-list failures deselected — same set fails at base commit `55bd5aa`)

---

## Setup (run once)

```bash
rm -f ~/.local/share/musicstreamer/twitch-token.txt
rm -f ~/.local/share/musicstreamer/oauth.log
```

---

## UAT 1 — Happy path (D-01, D-04, D-07, D-11; T-999.3-01, T-999.3-03, T-999.3-04)

- [ ] Launch: `uv run -- python -m musicstreamer`
- [ ] Hamburger menu → Accounts
- [ ] Click **Connect Twitch** → browser opens at `id.twitch.tv/oauth2/authorize?...`
- [ ] Log in with your Twitch account, authorize if prompted
- [ ] Browser redirects to `http://127.0.0.1:17823/#access_token=...` → "Logged in — you can close this window" → subprocess window auto-closes
- [ ] AccountsDialog status flips to **Connected** / button shows **Disconnect**
- [ ] Verify token file: `ls -la ~/.local/share/musicstreamer/twitch-token.txt` → `-rw-------`, > 10 bytes
- [ ] Play any Twitch station → stream starts within a few seconds (proves token GQL-compatible)
- [ ] `cat ~/.local/share/musicstreamer/oauth.log` → one JSON line `{"ts":...,"category":"Success","detail":"","provider":"twitch"}`, mode `-rw-------`
- [ ] `grep -E "(access_token|://|127\.0\.0\.1)" ~/.local/share/musicstreamer/oauth.log` → **no matches**

## UAT 2 — WindowClosedBeforeLogin (D-08, D-09, D-11)

- [ ] Disconnect Twitch (AccountsDialog → Disconnect → Yes)
- [ ] Click Connect Twitch → browser opens
- [ ] **Close the browser window WITHOUT logging in**
- [ ] Failure dialog appears titled "Twitch Connection Failed", body text "Login window was closed before completing", detail line, **Retry** + **Close** buttons
- [ ] Click Retry → new browser window opens (AccountsDialog stays open — inline relaunch)
- [ ] Close again → failure dialog reappears
- [ ] Click Close → returns to AccountsDialog, status "Not connected"
- [ ] `grep WindowClosedBeforeLogin ~/.local/share/musicstreamer/oauth.log` → ≥2 JSON lines

## UAT 3 — Google/YouTube regression (D-07)

- [ ] Trigger Google login (Import Stations → Google login, or existing entry point for `oauth_helper.py --mode google`)
- [ ] Log in with Google
- [ ] Cookies file written to `~/.local/share/musicstreamer/cookies.txt`
- [ ] No regression from pre-Phase-999.3 behavior

## UAT 4 — Token persistence

- [ ] After successful UAT 1, fully quit MusicStreamer
- [ ] Relaunch: `uv run -- python -m musicstreamer`
- [ ] Open AccountsDialog → status reads **Connected**
- [ ] Play a Twitch station → still works

---

## Sign-off

Reply with `UAT pass` when all four scenarios green, or paste failure details (oauth.log contents, dialog text, terminal output) for any that fail.

After sign-off, Plan 03 SUMMARY.md will be written and the phase completed via `/gsd-verify-work 999.3`.
