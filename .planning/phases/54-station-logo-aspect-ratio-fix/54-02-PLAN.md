---
phase: 54
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: false
requirements: [BUG-05]
must_haves:
  truths:
    - "User has visually confirmed whether portrait logos render pillarboxed (16w × 32h) or cropped (top+bottom missing) in the provider tree on their actual running build"
    - "User has visually confirmed whether landscape logos render letterboxed (32w × 16h) or cropped (left+right missing) in the provider tree on their actual running build"
    - "User has visually confirmed the icon column is uniformly 32×32 across square + landscape + portrait stations (SC #4)"
    - "User has explicitly chosen Path A (test-only ship — no production change) OR Path B-1 (apply ~10-line _art_paths.py canvas-paint patch in Plan 03)"
    - "Decision is recorded in 54-02-SUMMARY.md with screenshots referenced (or commit-attached) so Plan 03 has an unambiguous trigger"
  artifacts:
    - path: ".planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md"
      provides: "UAT outcome + Path A/B-1 decision + screenshot references"
      contains: "PATH A SHIP"
  key_links:
    - from: "Live app (provider tree row icons)"
      to: "User's eyes (visual UAT)"
      via: "manual screenshot capture against station id=2 + ClassicalRadio + synthetic portrait fixture"
      pattern: "Living Coffee|20th Century|portrait"
---

<objective>
Run the live UAT decision gate for Phase 54. The user inspects the provider-tree row icons against three concrete repros (landscape from live DB, square baseline from live DB, synthetic portrait installed via EditStationDialog) and signs off on either:

- **Path A SHIP** — current pipeline renders aspect-correctly on the user's machine; no production change needed; phase ships as the test-only regression-lock from Plan 01.
- **Path B-1 ESCALATE** — UAT shows actual cropping (top+bottom missing on portrait, or left+right missing on landscape); Plan 03 will apply the ~10-line canvas-paint patch in `musicstreamer/ui_qt/_art_paths.py`.

Purpose: RESEARCH.md §1 verified at HIGH confidence on Linux/Qt 6.11.0/offscreen platform that the existing pipeline pillarboxes/letterboxes correctly. The user's running environment may differ. This plan is the gate.

Output: `.planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md` with UAT screenshots + explicit Path A vs Path B-1 decision; Plan 03 either skipped or unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-station-logo-aspect-ratio-fix/54-CONTEXT.md
@.planning/phases/54-station-logo-aspect-ratio-fix/54-RESEARCH.md
@.planning/phases/54-station-logo-aspect-ratio-fix/54-VALIDATION.md
@.planning/phases/54-station-logo-aspect-ratio-fix/54-01-SUMMARY.md
@./CLAUDE.md

<repro_targets>
<!-- Authoritative repro targets, sourced from RESEARCH.md §5 (live DB audit 2026-04-29). -->

Live DB: ~/.local/share/musicstreamer/musicstreamer.sqlite3 (172 stations with station_art_path)

Square baseline (in DB):
  - Station id=10 "20th Century" (provider: ClassicalRadio)
  - Asset: ~/.local/share/musicstreamer/assets/10/station_art.jpg
  - Dimensions: 1000×1000 (1:1)
  - Expected tree-row render: 32×32 filling the cell, edge-to-edge

Landscape repro (in DB):
  - Station id=2 "Living Coffee: Smooth Jazz Radio - Relaxing Jazz & Sweet Bossa Nova for Calm at Home"
  - Provider: Cafe BGM (YouTube live thumb)
  - Asset: ~/.local/share/musicstreamer/assets/2/station_art.jpg
  - Dimensions: 1280×720 (16:9 landscape)
  - Expected tree-row render: 32w × 18h centered letterbox; transparent strips above + below show row's selection background

Portrait repro (NOT in DB — must synthesize):
  - 0/172 stations have a portrait-shaped logo
  - Generate a synthetic 50×100 red PNG to /tmp/portrait.png with:
    `python3 -c "from PySide6.QtGui import QPixmap; from PySide6.QtCore import Qt; from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); p=QPixmap(50,100); p.fill(Qt.red); p.save('/tmp/portrait.png','PNG')"`
  - Install via EditStationDialog → "Choose File…" button (line 257 in edit_station_dialog.py) on a throw-away test station
  - Note the original station_art_path of the chosen station BEFORE upload so it can be restored after UAT
  - Expected tree-row render: 16w × 32h centered pillarbox; transparent strips left + right show row's selection background
</repro_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate synthetic portrait fixture and verify live DB repro assets</name>
  <files>/tmp/portrait.png</files>
  <read_first>
    - .planning/phases/54-station-logo-aspect-ratio-fix/54-RESEARCH.md §4 "Live UAT Steps"
    - .planning/phases/54-station-logo-aspect-ratio-fix/54-RESEARCH.md §5 "Repro Station(s) from Live DB"
  </read_first>
  <action>
Step 1 — Generate the synthetic portrait PNG. From any shell:

```
python3 -c "from PySide6.QtGui import QPixmap; from PySide6.QtCore import Qt; from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); p=QPixmap(50,100); p.fill(Qt.red); p.save('/tmp/portrait.png','PNG')"
```

Verify the file exists:
```
ls -la /tmp/portrait.png
```
Expected: file exists, size ~150 bytes (red 50×100 PNG with default zlib compression).

Step 2 — Verify the synthetic image dimensions match expectations (sanity check):
```
python3 -c "from PySide6.QtGui import QPixmap; from PySide6.QtCore import Qt; from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); p=QPixmap('/tmp/portrait.png'); print(f'{p.width()}x{p.height()}')"
```
Expected output: `50x100`

Step 3 — Confirm the live DB and live assets exist for the in-DB repros:
```
ls -la ~/.local/share/musicstreamer/musicstreamer.sqlite3
ls -la ~/.local/share/musicstreamer/assets/2/station_art.jpg   # landscape repro (Living Coffee)
ls -la ~/.local/share/musicstreamer/assets/10/station_art.jpg  # square baseline (20th Century)
```
All three should exist. If any are missing, ask the user to confirm their data path.

The actual UAT (visual inspection in the running app) is in Task 2 (the human-verify checkpoint). This task only prepares the synthetic portrait fixture.
  </action>
  <verify>
    <automated>test -f /tmp/portrait.png && python3 -c "from PySide6.QtGui import QPixmap; from PySide6.QtCore import Qt; from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); p=QPixmap('/tmp/portrait.png'); assert p.width()==50 and p.height()==100, f'{p.width()}x{p.height()}'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /tmp/portrait.png` exits 0
    - The QPixmap dimensions probe prints `ok` (i.e. width=50, height=100)
    - `test -f ~/.local/share/musicstreamer/assets/2/station_art.jpg` exits 0 (landscape repro asset)
    - `test -f ~/.local/share/musicstreamer/assets/10/station_art.jpg` exits 0 (square baseline asset)
  </acceptance_criteria>
  <done>
    /tmp/portrait.png exists and is a valid 50×100 red PNG. Live DB landscape + square repro assets are present. Ready for visual UAT in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User UAT — inspect provider-tree row icons against 3 repros and decide Path A vs Path B-1</name>
  <files>.planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md, .planning/phases/54-station-logo-aspect-ratio-fix/uat-landscape.png, .planning/phases/54-station-logo-aspect-ratio-fix/uat-portrait.png</files>
  <read_first>
    - .planning/phases/54-station-logo-aspect-ratio-fix/54-RESEARCH.md §4 "Live UAT Steps"
    - .planning/phases/54-station-logo-aspect-ratio-fix/54-RESEARCH.md §5 "Repro Station(s) from Live DB"
  </read_first>
  <what-built>
Plan 01 added two synthetic-pixmap regression tests to tests/test_art_paths.py that lock the existing aspect-preservation behavior. Task 1 above generated /tmp/portrait.png so the user can install a synthetic portrait logo.

No production code has been modified. The user is now asked to visually inspect the running app's provider-tree row icons and decide whether the currently-shipping pipeline is correct on their machine (Path A) or actually crops portrait/landscape logos (Path B-1).
  </what-built>
  <action>
This is a human-verify checkpoint. Do NOT execute any code. Pause and present the following UAT instructions to the user; capture their decision in `.planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md`.

UAT instructions for the user:

1. **Launch the app.**
   Run the app the way the user normally does (e.g. `python -m musicstreamer` or the desktop entry). The provider tree appears in the left panel.

2. **UAT step #1 — Landscape repro (in DB).**
   In the provider tree, expand the "Cafe BGM" provider. Find the row "Living Coffee: Smooth Jazz Radio - Relaxing Jazz & Sweet Bossa Nova for Calm at Home" (station id=2).
   - Look at the 32px-tall icon in that row.
   - Expected (Path A): a 32-wide × ~16-tall thumbnail of the YouTube live image, centered vertically in the cell, with empty strips above and below where the row's selection-accent background is visible.
   - Anti-expected (Path B-1 trigger): the thumbnail fills the full 32×32 cell but the top and bottom of the YouTube image are CUT OFF (you only see the middle slice).
   - Capture a screenshot. Save to `.planning/phases/54-station-logo-aspect-ratio-fix/uat-landscape.png` (or paste in the resume signal).

3. **UAT step #2 — Square baseline (in DB).**
   Expand "ClassicalRadio". Find row "20th Century" (station id=10) — or any other ClassicalRadio station.
   - Expected: the 1000×1000 logo fills the 32×32 cell edge-to-edge with no visible cropping (because source is square).
   - Capture a screenshot if convenient.

4. **UAT step #3 — Synthetic portrait (NOT in DB; must install).**
   Pick a throw-away test station OR temporarily an existing station. **Note its current `station_art_path` BEFORE proceeding** so you can restore later.

   Open the station in EditStationDialog (right-click → Edit, or the edit affordance). Click **"Choose File…"** in the Logo row. Pick `/tmp/portrait.png` (the 50×100 red fixture from Task 1). Save the dialog.

   Look at that station's row in the provider tree.
   - Expected (Path A): a 16-wide × 32-tall vertical RED rectangle, centered horizontally in the 32×32 cell, with transparent strips on the LEFT and RIGHT showing the row's selection background.
   - Anti-expected (Path B-1 trigger): the red region fills the full 32×32 cell but the TOP and BOTTOM of the source 50×100 image are cropped — you would only see a horizontal red band rather than a full-height vertical bar.

   Capture a screenshot. Save to `.planning/phases/54-station-logo-aspect-ratio-fix/uat-portrait.png` (or paste in the resume signal).

   After capturing the screenshot, **restore the station's original `station_art_path`** (re-run "Choose File…" on the original asset, or clear the logo via the "Clear" button if the station was originally logo-less).

5. **UAT step #4 — Icon column uniformity (SC #4).**
   Scroll through the provider tree visually. Confirm the icon column is consistently 32 pixels wide across all stations regardless of their logo aspect ratio. Station names should align vertically; the row height should not jump between stations.

6. **Decide Path A or Path B-1.**
   - If steps 1, 2, 3, 4 all match the **Expected (Path A)** outcomes — letterboxing/pillarboxing/edge-to-edge as documented — → **Path A**. Plan 03 is skipped. Phase 54 ships as test-only regression-lock + this UAT signoff.
   - If step 1 OR step 3 shows the **Anti-expected (Path B-1 trigger)** outcome — actual cropping with logo edges missing — → **Path B-1**. Plan 03 will apply the ~10-line `_art_paths.py` canvas-paint patch.

7. **Record the decision.**
   Write the outcome in the resume signal AND ensure `.planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md` records:
   - The OS + Qt platform the user ran on (Linux X11 / Linux Wayland / Windows / macOS).
   - The chosen path: `PATH A SHIP` or `PATH B-1 ESCALATE`.
   - Screenshot file paths (or attach inline).
   - Per-step UAT pass/fail (steps 1–4).
   - User's signoff line ("approved" / explicit rejection text).
  </action>
  <verify>
    <automated>test -f .planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md && grep -qE "PATH A SHIP|PATH B-1 ESCALATE" .planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md</automated>
  </verify>
  <resume-signal>
Type one of:
- `approved path A` — UAT confirms current pipeline is aspect-correct on user's machine; no production change needed; Plan 03 is SKIPPED.
- `approved path B-1` — UAT shows actual cropping; Plan 03 will apply the canvas-paint patch.
- `issues: <description>` — to flag a problem before locking in either path (e.g. "step 3 fixture upload failed", "step 1 image is missing entirely", "portrait fixture loaded but tree row never refreshes").
  </resume-signal>
  <acceptance_criteria>
    - 54-02-SUMMARY.md exists at `.planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md`
    - `grep -E "PATH A SHIP|PATH B-1 ESCALATE" .planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md` returns at least one match
    - SUMMARY references at least one screenshot path (uat-landscape.png and/or uat-portrait.png)
    - SUMMARY records the user's OS / Qt platform
    - Decision is unambiguous (no "maybe" or "TBD" in the chosen-path field)
  </acceptance_criteria>
  <done>
    User has signed off on Path A (Plan 03 skipped) or Path B-1 (Plan 03 unblocked). Screenshots captured. SUMMARY written.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → live DB | The synthetic-portrait UAT step temporarily writes a `station_art_path` for an existing station. The user must restore the original after UAT. |
| /tmp filesystem | The portrait fixture is written to /tmp/portrait.png — host-shared but acceptable for a transient test asset. |

## STRIDE Threat Register

Per RESEARCH.md §6 (authoritative summary): UAT is read-mostly visual confirmation. The only mutation is a deliberate, user-controlled `station_art_path` swap that is restored at the end.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | Test station's `station_art_path` mutated during UAT step #3 | mitigate | Task 2 explicitly instructs the user to NOTE the original path BEFORE the upload and to RESTORE it after the screenshot is captured. SUMMARY records the affected station_id so a follow-up audit is trivial if restoration is missed. |
| T-54-05 | DoS | /tmp/portrait.png left in /tmp | accept | Negligible (a few hundred bytes). User is welcome to `rm /tmp/portrait.png` after UAT. |

No new ASVS-relevant surface introduced by UAT.
</threat_model>

<verification>
- 54-02-SUMMARY.md exists and contains the explicit `PATH A SHIP` or `PATH B-1 ESCALATE` decision token.
- Screenshots are referenced and (ideally) committed alongside SUMMARY.
- User's OS / Qt platform is documented for traceability against RESEARCH.md §1's "Open Question 1".
</verification>

<success_criteria>
- BUG-05 / SC #4 (viewport size/position uniformity) is visually confirmed by the user.
- Path A vs Path B-1 decision is locked, eliminating ambiguity for Plan 03.
- The phase is now in one of two terminal states: (a) ready to close on test-only ship after Plan 01 (Path A), or (b) ready to execute Plan 03 (Path B-1).
</success_criteria>

<output>
After completion, create `.planning/phases/54-station-logo-aspect-ratio-fix/54-02-SUMMARY.md`. Use this exact structure:

```markdown
# Phase 54 — Plan 02 — UAT Summary

**Date:** YYYY-MM-DD
**OS / Qt platform:** [Linux X11 | Linux Wayland | Windows | macOS]
**Decision:** [PATH A SHIP | PATH B-1 ESCALATE]

## UAT Step Results

| Step | Repro | Expected | Observed | Status |
|------|-------|----------|----------|--------|
| 1 | Landscape (Living Coffee, id=2) | 32w × 16h letterboxed | ... | ✅ / ❌ |
| 2 | Square baseline (20th Century, id=10) | 32×32 edge-to-edge | ... | ✅ / ❌ |
| 3 | Synthetic portrait (50×100 red) | 16w × 32h pillarboxed | ... | ✅ / ❌ |
| 4 | Icon column uniformity | 32×32 across all stations | ... | ✅ / ❌ |

## Screenshots
- `uat-landscape.png` — [description]
- `uat-portrait.png` — [description]

## Plan 03 Status
- If PATH A: Plan 03 will be marked SKIPPED (no production change). Phase 54 closes after this plan.
- If PATH B-1: Plan 03 is now unblocked and will apply the `_art_paths.py` canvas-paint patch.
```
</output>
