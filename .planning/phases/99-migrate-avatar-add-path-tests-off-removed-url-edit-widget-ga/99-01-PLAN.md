---
phase: 99-migrate-avatar-add-path-tests-off-removed-url-edit-widget-ga
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_twitch_provider_assign.py
  - tests/test_edit_station_dialog_avatar.py
autonomous: true
requirements: [TEST-REGRESSION-97x89B]
gap_closure: true

must_haves:
  truths:
    - "The 8 avatar add-path tests in tests/test_twitch_provider_assign.py pass (no AttributeError: url_edit)"
    - "tests/test_edit_station_dialog_avatar.py::test_twitch_url_enables_refresh_btn passes for all 3 URL scenarios (twitch→enabled, youtube→enabled, plain-mp3→disabled)"
    - "No reference to the removed url_edit widget remains in either test file"
    - "Zero production-code files change — only the two test files are modified"
    - "The full suite returns to the v2.2 test-clean baseline: the 9 previously-failing tests now pass and no NEW failures appear beyond the 2 documented pre-existing failures"
  artifacts:
    - path: "tests/test_twitch_provider_assign.py"
      provides: "8 migrated avatar add-path tests reading URL via the streams-table / _get_canonical_url_live path"
      contains: "def test_save_derives_provider_for_blank_twitch"
    - path: "tests/test_edit_station_dialog_avatar.py"
      provides: "test_twitch_url_enables_refresh_btn migrated to streams_table + _on_canonical_cell_changed"
      contains: "_on_canonical_cell_changed"
  key_links:
    - from: "tests/test_twitch_provider_assign.py"
      to: "EditStationDialog._get_canonical_url_live"
      via: "repo.list_streams fixture pre-populates streams_table row 0 during _populate()"
      pattern: "list_streams"
    - from: "tests/test_edit_station_dialog_avatar.py"
      to: "EditStationDialog._on_canonical_cell_changed"
      via: "streams_table.item(_canonical_row, _COL_URL).setText() then _on_canonical_cell_changed(row, _COL_URL)"
      pattern: "_on_canonical_cell_changed"
---

<objective>
Restore the v2.2 test-clean baseline by migrating the 9 avatar add-path tests that fail with
`AttributeError: 'EditStationDialog' object has no attribute 'url_edit'`. Phase 97 (D-01) removed the
`url_edit` widget and made the streams table the sole URL editor; Phase 89B's tests were never migrated.
Production wiring is intact — only the automated coverage references the removed widget.

Purpose: Close the critical audit gap TEST-REGRESSION-97x89B (v2.2-MILESTONE-AUDIT.md). A clean test
baseline is the gate for the v2.2 milestone.
Output: Two migrated test files; full suite back to baseline (9 failing → 0). No production change.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/4.0.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/4.0.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-migrate-avatar-add-path-tests-off-removed-url-edit-widget-ga/99-RESEARCH.md
@.planning/phases/99-migrate-avatar-add-path-tests-off-removed-url-edit-widget-ga/99-VALIDATION.md

<interfaces>
<!-- Verified against musicstreamer/ui_qt/edit_station_dialog.py on 2026-06-28. Use directly — no exploration. -->

Module-level constants in musicstreamer/ui_qt/edit_station_dialog.py:
- `_COL_URL = 0`        (line 279 — the URL column index in streams_table)
- `_COL_CANONICAL = 6`  (line 285 — canonical-marker column; NOT the URL column)

Methods on EditStationDialog:
- `_get_canonical_url_live(self) -> str` (line 1314) — reads `streams_table.item(self._canonical_row, _COL_URL).text()`; authoritative URL read path used by `_on_save` (line 2027).
- `_on_canonical_cell_changed(self, row: int, col: int) -> None` (line 1372) — drives `_refresh_avatar_btn.setEnabled(is_yt or is_twitch)`. Has an early-exit guard: returns immediately unless `row == self._canonical_row and col == _COL_URL`.
- `_on_url_text_changed(self) -> None` (line 1554) — NO-OP SHIM after Phase 97. Calling it does nothing; it does NOT enable the refresh button. Do not call it in migrated tests.
- `self._canonical_row` — set to 0 in `_populate()` (line 720). All fixtures here have `canonical_stream_id=None`, so it is always 0 after construction.

The `repo` fixture in tests/test_twitch_provider_assign.py returns a stream with
`url="https://www.twitch.tv/twitchdev"` via `list_streams`, which `_populate()` loads into
streams_table row 0 — so `_get_canonical_url_live()` already returns the correct URL after dialog
construction. The `url_edit.setText(...)` calls are therefore redundant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test_twitch_provider_assign.py (Pattern A — delete 8 redundant url_edit lines)</name>
  <files>tests/test_twitch_provider_assign.py</files>
  <read_first>
    - tests/test_twitch_provider_assign.py (full file — the 8 failing tests and the shared `repo` fixture at lines 42–54)
    - tests/test_edit_station_dialog.py lines 515–528, 581–596, 611–626 (already-passing analog: streams-table URL-setting pattern post-Phase-97)
    - musicstreamer/ui_qt/edit_station_dialog.py lines 1314–1325 (`_get_canonical_url_live`), 720 (`_canonical_row = 0` in `_populate`), 2020–2033 (`_on_save` reads URL via `_get_canonical_url_live().strip()`) — confirm signatures against current code before editing.
  </read_first>
  <action>
    Delete the 8 lines that call `d.url_edit.setText(...)` — currently at lines 116, 171, 224, 268,
    313, 351, 391, 426 (one per failing test). These are redundant: the shared `repo` fixture supplies
    the URL via `list_streams`, which `_populate()` loads into `streams_table` row 0; `_on_save` reads
    it via `_get_canonical_url_live()` (Phase 97 D-02). For `test_save_non_twitch_url_unchanged` (line
    224) the fixture is overridden to the same somafm URL the line set, so deletion is still correct.
    Optionally replace each deleted line with a one-line comment noting the URL is pre-loaded from
    `repo.list_streams` via `_populate()`. Do NOT touch the two already-passing source-grep tests
    `test_save_source_has_twitch_derivation` (line 68) and `test_on_save_has_inmemory_provider_assignment`
    (line 444) — they inspect source and have no dialog. Do NOT add a `set_canonical_url` helper
    (over-engineering per RESEARCH Don't-Hand-Roll). No production code changes.
  </action>
  <acceptance_criteria>
    - `grep -c 'url_edit' tests/test_twitch_provider_assign.py` returns 0
    - `.venv/bin/python -m pytest tests/test_twitch_provider_assign.py -q` exits 0 (10 passed: 8 migrated + 2 pre-existing source-grep tests)
    - `git diff --name-only` lists only tests/test_twitch_provider_assign.py (no production file changed)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'url_edit' tests/test_twitch_provider_assign.py | grep -qx 0 && .venv/bin/python -m pytest tests/test_twitch_provider_assign.py -q</automated>
  </verify>
  <done>All 8 url_edit lines removed; the file's 10 tests pass; no production file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate test_twitch_url_enables_refresh_btn (Pattern B — canonical cell + _on_canonical_cell_changed)</name>
  <files>tests/test_edit_station_dialog_avatar.py</files>
  <read_first>
    - tests/test_edit_station_dialog_avatar.py (full file — the failing test body at lines 75–105, its docstring at 76–82, and the `twitch_dialog` fixture)
    - tests/test_edit_station_dialog.py lines 515–528, 581–596, 611–626 (reference: `streams_table.item(row, _COL_URL).setText(...)` + `_on_canonical_cell_changed`)
    - musicstreamer/ui_qt/edit_station_dialog.py lines 279 (`_COL_URL = 0`), 1372–1403 (`_on_canonical_cell_changed` incl. its `row/col` guard), 1554–1558 (`_on_url_text_changed` no-op shim) — confirm against current code.
  </read_first>
  <action>
    In `test_twitch_url_enables_refresh_btn`, replace each of the 3 `d.url_edit.setText(url)` +
    `d._on_url_text_changed()` pairs (lines 87–88, 95–96, 102–103) with
    `d.streams_table.item(d._canonical_row, _COL_URL).setText(url)` followed by
    `d._on_canonical_cell_changed(d._canonical_row, _COL_URL)`. Do NOT call `_on_url_text_changed()` —
    it is a no-op shim and will leave the button disabled (RESEARCH Pitfall 1). Pass `_canonical_row`
    (== 0 here) and `_COL_URL` exactly; a wrong row or col silently no-ops the guard (Pitfall 2). Add the
    imports `from musicstreamer.ui_qt.edit_station_dialog import _COL_URL` and (if not already present)
    `from PySide6.QtWidgets import QTableWidgetItem` near the top of the file; the `twitch_dialog`
    fixture's row 0 already has a non-None item so the defensive `if item is None: setItem(...)` guard is
    not required. Keep all 3 existing assertions and their messages (twitch→True, youtube→True,
    plain-mp3→False). Update the docstring (lines 76–82) to describe the new mechanism — replace the
    `url_edit` / `_on_url_text_changed` wording with "setting the canonical streams-table URL cell and
    firing `_on_canonical_cell_changed`". No production code changes.
  </action>
  <acceptance_criteria>
    - `grep -c 'url_edit' tests/test_edit_station_dialog_avatar.py` returns 0
    - `grep -c '_on_url_text_changed' tests/test_edit_station_dialog_avatar.py` returns 0
    - `.venv/bin/python -m pytest tests/test_edit_station_dialog_avatar.py::test_twitch_url_enables_refresh_btn -q` exits 0
    - `.venv/bin/python -m pytest tests/test_edit_station_dialog_avatar.py -q` exits 0 (3 passed: 1 migrated + 2 pre-existing worker tests)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'url_edit\|_on_url_text_changed' tests/test_edit_station_dialog_avatar.py | grep -qx 0 && .venv/bin/python -m pytest tests/test_edit_station_dialog_avatar.py -q</automated>
  </verify>
  <done>The failing test passes for all 3 URL scenarios; no url_edit/_on_url_text_changed references remain; docstring updated; no production file touched.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate — confirm return to baseline</name>
  <files>tests/test_twitch_provider_assign.py, tests/test_edit_station_dialog_avatar.py</files>
  <read_first>
    - .planning/phases/99-migrate-avatar-add-path-tests-off-removed-url-edit-widget-ga/99-VALIDATION.md (Sampling Rate + Per-Task Verification Map)
    - Project memory note run-tests-with-venv-python.md: full suite >600s; system python3 yields false failures; two documented PRE-EXISTING failures unrelated to this phase.
  </read_first>
  <action>
    Run the full suite once as the baseline gate with `.venv/bin/python -m pytest -q` (allow up to
    600s timeout). Confirm the 9 previously-failing avatar add-path tests now pass and that NO NEW
    failures appear beyond the 2 documented pre-existing failures. If the run surfaces exactly the 2
    known pre-existing failures and nothing else, the baseline is restored — record their names/ids in
    the SUMMARY so they are not mistaken for regressions. If any failure other than those 2 appears,
    investigate whether the migration introduced it before declaring the phase done. Do NOT modify
    production code or unrelated tests to make the 2 pre-existing failures pass — they are out of scope
    for this gap-closure phase.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_twitch_provider_assign.py tests/test_edit_station_dialog_avatar.py -q` exits 0 (13 passed)
    - Full-suite run `.venv/bin/python -m pytest -q` shows the 9 previously-failing avatar add-path tests passing and no NEW failures beyond the 2 documented pre-existing ones
    - `git diff --name-only` lists only the two test files (zero production-code changes across the phase)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_twitch_provider_assign.py tests/test_edit_station_dialog_avatar.py -q</automated>
    <human-check>Full suite `.venv/bin/python -m pytest -q` returns to baseline: only the 2 documented pre-existing failures remain; record their ids in the SUMMARY.</human-check>
  </verify>
  <done>Quick-run command passes (13 tests); full suite back to baseline with only the 2 known pre-existing failures; no production file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase modifies only automated test files. No new production code path, no I/O, no network, no untrusted input, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | tests/* test-file edits | accept | Test-only change; no production code, no runtime data flow. Coverage scope is preserved (the 2 source-grep tests and 2 worker tests are left untouched), so the change cannot silently weaken assertions. |
| T-99-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed; no dependency surface changes. RESEARCH Package Legitimacy Audit: "Not applicable." |

**Conclusion:** No threats at or above `high` severity. The change is confined to automated test files,
introduces no production code path, no I/O, and no untrusted input. No ASVS L1 control is engaged.
</threat_model>

<verification>
- `grep -c 'url_edit' tests/test_twitch_provider_assign.py tests/test_edit_station_dialog_avatar.py` returns 0 for both files
- `.venv/bin/python -m pytest tests/test_twitch_provider_assign.py tests/test_edit_station_dialog_avatar.py -q` exits 0 (13 passed)
- Full suite `.venv/bin/python -m pytest -q` returns to baseline: 9 previously-failing tests pass; only the 2 documented pre-existing failures remain
- `git diff --name-only` lists exactly the two test files — zero production-code changes
</verification>

<success_criteria>
- All 9 previously-failing avatar add-path tests pass via the streams-table / `_get_canonical_url_live` path
- The 4 previously-passing tests in the two files still pass (13 total green)
- No reference to `url_edit` or the no-op `_on_url_text_changed` shim remains in either test file
- Full suite back to the v2.2 test-clean baseline (no NEW failures beyond the 2 documented pre-existing ones)
- Zero production-code files modified
</success_criteria>

<output>
Create `.planning/phases/99-migrate-avatar-add-path-tests-off-removed-url-edit-widget-ga/99-01-SUMMARY.md` when done.
Record the ids/names of the 2 documented pre-existing failures so they are not mistaken for regressions.
</output>
