---
phase: 79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w
plan: 02
type: execute
wave: 2
depends_on:
  - 79-01
files_modified:
  - musicstreamer/player.py
  - musicstreamer/__main__.py
  - tests/test_player_node_runtime.py
autonomous: true
requirements:
  - BUG-11

must_haves:
  truths:
    - "Player(node_runtime=NodeRuntime(available=True, path='/fake/node'))._youtube_resolve_worker passes opts['js_runtimes']['node']['path'] == '/fake/node' to yt_dlp.YoutubeDL (B-79-04)"
    - "Player(node_runtime=None)._youtube_resolve_worker passes opts['js_runtimes']['node']['path'] is None (backwards-compat: tests/test_cookies.py:157,190 stay green by construction) (B-79-05)"
    - "Player(node_runtime=NodeRuntime(available=False, path=None))._youtube_resolve_worker passes opts['js_runtimes']['node']['path'] is None (B-79-06)"
    - "_youtube_resolve_worker emits one INFO log line 'youtube resolve: node_path=<abs|None>' per call (D-13)"
    - "__main__._run_gui passes the existing node_runtime to Player(node_runtime=node_runtime) (D-07)"
    - "musicstreamer.yt_import logger is escalated to INFO in __main__.main alongside musicstreamer.player and musicstreamer.soma_import (Open Question 2 — POSITIVE answer per RESEARCH.md)"
    - "Phase 999.7 cookie temp-copy invariant preserved: yt_dlp.YoutubeDL construction still nests INSIDE cookie_utils.temp_cookies_copy ctxmgr (no reordering)"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "Player.__init__ accepts node_runtime kwarg; _youtube_resolve_worker uses yt_dlp_opts.build_js_runtimes and emits one INFO log per call"
      contains: "yt_dlp_opts.build_js_runtimes"
    - path: "musicstreamer/__main__.py"
      provides: "Threads node_runtime to Player(); escalates musicstreamer.yt_import logger to INFO"
      contains: "Player(node_runtime=node_runtime)"
    - path: "tests/test_player_node_runtime.py"
      provides: "Three-input regression matrix for _youtube_resolve_worker opts recording (B-79-04, B-79-05, B-79-06)"
      contains: "test_youtube_resolve_passes_node_path_when_available"
  key_links:
    - from: "musicstreamer/player.py"
      to: "musicstreamer.yt_dlp_opts.build_js_runtimes"
      via: "module-level import + call inside _youtube_resolve_worker opts dict"
      pattern: "yt_dlp_opts\\.build_js_runtimes\\("
    - from: "musicstreamer/__main__.py"
      to: "musicstreamer.player.Player"
      via: "Player(node_runtime=node_runtime) at line 220"
      pattern: "Player\\(node_runtime=node_runtime\\)"
    - from: "musicstreamer/player.py:_youtube_resolve_worker"
      to: "musicstreamer.cookie_utils.temp_cookies_copy"
      via: "ctxmgr wraps yt_dlp.YoutubeDL construction (Phase 999.7 invariant — UNCHANGED)"
      pattern: "with cookie_utils\\.temp_cookies_copy\\(\\) as cookiefile"
---

<objective>
Wire `NodeRuntime` into `Player._youtube_resolve_worker` via constructor dependency injection per D-05 / D-06 / D-07, replacing the inline `{"node": {"path": None}}` literal at `player.py:1063` with a call to `yt_dlp_opts.build_js_runtimes(self._node_runtime)` from Plan 79-01. Add one INFO log line per YT play per D-13. Thread the existing `node_runtime` from `__main__._run_gui` (already detected at line 215) into `Player()` at line 220. Also escalate the `musicstreamer.yt_import` logger to INFO in `__main__.main` so the Plan 79-03 INFO line surfaces at default verbosity.

Purpose: This is half of the BUG-11 fix — the Player playback path. After this plan, `.desktop`-launched MusicStreamer with fnm/nvm/volta/asdf-provided Node will pass an absolute path into yt-dlp's NodeJsRuntime resolver, eliminating the "stream exhausted" failure on JS-requiring YouTube streams. Backwards-compat is preserved by construction: `Player()` (no kwarg) continues to yield `js_runtimes == {"node": {"path": None}}`, which is exactly what `tests/test_cookies.py:157,190` already assert.

Output: Three files modified, one new test file. Helper-call wiring is single source of truth (D-10) — Plan 79-03 mirrors this shape for the scan-side.

Note on `__main__.py` ownership: This plan owns BOTH the Player-ctor kwarg pass-through (line 220) AND the `musicstreamer.yt_import` logger escalation (alongside existing line 236) — they touch the same file, so concentrating them in one plan keeps Wave 2 file-ownership disjoint from Plan 79-03 (which touches `yt_import.py`, `ui_qt/import_dialog.py`, `ui_qt/main_window.py`, and `tests/test_yt_import_library.py`). The yt_import logger escalation lands here even though the consumer is Plan 79-03 — it's a one-line addition documented as a Plan-79-03 dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-CONTEXT.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-VALIDATION.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From musicstreamer/runtime_check.py:36-39:
```python
@dataclass(frozen=True)
class NodeRuntime:
    available: bool
    path: Optional[str]
```

From musicstreamer/yt_dlp_opts.py (Plan 79-01 output):
```python
def build_js_runtimes(node_runtime: NodeRuntime | None) -> dict:
    # returns {"node": {"path": <abs-path-or-None>}}
```

From musicstreamer/player.py:281 (current):
```python
def __init__(self, parent: QObject | None = None) -> None:
```

From musicstreamer/player.py:1021 (current):
```python
def _youtube_resolve_worker(self, url: str) -> None:
```

From musicstreamer/player.py:1053-1071 (the opts dict to edit; line 1063 is the literal to substitute):
```python
opts = {
    "quiet": True,
    "no_warnings": True,
    "skip_download": True,
    "format": "best[protocol^=m3u8]/bestaudio/best",
    # Phase 999.9 comment block (KEEP) ...
    "js_runtimes": {"node": {"path": None}},   # <-- line 1063, SUBSTITUTE
    # BUG-YT-COOKIES comment block (KEEP) ...
    "remote_components": {"ejs:github"},
}
```

From musicstreamer/__main__.py:215-222 (current):
```python
node_runtime = runtime_check.check_node()
if not node_runtime.available:
    runtime_check.show_missing_node_dialog(parent=None)
# ...
player = Player()                                       # <-- line 220, edit
window = MainWindow(player, repo, node_runtime=node_runtime)
```

From musicstreamer/__main__.py:231-236 (current per-logger INFO escalations):
```python
logging.basicConfig(level=logging.WARNING)
# ...
logging.getLogger("musicstreamer.player").setLevel(logging.INFO)
logging.getLogger("musicstreamer.soma_import").setLevel(logging.INFO)
# <-- INSERT new line here: musicstreamer.yt_import
```

From tests/test_cookies.py:157, 190 (BACKWARDS-COMPAT INVARIANT — MUST stay green):
```python
assert captured_opts.get("js_runtimes") == {"node": {"path": None}}
```

From tests/test_cookies.py:21-30 (the `make_player(qtbot)` helper — DO NOT MODIFY; default no-arg `Player()` is the path that keeps lines 157, 190 green):
```python
def make_player(qtbot):
    from musicstreamer.player import Player
    mock_pipeline = MagicMock()
    mock_pipeline.get_bus.return_value = MagicMock()
    with patch("musicstreamer.player.Gst.ElementFactory.make", return_value=mock_pipeline):
        player = Player()
    player._pipeline = MagicMock()
    return player
```

From musicstreamer/player.py:76-78 (module logger — already exists):
```python
_log = logging.getLogger(__name__)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Player.__init__ node_runtime kwarg + rewire _youtube_resolve_worker opts + add INFO log; update __main__.py to thread node_runtime to Player() and add yt_import logger setLevel</name>
  <files>musicstreamer/player.py, musicstreamer/__main__.py</files>
  <read_first>
    - musicstreamer/player.py (full file, especially lines 76-78 module logger; line 281 Player.__init__; line 437 self._tracker init for placement reference; lines 1021-1098 _youtube_resolve_worker including lines 1053-1071 opts dict and the Phase 999.7 ctxmgr at line 1078).
    - musicstreamer/__main__.py:163-240 (the _run_gui body — line 215 node_runtime detection; line 220 Player() construction; lines 231-236 per-logger INFO setLevel block).
    - musicstreamer/ui_qt/main_window.py:190-202 — the verbatim ctor-kwarg pattern to mirror on Player (`*, node_runtime=None` keyword-only after the existing parent kwarg, stored as `self._node_runtime = node_runtime`).
    - musicstreamer/runtime_check.py:36-39 — NodeRuntime dataclass shape (for the import + type annotation).
    - musicstreamer/cookie_utils.py — confirms the existing `temp_cookies_copy()` ctxmgr that wraps yt_dlp.YoutubeDL at player.py:1078 (Phase 999.7 invariant; MUST NOT be reordered or unwrapped).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §2 (player.py edit map: kwarg placement, opts substitution line, INFO log placement, import line) and §4 (__main__.py edit map: line 220 token swap + line 237 logger insert).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md §"Common Pitfalls" Pitfall 1 (test_cookies.py backwards-compat invariant), Pitfall 3 (explicit conditional, NOT `and`), Pitfall 4 (runtime_check import is non-circular).
    - tests/test_cookies.py:21-30, 144-158, 184-190 (the two existing `js_runtimes == {"node": {"path": None}}` assertions; verify they stay green by construction after edits).
  </read_first>
  <behavior>
    - `Player.__init__` accepts a keyword-only `node_runtime: NodeRuntime | None = None` parameter (D-05, D-06), stored as `self._node_runtime` adjacent to other instance state (next to the existing tracker init around line 437 is acceptable, but immediately after `super().__init__(parent)` is also fine — placement is executor's discretion within the __init__ body).
    - Default `node_runtime=None` MUST preserve `js_runtimes == {"node": {"path": None}}` behavior: `make_player(qtbot)` constructs `Player()` with no args → `self._node_runtime is None` → `build_js_runtimes(None) == {"node": {"path": None}}`. Pre-existing `tests/test_cookies.py:157` and `tests/test_cookies.py:190` assertions stay green.
    - `_youtube_resolve_worker` body change is minimally invasive: (a) at line 1063 the value of the `"js_runtimes"` key is changed from the inline dict literal to `yt_dlp_opts.build_js_runtimes(self._node_runtime)`; (b) ONE new `_log.info("youtube resolve: node_path=%s", node_path)` call is inserted near the top of the worker, AFTER the cookies-corruption block at lines 1042-1051 but BEFORE the `opts = {` literal at line 1053; (c) `node_path` is computed as `self._node_runtime.path if self._node_runtime else None` (explicit conditional, Pitfall 3 — NOT `self._node_runtime and self._node_runtime.path`).
    - The Phase 999.7 `cookie_utils.temp_cookies_copy()` ctxmgr at line 1078 stays UNCHANGED in position and structure — `yt_dlp.YoutubeDL` construction continues to nest INSIDE the ctxmgr. No reordering.
    - Imports added to player.py: (a) `yt_dlp_opts` added to the existing `from musicstreamer import constants, cookie_utils, paths` import line (alphabetical insertion → `from musicstreamer import constants, cookie_utils, paths, yt_dlp_opts`); (b) `from musicstreamer.runtime_check import NodeRuntime` added at module level (direct import — Pitfall 4 verified non-circular). The Player ctor annotation uses forward-reference string `"NodeRuntime | None"` to avoid forcing a re-order of imports.
    - `__main__.py:220` changes from `player = Player()` to `player = Player(node_runtime=node_runtime)` (D-07). `node_runtime` is already in scope from line 215.
    - `__main__.py` per-logger setLevel block (currently lines 235-236) gains a third line: `logging.getLogger("musicstreamer.yt_import").setLevel(logging.INFO)` inserted AFTER the existing `musicstreamer.soma_import` line. Comment above the new line: `# Phase 79: surface scan_playlist node_path INFO line at default verbosity (consumed by Plan 79-03).`
    - No other lines in `__main__.py` change. No other lines in `player.py` change.
  </behavior>
  <action>
Edit `musicstreamer/player.py`:
1. Add `yt_dlp_opts` to the existing `from musicstreamer import constants, cookie_utils, paths` import line, in alphabetical order. The result is `from musicstreamer import constants, cookie_utils, paths, yt_dlp_opts`. Add `from musicstreamer.runtime_check import NodeRuntime` on a new module-level import line in the existing `musicstreamer.*` import group (direct import is safe — runtime_check.py does not import from player.py or yt_dlp_opts.py).
2. At `Player.__init__` (line 281, current signature `def __init__(self, parent: QObject | None = None) -> None:`), change the signature to add a keyword-only `node_runtime` parameter after `parent`: `def __init__(self, parent: QObject | None = None, *, node_runtime: "NodeRuntime | None" = None) -> None:`. Use the forward-reference string (quotes) for the annotation to avoid any future import-order brittleness. Inside the body, store `self._node_runtime = node_runtime` next to the existing instance attribute initialization (the line immediately after `super().__init__(parent)` is the canonical placement, mirroring `MainWindow.__init__:202`).
3. In `_youtube_resolve_worker` at line 1021, insert the INFO log block AFTER the cookies-corruption-emit at line 1051 (the `self.cookies_cleared.emit(...)` line, end of that conditional) and BEFORE the `opts = {` literal at line 1053. The block is exactly:
   ```
   node_path = self._node_runtime.path if self._node_runtime else None
   _log.info("youtube resolve: node_path=%s", node_path)
   ```
   Use the explicit conditional form (Pitfall 3 — `if self._node_runtime else None`, NOT `and`).
4. At line 1063, substitute the inline literal value `{"node": {"path": None}}` for the helper call `yt_dlp_opts.build_js_runtimes(self._node_runtime)`. The new line reads: `"js_runtimes": yt_dlp_opts.build_js_runtimes(self._node_runtime),`. KEEP the multi-line `# Phase 999.9` comment block above unchanged (it documents WHY js_runtimes is set; replacing the value doesn't invalidate the rationale).
5. Do NOT modify the `with cookie_utils.temp_cookies_copy() as cookiefile:` ctxmgr at line 1078 — Phase 999.7 invariant. Do NOT reorder lines 1042-1078. Do NOT touch `_play_youtube` (the caller of the worker).

Edit `musicstreamer/__main__.py`:
1. At line 220, change `player = Player()` to `player = Player(node_runtime=node_runtime)`. `node_runtime` is already in scope from line 215 (`node_runtime = runtime_check.check_node()`). No new import needed.
2. Insert a new logger setLevel line AFTER line 236 (`logging.getLogger("musicstreamer.soma_import").setLevel(logging.INFO)`):
   ```
   # Phase 79 / BUG-11: surface scan_playlist node_path INFO line at default
   # verbosity (consumed by Plan 79-03's yt_import.scan_playlist INFO log).
   logging.getLogger("musicstreamer.yt_import").setLevel(logging.INFO)
   ```
3. Do NOT modify line 39 (`player = Player()` inside `_run_smoke`) — the smoke harness intentionally does NOT exercise YouTube and keeps the default `node_runtime=None` per D-06 (backwards-compat).

After edits, run `uv run --with pytest pytest tests/test_cookies.py tests/test_player.py -x` to confirm pre-existing assertions stay green by construction (default `node_runtime=None` → `build_js_runtimes(None) == {"node": {"path": None}}` → both lines at test_cookies.py:157,190 still match).
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_cookies.py tests/test_player.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/player.py` imports `yt_dlp_opts`: `grep -E "from musicstreamer import .*yt_dlp_opts" musicstreamer/player.py` returns at least one match.
    - `musicstreamer/player.py` imports `NodeRuntime`: `grep -q "from musicstreamer.runtime_check import NodeRuntime" musicstreamer/player.py`.
    - `Player.__init__` signature contains `node_runtime`: `grep -E "def __init__\\(self,.*node_runtime" musicstreamer/player.py` returns one match.
    - `Player.__init__` stores `self._node_runtime`: `grep -q "self._node_runtime = node_runtime" musicstreamer/player.py`.
    - `_youtube_resolve_worker` calls `yt_dlp_opts.build_js_runtimes(self._node_runtime)`: `grep -c "yt_dlp_opts.build_js_runtimes(self._node_runtime)" musicstreamer/player.py` returns `1`.
    - The previous inline literal `{"node": {"path": None}}` is GONE from `player.py`: `grep -c '"node": {"path": None}' musicstreamer/player.py` returns `0` (the literal lives ONLY in `yt_dlp_opts.py` and its test now).
    - INFO log line is present in `_youtube_resolve_worker`: `grep -c 'youtube resolve: node_path=' musicstreamer/player.py` returns `1`.
    - INFO log uses explicit conditional (Pitfall 3): `grep -E "self._node_runtime\\.path if self._node_runtime else None" musicstreamer/player.py` returns at least one match.
    - Phase 999.7 invariant preserved: `grep -c "with cookie_utils.temp_cookies_copy() as cookiefile" musicstreamer/player.py` returns `1` (ctxmgr unchanged).
    - `__main__.py:220` updated: `grep -E "player = Player\\(node_runtime=node_runtime\\)" musicstreamer/__main__.py` returns at least one match.
    - `__main__.py` adds yt_import logger setLevel: `grep -c 'getLogger\\("musicstreamer.yt_import"\\)\\.setLevel(logging.INFO)' musicstreamer/__main__.py` returns `1`.
    - Existing tests stay green: `uv run --with pytest pytest tests/test_cookies.py::test_youtube_resolve_strips_cookie_marker_and_emits_signal tests/test_cookies.py::test_youtube_resolve_uses_temp_cookies_when_canonical_present -x` exits 0. (These map to the `tests/test_cookies.py:157,190` assertions that this plan must preserve by construction.)
    - Full Player test suite stays green: `uv run --with pytest pytest tests/test_player.py -x` exits 0.
    - `_run_smoke` Player() call at __main__.py:39 is UNCHANGED: `grep -n "player = Player()" musicstreamer/__main__.py` returns exactly one match (line 39, _run_smoke — NOT line 220).
  </acceptance_criteria>
  <done>
    `Player.__init__` accepts `node_runtime` kwarg; `_youtube_resolve_worker` uses `yt_dlp_opts.build_js_runtimes(self._node_runtime)` and emits the INFO log; `__main__._run_gui` threads `node_runtime` to `Player()`; `__main__.main` escalates the `musicstreamer.yt_import` logger to INFO. Phase 999.7 cookie-temp-copy invariant unchanged. Pre-existing `tests/test_cookies.py:157,190` assertions stay green by construction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_player_node_runtime.py with B-79-04, B-79-05, B-79-06 regression matrix</name>
  <files>tests/test_player_node_runtime.py</files>
  <read_first>
    - tests/test_cookies.py:21-30, 120-191 — the `make_player(qtbot)` helper (DO NOT MODIFY) and the verbatim `FakeYDL` opts-recording pattern. The new test file mirrors this opts-recording shape but constructs `Player(node_runtime=...)` inline (NOT via `make_player`) for the available/unavailable cases.
    - musicstreamer/player.py — the updated `Player.__init__` signature (`*, node_runtime=None`) and `_youtube_resolve_worker` body (from Task 1).
    - musicstreamer/runtime_check.py:36-39 — `NodeRuntime` dataclass for inline literals.
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §8 — verbatim test template (FakeYDL inner class per test, mock pipeline factory, opts capture). New file location rationale: tests/test_player.py is 577 lines; scope-isolation file per RESEARCH.md Open Question 4 recommendation.
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-VALIDATION.md — B-79-04, B-79-05, B-79-06 behaviors and exact pytest test names.
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md §"Code Examples" Examples 5 — full test-file reference.
  </read_first>
  <behavior>
    - `test_youtube_resolve_passes_node_path_when_available(qtbot, tmp_path, monkeypatch)`: constructs `Player(node_runtime=NodeRuntime(available=True, path="/fake/node"))` with a mocked GStreamer pipeline factory, mocks `yt_dlp.YoutubeDL` with a FakeYDL that records opts on `__init__`, calls `player._youtube_resolve_worker("https://youtube.com/watch?v=test")`, asserts `captured_opts["js_runtimes"] == {"node": {"path": "/fake/node"}}` (B-79-04).
    - `test_youtube_resolve_passes_none_when_no_node_runtime(qtbot, tmp_path, monkeypatch)`: constructs `Player()` (default `node_runtime=None`) via the existing `make_player(qtbot)` helper (re-imported from `tests.test_cookies`), same FakeYDL pattern, asserts `captured_opts["js_runtimes"] == {"node": {"path": None}}` (B-79-05 — backwards-compat).
    - `test_youtube_resolve_passes_none_when_unavailable(qtbot, tmp_path, monkeypatch)`: constructs `Player(node_runtime=NodeRuntime(available=False, path=None))`, same FakeYDL pattern, asserts `captured_opts["js_runtimes"] == {"node": {"path": None}}` (B-79-06).
    - All three tests use `monkeypatch.setattr(paths, "_root_override", str(tmp_path))` so the cookies-corruption check at the top of `_youtube_resolve_worker` cannot raise on the actual user's cookies file.
    - All three tests use `with patch("musicstreamer.player.Gst.ElementFactory.make", return_value=mock_pipeline):` to mock the GStreamer pipeline factory at Player construction time (mirrors `make_player` shape).
    - File is independent of `tests/test_player.py` (scope-isolation per RESEARCH.md OQ 4); imports `make_player` for the B-79-05 backwards-compat case so the no-arg branch matches the existing `tests/test_cookies.py:157,190` regression-lock path.
  </behavior>
  <action>
Create `tests/test_player_node_runtime.py` as a new scope-isolated test file (RESEARCH.md OQ 4 — `tests/test_player.py` is 577 lines, growing it is fine but a new file keeps regression-grep on "Phase 79" / "BUG-11" / "node_runtime" trivial).

Module docstring: "Tests for Player node_runtime kwarg + _youtube_resolve_worker opts threading (Phase 79 / BUG-11). Three-input regression matrix locks the B-79-04..B-79-06 contract."

After docstring: `from __future__ import annotations` (project convention).

Imports needed:
- `from unittest.mock import MagicMock, patch`
- `from musicstreamer.player import Player`
- `from musicstreamer.runtime_check import NodeRuntime`
- `from tests.test_cookies import make_player` — DO NOT redeclare this helper; the existing one is the canonical default-no-arg Player factory and is shared by other test files (test_cookies, test_player, etc.).

Three test functions. Each follows the FakeYDL pattern verbatim from `tests/test_cookies.py:130-146`:

```
def _make_fake_ydl():
    """Return (FakeYDL_class, captured_opts_dict). Closure captures opts on __init__."""
    captured: dict = {}
    class FakeYDL:
        def __init__(self, opts):
            captured.update(opts)
        def __enter__(self):
            return self
        def __exit__(self, *a):
            return False
        def extract_info(self, url, download=False):
            return {"url": "http://resolved.example/stream.m3u8"}
    return FakeYDL, captured
```

A small private helper `_make_player_with_node(qtbot, node_runtime)` constructs `Player(node_runtime=node_runtime)` with a mocked Gst pipeline factory (mirrors the body of `make_player` from `test_cookies.py:21-30` but takes a `node_runtime` argument). Three lines of body: build `mock_pipeline = MagicMock()`, set `mock_pipeline.get_bus.return_value = MagicMock()`, then `with patch("musicstreamer.player.Gst.ElementFactory.make", return_value=mock_pipeline): player = Player(node_runtime=node_runtime); player._pipeline = MagicMock(); return player`. The `player._pipeline = MagicMock()` line mirrors `make_player` line 30 — required so subsequent emit calls in `_youtube_resolve_worker` don't trip on a real pipeline.

Test 1 `test_youtube_resolve_passes_node_path_when_available(qtbot, tmp_path, monkeypatch)`:
- `monkeypatch.setattr(paths, "_root_override", str(tmp_path))` (import `from musicstreamer import paths` at the top)
- `nr = NodeRuntime(available=True, path="/fake/node")`
- `player = _make_player_with_node(qtbot, nr)`
- `FakeYDL, captured = _make_fake_ydl()`
- `import yt_dlp; with patch.object(yt_dlp, "YoutubeDL", FakeYDL): player._youtube_resolve_worker("https://youtube.com/watch?v=test")`
- `assert captured["js_runtimes"] == {"node": {"path": "/fake/node"}}`

Test 2 `test_youtube_resolve_passes_none_when_no_node_runtime(qtbot, tmp_path, monkeypatch)`:
- Same monkeypatch line.
- `player = make_player(qtbot)` (existing helper — default `Player()` with `node_runtime=None`)
- Same FakeYDL pattern.
- `assert captured["js_runtimes"] == {"node": {"path": None}}`
- Add a one-line docstring noting this is the B-79-05 backwards-compat case: same shape `tests/test_cookies.py:157,190` already assert; this test pins the invariant in the Phase 79 regression file too.

Test 3 `test_youtube_resolve_passes_none_when_unavailable(qtbot, tmp_path, monkeypatch)`:
- Same monkeypatch.
- `nr = NodeRuntime(available=False, path=None)`
- `player = _make_player_with_node(qtbot, nr)`
- Same FakeYDL pattern.
- `assert captured["js_runtimes"] == {"node": {"path": None}}`

DO NOT modify `tests/test_cookies.py` or `tests/test_player.py`. The `make_player` helper is shared and the existing assertions at `test_cookies.py:157, 190` MUST stay green by construction (Pitfall 1 / Open Question A6) — verified in Task 1's acceptance criteria.

DO NOT use `pytest.fixture` or `pytest.mark.parametrize` — three independent tests with shared helper, mirroring `tests/test_cookies.py` style.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_player_node_runtime.py tests/test_cookies.py tests/test_player.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_player_node_runtime.py` exists.
    - File defines exactly three `test_` functions matching VALIDATION.md names: `grep -E "^def test_(youtube_resolve_passes_node_path_when_available|youtube_resolve_passes_none_when_no_node_runtime|youtube_resolve_passes_none_when_unavailable)\(" tests/test_player_node_runtime.py` returns three matches.
    - File imports `make_player` from `tests.test_cookies`: `grep -q "from tests.test_cookies import make_player" tests/test_player_node_runtime.py`.
    - File does NOT redeclare `make_player`: `grep -c "^def make_player(" tests/test_player_node_runtime.py` returns `0`.
    - File imports `NodeRuntime`: `grep -q "from musicstreamer.runtime_check import NodeRuntime" tests/test_player_node_runtime.py`.
    - Three new tests pass: `uv run --with pytest pytest tests/test_player_node_runtime.py -x` exits 0 with `3 passed`.
    - Backwards-compat regression-lock holds: `uv run --with pytest pytest tests/test_cookies.py -x` exits 0 (Pitfall 1 invariant — pre-existing assertions at lines 157, 190 stay green).
    - Full Player suite still green: `uv run --with pytest pytest tests/test_player.py -x` exits 0 (no regression).
    - No `pytest.fixture` or `pytest.mark.parametrize` decorators (per Task 1 / Plan 79-01 inline-literal convention): `grep -E "@pytest\\.(fixture|mark)" tests/test_player_node_runtime.py` returns no matches.
  </acceptance_criteria>
  <done>
    Three regression tests pass: B-79-04, B-79-05, B-79-06. Phase 999.7 / Phase 999.9 / Phase 62 invariants all preserved. Pre-existing `tests/test_cookies.py` assertions at lines 157 and 190 stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process → yt-dlp subprocess | `yt_dlp.YoutubeDL` opts dict carries `js_runtimes["node"]["path"] = <abs-path>` which yt-dlp passes as `argv[0]` to `subprocess.run([<path>, "--version"])`. The path crosses from MusicStreamer's process state into a subprocess invocation. |
| in-process → stderr (logs) | New `_log.info("youtube resolve: node_path=%s", node_path)` line emits the resolved Node path to stderr (default Python logging handler). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering / Elevation of Privilege | `subprocess.run([<node_path>, "--version"])` invocation inside yt-dlp's `NodeJsRuntime._info` (path threaded from `self._node_runtime.path`) | mitigate (by construction) | `NodeRuntime.path` is sourced exclusively from `runtime_check._which_node` (POSIX: `shutil.which("node")`; constructs candidates from `os.path.join(home, "<known-vendor>", "bin", "node")` literals) and `_which_node_version_manager_fallback` (fnm/nvm/volta/asdf — same construction shape). No env-var input, no user-supplied input. yt-dlp uses argv as a list (no shell), so even shell metacharacters in a path would not be interpreted. **No new attack surface** — yt-dlp would invoke `node` either way; the only change is whether yt-dlp's own PATH lookup finds it (today) vs whether we hand it the resolved absolute path (after Phase 79). |
| T-79-02 | Information Disclosure | `_log.info("youtube resolve: node_path=%s", <path>)` emits the home-directory-anchored install location to stderr | accept | Consistent with existing `runtime_check._log.debug("Node.js detected at %s", path)` precedent at runtime_check.py:115 (same data, lower level). Logs go to stderr only — no file logging is configured anywhere in the app. User running the binary already knows their own home directory. No remote shipping of logs. |

**Verdict (ASVS L1):** No HIGH-severity threats. T-79-01 is structurally mitigated (path provenance is trusted in-tree code; argv-list shape rules out injection). T-79-02 is accepted (existing precedent + local-only stderr). Phase 79's overall threat surface is essentially zero — the change threads an absolute path (already resolved by `runtime_check.check_node()`) into yt-dlp's `js_runtimes` opt; yt-dlp would invoke the Node binary either way (via PATH lookup OR via absolute path), so this is structurally equivalent to the pre-Phase-79 state for an attacker. The Phase 79 fix narrows yt-dlp's failure mode (PATH lookup under stripped PATH) without widening any attack vector.
</threat_model>

<verification>
Per task <verify> blocks. Plan-level gate:
- `uv run --with pytest pytest tests/test_player_node_runtime.py tests/test_cookies.py tests/test_player.py -x` exits 0 with all tests passing (new + pre-existing).
- `grep -c '"node": {"path": None}' musicstreamer/player.py` returns `0` (the inline literal is gone from player.py — lives only in `musicstreamer/yt_dlp_opts.py` and its test file).
- `grep -c "build_js_runtimes(self._node_runtime)" musicstreamer/player.py` returns `1` (single source of truth via helper call).
- `grep -c 'getLogger("musicstreamer.yt_import").setLevel(logging.INFO)' musicstreamer/__main__.py` returns `1` (Plan 79-03's INFO log line will surface at default verbosity).
- Full test suite green: `uv run --with pytest pytest tests/ -x` exits 0.
</verification>

<success_criteria>
- `Player(node_runtime=NodeRuntime(available=True, path="/fake/node"))._youtube_resolve_worker` passes `js_runtimes == {"node": {"path": "/fake/node"}}` to yt-dlp (B-79-04).
- `Player()` no-arg construction preserves `js_runtimes == {"node": {"path": None}}` — `tests/test_cookies.py:157,190` stay green (B-79-05).
- `Player(node_runtime=NodeRuntime(available=False, path=None))` yields `js_runtimes == {"node": {"path": None}}` (B-79-06).
- One INFO log line per YT play in `_youtube_resolve_worker` (D-13).
- `__main__._run_gui` line 220 reads `player = Player(node_runtime=node_runtime)`.
- `__main__.main` escalates `musicstreamer.yt_import` logger to INFO alongside `musicstreamer.player` and `musicstreamer.soma_import`.
- Phase 999.7 cookie temp-copy invariant preserved (`cookie_utils.temp_cookies_copy()` ctxmgr unchanged in position and structure).
- Single source of truth: `grep -c "build_js_runtimes(" musicstreamer/player.py` returns `1`.
- Full test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-02-SUMMARY.md` when done. Include: edits made, behaviors pinned (B-79-04..B-79-06), and confirmation that the cookies invariant + the test_cookies.py:157/190 invariant both hold by grep + by green test run.
</output>
