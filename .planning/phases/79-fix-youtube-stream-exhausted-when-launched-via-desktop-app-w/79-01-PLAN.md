---
phase: 79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/yt_dlp_opts.py
  - tests/test_yt_dlp_opts.py
autonomous: true
requirements:
  - BUG-11

must_haves:
  truths:
    - "build_js_runtimes(None) returns {'node': {'path': None}} (D-02 — absent-Node branch preserves yt-dlp's own PATH-lookup fallback)"
    - "build_js_runtimes(NodeRuntime(available=True, path='/fake/node')) returns {'node': {'path': '/fake/node'}} (D-01 — resolved abs path threaded into yt-dlp opts; D-04 — regression-test bar)"
    - "build_js_runtimes(NodeRuntime(available=False, path=None)) returns {'node': {'path': None}} (D-02 — preserves None when Node genuinely absent)"
    - "Helper is strictly scoped to js_runtimes — does NOT own format, remote_components, quiet, skip_download, cookiefile, extract_flat (D-11)"
    - "Single source of truth: only this module builds js_runtimes; player.py and yt_import.py import from it (D-10)"
    - "Helper works identically on Linux and Windows — no sys.platform gate (D-03)"
  artifacts:
    - path: "musicstreamer/yt_dlp_opts.py"
      provides: "build_js_runtimes(node_runtime: NodeRuntime | None) -> dict — single source of truth for yt-dlp js_runtimes opts"
      exports: ["build_js_runtimes"]
      contains: "def build_js_runtimes(node_runtime"
    - path: "tests/test_yt_dlp_opts.py"
      provides: "Three-input matrix unit tests (B-79-01, B-79-02, B-79-03)"
      contains: "test_build_js_runtimes_none_input"
  key_links:
    - from: "musicstreamer/yt_dlp_opts.py"
      to: "musicstreamer.runtime_check.NodeRuntime"
      via: "module-level import"
      pattern: "from musicstreamer.runtime_check import NodeRuntime"
---

<objective>
Create the focused tiny module `musicstreamer/yt_dlp_opts.py` exporting `build_js_runtimes(node_runtime: NodeRuntime | None) -> dict` per D-10 / D-11. This is the single source of truth for the yt-dlp `js_runtimes` opt that Plans 79-02 and 79-03 will import and call. Ship three unit tests covering the locked input matrix (B-79-01..B-79-03) so the helper's contract is pinned BEFORE the two integration sites consume it.

Purpose: Eliminate drift between the two yt-dlp call sites (`Player._youtube_resolve_worker` and `yt_import.scan_playlist`) by routing both through a single helper. Closes the "thread the resolved Node path through to yt-dlp" gap left by commit `a06549f` (2026-04-25, fnm/nvm/volta/asdf detection).

Output: New module file + new test file. No production-code wiring (that lands in Plans 79-02 and 79-03). Both files import-only; no existing tests touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-CONTEXT.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From musicstreamer/runtime_check.py:36-39 (verified 2026-05-16):
```python
@dataclass(frozen=True)
class NodeRuntime:
    available: bool
    path: Optional[str]
```

Note: `NodeRuntime` is frozen — instances are constructed positionally OR by kwarg. The test file uses kwarg form (`NodeRuntime(available=True, path="/fake/node")`).

Sibling-module shape to mirror (musicstreamer/cookie_utils.py — `from __future__ import annotations` at top, module-level docstring with "Provides:" + "Call sites:" sections, no class, narrow public surface, type hints throughout).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create yt_dlp_opts.py helper module</name>
  <files>musicstreamer/yt_dlp_opts.py</files>
  <read_first>
    - musicstreamer/cookie_utils.py — sibling-module shape analog (header docstring "Provides:" + "Call sites:" sections, `from __future__ import annotations` first line, no class, narrow public surface). Verbatim header convention is project precedent per PATTERNS.md §1.
    - musicstreamer/runtime_check.py:36-39 — NodeRuntime dataclass shape (the type the helper consumes; `path: Optional[str]` is the field threaded into the return dict).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §1 — Pattern Assignment for this exact file, including header docstring template and helper body.
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md §"Code Examples" Example 3 — full module body reference (verbatim helper signature).
  </read_first>
  <behavior>
    - Function `build_js_runtimes(node_runtime: NodeRuntime | None) -> dict` returns `{"node": {"path": None}}` when `node_runtime is None`.
    - Returns `{"node": {"path": node_runtime.path}}` when `node_runtime is not None` (regardless of `node_runtime.available`).
    - Specifically: `NodeRuntime(available=False, path=None)` yields `{"node": {"path": None}}` — `available` is NOT consulted, only `path` is read.
    - Module imports `NodeRuntime` from `musicstreamer.runtime_check` at module level (Pitfall 4: verified non-circular — `runtime_check.py` does not import `musicstreamer.player`).
    - First line of file is `"""` (module docstring), per project convention.
  </behavior>
  <action>
Create `musicstreamer/yt_dlp_opts.py` as a focused tiny module per D-10. Header docstring mirrors `musicstreamer/cookie_utils.py`'s shape:
- Triple-quoted module docstring with one-line summary, "Provides:" block listing `build_js_runtimes(node_runtime: NodeRuntime | None) -> dict`, "Phase 79 / BUG-11 — single source of truth used by both yt-dlp call sites:" with the two call-site bullets (`musicstreamer/player.py::_youtube_resolve_worker`, `musicstreamer/yt_import.py::scan_playlist`), a "Why a module:" paragraph noting Phase 999.9 baseline + the `.desktop`-stripped PATH context, and a "Reference:" line citing `yt_dlp/utils/_jsruntime.py::_determine_runtime_path` (yt-dlp 2026.03.17).

After the docstring: `from __future__ import annotations` (mandatory FIRST import statement, project convention from CONVENTIONS.md Modern union syntax requirement).

Single import statement: `from musicstreamer.runtime_check import NodeRuntime`. Direct module-level import (verified non-circular per Pitfall 4 — `runtime_check.py` imports `PySide6.QtWidgets.QMessageBox` but never imports from `musicstreamer.player`/`yt_import`/`yt_dlp_opts`).

Single public function `build_js_runtimes(node_runtime: NodeRuntime | None) -> dict`. Body is two lines: (a) `path = node_runtime.path if node_runtime is not None else None`, (b) `return {"node": {"path": path}}`. Docstring inside the function summarizes the return shape and the absent-Node behavior (D-02: preserves yt-dlp's own PATH-lookup fallback).

Do NOT use `node_runtime and node_runtime.path` short-circuit — that conflates `None` with empty-string. Use the explicit conditional per Pitfall 3.

The module exposes ONLY `build_js_runtimes` (D-11: strictly scoped to `js_runtimes`; no `format`, `remote_components`, `quiet`, `skip_download`, `cookiefile`, `extract_flat`).
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_yt_dlp_opts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `musicstreamer/yt_dlp_opts.py` exists (CLI: `test -f musicstreamer/yt_dlp_opts.py`).
    - Source contains `def build_js_runtimes(node_runtime` exactly once (CLI: `grep -c "^def build_js_runtimes(node_runtime" musicstreamer/yt_dlp_opts.py` returns `1`).
    - Source contains `from musicstreamer.runtime_check import NodeRuntime` (CLI: `grep -q "from musicstreamer.runtime_check import NodeRuntime" musicstreamer/yt_dlp_opts.py`).
    - Source contains `from __future__ import annotations` (CLI: `grep -q "from __future__ import annotations" musicstreamer/yt_dlp_opts.py`).
    - Source does NOT mention `format`, `remote_components`, `cookiefile`, `extract_flat`, `quiet`, or `skip_download` outside of comments/docstrings — D-11 scope assertion: `grep -E '"(format|remote_components|cookiefile|extract_flat|quiet|skip_download)"' musicstreamer/yt_dlp_opts.py` returns no matches.
    - Behaviorally: importing `from musicstreamer.yt_dlp_opts import build_js_runtimes` succeeds at module level (no circular import). Verified by `python3 -c "from musicstreamer.yt_dlp_opts import build_js_runtimes; print(build_js_runtimes.__doc__[:40])"` not raising ImportError.
  </acceptance_criteria>
  <done>
    `musicstreamer/yt_dlp_opts.py` exists with the `build_js_runtimes` helper. Module is importable without circular-import errors. Public surface is exactly one function (B-79-01, B-79-02, B-79-03 pinning happens in Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_yt_dlp_opts.py with three-input matrix</name>
  <files>tests/test_yt_dlp_opts.py</files>
  <read_first>
    - musicstreamer/yt_dlp_opts.py — the just-created helper (executor verifies the call signature matches `build_js_runtimes(node_runtime)`).
    - musicstreamer/runtime_check.py:36-39 — NodeRuntime dataclass shape for inline-literal construction (`NodeRuntime(available=True, path="/fake/node")`).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §7 — verbatim test-file template (three test functions, no fixtures, no parametrize).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-VALIDATION.md — B-79-01, B-79-02, B-79-03 behaviors and per-test pytest commands.
  </read_first>
  <behavior>
    - `test_build_js_runtimes_none_input`: `build_js_runtimes(None) == {"node": {"path": None}}` (B-79-01).
    - `test_build_js_runtimes_available_path`: with `nr = NodeRuntime(available=True, path="/fake/node")`, `build_js_runtimes(nr) == {"node": {"path": "/fake/node"}}` (B-79-02).
    - `test_build_js_runtimes_unavailable_none_path`: with `nr = NodeRuntime(available=False, path=None)`, `build_js_runtimes(nr) == {"node": {"path": None}}` (B-79-03).
    - Three top-level test functions; no pytest fixtures; no parametrize; inline `NodeRuntime(...)` literals per CONTEXT.md Claude's Discretion.
    - All three tests pass when run together.
  </behavior>
  <action>
Create `tests/test_yt_dlp_opts.py` with three top-level test functions per B-79-01, B-79-02, B-79-03. Test names verbatim from VALIDATION.md and PATTERNS.md §7: `test_build_js_runtimes_none_input`, `test_build_js_runtimes_available_path`, `test_build_js_runtimes_unavailable_none_path`.

Module-level docstring summarizes scope: "Tests for musicstreamer.yt_dlp_opts — js_runtimes opts builder (Phase 79 / BUG-11)." Plus a one-paragraph rationale: three-input matrix locks the helper's contract; downstream integration tests in `tests/test_player.py` / `tests/test_player_node_runtime.py` and `tests/test_yt_import_library.py` assert the helper's output threads through to `yt_dlp.YoutubeDL` opts.

After the docstring: `from __future__ import annotations` (project convention).

Imports (only two needed): `from musicstreamer.runtime_check import NodeRuntime` and `from musicstreamer.yt_dlp_opts import build_js_runtimes`. NO `pytest`, NO `unittest.mock` — these tests are pure value comparisons.

Each test is one assertion, exactly the equality from VALIDATION.md. Use bare `assert ... == ...` form (project convention — no `assertEqual`). One-line docstring per function summarizes the behavior ID it pins (B-79-01 / B-79-02 / B-79-03) and the locked decision it derives from (D-02 for None-input branch; D-01 for path-threading; D-02 absent-branch for the False/None case).

Do NOT add helper fixtures, parametrize decorators, or shared setup. Three independent pure tests per CONTEXT.md Claude's Discretion ("inline literals; the dataclass is trivial to construct").
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_yt_dlp_opts.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_yt_dlp_opts.py` exists.
    - Source defines exactly three functions starting with `test_`: `grep -c "^def test_" tests/test_yt_dlp_opts.py` returns `3`.
    - Test function names match VALIDATION.md exactly: `grep -E "^def test_(build_js_runtimes_none_input|build_js_runtimes_available_path|build_js_runtimes_unavailable_none_path)\(" tests/test_yt_dlp_opts.py` returns three matches.
    - Behavior: all three tests pass: `uv run --with pytest pytest tests/test_yt_dlp_opts.py -x` exits 0 with `3 passed` in output.
    - Each test asserts on `build_js_runtimes(...)` directly (no indirection through yt-dlp mocks): `grep -c "build_js_runtimes(" tests/test_yt_dlp_opts.py` returns at least `3`.
    - Tests do NOT import `yt_dlp` (helper is pure): `grep -c "^import yt_dlp\|^from yt_dlp" tests/test_yt_dlp_opts.py` returns `0`.
    - Tests do NOT use `pytest.fixture` or `pytest.mark.parametrize` (per CONTEXT.md inline-literal Discretion): `grep -E "@pytest\.(fixture|mark)" tests/test_yt_dlp_opts.py` returns no matches.
  </acceptance_criteria>
  <done>
    Three unit tests pass: B-79-01, B-79-02, B-79-03. Helper's contract is pinned before Plans 79-02 and 79-03 wire it into Player and yt_import.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process | The helper accepts an already-validated `NodeRuntime` instance from `runtime_check.check_node()` and returns a dict. No external input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Information Disclosure | `_log.info("youtube ...: node_path=%s", path)` in Plans 79-02 / 79-03 | accept | The path is the user's home-directory-anchored install location (e.g. `/home/<user>/.local/share/fnm/...`). Consistent with existing `runtime_check._log.debug("Node.js detected at %s", path)` precedent. Logs go to stderr only; no file logging configured. No new sensitive-data exposure. *(Tracked in 79-02 and 79-03; surfaced here for register completeness.)* |
| T-79-02 | Tampering | `subprocess.run([<path>, "--version"])` (yt-dlp's call, argv[0]) | accept | `NodeRuntime.path` is sourced exclusively from `runtime_check._which_node` or `_which_node_version_manager_fallback`. Both compose paths from `os.path.join(home, "<known-vendor>", "bin", "node")` literals — never from env vars or user input. yt-dlp uses argv as a list (no shell), so even shell metacharacters in a path would not be interpreted. No new attack surface. |

**Verdict (ASVS L1):** This phase's threat surface is essentially zero. The change threads an absolute path (already resolved by trusted in-tree code) into a dict that yt-dlp uses as `subprocess` argv[0]. No HIGH-severity threats; both items are accepted with rationale. Plan 79-01 itself only creates a pure helper + unit tests — neither the helper nor the tests touch subprocess or logging surfaces.
</threat_model>

<verification>
Per task <verify> blocks. Plan-level gate:
- `uv run --with pytest pytest tests/test_yt_dlp_opts.py -x` exits 0 with three passing tests.
- `python3 -c "from musicstreamer.yt_dlp_opts import build_js_runtimes; from musicstreamer.runtime_check import NodeRuntime; print(build_js_runtimes(NodeRuntime(available=True, path='/x')))"` prints `{'node': {'path': '/x'}}` without raising.
</verification>

<success_criteria>
- `musicstreamer/yt_dlp_opts.py` exists and exports `build_js_runtimes(node_runtime: NodeRuntime | None) -> dict`.
- `tests/test_yt_dlp_opts.py` exists with three passing tests pinning B-79-01, B-79-02, B-79-03.
- Full test suite remains green: `uv run --with pytest pytest tests/ -x` exits 0 (no regressions; this plan only adds two files).
- Plan 79-02 and Plan 79-03 can both import `from musicstreamer import yt_dlp_opts` without circular-import issues.
</success_criteria>

<output>
Create `.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-01-SUMMARY.md` when done. Include: files created, behaviors pinned (B-79-01..B-79-03), and the helper's public signature for downstream consumers.
</output>
