---
phase: 79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w
plan: 03
type: execute
wave: 2
depends_on:
  - 79-01
files_modified:
  - musicstreamer/yt_import.py
  - musicstreamer/ui_qt/import_dialog.py
  - musicstreamer/ui_qt/main_window.py
  - tests/test_yt_import_library.py
autonomous: true
requirements:
  - BUG-11

must_haves:
  truths:
    - "yt_import.scan_playlist(url, node_runtime=NodeRuntime(available=True, path='/fake/node')) passes opts with js_runtimes['node']['path'] == '/fake/node' to yt_dlp.YoutubeDL (B-79-07)"
    - "yt_import.scan_playlist(url) — no node_runtime kwarg — passes opts with js_runtimes['node']['path'] is None (B-79-08, default backwards-compat)"
    - "yt_import.scan_playlist(url, node_runtime=NodeRuntime(available=False, path=None)) passes opts with js_runtimes['node']['path'] is None (B-79-09)"
    - "scan_playlist emits one INFO log line 'youtube scan: node_path=<abs|None>' per call (D-13; mirrors player-side INFO line)"
    - "ImportDialog.__init__ accepts keyword-only node_runtime=None kwarg; _YtScanWorker captures node_runtime at construction (Pitfall 6 — NOT lazy-read from parent in run())"
    - "MainWindow opens ImportDialog with node_runtime=self._node_runtime — single line edit at main_window.py:1405"
  artifacts:
    - path: "musicstreamer/yt_import.py"
      provides: "scan_playlist accepts node_runtime kwarg; opts dict INSERTS js_runtimes (NOT replaces — Pitfall 2); module logger _log emits one INFO line per scan"
      contains: "yt_dlp_opts.build_js_runtimes(node_runtime)"
    - path: "musicstreamer/ui_qt/import_dialog.py"
      provides: "ImportDialog.__init__ accepts keyword-only node_runtime; _YtScanWorker captures it at construction and forwards to scan_playlist in run()"
      contains: "node_runtime=self._node_runtime"
    - path: "musicstreamer/ui_qt/main_window.py"
      provides: "ImportDialog() ctor call at line 1405 threads self._node_runtime"
      contains: "ImportDialog(self.show_toast, self._repo, parent=self, node_runtime=self._node_runtime)"
    - path: "tests/test_yt_import_library.py"
      provides: "Three-input regression matrix for scan_playlist opts recording (B-79-07, B-79-08, B-79-09) using the existing _patch_youtubedl helper"
      contains: "test_scan_playlist_passes_node_path_when_available"
  key_links:
    - from: "musicstreamer/yt_import.py"
      to: "musicstreamer.yt_dlp_opts.build_js_runtimes"
      via: "module-level import + call inside scan_playlist opts dict"
      pattern: "yt_dlp_opts\\.build_js_runtimes\\(node_runtime\\)"
    - from: "musicstreamer/ui_qt/main_window.py"
      to: "musicstreamer/ui_qt/import_dialog.py"
      via: "ImportDialog ctor call with node_runtime=self._node_runtime"
      pattern: "ImportDialog\\(.*node_runtime=self\\._node_runtime"
    - from: "musicstreamer/ui_qt/import_dialog.py:_YtScanWorker"
      to: "musicstreamer.yt_import.scan_playlist"
      via: "run() forwards node_runtime captured at __init__"
      pattern: "scan_playlist\\(.*node_runtime=self\\._node_runtime"
---

<objective>
Wire `NodeRuntime` into `yt_import.scan_playlist` and the ImportDialog → _YtScanWorker → scan_playlist call chain per D-08 / D-09. The yt_import opts dict at `yt_import.py:62-71` INSERTS a new `js_runtimes` key (Pitfall 2 — this is an ADDITION, not a substitution; scan-side has no existing `js_runtimes` literal). Add a module logger `_log = logging.getLogger(__name__)` to yt_import.py (mirrors player.py:76-78) and emit one `"youtube scan: node_path=%s"` INFO line per call (D-13 parity).

Purpose: This is the other half of the BUG-11 fix — the playlist-scan path. D-09 explicitly rejected leaving the scan-side as a follow-up because that would intentionally ship a known parity bug. With Plans 79-02 and 79-03 both landed, the single-source-of-truth invariant (D-10) holds: both yt-dlp call sites route their `js_runtimes` through `yt_dlp_opts.build_js_runtimes`.

Note on `extract_flat`: Per Pitfall 2 and RESEARCH.md A5, `extract_flat='in_playlist'` short-circuits per-entry JS solving inside yt-dlp 2026.03.17 (YoutubeDL.py:1894-1909). The scan-side fix is therefore **defensive parity** — it pre-empts a future bug if the scan path ever stops using `extract_flat`, AND it keeps the single-source-of-truth invariant intact. The live-bug surface is exclusively the playback path (Plan 79-02); the live UAT in Plan 79-05 does not need to exercise scan.

Output: Four files modified (one production module gets a new key + logger; one UI module grows a kwarg + capture; one UI module threads the kwarg through one line; one test file gets three regression tests). The `musicstreamer.yt_import` logger escalation lives in `__main__.py` (already added by Plan 79-02) — this plan does not touch `__main__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-CONTEXT.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-VALIDATION.md
@.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From musicstreamer/runtime_check.py:36-39:
```python
@dataclass(frozen=True)
class NodeRuntime:
    available: bool
    path: Optional[str]
```

From musicstreamer/yt_dlp_opts.py (Plan 79-01 output):
```python
def build_js_runtimes(node_runtime: NodeRuntime | None) -> dict:
    # returns {"node": {"path": <abs-path-or-None>}}
```

From musicstreamer/yt_import.py:11-17 (current imports — NO module logger today):
```python
import os
import re
from typing import Callable, Optional

import yt_dlp

from musicstreamer import constants, cookie_utils, paths
```

From musicstreamer/yt_import.py:39-42 (current signature):
```python
def scan_playlist(
    url: str,
    toast_callback: Optional[Callable[[str], None]] = None,
) -> list[dict]:
```

From musicstreamer/yt_import.py:62-71 (the opts dict — js_runtimes is NOT here today; INSERT after `"skip_download": True,` at line 66):
```python
opts = {
    "extract_flat": "in_playlist",
    "quiet": True,
    "no_warnings": True,
    "skip_download": True,
    # BUG-YT-COOKIES: yt-dlp 2026.03.17+ requires the EJS remote component
    # when YouTube account cookies are detected (authenticated code path).
    # Same fix as player.py::_youtube_resolve_worker.
    "remote_components": {"ejs:github"},
}
```

From musicstreamer/ui_qt/import_dialog.py:74-93 (current _YtScanWorker — signature and run()):
```python
class _YtScanWorker(QThread):
    finished = Signal(list)
    error = Signal(str)

    def __init__(
        self,
        url: str,
        toast_callback: Optional[Callable[[str], None]] = None,
        parent=None,
    ):
        super().__init__(parent)
        self._url = url
        self._toast = toast_callback

    def run(self):
        try:
            results = yt_import.scan_playlist(self._url, toast_callback=self._toast)
            self.finished.emit(results)
        except Exception as exc:
            self.error.emit(str(exc))
```

From musicstreamer/ui_qt/import_dialog.py:168-184 (current ImportDialog.__init__):
```python
class ImportDialog(QDialog):
    # ...
    def __init__(self, toast_callback: Callable[[str], None], repo, parent=None):
        super().__init__(parent)
        self._toast = toast_callback
        self._repo = repo
        # ... (existing body)
        self._yt_scan_worker: Optional[QThread] = None
        # ... (existing body)
```

From musicstreamer/ui_qt/import_dialog.py:337 (current _YtScanWorker instantiation):
```python
self._yt_scan_worker = _YtScanWorker(url, toast_callback=self._toast, parent=self)
```

From musicstreamer/ui_qt/main_window.py:1405 (current ImportDialog ctor call):
```python
dlg = ImportDialog(self.show_toast, self._repo, parent=self)
```

(MainWindow already stores `self._node_runtime` at line 202 — no new state needed there.)

From tests/test_yt_import_library.py:30-42 (existing FakeYDL opts-recording helper — REUSE):
```python
def _patch_youtubedl(extract_info_return=None, extract_info_side_effect=None):
    fake_ydl = MagicMock()
    if extract_info_side_effect is not None:
        fake_ydl.extract_info.side_effect = extract_info_side_effect
    else:
        fake_ydl.extract_info.return_value = extract_info_return
    cm = MagicMock()
    cm.__enter__.return_value = fake_ydl
    cm.__exit__.return_value = False
    youtubedl_cls = MagicMock(return_value=cm)
    return patch("musicstreamer.yt_import.yt_dlp.YoutubeDL", youtubedl_cls), youtubedl_cls, fake_ydl
```

To capture opts: `youtubedl_cls.call_args[0][0]` is the first positional arg of `yt_dlp.YoutubeDL(opts)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire scan_playlist node_runtime kwarg + ADD js_runtimes to opts dict + add module logger and INFO log line</name>
  <files>musicstreamer/yt_import.py</files>
  <read_first>
    - musicstreamer/yt_import.py (full file, lines 1-100 — current imports, scan_playlist signature, opts dict structure, the corruption-check block at lines 55-60).
    - musicstreamer/yt_dlp_opts.py (Plan 79-01 output — the `build_js_runtimes(node_runtime)` helper signature).
    - musicstreamer/runtime_check.py:36-39 — NodeRuntime dataclass for the type annotation.
    - musicstreamer/player.py:76-78 — the existing module logger pattern to mirror (`_log = logging.getLogger(__name__)`).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §3 — verbatim yt_import edit map (kwarg add, logger add, INFO log placement, opts insertion site between `skip_download` and the BUG-YT-COOKIES comment block).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md §"Common Pitfalls" Pitfall 2 (yt_import scan_playlist js_runtimes is an INSERT not a REPLACE — there is no literal to substitute) and Pitfall 3 (explicit conditional, NOT `and`).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md §"Code Examples" Example 2 — full body reference for the scan_playlist edit.
  </read_first>
  <behavior>
    - `scan_playlist` signature grows a keyword-only `node_runtime: NodeRuntime | None = None` parameter inserted AFTER the existing `toast_callback` parameter and BEFORE the closing `) -> list[dict]:`. Use forward-reference string `"NodeRuntime | None"` for the annotation OR direct annotation (both safe per Pitfall 4 — runtime_check.py does not import yt_import.py).
    - Default `node_runtime=None` preserves backwards-compat for all existing call sites in tests (the existing `tests/test_yt_import_library.py` tests don't pass node_runtime; they should continue to work).
    - The opts dict at lines 62-71 INSERTS a new `"js_runtimes": yt_dlp_opts.build_js_runtimes(node_runtime),` entry AFTER `"skip_download": True,` (line 66) and BEFORE the `# BUG-YT-COOKIES` comment block (which precedes the `"remote_components": {"ejs:github"},` line). Per Pitfall 2, this is an ADDITION; the scan-side has no existing `js_runtimes` literal to substitute. The default yt-dlp shape (when omitted) is `{'deno': {}}` via `_clean_js_runtimes`; we override to `{'node': {...}}`.
    - Module-level logger added at top of yt_import.py: `import logging` (new import) and `_log = logging.getLogger(__name__)` (new module-level binding). Inserted after the existing `from typing import` import line and before `import yt_dlp`. Mirrors `musicstreamer/player.py:76-78` shape.
    - Module-level imports gain `yt_dlp_opts`: change `from musicstreamer import constants, cookie_utils, paths` to `from musicstreamer import constants, cookie_utils, paths, yt_dlp_opts` (alphabetical insertion). Add `from musicstreamer.runtime_check import NodeRuntime` on a new line in the same import group (direct import is safe — Pitfall 4 verified).
    - One new INFO log line near the top of `scan_playlist` body — AFTER the cookies-corruption block (which today ends at line 60 with the `toast_callback(...)` call) and BEFORE the `opts = {` literal at line 62. Form: `node_path = node_runtime.path if node_runtime else None` then `_log.info("youtube scan: node_path=%s", node_path)`. Use explicit conditional (Pitfall 3), NOT `node_runtime and node_runtime.path`.
    - Phase 999.7 invariant: the `with cookie_utils.temp_cookies_copy() as cookiefile:` ctxmgr at line 77 stays UNCHANGED in position and structure. The new INFO log lands BEFORE the ctxmgr (where opts are being built); the yt_dlp.YoutubeDL construction inside the ctxmgr continues to receive opts unchanged in shape.
    - Module-level docstring is unchanged. Public API surface of `scan_playlist` is documented in the existing docstring; the new kwarg can be appended as a one-line "node_runtime: optional NodeRuntime for explicit Node binary path (Phase 79 / BUG-11)" if executor wants, but is not required for behavior.
  </behavior>
  <action>
Edit `musicstreamer/yt_import.py`:

1. Imports block (top of file):
   - Add `import logging` as a new line in the stdlib import group (after `import os`, before `import re` — alphabetical).
   - Add `_log = logging.getLogger(__name__)` as a module-level binding AFTER all imports (before the `is_yt_playlist_url` function at line 20). Mirror `musicstreamer/player.py:76-78` shape.
   - Change `from musicstreamer import constants, cookie_utils, paths` to `from musicstreamer import constants, cookie_utils, paths, yt_dlp_opts` (insert `yt_dlp_opts` alphabetically).
   - Add `from musicstreamer.runtime_check import NodeRuntime` on a new line in the same `from musicstreamer.*` import group.

2. `scan_playlist` signature: change from current 2-arg form to add a keyword-only third parameter:
   ```
   def scan_playlist(
       url: str,
       toast_callback: Optional[Callable[[str], None]] = None,
       *,
       node_runtime: "NodeRuntime | None" = None,
   ) -> list[dict]:
   ```
   The `*,` marker forces `node_runtime` to be keyword-only — this is mandatory per Pitfall 5 / D-06 (avoids breaking the existing `toast_callback`-as-positional pattern in tests).

3. Inside `scan_playlist` body, insert the INFO log block AFTER the cookies-corruption conditional ends (today: after the indented `toast_callback("YouTube cookies cleared — re-import via Accounts menu.")` block at line 60) and BEFORE the `opts = {` literal at line 62. The block is exactly two lines:
   ```
   node_path = node_runtime.path if node_runtime else None
   _log.info("youtube scan: node_path=%s", node_path)
   ```
   (Explicit conditional per Pitfall 3 — NOT `node_runtime and node_runtime.path`.)

4. Insert the new `"js_runtimes": ...` entry into the `opts = { ... }` dict at lines 62-71. INSERTION (NOT substitution per Pitfall 2). The new line lands AFTER `"skip_download": True,` (currently line 66) and BEFORE the existing `# BUG-YT-COOKIES: yt-dlp 2026.03.17+ requires the EJS remote component` comment block. Include a short comment above the new line citing Phase 79 / BUG-11 and the single-source-of-truth rationale:
   ```
       # Phase 79 / BUG-11: thread the resolved Node executable path through to
       # yt-dlp so .desktop-stripped PATH launches don't fail JS-requiring scans.
       # Single source of truth shared with player._youtube_resolve_worker.
       # Note: extract_flat='in_playlist' short-circuits per-entry JS solving
       # today (yt_dlp/YoutubeDL.py:1894-1909) so this is defensive parity for
       # the helper invariant (D-10), not a live-bug fix on the scan path.
       "js_runtimes": yt_dlp_opts.build_js_runtimes(node_runtime),
   ```

5. DO NOT modify the `with cookie_utils.temp_cookies_copy() as cookiefile:` ctxmgr or the `yt_dlp.YoutubeDL(opts)` call inside it (lines 77-82). The opts dict is passed through unchanged in shape (now with the new `js_runtimes` key) into yt-dlp's library API call.

6. DO NOT touch `is_yt_playlist_url`, `_entry_is_live`, or `import_stations` — they're out of scope for Phase 79.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_yt_import_library.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `scan_playlist` signature contains `node_runtime`: `grep -E "^\\s+node_runtime: " musicstreamer/yt_import.py` returns at least one match.
    - `scan_playlist` signature has the `*,` keyword-only marker before `node_runtime`: `grep -B1 -E "^\\s+node_runtime:" musicstreamer/yt_import.py | grep -c "^\\s\\+\\*,$"` returns at least `1`.
    - `yt_import.py` imports `yt_dlp_opts`: `grep -E "from musicstreamer import .*yt_dlp_opts" musicstreamer/yt_import.py` returns one match.
    - `yt_import.py` imports `NodeRuntime`: `grep -q "from musicstreamer.runtime_check import NodeRuntime" musicstreamer/yt_import.py`.
    - `yt_import.py` has a module logger: `grep -c "^_log = logging.getLogger(__name__)" musicstreamer/yt_import.py` returns `1`.
    - `yt_import.py` imports `logging`: `grep -q "^import logging" musicstreamer/yt_import.py`.
    - `scan_playlist` calls `yt_dlp_opts.build_js_runtimes(node_runtime)` exactly once: `grep -c "yt_dlp_opts.build_js_runtimes(node_runtime)" musicstreamer/yt_import.py` returns `1`.
    - INFO log line is present: `grep -c '"youtube scan: node_path=' musicstreamer/yt_import.py` returns `1`.
    - INFO log uses explicit conditional (Pitfall 3): `grep -E "node_runtime\\.path if node_runtime else None" musicstreamer/yt_import.py` returns at least one match.
    - `js_runtimes` lands between `skip_download` and the `# BUG-YT-COOKIES` comment block: `grep -n "js_runtimes\\|skip_download\\|BUG-YT-COOKIES" musicstreamer/yt_import.py | sort -n` shows `skip_download` first, then `js_runtimes`, then `BUG-YT-COOKIES`.
    - Phase 999.7 invariant preserved: `grep -c "with cookie_utils.temp_cookies_copy() as cookiefile" musicstreamer/yt_import.py` returns `1`.
    - Existing tests pass (no regression): `uv run --with pytest pytest tests/test_yt_import_library.py -x` exits 0 (the existing tests don't pass `node_runtime` — they exercise the default `None` branch and continue to work).
  </acceptance_criteria>
  <done>
    `scan_playlist` accepts `node_runtime` kwarg; opts dict carries `"js_runtimes": yt_dlp_opts.build_js_runtimes(node_runtime)`; module logger emits one INFO line per scan; Phase 999.7 cookie temp-copy invariant unchanged. Existing tests pass without modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread node_runtime through ImportDialog → _YtScanWorker → scan_playlist call chain (UI plumbing)</name>
  <files>musicstreamer/ui_qt/import_dialog.py, musicstreamer/ui_qt/main_window.py</files>
  <read_first>
    - musicstreamer/ui_qt/import_dialog.py (full file — particularly lines 1-30 imports, lines 74-93 _YtScanWorker class, lines 168-200 ImportDialog.__init__ body, line 337 _YtScanWorker instantiation).
    - musicstreamer/ui_qt/main_window.py:195-202 — the existing `node_runtime=None` kwarg + storage pattern (Phase 79 mirrors the kwarg shape onto ImportDialog).
    - musicstreamer/ui_qt/main_window.py:1405 — the verbatim line to edit (`dlg = ImportDialog(self.show_toast, self._repo, parent=self)` → `dlg = ImportDialog(self.show_toast, self._repo, parent=self, node_runtime=self._node_runtime)`).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §5 (import_dialog edit map: _YtScanWorker __init__ kwarg, run() forwards node_runtime, ImportDialog __init__ keyword-only kwarg, _YtScanWorker instantiation at line 337) and §6 (main_window single-line ctor call edit).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-RESEARCH.md §"Common Pitfalls" Pitfall 5 (ImportDialog kwarg is keyword-only to avoid breaking existing test ctor calls) and Pitfall 6 (_YtScanWorker captures node_runtime at __init__, NOT lazy-reads from parent in run()).
  </read_first>
  <behavior>
    - `_YtScanWorker.__init__` accepts a keyword-only `node_runtime: NodeRuntime | None = None` parameter inserted AFTER the existing `toast_callback` and BEFORE `parent`. Stored as `self._node_runtime = node_runtime` on the worker. Captured at __init__ time per Pitfall 6 — NOT lazy-read from parent inside `run()` (the dialog could close mid-scan).
    - `_YtScanWorker.run()` forwards the captured worker-level `self._node_runtime` to `scan_playlist`: `yt_import.scan_playlist(self._url, toast_callback=self._toast, node_runtime=self._node_runtime)`.
    - `ImportDialog.__init__` accepts a keyword-only `node_runtime: NodeRuntime | None = None` parameter inserted AFTER `parent`. Stored as `self._node_runtime = node_runtime` adjacent to other instance state (after the existing `self._toast = toast_callback` / `self._repo = repo` block).
    - `_on_yt_scan_clicked` (the method that constructs `_YtScanWorker` at line 337) threads `node_runtime=self._node_runtime` into the `_YtScanWorker(...)` ctor call. Existing positional args (`url`) and keyword args (`toast_callback`, `parent`) stay unchanged.
    - `MainWindow._open_import_dialog` (the method around line 1405 that constructs `ImportDialog`) threads `node_runtime=self._node_runtime` into the `ImportDialog(...)` ctor call. `MainWindow.self._node_runtime` is already stored at `main_window.py:202` — no new state needed.
    - `node_runtime` is keyword-only on BOTH `ImportDialog.__init__` and `_YtScanWorker.__init__` (Pitfall 5 — existing test ctor calls in `tests/test_import_dialog.py` and `tests/test_import_dialog_qt.py` don't pass `node_runtime` and must continue to work; making the kwarg keyword-only with default None preserves them).
    - No other UI lines change. No method signatures other than `__init__` change. No event handlers change.
  </behavior>
  <action>
Edit `musicstreamer/ui_qt/import_dialog.py`:

1. Import `NodeRuntime` for type annotations. Add `from musicstreamer.runtime_check import NodeRuntime` in the existing `musicstreamer.*` import group (alongside the existing `from musicstreamer import yt_import` if present, OR as a new line if it's not). Direct import is safe — verified non-circular per Pitfall 4.

2. `_YtScanWorker.__init__` (currently lines 78-86): Change signature to add a keyword-only `node_runtime` after `toast_callback` and before `parent`:
   ```
   def __init__(
       self,
       url: str,
       toast_callback: Optional[Callable[[str], None]] = None,
       *,
       node_runtime: "NodeRuntime | None" = None,
       parent=None,
   ):
   ```
   Inside the body, store `self._node_runtime = node_runtime` adjacent to the existing `self._url = url` and `self._toast = toast_callback` assignments.

3. `_YtScanWorker.run()` (currently lines 87-93): Change the `scan_playlist` call to forward the captured worker-level node_runtime:
   ```
   results = yt_import.scan_playlist(
       self._url,
       toast_callback=self._toast,
       node_runtime=self._node_runtime,
   )
   ```
   Reformat as multi-line for readability OR keep on a single line — executor's discretion. The `try/except` block, the `finished.emit(...)`, and the `error.emit(...)` lines stay unchanged.

4. `ImportDialog.__init__` (currently line 173): Change signature to add keyword-only `node_runtime` after `parent`:
   ```
   def __init__(
       self,
       toast_callback: Callable[[str], None],
       repo,
       parent=None,
       *,
       node_runtime: "NodeRuntime | None" = None,
   ):
   ```
   Inside the body, store `self._node_runtime = node_runtime` adjacent to `self._toast = toast_callback` and `self._repo = repo` (the assignments inside the constructor body around lines 174-176).

5. `_YtScanWorker` instantiation site (line 337 currently): Change from `self._yt_scan_worker = _YtScanWorker(url, toast_callback=self._toast, parent=self)` to:
   ```
   self._yt_scan_worker = _YtScanWorker(
       url,
       toast_callback=self._toast,
       node_runtime=self._node_runtime,
       parent=self,
   )
   ```
   Reformat to multi-line for readability OR keep on a single line (whichever fits the surrounding code style).

Edit `musicstreamer/ui_qt/main_window.py`:

6. Line 1405 (the only ImportDialog instantiation site in main_window.py): Change `dlg = ImportDialog(self.show_toast, self._repo, parent=self)` to `dlg = ImportDialog(self.show_toast, self._repo, parent=self, node_runtime=self._node_runtime)`. `self._node_runtime` is already stored at `main_window.py:202` — no new MainWindow state needed.

7. No other lines in main_window.py change. The existing `_on_playback_error` toast at lines 795-815 stays unchanged (D-12: keep existing toast machinery; no new branch).

DO NOT modify `tests/test_import_dialog.py` or `tests/test_import_dialog_qt.py` — Pitfall 5 confirms `ImportDialog`'s existing ctor signature still works because the new kwarg is keyword-only with default None. Re-run those tests after the edit to confirm.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_import_dialog.py tests/test_import_dialog_qt.py tests/test_yt_import_library.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `_YtScanWorker.__init__` signature contains keyword-only `node_runtime`: `grep -E "node_runtime: " musicstreamer/ui_qt/import_dialog.py` returns at least two matches (worker + dialog).
    - `_YtScanWorker.__init__` stores it: `grep -c "self._node_runtime = node_runtime" musicstreamer/ui_qt/import_dialog.py` returns `2` (worker + dialog).
    - `_YtScanWorker.run()` forwards node_runtime: `grep -E "scan_playlist\\([^)]*node_runtime=self\\._node_runtime" musicstreamer/ui_qt/import_dialog.py` returns at least one match (may span multiple lines if reformatted — use `grep -Pzo` or inspect manually if needed).
    - `_YtScanWorker(...)` instantiation at the `_on_yt_scan_clicked` site passes `node_runtime`: `grep -E "_YtScanWorker\\([^)]*node_runtime=self\\._node_runtime" musicstreamer/ui_qt/import_dialog.py` returns at least one match.
    - `ImportDialog.__init__` signature contains keyword-only `node_runtime`: looking for the second `node_runtime: ` match in the file.
    - `main_window.py:1405` carries the new kwarg: `grep -E "ImportDialog\\(self\\.show_toast, self\\._repo, parent=self, node_runtime=self\\._node_runtime\\)" musicstreamer/ui_qt/main_window.py` returns one match. (Or the multi-line form — verify by inspecting line 1405 in the edit.)
    - Existing ImportDialog tests stay green: `uv run --with pytest pytest tests/test_import_dialog.py tests/test_import_dialog_qt.py -x` exits 0 (Pitfall 5 backwards-compat — no existing test ctor calls were modified).
    - Existing yt_import tests stay green: `uv run --with pytest pytest tests/test_yt_import_library.py -x` exits 0.
    - No new `_YtScanWorker` instantiation sites appear: `grep -c "_YtScanWorker(" musicstreamer/ui_qt/import_dialog.py` returns the same count as before the edit (one in the class def + one at the instantiation site — total 2 occurrences in source, same as today).
  </acceptance_criteria>
  <done>
    `node_runtime` threads from MainWindow → ImportDialog → _YtScanWorker → scan_playlist via dependency injection at each layer. All existing UI tests stay green. The dialog can be opened with or without `node_runtime` — the default `None` preserves test ctor compatibility.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/test_yt_import_library.py with B-79-07, B-79-08, B-79-09 regression matrix</name>
  <files>tests/test_yt_import_library.py</files>
  <read_first>
    - tests/test_yt_import_library.py (full file — lines 1-50 for imports + the existing `_patch_youtubedl` helper at lines 30-42; lines 70-120 for representative existing test shapes the new tests should mirror).
    - musicstreamer/runtime_check.py:36-39 — NodeRuntime dataclass for inline literals.
    - musicstreamer/yt_import.py — the updated `scan_playlist` signature and opts construction (from Task 1).
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-PATTERNS.md §9 — verbatim test template using `youtubedl_cls.call_args[0][0]` to capture the opts dict.
    - .planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-VALIDATION.md — B-79-07, B-79-08, B-79-09 test names and per-test pytest commands.
  </read_first>
  <behavior>
    - `test_scan_playlist_passes_node_path_when_available`: calls `yt_import.scan_playlist("https://youtube.com/@x/streams", node_runtime=NodeRuntime(available=True, path="/fake/node"))` inside the `_patch_youtubedl(extract_info_return={"entries": []})` context. Asserts `youtubedl_cls.call_args[0][0]["js_runtimes"] == {"node": {"path": "/fake/node"}}` (B-79-07).
    - `test_scan_playlist_default_none_node_runtime`: calls `yt_import.scan_playlist("https://youtube.com/@x/streams")` (no `node_runtime` kwarg) inside the same context. Asserts `youtubedl_cls.call_args[0][0]["js_runtimes"] == {"node": {"path": None}}` (B-79-08 — default backwards-compat).
    - `test_scan_playlist_passes_none_when_unavailable`: calls `yt_import.scan_playlist("https://youtube.com/@x/streams", node_runtime=NodeRuntime(available=False, path=None))` inside the same context. Asserts `youtubedl_cls.call_args[0][0]["js_runtimes"] == {"node": {"path": None}}` (B-79-09).
    - All three tests reuse the existing `_patch_youtubedl` helper at `tests/test_yt_import_library.py:30-42`. No new helper functions.
    - Each test imports `NodeRuntime` directly (`from musicstreamer.runtime_check import NodeRuntime`) — top of file if not already there.
  </behavior>
  <action>
Extend `tests/test_yt_import_library.py` by appending three new test functions at the END of the file (after the last existing test). DO NOT modify the existing `_patch_youtubedl` helper or any existing tests.

Add at the top of the file (in the existing import block) if not already present:
- `from musicstreamer.runtime_check import NodeRuntime`

Append three new test functions, each ~10 lines. Names match VALIDATION.md exactly:

Test 1 `test_scan_playlist_passes_node_path_when_available`:
- `p, youtubedl_cls, _ = _patch_youtubedl(extract_info_return={"entries": []})`
- `with p: yt_import.scan_playlist("https://youtube.com/@x/streams", node_runtime=NodeRuntime(available=True, path="/fake/node"))`
- `opts = youtubedl_cls.call_args[0][0]` (the dict passed to `yt_dlp.YoutubeDL(opts)`)
- `assert opts["js_runtimes"] == {"node": {"path": "/fake/node"}}`
- One-line docstring: "B-79-07: scan_playlist threads NodeRuntime.path into yt_dlp opts."

Test 2 `test_scan_playlist_default_none_node_runtime`:
- Same context-manager pattern, but no `node_runtime` kwarg on the `scan_playlist` call.
- `assert opts["js_runtimes"] == {"node": {"path": None}}`
- One-line docstring: "B-79-08: scan_playlist default (no node_runtime kwarg) yields {'path': None}."

Test 3 `test_scan_playlist_passes_none_when_unavailable`:
- Same context-manager pattern; `node_runtime=NodeRuntime(available=False, path=None)`.
- `assert opts["js_runtimes"] == {"node": {"path": None}}`
- One-line docstring: "B-79-09: scan_playlist with NodeRuntime(available=False, path=None) yields {'path': None} (D-02)."

DO NOT use `pytest.fixture` or `pytest.mark.parametrize` — three independent tests sharing the existing `_patch_youtubedl` helper, mirroring the style of every other test in the file.

DO NOT add new imports beyond `NodeRuntime`. `yt_import`, `patch`, `MagicMock`, and the `_patch_youtubedl` helper are already imported at the top of the file.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_yt_import_library.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_yt_import_library.py` contains the three new tests: `grep -E "^def test_(scan_playlist_passes_node_path_when_available|scan_playlist_default_none_node_runtime|scan_playlist_passes_none_when_unavailable)\(" tests/test_yt_import_library.py` returns three matches.
    - File imports `NodeRuntime`: `grep -q "from musicstreamer.runtime_check import NodeRuntime" tests/test_yt_import_library.py`.
    - File still imports the existing `_patch_youtubedl` helper (`grep -q "def _patch_youtubedl(" tests/test_yt_import_library.py`).
    - All three new tests pass (B-79-07, B-79-08, B-79-09): `uv run --with pytest pytest tests/test_yt_import_library.py::test_scan_playlist_passes_node_path_when_available tests/test_yt_import_library.py::test_scan_playlist_default_none_node_runtime tests/test_yt_import_library.py::test_scan_playlist_passes_none_when_unavailable -x` exits 0 with `3 passed`.
    - Full yt_import test file stays green: `uv run --with pytest pytest tests/test_yt_import_library.py -x` exits 0 (no regression in existing tests).
    - No `pytest.fixture` or `pytest.mark.parametrize` added to the new tests (consistent style): the three new test functions are at top-level with no decorators.
  </acceptance_criteria>
  <done>
    Three regression tests pass: B-79-07, B-79-08, B-79-09. The single-source-of-truth invariant (D-10) is observable at the test layer — both `tests/test_player_node_runtime.py` (from Plan 79-02) and the new entries in `tests/test_yt_import_library.py` assert the same shape comes out of `yt_dlp_opts.build_js_runtimes`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process → yt-dlp subprocess | `yt_dlp.YoutubeDL` opts dict now carries `js_runtimes["node"]["path"] = <abs-path>` on the scan-side too. yt-dlp passes this as `argv[0]` to `subprocess.run([<path>, "--version"])`. |
| in-process → stderr (logs) | New `_log.info("youtube scan: node_path=%s", node_path)` line emits the resolved Node path to stderr. |
| UI dialog → background thread | `_YtScanWorker` captures `node_runtime` at `__init__` (Pitfall 6) — NOT lazy-read from the dialog parent inside `run()`. The captured object is a frozen dataclass, so cross-thread access of `.path` (a str) is safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering / Elevation of Privilege | `subprocess.run([<node_path>, "--version"])` invocation inside yt-dlp's `NodeJsRuntime._info` (path threaded from scan-side `node_runtime`) | mitigate (by construction) | Same provenance as the playback path: `NodeRuntime.path` is sourced exclusively from in-tree `runtime_check._which_node` / `_which_node_version_manager_fallback`. No env-var or user input. argv-list shape rules out shell-injection regardless. No new attack surface. **NOTE:** RESEARCH.md A5 + Pitfall 2 confirm `extract_flat='in_playlist'` short-circuits per-entry JS solving today, so this surface is **inactive** under current yt-dlp behavior — the path may not actually be invoked. Defensive parity remains correct for the single-source-of-truth invariant. |
| T-79-02 | Information Disclosure | `_log.info("youtube scan: node_path=%s", <path>)` in scan_playlist emits the home-directory-anchored install location to stderr | accept | Identical mitigation rationale to player-side INFO line (Plan 79-02 T-79-02). Existing precedent: `runtime_check._log.debug("Node.js detected at %s", path)`. Local stderr only; no remote shipping. |
| T-79-03 | Tampering | `_YtScanWorker` captures `node_runtime` at construction (Pitfall 6) | mitigate | Capture-at-__init__ prevents a race where the dialog (and its `_node_runtime` ref) could close mid-scan and leave `run()` with a dangling reference. The captured `NodeRuntime` is a frozen dataclass — immutable; cross-thread reads of `.path` are safe Python semantics. **No new mitigation required beyond following the established pattern.** |

**Verdict (ASVS L1):** No HIGH-severity threats. Same trust-boundary shape as Plan 79-02; the scan-side adds nothing new. T-79-03 (cross-thread `NodeRuntime` capture) is structurally mitigated by the project's existing pattern of capturing state at `QThread.__init__` (verified in `import_dialog.py:78-93` shape — this plan only extends the pattern with one more captured field). Phase 79's overall threat surface is essentially zero — threading an absolute path (already resolved by trusted in-tree code) into yt-dlp's `js_runtimes` opt. Documented per the prompt's instruction: no skipping the threat_model block even when surface is zero.
</threat_model>

<verification>
Per task <verify> blocks. Plan-level gate:
- `uv run --with pytest pytest tests/test_yt_import_library.py tests/test_import_dialog.py tests/test_import_dialog_qt.py -x` exits 0.
- `grep -c "build_js_runtimes(node_runtime)" musicstreamer/yt_import.py` returns `1` (single source of truth).
- `grep -c '"youtube scan: node_path=' musicstreamer/yt_import.py` returns `1` (INFO log line present).
- `grep -E "ImportDialog\\(self\\.show_toast.*node_runtime=self\\._node_runtime" musicstreamer/ui_qt/main_window.py` returns at least one match (MainWindow threads node_runtime to ImportDialog).
- The full test suite remains green: `uv run --with pytest pytest tests/ -x` exits 0.
</verification>

<success_criteria>
- `scan_playlist(url, node_runtime=NodeRuntime(available=True, path="/fake/node"))` passes `js_runtimes == {"node": {"path": "/fake/node"}}` to yt-dlp (B-79-07).
- `scan_playlist(url)` (no kwarg) passes `js_runtimes == {"node": {"path": None}}` — default backwards-compat (B-79-08).
- `scan_playlist(url, node_runtime=NodeRuntime(available=False, path=None))` passes `js_runtimes == {"node": {"path": None}}` (B-79-09).
- One INFO log line per scan in `scan_playlist` (D-13 parity with player-side).
- `MainWindow → ImportDialog → _YtScanWorker → scan_playlist` call chain threads `node_runtime` correctly at every layer.
- `_YtScanWorker` captures `node_runtime` at `__init__`, not lazy-reads from parent in `run()` (Pitfall 6).
- All existing tests (`tests/test_import_dialog.py`, `tests/test_import_dialog_qt.py`, `tests/test_yt_import_library.py`) stay green by construction — Pitfall 5 (keyword-only with default None).
- Single source of truth: `grep -c "build_js_runtimes(" musicstreamer/yt_import.py` returns `1`.
</success_criteria>

<output>
Create `.planning/phases/79-fix-youtube-stream-exhausted-when-launched-via-desktop-app-w/79-03-SUMMARY.md` when done. Include: edits made (file count, line counts), behaviors pinned (B-79-07..B-79-09), confirmation that the `extract_flat='in_playlist'` short-circuit means this is defensive parity not a live bug fix (per Pitfall 2 / A5), and confirmation that all pre-existing tests still pass.
</output>
