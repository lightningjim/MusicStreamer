---
phase: 75
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - musicstreamer/ui_qt/toast.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "ToastOverlay reads QApplication.property('theme_name') lazily inside _rebuild_stylesheet (no caching)"
    - "theme='system' OR property unset/None produces the IMMUTABLE legacy QSS rgba(40, 40, 40, 220) + color: white"
    - "Non-system themes produce QSS with rgba({R}, {G}, {B}, 220) from palette ToolTipBase AND color: {ToolTipText.name()} lowercase"
    - "Both branches contain border-radius: 8px AND padding: 8px 12px — geometry invariance lock"
    - "Neither branch contains font-size:, font-family:, or font-weight: — typography invariance lock"
    - "changeEvent override filters on QEvent.PaletteChange ONLY (NOT StyleChange — recursion guard, RESEARCH Risk 1)"
    - "super().changeEvent(event) called LAST after handler logic"
  artifacts:
    - path: "musicstreamer/ui_qt/toast.py"
      provides: "_rebuild_stylesheet() helper + changeEvent(PaletteChange) override; hardcoded setStyleSheet block replaced"
      contains: "_rebuild_stylesheet"
  key_links:
    - from: "ToastOverlay.__init__"
      to: "self._rebuild_stylesheet()"
      via: "call replaces hardcoded setStyleSheet at lines 44-52"
      pattern: "self\\._rebuild_stylesheet\\(\\)"
    - from: "QApplication.setPalette() (apply_theme_palette or picker tile click)"
      to: "ToastOverlay.changeEvent(QEvent.PaletteChange)"
      via: "Qt's synchronous PaletteChange dispatch to every QWidget"
      pattern: "event\\.type\\(\\) == QEvent\\.PaletteChange"
---

<objective>
Replace ToastOverlay's hardcoded setStyleSheet block (lines 44-52) with a palette-driven `_rebuild_stylesheet()` helper that branches on `QApplication.instance().property("theme_name")`, AND add a `changeEvent` override filtered ON `QEvent.PaletteChange` ONLY to retint on live theme flips. Preserve the legacy rgba(40, 40, 40, 220) + white QSS verbatim for `theme = 'system'` (D-09 / UI-SPEC IMMUTABLE QSS LOCK). All other ToastOverlay lifetime/animation/geometry code stays untouched.

Purpose: This plan is the heart of Phase 75 — wires the toast widget to the Phase 66 theme system. PaletteChange is filtered alone (NOT StyleChange) to avoid `setStyleSheet()`-re-fires-StyleChange recursion (RESEARCH §Risk Surface §1; this is the FIRST `setStyleSheet`-inside-`changeEvent` site in the codebase, so the in-tree analog at `now_playing_panel.py:194-197` cannot be copied verbatim — narrow the filter).

Output: `musicstreamer/ui_qt/toast.py` with QPalette + QApplication imports added; `_rebuild_stylesheet` method; `changeEvent` override; `__init__` calls `_rebuild_stylesheet` instead of inline `setStyleSheet`. Geometry/typography/animation invariants preserved per UI-SPEC §Visual Invariance Locks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@musicstreamer/ui_qt/toast.py
@musicstreamer/ui_qt/now_playing_panel.py
@musicstreamer/theme.py

<interfaces>
Existing toast.py imports at lines 9-16 (preserve all, add 2):
- from PySide6.QtCore import (QAbstractAnimation, QEvent, QPropertyAnimation, QTimer, Qt) — QEvent already present
- from PySide6.QtWidgets import QGraphicsOpacityEffect, QLabel, QVBoxLayout, QWidget
- NEW: from PySide6.QtGui import QPalette
- NEW: add QApplication to the QtWidgets import line

changeEvent analog (now_playing_panel.py:194-197) — handler-first, super-last ordering, BUT Phase 75 NARROWS filter to PaletteChange ONLY (RESEARCH Risk 1).

UI-SPEC §Color §System-theme legacy fallback — IMMUTABLE QSS LOCK (UI-SPEC lines 96-103):
- selector: QLabel#ToastLabel
- background-color: rgba(40, 40, 40, 220)
- color: white
- border-radius: 8px
- padding: 8px 12px

UI-SPEC §Color §Non-system QSS build template (UI-SPEC lines 113-120):
- background-color: rgba({r}, {g}, {b}, 220) where {r}/{g}/{b} = palette.color(QPalette.ToolTipBase).red()/.green()/.blue()
- color: {text_hex} where {text_hex} = palette.color(QPalette.ToolTipText).name() (lowercase #rrggbb)
- border-radius: 8px (verbatim)
- padding: 8px 12px (verbatim)
- Alpha = literal integer 220; NOT 0.86, NOT 86%.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add imports + _rebuild_stylesheet helper + replace hardcoded setStyleSheet call in __init__</name>
  <files>musicstreamer/ui_qt/toast.py</files>
  <read_first>
    - musicstreamer/ui_qt/toast.py (entire file, 115 lines — modification target)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §toast.py (lines 106-194)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md §Pattern 2 (lines 178-211)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Color (lines 92-125)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-01, D-02, D-09, D-10 path (b)
  </read_first>
  <action>
    Three coordinated edits to musicstreamer/ui_qt/toast.py:

    (a) Imports (lines 9-16): add `from PySide6.QtGui import QPalette` as a new top-level import after the existing QtCore import block; add `QApplication` to the existing `from PySide6.QtWidgets import (...)` line. Resulting QtWidgets import line lists alphabetically: `QApplication, QGraphicsOpacityEffect, QLabel, QVBoxLayout, QWidget`. QEvent is already imported from QtCore (line 11) — do NOT re-import.

    (b) Replace lines 44-52 (the hardcoded inline `setStyleSheet(...)` block) with a single call: `self._rebuild_stylesheet()`. Placement: AFTER `layout.addWidget(self.label)` (line 42), BEFORE the `QGraphicsOpacityEffect` block (line 56). The replaced lines (44-52) are deleted in their entirety.

    (c) Add a new instance method `_rebuild_stylesheet(self) -> None` to the ToastOverlay class. Place it in the `# --- Internal ---` section (after line 96 marker, before `eventFilter`). The method body must implement:
      - Read `app = QApplication.instance()`
      - Read `theme_name = app.property("theme_name") if app is not None else None`
      - If `not theme_name or theme_name == "system":` then call `self.setStyleSheet(...)` with the IMMUTABLE legacy QSS from the &lt;interfaces&gt; block (UI-SPEC §Color lines 96-103). The literal substring `rgba(40, 40, 40, 220)` (with spaces after commas, integer 220) MUST appear verbatim. Use `color: white` (the word `white`, NOT `#ffffff`, NOT `#FFF`). Include `border-radius: 8px;` AND `padding: 8px 12px;`. Then `return`.
      - Else (non-system branch): `pal = self.palette()`; `bg = pal.color(QPalette.ToolTipBase)`; `fg = pal.color(QPalette.ToolTipText).name()`. Call `self.setStyleSheet(...)` with QSS containing `f" background-color: rgba({bg.red()}, {bg.green()}, {bg.blue()}, 220);"`, `f" color: {fg};"`, AND the SAME geometry pair (`border-radius: 8px;` and `padding: 8px 12px;`).
      - Both branches use the `QLabel#ToastLabel { ... }` selector wrapper exactly as in the current toast.py:45-52.
      - DO NOT add `font-size:`, `font-family:`, or `font-weight:` properties in either branch (UI-SPEC typography invariance lock).
      - The method returns None implicitly.

    Lazy read at every call — DO NOT cache `theme_name` on `self` (RESEARCH §Pattern 2, §4 — picker live-preview mutates the property mid-session, caching would freeze the toast).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; QT_QPA_PLATFORM=offscreen python -c "from PySide6.QtWidgets import QApplication, QWidget; import sys; app = QApplication.instance() or QApplication(sys.argv); from musicstreamer.ui_qt.toast import ToastOverlay; from musicstreamer.theme import THEME_PRESETS, build_palette_from_dict; app.setProperty('theme_name', 'system'); parent = QWidget(); toast = ToastOverlay(parent); qss = toast.styleSheet(); assert 'rgba(40, 40, 40, 220)' in qss, 'sys bg'; assert 'color: white' in qss, 'sys fg'; assert 'border-radius: 8px' in qss; assert 'padding: 8px 12px' in qss; [(_ for _ in ()).throw(AssertionError(f'forbidden {p} in system qss')) for p in ('font-size:', 'font-family:', 'font-weight:') if p in qss]; app.setProperty('theme_name', 'vaporwave'); app.setPalette(build_palette_from_dict(THEME_PRESETS['vaporwave'])); toast2 = ToastOverlay(parent); qss2 = toast2.styleSheet(); assert 'rgba(249, 214, 240, 220)' in qss2, f'vapor bg missing: {qss2!r}'; assert 'color: #3a2845' in qss2, f'vapor fg missing: {qss2!r}'; assert 'border-radius: 8px' in qss2; assert 'padding: 8px 12px' in qss2; [(_ for _ in ()).throw(AssertionError(f'forbidden {p} in vapor qss')) for p in ('font-size:', 'font-family:', 'font-weight:') if p in qss2]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - System branch (`app.setProperty("theme_name", "system")` before construct): `toast.styleSheet()` contains literal substrings `rgba(40, 40, 40, 220)`, `color: white`, `border-radius: 8px`, `padding: 8px 12px`
    - Non-system branch (vaporwave palette applied via build_palette_from_dict + setPalette): `toast.styleSheet()` contains `rgba(249, 214, 240, 220)` (from #f9d6f0) AND `color: #3a2845` (lowercase ToolTipText)
    - Both branches contain `border-radius: 8px` AND `padding: 8px 12px`
    - Neither branch contains `font-size:`, `font-family:`, or `font-weight:` substrings
    - Source grep: `grep -c '_rebuild_stylesheet' musicstreamer/ui_qt/toast.py` returns at least 2 (def + __init__ call; will be 3 after Task 2 adds changeEvent call)
    - Source grep: `grep -c 'from PySide6.QtGui import QPalette' musicstreamer/ui_qt/toast.py` returns 1
    - Source grep: `grep -E 'from PySide6.QtWidgets import.*QApplication' musicstreamer/ui_qt/toast.py` finds the QApplication import
    - The hardcoded inline `setStyleSheet(...)` block at the old lines 44-52 is GONE — `setStyleSheet` only appears inside `_rebuild_stylesheet` (source grep `grep -c 'setStyleSheet' musicstreamer/ui_qt/toast.py` returns exactly 2: one for the system-branch call, one for the non-system-branch call)
  </acceptance_criteria>
  <done>`_rebuild_stylesheet` exists; `__init__` calls it once; system branch produces IMMUTABLE legacy QSS; non-system branch produces palette-driven QSS with alpha=220 and lowercase ToolTipText hex; geometry/typography invariants hold in both branches.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add changeEvent override filtered on QEvent.PaletteChange ONLY (recursion guard)</name>
  <files>musicstreamer/ui_qt/toast.py</files>
  <read_first>
    - musicstreamer/ui_qt/toast.py (current state after Task 1)
    - musicstreamer/ui_qt/now_playing_panel.py:194-197 (changeEvent analog — order: handler logic FIRST, super() LAST)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §toast.py §New changeEvent override (lines 172-187) + §Shared Patterns §changeEvent (lines 615-635)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md §Risk Surface §1 (lines 490-498)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-03
  </read_first>
  <action>
    Add a `changeEvent(self, event) -> None` override to the ToastOverlay class. Place it adjacent to `_rebuild_stylesheet` (immediately after is fine), inside the `# --- Internal ---` section. Body — three logical lines:
      - `if event.type() == QEvent.PaletteChange:`
      - `    self._rebuild_stylesheet()`
      - `super().changeEvent(event)`

    CRITICAL filter narrowing — match on `QEvent.PaletteChange` ONLY, NOT the `(QEvent.PaletteChange, QEvent.StyleChange)` tuple that the analog at now_playing_panel.py:194-197 uses. Rationale: `setStyleSheet()` (called inside `_rebuild_stylesheet`) re-fires `QEvent.StyleChange`; matching on StyleChange would cause infinite recursion (RESEARCH §Risk 1, verified against Qt 6.11 docs + Qt Forum). The now_playing_panel and eq_response_curve analogs get away with both-event filters because they call setPalette()/update(), NOT setStyleSheet(). ToastOverlay is the FIRST `setStyleSheet`-inside-`changeEvent` site in the codebase and MUST narrow the filter.

    Ordering: handler logic FIRST, `super().changeEvent(event)` LAST (matches now_playing_panel.py:194-197 ordering — RESEARCH §2).

    Add a one-line comment above the `if event.type() ==` line explaining the narrowing: `# NB: PaletteChange ONLY — setStyleSheet() re-fires StyleChange (RESEARCH Risk 1).`

    Use the `# type: ignore[override]` comment on the def signature for codebase consistency with the now_playing_panel.py:194 analog.

    Verification of NO recursion: invoking the toast retint pathway under tests must not raise RecursionError. The verify command exercises a palette flip and asserts the toast retints exactly once.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; QT_QPA_PLATFORM=offscreen python -c "from PySide6.QtWidgets import QApplication, QWidget; import sys; app = QApplication.instance() or QApplication(sys.argv); from musicstreamer.ui_qt.toast import ToastOverlay; from musicstreamer.theme import THEME_PRESETS, build_palette_from_dict; app.setProperty('theme_name', 'system'); parent = QWidget(); toast = ToastOverlay(parent); qss_before = toast.styleSheet(); assert 'rgba(40, 40, 40, 220)' in qss_before; app.setProperty('theme_name', 'overrun'); app.setPalette(build_palette_from_dict(THEME_PRESETS['overrun'])); qss_after = toast.styleSheet(); assert qss_after != qss_before, 'changeEvent did not retrigger _rebuild_stylesheet'; assert 'rgba(26, 10, 24, 220)' in qss_after, f'overrun palette not picked up: {qss_after!r}'; assert 'color: #ffe8f4' in qss_after, f'overrun ToolTipText not picked up: {qss_after!r}'; print('OK')" &amp;&amp; grep -cE 'def changeEvent' /home/kcreasey/OneDrive/Projects/MusicStreamer/musicstreamer/ui_qt/toast.py | grep -q '^1$' &amp;&amp; (grep -v '^#' /home/kcreasey/OneDrive/Projects/MusicStreamer/musicstreamer/ui_qt/toast.py | grep -c 'QEvent\.StyleChange' | grep -q '^0$')</automated>
  </verify>
  <acceptance_criteria>
    - Source grep: `grep -cE 'def changeEvent' musicstreamer/ui_qt/toast.py` returns exactly 1
    - Source grep (filter narrowing): `grep -v '^#' musicstreamer/ui_qt/toast.py | grep -c 'QEvent\.StyleChange'` returns 0 (StyleChange MUST NOT appear in non-comment lines — recursion guard)
    - Source grep: `grep -cE 'QEvent\.PaletteChange' musicstreamer/ui_qt/toast.py` returns 1
    - Live behavior: starting with `theme_name='system'`, applying a non-system palette + setProperty mutation triggers `changeEvent(PaletteChange)`, which calls `_rebuild_stylesheet`. The toast's stylesheet for `overrun` contains `rgba(26, 10, 24, 220)` (from ToolTipBase #1a0a18) and `color: #ffe8f4` (from ToolTipText)
    - NO RecursionError raised during palette flip (the python script in verify returns 0)
    - super().changeEvent(event) appears AFTER the if block, not before (manual inspection — handler logic FIRST, super() LAST per RESEARCH §2)
    - `pytest tests/test_toast_overlay.py::test_14_stylesheet_color_contract -x` still passes if the test runs under default (no `theme_name` set) state because the legacy QSS branch fires when property is None (this test will be retrofitted in PLAN-06, but the underlying behavior must already support both code paths)
  </acceptance_criteria>
  <done>changeEvent override exists; filters on PaletteChange ONLY; handler logic first, super() last; live palette flip retints toast without recursion; non-comment grep for QEvent.StyleChange returns 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `theme_custom` JSON → palette → palette.color(ToolTipBase/ToolTipText).red()/.green()/.blue()/.name() | New consumption boundary for the two new keys; flows through the existing validator at `theme.py:179-186` upstream. By the time ToastOverlay reads palette colors, they have been validated by `_is_valid_hex` and converted via `QColor`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04 | Tampering (V5 Input Validation) | `toast.py::_rebuild_stylesheet` | accept | `palette().color(QPalette.ToolTipBase)` returns a `QColor`; `.red()/.green()/.blue()` are int methods bounded 0-255; `.name()` returns validated `#rrggbb` lowercase. F-string interpolation uses only these (integers and Qt-validated hex), never raw user-controlled strings. Upstream validation via `_is_valid_hex` at `theme.py:180` and `getattr(QPalette.ColorRole, role_name, None)` at `theme.py:182-184` ensures no malformed hex or unknown role reaches the palette (RESEARCH §Security Domain). |
| T-75-05 | Denial of Service (recursion) | `toast.py::changeEvent` | mitigate | Filter narrowed to `event.type() == QEvent.PaletteChange` ONLY (NOT StyleChange). `setStyleSheet()` re-fires `StyleChange` internally per Qt 6.11; matching on StyleChange would cause infinite recursion. Source-grep gate `grep -v '^#' toast.py | grep -c 'QEvent.StyleChange'` enforces 0 occurrences in non-comment lines. |
</threat_model>

<verification>
- Source grep gates from each task's acceptance_criteria.
- Behavior verification command in each task's `<automated>` block exercises both branches and the live retint path.
- No recursion (RecursionError) when triggering palette change.
- `pytest tests/test_toast_overlay.py -x` continues to pass for the 13 tests that do not exercise the styleSheet contract; test_14 (line 143) will be retrofitted in PLAN-06.
</verification>

<success_criteria>
- ToastOverlay has `_rebuild_stylesheet` and `changeEvent` methods.
- System branch produces the IMMUTABLE legacy QSS.
- Non-system branch produces palette-driven QSS with alpha=220 and lowercase ToolTipText hex.
- changeEvent filter narrowed to PaletteChange — non-comment source contains NO `QEvent.StyleChange` reference.
- Live palette flip retints the toast without recursion.
- All UI-SPEC §Visual Invariance Locks honored (border-radius, padding, no font-* properties, animations untouched, lifetime attributes preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-03-SUMMARY.md`.
</output>
