---
phase: 75
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - musicstreamer/ui_qt/theme_picker_dialog.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "Tile click on any preset/custom/system writes app.setProperty('theme_name', theme_id) BEFORE the existing app.setPalette() call"
    - "ToastOverlay sees correct theme_name when Qt dispatches PaletteChange synchronously inside setPalette()"
    - "Existing accent re-impose, JSON parse, _refresh_active_tile, and tile-click branch logic unchanged"
  artifacts:
    - path: "musicstreamer/ui_qt/theme_picker_dialog.py"
      provides: "_on_tile_clicked mirrors apply_theme_palette by setting QApplication property"
      contains: "app.setProperty(\"theme_name\""
  key_links:
    - from: "_on_tile_clicked(theme_id)"
      to: "app.setProperty('theme_name', theme_id)"
      via: "single line inserted right after app = QApplication.instance() at line 264"
      pattern: "app\\.setProperty\\(\"theme_name\""
---

<objective>
Mirror the Phase 75 PLAN-01 setProperty call inside `theme_picker_dialog._on_tile_clicked` so the live-preview tile-click path keeps the `QApplication.property("theme_name")` in sync with whatever theme is currently previewed. RESEARCH §1 identifies this as one of two mandatory write sites (the other is `apply_theme_palette` in PLAN-01).

Purpose: When the user clicks a tile, `app.setPalette()` synchronously dispatches `PaletteChange` to every QWidget, including ToastOverlay. ToastOverlay's `changeEvent` then reads `app.property("theme_name")` to decide system vs non-system QSS branch. The property MUST already hold the new theme_id by the time `setPalette()` fires its synchronous dispatch — hence the property is set BEFORE the palette mutation.

Output: `musicstreamer/ui_qt/theme_picker_dialog.py` with one new line in `_on_tile_clicked`. No other change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@musicstreamer/ui_qt/theme_picker_dialog.py
@musicstreamer/theme.py

<interfaces>
Existing _on_tile_clicked signature (theme_picker_dialog.py:260):
- def _on_tile_clicked(self, theme_id: str) -> None

Existing function body shape (lines 260-285):
- line 262-263: self._selected_theme_id = theme_id; self._active_tile_id = theme_id
- line 264: app = QApplication.instance()
- lines 266-278: three-way branch on theme_id (system/custom/preset) that calls app.setPalette(...)
- lines 280-283: accent re-impose (Phase 59 D-02 layering)
- line 285: self._refresh_active_tile()

PLAN-01 mirror site (apply_theme_palette at theme.py:206):
- theme_name = repo.get_setting("theme", "system")
- app.setProperty("theme_name", theme_name)  ← Phase 75 PLAN-01 added this

This plan adds the SAME setProperty call inside _on_tile_clicked, but the value is `theme_id` (the function parameter), not a repo lookup. Position: immediately after line 264 (`app = QApplication.instance()`), BEFORE any branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mirror app.setProperty("theme_name", theme_id) inside _on_tile_clicked</name>
  <files>musicstreamer/ui_qt/theme_picker_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/theme_picker_dialog.py lines 260-285 (_on_tile_clicked function body)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §theme_picker_dialog.py (lines 197-229)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md §1 Specific File:Line Answers (lines 282-291)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-10 path (b)
  </read_first>
  <action>
    Edit `musicstreamer/ui_qt/theme_picker_dialog.py::_on_tile_clicked`. Insert a single new line immediately AFTER the existing `app = QApplication.instance()` line (currently line 264) and BEFORE the `if theme_id == "system":` branch (currently line 266). The new line is exactly: `app.setProperty("theme_name", theme_id)`.

    Use the `theme_id` parameter (the function argument), NOT a repo lookup. This mirrors PLAN-01's call inside `apply_theme_palette` but the value source differs — the picker dialog has the theme_id directly from the tile-click signal.

    Rationale (RESEARCH §1): the property MUST be set BEFORE `setPalette()` because Qt dispatches `PaletteChange` synchronously inside `setPalette()`; ToastOverlay's `changeEvent` reads `app.property("theme_name")` inside that synchronous dispatch and needs to see the new theme_id, not the old one.

    DO NOT change any branch logic (system/custom/preset branches at lines 266-278 stay verbatim). DO NOT touch the accent re-impose block at lines 280-283. DO NOT touch `_refresh_active_tile` at line 285. No new imports — `QApplication` is already imported at the module level (visible at line 264). DO NOT add a docstring or comment line above the insertion (the surrounding code is uncommented; minimal diff).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; grep -cE 'app\.setProperty\("theme_name", theme_id\)' musicstreamer/ui_qt/theme_picker_dialog.py | grep -q '^1$' &amp;&amp; QT_QPA_PLATFORM=offscreen python -c "from PySide6.QtWidgets import QApplication; import sys; app = QApplication.instance() or QApplication(sys.argv); from musicstreamer.ui_qt.theme_picker_dialog import ThemePickerDialog; from musicstreamer.theme import THEME_PRESETS; class FakeRepo: 
 def __init__(self): self._d = {}
 def get_setting(self, k, default=''): return self._d.get(k, default)
 def set_setting(self, k, v): self._d[k] = v
repo = FakeRepo(); repo.set_setting('theme', 'system'); dlg = ThemePickerDialog(repo); dlg._on_tile_clicked('vaporwave'); assert app.property('theme_name') == 'vaporwave', f'expected vaporwave got {app.property(\"theme_name\")!r}'; dlg._on_tile_clicked('system'); assert app.property('theme_name') == 'system', f'expected system got {app.property(\"theme_name\")!r}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source grep: `grep -cE 'app\.setProperty\("theme_name", theme_id\)' musicstreamer/ui_qt/theme_picker_dialog.py` returns exactly 1
    - The new line appears AFTER `app = QApplication.instance()` and BEFORE the `if theme_id == "system":` branch (manual inspection — `grep -n 'app = QApplication.instance()' musicstreamer/ui_qt/theme_picker_dialog.py` line number is one less than `grep -n 'app.setProperty("theme_name", theme_id)' musicstreamer/ui_qt/theme_picker_dialog.py`)
    - Behavior: calling `dlg._on_tile_clicked("vaporwave")` results in `app.property("theme_name") == "vaporwave"`
    - Behavior: calling `dlg._on_tile_clicked("system")` results in `app.property("theme_name") == "system"`
    - All existing `tests/test_theme_picker_dialog.py` tests still pass: `pytest tests/test_theme_picker_dialog.py -x` returns 0 (no regression on the 17 pre-existing tile/apply/cancel/customize tests)
    - No other line in theme_picker_dialog.py is modified (`git diff musicstreamer/ui_qt/theme_picker_dialog.py | grep -E '^[-+]' | grep -v '^[-+]{3}' | wc -l` returns ≤ 1 net insertion, i.e. one `+` line and no `-` lines)
  </acceptance_criteria>
  <done>_on_tile_clicked sets app.setProperty("theme_name", theme_id) once, immediately after QApplication.instance() and before any branch; existing logic and tests unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `theme_id` (function parameter from tile-click signal) → `QApplication.setProperty` | `theme_id` originates from the picker dialog's own `_tiles` dict keys, which are set from the controlled `THEME_PRESETS` keys at dialog construction. No untrusted external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06 | Tampering (V5 — N/A) | `theme_picker_dialog.py::_on_tile_clicked` | accept | `theme_id` is constrained to the dialog's own controlled `_tiles` dict keys (sourced from `THEME_PRESETS` + `"custom"` literal). No path for external string injection. `QApplication.setProperty` accepts any `QVariant`-convertible value safely; downstream consumers (`apply_theme_palette`, `ToastOverlay._rebuild_stylesheet`) handle unknown values defensively (RESEARCH §6 §Risk Surface §2). |
</threat_model>

<verification>
- Source grep gate above.
- Behavior verification: tile-click → property reads back as expected for both preset and system branches.
- Regression: existing 17 picker tests still pass.
</verification>

<success_criteria>
- `app.setProperty("theme_name", theme_id)` called exactly once inside `_on_tile_clicked`, placed before any branch.
- ToastOverlay (and any future widget) sees the correct theme_name during the synchronous PaletteChange dispatch.
- No regression in existing picker tests.
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-04-SUMMARY.md`.
</output>
