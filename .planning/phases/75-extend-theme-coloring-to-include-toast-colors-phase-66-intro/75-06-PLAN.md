---
phase: 75
plan: 06
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - tests/test_toast_overlay.py
  - tests/test_theme.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "tests/test_toast_overlay.py:143 assertion gated to theme='system' explicit setup"
    - "Non-system theme QSS assertions land for at least one preset (vaporwave + overrun cover both bright + dark)"
    - "changeEvent(PaletteChange) triggers stylesheet rebuild — snapshot before/after assertion"
    - "Geometry + typography invariance asserted in BOTH branches"
    - "tests/test_theme.py covers 11-role EDITABLE_ROLES, per-preset locked hex pairs, system stays empty, apply_theme_palette sets QApplication property"
  artifacts:
    - path: "tests/test_toast_overlay.py"
      provides: "system-gated legacy assertion + non-system palette assertions + changeEvent retint + invariance locks"
      contains: "rgba(40, 40, 40, 220)"
    - path: "tests/test_theme.py"
      provides: "11-role count, locked hex pin per preset, system-stays-empty, apply_theme_palette property assertion"
      contains: "ToolTipBase"
  key_links:
    - from: "qapp.setProperty('theme_name', ...) + qapp.setPalette(...)"
      to: "ToastOverlay.changeEvent(PaletteChange) → _rebuild_stylesheet"
      via: "snapshot-mutate-assert (qss_before != qss_after)"
      pattern: "qss_after != qss_before"
---

<objective>
Land the test-surface coverage for the toast widget retint plumbing (PLAN-03) and the theme.py foundation (PLAN-01):

- `tests/test_toast_overlay.py`: gate the existing line-143 assertion to `theme='system'` setup; add tests for non-system palette-driven QSS, changeEvent-triggered rebuild, geometry invariance, typography invariance.
- `tests/test_theme.py`: retrofit `test_all_presets_cover_9_roles` to 11; add locked-hex pin per preset (12 assertions); assert system preset stays {}; assert EDITABLE_ROLES tail; assert apply_theme_palette sets `QApplication.property("theme_name")` for both preset and system inputs.

Purpose: This plan locks the Phase 75 behavioral contract in test code. Every UI-SPEC §Test surface row (75-UI-SPEC.md:298-316) that targets test_toast_overlay.py or test_theme.py lands here.

Output: Two test files extended additively (one assertion renamed, the rest are new tests). Both test files must run green after this plan: `pytest tests/test_toast_overlay.py tests/test_theme.py -x`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-VALIDATION.md
@tests/test_toast_overlay.py
@tests/test_theme.py
@musicstreamer/ui_qt/toast.py
@musicstreamer/theme.py

<interfaces>
Existing tests/test_toast_overlay.py fixtures:
- parent_widget (lines 19-26) — plain QWidget + qtbot.addWidget + qtbot.waitExposed
- 14 tests at lines 29-145 — preserve all of them

Existing tests/test_toast_overlay.py:140-144 (test_14_stylesheet_color_contract) — RETROFIT, NOT DELETE:
- def test_14_stylesheet_color_contract(qtbot, parent_widget):
-     toast = ToastOverlay(parent_widget)
-     qss = toast.styleSheet()
-     assert "rgba(40, 40, 40, 220)" in qss
-     assert "border-radius: 8px" in qss

UI-SPEC §Color LOCKED preset hex pairs (75-UI-SPEC.md:81-89):
- vaporwave: ToolTipBase #f9d6f0 → rgb(249, 214, 240), ToolTipText #3a2845
- overrun: ToolTipBase #1a0a18 → rgb(26, 10, 24), ToolTipText #ffe8f4
- gbs: ToolTipBase #2d5a2a → rgb(45, 90, 42), ToolTipText #f0f5e8
- gbs_after_dark: ToolTipBase #d5e8d3 → rgb(213, 232, 211), ToolTipText #0a1a0d
- dark: ToolTipBase #181820 → rgb(24, 24, 32), ToolTipText #f0f0f0
- light: ToolTipBase #2a2a32 → rgb(42, 42, 50), ToolTipText #f5f5f5

Existing tests/test_theme.py fixtures:
- repo (lines 129-135) — real Repo over temp SQLite
- qapp — pytest-qt builtin (used at line 140)

Existing tests/test_theme.py to retrofit:
- test_all_presets_cover_9_roles (lines 202-209) — rename to _11_roles, body changes only the assertion name + length expectation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retrofit test_toast_overlay.py:143 + add non-system / changeEvent / invariance tests</name>
  <files>tests/test_toast_overlay.py</files>
  <read_first>
    - tests/test_toast_overlay.py (entire file — 145 lines; parent_widget fixture lines 19-26; existing test 14 at lines 140-144)
    - musicstreamer/ui_qt/toast.py (current state after PLAN-03)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §tests/test_toast_overlay.py (lines 298-371)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Test surface (lines 298-316)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-13
  </read_first>
  <action>
    Edit `tests/test_toast_overlay.py` (extend; do NOT delete existing tests).

    (a) Retrofit `test_14_stylesheet_color_contract` at lines 140-144. Rename function to `test_14_stylesheet_system_theme_color_contract`. Add `qapp` to the parameter list (alongside `qtbot, parent_widget`). At the top of the body, before `ToastOverlay(parent_widget)`, add `qapp.setProperty("theme_name", "system")`. Keep the existing `assert "rgba(40, 40, 40, 220)" in qss` (now gated by the explicit system setup). Add additional assertions: `assert "color: white" in qss`, `assert "padding: 8px 12px" in qss` (existing border-radius assertion stays).

    (b) Add new test `test_stylesheet_non_system_uses_tooltip_palette(qtbot, parent_widget, qapp)`. Body: `qapp.setProperty("theme_name", "vaporwave")`; import `from musicstreamer.theme import THEME_PRESETS, build_palette_from_dict`; `qapp.setPalette(build_palette_from_dict(THEME_PRESETS["vaporwave"]))`; construct toast; assert toast.styleSheet() contains: `"rgba(249, 214, 240, 220)"` (vaporwave ToolTipBase rgb), `"color: #3a2845"` (vaporwave ToolTipText lowercase), `"border-radius: 8px"`, `"padding: 8px 12px"`.

    (c) Add new test `test_stylesheet_non_system_overrun_palette(qtbot, parent_widget, qapp)`. Same shape as (b) but for overrun: set property to "overrun", apply overrun palette, assert toast.styleSheet() contains `"rgba(26, 10, 24, 220)"` AND `"color: #ffe8f4"`. (Two preset hex pin tests cover bright+dark families.)

    (d) Add new test `test_changeEvent_palette_change_rebuilds_qss(qtbot, parent_widget, qapp)`. Snapshot-mutate-assert pattern: set property to "system", construct toast, snapshot `qss_before = toast.styleSheet()`; assert `"rgba(40, 40, 40, 220)" in qss_before`. Then set property to "vaporwave" and apply the vaporwave palette via setPalette (Qt dispatches PaletteChange synchronously). Read `qss_after = toast.styleSheet()`; assert `qss_after != qss_before`; assert `"rgba(249, 214, 240, 220)" in qss_after`.

    (e) Add new test `test_stylesheet_no_font_properties(qtbot, parent_widget, qapp)`. Iterate over four theme_name values: "system", "vaporwave", "overrun", "dark". For each, set the property, apply the matching palette if non-system (use `build_palette_from_dict(THEME_PRESETS[theme])` for non-system), construct a fresh ToastOverlay, read qss, assert: `"font-size:" not in qss`, `"font-family:" not in qss`, `"font-weight:" not in qss`. (Use `parent_widget` for each construction; multiple ToastOverlay instances under the same parent is fine — Phase 37 invariant: lifetime is parent-owned, fresh construct per-test is safe.)

    (f) Add new test `test_stylesheet_geometry_invariant_both_branches(qtbot, parent_widget, qapp)`. Same iteration as (e); for each theme, assert both `"border-radius: 8px"` and `"padding: 8px 12px"` are in the qss.

    Reuse existing `parent_widget` fixture verbatim. Use `qapp` from pytest-qt. No new fixtures. Add the imports at the top of the file (after existing imports): `from musicstreamer.theme import THEME_PRESETS, build_palette_from_dict`.

    IMPORTANT — cross-test pollution: the pytest-qt `qapp` is session-scoped. EVERY new test must set `qapp.setProperty("theme_name", ...)` explicitly at the start, never assume the property is None. The retrofit of test_14 already does this; the new tests must each do the same. Do NOT add an autouse cleanup fixture (additive minimum-diff per Phase 75 budget).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; pytest tests/test_toast_overlay.py -x -v 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing tests 1-13 still pass
    - `test_14_stylesheet_system_theme_color_contract` passes (renamed from test_14_stylesheet_color_contract) — sets `qapp.setProperty("theme_name", "system")` explicitly and asserts `"rgba(40, 40, 40, 220)"`, `"color: white"`, `"border-radius: 8px"`, `"padding: 8px 12px"`
    - `test_stylesheet_non_system_uses_tooltip_palette` passes — asserts vaporwave `"rgba(249, 214, 240, 220)"` + `"color: #3a2845"` + geometry
    - `test_stylesheet_non_system_overrun_palette` passes — asserts overrun `"rgba(26, 10, 24, 220)"` + `"color: #ffe8f4"`
    - `test_changeEvent_palette_change_rebuilds_qss` passes — snapshot before/after differs; non-system substring appears after flip
    - `test_stylesheet_no_font_properties` passes for all four theme branches
    - `test_stylesheet_geometry_invariant_both_branches` passes for all four theme branches
    - `pytest tests/test_toast_overlay.py -x` returns 0
    - Source grep: `grep -c 'test_14_stylesheet_color_contract' tests/test_toast_overlay.py` returns 0 (function renamed, no stale reference)
    - Source grep: `grep -c 'test_14_stylesheet_system_theme_color_contract' tests/test_toast_overlay.py` returns 1
    - Source grep: `grep -c '"rgba(249, 214, 240, 220)"' tests/test_toast_overlay.py` returns at least 2 (vaporwave palette test + changeEvent test)
  </acceptance_criteria>
  <done>tests/test_toast_overlay.py covers: system-gated legacy QSS, non-system vaporwave + overrun palette-driven QSS, changeEvent retint rebuild, no-font-properties invariance, geometry-invariant both branches. All 20 tests pass (14 pre-existing renamed + retrofit + 5 new).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Retrofit test_theme.py "9 roles" → "11 roles" + add locked-hex / system-empty / apply_theme_palette property tests</name>
  <files>tests/test_theme.py</files>
  <read_first>
    - tests/test_theme.py lines 196-226 (existing per-preset assertion patterns: `_GBS_LOCKED` dict + `test_gbs_preset_locked_hex_match` + `test_all_presets_cover_9_roles` + `test_apply_theme_palette_uses_repo_setting`)
    - musicstreamer/theme.py (current state after PLAN-01)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §tests/test_theme.py (lines 375-442)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Test surface (lines 306-310)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-15
  </read_first>
  <action>
    Edit `tests/test_theme.py` (extend; one rename + new tests).

    (a) Retrofit `test_all_presets_cover_9_roles` at lines 202-209. Rename function to `test_all_presets_cover_11_roles`. Update the docstring to read `"""Every non-system preset must define all 11 EDITABLE_ROLES (Phase 75 D-08)."""`. Add a new assertion at the TOP of the body: `assert len(EDITABLE_ROLES) == 11`. The existing inner for-loop already iterates `EDITABLE_ROLES` and works correctly — no logic change.

    (b) Add new test `test_tooltip_role_locked_hex_per_preset()` (no fixtures needed). Body: build a dict `expected = {"vaporwave": ("#f9d6f0", "#3a2845"), "overrun": ("#1a0a18", "#ffe8f4"), "gbs": ("#2d5a2a", "#f0f5e8"), "gbs_after_dark": ("#d5e8d3", "#0a1a0d"), "dark": ("#181820", "#f0f0f0"), "light": ("#2a2a32", "#f5f5f5")}`. For each `theme_id, (bg, fg) in expected.items()`, assert `THEME_PRESETS[theme_id]["ToolTipBase"] == bg` AND `THEME_PRESETS[theme_id]["ToolTipText"] == fg`. Docstring: `"""UI-SPEC §Color: 12 LOCKED ToolTipBase/ToolTipText hex assertions across 6 non-system presets."""`.

    (c) Add new test `test_system_preset_stays_empty()`. Body: `assert THEME_PRESETS["system"] == {}`. Docstring: `"""Phase 66 D-23 invariant — system sentinel must not gain toast keys."""`.

    (d) Add new test `test_editable_roles_appends_tooltip_pair_last()`. Body: `assert len(EDITABLE_ROLES) == 11; assert EDITABLE_ROLES[-2:] == ("ToolTipBase", "ToolTipText")`. Docstring: `"""Phase 75 D-05 — EDITABLE_ROLES grows to 11 with new keys APPENDED last (no reorder)."""`.

    (e) Add new test `test_apply_theme_palette_sets_theme_name_property(qapp, repo)`. Body: `repo.set_setting("theme", "gbs")`; `apply_theme_palette(qapp, repo)`; `assert qapp.property("theme_name") == "gbs"`.

    (f) Add new test `test_apply_theme_palette_sets_property_for_system(qapp, repo)`. Body: `repo.set_setting("theme", "system")`; `apply_theme_palette(qapp, repo)`; `assert qapp.property("theme_name") == "system"`.

    Use existing `qapp` (pytest-qt) and `repo` (lines 129-135 real Repo) fixtures verbatim. All new tests in (b)/(c)/(d) take no fixtures (pure pure-data assertions on THEME_PRESETS / EDITABLE_ROLES).

    DO NOT delete any existing test. DO NOT touch the `_GBS_LOCKED` dict at lines 114-126 (Phase 66 D-05 lock — verbatim brand-site hex). The new (b) test only ADDS assertions; the existing `test_gbs_preset_locked_hex_match` still passes.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; pytest tests/test_theme.py -x -v 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test_all_presets_cover_11_roles` passes — `len(EDITABLE_ROLES) == 11` AND for-loop over 11 roles × 6 presets succeeds
    - `test_tooltip_role_locked_hex_per_preset` passes — 12 assertions all green (6 presets × 2 keys, exact LOCKED hex values)
    - `test_system_preset_stays_empty` passes — `THEME_PRESETS["system"] == {}`
    - `test_editable_roles_appends_tooltip_pair_last` passes — length 11, tail is ("ToolTipBase", "ToolTipText")
    - `test_apply_theme_palette_sets_theme_name_property` passes — `qapp.property("theme_name") == "gbs"` after applying theme="gbs"
    - `test_apply_theme_palette_sets_property_for_system` passes — `qapp.property("theme_name") == "system"` after applying theme="system"
    - All pre-existing test_theme.py tests still pass (`test_gbs_preset_locked_hex_match`, `test_apply_theme_palette_uses_repo_setting`, `test_apply_theme_palette_loads_custom_json`, etc.)
    - `pytest tests/test_theme.py -x` returns 0
    - Source grep: `grep -c 'test_all_presets_cover_9_roles' tests/test_theme.py` returns 0
    - Source grep: `grep -c 'test_all_presets_cover_11_roles' tests/test_theme.py` returns 1
    - Source grep: `grep -c 'test_tooltip_role_locked_hex_per_preset' tests/test_theme.py` returns 1
  </acceptance_criteria>
  <done>tests/test_theme.py covers 11-role count, per-preset locked hex pin (12 assertions), system stays empty, EDITABLE_ROLES tail order, and apply_theme_palette QApplication property set for both preset and system inputs. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only changes. No new code paths through trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-08 | Tampering (V5 — N/A) | Test files only | accept | Tests are read-only consumers of THEME_PRESETS, EDITABLE_ROLES, apply_theme_palette, and ToastOverlay. They lock the behavioral contract introduced by PLAN-01 + PLAN-03. The V5 mitigation for theme_custom JSON lives upstream in `theme.py:179-186` (no Phase 75 change needed; see PLAN-01 threat model). |
</threat_model>

<verification>
- `pytest tests/test_toast_overlay.py tests/test_theme.py -x` returns 0
- Behavioral coverage matrix from UI-SPEC §Test surface (toast + theme rows) all green
- No deletion of existing tests; only one rename (test_14 → test_14_..._system_theme_...)
</verification>

<success_criteria>
- All 20+ tests in `tests/test_toast_overlay.py` pass (14 pre-existing + retrofit + 5 new).
- All tests in `tests/test_theme.py` pass (pre-existing + 6 new + 1 renamed).
- Behavior contracts locked: system-gated legacy QSS, non-system palette-driven QSS, changeEvent retint, geometry/typography invariants, 11-role EDITABLE_ROLES, per-preset hex pins, system stays empty, apply_theme_palette property.
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-06-SUMMARY.md`.
</output>
