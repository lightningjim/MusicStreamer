---
phase: 75
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - musicstreamer/ui_qt/theme_editor_dialog.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "ROLE_LABELS gains 'ToolTipBase': 'Toast background' and 'ToolTipText': 'Toast text' (UI-SPEC §Copywriting Contract)"
    - "Editor auto-grows to 11 _ColorRow instances via existing EDITABLE_ROLES iteration (no layout code change)"
    - "_on_role_color_changed sets app.setProperty('theme_name', 'custom') so toast retints correctly while editing from theme='system' (RESEARCH §Risk 8)"
  artifacts:
    - path: "musicstreamer/ui_qt/theme_editor_dialog.py"
      provides: "Two new ROLE_LABELS entries + setProperty mirror in _on_role_color_changed"
      contains: "Toast background"
  key_links:
    - from: "_on_role_color_changed (per-row swatch click)"
      to: "app.setProperty('theme_name', 'custom')"
      via: "RESEARCH §Risk 8 mitigation — ensures ToastOverlay sees 'custom' branch, not stale 'system'"
      pattern: "app\\.setProperty\\(\"theme_name\", \"custom\"\\)"
---

<objective>
Two additive edits to `musicstreamer/ui_qt/theme_editor_dialog.py`:
1. Append `"ToolTipBase": "Toast background"` and `"ToolTipText": "Toast text"` to the `ROLE_LABELS` dict (locked verbatim per UI-SPEC §Copywriting Contract).
2. Inside `_on_role_color_changed`, set `app.setProperty("theme_name", "custom")` so a user who enters the editor from `theme='system'` and starts editing sees the toast retint immediately to the "custom" branch (RESEARCH §Risk 8 mitigation, recommended YES in RESEARCH summary).

Purpose: The editor row-layout iterates `EDITABLE_ROLES` at lines 161-167; once PLAN-01 appends `ToolTipBase` + `ToolTipText` to that tuple, the editor auto-grows to 11 rows. The ONLY missing piece is the human-readable labels in `ROLE_LABELS` — without them, line 162 `label = ROLE_LABELS[role_name]` raises `KeyError`. The setProperty mirror handles the system→edit→toast-stays-grey edge case identified in RESEARCH §Risk 8.

Output: Two minimal edits in `theme_editor_dialog.py`. No layout code change, no new tests added here (test coverage lives in PLAN-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@musicstreamer/ui_qt/theme_editor_dialog.py
@musicstreamer/theme.py

<interfaces>
Existing ROLE_LABELS dict (theme_editor_dialog.py:37-47) — 9 entries currently:
- "Window": "Window background"
- "WindowText": "Window text"
- "Base": "Base"
- "AlternateBase": "Alternating row"
- "Text": "Body text"
- "Button": "Button background"
- "ButtonText": "Button text"
- "HighlightedText": "Selected text"
- "Link": "Hyperlink"

Existing iteration loop (theme_editor_dialog.py:161-167):
- for role_name in EDITABLE_ROLES:
- label = ROLE_LABELS[role_name]   ← KeyError if entry missing
- initial_hex = self._role_hex_dict.get(role_name, "#cccccc")
- row = _ColorRow(role_name, label, initial_hex, self)
- row.color_changed.connect(self._on_role_color_changed)
- root.addWidget(row)
- self._rows[role_name] = row

Existing _on_role_color_changed slot (theme_editor_dialog.py:253-268):
- Slot fired by _ColorRow.color_changed signal
- Updates self._role_hex_dict[role_name] = new_hex
- Calls app.setPalette() to apply the in-progress edit
- Re-imposes accent override (Phase 59 layering)

UI-SPEC §Copywriting Contract (75-UI-SPEC.md:190-194 + 215-223): locked labels are "Toast background" and "Toast text" (NOT "ToolTip background", NOT "Notification color"). User-facing term is "Toast" (the app's term).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append "ToolTipBase" and "ToolTipText" entries to ROLE_LABELS</name>
  <files>musicstreamer/ui_qt/theme_editor_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/theme_editor_dialog.py lines 37-47 (ROLE_LABELS dict)
    - musicstreamer/ui_qt/theme_editor_dialog.py lines 159-167 (iteration loop)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §theme_editor_dialog.py (lines 233-273)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Copywriting Contract (lines 215-223)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-05, D-08
  </read_first>
  <action>
    Edit `musicstreamer/ui_qt/theme_editor_dialog.py` ROLE_LABELS dict at lines 37-47. Append two new key-value pairs AFTER the existing `"Link": "Hyperlink"` entry: `"ToolTipBase": "Toast background"` and `"ToolTipText": "Toast text"`. Preserve the existing 9 entries verbatim — do NOT change "Window background", "Hyperlink", etc. Match formatting: 4-space indent, double-quoted keys/values, comma-terminated, trailing comma after the new last entry.

    The user-facing label uses "Toast" (the app's term) over "ToolTip" (Qt's role-name term) per UI-SPEC §Copywriting Contract. Labels are exactly `Toast background` and `Toast text` (single-word "Toast", lowercase second word, NO ellipsis, NO trailing colon — the swatch tooltip and dialog title are auto-derived from this label via `f"Edit {role_label}…"` at `_ColorRow.__init__` and similar).

    After this edit, the dict has exactly 11 entries.

    Layout code at lines 159-167 is UNCHANGED — it iterates `EDITABLE_ROLES` (which PLAN-01 already grew to 11) and looks up labels by name; the new entries fill that lookup. No layout, no setFixedHeight, no QScrollArea adjustments needed (UI-SPEC §Spacing confirms 11 rows fit comfortably).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; python -c "from musicstreamer.ui_qt.theme_editor_dialog import ROLE_LABELS; assert ROLE_LABELS['ToolTipBase'] == 'Toast background', f'got {ROLE_LABELS.get(\"ToolTipBase\")!r}'; assert ROLE_LABELS['ToolTipText'] == 'Toast text', f'got {ROLE_LABELS.get(\"ToolTipText\")!r}'; assert ROLE_LABELS['Window'] == 'Window background', 'Window label changed'; assert ROLE_LABELS['Link'] == 'Hyperlink', 'Link label changed'; assert len(ROLE_LABELS) == 11, f'expected 11 entries, got {len(ROLE_LABELS)}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ROLE_LABELS["ToolTipBase"] == "Toast background"`
    - `ROLE_LABELS["ToolTipText"] == "Toast text"`
    - `len(ROLE_LABELS) == 11`
    - Existing 9 entries unchanged (Window through Link labels preserved verbatim)
    - Source grep: `grep -c '"Toast background"' musicstreamer/ui_qt/theme_editor_dialog.py` returns 1
    - Source grep: `grep -c '"Toast text"' musicstreamer/ui_qt/theme_editor_dialog.py` returns 1
    - Editor dialog instantiates without `KeyError`: `python -c "from musicstreamer.ui_qt.theme_editor_dialog import ThemeEditorDialog"` imports cleanly
  </acceptance_criteria>
  <done>ROLE_LABELS contains 11 entries; new "ToolTipBase"/"ToolTipText" entries map to "Toast background"/"Toast text" verbatim; existing labels unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Set app.setProperty('theme_name', 'custom') inside _on_role_color_changed (RESEARCH Risk 8 mitigation)</name>
  <files>musicstreamer/ui_qt/theme_editor_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/theme_editor_dialog.py lines 253-268 (_on_role_color_changed slot)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md §Risk Surface §8 (lines 519-520) + §1 closing paragraph (lines 293-295)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §theme_editor_dialog.py §Adaptation notes (lines 269-274 — "Optional: RESEARCH §Risk 8 mitigation: ... Recommend YES")
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md "Claude's Discretion" item ("Whether ... _on_role_color_changed ..." — Recommended YES)
  </read_first>
  <action>
    Edit `_on_role_color_changed` slot inside `theme_editor_dialog.py` (lines 253-268). At the TOP of the method body (immediately after the def line and any docstring), add the two-line sequence: `app = QApplication.instance()` followed by `app.setProperty("theme_name", "custom")`. The value is the literal string `"custom"` — not a variable, not from repo.

    Rationale (RESEARCH §Risk 8): a user who picks `theme='system'` then opens Customize… enters the editor while `app.property("theme_name")` is `"system"`. As they edit roles, the running palette diverges from system, but ToastOverlay's QSS branch still reads `"system"` from the property and renders legacy dark-grey QSS — wrong preview during edit. Flipping the property to `"custom"` on the FIRST role edit makes the toast retint correctly to the in-progress palette.

    Placement: BEFORE the existing `self._role_hex_dict[role_name] = new_hex` write and BEFORE the `app.setPalette()` call. The QApplication.instance() retrieval may already exist further down in the function body; if so, you may consolidate the import or move the retrieval up — minimal-diff preferred (a duplicate `app = QApplication.instance()` is acceptable if the existing one is far down; alternatively, hoist the existing retrieval to the top). DO NOT introduce a None-check on `app` here — the slot only fires when the dialog is alive, which guarantees a running QApplication.

    DO NOT change the slot signature, the `_role_hex_dict` update logic, the `app.setPalette()` call, or the accent re-impose block. NO new imports — `QApplication` is already imported at module level (used elsewhere in the file).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; grep -cE 'app\.setProperty\("theme_name", "custom"\)' musicstreamer/ui_qt/theme_editor_dialog.py | grep -q '^1$' &amp;&amp; QT_QPA_PLATFORM=offscreen python -c "from PySide6.QtWidgets import QApplication; import sys; app = QApplication.instance() or QApplication(sys.argv); from musicstreamer.ui_qt.theme_editor_dialog import ThemeEditorDialog; class FakeRepo:
 def __init__(self): self._d = {}
 def get_setting(self, k, default=''): return self._d.get(k, default)
 def set_setting(self, k, v): self._d[k] = v
repo = FakeRepo(); repo.set_setting('theme', 'system'); app.setProperty('theme_name', 'system'); dlg = ThemeEditorDialog(repo, source_preset='vaporwave'); dlg._on_role_color_changed('Window', '#abcdef'); assert app.property('theme_name') == 'custom', f'expected custom got {app.property(\"theme_name\")!r}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source grep: `grep -cE 'app\.setProperty\("theme_name", "custom"\)' musicstreamer/ui_qt/theme_editor_dialog.py` returns exactly 1
    - Behavior: starting with `app.property("theme_name") == "system"`, invoking `dlg._on_role_color_changed("Window", "#abcdef")` results in `app.property("theme_name") == "custom"`
    - The setProperty call is placed BEFORE the `app.setPalette()` call inside the same method (manual inspection — line-ordering check)
    - All existing `tests/test_theme_editor_dialog.py` tests still pass: `pytest tests/test_theme_editor_dialog.py -x -k "not 9_color"` returns 0 (the 9_color test is retrofitted in PLAN-07; all other tests must continue to pass)
    - The slot signature, _role_hex_dict update, setPalette call, and accent re-impose block are unchanged
  </acceptance_criteria>
  <done>_on_role_color_changed sets app.setProperty("theme_name", "custom") at the top of the slot, before any other mutation; toast retints correctly when user edits roles while entering from theme='system'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds two labels and one setProperty call. ROLE_LABELS values are dialog-local strings, never user input. The setProperty value is the literal `"custom"`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-07 | Tampering (V5 — N/A) | `theme_editor_dialog.py` | accept | All new values are literal strings hardcoded in the source. No external input flows into ROLE_LABELS or the setProperty call. RESEARCH §Security Domain identifies V5 as the only applicable ASVS category for Phase 75, and V5 applies to the `theme_custom` JSON consumption boundary in `theme.py` (covered by PLAN-01 threat model), not the editor's UI labels or property mirror. |
</threat_model>

<verification>
- Source grep gates above.
- Behavior verification: editor instantiates without KeyError; _on_role_color_changed flips property from 'system' to 'custom'.
- Regression: existing editor tests pass (except the 9-row count test, which PLAN-07 will retrofit).
</verification>

<success_criteria>
- ROLE_LABELS has 11 entries with "Toast background" and "Toast text" labels.
- Editor auto-grows to 11 rows via existing EDITABLE_ROLES iteration (no layout code change).
- _on_role_color_changed sets QApplication property "theme_name" to "custom" on every role edit.
- No regression in existing editor tests other than the 9-row count test (retrofitted in PLAN-07).
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-05-SUMMARY.md`.
</output>
