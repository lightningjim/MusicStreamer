# SECURITY.md — Phase 87.1 Audit

**Phase:** 87.1 — GBS.FM session-expiry re-login prompt (surface GbsAuthExpired)
**Audit Date:** 2026-06-18
**ASVS Level:** 1
**block_on:** high
**Threats Closed:** 15/15
**Open Threats (BLOCKERS):** 0

---

## Threat Verification

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| T-87.1-01 | Elevation of Privilege | mitigate | CLOSED | `gbs_relogin_handler.py:81` — `if self._login_proc is not None: return`; test `test_single_flight_dedup` at `tests/test_gbs_relogin_handler.py:38` |
| T-87.1-02 | Denial of Service | mitigate | CLOSED | `gbs_relogin_handler.py:223` — `self._login_proc = None` BEFORE emit in `_on_error`; FailedToStart guard verified; test `test_failed_to_start_resets_guard` at `tests/test_gbs_relogin_handler.py:178` |
| T-87.1-03 | Tampering | mitigate | CLOSED | `gbs_relogin_handler.py:188-193` — writes only to `paths.gbs_cookies_path()`, `os.makedirs(exist_ok=True)`, `os.chmod(cookies_path, 0o600)` immediately after write; test `test_relogin_succeeded_on_valid_cookies` asserts `mode == 0o600` at `tests/test_gbs_relogin_handler.py:121` |
| T-87.1-04 | Tampering | mitigate | CLOSED | `gbs_relogin_handler.py:179-187` — 4-condition gate: `exit_code == 0 AND netscape_text AND success_category AND _validate_gbs_cookies(netscape_text)`; test `test_relogin_failed_on_nonzero_exit` at `tests/test_gbs_relogin_handler.py:129` |
| T-87.1-05 | Information Disclosure | mitigate | CLOSED | `gbs_relogin_handler.py:216-219` — only `QProcess.ProcessError` enum logged in `_on_error`; no cookie bytes in `_on_finished`; comment at line 128 documents the constraint; source review confirms no credential bytes passed to any logging call |
| T-87.1-06 | Tampering | mitigate | CLOSED | `now_playing_panel.py:1297-1300` — `worker.auth_expired.connect(self._on_gbs_expiry_detected, _Qt.QueuedConnection)`; `gbs_marquee.py:476` — `auth_expired = Signal()` (parameterless); QueuedConnection enforced in `attach_gbs_marquee_worker` |
| T-87.1-07 | Information Disclosure | accept | CLOSED | `gbs_marquee.py:624` — `_log.warning("gbs.marquee.auth_expired url=%s", MARQUEE_URL)` logs only the public constant `MARQUEE_URL`; no cookies or credentials; log line is unchanged from pre-phase behavior |
| T-87.1-08 | Tampering | mitigate | CLOSED | `now_playing_panel.py:707` — `_expiry_primary.setTextFormat(Qt.TextFormat.PlainText)`; `now_playing_panel.py:714` — `_expiry_sub.setTextFormat(Qt.TextFormat.PlainText)`; both label strings are static literals |
| T-87.1-09 | Spoofing | mitigate | CLOSED | `now_playing_panel.py:3152` — `if token != self._gbs_poll_token: return` at top of `_on_gbs_playlist_error`; `now_playing_panel.py:2996` — `_gbs_expiry_widget.setVisible(False)` in `_refresh_gbs_visibility` non-GBS branch (navigate-away hide); test `test_gbs_expiry_prompt_hides_on_station_change` at `tests/test_now_playing_gbs_expiry.py:202` |
| T-87.1-10 | Elevation of Privilege | mitigate | CLOSED | `now_playing_panel.py:3215` — `self._gbs_relogin_btn.setEnabled(False)` before calling handler; handler single-flight guard at `gbs_relogin_handler.py:81` is second layer; test `test_relogin_button_click_launches_after_detection` at `tests/test_now_playing_gbs_expiry.py:330` asserts `isEnabled() is False` |
| T-87.1-04-01 | Spoofing | accept | CLOSED | Accepted risk: detection keys on `/accounts/login/` substring in Location header from `gbs.fm` over TLS; worst case is a benign re-login prompt with no credential leak; login target is the trusted `oauth_helper` subprocess, not the redirect |
| T-87.1-04-02 | Information Disclosure | mitigate | CLOSED | `gbs_api.py:201,212` — `GbsAuthExpiredError` message carries only the request URL (`f"Session expired (302→login from {url})"`); no response body or cookie values included |
| T-87.1-04-03 | Denial of Service | mitigate | CLOSED | `now_playing_panel.py:3157` — `self._gbs_poll_timer.stop()` in `_on_gbs_playlist_error` auth_expired branch; no new timer introduced in Phase 87.1 Plan 04 |
| T-87.1-05-01 | Elevation of Privilege | mitigate | CLOSED | `now_playing_panel.py:3172` — `_on_gbs_playlist_error` auth_expired branch calls `self._on_gbs_expiry_detected()` (reveal-only); `now_playing_panel.py:3217` — `notify_expiry_detected` called only from `_on_gbs_relogin_clicked`; tests at `tests/test_now_playing_gbs_expiry.py:267,304` assert `start_calls == 0` on detection, line 326 asserts `start_calls == 1` on click |
| T-87.1-05-02 | Denial of Service | accept | CLOSED | Accepted risk: single-flight de-dup (WR-03) in `GbsReloginHandler` is unchanged and guards button-gated launch; `_on_gbs_expiry_detected` calls only `setVisible(True)` which is idempotent; no new timer or retry introduced |

---

## Accepted Risks Log

| Threat ID | Rationale |
|-----------|-----------|
| T-87.1-07 | Existing `_log.warning` for marquee auth_expired logs only the public `MARQUEE_URL` constant. No credentials touched. No change required. |
| T-87.1-04-01 | 302 Location auth-expiry detection over TLS from gbs.fm. Worst-case spoofed redirect produces a benign user-visible re-login prompt. Login subprocess is the trusted `oauth_helper`, not the redirect target. Accepted by design. |
| T-87.1-05-02 | Concurrent expiry signals from playlist poller and marquee worker both call the reveal-only `_on_gbs_expiry_detected` (idempotent `setVisible(True)`). Subprocess spam is blocked by the button-disable + handler single-flight guard. Accepted. |
| T-87.1-SC (01/04/05) | No package installs in any plan of this phase. All primitives (PySide6.QtCore.QProcess, gbs_api, subprocess_utils, paths) were present before Phase 87.1. No Package Legitimacy Gate required. |

---

## Unregistered Flags

None. All threat flags in SUMMARY.md files map to registered threat IDs (T-87.1-04-02 in 87.1-04-SUMMARY.md, T-87.1-05-01 in 87.1-05-SUMMARY.md). No new attack surface appeared without a threat mapping.

---

## Scope

Plans audited: 87.1-01 through 87.1-05.
Files audited: `musicstreamer/ui_qt/gbs_relogin_handler.py`, `musicstreamer/gbs_marquee.py`, `musicstreamer/gbs_api.py`, `musicstreamer/ui_qt/now_playing_panel.py`, `tests/test_gbs_relogin_handler.py`, `tests/test_now_playing_gbs_expiry.py`.

---

## Security Audit 2026-06-18

| Metric | Count |
|--------|-------|
| Threats found | 15 |
| Closed | 15 |
| Open | 0 |

Register origin: `register_authored_at_plan_time: true` (all 5 PLANs authored a `<threat_model>` STRIDE block). Auditor ran in verify-mitigations mode (no new-threat scan). gsd-security-auditor verdict: **## SECURED**, 15/15 closed.
