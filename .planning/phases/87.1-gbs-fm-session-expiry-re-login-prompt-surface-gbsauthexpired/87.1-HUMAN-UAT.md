---
status: partial
phase: 87.1-gbs-fm-session-expiry-re-login-prompt-surface-gbsauthexpired
source: [87.1-VERIFICATION.md]
started: 2026-06-17T00:00:00Z
updated: 2026-06-17T20:10:00Z
---

## Current Test

[detection fix landed in Plan 87.1-04 — Tests 1 & 2 are now UNBLOCKED and awaiting a human with a live GBS session to re-run]

## Tests

### 1. Real GBS login from inline prompt refreshes the playlist
expected: With a deliberately expired/cleared GBS cookie, playing a GBS.FM station shows the inline "GBS session expired" prompt in the now-playing panel. Clicking "Log in again" opens the real `oauth_helper --mode gbs` WebEngine login window. After a real login, the expiry prompt hides, `_gbs_playlist_widget` becomes visible with playlist data, the poll timer restarts (≤15s), and the marquee updates on the next cadence tick.
result: pending
note: "Original blocker (no prompt on real expiry) is FIXED by Plan 87.1-04 — _open_with_cookies now detects 302→/accounts/login/ and raises GbsAuthExpiredError under the real urllib redirect chain (regression-tested at AbstractHTTPHandler.do_open). Detection is verified by automation; the real expired-session → real oauth_helper login → playlist-resume chain still needs a human re-run."

### 2. Cancel leaves a clear inline state with audio + ICY live
expected: Trigger expiry, click "Log in again", then close the login window without logging in. The expiry prompt remains visible (non-dismissive), the "Log in again" button is re-enabled for retry, and audio playback + ICY now-playing metadata continue uninterrupted. No spinner, toast, or empty-state appears (no silent dead-end).
result: pending
note: "Previously blocked by the Test 1 detection bug; now reachable since the prompt can appear. Awaiting human re-run."

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

- truth: "On real GBS session expiry while a GBS.FM station is active, the inline 'GBS session expired' prompt appears in the now-playing panel (GBS-AUTH-EXP-01)."
  status: resolved
  resolution: "Detection root cause FIXED by gap-closure Plan 87.1-04 (commits f8d07bfd GREEN, 831801c4 AUDIT). _open_with_cookies now installs a no-redirect handler, inspects the stopped 302's Location, and raises GbsAuthExpiredError when it contains /accounts/login/; non-login 3xx are re-followed; 200 JSON passes through unchanged. _fetch_marquee gained a narrow `except GbsAuthExpiredError: raise` so the marquee auth_expired Signal is reachable. Five real-chain regression tests (patched at urllib AbstractHTTPHandler.do_open — not at _open_with_cookies) lock the behavior; full GBS/oauth/now-playing surface (353 tests) green. NOTE: code defect resolved + automation-verified, but the end-to-end Human UAT Tests 1 & 2 (real expired session + real oauth_helper login) remain PENDING human re-run — see Tests above."
  severity: blocker
  test: 1
  root_cause: "musicstreamer/gbs_api.py:_open_with_cookies (lines 158-172) builds its opener with only HTTPCookieProcessor — no no-redirect handler. urllib's default HTTPRedirectHandler AUTO-FOLLOWS the 302→/accounts/login/ and returns the 200 login page, so the `except HTTPError`/302 branch (lines 169-172) is dead code for GET redirects and GbsAuthExpiredError is never raised. fetch_active_playlist then does json.loads(login_page_html) → JSONDecodeError. Phase 87.1 CONTEXT assumed 'detection ALREADY exists (done)'; that assumption is false for the GET playlist/marquee/token/search paths. Confirmed live: _open_with_cookies('/ajax') returned status 200, final URL https://gbs.fm/accounts/login/?next=/ajax — GbsAuthExpiredError NOT raised."
  artifacts:
    - path: "musicstreamer/gbs_api.py"
      issue: "_open_with_cookies follows the 302→login (default redirect handler) instead of detecting it; the HTTPError-302 branch never fires for GET. Affects every GET caller: fetch_active_playlist, fetch_user_tokens, search, fetch_artist_songs, fetch_album_songs (and the marquee fetch if it routes through here)."
      resolved_by: "Plan 87.1-04 — _NoRedirect handler + Location /accounts/login/ detection raises GbsAuthExpiredError."
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      issue: "_GbsPollWorker.run (lines 109-113) only emits 'auth_expired' when isinstance(exc, GbsAuthExpiredError); the JSONDecodeError falls to the generic-error branch → silent fallback, no expiry prompt. Correct once detection upstream is fixed."
      resolved_by: "Upstream detection fixed — GbsAuthExpiredError now propagates, so _GbsPollWorker.run emits 'auth_expired' and the inline prompt path is reachable."
  missing: []
  debug_session: ""
