---
status: complete
phase: 87.1-gbs-fm-session-expiry-re-login-prompt-surface-gbsauthexpired
source: [87.1-VERIFICATION.md, 87.1-05-SUMMARY.md]
started: 2026-06-17T00:00:00Z
updated: 2026-06-18T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Real GBS login from inline prompt refreshes the playlist
expected: With a deliberately expired/cleared GBS cookie, playing a GBS.FM station shows the inline "GBS session expired" prompt in the now-playing panel — and the login window does NOT auto-launch. Clicking "Log in again" (and only then) opens the real `oauth_helper --mode gbs` WebEngine login window. After a real login, the expiry prompt hides, `_gbs_playlist_widget` becomes visible with playlist data, the poll timer restarts (≤15s), and the marquee updates on the next cadence tick.
result: pass
prior_result: "issue — 'It's there now but it's auto launching the login window' (auto-launch defect, fixed by 87.1-05 commit b9cc5d83)"
note: "Re-test 2026-06-18: prompt appears, no auto-launch, login opens only on button click, playlist refreshes after real login. Auto-launch defect confirmed resolved."

### 2. Cancel leaves a clear inline state with audio + ICY live
expected: Trigger expiry, click "Log in again", then close the login window without logging in. The expiry prompt remains visible (non-dismissive), the "Log in again" button is re-enabled for retry, and audio playback + ICY now-playing metadata continue uninterrupted. No spinner, toast, or empty-state appears (no silent dead-end).
result: pass
prior_result: "blocked (prior-phase) — unreachable while detection auto-launched; unblocked by 87.1-05"
note: "Re-test 2026-06-18: closed login window without logging in — prompt stayed visible, 'Log in again' re-enabled, audio + ICY metadata continued uninterrupted, no dead-end. GBS-AUTH-EXP-03 confirmed live."

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "On real GBS session expiry, the inline prompt appears and the oauth_helper login window opens ONLY when the user clicks 'Log in again' (D-01 / GBS-AUTH-EXP-01 — user-initiated, non-dismissive)."
  status: failed
  reason: "User reported: 'It's there now but it's auto launching the login window.' The prompt appears (detection works), but the login subprocess auto-launches on detection instead of on button click."
  severity: major
  test: 1
  root_cause: "Auth-expiry detection auto-fires the login subprocess. `_on_gbs_playlist_error('auth_expired')` shows the inline prompt (now_playing_panel.py:3168 setVisible(True)) and THEN calls `self._gbs_relogin_handler.notify_expiry_detected()` (now_playing_panel.py:3172), which unconditionally calls `_launch()` (gbs_relogin_handler.py:74-83 — only guarded by the single-flight `_login_proc is not None` check, not by user intent). The marquee worker's `auth_expired` Signal is likewise wired straight to `notify_expiry_detected` (now_playing_panel.py:1296-1297), so the marquee poller also auto-launches. The intended user-initiated path — the 'Log in again' button → `_on_gbs_relogin_clicked` → `notify_expiry_detected()` (now_playing_panel.py:3214) — is thereby pre-empted. Net effect: the prompt and the login window appear together with no user action, defeating the non-dismissive/user-initiated contract and making Test 2's cancel flow untestable."
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py:3172"
      issue: "_on_gbs_playlist_error auth_expired branch calls notify_expiry_detected() directly after showing the prompt — this is the auto-launch the user observed. Should only show the prompt; launch must be gated behind the button click (line 3214)."
    - path: "musicstreamer/ui_qt/now_playing_panel.py:1296-1297"
      issue: "marquee worker.auth_expired connected directly to handler.notify_expiry_detected — the marquee poller auto-launches the login window with no user action. Should surface/ensure the inline prompt instead of launching."
    - path: "musicstreamer/ui_qt/gbs_relogin_handler.py:74-83"
      issue: "notify_expiry_detected() unconditionally _launch()es (only single-flight guarded). Consider separating 'a consumer detected expiry → ensure prompt shown' from 'user requested re-login → launch subprocess', so detection events never spawn the window."
  missing:
    - "Gate the oauth_helper launch behind the explicit 'Log in again' button click only. Remove the direct notify_expiry_detected() call in _on_gbs_playlist_error (now_playing_panel.py:3172) so detection only reveals the inline prompt; keep the launch on _on_gbs_relogin_clicked (line 3214)."
    - "Marquee path (USER DECISION 2026-06-17): on marquee auth_expired, SURFACE the inline expiry prompt (reveal/ensure _gbs_expiry_widget visible) rather than auto-launching the login subprocess. Rewire now_playing_panel.py:1296-1297 to a panel slot that shows the prompt instead of connecting straight to handler.notify_expiry_detected. Do NOT no-op — the marquee must still signal expiry via the prompt when only the marquee is active (zero-token/collapsed)."
    - "Preserve single-flight de-dup and the GBS-AUTH-EXP-03 cancel behavior (prompt stays, button re-enabled) once the launch is user-gated."
    - "Add a regression test asserting that an auth_expired playlist error AND a marquee auth_expired both show the prompt but do NOT start the QProcess until the 'Log in again' button is clicked."
  debug_session: ""
