# Phase 87.1: GBS.FM Session-Expiry Re-login Prompt - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-17
**Phase:** 87.1-gbs-fm-session-expiry-re-login-prompt-surface-gbsauthexpired
**Areas discussed:** Prompt surface & form (selected); Login launch path / Post-login retry-refresh / Scope-unify call sites (deferred to Claude's discretion)

---

## Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Prompt surface & form | Where/how the re-login affordance appears for the failed playlist load | ✓ |
| Login launch path | Direct `oauth_helper --mode gbs` vs open AccountsDialog focused on GBS | |
| Post-login retry/refresh | What auto-recovers on successful re-login | |
| Scope: unify call sites | Migrate existing passive toasts onto the shared handler | |

**User's choice:** Discuss only "Prompt surface & form"; capture context for the rest.

---

## Prompt surface & form

### Q1 — Form of the prompt

| Option | Description | Selected |
|--------|-------------|----------|
| Inline panel state | Persistent inline message + "Log in again" button replacing the failed playlist sub-region | ✓ |
| Actionable toast | Clickable toast launching login (auto-fades, conflicts with non-dismissive) | |
| Banner across panel | Reuse announcement_banner pattern (dismissible, fights non-dismissive) | |

**User's choice:** Inline panel state.

### Q2 — Playback while prompt is shown

| Option | Description | Selected |
|--------|-------------|----------|
| Keep playing, swap only GBS area | Audio + ICY metadata stay live; only GBS sub-region shows the prompt | ✓ |
| Prompt takes over panel | Inline prompt occupies main now-playing area | |

**User's choice:** Keep playing, swap only the GBS active-playlist sub-region.

### Q3 — Poll timer behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Stop until re-login | Halt poll while expired; resume only after successful re-login | ✓ |
| Keep polling quietly | Timer keeps running, re-302s each interval | |

**User's choice:** Stop polling until re-login succeeds.

### Q4 — Wording

| Option | Description | Selected |
|--------|-------------|----------|
| Session expired + button | "GBS session expired" + sub-line + "Log in again" button | ✓ |
| Minimal action-only | Just a "Log in to GBS again" button, no explanation | |
| You decide | Claude's discretion, follow existing toast voice | |

**User's choice:** "GBS session expired" + explanatory sub-line + "Log in again" button (exact micro-copy refinable in planning).

---

## Claude's Discretion

User chose "Capture context now" for the remaining three gray areas. Recommended directions recorded in CONTEXT.md `<decisions>` → Claude's Discretion:
- **Login launch path** — bias toward a shared launcher that starts `oauth_helper --mode gbs` directly (one-click re-login), extracted from the AccountsDialog QProcess wiring; this IS the shared handler core (GBS-AUTH-EXP-02).
- **Post-login retry/refresh** — shared handler emits "re-login succeeded"; playlist loader reloads + resumes poll, marquee refreshes; explicit user actions (vote/submit) re-triggered by the user.
- **Scope: unify call sites** — keep phase anchored on playlist loader + reusable shared handler (87 marquee / 87b zero-token); migrate other toasts only if low-cost.
- **Concurrent expiry / single-flight** — shared handler de-dupes so only one re-login launch is active at a time.

## Deferred Ideas

- Full consistency sweep migrating every passive "reconnect via Accounts" toast onto the actionable handler — opportunistic only this phase; can be a follow-up.
- Changing the marquee's quiet-failure policy (D-18) into a user-visible surface — a planning decision, not assumed.
