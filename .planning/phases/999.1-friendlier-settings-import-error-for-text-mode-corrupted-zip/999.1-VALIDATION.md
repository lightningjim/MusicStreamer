---
phase: 999.1
slug: friendlier-settings-import-error-for-text-mode-corrupted-zip
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-19
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x (pyproject `[tool.pytest.ini_options]`, `testpaths=["tests"]`) |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `.venv/bin/python -m pytest tests/test_settings_export.py -q` |
| **Full suite command** | `.venv/bin/python -m pytest -q` |
| **Estimated runtime** | quick ~5s; full suite >600s (scope to the file during dev) |

**Note:** Use `.venv/bin/python` — system `python3` lacks PySide6 and yields false failures. The detection logic lives in `settings_export.py` (pure stdlib `zipfile`) and its unit tests MUST NOT import Qt, so they run fast and headless.

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest tests/test_settings_export.py -q`
- **After every plan wave:** Run the same file-scoped command (this phase touches only settings_export + a UI handler branch)
- **Before `/gsd:verify-work`:** `tests/test_settings_export.py` must be green
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.1-01-xx | 01 | 1 | REQ (text-mode detection) | T-999.1-01 (zip-bomb/oversize read) | Bounded full-file read of a SMALL settings zip; PK magic + `\x0d\x0a` scan; no decompression of attacker data | unit | `.venv/bin/python -m pytest tests/test_settings_export.py -q` | ✅ | ⬜ pending |
| 999.1-01-xx | 01 | 1 | REQ (generic+hint fallback, D-03) | — | Non-text-mode BadZipFile → generic+hint message, no misclassification | unit | `.venv/bin/python -m pytest tests/test_settings_export.py -q` | ✅ | ⬜ pending |
| 999.1-02-xx | 02 | 2 | REQ (dialog presentation, D-02/D-05) | — | Sentinel string surfaces modal `QMessageBox.warning`, not a truncated toast | manual + unit-where-feasible | see Manual-Only below | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Exact task IDs assigned by the planner; this map seeds the validation contract — planner refines to match real task numbers.*

---

## Wave 0 Requirements

- [x] `tests/test_settings_export.py` — already exists (35 tests, including `test_preview_invalid_zip` matching `"Not a valid ZIP archive"` as a regex substring — survives the D-03 longer string). No new framework needed.

*Existing infrastructure covers all automated phase requirements — new tests are added to the existing file.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Modal dialog visually appears with locked title/body | D-02 / D-05 | Requires a running Qt event loop + real file-picker import; pure-logic unit tests deliberately avoid Qt | In-app: import a settings zip that was transferred in text mode (or a fixture mangled LF→CRLF). Confirm a modal "Settings import failed" dialog appears with body "This backup looks corrupted by a text-mode file transfer. Re-copy it in binary mode and try again." and a single OK/Close button — NOT a transient toast. |

*The signal→handler wiring (sentinel `"TEXT_MODE_ZIP"` → dialog branch) may also be exercised with pytest-qt if cheap; the dialog's visual appearance is the manual confirmation.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (none — existing test file)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
