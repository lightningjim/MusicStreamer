---
phase: 999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip
plan: 02
type: execute
wave: 2
depends_on:
  - 999.1-01
files_modified:
  - musicstreamer/ui_qt/main_window.py
autonomous: false
requirements:
  - REQ-999.1-DIALOG
user_setup: []

must_haves:
  truths:
    - "The import-preview worker catches TextModeCorruptionError specifically and emits the sentinel 'TEXT_MODE_ZIP' (D-02 discriminator)"
    - "The text-mode case surfaces a modal QMessageBox.warning, NOT the truncating toast (D-02)"
    - "The dialog shows the exact locked D-05 title and body wording"
    - "All other import errors still fall through to the existing toast (D-03 generic tier)"
    - "Only the file-picker settings-import path is touched; the Flatpak wizard is unchanged (D-04)"
  artifacts:
    - path: "musicstreamer/ui_qt/main_window.py"
      provides: "TextModeCorruptionError catch in _ImportPreviewWorker.run + sentinel branch in _on_import_preview_error"
      contains: "TEXT_MODE_ZIP"
  key_links:
    - from: "musicstreamer/ui_qt/main_window.py _ImportPreviewWorker.run"
      to: "_on_import_preview_error"
      via: "error.emit('TEXT_MODE_ZIP') sentinel over the QThread error signal"
      pattern: "TEXT_MODE_ZIP"
    - from: "_on_import_preview_error sentinel branch"
      to: "QMessageBox.warning"
      via: "modal dialog with D-05 title/body"
      pattern: "QMessageBox\\.warning"
---

<objective>
Wire the typed `TextModeCorruptionError` (from Plan 01) through the import-preview
worker and into a modal recovery dialog. `_ImportPreviewWorker.run()` catches the typed
error specifically and emits the sentinel string `"TEXT_MODE_ZIP"` (mirroring the
established `"auth_expired"` idiom). `_on_import_preview_error` branches on that sentinel
to show `QMessageBox.warning` with the exact D-05 wording, returning before the existing
toast path; all other errors keep the current toast (D-03 generic tier).

Purpose: Surface the actionable, fully-readable recovery instruction as a modal the user
must acknowledge (D-02) instead of an 80-char-truncated transient toast.

Output: Two edits to `main_window.py` — a specific `except` in the worker and a sentinel
branch in the error handler. No new imports (QMessageBox already imported at line 40).
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-CONTEXT.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-RESEARCH.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-VALIDATION.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-01-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor extends. Extracted from codebase 2026-06-19. -->

main_window.py:40 — QMessageBox is already imported (no new import needed).

main_window.py:106-121 — _ImportPreviewWorker (current run body):
  error = Signal(str)
  def run(self):
      try:
          repo = Repo(db_connect())
          result = settings_export.preview_import(self._zip_path, repo)
          self.preview_finished.emit(result)
      except Exception as exc:
          self.error.emit(str(exc))

main_window.py:138-150 — _GbsImportWorker sentinel precedent ("auth_expired"):
  if isinstance(exc, gbs_api.GbsAuthExpiredError):
      self.error.emit("auth_expired")
  else:
      self.error.emit(str(exc))

main_window.py:1528-1534 — _on_import_preview_error (current handler):
  def _on_import_preview_error(self, msg: str) -> None:
      self._end_busy()
      truncated = msg[:80] + "…" if len(msg) > 80 else msg
      self.show_toast(f"Invalid settings file: {truncated}")

From Plan 01: musicstreamer.settings_export.TextModeCorruptionError (subclass of ValueError).
EXCLUDED (D-04): musicstreamer/ui_qt/flatpak_import_wizard.py — do not modify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit TEXT_MODE_ZIP sentinel from _ImportPreviewWorker and branch to modal dialog</name>
  <files>musicstreamer/ui_qt/main_window.py</files>
  <read_first>
    - musicstreamer/ui_qt/main_window.py line 40 (confirm QMessageBox already imported)
    - musicstreamer/ui_qt/main_window.py lines 106-121 (_ImportPreviewWorker.run — add specific except)
    - musicstreamer/ui_qt/main_window.py lines 138-150 (_GbsImportWorker auth_expired sentinel analog to mirror)
    - musicstreamer/ui_qt/main_window.py lines 955-987 (existing QMessageBox actionable-dialog precedent)
    - musicstreamer/ui_qt/main_window.py lines 1505-1534 (_on_import_settings wiring + _on_import_preview_error)
  </read_first>
  <action>
    In `_ImportPreviewWorker.run()` (lines 114-121), add a specific
    `except settings_export.TextModeCorruptionError:` clause BEFORE the existing
    `except Exception as exc:` clause (ordering matters — the broad clause must come last
    or the sentinel is never emitted). In the new clause, `self.error.emit("TEXT_MODE_ZIP")`.
    Leave the generic clause emitting `str(exc)` unchanged. `settings_export` is already
    imported at module scope (used at line 118); reference
    `settings_export.TextModeCorruptionError` directly — do not add a new import.

    In `_on_import_preview_error` (lines 1528-1534), after the existing
    `self._end_busy()` first line, add a branch: `if msg == "TEXT_MODE_ZIP":` call
    `QMessageBox.warning(self, "Settings import failed", "This backup looks corrupted by a
    text-mode file transfer. Re-copy it in binary mode and try again.")` then `return`.
    Use the EXACT D-05 title and body wording verbatim (single OK button — the static
    `warning` helper provides it). The existing `truncated`/`show_toast` lines stay below
    the branch and continue to handle every other error (D-03 generic tier). Do NOT touch
    `_on_import_preview_ready`, the worker wiring at 1512-1519, or
    `flatpak_import_wizard.py` (D-04 excludes the Flatpak path).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast,sys; src=open('musicstreamer/ui_qt/main_window.py').read(); assert 'TextModeCorruptionError' in src and src.count('TEXT_MODE_ZIP')>=2 and 'Settings import failed' in src and 'Re-copy it in binary mode and try again.' in src; ast.parse(src); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_ImportPreviewWorker.run` has `except settings_export.TextModeCorruptionError:` placed BEFORE `except Exception`, emitting `self.error.emit("TEXT_MODE_ZIP")`
    - `_on_import_preview_error` calls `self._end_busy()` first, then `if msg == "TEXT_MODE_ZIP": QMessageBox.warning(self, "Settings import failed", "This backup looks corrupted by a text-mode file transfer. Re-copy it in binary mode and try again."); return`
    - The exact D-05 title ("Settings import failed") and body string are present verbatim
    - All other error messages still reach the existing `show_toast` path (D-03)
    - No new import added (QMessageBox already at line 40); flatpak_import_wizard.py untouched (D-04)
    - `main_window.py` parses (ast.parse succeeds) and the sentinel token appears in both emit and branch sites
  </acceptance_criteria>
  <done>Worker emits the sentinel for text-mode errors; handler shows the D-05 modal for the sentinel and the toast for everything else; module parses cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the modal renders with the locked D-05 wording</name>
  <action>Manual visual confirmation only — no code changes. Confirm the text-mode modal and the unchanged generic toast per the steps below.</action>
  <what-built>
    The file-picker settings-import path now detects a text-mode-corrupted backup and
    shows a modal QMessageBox ("Settings import failed" with the binary-mode recovery
    instruction) instead of the truncated toast. Detection + raise logic (Plan 01) is
    unit-tested headless; the modal's visual appearance requires a running Qt event loop
    and a real import action, so it is confirmed manually here.
  </what-built>
  <how-to-verify>
    1. Prepare a text-mode-corrupted fixture: take any valid settings backup `.zip`
       exported from the app and rewrite each lone `0x0A` byte to `0x0D 0x0A` (simulating
       a text-mode transfer). On Linux:
       `.venv/bin/python -c "import sys; p=sys.argv[1]; b=open(p,'rb').read(); out=bytearray(); prev=0
       \nfor c in b:\n    out.extend(b'\r\n') if (c==0x0a and prev!=0x0d) else out.append(c)\n    prev=c\nopen(p+'.mangled.zip','wb').write(bytes(out))" /path/to/backup.zip`
       (or reuse the LF->CRLF mangling helper added in Plan 01's tests).
    2. Launch the app with `.venv/bin/python -m musicstreamer` (or the project's normal run command).
    3. Open Settings -> Import Settings (the file-picker path), and select the
       `*.mangled.zip` file from step 1.
    4. CONFIRM: a MODAL dialog appears titled exactly "Settings import failed" with body
       exactly "This backup looks corrupted by a text-mode file transfer. Re-copy it in
       binary mode and try again." and a single OK/Close button — NOT a transient toast,
       and the text is NOT truncated.
    5. CONFIRM (regression): import a different, genuinely-garbage non-zip file (e.g. a
       renamed .txt). It should still show the EXISTING toast ("Invalid settings file: ..."),
       not the modal.
  </how-to-verify>
  <verify>
    <human-check>Modal shows exact D-05 title/body for the text-mode fixture; generic toast still fires for a non-zip file.</human-check>
  </verify>
  <acceptance_criteria>
    - Modal titled "Settings import failed" with the exact D-05 body appears for the text-mode-mangled fixture (not a toast, not truncated)
    - A genuinely-garbage non-zip still triggers the existing "Invalid settings file: ..." toast
  </acceptance_criteria>
  <done>Human confirms the D-05 modal and the unchanged generic toast.</done>
  <resume-signal>Type "approved" if the modal shows the exact D-05 wording and the generic toast still fires for other errors; otherwise describe what appeared.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QThread worker -> UI thread | The error sentinel string crosses the QueuedConnection signal hop into the UI handler |
| file picker -> settings-import path | User selects a `.zip`; only this path is in scope (D-04 excludes Flatpak wizard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-04 | Spoofing (sentinel collision) | `error.emit("TEXT_MODE_ZIP")` vs a real error message that happens to equal the sentinel | accept | Mirrors the established `"auth_expired"` sentinel idiom in the same file; no real exception message is the bare token `TEXT_MODE_ZIP`, and the only producer is the specific `except TextModeCorruptionError` clause. Residual severity: LOW. |
| T-999.1-05 | Information disclosure | dialog body text | mitigate | Body is a fixed locked D-05 string with no interpolation of the file path or exception detail — no user/filesystem data leaks into the modal. Residual severity: LOW. |
| T-999.1-06 | Elevation/scope creep | Flatpak import wizard | mitigate | D-04 excludes `flatpak_import_wizard.py`; this plan does not touch it. Acceptance criteria assert it is unchanged. Residual severity: LOW. |

No npm/pip/cargo installs in this plan — no T-999.1-SC supply-chain checkpoint required.
Residual severity across the register: LOW. No high-severity items.
</threat_model>

<verification>
- `.venv/bin/python -c "import ast; ast.parse(open('musicstreamer/ui_qt/main_window.py').read())"` succeeds
- Sentinel "TEXT_MODE_ZIP" appears at both the emit site and the handler branch
- Exact D-05 title and body strings present verbatim
- flatpak_import_wizard.py shows no diff (D-04)
- Human checkpoint confirms the modal renders with locked wording and the toast still fires for other errors
</verification>

<success_criteria>
- _ImportPreviewWorker catches TextModeCorruptionError specifically and emits the sentinel (D-02 discriminator)
- _on_import_preview_error shows the D-05 modal for the sentinel and the toast for all else (D-02 + D-03)
- Only the file-picker path changed; Flatpak wizard untouched (D-04)
- Human verification of the modal passes
</success_criteria>

<output>
Create `.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-02-SUMMARY.md` when done
</output>
