---
phase: 87-gbs-fm-marquee-themed-day-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png
  - tests/fixtures/gbs_themed_logos/canonical-001.png
  - tests/fixtures/gbs_themed_logos/MANIFEST.md
  - tests/fixtures/gbs_marquee/2026-05-25_homepage.html
  - tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json
  - tests/fixtures/gbs_marquee/MANIFEST.md
autonomous: false
requirements:
  - GBS-THEME-06
  - GBS-MARQ-06
  - GBS-MARQ-07
tags:
  - gbs.fm
  - themed-day
  - fixture-harvest
  - requirements-edit

must_haves:
  truths:
    - "REQUIREMENTS.md GBS-MARQ-06 cites paths.gbs_cookies_path() + gbs_api.load_auth_context(), NOT gbs_auth.py / QWebEngineProfile (D-07)"
    - "ROADMAP.md Phase 87 Success Criterion #4 cites the cookies-jar reuse path, NOT gbs_auth.py constants (D-08)"
    - "tests/fixtures/gbs_themed_logos/ contains today's live themed-day PNG (Memorial Day 'da troops' window, D-01)"
    - "tests/fixtures/gbs_marquee/ contains raw harvested bytes from gbs.fm homepage + /ajax cold-start (Pitfall #3)"
    - "Both fixture directories contain a MANIFEST.md sidecar mapping each file to (capture date, SHA-256, label, source URL, capture method) per D-06"
    - "D-02: Harvest is inline in this phase (no /gsd:spike split) — Plan 87-01 IS the harvest. Resolved structurally by this plan existing as Wave 1."
    - "D-03: Harvest plan committed BEFORE Plan 87-02 (parser-lock) — wave dependency 87-02 → [87-01] enforces this. Researcher RESEARCH.md already consumed the harvest premise; no re-research after this commit."
    - "D-10: Marquee endpoint identity locked by Plan 87-02 (parser-lock) from this plan's raw captures (homepage HTML + /ajax cold). No live re-probe by the researcher after Plan 87-02; the endpoint URL constant in gbs_marquee.py is the canonical lock."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Rewritten GBS-MARQ-06 wording per D-07"
      contains: "paths.gbs_cookies_path"
    - path: ".planning/ROADMAP.md"
      provides: "Rewritten Phase 87 Success Criterion #4 per D-08"
      contains: "paths.gbs_cookies_path"
    - path: "tests/fixtures/gbs_themed_logos/MANIFEST.md"
      provides: "Schema + entries for themed and canonical logo fixtures"
      contains: "SHA-256"
    - path: "tests/fixtures/gbs_marquee/MANIFEST.md"
      provides: "Schema + entries for marquee snapshots"
      contains: "capture date"
  key_links:
    - from: ".planning/REQUIREMENTS.md GBS-MARQ-06"
      to: "musicstreamer/paths.py gbs_cookies_path()"
      via: "verbatim citation per Pitfall #1 replacement text"
      pattern: "paths\\.gbs_cookies_path"
    - from: "tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png"
      to: "MANIFEST.md row recording its SHA-256"
      via: "hashlib.sha256(file_bytes).hexdigest()"
      pattern: "[0-9a-f]{64}"
---

<objective>
Plan 87-01 is the TIME-SENSITIVE harvest wave. Today (2026-05-25) is Memorial Day and gbs.fm is serving the live "da troops" themed logo plus a marquee that almost certainly contains the trigger phrase. Themed-day windows are limited; the next is Halloween 2026 (~5 months out). This wave captures the live data while ALSO performing the two MANDATORY source-artifact edits (REQUIREMENTS.md GBS-MARQ-06 rewrite per D-07; ROADMAP.md Phase 87 Success Criterion #4 rewrite per D-08). Editing the source artifacts BEFORE the harvest makes the rewritten requirements the explicit contract under which the rest of Phase 87 ships.

Purpose:
- Lock the rewritten GBS-MARQ-06 + ROADMAP SC#4 wording so downstream plans build against the cookies-jar reality, not the phantom gbs_auth.py / QWebEngineProfile framing.
- Capture today's live themed-day fixtures (logo PNG + raw marquee bytes from BOTH likely sources per Pitfall #3) for Plan 87-02 parser-lock + Plan 87-04 themed-day correlator.
- Establish the fixture directory layout + MANIFEST.md schema that all future themed-day windows accrete into (per D-04 / D-06).

Output:
- Rewritten REQUIREMENTS.md GBS-MARQ-06
- Rewritten ROADMAP.md Phase 87 Success Criterion #4
- `tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png` + canonical-001.png (if available) + MANIFEST.md
- `tests/fixtures/gbs_marquee/2026-05-25_homepage.html` + `2026-05-25_ajax_cold.json` + MANIFEST.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@musicstreamer/gbs_api.py
@musicstreamer/paths.py

<interfaces>
<!-- Phase 76 cookies + auth surface that GBS-MARQ-06 must cite verbatim. -->
<!-- Quoting these prevents the phantom gbs_auth.py / QWebEngineProfile framing (D-07/D-08, Pitfall #1). -->

From musicstreamer/paths.py:54-60:
```python
def gbs_cookies_path() -> str:
    """Phase 60 D-04 (ladder #3 LOCKED): GBS.FM session cookies file.

    Mirrors the YouTube cookies path shape. Pure — does not create the
    directory. Caller writes with 0o600 perms (Phase 999.7 convention).
    """
    return os.path.join(_root(), "gbs-cookies.txt")
```

From musicstreamer/gbs_api.py:92-113:
```python
def load_auth_context() -> Optional[http.cookiejar.MozillaCookieJar]:
    """Return a loaded MozillaCookieJar from paths.gbs_cookies_path() or None."""
```

From musicstreamer/gbs_api.py:158-172:
```python
def _open_with_cookies(url, cookies, timeout=_TIMEOUT_READ):
    """Send GET with cookies; return urlopen response. Raises GbsAuthExpiredError on 302→/accounts/login/."""
```

Dev fixture cookies: `~/.local/share/musicstreamer/dev-fixtures/gbs.fm.cookies.txt`. Phase 76 sessionid was set to expire 2026-05-17 — confirm freshness before harvest.
</interfaces>

<assumptions>
- The user is the operator and can refresh Phase 76 GBS dev cookies if needed before harvest fires. This plan is `autonomous: false` — Task 2 (live harvest) pauses for the operator.
- Anonymous-fallback per D-11 covers the case where dev cookies are stale: Task 2's harvest script tries cookies first, then anonymous urlopen if `load_auth_context()` returns None.
- Today is the Memorial Day "da troops" window per ROADMAP.md and CONTEXT.md. If the live page has already rolled over to a non-themed canonical state, capture what is live and continue — record the actual condition in MANIFEST.md.
- Pitfall #3 marquee-endpoint ambiguity is resolved AFTER this plan (by Plan 87-02 dissecting the raw bytes); Plan 87-01 captures BOTH likely sources (`/` homepage HTML + `/ajax` cold-start) so the researcher's dissection has full coverage.
- GBS-MARQ-07's literal "10+ samples" is implicitly relaxed (Pitfall #8): Plan 87-01 commits the real-captured samples; synthetic variations (parser-robustness samples) land in Plan 87-02 alongside the parser.
- Pitfall #2 (Player signals don't exist) is noted but not actioned by this plan — Plan 87-03 wires through `NowPlayingPanel.bind_station` + `NowPlayingPanel.on_playing_state_changed` instead.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md GBS-MARQ-06 + ROADMAP.md Phase 87 Success Criterion #4</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md (D-07 + D-08 verbatim replacement text)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md §Pitfall #1 (verbatim replacement quotes)
    - .planning/REQUIREMENTS.md lines 47-63 (current GBS-THEME + GBS-MARQ block)
    - .planning/ROADMAP.md lines 125-138 (current Phase 87 entry + Success Criteria 1-5)
    - musicstreamer/paths.py:54-60 (gbs_cookies_path docstring — source citation)
    - musicstreamer/gbs_api.py:92-113 (load_auth_context — source citation)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Locate the line beginning `- [ ] **GBS-MARQ-06**:` (currently around line 62 — verify by grep before edit). Replace the entire bullet body with the verbatim D-07 rewrite text from 87-CONTEXT.md / 87-RESEARCH.md Pitfall #1:

    `- [ ] **GBS-MARQ-06**: The marquee fetcher reuses Phase 76 GBS authenticated session via paths.gbs_cookies_path() (cookies-jar file) and musicstreamer.gbs_api.load_auth_context() (loader). The marquee module imports these — does NOT construct a QWebEngineProfile, does NOT write a parallel cookies file, and does NOT instantiate oauth_helper. A source-grep drift-guard test (test_marquee_module_reuses_phase76_auth_only) confirms.`

    Then edit `.planning/ROADMAP.md`. Locate Phase 87's Success Criteria block (currently lines 132-137). Replace Success Criterion #4 wording (the line that currently cites `GBS_WEB_PROFILE_NAME` + `GBS_WEB_STORAGE_PATH` + `musicstreamer/gbs_auth.py`) with the D-08 verbatim text:

    `4. Marquee fetcher imports paths.gbs_cookies_path() + musicstreamer.gbs_api.load_auth_context(); source-grep drift-guard confirms no parallel cookie file is written and no QtWebEngine session is instantiated.`

    Preserve surrounding success-criteria numbering (1, 2, 3, 4, 5). Do not modify other success criteria. Do not touch the Phases checklist line (Phase 87 description) — only the Success Criteria block.

    After both edits: run a grep sanity check on the affected files:
    - `grep -n "GBS_WEB_PROFILE_NAME" .planning/REQUIREMENTS.md .planning/ROADMAP.md` MUST return empty.
    - `grep -n "gbs_auth\.py" .planning/REQUIREMENTS.md .planning/ROADMAP.md` MUST return empty.
    - `grep -n "paths.gbs_cookies_path" .planning/REQUIREMENTS.md .planning/ROADMAP.md` MUST find at least 2 hits (one per file).

    These edits implement D-07 + D-08 verbatim. The rewritten wording is the contract Plan 87-06's drift-guard will enforce.
  </action>
  <acceptance_criteria>
    - `grep -c "paths.gbs_cookies_path" .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c "paths.gbs_cookies_path" .planning/ROADMAP.md` returns ≥ 1.
    - `grep -E "GBS_WEB_PROFILE_NAME|GBS_WEB_STORAGE_PATH|gbs_auth\.py" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns no hits (exit non-zero from grep is OK).
    - `grep -n "load_auth_context" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns ≥ 2 hits.
    - Surrounding requirement IDs (GBS-MARQ-01..05, GBS-MARQ-07, GBS-THEME-01..06) are textually unchanged (verify by diff scope).
  </acceptance_criteria>
  <verify>
    <automated>! grep -E "GBS_WEB_PROFILE_NAME|GBS_WEB_STORAGE_PATH|gbs_auth\.py" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>REQUIREMENTS.md GBS-MARQ-06 cites cookies-jar path; ROADMAP.md Phase 87 SC#4 cites cookies-jar path; phantom-framing identifiers absent from both files.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: TIME-SENSITIVE live fixture harvest — Memorial Day "da troops" window</name>
  <files>
    tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png,
    tests/fixtures/gbs_themed_logos/canonical-001.png,
    tests/fixtures/gbs_themed_logos/MANIFEST.md,
    tests/fixtures/gbs_marquee/2026-05-25_homepage.html,
    tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json,
    tests/fixtures/gbs_marquee/MANIFEST.md
  </files>
  <read_first>
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md §canonical_refs §Dev fixture (live capture target — TODAY)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md §Pitfall #3 (fixture-capture script sketch — lines 506-534)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md §D-06 (fixture layout + MANIFEST schema)
    - musicstreamer/gbs_api.py:92-172 (load_auth_context + _open_with_cookies — auth surface used by harvest)
  </read_first>
  <what-built>
    A one-shot harvest script (NOT committed to musicstreamer/ — runs once from a scratch location, e.g. `/tmp/87-01-harvest.py`) that:
    1. Verifies / refreshes the dev cookies file at `~/.local/share/musicstreamer/dev-fixtures/gbs.fm.cookies.txt` (per CONTEXT canonical_refs — sessionid was set to expire 2026-05-17, may need refresh).
    2. Uses `urllib.request.HTTPCookieProcessor` with `MozillaCookieJar.load()` of the dev cookies file to GET:
       - `https://gbs.fm/images/logo_3.png` → `tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png`
       - `https://gbs.fm/` → `tests/fixtures/gbs_marquee/2026-05-25_homepage.html`
       - `https://gbs.fm/ajax?position=0&last_comment=0&last_removal=0&last_add=0&now_playing=0` → `tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json`
    3. Computes `hashlib.sha256(logo_bytes).hexdigest()` of the themed PNG and the canonical PNG (if Kyle has a browser-cached non-themed `logo_3.png` available — copy it to `tests/fixtures/gbs_themed_logos/canonical-001.png` and hash it; if no canonical available, document this in MANIFEST.md and proceed — canonical entries accrete per D-04).
    4. Writes `tests/fixtures/gbs_themed_logos/MANIFEST.md` and `tests/fixtures/gbs_marquee/MANIFEST.md` (D-06 schema: markdown table mapping filename → capture_date, sha256, theme_label / source_url, capture_method, notes). Examples of schema rows in the body below.
    5. Anonymous fallback per D-11: if `load_auth_context()` returns None OR if cookies are stale (302→login), retry each GET with plain `urllib.request.urlopen` (no cookie handler). Record `capture_method = anonymous` in MANIFEST when this path fires.

    Suggested MANIFEST.md schema (D-06):

    ```markdown
    # GBS Themed Logos — Fixture MANIFEST

    Schema: filename | capture_date | sha256 | theme_label | source_url | capture_method | notes

    | filename | capture_date | sha256 | theme_label | source_url | capture_method | notes |
    |----------|--------------|--------|-------------|------------|-----------------|-------|
    | 2026-05-25_memorial-day_da-troops.png | 2026-05-25 | <hash> | da troops (Memorial Day) | https://gbs.fm/images/logo_3.png | cookies | live harvest |
    | canonical-001.png | <date or "unknown — browser cache"> | <hash> | canonical | https://gbs.fm/images/logo_3.png | <method> | non-themed baseline |
    ```

    The harvest script is throwaway — it lives at `/tmp/87-01-harvest.py` or in the operator's scratch space, NOT committed. Only the captured fixture files + MANIFESTs are committed.
  </what-built>
  <how-to-verify>
    Operator (Kyle) executes the harvest with valid Phase 76 GBS dev cookies and confirms by checking:

    1. `ls tests/fixtures/gbs_themed_logos/` shows at least `2026-05-25_memorial-day_da-troops.png` + `MANIFEST.md`. If `canonical-001.png` was available from browser cache, it is also present.
    2. `ls tests/fixtures/gbs_marquee/` shows `2026-05-25_homepage.html` + `2026-05-25_ajax_cold.json` + `MANIFEST.md`.
    3. `python3 -c "import hashlib, pathlib; print(hashlib.sha256(pathlib.Path('tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png').read_bytes()).hexdigest())"` prints a 64-char hex digest that matches the MANIFEST entry for that file.
    4. `wc -c tests/fixtures/gbs_marquee/2026-05-25_homepage.html` reports a non-zero byte count (sanity: gbs.fm homepage is not empty).
    5. `grep -i "da troops" tests/fixtures/gbs_marquee/2026-05-25_homepage.html tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json` SHOULD return at least one hit somewhere (confirms today's themed marquee text was captured — Pitfall #3 evidence). If neither file contains "da troops" / "memorial" / "troops" substrings, operator records the actual observed marquee text in MANIFEST.md `notes` column AND flags this in the resume signal — Plan 87-04's correlator design may need to widen the keyword set.
    6. MANIFEST.md files contain markdown tables matching the D-06 schema described above.
  </how-to-verify>
  <resume-signal>
    Type "harvest committed" (and any notes about cookie freshness / observed marquee text / anonymous-fallback usage) when the fixtures + MANIFESTs are in place. If today's live page does NOT show the expected themed-day state (logo is canonical, or marquee lacks the trigger phrase), describe what was observed instead so Plan 87-04 can decide whether to widen the keyword set or whether the harvest must wait for the next themed window.
  </resume-signal>
  <acceptance_criteria>
    - `test -s tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png` (file exists, non-zero size).
    - `test -s tests/fixtures/gbs_marquee/2026-05-25_homepage.html` (file exists, non-zero size).
    - `test -s tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json` (file exists, non-zero size).
    - `test -s tests/fixtures/gbs_themed_logos/MANIFEST.md` (MANIFEST present, non-zero size).
    - `test -s tests/fixtures/gbs_marquee/MANIFEST.md` (MANIFEST present, non-zero size).
    - MANIFEST.md files contain a markdown table header row with columns `filename`, `capture_date`, `sha256`, and at least the themed PNG row.
    - SHA-256 in MANIFEST equals the computed digest of the corresponding file (re-hash spot-check passes).
  </acceptance_criteria>
  <verify>
    <automated>test -s tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png && test -s tests/fixtures/gbs_marquee/2026-05-25_homepage.html && test -s tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json && test -s tests/fixtures/gbs_themed_logos/MANIFEST.md && test -s tests/fixtures/gbs_marquee/MANIFEST.md</automated>
  </verify>
  <done>Themed-day PNG + marquee snapshots from both candidate endpoints + MANIFESTs committed; SHA-256 of the themed PNG recorded in MANIFEST and reproducible by re-hashing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → harvest script | Throwaway script reads Phase 76 dev cookies; treat as a one-shot ingress |
| gbs.fm → fixture files | Operator-controlled HTML/JSON/PNG bytes land on disk for future test consumption |
| Repo → committed fixtures | Source-controlled bytes; once committed, downstream plans treat as canonical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01-01 | Information Disclosure | Dev cookies file at `~/.local/share/musicstreamer/dev-fixtures/gbs.fm.cookies.txt` | mitigate | Phase 76 already enforces 0o600 perms; harvest READS only, never re-writes; throwaway script never copies cookies into the repo |
| T-87-01-02 | Information Disclosure | Captured `2026-05-25_ajax_cold.json` may embed session-bound user state (username, queue items) | mitigate | Operator MUST eyeball the captured JSON before commit and redact any PII fields (username, email, IP). MANIFEST.md `notes` column records redaction actions |
| T-87-01-03 | Tampering | gbs.fm operator could serve unexpected content during harvest window | accept | Acceptable — Plan 87-04's correlator is intentionally tolerant via D-12 fallback ("unknown_theme_observed"); harvest captures whatever is live |
| T-87-01-04 | Repudiation | MANIFEST entries claim a SHA-256 that doesn't match the file | mitigate | Acceptance check spot-checks one hash; Plan 87-04's fixture-loader test re-hashes every committed file and asserts MANIFEST parity |
| T-87-01-05 | Spoofing | Stale dev cookies authenticate to a different account or fail loudly | mitigate | D-11 anonymous fallback: if cookies stale (302→login), retry with plain urlopen; record `capture_method=anonymous` in MANIFEST so Plan 87-02 parser-lock knows the cookie path was not exercised |
| T-87-01-SC | Tampering | No package installs in this plan | n/a | Plan 87-01 introduces zero new packages; slopcheck N/A |
</threat_model>

<verification>
- `grep -c "paths.gbs_cookies_path" .planning/REQUIREMENTS.md` ≥ 1 AND `grep -c "paths.gbs_cookies_path" .planning/ROADMAP.md` ≥ 1
- `grep -E "GBS_WEB_PROFILE_NAME|GBS_WEB_STORAGE_PATH|gbs_auth\.py" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns empty (D-07 / D-08 enforced)
- All five fixture files + two MANIFEST.md files present and non-empty
- MANIFEST.md SHA-256 matches `hashlib.sha256(file_bytes).hexdigest()` for the themed-day PNG (spot-check)
- Phase 76 dev cookies remain at 0o600 and have NOT been re-written by the harvest script
</verification>

<success_criteria>
- REQUIREMENTS.md and ROADMAP.md edits land per D-07 / D-08 exact wording; no phantom-framing identifiers remain in either file.
- Today's live themed-day fixtures are committed under `tests/fixtures/gbs_themed_logos/` and the candidate marquee bytes are committed under `tests/fixtures/gbs_marquee/`.
- Both fixture directories include a MANIFEST.md sidecar following the D-06 schema.
- Plan 87-02 (parser-lock) has the raw bytes it needs to identify the marquee endpoint and delimiter without re-probing gbs.fm.
- Plan 87-04 (themed-day correlator) has at least one harvested themed PNG + its SHA-256 to seed `GBS_LOGO_BASELINE_HASHES`.
</success_criteria>

<output>
Create `.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-01-SUMMARY.md` when done. Record:
- Whether the cookie path or anonymous fallback was used (D-11 outcome).
- The SHA-256 of the harvested themed PNG (for Plan 87-04 to consume directly).
- A one-line summary of what `grep -i "da troops"` found in the marquee captures (Pitfall #3 evidence pointer for Plan 87-02 dissection).
- Whether any canonical (non-themed) `logo_3.png` was available from local cache; if yes, its SHA-256.
- A pointer to the rewritten REQUIREMENTS.md and ROADMAP.md lines (line numbers post-edit).
</output>
