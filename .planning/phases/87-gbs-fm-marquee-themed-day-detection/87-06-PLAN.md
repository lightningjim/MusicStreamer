---
phase: 87-gbs-fm-marquee-themed-day-detection
plan: 06
type: execute
wave: 6
depends_on:
  - 87-05
files_modified:
  - tests/test_gbs_marquee_drift_guard.py
  - todos/2026-05-25-gbs-theme-hash-baseline-grow.md
autonomous: true
requirements:
  - GBS-MARQ-06
  - GBS-THEME-06
tags:
  - gbs.fm
  - drift-guard
  - follow-up-todo

must_haves:
  truths:
    - "tests/test_gbs_marquee_drift_guard.py exists with test_marquee_module_reuses_phase76_auth_only enforcing D-07 source-grep rules"
    - "Same file contains test_themed_logo_never_persists enforcing D-05 no-disk-write invariant"
    - "Same file contains test_themed_logo_targets_logo_slot_only enforcing GBS-THEME-03 source-grep rule (no cover_label / set_station_art / set_cover in gbs_marquee.py)"
    - "Same file contains test_no_toast_in_themed_day_path enforcing GBS-THEME-05 + D-18 source-grep rule (no show_toast / libnotify / QSystemTrayIcon in gbs_marquee.py + announcement_banner.py)"
    - "todos/2026-05-25-gbs-theme-hash-baseline-grow.md exists with resolves_phase: 87, next_window: 2026-10-31 — GBS-THEME-06 D-04 follow-up record"
    - "All drift-guards pass against the post-Plan-87-05 codebase (no banned identifiers leaked into target files)"
    - "Full pytest suite green at phase close"
  artifacts:
    - path: "tests/test_gbs_marquee_drift_guard.py"
      provides: "Four source-grep drift-guards mirroring tests/test_fake_player_no_inline.py + tests/test_constants_drift.py patterns"
      contains: "test_marquee_module_reuses_phase76_auth_only"
    - path: "todos/2026-05-25-gbs-theme-hash-baseline-grow.md"
      provides: "GBS-THEME-06 D-04 follow-up reminder for next themed-day window (Halloween 2026)"
      contains: "resolves_phase: 87"
  key_links:
    - from: "tests/test_gbs_marquee_drift_guard.py::test_marquee_module_reuses_phase76_auth_only"
      to: "musicstreamer/gbs_marquee.py"
      via: "Path.read_text + ban-list + required-imports assertions"
      pattern: "QWebEngineProfile|GBS_WEB_PROFILE_NAME"
    - from: "todos/2026-05-25-gbs-theme-hash-baseline-grow.md frontmatter"
      to: "GBS-THEME-06 verification re-eval"
      via: "resolves_phase: 87 + next_window: 2026-10-31"
      pattern: "resolves_phase"
---

<objective>
Plan 87-06 is the final drift-guard wave + follow-up todo creation. Phase 87's correctness depends on three source-level invariants that behavioral tests cannot catch (per `memory/feedback_gstreamer_mock_blind_spot.md` — behavioral mocks pass through any call; only source-grep gates catch construction-time pollution):

1. **D-07 / GBS-MARQ-06 / Pitfall #1 reuse path**: `gbs_marquee.py` MUST import from `musicstreamer.gbs_api` AND `musicstreamer.paths`; MUST NOT contain `QWebEngineProfile`, `GBS_WEB_PROFILE_NAME`, `GBS_WEB_STORAGE_PATH`, `oauth_helper`, or any parallel cookies file write.
2. **D-05 / GBS-THEME-04 themed-logo non-persistence**: `gbs_marquee.py` MUST NOT contain `repo.set_setting`, `.save(`, `open(`, or any other disk-write surface on the themed-logo bytes.
3. **GBS-THEME-03 / GBS-THEME-05 / D-18 invariants**: `gbs_marquee.py` MUST NOT touch `cover_label`, `set_station_art`, or `set_cover`; MUST NOT call `show_toast`, `libnotify`, or instantiate `QSystemTrayIcon`. `announcement_banner.py` MUST NOT call show_toast or other notification surfaces either.

The drift-guards are direct mirrors of `tests/test_fake_player_no_inline.py` (Phase 77 D-17 enforcer) + `tests/test_constants_drift.py:48-60` (Phase 61 D-02 enforcer). The mirror shape is `Path(__file__).resolve().parent.parent / "musicstreamer" / "gbs_marquee.py"` → `read_text` → assert keyword presence/absence.

In addition to the drift-guards, this plan creates the GBS-THEME-06 D-04 follow-up todo: a markdown file under `todos/` recording that the baseline hash table currently has fewer entries than the literal "3+/5+" GBS-THEME-06 wording calls for, and that the next themed-day window (Halloween 2026, approximately 2026-10-31) is the next opportunity to harvest additional fixtures. The todo file uses the GSD `resolves_phase` frontmatter convention so the harvest can later be tracked back to Phase 87.

Purpose:
- Lock in source-grep invariants that protect Phase 87's architecture against future refactors.
- Record the GBS-THEME-06 baseline-grow obligation so it surfaces at the next themed-day window.
- Run the full pytest suite as the final verification gate before phase closure.

Output:
- `tests/test_gbs_marquee_drift_guard.py` with 4 source-grep tests.
- `todos/2026-05-25-gbs-theme-hash-baseline-grow.md` with GSD frontmatter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-05-SUMMARY.md
@musicstreamer/gbs_marquee.py
@musicstreamer/ui_qt/announcement_banner.py
@tests/test_fake_player_no_inline.py
@tests/test_constants_drift.py

<interfaces>
<!-- Mirror tests/test_fake_player_no_inline.py shape exactly (Phase 77 D-17 drift-guard precedent). -->

From tests/test_fake_player_no_inline.py:17-53 (mirror this shape):
```python
from __future__ import annotations
from pathlib import Path
import re

ROOT = Path(__file__).resolve().parent.parent / "tests"
FAKE_PLAYER_RE = re.compile(r'^\s*class\s+_?FakePlayer\s*\(QObject\)', re.M)
ALLOWED = {"_fake_player.py"}

def test_no_inline_fake_player_subclass_in_tests():
    offenders = []
    for py in sorted(ROOT.rglob("*.py")):
        if py.name in ALLOWED:
            continue
        text = py.read_text(encoding="utf-8")
        if FAKE_PLAYER_RE.search(text):
            offenders.append(str(py.relative_to(ROOT.parent)))
    assert not offenders, "..."
```

Adapt for Phase 87 single-file targets — instead of `rglob`, read one file (`musicstreamer/gbs_marquee.py`) once, then run multiple assertions against the source text. Per 87-RESEARCH.md §Pattern 4:

```python
GBS_MARQUEE_SRC = Path(__file__).parent.parent / "musicstreamer" / "gbs_marquee.py"

def test_marquee_module_reuses_phase76_auth_only():
    src = GBS_MARQUEE_SRC.read_text(encoding="utf-8")
    assert "from musicstreamer import" in src and "gbs_api" in src
    assert "paths" in src
    banned = [
        "QWebEngineProfile", "QWebEnginePage",
        "GBS_WEB_PROFILE_NAME", "GBS_WEB_STORAGE_PATH",
        "oauth_helper", "_GbsLoginWindow",
    ]
    for b in banned:
        assert b not in src, f"gbs_marquee.py must not reference {b!r}"
```

<!-- IMPORTANT: comment hygiene — grep counts comments. Per planner instructions: -->
<!-- If a drift-guard greps for an identifier in source text, any comment that mentions -->
<!-- the banned identifier ALSO matches. The acceptance criteria for this plan's drift-guard tests -->
<!-- must therefore filter out comments via Python re-based comment-stripping OR use a regex anchored -->
<!-- to non-comment lines. The simplest defensive form: strip `#` comments from the source before -->
<!-- the ban-check. The required-imports check operates on raw text (imports are not comments). -->
</interfaces>

<assumptions>
- The single-file target for source-grep is `musicstreamer/gbs_marquee.py` for drift-guards #1, #2, #3. The fourth drift-guard (`test_no_toast_in_themed_day_path`) additionally inspects `musicstreamer/ui_qt/announcement_banner.py` (the banner is the only other Phase 87 file with potential toast-call risk).
- The comment-hygiene problem (per planner notes: `grep -c` counts comments → header prose can self-invalidate): the drift-guard tests strip `#` comments from the source text before the ban-check. Stripping rule: split on lines; for each line, strip everything from the first `#` character onward (defensive; misses string-embedded `#` but Phase 87 source is unlikely to contain banned-identifier substrings inside string literals). The required-imports check (drift-guard #1) operates on the un-stripped text because `import` statements are not comments.
- `todos/` is the canonical location for follow-up todos per the GSD convention (read existing entries at `.planning/notes/` AND `todos/` to identify the actual project convention — STATE.md references `.planning/notes/` for "Pending Todos"; this plan creates `todos/2026-05-25-gbs-theme-hash-baseline-grow.md` per CONTEXT D-04 literal naming, and the planner SHOULD also append a pointer to STATE.md's Pending Todos list if STATE.md edits are within scope — or defer to the orchestrator's commit hook). If `todos/` directory does NOT exist in the repo, create it.
- GBS-THEME-06's literal "3+/5+" wording IS RELAXED per CONTEXT D-04. The verification-time interpretation: the baseline TABLE STRUCTURE shipped (a `dict[str, str]` at `gbs_marquee.py`), at least 1 entry exists, and the todo records the obligation to accrete more. The todo's `next_window` field is `2026-10-31` per D-04 (Halloween 2026 — next themed window after Memorial Day 2026-05-25).
- The Plan 87-06 drift-guards must pass GREEN at commit time. If any drift-guard test fails, STOP — Plan 87-05 (or an earlier plan) introduced a banned identifier that must be fixed before phase closure. The fix happens in the originating plan's file, not in Plan 87-06. This plan is intentionally test-only + todo creation; production code edits would mean a prior plan regressed.
- The `_set_uri ordering` guard from Phase 90's pattern is NOT applicable to Phase 87 (no playback path is modified); skipped.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_gbs_marquee_drift_guard.py with 4 source-grep drift-guards</name>
  <files>tests/test_gbs_marquee_drift_guard.py</files>
  <read_first>
    - tests/test_fake_player_no_inline.py (mirror this file shape exactly — module docstring, ROOT constant, ALLOWED constant, single-purpose test function)
    - tests/test_constants_drift.py:48-60 (ban-list pattern with rglob + read_text — same shape, multiple bans per test)
    - musicstreamer/gbs_marquee.py (the target file under test — read in full to confirm the actual current state of imports / banned identifiers / log event names emitted)
    - musicstreamer/ui_qt/announcement_banner.py (target for drift-guard #4 — toast / notification surface check)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md §D-05, D-07, D-18 (the source-level invariants this test enforces)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md §Pattern 4 (drift-guard sketch + refinement note about `open(` ban)
  </read_first>
  <behavior>
    - File: `tests/test_gbs_marquee_drift_guard.py`. Module docstring cites Phase 87 + D-05 + D-07 + GBS-MARQ-06 + GBS-THEME-03/04/05 + the source-grep philosophy from `memory/feedback_gstreamer_mock_blind_spot.md`.
    - Module-level imports: `from __future__ import annotations`, `from pathlib import Path`, `import re`.
    - Module-level constants:
      - `GBS_MARQUEE_SRC = Path(__file__).resolve().parent.parent / "musicstreamer" / "gbs_marquee.py"`
      - `ANNOUNCEMENT_BANNER_SRC = Path(__file__).resolve().parent.parent / "musicstreamer" / "ui_qt" / "announcement_banner.py"`
    - Module-level helper `_strip_comments(text: str) -> str`: for each line, drop everything from the first `#` onward (defensive against `# noqa` etc.). Returns the comment-stripped source. Used by all 4 ban-check tests to prevent header-prose self-invalidation.
    - Test 1 — `test_marquee_module_reuses_phase76_auth_only`:
      - Read `GBS_MARQUEE_SRC.read_text(encoding="utf-8")`.
      - Required imports check (operates on raw source — imports are never comments): assert `"from musicstreamer import" in src` AND `"gbs_api" in src` AND `"paths" in src`. (More precise: `re.search(r'^from\s+musicstreamer\s+import\s+.*gbs_api', src, re.M)` AND `re.search(r'^from\s+musicstreamer\s+import\s+.*paths', src, re.M)` OR they appear on separate import lines.)
      - Comment-strip → `stripped = _strip_comments(src)`.
      - Ban-list (all checked against `stripped`):
        - `QWebEngineProfile`
        - `QWebEnginePage`
        - `GBS_WEB_PROFILE_NAME`
        - `GBS_WEB_STORAGE_PATH`
        - `oauth_helper`
        - `_GbsLoginWindow`
      - For each banned identifier, `assert b not in stripped, f"gbs_marquee.py must not reference {b!r} (D-07 / Pitfall #1)"`.
    - Test 2 — `test_themed_logo_never_persists`:
      - Read `GBS_MARQUEE_SRC.read_text(encoding="utf-8")`; comment-strip.
      - Ban-list:
        - `repo.set_setting` (no SQLite write — D-05)
        - `set_station_art` (no station-art table touch — D-05)
        - `.save(` (no `pixmap.save()` — D-05)
        - `open(` (no file IO at all in gbs_marquee.py — Pattern 4 refinement note)
        - Note: the `open(` ban is aggressive per the refinement note. The acceptance check below verifies it does NOT fire (meaning gbs_marquee.py does not use `open(` for any purpose, consistent with the hardcoded `GBS_LOGO_BASELINE_HASHES: dict` from Plan 87-04 Task 1).
      - For each, `assert b not in stripped, f"gbs_marquee.py must not call {b!r} (D-05 — themed logo session-scoped only)"`.
    - Test 3 — `test_themed_logo_targets_logo_slot_only`:
      - Read `GBS_MARQUEE_SRC.read_text(encoding="utf-8")`; comment-strip.
      - Ban-list:
        - `cover_label` (themed logo never touches the cover slot — GBS-THEME-03)
        - `set_station_art` (cross-coverage with D-05; doubles down on no station-art table write)
        - `set_cover` (catch-all for any cover-setter pattern)
      - For each, `assert b not in stripped, f"gbs_marquee.py must not reference {b!r} (GBS-THEME-03 — themed logo targets logo_label only)"`.
    - Test 4 — `test_no_toast_in_themed_day_path`:
      - Read `GBS_MARQUEE_SRC.read_text(encoding="utf-8")`; comment-strip → `marquee_stripped`.
      - Read `ANNOUNCEMENT_BANNER_SRC.read_text(encoding="utf-8")`; comment-strip → `banner_stripped`.
      - Combined check: for each banned identifier, assert NOT in `marquee_stripped` AND NOT in `banner_stripped`.
      - Ban-list:
        - `show_toast` (no toast surface — GBS-THEME-05 + D-18)
        - `libnotify` (no native libnotify call)
        - `QSystemTrayIcon` (no tray icon instantiation)
      - For each, assert with informative message identifying which file fails: `assert b not in marquee_stripped, f"gbs_marquee.py must not call {b!r} (GBS-THEME-05 / D-18)"`; similar for banner.
    - Optional Test 5 — `test_worker_run_calls_exec_loop`:
      - Read `GBS_MARQUEE_SRC.read_text(encoding="utf-8")`; comment-strip.
      - Assert `re.search(r'def\s+run\s*\(\s*self\s*\)\s*(?:->[^:]+)?\s*:\s*\n[^\n]*self\.exec_\(\)', stripped, re.M)` finds a match (Pitfall #7 enforcer at source-grep level). This complements the behavioral cadence test from Plan 87-03 with a source-grep enforcer. (Researcher noted in 87-RESEARCH.md that the behavioral test is the canonical enforcer; this is belt-and-suspenders.)
      - If the regex is too brittle for the actual indentation in the source, simplify: `assert "self.exec_()" in stripped, "GbsMarqueeWorker.run must call self.exec_() (Pitfall #7)"`.
    - All tests use bound functions (no inner classes / no monkeypatching of paths). Each test reads the file fresh — no shared state between tests.
  </behavior>
  <action>
    Step 1 — Create `tests/test_gbs_marquee_drift_guard.py` per the behavior block above. Module docstring opens with "Phase 87 drift-guards — source-level enforcement of D-05, D-07, GBS-THEME-03/05, D-18 invariants. Mirrors tests/test_fake_player_no_inline.py (Phase 77 D-17) and tests/test_constants_drift.py:48-60 (Phase 61 D-02). Per memory/feedback_gstreamer_mock_blind_spot.md — behavioral mocks can pass through pipeline calls; only source-level grep catches construction-time pollution. Phase 87's correctness depends on these tests staying GREEN across all future commits."

    Step 2 — Implement `_strip_comments(text: str) -> str` per the assumption block: split lines, drop everything from first `#` onward per line, rejoin. Comments inside string literals would be incorrectly stripped, but Phase 87 source code contains no banned-identifier substrings inside string literals (audit by visual inspection of the gbs_marquee.py source post-Plan-87-04).

    Step 3 — Implement Test 1 (test_marquee_module_reuses_phase76_auth_only). The required-imports check operates on raw text (imports are not comments). The ban-list check operates on `_strip_comments(src)`.

    Step 4 — Implement Tests 2, 3, 4 (and optionally 5) per the behavior block. Each ban-list assertion has a per-identifier `assert b not in stripped, f"..."` so the failure message identifies WHICH banned identifier was found.

    Step 5 — Run the new drift-guard tests against the post-Plan-87-05 codebase:
    `uv run --with pytest pytest tests/test_gbs_marquee_drift_guard.py -x`
    All tests MUST pass. If any test fails:
    - DO NOT add the failing identifier to ALLOWED.
    - DO NOT relax the ban-list.
    - GO BACK to the originating plan's file (likely Plan 87-03 or Plan 87-04 introduced the banned identifier) and remove it there. Plan 87-06 is intentionally an enforcement gate; it does NOT rewrite production code.

    Step 6 — Run the full suite once more to confirm no regressions: `uv run --with pytest pytest`.
  </action>
  <acceptance_criteria>
    - `test -f tests/test_gbs_marquee_drift_guard.py`
    - `grep -c "^def test_marquee_module_reuses_phase76_auth_only" tests/test_gbs_marquee_drift_guard.py` returns 1
    - `grep -c "^def test_themed_logo_never_persists" tests/test_gbs_marquee_drift_guard.py` returns 1
    - `grep -c "^def test_themed_logo_targets_logo_slot_only" tests/test_gbs_marquee_drift_guard.py` returns 1
    - `grep -c "^def test_no_toast_in_themed_day_path" tests/test_gbs_marquee_drift_guard.py` returns 1
    - `uv run --with pytest pytest tests/test_gbs_marquee_drift_guard.py -x` exits 0 (all 4-5 drift-guards GREEN)
    - `uv run --with pytest pytest` exits 0 (full suite green at phase close)
    - `grep -c "_strip_comments" tests/test_gbs_marquee_drift_guard.py` returns ≥ 5 (helper definition + 4 usages across the 4 tests)
  </acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_gbs_marquee_drift_guard.py -x && uv run --with pytest pytest</automated>
  </verify>
  <done>4 (or 5) source-grep drift-guards land green; full pytest suite passes; phantom-framing identifiers + disk-write surfaces + cover-slot writes + toast surfaces are source-level forbidden in gbs_marquee.py + announcement_banner.py.</done>
</task>

<task type="auto">
  <name>Task 2: Create GBS-THEME-06 follow-up todo</name>
  <files>todos/2026-05-25-gbs-theme-hash-baseline-grow.md</files>
  <read_first>
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md §D-04 (verbatim todo wording + resolves_phase + next_window)
    - .planning/STATE.md §Pending Todos (existing todo file conventions in the project — e.g., `.planning/notes/2026-03-21-sdr-live-radio-support.md`)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-01-SUMMARY.md (how many baseline hashes were captured today — informs the todo body)
  </read_first>
  <action>
    Create `todos/2026-05-25-gbs-theme-hash-baseline-grow.md`. If `todos/` directory does not exist in the repo, create it (`mkdir -p todos`).

    Content shape (markdown with YAML frontmatter — mirror the project's existing notes convention):

    ```markdown
    ---
    created: 2026-05-25
    resolves_phase: 87
    next_window: 2026-10-31
    requirement_id: GBS-THEME-06
    status: open
    priority: P3
    ---

    # GBS Themed-Day Hash Baseline — Grow over time

    ## Context

    Phase 87 shipped the GBS.FM themed-day detection structure (Plan 87-04) with `GBS_LOGO_BASELINE_HASHES` populated by Plan 87-01's live Memorial Day "da troops" harvest.

    GBS-THEME-06's literal wording calls for "3+ themed-day responses and 5+ non-themed-day responses". Per CONTEXT D-04, this literal is RELAXED for v2.2: the baseline table STRUCTURE ships now; entries accrete over time as future themed-day windows fire. This todo tracks the obligation to grow the table.

    ## What was captured 2026-05-25

    See `.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-01-SUMMARY.md` and `tests/fixtures/gbs_themed_logos/MANIFEST.md` for the captured hashes. As of phase close, the baseline contains:

    - 1 themed-day entry: 2026-05-25 Memorial Day "da troops"
    - N canonical entries: see MANIFEST (likely 0 if browser cache was unavailable, or 1+ if cached canonical logos were also hashed)

    ## Next themed-day window

    **next_window: 2026-10-31** — Halloween 2026. gbs.fm typically swaps `logo_3.png` for a spooky-themed PNG during the Halloween window AND injects a keyword matching the D-12 frozenset (`spooky` / `halloween`).

    When the next window fires:

    1. Re-run the Plan 87-01 harvest script (see `87-01-SUMMARY.md` for the canonical script shape) against the live `https://gbs.fm/images/logo_3.png` and `https://gbs.fm/` + `/ajax` endpoints.
    2. Add the new harvested PNG to `tests/fixtures/gbs_themed_logos/` as `<YYYY-MM-DD>_halloween_<slug>.png` (or seasonal equivalent).
    3. Compute SHA-256, append entry to `MANIFEST.md`, and add to `GBS_LOGO_BASELINE_HASHES` in `musicstreamer/gbs_marquee.py`.
    4. Add the new marquee snapshot to `tests/fixtures/gbs_marquee/<YYYY-MM-DD>_*.{txt,json}` and append to `MANIFEST.md`.
    5. Verify the GBS_THEMED_DAY_KEYWORDS frozenset includes the expected keyword for the window; extend if not present.

    Subsequent windows (Christmas 2026, Valentine's Day 2027, etc.) accrete entries through the same flow.

    ## Acceptance criteria for closing this todo

    - `len(GBS_LOGO_BASELINE_HASHES) >= 3` AND `len([v for v in GBS_LOGO_BASELINE_HASHES.values() if v == "canonical"]) >= 5` (the GBS-THEME-06 literal).
    - OR: an explicit decision is recorded that the structure-only-ships interpretation is sufficient long-term and GBS-THEME-06's literal is officially relaxed in REQUIREMENTS.md.

    ## Pointers

    - Phase: `.planning/phases/87-gbs-fm-marquee-themed-day-detection/`
    - Phase context: `87-CONTEXT.md` §D-04
    - Harvest script reference: `87-01-SUMMARY.md`
    - Phase 87 closure marker: `87-06-SUMMARY.md` (this plan's deliverable)
    ```

    Verify the frontmatter parses as valid YAML and the `resolves_phase`, `next_window`, `requirement_id` fields are present and correct.

    Also append a line to `.planning/STATE.md` under `### Pending Todos`:
    ```
    - todos/2026-05-25-gbs-theme-hash-baseline-grow.md (GBS-THEME-06 D-04 follow-up; next_window 2026-10-31)
    ```
    Use Edit (not Write) on STATE.md to preserve everything else.
  </action>
  <acceptance_criteria>
    - `test -f todos/2026-05-25-gbs-theme-hash-baseline-grow.md`
    - `grep -c "^resolves_phase: 87" todos/2026-05-25-gbs-theme-hash-baseline-grow.md` returns 1
    - `grep -c "^next_window: 2026-10-31" todos/2026-05-25-gbs-theme-hash-baseline-grow.md` returns 1
    - `grep -c "^requirement_id: GBS-THEME-06" todos/2026-05-25-gbs-theme-hash-baseline-grow.md` returns 1
    - `grep -c "2026-05-25-gbs-theme-hash-baseline-grow" .planning/STATE.md` returns ≥ 1 (pointer added to Pending Todos)
    - YAML frontmatter parses without error: `python3 -c "import yaml, re; t = open('todos/2026-05-25-gbs-theme-hash-baseline-grow.md').read(); m = re.match(r'^---\n(.*?)\n---', t, re.S); assert m; print(yaml.safe_load(m.group(1)))"` exits 0 (assumes pyyaml is in dev env — if not, omit this acceptance check and rely on the grep checks above)
  </acceptance_criteria>
  <verify>
    <automated>test -f todos/2026-05-25-gbs-theme-hash-baseline-grow.md && grep -q "^resolves_phase: 87" todos/2026-05-25-gbs-theme-hash-baseline-grow.md && grep -q "^next_window: 2026-10-31" todos/2026-05-25-gbs-theme-hash-baseline-grow.md && grep -q "2026-05-25-gbs-theme-hash-baseline-grow" .planning/STATE.md</automated>
  </verify>
  <done>todos/2026-05-25-gbs-theme-hash-baseline-grow.md exists with valid frontmatter (resolves_phase: 87, next_window: 2026-10-31, requirement_id: GBS-THEME-06); STATE.md Pending Todos lists the new entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drift-guard source-grep → production code | Read-only; tests fail if production code crosses defined surface |
| Comment-stripping → ban-check | Defensive against header-prose self-invalidation; misses string-embedded banned tokens (acceptable — no such case in Phase 87 source) |
| Follow-up todo → future agent | The `next_window` and `resolves_phase` fields are agent-readable; future planning sessions surface this todo for action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06-01 | Tampering | Future refactor adds `QWebEngineProfile` to gbs_marquee.py under a different identifier (e.g., aliased import) | accept | Drift-guard catches the exact identifier `QWebEngineProfile` in source; aliased imports (`from ... import QWebEngineProfile as X`) still contain the literal string in the import line — caught. Importing the module without aliasing (`import PySide6.QtWebEngineCore` then `PySide6.QtWebEngineCore.QWebEngineProfile`) WOULD bypass the grep — but is unlikely to be introduced inadvertently |
| T-87-06-02 | Tampering | Future refactor writes themed logo to disk via a wrapper function the grep doesn't catch | accept | Banned patterns (`open(`, `.save(`, `repo.set_setting`) cover the most common surfaces. A new wrapper would need to use one of these underlying primitives, which the grep catches. If the future code uses a non-Python persistence path (e.g., `subprocess.run(["cp", ...])`), the grep misses — but such a code path would not have a legitimate reason to exist in `gbs_marquee.py` |
| T-87-06-03 | Tampering | Comment-stripping helper has a bug that hides a real banned-identifier hit | mitigate | The helper is simple and small (one-line-per-line, drop-everything-after-`#`); review carefully during code authoring. The required-imports check operates on raw text (no comment stripping) — providing a second axis of detection |
| T-87-06-04 | Repudiation | The follow-up todo is misfiled or has wrong frontmatter and a future agent misses it | mitigate | Acceptance check verifies the frontmatter via grep; STATE.md Pending Todos list also references the file; double-bookkeeping reduces risk |
| T-87-06-05 | Denial of Service | A future commit triggers the drift-guard, blocking all PRs | accept | This IS the intended behavior. The drift-guard is meant to be a hard gate. If a legitimate refactor needs to relax a ban, the ban-list edit requires explicit reasoning + planner sign-off — by design |
| T-87-06-SC | Tampering | No package installs in this plan | n/a | Plan 87-06 introduces zero new packages; slopcheck N/A |
</threat_model>

<verification>
- `tests/test_gbs_marquee_drift_guard.py` ships with 4 (or 5) drift-guard tests
- All drift-guards GREEN against the post-Plan-87-05 codebase
- Full pytest suite green (no regressions)
- `todos/2026-05-25-gbs-theme-hash-baseline-grow.md` exists with `resolves_phase: 87` + `next_window: 2026-10-31` + `requirement_id: GBS-THEME-06`
- STATE.md Pending Todos lists the new file
- Phase 87 closure markers are in place: all phase requirement IDs are addressed across plans, all CONTEXT D-XX decisions referenced, all source-grep invariants protected
</verification>

<success_criteria>
- Phase 87's architectural invariants (cookies-jar reuse, no QtWebEngine, no disk write of themed logo, no toast on themed-day detection, banner is PlainText) are protected by source-grep tests that fail loud on regression.
- The GBS-THEME-06 baseline-grow obligation is recorded in a follow-up todo that surfaces at the next themed-day window.
- All 13 phase requirement IDs are addressed across Plans 87-01 through 87-06.
- Full pytest suite is green; Phase 87 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-06-SUMMARY.md` when done. Record:
- The exact pass/fail outcome of each of the 4 (or 5) drift-guard tests against the post-Plan-87-05 codebase (all should be green).
- Whether the optional Test 5 (worker.exec_() source-grep) was implemented.
- Any banned identifiers that nearly leaked through but were caught at this gate (instructive for future phases).
- Confirmation that `todos/2026-05-25-gbs-theme-hash-baseline-grow.md` is committed and STATE.md is updated.
- Phase 87 close-out checklist: all 13 requirement IDs addressed, all D-01..D-19 decisions referenced, all RESEARCH.md pitfalls (1-8) mitigated.
</output>
