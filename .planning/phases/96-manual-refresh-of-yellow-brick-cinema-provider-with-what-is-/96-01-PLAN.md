---
phase: 96
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_repo.py
  - tests/test_station_tree_model.py
  - tests/test_edit_station_dialog.py
  - tests/test_live_refresh_dialog.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - "Every Phase 96 decision (D-01..D-10) has a failing-or-skipped automated test referencing its exact identifier before implementation begins"
    - "All four Wave 0 test targets named in 96-VALIDATION.md exist and are collectible by pytest"
  artifacts:
    - path: tests/test_live_refresh_dialog.py
      provides: "D-05/D-06/D-07/D-08/D-09/D-10 dialog-logic test scaffolding (NEW file)"
      contains: "test_suggestions_pre_order_no_auto_apply"
    - path: tests/test_repo.py
      provides: "D-01/D-03/D-04 migration idempotency + round-trip + list_flagged_stations_for_provider tests"
      contains: "test_live_url_syncs_from_channel_migration_idempotent"
    - path: tests/test_station_tree_model.py
      provides: "D-04 _TreeNode.provider_id test"
      contains: "test_tree_node_carries_provider_id"
    - path: tests/test_edit_station_dialog.py
      provides: "D-02 flag-checkbox gating test"
      contains: "test_live_resync_checkbox_gating"
  key_links:
    - from: tests/test_repo.py
      to: musicstreamer/repo.py
      via: "db_init / Repo setter+query method calls"
      pattern: "set_live_url_syncs_from_channel|list_flagged_stations_for_provider"
---

<objective>
Create the Nyquist Wave 0 test scaffolding for Phase 96 so every D-01..D-10 decision has a named automated test BEFORE any production code is written. This is the feedback-sampling substrate the rest of the phase is graded against.

Purpose: Per 96-VALIDATION.md, four test files/additions must exist and be collectible so that each subsequent implementation plan can run a scoped `-x -q` check that goes RED→GREEN. Without these, the per-task verify commands in plans 02–05 reference non-existent tests.
Output: 1 new test file + 3 test-file additions, all collectible by pytest, all RED (failing) or xfail-marked against the not-yet-built production code.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-VALIDATION.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md

<interfaces>
<!-- Existing Repo fixture + test patterns to mirror. Extracted from codebase. -->

tests/test_repo.py fixture (lines 8-14):
- `@pytest.fixture def repo(tmp_path)` → sqlite3 connect, row_factory=Row, foreign_keys ON, db_init(con), returns Repo(con)
- `repo.con` is the live connection; `db_init(repo.con)` may be called again for idempotency tests
- `repo.create_station()` → int (inserts a 'New Station' row, no streams); `repo.get_station(id)` → Station

Migration-idempotency analog: tests/test_repo.py:229-253 `test_cover_art_source_migration_idempotent`
- calls db_init twice, reads `PRAGMA table_info('stations')`, asserts column present with expected type/notnull/default
- PRAGMA row tuple indices: name=idx1, type=idx2, notnull=idx3, dflt_value=idx4

tests/test_station_tree_model.py:34-55 — model unit-test structure (build StationTreeModel from a list[Station], assert node fields)

Production identifiers these tests will reference (NOT YET BUILT — tests must be RED):
- repo.set_live_url_syncs_from_channel(station_id: int, value: bool)
- repo.set_live_url_title_anchor(station_id: int, title: Optional[str])
- repo.set_provider_channel_scan_url(provider_id: int, url: Optional[str])
- repo.list_flagged_stations_for_provider(provider_id: int) -> list[Station]
- Station.live_url_syncs_from_channel: bool, Station.live_url_title_anchor: Optional[str]
- _TreeNode.provider_id (station_tree_model.py)
- live_refresh_dialog.LiveRefreshDialog, live_refresh_dialog._LiveRefreshScanWorker
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo migration + setter + query tests (D-01/D-03/D-04/D-06)</name>
  <files>tests/test_repo.py</files>
  <read_first>
    - tests/test_repo.py (the `repo` fixture at lines 8-14 and the idempotency analog `test_cover_art_source_migration_idempotent` at 229-253)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ Migration Test Pattern, lines 804-846)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-VALIDATION.md (Per-Decision Verification Map rows for D-01/D-03/D-04/D-06)
  </read_first>
  <action>
    Append to tests/test_repo.py these named tests, mirroring the `repo` fixture and the `test_cover_art_source_migration_idempotent` PRAGMA-introspection shape:
    - `test_live_url_syncs_from_channel_migration_idempotent` — db_init twice; assert `stations` has column `live_url_syncs_from_channel` with type INTEGER, notnull==1, dflt_value=="0" (D-01).
    - `test_live_url_syncs_from_channel_round_trip` — create_station, assert get_station(sid).live_url_syncs_from_channel is False, set_live_url_syncs_from_channel(sid, True) → True, set False → False (D-01).
    - `test_station_live_flag_loaded_from_db` — set flag True, then assert BOTH get_station AND the list_stations entry for that id report live_url_syncs_from_channel True (D-01, guards Pitfall 2).
    - `test_live_url_title_anchor_migration_idempotent` — assert `stations.live_url_title_anchor` present, type TEXT, notnull==0 (nullable) (D-03).
    - `test_live_url_title_anchor_round_trip` — set_live_url_title_anchor(sid, "Anchor") then get_station(sid).live_url_title_anchor == "Anchor"; set None → None (D-03).
    - `test_provider_channel_scan_url_migration_idempotent` — assert `providers.channel_scan_url` present, type TEXT, nullable (D-04).
    - `test_list_flagged_stations_for_provider` — ensure_provider, insert two stations under it, flag exactly one via set_live_url_syncs_from_channel, assert list_flagged_stations_for_provider(provider_id) returns only the flagged station (D-06).
    These reference repo methods that do NOT exist yet — tests MUST be RED on first run (collection succeeds, assertions/AttributeError fail). Do not stub the production methods here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py --collect-only -q 2>&1 | grep -E "test_live_url_syncs_from_channel_migration_idempotent|test_list_flagged_stations_for_provider"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_repo.py --collect-only` lists all 7 new test ids (collection does not error).
    - Running `.venv/bin/python -m pytest tests/test_repo.py -k "live_url or channel_scan or flagged_stations" -q` shows these tests FAILING (RED) — not passing, not erroring at collection.
    - No production code in musicstreamer/ is modified by this task.
  </acceptance_criteria>
  <done>7 repo tests collectible and RED, referencing the exact production identifiers from the interfaces block.</done>
</task>

<task type="auto">
  <name>Task 2: Tree-model + EditStationDialog gating tests (D-04/D-02)</name>
  <files>tests/test_station_tree_model.py, tests/test_edit_station_dialog.py</files>
  <read_first>
    - tests/test_station_tree_model.py (lines 1-55 — fixture + model construction + node-assertion pattern)
    - tests/test_edit_station_dialog.py (existing test structure for constructing EditStationDialog with a Station + faking URL text)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ station_tree_model `_TreeNode.provider_id`; § edit_station_dialog URL gate lines 676-700)
  </read_first>
  <action>
    Add to tests/test_station_tree_model.py:
    - `test_tree_node_carries_provider_id` — build a StationTreeModel from stations that have provider_id set; locate the provider _TreeNode for that group; assert `node.provider_id` equals the first station's provider_id; assert an Ungrouped (provider_id=None) group's node has provider_id None (D-04, guards Pitfall 4).
    Add to tests/test_edit_station_dialog.py:
    - `test_live_resync_checkbox_gating` — construct EditStationDialog; drive `_on_url_text_changed` (or set url_edit text + trigger) with a youtube.com URL → assert `_live_resync_checkbox.isEnabled()` True; with a twitch.tv URL → assert False; with a non-YT/non-Twitch URL → assert False AND checkbox unchecked (D-02, the YouTube-ONLY gate; Twitch is explicitly excluded — distinct from the avatar gate which allows Twitch).
    Both reference attributes (`_TreeNode.provider_id`, `_live_resync_checkbox`) that do NOT exist yet — tests MUST be RED. Use `.venv/bin/python` Qt env. If a Qt offscreen platform fixture exists in the file, reuse it; do not add `QApplication` boilerplate if the file already provides it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_tree_model.py::test_tree_node_carries_provider_id tests/test_edit_station_dialog.py::test_live_resync_checkbox_gating --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - Both new test ids are collectible.
    - `.venv/bin/python -m pytest tests/test_station_tree_model.py::test_tree_node_carries_provider_id tests/test_edit_station_dialog.py::test_live_resync_checkbox_gating -q` shows both FAILING (RED).
    - The Twitch assertion explicitly asserts the checkbox is DISABLED for twitch.tv (YouTube-only gate per D-02).
  </acceptance_criteria>
  <done>Two gating/provider-id tests collectible and RED with exact identifier references.</done>
</task>

<task type="auto">
  <name>Task 3: NEW tests/test_live_refresh_dialog.py (D-05/D-06/D-07/D-08/D-09/D-10)</name>
  <files>tests/test_live_refresh_dialog.py</files>
  <read_first>
    - tests/test_station_tree_model.py (lines 1-31 — for fixture/isolation structure to mirror in a NEW dialog-logic test file)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-VALIDATION.md (Per-Decision Verification Map rows D-05..D-10)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ LiveRefreshDialog; § Conservative unchecked-by-default D-10; § QThread Worker Shape)
  </read_first>
  <action>
    Create NEW file tests/test_live_refresh_dialog.py mirroring the repo-fixture isolation pattern (use a tmp_path sqlite Repo where DB state is needed). Add these RED tests referencing musicstreamer.ui_qt.live_refresh_dialog.LiveRefreshDialog / _LiveRefreshScanWorker:
    - `test_scan_worker_uses_qthread` — assert `_LiveRefreshScanWorker` is a subclass of PySide6.QtCore.QThread, exposes `finished` and `error` Signals, and its __init__ accepts a `node_runtime` keyword (D-09 + node_runtime landmine).
    - `test_suggestions_pre_order_no_auto_apply` — drive the dialog's row-suggestion builder (the title-anchor pre-ordering helper, e.g. `_build_row_suggestions`) with a station whose anchor exactly matches a scan-result title; assert the matching live stream is ordered first in that row's suggestions BUT no mapping/URL is auto-applied (the staged-change set for that row is empty until the user picks) (D-05).
    - `test_apply_remap_preserves_metadata` — stage a REMAP of a flagged station to a new live URL; invoke the apply path; assert repo.update_stream was called for the position-1 stream with the new URL while label/quality/stream_type/codec/bitrate are preserved from the existing stream, and set_live_url_title_anchor was updated (D-06; use a real tmp Repo + a station with a multi-field stream, OR a mock repo asserting call args).
    - `test_apply_drop_and_add_actions` — stage one DROP (ticked) and one ADD (ticked, with a name); apply; assert delete_station called for the dropped station id and insert_station called for the add, followed by set_live_url_syncs_from_channel(True) + set_live_url_title_anchor for the new station (D-07).
    - `test_name_field_prepopulation` — assert an ADD row's name field defaults to the scan result's YouTube title, and a REMAP/REPLACE row's name field defaults to the EXISTING station name (not the channel title) (D-08).
    - `test_conservative_defaults` — assert that for staged rows, DROP and ADD rows are UNCHECKED by default while a REMAP row is checked/active; assert an unresolved flagged station (no user action) produces NO update_stream / NO delete_station call on apply (D-10).
    All reference not-yet-built code — tests MUST be RED. Where the dialog's apply logic can be unit-tested without a live Qt event loop, prefer testing a pure helper (e.g. an apply function that takes (repo, staged_changes)); the implementer in Plan 04 will expose that seam. If a test genuinely needs the dialog widget, use the project's offscreen Qt fixture pattern.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_live_refresh_dialog.py exists and collects all 6 named tests without import-time collection errors that abort the whole file (an ImportError on the not-yet-existing module is acceptable as a RED signal only if pytest still reports the file; otherwise gate the import so collection lists the test ids and they fail at call time).
    - Each of the 6 decision rows (D-05..D-10) maps to exactly one named test as listed.
    - `test_conservative_defaults` explicitly asserts DROP+ADD default-unchecked AND that an untouched flagged station triggers no destructive/mutating repo call (D-10 safety posture).
  </acceptance_criteria>
  <done>New dialog-logic test file with 6 RED tests covering D-05..D-10, each asserting the conservative/no-auto-apply invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → repo/db | Tests construct throwaway tmp_path SQLite DBs only; no production data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | Wave 0 tests assert ON not-yet-built code | mitigate | Tests reference exact production identifiers from the interfaces block so a typo'd identifier in Plans 02–05 fails the gate rather than silently passing |
| T-96-02 | Repudiation | Test gives false-green if it passes pre-implementation | mitigate | Acceptance criteria require RED status before implementation; no production stubs added in this plan |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 96 (96-RESEARCH.md Package Legitimacy Audit: zero new packages); nothing to gate |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_repo.py tests/test_station_tree_model.py tests/test_edit_station_dialog.py tests/test_live_refresh_dialog.py --collect-only -q` lists all new test ids.
- Running the four new-test selections shows RED (failing) status — never green, never collection-aborted.
- `git diff --stat` shows changes ONLY under tests/.
</verification>

<success_criteria>
- All four Wave 0 test targets from 96-VALIDATION.md exist and are collectible.
- Every D-01..D-10 decision has at least one named automated test (D-10's summary-shown sub-item remains manual UAT per VALIDATION.md).
- No production code modified.
</success_criteria>

<output>
Create `.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-01-SUMMARY.md` when done.
</output>
