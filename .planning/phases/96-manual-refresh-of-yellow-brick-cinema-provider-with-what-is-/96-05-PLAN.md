---
phase: 96
plan: 05
type: execute
wave: 3
depends_on: ["96-03", "96-04"]
files_modified:
  - musicstreamer/ui_qt/station_list_panel.py
  - musicstreamer/ui_qt/main_window.py
  - tests/test_station_list_panel.py
autonomous: false
requirements: [D-04, D-09, D-10]
must_haves:
  truths:
    - "Right-clicking a provider row (with a non-null provider_id) shows a 'Refresh live streams…' action that opens the LiveRefreshDialog scoped to that provider's flagged stations (D-04)"
    - "node_runtime is threaded MainWindow → StationListPanel → LiveRefreshDialog → _LiveRefreshScanWorker → scan_playlist so YouTube extraction works under .desktop launchers (D-09 / node-runtime landmine)"
    - "After a successful refresh apply, the station list reloads so remapped/added/dropped stations are reflected (D-10)"
  artifacts:
    - path: musicstreamer/ui_qt/station_list_panel.py
      provides: "provider-row context-menu branch + provider_refresh_requested signal + node_runtime param"
      contains: "provider_refresh_requested"
    - path: musicstreamer/ui_qt/main_window.py
      provides: "node_runtime passed to StationListPanel + provider_refresh_requested wiring + reload on refresh_complete"
      contains: "provider_refresh_requested"
  key_links:
    - from: musicstreamer/ui_qt/station_list_panel.py
      to: musicstreamer/ui_qt/live_refresh_dialog.py
      via: "provider_refresh_requested → MainWindow opens LiveRefreshDialog(node_runtime=...)"
      pattern: "provider_refresh_requested"
    - from: musicstreamer/ui_qt/main_window.py
      to: musicstreamer/ui_qt/station_list_panel.py
      via: "StationListPanel(repo, parent=..., node_runtime=self._node_runtime)"
      pattern: "StationListPanel"
---

<objective>
Wire the provider context menu (D-04) to the refresh dialog and close the node-runtime landmine (D-09): right-click a provider row, choose "Refresh live streams…", MainWindow opens LiveRefreshDialog scoped to that provider, with node_runtime threaded the whole way so the scan works under GNOME .desktop launchers. On apply, the station list reloads.

Purpose: Plans 03+04 built the eligibility surface and the dialog; this plan makes them reachable from the sidebar and feeds the scan its node runtime (StationListPanel does NOT currently receive node_runtime — Pitfall 3). This is the integration seam, ending with a human-verify checkpoint because the refresh flow and the review-and-confirm summary (D-10) are visual/functional.
Output: Provider context menu + signal + node_runtime threading + reload-on-complete + a blocking human-verify checkpoint.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md

<interfaces>
<!-- Exact source anchors. Extracted from codebase. -->

station_list_panel.py:
- __init__ signature L92: `def __init__(self, repo, parent: QWidget | None = None) -> None:` — add `*, node_runtime=None`, store `self._node_runtime`.
- existing signals (near L87-90): station_activated, station_favorited, edit_requested, new_station_requested — add `provider_refresh_requested = Signal(int, str)` (provider_id, provider_name).
- _on_tree_context_menu L682-694: currently `station = self.model.station_for_index(source_idx); if station is None: return` then a station-only menu. Insert a provider branch BEFORE the station path using `node = source_idx.internalPointer()` and `node.kind == "provider" and node.provider_id is not None` (Pitfall 4 — Ungrouped has provider_id None and gets no menu).

main_window.py:
- StationListPanel constructed at L403: `self.station_panel = StationListPanel(repo, parent=self._splitter)` — add `node_runtime=self._node_runtime` (self._node_runtime set at L270).
- existing panel signal wiring at L548-550 (edit_requested, new_station_requested → slots) — add provider_refresh_requested connection there.
- self._node_runtime available at L270; ImportDialog already gets it at L1560 (precedent).
- _refresh_station_list() reloads the tree (used by _on_edit_requested L1345, _on_station_deleted L1355) — call it on refresh_complete.
- show_toast(text, duration_ms=3000) at L661.

Plan-04 contract: LiveRefreshDialog(repo, provider_id, provider_name, channel_scan_url, *, node_runtime=None, toast_callback=None, parent=None); emits refresh_complete.
repo.list_providers() now returns Provider with channel_scan_url (Plan 02) — used to resolve channel_scan_url for the right-clicked provider_id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider context-menu branch + signal + node_runtime param in StationListPanel (D-04/D-09)</name>
  <files>musicstreamer/ui_qt/station_list_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/station_list_panel.py lines 87-92 (signals + __init__) and 682-694 (_on_tree_context_menu)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (section station_list_panel Provider Context Menu Branch, lines 598-668)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md (Critical Code Path 3; Pitfall 3 node_runtime; Pitfall 4 Ungrouped gate)
  </read_first>
  <behavior>
    - A provider _TreeNode with provider_id set yields a "Refresh live streams…" menu that emits provider_refresh_requested(provider_id, provider_name)
    - A provider node with provider_id None (Ungrouped) yields NO menu
  </behavior>
  <action>
    Add `provider_refresh_requested = Signal(int, str)` to StationListPanel's signal declarations (D-04). Add `*, node_runtime=None` to __init__ and store `self._node_runtime = node_runtime` (Pitfall 3 — the panel holds it for symmetry; MainWindow opens the dialog off the signal).
    In _on_tree_context_menu, BEFORE the existing station path, read `node = source_idx.internalPointer()`; if `node is not None and node.kind == "provider" and node.provider_id is not None`, build a QMenu with a single "Refresh live streams…" action, exec at the viewport-global pos, and on that action emit `provider_refresh_requested(node.provider_id, node.provider_name or "")` then return. Leave the existing station-only menu untouched below the branch. Ungrouped provider rows (provider_id None) fall through and show no menu (Pitfall 4).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_tree_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep for provider_refresh_requested in station_list_panel.py shows the Signal declaration AND the emit inside the provider branch.
    - The provider branch guards on node.provider_id is not None (Ungrouped gets no menu).
    - __init__ accepts and stores node_runtime.
    - `.venv/bin/python -c "import musicstreamer.ui_qt.station_list_panel"` imports without error; existing station_list_panel/tree tests pass.
  </acceptance_criteria>
  <done>Provider context-menu branch + signal + node_runtime param added; Ungrouped gated out.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MainWindow wiring — node_runtime, open LiveRefreshDialog, reload on complete (D-04/D-09/D-10)</name>
  <files>musicstreamer/ui_qt/main_window.py</files>
  <read_first>
    - musicstreamer/ui_qt/main_window.py lines 263-270 (node_runtime field), 403 (StationListPanel construction), 548-550 (panel signal wiring), 1337-1360 (_on_edit_requested dialog-open + _refresh_station_list + _on_station_deleted), 661 (show_toast), 1560 (ImportDialog node_runtime precedent)
    - tests/test_station_list_panel.py (existing Qt-fixture/offscreen pattern to mirror for the new wiring test)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md (Critical Code Path 2 node-runtime wiring chain; Open Question 3 empty-flagged UX)
  </read_first>
  <behavior>
    - StationListPanel is constructed WITH node_runtime
    - provider_refresh_requested opens LiveRefreshDialog scoped to provider_id with the provider's channel_scan_url and node_runtime
    - refresh_complete reloads the station list
  </behavior>
  <action>
    At L403, change construction to `StationListPanel(repo, parent=self._splitter, node_runtime=self._node_runtime)` (Pitfall 3 — closes the node-runtime gap).
    Add a slot `_on_provider_refresh_requested(self, provider_id: int, provider_name: str)` that: resolves the provider's channel_scan_url by finding the Provider with that id from repo.list_providers() (Plan 02 now returns channel_scan_url); if empty/None, show a toast prompting the user to set the refresh channel URL via Edit Station, then return (RESEARCH Open Question 3); otherwise construct `LiveRefreshDialog(self._repo, provider_id, provider_name, channel_scan_url, node_runtime=self._node_runtime, toast_callback=self.show_toast, parent=self)`, connect its refresh_complete to self._refresh_station_list, and call dlg.exec(). Wire `self.station_panel.provider_refresh_requested.connect(self._on_provider_refresh_requested)` next to the existing panel-signal connections (~L548-550). Import LiveRefreshDialog at the existing dialog-import region.
    Also add a wiring test to tests/test_station_list_panel.py (mirroring that file's existing offscreen-Qt fixture pattern), e.g. `test_provider_refresh_wiring`, asserting: (a) StationListPanel is constructed accepting a `node_runtime` kwarg and stores it; (b) the panel exposes a `provider_refresh_requested` Signal; (c) `MainWindow._on_provider_refresh_requested` is defined (a real method/attribute, not a stub) and, when invoked for a provider whose channel_scan_url is set, constructs a LiveRefreshDialog (assert via monkeypatching the LiveRefreshDialog symbol in main_window and checking it was instantiated with node_runtime + channel_scan_url). Use `.venv/bin/python` Qt env; do not add QApplication boilerplate if the file already provides a fixture.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_list_panel.py -k "provider_refresh_wiring" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep shows StationListPanel constructed with node_runtime=self._node_runtime.
    - grep shows provider_refresh_requested connected and _on_provider_refresh_requested defined, constructing LiveRefreshDialog with node_runtime + channel_scan_url.
    - Empty channel_scan_url path shows a toast and does not open the dialog.
    - main_window imports cleanly.
    - tests/test_station_list_panel.py::test_provider_refresh_wiring PASSES, asserting: StationListPanel takes a node_runtime kwarg, provider_refresh_requested Signal exists on the panel, and MainWindow._on_provider_refresh_requested is defined and constructs LiveRefreshDialog with node_runtime + channel_scan_url (W3 — replaces the bare import smoke check).
    - `.venv/bin/python -m pytest tests/ -k "main_window or station_panel or live_refresh or station_list_panel" -q` passes.
  </acceptance_criteria>
  <done>node_runtime threaded to the panel; provider-refresh signal opens the scoped dialog; list reloads on apply; empty-scan-URL guarded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify provider-refresh end-to-end (D-09/D-10)</name>
  <what-built>
    Provider right-click "Refresh live streams…" context menu → LiveRefreshDialog (scan + two-panel review + apply) wired end-to-end with node_runtime threaded for YouTube scanning. Covers the visual/functional decisions that have no automated check: the review-and-confirm summary before Apply (D-10) and the real YBC-style channel re-sync (D-01..D-10).
  </what-built>
  <how-to-verify>
    1. Launch the app with `.venv/bin/python -m musicstreamer` (or the .desktop launcher to also exercise the node-runtime landmine, Pitfall 3).
    2. Open Edit Station on a real YouTube live station (e.g. a Yellow Brick Cinema / Cafe BGM watch?v= station). Confirm the "Re-sync live URL from channel" checkbox is ENABLED (and would be disabled if you paste a twitch.tv URL). Tick it, enter the channel URL `https://youtube.com/@YellowBrickCinema/streams`, Save.
    3. In the sidebar, right-click the provider row for that channel. Confirm a "Refresh live streams…" item appears (and does NOT appear on the "Ungrouped" row).
    4. Click it. Confirm the scan runs without freezing the UI and the dialog shows your flagged station(s) on one side and the channel's currently-live streams on the other.
    5. Map a flagged station to a currently-live stream (REMAP). Confirm DROP and ADD rows are UNCHECKED by default. Confirm a summary of staged changes is shown BEFORE you click Apply (D-10).
    6. Click Apply. Confirm the station list reloads and the remapped station now plays the new live URL (no "stream exhausted"). Confirm an untouched flagged station is unchanged and was NOT deleted.
  </how-to-verify>
  <action>Human-only verification: the implementer has nothing to code in this task. Launch the app and walk the how-to-verify steps; this gate exists because the review-and-confirm summary (D-10) and the real-channel re-sync (D-01..D-10) are visual/functional and have no automated check. Do not auto-approve.</action>
  <verify><human-check>All how-to-verify steps observed: YouTube-only checkbox gate, provider-row menu (absent on Ungrouped), non-freezing scan, two-panel review, conservative unchecked drop/add, summary-before-Apply, list reload + new URL plays, untouched station preserved.</human-check></verify>
  <done>User typed "approved" after confirming the end-to-end refresh flow and the D-10 conservative/summary behavior.</done>
  <resume-signal>Type "approved" or describe any issue (e.g. menu missing, scan froze, summary absent, station deleted unexpectedly).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user right-click → provider scan | Provider context menu triggers a network scan of a stored channel URL |
| stored channel_scan_url → yt-dlp | The persisted URL is passed to scan_playlist (network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-14 | Denial of Service / SSRF (limited) | scan of stored channel_scan_url | mitigate | URL was validated by is_yt_playlist_url at store time (Plan 03 T-96-06); empty URL guarded with a toast; URL goes to yt-dlp (handles network), not raw urllib — acceptable for a user-trusted desktop field |
| T-96-15 | Elevation of Privilege | provider menu on Ungrouped (provider_id None) | mitigate | Menu gated on node.provider_id is not None; Ungrouped rows get no refresh action (Pitfall 4) |
| T-96-16 | Tampering | node_runtime missing → silent scan failure under .desktop | mitigate | node_runtime threaded MainWindow→panel→dialog→worker→scan_playlist (Pitfall 3); checkpoint step 1 exercises the .desktop launcher path |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (96-RESEARCH.md audit) |
</threat_model>

<verification>
- `.venv/bin/python -c "import musicstreamer.ui_qt.main_window; import musicstreamer.ui_qt.station_list_panel"` imports cleanly.
- `.venv/bin/python -m pytest tests/ -k "station_panel or tree_model or live_refresh or main_window" -q` green.
- Human-verify checkpoint approved (covers D-10 summary + D-01..D-10 real-channel re-sync).
</verification>

<success_criteria>
- Provider context menu reaches the refresh dialog, scoped to the provider; Ungrouped excluded.
- node_runtime threaded end-to-end; scan works under .desktop launchers.
- Station list reloads after apply; empty-scan-URL guarded with a prompt.
- Human-verify checkpoint passes.
</success_criteria>

<output>
Create `.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-05-SUMMARY.md` when done.
</output>
