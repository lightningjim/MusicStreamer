---
phase: 81
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/repo.py
  - tests/test_repo.py
autonomous: true
requirements: []
tags:
  - sqlite
  - sort
  - collation

must_haves:
  truths:
    - "D-01: Case-insensitive collation lives in SQLite (in repo.py ORDER BY clauses), not in Python; downstream UI consumers do not re-sort."
    - "D-02: COLLATE NOCASE applies to BOTH ORDER BY columns — COALESCE(p.name,'') and s.name — in both target queries."
    - "D-03: Exactly two queries are modified: Repo.list_stations() at repo.py:441 and Repo.list_favorite_stations() at repo.py:678."
    - "D-04: Out-of-scope sort sites stay untouched — Repo.list_providers() (repo.py:324), station_list_panel.py:505 (provider filter chips), station_list_panel.py:516 (tag chips), recent panel, search results, EditStationDialog stream rows."
    - "D-05: SQLite built-in NOCASE collation only — no Unicode-aware fold, no locale.strcoll, no natural-numeric sort."
    - "Behavioral truth: list_stations() returns mixed-case fixture stations (e.g. aardvark, deepSpace, Drone Zone, Groove Salad, Zenith) in case-insensitive A→Z order."
    - "Behavioral truth: list_favorite_stations() returns the favorite-filtered subset in the same case-insensitive A→Z order."
    - "Drift-guard truth: source-grep test fails if COLLATE NOCASE literal is removed from either target ORDER BY clause in musicstreamer/repo.py."
  artifacts:
    - path: "musicstreamer/repo.py"
      provides: "Two ORDER BY clauses with COLLATE NOCASE on both COALESCE(p.name,'') and s.name"
      contains: "COLLATE NOCASE"
    - path: "tests/test_repo.py"
      provides: "Behavioral interleave tests for list_stations + list_favorite_stations, plus a COLLATE NOCASE drift-guard test"
      contains: "case_insensitive"
  key_links:
    - from: "musicstreamer/repo.py:441 (list_stations)"
      to: "StationTreeModel._populate (ui_qt/station_tree_model.py)"
      via: "fetch order inherited; no Python-side re-sort"
      pattern: "ORDER BY COALESCE\\(p\\.name,''\\) COLLATE NOCASE, s\\.name COLLATE NOCASE"
    - from: "musicstreamer/repo.py:678 (list_favorite_stations)"
      to: "MainWindow._refresh_station_list → favorites view"
      via: "fetch order inherited; no Python-side re-sort"
      pattern: "ORDER BY COALESCE\\(p\\.name,''\\) COLLATE NOCASE, s\\.name COLLATE NOCASE"
---

<objective>
Fix the station-list alphabetical sort so mixed-case names interleave naturally instead of grouping ASCII A–Z before a–z. Append the SQLite `COLLATE NOCASE` modifier to both ORDER BY columns in the two repo queries that feed the station tree and the favorites view, then lock the behavior with two behavioral tests plus a source-grep drift-guard.

Purpose: closes the post-Phase-74 SomaFM-reimport bug where `deepSpace`, `Drone Zone`, `Groove Salad` were visually split into uppercase and lowercase blocks.
Output: surgical 2-line SQL edit in `musicstreamer/repo.py` + 3 new tests in `tests/test_repo.py`. No new files. No schema bump. No UI changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-station-list-alphabetical-sorting-is-case-sensitive-a-z-then/81-CONTEXT.md
@musicstreamer/repo.py
@tests/test_repo.py

<interfaces>
<!-- The two ORDER BY clauses to modify (verbatim current state, repo.py:435-443 and repo.py:671-680). -->
<!-- Executor: append " COLLATE NOCASE" to each of the two columns in each ORDER BY. No other line in repo.py changes. -->

repo.py:435-443 (Repo.list_stations):
    def list_stations(self) -> List[Station]:
        rows = self.con.execute(
            """
            SELECT s.*, p.name AS provider_name
            FROM stations s
            LEFT JOIN providers p ON p.id = s.provider_id
            ORDER BY COALESCE(p.name,''), s.name
            """
        ).fetchall()

repo.py:671-680 (Repo.list_favorite_stations):
    def list_favorite_stations(self) -> List[Station]:
        rows = self.con.execute(
            """
            SELECT s.*, p.name AS provider_name
            FROM stations s
            LEFT JOIN providers p ON p.id = s.provider_id
            WHERE s.is_favorite = 1
            ORDER BY COALESCE(p.name,''), s.name
            """
        ).fetchall()

Target form (BOTH queries):
    ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE

Test fixture helpers already in tests/test_repo.py:
    @pytest.fixture
    def repo(tmp_path):  # tests/test_repo.py:7 — clean DB
        ...

Repo mutators used by tests:
    repo.create_station() -> int                                # tests/test_repo.py:72 precedent
    repo.update_station(sid, name, provider_id, tags, art, fb)  # tests/test_repo.py:79 precedent (None for provider_id leaves it NULL)
    repo.set_station_favorite(sid, True)                        # repo.py:658

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append COLLATE NOCASE to both ORDER BY clauses in repo.py</name>
  <files>musicstreamer/repo.py</files>
  <read_first>
    - musicstreamer/repo.py (the file being modified — lines 435-443 and 671-680 are the two ORDER BY sites)
    - .planning/phases/81-station-list-alphabetical-sorting-is-case-sensitive-a-z-then/81-CONTEXT.md (D-01..D-05 contract)
  </read_first>
  <action>
    Make exactly two single-line edits in `musicstreamer/repo.py`:

    1. In `Repo.list_stations()` (around line 441), change the line
       `            ORDER BY COALESCE(p.name,''), s.name`
       to
       `            ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE`

    2. In `Repo.list_favorite_stations()` (around line 678), change the line
       `            ORDER BY COALESCE(p.name,''), s.name`
       to
       `            ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE`

    Per D-02, `COLLATE NOCASE` MUST appear on BOTH columns in BOTH queries — four total insertions of the literal `COLLATE NOCASE`.

    Do NOT modify any other line in repo.py. Do NOT touch `Repo.list_providers()` (out of scope per D-04). Do NOT add a new method, helper, or import. Do NOT change `Repo` signatures or return shapes. Do NOT add an index on `stations.name`. Do NOT bump `user_version` (D-05 — this is a query-side annotation, not a schema change).
  </action>
  <verify>
    <automated>grep -c "ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE" musicstreamer/repo.py | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE" musicstreamer/repo.py` returns exactly `2`.
    - `grep -n "ORDER BY COALESCE" musicstreamer/repo.py` shows exactly 2 hits, both at the modified line numbers (≈441 and ≈678), and both end with `COLLATE NOCASE`.
    - `git diff musicstreamer/repo.py` shows only those 2 line changes (no incidental edits, no whitespace churn elsewhere).
    - `python -c "import musicstreamer.repo"` succeeds (module still parses).
    - `pytest tests/test_repo.py -q` exits 0 (no pre-existing test regresses; the new tests from Task 2 are not yet present).
  </acceptance_criteria>
  <done>
    Both target ORDER BY clauses carry `COLLATE NOCASE` on both columns; repo.py imports cleanly; existing test suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add behavioral interleave tests + COLLATE NOCASE drift-guard to tests/test_repo.py</name>
  <files>tests/test_repo.py</files>
  <read_first>
    - tests/test_repo.py (existing fixtures `repo` at line 7, mutator patterns at lines 72-149)
    - musicstreamer/repo.py (verify `create_station`, `update_station`, `set_station_favorite` signatures used below)
    - .planning/phases/81-station-list-alphabetical-sorting-is-case-sensitive-a-z-then/81-CONTEXT.md (D-03 / D-05 / Claude's Discretion test approach)
  </read_first>
  <action>
    Append three new tests to the END of `tests/test_repo.py` (do NOT touch existing tests). Use the existing `repo` fixture (line 7).

    Use a shared helper inside the test module — define it once at module scope above the new tests:

        def _seed_mixed_case_stations(repo, names):
            ids = []
            for n in names:
                sid = repo.create_station()
                repo.update_station(sid, n, None, "", None, None)
                ids.append(sid)
            return ids

    Test 1 — `test_list_stations_case_insensitive_order`:
      - Seed names in scrambled insertion order: `["Zenith", "deepSpace", "aardvark", "Groove Salad", "Drone Zone"]` via `_seed_mixed_case_stations`.
      - Call `repo.list_stations()`, extract `[s.name for s in result]`.
      - Assert the order equals `["aardvark", "deepSpace", "Drone Zone", "Groove Salad", "Zenith"]`.
      - Inline-comment the assertion with `# D-01/D-02/D-03/D-05: SQLite NOCASE collation interleaves case-insensitively`.

    Test 2 — `test_list_favorite_stations_case_insensitive_order`:
      - Seed the same 5 names with `_seed_mixed_case_stations`.
      - Mark a deliberate subset as favorites: pick the 1st, 3rd, and 5th seeded ids and call `repo.set_station_favorite(sid, True)` on each.
      - Call `repo.list_favorite_stations()`, extract names.
      - Assert the result is the case-insensitive-sorted subset of the three favorited names. Compute the expected list as `sorted([names_subset], key=str.casefold)` — do NOT hardcode (keeps test resilient if the picked subset changes during review).
      - Inline-comment `# D-02/D-03: ORDER BY ... COLLATE NOCASE applies to favorites query too`.

    Test 3 — `test_collate_nocase_drift_guard`:
      - Open `musicstreamer/repo.py` via `pathlib.Path(__file__).resolve().parent.parent / "musicstreamer" / "repo.py"`, read text.
      - Strip comment-only lines first: `lines = [ln for ln in source.splitlines() if not ln.lstrip().startswith("#")]`; `body = "\n".join(lines)` (header-prose hygiene per planner rules).
      - Assert `body.count("ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE") == 2`.
      - Failure message must name the phase: `"Phase 81 D-03: both list_stations and list_favorite_stations must keep COLLATE NOCASE on both ORDER BY columns"`.
      - Inline-comment `# Source-grep drift-guard precedent: Phase 51 / 55 / 61 / 63`.

    Do NOT introduce new fixtures, conftest changes, or imports beyond `pathlib` (standard library). Do NOT touch tests for `list_providers`, recent panel, or filter chips — out of scope per D-04.
  </action>
  <verify>
    <automated>pytest tests/test_repo.py -k "case_insensitive_order or collate_nocase_drift_guard" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_repo.py::test_list_stations_case_insensitive_order -q` exits 0.
    - `pytest tests/test_repo.py::test_list_favorite_stations_case_insensitive_order -q` exits 0.
    - `pytest tests/test_repo.py::test_collate_nocase_drift_guard -q` exits 0.
    - Negative check: temporarily reverting Task 1 (manual local trial, NOT committed) causes all three new tests to FAIL — confirms the tests would have caught the bug pre-fix.
    - `pytest tests/test_repo.py -q` exits 0 (no regression in the 30+ existing test_repo tests).
    - `git diff tests/test_repo.py` shows only the helper + 3 new tests appended at end; no existing test modified.
  </acceptance_criteria>
  <done>
    Three new tests added and passing; all pre-existing test_repo tests still green; drift-guard literally fails if `COLLATE NOCASE` is removed from either target ORDER BY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Phase 81 modifies only the SQL `ORDER BY` modifier on two pre-existing repo queries. No new user input crosses a boundary; no new auth surface; no new file I/O; no new network path; no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-00 | (no new threats) | `Repo.list_stations` / `Repo.list_favorite_stations` ORDER BY clauses | accept | `COLLATE NOCASE` is a SQL collation keyword, not interpolated user data. No parameters added to either repo method. No new input surfaces, auth, file I/O, or network paths. Threat surface unchanged from pre-Phase-81 baseline. |

No package installs in this phase, so the Package Legitimacy Audit / T-{phase}-SC supply-chain row does not apply.
</threat_model>

<verification>
- Source assertion: `grep -c "ORDER BY COALESCE(p.name,'') COLLATE NOCASE, s.name COLLATE NOCASE" musicstreamer/repo.py` returns `2`.
- Source assertion: `grep -n "ORDER BY COALESCE" musicstreamer/repo.py` returns exactly 2 lines, both ending in `COLLATE NOCASE`.
- Behavioral test command: `pytest tests/test_repo.py -k "case_insensitive_order or collate_nocase_drift_guard" -q` exits 0.
- Regression net: `pytest tests/test_repo.py -q` exits 0.
- Module-import smoke: `python -c "import musicstreamer.repo; import musicstreamer.ui_qt.station_tree_model"` succeeds.
- Diff scope check: `git diff --stat` reports modifications ONLY in `musicstreamer/repo.py` and `tests/test_repo.py`.
- Out-of-scope grep (D-04): `grep -n "COLLATE NOCASE" musicstreamer/repo.py` returns exactly the 2 expected sites and no others (e.g. `list_providers` line 324 area stays untouched).
</verification>

<success_criteria>
- Both Tasks 1 and 2 land in a single atomic plan execution.
- `musicstreamer/repo.py` carries `COLLATE NOCASE` on both columns of both target ORDER BY clauses; no other line in repo.py changes.
- `tests/test_repo.py` contains the three new tests (`test_list_stations_case_insensitive_order`, `test_list_favorite_stations_case_insensitive_order`, `test_collate_nocase_drift_guard`) and all three pass.
- Full `pytest tests/test_repo.py -q` is green.
- D-01..D-05 are each cited as a `must_haves.truths` entry above and verifiable from the source diff.
- Phase ROADMAP entry can flip from `[ ]` to `[x]` after this single plan.
</success_criteria>

<output>
Create `.planning/phases/81-station-list-alphabetical-sorting-is-case-sensitive-a-z-then/81-01-SUMMARY.md` when done. Summary MUST cite each of D-01..D-05 with one line on how it was honored, list the exact line numbers of the two modified ORDER BY clauses, name the three added tests, and include the post-fix `grep -c` output proving `COLLATE NOCASE` appears exactly twice in repo.py.
</output>
</content>
</invoke>