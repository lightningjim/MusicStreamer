---
phase: 03-icy-metadata-display
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - musicstreamer/player.py
  - tests/test_player_tag.py
autonomous: true
requirements:
  - NOW-01
  - NOW-02
  - NOW-03

must_haves:
  truths:
    - "_on_gst_tag extracts Gst.TAG_TITLE and calls stored on_title callback"
    - "Multiple TAG messages each invoke on_title with the new title value"
    - "_fix_icy_encoding corrects latin-1 mojibake back to UTF-8"
    - "on_title callback is cleared to None on stop() preventing stale updates"
    - "TAG messages without Gst.TAG_TITLE are silently ignored"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "_on_gst_tag method, _fix_icy_encoding function, _on_title instance var"
      contains: "def _on_gst_tag"
    - path: "tests/test_player_tag.py"
      provides: "Unit tests for TAG handling and ICY encoding"
      contains: "def test_"
  key_links:
    - from: "musicstreamer/player.py"
      to: "GLib.idle_add"
      via: "_on_gst_tag marshals on_title call to main loop"
      pattern: "GLib\\.idle_add.*self\\._on_title"
    - from: "musicstreamer/player.py"
      to: "_fix_icy_encoding"
      via: "TAG handler applies encoding fix before callback"
      pattern: "_fix_icy_encoding\\(value\\)"
---

<objective>
Add GStreamer TAG bus handling to Player: connect message::tag, extract Gst.TAG_TITLE, apply ICY encoding fix, call on_title callback via GLib.idle_add. Clear callback on stop() to prevent stale updates.

Purpose: This is the data pipeline that feeds track titles to the UI. Without it, the now-playing panel has nothing to display.
Output: Updated player.py with TAG handling + test_player_tag.py with full unit coverage.
</objective>

<execution_context>
@/home/kcreasey/.claude/get-shit-done/workflows/execute-plan.md
@/home/kcreasey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-icy-metadata-display/03-CONTEXT.md
@.planning/phases/03-icy-metadata-display/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From musicstreamer/player.py (current):
```python
class Player:
    def __init__(self):
        self._pipeline = Gst.ElementFactory.make("playbin3", "player")
        bus = self._pipeline.get_bus()
        bus.add_signal_watch()
        bus.connect("message::error", self._on_gst_error)
        self._yt_proc = None

    def play(self, station: Station, on_title: callable): ...
    def stop(self): ...
    def _play_youtube(self, url, fallback_name, on_title): ...
    def _set_uri(self, uri, title, on_title): ...
```

From musicstreamer/models.py:
```python
@dataclass
class Station:
    id: int
    name: str
    url: str
    provider_id: Optional[int]
    provider_name: Optional[str]
    tags: str
    station_art_path: Optional[str]
    album_fallback_path: Optional[str]
```
</interfaces>
</context>

<feature>
  <name>GStreamer TAG Bus Handler with ICY Encoding Fix</name>
  <files>musicstreamer/player.py, tests/test_player_tag.py</files>
  <behavior>
    - Test 1: _fix_icy_encoding("R\u00c3\u00b6yksopp") returns "Royksopp" (UTF-8 mojibake correction)
    - Test 2: _fix_icy_encoding("Artist - Title") returns "Artist - Title" (clean ASCII passthrough)
    - Test 3: _fix_icy_encoding("caf\u00e9") returns "caf\u00e9" (genuine latin-1 passthrough)
    - Test 4: _on_gst_tag with TAG_TITLE present calls on_title with the title string
    - Test 5: _on_gst_tag with TAG_TITLE absent does NOT call on_title
    - Test 6: Multiple _on_gst_tag calls each invoke on_title with updated title
    - Test 7: After stop(), _on_title is None -- stale TAG message does not call callback
    - Test 8: play() stores on_title callback as self._on_title
    - Test 9: _on_gst_tag calls GLib.idle_add (not direct callback) for thread safety
  </behavior>
  <implementation>
    See tasks below for RED then GREEN phases.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for TAG handler and ICY encoding</name>
  <files>tests/test_player_tag.py</files>
  <read_first>
    - musicstreamer/player.py
    - tests/test_filter_utils.py
    - musicstreamer/models.py
  </read_first>
  <action>
Create tests/test_player_tag.py with the following test cases. Use unittest.mock to mock GStreamer objects (Gst.Message, Gst.TagList) since tests run without a display server.

Test structure:

1. `test_fix_icy_encoding_mojibake` -- Create a string that is UTF-8 bytes decoded as latin-1 (simulate "Royksopp" appearing as multi-byte garbage). Call `_fix_icy_encoding(bad_string)`. Assert result equals the correct UTF-8 string.

2. `test_fix_icy_encoding_ascii_passthrough` -- Call `_fix_icy_encoding("Artist - Title")`. Assert returns "Artist - Title" unchanged.

3. `test_fix_icy_encoding_latin1_passthrough` -- Call `_fix_icy_encoding("cafe\u0301")` or a genuine latin-1 string like `"caf\u00e9"`. Assert returns unchanged.

4. `test_on_gst_tag_calls_on_title` -- Create a Player, set `player._on_title` to a Mock. Create a mock `msg` where `msg.parse_tag()` returns a mock taglist, and `taglist.get_string("title")` returns `(True, "Some Track")`. Patch `GLib.idle_add` to capture the call. Call `player._on_gst_tag(mock_bus, msg)`. Assert `GLib.idle_add` was called with the on_title mock and the encoded-fixed title.

5. `test_on_gst_tag_ignores_missing_title` -- Same as above but `taglist.get_string("title")` returns `(False, "")`. Assert `GLib.idle_add` was NOT called.

6. `test_on_gst_tag_multiple_updates` -- Call `_on_gst_tag` twice with different titles. Assert `GLib.idle_add` called twice with respective titles.

7. `test_stop_clears_on_title` -- Create a Player, set `player._on_title = some_mock`. Call `player.stop()`. Assert `player._on_title is None`.

8. `test_play_stores_on_title` -- Create a Player, call `player.play(station, callback)`. Assert `player._on_title is callback`. (Mock _set_uri and _play_youtube to prevent actual playback.)

9. `test_on_gst_tag_uses_idle_add` -- Verify that `_on_gst_tag` calls `GLib.idle_add` rather than invoking `_on_title` directly.

Import `_fix_icy_encoding` from `musicstreamer.player` (it will be a module-level function). Import `Player` from `musicstreamer.player`.

For Player instantiation in tests, patch `Gst.ElementFactory.make` to return a mock pipeline (so no actual GStreamer pipeline is created). The mock pipeline's `get_bus()` should return a mock bus with `add_signal_watch()` and `connect()` as no-ops.

Run tests -- they MUST fail (RED phase) because `_on_gst_tag`, `_fix_icy_encoding`, and `_on_title` attribute do not exist yet.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && uv run --with pytest pytest tests/test_player_tag.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_player_tag.py exists
    - tests/test_player_tag.py contains "from musicstreamer.player import Player"
    - tests/test_player_tag.py contains "from musicstreamer.player import _fix_icy_encoding"
    - tests/test_player_tag.py contains "def test_fix_icy_encoding_mojibake"
    - tests/test_player_tag.py contains "def test_on_gst_tag_calls_on_title"
    - tests/test_player_tag.py contains "def test_stop_clears_on_title"
    - tests/test_player_tag.py contains "def test_on_gst_tag_uses_idle_add"
    - tests/test_player_tag.py contains at least 8 test functions (grep -c "def test_" returns >= 8)
    - Running pytest on the file produces FAILED (ImportError or AttributeError -- functions don't exist yet)
  </acceptance_criteria>
  <done>Test file exists with 8+ test cases. All tests fail because production code lacks _on_gst_tag and _fix_icy_encoding. RED phase complete.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Implement TAG handler and ICY encoding in player.py</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    - musicstreamer/player.py
    - tests/test_player_tag.py
    - .planning/phases/03-icy-metadata-display/03-RESEARCH.md
  </read_first>
  <action>
Modify musicstreamer/player.py to make all tests pass:

1. Add module-level function `_fix_icy_encoding(s: str) -> str`:
```python
def _fix_icy_encoding(s: str) -> str:
    """Re-encode latin-1 mojibake back to proper UTF-8."""
    try:
        return s.encode('latin-1').decode('utf-8')
    except (UnicodeDecodeError, UnicodeEncodeError):
        return s
```

2. In `Player.__init__`:
   - Add `self._on_title = None` after existing `self._yt_proc = None`
   - Add `bus.connect("message::tag", self._on_gst_tag)` after existing `bus.connect("message::error", ...)`

3. Add method `Player._on_gst_tag(self, bus, msg)`:
```python
def _on_gst_tag(self, bus, msg):
    taglist = msg.parse_tag()
    found, value = taglist.get_string(Gst.TAG_TITLE)
    if not found:
        return
    title = _fix_icy_encoding(value)
    if self._on_title:
        GLib.idle_add(self._on_title, title)
```

4. In `Player.play()`:
   - Add `self._on_title = on_title` as the first line of the method body (before any URL checks)

5. In `Player.stop()`:
   - Add `self._on_title = None` as the first line (before `_stop_yt_proc`)

6. In `Player._set_uri()`:
   - Remove the `on_title(title)` call at the end. The station name will be set by the UI directly in _play_station, and ICY TAG messages will update the track title via the bus handler. (The UI sets station name immediately; TAG updates track title asynchronously.)
   - Keep the method signature unchanged (on_title param stays for compatibility but is no longer called here since play() stores it).

7. In `Player._play_youtube()`:
   - Keep `on_title(fallback_name)` call -- YouTube has no TAG bus messages, so the direct call is correct.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && uv run --with pytest pytest tests/test_player_tag.py -x -q && uv run --with pytest pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/player.py contains "def _fix_icy_encoding(s: str) -> str:"
    - musicstreamer/player.py contains "def _on_gst_tag(self, bus, msg):"
    - musicstreamer/player.py contains "self._on_title = None" (in __init__)
    - musicstreamer/player.py contains "self._on_title = on_title" (in play())
    - musicstreamer/player.py contains "bus.connect(\"message::tag\", self._on_gst_tag)"
    - musicstreamer/player.py contains "GLib.idle_add(self._on_title, title)"
    - musicstreamer/player.py contains "taglist.get_string(Gst.TAG_TITLE)"
    - Player.stop() sets self._on_title = None
    - All tests in tests/test_player_tag.py pass (exit 0)
    - All existing tests in tests/ still pass (exit 0)
  </acceptance_criteria>
  <done>All TAG handler tests pass. Existing tests still pass. _fix_icy_encoding corrects mojibake. _on_gst_tag extracts TAG_TITLE and calls on_title via idle_add. stop() clears callback. GREEN phase complete.</done>
</task>

</tasks>

<verification>
- `uv run --with pytest pytest tests/test_player_tag.py -x -q` exits 0
- `uv run --with pytest pytest tests/ -q` exits 0 (full suite including existing 28 tests)
- `grep -c "def test_" tests/test_player_tag.py` returns >= 8
</verification>

<success_criteria>
- Player handles GStreamer TAG messages and extracts track titles
- ICY encoding mojibake is corrected before reaching the UI
- Stale callbacks are prevented after stop()
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-icy-metadata-display/03-01-SUMMARY.md`
</output>
