---
phase: 96.1-show-currently-live-stream-titles-for-newly-discovered-strea
plan: "03"
subsystem: live_refresh_dialog
tags: [wave-3, tdd-green, live_refresh_dialog, discover, node_runtime, d02, d04, d05, d06, d07, d08]

dependency_graph:
  requires:
    - phase: 96.1-01
      provides: "7 RED dialog stubs for _DiscoverRowWidget + worker resolve threading"
    - phase: 96.1-02
      provides: "resolve_live_title + _is_blank_title in yt_import.py"
  provides:
    - "_LiveRefreshScanWorker.run() resolves blank titles off-thread threading node_runtime (D-02)"
    - "_DiscoverRowWidget: read-only PlainText title, add/map toggle, flagged dropdown, unchecked default (D-04/D-06/D-07/D-08)"
    - "_populate_rows Newly-Discovered section lists every scan entry with no dedup (D-05)"
  affects:
    - musicstreamer/ui_qt/live_refresh_dialog.py

tech-stack:
  added: []
  patterns:
    - "QButtonGroup + QRadioButton pair for add/map action toggle in _DiscoverRowWidget"
    - "D-02: blank-resolution loop in _LiveRefreshScanWorker.run() after scan_playlist, before finished.emit"
    - "D-04/CR-01/T-39-01: setTextFormat(Qt.PlainText) on scan-derived title label (mirrors _RowWidget lines 322-331)"
    - "Duck-type: _DiscoverRowWidget exposes ._station, is_staged(), build_staged_change() matching _RowWidget interface"
    - "stream_id resolution: mirrors _RowWidget lines 414-428 exactly (position==1, fallback streams[0].id)"

key-files:
  created: []
  modified:
    - musicstreamer/ui_qt/live_refresh_dialog.py

key-decisions:
  - "_DiscoverRowWidget uses QRadioButton pair (Add / Map) rather than QComboBox action selector for clearer visual affordance"
  - "_on_action_toggled / _on_map_combo_changed keep self._station in sync so _on_apply duck-type works without widget introspection"
  - "_row_widgets type widened to List[QWidget] (was List[_RowWidget]) to accommodate both row types without Union complexity"
  - "No status_update Signal added to _LiveRefreshScanWorker — plan spec says keep it simple; finished/error signals are sufficient"

patterns-established:
  - "_DiscoverRowWidget build_staged_change: add mode returns action='add'; map mode returns action='remap' with stream_id resolved from chosen.streams"
  - "Blank-title resolution loop: for entry in results: if _is_blank_title(...): entry['title'] = resolve_live_title(..., node_runtime=self._node_runtime)"

requirements-completed: []

duration: ~2min
completed: "2026-06-22"
---

# Phase 96.1 Plan 03: Wire Display/Action Half — _DiscoverRowWidget + Worker Resolution Summary

**`_LiveRefreshScanWorker.run()` extended to resolve blank titles off-thread, `_DiscoverRowWidget` added with read-only PlainText title + add/map toggle + flagged-station dropdown, and `_populate_rows` updated to list every scan entry in a "Newly discovered" section — turns all 7 Plan-01 dialog RED stubs GREEN**

## Performance

- **Duration:** ~2 min
- **Completed:** 2026-06-22
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments

- **Task 1 (D-02):** Extended `_LiveRefreshScanWorker.run()` in-place (no new QThread class). After `scan_playlist` returns and before `finished.emit(results)`, a loop over results calls `yt_import._is_blank_title()` gate and then `yt_import.resolve_live_title(url, node_runtime=self._node_runtime)` for each blank entry. Worker emits Signals only — no widget access (T-96.1-06).
- **Task 2 (D-04/D-06/D-07/D-08):** Added `_DiscoverRowWidget(entry, flagged_stations, provider_name)` modeled on `_RowWidget`. Read-only title `QLabel` calls `setTextFormat(Qt.PlainText)` (CR-01/T-39-01). Unchecked by default. Add/map action toggle via `QRadioButton` pair in `QButtonGroup`. `QComboBox _map_combo` populated from `flagged_stations` with `Station` userData. `build_staged_change`: add mode returns `action="add"`; map mode mirrors `_RowWidget` lines 414-428 primary-stream-id resolution and returns `action="remap"` with `stream_id`. Duck-type compatible with `_on_apply` loop.
- **Task 3 (D-05):** Replaced old `_RowWidget("add", ...)` loop in `_populate_rows` with `_DiscoverRowWidget` per scan entry — no dedup, every scan result listed. Header label "Newly discovered on channel (unchecked by default):". `_row_widgets` type widened to `List[QWidget]`. `_on_apply` and `apply_refresh`/WR-03 guard unchanged.

## Task Commits

1. **Task 1: Extend _LiveRefreshScanWorker.run() to resolve blank titles off-thread** — `d7b0a500` (feat)
2. **Task 2: Add _DiscoverRowWidget** — `ed75f59f` (feat)
3. **Task 3: Replace ADD section in _populate_rows with Newly-Discovered section** — `0ae883f0` (feat)

## Files Created/Modified

- `musicstreamer/ui_qt/live_refresh_dialog.py` — Extended `_LiveRefreshScanWorker.run()`, added `_DiscoverRowWidget`, updated `_populate_rows`, widened `_row_widgets` type hint, added `QButtonGroup`/`QRadioButton` to imports

## Test Results

```
48 passed, 1 warning in 0.43s
```

- 41 pre-existing tests: GREEN (no regression)
- 7 Phase 96.1 dialog stubs: GREEN (all RED stubs now passing)
- Total 13 Phase 96.1 tests GREEN (6 yt_import from Plan 02 + 7 dialog from this plan)

## Decisions Made

- `_DiscoverRowWidget` uses a `QRadioButton` pair (Add / Map onto existing) rather than a `QComboBox` action selector for clearer visual affordance and simpler test interaction
- `_on_action_toggled` / `_on_map_combo_changed` keep `self._station` synchronized so `_on_apply`'s `row._station.id if row._station else None` duck-type expression works
- `_row_widgets` type widened to `List[QWidget]` (was `List[_RowWidget]`) to accommodate both row types cleanly
- No `status_update` Signal added to worker — plan says keep it simple; `finished`/`error` are sufficient

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all 7 Plan-01 dialog stubs are now GREEN. All 48 tests in the scoped run pass.

## Threat Flags

None — no new network endpoints, auth paths, or file access patterns beyond what the plan's threat model covers:
- T-39-01/CR-01: `setTextFormat(Qt.PlainText)` applied to discover title label (mitigated)
- T-96.1-06: worker emits Signals only, no widget access (mitigated)
- T-96.1-07: discover rows unchecked by default (mitigated)
- T-96.1-05: WR-03 guard in `apply_refresh` unchanged, covers discover-row `action="remap"` output (mitigated)

## Self-Check: PASSED

- FOUND: `.planning/phases/96.1-.../96.1-03-SUMMARY.md` (this file)
- FOUND: commit `d7b0a500` (Task 1 — worker blank-title resolution)
- FOUND: commit `ed75f59f` (Task 2 — _DiscoverRowWidget)
- FOUND: commit `0ae883f0` (Task 3 — _populate_rows Newly Discovered section)
- VERIFIED: `grep -c "class _DiscoverRowWidget" live_refresh_dialog.py` = 1
- VERIFIED: `grep -c "setTextFormat(Qt.PlainText)" live_refresh_dialog.py` = 4 (>= 3)
- VERIFIED: 48/48 tests pass in scoped run (`tests/test_live_refresh_dialog.py tests/test_yt_import_library.py`)

---
*Phase: 96.1-show-currently-live-stream-titles-for-newly-discovered-strea*
*Completed: 2026-06-22*
