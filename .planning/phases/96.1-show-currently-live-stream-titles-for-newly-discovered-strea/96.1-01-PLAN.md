---
phase: 96.1-show-currently-live-stream-titles-for-newly-discovered-strea
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_yt_import_library.py
  - tests/test_live_refresh_dialog.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Wave 0: red test stubs exist for the blank-title predicate (D-01)"
    - "Wave 0: red test stubs exist for resolve_live_title node-runtime threading + no-extract_flat + fallback (D-02, D-03)"
    - "Wave 0: red test stubs exist for the discover-row PlainText title, no-dedup listing, add/map staged changes, flagged-station dropdown, unchecked-by-default (D-04, D-05, D-06, D-07, D-08)"
  artifacts:
    - path: "tests/test_yt_import_library.py"
      provides: "6 new RED test functions for the yt_import title-resolution helpers"
      contains: "def test_is_blank_title_predicate"
    - path: "tests/test_live_refresh_dialog.py"
      provides: "7 new RED test functions for the dialog discover section"
      contains: "def test_discover_row_title_label_plaintext"
  key_links:
    - from: "tests/test_yt_import_library.py"
      to: "musicstreamer.yt_import._is_blank_title / resolve_live_title"
      via: "module attribute import (will not exist yet -> RED)"
      pattern: "yt_import\\.(resolve_live_title|_is_blank_title|_fallback_title)"
    - from: "tests/test_live_refresh_dialog.py"
      to: "musicstreamer.ui_qt.live_refresh_dialog._DiscoverRowWidget"
      via: "module attribute import (will not exist yet -> RED)"
      pattern: "_DiscoverRowWidget"
---

<objective>
Wave 0: author the failing test stubs (RED) for Phase 96.1 across the two touched test files so the implementation waves (yt_import backend in Plan 02, dialog UI in Plan 03) have red tests to turn green. No production code changes in this plan.

Purpose: VALIDATION.md mandates a Wave 0 test-stub plan (~13 functions). Writing the contracts first makes every downstream decision (D-01..D-08) verifiable and prevents shallow implementation.
Output: 6 new test functions in tests/test_yt_import_library.py and 7 new test functions in tests/test_live_refresh_dialog.py, all expected to fail (RED) because the production symbols do not exist yet.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-CONTEXT.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-RESEARCH.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-VALIDATION.md

<interfaces>
<!-- Symbols these test stubs reference. They do NOT exist yet (Plan 02 / 03 create them). Importing them is what makes the stubs RED. -->

To be created in Plan 02 (musicstreamer/yt_import.py):
  def _is_blank_title(title: str) -> bool
  def _fallback_title(url: str) -> str          # "Untitled — watch?v=<id>"
  def resolve_live_title(url: str, *, node_runtime: "NodeRuntime | None" = None) -> str

To be created in Plan 03 (musicstreamer/ui_qt/live_refresh_dialog.py):
  class _DiscoverRowWidget(QWidget):
      def __init__(self, entry: dict, flagged_stations: list, provider_name: str, parent=None)
      def is_staged(self) -> bool
      def build_staged_change(self, station_id=None) -> Optional[dict]
      # exposes ._station (None in add mode, chosen Station in map mode) for _on_apply duck-type

Existing test helpers to reuse VERBATIM:
  tests/test_yt_import_library.py:31  _patch_youtubedl(extract_info_return=None, extract_info_side_effect=None)
        -> returns (patch_cm, youtubedl_cls, fake_ydl); opts via youtubedl_cls.call_args[0][0]
  tests/test_yt_import_library.py:22  autouse fixture _reset_root_override (inherited)
  tests/test_live_refresh_dialog.py:56  _require_module()  (raises stored _IMPORT_ERROR)
  tests/test_live_refresh_dialog.py:46  db_repo fixture (real SQLite Repo)
  NodeRuntime: from musicstreamer.runtime_check import NodeRuntime  (NodeRuntime(available=True, path="/fake/node"))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 RED test stubs to tests/test_yt_import_library.py</name>
  <read_first>
    - tests/test_yt_import_library.py (entire file — reuse _patch_youtubedl at line 31, the autouse fixture at line 22, the node-path assertion idiom at lines 211-220, and the no-extract_flat idiom at lines 320-333)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-RESEARCH.md (Validation Architecture > Phase Requirements -> Test Map; Code Examples section for resolve_live_title)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md (tests/test_yt_import_library.py section, lines 311-367)
  </read_first>
  <action>
    Append six new test functions to tests/test_yt_import_library.py, mirroring the existing _patch_youtubedl helper and node-path opts-inspection idiom (opts = youtubedl_cls.call_args[0][0]).
    - test_is_blank_title_predicate (D-01): assert yt_import._is_blank_title("") is True, yt_import._is_blank_title("Untitled") is True, yt_import._is_blank_title("  ") is True, yt_import._is_blank_title("UNTITLED") is True (case-insensitive), and yt_import._is_blank_title("Lofi beats 24/7") is False.
    - test_resolve_live_title_returns_resolved (D-01): patch via _patch_youtubedl(extract_info_return={"title": "Real Live Title"}); assert yt_import.resolve_live_title("https://www.youtube.com/watch?v=abc123") == "Real Live Title".
    - test_resolve_live_title_threads_node_runtime (D-02 LANDMINE): patch via _patch_youtubedl; call resolve_live_title(url, node_runtime=NodeRuntime(available=True, path="/fake/node")); read opts = youtubedl_cls.call_args[0][0]; assert opts["js_runtimes"] == {"node": {"path": "/fake/node"}} (the build_js_runtimes shape proven in test_scan_playlist_passes_node_path_when_available).
    - test_resolve_live_title_no_extract_flat (Pitfall 1): patch via _patch_youtubedl; call resolve_live_title; read opts = youtubedl_cls.call_args[0][0]; assert "extract_flat" not in opts.
    - test_resolve_live_title_fallback_on_error (D-03): patch via _patch_youtubedl(extract_info_side_effect=Exception("boom")); assert yt_import.resolve_live_title("https://www.youtube.com/watch?v=abc123") == "Untitled — watch?v=abc123" (no exception escapes).
    - test_fallback_title_includes_video_id (D-03): assert yt_import._fallback_title("https://www.youtube.com/watch?v=abc123") == "Untitled — watch?v=abc123"; and a URL with no v= param returns a string starting with "Untitled — ".
    Use the em dash character (—, U+2014) per D-03 wording. These are RED because the symbols do not exist yet; the failure mode is AttributeError/ImportError, which is the expected Wave 0 state.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_yt_import_library.py -k "is_blank_title or resolve_live_title or fallback_title" -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "def test_is_blank_title_predicate\|def test_resolve_live_title_returns_resolved\|def test_resolve_live_title_threads_node_runtime\|def test_resolve_live_title_no_extract_flat\|def test_resolve_live_title_fallback_on_error\|def test_fallback_title_includes_video_id" tests/test_yt_import_library.py` returns 6.
    - Behavior: the six tests are collected (no collection error) and FAIL/ERROR with AttributeError on `yt_import.resolve_live_title` / `_is_blank_title` / `_fallback_title` (RED — symbols not implemented yet).
    - The new tests reuse the existing `_patch_youtubedl` helper (no re-declaration).
  </acceptance_criteria>
  <done>6 RED test functions added to tests/test_yt_import_library.py covering D-01, D-02, D-03 and Pitfall 1; all fail because the yt_import symbols do not yet exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add 7 RED test stubs to tests/test_live_refresh_dialog.py</name>
  <read_first>
    - tests/test_live_refresh_dialog.py (entire file — reuse _require_module at line 56, db_repo at line 46, the qtbot PlainText idiom in test_row_labels_use_plaintext_against_injection at lines 369-393, the worker-threading idiom in test_scan_worker_forwards_node_runtime at lines 105-136, and the MagicMock repo idiom at lines 209-249)
    - musicstreamer/ui_qt/live_refresh_dialog.py (_RowWidget lines 282-428 for the duck-type interface _DiscoverRowWidget must satisfy: is_staged(), build_staged_change(station_id=None), ._station; _LiveRefreshScanWorker.run lines 86-96)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md (tests/test_live_refresh_dialog.py section, lines 371-441)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-RESEARCH.md (Pattern 4 _DiscoverRowWidget + build_staged_change Map Mode code example)
  </read_first>
  <action>
    Append seven new test functions to tests/test_live_refresh_dialog.py. Each opens with _require_module(). Import _DiscoverRowWidget and _LiveRefreshScanWorker from musicstreamer.ui_qt.live_refresh_dialog (will not exist for _DiscoverRowWidget -> RED). Use SimpleNamespace / Station / StationStream and MagicMock repos as the existing tests do; build the discover entry as a dict {"title": ..., "url": ..., "provider": ...} matching scan_playlist output shape.
    - test_scan_worker_threads_node_runtime_into_resolve (D-02): patch musicstreamer.yt_import.scan_playlist to return [{"title": "", "url": "https://www.youtube.com/watch?v=abc", "provider": "YBC"}] AND patch musicstreamer.yt_import.resolve_live_title; construct _LiveRefreshScanWorker(url, node_runtime=sentinel); call worker.run(); assert resolve_live_title was called once with node_runtime=sentinel (kwarg). Mirror test_scan_worker_forwards_node_runtime at lines 105-136.
    - test_discover_row_title_label_plaintext (D-04 / CR-01 / T-39-01): qtbot test; build entry with title = "<img src=http://attacker/x.png>"; instantiate _DiscoverRowWidget(entry, [], "YBC"); qtbot.addWidget(row); collect QLabel children whose text contains the evil string; assert at least one and that every such label.textFormat() == Qt.PlainText. Mirror test_row_labels_use_plaintext_against_injection at lines 369-393.
    - test_discover_section_no_dedup (D-05): build a LiveRefreshDialog (or call _populate_rows directly) where one scan entry URL is also the anchor-closest match of a flagged station REMAP row; assert a _DiscoverRowWidget exists for EVERY scan entry (count of discover rows == len(scan_results)), proving no dedup hiding. Use MagicMock repo with list_flagged_stations_for_provider returning one Station; if a full dialog is hard to build headlessly, assert against _populate_rows row construction by counting _DiscoverRowWidget instances in self._row_widgets.
    - test_discover_row_add_staged_change (D-06 add): _DiscoverRowWidget in add mode, checked; assert build_staged_change() == {"action": "add", "name": <name or title>, "scan_result": entry, "provider_name": "YBC"}.
    - test_discover_row_map_staged_change (D-06 map): qtbot test; create a Station with streams=[StationStream(id=10, position=1, ...)]; _DiscoverRowWidget(entry, [station], "YBC"); switch the row to map mode and select that station in the dropdown; check the box; assert build_staged_change() == {"action": "remap", "station_id": station.id, "stream_id": 10, "scan_result": entry}.
    - test_discover_row_map_dropdown_from_flagged (D-07): pass flagged_stations=[stationA, stationB]; switch to map mode; assert the map QComboBox has 2 items whose userData are stationA and stationB respectively.
    - test_discover_row_unchecked_by_default (D-08): instantiate _DiscoverRowWidget(entry, [], "YBC"); assert row.is_staged() is False immediately after construction.
    These are RED because _DiscoverRowWidget does not exist yet; test_scan_worker_threads_node_runtime_into_resolve is RED because resolve_live_title does not exist and run() does not yet call it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -k "discover or threads_node_runtime_into_resolve" -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "def test_scan_worker_threads_node_runtime_into_resolve\|def test_discover_row_title_label_plaintext\|def test_discover_section_no_dedup\|def test_discover_row_add_staged_change\|def test_discover_row_map_staged_change\|def test_discover_row_map_dropdown_from_flagged\|def test_discover_row_unchecked_by_default" tests/test_live_refresh_dialog.py` returns 7.
    - Behavior: the seven tests are collected (no collection-time syntax error) and FAIL/ERROR (ImportError on _DiscoverRowWidget, AttributeError on resolve_live_title) — RED.
    - test_discover_row_title_label_plaintext asserts `Qt.PlainText` on the untrusted title label (the CR-01 / T-39-01 contract).
    - The pre-existing dialog tests still pass: `.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -k "not discover and not threads_node_runtime_into_resolve" -q` is green.
  </acceptance_criteria>
  <done>7 RED test functions added to tests/test_live_refresh_dialog.py covering D-02 (worker resolve threading), D-04, D-05, D-06, D-07, D-08; all fail because _DiscoverRowWidget / resolve_live_title do not exist yet, while existing dialog tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network (YouTube) → app | scan-derived titles cross into the process; untrusted (this plan only encodes the contract in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96.1-01 | Tampering/Spoofing | scan-derived title rendered in QLabel | mitigate | test_discover_row_title_label_plaintext asserts Qt.PlainText on the untrusted title label (CR-01 / T-39-01, D-04); the runtime mitigation lands in Plan 03 |
| T-96.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero external packages); nothing to verify |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_yt_import_library.py tests/test_live_refresh_dialog.py -q` collects all new stubs without a syntax/collection error.
- 13 new test functions present and RED (failing on missing symbols); all pre-existing tests in both files still pass.
- No production source files (musicstreamer/**) are modified by this plan.
</verification>

<success_criteria>
- 6 RED stubs in tests/test_yt_import_library.py + 7 RED stubs in tests/test_live_refresh_dialog.py.
- Every Wave-0 test named in VALIDATION.md exists.
- Pre-existing 35-test baseline (26 + 9) remains green.
</success_criteria>

<output>
Create `.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-01-SUMMARY.md` when done
</output>
