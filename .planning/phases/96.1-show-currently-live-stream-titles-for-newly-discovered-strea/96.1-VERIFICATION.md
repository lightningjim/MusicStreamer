---
phase: 96.1-show-currently-live-stream-titles-for-newly-discovered-strea
verified: 2026-06-22T03:49:39Z
status: passed
score: 8/8 must-haves verified
has_blocking_gaps: false
overrides_applied: 0
---

# Phase 96.1: Show Live Titles for Newly-Discovered Streams — Verification Report

**Phase Goal:** Refine the Phase 96 `LiveRefreshDialog` so that newly-discovered currently-live YouTube streams (live streams on the channel with no existing flagged station) are surfaced with a reliable, identifiable title and can be mapped/merged onto an existing flagged station. Fix: targeted per-video metadata fetch for blank/Untitled flat-scan entries, off-thread with node-runtime threading, plus a display section with add/map actions.
**Verified:** 2026-06-22T03:49:39Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (D-01..D-08)

| #  | Decision | Truth | Status | Evidence |
|----|----------|-------|--------|----------|
| 1  | D-01 | Hybrid: flat scan kept; only blank/"Untitled" entries get a per-video fetch (`_is_blank_title`, `resolve_live_title`) | VERIFIED | `yt_import.py:74-81` (`_is_blank_title`), `yt_import.py:322-383` (`resolve_live_title`); worker loop at `live_refresh_dialog.py:99-103` checks `_is_blank_title` before calling `resolve_live_title` — non-blank entries are never re-resolved |
| 2  | D-02 | Off-thread + node-runtime: resolution in `_LiveRefreshScanWorker.run()` (no widget access); `resolve_live_title` threads `build_js_runtimes(node_runtime)` (NOT inline), no `extract_flat` | VERIFIED | Worker `run()` at lines 86-106 emits Signals only, no widget access; `resolve_live_title` opts at lines 351-368 use `yt_dlp_opts.build_js_runtimes(node_runtime)` (not an inline dict) and do not contain `extract_flat`; worker passes `node_runtime=self._node_runtime` at line 102 |
| 3  | D-03 | Fallback: never a bare-blank title (`_fallback_title` → `"Untitled — watch?v=<id>"`) | VERIFIED | `yt_import.py:84-99`: `_fallback_title` returns `f"Untitled — watch?v={video_ids[0]}"` when `v=` present, else `f"Untitled — {url}"`; em dash U+2014 confirmed; `resolve_live_title` returns `_fallback_title(url)` on exception (line 378) and on blank resolved title (line 383) |
| 4  | D-04 | Dedicated "Newly discovered" section; read-only title rendered `Qt.PlainText` (T-39-01 / CR-01) | VERIFIED | Section header at `live_refresh_dialog.py:806-810`; `_DiscoverRowWidget.__init__` at lines 511-514: `title_label.setTextFormat(Qt.PlainText)` — mandatory guard confirmed; 4 total `setTextFormat(Qt.PlainText)` calls in the file (2 in `_RowWidget`, 1 new in `_DiscoverRowWidget`) |
| 5  | D-05 | No dedup hiding — every live stream listed | VERIFIED | `_populate_rows` lines 812-820: `for entry in scan_results: row = _DiscoverRowWidget(...)` with no dedup filter; comment at line 802-804 explicitly states "NO dedup — every scan_result gets a row"; covered by `test_discover_section_no_dedup` |
| 6  | D-06 | Each row offers Add-as-new OR Map/merge | VERIFIED | `_DiscoverRowWidget` lines 536-543: two `QRadioButton` in a `QButtonGroup` ("Add as new station" / "Map onto existing station"); `build_staged_change` lines 596-627 emits `action="add"` or `action="remap"` per radio state |
| 7  | D-07 | Map target from flagged-station dropdown; duplicate-target guard fail-closed by URL dedup AND station_id dedup (WR-01 fix) | VERIFIED | Map combo populated at lines 549-553 from `flagged_stations`; WR-01 station_id guard at `apply_refresh` lines 241-255: deduplicates `target_station_ids` across all remap rows — same station double-mapped raises `ValueError` before any mutation; URL guard already present at lines 220-232 |
| 8  | D-08 | Newly-discovered rows unchecked by default | VERIFIED | `_DiscoverRowWidget.__init__` line 503: `self._check.setChecked(default_check_state("discover"))` where `default_check_state("discover")` returns `False` (only `"remap"` returns True, per lines 177-183) |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `musicstreamer/yt_import.py` | `_is_blank_title`, `_fallback_title`, `resolve_live_title` | VERIFIED | All three present at lines 74, 84, 322; module docstring updated to list `resolve_live_title` |
| `musicstreamer/ui_qt/live_refresh_dialog.py` | `_DiscoverRowWidget`, extended `_LiveRefreshScanWorker.run`, extended `_populate_rows` | VERIFIED | `_DiscoverRowWidget` at line 464; worker blank-resolution loop at lines 98-103; "Newly discovered" section in `_populate_rows` at lines 801-820 |
| `tests/test_yt_import_library.py` | 6 test functions covering D-01, D-02, D-03 | VERIFIED | All 6 functions present and passing |
| `tests/test_live_refresh_dialog.py` | 7 test functions covering D-02, D-04, D-05, D-06, D-07, D-08 | VERIFIED | All 7 functions present and passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `live_refresh_dialog.py:_LiveRefreshScanWorker.run` | `yt_import.resolve_live_title` | blank-title loop threading `self._node_runtime` | VERIFIED | `live_refresh_dialog.py:100-103`: `yt_import.resolve_live_title(entry["url"], node_runtime=self._node_runtime)` |
| `live_refresh_dialog.py:_DiscoverRowWidget` | `apply_refresh` remap path | `build_staged_change` returns `action=remap` with `station_id+stream_id` | VERIFIED | `_DiscoverRowWidget.build_staged_change` lines 614-619 emits `{"action": "remap", "station_id": chosen.id, "stream_id": primary_stream_id, "scan_result": self._entry}` |
| `live_refresh_dialog.py:_populate_rows` | `self._flagged_stations` | `_DiscoverRowWidget` map-dropdown source | VERIFIED | `_DiscoverRowWidget(entry, self._flagged_stations, ...)` at line 814 |
| `yt_import.py:resolve_live_title` | `yt_dlp_opts.build_js_runtimes` | `js_runtimes` opts key (D-02 landmine) | VERIFIED | `yt_import.py:364`: `"js_runtimes": yt_dlp_opts.build_js_runtimes(node_runtime)` |
| `yt_import.py:resolve_live_title` | `cookie_utils.temp_cookies_copy` | cookie-safety context manager | VERIFIED | `yt_import.py:370`: `with cookie_utils.temp_cookies_copy() as cookiefile:` |

### Code Review Findings Resolution

| Finding | Severity | Status | Evidence |
|---------|----------|--------|---------|
| WR-01: Same-station double-targeting (station_id dedup) | Warning | FIXED | `apply_refresh` lines 241-255: `target_station_ids` dedup pass raises `ValueError` on duplicate station_ids before any mutation |
| WR-02: Dead imports `QFormLayout` / `QPushButton` | Warning | FIXED | `grep "QFormLayout\|QPushButton" live_refresh_dialog.py` returns empty — both removed from imports |
| IN-01: `_on_add_selection_changed` one-shot divergence | Info | Acceptable | `_DiscoverRowWidget` name field is intentionally one-shot; documented in class docstring |
| IN-02: `replace` action dead code | Info | Pre-existing | `_RowWidget` scaffolding from Phase 96; out of scope for 96.1 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `_is_blank_title("")` is True | test_is_blank_title_predicate | PASS | PASS |
| `resolve_live_title` threads node_runtime via `build_js_runtimes` | test_resolve_live_title_threads_node_runtime | PASS | PASS |
| `resolve_live_title` has no `extract_flat` in opts | test_resolve_live_title_no_extract_flat | PASS | PASS |
| D-03 fallback on exception | test_resolve_live_title_fallback_on_error | PASS | PASS |
| Discover title label uses `Qt.PlainText` | test_discover_row_title_label_plaintext | PASS | PASS |
| Discover rows unchecked by default | test_discover_row_unchecked_by_default | PASS | PASS |
| Map staged change emits remap with station_id + stream_id | test_discover_row_map_staged_change | PASS | PASS |
| Full scoped suite (49 tests) | `.venv/bin/python -m pytest tests/test_live_refresh_dialog.py tests/test_yt_import_library.py -q` | 49 passed, 0 failed | PASS |

### Known Pre-Existing Failure (Not a Regression)

`tests/test_yt_dlp_opts_drift.py::test_yt_import_uses_build_js_runtimes` fails with count=4 (asserts exactly 1). This is a stale Phase-79 test that predates `fetch_channel_avatar` (count went to 2) and `resolve_live_title` (count went to 4). Not introduced by Phase 96.1; pre-existing before this phase.

### Anti-Patterns Found

None. No `TBD`, `FIXME`, `XXX`, placeholder strings, or stub implementations found in the modified files.

### Human Verification Required

None. All observable truths are programmatically verifiable against the codebase and test suite.

---

_Verified: 2026-06-22T03:49:39Z_
_Verifier: Claude (gsd-verifier)_
