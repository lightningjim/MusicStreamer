---
phase: 41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/media_keys/__init__.py
  - musicstreamer/media_keys/base.py
  - musicstreamer/media_keys/smtc.py
  - tests/test_media_keys_scaffold.py
autonomous: true
requirements: [MEDIA-01]
must_haves:
  truths:
    - "A `musicstreamer.media_keys` package exists and is importable on Linux without pulling in any Windows-only dependencies."
    - "`media_keys.create(player, repo)` returns a `MediaKeysBackend` subclass whose identity is selected by `sys.platform`."
    - "The abstract `MediaKeysBackend` defines the exact signal + method surface declared in D-02."
    - "On Linux, when D-Bus is unreachable, `create()` still returns a usable no-op backend and the app does not crash at startup."
    - "The Windows stub raises `NotImplementedError` with a TODO-43.1 comment, and is never imported on Linux."
  artifacts:
    - path: "musicstreamer/media_keys/__init__.py"
      provides: "create() factory + package export"
      exports: ["create", "MediaKeysBackend"]
    - path: "musicstreamer/media_keys/base.py"
      provides: "MediaKeysBackend abstract class + NoOpMediaKeysBackend fallback"
      exports: ["MediaKeysBackend", "NoOpMediaKeysBackend"]
    - path: "musicstreamer/media_keys/smtc.py"
      provides: "Windows stub (raises NotImplementedError pending Phase 43.1)"
    - path: "tests/test_media_keys_scaffold.py"
      provides: "pytest coverage of factory selection + abstract API surface + no-op fallback"
  key_links:
    - from: "musicstreamer/media_keys/__init__.py"
      to: "musicstreamer/media_keys/base.py"
      via: "import"
      pattern: "from musicstreamer.media_keys.base import"
    - from: "musicstreamer/media_keys/__init__.py"
      to: "musicstreamer/media_keys/mpris2.py"
      via: "lazy import under sys.platform guard"
      pattern: "if sys.platform.*linux.*import.*mpris2"
---

<objective>
Scaffold the `musicstreamer/media_keys/` package: the abstract `MediaKeysBackend` class (D-02 exact surface), a `NoOpMediaKeysBackend` for D-06 failure modes, the `create(player, repo)` platform factory (D-01), and the Windows `smtc.py` stub that raises `NotImplementedError` pending Phase 43.1.

Purpose: Establish the package contract that Plan 02 (Linux MPRIS2 backend) and Phase 43.1 (Windows SMTC backend) both implement against. This plan contains NO D-Bus code — that is Plan 02's responsibility. Keeping scaffolding separate means Plan 02 cannot drift from the abstract API, and the factory's platform dispatch + D-Bus fallback are independently testable.

Output: A Linux-importable `media_keys` package whose factory correctly selects Linux/Windows/no-op, an abstract base class with the D-02 signal+method surface, and pytest coverage that does not require a live D-Bus session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-platform-media-keys/41-CONTEXT.md
@musicstreamer/player.py
@musicstreamer/models.py

<interfaces>
<!-- Key types and contracts. Executor should use these directly — no codebase exploration needed. -->

From musicstreamer/models.py (the Station model publish_metadata consumes):
```python
@dataclass
class Station:
    id: int
    name: str
    provider_id: Optional[int]
    provider_name: Optional[str]
    tags: str
    station_art_path: Optional[str]          # relative asset path
    album_fallback_path: Optional[str]
    icy_disabled: bool = False
    streams: List[StationStream] = field(default_factory=list)
    last_played_at: Optional[str] = None
    is_favorite: bool = False
```

From musicstreamer/player.py (signals the backend will later connect to; wired in Plan 03):
```python
class Player(QObject):
    title_changed              = Signal(str)
    failover                   = Signal(object)
    playback_error             = Signal(str)
    # Public methods: play(station), pause(), stop(), set_volume(float)
    # NOTE: Player has NO "play_pause" helper — Plan 03 will add a bridge slot in MainWindow.
```

D-02 exact abstract API (from 41-CONTEXT.md — DO NOT paraphrase; match verbatim):
```python
from typing import Literal
from PySide6.QtCore import QObject, Signal
from PySide6.QtGui import QPixmap
from musicstreamer.models import Station

class MediaKeysBackend(QObject):
    play_pause_requested = Signal()
    stop_requested       = Signal()
    next_requested       = Signal()   # wired but may never emit
    previous_requested   = Signal()   # wired but may never emit

    def publish_metadata(self, station: Station | None, title: str,
                         cover_pixmap: QPixmap | None) -> None: ...
    def set_playback_state(self, state: Literal["playing", "paused", "stopped"]) -> None: ...
    def shutdown(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create abstract MediaKeysBackend + NoOpMediaKeysBackend in base.py</name>
  <files>musicstreamer/media_keys/__init__.py, musicstreamer/media_keys/base.py, tests/test_media_keys_scaffold.py</files>
  <behavior>
    - `from musicstreamer.media_keys import MediaKeysBackend, NoOpMediaKeysBackend, create` succeeds on Linux without importing mpris2 or smtc.
    - `MediaKeysBackend(QObject)` exposes exactly four signals: `play_pause_requested`, `stop_requested`, `next_requested`, `previous_requested` (all `Signal()` — no payload).
    - `MediaKeysBackend.publish_metadata`, `set_playback_state`, and `shutdown` are declared as abstract (raise `NotImplementedError` in the base; signatures match D-02 verbatim).
    - `NoOpMediaKeysBackend(MediaKeysBackend)` implements all three abstract methods as no-ops (return None) and can be instantiated with no args.
    - `set_playback_state` accepts Literal["playing", "paused", "stopped"] values only — any other string raises `ValueError` so downstream wiring bugs surface early (enforce via the base class's non-abstract input-validation wrapper that delegates to a protected `_apply_playback_state` abstract method). Keep this simple: the Literal validation lives in the base.
    - Tests (in `tests/test_media_keys_scaffold.py`) use pytest-qt fixtures (`qtbot`) and cover:
      - Test 1: `NoOpMediaKeysBackend()` instantiates, emitting no signals, and all three methods return None.
      - Test 2: `backend.play_pause_requested.emit()` is observable via `qtbot.waitSignal` when connected.
      - Test 3: Calling `NoOpMediaKeysBackend().set_playback_state("invalid")` raises `ValueError`.
      - Test 4: Accessing `MediaKeysBackend` (the true abstract) does NOT itself raise on import (it's a QObject subclass; Python abstract is duck-typed via NotImplementedError at method call time — if you use `abc.ABCMeta`, mixing metaclasses with `type(QObject)` will blow up, so DO NOT use `abc` — use NotImplementedError in method bodies instead; this is a PySide6 pitfall).
    - `musicstreamer/media_keys/__init__.py` re-exports `MediaKeysBackend`, `NoOpMediaKeysBackend`, and `create` (stubbed in this task — real implementation in Task 2). For now the `create` symbol can be a placeholder that returns `NoOpMediaKeysBackend()` to keep the import graph clean; Task 2 replaces it with the real platform dispatch.
  </behavior>
  <action>
    Create `musicstreamer/media_keys/__init__.py` with the re-exports above and a placeholder `create` that returns `NoOpMediaKeysBackend()`. DO NOT import `mpris2` or `smtc` from `__init__.py` eagerly — those are lazy inside `create()` (added properly in Task 2).

    Create `musicstreamer/media_keys/base.py` with `MediaKeysBackend(QObject)` and `NoOpMediaKeysBackend(MediaKeysBackend)`. Match the D-02 signal + method surface verbatim from the interfaces block. Use `NotImplementedError` for abstract methods — NOT `abc.ABCMeta` (metaclass conflict with `type(QObject)` — PySide6 docs are explicit on this; see https://doc.qt.io/qtforpython-6/PySide6/QtCore/QObject.html#multiple-inheritance). Include typed imports: `from typing import Literal`, `from PySide6.QtCore import QObject, Signal`, `from PySide6.QtGui import QPixmap`, `from musicstreamer.models import Station`.

    Per D-06, add module-level `_log = logging.getLogger(__name__)` for use by downstream plans.

    Create `tests/test_media_keys_scaffold.py` using pytest-qt `qtbot` fixture with the four tests listed in behavior. Use the existing test conventions (see `tests/test_*.py` — same offscreen Qt pattern already established in Phase 36).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_media_keys_scaffold.py -x -v</automated>
  </verify>
  <done>All 4 tests pass. `python -c "from musicstreamer.media_keys import MediaKeysBackend, NoOpMediaKeysBackend, create; b=create(None,None); b.set_playback_state('stopped')"` prints nothing and exits 0. `grep -n "import abc\|ABCMeta" musicstreamer/media_keys/base.py` returns no matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement create() platform factory + Windows smtc.py stub</name>
  <files>musicstreamer/media_keys/__init__.py, musicstreamer/media_keys/smtc.py, tests/test_media_keys_scaffold.py</files>
  <behavior>
    - `create(player, repo)` dispatches:
      - `sys.platform.startswith("linux")` → lazy-import `mpris2`, try `from .mpris2 import LinuxMprisBackend; backend = LinuxMprisBackend(player, repo)`. If the import fails with `ImportError` (Plan 02 not landed yet — this is the expected transient state between Plan 01 ship and Plan 02 ship) OR if construction raises any `Exception`, log a warning and return `NoOpMediaKeysBackend()` (D-06).
      - `sys.platform == "win32"` → lazy-import `smtc` and call its factory which raises `NotImplementedError("MEDIA-03: Windows SMTC backend deferred to Phase 43.1")`. Catch that specific NotImplementedError, log a warning, and return `NoOpMediaKeysBackend()` so the app doesn't crash on Windows during Phase 41-42 testing.
      - Any other platform → log a warning, return `NoOpMediaKeysBackend()`.
    - `smtc.py` contains a `create_windows_backend(player, repo)` function that unconditionally raises `NotImplementedError("MEDIA-03: Windows SMTC backend deferred to Phase 43.1 — see .planning/ROADMAP.md Phase 43.1")`. Add a module-level `# TODO-43.1:` comment block at the top explaining the winrt pattern that will land later.
    - `smtc.py` MUST NOT import `winrt` at module scope — keep it importable on Linux for test purposes (tests on Linux will call `create_windows_backend` and assert the NotImplementedError).
    - Tests extend `tests/test_media_keys_scaffold.py`:
      - Test 5: On Linux (detected via `sys.platform`), `create(None, None)` returns either a `LinuxMprisBackend` (if Plan 02 has shipped — won't be true in this plan) OR a `NoOpMediaKeysBackend` (the fallback). Assert `isinstance(b, MediaKeysBackend)`.
      - Test 6: Monkeypatch `sys.platform` to `"win32"` and assert `create(None, None)` returns `NoOpMediaKeysBackend` (because smtc raises NotImplementedError which is caught).
      - Test 7: Monkeypatch `sys.platform` to `"freebsd"` and assert `create(None, None)` returns `NoOpMediaKeysBackend`.
      - Test 8: `from musicstreamer.media_keys.smtc import create_windows_backend` then calling it raises `NotImplementedError` with "43.1" in the message.
      - Test 9: Verify a Linux-side import does NOT pull in `winrt` — `sys.modules` has no `winrt` key after `import musicstreamer.media_keys`.
    - Monkeypatching `sys.platform` is done via `monkeypatch.setattr(sys, "platform", "win32")`.
  </behavior>
  <action>
    Replace the placeholder `create` in `musicstreamer/media_keys/__init__.py` with the real dispatcher. Use a top-of-module `_log = logging.getLogger(__name__)`. Wrap both backend-construction paths in try/except so any failure (ImportError from missing `mpris2`, or runtime D-Bus failure inside Plan 02's `LinuxMprisBackend.__init__`) produces `_log.warning("Media keys disabled: %s", e)` and falls back to `NoOpMediaKeysBackend()`. This is the D-06 failure mode.

    Create `musicstreamer/media_keys/smtc.py` with the stub `create_windows_backend(player, repo)` and the TODO-43.1 header comment.

    Extend `tests/test_media_keys_scaffold.py` with tests 5-9. For the `winrt` absence check (Test 9), use `assert all(not m.startswith("winrt") for m in sys.modules)`.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_media_keys_scaffold.py -x -v</automated>
  </verify>
  <done>All 9 tests pass. `grep -rn "import winrt\|from winrt" musicstreamer/media_keys/` returns no matches. `grep -n "43.1" musicstreamer/media_keys/smtc.py` returns the stub comment + NotImplementedError message. On Linux, running `python -c "import musicstreamer.media_keys as m; print(type(m.create(None,None)).__name__)"` prints `NoOpMediaKeysBackend` (Plan 02 not landed yet).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory → caller | Factory must never raise — all failure modes degrade to NoOp (app startup must never block on media keys per D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Denial-of-Service | `create()` factory | mitigate | All backend construction wrapped in try/except; any exception logs warning + returns NoOp. Tested via Tests 5-7. |
| T-41-02 | Tampering | `smtc.py` on Linux | mitigate | `smtc.py` module-scope contains no `winrt` imports; Test 9 asserts `winrt` never appears in `sys.modules` after Linux import. |
| T-41-03 | Information Disclosure | Log messages in `create()` fallback | accept | Warning log contains only exception message (no station data, no user identifiers). Low value target. |
</threat_model>

<verification>
- Package importable on Linux: `python -c "from musicstreamer.media_keys import create, MediaKeysBackend, NoOpMediaKeysBackend"` exits 0.
- No eager winrt import: `python -c "import musicstreamer.media_keys, sys; assert not any(m.startswith('winrt') for m in sys.modules)"` exits 0.
- Abstract API surface matches D-02: grep the base class for all four signal names and all three method names.
- Tests: `uv run --with pytest --with pytest-qt pytest tests/test_media_keys_scaffold.py -v` — 9/9 pass.
- Full test suite still passes: `uv run --with pytest --with pytest-qt pytest -x` — no regressions (expect 399+ passing, 1 pre-existing failure per STATE.md).
</verification>

<success_criteria>
1. `musicstreamer/media_keys/` package with `__init__.py`, `base.py`, `smtc.py` committed.
2. `MediaKeysBackend` abstract class exposes the exact D-02 signal + method surface (verbatim).
3. `NoOpMediaKeysBackend` usable as the D-06 fallback and safe to call all methods on.
4. `create(player, repo)` dispatches on `sys.platform` and degrades to NoOp on any failure.
5. Windows path does not import `winrt` on Linux (lazy, module-guarded).
6. `tests/test_media_keys_scaffold.py` covers 9 cases and all pass.
7. `grep -rn "dbus-python\|import dbus$" musicstreamer/media_keys/` returns zero matches (D-07).
</success_criteria>

<output>
After completion, create `.planning/phases/41-platform-media-keys/41-01-SUMMARY.md`.
</output>
