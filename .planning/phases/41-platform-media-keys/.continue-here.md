---
context: phase
phase: 41-platform-media-keys
task: 0
total_tasks: 6
status: paused
last_updated: 2026-04-15T01:48:07.282Z
---

<current_state>
Mid `/gsd-discuss-phase 41 --chain`. Discussion complete, CONTEXT.md written and committed (f15dea3). Planner produced 3 sequential plans (41-01, 41-02, 41-03) — all PLAN.md files exist in the phase directory. ROADMAP.md updated with the 3 plans for Phase 41 (`0/3` in progress table). User paused immediately before the wave-1 executor dispatch for plan 41-01.

No code changes made this session for Phase 41 yet. All prior phase work (40.1, 45, 46, 47, Phase 41 split, Phase 43.1 insertion) is committed and clean.
</current_state>

<completed_work>

Committed during this session:
- 07c2b82 fix(40.1-04): Fetch button + AA logo fetch wiring (inline follow-up to 40.1-04)
- 55963ac merge: plan 40.1-05 icy-disable-toggle
- 808f87d merge: plan 40.1-06 playing-timer
- d685942 fix(player): NULL-state barrier (partial — 2 sites)
- 3cb640b test(40.1): complete UAT — 11/11 passed
- f797cb7 docs(40.1): code review report
- bdca8ae fix(40.1): WR-01/02/03 monotonic fetch-token pattern
- afc6343 fix(40.1): WR-04 cover-art token guard
- 5ebe9bc docs(40.1): code review fix report
- 170d196 docs(40.1,45): security threat verification
- 41a6f20 test(45): Nyquist integration tests
- 7dfdd42 docs(40.1,45): Nyquist validation strategy
- bcb938c docs(40.1,45): UI audit reviews
- 19328d1 fix(player): extend NULL-state barrier to all 6 teardown sites (followup to d685942 after user reproduced crash)
- 83f4be2 docs(45): phase 45 CONTEXT.md
- 543df75 merge: plan 45-01 (station-icon loader dedup)
- 743579e docs(46): pre-planning scope notes (incl. QThread teardown)
- 408b2bc docs(47): phase 47 (stats-for-nerds + AutoEQ); SEED-005 + SEED-007 harvested
- e3bb5c4 docs(roadmap): Phase 41 narrowed to Linux; Phase 43.1 inserted; Phase 44 amended
- f15dea3 docs(41): phase 41 CONTEXT.md (Linux MPRIS2 narrowed scope)
- (planner commits for 41-01/02/03 PLAN.md — included in the planner agent's commits)

Phases 40.1 and 45 passed all gates: UAT, code review + fixes, security audit, Nyquist validation, UI audit.
</completed_work>

<remaining_work>

Phase 41 (Linux Media Keys — MPRIS2), 3 plans in 3 sequential waves:

**Wave 1 — 41-01 Scaffolding** (no deps):
- Task 1: `musicstreamer/media_keys/` package (base.py + __init__.py factory + smtc.py Windows stub + NoOpMediaKeysBackend)
- Task 2: `tests/test_media_keys_scaffold.py` unit tests

**Wave 2 — 41-02 LinuxMprisBackend** (depends on 41-01):
- Task 1: QtDBus Root + Player adaptors, service registration
- Task 2: Cover-art PNG cache in `paths.user_cache_dir() / "mpris-art" / "{station_id}.png"` + mpris2 tests

**Wave 3 — 41-03 Wiring + UAT** (depends on 41-01, 41-02):
- Task 1: MainWindow wiring — signal bridges + set_playback_state + closeEvent shutdown
- Task 2: **Human UAT checkpoint** — playerctl status/metadata + keyboard media-key verification
</remaining_work>

<decisions_made>

See `41-CONTEXT.md` for the 7 locked decisions (D-01 through D-07). Key ones:
- Phase 41 narrowed to Linux-only (Windows SMTC → Phase 43.1)
- Factory `media_keys.create(player, repo) -> MediaKeysBackend` abstract + subclass pattern
- Next/Previous: MPRIS2 `CanGoNext = CanGoPrevious = false` (no queue concept)
- Cover art: serialize QPixmap → stable per-station PNG in user cache dir, publish `file://` URI
- Failure mode: D-Bus unavailable → log warning + return NoOp backend; startup never blocks
- No new dependencies (QtDBus ships with PySide6)
</decisions_made>

<blockers>

None. All prerequisites ready. Plans committed, CONTEXT.md committed, roadmap structure updated.
</blockers>

## Required Reading (in order)

1. `.planning/phases/41-platform-media-keys/41-CONTEXT.md` — locked decisions (D-01 through D-07)
2. `.planning/phases/41-platform-media-keys/41-01-PLAN.md` — wave 1 plan (scaffolding)
3. `.planning/phases/41-platform-media-keys/41-02-PLAN.md` — wave 2 plan (Linux backend)
4. `.planning/phases/41-platform-media-keys/41-03-PLAN.md` — wave 3 plan (wiring + UAT)
5. `.planning/HANDOFF.json` — structured state for this pause

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Partial teardown barrier | d685942 fixed NULL-state race only in `_try_next_stream` + `_set_uri`; `_play_youtube`, `_play_twitch`, `pause`, `stop` still crashed on YT auto-start. Every `set_state(NULL)` must be followed by `get_state(Gst.CLOCK_TIME_NONE)`. | advisory | grep `set_state(Gst.State.NULL)` before declaring teardown fixes complete; every match must be followed within 2 lines by `get_state(Gst.CLOCK_TIME_NONE)`. Fixed in 19328d1. |
| Worktree executor races on .git/config.lock | Dispatching multiple executor agents in a single message causes concurrent `git worktree add` to race on `.git/config.lock`, corrupting the lock file and producing cwd-loss faults. | advisory | Dispatch worktree executors sequentially (one Agent call per message), not in parallel. Same lesson from 40.1 HANDOFF. |

## Infrastructure State

- **Git**: `main` at HEAD including all commits above + planner's PLAN.md commits. Working tree clean except pre-existing tracked/untracked files from before session (desktop file, seed files, .clone/, old phase dirs, etc. — NOT caused by this session).
- **Python venv**: `.venv/bin/python` — has `yt_dlp`, PySide6, pytest-qt. System Python does NOT have yt_dlp (use venv for any import_dialog tests).
- **User environment**: Linux on Wayland. Windows VM available for Phase 43 / 43.1 validation later.

<context>
User invoked `/gsd-pause-work` right after the planner finished Phase 41 plans. Likely wrapping for the night or switching context. Resume = dispatch 41-01 executor in a worktree; the chain auto-advances through 41-02 and 41-03 (stopping at the human UAT checkpoint in 41-03 Task 2). The --chain intent is still active — the resume should honor it.
</context>

<next_action>
Start with: Dispatch `gsd-executor` for plan 41-01 in a worktree, based on current main. Plan path: `.planning/phases/41-platform-media-keys/41-01-PLAN.md`. After merge, continue to 41-02, then 41-03 (stop at UAT checkpoint for human verification of playerctl + keyboard media keys).
</next_action>
