---
phase: 77-test-infrastructure-stabilization-fix-pre-existing-test-doub
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - tests/test_main_window_integration.py
  - tests/test_main_window_gbs.py
  - tests/test_main_window_soma.py
  - tests/test_now_playing_panel.py
  - tests/test_phase72_now_playing_panel.py
  - tests/test_ui_qt_scaffold.py
  - tests/test_main_window_media_keys.py
  - tests/test_discovery_dialog.py
  - tests/test_stream_picker.py
  - tests/test_phase72_1_stream_picker_reflow.py
  - tests/ui_qt/test_main_window_node_indicator.py
  - tests/test_main_window_underrun.py
  - tests/test_phase72_integration.py
  - tests/test_phase72_compact_toggle.py
  - tests/test_phase72_peek_overlay.py
  - tests/test_phase72_assumptions.py
autonomous: true
requirements: [INFRA-01]
tags: [test-infrastructure, fakeplayer, migration]

must_haves:
  truths:
    - "All 11 inline FakePlayer(QObject) class definitions in tests/ are deleted"
    - "Each former FakePlayer site imports the shared base via `from tests._fake_player import FakePlayer`"
    - "5 transitive imports `from tests.test_main_window_integration import FakePlayer` are redirected to `from tests._fake_player import FakePlayer`"
    - "tests/test_main_window_gbs.py and tests/test_main_window_soma.py no longer declare the wrong `audio_caps_detected = Signal(object)` arity — the shared module's `Signal(int, int, int)` arity auto-fixes this drift"
    - "tests/test_equalizer_dialog.py:40 non-QObject FakePlayer remains inline (CONTEXT D-09 Option A) with a one-line comment documenting intentional non-migration"
    - "tests/test_fake_player_no_inline.py drift-guard from Plan 77-01 turns GREEN (zero inline FakePlayer(QObject) offenders)"
    - "All previously-green tests in the affected 16 files (11 migration sites + 5 transitive-import consumers) still pass after migration"
  artifacts:
    - path: "tests/test_main_window_integration.py"
      provides: "MainWindow integration tests — inline FakePlayer deleted; imports shared base"
      contains: "from tests._fake_player import FakePlayer"
    - path: "tests/test_main_window_gbs.py"
      provides: "GBS MainWindow tests — arity drift auto-fixed by shared import"
      contains: "from tests._fake_player import FakePlayer"
    - path: "tests/test_main_window_soma.py"
      provides: "SomaFM MainWindow tests — arity drift auto-fixed by shared import"
      contains: "from tests._fake_player import FakePlayer"
    - path: "tests/test_equalizer_dialog.py"
      provides: "Equalizer dialog tests — non-QObject FakePlayer stays inline with documenting comment"
      contains: "Phase 77 D-09: intentionally non-QObject"
  key_links:
    - from: "11 migration sites + 5 transitive consumers"
      to: "tests/_fake_player.py"
      via: "from tests._fake_player import FakePlayer"
      pattern: "from tests._fake_player import"
---

<objective>
Migrate all 11 inline `FakePlayer(QObject)` class declarations in `tests/` to import the shared base from `tests/_fake_player.py` (created in Plan 77-01). Plus redirect 5 transitive `from tests.test_main_window_integration import FakePlayer` imports to the shared module. After this plan lands, the D-17 drift-guard `tests/test_fake_player_no_inline.py` (Plan 77-01 Task 3) turns GREEN, and the existing `audio_caps_detected` arity drift at the gbs/soma sites is auto-fixed because the shared module declares the production-correct `Signal(int, int, int)` arity.

Purpose: this plan closes the "long tail" of FakePlayer-drift recurrence. Every site that constructs `MainWindow(player=FakePlayer(), repo=FakeRepo(), ...)` now consumes the same source of truth, and the drift-guards from Plan 77-01 fire on any future regression.

Output: 16 modified test files with deleted inline class definitions + redirected imports. Zero production-code changes. No new files (the new file is Plan 77-01's deliverable, already landed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/TESTING.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-CONTEXT.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-RESEARCH.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-PATTERNS.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-01-SUMMARY.md
@tests/_fake_player.py

<interfaces>
<!-- The 11 inline FakePlayer(QObject) sites that this plan migrates (from grep at 2026-05-17). The line numbers
     are starting-line-of-class for each; the full class body extends ~30-50 lines below the start. -->

Migration sites (delete inline class + add `from tests._fake_player import FakePlayer`):

| File | Class start line | Notes |
|------|------------------|-------|
| tests/test_main_window_integration.py | 31 | SEED — most complete inline FakePlayer; body became the basis for tests/_fake_player.py in Plan 77-01 |
| tests/test_main_window_gbs.py | 25 | `_FakePlayer` (underscore-prefix); has WRONG `audio_caps_detected = Signal(object)` arity at line 34 — migration AUTO-FIXES to Signal(int, int, int) |
| tests/test_main_window_soma.py | 33 | `_FakePlayer` (underscore-prefix); same wrong arity at line 42 — migration AUTO-FIXES |
| tests/test_now_playing_panel.py | 29 | Partial — only 6 signals; gains 12 more from shared base (safe: connect-to-never-emitted Signal is a no-op in Qt) |
| tests/test_phase72_now_playing_panel.py | 34 | Partial — same 6-signal shape as test_now_playing_panel.py |
| tests/test_ui_qt_scaffold.py | 16 | `_FakePlayer` (underscore-prefix) |
| tests/test_main_window_media_keys.py | 54 | Partial |
| tests/test_discovery_dialog.py | 39 | Used only for `discovery_dialog`, not full MainWindow |
| tests/test_stream_picker.py | 32 | Partial |
| tests/test_phase72_1_stream_picker_reflow.py | 79 | Partial |
| tests/ui_qt/test_main_window_node_indicator.py | 35 | `_FakePlayer` (underscore-prefix); subdirectory site |

Transitive import sites (redirect `from tests.test_main_window_integration import FakePlayer` → `from tests._fake_player import FakePlayer`):

| File | Line | Current import |
|------|------|----------------|
| tests/test_main_window_underrun.py | 18 | `from tests.test_main_window_integration import FakePlayer, FakeRepo` — split: FakePlayer→shared, FakeRepo stays |
| tests/test_phase72_1_stream_picker_reflow.py | 422 | `from tests.test_main_window_integration import ( ... )` — split required |
| tests/test_phase72_integration.py | 50 | `from tests.test_main_window_integration import FakePlayer, FakeRepo, _make_station` — split: FakePlayer→shared, others stay |
| tests/test_phase72_compact_toggle.py | 41 | same shape |
| tests/test_phase72_peek_overlay.py | 32 | same shape |
| tests/test_phase72_assumptions.py | 52 | same shape |

Non-migration site (stays inline by design):
- tests/test_equalizer_dialog.py:40 — `class FakePlayer:` WITHOUT (QObject), no Signals, only `.calls` log + 3 EQ method stubs. D-09 Option A: leave inline. Add a one-line comment at line 40 documenting intentional non-migration: `# Phase 77 D-09 (Option A): intentionally non-QObject; EQ-only double for EqualizerDialog tests. The D-17 drift-guard regex `class\s+_?FakePlayer\s*\(QObject\)` excludes this declaration by design.`

FakeRepo migration is OUT OF SCOPE per PATTERNS.md L264 — each migration site also declares an inline `FakeRepo` (10 confirmed) but no recurring-drift evidence exists, and `tests/conftest.py:132` already has a different `_FakeRepo` shape. Phase 77 D-09 only canonicalizes FakePlayer.

From tests/test_main_window_integration.py current shape after migration:

```
# OLD (lines 27-84) — DELETED in this plan:
# class FakePlayer(QObject):
#     title_changed = Signal(str)
#     ...
#     def shutdown_underrun_tracker(self) -> None: pass

# NEW (replacement near top of file, after stdlib imports):
from tests._fake_player import FakePlayer
```

The `class FakeRepo:` at line ~87 in test_main_window_integration.py is preserved (it's still consumed by 5 transitive importers; not in scope for this plan).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the SEED file (tests/test_main_window_integration.py) + redirect its 5 transitive consumers</name>
  <files>
    tests/test_main_window_integration.py,
    tests/test_main_window_underrun.py,
    tests/test_phase72_1_stream_picker_reflow.py,
    tests/test_phase72_integration.py,
    tests/test_phase72_compact_toggle.py,
    tests/test_phase72_peek_overlay.py,
    tests/test_phase72_assumptions.py
  </files>
  <read_first>
    - tests/_fake_player.py (Plan 77-01 deliverable — confirm 18 signals present, class name is `FakePlayer` not `_FakePlayer`)
    - tests/test_main_window_integration.py:1-90 (full FakePlayer inline body + the FakeRepo declaration that immediately follows and stays)
    - tests/test_main_window_underrun.py:1-30 (transitive import pattern: `from tests.test_main_window_integration import FakePlayer, FakeRepo` — split needed)
    - tests/test_phase72_1_stream_picker_reflow.py:419-440 (multi-name transitive import; split needed)
    - tests/test_phase72_integration.py:48-55 (transitive import shape)
    - tests/test_phase72_compact_toggle.py:39-45 (transitive import shape)
    - tests/test_phase72_peek_overlay.py:30-35 (transitive import shape)
    - tests/test_phase72_assumptions.py:50-58 (transitive import shape)
    - 77-PATTERNS.md L247-265 (per-site migration notes; SEED file is the lowest-risk migration since the shared module derives from its body)
  </read_first>
  <behavior>
    - Behavior 1: After migration, `tests/test_main_window_integration.py` no longer contains an inline `class FakePlayer(QObject):` declaration; the file still exports `FakeRepo` and `_make_station` for transitive consumers
    - Behavior 2: All 5 transitive consumers import `FakePlayer` from `tests._fake_player` and continue importing `FakeRepo` (and any other names) from `tests.test_main_window_integration`
    - Behavior 3: `uv run pytest tests/test_main_window_integration.py tests/test_main_window_underrun.py tests/test_phase72_integration.py tests/test_phase72_compact_toggle.py tests/test_phase72_peek_overlay.py tests/test_phase72_assumptions.py tests/test_phase72_1_stream_picker_reflow.py -x` exits 0 with no `AttributeError: ... has no attribute 'underrun_recovery_started'` and no other regression
  </behavior>
  <action>
    For `tests/test_main_window_integration.py`:
    1. Locate the `class FakePlayer(QObject):` block (starts at line 31, extends ~50 lines through the `shutdown_underrun_tracker` method around line 84).
    2. Delete the entire class body — class declaration through the last `def shutdown_underrun_tracker(self) -> None: pass` line.
    3. In the location where the class was deleted, place a single import line: `from tests._fake_player import FakePlayer`. Place it grouped with the other test-helper imports near the top of the file (after stdlib imports, before any project imports). If the file already imports other names from `tests.` modules, sort consistently with existing convention.
    4. Verify the existing `class FakeRepo:` declaration (starts around line 87 in the original) and `_make_station` helper remain unchanged — they are still consumed by 5 transitive importers and are out of scope for migration this phase.
    5. Confirm the file still parses (`python -c "import tests.test_main_window_integration"` exits 0).

    For each of the 5 transitive consumers, apply the same surgical edit:

    `tests/test_main_window_underrun.py` line 18:
    - OLD: `from tests.test_main_window_integration import FakePlayer, FakeRepo`
    - NEW (split into two lines):
      ```
      from tests._fake_player import FakePlayer
      from tests.test_main_window_integration import FakeRepo
      ```

    `tests/test_phase72_integration.py` line ~50:
    - OLD: `from tests.test_main_window_integration import FakePlayer, FakeRepo, _make_station`
    - NEW (split):
      ```
      from tests._fake_player import FakePlayer
      from tests.test_main_window_integration import FakeRepo, _make_station
      ```

    `tests/test_phase72_compact_toggle.py` line ~41: same shape as test_phase72_integration — split into two import lines.

    `tests/test_phase72_peek_overlay.py` line ~32: same shape — split.

    `tests/test_phase72_assumptions.py` line ~52: same shape — split.

    `tests/test_phase72_1_stream_picker_reflow.py` line ~422: function-local multi-name transitive import of the form `from tests.test_main_window_integration import (FakePlayer as MWFakePlayer, FakeRepo as MWFakeRepo, _make_station as mw_make_station,)`. Line 448 uses `MainWindow(MWFakePlayer(), repo)` — the `as MWFakePlayer` alias MUST be preserved. Split into TWO function-local import lines: `from tests._fake_player import FakePlayer as MWFakePlayer` AND `from tests.test_main_window_integration import (FakeRepo as MWFakeRepo, _make_station as mw_make_station,)`. Do NOT drop the alias or rename `MWFakePlayer` usage at line 448.

    Do NOT touch other lines in these files. Do NOT reorganize import blocks. Do NOT add or remove `__init__.py` imports. The split-import pattern is the cleanest way to redirect a single name from a multi-name transitive import without breaking the other names.

    After all 7 file edits land in a single commit, run the verification command to confirm zero regressions. If any test fails, the most likely cause is:
    - Shared `FakePlayer` is missing a signal a consumer connects to → re-grep `Player.__dict__` and check Plan 77-01 Task 1 deliverable (parity test should have caught this)
    - Shared `FakePlayer` arity differs from a consumer's `.emit(...)` call → check the arity parity test
    - A consumer imported something else from `test_main_window_integration` that the split broke → re-read the consumer's full import block to ensure all names are accounted for
  </action>
  <verify>
    <automated>uv run pytest tests/test_main_window_integration.py tests/test_main_window_underrun.py tests/test_phase72_integration.py tests/test_phase72_compact_toggle.py tests/test_phase72_peek_overlay.py tests/test_phase72_assumptions.py tests/test_phase72_1_stream_picker_reflow.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^class FakePlayer\(QObject\):" tests/test_main_window_integration.py` returns 0 (inline class deleted)
    - `grep -F "from tests._fake_player import FakePlayer" tests/test_main_window_integration.py` returns ≥ 1 line
    - `grep -cE "^class FakeRepo:" tests/test_main_window_integration.py` returns 1 (FakeRepo preserved)
    - All 5 transitive consumers contain `from tests._fake_player import FakePlayer` (verify each with `grep -F "from tests._fake_player import FakePlayer" {file}` returns ≥ 1 line)
    - None of the 5 transitive consumers contain `from tests.test_main_window_integration import FakePlayer` (verify each with `grep -F "from tests.test_main_window_integration import" {file} | grep -F "FakePlayer"` returns 0 — FakePlayer no longer imported from that path)
    - `tests/test_phase72_1_stream_picker_reflow.py` preserves the `MWFakePlayer` alias: `grep -F "from tests._fake_player import FakePlayer as MWFakePlayer" tests/test_phase72_1_stream_picker_reflow.py` returns ≥ 1 line AND `grep -F "MWFakePlayer()" tests/test_phase72_1_stream_picker_reflow.py` returns ≥ 1 line (line 448 usage intact)
    - Test command above exits 0
  </acceptance_criteria>
  <done>The SEED file's inline FakePlayer is deleted, all 5 transitive consumers redirect to the shared module, and the full test run across these 7 files passes. The SEED file still serves as the home for FakeRepo + _make_station (out-of-scope for FakePlayer migration).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the 10 remaining inline FakePlayer(QObject) sites + add the non-migration comment to test_equalizer_dialog.py</name>
  <files>
    tests/test_main_window_gbs.py,
    tests/test_main_window_soma.py,
    tests/test_now_playing_panel.py,
    tests/test_phase72_now_playing_panel.py,
    tests/test_ui_qt_scaffold.py,
    tests/test_main_window_media_keys.py,
    tests/test_discovery_dialog.py,
    tests/test_stream_picker.py,
    tests/test_phase72_1_stream_picker_reflow.py,
    tests/ui_qt/test_main_window_node_indicator.py,
    tests/test_equalizer_dialog.py
  </files>
  <read_first>
    - tests/_fake_player.py (Plan 77-01 deliverable)
    - tests/test_main_window_gbs.py:25-50 (DRIFT site — `_FakePlayer(QObject)` with WRONG `Signal(object)` arity at line 34; migration auto-fixes)
    - tests/test_main_window_soma.py:33-55 (DRIFT site — same wrong arity at line 42)
    - tests/test_now_playing_panel.py:29-60 (partial — 6 signals + 9 methods incl set_eq_*)
    - tests/test_phase72_now_playing_panel.py:34-65 (partial — 6 signals)
    - tests/test_ui_qt_scaffold.py:16-50 (`_FakePlayer`)
    - tests/test_main_window_media_keys.py:54-90 (partial)
    - tests/test_discovery_dialog.py:39-70 (used for discovery_dialog only, not MainWindow — over-signaling from shared base is harmless)
    - tests/test_stream_picker.py:32-65
    - tests/test_phase72_1_stream_picker_reflow.py:79-110 (inline class at the top of file; transitive consumer of test_main_window_integration at line 422 — Task 1 handled the latter)
    - tests/ui_qt/test_main_window_node_indicator.py:35-70 (`_FakePlayer` in subdirectory)
    - tests/test_equalizer_dialog.py:40-55 (non-QObject FakePlayer — STAYS inline with explanatory comment)
    - 77-PATTERNS.md L228-260 (per-site migration notes including the auto-fix semantic for gbs/soma)
    - 77-CONTEXT.md D-09 (Option A for test_equalizer_dialog — non-migration with rationale)
  </read_first>
  <behavior>
    - Behavior 1: All 10 remaining inline `FakePlayer(QObject)` (or `_FakePlayer(QObject)`) declarations are deleted; each file imports the shared base
    - Behavior 2: At sites where the local class was named `_FakePlayer` (underscore-prefix), any in-file references to the symbol are updated to use the imported name `FakePlayer` (or the import is aliased `from tests._fake_player import FakePlayer as _FakePlayer` to minimize the diff at the call site — planner choice; recommendation: alias for the 4 sites that use `_FakePlayer` to keep call-site diff minimal)
    - Behavior 3: `audio_caps_detected = Signal(object)` arity drift at gbs/soma is auto-fixed (the shared module declares `Signal(int, int, int)`)
    - Behavior 4: `tests/test_equalizer_dialog.py:40` keeps its non-QObject `class FakePlayer:` declaration but gains a one-line comment documenting CONTEXT D-09 Option A
    - Behavior 5: `tests/test_fake_player_no_inline.py` drift-guard from Plan 77-01 Task 3 turns GREEN — zero offenders, since all 11 inline QObject FakePlayer sites are now gone
    - Behavior 6: Full-suite run across all 11 modified files passes with no regressions
  </behavior>
  <action>
    Migration pattern (apply to each of the 10 sites):

    1. Locate the inline class definition: `class FakePlayer(QObject):` OR `class _FakePlayer(QObject):` near the top of the file.
    2. Delete the entire class body (class line through the last method definition). If `__init__` records call attributes, those attributes are already provided by the shared `FakePlayer.__init__` per Plan 77-01 Task 1.
    3. Replace with a single import line near the top of the file:
       - For files that used `class FakePlayer(QObject):` (5 sites: `test_now_playing_panel`, `test_phase72_now_playing_panel`, `test_main_window_media_keys`, `test_discovery_dialog`, `test_stream_picker`, `test_phase72_1_stream_picker_reflow`): `from tests._fake_player import FakePlayer`
       - For files that used `class _FakePlayer(QObject):` with underscore-prefix (4 sites: `test_main_window_gbs`, `test_main_window_soma`, `test_ui_qt_scaffold`, `tests/ui_qt/test_main_window_node_indicator`): use the aliased import `from tests._fake_player import FakePlayer as _FakePlayer` to preserve the existing in-file references to the symbol `_FakePlayer` with zero call-site diff. This is the lowest-friction migration shape per PATTERNS.md L228-235.
    4. Confirm the file still imports any other names it needs (Signal, QObject are no longer needed FOR the FakePlayer declaration — but DO NOT remove those imports if they are used elsewhere in the file for other purposes; only delete imports that are now unreferenced).
    5. Run `uv run pytest tests/{file} -x` to confirm no regression.

    Site-by-site:

    1. `tests/test_main_window_gbs.py` — class `_FakePlayer(QObject)` at line 25. Delete inline class (lines 25-~50). Add `from tests._fake_player import FakePlayer as _FakePlayer`. The wrong `audio_caps_detected = Signal(object)` arity at line 34 is removed by class deletion; production-correct `Signal(int, int, int)` arrives via the shared base.

    2. `tests/test_main_window_soma.py` — class `_FakePlayer(QObject)` at line 33. Delete inline (lines 33-~55). Add `from tests._fake_player import FakePlayer as _FakePlayer`. Same auto-fix for `audio_caps_detected` arity.

    3. `tests/test_now_playing_panel.py` — class `FakePlayer(QObject)` at line 29. Delete inline (lines 29-~60). Add `from tests._fake_player import FakePlayer`. The shared base provides 12 additional signals beyond the local 6; this is safe (Qt does not raise on connecting to or never-emitting a Signal).

    4. `tests/test_phase72_now_playing_panel.py` — class `FakePlayer(QObject)` at line 34. Delete inline (lines 34-~65). Add `from tests._fake_player import FakePlayer`.

    5. `tests/test_ui_qt_scaffold.py` — class `_FakePlayer(QObject)` at line 16. Delete inline (lines 16-~50). Add `from tests._fake_player import FakePlayer as _FakePlayer`.

    6. `tests/test_main_window_media_keys.py` — class `FakePlayer(QObject)` at line 54. Delete inline (lines 54-~90). Add `from tests._fake_player import FakePlayer`.

    7. `tests/test_discovery_dialog.py` — class `FakePlayer(QObject)` at line 39. Delete inline (lines 39-~70). Add `from tests._fake_player import FakePlayer`. Used only for `discovery_dialog`, not full MainWindow — over-signaling from shared base is harmless.

    8. `tests/test_stream_picker.py` — class `FakePlayer(QObject)` at line 32. Delete inline (lines 32-~65). Add `from tests._fake_player import FakePlayer`.

    9. `tests/test_phase72_1_stream_picker_reflow.py` — class `FakePlayer(QObject)` at line 79. Delete inline (lines 79-~110). Add `from tests._fake_player import FakePlayer`. (Note: Task 1 already handled the transitive import at line 422 in this same file — both edits must coexist correctly.)

    10. `tests/ui_qt/test_main_window_node_indicator.py` — class `_FakePlayer(QObject)` at line 35. Delete inline (lines 35-~70). Add `from tests._fake_player import FakePlayer as _FakePlayer`. SUBDIRECTORY SITE — the D-17 drift-guard regex uses rglob to descend here.

    Non-migration site:

    11. `tests/test_equalizer_dialog.py` — `class FakePlayer:` (NO QObject) at line 40. STAYS inline. Add a one-line comment immediately above the class declaration:
        ```
        # Phase 77 D-09 (Option A): intentionally non-QObject; EQ-only test double for
        # EqualizerDialog tests. The dialog calls player.set_eq_*() but does not connect
        # to player signals. The D-17 drift-guard regex `class\s+_?FakePlayer\s*\(QObject\)`
        # at tests/test_fake_player_no_inline.py excludes this declaration by design.
        ```
        No code change to the class body. Just the documenting comment.

    Critical anti-patterns to avoid:
    - Do NOT remove unrelated imports from any file. The QObject/Signal imports may still be used elsewhere in the same file for non-FakePlayer purposes (e.g., other test doubles, signal-spy patterns).
    - Do NOT change FakeRepo declarations — those are out of scope.
    - Do NOT add `tests/_fake_player.py` as an autouse fixture in conftest.py — D-07 prescribes direct-import-only opt-in (matches `_FakeRepo` shape).
    - Do NOT touch `tests/test_equalizer_dialog.py` class body — comment only.
  </action>
  <verify>
    <automated>uv run python -c "import re; from pathlib import Path; ROOT = Path('tests'); FAKE_PLAYER_RE = re.compile(r'^\s*class\s+_?FakePlayer\s*\(QObject\)', re.M); ALLOWED = {'_fake_player.py'}; offenders = [str(p) for p in ROOT.rglob('*.py') if p.name not in ALLOWED and FAKE_PLAYER_RE.search(p.read_text(encoding='utf-8'))]; assert len(offenders) == 0, f'D-17 drift-guard still RED — {len(offenders)} inline sites remain: {offenders}'; print('D-17 GREEN')" && uv run pytest tests/test_main_window_gbs.py tests/test_main_window_soma.py tests/test_now_playing_panel.py tests/test_phase72_now_playing_panel.py tests/test_ui_qt_scaffold.py tests/test_main_window_media_keys.py tests/test_discovery_dialog.py tests/test_stream_picker.py tests/test_phase72_1_stream_picker_reflow.py tests/ui_qt/test_main_window_node_indicator.py tests/test_equalizer_dialog.py tests/test_fake_player_no_inline.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 10 QObject-subclass migration sites have `from tests._fake_player import FakePlayer` (or `... as _FakePlayer`) — verify via `grep -lE "from tests\._fake_player import FakePlayer" tests/test_main_window_gbs.py tests/test_main_window_soma.py tests/test_now_playing_panel.py tests/test_phase72_now_playing_panel.py tests/test_ui_qt_scaffold.py tests/test_main_window_media_keys.py tests/test_discovery_dialog.py tests/test_stream_picker.py tests/test_phase72_1_stream_picker_reflow.py tests/ui_qt/test_main_window_node_indicator.py | wc -l` returns 10
    - Zero inline `class _?FakePlayer(QObject)` declarations remain in `tests/` (verify via the `python -c` rglob+regex check in the verify command above — must return 0)
    - `tests/test_fake_player_no_inline.py` from Plan 77-01 turns GREEN (this is the same condition as the bullet above, automated)
    - `tests/test_equalizer_dialog.py:40` still has `class FakePlayer:` (without `(QObject)`) AND has the new `Phase 77 D-09` comment immediately above (verify via `grep -F "Phase 77 D-09" tests/test_equalizer_dialog.py` returns ≥ 1 line)
    - Specific arity-drift verification: `grep -F "audio_caps_detected = Signal(object)" tests/test_main_window_gbs.py tests/test_main_window_soma.py` returns 0 lines (the wrong arity is gone)
    - Test command above exits 0 — all 11 affected files pass plus the now-GREEN no-inline drift-guard
  </acceptance_criteria>
  <done>All 11 inline FakePlayer(QObject) sites migrated; gbs/soma audio_caps_detected arity drift auto-fixed; test_equalizer_dialog's non-QObject FakePlayer documented as intentional non-migration; D-17 drift-guard from Plan 77-01 turns GREEN; full-suite run across 11 affected files plus the no-inline guard exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → shared test-helper module | All migrated test files now import from `tests/_fake_player.py` — same trust domain (repo-controlled). No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-T | Tampering | A migration replaces a working inline FakePlayer with the shared base, but the shared base is missing a behavior the inline class provided (e.g., a custom method) | mitigate | Per-task pytest run on the 11 modified files catches missing behaviors immediately. PATTERNS.md L228-260 confirms 9 of the 10 sites use only the standard `set_volume`/`play`/`pause`/`stop`/EQ method surface that the shared base provides; the 10th (`test_equalizer_dialog`) is intentionally not migrated. |
| T-77-02-T2 | Tampering | `audio_caps_detected` arity change from `Signal(object)` to `Signal(int, int, int)` breaks a test that was emitting with 1 arg | mitigate | The PRODUCTION code emits with 3 args (`player.py:838`); only the test-double declarations had wrong arity. No test should be emitting `audio_caps_detected.emit(something_single)` against a FakePlayer — that would have been broken against production already. Verified by Task 2 pytest run. |
| T-77-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (entire migration is import-line edits). Package Legitimacy Audit not required. |
</threat_model>

<verification>
1. `uv run python -c "...rglob+regex check..."` (in Task 2 verify) returns 0 offenders → D-17 GREEN
2. `uv run pytest tests/ -x` runs through (or at least reaches Plan 77-03/04/05 test files; if it stops earlier, the failure must be in a file this plan touched)
3. Spot-check `tests/test_equalizer_dialog.py` still passes (`uv run pytest tests/test_equalizer_dialog.py -x`)
</verification>

<success_criteria>
- [ ] 11 inline `FakePlayer(QObject)` sites in tests/ migrated to `from tests._fake_player import FakePlayer`
- [ ] 5 transitive `from tests.test_main_window_integration import FakePlayer` consumers redirected
- [ ] `tests/test_equalizer_dialog.py` non-QObject FakePlayer stays inline with documenting comment (D-09 Option A)
- [ ] `tests/test_fake_player_no_inline.py` (Plan 77-01) turns GREEN
- [ ] gbs/soma `audio_caps_detected = Signal(object)` arity drift auto-fixed
- [ ] All 16 modified files + the no-inline guard pass `uv run pytest -x`
- [ ] No production-code changes (musicstreamer/ byte-identical)
</success_criteria>

<output>
Create `.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-02-SUMMARY.md`. Record: count of migrated sites (must be 11 QObject sites + 1 non-migration documenting comment), count of transitive consumers redirected (must be 5), verification that D-17 drift-guard from Plan 77-01 is GREEN, and the test runtimes for the 11 affected files before/after migration (sanity check — should be unchanged within noise).
</output>
