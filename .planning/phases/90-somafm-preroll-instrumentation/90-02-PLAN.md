---
phase: 90-somafm-preroll-instrumentation
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - musicstreamer/player.py
  - tests/test_player.py
autonomous: true
requirements: [SOMA-PRE-01, SOMA-PRE-04, SOMA-PRE-05, D-08]
user_setup: []

must_haves:
  truths:
    - "The SomaFM preroll gate emits `preroll_start` (with chosen URL + station name/id), `preroll_skipped_throttle`, `preroll_skipped_empty` (unfetched + fetched_empty), and `preroll_handoff_complete` to the `musicstreamer.preroll` logger — purely additive, zero behavior change (SOMA-PRE-01, D-03, D-06)"
    - "The `preroll_start` event records the URL chosen by the UNCHANGED `random.choice(urls)` selection (D-06)"
    - "Auto-staleness re-fetch fires ONLY when fetched_at IS NOT NULL AND prerolls==[] AND age > 7-day threshold AND station not already in-flight, closing the fetched-with-0 trap (D-08, SOMA-PRE-04 recovery)"
    - "Phase 84 D-11 buffer-duration→uri ordering is preserved: no log/staleness code injects a set_property call between the pending-buffer apply and the uri bind (SOMA-PRE-05, D-10)"
    - "Phase 83 preroll drift-guard stays green; throttle + start-timestamp semantics unchanged (SOMA-PRE-05)"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "additive preroll log calls + D-08 auto-staleness branch + _PREROLL_STALE_THRESHOLD_S constant"
      contains: "musicstreamer.preroll"
    - path: "tests/test_player.py"
      provides: "log-call behavior tests + 3 auto-staleness tests"
      contains: "preroll_start"
  key_links:
    - from: "musicstreamer/player.py"
      to: "musicstreamer.preroll logger"
      via: "logging.getLogger('musicstreamer.preroll').info(...) at gate + handoff"
      pattern: "getLogger\\(.musicstreamer.preroll.\\)"
    - from: "player.py _try_next_stream fetched-empty branch"
      to: "self._preroll_backfill_worker"
      via: "D-08 staleness threading.Thread"
      pattern: "_PREROLL_STALE_THRESHOLD_S"
---

<objective>
Wire the five preroll event log calls into `player.py`'s SomaFM preroll path
(`_try_next_stream` gate + `_on_preroll_about_to_finish` handoff) as PURELY ADDITIVE
statements, and add the D-08 auto-staleness re-fetch branch that closes the latent
"fetched-with-0 never re-fetches" trap. This is the highest-risk plan: SOMA-PRE-05
demands ZERO regression to Phase 84 buffer adaptation.

Purpose: Make every per-station preroll decision legible (which URL was chosen, why a
preroll was skipped) so the user's manual all-stations run-through (SOMA-PRE-04 verify
half) produces decisive evidence; and self-heal stations stuck at fetched-with-0 (D-08).
Output: Additive log calls at 5 decision points, the `_PREROLL_STALE_THRESHOLD_S` constant
(7 days), the auto-staleness branch, and tests covering log emission + staleness gating.

NON-NEGOTIABLE (D-10 / SOMA-PRE-05): Log calls invoke ONLY
`logging.getLogger("musicstreamer.preroll").info(...)`. They MUST NOT call
`self._pipeline.set_property(...)`, and no log/staleness statement may be inserted between
the pending-buffer apply and the `set_property("uri", ...)` bind in `_try_next_stream` or
`_on_preroll_about_to_finish`. The `random.choice(urls)` selection is UNCHANGED (D-06).

DEFERRAL: SOMA-PRE-03 (network probe) is NOT in this plan — deferred to Phase 90b.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-somafm-preroll-instrumentation/90-CONTEXT.md
@.planning/phases/90-somafm-preroll-instrumentation/90-PATTERNS.md
@.planning/phases/90-somafm-preroll-instrumentation/90-RESEARCH.md
@.planning/phases/90-somafm-preroll-instrumentation/90-VALIDATION.md
@.planning/phases/90-somafm-preroll-instrumentation/90-01-SUMMARY.md

<interfaces>
<!-- Consumed contracts. From Plan 90-01 + existing player.py. Use directly — no exploration. -->

From Plan 90-01 (musicstreamer/preroll_log.py):
  Logger name: "musicstreamer.preroll"  (INFO level, RotatingFileHandler installed at startup)

From musicstreamer/player.py — existing SomaFM gate (~lines 749-772, the self-analog to extend):
```
if (station.provider_name == "SomaFM"
    and (self._last_preroll_played_at is None
         or time.monotonic() - self._last_preroll_played_at > 600)):
    urls = list(getattr(station, "prerolls", []) or [])
    if urls:
        preroll_url = random.choice(urls)        # D-06 — UNCHANGED
        self._start_preroll(preroll_url)
        return
    elif (getattr(station, "prerolls_fetched_at", None) is None
          and station.id not in self._backfill_in_flight):
        self._backfill_in_flight.add(station.id)
        threading.Thread(target=self._preroll_backfill_worker,
                         args=(station.id, station.name), daemon=True).start()
    # else (D-04 / Pitfall 5): fetched, genuinely-empty — skip silently.
self._try_next_stream()
```

From musicstreamer/player.py — handoff slot (~line 1577):
```
self._preroll_in_flight = False
if not self._streams_queue:
    self._try_next_stream()
    return
# ... gapless set_property("uri", ...) far downstream (~line 1639)
```

From musicstreamer/player.py — backfill worker (Pattern 4, thread-local Repo, ~lines 2023-2041):
  `_preroll_backfill_worker(station_id, station_name)` — db_connect() inside, con.close() in finally, D-04 silent swallow.

Drift-guard (tests/test_player.py:1201): `test_phase_83_preroll_drift_guard` source-greps player.py
for the literal `"SomaFM"` and `_last_preroll_played_at` — asserts EXISTENCE (>=1 match), not exact count.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject additive preroll log calls at the gate + handoff (zero behavior change)</name>
  <files>musicstreamer/player.py, tests/test_player.py</files>
  <read_first>
    - musicstreamer/player.py lines 740-780 (the `_try_next_stream` SomaFM gate)
    - musicstreamer/player.py lines 1560-1650 (`_on_preroll_about_to_finish` — `_preroll_in_flight = False` at ~1577, gapless uri swap at ~1639)
    - .planning/phases/90-somafm-preroll-instrumentation/90-PATTERNS.md (player.py injection-point section — exact placements)
    - .planning/phases/90-somafm-preroll-instrumentation/90-RESEARCH.md (Pitfall 1, Pitfall 2; Code Examples)
    - tests/test_player.py lines 1190-1215 (`test_phase_83_preroll_drift_guard`), lines 870-900 (throttle-timestamp tests)
    - tests/test_buffer_events_log.py (caplog/handler fixture pattern for asserting log lines)
  </read_first>
  <behavior>
    - Playing a SomaFM station with non-empty prerolls logs `preroll_start station_name=... station_id=... url=...` where `url` is exactly the value returned by `random.choice(urls)` (D-06), emitted BEFORE `_start_preroll`.
    - Playing a SomaFM station while the 600s throttle window is active logs `preroll_skipped_throttle station_name=... station_id=... remaining_s=...` and does NOT start a preroll.
    - Playing a SomaFM station that is unfetched (prerolls_fetched_at is None, empty prerolls) logs `preroll_skipped_empty ... reason=unfetched`.
    - Playing a SomaFM station fetched-but-empty logs `preroll_skipped_empty ... reason=fetched_empty`.
    - Completing a gapless handoff logs `preroll_handoff_complete station_name=... station_id=...` after `_preroll_in_flight = False`, before the empty-queue check.
    - `test_phase_83_preroll_drift_guard`, `test_throttle_timestamp_set_on_start`, `test_wr05_throttle_documents_attempted_semantics`, and all existing `-k preroll` tests stay green.
  </behavior>
  <action>
    In `_try_next_stream`, add the four log calls described in 90-PATTERNS.md "Log call injection points", using ONLY `logging.getLogger("musicstreamer.preroll").info(...)`:
    (1) a `preroll_skipped_throttle` probe BEFORE the existing combined gate (a separate read-only `if provider_name == "SomaFM" and _last_preroll_played_at is not None and within 600s` block — no state mutation, do NOT restructure the existing gate condition);
    (2) `preroll_start` immediately AFTER `preroll_url = random.choice(urls)` and BEFORE `self._start_preroll(preroll_url)` — log the chosen `preroll_url` verbatim (D-06; selection logic unchanged);
    (3) `preroll_skipped_empty ... reason=unfetched` inside the existing `elif prerolls_fetched_at is None` branch (before the `threading.Thread` call);
    (4) `preroll_skipped_empty ... reason=fetched_empty` in the `else` (fetched-empty) branch.
    In `_on_preroll_about_to_finish`, add `preroll_handoff_complete` AFTER `self._preroll_in_flight = False` and BEFORE the `if not self._streams_queue:` check, using `self._current_station_name` / `self._current_station_id`.
    Event names are EXACTLY: `preroll_start`, `preroll_skipped_throttle`, `preroll_skipped_empty`, `preroll_handoff_complete` (and reserve `preroll_error` — do not wire it). NO `set_property` calls in any added line. NO change to `random.choice`, throttle math, or `_set_uri` ordering.
    Extend tests/test_player.py with: `test_preroll_log_start_event_includes_url_and_station`, `test_preroll_log_skipped_throttle_event`, `test_preroll_log_skipped_empty_fetched`, `test_preroll_log_handoff_complete_event` (assert on the `musicstreamer.preroll` logger via caplog/attached handler).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player.py -k "preroll or throttle or drift_guard" -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_player.py -k "preroll or throttle or drift_guard" -q --timeout=30` exits 0 (new log tests + existing preroll/throttle/drift-guard tests all green).
    - `grep -c "preroll_start\|preroll_skipped_throttle\|preroll_skipped_empty\|preroll_handoff_complete" musicstreamer/player.py` returns >= 4 (all four event names wired).
    - Zero-behavior-change source assertion: between `random.choice(urls)` and `self._start_preroll(preroll_url)` only an `info(...)` log call is inserted — `grep -n "set_property" musicstreamer/player.py` shows no new set_property between the buffer apply and uri bind in the gate (D-11 guard, confirmed by Task 2 suite).
    - `grep -q "random.choice(urls)" musicstreamer/player.py` still succeeds (selection unchanged, D-06).
    - `preroll_start` line logs the chosen URL: `grep -q "preroll_start.*url=" musicstreamer/player.py` succeeds.
  </acceptance_criteria>
  <done>Five additive log calls wired at the gate + handoff; new log-emission tests green; drift-guard + throttle + selection unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-08 auto-staleness re-fetch branch + run the D-11 / D-09 regression gate</name>
  <files>musicstreamer/player.py, tests/test_player.py</files>
  <read_first>
    - musicstreamer/player.py lines 740-780 (the fetched-empty `else` branch to extend) + lines 2010-2045 (`_preroll_backfill_worker` signature + Pattern 4)
    - .planning/phases/90-somafm-preroll-instrumentation/90-PATTERNS.md (Pattern 3 auto-staleness branch — exact code shape)
    - .planning/phases/90-somafm-preroll-instrumentation/90-RESEARCH.md (Pitfall 2, Pitfall 3, Pitfall 4; staleness threshold rationale)
    - tests/test_player_buffer_growth.py lines 178-310 (the 14 D-11 ordering tests — `_set_uri` buffer-duration→uri)
    - .claude/skills/spike-findings-musicstreamer/SKILL.md (GStreamer/Qt/GLib bus-handler threading landmines bearing on QThread/daemon-thread worker changes)
  </read_first>
  <behavior>
    - A SomaFM station with `prerolls_fetched_at` set (NOT None), zero prerolls, and `now - fetched_at > 7 days`, not already in `_backfill_in_flight`, schedules `_preroll_backfill_worker(station.id, station.name)` on a daemon thread and adds the id to `_backfill_in_flight`.
    - A station WITH prerolls never triggers the staleness branch (it plays a preroll instead).
    - A station fetched-empty but recently (age <= threshold) does NOT trigger re-fetch.
    - The D-08 branch is mutually exclusive with the D-13 unfetched branch (one needs fetched_at IS NULL, the other IS NOT NULL) — confirmed by a code comment.
    - All 14 Phase 84 D-11 ordering tests stay green (SOMA-PRE-05); the re-fetch reuses Pattern 4 single-flight (D-09).
  </behavior>
  <action>
    Add a module-level constant `_PREROLL_STALE_THRESHOLD_S: int = 7 * 24 * 3600` near the top of player.py (Claude's Discretion per D-08; 7 days — cite RESEARCH A1 rationale in a comment).
    In the fetched-empty `else` branch of the gate (after the `preroll_skipped_empty reason=fetched_empty` log from Task 1), add the D-08 sub-condition exactly as in 90-PATTERNS.md Pattern 3: if `getattr(station, "prerolls_fetched_at", None) is not None and int(time.time()) - station.prerolls_fetched_at > _PREROLL_STALE_THRESHOLD_S and station.id not in self._backfill_in_flight`, then `self._backfill_in_flight.add(station.id)` and start a daemon `threading.Thread(target=self._preroll_backfill_worker, args=(station.id, station.name), daemon=True)`. Add a comment noting mutual exclusivity with the D-13 unfetched branch (Pitfall 3) and that the worker discards the id in its finally (T-83-10 single-flight, D-09). Do NOT add a DB read in the hot path — use the in-memory `station.prerolls_fetched_at` (Pitfall 2). Reuse the EXISTING `_preroll_backfill_worker` (D-09: prefer the reliable capture path; the manual full-sweep lever lives in Plan 03). Do NOT clear/delete existing rows (Pitfall 4 — worker only inserts where 0 rows).
    Extend tests/test_player.py with: `test_auto_staleness_refetch_triggers_for_old_empty_station`, `test_auto_staleness_no_refetch_when_prerolls_exist`, `test_auto_staleness_no_refetch_when_recently_fetched` (patch `time.time`/threshold and assert `threading.Thread` start / `_backfill_in_flight` membership; mock the worker target so no real network).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player_buffer_growth.py tests/test_player.py -k "staleness or preroll or buffer or drift_guard" -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_player_buffer_growth.py -q --timeout=30` exits 0 — all 14 Phase 84 D-11 ordering tests GREEN (SOMA-PRE-05 regression gate).
    - `.venv/bin/python -m pytest tests/test_player.py -k staleness -q --timeout=30` exits 0 (3 new staleness tests green).
    - `grep -q "_PREROLL_STALE_THRESHOLD_S" musicstreamer/player.py` succeeds and the constant equals `7 * 24 * 3600`.
    - The D-08 branch requires fetched_at IS NOT NULL: `grep -A4 "fetched_empty" musicstreamer/player.py | grep -q "prerolls_fetched_at, None) is not None"` (mutual exclusivity with D-13).
    - Drift-guard + full preroll suite still green: `.venv/bin/python -m pytest tests/test_player.py -k "preroll or drift_guard" -q --timeout=30` exits 0.
  </acceptance_criteria>
  <done>D-08 auto-staleness branch closes the fetched-with-0 trap via the existing Pattern-4 worker; the 14 D-11 buffer-ordering tests and the full preroll suite re-run clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SomaFM API → DB (via re-fetch worker) | upstream preroll URLs inserted into `station_prerolls` |
| background daemon thread → SQLite | re-fetch worker writes off the main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-03 | Tampering | upstream preroll URL inserted by D-08 staleness re-fetch | mitigate | Re-fetch reuses `_preroll_backfill_worker` → `repo.insert_preroll`, whose Phase 83 T-83-01 scheme gate rejects non-HTTP(S) URLs (`file://`, `javascript:`). Do NOT bypass insert_preroll. |
| T-90-04 | Denial of Service | repeated re-fetch on every play of a stuck station | mitigate | `_backfill_in_flight` single-flight (T-83-10) + 7-day staleness threshold gate the trigger; worker discards id in finally. |
| T-90-05 | Tampering (concurrency) | SQLite write from daemon thread | mitigate | Pattern 4 thread-local Repo via `db_connect()` inside the worker (existing `_preroll_backfill_worker` discipline, D-09); no shared main-thread Repo. |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib + existing project modules only (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- Phase 84 D-11 replay: `.venv/bin/python -m pytest tests/test_player_buffer_growth.py -q --timeout=30` exits 0 (SOMA-PRE-05 gate).
- Phase 83 drift-guard: `.venv/bin/python -m pytest tests/test_player.py::test_phase_83_preroll_drift_guard -q` exits 0.
- Source drift-guard for `_set_uri` ordering preserved: no new `set_property` between buffer-duration apply and uri bind (covered by the 14 growth tests).
- All four preroll event names + handoff present in player.py; `random.choice` selection unchanged (D-06).
</verification>

<success_criteria>
- Instrumentation added at `_try_next_stream` + `_on_preroll_about_to_finish` decision boundaries with zero behavior change (SOMA-PRE-01, D-10).
- `preroll_start` records the randomly-chosen URL; selection stays `random.choice` (D-06).
- D-08 auto-staleness re-fetch closes the fetched-with-0 trap via the existing Pattern-4 single-flight worker (SOMA-PRE-04 recovery, D-09).
- Phase 84 D-11 12-event replay (14 tests) re-runs clean; Phase 83 drift-guard green (SOMA-PRE-05).
- SOMA-PRE-03 probe NOT present (deferred to 90b).
</success_criteria>

<output>
Create `.planning/phases/90-somafm-preroll-instrumentation/90-02-SUMMARY.md` when done
</output>
