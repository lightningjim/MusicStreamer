---
phase: 90-somafm-preroll-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/preroll_log.py
  - musicstreamer/paths.py
  - musicstreamer/__main__.py
  - tests/test_preroll_events_log.py
  - tests/test_paths.py
autonomous: true
requirements: [SOMA-PRE-01]
user_setup: []

must_haves:
  truths:
    - "A named logger `musicstreamer.preroll` writes structured INFO lines to a size-rotated `preroll-events.log` (SOMA-PRE-01)"
    - "`paths.preroll_events_log_path()` resolves to `{data_root}/preroll-events.log` and honors `_root_override` for test isolation (SOMA-PRE-01, D-04)"
    - "The handler installs once (idempotent) at startup after migration, mirroring buffer_log (SOMA-PRE-01, D-03)"
    - "SOMA-PRE-03 (30s network probe + preroll-probe.log) is DEFERRED to conditional Phase 90b — NOT built here"
  artifacts:
    - path: "musicstreamer/preroll_log.py"
      provides: "install_preroll_events_handler() — RotatingFileHandler on musicstreamer.preroll logger"
      contains: "def install_preroll_events_handler"
      min_lines: 20
    - path: "musicstreamer/paths.py"
      provides: "preroll_events_log_path() path helper"
      contains: "def preroll_events_log_path"
    - path: "tests/test_preroll_events_log.py"
      provides: "5-test mirror of test_buffer_events_log.py for the preroll logger"
      contains: "musicstreamer.preroll"
  key_links:
    - from: "musicstreamer/preroll_log.py"
      to: "musicstreamer/paths.py"
      via: "paths.preroll_events_log_path()"
      pattern: "preroll_events_log_path"
    - from: "musicstreamer/__main__.py"
      to: "musicstreamer/preroll_log.py"
      via: "install_preroll_events_handler() called after run_migration()"
      pattern: "install_preroll_events_handler"
---

<objective>
Build the preroll-event logging substrate: a new `musicstreamer/preroll_log.py` module
that installs a size-rotated `RotatingFileHandler` on a dedicated `musicstreamer.preroll`
logger, a `paths.preroll_events_log_path()` helper, and the startup install wiring — all
strict mirrors of the Phase 78 `buffer_log.py` pattern. This is the legibility layer
(SOMA-PRE-01 / D-03 / D-04): downstream plans emit events into this logger.

Purpose: Make the SomaFM preroll gate's decision path legible so the user's manual
all-stations run-through (SOMA-PRE-04 verify half) has a complete, openable record.
Output: `preroll_log.py`, `paths.preroll_events_log_path()`, install site in `__main__.py`,
plus a 5-test mirror and 2 path tests (Wave 0 for the rest of the phase).

DEFERRAL (explicit, not a gap): SOMA-PRE-03 (the opt-in 30s `requests.get` network probe
+ `preroll-probe.log`) is OUT OF SCOPE — deferred to conditional Phase 90b per D-01. Do
NOT build a probe or any network-listening harness in this plan.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-somafm-preroll-instrumentation/90-CONTEXT.md
@.planning/phases/90-somafm-preroll-instrumentation/90-PATTERNS.md
@.planning/phases/90-somafm-preroll-instrumentation/90-VALIDATION.md

<interfaces>
<!-- Analog contracts the executor mirrors. Extracted from codebase. Use directly — no exploration. -->

From musicstreamer/buffer_log.py (the exact template):
```
def install_buffer_events_handler() -> None:
    path = paths.buffer_events_log_path()
    log = logging.getLogger("musicstreamer.player")
    for h in log.handlers:
        if isinstance(h, RotatingFileHandler) and h.baseFilename == path:
            return  # already installed — short-circuit
    handler = RotatingFileHandler(path, maxBytes=1_048_576, backupCount=3, encoding="utf-8")
    handler.setFormatter(logging.Formatter("%(asctime)s %(message)s"))
    log.addHandler(handler)
```

From musicstreamer/paths.py:
```
_root_override: str | None = None          # line 25 — test monkeypatch hook
def _root() -> str: ...                     # line 28
def buffer_events_log_path() -> str:        # line 68
    return os.path.join(_root(), "buffer-events.log")
```

From musicstreamer/__main__.py (install site, line 250-261):
```
migration.run_migration()                   # line 250 — DATA_DIR exists after this
...
from musicstreamer.buffer_log import install_buffer_events_handler   # line 260
install_buffer_events_handler()             # line 261
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preroll_events_log_path() and write the Wave 0 test mirror</name>
  <files>musicstreamer/paths.py, tests/test_paths.py, tests/test_preroll_events_log.py</files>
  <read_first>
    - musicstreamer/paths.py (lines 25-90 — `_root_override`, `_root()`, `buffer_events_log_path()`)
    - tests/test_buffer_events_log.py (entire file — the 5-test template + `_clean_player_handlers` fixture)
    - tests/test_paths.py (locate the existing buffer_events_log_path test to mirror)
    - .planning/phases/90-somafm-preroll-instrumentation/90-PATTERNS.md (paths + test_preroll_events_log sections)
  </read_first>
  <behavior>
    - `paths.preroll_events_log_path()` returns `os.path.join(_root(), "preroll-events.log")`.
    - With `_root_override` set to a tmp dir, the returned path is `{tmp}/preroll-events.log`.
    - tests/test_preroll_events_log.py mirrors tests/test_buffer_events_log.py 5 tests, but for logger `musicstreamer.preroll` and file `preroll-events.log`: handler-attached, emit-writes-line, rotation-at-1MB, never-creates-backup-4, record-reaches-both-sinks (propagate is True).
  </behavior>
  <action>
    Add `preroll_events_log_path()` to paths.py immediately after `buffer_events_log_path()` (after line 70), returning `os.path.join(_root(), "preroll-events.log")` with a docstring citing Phase 90 D-04. Do NOT alter `_root()` or `_root_override`.
    Extend tests/test_paths.py with `test_preroll_events_log_path_returns_correct_path` and `test_preroll_events_log_path_respects_root_override` (mirror the existing buffer-log path tests; use `monkeypatch.setattr(paths, "_root_override", str(tmp_path))`).
    Create tests/test_preroll_events_log.py as a verbatim structural mirror of tests/test_buffer_events_log.py: a `_clean_preroll_handlers` autouse fixture substituting `"musicstreamer.preroll"` for `"musicstreamer.player"`, importing `install_preroll_events_handler` from `musicstreamer.preroll_log`, and asserting against `paths.preroll_events_log_path()`. Five tests: `test_handler_attached_to_preroll_logger`, `test_emit_writes_line_to_file` (emit a `preroll_start ...` line), `test_rotation_at_1mb`, `test_never_creates_backup_4`, `test_record_reaches_both_sinks` (assert `logging.getLogger("musicstreamer.preroll").propagate is True`). These tests are RED until Task 2 lands `preroll_log.py`.
    No fenced code in the production change beyond the one-line path return; no behavior change to any existing path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_paths.py -k preroll -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def preroll_events_log_path" musicstreamer/paths.py` succeeds.
    - `.venv/bin/python -m pytest tests/test_paths.py -k preroll -q` exits 0 (2 path tests green).
    - `grep -q "musicstreamer.preroll" tests/test_preroll_events_log.py` succeeds; the 5 logger tests exist (collection succeeds, they may be RED pending Task 2).
    - No existing path test regresses: `.venv/bin/python -m pytest tests/test_paths.py -q` exits 0 for the non-preroll tests.
  </acceptance_criteria>
  <done>preroll_events_log_path() exists and is path-tested; test_preroll_events_log.py exists with 5 mirrored logger tests (RED, awaiting Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create preroll_log.py and wire its idempotent install in __main__.py</name>
  <files>musicstreamer/preroll_log.py, musicstreamer/__main__.py</files>
  <read_first>
    - musicstreamer/buffer_log.py (entire file — the install template, rotation params, idempotency loop)
    - musicstreamer/__main__.py (lines 250-261 — migration + buffer-log install ordering)
    - tests/test_preroll_events_log.py (the 5 tests this task must turn GREEN — from Task 1)
    - .planning/phases/90-somafm-preroll-instrumentation/90-RESEARCH.md (Pitfall 1 install-after-migration, Pitfall 6 setLevel INFO)
  </read_first>
  <behavior>
    - `install_preroll_events_handler()` attaches exactly one `RotatingFileHandler(maxBytes=1_048_576, backupCount=3, encoding="utf-8")` on logger `musicstreamer.preroll` at `paths.preroll_events_log_path()`.
    - Calling it twice attaches only one handler (idempotent via the `baseFilename` comparison loop).
    - The logger level is set to INFO so gate INFO lines are emitted (Pitfall 6); `propagate` is left at its default True (Pitfall 5 parity).
    - After migration runs at startup, the install is invoked so the file opens against an existing DATA_DIR (Pitfall 1).
  </behavior>
  <action>
    Create musicstreamer/preroll_log.py as a strict mirror of buffer_log.py's `install_buffer_events_handler`: import `logging`, `RotatingFileHandler`, and `from musicstreamer import paths`. Define `install_preroll_events_handler()` that resolves `paths.preroll_events_log_path()`, gets logger `"musicstreamer.preroll"`, short-circuits if a `RotatingFileHandler` with matching `baseFilename` is already attached, then sets `log.setLevel(logging.INFO)` (Pitfall 6), attaches a `RotatingFileHandler(path, maxBytes=1_048_576, backupCount=3, encoding="utf-8")` with formatter `"%(asctime)s %(message)s"`. Do NOT set `log.propagate = False`.
    In musicstreamer/__main__.py, immediately after the existing `install_buffer_events_handler()` call (line 261), add `from musicstreamer.preroll_log import install_preroll_events_handler` then `install_preroll_events_handler()`. This MUST stay after `migration.run_migration()` (Pitfall 1). Mirror the existing comment style.
    No probe, no network code, no `requests` import (SOMA-PRE-03 is deferred to 90b).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_preroll_events_log.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_preroll_events_log.py -q` exits 0 (all 5 tests GREEN).
    - `grep -q "log.setLevel(logging.INFO)" musicstreamer/preroll_log.py` succeeds (Pitfall 6).
    - `grep -vc '^#' musicstreamer/preroll_log.py` shows no `propagate = False` line: `grep -c "propagate = False" musicstreamer/preroll_log.py` returns 0.
    - `grep -c "install_preroll_events_handler" musicstreamer/__main__.py` returns >= 2 (import + call), and the call appears after the `run_migration()` line (verify ordering by `grep -n`).
    - No `requests` or probe code: `grep -c "requests" musicstreamer/preroll_log.py` returns 0.
  </acceptance_criteria>
  <done>preroll_log.py installs an idempotent INFO RotatingFileHandler on musicstreamer.preroll; __main__.py installs it post-migration; the 5 mirror tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → local filesystem | `preroll-events.log` written under the user data dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Information Disclosure | `preroll-events.log` (world-readable diagnostic file) | accept | Diagnostic data only — station names + upstream preroll URLs, no credentials/PII. Mirrors Phase 78 D-03 default-perms precedent (deliberate departure from oauth_log.py 0o600). |
| T-90-02 | Denial of Service | unbounded log growth | mitigate | `RotatingFileHandler(maxBytes=1_048_576, backupCount=3)` caps disk to ~4MB — identical to buffer_log.py. |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — stdlib `logging` + existing `musicstreamer.paths` only (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_preroll_events_log.py tests/test_paths.py -k "preroll or path" -q` exits 0.
- `preroll_events_log_path()` and `install_preroll_events_handler()` both grep-present.
- `__main__.py` install ordering: `install_preroll_events_handler` appears after `run_migration`.
</verification>

<success_criteria>
- New `musicstreamer/preroll_log.py` writes size-rotated structured events to `~/.local/share/musicstreamer/preroll-events.log` (SOMA-PRE-01, D-03).
- `paths.preroll_events_log_path()` exists, mirrors `buffer_events_log_path()`, test-isolated via `_root_override` (D-04).
- 5-test mirror + 2 path tests GREEN; Wave 0 logging substrate ready for Plans 02/03.
- SOMA-PRE-03 deferral to Phase 90b documented; no probe/network code present.
</success_criteria>

<output>
Create `.planning/phases/90-somafm-preroll-instrumentation/90-01-SUMMARY.md` when done
</output>
