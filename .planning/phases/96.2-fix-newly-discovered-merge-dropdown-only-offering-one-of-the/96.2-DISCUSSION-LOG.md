# Phase 96.2: Fix Newly-Discovered Merge Dropdown - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-23
**Phase:** 96.2-fix-newly-discovered-merge-dropdown-only-offering-one-of-the
**Areas discussed:** Merge-target scope, Merge behavior (flag), Dropdown presentation

---

## Merge-target scope (the core fix)

| Option | Description | Selected |
|--------|-------------|----------|
| All stations for this provider | Every saved station under the same provider (~19), regardless of the live-sync flag. Needs a new list_stations_for_provider query. | ✓ |
| All YouTube stations (any provider) | Every YouTube-backed station, even under other providers. Widest net. | |
| Provider stations w/ a stale/dead stream | Only this provider's stations whose primary stream looks dead/stale. | |

**User's choice:** All stations for this provider
**Notes:** Un-defers Phase 96.1's deferred "map into ANY station," narrowed to same-provider. The flagged-only filter (1 of 19) was the whole bug.

---

## Merge behavior — flag on merge

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — enable live-sync on merge | Set live_url_syncs_from_channel=1; station joins future refreshes. | |
| No — leave the flag unchanged | Re-point stream URL/title anchor only; merge stays a one-time fix. | ✓ |
| You decide | Claude picks based on apply_refresh/anchor code paths. | |

**User's choice:** No — leave the flag unchanged
**Notes:** Merging should not silently enroll a station into the auto-refresh workflow.

---

## Dropdown presentation

| Option | Description | Selected |
|--------|-------------|----------|
| Alphabetical by name | Keep ORDER BY name COLLATE NOCASE; plain station names. | ✓ |
| Flagged/stale stations first | Surface likely targets at top, then rest A–Z. | |
| You decide | Claude chooses ordering + optional disambiguation hint. | |

**User's choice:** Alphabetical by name
**Notes:** No grouping or disambiguation hints this phase.

---

## Claude's Discretion

- New `list_stations_for_provider` query vs. filtering an existing list in the dialog.
- Which `stream_id` on the target station the remap path re-points (preferred_stream_id vs first/primary).
- Whether to reword the "(no flagged stations)" empty-state combo text.

## Deferred Ideas

- Cross-provider / all-YouTube target set.
- Auto-enrolling merged stations into live-sync (explicitly rejected — D-03).
- Stale-only filtering / flagged-first ordering / URL disambiguation hints in the dropdown.
