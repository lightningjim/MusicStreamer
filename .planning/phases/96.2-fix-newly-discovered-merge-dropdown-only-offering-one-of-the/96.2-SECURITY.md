---
phase: 96.2
slug: fix-newly-discovered-merge-dropdown-only-offering-one-of-the
status: verified
threats_open: 0
asvs_level: 1
created: 2026-06-23
---

# Phase 96.2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| User (QComboBox selection) → dialog apply | Merge target is selected from a `QComboBox` via `userData` (a `Station` object), NOT free text. No new user-controlled string input crosses into the repo layer. | `Station` object reference (no untrusted string) |
| Dialog → SQLite (repo) | `list_stations_for_provider(provider_id)` is a parameterized read-only SELECT (`WHERE s.provider_id = ?`). No string interpolation. | Integer `provider_id` bind parameter |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-96.2-01 | Tampering | `list_stations_for_provider` SQL | accept | Parameterized query (`?` bind for `provider_id`); no new user-controlled input — identical injection surface to the proven `list_flagged_stations_for_provider` template. | closed |
| T-96.2-02 | Information Disclosure | merge dropdown now lists all provider stations | accept | Scope is the same single provider the user already owns and is refreshing (D-01 same-provider boundary; cross-provider explicitly deferred). No new data exposed across trust boundaries. | closed |
| T-96.2-03 | Spoofing/Injection | discovered-stream title label | mitigate | Untrusted scan title keeps its existing `setTextFormat(Qt.PlainText)` guard (CR-01 / T-39-01) — verified present in `live_refresh_dialog.py` (lines 356, 361, 513). Unchanged this phase; no new sink introduced. | closed |
| T-96.2-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH.md §Package Legitimacy Audit: not applicable). | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-96.2-01 | T-96.2-01 | New code is a parameterized read-only SELECT mirroring the proven `list_flagged_stations_for_provider` query; merge target is a `Station` object via `QComboBox.userData`, never free text. No high-severity threat introduced. | Kyle Creasey | 2026-06-23 |
| AR-96.2-02 | T-96.2-02 | Dropdown lists stations for the same single provider the user already owns and is actively refreshing; no cross-trust-boundary disclosure. | Kyle Creasey | 2026-06-23 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-06-23 | 4 | 4 | 0 | gsd:secure-phase (register authored at plan time; T-96.2-03 PlainText guard verified in implementation) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-06-23
