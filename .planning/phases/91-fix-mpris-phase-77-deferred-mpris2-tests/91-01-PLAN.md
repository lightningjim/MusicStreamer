---
phase: 91-fix-mpris-phase-77-deferred-mpris2-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/RETROSPECTIVE.md
autonomous: true
requirements: [FIX-MPRIS-01, FIX-MPRIS-02, FIX-MPRIS-03]

must_haves:
  truths:
    - "The 7 Phase-77-deferred MPRIS2 tests are verified green on this host (12 passed, 1 skipped, 0 failed)"
    - "The two FakePlayer drift-guards stay green (3 passed)"
    - "The SC1 grep in the ROADMAP Phase 91 block is honest — the anchored PCRE form returns exactly 1 (only tests/_fake_player.py)"
    - "The SC2 baseline count in the ROADMAP Phase 91 block reflects the current pre-phase pytest passed-count, not the historical 1462"
    - "FIX-MPRIS-01, FIX-MPRIS-02, FIX-MPRIS-03 are flipped to Complete and the Phase 91 ROADMAP checkbox is - [x]"
    - "A one-line RETROSPECTIVE note records that Phase 91 verified Phase 77 close-out (378440c) and changed no test/production code"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 91 detail block with corrected SC1 grep, refreshed SC2 baseline, checked checkbox"
      contains: "grep -rnP"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FIX-MPRIS-01/02/03 marked Complete in both the requirement list and traceability table"
      contains: "FIX-MPRIS-01 | Phase 91 | Complete"
    - path: ".planning/RETROSPECTIVE.md"
      provides: "Phase 91 close-out note citing 378440c, no test/code changes"
      contains: "378440c"
  key_links:
    - from: ".planning/ROADMAP.md Phase 91 SC1 line"
      to: "tests/_fake_player.py:37 (sole FakePlayer(QObject) declaration)"
      via: "anchored PCRE grep returning 1"
      pattern: "grep -rnP '\\^\\\\s\\*class"
    - from: ".planning/REQUIREMENTS.md FIX-MPRIS rows"
      to: "Phase 77 close-out commit 378440c"
      via: "status flip Pending -> Complete backed by D-08 verification evidence"
      pattern: "FIX-MPRIS-0[123].*Complete"
---

<objective>
Close out Phase 91 (FIX-MPRIS-01/02/03) as bookkeeping-only. The 7 D-03-deferred
MPRIS2 cluster-2 cross-file failures from Phase 77 were already turned green by
Phase 77 close-out commit `378440c` ("fix(77-06): unregister MPRIS2 OBJECT_PATH
in fixture teardown — cluster 2 close-out"). This plan does NOT repair anything.
It (1) re-verifies the closure still holds on this host, (2) fixes the provably
miswritten SC1 grep in the ROADMAP Phase 91 detail block, (3) refreshes the stale
SC2 baseline count, and (4) flips the three requirement statuses + the ROADMAP
checkbox + adds a one-line retrospective note.

Purpose: Phase 86 (Flatpak) PKG-LIN-FP-08 acceptance is gated on FIX-MPRIS landing
with a clean, honestly-asserted MPRIS2 test baseline. This plan provides that
closure record.

Output: Edits to `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`,
`.planning/RETROSPECTIVE.md`, plus the Phase 91 SUMMARY with captured verification
evidence.

HARD SCOPE FENCE (CONTEXT.md `<deferred>` "Out of scope"): No edits to ANY file
under `tests/` or `musicstreamer/`. No fixture edits to `tests/conftest.py`. This
plan touches only `.planning/` documents. The pytest runs are READ-ONLY evidence
gathering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-fix-mpris-phase-77-deferred-mpris2-tests/91-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v2.1-phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-VERIFICATION.md

<facts>
<!-- Pre-verified anchors. Executor uses these directly; no exploration needed. -->

- ROADMAP Phase 91 detail block is at `.planning/ROADMAP.md` starting line 72.
  - SC1 line (line 79) currently reads:
    `  1. \`grep -rn "class FakePlayer(QObject)" tests/ | wc -l\` returns exactly 1 and the only declaration site is \`tests/_fake_player.py\` (Pitfall 15 verified at source level before any test edits).`
  - SC2 line (line 80) currently contains the stale phrase `1462-test baseline`.
  - Phase 91 checkbox in the "### Phases" list is at line 30: `- [ ] **Phase 91: FIX-MPRIS ...`
- The miswritten SC1 grep `grep -rn "class FakePlayer(QObject)" tests/ | wc -l`
  returns **2** today, because `tests/test_fake_player_no_inline.py:28` mentions
  the literal `class FakePlayer(QObject)` inside its own docstring. It can NEVER
  return 1 while that docstring exists. (D-05: do NOT edit that docstring.)
- The D-04 anchored replacement
  `grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l`
  returns **1** today, matching only `tests/_fake_player.py:37`. Verified by hand
  immediately before this plan was written.
- REQUIREMENTS.md FIX-MPRIS markers exist in TWO places:
  - Requirement-list checkboxes: lines 97, 98, 99 each begin `- [ ] **FIX-MPRIS-0X**`.
  - Traceability table: lines 213, 214, 215 each read `| FIX-MPRIS-0X | Phase 91 | Pending |`.
- FIX-MPRIS-03 / SC3 drift-guard `test_richtext_baseline_unchanged_by_phase_71`
  lives in `tests/test_constants_drift.py`.
- `.planning/RETROSPECTIVE.md` EXISTS — it is the correct D-11 destination
  (project convention confirmed). Append a new dated subsection at end of file.
- Phase 77-VERIFICATION.md "Truth 5" already proves the cross-file ordering case
  (`test_main_window_integration.py → test_media_keys_mpris2.py`: 77 passed,
  1 skipped, 1 pre-existing hamburger fail). Per D-09, CITE this; do NOT re-run
  the multi-order pair locally.
- Phase 77-VERIFICATION.md "Deferred Items" row 1 names commit `378440c` as the
  close-out for the 7 MPRIS2 cross-file failures — the canonical pointer (D-11).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture verification evidence (D-08, read-only)</name>
  <read_first>
    - .planning/phases/91-fix-mpris-phase-77-deferred-mpris2-tests/91-CONTEXT.md (D-06, D-07, D-08, D-09 — locked verification command set and baseline-capture rule)
    - .planning/milestones/v2.1-phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-VERIFICATION.md ("Truth 5" + "Deferred Items" row 1 = commit 378440c)
    - tests/test_fake_player_no_inline.py (line 23 PCRE, line 28 docstring literal, line 50 runtime offender check — READ-ONLY, do not edit)
  </read_first>
  <files>(read-only — no file edits; pytest runs + greps only)</files>
  <action>
    Run the four locked D-08 checks IN THIS ORDER and capture exact stdout for the
    SUMMARY. Do NOT edit any test/fixture/production file — these runs are evidence
    only.

    1. `uv run pytest tests/test_media_keys_mpris2.py` — record the pass/skip/fail
       line. Expect "12 passed, 1 skipped" with 0 failed. The 1 skip is
       `test_playerctl_lists_service` (playerctl binary absent — a legitimate
       environmental skip per D-01, NOT a cluster mask).
    2. `uv run pytest tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py`
       — expect "3 passed" (the two FakePlayer drift-guards green).
    3. The D-04 anchored SC1 grep:
       `grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l`
       — expect output `1`, and the single match is `tests/_fake_player.py:37`.
       ALSO record the old miswritten form
       `grep -rn "class FakePlayer(QObject)" tests/ | wc -l` returning `2` (proves
       why the SC1 line must change — D-03).
    4. Runtime offender count == 0: this is asserted by
       `tests/test_fake_player_no_inline.py::test_no_inline_fake_player_subclass_in_tests`
       passing in check 2 above (the runtime drift-guard at line 50). Cite that the
       test passed as the offender==0 evidence; no separate command needed.

    5. SC3 / FIX-MPRIS-03 drift-guard:
       `uv run pytest tests/test_constants_drift.py::test_richtext_baseline_unchanged_by_phase_71`
       — expect "1 passed" (no source-introspection regression).

    6. CROSS-FILE ORDERING (D-09): do NOT re-run the multi-order pair. Cite
       Phase 77-VERIFICATION.md "Truth 5" by reference (77 passed, 1 skipped,
       1 pre-existing hamburger fail). Note in evidence that the lone fail is the
       documented Phase 74/76 hamburger carry-over, not MPRIS.

    7. BASELINE ANCHOR (D-06/D-07): immediately capture the current full-suite
       passed-count by running `uv run pytest tests/ -q` (or
       `--collect-only`-then-run if faster) and record the exact
       "N passed, M skipped, K failed" line. This N is the SC2 baseline anchor that
       Task 3 writes into the ROADMAP. Per CONTEXT D-06, today's expected shape is
       roughly "1821 passed, 1 skipped, 5 failed" — record the ACTUAL numbers, do
       not hardcode. The 5 non-MPRIS failures are catalogued in CONTEXT.md
       `<deferred>` and are out of scope (do not fix). Record N (passed count) for
       the `passed >= N` SC2 assertion.

    Stage all seven evidence captures verbatim for the SUMMARY. This task makes NO
    file edits.
  </action>
  <verify>
    <automated>uv run pytest tests/test_media_keys_mpris2.py tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py tests/test_constants_drift.py::test_richtext_baseline_unchanged_by_phase_71 -q 2>&1 | tail -5; echo "SC1-anchored:"; grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_media_keys_mpris2.py` output line contains "12 passed" and "1 skipped" and does NOT contain " failed" (0 failures).
    - `uv run pytest tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py` output line contains "3 passed".
    - `grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l` outputs exactly `1`.
    - The single grep match is `tests/_fake_player.py:37`.
    - `grep -rn "class FakePlayer(QObject)" tests/ | wc -l` outputs `2` (recorded as the justification for the SC1 fix).
    - `uv run pytest tests/test_constants_drift.py::test_richtext_baseline_unchanged_by_phase_71` output contains "1 passed".
    - The full-suite `uv run pytest tests/ -q` passed-count N is captured verbatim (the SC2 baseline anchor).
    - Zero files under tests/ or musicstreamer/ are modified (`git status --porcelain tests/ musicstreamer/` is empty).
  </acceptance_criteria>
  <done>All seven D-08 verification captures recorded verbatim for the SUMMARY; the MPRIS2 closure is confirmed green on this host; the SC2 baseline passed-count N is captured; no test/production files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the miswritten SC1 grep in the ROADMAP Phase 91 block (D-03/D-04)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 91 detail block, lines 72-84; SC1 line is line 79)
    - .planning/phases/91-fix-mpris-phase-77-deferred-mpris2-tests/91-CONTEXT.md (D-03, D-04, D-05 — the SC1 grep is provably wrong and the locked replacement form)
    - tests/test_fake_player_no_inline.py (line 28 — the docstring literal that makes the old grep return 2; READ-ONLY, do NOT edit per D-05)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    In `.planning/ROADMAP.md`, Phase 91 detail block, replace the SC1 line (line 79).

    CURRENT (returns 2, can never pass):
    `  1. \`grep -rn "class FakePlayer(QObject)" tests/ | wc -l\` returns exactly 1 and the only declaration site is \`tests/_fake_player.py\` (Pitfall 15 verified at source level before any test edits).`

    REPLACE WITH (D-04 anchored PCRE form, returns 1):
    `  1. \`grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l\` returns exactly 1 and the only declaration site is \`tests/_fake_player.py:37\` (Pitfall 15 verified at source level; anchored to a real class declaration so the docstring literal in \`tests/test_fake_player_no_inline.py:28\` no longer false-positives — D-04).`

    Do NOT edit `tests/test_fake_player_no_inline.py` to remove its docstring literal
    (D-05 — the runtime drift-guard at line 50 already enforces the invariant
    programmatically; the docstring is the correct English documentation of the rule).
    The fix is ROADMAP-only.
  </action>
  <verify>
    <automated>grep -c "grep -rnP '\^" .planning/ROADMAP.md; grep -c 'grep -rn "class FakePlayer(QObject)" tests/ | wc -l' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The ROADMAP Phase 91 SC1 line contains the literal `grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:'`.
    - The ROADMAP no longer contains the bare miswritten form `grep -rn "class FakePlayer(QObject)" tests/ | wc -l` in the Phase 91 SC1 line (the count of that exact string drops to 0).
    - `tests/test_fake_player_no_inline.py` is byte-identical (`git status --porcelain tests/test_fake_player_no_inline.py` is empty).
    - The replacement SC1 grep, when run, outputs `1` (re-confirm: `grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l` == 1).
  </acceptance_criteria>
  <done>The ROADMAP Phase 91 SC1 line carries the D-04 anchored PCRE grep that honestly returns 1; the docstring literal in tests/ is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Flip statuses + refresh SC2 baseline + retrospective note (D-06/D-10/D-11)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 97-99 requirement checkboxes; lines 213-215 traceability rows for FIX-MPRIS-01/02/03)
    - .planning/ROADMAP.md (line 30 Phase 91 checkbox; line 80 SC2 "1462-test baseline" phrase)
    - .planning/RETROSPECTIVE.md (end of file — D-11 append point)
    - .planning/phases/91-fix-mpris-phase-77-deferred-mpris2-tests/91-CONTEXT.md (D-06, D-10, D-11)
    - Task 1 captured baseline passed-count N (the SC2 anchor)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/RETROSPECTIVE.md</files>
  <action>
    Make the bookkeeping flips. Use the baseline passed-count N captured in Task 1.

    A. REQUIREMENTS.md status flips (D-10), in BOTH locations:
       - Requirement-list checkboxes: change line 97 `- [ ] **FIX-MPRIS-01**` ->
         `- [x] **FIX-MPRIS-01**`; same for FIX-MPRIS-02 (line 98) and FIX-MPRIS-03
         (line 99).
       - Traceability table: change lines 213-215 from
         `| FIX-MPRIS-0X | Phase 91 | Pending |` to
         `| FIX-MPRIS-0X | Phase 91 | Complete |` for X = 1, 2, 3.

    B. ROADMAP.md checkbox flip (D-10): change line 30 from
       `- [ ] **Phase 91: FIX-MPRIS ...` to `- [x] **Phase 91: FIX-MPRIS ...`
       (preserve the rest of the line verbatim).

    C. ROADMAP.md SC2 baseline refresh (D-06): in the Phase 91 detail block SC2 line
       (line 80), replace the stale `1462-test baseline` with the current count.
       New SC2 wording: `... no test-runtime regressions vs. the Phase 91 pre-phase
       baseline of N passed (captured 2026-06-02); verification asserts
       passed >= N, never shrinkage.` — substitute the actual N from Task 1. Per
       D-06 this is no semantic change; the criterion always asserted "no MPRIS
       test-runtime regressions vs. baseline".

    D. RETROSPECTIVE.md note (D-11): append a new dated subsection at end of file.
       One line of substance:
       "Phase 91 (FIX-MPRIS) closed as bookkeeping-only: verified Phase 77 close-out
       commit 378440c still holds (MPRIS2 cluster-2: 12 passed, 1 skipped, 0 failed),
       corrected the miswritten SC1 grep to an anchored PCRE form, refreshed the SC2
       baseline to N passed, and flipped FIX-MPRIS-01/02/03 to Complete. No test or
       production-code changes." Use a heading like
       `## Phase 91 — FIX-MPRIS Close-Out (2026-06-02)`.
  </action>
  <verify>
    <automated>grep -c "FIX-MPRIS-0[123] | Phase 91 | Complete" .planning/REQUIREMENTS.md; grep -c '\- \[x\] \*\*Phase 91:' .planning/ROADMAP.md; grep -c "378440c" .planning/RETROSPECTIVE.md; grep -c "1462-test baseline" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FIX-MPRIS-0[123] | Phase 91 | Complete" .planning/REQUIREMENTS.md` outputs `3`.
    - `grep -c "FIX-MPRIS-0[123] | Phase 91 | Pending" .planning/REQUIREMENTS.md` outputs `0`.
    - REQUIREMENTS.md lines 97-99 each begin `- [x] **FIX-MPRIS-0X**`.
    - `.planning/ROADMAP.md` line 30 begins `- [x] **Phase 91: FIX-MPRIS`.
    - `grep -c "1462-test baseline" .planning/ROADMAP.md` outputs `0` (stale phrase removed).
    - The ROADMAP Phase 91 SC2 line contains the captured baseline passed-count N and the phrase `passed >= ` (the never-shrinkage assertion).
    - `.planning/RETROSPECTIVE.md` contains a Phase 91 heading and the string `378440c`.
  </acceptance_criteria>
  <done>FIX-MPRIS-01/02/03 are Complete in both REQUIREMENTS locations; the ROADMAP Phase 91 checkbox is - [x]; the SC2 baseline reflects the current count; the RETROSPECTIVE records the close-out citing 378440c with no test/code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced by this phase. No code, no network endpoints, no auth paths, no
file-access patterns, no schema changes. The phase modifies only `.planning/`
markdown documents and runs the test suite read-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-NA | N/A | `.planning/` doc edits + read-only pytest runs | accept | No new attack surface: zero production-code, zero network, zero auth, zero file-access, zero schema changes. Bookkeeping-only close-out per CONTEXT.md `<deferred>` "Out of scope". |

No `T-91-SC` supply-chain entry: this phase installs no packages. The pytest runs
use the existing pinned `uv` environment with no new dependencies.
</threat_model>

<verification>
Phase-level checks (all read-only or `.planning/`-only):

1. MPRIS2 closure green: `uv run pytest tests/test_media_keys_mpris2.py` → 12 passed, 1 skipped, 0 failed.
2. Drift-guards green: `uv run pytest tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py` → 3 passed.
3. SC3 guard green: `uv run pytest tests/test_constants_drift.py::test_richtext_baseline_unchanged_by_phase_71` → 1 passed.
4. SC1 honest: `grep -rnP '^\s*class\s+FakePlayer\s*\(\s*QObject\s*\)\s*:' tests/ | wc -l` → 1.
5. ROADMAP SC1 line carries the anchored PCRE form; old bare form gone.
6. SC2 baseline refreshed; `1462-test baseline` phrase removed.
7. FIX-MPRIS-01/02/03 = Complete (both REQUIREMENTS locations); Phase 91 checkbox = - [x].
8. RETROSPECTIVE note present citing 378440c.
9. `git status --porcelain tests/ musicstreamer/` empty (scope fence honored).
</verification>

<success_criteria>
- The 7 Phase-77-deferred MPRIS2 tests verified green on this host (D-08 evidence captured).
- ROADMAP Phase 91 SC1 grep is the D-04 anchored form and honestly returns 1.
- ROADMAP Phase 91 SC2 baseline reflects the current pre-phase passed-count (no `1462`).
- FIX-MPRIS-01, FIX-MPRIS-02, FIX-MPRIS-03 flipped Pending → Complete.
- Phase 91 ROADMAP checkbox flipped - [ ] → - [x].
- RETROSPECTIVE.md one-line close-out note citing commit 378440c, no test/code changes.
- Zero edits under tests/ or musicstreamer/ (hard scope fence).
</success_criteria>

<output>
Create `.planning/phases/91-fix-mpris-phase-77-deferred-mpris2-tests/91-01-SUMMARY.md`
when done. Include the verbatim Task 1 verification captures (all seven), the
captured SC2 baseline passed-count N, the cited Phase 77-VERIFICATION.md "Truth 5"
reference, and confirmation that no test/production files were modified.
</output>
