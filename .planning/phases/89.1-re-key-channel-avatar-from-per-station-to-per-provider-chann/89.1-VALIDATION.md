---
phase: 89.1
slug: re-key-channel-avatar-from-per-station-to-per-provider-chann
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-16
---

# Phase 89.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml / pytest.ini (existing) |
| **Quick run command** | `.venv/bin/python -m pytest tests/test_repo_migration.py tests/test_cover_art_avatar.py -q` |
| **Full suite command** | `.venv/bin/python -m pytest -q` |
| **Estimated runtime** | quick ~30s · full >600s (scope per project memory) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command (migration + avatar-lookup tests)
- **After every plan wave:** Run scoped tests for the touched modules (`tests/test_repo*`, `tests/test_cover_art*`, `tests/test_assets*`)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds (scoped); full suite reserved for pre-verify

---

## Per-Task Verification Map

> Populated/refined by the planner. Concrete tasks come from PLAN.md; this is the validation skeleton.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 89.1-01-xx | 01 | 1 | D-11 (providers.avatar_path ALTER) | T-89.1-01 | additive idempotent migration; double-run no-op | unit | `.venv/bin/python -m pytest tests/test_repo_migration.py -q` | ❌ W0 | ⬜ pending |
| 89.1-01-xx | 01 | 1 | D-01/02/03 (backfill + clean orphans) | T-89.1-02 | most-recent sibling wins; old file deleted only after commit; crash-safe (copy not rename) | unit | `.venv/bin/python -m pytest tests/test_repo_migration.py -q` | ❌ W0 | ⬜ pending |
| 89.1-02-xx | 02 | 2 | D-04/05/06 (resolve via provider) | — | provider avatar resolved via provider_id; NULL→thumbnail; not-yet-fetched→existing behavior | unit | `.venv/bin/python -m pytest tests/test_cover_art_avatar.py -q` | ❌ W0 | ⬜ pending |
| 89.1-02-xx | 02 | 2 | D-09/10 (provider-keyed persist + storage) | T-89.1-03 | atomic write; {provider_id}.png; not routed through broad update | unit | `.venv/bin/python -m pytest tests/test_assets_avatar.py tests/test_repo_provider_avatar.py -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_repo_migration.py` — extend with providers.avatar_path additive-ALTER idempotency + backfill correctness (most-recent wins, orphan deleted, provider_id-NULL skipped)
- [ ] `tests/test_cover_art_avatar.py` — re-point `_channel_avatar_lookup` mocks from `channel_avatar_path` to `provider_avatar_path` with `spec=` to prevent silent MagicMock auto-creation (RESEARCH finding 5)
- [ ] `tests/test_assets_avatar.py` / `tests/test_repo_provider_avatar.py` — provider-keyed write + persist coverage

*Existing pytest infrastructure covers the framework; only new test cases needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| EditStationDialog reuse-on-open shows provider avatar without network fetch | D-07 | Qt dialog + live network; auto-fetch worker is GUI/thread bound | Open Edit on a station whose provider already has an avatar; confirm preview populates instantly and no fetch worker fires |
| "Refresh avatar" updates all sibling stations + shared-effect hint visible | D-08 | Cross-station visual effect across now-playing rebinds | Refresh avatar on one Lofi Girl stream; confirm a sibling stream's now-playing avatar updates; confirm the shared-effect hint text/tooltip is present |
| Upgrade backfill preserves avatars with zero refetch | D-01 | Requires real pre-89.1 on-disk DB + files | On an upgraded DB with existing per-station avatars, launch app; confirm avatars still render and channel-avatars/ holds {provider_id}.png (no {station_id}.png orphans) |

*Migration logic itself is automated; only the Qt/visual and upgrade-on-real-data behaviors are manual.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (scoped)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
