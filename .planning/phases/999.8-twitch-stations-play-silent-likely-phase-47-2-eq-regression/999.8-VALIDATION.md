---
phase: 999.8
slug: twitch-stations-play-silent-likely-phase-47-2-eq-regression
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-24
---

# Phase 999.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing) |
| **Config file** | tests/conftest.py (existing) |
| **Quick run command** | `uv run --with pytest pytest tests/test_player.py -x` |
| **Full suite command** | `uv run --with pytest pytest` |
| **Estimated runtime** | ~30 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `uv run --with pytest pytest tests/test_player.py -x`
- **After every plan wave:** Run `uv run --with pytest pytest`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-01..D-09 | — | N/A (local-only audio fix) | unit + manual harness | see plans | ⬜ W0 pending | ⬜ pending |

*Populated by gsd-planner from PLAN.md task IDs. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_player.py` — extend with caps-negotiation unit test (D-03)
- [ ] `scripts/eq_audit.sh` OR `tests/integration/audio_filter_smoke.py` — manual matrix harness (D-04)
- [ ] `tests/integration/audio_filter_urls.example.txt` — gitignored real `audio_filter_urls.txt` template per researcher recommendation

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 15-cell stream × EQ-state matrix produces audio in every cell | D-02 | Requires live network + real station URLs (Twitch/YouTube/ShoutCast/AudioAddict/Radio-Browser) | Populate `tests/integration/audio_filter_urls.txt` from live station list, run `scripts/eq_audit.sh` (or `python tests/integration/audio_filter_smoke.py`), expect non-zero exit on any silent cell |
| Preamp axis (-30, 0, +12 dB) audible across all stream types | D-02 | Live audio | Run harness with `--preamp` axis enabled |
| Band-count axis (1, 5, 10, 20+) audible after `_rebuild_eq_element` NULL transition | D-02 | Live audio + state transition | Run harness with `--bands` axis enabled |
| In-app Twitch playback returns audio after fix | D-06 | Requires GUI launch + listening | Launch app, play Twitch station, confirm audio is audible |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
