---
phase: 60.4-two-informationals-1-under-search-show-the-amount-of-tokens-
verified: 2026-05-07T22:29:25Z
status: passed
score: 13/13 D-NN must-haves verified
overrides_applied: 0
re_verification:
  is_re_verification: false
---

# Phase 60.4: Two GBS.FM Informationals — Verification Report

**Phase Goal (from CONTEXT.md `<domain>`):** Surface two GBS.FM playlist-state pieces — (1) per-user token counter in the search dialog, color-tiered with optimistic decrement; (2) `state["queue_summary"]` rendered verbatim as the first row of the active-playlist widget (Phase 60-10 D-10c REVERSED — gbs.fm jargon preserved per user discussion 2026-05-07).

**Verified:** 2026-05-07T22:29:25Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## D-NN Decision Verification (13/13)

Each decision is mapped to live-codebase evidence (file:line) and the test that locks it.

| # | Decision | Status | Evidence |
|---|----------|--------|----------|
| D-T1 | Refetch on dialog open + after every successful submit; no periodic poll | VERIFIED | `gbs_search_dialog.py:480-481` (False→True transition kick in `_refresh_login_gate`); `gbs_search_dialog.py:1104-1106` (`_on_submit_finished` success branch kick); no `QTimer`/poll for tokens (`grep` returned only `_kick_token_worker` matches at lines 481/485/1106). Tests: `test_token_refetch_on_dialog_open`, `test_token_refetch_after_submit_success` (PASS) |
| D-T2 | On any fetch failure → `Tokens: —` + clear stylesheet + silent log; NO toast | VERIFIED | `gbs_search_dialog.py:535-540` (non-`auth_expired` branch in `_on_token_error`: `_log.warning(...)`, `setText("Tokens: —")`, `setStyleSheet("")`, no `_toast`). Test `test_token_fetch_parse_miss_shows_emdash` asserts no toast capture (PASS) |
| D-T3 | Optimistic decrement on submit success (48→47), then server-confirm | VERIFIED | `gbs_search_dialog.py:1100-1106` — `_last_known_tokens = max(0, _last_known_tokens - 1)` ⇒ `setText(f"Tokens: {n}")` ⇒ `_apply_token_color(n)` ⇒ `_kick_token_worker(cookies)`. Test `test_token_optimistic_decrement_on_submit` asserts decrement to 4 from 5 (PASS) |
| D-T4 | `_token_label` placed in top row, right of `_breadcrumb_label` | VERIFIED | `gbs_search_dialog.py:351-358` — `top_row.addWidget(self._breadcrumb_label)` (line 351) then `self._token_label = QLabel(...)` (355) and `top_row.addWidget(self._token_label)` (358) before `root.addLayout(top_row)` (359). Test `test_token_label_placed_after_breadcrumb` asserts shared parent + PlainText (PASS) |
| D-T5 | Compact `Tokens: N` wording | VERIFIED | `gbs_search_dialog.py:512` (`f"Tokens: {int(count)}"`); `gbs_search_dialog.py:1102` (`f"Tokens: {self._last_known_tokens}"`); `gbs_search_dialog.py:532, 538, 1124` (`"Tokens: —"`). Test `test_token_label_format_compact` asserts text equals `"Tokens: 48"` (PASS) |
| D-T6 | Three-tier color (red 0 / amber 1–3 / default 4+ AND `—`) | VERIFIED | `gbs_search_dialog.py:542-556` `_apply_token_color`: `None` → `""`, `0` → `ERROR_COLOR_HEX`, `1≤n≤3` → `WARNING_COLOR_HEX`, `n≥4` → `""`. Theme constant: `_theme.py:48` `WARNING_COLOR_HEX: str = "#d4a017"`. Tests: `test_token_color_tier_red_at_zero`, `test_token_color_tier_amber_1_to_3`, `test_token_color_tier_default_4plus`, `test_token_color_tier_default_for_emdash` (4 PASS) |
| D-T7 | `_token_label.setVisible(False)` while login gate is active | VERIFIED | `gbs_search_dialog.py:472-473` (`setVisible(logged_in)` in `_refresh_login_gate`). Constructor `_token_label.setVisible(False)` at line 357 covers cold-start. Test `test_token_label_hidden_when_logged_out` asserts `isHidden() is True` after no-cookies construction (PASS) |
| D-T8 | `Tokens: —` flip + `_refresh_login_gate()` on auth-expired mid-session | VERIFIED | `gbs_search_dialog.py:1117-1127` (`_on_submit_error` `auth_expired` branch): `_last_known_tokens = None` ⇒ `setText("Tokens: —")` ⇒ `setStyleSheet("")` ⇒ `_toast(...)` ⇒ `_refresh_login_gate()`. Order documented as load-bearing in code comment. Test `test_token_label_flips_to_emdash_on_auth_expired` asserts the full sequence + toast capture (PASS) |
| D-S1 | Summary line as first row of `_gbs_playlist_widget`, ABOVE `▶ {icy}` | VERIFIED | `now_playing_panel.py:1115` (`_gbs_playlist_widget.clear()`) → `1132-1134` (`queue_summary` block) → `1136-1139` (`▶ {icy}` block). Order: clear → summary → ▶ → queue rows → Score. Test `test_gbs_summary_row_renders_at_index_0` asserts index 0 starts with `· ` and contains queue_summary text; index 1 is the `▶` row (PASS) |
| D-S2 | Render verbatim — gbs.fm jargon preserved (Phase 60-10 D-10c REVERSED) — citation in plan, code comment, test comment | VERIFIED | (a) Plan body `60.4-03-PLAN.md` line 13 ("D-S2 # jargon policy — REVERSAL of Phase 60-10 D-10c"), line 47-49 (full reversal block). (b) In-code comment `now_playing_panel.py:1117` (`# REVERSES Phase 60-10 D-10c per user discussion 2026-05-07`). (c) Test comment `tests/test_now_playing_panel.py:1067` (`# Phase 60.4 D-S2 / D-S3 — REVERSAL of Phase 60-10 D-10c`). Production code is a single `addItem(QListWidgetItem(f"· {queue_summary}"))` with NO sanitization function. Tests: `test_gbs_summary_preserves_jargon_verbatim` asserts `"dongs"` survives in rendered widget text; `test_gbs_playlist_populates_from_mock_state` polarity flipped from `assert not any(...)` to `assert any("· Playlist is 11:21" in t ...)` (line 1072). All PASS |
| D-S3 | `· ` (U+00B7 middle-dot) + space prefix on summary row | VERIFIED | `now_playing_panel.py:1134` literal `f"· {queue_summary}"` — U+00B7 is the middle-dot character. Test `test_gbs_summary_row_has_middle_dot_prefix` asserts `item_0.startswith("· ")` (PASS) |
| D-S4 | Skip summary row when `state.get("queue_summary")` is falsy | VERIFIED | `now_playing_panel.py:1132-1133` (`queue_summary = state.get("queue_summary")` followed by `if queue_summary:` — covers `None`, empty string, post-`.strip()` empty payload at `gbs_api.py:287`). Test `test_gbs_summary_skipped_when_queue_summary_absent` covers two cases (key absent + empty value); both confirm no `· ` row exists and `▶ ` is at index 0 (PASS) |
| D-S5 | Summary row inherits `_gbs_playlist_widget` visibility (no separate visibility code) | VERIFIED | No new visibility code added in 60.4. Existing auth-expired hide path: `now_playing_panel.py:1171-1175` — `_on_gbs_playlist_error('auth_expired')` calls `_gbs_playlist_widget.setVisible(False)` + `clear()`, hiding the summary row symmetrically with all other rows. Existing test `test_gbs_auth_expired_hides_widget_no_toast` continues GREEN (PASS) — no new test required, inheritance is structural |

**Score: 13/13 D-NN VERIFIED**

---

## Invariant Checks

| Invariant | Expected | Actual | Status |
|-----------|----------|--------|--------|
| T-40-04 plain-text invariant: `git grep -E "setTextFormat\(.*RichText\|setHtml\|TextFormat\.RichText" musicstreamer/ui_qt/ \| wc -l` | 2 (pre-existing baseline; 60.4 is purely PlainText additions) | 2 (`edit_station_dialog.py` + `now_playing_panel.py` — both pre-existing `_sibling_label.setTextFormat(Qt.RichText)`) | PASS |
| `grep -c "def _on_token_fetched" musicstreamer/ui_qt/gbs_search_dialog.py` | 1 | 1 | PASS |
| `grep -c "def _on_token_error" musicstreamer/ui_qt/gbs_search_dialog.py` | 1 | 1 | PASS |
| `grep -c "fetch_user_tokens" tests/conftest.py` | ≥ 1 | 1 (line 60: `"fetch_user_tokens",  # Phase 60.4 D-T1: token-count scraper`) | PASS |
| `grep -c "WARNING_COLOR_HEX" musicstreamer/ui_qt/_theme.py` | ≥ 1 | 2 (docstring mention + constant declaration at line 48) | PASS |
| `grep -nE "if logged_in and not prev_logged_in" musicstreamer/ui_qt/gbs_search_dialog.py \| wc -l` (transition guard) | 1 | 1 | PASS |
| `grep -nE "finished = Signal\(int, int\)\|error = Signal\(int, str\)" musicstreamer/ui_qt/gbs_search_dialog.py \| wc -l` (signal arity) | 2 | 2 | PASS |
| Pitfall A: NO `self.sender()` in `_on_token_fetched`/`_on_token_error` (positional guard) | 0 matches | 0 matches | PASS |
| `from musicstreamer.ui_qt._theme import WARNING_COLOR_HEX` succeeds AND equals `'#d4a017'` | OK | OK | PASS |
| `from musicstreamer.ui_qt.gbs_search_dialog import _GbsTokenWorker` succeeds | OK | OK | PASS |
| `from musicstreamer.gbs_api import fetch_user_tokens, _TOKEN_RE` succeeds | OK | OK | PASS |
| QA-05 lambda-in-connect lock test (`test_no_self_capturing_lambdas_in_dialog`) | PASS | PASS | PASS |

---

## Phase 60-10 D-10c Reversal Citation Audit

The reversal MUST appear in three places (D-S2 specific):

| Location | Required Phrasing | Verified |
|----------|-------------------|----------|
| Plan body `60.4-03-PLAN.md` | `REVERSAL of Phase 60-10 D-10c` | YES (line 13: "D-S2 # jargon policy — REVERSAL of Phase 60-10 D-10c"; line 22, 43, 47-49 expand the rationale; line 208 prescribes the test-comment rewrite) |
| In-code comment near new `addItem` block in `now_playing_panel.py::_on_gbs_playlist_ready` | `REVERSES Phase 60-10 D-10c` | YES (line 1117: `# REVERSES Phase 60-10 D-10c per user discussion 2026-05-07: gbs.fm`; back-reference at 1152) |
| Test comment near flipped polarity assertion in `tests/test_now_playing_panel.py` | `REVERSAL of Phase 60-10 D-10c` | YES (line 1067: `# Phase 60.4 D-S2 / D-S3 — REVERSAL of Phase 60-10 D-10c per user`; immediately above the flipped assertion at line 1072: `assert any("· Playlist is 11:21" in t for t in items)`) |

All three citation locations VERIFIED. Future readers of any of the three artifacts will see the explicit reversal context.

---

## Test Suite Status

`pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py tests/test_now_playing_panel.py -x --no-header -q`

**Result: 190 passed, 5 warnings in 1.16s** — exact match with the prompt's invariant expectation (190 passed quick-loop GREEN).

Per-class focused runs:

| Test Group | Result |
|------------|--------|
| `tests/test_gbs_api.py::TestFetchUserTokens` (Plan 01 — 6 tests) | 6 passed |
| `tests/test_gbs_search_dialog.py -k "token"` (Plan 02 — 13 new + 1 pre-existing matching `-k "token"`) | 14 passed (13 new tests + `test_submit_inline_error_on_token_quota` regression-lock) |
| Plan 03 token tests (`test_gbs_summary_*` + polarity flip + auth-expired inheritance) | 6 passed |
| `test_no_self_capturing_lambdas_in_dialog` (QA-05 invariant) | 1 passed |

Pre-existing failures listed in `deferred-items.md` (D-Bus name collision in MPRIS test, Qt fatal in `test_import_dialog_qt` mid-suite, `station_list_panel` + `twitch_auth` assertion failures, `edit_station_dialog` flake) are NOT Phase 60.4 regressions and were verified pre-existing on the pre-60.4 baseline by both Plan 01 and Plan 03 executors. Tracked properly per the `<deviation_rules>` SCOPE BOUNDARY convention.

---

## File Modification Audit

Files modified per Phase 60.4 contract:

| File | Plan | Verified |
|------|------|----------|
| `musicstreamer/gbs_api.py` | 01 | YES — `_TOKEN_RE` (line 218) + `fetch_user_tokens` (line 293) + section header at line 291 |
| `musicstreamer/ui_qt/_theme.py` | 02 | YES — `WARNING_COLOR_HEX = "#d4a017"` at line 48 + docstring extension lines 15-19, 30-34 |
| `musicstreamer/ui_qt/gbs_search_dialog.py` | 02 | YES — `_GbsTokenWorker` class (line 219), `_token_label` (line 355), `_refresh_login_gate` extension (lines 449-481), `_kick_token_worker` (485), `_on_token_fetched` (501), `_on_token_error` (516), `_apply_token_color` (542), submit-success branch (1095-1107), auth-expired flip (1117-1127), import (line 49) |
| `musicstreamer/ui_qt/now_playing_panel.py` | 03 | YES — single guarded `addItem` at lines 1132-1134 + comment block 1116-1135 + back-reference at 1152 |
| `tests/test_gbs_api.py` | 01 | YES — `TestFetchUserTokens` class (line 934, 6 tests) |
| `tests/test_gbs_search_dialog.py` | 02 | YES — `dialog_logged_in` fixture extension (lines 51, 56), `_stub_token_worker` helper (820-829), 13 token tests (832-1012) |
| `tests/test_now_playing_panel.py` | 03 | YES — polarity flip at line 1072 + comment rewrite at 1067 + 4 new tests (`test_gbs_summary_row_renders_at_index_0` 2389, `test_gbs_summary_preserves_jargon_verbatim` 2435, `test_gbs_summary_row_has_middle_dot_prefix` 2471, `test_gbs_summary_skipped_when_queue_summary_absent` 2501) |
| `tests/conftest.py` | 01 | YES — `mock_gbs_api` `spec=[...]` extended with `"fetch_user_tokens"` (line 60) |

All 8 in-scope files modified per contract; no out-of-scope code touched.

---

## Anti-Pattern Scan

No blocker anti-patterns found in 60.4 modifications:

- No `TODO`/`FIXME`/`XXX`/`HACK` introduced in production code (the existing `mpris2.py:258` TODO is pre-existing per `deferred-items.md`).
- No empty `return null`/`return {}`/`return []` stubs in new functions; `fetch_user_tokens` always returns `Optional[int]` with explicit semantics.
- No hardcoded empty render data — `queue_summary` renders only when truthy; `_token_label` text is always derived from real data or the documented `—` placeholder.
- No `console.log`-only handlers; all error paths use `_log.warning(...)` (Pitfall 3 — silent log; no toast spam).
- No lambda captures in QA-05 critical paths; bound-method connections verified by the existing `test_no_self_capturing_lambdas_in_dialog` regression-lock.

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Module imports succeed | `python3 -c "from musicstreamer.gbs_api import fetch_user_tokens, _TOKEN_RE; from musicstreamer.ui_qt._theme import WARNING_COLOR_HEX; from musicstreamer.ui_qt.gbs_search_dialog import _GbsTokenWorker"` | exit 0 | PASS |
| `WARNING_COLOR_HEX` matches contract | `python3 -c "from musicstreamer.ui_qt._theme import WARNING_COLOR_HEX; assert WARNING_COLOR_HEX == '#d4a017'"` | exit 0 | PASS |
| Quick-loop test triplet exits clean | `pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py tests/test_now_playing_panel.py -x --no-header -q` | 190 passed | PASS |
| Token-class focused run | `pytest tests/test_gbs_api.py::TestFetchUserTokens -x` | 6 passed | PASS |
| Token-UI focused run | `pytest tests/test_gbs_search_dialog.py -k "token" --no-header -q` | 14 passed | PASS |

---

## Gaps Summary

**No gaps found.** All 13 D-NN decisions are observably true in the live codebase, all 12 invariant checks pass, the Phase 60-10 D-10c reversal citation is present in all three required locations (plan, code, test), and the quick-loop test suite is GREEN at 190 passed.

The pre-existing environmental test failures listed in `deferred-items.md` are out-of-scope per the `<deviation_rules>` SCOPE BOUNDARY convention and were independently verified pre-existing on the pre-60.4 baseline by the Plan 01 and Plan 03 executors. They will be addressed in a future stabilization phase (likely 60.5+).

The Plan 02 worktree-discipline deviation (one commit briefly landing on `main` instead of the worktree branch, recovered via `git merge --ff-only main`) is documented in `60.4-02-SUMMARY.md` Deviations §1; the end-state of the codebase is exactly what Plan 02 prescribed, so it is a process-only deviation with no code-level impact.

---

## VERIFICATION PASSED

All 13 D-NN satisfied, all invariants preserved, no gaps. **Phase 60.4 ready to ship.**

---

*Verified: 2026-05-07T22:29:25Z*
*Verifier: Claude (gsd-verifier, goal-backward methodology)*
