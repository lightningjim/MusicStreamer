---
phase: 56-windows-di-fm-smtc-start-menu
plan: 04
type: execute
wave: 2
depends_on: ["56-03"]
files_modified:
  - tests/test_aumid_string_parity.py
  - packaging/windows/README.md
  - musicstreamer/__main__.py
  - packaging/windows/MusicStreamer.iss
autonomous: false
requirements: [WIN-02]
tags: [windows, smtc, aumid, drift-guard, docs, conditional-fix]

must_haves:
  truths:
    - "Plan branches based on Plan 56-03 D-09 classification: docs-only (D-09 #1/#2), code-fix (D-09 #3), or investigate (D-09 #4) (D-10)"
    - "AUMID string drift guard pytest exists at tests/test_aumid_string_parity.py and passes (D-09 #3 prevention; T-56-02 mitigation)"
    - "Drift guard reads BOTH musicstreamer/__main__.py and packaging/windows/MusicStreamer.iss as text and asserts the literal AUMID strings match"
    - "Drift guard is Linux-CI-safe — no Windows-only dependencies, no subprocess calls"
    - "If D-09 #1 or #2: README diff in packaging/windows/README.md instructs users to launch via Start Menu shortcut, not python -m musicstreamer (Pitfall #4 mitigation)"
    - "If D-09 #3: musicstreamer/__main__.py and packaging/windows/MusicStreamer.iss AUMID literals are aligned and the drift guard would have caught the prior drift"
    - "If D-09 #4: investigation note appended to 56-03-DIAGNOSTIC-LOG.md with next-step proposal (may include splitting WIN-02 into a follow-up phase)"
  artifacts:
    - path: "tests/test_aumid_string_parity.py"
      provides: "Linux-CI pytest that fails if __main__.py AUMID literal != MusicStreamer.iss AUMID literal"
      contains: "def test_aumid_string_parity"
    - path: "packaging/windows/README.md"
      provides: "User-facing note: launch via Start Menu shortcut for SMTC binding (Pitfall #4 mitigation)"
      contains: "Start Menu shortcut"
  key_links:
    - from: "tests/test_aumid_string_parity.py"
      to: "musicstreamer/__main__.py"
      via: "Path('musicstreamer/__main__.py').read_text() + regex extract"
      pattern: "musicstreamer/__main__.py"
    - from: "tests/test_aumid_string_parity.py"
      to: "packaging/windows/MusicStreamer.iss"
      via: "Path('packaging/windows/MusicStreamer.iss').read_text() + regex extract"
      pattern: "packaging/windows/MusicStreamer.iss"
---

<objective>
Conditional WIN-02 fix + permanent regression protection. This plan reads `56-03-SUMMARY.md` to determine its scope per D-10:

1. **D-09 #1 (stale shortcut)** OR **D-09 #2 (launch path)** → docs-only path. Add the AUMID drift-guard pytest (always — RESEARCH.md Open Question #1 recommendation) + the README launch-discipline note. No production code change.

2. **D-09 #3 (string drift)** → code-fix path. Align the AUMID literals in `__main__.py` and `MusicStreamer.iss` (whichever drifted) + add the drift guard pytest + README note. Rebuild the installer (Plan 56-05 will UAT).

3. **D-09 #4 (unknown root cause)** → investigate path. Append findings to `56-03-DIAGNOSTIC-LOG.md`; Plan 56-05 may need to split WIN-02 to a follow-up phase. The drift guard pytest still ships as zero-cost regression protection.

The drift-guard pytest (`tests/test_aumid_string_parity.py`) ships in ALL THREE branches because it's a 10-line Linux-CI test with permanent value (T-56-02 mitigation, Pitfall #6).

Purpose: closes WIN-02 with the smallest correct change indicated by Plan 03's diagnostic + ships permanent regression protection against future AUMID literal drift.
Output: drift-guard pytest + README note (always); AUMID literal alignment + installer rebuild (only if D-09 #3); investigation note (only if D-09 #4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-PATTERNS.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md
@musicstreamer/__main__.py
@packaging/windows/MusicStreamer.iss
@packaging/windows/README.md
@.claude/skills/spike-findings-musicstreamer/SKILL.md

<interfaces>
<!-- Source-of-truth literal locations (regex anchors for the drift guard): -->

```python
# musicstreamer/__main__.py line 99:
def _set_windows_aumid(app_id: str = "org.lightningjim.MusicStreamer") -> None:
```

```inno
; packaging/windows/MusicStreamer.iss line 71:
    AppUserModelID: "org.lightningjim.MusicStreamer"
```

<!-- Drift guard regexes (RESEARCH.md Pitfall #6 verbatim) -->
<!-- main_match = re.search(r'app_id:\s*str\s*=\s*"([^"]+)"', main_py) -->
<!-- iss_match = re.search(r'AppUserModelID:\s*"([^"]+)"', iss) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Read 56-03 SUMMARY and confirm branch</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md (Plan 03 outcome)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (raw readouts)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-09 shortlist + D-10 scope rule)
  </read_first>
  <what-built>
    Branch determination — confirms which of the three paths (docs-only / code-fix / investigate) this plan executes.
  </what-built>
  <how-to-verify>
    Read `56-03-SUMMARY.md`. Locate the D-09 classification (#1/#2/#3/#4). Map to branch:
    - D-09 #1 or #2 → DOCS-ONLY path (skip Task 4 code-fix subtasks)
    - D-09 #3 → CODE-FIX path (execute Task 4)
    - D-09 #4 → INVESTIGATE path (execute Task 5 instead of Task 4)

    Tasks 2 (drift guard pytest) and 3 (README note) execute in ALL THREE branches.
  </how-to-verify>
  <resume-signal>
    Reply with: "Branch: <docs-only|code-fix|investigate>; D-09: #<1|2|3|4>; tasks to execute: 2, 3, and <none|4|5>".
  </resume-signal>
  <action>Read 56-03-SUMMARY.md, locate the D-09 classification, and announce which branch (docs-only / code-fix / investigate) this plan will execute. Tasks 2 + 3 always run; Task 4 runs only on code-fix; Task 5 runs only on investigate.</action>
  <verify>
    <automated>test -f .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md && grep -E 'docs-only|code-fix|investigate' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md</automated>
  </verify>
  <done>Branch decision recorded in resume-signal; executor knows which of Tasks 4 / 5 to skip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AUMID string parity drift-guard pytest (ALL BRANCHES)</name>
  <files>tests/test_aumid_string_parity.py</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-09 #3 prevention; Claude's Discretion item)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Common Pitfalls → Pitfall 6: AUMID-string drift; Open Question #1 recommendation to ship)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-PATTERNS.md ("tests/test_aumid_string_parity.py — file-text regex parity" section, especially the verbatim recommended pattern)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md (Per-Task Verification Map task 56-03-01)
    - musicstreamer/__main__.py (line 99 — confirm `app_id: str = "org.lightningjim.MusicStreamer"` exact format)
    - packaging/windows/MusicStreamer.iss (line 71 — confirm `AppUserModelID: "org.lightningjim.MusicStreamer"` exact format)
  </read_first>
  <behavior>
    - test_aumid_string_parity reads both source files as text, extracts each AUMID literal via regex, and asserts they match.
    - If `__main__.py` literal is changed (e.g., to `org.lightningjim.MusicStreamer2`), the test fails with a diff message naming both literal values.
    - If `MusicStreamer.iss` literal is changed, the test fails the same way.
    - If either regex finds nothing, the test fails with a clear "regex didn't match" message (not a confusing AttributeError).
    - The test runs on Linux CI without any Windows tooling (no PowerShell, no Inno Setup, no subprocess).
    - The test runs in < 0.1s (file reads only).
  </behavior>
  <action>
    Create `tests/test_aumid_string_parity.py` (NEW FILE) with this content:

    ```python
    """Phase 56 / D-09 #3: AUMID literal parity between __main__.py and MusicStreamer.iss.

    A typo in either file silently breaks the SMTC overlay binding without any
    runtime error or Linux-CI test failure. This guard catches drift at unit-test
    time (Linux-CI safe -- no Windows dependency).

    Mitigates T-56-02 (Tampering: silent AUMID drift causes packaging regression).
    """
    import re
    from pathlib import Path


    def test_aumid_string_parity():
        """D-09 #3: __main__.py and MusicStreamer.iss must declare the same AUMID literal."""
        repo_root = Path(__file__).parent.parent
        main_py = (repo_root / "musicstreamer" / "__main__.py").read_text()
        iss = (repo_root / "packaging" / "windows" / "MusicStreamer.iss").read_text()
        main_match = re.search(r'app_id:\s*str\s*=\s*"([^"]+)"', main_py)
        iss_match = re.search(r'AppUserModelID:\s*"([^"]+)"', iss)
        assert main_match is not None, (
            "AUMID default arg not found in musicstreamer/__main__.py "
            "(expected pattern: app_id: str = \"...\")"
        )
        assert iss_match is not None, (
            "AppUserModelID directive not found in packaging/windows/MusicStreamer.iss "
            "(expected pattern: AppUserModelID: \"...\")"
        )
        assert main_match.group(1) == iss_match.group(1), (
            f"AUMID drift: __main__.py='{main_match.group(1)}' "
            f"iss='{iss_match.group(1)}'"
        )
    ```

    Note: file path resolution uses `Path(__file__).parent.parent` (anchors to repo root). This is more robust than relying on pytest's CWD per PATTERNS.md.

    DO NOT:
    - Import from `musicstreamer.*` — the test reads source files as text, not via Python import (the `_set_windows_aumid` function is Windows-only and would attempt ctypes import on Linux).
    - Add a `@pytest.mark.skipif(sys.platform != "win32")` — the WHOLE POINT is to run on Linux CI to catch drift BEFORE a Windows packaging regression.
    - Hardcode the expected literal as `"org.lightningjim.MusicStreamer"` — the test is checking PARITY, not a specific value. If both files change to `"org.lightningjim.MusicStreamer.v2"` in lockstep, the test should still pass.
    - Add fixtures.
    - Use `subprocess` to run anything.
  </action>
  <verify>
    <automated>uv run pytest tests/test_aumid_string_parity.py -x -v 2>&1 | tail -10 && grep -c '^def test_aumid_string_parity' tests/test_aumid_string_parity.py | grep -q '^1$' && ! grep -q 'skipif\|sys.platform' tests/test_aumid_string_parity.py && ! grep -q 'from musicstreamer' tests/test_aumid_string_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_aumid_string_parity.py -x` exits 0 (current literals are aligned per Phase 43.1 / 44 verification — test passes on first run)
    - `grep -c '^def test_aumid_string_parity' tests/test_aumid_string_parity.py` returns 1
    - `grep -c 'skipif\|sys.platform' tests/test_aumid_string_parity.py` returns 0 (must run on Linux CI)
    - `grep -c 'from musicstreamer' tests/test_aumid_string_parity.py` returns 0 (no Python import — file-text only)
    - Negative-path sanity (one-off check, NOT committed): manually mutating either AUMID literal causes the test to FAIL with the diff message; revert the mutation before committing
    - Full repo regression: `uv run pytest -x` exits 0
  </acceptance_criteria>
  <done>
    `tests/test_aumid_string_parity.py` exists, contains exactly one test function, runs on Linux CI without `skipif`, reads both source files via `Path.read_text()`, asserts literal parity. Pytest passes. Full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Start Menu launch-path note to packaging/windows/README.md (ALL BRANCHES)</name>
  <files>packaging/windows/README.md</files>
  <read_first>
    - packaging/windows/README.md (current state — find the natural place for the note, e.g. a "Running MusicStreamer" or "Post-Install" section)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-09 #2 + Claude's Discretion item: "Phrasing of the README / packaging note that reminds users to launch via the Start Menu shortcut")
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Pitfall 4: bare `python -m musicstreamer` yields false negative for SMTC)
  </read_first>
  <action>
    Append (or insert at the natural location for "Running MusicStreamer") a section like this. Use this exact text unless the README has a section that absorbs it more naturally:

    ```markdown
    ## Launching MusicStreamer (SMTC overlay binding)

    **Always launch MusicStreamer via the Start Menu shortcut** ("Start" → type
    "MusicStreamer" → Enter), NOT via `python -m musicstreamer` from a terminal.

    Why: the Start Menu shortcut carries the `AppUserModelID` property
    (`org.lightningjim.MusicStreamer`) that Windows uses to bind the SMTC media
    overlay (Win+K) to the app's display name. Launching via `python -m
    musicstreamer` bypasses this binding — Windows shows "Unknown app" in SMTC
    instead of "MusicStreamer". This is documented Microsoft behaviour for
    desktop apps without an MSIX manifest.

    During installation, leave the "Run MusicStreamer" checkbox **unchecked** at
    the end of the installer wizard. The installer's Run flag launches the app
    via the installer process tree, which also bypasses the AUMID binding. After
    install, launch normally via the Start Menu.

    *(Phase 56 / WIN-02 — see `.planning/phases/56-windows-di-fm-smtc-start-menu/`
    for the full diagnostic procedure if the SMTC overlay still shows "Unknown
    app" after installation.)*
    ```

    If the README is a different style (e.g. very terse), adapt the wording but keep:
    1. "Launch via Start Menu shortcut, not `python -m musicstreamer`" as the headline rule
    2. The reason (AUMID binding for SMTC)
    3. The "Run checkbox unchecked" guidance during install (D-09 #2 hypothesis mitigation)

    DO NOT:
    - Remove or rewrite existing README content.
    - Add screenshots (out of scope for this plan).
    - Reference internal `.planning/` paths in user-facing text (the parenthetical reference at the end is acceptable since this README is developer-facing in `packaging/windows/`).
  </action>
  <verify>
    <automated>grep -ic 'Start Menu shortcut' packaging/windows/README.md | grep -qv '^0$' && grep -ic 'AppUserModelID\|AUMID\|SMTC' packaging/windows/README.md | grep -qv '^0$' && grep -c 'unchecked' packaging/windows/README.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic 'Start Menu shortcut' packaging/windows/README.md` returns >= 1
    - `grep -ic 'AppUserModelID\|AUMID\|SMTC' packaging/windows/README.md` returns >= 1
    - `grep -c 'unchecked' packaging/windows/README.md` returns >= 1 (D-09 #2 mitigation guidance)
    - Existing README content is preserved (compare line count: should grow, not shrink)
  </acceptance_criteria>
  <done>
    README has a clear, user-actionable note instructing Start-Menu launch + unchecked Run checkbox. Existing content preserved. Wording references the actual symptom (SMTC "Unknown app") so future Kyle can find it via grep.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: CONDITIONAL — AUMID literal alignment fix (only if D-09 #3 / code-fix branch)</name>
  <files>musicstreamer/__main__.py, packaging/windows/MusicStreamer.iss</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (the actual drift readout — which file has which literal?)
    - musicstreamer/__main__.py (line 99 — current `app_id` default literal)
    - packaging/windows/MusicStreamer.iss (line 71 — current `AppUserModelID:` literal)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-09 #3 + D-10)
  </read_first>
  <decision>
    SKIP THIS TASK if Task 1 confirmed branch is "docs-only" or "investigate". Otherwise (D-09 #3 / code-fix branch):

    Plan 56-03's diagnostic log will name which file holds the WRONG literal and which holds the CORRECT literal. Decide which way to align (typically: align the file that drifted to match the canonical literal `org.lightningjim.MusicStreamer`).
  </decision>
  <context>
    The canonical literal is `org.lightningjim.MusicStreamer` (used since Phase 43.1, referenced across PROJECT.md and Phase 44 D-04). DO NOT change the canonical value unless the user explicitly directs otherwise — only fix the drifted side.

    After alignment:
    1. Re-run the drift guard pytest (Task 2) — must pass.
    2. Plan 56-05 (UAT) will rebuild the installer + re-run D-08 step 4 force-fresh install + verify SMTC overlay reads "MusicStreamer".
  </context>
  <options>
    <option id="align-main-py">
      <name>Align __main__.py to match MusicStreamer.iss</name>
      <pros>Use this if the .iss has the correct canonical literal and __main__.py drifted.</pros>
      <cons>Touches Python source; minor regression risk in startup path (mitigated by `_set_windows_aumid` already being well-tested in Phase 43.1).</cons>
    </option>
    <option id="align-iss">
      <name>Align MusicStreamer.iss to match __main__.py</name>
      <pros>Use this if Python has the correct canonical literal and the .iss drifted.</pros>
      <cons>Requires installer rebuild for Plan 56-05 UAT.</cons>
    </option>
    <option id="align-both-to-new">
      <name>Align both to a NEW canonical literal (RARE)</name>
      <pros>Use only if the user has decided to rename the AUMID (e.g. domain change).</pros>
      <cons>Breaks backward compatibility with any existing installs; requires explicit user decision recorded in CONTEXT.md follow-up. Default is to AVOID this unless explicitly directed.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with the chosen option + the literal change made (e.g. "Aligned __main__.py:99: changed `org.lightningjim.MusicStreamer2` → `org.lightningjim.MusicStreamer`"). Confirm `uv run pytest tests/test_aumid_string_parity.py -x` now exits 0.
  </resume-signal>
  <action>SKIP if branch is docs-only / investigate. If code-fix: identify the drifted file from 56-03-DIAGNOSTIC-LOG.md. Edit the drifted file to match the canonical literal `org.lightningjim.MusicStreamer` (default — do NOT change the canonical value unless explicitly directed by the user). Single-character edit only. Re-run the drift guard pytest from Task 2; it must now pass.</action>
  <verify>
    <automated>uv run pytest tests/test_aumid_string_parity.py -x</automated>
  </verify>
  <done>If executed: drifted literal aligned to canonical `org.lightningjim.MusicStreamer`; drift guard pytest GREEN; resume-signal records the literal change (file:line, before → after). If skipped: resume-signal records "SKIPPED — branch is docs-only/investigate".</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: CONDITIONAL — Investigation note (only if D-09 #4 / investigate branch)</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (the four readouts that didn't surface a root cause)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Open Questions; Assumptions Log; State of the Art)
    - https://learn.microsoft.com/en-us/windows/configuration/store/find-aumid (Microsoft docs anchor for next-step research)
  </read_first>
  <what-built>
    Investigation note + recommendation for next-step.
  </what-built>
  <how-to-verify>
    Append a new section to `56-03-DIAGNOSTIC-LOG.md`:

    ```markdown
    ## D-09 #4: Unknown Root Cause — Investigation Note (Plan 56-04 Task 5)

    Force-fresh install completed; Get-StartApps shows correct AppID; in-process
    AUMID readback matches; yet SMTC overlay still reads "<observed text>".

    ### Hypotheses to test next
    1. <Hypothesis 1 — e.g. "Check whether app's WPF/Qt window class is registered
       with a different identity than the process AUMID.">
    2. <Hypothesis 2 — e.g. "Verify the ssmtc.py SystemMediaTransportControls
       binding isn't passing an explicit Source override that competes with the
       process AUMID.">
    3. <Hypothesis 3 — e.g. "Test on a Win11 machine other than the spike VM to
       rule out per-VM registry corruption.">

    ### Recommendation for Plan 56-05 / phase scope
    - Option A: Plan 56-05 ships UAT with WIN-01 only; WIN-02 splits to Phase 56.1.
    - Option B: Run additional diagnostic round (would-be Plan 56-06) before UAT.
    - Option C: Accept "Unknown app" symptom for v2.1.56 ship and re-open as a
      backlog phase after the milestone.

    Decision: <chosen option + rationale>.
    ```

    Discuss the next-step proposal with the user. If splitting WIN-02 to a follow-up phase: this is a planning-level decision that should be recorded in `.planning/STATE.md` Roadmap Evolution and `.planning/ROADMAP.md`.
  </how-to-verify>
  <resume-signal>
    Reply with the appended investigation note + chosen option (A/B/C) and rationale. If splitting WIN-02 to a follow-up phase, recommend the user run `/gsd-add-phase` separately after this plan completes.
  </resume-signal>
  <action>SKIP if branch is docs-only / code-fix. If investigate: append a `## D-09 #4: Unknown Root Cause — Investigation Note` section to 56-03-DIAGNOSTIC-LOG.md (template per &lt;how-to-verify&gt;) with: 3 hypotheses to test next, the chosen Option (A: WIN-01-only ship + WIN-02 split, B: more diagnostic, C: accept + backlog), and rationale. Discuss with user before finalising.</action>
  <verify>
    <automated>grep -c '## D-09 #4' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>If executed: section `## D-09 #4: Unknown Root Cause — Investigation Note` exists in 56-03-DIAGNOSTIC-LOG.md with hypotheses + chosen option (A/B/C) + rationale. If A chosen: user is recommended to run `/gsd-add-phase` for the WIN-02 follow-up. If skipped: resume-signal records "SKIPPED — branch is docs-only/code-fix".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drift guard test ↔ source files | Test reads `__main__.py` and `MusicStreamer.iss` as text and compares string literals. The trust boundary is "the source files are the source of truth"; the test fails if they diverge. |
| README ↔ end user | User-facing docs guide the SMTC binding behaviour by influencing launch path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02 | Tampering | Future copy-paste typo causes silent AUMID drift between `__main__.py` and `MusicStreamer.iss` (no build error, no Windows-CI failure) | mitigate | `tests/test_aumid_string_parity.py` runs on Linux CI on every commit. Catches drift before any Windows packaging regression ships. RESEARCH.md Open Question #1 explicitly recommends shipping; this plan ships in all branches. |
| T-56-Doc-A | Tampering | User ignores README launch-discipline note and continues launching via `python -m musicstreamer`, then files a false WIN-02 regression | accept | README is the canonical user-facing docs surface for `packaging/windows/`; Phase 56-05 UAT explicitly verifies on a Start-Menu launch. Single-author / personal-project context: user reads their own README. |
| T-56-Inst-A | Tampering | Installer rebuild for the code-fix branch could pick up unrelated source changes mid-rebuild | mitigate | Plan 56-05 (UAT) explicitly rebuilds from current commit and runs the D-08 step 4 force-fresh install. Drift guard pytest must be GREEN before any rebuild. |
| T-56-Cond-A | Repudiation | Branch (docs-only / code-fix / investigate) executed without recording WHY | mitigate | Task 1 explicitly records the branch decision in the resume-signal; Task 6 of Plan 56-03 records the D-09 classification in `56-03-DIAGNOSTIC-LOG.md`. The audit trail is two-step (diagnostic log → branch resume signal). |
</threat_model>

<verification>
- `tests/test_aumid_string_parity.py` exists with exactly one test function
- `uv run pytest tests/test_aumid_string_parity.py -x` exits 0
- `uv run pytest -x` (full repo regression) exits 0
- `grep -ic 'Start Menu shortcut' packaging/windows/README.md` returns >= 1
- IF code-fix branch: `git diff musicstreamer/__main__.py packaging/windows/MusicStreamer.iss` shows the AUMID-literal alignment (only)
- IF investigate branch: `grep -c '## D-09 #4' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` returns 1
</verification>

<success_criteria>
- WIN-02 closure-state determined per D-10 (docs-only confirms the fix; code-fix delivers the alignment; investigate produces a clear next-step recommendation)
- AUMID drift guard pytest exists and is GREEN — permanent regression protection against T-56-02 (mitigation in ALL branches)
- README launch-discipline note exists — Pitfall #4 mitigation in user-facing docs
- No production code change unless D-09 #3 confirmed (Plan 56-03 diagnostic-driven scope discipline)
- Plan 56-05 (UAT) has a clear input: docs-only → just rerun SMTC observation; code-fix → rebuild installer + force-fresh; investigate → may defer or split phase
</success_criteria>

<output>
After completion, create `.planning/phases/56-windows-di-fm-smtc-start-menu/56-04-SUMMARY.md` documenting:
- Branch executed (docs-only / code-fix / investigate)
- Drift guard pytest landed (file path + 1 test)
- README diff (line range or section heading added)
- IF code-fix: literal that was changed (file:line, before → after)
- IF investigate: recommendation for next step (option A/B/C)
- Whether installer needs to be rebuilt for Plan 56-05 UAT (true if code-fix branch, false otherwise)
</output>
