---
phase: 56-windows-di-fm-smtc-start-menu
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md
autonomous: false
requirements: [WIN-02]
tags: [windows, smtc, aumid, diagnostic, win11-vm, manual-uat]

user_setup:
  - service: windows-11-vm
    why: "WIN-02 SMTC overlay binding is shell-mediated and not unit-testable on Linux (D-11). Diagnostic readouts require running `Get-StartApps`, force-uninstall/reinstall, and SMTC inspection on a real Win11 host."
    env_vars: []
    dashboard_config:
      - task: "Power on the existing Win11 VM (Phase 43 / 44 spike rig). Confirm Inno Setup 6.3+ + Miniforge env are available."
        location: "Win11 VM"
      - task: "Build the freshly-built installer once on the VM (or copy from build host): `./packaging/windows/build.ps1` produces `dist/installer/MusicStreamer-2.1.56-win64-setup.exe`. Required as the install artifact for D-08 step 4."
        location: "Win11 VM PowerShell"

must_haves:
  truths:
    - "PRE-FIX READOUT captured: Get-StartApps shows the current installed-shortcut state for MusicStreamer (D-08 step 1)"
    - "PRE-FIX READOUT captured: in-process AUMID readback via GetCurrentProcessExplicitAppUserModelID matches/diverges from shortcut value (D-08 step 2)"
    - "PRE-FIX READOUT captured: SMTC overlay state with playing stream documented (D-08 step 3)"
    - "FORCE-FRESH-INSTALL executed: uninstall via Settings → delete %LOCALAPPDATA%\\Programs\\MusicStreamer → delete %APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\MusicStreamer.lnk → reinstall from fresh installer (D-08 step 4)"
    - "POST-FIX READOUT captured: re-run Get-StartApps + SMTC inspection and document outcome"
    - "ROOT CAUSE classified per D-09 shortlist: #1 stale shortcut, #2 launch path issue, #3 string drift, or unknown — drives Plan 56-04 scope (D-10)"
    - "Diagnostic log committed at .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md with all readouts, screenshots/transcripts, and root-cause classification"
  artifacts:
    - path: ".planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md"
      provides: "Win11 VM diagnostic readouts (D-08 steps 1-4) + root-cause classification (D-09) + Plan 04 scope decision (D-10)"
      contains: "## D-08 Step 1: Get-StartApps Readout"
  key_links:
    - from: "56-03 root-cause classification"
      to: "56-04 fix scope"
      via: "DIAGNOSTIC-LOG.md → Plan 56-04 reads to decide code-change vs docs-only path (D-10)"
      pattern: "Root cause: D-09 #[0-9]"
---

<objective>
Execute the Win11 VM diagnostic procedure for WIN-02 (D-07, D-08, D-09) BEFORE any code change. This plan is diagnose-FIRST per CONTEXT.md D-07: the in-process AUMID wiring (`__main__.py:99-125`) and Inno Setup `AppUserModelID` directive (`MusicStreamer.iss:71`) are already correct in code, yet SMTC reads "Unknown app" in practice. The fault is operational/environmental, not a refactoring need.

This plan ships ONLY a diagnostic log. The classification of the root cause (D-09 shortlist: #1 stale shortcut, #2 launch path, #3 string drift, or unknown) determines Plan 56-04's scope per D-10.

**Critical correction from RESEARCH.md (overrides CONTEXT.md D-08 step 1):** Use Microsoft's documented `Get-StartApps | Where-Object Name -like 'MusicStreamer*'` for the AUMID readout. The original `(New-Object -ComObject Shell.Application).Namespace(...).ParseName('lnk').ExtendedProperty('System.AppUserModel.ID')` snippet returns `$null` on per-user installs (Pitfall #3) and is fragile. `Get-StartApps` queries the same registry/cache that the shell ACTUALLY uses for SMTC binding.

Purpose: produces the empirical evidence Plan 56-04 needs to scope its work. No code change in this plan.
Output: `.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` with all four D-08 step readouts + root-cause classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-PATTERNS.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md
@musicstreamer/__main__.py
@packaging/windows/MusicStreamer.iss
@.claude/skills/spike-findings-musicstreamer/SKILL.md
@.claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md

<interfaces>
<!-- Existing AUMID wiring (already correct per Phase 43.1 verification): -->

```python
# musicstreamer/__main__.py line 99:
def _set_windows_aumid(app_id: str = "org.lightningjim.MusicStreamer") -> None:
    """Set the Windows AppUserModelID so the SMTC overlay + taskbar group
    use a stable per-app identifier."""
    # ... (lines 99-125: ctypes setup, LPCWSTR argtypes, shell32.SetCurrentProcessExplicitAppUserModelID call)

# musicstreamer/__main__.py line 130:
_set_windows_aumid()  # Phase 43.1: before QApplication (binds on first window)
```

```inno
; packaging/windows/MusicStreamer.iss lines 64-71:
; AppUserModelID must match exactly the string passed to SetCurrentProcessExplicitAppUserModelID
; in musicstreamer/__main__.py::_set_windows_aumid -- otherwise SMTC shows "Unknown app".
[Icons]
Name: "{userprograms}\MusicStreamer"; \
    Filename: "{app}\MusicStreamer.exe"; \
    WorkingDir: "{app}"; \
    Comment: "Stream radio and music"; \
    AppUserModelID: "org.lightningjim.MusicStreamer"
```

<!-- Diagnostic procedure (D-08 with RESEARCH.md correction): -->
<!-- Step 1: Get-StartApps | Where-Object Name -like 'MusicStreamer*' -->
<!-- Step 2: Launch via Start Menu, capture in-process AUMID readback -->
<!-- Step 3: SMTC overlay inspection while playing a stream -->
<!-- Step 4: Force fresh install (uninstall + delete LNK + reinstall) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Win11 VM environment readiness check</name>
  <files>none — VM check only</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-07, D-08 — diagnose-first protocol)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Environment Availability table — confirms VM + Inno Setup 6.3+ + DI.fm premium account requirements)
    - .claude/skills/spike-findings-musicstreamer/SKILL.md (auto-load triggers — Windows packaging context)
    - .claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md ("Build & runtime layout" sections — confirms what a freshly built installer looks like under `dist/installer/`)
  </read_first>
  <what-built>
    Nothing yet — this is a pre-flight check before the human runs the diagnostic on the VM.
  </what-built>
  <how-to-verify>
    Confirm the following on the Win11 VM:
    1. VM is powered on, Win11 22H2+ (`winver` confirms build).
    2. Conda env activated for build (`conda activate musicstreamer-build`) per Phase 43 README.
    3. `iscc.exe` is on PATH (`Get-Command iscc` returns a result).
    4. A fresh build artifact exists at `dist/installer/MusicStreamer-*-win64-setup.exe` (built from current source). If not, run `./packaging/windows/build.ps1`.
    5. DI.fm premium credentials available for later UAT (Plan 56-05). For Plan 56-03 only the SMTC half is exercised — DI.fm playback is NOT required here.
    6. The user has BOTH PowerShell windows ready: one elevated (for force-uninstall + delete LNK), one regular (for `Get-StartApps` and process inspection).
  </how-to-verify>
  <resume-signal>
    Reply "VM ready" with the build number of `dist/installer/MusicStreamer-*-win64-setup.exe` confirmed present, OR reply "BLOCKED: <reason>" if any precondition is missing.
  </resume-signal>
  <action>Manual VM readiness check — see &lt;how-to-verify&gt;. Six environmental preconditions must be confirmed before any diagnostic command runs. No code change.</action>
  <verify>
    <automated>MISSING — checkpoint:human-action; verification is the human responding "VM ready" with build number per &lt;resume-signal&gt;</automated>
  </verify>
  <done>Human confirms: (1) VM powered on Win11 22H2+, (2) musicstreamer-build conda env active, (3) iscc on PATH, (4) freshly-built installer present at dist/installer/, (5) DI.fm credentials available, (6) two PowerShell windows ready (one elevated, one regular).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: D-08 Step 1 — Get-StartApps AUMID readout (PRE-FIX)</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-08 step 1)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Pitfall #3: ExtendedProperty('System.AppUserModel.ID') returns null on per-user installs — REPLACED with Get-StartApps; State of the Art table; Code Examples → Example 3 step 1 verbatim PowerShell)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md (Manual-Only Verifications row "Shortcut AUMID matches in-process AUMID")
  </read_first>
  <what-built>
    Nothing yet — pre-fix readout to establish baseline.
  </what-built>
  <how-to-verify>
    On the Win11 VM, in a regular (non-elevated) PowerShell window, run EXACTLY:

    ```powershell
    Get-StartApps | Where-Object Name -like 'MusicStreamer*'
    ```

    Expected outcomes (record EXACTLY which one occurs):
    - **Outcome A — happy path:** A row appears with `Name = MusicStreamer` and `AppID = org.lightningjim.MusicStreamer`. → record this; SMTC binding *should* work; if step 3 still shows "Unknown app", root cause is something we have not yet hypothesized.
    - **Outcome B — empty output:** Cmdlet returns no rows. → shortcut not registered as a Start Menu app. Check `%APPDATA%\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk` exists with `Test-Path`. Likely root cause: D-09 #1 (stale or never-properly-registered shortcut).
    - **Outcome C — AppID empty/wrong:** Row appears but `AppID` is blank or shows a string OTHER than `org.lightningjim.MusicStreamer`. → likely root cause: D-09 #1 (LNK lacks property — Pitfall #5) OR D-09 #3 (string drift between installer and `__main__.py`).

    Also run as a cross-check:
    ```powershell
    Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk"
    ```
    Expected: `True`. If `False`, the shortcut wasn't installed at all (likely a per-machine install instead of per-user — see `MusicStreamer.iss` for the install scope; document discrepancy).

    **Fallback ONLY if `Get-StartApps` itself fails** (e.g., StartLayout module missing — extremely unlikely on Win11):
    ```powershell
    (New-Object -ComObject Shell.Application).Namespace("$env:APPDATA\Microsoft\Windows\Start Menu\Programs").ParseName('MusicStreamer.lnk').ExtendedProperty('System.AppUserModel.ID')
    ```
    Note: per Pitfall #3 this can return `$null` even when AUMID is set; use ONLY as a fallback, not as authority.

    Record both outputs in `56-03-DIAGNOSTIC-LOG.md` (create the file if it doesn't exist) under the section heading `## D-08 Step 1: Get-StartApps Readout (PRE-FIX)` with the literal command, the literal output, and the classified outcome (A/B/C).
  </how-to-verify>
  <resume-signal>
    Reply with the recorded outcome letter ("A", "B", or "C") and paste the literal `Get-StartApps` output, OR reply "BLOCKED: <reason>".
  </resume-signal>
  <action>Run `Get-StartApps | Where-Object Name -like 'MusicStreamer*'` on the VM, plus the `Test-Path` cross-check. Append the literal output + outcome classification to 56-03-DIAGNOSTIC-LOG.md under heading `## D-08 Step 1: Get-StartApps Readout (PRE-FIX)`. Use `Get-StartApps` as the AUTHORITATIVE source per RESEARCH.md (overrides CONTEXT.md D-08 step 1 ExtendedProperty snippet). Do NOT use the ExtendedProperty fallback unless `Get-StartApps` itself fails.</action>
  <verify>
    <automated>grep -c '## D-08 Step 1: Get-StartApps Readout' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>Section `## D-08 Step 1: Get-StartApps Readout (PRE-FIX)` exists in 56-03-DIAGNOSTIC-LOG.md containing the literal command, literal output, and outcome classification (A/B/C). The `Test-Path` cross-check result is also recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: D-08 Step 2 — In-process AUMID readback via debug log (PRE-FIX)</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-08 step 2)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Code Examples → Example 3 step 2 alternative recommended approach: one-line debug log on startup)
    - musicstreamer/__main__.py (lines 99-130 — the AUMID setter and call site)
  </read_first>
  <what-built>
    Nothing — readback from currently-installed app.
  </what-built>
  <how-to-verify>
    The in-process AUMID readback uses `GetCurrentProcessExplicitAppUserModelID` and must run inside the running process. Two approved methods (RESEARCH.md Example 3 step 2):

    **Method A (RECOMMENDED — simpler, no code change):** Launch MusicStreamer via the Start Menu shortcut. After startup, check the log file for the existing `_set_windows_aumid` log line. Per `__main__.py:99-125` there's no readback log today — so this method only works if Phase 43.1 left a readback line in place. If the log shows `AUMID set to: org.lightningjim.MusicStreamer` (or similar), record that string. If no AUMID-related log line exists, proceed to Method B.

    **Method B (one-shot diagnostic patch — REVERT after readback):** TEMPORARILY patch `musicstreamer/__main__.py::_set_windows_aumid` to log the readback. Add this AT THE END of the function body (after line 125):
    ```python
    # Phase 56 / D-08 step 2 diagnostic — REVERT after capturing the log line
    readback = ctypes.c_wchar_p()
    shell32.GetCurrentProcessExplicitAppUserModelID.argtypes = [ctypes.POINTER(ctypes.c_wchar_p)]
    shell32.GetCurrentProcessExplicitAppUserModelID.restype = ctypes.HRESULT
    shell32.GetCurrentProcessExplicitAppUserModelID(ctypes.byref(readback))
    logging.info("AUMID readback: %r", readback.value)
    ```
    Run the app via the Start Menu shortcut once, capture the log line (look in console output if launched from Miniforge prompt, OR add `-v` flag if needed). Then REVERT the patch (`git checkout musicstreamer/__main__.py` or manually delete the added lines).

    Expected: log line reads `AUMID readback: 'org.lightningjim.MusicStreamer'`. Compare against the AppID from Step 1.

    Record in `56-03-DIAGNOSTIC-LOG.md` under section `## D-08 Step 2: In-Process AUMID Readback (PRE-FIX)`:
    - Which method was used (A or B)
    - The readback string (exact value)
    - Whether it MATCHES the Step 1 AppID (or which value differs)
    - If Method B was used, confirm the patch was reverted (`git status` shows `__main__.py` clean)
  </how-to-verify>
  <resume-signal>
    Reply with the readback string + match/mismatch result vs Step 1, e.g. "Readback: 'org.lightningjim.MusicStreamer' — MATCHES Step 1 AppID" or "Readback: '<other>' — MISMATCH". Confirm `git status` is clean if Method B was used.
  </resume-signal>
  <action>Try Method A first (existing startup log line). If no AUMID log line exists, apply Method B's temporary diagnostic patch to musicstreamer/__main__.py::_set_windows_aumid (verbatim 5-line block per &lt;how-to-verify&gt;), launch MusicStreamer via Start Menu shortcut, capture the AUMID readback log line, then REVERT the patch via `git checkout musicstreamer/__main__.py`. Append findings (method used + readback string + match/mismatch vs Step 1) to 56-03-DIAGNOSTIC-LOG.md under heading `## D-08 Step 2: In-Process AUMID Readback (PRE-FIX)`.</action>
  <verify>
    <automated>grep -c '## D-08 Step 2: In-Process AUMID Readback' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md | grep -qv '^0$' && git diff --quiet musicstreamer/__main__.py</automated>
  </verify>
  <done>Section `## D-08 Step 2: In-Process AUMID Readback (PRE-FIX)` exists in 56-03-DIAGNOSTIC-LOG.md with: method used (A/B), readback string, match/mismatch verdict vs Step 1. `git diff musicstreamer/__main__.py` is empty (Method B patch was reverted if applied — T-56-Diag-B mitigation).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: D-08 Step 3 — SMTC overlay state inspection (PRE-FIX)</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-08 step 3)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Code Examples → Example 3 step 3; Common Pitfalls → Pitfall 4: bare `python -m musicstreamer` yields false negative)
  </read_first>
  <what-built>
    Nothing — observation of current SMTC behavior.
  </what-built>
  <how-to-verify>
    1. Launch MusicStreamer via the **Start Menu shortcut** (NOT `python -m musicstreamer` — Pitfall #4: it bypasses the shortcut-AUMID binding and yields a false negative).
    2. Start playback of any stream (a non-DI.fm station works fine — e.g., SomaFM Drone Zone — so this step doesn't depend on Plan 56-01/02 being complete).
    3. Open the SMTC overlay: press `Win+K` (or click the media flyout icon on the taskbar).
    4. Observe the app name displayed above the media controls. Record EXACTLY what it says.

    Also run this PowerShell cross-check while the app is playing:
    ```powershell
    Get-StartApps | Format-Table -AutoSize
    ```
    Confirm `MusicStreamer` row is present with the expected AppID.

    Then take a screenshot of the SMTC overlay (or transcribe the exact app-name text). Save the screenshot to `.planning/phases/56-windows-di-fm-smtc-start-menu/screenshots/56-03-smtc-prefix.png` if practical (create the directory). If screenshots aren't practical on the VM, a verbatim text transcription is acceptable.

    Record in `56-03-DIAGNOSTIC-LOG.md` under section `## D-08 Step 3: SMTC Overlay (PRE-FIX)`:
    - Exact app-name string shown by SMTC (e.g., "Unknown app", "MusicStreamer", "MusicStreamer.exe", or other)
    - Stream that was playing during observation
    - Path to screenshot (or full transcribed text)
    - Settings → System → Notifications check: does "MusicStreamer" appear in the per-app notification list? (Yes/No)
  </how-to-verify>
  <resume-signal>
    Reply with the exact SMTC overlay text (e.g., "Unknown app" or "MusicStreamer") and screenshot path (or transcription). Confirm whether Settings → Notifications lists MusicStreamer.
  </resume-signal>
  <action>Launch MusicStreamer via the Start Menu shortcut (NOT `python -m musicstreamer` — Pitfall #4). Start any stream. Open SMTC via Win+K. Record exact app-name shown. Take screenshot to .planning/phases/56-windows-di-fm-smtc-start-menu/screenshots/56-03-smtc-prefix.png OR transcribe verbatim. Append to 56-03-DIAGNOSTIC-LOG.md under `## D-08 Step 3: SMTC Overlay (PRE-FIX)` with: exact SMTC text, stream played, screenshot path, Settings → Notifications listing check result.</action>
  <verify>
    <automated>grep -c '## D-08 Step 3: SMTC Overlay' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>Section `## D-08 Step 3: SMTC Overlay (PRE-FIX)` exists in 56-03-DIAGNOSTIC-LOG.md with: exact app-name string from SMTC, stream played, screenshot path or transcription, Settings → Notifications inclusion check.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: D-08 Step 4 — Force-fresh install procedure</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-08 step 4 — the canonical force-fresh recipe)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Pitfall #5: reinstall over top doesn't always rewrite the LNK property bag — D-08 step 4 sidesteps this)
  </read_first>
  <what-built>
    Force-fresh install — sidesteps both D-09 #1 (stale shortcut) and D-09 #2 (post-install Run checkbox bypass) hypotheses in one motion.
  </what-built>
  <how-to-verify>
    Run on the Win11 VM, in an **elevated** PowerShell window:

    ```powershell
    # Step 1: Uninstall via Settings UI (or cmd-line equivalent if available)
    # Settings → Apps → MusicStreamer → Uninstall. Wait for completion.

    # Step 2: Force-clean install dir if it lingers
    Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Programs\MusicStreamer" -ErrorAction SilentlyContinue

    # Step 3: Force-clean stale Start Menu shortcut
    # (CRITICAL: Inno Setup may not rewrite the LNK property bag on reinstall-over-top, Pitfall #5)
    Remove-Item "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk" -ErrorAction SilentlyContinue

    # Step 4: Sanity check — neither path should exist now
    Test-Path "$env:LOCALAPPDATA\Programs\MusicStreamer"
    Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk"
    # Both should print False.

    # Step 5: Run the freshly-built installer (path may differ — adjust to actual artifact name)
    & "$PWD\dist\installer\MusicStreamer-2.1.56-win64-setup.exe"
    # During installer wizard: leave the post-install "Run MusicStreamer" checkbox UNCHECKED
    # (Phase 44 D-04 default; D-09 #2 hypothesis — checked Run launches via installer process tree, NOT Start Menu shortcut, bypassing AUMID binding).

    # Step 6: After install completes, do NOT launch yet. Re-run the Step 1 readout:
    Get-StartApps | Where-Object Name -like 'MusicStreamer*'
    # Expected: row with AppID = org.lightningjim.MusicStreamer
    ```

    Then **launch MusicStreamer via the Start Menu shortcut** (Win → type "MusicStreamer" → Enter). Start a stream. Open SMTC overlay (Win+K). Observe app name.

    Record in `56-03-DIAGNOSTIC-LOG.md` under section `## D-08 Step 4: Force-Fresh Install (POST-FIX)`:
    - Confirmation that uninstall + force-delete steps completed (both `Test-Path` returned False)
    - The installer artifact name + version that was installed
    - The post-install `Get-StartApps` output (compare to Step 1 PRE-FIX output)
    - The post-install SMTC overlay app name (compare to Step 3 PRE-FIX output)
    - Screenshot of post-fix SMTC at `.planning/phases/56-windows-di-fm-smtc-start-menu/screenshots/56-03-smtc-postfix.png` (or transcription)
  </how-to-verify>
  <resume-signal>
    Reply with the post-fix SMTC overlay text and post-fix `Get-StartApps` output. Note specifically: "FIXED by force-fresh install" if SMTC now reads "MusicStreamer", or "STILL BROKEN: <observed text>" if the symptom persists.
  </resume-signal>
  <action>Execute the force-fresh recipe in elevated PowerShell EXACTLY per &lt;how-to-verify&gt; (uninstall → Remove-Item LOCALAPPDATA/Programs/MusicStreamer → Remove-Item APPDATA/.../MusicStreamer.lnk → Test-Path both must be False → run installer with Run checkbox UNCHECKED → re-run Get-StartApps). Then launch via Start Menu and inspect SMTC. Append all readouts (uninstall confirmation, installer artifact name+version, post-install Get-StartApps output, post-fix SMTC text, screenshot path) to 56-03-DIAGNOSTIC-LOG.md under `## D-08 Step 4: Force-Fresh Install (POST-FIX)`.</action>
  <verify>
    <automated>grep -c '## D-08 Step 4: Force-Fresh Install' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>Section `## D-08 Step 4: Force-Fresh Install (POST-FIX)` exists in 56-03-DIAGNOSTIC-LOG.md with: uninstall+force-clean confirmation (both Test-Path False), installer artifact name, post-install Get-StartApps output, post-fix SMTC text, screenshot path or transcription. Result note "FIXED" or "STILL BROKEN" recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 6: D-09 Root-cause classification + D-10 Plan 04 scope decision</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-09 root-cause shortlist + D-10 scope decision rule)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Open Questions + Assumptions Log A3)
    - The four PRE/POST-FIX readouts captured in this plan's earlier checkpoints
  </read_first>
  <decision>
    Classify the root cause per D-09 shortlist and decide Plan 56-04's code-change scope per D-10:

    **D-10 rule:** environmental cause (D-09 #1 or #2) → Plan 04 docs-only. Wiring bug (D-09 #3 or unknown) → Plan 04 patches the offending file + re-runs UAT.
  </decision>
  <context>
    Cross-reference the four readouts (Tasks 2-5) to classify:
    - **D-09 #1 — Stale shortcut:** PRE-fix Step 1 showed empty AppID OR no row; POST-fix Step 1 shows correct AppID; POST-fix SMTC reads "MusicStreamer". → SYMPTOM RESOLVED by force-fresh install. No code change needed (docs-only Plan 04).
    - **D-09 #2 — Wrong launch path:** PRE-fix Step 3 showed "Unknown app" but PRE-fix Step 1 showed correct AppID; POST-fix Step 3 (launching strictly via Start Menu) shows "MusicStreamer". → SYMPTOM RESOLVED by launch discipline. No code change needed (docs-only Plan 04 — README note about Start Menu launch path).
    - **D-09 #3 — String drift:** PRE-fix Step 1 shows AppID DIFFERENT from `org.lightningjim.MusicStreamer` literal in `MusicStreamer.iss:71` OR `__main__.py:99`. → CODE CHANGE NEEDED in Plan 04 to align literals + ship the optional pytest drift guard.
    - **D-09 #4 — Unknown:** None of the above; SMTC still reads "Unknown app" after force-fresh install with correct AppID and matching in-process readback. → CODE CHANGE NEEDED — Plan 04 investigates further (likely involves Inno Setup `[Icons]` directive review or a new Microsoft docs trail).
  </context>
  <options>
    <option id="docs-only">
      <name>Docs-only Plan 04 (D-09 #1 or #2 confirmed)</name>
      <pros>SMTC fixed by force-fresh install OR strict launch path. Phase 04 ships a README diff explaining "launch via Start Menu shortcut" + (optional) the AUMID drift-guard pytest as belt-and-suspenders. No production code change.</pros>
      <cons>If a future contributor breaks the AUMID literal, no code-side guard catches it (mitigated by including the optional drift-guard pytest in Plan 04).</cons>
    </option>
    <option id="code-fix">
      <name>Code-fix Plan 04 (D-09 #3 confirmed — string drift)</name>
      <pros>Aligns AUMID literal across `__main__.py` and `MusicStreamer.iss`. Ships drift-guard pytest. Re-builds installer + UAT in Plan 05.</pros>
      <cons>Requires fresh installer rebuild. Plan 04 wave goes from a docs change to a code+test+rebuild change.</cons>
    </option>
    <option id="investigate">
      <name>Investigate Plan 04 (D-09 #4 — unknown root cause)</name>
      <pros>Honest acknowledgement that the diagnostic procedure didn't surface a root cause. Plan 04 becomes a deeper-dive WIN-02 investigation (e.g., re-read Microsoft docs, check Inno Setup `[Icons]` directive against current Microsoft AUMID resolution sequence).</pros>
      <cons>Phase scope expands; no guarantee the deeper dive reveals the cause without VM iteration. May require splitting WIN-02 into a follow-up phase.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with: "Decision: <docs-only|code-fix|investigate>; D-09 classification: <#1|#2|#3|#4>; rationale: <one sentence citing the readouts>". Append the decision section to `56-03-DIAGNOSTIC-LOG.md` under heading `## D-09 Root Cause + D-10 Plan 04 Scope`.</resume-signal>
  <action>Cross-reference the 4 PRE/POST-FIX readouts in 56-03-DIAGNOSTIC-LOG.md against the D-09 classification table in &lt;context&gt;. Choose ONE of: docs-only (D-09 #1 or #2 confirmed), code-fix (D-09 #3 string drift), investigate (D-09 #4 unknown). Append a `## D-09 Root Cause + D-10 Plan 04 Scope` section to 56-03-DIAGNOSTIC-LOG.md with the classification + chosen branch + one-sentence rationale citing the specific readouts.</action>
  <verify>
    <automated>grep -c '## D-09 Root Cause' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md | grep -qv '^0$' && git diff --quiet musicstreamer/ packaging/</automated>
  </verify>
  <done>Section `## D-09 Root Cause + D-10 Plan 04 Scope` exists in 56-03-DIAGNOSTIC-LOG.md with: D-09 classification (#1/#2/#3/#4), branch decision (docs-only/code-fix/investigate), and rationale citing the specific PRE/POST readouts. `git diff musicstreamer/ packaging/` is empty (no production code change in this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Win11 VM ↔ build host | Diagnostic readouts cross from VM PowerShell into the planning log on the build host. Manual transcription / screenshot transfer; no automated trust path. |
| `__main__.py` AUMID literal ↔ `MusicStreamer.iss` AUMID literal | Two unlinked string literals; drift between them silently breaks SMTC binding without runtime error (T-56-02). This plan's diagnostic surfaces drift if present. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02 | Tampering | AUMID literal drift between `__main__.py:99` and `MusicStreamer.iss:71` (silent SMTC regression — no build error, no Linux test failure) | mitigate | D-08 step 1 (`Get-StartApps`) reveals the actual AppID; the diagnostic surfaces drift if present. Plan 56-04 will add the optional pytest drift guard (Pitfall #6) regardless of D-09 outcome — recommended in RESEARCH.md Open Question #1 as zero-cost permanent regression protection. |
| T-56-Diag-A | Information Disclosure | Diagnostic log captures the user's home-folder paths (`%APPDATA%`, `%LOCALAPPDATA%`) and any installed-app inventory from `Get-StartApps` | accept | Single-author personal project; the diagnostic log is committed to the same private repo as the rest of `.planning/`. No PII or credentials in the readouts (AUMID is a public string; Start Menu inventory is benign). |
| T-56-Diag-B | Repudiation | Method B in Task 3 temporarily patches `__main__.py` for the AUMID readback; if not reverted, the patch ships in the next build | mitigate | Task 3 explicitly requires confirming `git status` is clean post-readback. The signal-text instructs the human to verify. Method A is recommended first, reducing the surface where this matters. |
| T-56-Diag-C | Denial of Service | Force-uninstall + delete steps in Task 5 destroy the user's MusicStreamer install state | accept | Single-author repo, VM environment, no shared install. User explicitly opts in by running the elevated PowerShell commands. |
</threat_model>

<verification>
- `.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` exists
- `grep -c '## D-08 Step 1' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` returns >= 1
- `grep -c '## D-08 Step 2' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` returns >= 1
- `grep -c '## D-08 Step 3' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` returns >= 1
- `grep -c '## D-08 Step 4' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` returns >= 1
- `grep -c '## D-09 Root Cause' .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md` returns >= 1
- `git status musicstreamer/__main__.py` is clean (no Method B residual patch)
- `git status musicstreamer/url_helpers.py` and `musicstreamer/player.py` are unaffected by this plan
</verification>

<success_criteria>
- All four D-08 steps executed on the Win11 VM with PRE-FIX and POST-FIX readouts captured
- Get-StartApps used as the authoritative AUMID readout (RESEARCH.md correction over CONTEXT.md D-08 step 1)
- Root cause classified per D-09 shortlist (#1/#2/#3/#4)
- Plan 56-04 scope decided per D-10 (docs-only / code-fix / investigate)
- Diagnostic log committed at `.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md`
- No production code change in this plan (`git diff musicstreamer/` is empty)
</success_criteria>

<output>
After completion, create `.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md` linking to the diagnostic log + recording the D-09 classification + Plan 04 scope decision. Plan 56-04's first action will be to read this SUMMARY to determine its branch.
</output>
