---
phase: 56-windows-di-fm-smtc-start-menu
plan: 05
type: execute
wave: 3
depends_on: ["56-01", "56-02", "56-03", "56-04"]
files_modified:
  - .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md
autonomous: false
requirements: [WIN-01, WIN-02]
tags: [uat, win11-vm, di-fm, smtc, settings-import-roundtrip]

user_setup:
  - service: windows-11-vm
    why: "Final UAT for both halves: WIN-01 DI.fm playback (D-12 — fresh AA import + settings-import ZIP roundtrip) and WIN-02 SMTC overlay verification on the freshly-built installer (D-11)."
    env_vars: []
    dashboard_config:
      - task: "Win11 VM with conda env activated; freshly built installer at dist/installer/MusicStreamer-2.1.56-win64-setup.exe (Plan 56-04 may have already rebuilt if D-09 #3 branch)"
        location: "Win11 VM"
      - task: "DI.fm premium account credentials available for fresh AA import test (D-12 part a)"
        location: "User has the credentials"
      - task: "Linux-exported settings ZIP containing at least one stored https://prem*.di.fm/... row available for transfer to VM (D-12 part b — Phase 42 settings-import roundtrip)"
        location: "Linux build host: export via Settings → Export, transfer ZIP to VM"

must_haves:
  truths:
    - "WIN-01a verified: DI.fm Lounge plays end-to-end on Win11 VM via fresh AA import — audible audio + ICY title display within 10 seconds (ROADMAP SC #1, D-12 part a)"
    - "WIN-01b verified: DI.fm channel plays end-to-end on Win11 VM via Phase 42 settings-import ZIP roundtrip — confirms rewrite engages on URL boundary regardless of how URL entered the DB (D-12 part b)"
    - "WIN-02 verified: SMTC overlay (Win+K) shows 'MusicStreamer' (NOT 'Unknown app') while a stream is playing, after launching via Start Menu shortcut on a freshly-installed app (ROADMAP SC #2, SC #3, SC #4)"
    - "AUMID parity verified: Get-StartApps AppID column matches the in-process readback string AND matches the canonical literal `org.lightningjim.MusicStreamer` (ROADMAP SC #4)"
    - "All four ROADMAP success criteria attestations recorded in 56-05-UAT-LOG.md with screenshots/transcripts"
  artifacts:
    - path: ".planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md"
      provides: "Final UAT attestations for WIN-01 (both subpaths) and WIN-02 with verbatim observations + screenshots/transcriptions"
      contains: "## WIN-01a: DI.fm Lounge fresh AA import"
  key_links:
    - from: "56-05 UAT outcome"
      to: "phase completion gate"
      via: "All 4 ROADMAP SCs attested → /gsd-verify-work green-lights phase complete"
      pattern: "WIN-01a.*PASS\\|WIN-01b.*PASS\\|WIN-02.*PASS"
---

<objective>
Final UAT for Phase 56 covering both WIN-01 (DI.fm playback) and WIN-02 (SMTC overlay) on the Win11 VM. Per CONTEXT.md D-11 (no automated SMTC test) and D-12 (DI.fm UAT must cover BOTH fresh AA import AND settings-import ZIP roundtrip), this plan executes three manual verifications:

1. **WIN-01a:** DI.fm Lounge plays from a FRESH AA import on the VM (rewrite engages on the AA-import-inserted URL).
2. **WIN-01b:** DI.fm channel plays from a Phase 42 settings-import ZIP roundtrip (rewrite engages on a DB row that was migrated from a Linux-exported ZIP — confirms the rewrite is at the play-time URI boundary, not insert-time).
3. **WIN-02:** SMTC overlay reads "MusicStreamer" while a stream is playing on a freshly-installed app launched via Start Menu shortcut.

The UAT log captures attestations for all four ROADMAP success criteria.

Purpose: closes the phase. Without these manual attestations, neither WIN-01 nor WIN-02 can be considered shipped — the automated tests in Plans 01/02/04 prove the helper logic works on Linux but cannot prove the Win11 GStreamer pipeline accepts the rewritten URL or that Windows shell binds the SMTC display name correctly.
Output: `.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` with PASS/FAIL attestations + observations for all three verifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-01-SUMMARY.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-02-SUMMARY.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-SUMMARY.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-04-SUMMARY.md
@.claude/skills/spike-findings-musicstreamer/SKILL.md
@.claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md

<interfaces>
<!-- ROADMAP success criteria the UAT attests against (all 4 must PASS): -->

<!-- SC #1 (WIN-01): DI.fm premium stream plays on Windows -->
<!-- SC #2 (WIN-02): Start Menu shortcut carries System.AppUserModel.ID=org.lightningjim.MusicStreamer -->
<!-- SC #3 (WIN-02): SMTC overlay shows "MusicStreamer" (not "Unknown app") -->
<!-- SC #4 (WIN-02): AUMID on the shortcut matches the in-process AUMID -->

<!-- VALIDATION.md Manual-Only Verifications table (rows 1-4) is the literal test script. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-UAT — Build verification + freshly-built installer present</name>
  <files>none — VM check only</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-04-SUMMARY.md (if Plan 56-04 was code-fix branch, the installer must have been rebuilt — confirm)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md (Manual-Only Verifications table — confirms what each row needs)
    - .claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md (build & runtime layout — confirms what `dist/installer/...exe` looks like and what flags `build.ps1` accepts)
  </read_first>
  <what-built>
    Verification that the build artifact and VM environment are ready for the three UAT scenarios.
  </what-built>
  <how-to-verify>
    On the Win11 VM:

    1. Confirm Plan 56-01, 56-02, 56-04 commits are present in the working tree (`git log --oneline -10` shows the WIN-01 helper, wire, and drift-guard commits).
    2. Check whether 56-04 was code-fix branch:
       - If 56-04-SUMMARY.md says branch was `code-fix` (D-09 #3): rebuild installer fresh: `./packaging/windows/build.ps1 -Clean` (or equivalent — Phase 43 README has the build commands). Confirm new artifact at `dist/installer/MusicStreamer-2.1.56-win64-setup.exe`.
       - If 56-04-SUMMARY.md says branch was `docs-only` or `investigate`: the existing installer from Plan 56-03 Task 5 is sufficient, BUT it does NOT contain the WIN-01 fix. So **always rebuild** to bake in the WIN-01 helper + wire-in. Run `./packaging/windows/build.ps1` regardless of 56-04 branch.
    3. Confirm full Linux test suite is green: `uv run pytest -x` exits 0 (run on the build host, not the VM).
    4. Confirm the Linux-exported settings ZIP for WIN-01b is available on the VM (transferred from Linux build host). The ZIP MUST contain at least one DI.fm row with `https://` scheme (verify by extracting the ZIP and grep'ing the SQLite dump or station list for `https://prem*.di.fm`).
    5. Confirm DI.fm premium account credentials are available for the fresh AA import (WIN-01a).
    6. Confirm the VM has been completely uninstalled per D-08 step 4 procedure (delete `%LOCALAPPDATA%\Programs\MusicStreamer` + delete `%APPDATA%\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk`). Re-confirm with `Test-Path` on both — both must return False before the fresh installer runs.
    7. Run the freshly-built installer. Leave the post-install Run checkbox UNCHECKED.

    Pre-UAT readiness check confirmation: `Get-StartApps | Where-Object Name -like 'MusicStreamer*'` shows AppID = `org.lightningjim.MusicStreamer`.
  </how-to-verify>
  <resume-signal>
    Reply "READY" with: (a) commit hash of the build host source tree at install time, (b) confirmation that the installer is freshly rebuilt post-Plan-56-02 wiring, (c) confirmation that the Linux-exported settings ZIP is on the VM and contains a DI.fm `https://` row, (d) post-install `Get-StartApps` AppID value.
    OR reply "BLOCKED: <reason>".
  </resume-signal>
  <action>Build host: run `uv run pytest -x`; must exit 0. Run `./packaging/windows/build.ps1` (always — even if 56-04 was docs-only) to bake in WIN-01 helper + wire from Plan 56-02. Transfer fresh installer to VM. On VM: force-uninstall per D-08 step 4 (Remove-Item LOCALAPPDATA + APPDATA LNK; Test-Path both must be False). Install fresh; leave post-install Run checkbox UNCHECKED. Cross-check: `Get-StartApps | Where-Object Name -like 'MusicStreamer*'` shows AppID = `org.lightningjim.MusicStreamer`.</action>
  <verify>
    <automated>uv run pytest -x 2>&1 | tail -5</automated>
  </verify>
  <done>Build host pytest -x is GREEN. Freshly-built installer present at dist/installer/. VM is clean (both Test-Path return False before install). Fresh installer runs to completion with Run checkbox UNCHECKED. Post-install Get-StartApps shows correct AppID. Linux-exported settings ZIP with DI.fm https row is staged on VM. DI.fm credentials in hand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: WIN-01a UAT — DI.fm Lounge plays from fresh AA import</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-12 part a: fresh AA import path)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md (Manual-Only Verifications row 3: "DI.fm Lounge plays from fresh AA import")
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-01-SUMMARY.md (helper that's being exercised under the hood)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-02-SUMMARY.md (player wire-in line)
    - .claude/skills/spike-findings-musicstreamer/references/windows-gstreamer-bundling.md (DI.fm HTTPS landmine context — explains WHY this UAT matters)
  </read_first>
  <what-built>
    WIN-01 helper + player wire (Plans 56-01 + 56-02) — Plan 05 verifies they make DI.fm playable on Windows where it previously failed with `error (-5)`.
  </what-built>
  <how-to-verify>
    On the Win11 VM, with MusicStreamer freshly installed and launched via Start Menu shortcut:

    1. Open Settings → AudioAddict → enter premium credentials → trigger AA import (this is the "fresh AA import" path per D-12 part a).
    2. Wait for the import to complete (status indicator in Settings).
    3. In the station list, navigate to DI.fm → select a channel (DI.fm Lounge is the canonical test channel per D-12).
    4. Click Play.
    5. Within 10 seconds, expect:
       - Audible audio (use the VM's audio passthrough if needed; or look at the volume meter).
       - ICY title display in the Now Playing panel (artist/track text appears within ~10s).
    6. **Negative-side check** — open the app's debug log (per Phase 43 logs path) and confirm there's a single `aa_normalize_stream_url: DI.fm https->http: ...` line at DEBUG level. This proves the rewrite engaged. If no such log line: the wire-in didn't fire — investigate.

    Record in `.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` under section `## WIN-01a: DI.fm Lounge fresh AA import`:
    - Channel name played
    - Time to first audio (seconds, approximate)
    - First ICY title displayed (or "no ICY received in 30s" if none)
    - Debug log excerpt showing `aa_normalize_stream_url` rewrite line (sanitize the listen_key in the recorded log line: replace with `listen_key=<REDACTED>`)
    - Result: PASS or FAIL with reason

    PASS criteria (ROADMAP SC #1 part a):
    - Audible audio within 10 seconds AND ICY title displayed within 30 seconds AND debug log shows the rewrite fired.

    FAIL behavior:
    - If FAIL, capture the GStreamer error code from the log (likely `error (-5)` if the rewrite didn't fire, or a different error if there's a new issue). Update the Plan 02 wire-in or the Plan 01 helper as needed.
  </how-to-verify>
  <resume-signal>
    Reply with verbatim: "WIN-01a: PASS — DI.fm Lounge played, first ICY title '<title>', first audio in <N>s, debug log confirms rewrite" OR "WIN-01a: FAIL — <error code from log + investigation note>".
  </resume-signal>
  <action>On the freshly-installed VM (launched via Start Menu shortcut): trigger fresh AA import in Settings → AudioAddict → enter premium credentials. Once import completes, navigate to DI.fm → DI.fm Lounge → Play. Observe audible audio + ICY title within 10s. Open debug log; confirm one `aa_normalize_stream_url: DI.fm https->http:` line. Append PASS/FAIL attestation + channel name + time-to-first-audio + first ICY title + sanitized debug log excerpt (REPLACE listen_key value with `<REDACTED>` before recording — T-56-03) to 56-05-UAT-LOG.md under heading `## WIN-01a: DI.fm Lounge fresh AA import`.</action>
  <verify>
    <automated>grep -c '## WIN-01a' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>Section `## WIN-01a: DI.fm Lounge fresh AA import` exists in 56-05-UAT-LOG.md with: channel played, time to first audio, first ICY title, sanitized debug log excerpt (no literal listen_key value), PASS/FAIL verdict with reason.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: WIN-01b UAT — DI.fm channel plays from Phase 42 settings-import ZIP roundtrip</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-12 part b: settings-import ZIP roundtrip — proves rewrite is at PLAY-TIME URI boundary, not at insert-time)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md (Manual-Only Verifications row 4)
  </read_first>
  <what-built>
    Same WIN-01 wiring as Task 2, but exercised against a DB row that was MIGRATED from Linux (NOT freshly inserted via AA import on the VM). This is the strict test of D-01 ("rewrite at the URI boundary, not at insert sites").
  </what-built>
  <how-to-verify>
    Pre-condition: a Linux-exported settings ZIP containing at least one DI.fm `https://` row exists on the VM (Task 1 verified this).

    On the Win11 VM, with MusicStreamer running:

    1. **Reset the DB state** to simulate a clean machine receiving the ZIP. Two options:
       - Option A (safer): close MusicStreamer, delete `%APPDATA%\musicstreamer\musicstreamer.sqlite3`, relaunch via Start Menu, skip the AA import on first run.
       - Option B (faster but messier): just import the ZIP on top of the current DB; the imported DI.fm row will replace existing rows by ID. Either works for the test; Option A is the cleanest.
    2. Open Settings → Import → select the Linux-exported ZIP. Confirm the import succeeds (toast or settings indicator).
    3. In the station list, locate a DI.fm station that came from the ZIP (NOT one freshly imported in Task 2). Verify it's the imported one by:
       - Checking `Get-Content "$env:APPDATA\musicstreamer\musicstreamer.sqlite3"` is NOT possible directly (binary), so instead use the in-app station editor → Streams section to see the URL. It MUST start with `https://` (the rewrite has not engaged yet because we haven't pressed play).
    4. Click Play on this imported DI.fm station.
    5. Same expectations as Task 2: audible audio within 10s + ICY title within 30s.
    6. **Negative-side check** — open the debug log and confirm the `aa_normalize_stream_url: DI.fm https->http: ...` line fired for THIS play (not just Task 2's play).

    Record in `56-05-UAT-LOG.md` under section `## WIN-01b: DI.fm settings-import ZIP roundtrip`:
    - Reset method used (Option A or B)
    - Original `https://` URL from the imported station (sanitize listen_key)
    - Channel name played
    - Time to first audio
    - First ICY title displayed
    - Debug log excerpt showing rewrite (sanitize listen_key)
    - Result: PASS or FAIL

    PASS criteria (ROADMAP SC #1 part b — the strict D-12 test):
    - Imported station's stored URL is `https://`, but playback succeeds AND debug log confirms rewrite to `http://` engaged at play-time.

    FAIL behavior: same as Task 2.
  </how-to-verify>
  <resume-signal>
    Reply with verbatim: "WIN-01b: PASS — settings-import DI.fm row played from stored https URL, rewrite engaged at play-time per debug log" OR "WIN-01b: FAIL — <observation>".
  </resume-signal>
  <action>Reset DB state (Option A preferred: close app, delete %APPDATA%\musicstreamer\musicstreamer.sqlite3, relaunch). Settings → Import → select the Linux-exported ZIP. Locate an imported DI.fm station; verify station editor shows stored URL starts with `https://` (rewrite has NOT yet engaged at insert time per D-01). Press Play. Observe audible audio + ICY title within 10s. Confirm fresh `aa_normalize_stream_url` debug log line for THIS play. Append attestation (reset method, sanitized stored URL, channel, time-to-first-audio, first ICY, sanitized debug log, PASS/FAIL verdict) to 56-05-UAT-LOG.md under `## WIN-01b: DI.fm settings-import ZIP roundtrip`.</action>
  <verify>
    <automated>grep -c '## WIN-01b' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>Section `## WIN-01b: DI.fm settings-import ZIP roundtrip` exists in 56-05-UAT-LOG.md with: reset method, sanitized stored https URL from the imported row, channel played, time to first audio, first ICY title, sanitized debug log excerpt (proving rewrite engaged at play-time on a row that arrived via ZIP), PASS/FAIL verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: WIN-02 UAT — SMTC overlay reads "MusicStreamer" with playing stream</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-CONTEXT.md (D-08, D-11 — manual UAT only)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-VALIDATION.md (Manual-Only Verifications rows 1 + 2)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-RESEARCH.md (Common Pitfalls → Pitfall 4: bare `python -m musicstreamer` yields false negative)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (the PRE-FIX baseline — for before/after comparison)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-04-SUMMARY.md (whether Plan 04 made any code-side change that affects this UAT)
  </read_first>
  <what-built>
    WIN-02 closure: SMTC overlay shows the friendly app name. Depending on Plan 56-04 branch:
    - docs-only / investigate: the fix was Plan 56-03 Task 5 (force-fresh install); this UAT is the ATTESTATION that the fix took.
    - code-fix: the fix was Plan 56-04 Task 4 (literal alignment) + the freshly rebuilt installer; this UAT proves the alignment closed the gap.
  </what-built>
  <how-to-verify>
    On the Win11 VM, with MusicStreamer freshly installed (per Task 1) and launched via Start Menu shortcut:

    1. Pre-check (covers ROADMAP SC #2 + #4):
       ```powershell
       Get-StartApps | Where-Object Name -like 'MusicStreamer*'
       ```
       Expected: row with `Name = MusicStreamer`, `AppID = org.lightningjim.MusicStreamer`. Record the literal output.

    2. Cross-check in-process AUMID (ROADMAP SC #4):
       Use Method A from Plan 56-03 Task 3 (read the existing AUMID log line on startup if Phase 43.1 left one in place), OR check it via the temporary debug-log patch from Method B. Record the readback string. Confirm it matches the AppID from step 1.

    3. Start playback of any stream (DI.fm Lounge, SomaFM, anything that produces audio).

    4. Open SMTC overlay: press `Win+K` (or click the media flyout in the taskbar).

    5. Observe the app name displayed above the media controls. Take a screenshot to `.planning/phases/56-windows-di-fm-smtc-start-menu/screenshots/56-05-smtc-final.png` (create dir if needed).

    6. Verify against ROADMAP SC #3:
       - Expected: SMTC overlay reads literally "MusicStreamer".
       - FAIL conditions: "Unknown app", empty string, "MusicStreamer.exe", any other variant.

    Record in `56-05-UAT-LOG.md` under section `## WIN-02: SMTC overlay reads "MusicStreamer"`:
    - `Get-StartApps` literal output (AppID column)
    - In-process AUMID readback (literal string)
    - Match check: AppID == readback == `org.lightningjim.MusicStreamer` (Y/N)
    - Stream played during SMTC observation
    - SMTC overlay text observed (verbatim)
    - Screenshot path
    - Result: PASS or FAIL with reason

    PASS criteria (ROADMAP SC #2, #3, #4 simultaneously):
    - Get-StartApps shows AppID = `org.lightningjim.MusicStreamer` (SC #2)
    - In-process readback == AppID (SC #4)
    - SMTC overlay text reads "MusicStreamer" (SC #3)

    FAIL behavior:
    - If only SC #3 fails (AppID + readback OK but SMTC still shows "Unknown app"): root cause may be deeper than D-09 #1/#2/#3 — escalate to follow-up phase via D-09 #4 / Plan 56-04 Task 5 path.
    - If SC #2 or #4 fail: regression in Plan 56-04 — re-verify literal alignment + drift-guard pytest.
  </how-to-verify>
  <resume-signal>
    Reply with verbatim: "WIN-02: PASS — SMTC reads 'MusicStreamer', AppID=`<value>`, readback=`<value>`, screenshot at <path>" OR "WIN-02: FAIL — SMTC reads '<observed>', AppID=<...>, readback=<...>, root cause guess: <D-09 # or unknown>".
  </resume-signal>
  <action>Run `Get-StartApps | Where-Object Name -like 'MusicStreamer*'` on the freshly-installed VM. Capture in-process AUMID readback (Method A or B from Plan 56-03 Task 3). Start any stream. Open SMTC via Win+K. Observe app name. Screenshot to .planning/phases/56-windows-di-fm-smtc-start-menu/screenshots/56-05-smtc-final.png OR transcribe verbatim. Append to 56-05-UAT-LOG.md under `## WIN-02: SMTC overlay reads "MusicStreamer"` with: literal Get-StartApps output, in-process readback, match check (Y/N), stream played, exact SMTC text, screenshot path, PASS/FAIL verdict.</action>
  <verify>
    <automated>grep -c '## WIN-02' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md | grep -qv '^0$'</automated>
  </verify>
  <done>Section `## WIN-02: SMTC overlay reads "MusicStreamer"` exists in 56-05-UAT-LOG.md with: literal Get-StartApps AppID, in-process readback, AppID == readback == `org.lightningjim.MusicStreamer` match check, stream played, verbatim SMTC overlay text, screenshot path, PASS/FAIL verdict.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: Phase completion attestation</name>
  <files>.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md</files>
  <read_first>
    - 56-05-UAT-LOG.md (the three attestations from Tasks 2-4)
    - .planning/ROADMAP.md (Phase 56 entry — the four success criteria)
  </read_first>
  <decision>
    Based on Tasks 2-4 outcomes, decide:
    - **All three PASS** → Phase 56 ready to ship; user runs `/gsd-verify-work` (or equivalent goal-backward verification) and `/gsd-complete-phase`.
    - **WIN-01a or WIN-01b FAIL** → Plan 56-01 or 56-02 fix needed; reopen those plans for revision.
    - **WIN-02 FAIL** → Plan 56-04 needs deeper investigation OR WIN-02 splits to a follow-up phase (Plan 56-04 Task 5 already provides the framework for this option).
    - **Mixed results** → record in the UAT log; user decides whether to ship partial (e.g., WIN-01 only) and split WIN-02 to follow-up.
  </decision>
  <context>
    This task is the gate before phase complete. Do not proceed to `/gsd-verify-work` if any of the three attestations is FAIL; instead, return to the responsible plan for revision.
  </context>
  <options>
    <option id="ship-phase">
      <name>Ship Phase 56 (all three PASS)</name>
      <pros>Both WIN-01 and WIN-02 closed; ROADMAP SCs #1-#4 attested; phase complete.</pros>
      <cons>None — this is the happy path.</cons>
    </option>
    <option id="revise-win01">
      <name>Revise WIN-01 (Plan 56-01 or 56-02 fix)</name>
      <pros>Targeted fix where the actual gap is.</pros>
      <cons>Adds a revision cycle; UAT 2/3 must rerun after the fix lands.</cons>
    </option>
    <option id="split-win02">
      <name>Split WIN-02 to follow-up phase (e.g. Phase 56.1)</name>
      <pros>Ships WIN-01 in v2.1.56 immediately; WIN-02 gets its own deeper-investigation phase. Avoids blocking v2.1.56 on a gnarly shell-binding bug.</pros>
      <cons>WIN-02 stays open across milestone boundary if v2.1 ships before resolution.</cons>
    </option>
    <option id="defer-phase">
      <name>Defer phase ship pending further investigation</name>
      <pros>Honest acknowledgement that something is blocking; user reopens Plan 03 or 04 with new hypotheses.</pros>
      <cons>Phase remains open; user / future Claude must remember to come back.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with the chosen option + any follow-up actions. If "ship-phase", confirm willingness to proceed to `/gsd-verify-work` next.
  </resume-signal>
  <action>Cross-reference the three PASS/FAIL attestations recorded in Tasks 2-4. Choose ONE option per &lt;options&gt;. Append a `## Phase Completion Decision` section to 56-05-UAT-LOG.md naming the chosen option + follow-up actions. If "ship-phase": next step is `/gsd-verify-work` then `/gsd-complete-phase`. If revision: reopen the responsible plan. If split: recommend `/gsd-add-phase` for the follow-up.</action>
  <verify>
    <automated>grep -ic 'PASS\|FAIL' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md | awk '$1 >= 3 {exit 0} {exit 1}' && ! grep -E 'listen_key=[a-zA-Z0-9]{3,}' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md</automated>
  </verify>
  <done>UAT log contains at least 3 PASS/FAIL attestations (one per WIN-01a / WIN-01b / WIN-02). No literal listen_key values present (T-56-03 — all REDACTED). `## Phase Completion Decision` section exists with chosen option + follow-up actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linux build host ↔ Win11 VM | Source tree + installer artifact + settings ZIP all transferred manually. No auto-sync; trust is "human moves the right files." |
| UAT log ↔ phase-complete decision | Manual attestations gate phase completion; no automated verification of WIN-02 SMTC display name on Linux CI is possible (D-11). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03 | Information Disclosure | UAT log captures `aa_normalize_stream_url` debug line containing DI.fm `listen_key=...` query parameter | mitigate | Tasks 2 + 3 explicitly instruct the human to sanitize listen_key in recorded log lines (replace with `<REDACTED>`). The UAT log lives in `.planning/` (private repo) but the discipline is established for any future log copy-paste into shared docs. |
| T-56-UAT-A | Spoofing | Tester records "PASS" without actually performing the verification | accept | Single-author personal project; no incentive to falsify; the UAT log is for Kyle's own future reference. The structured attestation format (verbatim quotes from `Get-StartApps`, screenshot paths) makes accidental skipping obvious. |
| T-56-UAT-B | Tampering | UAT runs on stale install (didn't rebuild post-Plan-56-02 wiring) → false NEGATIVE for WIN-01 | mitigate | Task 1 explicitly requires confirming the freshly-built installer was rebuilt POST Plan 56-02 wiring AND that the post-uninstall force-clean steps removed the prior LNK + install dir. Two-pronged check (commit hash of source + Test-Path on uninstall paths). |
| T-56-UAT-C | Denial of Service | DI.fm premium account credentials lost mid-UAT | accept | Single-author project; user controls credentials. If credentials lapse, the AA import test fails for an environmental reason; reschedule UAT after credential refresh. |
| T-56-UAT-D | Repudiation | UAT outcome not durably recorded → ambiguity in phase complete state | mitigate | Task 5 explicitly creates `56-05-UAT-LOG.md` with the four ROADMAP SC attestations, screenshot paths, and the phase-completion decision. The log is committed to git. |
</threat_model>

<verification>
- `.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` exists
- `grep -c '## WIN-01a' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` returns >= 1
- `grep -c '## WIN-01b' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` returns >= 1
- `grep -c '## WIN-02' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` returns >= 1
- `grep -ic 'PASS\|FAIL' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` returns >= 3 (one PASS/FAIL per attestation)
- `grep -E 'listen_key=[a-zA-Z0-9]{3,}' .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md` returns 0 matches (T-56-03 — listen_key must be REDACTED, not literal)
- Full repo regression on build host: `uv run pytest -x` exits 0
</verification>

<success_criteria>
- WIN-01a (DI.fm fresh AA import): PASS attested with channel name, time to first audio, first ICY title, debug log evidence (D-12 part a, ROADMAP SC #1)
- WIN-01b (DI.fm settings-import ZIP roundtrip): PASS attested with stored-https-URL evidence + play-time rewrite log evidence (D-12 part b, ROADMAP SC #1 strict test)
- WIN-02 SMTC + AUMID: PASS attested with `Get-StartApps` AppID, in-process readback, SMTC overlay text "MusicStreamer", screenshot (ROADMAP SC #2, #3, #4)
- All listen_key references in the UAT log are REDACTED (T-56-03)
- Phase-completion decision recorded in Task 5 (ship / revise / split / defer)
</success_criteria>

<output>
After completion, create `.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-SUMMARY.md` with:
- Three PASS/FAIL attestations summarized
- Phase-completion decision from Task 5
- Pointers to UAT log + screenshots
- If "ship-phase": next-step note "Proceed to /gsd-verify-work then /gsd-complete-phase"
- If revision/split: next-step note with which plan to reopen or which follow-up phase to add
</output>
