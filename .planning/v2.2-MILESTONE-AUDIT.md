---
milestone: v2.2
audited: 2026-06-27
status: gaps_found
scores:
  requirements: 62/64 (2 genuinely pending human action; 14 done-but-doc-stale)
  phases: 26/26 executed
  integration: 4/4 chains wired (production)
  flows: 4/4 E2E flows complete (1 with broken test coverage)
gaps:
  critical:
    - id: "TEST-REGRESSION-97x89B"
      type: "failing tests"
      severity: critical
      detail: "Phase 97 D-01 removed the url_edit widget from EditStationDialog (streams table is now the sole URL editor); Phase 89B/avatar add-path tests were never migrated. 9 tests fail at collection/runtime with AttributeError: 'EditStationDialog' object has no attribute 'url_edit'."
      files:
        - "tests/test_twitch_provider_assign.py (8 failed)"
        - "tests/test_edit_station_dialog_avatar.py::test_twitch_url_enables_refresh_btn (1 failed)"
      evidence: ".venv/bin/python -m pytest -> 9 failed, 4 passed (2026-06-27)"
      impact: "Twitch/YouTube avatar add-path (a milestone feature) has zero passing automated coverage. Production wiring is intact; only the tests are broken."
      fix: "Migrate the 9 tests to set the station URL via the streams table / _get_canonical_url_live path instead of the removed url_edit widget."
  requirements:
    - id: "VER-02-J"
      status: "pending (human action required)"
      phase: "88"
      detail: "Win11 VM packaging UAT — fresh install of v2.2 installer on a Win11 VM that previously had v2.1, all golden-path features verified. This is an inherently human, target-platform test. Not automatable; not done."
      verification_status: "Phase 88 UAT.md=passed for code; VM clean-install UAT not yet run."
human_uat_pending:  # built + automated-verified; awaiting human acceptance on target platform
  - phase: "87B"  # GBS zero-token single-song add (live tokens==0 capture)
  - phase: "88.1" # SMTC media overlay + media keys on bundled Windows build
  - phase: "88.2" # GBS.FM in-app login dialog on bundled Windows build
  - phase: "89.1" # channel avatar re-key per-provider UAT
  - phase: "96.2" # newly-discovered merge dropdown (1 skipped/unresolved)
doc_drift:  # NOT gaps — requirements done/verified but traceability table never updated
  - ids: ["PKG-LIN-FP-02","PKG-LIN-FP-05","PKG-LIN-FP-07","PKG-LIN-FP-08"]
    note: "Verified in-sandbox via Phase 86 human UAT (86-VERIFICATION.md SC1-SC4 PASS). Table still says Pending."
  - ids: ["PKG-LIN-FP-01","PKG-LIN-FP-03","PKG-LIN-FP-04","PKG-LIN-FP-09","PKG-LIN-FP-10","PKG-LIN-FP-11"]
    note: "Manifest/build-config requirements satisfied by the shipped .flatpak (app-id locked, GPG-signed bundle per project memory). Table not reconciled."
  - ids: ["WIN-02"]
    note: "Inno Setup [Icons] AUMID directive — code-complete in Phase 88; acceptance folds into VER-02-J VM UAT."
  - ids: ["ART-AVATAR-01","ART-AVATAR-02"]
    note: "Implemented in code: repo.py:321 ADD COLUMN channel_avatar_path TEXT; paths.py:115 channel-avatars dir. Checkbox never ticked (later re-keyed per-provider in 89.1)."
conditional_not_fired:
  - id: "MON-BUFFER-01"
    note: "Trigger-gated (Phase 93 conditional). Trigger never fired; closed as not-needed."
nyquist:
  validation_files: 22
  overall: "substantially compliant — 22 phases carry VALIDATION.md; remaining are spike/trivial-fix phases"
---

# v2.2 Milestone Audit — Package Building and QOL features/tweaks

**Status: ⚠ gaps_found** (1 critical test regression + 1 genuinely-pending human UAT; remainder is doc reconciliation + human-UAT debt)

## Summary

All 26 phases executed; all 91 plans have summaries. Cross-phase integration is sound — the
integration checker traced all four feature chains (channel avatar, codec/bitrate stats, URL
canonicalization, MPRIS) and found them wired end-to-end in production with concrete file:line
evidence. E2E flows complete.

The milestone is **functionally done**. What blocks a clean close:

### 1. Critical — 9 failing tests (cross-phase regression)
Phase 97 removed the `url_edit` widget; Phase 89B's Twitch/YouTube avatar add-path tests were
never migrated and now fail with `AttributeError`. Production code is fine (the add-path reads the
URL via `_get_canonical_url_live`, not `url_edit`), but the feature's automated coverage is broken.
This contradicts the milestone's test-clean baseline and is a reproducible defect, so it forces
`gaps_found`.

**Fix:** migrate the 9 tests (`test_twitch_provider_assign.py` ×8 +
`test_edit_station_dialog_avatar.py::test_twitch_url_enables_refresh_btn`) to set the URL via the
streams table instead of `url_edit`. ~1 small fix plan.

### 2. Genuinely pending — VER-02-J (Win11 VM packaging UAT)
A human must do a fresh install of the v2.2 Windows installer on a Win11 VM (over a prior v2.1) and
verify the golden path. Not automatable. WIN-02 (AUMID directive) is code-complete and its
acceptance folds into this VM test.

### 3. Human-UAT debt (built + auto-verified, awaiting human acceptance)
Phases 87B, 88.1, 88.2, 89.1, 96.2 carry 13 outstanding human-UAT items (mostly Windows/packaging
acceptance). These were surfaced by `/gsd:audit-uat` and are acceptance checks, not unbuilt code.

### 4. Documentation drift (no code impact)
The REQUIREMENTS.md traceability table shows 15 "Pending" rows, but 14 are actually done:
- Flatpak FP-02/05/07/08 were **verified in-sandbox** via Phase 86 human UAT (86-VERIFICATION.md SC1–SC4 PASS).
- FP-01/03/04/09/10/11 are manifest/build requirements satisfied by the shipped signed `.flatpak`.
- ART-AVATAR-01/02 are implemented (`repo.py:321`, `paths.py:115`).
- WIN-02 is code-complete.
- MON-BUFFER-01 is a conditional that never fired.

The table needs reconciliation, but none of this is missing work.

## Integration Verdict (from gsd-integration-checker)

| Chain | Status | Evidence |
|-------|--------|----------|
| Channel avatar (89/89A/89.1/89B/89c) | WIRED (prod); coverage PARTIAL | producer edit_station_dialog → assets.write_provider_avatar → repo.update_provider_avatar_path → mappers `provider_avatar_path` → now_playing_panel + cover_art. No dangling old-key reads. Add-path tests broken (see gap #1). |
| Codec/bitrate stats (98) | WIRED | player.audio_format_detected → main_window.py:551 QueuedConnection → now_playing.update_detected_format |
| URL canonicalization (97) | WIRED | Station.canonical_url read by all 5 metadata consumers; playback intentionally left on stream URL |
| MPRIS (91 → gates FP-08) | WIRED | mpris2.py present; 12 passed/1 skipped(env); FP-08 verified in-sandbox (86-VERIFICATION SC4 PASS) |

## Recommendation

Close the test regression (gap #1) before completing the milestone — it's a quick, well-scoped
fix. VER-02-J and the human-UAT debt are acceptance activities that can either be done now or
explicitly accepted as deferred at milestone close (documented in MILESTONES.md). Reconcile the
REQUIREMENTS.md traceability table as part of either path.
