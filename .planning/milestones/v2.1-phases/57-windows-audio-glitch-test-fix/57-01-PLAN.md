---
phase: 57-windows-audio-glitch-test-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_media_keys_smtc.py
autonomous: true
requirements: [WIN-04]
tags: [windows, smtc, test-fix, asyncmock, win-04]

must_haves:
  truths:
    - "test_thumbnail_from_in_memory_stream passes — store_async is awaited correctly via AsyncMock, no `MagicMock not awaitable` error (ROADMAP SC #3)"
    - "Full test suite passes with no new failures (ROADMAP SC #4)"
    - "Production smtc.py unchanged — D-10 invariant preserved (test-only fix)"
  artifacts:
    - path: "tests/test_media_keys_smtc.py"
      provides: "AsyncMock-aware DataWriter().store_async fixture (D-08 minimal patch)"
      contains: "AsyncMock"
  key_links:
    - from: "tests/test_media_keys_smtc.py::_build_winrt_stubs"
      to: "tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream"
      via: "DataWriter.return_value.store_async = AsyncMock(...)"
      pattern: "DataWriter\\.return_value\\.store_async"
---

<objective>
Replace the implicit MagicMock for `DataWriter().store_async` in `tests/test_media_keys_smtc.py::_build_winrt_stubs` with an `AsyncMock` so `await writer.store_async()` in `_await_store` resolves cleanly. Closes WIN-04.

Purpose: `test_thumbnail_from_in_memory_stream` currently raises `TypeError: object MagicMock can't be used in 'await' expression` because `_build_winrt_stubs` returns a plain `MagicMock` for the `DataWriter` class and its instances inherit `MagicMock` for every attribute — including `store_async`, which production code awaits. Per CONTEXT D-08/D-09, the minimal fix is one attribute on the `DataWriter` class mock plus an `AsyncMock` import; production `smtc.py` is NOT touched (D-10).

Output: Modified `tests/test_media_keys_smtc.py` (one new import addition + one new attribute set), `test_thumbnail_from_in_memory_stream` passing, full Linux test suite green.

This plan is independent of Track A (WIN-03 diagnostic + fixes) and lands first to clear CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md
@.planning/phases/57-windows-audio-glitch-test-fix/57-PATTERNS.md
@tests/test_media_keys_smtc.py
@musicstreamer/media_keys/smtc.py

<interfaces>
<!-- Production code (READ-ONLY this plan; D-10) -->
<!-- musicstreamer/media_keys/smtc.py:52 -->
async def _await_store(writer):
    await writer.store_async()

<!-- Driven by smtc.py:248 (approx) via asyncio.run(_await_store(writer)) inside _build_thumbnail_ref. -->
<!-- Only ONE awaited winrt method exists in production code today: writer.store_async(). -->
<!-- D-09 explicitly scopes this fix to that one attribute. -->

<!-- Existing fixture site (TO BE PATCHED) -->
<!-- tests/test_media_keys_smtc.py:13 (current import) -->
from unittest.mock import MagicMock

<!-- tests/test_media_keys_smtc.py:93-96 (current fixture body, the patch site) -->
storage_streams = types.ModuleType("winrt.windows.storage.streams")
storage_streams.InMemoryRandomAccessStream = MagicMock(name="InMemoryRandomAccessStream")
storage_streams.DataWriter = MagicMock(name="DataWriter")
storage_streams.RandomAccessStreamReference = MagicMock(name="RandomAccessStreamReference")

<!-- Failing test (post-fix expectation: passes) -->
<!-- tests/test_media_keys_smtc.py:438 -->
def test_thumbnail_from_in_memory_stream(mock_winrt_modules, qtbot, tmp_path, monkeypatch, qapp):
    ...
    assert stream_mod.DataWriter.called, "DataWriter must wrap the stream"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AsyncMock to import + attach AsyncMock store_async to DataWriter mock</name>
  <files>tests/test_media_keys_smtc.py</files>
  <read_first>
    - tests/test_media_keys_smtc.py (full file — verify line 13 import shape and lines 93-96 fixture shape; the file is the only edit site)
    - musicstreamer/media_keys/smtc.py (READ-ONLY; confirm `await writer.store_async()` is the only awaited winrt call in production — gives confidence the one-attribute fix is sufficient under D-09)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-CONTEXT.md (decisions D-08 minimal patch shape; D-09 scope-to-store_async-only; D-10 production-untouched invariant)
    - .planning/phases/57-windows-audio-glitch-test-fix/57-PATTERNS.md (the `tests/test_media_keys_smtc.py::_build_winrt_stubs` section — exact diff shape locked at the bottom of that section)
  </read_first>
  <behavior>
    - Test 1: Before patch — `test_thumbnail_from_in_memory_stream` fails with `TypeError: object MagicMock can't be used in 'await' expression` (or similar). Lock RED state by running the test once before applying the patch.
    - Test 2: After patch — `test_thumbnail_from_in_memory_stream` passes; the existing assertion `assert stream_mod.DataWriter.called, "DataWriter must wrap the stream"` (line 456) still holds.
    - Test 3: After patch — full `pytest tests/test_media_keys_smtc.py` is green (no other test in this file regresses on the AsyncMock change because no other test awaits `DataWriter().store_async()`).
    - Test 4: After patch — full `pytest` suite is green (WIN-04 ROADMAP SC #4: no new failures elsewhere).
  </behavior>
  <action>
    Two-line edit in tests/test_media_keys_smtc.py. NO other file is edited (D-10 invariant: musicstreamer/media_keys/smtc.py stays read-only this plan).

    EDIT 1 — line 13 (the existing `from unittest.mock import MagicMock`):

    Current:
    ```python
    from unittest.mock import MagicMock
    ```

    Replace with:
    ```python
    from unittest.mock import AsyncMock, MagicMock
    ```

    Rationale: Top-of-file `from`-import matches the existing convention (line 13 already uses `from unittest.mock import MagicMock`). Inline import inside `_build_winrt_stubs` would also work but breaks the existing convention and adds one more line; D-08 explicitly permits either, so we pick the conventional shape per CONTEXT Claude's Discretion section.

    EDIT 2 — at line 95 (the `storage_streams.DataWriter = MagicMock(name="DataWriter")` assignment), add ONE new line immediately after it:

    Current (lines 93-96):
    ```python
    storage_streams = types.ModuleType("winrt.windows.storage.streams")
    storage_streams.InMemoryRandomAccessStream = MagicMock(name="InMemoryRandomAccessStream")
    storage_streams.DataWriter = MagicMock(name="DataWriter")
    storage_streams.RandomAccessStreamReference = MagicMock(name="RandomAccessStreamReference")
    ```

    Replace with (single inserted line, comment + assignment):
    ```python
    storage_streams = types.ModuleType("winrt.windows.storage.streams")
    storage_streams.InMemoryRandomAccessStream = MagicMock(name="InMemoryRandomAccessStream")
    storage_streams.DataWriter = MagicMock(name="DataWriter")
    # Phase 57 / WIN-04 D-08: instances of DataWriter must carry an awaitable
    # `store_async` attribute. Production `_await_store` (smtc.py:52) awaits it.
    # Scoped per D-09 to ONLY this attribute — no broader winrt-async audit.
    storage_streams.DataWriter.return_value.store_async = AsyncMock(name="store_async")
    storage_streams.RandomAccessStreamReference = MagicMock(name="RandomAccessStreamReference")
    ```

    The comment is part of the patch (3 lines + 1 assignment = 4 added lines net). It documents the WIN-04 lineage so a future contributor reading `_build_winrt_stubs` understands why this single attribute deviates from the surrounding `MagicMock`-only pattern.

    Why `return_value.store_async` and not just `store_async`:
    - `DataWriter` is the CLASS mock (`MagicMock(name="DataWriter")`).
    - Production code calls `DataWriter(stream)` → `MagicMock`'s `return_value` is the instance mock.
    - `_build_thumbnail_ref` does `writer = DataWriter(stream)` then `await writer.store_async()`.
    - So the awaitable must live on `DataWriter.return_value.store_async`, not on the class mock itself.
    - Setting `DataWriter.store_async = AsyncMock(...)` would NOT fix the test — the await happens on an instance.

    Per D-09 scope: do NOT broaden — `InMemoryRandomAccessStream`, `RandomAccessStreamReference.create_from_stream`, and `writer.write_bytes(data)` remain plain `MagicMock` because they are synchronous in production (smtc.py:210+). If a future async winrt method enters production, it will get its own `AsyncMock` then; no anticipatory work this phase.

    NO edit to:
    - `musicstreamer/media_keys/smtc.py` (D-10 invariant — production code is correct, Phase 43.1 UAT validated `asyncio.run(_await_store(writer))` against real winrt).
    - `_make_media_player_instance` (lines 76-88) — unrelated to the WIN-04 surface.
    - `mock_winrt_modules` fixture (lines 118-129) — fixture cleanup is unchanged.
    - Any other test file in `tests/` — grep confirms there is no existing AsyncMock usage anywhere in `tests/`, so this patch introduces the convention freshly per D-08 (no fan-out to update).
  </action>
  <verify>
    <automated>pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x -v 2>&amp;1 | tail -20 &amp;&amp; pytest tests/test_media_keys_smtc.py -x 2>&amp;1 | tail -10 &amp;&amp; pytest 2>&amp;1 | tail -10 &amp;&amp; grep -nq "from unittest.mock import AsyncMock, MagicMock" tests/test_media_keys_smtc.py &amp;&amp; grep -nq "DataWriter.return_value.store_async = AsyncMock" tests/test_media_keys_smtc.py &amp;&amp; { git diff --quiet musicstreamer/media_keys/smtc.py || { echo "FAIL: smtc.py was edited (D-10 violation)"; exit 1; }; } &amp;&amp; echo "OK: smtc.py untouched (D-10 preserved)"</automated>
  </verify>
  <done>
    - `pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` exits 0
    - `pytest tests/test_media_keys_smtc.py -x` exits 0 (no regression in sibling SMTC tests)
    - `pytest` (full suite) exits 0 (ROADMAP SC #4)
    - `grep -q "from unittest.mock import AsyncMock, MagicMock" tests/test_media_keys_smtc.py` returns 0 (import added per D-08)
    - `grep -q "DataWriter.return_value.store_async = AsyncMock" tests/test_media_keys_smtc.py` returns 0 (the one-attribute fix is in place)
    - `git diff musicstreamer/media_keys/smtc.py` is empty (D-10: production code unchanged)
    - `git diff tests/test_media_keys_smtc.py` shows ONLY the import line and the one-attribute insertion (plus its 3-line comment) — no other lines touched (D-09 minimal-patch invariant)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced this plan) | Test-only patch; no runtime/IPC/network surface change. The mock object graph is process-local pytest fixture state, untrusted-by-construction (it's the test, not data crossing a trust boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | Tampering | tests/test_media_keys_smtc.py fixture state | accept | Test fixtures are not a trust boundary; AsyncMock is stdlib and process-local. No PII, no network, no persistence. |
| T-57-01-02 | Information Disclosure | smtc.py production async path | accept | Production code untouched (D-10). Phase 43.1 UAT already validated `asyncio.run(_await_store(writer))` against real winrt; risk profile unchanged. |

**Phase-level note:** WIN-04 changes test infrastructure only — no high-severity threats. Existing GStreamer URI funnel + DI.fm rewrite invariants (Phase 56 D-04) are not touched by this plan.
</threat_model>

<verification>
- `pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` exits 0 (ROADMAP SC #3)
- `pytest tests/test_media_keys_smtc.py -x` exits 0 (sibling tests unaffected)
- `pytest` (full suite) exits 0 (ROADMAP SC #4)
- `git diff musicstreamer/media_keys/smtc.py` is empty (D-10)
</verification>

<success_criteria>
- ROADMAP Phase 57 SC #3: `test_thumbnail_from_in_memory_stream` passes — `store_async` is awaited correctly via `AsyncMock`, no `MagicMock not awaitable` error.
- ROADMAP Phase 57 SC #4: Full test suite passes with no new failures on the fix branch.
- D-08 minimal patch shape: import-line addition + one-attribute insertion.
- D-09 scope: only `store_async` gets `AsyncMock`; other winrt mocks unchanged.
- D-10 invariant: `musicstreamer/media_keys/smtc.py` not edited.
</success_criteria>

<output>
After completion, create `.planning/phases/57-windows-audio-glitch-test-fix/57-01-SUMMARY.md` documenting:
- The exact diff applied (import line + DataWriter.return_value.store_async assignment).
- Confirmation that `pytest tests/test_media_keys_smtc.py::test_thumbnail_from_in_memory_stream -x` now passes.
- Confirmation that `git diff musicstreamer/media_keys/smtc.py` is empty (D-10 preserved).
- Confirmation that the full pytest suite is green.
- Note for Plan 57-05 UAT: WIN-04 closure can be verified solely by running the full test suite on the merge candidate branch — no Win11 VM step required.
</output>
