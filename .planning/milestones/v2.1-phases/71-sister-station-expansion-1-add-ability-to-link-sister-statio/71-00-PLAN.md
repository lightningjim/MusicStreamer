---
phase: 71
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_station_siblings.py
  - tests/test_add_sibling_dialog.py
  - tests/test_settings_export.py
  - tests/test_edit_station_dialog.py
  - tests/test_now_playing_panel.py
  - tests/test_constants_drift.py
autonomous: true
requirements:
  - D-01
  - D-05
  - D-06
  - D-07
  - D-08
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
decisions_covered:
  - D-01
  - D-05
  - D-06
  - D-07
  - D-08
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
must_haves:
  truths:
    - "All 22 RED tests from 71-VALIDATION.md fail with ImportError/AttributeError (RED contract; not pytest.fail placeholders)"
    - "test_station_siblings.py exists with _mk factory + repo fixture (PRAGMA foreign_keys = ON) mirroring tests/test_aa_siblings.py and tests/test_repo.py"
    - "test_add_sibling_dialog.py exists with FakeRepo + qtbot dialog fixture mirroring tests/test_discovery_dialog.py"
    - "test_constants_drift.py extended with test_richtext_baseline_unchanged_by_phase_71 pinning EXPECTED_RICHTEXT_COUNT = 3 (4 today minus EditStationDialog _sibling_label removed in Plan 71-03)"
    - "Full suite + the 22 new RED tests run cleanly without runtime errors; 22 RED failures are the expected RED state"
  artifacts:
    - path: "tests/test_station_siblings.py"
      provides: "Pure helper + repo CRUD RED tests"
      contains: "def _mk("
    - path: "tests/test_add_sibling_dialog.py"
      provides: "AddSiblingDialog picker RED tests"
      contains: "class FakeRepo"
    - path: "tests/test_constants_drift.py"
      provides: "Extended with test_richtext_baseline_unchanged_by_phase_71"
      contains: "EXPECTED_RICHTEXT_COUNT"
  key_links:
    - from: "tests/test_station_siblings.py"
      to: "musicstreamer.repo + musicstreamer.url_helpers"
      via: "import-from contract"
      pattern: "from musicstreamer\\.(repo|url_helpers) import"
    - from: "tests/test_add_sibling_dialog.py"
      to: "musicstreamer.ui_qt.add_sibling_dialog"
      via: "import AddSiblingDialog"
      pattern: "from musicstreamer\\.ui_qt\\.add_sibling_dialog import AddSiblingDialog"
---

<objective>
Wave 0 RED contract for Phase 71. Create all test scaffolding for sibling-station expansion: pure helpers (find_manual_siblings, merge_siblings), repo CRUD (add_sibling_link, remove_sibling_link, list_sibling_links), schema (station_siblings table), AddSiblingDialog picker behavior, EditStationDialog chip-row behavior, NowPlaying merged display, settings_export ZIP round-trip, and T-40-04 RichText baseline drift-guard.

Purpose: Lock the exact behavioral contract Plans 71-01 through 71-07 will satisfy. Per project convention (Phases 47/62/68/70), Wave 0 uses **fixture mirroring** — tests import from non-existent / not-yet-extended modules and the resulting ImportError/AttributeError IS the RED state. NO pytest.fail placeholders.

Output: 22 RED tests across 6 test files. All RED at end of plan. All turn GREEN by end of Wave 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-CONTEXT.md
@.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-RESEARCH.md
@.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-UI-SPEC.md
@.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-VALIDATION.md
@.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-PATTERNS.md
@tests/test_aa_siblings.py
@tests/test_repo.py
@tests/test_discovery_dialog.py
@tests/test_edit_station_dialog.py
@tests/test_now_playing_panel.py
@tests/test_settings_export.py
@tests/test_constants_drift.py

<interfaces>
<!-- Tests written here lock these contracts for Plans 71-01..07 -->

Repo CRUD (Plan 71-01 implements):
  Repo.add_sibling_link(self, a_id: int, b_id: int) -> None       # INSERT OR IGNORE, normalize (lo, hi)
  Repo.remove_sibling_link(self, a_id: int, b_id: int) -> None    # normalize (lo, hi), silent no-op if absent
  Repo.list_sibling_links(self, station_id: int) -> list[int]     # UNION query, returns sibling ids

url_helpers (Plan 71-02 implements):
  find_manual_siblings(stations, current_station_id: int, link_ids: list[int]) -> list[tuple[str, int, str]]
  merge_siblings(aa_siblings, manual_siblings) -> list[tuple[str, int, str]]   # AA wins dedup by station_id

AddSiblingDialog (Plan 71-04 implements):
  class AddSiblingDialog(QDialog) at musicstreamer/ui_qt/add_sibling_dialog.py
  __init__(self, station, repo, parent=None)
  Public read-only attr after exec(): dialog._linked_station_name: str
  Children accessible via objectName / findChild:
    self._provider_combo (QComboBox)
    self._search_edit (QLineEdit)
    self._station_list (QListWidget)
    self._button_box (QDialogButtonBox) with Ok button text "Link Station", Cancel "Don't Link"

EditStationDialog (Plan 71-03 implements):
  sibling_toast = Signal(str)  # new class-level signal
  self._sibling_row_widget (QWidget) with FlowLayout(self._sibling_row_widget, h_spacing=4, v_spacing=4)
  Each manual chip is a compound QWidget with objectName f"sibling_chip_{sibling_id}"
    Children: [name_btn: QPushButton, unlink_btn: QPushButton("×")]
  AA chips are bare QPushButton, no compound wrapper (objectName f"sibling_aa_chip_{sibling_id}")
  "+ Add sibling" button: QPushButton with objectName "add_sibling_btn"

NowPlayingPanel (Plan 71-05 swaps call site):
  _refresh_siblings() — calls merge_siblings(find_aa_siblings(...), find_manual_siblings(...))

settings_export (Plan 71-07 implements):
  _station_to_dict — emits "siblings": [partner_names] list
  commit_import — second pass resolves sibling names → ids → INSERT OR IGNORE into station_siblings
  Forward-compat read: list(data.get("siblings") or [])
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZIP import → DB | Untrusted JSON in ZIP siblings list crosses into station_siblings inserts |
| User-typed station name → Qt chip label | Tampering surface for HTML injection (mitigated by setText plain-text default) |
| sibling://{id} href payload → int() | Non-integer payload mitigated by Phase 51 try/except ValueError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | T (Tampering) | EditStationDialog chip label | mitigate | Test `test_chip_label_uses_plain_text_no_richtext` asserts compound chip's name_btn is a QPushButton (not a QLabel with RichText). T-40-04 invariant. |
| T-71-02 | T (Tampering) | NowPlaying _sibling_label HTML output | mitigate | Existing `html.escape(station_name, quote=True)` in url_helpers.render_sibling_html line 263; reused unchanged via merge_siblings; existing test_aa_siblings.py covers escape behavior — no new test needed. |
| T-71-03 | T (Tampering) | ZIP siblings list import | mitigate | Test `test_siblings_unresolved_name_silently_dropped` asserts unresolved names produce no DB write and no exception. |
| T-71-04 | T (Tampering) | sibling://{id} href in chip click | mitigate | Existing Phase 51 try/except ValueError at edit_station_dialog.py:1260; reused — Wave 0 test `test_chip_click_emits_navigate_signal` exercises the happy path. No new mitigation needed. |
| T-71-05 | I (Information Disclosure) | Manual sibling cross-provider tooltip | accept | Tooltip displays provider name only (already in DB, already visible elsewhere in app). No additional disclosure. |

</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_station_siblings.py with all pure helper + repo CRUD RED tests</name>
  <files>tests/test_station_siblings.py</files>
  <read_first>
    - tests/test_aa_siblings.py (lines 1-50 for imports + _mk factory shape; full file for one-assertion-per-test pattern)
    - tests/test_repo.py (lines 1-20 for repo fixture pattern with PRAGMA foreign_keys = ON)
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-PATTERNS.md §"tests/test_station_siblings.py" (lines 150-213) for exact _mk extension and fixture pattern
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-VALIDATION.md §"Per-Task Verification Map" (lines 41-65) for the exact 13 test names this file must contain
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-RESEARCH.md §"Pitfall 6: PRAGMA foreign_keys = ON" (lines 441-445) — fixture MUST set this pragma
  </read_first>
  <action>
    Create NEW file `tests/test_station_siblings.py`. Follow `tests/test_aa_siblings.py` shape exactly. Module-level imports: `sqlite3`, `pytest`, `from musicstreamer.models import Station, StationStream`, `from musicstreamer.repo import Repo, db_init`, `from musicstreamer.url_helpers import find_manual_siblings, merge_siblings` (the latter two imports WILL FAIL at collection time today — that IS the RED state per project convention; do NOT use pytest.importorskip or pytest.fail).

    Define an extended `_mk(id_, name, provider_name=None, url="http://example.com/stream")` factory per 71-PATTERNS.md lines 175-181 — adds the `provider_name` kwarg over the test_aa_siblings.py original. Use `Station(id=id_, name=name, provider_id=None, provider_name=provider_name, tags="", station_art_path=None, album_fallback_path=None, streams=[StationStream(id=id_*10, station_id=id_, url=url, position=1)])`.

    Define a `repo(tmp_path)` pytest fixture: `con = sqlite3.connect(str(tmp_path / "test.db")); con.row_factory = sqlite3.Row; con.execute("PRAGMA foreign_keys = ON;"); db_init(con); return Repo(con)`. The PRAGMA is mandatory per 71-RESEARCH Pitfall 6 — without it CASCADE tests fail silently.

    Write the following 13 tests verbatim from 71-VALIDATION.md per-task map. One assertion per test, mirroring test_aa_siblings.py shape:
    - `test_schema_create_with_check_unique_cascade` — call `db_init(con)`, then `con.execute("INSERT INTO station_siblings(a_id, b_id) VALUES (2, 1)")` MUST raise `sqlite3.IntegrityError` (CHECK violation: a_id < b_id).
    - `test_db_init_idempotent_with_siblings_table` — call db_init twice on same connection; second call must not raise.
    - `test_cascade_on_station_delete` — seed 2 stations + 1 link via direct INSERT; `DELETE FROM stations WHERE id = ?` partner; assert `SELECT COUNT(*) FROM station_siblings` is 0.
    - `test_add_sibling_link_round_trip` — repo.add_sibling_link(1, 2), then repo.list_sibling_links(1) returns [2].
    - `test_add_sibling_link_idempotent` — call add_sibling_link(1,2) twice; SELECT COUNT(*) returns 1; second call does not raise.
    - `test_add_sibling_link_normalizes_order` — add_sibling_link(2, 1) (reversed); SELECT a_id, b_id returns (1, 2). Also list_sibling_links(2) returns [1].
    - `test_remove_sibling_link` — add then remove; list_sibling_links returns [].
    - `test_remove_sibling_link_noop_when_absent` — remove_sibling_link(1, 2) on empty table does not raise.
    - `test_list_sibling_links_symmetric` — add (1,2); list_sibling_links(1) == [2] AND list_sibling_links(2) == [1].
    - `test_find_manual_siblings_tuple_shape` — assert each tuple is (provider_name_str, station_id_int, station_name_str).
    - `test_find_manual_siblings_excludes_self` — link_ids includes current_station_id; result excludes it (defensive).
    - `test_find_manual_siblings_sorts_alphabetically` — three stations B/A/C linked; result order is A, B, C (casefold).
    - `test_merge_siblings_dedup_by_station_id` — same station_id in both aa_list and manual_list; result has it once with the AA tuple (AA precedence per 71-RESEARCH Q2).

    Tests must use the `repo` fixture for CRUD/schema tests; pure-helper tests use `_mk` only (no DB).

    Per 71-RESEARCH Pitfall 6: explicitly set `PRAGMA foreign_keys = ON` in the fixture (not just rely on `db_connect()` since tests create their own sqlite3.connect).

    Do NOT use pytest.importorskip, pytest.fail, or @pytest.mark.skip. Module-level imports failing IS the RED state.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_station_siblings.py --collect-only 2>&1 | grep -E "(ImportError|13 tests collected|errors)" | head -5</automated>
  </verify>
  <done>
    File exists. Collection produces ImportError on `find_manual_siblings, merge_siblings` import — RED state confirmed.
    Or, if module-level imports succeed somehow (e.g., partial helper exists), all 13 tests must FAIL (RED state).
    13 distinct `def test_*` functions present (verified by `grep -c "^def test_" tests/test_station_siblings.py` returning 13).
  </done>
  <validation_map>
    71-VALIDATION.md rows: D-05, D-06, D-08, Merge layer, Symmetric storage, Idempotent CRUD (3 rows), find_manual_siblings (3 rows) — 11 of the 13 test names map 1:1; the 2 non-mapped (`test_cascade_on_station_delete`, `test_add_sibling_link_round_trip`) flow from D-08 + Symmetric storage.
  </validation_map>
  <acceptance_criteria>
    - `grep -c "^def test_" tests/test_station_siblings.py` returns exactly 13.
    - `grep -c "from musicstreamer.url_helpers import find_manual_siblings, merge_siblings" tests/test_station_siblings.py` returns 1 (module-level import per fixture-mirroring convention).
    - `grep -c "PRAGMA foreign_keys = ON" tests/test_station_siblings.py` returns at least 1 (fixture mandatory per Pitfall 6).
    - `grep -c "import_orskip\|pytest\.fail\|pytest\.skip" tests/test_station_siblings.py` returns 0 (no placeholders).
    - Collection time: `pytest tests/test_station_siblings.py --collect-only` either reports `errors during collection` due to ImportError (acceptable RED), OR collects 13 items that all fail when run.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/test_add_sibling_dialog.py with picker behavior RED tests</name>
  <files>tests/test_add_sibling_dialog.py</files>
  <read_first>
    - tests/test_discovery_dialog.py (lines 1-100 for QDialog test structure, FakeRepo pattern, qtbot usage)
    - tests/test_edit_station_dialog.py (lines 30-65 for MagicMock repo fixture + qtbot dialog fixture)
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-PATTERNS.md §"tests/test_add_sibling_dialog.py" (lines 216-252) for FakeRepo extension pattern
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-UI-SPEC.md §"AddSiblingDialog" (lines 228-289) for widget objectNames, button labels ("Link Station" / "Don't Link"), exclusion rules, empty-state copy
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-VALIDATION.md per-task verification map rows for D-12, D-13
  </read_first>
  <action>
    Create NEW file `tests/test_add_sibling_dialog.py`. Module-level imports: `pytest`, `from PySide6.QtCore import Qt`, `from PySide6.QtWidgets import QDialogButtonBox, QListWidget`, `from musicstreamer.models import Station, StationStream, Provider`, `from musicstreamer.ui_qt.add_sibling_dialog import AddSiblingDialog` (import WILL FAIL — RED state).

    Build `FakeRepo` class per 71-PATTERNS.md lines 222-241:
    ```
    class FakeRepo:
        def __init__(self):
            self.add_sibling_link_calls = []
            self._providers = []
            self._stations = []
            self._sibling_links = []
        def list_providers(self): return self._providers
        def list_stations(self): return self._stations
        def list_sibling_links(self, station_id): return self._sibling_links
        def add_sibling_link(self, a_id, b_id): self.add_sibling_link_calls.append((a_id, b_id))
    ```

    Build a `station(repo)` fixture: a Station with id=1, name="Test Station", provider_id=1, provider_name="ProviderA", streams=[StationStream(id=10, station_id=1, url="http://example.com/stream", position=1)].

    Build a `dialog(qtbot, station, repo)` fixture: `d = AddSiblingDialog(station, repo, parent=None); qtbot.addWidget(d); return d`.

    Write the following 9 RED tests (all from 71-VALIDATION.md D-12, D-13 rows + 71-UI-SPEC interaction contract):
    - `test_dialog_window_title_is_add_sibling_station` — `dialog.windowTitle() == "Add Sibling Station"` (UI-SPEC line 234).
    - `test_ok_button_label_is_link_station` — `dialog._button_box.button(QDialogButtonBox.Ok).text() == "Link Station"` (UI-SPEC line 261; CONTEXT D-13 dismiss not "OK").
    - `test_dismiss_button_label_is_dont_link` — `dialog._button_box.button(QDialogButtonBox.Cancel).text() == "Don't Link"` (UI-SPEC line 261).
    - `test_provider_combo_defaults_to_current_station_provider` — repo._providers includes ProviderA + ProviderB; dialog._provider_combo.currentText() == "ProviderA" (CONTEXT D-12).
    - `test_provider_switch_reloads_station_list` — change combo to a different provider via setCurrentIndex; assert _station_list contents reflect new provider filter (CONTEXT D-12).
    - `test_self_excluded_from_list` — repo._stations contains current station + another; only the "another" appears in _station_list (CONTEXT D-13).
    - `test_already_linked_excluded_from_list` — repo._sibling_links = [42] and _stations contains station id=42 in same provider; that station does NOT appear in _station_list (RESEARCH Pitfall 4 + UI-SPEC line 269).
    - `test_ok_disabled_initially` — dialog._button_box.button(QDialogButtonBox.Ok).isEnabled() is False before any selection.
    - `test_accept_calls_add_sibling_link` — populate stations; select item via `dialog._station_list.setCurrentRow(0)`; trigger accept (e.g., `dialog._on_accept()`); assert `repo.add_sibling_link_calls == [(1, target_id)]` (CONTEXT D-13).

    All `currentIndexChanged`-driven assertions must wait for the slot to run; for sync slot connections, the change call is sufficient. If `qtbot.waitSignal` is used, set a 100 ms timeout.

    Do NOT use pytest.fail / importorskip placeholders. Module-level import failure IS the RED state.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_add_sibling_dialog.py --collect-only 2>&1 | grep -E "(ImportError|9 tests|errors)" | head -5</automated>
  </verify>
  <done>
    File exists with 9 RED tests; collection ImportError on AddSiblingDialog import = RED state.
  </done>
  <validation_map>
    71-VALIDATION.md rows D-12 (provider_switch_reloads_station_list, provider_combo_defaults), D-13 (self_excluded, already_linked_excluded, ok_disabled_initially, ok_enabled_only_on_single_select via accept_calls_add_sibling_link); UI-SPEC copywriting rows (window_title, ok_button_label, dismiss_button_label).
  </validation_map>
  <acceptance_criteria>
    - `grep -c "^def test_" tests/test_add_sibling_dialog.py` returns exactly 9.
    - `grep -c "from musicstreamer.ui_qt.add_sibling_dialog import AddSiblingDialog" tests/test_add_sibling_dialog.py` returns 1.
    - `grep -c "Link Station\|Don't Link" tests/test_add_sibling_dialog.py` returns at least 2 (assertions on exact copy).
    - `grep -c "pytest\.fail\|pytest\.skip\|importorskip" tests/test_add_sibling_dialog.py` returns 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend tests/test_edit_station_dialog.py + test_now_playing_panel.py + test_settings_export.py + test_constants_drift.py</name>
  <files>tests/test_edit_station_dialog.py, tests/test_now_playing_panel.py, tests/test_settings_export.py, tests/test_constants_drift.py</files>
  <read_first>
    - tests/test_edit_station_dialog.py (lines 30-110 for repo MagicMock fixture, dialog fixture, existing chip-pattern tests)
    - tests/test_now_playing_panel.py (lines 1-130 for FakeRepo class and bind_station tests)
    - tests/test_settings_export.py (lines 1-80 for repo fixture and seeded_repo fixture; lines 100-200 for existing build_zip / commit_import tests as a shape reference)
    - tests/test_constants_drift.py (entire file for grep-gate idiom; the existing test_no_org_example_literal_remains_in_python_sources is the analog)
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-PATTERNS.md §"tests/test_edit_station_dialog.py" (lines 818-852) for Signal capture pattern, §"tests/test_settings_export.py" (lines 786-812) for forward-compat test shape, §"tests/test_now_playing_panel.py" (lines 858-884) for merged-display test shape, §"tests/test_constants_drift.py" (lines 888-935) for grep-gate pattern
    - .planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-VALIDATION.md per-task map rows for D-01, D-07, D-11, D-14, D-15, T-40-04, Navigation invariant
  </read_first>
  <action>
    Extend (do NOT rewrite) the four test files. All new tests are RED at the end of this task.

    **tests/test_edit_station_dialog.py — append 6 new tests:**
    Extend the existing MagicMock `repo` fixture (or a new sub-fixture) so `repo.list_sibling_links.return_value = []` and `repo.add_sibling_link`/`repo.remove_sibling_link` are MagicMocks (defaults are fine). If the existing repo fixture is parametrized or otherwise inflexible, add a new `repo_with_siblings` fixture in the same file.

    New tests (all RED until Plan 71-03 lands):
    - `test_add_sibling_button_present` — find child via `dialog.findChild(QPushButton, "add_sibling_btn")` is not None (D-11; UI-SPEC line 219).
    - `test_manual_chip_has_x_button` — repo.list_sibling_links returns [42]; refresh; find `sibling_chip_42` compound; assert it contains a QPushButton with text "×" (D-14; UI-SPEC line 188).
    - `test_aa_chip_has_no_x_button` — seed an AA-pair station; refresh; the AA chip should be a bare QPushButton (no compound widget with sibling_chip_ objectName) — assert via `dialog.findChild(QWidget, f"sibling_chip_{aa_id}") is None` (D-15).
    - `test_x_click_calls_remove_sibling_link` — find sibling_chip_42 compound; click its second QPushButton (the "×"); assert `repo.remove_sibling_link.call_args == call(station_id, 42)` (D-14).
    - `test_x_click_fires_unlinked_toast` — connect `dialog.sibling_toast` to a list; click "×"; assert the list contains exactly one entry matching `r"^Unlinked from "` (D-14; UI-SPEC line 142).
    - `test_chip_click_emits_navigate_signal` — connect `dialog.navigate_to_sibling` to a list; click the name button of `sibling_chip_42`; assert list == [42] (Navigation invariant; UI-SPEC interaction contract line 312).

    **tests/test_now_playing_panel.py — append 1 new test + extend FakeRepo:**
    Extend the existing FakeRepo class with `list_sibling_links(self, station_id) -> list[int]` returning a `self._sibling_links` attribute (default []). If the existing FakeRepo is defined in conftest or shared, prefer adding the method inline or with a separate FakeRepoWithSiblings subclass that returns a `list_sibling_links` per test.

    New test:
    - `test_now_playing_shows_merged_siblings` — per 71-PATTERNS.md lines 870-884: a station with an AA URL whose partner exists in `stations`; ALSO a manual sibling (separate station id linked via `_sibling_links`). After bind_station, `panel._sibling_label.isVisible()` is True AND `panel._sibling_label.text()` contains both the AA partner name AND the manual partner name (D-01; "merged into one Also on: line" per CONTEXT).

    **tests/test_settings_export.py — append 3 new tests:**
    Reuse the existing `repo` fixture (lines 29-35 — already has PRAGMA foreign_keys = ON).

    New tests:
    - `test_siblings_round_trip` — seed 2 linked stations in repo; build_zip to tmp_path; commit_import the ZIP into a fresh empty repo; assert `fresh_repo.list_sibling_links(new_a_id) == [new_b_id]` (D-07).
    - `test_siblings_missing_key_defaults_empty` — construct a station_data dict WITHOUT a `siblings` key; run commit_import path with this dict; assert no exception AND `SELECT COUNT(*) FROM station_siblings` returns 0 (D-07 forward-compat).
    - `test_siblings_unresolved_name_silently_dropped` — build a ZIP whose JSON has `"siblings": ["NonExistent Station"]`; import into a fresh repo; assert no exception, no rows in `station_siblings`, and the station itself imported normally (D-07).

    **tests/test_constants_drift.py — append 1 new test:**
    At top of file (or near existing grep-gate patterns) add module-level constant `EXPECTED_RICHTEXT_COUNT = 3`. Rationale: today's baseline of `setTextFormat(Qt.RichText)` literal calls across `musicstreamer/` is 4 (3 in `now_playing_panel.py` at lines 355, 617, 633; 1 in `edit_station_dialog.py` at line 487). Plan 71-03 will remove the EditStationDialog one → 3 remaining. Plan 71-08 may revisit if needed.

    New test (RED today, GREEN after Plan 71-03):
    - `test_richtext_baseline_unchanged_by_phase_71` — per 71-PATTERNS.md lines 911-933: scan `pkg_root.rglob("*.py")`, count occurrences of the LITERAL string `"setTextFormat(Qt.RichText)"` (this is the strict call pattern; comments containing "RichText" do not match because they lack the `setTextFormat(...)` prefix). Assert `count == EXPECTED_RICHTEXT_COUNT` with a message naming Phase 71 + T-40-04. Today this fails (count=4); after Plan 71-03 lands it passes (count=3).

    All new tests added by append — preserve existing tests verbatim.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py tests/test_now_playing_panel.py tests/test_settings_export.py tests/test_constants_drift.py -k "sibling or richtext_baseline" --collect-only 2>&1 | grep -E "(collected|errors)" | head -5</automated>
  </verify>
  <done>
    All 11 new tests (6 + 1 + 3 + 1) collected (or fail with AttributeError on findChild/attribute access — RED state).
    Pre-existing tests in these 4 files remain unmodified (verified by spot-check; no test was removed or renamed).
  </done>
  <validation_map>
    Edit dialog: D-11 (add_sibling_button_present), D-14 (manual_chip_has_x, x_click_calls_remove, x_click_fires_toast), D-15 (aa_chip_has_no_x), Navigation invariant (chip_click_emits_navigate).
    NowPlaying: D-01 (now_playing_shows_merged_siblings).
    Settings export: D-07 (3 rows: round_trip, missing_key, unresolved).
    Drift-guard: T-40-04 (richtext_baseline_unchanged).
  </validation_map>
  <acceptance_criteria>
    - `grep -c "^def test_.*sibling\|^def test_richtext_baseline" tests/test_edit_station_dialog.py tests/test_now_playing_panel.py tests/test_settings_export.py tests/test_constants_drift.py | awk -F: '{s+=$2} END {print s}'` returns at least 11.
    - `grep -c "EXPECTED_RICHTEXT_COUNT = 3" tests/test_constants_drift.py` returns exactly 1.
    - `git diff --stat tests/test_edit_station_dialog.py tests/test_now_playing_panel.py tests/test_settings_export.py tests/test_constants_drift.py` shows only additions (no `-` lines in the existing test bodies; only fixture extensions may show modifications).
    - Full file collection: `pytest tests/test_edit_station_dialog.py tests/test_now_playing_panel.py tests/test_settings_export.py tests/test_constants_drift.py --collect-only` succeeds (the new tests fail at run-time, not collection-time, since they exercise existing modules).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Confirm full RED state — 22 tests pending, baseline grep numbers locked</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - tests/test_station_siblings.py (just created)
    - tests/test_add_sibling_dialog.py (just created)
    - tests/test_edit_station_dialog.py (just extended)
    - tests/test_now_playing_panel.py (just extended)
    - tests/test_settings_export.py (just extended)
    - tests/test_constants_drift.py (just extended)
  </read_first>
  <action>
    Run the verification commands listed below and record exact counts. Do NOT write fixes for any failures here — failures are the expected RED state. If the count comes out wrong (e.g., 21 tests instead of 22 — one missed), return to Task 1/2/3 and add the missing test.

    Then commit only if at least 22 new test functions exist and the RichText baseline (count==4 today) is locked into `EXPECTED_RICHTEXT_COUNT = 3` (the post-Plan-71-03 target).

    No files are modified in this task — it is a verification gate.
  </action>
  <verify>
    <automated>echo "=== test counts ===" && grep -c "^def test_" tests/test_station_siblings.py tests/test_add_sibling_dialog.py 2>&1 && echo "=== new tests in extended files ===" && grep -c "^def test_.*sibling\|^def test_richtext_baseline" tests/test_edit_station_dialog.py tests/test_now_playing_panel.py tests/test_settings_export.py tests/test_constants_drift.py 2>&1 && echo "=== RichText baseline today ===" && grep -rn "setTextFormat(Qt\.RichText)" musicstreamer/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    `tests/test_station_siblings.py`: 13 test functions.
    `tests/test_add_sibling_dialog.py`: 9 test functions.
    Extended files total: 11 new tests (6 + 1 + 3 + 1).
    Grand total: 33 new test entries — WAIT, total should be 13 + 9 = 22 from new files + 11 from extensions = 33 entries. Actually per 71-VALIDATION.md we lock 22 unique decision/invariant rows, but multiple tests can map to one row. The Wave 0 commitment is 22 RED tests minimum; this plan delivers ≥33 across the rows. Reconcile if mismatch — the per-row coverage matters more than the absolute count.
    Today's RichText baseline (literal `setTextFormat(Qt.RichText)`): exactly 4 occurrences across musicstreamer/.
    `EXPECTED_RICHTEXT_COUNT = 3` constant present in tests/test_constants_drift.py (the post-Plan-71-03 target).
  </done>
  <validation_map>
    All 22 rows in 71-VALIDATION.md per-task map are now mapped to at least one test function across Tasks 1-3.
  </validation_map>
  <acceptance_criteria>
    - Output of verify command shows test counts ≥ 22 across the new files + extensions.
    - Baseline literal `setTextFormat(Qt.RichText)` count is 4 today (will become 3 after Plan 71-03).
    - No additional source-file modifications in this task.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all 4 tasks, the test suite must be in a clean RED state for Phase 71's contract:

```bash
# Step 1: Confirm full suite still collects (no syntax errors in new tests)
uv run --with pytest --with pytest-qt pytest tests/ --collect-only -q 2>&1 | tail -5

# Step 2: Confirm Phase 71 tests are RED (expected failures)
uv run --with pytest --with pytest-qt pytest tests/test_station_siblings.py tests/test_add_sibling_dialog.py -x --tb=line 2>&1 | tail -10
# Expected: import errors OR test failures — RED state

# Step 3: Confirm existing 399+ tests still GREEN (no regression from the test extensions)
uv run --with pytest --with pytest-qt pytest tests/ --ignore=tests/test_station_siblings.py --ignore=tests/test_add_sibling_dialog.py -x --tb=short -q 2>&1 | tail -5
# Expected: all existing tests pass; the 11 new RED tests in extended files are ignored

# Step 4: Confirm RichText baseline count today is exactly 4
grep -rn "setTextFormat(Qt\.RichText)" musicstreamer/ | wc -l
# Expected: 4
```
</verification>

<success_criteria>
- 22+ RED tests across 6 files (2 new + 4 extended) cover all 22 rows in 71-VALIDATION.md.
- New tests fail via ImportError (preferred RED state per project convention) or AttributeError (e.g., missing repo methods, missing widget objectNames).
- No pytest.fail / pytest.skip / importorskip placeholders anywhere in new tests.
- The existing 399+ tests in the suite remain GREEN — Wave 0 must not regress any existing test.
- `tests/test_constants_drift.py` contains `EXPECTED_RICHTEXT_COUNT = 3` (the post-Plan-71-03 target; today's count of 4 means that test is RED until Plan 71-03 removes EditStationDialog._sibling_label).
- All test fixtures that exercise CASCADE behavior include `PRAGMA foreign_keys = ON` (71-RESEARCH Pitfall 6).
</success_criteria>

<output>
After completion, create `.planning/phases/71-sister-station-expansion-1-add-ability-to-link-sister-statio/71-00-SUMMARY.md` listing:
- Files created (2 new test files).
- Files extended (4 files; lines added per file).
- Final RED test count per file.
- Today's RichText baseline (4) and the locked EXPECTED_RICHTEXT_COUNT (3).
- Any deviations from the 22-row coverage commitment in 71-VALIDATION.md.
</output>
