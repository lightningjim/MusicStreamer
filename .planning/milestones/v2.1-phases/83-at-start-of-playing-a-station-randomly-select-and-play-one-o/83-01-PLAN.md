---
phase: 83-at-start-of-playing-a-station-randomly-select-and-play-one-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/repo.py
  - musicstreamer/models.py
  - tests/test_repo.py
autonomous: true
requirements: []
tags: [phase-83, schema, repo, models, somafm, preroll, db-init, station-prerolls, prerolls-fetched-at]
must_haves:
  truths:
    - "D-01: `station_prerolls(id, station_id, url, position)` table exists after `db_init`, with `FOREIGN KEY(station_id) REFERENCES stations(id) ON DELETE CASCADE`"
    - "D-04: `stations.prerolls_fetched_at` INTEGER column exists, nullable, no DEFAULT"
    - "D-15: schema additions are idempotent — running `db_init` twice raises no exception and leaves the schema unchanged"
    - "D-01/D-03: `Repo.list_prerolls(station_id) -> list[str]` returns preroll URLs in `position` ASC order; empty list when no rows"
    - "D-02 (consumed by 83-02): `Repo.insert_preroll(station_id, url, position) -> int` validates `url` starts with `http://` or `https://` (ASVS V5 — reject non-HTTP(S) schemes); caps a single call's accepted position values at 50 (DoS mitigation)"
    - "D-04: `Repo.set_prerolls_fetched_at(station_id, epoch_seconds)` UPDATEs the new column; idempotent"
    - "D-01 + Pitfall 6 option 3a: `Station` dataclass carries `prerolls: list[str]` and `prerolls_fetched_at: Optional[int]`; all 4 Station-builder sites (`list_stations`, `get_station`, `list_recently_played`, `list_favorite_stations`) eager-load both fields"
    - "CASCADE invariant: `Repo.delete_station(sid)` removes all `station_prerolls` rows for that station via FK ON DELETE CASCADE"
  artifacts:
    - path: "musicstreamer/repo.py"
      provides: "station_prerolls CREATE + prerolls_fetched_at ALTER inside db_init; insert_preroll/list_prerolls/set_prerolls_fetched_at methods; eager-load on 4 Station builders"
      contains: "station_prerolls"
    - path: "musicstreamer/models.py"
      provides: "Station.prerolls + Station.prerolls_fetched_at fields"
      contains: "prerolls_fetched_at"
    - path: "tests/test_repo.py"
      provides: "migration + new-method + CASCADE + eager-load tests"
      contains: "station_prerolls"
  key_links:
    - from: "musicstreamer/repo.py:db_init"
      to: "station_prerolls table + prerolls_fetched_at column"
      via: "executescript CREATE + try/except OperationalError ALTER"
      pattern: "station_prerolls"
    - from: "musicstreamer/repo.py:Repo.list_stations / get_station / list_recently_played / list_favorite_stations"
      to: "Station(prerolls=..., prerolls_fetched_at=...)"
      via: "eager-load self.list_prerolls(row id) + row['prerolls_fetched_at']"
      pattern: "prerolls=self.list_prerolls"
---

<objective>
Schema + model + repo layer for Phase 83 SomaFM prerolls. Adds (1) the new `station_prerolls(id, station_id, url, position)` table with `ON DELETE CASCADE` inside `repo.py`'s `db_init` `executescript` block (NOT `migration.py` — per PATTERNS.md routing note); (2) the new `stations.prerolls_fetched_at INTEGER NULL` column via the idempotent `try/except sqlite3.OperationalError` ALTER pattern Phase 82 used at `repo.py:269-282`; (3) three new `Repo` methods (`insert_preroll`, `list_prerolls`, `set_prerolls_fetched_at`) with URL-scheme validation and per-call insert cap (security additions per RESEARCH §Security Domain); (4) `Station` dataclass extension with `prerolls: list[str]` and `prerolls_fetched_at: Optional[int]`; (5) eager-load on all 4 Station-builder sites (`list_stations`, `get_station`, `list_recently_played`, `list_favorite_stations`); (6) `tests/test_repo.py` appendix exercising migration idempotency, column shape, list ordering, setter round-trip across all 4 builders, CASCADE, URL-scheme rejection, and per-call cap.

Purpose: this is the data foundation Plans 83-02 (importer extension) and 83-03 (Player layer) build on. Pure additive schema; no existing rows are modified. Foundational interface contracts for downstream plans.

Output: ~80-line additions in `musicstreamer/repo.py` (CREATE + ALTER + 3 methods + 4 builder kwarg edits) + ~6-line additions in `musicstreamer/models.py` (2 new Station fields) + ~150-line appendix in `tests/test_repo.py` (~9 tests).

Wave 1 — no dependencies. Plans 83-02 and 83-03 both depend on this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-CONTEXT.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md
@.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-VALIDATION.md
@musicstreamer/repo.py
@musicstreamer/models.py
@tests/test_repo.py

<interfaces>
<!-- Key contracts the executor must follow. PATTERNS.md is the authoritative analog map. -->

From musicstreamer/repo.py:124-137 (existing `station_streams` CREATE — exact shape mirror inside the `db_init` `executescript` block):
The new `station_prerolls` table mirrors this body shape: `INTEGER PRIMARY KEY AUTOINCREMENT`, `station_id INTEGER NOT NULL`, columns, then `FOREIGN KEY(station_id) REFERENCES stations(id) ON DELETE CASCADE`.

From musicstreamer/repo.py:269-282 (Phase 82 idempotent ALTER block — verbatim shape mirror):
Wrap a single `con.execute("ALTER TABLE stations ADD COLUMN prerolls_fetched_at INTEGER")` + `con.commit()` in `try: ... except sqlite3.OperationalError: pass`. Place this AFTER the legacy `stations_new` rebuild block (~repo.py:251) and AFTER the Phase 82 `preferred_stream_id` ALTER (~repo.py:269-282) so the column lands on the rebuilt-or-fresh table (Pitfall 2 — same reason called out in the Phase 82 comment).

From musicstreamer/repo.py:351-371 (existing `list_streams` / `insert_stream` shape — exact mirror for new preroll methods):
`list_prerolls(station_id) -> list[str]`: `SELECT url FROM station_prerolls WHERE station_id=? ORDER BY position`, return `[r["url"] for r in rows]`. (Returns plain str list per RESEARCH Operation 2 — Player only needs URLs; no `Preroll` dataclass needed.)
`insert_preroll(station_id, url, position) -> int`: parameterized INSERT, `self.con.commit()`, return `int(cur.lastrowid)`. BUT must validate `url.startswith(("http://", "https://"))` BEFORE the INSERT (ASVS V5 per RESEARCH §Security Domain — defense in depth alongside `soma_import._safe_urlopen_request`); raise `ValueError` on a non-HTTP(S) scheme. Also reject `position > 50` to cap per-channel inserts (DoS mitigation per RESEARCH §Security Domain).

From musicstreamer/repo.py:574-580 (Phase 82 `set_preferred_stream` — verbatim mirror for `set_prerolls_fetched_at`):
Single `self.con.execute("UPDATE stations SET prerolls_fetched_at = ? WHERE id = ?", (epoch_seconds, station_id))` + `self.con.commit()`. No FK; nullable.

From musicstreamer/repo.py:450,487,582,697 (4 Station-builder sites — each must be updated identically):
Every `Station(...)` constructor call must add two new kwargs: `prerolls=self.list_prerolls(r["id"])` (eager sub-query mirroring the existing `streams=self.list_streams(r["id"])`) AND `prerolls_fetched_at=r["prerolls_fetched_at"]`. The SELECT statements already do `SELECT s.*` or `SELECT stations.*` so the new column flows through automatically; only the dataclass constructor calls need updating.

From musicstreamer/models.py:26-40 (current Station — append two fields at the bottom mirroring the existing `streams` + `preferred_stream_id` shape):
```python
prerolls: List[str] = field(default_factory=list)              # Phase 83 D-01/D-03
prerolls_fetched_at: Optional[int] = None                      # Phase 83 D-04
```
Use `List[str]` to match the file's existing `typing.List` import idiom (line 2: `from typing import Optional, List, Literal`).

From tests/test_repo.py:875-922 (Phase 82 migration tests — exact template for Phase 83 schema tests) and tests/test_repo.py:461-612 (stream CRUD + CASCADE tests — exact template for Phase 83 preroll CRUD + CASCADE tests).

From tests/test_repo.py:937-971 (Phase 82 setter-round-trip-across-builders test — exact template for `test_set_prerolls_fetched_at_round_trips_via_list_stations` etc.).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema additions in repo.py db_init + Station dataclass fields</name>
  <files>musicstreamer/repo.py, musicstreamer/models.py</files>
  <read_first>
    - musicstreamer/repo.py lines 88-148 (`db_init` executescript block — verbatim baseline; new `CREATE TABLE IF NOT EXISTS station_prerolls` lands inside this)
    - musicstreamer/repo.py lines 124-137 (existing `station_streams` CREATE — exact column-style mirror for new table)
    - musicstreamer/repo.py lines 249-282 (Phase 82 ALTER block + the legacy `stations_new` rebuild Pitfall 2 comment — new ALTER lands AFTER both)
    - musicstreamer/repo.py lines 334-371 (Repo class top + `list_streams`/`insert_stream` — class layout reference; new methods land in the same band)
    - musicstreamer/models.py lines 26-40 (Station dataclass — two new fields appended at the bottom)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md (authoritative analog map — schema section)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md §"Operation 3: migration in db_init"
  </read_first>
  <behavior>
    - After running `db_init` on a fresh `sqlite3` in-memory connection: `station_prerolls` table exists with columns `id (INTEGER PK AUTOINCREMENT)`, `station_id (INTEGER NOT NULL)`, `url (TEXT NOT NULL)`, `position (INTEGER NOT NULL)`, and FK on `station_id` REFERENCES `stations(id)` with `ON DELETE CASCADE`.
    - After running `db_init` on a fresh connection: `stations.prerolls_fetched_at` column exists, type INTEGER, nullable (`notnull=0`), no DEFAULT (`dflt_value IS NULL`).
    - Running `db_init` a second time on the same connection raises no exception and leaves the schema unchanged (idempotent — D-15).
    - `Station` dataclass accepts `prerolls=[...]` and `prerolls_fetched_at=...` kwargs; defaults are `[]` and `None` respectively.
  </behavior>
  <action>
    Per D-01, D-04, D-15, and PATTERNS.md §"Idempotent additive schema". `db_init` is the canonical migration entry point — `migration.py` is the platformdirs first-launch helper, NOT a DDL container (PATTERNS.md routing note at top).

    **musicstreamer/repo.py** — two distinct edits:

    1. Inside the existing `db_init` `executescript` block (the multi-statement string at roughly lines 89-148), AFTER the existing `CREATE TABLE IF NOT EXISTS station_streams (...)` statement, ADD the new `CREATE TABLE IF NOT EXISTS station_prerolls (...)` statement. Body: `id INTEGER PRIMARY KEY AUTOINCREMENT, station_id INTEGER NOT NULL, url TEXT NOT NULL, position INTEGER NOT NULL, FOREIGN KEY(station_id) REFERENCES stations(id) ON DELETE CASCADE`. The CREATE is inside the executescript so a fresh DB gets it in the same script as `stations` (PATTERNS.md §"Idempotent additive schema" — placement guidance).

    2. AFTER the Phase 82 `preferred_stream_id` ALTER block at lines 269-282 (which itself lands AFTER the legacy `stations_new` rebuild block per its own Pitfall 2 comment), ADD a NEW idempotent ALTER block for `prerolls_fetched_at`. The block mirrors Phase 82 exactly: `try: con.execute("ALTER TABLE stations ADD COLUMN prerolls_fetched_at INTEGER"); con.commit()` wrapped in `except sqlite3.OperationalError: pass`. Header comment must cite "Phase 83 D-04/D-15 — lazy-backfill timestamp; nullable INTEGER no DEFAULT; idempotent via OperationalError catch; lands AFTER the stations_new rebuild block for the same Pitfall 2 reason called out in the Phase 82 comment above."

    **musicstreamer/models.py** — append two fields to the `Station` dataclass (the file at lines 26-40), at the bottom AFTER the existing `preferred_stream_id` field, preserving the existing trailing-default ordering invariant:

    ```
    prerolls: List[str] = field(default_factory=list)              # Phase 83 D-01/D-03
    prerolls_fetched_at: Optional[int] = None                      # Phase 83 D-04
    ```

    Use `List[str]` (capital L) to match the file's `from typing import Optional, List, Literal` import (line 2). `field` and `Optional` are already imported.

    Hard constraints (do NOT violate):
    - Do NOT touch `musicstreamer/migration.py` (PATTERNS.md routing note — it is the first-launch helper, not a DDL file).
    - Do NOT bump SQLite `user_version` — the project's pattern is idempotent `CREATE TABLE IF NOT EXISTS` + `try/except OperationalError ALTER` (RESEARCH §"Don't Hand-Roll" — established pattern).
    - Do NOT rewrite or reorder the existing `db_init` body beyond the additions described above. The legacy `stations_new` rebuild and Phase 82 ALTER must remain in their current positions; new code lands strictly AFTER them.
    - Do NOT modify any of the existing Station fields (e.g. `streams`, `preferred_stream_id`) — only append two new ones.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py -x -q --co 2>&1 | tail -5  # smoke: test collection still succeeds after schema edits</automated>
  </verify>
  <done>
    - `grep -c 'CREATE TABLE IF NOT EXISTS station_prerolls' musicstreamer/repo.py` outputs `1`
    - `grep -c 'ADD COLUMN prerolls_fetched_at INTEGER' musicstreamer/repo.py` outputs `1`
    - `grep -c 'prerolls_fetched_at' musicstreamer/models.py` outputs `1` (the new field)
    - `grep -c 'prerolls: List\[str\]' musicstreamer/models.py` outputs `1`
    - `python -c "import sqlite3; from musicstreamer.repo import db_init; c=sqlite3.connect(':memory:'); c.row_factory=sqlite3.Row; db_init(c); rows=c.execute(\"PRAGMA table_info('station_prerolls')\").fetchall(); assert {r[1] for r in rows} == {'id','station_id','url','position'}, rows; cols=c.execute(\"PRAGMA table_info('stations')\").fetchall(); assert any(r[1]=='prerolls_fetched_at' and r[2]=='INTEGER' and r[3]==0 and r[4] is None for r in cols), cols; db_init(c); print('OK')"` exits 0 and prints `OK` (proves table exists with correct columns, ALTER added nullable no-default INTEGER column, idempotent on second `db_init`)
    - `uv run pytest tests/test_repo.py -x -q` exits 0 (existing tests still pass with the new column/table present; new tests land in Task 3)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repo methods (insert_preroll, list_prerolls, set_prerolls_fetched_at) + 4 Station-builder eager-loads</name>
  <files>musicstreamer/repo.py</files>
  <read_first>
    - musicstreamer/repo.py lines 351-371 (existing `list_streams` / `insert_stream` — exact body-shape mirror for `list_prerolls` / `insert_preroll`)
    - musicstreamer/repo.py lines 574-580 (Phase 82 `set_preferred_stream` — exact body-shape mirror for `set_prerolls_fetched_at`)
    - musicstreamer/repo.py lines 450-486 (`list_stations` — Station-builder kwarg site 1; see exact line for the `Station(...)` constructor inside the row loop)
    - musicstreamer/repo.py lines 487-514 (`get_station` — Station-builder kwarg site 2)
    - musicstreamer/repo.py lines 582-670 (`list_recently_played` — Station-builder kwarg site 3)
    - musicstreamer/repo.py lines 697-end (`list_favorite_stations` — Station-builder kwarg site 4)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-RESEARCH.md §"Operation 2: Repo additions" + §"Security Domain"
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-PATTERNS.md §"Eager-load on Station builder (4 sites)"
  </read_first>
  <behavior>
    - `repo.insert_preroll(station_id=1, url="https://somafm.com/prerolls/x.m4a", position=1)` returns the new row id (positive int); subsequent `repo.list_prerolls(1)` returns `["https://somafm.com/prerolls/x.m4a"]`.
    - `repo.insert_preroll(1, "file:///etc/passwd", 1)` raises `ValueError` (URL scheme not in `http://` / `https://` — ASVS V5 input validation; RESEARCH §Security Domain T-83-01).
    - `repo.insert_preroll(1, "javascript:alert(1)", 1)` raises `ValueError` (same).
    - `repo.insert_preroll(1, "https://...", 51)` raises `ValueError` (per-call cap of 50; DoS mitigation per RESEARCH §Security Domain).
    - `repo.list_prerolls(1)` returns URLs in `ORDER BY position` ascending; empty list when no rows.
    - `repo.set_prerolls_fetched_at(1, 1700000000)` UPDATEs the column to `1700000000`; calling it a second time with a different value overwrites the first (last-write-wins).
    - `repo.list_stations()` / `get_station(sid)` / `list_recently_played(5)` / `list_favorite_stations()` each return `Station` instances whose `prerolls` field contains the URLs inserted via `insert_preroll` (in position order) and whose `prerolls_fetched_at` field equals the value written via `set_prerolls_fetched_at` (or `None` if never written).
    - `repo.delete_station(sid)` removes the station; subsequent `SELECT FROM station_prerolls WHERE station_id=sid` returns 0 rows (FK CASCADE).
  </behavior>
  <action>
    Per D-01, D-03, D-04, RESEARCH §Security Domain ASVS V5 + DoS, and PATTERNS.md analog assignments.

    **Add three new `Repo` methods.** Place them in proximity to their respective analogs:
    - `insert_preroll` and `list_prerolls` go alongside `insert_stream` / `list_streams` (~repo.py:351-371 band).
    - `set_prerolls_fetched_at` goes alongside `set_preferred_stream` (~repo.py:574-580 band).

    Method contracts (mirror the cited analogs exactly except for the listed deviations):

    1. `insert_preroll(self, station_id: int, url: str, position: int) -> int`
       - **First**: validate `url`. If `not url.startswith(("http://", "https://"))` raise `ValueError(f"insert_preroll: refusing non-HTTP(S) scheme: {url!r}")`. Defense in depth alongside `soma_import._safe_urlopen_request` per RESEARCH §Security Domain (T-83-01 — ASVS V5 Input Validation).
       - **Then**: validate `position`. If `position > 50` raise `ValueError(f"insert_preroll: per-channel cap is 50; got position={position}")`. Live SomaFM API max is 5 per channel; 50 is 10× headroom (T-83-02 — DoS mitigation per RESEARCH §Security Domain).
       - **Then**: `cur = self.con.execute("INSERT INTO station_prerolls(station_id, url, position) VALUES (?, ?, ?)", (station_id, url, position))`. Parameterized — Tampering-via-SQLi mitigation T-83-03 is satisfied automatically.
       - `self.con.commit()`; `return int(cur.lastrowid)`.

    2. `list_prerolls(self, station_id: int) -> List[str]`
       - `rows = self.con.execute("SELECT url FROM station_prerolls WHERE station_id = ? ORDER BY position", (station_id,)).fetchall()`
       - `return [r["url"] for r in rows]`
       - The `ORDER BY position` is load-bearing — Player.play uses `random.choice` on the returned list (Plan 83-03), but other callers expect deterministic position-order iteration. Type annotation uses `List[str]` (matching `from typing import List` already imported at repo.py top).

    3. `set_prerolls_fetched_at(self, station_id: int, epoch_seconds: int) -> None`
       - Header docstring: `"""Phase 83 D-04: marks a SomaFM station as 'fetched' so the lazy backfill gate does not re-fetch. Set on both happy path (>=1 preroll) and 0-preroll path so legitimately-empty channels (Seven Inch Soul etc.) do not hammer the API on every Play press."""`
       - `self.con.execute("UPDATE stations SET prerolls_fetched_at = ? WHERE id = ?", (epoch_seconds, station_id))`
       - `self.con.commit()`
       - No return value.

    **Eager-load on all 4 Station-builder sites.** In each of `list_stations`, `get_station`, `list_recently_played`, `list_favorite_stations`, locate the `Station(...)` constructor invocation inside the per-row loop. Add two new kwargs to that call (anywhere AFTER the existing positional/keyword args; following the file's existing style — Phase 82 added `preferred_stream_id=r["preferred_stream_id"]` in the same band):

    - `prerolls=self.list_prerolls(r["id"])` — eager sub-query, mirrors the existing `streams=self.list_streams(r["id"])` pattern (PATTERNS.md §"Eager-load on Station builder").
    - `prerolls_fetched_at=r["prerolls_fetched_at"]` — column already in SELECT via `SELECT s.*` or `SELECT stations.*`; no SELECT rewrite needed.

    The row variable name may differ per builder (`r`, `row`, etc.) — use whichever the existing constructor call in that function uses. Do not rename existing variables.

    Hard constraints:
    - All four builders MUST be updated. Missing one leaves a `Station` with `prerolls=[]` and `prerolls_fetched_at=None` even when the row has data — silent bug that Plan 83-03 cannot detect without a multi-builder test (which Task 3 below ships).
    - Do NOT introduce a `Preroll` dataclass — RESEARCH Operation 2 mandates `list[str]` only (Player only consumes URLs).
    - Do NOT introduce a JOIN-based aggregate query — extra single-row sub-query per station is acceptable (RESEARCH Assumption A8: ~1ms for a 200-station library).
    - Do NOT add SELECT-list edits — `SELECT s.*` / `SELECT stations.*` already includes the new `prerolls_fetched_at` column.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py -x -q --co 2>&1 | tail -5  # smoke: collection still succeeds</automated>
  </verify>
  <done>
    - `grep -c 'def insert_preroll' musicstreamer/repo.py` outputs `1`
    - `grep -c 'def list_prerolls' musicstreamer/repo.py` outputs `1`
    - `grep -c 'def set_prerolls_fetched_at' musicstreamer/repo.py` outputs `1`
    - `grep -c 'http://.*https://' musicstreamer/repo.py` outputs at least `1` (URL-scheme validation literal — ASVS V5)
    - `grep -c 'position > 50' musicstreamer/repo.py` outputs `1` (per-call cap — DoS mitigation)
    - `grep -c 'prerolls=self.list_prerolls' musicstreamer/repo.py` outputs exactly `4` (one per Station-builder site)
    - `grep -c 'prerolls_fetched_at=' musicstreamer/repo.py` outputs at least `5` (4 builder kwarg sites + at least 1 inside `set_prerolls_fetched_at`)
    - `uv run pytest tests/test_repo.py -x -q` exits 0 (existing tests still pass; new tests land in Task 3)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_repo.py appendix — migration + new-method + CASCADE + eager-load + URL-scheme + cap</name>
  <files>tests/test_repo.py</files>
  <read_first>
    - tests/test_repo.py lines 875-922 (Phase 82 migration tests — verbatim template for column/idempotency shape tests)
    - tests/test_repo.py lines 937-971 (Phase 82 setter round-trip via all 4 Station-builders — exact template)
    - tests/test_repo.py lines 461-612 (stream CRUD + CASCADE tests — exact template for preroll CRUD + CASCADE)
    - musicstreamer/repo.py (Repo public API as edited in Tasks 1 + 2)
    - .planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-VALIDATION.md (per-decision test map — names anchor to these)
  </read_first>
  <behavior>
    - `test_station_prerolls_table_schema_after_db_init`: PRAGMA table_info on `station_prerolls` shows exactly the 4 columns {id, station_id, url, position} with `station_id NOT NULL`, `url NOT NULL`, `position NOT NULL`; foreign_key_list includes `station_id REFERENCES stations(id) ON DELETE CASCADE`.
    - `test_prerolls_fetched_at_column_after_db_init`: PRAGMA table_info on `stations` includes `prerolls_fetched_at` with type INTEGER, `notnull=0`, `dflt_value IS NULL`.
    - `test_db_init_is_idempotent_for_phase_83_additions`: running `db_init(con)` twice raises no exception; PRAGMA still shows expected schema.
    - `test_insert_preroll_and_list_prerolls_round_trip`: insert 3 URLs at positions 1, 2, 3; `list_prerolls(sid)` returns them in position order; `list_prerolls(other_sid)` returns `[]`.
    - `test_list_prerolls_orders_by_position_not_insert_order`: insert positions 3, 1, 2 in that arrival order; list returns URLs in position 1, 2, 3 order.
    - `test_insert_preroll_rejects_non_http_scheme`: `insert_preroll(sid, "file:///etc/passwd", 1)` and `insert_preroll(sid, "javascript:alert(1)", 1)` and `insert_preroll(sid, "ftp://host/x", 1)` each raise `ValueError`; no row written (post-call `list_prerolls(sid)` is empty). (T-83-01 ASVS V5.)
    - `test_insert_preroll_rejects_position_over_cap`: `insert_preroll(sid, "https://x/y.m4a", 51)` raises `ValueError`; `insert_preroll(sid, "https://x/y.m4a", 50)` succeeds. (T-83-02 DoS cap.)
    - `test_set_prerolls_fetched_at_round_trip`: write `ts=1700000000` via `set_prerolls_fetched_at(sid, ts)`; assert each of `list_stations()`, `get_station(sid)`, `list_recently_played()`, `list_favorite_stations()` returns a `Station` with `prerolls_fetched_at == 1700000000` (the round-trip-across-all-4-builders shape Phase 82 used).
    - `test_eager_load_prerolls_via_all_4_station_builders`: insert 2 prerolls; assert the same `Station.prerolls` list is present on instances returned from each of the 4 builders.
    - `test_delete_station_cascades_station_prerolls`: insert station + 2 prerolls; `repo.delete_station(sid)`; `SELECT FROM station_prerolls WHERE station_id=sid` returns 0 rows.
  </behavior>
  <action>
    Per D-01, D-04, D-14, D-15, and PATTERNS.md test analogs.

    Append a Phase 83 section to `tests/test_repo.py` (no new file — appendix style mirrors Phase 82 at lines 875-971). Use existing fixtures (`repo`, `db_connect`, `_seed_two_stream_station` or similar) — inspect the file's current fixture inventory and reuse rather than reinvent.

    All 10 tests above MUST be present. Each test name should match `test_<behavior>_<phase_83_or_short_anchor>` style and must correspond to the per-decision rows in 83-VALIDATION.md's test map.

    Implementation notes for the executor:
    - PRAGMA-based schema tests follow Phase 82 idiom (`con.execute("PRAGMA table_info('...')").fetchall()`); use `(row[1], row[2], row[3], row[4])` for `(name, type, notnull, dflt_value)`.
    - For the FK assertion in `test_station_prerolls_table_schema_after_db_init`, use `con.execute("PRAGMA foreign_key_list('station_prerolls')").fetchall()` — assert one row whose `(table, from_, to, on_delete)` equals `('stations', 'station_id', 'id', 'CASCADE')`. Index positions in PRAGMA results: `(id, seq, table, from, to, on_update, on_delete, match)` — `on_delete` is index 6.
    - For URL-scheme rejection: `with pytest.raises(ValueError): repo.insert_preroll(sid, "file:///etc/passwd", 1)`; post-assertion `assert repo.list_prerolls(sid) == []` (no partial write).
    - For per-call cap: `with pytest.raises(ValueError): repo.insert_preroll(sid, "https://x/y.m4a", 51)` AND `assert repo.insert_preroll(sid, "https://x/y.m4a", 50) > 0` (50 still passes).
    - For the 4-builder round-trip: the existing `_seed_two_stream_station(repo)` Phase 82 helper (or similar) likely returns a station id that is reachable from `list_stations` AND `list_favorite_stations` (requires `repo.favorite(...)` or similar) AND `list_recently_played` (requires `repo.update_last_played(sid, ...)`). Follow the Phase 82 setter-round-trip-test at tests/test_repo.py:937-971 — replicate its fixture setup verbatim.
    - For CASCADE: PRAGMA `foreign_keys=ON` must be set on the test connection for the FK to actually fire. The project's `db_connect()` helper handles this for production but in-memory tests may need an explicit `con.execute("PRAGMA foreign_keys=ON")` after the connection opens (check the existing `test_cascade_delete` at tests/test_repo.py:584-594 for the exact pattern used there).

    Hard constraints:
    - Do NOT mock the SQLite layer — these are integration tests against an actual in-memory connection (mirrors Phase 82 idiom; Phase 74 confirms the project pattern).
    - Do NOT skip any test conditionally — every test runs on every platform.
    - Do NOT introduce new fixtures unless an existing one cannot be reused; if a new fixture is required, place it in `tests/conftest.py` only if it has cross-file utility.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py -k "station_prerolls or prerolls_fetched_at or insert_preroll or list_prerolls or set_prerolls_fetched_at or eager_load_prerolls or delete_station_cascades_station_prerolls" -x -q</automated>
  </verify>
  <done>
    - All 10 new tests above pass under the verify command
    - `grep -c 'def test_.*preroll' tests/test_repo.py` outputs at least `10`
    - `uv run pytest tests/test_repo.py -x -q` exits 0 (no regressions in Phase 47/74/82/etc. tests)
    - Full quick-run command passes: `uv run pytest tests/test_player.py tests/test_soma_import.py tests/test_repo.py -q` exits 0 (no regressions outside this plan)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External JSON → Repo.insert_preroll | Plan 83-02 will feed URLs from the SomaFM API (`https://api.somafm.com/channels.json`) into `Repo.insert_preroll`. While the transport is HTTPS-pinned in `soma_import._safe_urlopen_request`, defense-in-depth requires schema-layer validation at the persistence boundary. The Repo layer is the last gate before bytes hit SQLite. |
| In-memory `Station.prerolls` → Player layer | Plan 83-03 reads `Station.prerolls` (a `list[str]`) and feeds the chosen URL to GStreamer's `souphttpsrc`. The validation here guarantees Player can trust every URL it receives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | `Repo.insert_preroll` URL ingestion | mitigate | Validate scheme: refuse anything that is not `http://` or `https://`. Raises `ValueError` BEFORE the INSERT. Defense in depth alongside `soma_import._safe_urlopen_request` (RESEARCH §Security Domain ASVS V5 Input Validation). Test: `test_insert_preroll_rejects_non_http_scheme`. |
| T-83-02 | Denial of Service | `Repo.insert_preroll` per-channel preroll volume | mitigate | Cap position parameter at 50. Live SomaFM API max is 5 per channel; 50 is 10× headroom. Defends against a hostile / compromised SomaFM response that injects 1M preroll entries (RESEARCH §Security Domain DoS pattern). Test: `test_insert_preroll_rejects_position_over_cap`. |
| T-83-03 | Tampering | SQL injection via preroll URL | mitigate | Parameterized queries throughout (`?` placeholders + tuple binding); same idiom as every other Repo method. Verified by code-pattern review (RESEARCH §Security Domain pattern). |
| T-83-04 | Information Disclosure | `station_prerolls.url` exposes CDN paths | accept | Preroll URLs are public SomaFM CDN paths intended for unauthenticated playback (RESEARCH Assumption A6 — confirmed via `gst-launch-1.0` 2026-05-22). No PII, no secrets. |
| T-83-05 | Repudiation | Lost provenance on preroll URLs | accept | `station_prerolls` has no audit-trail column. Consistent with the rest of the schema (`station_streams` has no provenance either). Re-import flow refreshes from the canonical SomaFM API; no need to attribute individual rows. |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (Repo / models / tests edits only; stdlib + project-internal imports only). |
</threat_model>

<verification>
- `uv run pytest tests/test_repo.py -k "station_prerolls or prerolls_fetched_at or insert_preroll or list_prerolls or set_prerolls_fetched_at or eager_load_prerolls or delete_station_cascades_station_prerolls" -x -q` — all 10 new tests pass
- `uv run pytest tests/test_repo.py -x -q` — full repo suite green (Phase 47/74/82 regressions caught)
- Quick run: `uv run pytest tests/test_player.py tests/test_soma_import.py tests/test_repo.py -q` exits 0
- Full suite: `uv run pytest -q --tb=short` exits 0
- Schema sanity: the inline `python -c` script in Task 1's `<done>` block prints `OK`
- Source-grep: `grep -c 'def insert_preroll\|def list_prerolls\|def set_prerolls_fetched_at' musicstreamer/repo.py` outputs `3`; `grep -c 'prerolls=self.list_prerolls' musicstreamer/repo.py` outputs `4` (each builder updated)
</verification>

<success_criteria>
- `station_prerolls(id, station_id, url, position)` table exists with ON DELETE CASCADE on station_id after `db_init`
- `stations.prerolls_fetched_at` INTEGER nullable no-default column exists after `db_init`
- Both schema additions are idempotent (D-15)
- `Repo.insert_preroll` rejects non-HTTP(S) schemes (ASVS V5) and per-call positions > 50 (DoS mitigation)
- `Repo.list_prerolls` returns URLs in position order
- `Repo.set_prerolls_fetched_at` round-trips via every Station-builder (`list_stations`, `get_station`, `list_recently_played`, `list_favorite_stations`)
- `Station.prerolls: list[str]` and `Station.prerolls_fetched_at: Optional[int]` are populated by every Station-builder
- `Repo.delete_station(sid)` cascades to `station_prerolls`
- 10 new tests in `tests/test_repo.py` cover all of the above; full suite green
</success_criteria>

<output>
Create `.planning/phases/83-at-start-of-playing-a-station-randomly-select-and-play-one-o/83-01-SUMMARY.md` when done. Record:
- Exact `db_init` line range where the `station_prerolls` CREATE landed
- Exact `db_init` line range where the `prerolls_fetched_at` ALTER landed
- File:line of each of the 3 new Repo methods
- File:line of the 4 Station-builder kwarg edits
- Confirmation that `migration.py` was NOT modified (PATTERNS.md routing invariant)
- Test count (10 new tests in tests/test_repo.py)
- Note any test fixture additions (likely none — existing fixtures should suffice)
</output>
</content>
