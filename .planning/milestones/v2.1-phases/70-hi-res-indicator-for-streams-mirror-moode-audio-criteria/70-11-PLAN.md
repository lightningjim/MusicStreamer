---
phase: 70
plan: 11
type: execute
wave: 5
depends_on: [70-01, 70-02, 70-03, 70-04, 70-05, 70-06, 70-07, 70-08, 70-09, 70-10]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [HRES-01]
tags: [docs, requirements, roadmap, traceability]

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md ### Features (FEAT) gains a new HRES-01 row after THEME-01 with text approved by RESEARCH.md line 26"
    - ".planning/REQUIREMENTS.md ## Traceability table gains a 'HRES-01 | Phase 70 | Complete' row (or Pending if Phase 70 not yet sealed)"
    - ".planning/REQUIREMENTS.md coverage footer counts bumped: total = 21, mapped = 21, complete +1 (if sealed) or pending +1"
    - ".planning/REQUIREMENTS.md trailing 'Last updated' line bumped to today's date with HRES-01 attribution"
    - ".planning/ROADMAP.md Phase 70 entry Goal field replaced from '[To be planned]' with the locked phase-goal text (mirrors CONTEXT.md <domain> first paragraph + UI-SPEC's summary)"
    - ".planning/ROADMAP.md Phase 70 Requirements line updated from 'TBD' to 'HRES-01'"
    - ".planning/ROADMAP.md Phase 70 Plans count updated from '0 plans' to '12 plans' (Plans 70-00 through 70-11)"
    - ".planning/ROADMAP.md Phase 70 Plans checklist enumerates all 12 plan filenames"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "HRES-01 requirement row + Traceability entry + coverage counts updated"
      contains: "HRES-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 70 goal/requirements/plans updated from placeholders"
      contains: "HRES-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md HRES-01 row"
      to: ".planning/REQUIREMENTS.md ## Traceability table"
      via: "single requirement ID anchors both rows; coverage footer reflects the new total"
      pattern: "HRES-01"
    - from: ".planning/ROADMAP.md Phase 70 entry"
      to: ".planning/REQUIREMENTS.md HRES-01"
      via: "Requirements line in ROADMAP cites HRES-01"
      pattern: "Requirements.*HRES-01"
---

<objective>
Wave 5 docs polish — land the HRES-01 requirement row in REQUIREMENTS.md and finalize the ROADMAP.md Phase 70 entry that has held placeholders since the phase was added (2026-05-02). This plan is documentation-only; it changes no production code and adds no tests.

Purpose: phase-completion-decision plumbing — REQUIREMENTS.md is the canonical traceability anchor; ROADMAP.md surfaces phase scope and plan inventory. Plan 70-00..70-10 cited `requirements: [HRES-01]` in their frontmatter; this plan validates the citation by landing the row.

Output: REQUIREMENTS.md and ROADMAP.md updated; no functional change. The phase-completion hook (Phase 63 / VER-01) will bump pyproject.toml version to 2.1.70 when /gsd-verify-work seals Phase 70 — that bump is independent of this docs plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md

<interfaces>
<!-- Locked HRES-01 text proposed by RESEARCH.md line 26. -->

REQUIREMENTS.md ### Features (FEAT) — current last row at line 43 (THEME-01). Phase 70 inserts:
- **HRES-01**: User sees an automatic two-tier audio-quality badge ("LOSSLESS" / "HI-RES") next to each station's now-playing panel, station-tree row, stream-picker entry, and EditStationDialog row, plus a "Hi-Res only" filter chip and a hi-res-preferring tiebreak in stream failover ordering — all driven from negotiated GStreamer caps cached per stream after first replay, mirroring moOde Audio's Hi-Res convention.

REQUIREMENTS.md ## Traceability table — appends:
| HRES-01 | Phase 70 | Pending | (status flips to "Complete" when the phase-completion hook seals the phase; planner ships with "Pending" — phase-evolution will flip to "Complete" at /gsd-verify-work time)

REQUIREMENTS.md coverage footer:
- v2.1 requirements: 20 → 21
- Mapped to phases: 20 → 21
- Complete: 18 (unchanged at landing time; +1 after phase seal)
- Pending: 2 → 3 (WIN-02 + WIN-05's WIP, plus the new HRES-01 row pre-seal); after phase seal → 2 (only WIN-02 + WIN-05 remain).

ROADMAP.md Phase 70 entry — current lines 575-583:
```
### Phase 70: Hi-res indicator for streams (mirror moOde audio criteria)

**Goal:** [To be planned]
**Requirements**: TBD
**Depends on:** Phase 69
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd-plan-phase 70 to break down)
```

Phase 70 final entry (locked here):
- Goal: "Surface a two-tier audio-quality badge ('LOSSLESS' / 'HI-RES') across the now-playing panel, station-tree rows, stream-picker entries, and EditStationDialog rows, plus a 'Hi-Res only' filter chip and a hi-res-preferring tiebreak in stream failover ordering. Detection is runtime-driven from negotiated GStreamer caps; the rate/depth are persisted per stream so the badge appears on tree rows after the first replay. Mirrors moOde Audio's Hi-Res convention."
- Requirements: HRES-01
- Plans: 12 plans (70-00 RED contract → 70-11 docs polish)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert HRES-01 row in REQUIREMENTS.md ### Features (FEAT) + Traceability table + coverage footer + Last-updated stamp</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (lines 30-120 — current Features + Traceability + coverage footer)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (line 26 — proposed HRES-01 text)
  </read_first>
  <action>
    1) Open .planning/REQUIREMENTS.md.

    2) In `### Features (FEAT)` section after the THEME-01 row at line 43, append a new bullet:
       - `- [ ] **HRES-01**: User sees an automatic two-tier audio-quality badge ("LOSSLESS" / "HI-RES") next to each station's now-playing panel, station-tree row, stream-picker entry, and EditStationDialog row, plus a "Hi-Res only" filter chip and a hi-res-preferring tiebreak in stream failover ordering — all driven from negotiated GStreamer caps cached per stream after first replay, mirroring moOde Audio's Hi-Res convention.`
       (Leave the checkbox unchecked at landing; the phase-completion hook flips it to `[x]` when /gsd-verify-work seals Phase 70.)

    3) In the `## Traceability` table at lines 84-105, append a new row at the bottom of the table (after the WIN-05 row at line 105):
       - `| HRES-01 | Phase 70 | Pending |`
       (Pending → flips to "Complete" at phase seal.)

    4) Bump coverage footer numbers at line 107-112:
       - "v2.1 requirements: 20 total" → "v2.1 requirements: 21 total"
       - "Mapped to phases: 20 ✓" → "Mapped to phases: 21 ✓"
       - "Unmapped: 0 ✓" — unchanged.
       - "Complete: 18" — unchanged at landing (HRES-01 lands Pending).
       - "Pending: 2 (WIN-02 — SMTC Start Menu shortcut with AUMID; WIN-05 — AAC Win11 UAT)" → "Pending: 3 (WIN-02 — SMTC Start Menu shortcut with AUMID; WIN-05 — AAC Win11 UAT; HRES-01 — hi-res indicator)".

    5) Replace the `*Last updated:* ...` line at line 116 with:
       - `*Last updated: 2026-05-12 — HRES-01 (two-tier hi-res audio badge + caps-driven cache + Hi-Res only filter chip + rate/depth tiebreak in stream ordering) added for Phase 70.*`
       (Or whatever today's date is at execution time — the bullet text is the load-bearing part.)

    6) Do not edit any other Requirement row or Traceability row. Do not edit the "Out of Scope" or "Future Requirements" sections.
  </action>
  <verify>
    <automated>grep -c "HRES-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HRES-01" .planning/REQUIREMENTS.md` returns ≥ 3 (Features row + Traceability row + Pending coverage line + Last-updated stamp = 3-4 mentions).
    - HRES-01 Features row contains the verbatim text from RESEARCH.md line 26.
    - Traceability table has a single new row `| HRES-01 | Phase 70 | Pending |`.
    - Coverage footer counts updated: total → 21, mapped → 21.
    - Last-updated stamp bumped with HRES-01 attribution.
    - No other Requirement row text changed.
    - File is valid markdown (no broken table syntax).
  </acceptance_criteria>
  <done>HRES-01 row + Traceability entry + coverage footer + Last-updated stamp landed in REQUIREMENTS.md; no other rows touched.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 70 entry — Goal, Requirements, Plans count, Plans checklist</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    .planning/ROADMAP.md (lines 570-595 — current Phase 70 entry with placeholders)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (<domain> for the canonical goal prose)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (line 26 HRES-01 text)
  </read_first>
  <action>
    1) Open .planning/ROADMAP.md and find the Phase 70 entry (line 575).

    2) Replace `**Goal:** [To be planned]` with the locked goal text:
       - `**Goal:** Surface a two-tier audio-quality badge ("LOSSLESS" / "HI-RES") across the now-playing panel, station-tree rows, stream-picker entries, and EditStationDialog rows, plus a "Hi-Res only" filter chip and a hi-res-preferring tiebreak in stream failover ordering. Detection is runtime-driven from negotiated GStreamer caps; the rate/depth are persisted per stream so the badge appears on tree rows after the first replay. Mirrors moOde Audio's Hi-Res convention.`

    3) Replace `**Requirements**: TBD` with:
       - `**Requirements**: HRES-01`

    4) Replace `**Plans:** 0 plans` with:
       - `**Plans:** 12 plans`

    5) Replace the placeholder Plans checklist `- [ ] TBD (run /gsd-plan-phase 70 to break down)` with the full 12-plan checklist:
       - `- [ ] 70-00-PLAN.md — Wave 0 RED contract (tests + empty hi_res.py skeleton)`
       - `- [ ] 70-01-PLAN.md — Wave 1 hi_res.py pure helpers (classify_tier, bit_depth_from_format, best_tier_for_station, TIER_LABEL constants)`
       - `- [ ] 70-02-PLAN.md — Wave 1 schema + StationStream + Repo CRUD (sample_rate_hz, bit_depth)`
       - `- [ ] 70-03-PLAN.md — Wave 1 stream_ordering rate/depth tiebreak`
       - `- [ ] 70-04-PLAN.md — Wave 2 Player audio-sink-pad caps watcher + queued audio_caps_detected Signal`
       - `- [ ] 70-05-PLAN.md — Wave 2 MainWindow fan-out (repo write + quality_map rebuild)`
       - `- [ ] 70-06-PLAN.md — Wave 3 Now-playing _quality_badge + picker tier suffix`
       - `- [ ] 70-07-PLAN.md — Wave 3 Tree-row pill (station_star_delegate extension)`
       - `- [ ] 70-08-PLAN.md — Wave 3 EditStationDialog "Audio quality" column`
       - `- [ ] 70-09-PLAN.md — Wave 4 "Hi-Res only" filter chip + proxy set_quality_map / set_hi_res_only`
       - `- [ ] 70-10-PLAN.md — Wave 5 Settings export ZIP round-trip forward-compat`
       - `- [ ] 70-11-PLAN.md — Wave 5 Docs polish (HRES-01 row + ROADMAP entry)`

    6) Do not edit any other Phase entry. Do not edit the Roadmap-Evolution log section.
  </action>
  <verify>
    <automated>grep -A 16 "### Phase 70:" .planning/ROADMAP.md | head -18</automated>
  </verify>
  <acceptance_criteria>
    - Phase 70 entry Goal line no longer contains `[To be planned]`.
    - Phase 70 Requirements line contains `HRES-01`.
    - Phase 70 Plans count line reads `**Plans:** 12 plans`.
    - Phase 70 Plans checklist enumerates all 12 plan filenames (70-00 through 70-11) with `- [ ]` checkboxes (unchecked at landing).
    - `grep -c "TBD" .planning/ROADMAP.md` returns the SAME count as pre-Plan-70-11 MINUS the 2-3 TBDs removed from the Phase 70 entry (verify by reading the ROADMAP before and after).
    - No other Phase entry touched.
    - `grep -c "HRES-01" .planning/ROADMAP.md` returns ≥ 1 (the Requirements line).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 70 entry finalized: real Goal + Requirements: HRES-01 + 12-plan count + full plan checklist; no other phase touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs editing | No trust boundary — Markdown edits to local planning files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-31 | Tampering | Markdown coverage-footer drift (mismatch between Features count and Traceability count) | mitigate | Footer numbers explicitly updated to 21/21; acceptance_criteria gate-checks both. |
| T-70-32 | Repudiation | Last-updated stamp staleness | mitigate | Plan explicitly bumps the stamp with HRES-01 attribution. |

ASVS L1 mapping: N/A — documentation change only.
</threat_model>

<verification>
- `grep -c "HRES-01" .planning/REQUIREMENTS.md` returns ≥ 3.
- `grep -c "HRES-01" .planning/ROADMAP.md` returns ≥ 1.
- `grep -A 16 "### Phase 70:" .planning/ROADMAP.md` shows no `[To be planned]` and no `TBD` in the Goal/Requirements/Plans/checklist lines.
- REQUIREMENTS.md coverage footer reads "v2.1 requirements: 21 total" and "Mapped to phases: 21 ✓".
- No production code touched.
</verification>

<success_criteria>
1. HRES-01 row appended to REQUIREMENTS.md ### Features (FEAT) with verbatim RESEARCH.md text.
2. HRES-01 row appended to Traceability table (status Pending).
3. Coverage footer counts bumped (20 → 21).
4. Last-updated stamp bumped with HRES-01 attribution.
5. ROADMAP.md Phase 70 entry Goal + Requirements + Plans count + Plans checklist all finalized.
6. No other rows or entries touched in either file.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-11-SUMMARY.md`
</output>
