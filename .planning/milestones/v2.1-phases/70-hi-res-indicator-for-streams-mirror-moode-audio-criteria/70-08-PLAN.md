---
phase: 70
plan: 08
type: execute
wave: 3
depends_on: [70-01, 70-02]
files_modified:
  - musicstreamer/ui_qt/edit_station_dialog.py
autonomous: true
requirements: [HRES-01]
tags: [edit-dialog, table-column, read-only, tdd-green]

must_haves:
  truths:
    - "edit_station_dialog.py defines new constant _COL_AUDIO_QUALITY = 5 after the existing _COL_POSITION = 4"
    - "streams_table is constructed with 6 columns (was 5); header label 'Audio quality' (Sentence case — UI-SPEC OD-8 lock to disambiguate from existing _COL_QUALITY=1 'Quality' column)"
    - "Header tooltip on _COL_AUDIO_QUALITY: 'Auto-detected from playback. Hi-Res ≥ 48 kHz or ≥ 24-bit on a lossless codec.' (UI-SPEC OD-8 verbatim)"
    - "Column width 90 px, QHeaderView.Fixed mode"
    - "Per-row cell uses QTableWidgetItem(TIER_LABEL_PROSE[tier]) — 'Hi-Res' / 'Lossless' / '' — left-aligned (default), read-only via setFlags(... & ~Qt.ItemIsEditable)"
    - "Tier derived per-row from classify_tier(stream.codec, stream.sample_rate_hz, stream.bit_depth)"
    - "All Wave 0 tests/test_edit_station_dialog.py for Audio quality column turn GREEN"
  artifacts:
    - path: "musicstreamer/ui_qt/edit_station_dialog.py"
      provides: "_COL_AUDIO_QUALITY column + per-row prose cell + header tooltip"
      contains: "_COL_AUDIO_QUALITY = 5"
  key_links:
    - from: "edit_station_dialog._add_stream_row (or equivalent row-construction site)"
      to: "musicstreamer/hi_res.py classify_tier + TIER_LABEL_PROSE"
      via: "tier = classify_tier(...); QTableWidgetItem(TIER_LABEL_PROSE[tier])"
      pattern: "TIER_LABEL_PROSE"
---

<objective>
Wave 3 GREEN — surface the cached tier on the EditStationDialog streams table as a read-only "Audio quality" column. Disambiguates from the existing `_COL_QUALITY=1` "Quality" column (which stores user-authored quality string like "FLAC 1411") via the UI-SPEC OD-8-locked header text "Audio quality" (Sentence case).

Purpose: DP-01 surface 4 (EditStationDialog Quality column) + DP-06. Read-only — Phase 70 has no manual override (DS-03). Mixed-case prose (`Hi-Res` / `Lossless`) per UI-SPEC Copywriting Contract (cell context favors prose; uppercase is reserved for the small-area badge/pill).

Output: tests/test_edit_station_dialog.py Wave 0 RED stubs for Audio quality column + prose label + read-only + header tooltip turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-01-SUMMARY.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-02-SUMMARY.md
@musicstreamer/ui_qt/edit_station_dialog.py

<interfaces>
<!-- Phase 47.3 column constants + streams_table construction. -->

From musicstreamer/ui_qt/edit_station_dialog.py (lines 208-213 — existing column constants):
```
_COL_URL = 0
_COL_QUALITY = 1       # NOTE: user-authored quality string (e.g., "FLAC 1411") — NOT the Phase 70 tier
_COL_CODEC = 2
_COL_BITRATE = 3
_COL_POSITION = 4
```

From musicstreamer/ui_qt/edit_station_dialog.py (lines 398-414 — streams_table setup):
```
self.streams_table = QTableWidget(0, 5)
self.streams_table.setHorizontalHeaderLabels(
    ["URL", "Quality", "Codec", "Bitrate (kbps)", "Position"]
)
...
hdr.setSectionResizeMode(_COL_POSITION, QHeaderView.Fixed)
self.streams_table.setColumnWidth(_COL_POSITION, 60)
self.streams_table.setItemDelegateForColumn(_COL_BITRATE, _BitrateDelegate(self))
```

From musicstreamer/hi_res.py (Plan 70-01):
- classify_tier(codec, sample_rate_hz, bit_depth) -> str
- TIER_LABEL_PROSE: dict[str, str] = {"hires": "Hi-Res", "lossless": "Lossless", "": ""}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _COL_AUDIO_QUALITY = 5 constant + extend streams_table to 6 columns + header 'Audio quality' + tooltip + 90px Fixed width</name>
  <files>musicstreamer/ui_qt/edit_station_dialog.py</files>
  <read_first>
    musicstreamer/ui_qt/edit_station_dialog.py (lines 200-220 — column constants; lines 390-440 — streams_table setup + delegate registration; lines 560-700 — _add_stream_row / row construction)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 168-170 — Copywriting Contract header text; lines 258-269 — Component Inventory item 4)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DP-01, DP-06)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 836-894 — edit_station_dialog modify section)
    musicstreamer/hi_res.py (Plan 70-01)
  </read_first>
  <behavior>
    - Append constant `_COL_AUDIO_QUALITY = 5` after `_COL_POSITION = 4` (UI-SPEC OD-8 placement keeps existing indices stable for snapshot tests + the existing _BitrateDelegate registration at line 414).
    - Change `QTableWidget(0, 5)` → `QTableWidget(0, 6)`.
    - Extend setHorizontalHeaderLabels list: `["URL", "Quality", "Codec", "Bitrate (kbps)", "Position", "Audio quality"]`. NOTE: "Audio quality" is Sentence case (two words, only first letter capitalized) per UI-SPEC OD-8 — disambiguates from existing "Quality".
    - Add `hdr.setSectionResizeMode(_COL_AUDIO_QUALITY, QHeaderView.Fixed)` and `self.streams_table.setColumnWidth(_COL_AUDIO_QUALITY, 90)` (UI-SPEC OD-8 width lock).
    - Add header tooltip: `self.streams_table.horizontalHeaderItem(_COL_AUDIO_QUALITY).setToolTip("Auto-detected from playback. Hi-Res ≥ 48 kHz or ≥ 24-bit on a lossless codec.")` (UI-SPEC OD-8 verbatim).
    - No delegate registration for this column (cells are read-only; default QTableWidgetItem rendering is sufficient).
  </behavior>
  <action>
    1) In the column constants block at edit_station_dialog.py:208-213, append `_COL_AUDIO_QUALITY = 5` AFTER `_COL_POSITION = 4`. Add a comment `# Phase 70 — read-only auto-detected tier (DS-03); disambiguates from _COL_QUALITY user-authored quality string`.

    2) In the streams_table setup block at ~398-414, change the column count from 5 to 6 in `QTableWidget(0, ...)`.

    3) Extend `setHorizontalHeaderLabels` list to include `"Audio quality"` at index 5. Sentence case — UI-SPEC OD-8 lock.

    4) Add `hdr.setSectionResizeMode(_COL_AUDIO_QUALITY, QHeaderView.Fixed)` and `self.streams_table.setColumnWidth(_COL_AUDIO_QUALITY, 90)`. Insert these adjacent to the existing _COL_POSITION resize-mode + width lines.

    5) Add the header tooltip on `_COL_AUDIO_QUALITY` immediately after the column-width call. Use `self.streams_table.horizontalHeaderItem(_COL_AUDIO_QUALITY).setToolTip(...)`. Tooltip string is UI-SPEC OD-8 verbatim.

    6) Per planner critical-rules: NO fenced code blocks in <action>. Source the table-setup extension verbatim from PATTERNS.md lines 878-885 + UI-SPEC §Component Inventory item 4.

    Compatibility: do NOT touch any other column or any reference to _COL_QUALITY=1 / _COL_CODEC=2 / _COL_BITRATE=3 / _COL_POSITION=4. The existing _BitrateDelegate registration on _COL_BITRATE at line 414 remains intact.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py -k "audio_quality_column" -x</automated>
  </verify>
  <acceptance_criteria>
    - `_COL_AUDIO_QUALITY = 5` constant exists at module scope.
    - streams_table is constructed with 6 columns (`QTableWidget(0, 6)`).
    - Header at index 5 reads exactly "Audio quality" (Sentence case).
    - Column 5 width is 90 px (verify via streams_table.columnWidth(5) at runtime).
    - Section resize mode for column 5 is QHeaderView.Fixed.
    - Header tooltip on column 5 matches UI-SPEC OD-8 verbatim.
    - Existing column indices (_COL_URL=0, _COL_QUALITY=1, _COL_CODEC=2, _COL_BITRATE=3, _COL_POSITION=4) unchanged — `grep -c '_COL_URL = 0' musicstreamer/ui_qt/edit_station_dialog.py` == 1; same for the other four.
    - Tests/test_edit_station_dialog.py::test_audio_quality_column_present_and_read_only and test_audio_quality_header_tooltip GREEN (the read-only-cell behavior is fully validated in Task 2).
    - Existing tests pass: `uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py -k "not (audio_quality or quality_column)" -x` exits 0.
  </acceptance_criteria>
  <done>_COL_AUDIO_QUALITY constant + 6-column streams_table + 'Audio quality' header + 90px Fixed width + UI-SPEC OD-8 header tooltip landed; existing column indices unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate _COL_AUDIO_QUALITY cell on each row with classify_tier(...) → TIER_LABEL_PROSE → QTableWidgetItem (read-only, left-aligned)</name>
  <files>musicstreamer/ui_qt/edit_station_dialog.py</files>
  <read_first>
    musicstreamer/ui_qt/edit_station_dialog.py (find the row-construction site — likely _add_stream_row or _populate or inside the stream-iteration in the dialog's setup_streams — search for setItem with _COL_QUALITY to find the analog block, around lines 560-700)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 168-169 — cell text Title-Case prose; lines 260-268 — read-only flag + alignment)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 886-893 — read-only cell pattern)
    musicstreamer/hi_res.py (Plan 70-01)
  </read_first>
  <behavior>
    - For every row added to streams_table (existing setItem block at line ~691 for _COL_QUALITY is the closest analog), add a sibling setItem for _COL_AUDIO_QUALITY.
    - Compute `tier = classify_tier(stream.codec, stream.sample_rate_hz, stream.bit_depth)` per-row.
    - Build cell: `item = QTableWidgetItem(TIER_LABEL_PROSE[tier])` — "Hi-Res" / "Lossless" / "".
    - Make it read-only: `item.setFlags(item.flags() & ~Qt.ItemIsEditable)`.
    - Default left-alignment (QTableWidgetItem default for text) — do NOT set Qt.AlignCenter per UI-SPEC OD-8 lock.
    - `self.streams_table.setItem(row_idx, _COL_AUDIO_QUALITY, item)`.
  </behavior>
  <action>
    1) Add `from musicstreamer.hi_res import classify_tier, TIER_LABEL_PROSE` to module imports if not already present (likely needed; verify the existing import block).

    2) Find the row-construction site (search for `_COL_QUALITY` setItem at line ~691). Add a sibling setItem for `_COL_AUDIO_QUALITY` adjacent to the existing _COL_QUALITY setItem call. Body:
       - Read stream attributes: `tier = classify_tier(stream.codec, stream.sample_rate_hz, stream.bit_depth)`.
       - Construct `QTableWidgetItem(TIER_LABEL_PROSE[tier])`.
       - Apply read-only flag: `item.setFlags(item.flags() & ~Qt.ItemIsEditable)`.
       - setItem onto the table at (row_idx, _COL_AUDIO_QUALITY).

    3) Existing tests for the table's per-row data may need extension — that's the Wave 0 RED stub coverage. The Plan 70-00 stubs already pin the expected values (FLAC 96/24 → "Hi-Res"; FLAC 44/16 → "Lossless"; MP3 → "").

    4) Per planner critical-rules: NO fenced code blocks in <action>. Source the row-construction extension from PATTERNS.md lines 886-893.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py -k "audio_quality" -x</automated>
  </verify>
  <acceptance_criteria>
    - For a station with one FLAC 96/24 stream, the streams_table cell at (0, _COL_AUDIO_QUALITY) renders "Hi-Res".
    - For a station with one FLAC 44/16 stream, the cell renders "Lossless".
    - For a station with one MP3 stream, the cell renders "" (empty string).
    - Cell is read-only: `streams_table.item(0, _COL_AUDIO_QUALITY).flags() & Qt.ItemIsEditable` is 0.
    - All Wave 0 tests/test_edit_station_dialog.py Phase 70 stubs GREEN.
    - Module import: `grep -c 'from musicstreamer.hi_res import' musicstreamer/ui_qt/edit_station_dialog.py` == 1.
    - No regression in existing dialog tests: `uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py -x` exits 0 (or fails only on Wave 0 RED stubs unrelated to Audio quality column).
    - T-40-04 grep baseline: edit_station_dialog.py setTextFormat/setHtml/RichText count remains UNCHANGED (Plan 70-08 adds plain-text QTableWidgetItems only — no QLabel.setTextFormat additions; Phase 51 _sibling_label is the existing RichText user; Phase 70 does NOT touch it).
  </acceptance_criteria>
  <done>Per-row Audio quality cell renders TIER_LABEL_PROSE; cell read-only; all Wave 0 Phase 70 dialog tests GREEN; no regression in existing dialog tests; T-40-04 baseline unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classify_tier return → TIER_LABEL_PROSE lookup | Closed-enum 3-key dict; KeyError-loud on out-of-enum. |
| QTableWidgetItem(label) | Plain-text rendering by QTableWidget default; no markup interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-23 | Tampering | Audio quality cell text | mitigate | Closed-enum lookup via TIER_LABEL_PROSE; QTableWidgetItem renders plain text. |
| T-70-24 | Tampering | Cell editability | mitigate | setFlags(... & ~Qt.ItemIsEditable) — read-only enforcement; matches DS-03 no-manual-override invariant. |

ASVS L1 mapping: V5 Input Validation — closed-enum prose label + read-only cell flag.
</threat_model>

<verification>
- `uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py -x` exits 0.
- Grep gates as enumerated under acceptance_criteria all hold.
- T-40-04 baseline on edit_station_dialog.py setTextFormat/setHtml/RichText count is UNCHANGED — Plan 70-08 adds zero QLabel.setTextFormat or RichText calls. Verify: `grep -c 'setTextFormat\|setHtml\|RichText' musicstreamer/ui_qt/edit_station_dialog.py` returns the same number as pre-Plan-70-08 (which is whatever the current value is in the codebase — confirm with the executor).
</verification>

<success_criteria>
1. _COL_AUDIO_QUALITY = 5 constant defined; streams_table grows to 6 columns.
2. Header "Audio quality" (Sentence case) + 90 px Fixed width + UI-SPEC OD-8 tooltip.
3. Per-row cell renders TIER_LABEL_PROSE['hires' | 'lossless' | ''] from classify_tier.
4. Cell is read-only (no Qt.ItemIsEditable flag).
5. Existing column indices unchanged.
6. All Wave 0 audio_quality RED tests GREEN; no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-08-SUMMARY.md`
</output>
