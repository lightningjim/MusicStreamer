---
phase: 70
plan: 09
type: execute
wave: 4
depends_on: [70-01, 70-02, 70-05, 70-06]
files_modified:
  - musicstreamer/ui_qt/station_list_panel.py
  - musicstreamer/ui_qt/station_filter_proxy.py
autonomous: true
requirements: [HRES-01]
tags: [filter-chip, proxy, pitfall-7, tdd-green]

must_haves:
  truths:
    - "station_filter_proxy.py declares self._hi_res_only: bool = False and self._hi_res_station_ids: set[int] = set() adjacent to Phase 68 _live_only fields"
    - "set_quality_map(quality_map: dict[int, str]) updates _hi_res_station_ids = {sid for sid, tier in (quality_map or {}).items() if tier == 'hires'}; calls invalidate() ONLY when _hi_res_only is True (Pitfall 7 invalidate-guard mirror of Phase 68 set_live_map)"
    - "set_hi_res_only(enabled: bool) always invalidates"
    - "clear_all() resets _hi_res_only = False before invalidate"
    - "has_active_filter() returns True if any of search_text / provider_set / tag_set / _live_only / _hi_res_only is truthy"
    - "filterAcceptsRow station-branch short-circuits to False when _hi_res_only and station_id not in _hi_res_station_ids"
    - "station_list_panel.py constructs _hi_res_chip as QPushButton('Hi-Res only') parallel to _live_chip (lines 280-298); setCheckable + chipState property + _CHIP_QSS reuse verbatim; initial setVisible(False) per F-02; toggled.connect to bound method _on_hi_res_chip_toggled (NOT lambda — QA-05 pattern)"
    - "update_quality_map(quality_map: dict[int, str]) public method on StationListPanel forwards to proxy.set_quality_map AND calls set_hi_res_chip_visible(any('hires' tier in quality_map values))"
    - "set_hi_res_chip_visible(visible: bool) parallels set_live_chip_visible at line 574-587 — flip visibility AND uncheck if hiding while checked"
    - "MainWindow Plan 70-05 fan-out hasattr-guard now matches the new update_quality_map method"
    - "Tooltip 'Show only stations with at least one Hi-Res stream' on chip; accessibleName 'Hi-Res only filter'"
    - "All Wave 0 tests/test_station_filter_proxy.py + tests/test_station_list_panel.py for hi_res_only + chip visibility + Pitfall 7 invalidate-guard turn GREEN"
  artifacts:
    - path: "musicstreamer/ui_qt/station_filter_proxy.py"
      provides: "set_quality_map + set_hi_res_only + Pitfall 7 invalidate-guard + filterAcceptsRow hi-res branch"
      contains: "_hi_res_station_ids"
    - path: "musicstreamer/ui_qt/station_list_panel.py"
      provides: "_hi_res_chip QPushButton + update_quality_map + set_hi_res_chip_visible + _on_hi_res_chip_toggled bound-method slot"
      contains: "_hi_res_chip"
  key_links:
    - from: "station_list_panel.update_quality_map"
      to: "station_filter_proxy.set_quality_map + set_hi_res_chip_visible"
      via: "single coupling point — MainWindow fan-out calls update_quality_map; panel forwards to proxy + flips chip visibility"
      pattern: "set_quality_map"
    - from: "_hi_res_chip.toggled"
      to: "proxy.set_hi_res_only"
      via: "_on_hi_res_chip_toggled bound-method slot"
      pattern: "_on_hi_res_chip_toggled"
---

<objective>
Wave 4 GREEN — Hi-Res-only filter chip in StationListPanel + StationFilterProxyModel.set_quality_map / set_hi_res_only methods + Pitfall 7 invalidate-guard. Mirrors Phase 68 `_live_chip` / `set_live_map` / `set_live_only` architecture verbatim.

Purpose: F-01 (Hi-Res only filter chip) + F-02 (chip visibility gate — hidden by default; visible when any station in library has a cached Hi-Res stream) + Pitfall 7 (Phase 68) invalidate-guard avoids tree-flicker when chip is off.

Output: tests/test_station_filter_proxy.py + tests/test_station_list_panel.py Wave 0 RED stubs for hi-res-only + Pitfall 7 + chip visibility gate turn GREEN. The MainWindow fan-out path Plan 70-05 wired via hasattr-guard now finds the matching update_quality_map method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-01-SUMMARY.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-05-SUMMARY.md
@musicstreamer/ui_qt/station_list_panel.py
@musicstreamer/ui_qt/station_filter_proxy.py
@.planning/phases/68-add-feature-for-detecting-live-performance-streams-di-fm-and/68-CONTEXT.md

<interfaces>
<!-- Phase 68 set_live_map / set_live_only architecture — the verbatim template. -->

From musicstreamer/ui_qt/station_filter_proxy.py (Phase 68 baseline, lines 33-148):
- self._live_only: bool = False
- self._live_channel_keys: set[str] = set()
- def set_live_map(self, live_map): updates _live_channel_keys; if self._live_only: self.invalidate()  # Pitfall 7
- def set_live_only(self, enabled): self._live_only = bool(enabled); self.invalidate()  # always
- def clear_all(self): ... self._live_only = False; self.invalidate()
- def has_active_filter(self): return bool(self._search_text or self._provider_set or self._tag_set or self._live_only)
- def filterAcceptsRow(...): station-branch checks _live_only against _live_channel_keys

From musicstreamer/ui_qt/station_list_panel.py (Phase 68 baseline, lines 271-298 + 554-587):
- self._live_chip = QPushButton("Live now", live_chip_row)
- self._live_chip.setCheckable(True); setProperty("chipState", "unselected"); setStyleSheet(_CHIP_QSS)
- visibility gated by audioaddict_listen_key
- toggled.connect(self._on_live_chip_toggled)
- def _on_live_chip_toggled(checked): self._set_chip_state(...); self._proxy.set_live_only(checked)
- def update_live_map(live_map): self._proxy.set_live_map(live_map)
- def set_live_chip_visible(visible): self._live_chip.setVisible(visible); if not visible and isChecked: setChecked(False)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend StationFilterProxyModel with _hi_res_only + _hi_res_station_ids + set_quality_map + set_hi_res_only + Pitfall 7 invalidate-guard + filterAcceptsRow branch + clear_all + has_active_filter</name>
  <files>musicstreamer/ui_qt/station_filter_proxy.py</files>
  <read_first>
    musicstreamer/ui_qt/station_filter_proxy.py (lines 1-148 — full file)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (F-01, F-02, F-03)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 470-523 Pattern 5; lines 652-660 Pitfall 7)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 725-835 — station_filter_proxy modify section verbatim)
    .planning/phases/68-add-feature-for-detecting-live-performance-streams-di-fm-and/68-CONTEXT.md (Pitfall 7 / D-04 prior-art reference)
    tests/test_station_filter_proxy.py (lines 200-275 — Phase 68 set_live_only + Pitfall 7 test analogs)
  </read_first>
  <behavior>
    - Add instance fields in `__init__` adjacent to existing `_live_only` / `_live_channel_keys`:
      - `self._hi_res_only: bool = False`
      - `self._hi_res_station_ids: set[int] = set()`
    - Add `set_quality_map(self, quality_map: dict[int, str]) -> None`:
      - `self._hi_res_station_ids = {sid for sid, tier in (quality_map or {}).items() if tier == "hires"}`
      - `if self._hi_res_only: self.invalidate()` — Pitfall 7 invalidate-guard mirror.
    - Add `set_hi_res_only(self, enabled: bool) -> None`:
      - `self._hi_res_only = bool(enabled)`
      - `self.invalidate()` — always invalidates (predicate state changed).
    - Extend `clear_all`: add `self._hi_res_only = False` before the existing invalidate() call.
    - Extend `has_active_filter`: add `or self._hi_res_only` to the OR chain.
    - Extend `filterAcceptsRow` station-branch: add a short-circuit `if self._hi_res_only and int(node.station.id) not in self._hi_res_station_ids: return False`. Place this AND-composed with the existing _live_only branch (either before or after — both yield AND semantics).
  </behavior>
  <action>
    Read station_filter_proxy.py to confirm exact line numbers (~33-39 state fields, ~57-79 set_live_map / set_live_only, ~81-90 clear_all, ~92-100 has_active_filter, ~106-148 filterAcceptsRow). Add the new fields + methods + branch additions per <behavior> spec.

    Key invariant — Pitfall 7 mirror: set_quality_map MUST guard `self.invalidate()` behind `if self._hi_res_only:`. set_hi_res_only MUST always invalidate. The Wave 0 test test_set_quality_map_no_invalidate_when_chip_off in tests/test_station_filter_proxy.py monkey-patches invalidate and counts calls to verify this.

    Per planner critical-rules: NO fenced code blocks in <action>. Source the body from PATTERNS.md lines 769-784 (set_quality_map + set_hi_res_only) + lines 826-832 (filterAcceptsRow branch).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_station_filter_proxy.py -k "hi_res or quality_map" -x</automated>
  </verify>
  <acceptance_criteria>
    - StationFilterProxyModel has `_hi_res_only` and `_hi_res_station_ids` instance attrs.
    - `set_quality_map` updates _hi_res_station_ids correctly (only "hires" entries enrolled; "lossless" and "" filtered out).
    - Pitfall 7: when _hi_res_only=False, set_quality_map does NOT call invalidate.
    - Pitfall 7: when _hi_res_only=True, set_quality_map DOES call invalidate.
    - set_hi_res_only always invalidates.
    - clear_all() resets _hi_res_only to False.
    - has_active_filter() returns True when _hi_res_only=True.
    - filterAcceptsRow: when _hi_res_only=True and station.id not in _hi_res_station_ids, returns False.
    - filterAcceptsRow: when _hi_res_only=True and station.id IN _hi_res_station_ids, returns True (existing other-filter logic still composes AND).
    - All Wave 0 tests/test_station_filter_proxy.py Phase 70 stubs GREEN: test_set_hi_res_only_with_quality_map_filters_stations, test_set_quality_map_no_invalidate_when_chip_off, test_clear_all_clears_hi_res_only.
    - No regression: `uv run --with pytest --with pytest-qt pytest tests/test_station_filter_proxy.py -x` exits 0 (Phase 68 _live_only tests still GREEN).
  </acceptance_criteria>
  <done>StationFilterProxyModel carries hi_res_only + quality_map machinery + Pitfall 7 invalidate-guard + filterAcceptsRow branch; all Wave 0 proxy RED stubs GREEN; Phase 68 _live_only invariants preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _hi_res_chip QPushButton to station_list_panel.py + _on_hi_res_chip_toggled slot + update_quality_map + set_hi_res_chip_visible</name>
  <files>musicstreamer/ui_qt/station_list_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/station_list_panel.py (lines 271-330 — _live_chip construction; lines 554-590 — _on_live_chip_toggled + update_live_map + set_live_chip_visible)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 271-285 — Component Inventory item 5; lines 172-174 — Copywriting Contract tooltip + accessibleName)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (F-01, F-02, F-03)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 634-721 — station_list_panel modify section)
    musicstreamer/ui_qt/station_filter_proxy.py (post-Task-1 — set_quality_map + set_hi_res_only available)
  </read_first>
  <behavior>
    - Add `self._hi_res_chip = QPushButton("Hi-Res only", live_chip_row)` IN THE SAME live_chip_row HBoxLayout as _live_chip (UI-SPEC Component Inventory item 5 — placement decision lock; planner-discretion alternative was new sibling row, declined here for minimum-row-count visual).
    - Setup chip:
      - `setCheckable(True)`
      - `setProperty("chipState", "unselected")`
      - `setStyleSheet(_CHIP_QSS)` — verbatim reuse of Phase 68 chip QSS (lines 53-67).
      - `setVisible(False)` — F-02 default.
      - `setToolTip("Show only stations with at least one Hi-Res stream")` — UI-SPEC Copywriting Contract.
      - `setAccessibleName("Hi-Res only filter")` — UI-SPEC a11y.
      - `toggled.connect(self._on_hi_res_chip_toggled)` — bound-method connection, NOT lambda (QA-05 pattern per Phase 68 + STATE.md decisions).
    - Add the chip widget to `lc_layout` between the existing `_live_chip` add and `addStretch(1)`. UI-SPEC Component Inventory item 5 lock: `setSpacing(8)` between chips — verify the existing lc_layout has a default spacing setting (Qt default is 6 px; UI-SPEC locks 8 px between chips). If lc_layout has no explicit setSpacing call, add `lc_layout.setSpacing(8)` BEFORE the chip adds, or use addSpacing(8) between the chips. Implementer chooses the minimal-diff path that matches the UI-SPEC visual cadence.
    - Add `_on_hi_res_chip_toggled(self, checked: bool) -> None`:
      - `self._set_chip_state(self._hi_res_chip, checked)` — mirrors Phase 68 toggle slot at line 562.
      - `self._proxy.set_hi_res_only(checked)` — drives the proxy predicate.
    - Add `update_quality_map(self, quality_map: dict[int, str]) -> None` (parallels update_live_map at line 565-572):
      - `self._proxy.set_quality_map(quality_map)` — forwards to proxy.
      - `self.set_hi_res_chip_visible(any(t == "hires" for t in (quality_map or {}).values()))` — F-02 reactive visibility gate.
    - Add `set_hi_res_chip_visible(self, visible: bool) -> None` (parallels set_live_chip_visible at line 574-587):
      - `self._hi_res_chip.setVisible(visible)`
      - `if not visible and self._hi_res_chip.isChecked(): self._hi_res_chip.setChecked(False)` — prevents stuck filter when chip hides while checked.
  </behavior>
  <action>
    1) Find the _live_chip construction site at station_list_panel.py:280-298. Insert _hi_res_chip construction parallel — same live_chip_row parent, same QSS, same setup pattern. Place AFTER the existing _live_chip widget add (so the chip-row order reads: Live now → Hi-Res only → stretch).

    2) If the existing lc_layout does not setSpacing(8), add `lc_layout.setSpacing(8)` BEFORE the chip adds. Alternative: insert `lc_layout.addSpacing(8)` between the two chip adds. Match the existing project idiom — read the live_chip_row constructor at line 282 for the spacing convention. UI-SPEC Component Inventory item 5 says 8 px gap.

    3) Connect `toggled.connect(self._on_hi_res_chip_toggled)` — bound method, NOT lambda. QA-05 / Phase 68 invariant.

    4) Add the three new methods (`_on_hi_res_chip_toggled`, `update_quality_map`, `set_hi_res_chip_visible`) to the StationListPanel class. Place them adjacent to the existing Phase 68 methods (`_on_live_chip_toggled` at line 554, `update_live_map` at line 565, `set_live_chip_visible` at line 574).

    5) The MainWindow Plan 70-05 fan-out `hasattr(self.station_panel, "update_quality_map")` guard now matches the new method.

    6) Per planner critical-rules: NO fenced code blocks in <action>. Source the chip-construction shape from PATTERNS.md lines 659-678 + the three slot bodies from PATTERNS.md lines 681-721.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_station_list_panel.py -k "hi_res_chip" -x</automated>
  </verify>
  <acceptance_criteria>
    - StationListPanel has `_hi_res_chip` attribute, a QPushButton with text "Hi-Res only", checkable, _CHIP_QSS styled.
    - Initial visibility: False (F-02 default).
    - Chip is added to the same live_chip_row HBoxLayout as _live_chip.
    - Tooltip: "Show only stations with at least one Hi-Res stream".
    - AccessibleName: "Hi-Res only filter".
    - `_on_hi_res_chip_toggled(True)` invokes `self._proxy.set_hi_res_only(True)`.
    - `update_quality_map({1: "hires"})` invokes `proxy.set_quality_map({1: "hires"})` AND flips _hi_res_chip visibility to True (any hires in map).
    - `update_quality_map({1: "lossless", 2: ""})` does NOT flip _hi_res_chip visible.
    - `set_hi_res_chip_visible(False)` unchecks the chip if it was checked (prevents stuck filter).
    - Bound-method connect (not lambda): `grep -v '^[[:space:]]*#' musicstreamer/ui_qt/station_list_panel.py | grep -c 'self._hi_res_chip.toggled.connect(self._on_hi_res_chip_toggled)'` == 1; same line MUST NOT use `lambda`.
    - All Wave 0 tests/test_station_list_panel.py Phase 70 stubs GREEN: test_hi_res_chip_hidden_when_no_hi_res_streams, test_hi_res_chip_visible_after_update_quality_map_with_hires, test_set_hi_res_chip_visible_unchecks_when_hiding, test_hi_res_chip_toggle_calls_proxy_set_hi_res_only.
    - No regression: `uv run --with pytest --with pytest-qt pytest tests/test_station_list_panel.py -x` exits 0.
    - MainWindow Plan 70-05 fan-out hasattr-guard now matches: `hasattr(panel, 'update_quality_map')` returns True at runtime.
  </acceptance_criteria>
  <done>_hi_res_chip + 3 new public methods on StationListPanel; chip uses verbatim Phase 68 QSS + bound-method connect; F-02 visibility gate + Pitfall 7 invalidate-guard end-to-end; all Wave 0 chip-related RED tests GREEN; MainWindow fan-out hasattr-guard satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quality_map: dict[int, str] → set_quality_map | Keys are station ids (int from repo), values are closed-enum tier strings; set-comprehension filters to "hires" only. No injection surface. |
| filterAcceptsRow read of node.station.id | Station objects come from the in-process StationTreeModel; id is int. Membership test against _hi_res_station_ids set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-25 | Tampering | set_quality_map input | mitigate | (quality_map or {}).items() defensive; filter to "hires" only; comprehension is total over any dict. |
| T-70-26 | DoS | Pitfall 7 violation causing invalidate-storm | mitigate | invalidate() guarded behind `if self._hi_res_only:` in set_quality_map; only set_hi_res_only forces re-filter. Tested in tests/test_station_filter_proxy.py::test_set_quality_map_no_invalidate_when_chip_off. |
| T-70-27 | Spoofing | _hi_res_chip toggle as lambda capturing self | mitigate | Bound-method connect per QA-05 / Phase 68 — no lambda lifetime hazards. |

ASVS L1 mapping: V5 Input Validation — defensive `(quality_map or {})` guard + closed-enum filter; QA-05 bound-method connect.
</threat_model>

<verification>
- `uv run --with pytest --with pytest-qt pytest tests/test_station_filter_proxy.py tests/test_station_list_panel.py -x` exits 0.
- `uv run --with pytest --with pytest-qt pytest tests/test_main_window.py -x` exits 0 (Plan 70-05 fan-out path now lands on real methods).
- Pitfall 7 invalidate-guard test test_set_quality_map_no_invalidate_when_chip_off GREEN.
- Bound-method connect (QA-05) preserved.
</verification>

<success_criteria>
1. StationFilterProxyModel has hi_res_only + quality_map machinery + Pitfall 7 invalidate-guard.
2. StationListPanel _hi_res_chip rendered parallel to _live_chip with F-02 visibility gate.
3. update_quality_map + set_hi_res_chip_visible public methods on the panel.
4. _on_hi_res_chip_toggled bound-method (no lambda).
5. All Wave 0 proxy + panel RED stubs GREEN.
6. MainWindow Plan 70-05 fan-out path hasattr-guards now match.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-09-SUMMARY.md`
</output>
