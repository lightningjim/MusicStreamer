---
phase: 70
plan: 01
type: execute
wave: 1
depends_on: [70-00]
files_modified:
  - musicstreamer/hi_res.py
autonomous: true
requirements: [HRES-01]
tags: [hi-res, classifier, pure-helper, tdd-green]

must_haves:
  truths:
    - "musicstreamer/hi_res.py exports bit_depth_from_format, classify_tier, best_tier_for_station, TIER_LABEL_BADGE, TIER_LABEL_PROSE"
    - "classify_tier honors D-01 (two tiers only), D-02 (FLAC/ALAC + (>48000 Hz OR >16-bit) → 'hires'; FLAC/ALAC + below → 'lossless'), D-03 (FLAC/ALAC with rate=0 depth=0 → 'lossless'), D-04 (lossy codecs → '')"
    - "bit_depth_from_format covers DS-02 GstAudioFormat mapping (S16*/U16* → 16, S24*/U24*/S24_32*/U24_32* → 24, S32*/U32*/F32*/F64* → 32, else → 0)"
    - "best_tier_for_station returns 'hires' if any stream classifies as 'hires', else 'lossless' if any classifies as 'lossless', else ''"
    - "All Wave 0 tests in tests/test_hi_res.py turn GREEN (RED-on-hi_res-axis only — Station-construction RED axis remains until Plan 70-02)"
  artifacts:
    - path: "musicstreamer/hi_res.py"
      provides: "Pure-Python classifier helpers + tier label constants"
      contains: "def classify_tier"
      min_lines: 50
  key_links:
    - from: "musicstreamer/hi_res.py"
      to: "musicstreamer.models.Station / StationStream"
      via: "best_tier_for_station reads station.streams iterable; each item must expose .codec, .sample_rate_hz, .bit_depth"
      pattern: "best_tier_for_station"
---

<objective>
Wave 1 GREEN — implement the pure-Python classifier helpers in `musicstreamer/hi_res.py`. Plan 70-00 left the file as a docstring-only skeleton with RED tests pinned to its public API; this plan fills in the body so those tests turn GREEN.

Purpose: One concept per file (mirrors `musicstreamer/eq_profile.py`); pure (no GStreamer / no I/O / no Qt) so the module is safely imported from `player.py`, `now_playing_panel.py`, `station_star_delegate.py`, `station_filter_proxy.py`, `edit_station_dialog.py`, and `stream_ordering.py` without circular-import risk or runtime overhead.

Output: tests/test_hi_res.py GREEN on every test that does NOT depend on StationStream.sample_rate_hz/bit_depth (Plan 70-02 closes that axis).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-00-SUMMARY.md

<interfaces>
<!-- The exact contract Plan 70-00's RED tests pinned. -->

Module musicstreamer/hi_res.py must export:
- TIER_LABEL_BADGE: dict[str, str] = {"hires": "HI-RES", "lossless": "LOSSLESS", "": ""}
- TIER_LABEL_PROSE: dict[str, str] = {"hires": "Hi-Res",  "lossless": "Lossless", "": ""}
- bit_depth_from_format(format_str: str) -> int
- classify_tier(codec: str, sample_rate_hz: int, bit_depth: int) -> str  # "" | "lossless" | "hires"
- best_tier_for_station(station) -> str

Internal constants (private):
- _FORMAT_BIT_DEPTH: dict[str, int]  # DS-02 mapping
- _HIRES_RATE_THRESHOLD_HZ: int = 48_000
- _HIRES_BIT_DEPTH_THRESHOLD: int = 16
- _LOSSLESS_CODECS: set[str] = {"FLAC", "ALAC"}

From musicstreamer/stream_ordering.py (analog for case/None-safe lookup idiom):
```
def codec_rank(codec: str) -> int:
    return _CODEC_RANK.get((codec or "").strip().upper(), 0)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement musicstreamer/hi_res.py pure helpers + TIER_LABEL constants</name>
  <files>musicstreamer/hi_res.py</files>
  <read_first>
    musicstreamer/hi_res.py (Wave 0 docstring-only skeleton)
    musicstreamer/eq_profile.py (module-shape analog, lines 1-30)
    musicstreamer/stream_ordering.py (case/None-safe lookup analog, lines 17-40)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (D-01..D-05, DS-02)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 346-422 — Pattern 3 verbatim)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 48-112)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 156-198 Copywriting Contract)
    tests/test_hi_res.py (Wave 0 RED stubs — read to confirm the expected signatures and constant values)
  </read_first>
  <behavior>
    - bit_depth_from_format(format_str: str) -> int — accepts any string; returns int per DS-02 mapping; unknown/None/empty → 0. Case-sensitive (GstAudioFormat is canonical upper-case short codes per RESEARCH Assumption A2). MUST handle None safely (treat as empty).
    - classify_tier(codec: str, sample_rate_hz: int, bit_depth: int) -> str — returns one of {"", "lossless", "hires"}. Codec compared case-insensitive + whitespace-stripped + None-safe (idiom: `(codec or "").strip().upper()`). Lossy codec → "". FLAC/ALAC + (rate > 48000 OR depth > 16) → "hires". FLAC/ALAC + everything else (including rate=0 depth=0) → "lossless" (D-03 fallback). int() coerces sample_rate_hz / bit_depth defensively against None / falsy.
    - best_tier_for_station(station) -> str — reads station.streams; iterates; aggregates with hi-res > lossless > "". MUST handle station with empty/None streams list. Tier set membership: if "hires" in tiers return "hires"; elif "lossless" in tiers return "lossless"; else return "".
    - TIER_LABEL_BADGE / TIER_LABEL_PROSE — module-level dict[str, str] constants per UI-SPEC Copywriting Contract (badge = all-caps "HI-RES" / "LOSSLESS"; prose = title-case "Hi-Res" / "Lossless"; "" maps to "" so caller can dispatch a single-key lookup without branching).
  </behavior>
  <action>
    Replace the docstring-only skeleton in musicstreamer/hi_res.py with the full implementation. File structure (top → bottom):

    1. Module docstring (KEEP from Wave 0 — extend with the locked behavioral rules per D-01..D-05).
    2. `from __future__ import annotations` (mirrors eq_profile.py / stream_ordering.py).
    3. Private constants block: `_FORMAT_BIT_DEPTH` dict, `_HIRES_RATE_THRESHOLD_HZ = 48_000`, `_HIRES_BIT_DEPTH_THRESHOLD = 16`, `_LOSSLESS_CODECS = {"FLAC", "ALAC"}`.
    4. Public constants block: `TIER_LABEL_BADGE: dict[str, str] = {"hires": "HI-RES", "lossless": "LOSSLESS", "": ""}` and `TIER_LABEL_PROSE: dict[str, str] = {"hires": "Hi-Res", "lossless": "Lossless", "": ""}`.
    5. `def bit_depth_from_format(format_str: str) -> int` — single-line body `return _FORMAT_BIT_DEPTH.get((format_str or ""), 0)`. Type hint matches the contract.
    6. `def classify_tier(codec: str, sample_rate_hz: int, bit_depth: int) -> str` — body normalizes codec (lines 397-401 of RESEARCH.md verbatim), early-returns "" if not in `_LOSSLESS_CODECS`, coerces rate/depth via `int(... or 0)`, applies the OR-threshold predicate (`rate > 48_000 or depth > 16`), returns "hires" / "lossless".
    7. `def best_tier_for_station(station) -> str` — tier-set comprehension across `station.streams or []`; hi-res > lossless > "" precedence. Pure (no DB calls, no Qt). Per RESEARCH Pattern 3 lines 407-421.

    Source the entire module body from 70-RESEARCH.md Pattern 3 (lines 346-422) verbatim — do not improvise. The Phase 47.1 codec_rank precedent at stream_ordering.py:25-31 is the load-bearing prior art for the case/None-safe codec normalization (whitespace tolerance + None safety).

    Per UI-SPEC Copywriting Contract (line 182-198), TIER_LABEL_BADGE / TIER_LABEL_PROSE are module-level dicts so a future i18n pass has one swap point. Do NOT inline the strings in branch logic — callers MUST look them up via the dict for the swap-point invariant to hold.

    DS-02 mapping per CONTEXT.md decision: 8-bit formats (S8 / U8) explicitly map to 0 (not 8) — they are below the hi-res classification floor and would only generate noise in the badge layer. Other "anything else" GstAudioFormat strings (e.g., DSD bitstreams, mu-law, A-law) map to 0 via dict.get(default=0).

    Per planner critical-rules: do NOT include fenced code blocks inside this <action>. The implementation lives in the file; PATTERNS.md and RESEARCH.md carry the load-bearing excerpts.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_hi_res.py -k "classify_tier or bit_depth_from_format or tier_label" -x</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/hi_res.py ≥ 50 lines (excluding blank lines and comments).
    - `python -c "from musicstreamer.hi_res import classify_tier, bit_depth_from_format, best_tier_for_station, TIER_LABEL_BADGE, TIER_LABEL_PROSE; print('ok')"` prints "ok".
    - `classify_tier("FLAC", 44100, 16) == "lossless"`; `classify_tier("FLAC", 96000, 24) == "hires"`; `classify_tier("FLAC", 48000, 24) == "hires"`; `classify_tier("FLAC", 96000, 16) == "hires"`; `classify_tier("FLAC", 0, 0) == "lossless"`; `classify_tier("ALAC", 0, 0) == "lossless"`; `classify_tier("MP3", 0, 0) == ""`; `classify_tier("AAC", 96000, 24) == ""`; `classify_tier(None, 0, 0) == ""`; `classify_tier("  flac  ", 44100, 16) == "lossless"`.
    - `bit_depth_from_format("S16LE") == 16`; `bit_depth_from_format("S24LE") == 24`; `bit_depth_from_format("S24_32LE") == 24`; `bit_depth_from_format("F32LE") == 32`; `bit_depth_from_format("F64LE") == 32`; `bit_depth_from_format("S8") == 0`; `bit_depth_from_format("") == 0`; `bit_depth_from_format(None) == 0`; `bit_depth_from_format("UNKNOWN") == 0`.
    - `TIER_LABEL_BADGE["hires"] == "HI-RES"`; `TIER_LABEL_BADGE["lossless"] == "LOSSLESS"`; `TIER_LABEL_BADGE[""] == ""`.
    - `TIER_LABEL_PROSE["hires"] == "Hi-Res"`; `TIER_LABEL_PROSE["lossless"] == "Lossless"`; `TIER_LABEL_PROSE[""] == ""`.
    - tests/test_hi_res.py::test_classify_tier_truth_table, test_bit_depth_from_format, test_tier_label_badge_constants, test_tier_label_prose_constants all GREEN.
    - tests/test_hi_res.py::test_best_tier_for_station may still be RED if it depends on StationStream(sample_rate_hz=N, bit_depth=N) — that turns GREEN in Plan 70-02. Test pre-condition: best_tier_for_station MUST be callable and return correct values when passed a duck-typed Station object with hand-constructed `.streams` list.
    - No new external dependencies; module imports only stdlib + `__future__`.
    - Grep gate: `grep -c "import gi\|import Gst\|import Qt\|import PySide6" musicstreamer/hi_res.py` returns 0 — module is pure (no Qt/GStreamer imports).
  </acceptance_criteria>
  <done>tests/test_hi_res.py turns GREEN on classify_tier + bit_depth_from_format + TIER_LABEL constants; best_tier_for_station GREEN with duck-typed station; module is pure-Python (no Qt/GStreamer/I-O imports).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → classify_tier | Codec strings come from import sources (user-edited DB fields, AA / SomaFM / GBS imports). Tier text from classify_tier is closed-enum {"", "lossless", "hires"} — no injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | classify_tier return value used as label key | mitigate | Return value is closed-enum {"", "lossless", "hires"}; TIER_LABEL_BADGE / TIER_LABEL_PROSE dicts only carry the three keys → any other return value would KeyError on lookup (fail-loud, not silent injection). |
| T-70-02 | Tampering | bit_depth_from_format input | mitigate | Returns 0 for any unknown format string (closed allowlist via _FORMAT_BIT_DEPTH dict.get default=0). Cannot execute attacker-controlled content. |
| T-70-03 | Information disclosure | best_tier_for_station traversal of station.streams | accept | Station objects are owned by the in-process repo; no cross-process exposure. |

ASVS L1 mapping: V5 Input Validation — closed-enum return values + closed-allowlist dict lookup on the only attacker-influenceable inputs (codec string, format string).
</threat_model>

<verification>
- `uv run --with pytest pytest tests/test_hi_res.py -x` exits 0 OR fails ONLY on test_best_tier_for_station tests that construct `StationStream(sample_rate_hz=N, bit_depth=N)` (those turn GREEN in Plan 70-02).
- `uv run --with pytest pytest -x` (full suite) reveals no regressions outside the Wave 0 RED stubs.
- Module purity: `grep -c "import gi\|import Gst\|from gi\|import PySide6\|from PySide6\|import Qt\|from Qt" musicstreamer/hi_res.py` returns 0.
- Module size: `grep -v '^[[:space:]]*$' musicstreamer/hi_res.py | grep -cv '^[[:space:]]*#'` ≥ 30 (non-blank non-comment lines).
</verification>

<success_criteria>
1. musicstreamer/hi_res.py exports the five public symbols (TIER_LABEL_BADGE, TIER_LABEL_PROSE, bit_depth_from_format, classify_tier, best_tier_for_station).
2. classify_tier honors D-01..D-04 + D-05 label spelling + DS-02 mapping.
3. tests/test_hi_res.py classify_tier / bit_depth_from_format / TIER_LABEL constants tests turn GREEN.
4. Module is pure: zero GStreamer/Qt/I-O imports.
5. No regression in pre-existing tests across the suite.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-01-SUMMARY.md`
</output>
