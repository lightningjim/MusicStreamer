---
phase: 70
plan: 03
type: execute
wave: 1
depends_on: [70-00, 70-02]
files_modified:
  - musicstreamer/stream_ordering.py
autonomous: true
requirements: [HRES-01]
tags: [stream-ordering, sort-key, tiebreak, tdd-green]

must_haves:
  truths:
    - "order_streams sort key includes -sample_rate_hz and -bit_depth between -bitrate_kbps and position"
    - "Within the same codec rank, FLAC-96/24 sorts above FLAC-44/16"
    - "Unknown rate/depth (0) preserves prior behavior — sorts last among same codec with same bitrate"
    - "Cross-codec hi-res promotion forbidden (S-02): a hi-res AAC does NOT outrank a CD-FLAC because codec_rank stays primary"
    - "Existing test_gbs_flac_ordering regression test still passes (rate=0 depth=0 → no behavior change)"
  artifacts:
    - path: "musicstreamer/stream_ordering.py"
      provides: "order_streams sort key extended with rate/depth tiebreak"
      contains: "-s.sample_rate_hz"
  key_links:
    - from: "musicstreamer/stream_ordering.py order_streams"
      to: "musicstreamer/models.py StationStream.sample_rate_hz / .bit_depth"
      via: "sort key reads s.sample_rate_hz, s.bit_depth"
      pattern: "s\\.sample_rate_hz"
---

<objective>
Wave 1 GREEN — extend `order_streams` sort key with rate/depth tiebreak per S-01. Hi-res FLAC outranks CD FLAC within the same codec rank; lossless-over-lossy invariant preserved per S-02.

Purpose: Failover-time ordering now prefers higher-quality variants of the same codec when multiple streams (e.g., a station offering FLAC-44/16 AND FLAC-96/24) coexist. The existing -bitrate_kbps tiebreak stays primary among same-codec streams; rate/depth break further ties.

Output: tests/test_stream_ordering.py::test_hires_flac_outranks_cd_flac turns GREEN; test_gbs_flac_ordering regression still passes (rate/depth default 0 leaves GBS fixtures unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@musicstreamer/stream_ordering.py

<interfaces>
<!-- Current Phase 47.1 sort key (pre-Phase 70). -->

From musicstreamer/stream_ordering.py order_streams (lines 43-64 — Phase 47.1):
```
known = [s for s in streams if (s.bitrate_kbps or 0) > 0]
unknown = [s for s in streams if (s.bitrate_kbps or 0) <= 0]
known_sorted = sorted(
    known,
    key=lambda s: (
        -quality_rank(s.quality),
        -codec_rank(s.codec),
        -(s.bitrate_kbps or 0),
        s.position,
    ),
)
unknown_sorted = sorted(unknown, key=lambda s: s.position)
return known_sorted + unknown_sorted
```

Phase 70 S-01 final sort key:
```
(
    -quality_rank(s.quality),
    -codec_rank(s.codec),
    -(s.bitrate_kbps or 0),
    -(s.sample_rate_hz or 0),
    -(s.bit_depth or 0),
    s.position,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend order_streams sort key with -sample_rate_hz and -bit_depth tiebreak terms</name>
  <files>musicstreamer/stream_ordering.py</files>
  <read_first>
    musicstreamer/stream_ordering.py (lines 1-64 — current implementation)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (S-01, S-02)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 225-259 — sort-key extension pattern)
    tests/test_stream_ordering.py (lines 1-180 — _s helper + existing tests including test_gbs_flac_ordering regression target at lines 147-172)
  </read_first>
  <behavior>
    - Insert two new tiebreak terms `-(s.sample_rate_hz or 0)` and `-(s.bit_depth or 0)` between the existing `-(s.bitrate_kbps or 0)` and `s.position` terms in the known_sorted key lambda.
    - Order matters: sample_rate_hz before bit_depth — matches schema column order and CONTEXT S-01 final key.
    - Unknown rate/depth (0) values fall behind known values via `-0 = 0` ties (then breaks to position).
    - Lossless-over-lossy invariant: codec_rank stays the primary tiebreak after quality_rank, so a hi-res AAC (rare/synthetic) still loses to a CD-FLAC because codec_rank(FLAC)=3 > codec_rank(AAC)=2. S-02 lock.
    - No new helper functions, no new constants — single sort-key extension.
    - The `unknown_sorted` branch (streams with bitrate_kbps <= 0) is NOT extended — those still sort by position only. Streams with bitrate=0 but rate/depth populated are extraordinarily unlikely (caps detection happens during playback, which implies the stream was played, which implies bitrate is usually known via ICY or initial parsing). Documented behavior: unknown bitrate → unknown ordering; rate/depth there is irrelevant.
  </behavior>
  <action>
    Edit musicstreamer/stream_ordering.py `order_streams` function. In the `key=lambda s: (...)` tuple for `known_sorted`, insert two new lines BETWEEN the existing `-(s.bitrate_kbps or 0),` term and the `s.position,` term:
    - `-(s.sample_rate_hz or 0),`
    - `-(s.bit_depth or 0),`

    Defensive `or 0` mirrors the existing bitrate_kbps idiom — None-safe and 0-safe. The negation makes larger values sort first (the project's existing descending-numeric idiom).

    Update the docstring (if present near the sort key) to mention S-01 — rate/depth as secondary tiebreak within same codec rank. Do NOT touch the unknown_sorted branch.

    Per planner critical-rules: NO fenced code blocks in <action>. Source the extension shape from PATTERNS.md lines 246-256.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_stream_ordering.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `order_streams` returns FLAC-96/24 before FLAC-44/16 when both have the same bitrate_kbps and codec (test_hires_flac_outranks_cd_flac GREEN).
    - test_gbs_flac_ordering regression test passes — GBS fixtures use bitrate_kbps=1411 sentinel but sample_rate_hz=0, bit_depth=0; the new -0 -0 tiebreak ties for both streams, the existing tiebreaks decide the order.
    - Cross-codec invariant: a stream with `codec="AAC", sample_rate_hz=96000, bit_depth=24` does NOT outrank a stream with `codec="FLAC", sample_rate_hz=44100, bit_depth=16` (codec_rank stays primary after quality_rank — S-02 lock).
    - Unknown bitrate branch unchanged: `order_streams([_s("FLAC", 0, position=2, sample_rate_hz=96000), _s("FLAC", 0, position=1)])` returns position=1 first (unknown_sorted still sorts by position only).
    - Grep gate: `grep -v '^[[:space:]]*#' musicstreamer/stream_ordering.py | grep -c -- '-(s.sample_rate_hz or 0)'` == 1.
    - Grep gate: `grep -v '^[[:space:]]*#' musicstreamer/stream_ordering.py | grep -c -- '-(s.bit_depth or 0)'` == 1.
    - No new top-level imports added to stream_ordering.py.
    - No regression: full test_stream_ordering.py suite passes (`uv run --with pytest pytest tests/test_stream_ordering.py -x` exits 0).
  </acceptance_criteria>
  <done>order_streams sort key extended with rate/depth tiebreak; test_hires_flac_outranks_cd_flac GREEN; test_gbs_flac_ordering regression preserved; cross-codec invariant intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → order_streams | Inputs are StationStream instances from `repo.list_streams`; sample_rate_hz / bit_depth are int-coerced at the dataclass level. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-07 | Tampering | sort-key value reading | accept | sample_rate_hz / bit_depth flow from caps detection → repo → StationStream → sort; values are int-bound at the dataclass and parameterized-SQL layer. No injection surface. |
| T-70-08 | Repudiation | Sort order change without log | accept | order_streams is a pure function; no logging is added. The Wave 0 sort tests (test_hires_flac_outranks_cd_flac + test_gbs_flac_ordering regression) lock both new and existing behavior. |

ASVS L1 mapping: V5 Input Validation — the sort function is total and pure; no input validation needed beyond dataclass type hints.
</threat_model>

<verification>
- `uv run --with pytest pytest tests/test_stream_ordering.py -x` exits 0.
- `uv run --with pytest pytest tests/test_hi_res.py tests/test_repo.py tests/test_stream_ordering.py -x` exits 0 (full Wave 1 quick-suite GREEN).
- Grep gate: `grep -v '^[[:space:]]*#' musicstreamer/stream_ordering.py | grep -c 'sample_rate_hz\|bit_depth'` == 2.
- order_streams remains pure: `grep -c 'import gi\|import Gst\|import PySide6\|import sqlite3' musicstreamer/stream_ordering.py` == 0.
</verification>

<success_criteria>
1. order_streams sort key carries -sample_rate_hz and -bit_depth between -bitrate_kbps and position.
2. Hi-res FLAC outranks CD FLAC within same codec rank.
3. Cross-codec hi-res promotion forbidden (codec_rank still primary post-quality-rank).
4. test_gbs_flac_ordering regression test passes.
5. No new imports; module purity preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-03-SUMMARY.md`
</output>
