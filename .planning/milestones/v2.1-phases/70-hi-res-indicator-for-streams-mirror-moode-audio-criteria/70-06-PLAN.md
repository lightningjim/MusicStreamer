---
phase: 70
plan: 06
type: execute
wave: 3
depends_on: [70-01, 70-02, 70-05]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: true
requirements: [HRES-01]
tags: [now-playing, badge, qlabel, qss, picker, tdd-green]

must_haves:
  truths:
    - "now_playing_panel.py constructs a _quality_badge QLabel sibling of _live_badge in icy_row at line 378-395, placed IMMEDIATELY LEFT of _live_badge (UI-SPEC DP-04 lock)"
    - "_quality_badge.setTextFormat(Qt.PlainText) is called (V5 ASVS plain-text invariant; T-40-04 grep baseline grows from 4 → 5)"
    - "_quality_badge QSS is Phase 68 LIVE QSS VERBATIM — palette(highlight) bg, palette(highlighted-text) fg, border-radius 8px, padding 2px 6px, font-weight bold"
    - "_refresh_quality_badge() method mirrors _refresh_live_status' slot-never-raise idiom (line 1424-1483 analog); reads currently-playing stream from self._station.streams (no repo round-trip per RESEARCH OQ 1); computes tier via classify_tier; sets badge text via TIER_LABEL_BADGE; sets visibility; sets tooltip per UI-SPEC Copywriting Contract; sets accessibleName"
    - "_refresh_quality_badge is called from: _on_station_bind, MainWindow → _on_audio_caps_detected fan-out path, theme refresh hooks"
    - "Stream picker formatter (_populate_stream_picker at lines 1028-1037) appends ' — {TIER_LABEL_BADGE[tier]}' when tier is non-empty (DP-05 / UI-SPEC OD-7 — em-dash separator, same font/weight/color as rest of item text)"
    - "All Wave 0 tests in tests/test_now_playing_panel.py for _quality_badge + picker tier-suffix turn GREEN"
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "_quality_badge QLabel + _refresh_quality_badge slot + picker tier-suffix"
      contains: "_quality_badge = QLabel"
  key_links:
    - from: "now_playing_panel.py _refresh_quality_badge"
      to: "musicstreamer/hi_res.py classify_tier + TIER_LABEL_BADGE + TIER_LABEL_PROSE"
      via: "tier = classify_tier(s.codec, s.sample_rate_hz, s.bit_depth); badge.setText(TIER_LABEL_BADGE[tier])"
      pattern: "TIER_LABEL_BADGE"
    - from: "MainWindow._on_audio_caps_detected fan-out"
      to: "now_playing._refresh_quality_badge"
      via: "Plan 70-05 hasattr guard now matches"
      pattern: "_refresh_quality_badge"
---

<objective>
Wave 3 GREEN — surface the quality tier on the now-playing panel: new `_quality_badge` QLabel + `_refresh_quality_badge` slot + stream picker tier-suffix.

Purpose: DP-01 surface 1 (now-playing badge) + DP-05 (stream picker tier suffix). UI-SPEC Component Inventory items 1 + 3 lock the visual contract. Badge uses Phase 68 LIVE QSS verbatim so all 7 preset themes + Custom palette get the contrast attestation for free.

Output: tests/test_now_playing_panel.py Wave 0 RED stubs for quality_badge visibility + tier-text + tooltip + picker suffix turn GREEN. The badge appears immediately left of the existing LIVE badge in `icy_row`; both can co-exist when a station is live AND its cached stream is hi-res.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-01-SUMMARY.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-02-SUMMARY.md
@musicstreamer/ui_qt/now_playing_panel.py

<interfaces>
<!-- Phase 68 LIVE badge anchor + Phase 68 picker formatter — verbatim templates. -->

From musicstreamer/ui_qt/now_playing_panel.py icy_row (lines 378-395 — Phase 68 LIVE badge):
```
icy_row = QHBoxLayout()
icy_row.setContentsMargins(0, 0, 0, 0)
icy_row.setSpacing(6)
self._live_badge = QLabel("LIVE", self)
self._live_badge.setTextFormat(Qt.PlainText)
self._live_badge.setVisible(False)
self._live_badge.setStyleSheet(
    "QLabel {"
    " background-color: palette(highlight);"
    " color: palette(highlighted-text);"
    " border-radius: 8px;"
    " padding: 2px 6px;"
    " font-weight: bold;"
    "}"
)
icy_row.addWidget(self._live_badge)
icy_row.addWidget(self.icy_label, 1)
```

Phase 70 final order in icy_row: `[_quality_badge] 6px [_live_badge] 6px [icy_label →]`

From musicstreamer/ui_qt/now_playing_panel.py _populate_stream_picker (lines 1028-1037 — Phase 68 baseline):
```
for s in streams:
    label = f"{s.quality} — {s.codec}" if s.codec else s.quality or s.label or "stream"
    self.stream_combo.addItem(label, userData=s.id)
```

From musicstreamer/hi_res.py (Plan 70-01):
- classify_tier(codec, sample_rate_hz, bit_depth) -> str
- TIER_LABEL_BADGE: dict[str, str] = {"hires": "HI-RES", "lossless": "LOSSLESS", "": ""}
- TIER_LABEL_PROSE: dict[str, str] = {"hires": "Hi-Res", "lossless": "Lossless", "": ""}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct _quality_badge QLabel + plain-text lock + Phase 68 LIVE QSS verbatim + insert into icy_row LEFT of _live_badge</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py (lines 365-410 — icy_row + LIVE badge construction)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 213-228 — Component Inventory item 1; lines 218-231 — QSS verbatim, DP-07; lines 305-316 — Layout Decisions)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DP-01, DP-04, DP-07)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 415-448 — badge QLabel pattern verbatim)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 753-776 — verbatim badge template)
  </read_first>
  <behavior>
    - Add `self._quality_badge = QLabel("", self)` declaration before the existing `self._live_badge = QLabel(...)` line (~378-395).
    - Set initial state: `setTextFormat(Qt.PlainText)` (V5 ASVS lock), `setVisible(False)`, `setStyleSheet(<Phase 68 LIVE QSS verbatim>)`.
    - Insert into `icy_row` BEFORE the existing `_live_badge` add: `icy_row.addWidget(self._quality_badge)` ; `icy_row.addWidget(self._live_badge)` ; `icy_row.addWidget(self.icy_label, 1)`.
    - Retain `icy_row.setSpacing(6)` — UI-SPEC OD-12 lock.
    - QSS reuses palette(highlight) / palette(highlighted-text) — automatically inherits Phase 66 theme switches.
    - The empty-string initial text + setVisible(False) means the badge is invisible until _refresh_quality_badge sets a real tier.
  </behavior>
  <action>
    Read the existing icy_row block at now_playing_panel.py:378-395. Insert the _quality_badge construction code in the same block, immediately before the _live_badge construction lines. Order in icy_row.addWidget calls: _quality_badge → _live_badge → icy_label (with stretch=1).

    QSS string: copy the existing `_live_badge.setStyleSheet(...)` argument VERBATIM — same six-line concatenated string. Both badges use IDENTICAL QSS so they look identical when both visible.

    Per planner critical-rules: NO fenced code blocks in <action>. Source the verbatim QSS from PATTERNS.md lines 421-435 + UI-SPEC Component Inventory item 1.

    NOTE T-40-04 grep baseline: the plain-text invariant test currently asserts `grep -c "setTextFormat|setHtml|RichText" now_playing_panel.py` == 4 (per CONTEXT 60.3-01 STATE.md decision). Phase 70 adds 1 → baseline becomes 5. Plan 70-08 does NOT add to this count (EditStationDialog already uses Qt.RichText for the sibling_label — a Phase 51 deviation lock; new Audio quality column is plain text via QTableWidgetItem default rendering). The Plan 70-06 +1 IS the only Phase 70 contribution to the count. Update whichever test file owns the grep-lock — find it via `grep -rn "setTextFormat.*setHtml.*RichText" tests/` — and bump the count.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_now_playing_panel.py -k "quality_badge and (visible or hidden or text)" -x</automated>
  </verify>
  <acceptance_criteria>
    - `now_playing_panel.py` defines `self._quality_badge = QLabel(...)` adjacent to `self._live_badge`.
    - `self._quality_badge.setTextFormat(Qt.PlainText)` called.
    - `self._quality_badge.setVisible(False)` called.
    - `self._quality_badge.setStyleSheet(...)` argument matches `self._live_badge.setStyleSheet(...)` argument character-for-character.
    - icy_row order: _quality_badge → _live_badge → icy_label (verify via `grep -A 5 'icy_row.addWidget' now_playing_panel.py` or inspect.getsourcelines).
    - tests/test_now_playing_panel.py::test_quality_badge_visible_for_hires_stream + test_quality_badge_hidden_for_lossy_stream + test_quality_badge_text_matches_tier (defined in Wave 0) — these become GREEN AFTER Task 2 wires _refresh_quality_badge; Task 1 alone ensures the badge widget EXISTS (a Task 1-isolated test could be `panel._quality_badge is not None and isinstance(panel._quality_badge, QLabel)`).
    - Grep gate (T-40-04 plain-text invariant baseline): `grep -c 'setTextFormat\|setHtml\|RichText' musicstreamer/ui_qt/now_playing_panel.py` returns 5 (post-Plan-70-06; pre-Plan was 4). Update the test file that locks the baseline by +1.
    - Grep gate (QSS verbatim — both badges should share the QSS string): `grep -c 'background-color: palette(highlight)' musicstreamer/ui_qt/now_playing_panel.py` returns ≥ 2 (one per badge).
    - No regression in existing now_playing_panel tests: `uv run --with pytest --with pytest-qt pytest tests/test_now_playing_panel.py -x` exits 0 (or fails only on Wave 0 RED stubs that turn GREEN in Task 2).
  </acceptance_criteria>
  <done>_quality_badge QLabel constructed with plain-text lock + Phase 68 LIVE QSS verbatim + correct icy_row position; T-40-04 grep baseline bumped 4 → 5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _refresh_quality_badge slot + wire to _on_station_bind + theme refresh + add picker tier suffix (DP-05 + UI-SPEC OD-7)</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    musicstreamer/ui_qt/now_playing_panel.py (lines 1020-1064 _populate_stream_picker; lines 1380-1490 _refresh_live_status analog)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 159-174 — Copywriting Contract tooltip / picker / accessible-name; lines 213-228 — Component Inventory item 1; lines 248-256 — Component Inventory item 3)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (D-03 FLAC-cold-start defaults to Lossless; DP-04 placement; DP-05 picker formatter)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 450-502 — refresh-slot pattern + picker tier-suffix pattern)
    musicstreamer/hi_res.py (Plan 70-01 — classify_tier + TIER_LABEL_BADGE + TIER_LABEL_PROSE)
  </read_first>
  <behavior>
    - `_refresh_quality_badge(self) -> None` slot. Slot-never-raise idiom (matches Phase 68 _refresh_live_status at lines 1424-1483):
      1. Get the currently-playing stream from `self._station.streams` (preferred — no repo round-trip per RESEARCH OQ 1). The stream picker holds `self._streams` and the current selection index; alternative: read directly from `self._station` which is the bound station object. Implementer chooses the path that yields the currently-bound stream object's `codec`, `sample_rate_hz`, `bit_depth`.
      2. If no station bound (`self._station is None`) or no current stream resolvable, hide the badge and return.
      3. Compute `tier = classify_tier(s.codec, s.sample_rate_hz, s.bit_depth)` (import from musicstreamer.hi_res).
      4. Set badge text: `self._quality_badge.setText(TIER_LABEL_BADGE[tier])`.
      5. Set visibility: `self._quality_badge.setVisible(bool(tier))`.
      6. Set tooltip per UI-SPEC Copywriting Contract:
         - If tier and rate>0 and depth>0 → `f"{TIER_LABEL_PROSE[tier]} — {rate//1000} kHz / {depth}-bit"` (e.g., `"Hi-Res — 96 kHz / 24-bit"`).
         - If tier and (rate==0 or depth==0) → `f"{TIER_LABEL_PROSE[tier]} — playback caps not yet detected"` (e.g., `"Lossless — playback caps not yet detected"` — D-03 fallback prose).
         - If partial (rate known but depth==0, or vice versa) → omit the unknown half: `f"{TIER_LABEL_PROSE[tier]} — 96 kHz"` or `f"{TIER_LABEL_PROSE[tier]} — 24-bit"`.
         - If tier is empty → clear tooltip (`setToolTip("")`).
      7. Set accessibleName: `self._quality_badge.setAccessibleName(f"Stream quality: {TIER_LABEL_PROSE[tier]}")` when tier set; clear when not (`setAccessibleName("")`).
      8. Wrap body in `try: ... except Exception: ... self._quality_badge.setVisible(False); ... pass` (slot-never-raise — match Phase 68 idiom).
    - Wire `_refresh_quality_badge` calls from:
      - `_on_station_bind` (whenever a new station is bound; find this method).
      - The Plan 70-05 fan-out path: MainWindow._on_audio_caps_detected calls `now_playing._refresh_quality_badge()` AFTER repo write. The hasattr guard in Plan 70-05 now matches the existing method.
      - Theme refresh hook if one exists (find it via `grep -nE "theme_change|on_theme|apply_theme" now_playing_panel.py`). If a theme hook exists, call _refresh_quality_badge from it so the badge re-renders on palette change. If no theme hook exists, skip.
      - Stream-picker selection change slot (so when the user picks a different stream, the badge reflects the newly-selected stream's tier).
    - Picker tier-suffix (UI-SPEC OD-7 / DP-05): inside `_populate_stream_picker` (lines 1028-1037), modify the per-stream label construction:
      - Existing: `label = f"{s.quality} — {s.codec}" if s.codec else s.quality or s.label or "stream"`.
      - Phase 70: `base_label = ...` (same as current), then `tier = classify_tier(s.codec, s.sample_rate_hz, s.bit_depth)`, then `tier_suffix = TIER_LABEL_BADGE.get(tier, "")`, then `label = f"{base_label} — {tier_suffix}" if tier_suffix else base_label`. Em-dash separator (U+2014) — matches existing line 1035 idiom.
  </behavior>
  <action>
    1) Add a module-top import: `from musicstreamer.hi_res import classify_tier, TIER_LABEL_BADGE, TIER_LABEL_PROSE` (single import line near the existing musicstreamer-internal imports).

    2) Add `_refresh_quality_badge` method to NowPlayingPanel class. Place it adjacent to `_refresh_live_status` (line 1424+). Body follows the 8-step <behavior> spec; emulate the Phase 68 slot-never-raise idiom verbatim.

    3) Wire `_refresh_quality_badge` calls. The minimal set: from `_on_station_bind` (find it; should be the slot triggered when a new station is loaded into the panel) and from `_on_stream_selected` (the stream picker selection change). Also from any post-caps refresh trigger — the hasattr-guarded call in MainWindow Plan 70-05 already lands the cross-component path.

    4) Modify `_populate_stream_picker` per UI-SPEC OD-7. The two-line addition (compute tier + conditionally suffix) goes inside the existing `for s in streams:` loop at line 1029-1036. Em-dash separator matches existing format.

    5) Per UI-SPEC §Copywriting Contract failure-mode visual contract (lines 200-208): for FLAC with rate=0 / depth=0, the badge MUST appear (tier="lossless") and tooltip MUST say "playback caps not yet detected". This is the D-03 default-to-Lossless fallback — verify the truth-table by hand against Plan 70-01 classify_tier behavior.

    6) Per planner critical-rules: NO fenced code blocks in <action>. Source the slot body from PATTERNS.md lines 450-475 (refresh-slot shape) + UI-SPEC Component Inventory item 1 (tooltip format) + RESEARCH lines 753-776 (badge construction reference).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_now_playing_panel.py -k "quality_badge or picker_label" -x</automated>
  </verify>
  <acceptance_criteria>
    - `NowPlayingPanel._refresh_quality_badge` method exists with no args (besides self).
    - All Wave 0 quality_badge tests in tests/test_now_playing_panel.py turn GREEN: test_quality_badge_visible_for_hires_stream, test_quality_badge_hidden_for_lossy_stream, test_quality_badge_text_matches_tier, test_quality_badge_tooltip_when_caps_known, test_quality_badge_tooltip_cold_start.
    - All Wave 0 picker tests turn GREEN: test_picker_label_appends_tier_suffix, test_picker_label_no_suffix_for_lossy_stream.
    - Slot-never-raise: feeding a broken station (e.g., `panel._station = None`) into `_refresh_quality_badge()` does NOT raise.
    - Grep gate: `grep -v '^[[:space:]]*#' musicstreamer/ui_qt/now_playing_panel.py | grep -c '_refresh_quality_badge'` ≥ 3 (method def + ≥2 call sites).
    - Grep gate: `grep -c 'from musicstreamer.hi_res import' musicstreamer/ui_qt/now_playing_panel.py` == 1 (single import line carrying the three symbols).
    - Plan 70-05 hasattr guard now matches: `hasattr(now_playing, '_refresh_quality_badge')` returns True at runtime.
    - No regression in existing now_playing_panel tests: `uv run --with pytest --with pytest-qt pytest tests/test_now_playing_panel.py -x` exits 0 (Wave 0 RED stubs now GREEN; existing tests unaffected).
    - Em-dash in picker suffix (U+2014, not ASCII `-`): grep gate `grep -v '^[[:space:]]*#' musicstreamer/ui_qt/now_playing_panel.py | grep -c -- ' — '` ≥ 2 (existing line 1035 + Phase 70 suffix line).
  </acceptance_criteria>
  <done>_refresh_quality_badge slot + picker tier-suffix + multi-call-site wiring landed; all Wave 0 quality_badge + picker RED tests GREEN; slot-never-raise idiom honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classify_tier return → TIER_LABEL_BADGE dict lookup | Closed-enum 3-key dict; KeyError-loud on injection attempt (closed-allowlist invariant). |
| Badge text → QLabel.setText with Qt.PlainText | Plain-text format set at widget construction; T-40-04 invariant; any future _quality_badge.setHtml call would also need to drop the PlainText (and would fail the grep-baseline test). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-17 | Tampering | Badge text from classify_tier | mitigate | Plain-text lock via setTextFormat(Qt.PlainText); closed-enum TIER_LABEL_BADGE lookup; KeyError-loud on out-of-enum tier values. |
| T-70-18 | Tampering | Picker label tier suffix | mitigate | QComboBox.addItem(label, userData=s.id) renders label as plain text natively (no rich-text path); tier comes from closed-enum TIER_LABEL_BADGE. |
| T-70-19 | Tampering | Tooltip prose with rate/depth interpolation | accept | Tooltips are plain text rendered by Qt's tooltip widget; rate (int) + depth (int) are dataclass-bound. |

ASVS L1 mapping: V5 Input Validation — closed-enum lookups + plain-text setTextFormat lock cover the new label surfaces.
</threat_model>

<verification>
- `uv run --with pytest --with pytest-qt pytest tests/test_now_playing_panel.py -x` exits 0 (all Wave 0 quality_badge + picker tests GREEN; no regression).
- T-40-04 grep baseline bumped 4 → 5: `grep -c 'setTextFormat\|setHtml\|RichText' musicstreamer/ui_qt/now_playing_panel.py` == 5. Update the test that locks the baseline (find via `grep -rn "now_playing_panel.*setTextFormat\|baseline.*setTextFormat" tests/`).
- MainWindow Plan 70-05 hasattr guard now matches the existing _refresh_quality_badge method.
- Em-dash separator preserved (U+2014, not ASCII `-`).
</verification>

<success_criteria>
1. _quality_badge QLabel constructed in icy_row LEFT of _live_badge with Phase 68 LIVE QSS verbatim + Qt.PlainText lock.
2. _refresh_quality_badge slot mirrors _refresh_live_status' slot-never-raise idiom; honors UI-SPEC Copywriting Contract for badge text + tooltip + accessibleName.
3. _refresh_quality_badge called from _on_station_bind, stream-picker-selection slot, MainWindow fan-out (already hasattr-guarded).
4. Stream picker formatter (_populate_stream_picker) appends " — TIER" via em-dash when tier is non-empty.
5. T-40-04 grep baseline bumped 4 → 5 in the locking test file.
6. All Wave 0 quality_badge + picker RED tests GREEN; no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-06-SUMMARY.md`
</output>
