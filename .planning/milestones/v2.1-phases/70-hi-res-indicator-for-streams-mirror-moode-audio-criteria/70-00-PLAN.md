---
phase: 70
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_hi_res.py
  - tests/test_player_caps.py
  - tests/test_repo.py
  - tests/test_stream_ordering.py
  - tests/test_settings_export.py
  - tests/test_station_filter_proxy.py
  - tests/test_station_star_delegate.py
  - tests/test_now_playing_panel.py
  - tests/test_station_list_panel.py
  - tests/test_edit_station_dialog.py
  - musicstreamer/hi_res.py
autonomous: true
requirements: [HRES-01]
tags: [hi-res, gstreamer, qt, badge, classifier, tdd-red, wave-0]

must_haves:
  truths:
    - "Wave 0 RED contract exists: tests reference identifiers that Wave 1+ implements"
    - "tests/test_hi_res.py imports classify_tier / bit_depth_from_format / best_tier_for_station / TIER_LABEL_BADGE / TIER_LABEL_PROSE and raises ImportError or AttributeError"
    - "tests/test_player_caps.py imports Player and expects audio_caps_detected Signal + _on_caps_negotiated handler (RED until Wave 2)"
    - "tests/test_repo.py extension references sample_rate_hz / bit_depth kwargs on insert_stream / update_stream + test_db_init_idempotent_for_sample_rate_hz"
    - "tests/test_stream_ordering.py extension references _s helper kwargs sample_rate_hz / bit_depth and test_hires_flac_outranks_cd_flac"
    - "tests/test_settings_export.py extension references sample_rate_hz / bit_depth keys round-trip + missing-key forward-compat"
    - "tests/test_station_filter_proxy.py extension references set_quality_map / set_hi_res_only + Pitfall 7 invalidate-guard mirror"
    - "tests/test_station_star_delegate.py extension references HI-RES pill paint + sizeHint growth"
    - "tests/test_now_playing_panel.py extension references _quality_badge visibility + picker tier suffix"
    - "tests/test_station_list_panel.py extension references _hi_res_chip visibility gate"
    - "tests/test_edit_station_dialog.py extension references Audio quality column read-only + prose label"
    - "musicstreamer/hi_res.py exists as an empty module skeleton with the public-API docstring (no implementation)"
  artifacts:
    - path: "musicstreamer/hi_res.py"
      provides: "Empty module skeleton with module docstring listing the public API"
      contains: "Hi-Res audio classification helpers"
    - path: "tests/test_hi_res.py"
      provides: "RED stubs covering classify_tier truth table + bit_depth_from_format + best_tier_for_station"
      contains: "from musicstreamer.hi_res import"
    - path: "tests/test_player_caps.py"
      provides: "RED stubs covering notify::caps → queued Signal → repo.update_stream"
      contains: "audio_caps_detected"
  key_links:
    - from: "tests/test_hi_res.py"
      to: "musicstreamer/hi_res.py"
      via: "from musicstreamer.hi_res import classify_tier, bit_depth_from_format, best_tier_for_station, TIER_LABEL_BADGE, TIER_LABEL_PROSE"
      pattern: "from musicstreamer\\.hi_res import"
    - from: "tests/test_player_caps.py"
      to: "musicstreamer/player.py"
      via: "Player().audio_caps_detected Signal"
      pattern: "audio_caps_detected"
---

<objective>
Wave 0 RED contract for Phase 70. Lock the public API of `musicstreamer/hi_res.py` and the Player caps-detection contract via failing tests + an empty module skeleton. Every test added here MUST be RED (ImportError / AttributeError / assertion failure) until Wave 1+ ships. Mirrors the project's standard RED-first idiom (Phase 62-00, 60.3-01, 999.7-01).

Purpose: Encode the locked CONTEXT decisions (D-01..D-05, DS-01..DS-05, T-01..T-06) and UI-SPEC copy/visibility rules as executable contracts BEFORE any production code is written. Plans 01-09 then turn the RED tests GREEN one surface at a time.

Output:
- `musicstreamer/hi_res.py` empty skeleton (module docstring only, no implementation).
- `tests/test_hi_res.py` NEW — pure-helper RED stubs per T-01.
- `tests/test_player_caps.py` NEW — Player caps + queued-Signal RED stubs per T-06.
- Extensions to 7 existing test modules (repo / stream_ordering / settings_export / station_filter_proxy / station_star_delegate / now_playing_panel / station_list_panel / edit_station_dialog) — each gains RED stubs that pin the contracts later waves must satisfy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-VALIDATION.md
@.claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md

<interfaces>
<!-- Public API of musicstreamer/hi_res.py the RED tests reference. Wave 1 (Plan 70-01) implements; Wave 0 only locks the import surface. -->

From musicstreamer/hi_res.py (Wave 1 will implement; Wave 0 leaves empty skeleton):
- TIER_LABEL_BADGE: dict[str, str] = {"hires": "HI-RES", "lossless": "LOSSLESS", "": ""}
- TIER_LABEL_PROSE: dict[str, str] = {"hires": "Hi-Res",  "lossless": "Lossless", "": ""}
- bit_depth_from_format(format_str: str) -> int
- classify_tier(codec: str, sample_rate_hz: int, bit_depth: int) -> str  # returns "" | "lossless" | "hires"
- best_tier_for_station(station) -> str  # max across station.streams

From musicstreamer/player.py (Wave 2 / Plan 70-04 will implement):
- audio_caps_detected = Signal(int, int, int)  # (stream_id, rate_hz, bit_depth) queued → main
- _on_caps_negotiated(self, pad, _pspec) -> None  # streaming-thread handler
- _caps_armed_for_stream_id: int  # per-URL one-shot guard

From musicstreamer/repo.py (Wave 1 / Plan 70-02 will implement):
- Repo.insert_stream(..., sample_rate_hz: int = 0, bit_depth: int = 0) -> int
- Repo.update_stream(..., sample_rate_hz: int = 0, bit_depth: int = 0) -> None
- StationStream.sample_rate_hz: int = 0
- StationStream.bit_depth: int = 0

From musicstreamer/ui_qt/station_filter_proxy.py (Wave 4 / Plan 70-09 will implement):
- StationFilterProxyModel.set_quality_map(quality_map: dict[int, str]) -> None
- StationFilterProxyModel.set_hi_res_only(enabled: bool) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create musicstreamer/hi_res.py empty skeleton + tests/test_hi_res.py RED stubs (T-01)</name>
  <files>musicstreamer/hi_res.py, tests/test_hi_res.py</files>
  <read_first>
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (D-01..D-05, DS-02, T-01)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 346-422 — Pattern 3 classify_tier + bit_depth_from_format + best_tier_for_station)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 48-112 — module header pattern + enum-dict idiom + TIER_LABEL constants)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 156-198 — Copywriting Contract, TIER_LABEL_BADGE + TIER_LABEL_PROSE forward-i18n constants)
    musicstreamer/eq_profile.py (module-shape analog — read lines 1-30)
    musicstreamer/stream_ordering.py (enum-dict + case/None-safe lookup analog — read lines 17-40)
    tests/test_stream_ordering.py (parametrize truth-table idiom — read lines 1-80)
  </read_first>
  <behavior>
    - musicstreamer/hi_res.py: docstring only, no executable body. The docstring MUST list the five public symbols Wave 1 will provide (TIER_LABEL_BADGE, TIER_LABEL_PROSE, bit_depth_from_format, classify_tier, best_tier_for_station). NO imports. NO definitions. Importing the module from a test MUST succeed; importing any symbol MUST raise ImportError.
    - tests/test_hi_res.py: parametrized truth-table tests that REFERENCE the public API by name and assert specific outputs.
    - test_classify_tier_truth_table — parametrize at least: ("FLAC",44100,16)→"lossless"; ("FLAC",96000,24)→"hires"; ("FLAC",48000,24)→"hires"; ("FLAC",96000,16)→"hires"; ("FLAC",0,0)→"lossless" (D-03); ("ALAC",0,0)→"lossless"; ("MP3",0,0)→""; ("AAC",96000,24)→"" (D-04 no-hires-for-lossy); ("flac",44100,16)→"lossless" (case-insensitive); (None,0,0)→"" (None-safe); ("",0,0)→""; ("  FLAC  ",44100,16)→"lossless" (whitespace-tolerant).
    - test_bit_depth_from_format — exhaustive over DS-02 mapping: "S16LE"→16, "S16BE"→16, "U16LE"→16, "U16BE"→16, "S24LE"→24, "S24BE"→24, "S24_32LE"→24, "S24_32BE"→24, "S32LE"→32, "F32LE"→32, "F32BE"→32, "F64LE"→32, "S8"→0, "U8"→0, ""→0, ("UNKNOWN" or similar bogus)→0.
    - test_best_tier_for_station — uses a small Station-like double (or musicstreamer.models.Station + StationStream) with three streams (one MP3, one FLAC 44/16, one FLAC 96/24); asserts result is "hires". Add empty-streams variant returning "".
    - test_tier_label_badge_constants — asserts TIER_LABEL_BADGE["hires"] == "HI-RES", TIER_LABEL_BADGE["lossless"] == "LOSSLESS", TIER_LABEL_BADGE[""] == "".
    - test_tier_label_prose_constants — asserts TIER_LABEL_PROSE["hires"] == "Hi-Res", TIER_LABEL_PROSE["lossless"] == "Lossless", TIER_LABEL_PROSE[""] == "".
    - All tests MUST currently fail with ImportError (cannot import name 'classify_tier' from 'musicstreamer.hi_res') or AttributeError. RED status is the contract; Plan 70-01 turns them GREEN.
  </behavior>
  <action>
    Create musicstreamer/hi_res.py containing ONLY a triple-quoted module docstring titled "Hi-Res audio classification helpers (Phase 70)" describing the public API (TIER_LABEL_BADGE, TIER_LABEL_PROSE, bit_depth_from_format, classify_tier, best_tier_for_station) and reiterating the load-bearing constraints from PATTERNS.md lines 53-70 (pure, no GStreamer, no I/O, no Qt). The module body is empty (no `from __future__` import, no constants, no functions) so the named imports in tests/test_hi_res.py raise ImportError.

    Create tests/test_hi_res.py mirroring the parametrize idiom in tests/test_stream_ordering.py (lines 14-39). Tests reference symbols by their final names per D-01..D-05 + DS-02 + UI-SPEC Copywriting Contract. Use `from musicstreamer.hi_res import classify_tier, bit_depth_from_format, best_tier_for_station, TIER_LABEL_BADGE, TIER_LABEL_PROSE` at module top. Add the truth-table cases enumerated under <behavior>. For test_best_tier_for_station, construct StationStream + Station via `musicstreamer.models` (already exists pre-Phase 70 — does NOT yet have sample_rate_hz/bit_depth fields, so use keyword args sample_rate_hz=N, bit_depth=N which will fail until Plan 70-02 ships; this is the RED-on-two-axes Phase 62-00 idiom: Plan 70-01 turns hi_res import GREEN, Plan 70-02 turns Station construction GREEN). Mark no skips; the test file must be importable but every test inside fails.

    Do NOT inline production code. Do NOT add a placeholder pytest.fail() — let the natural ImportError be the RED.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_hi_res.py -x --co -q 2>&1 | grep -E "test_classify_tier|test_bit_depth_from_format|test_best_tier_for_station|test_tier_label" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File musicstreamer/hi_res.py exists; `python -c "import musicstreamer.hi_res"` succeeds.
    - `python -c "from musicstreamer.hi_res import classify_tier"` raises ImportError.
    - File tests/test_hi_res.py exists with at least the five test functions named above.
    - `uv run --with pytest pytest tests/test_hi_res.py -x` exits non-zero (RED).
    - Test file collection (`pytest --co`) lists test_classify_tier_truth_table, test_bit_depth_from_format, test_best_tier_for_station, test_tier_label_badge_constants, test_tier_label_prose_constants.
  </acceptance_criteria>
  <done>tests/test_hi_res.py is collected by pytest, all five tests fail with ImportError; musicstreamer/hi_res.py exists with docstring only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_player_caps.py RED stubs (T-06 + threading invariant)</name>
  <files>tests/test_player_caps.py</files>
  <read_first>
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DS-01, DS-02, T-06)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 214-314 Pattern 1 + 1b; lines 632-690 Pitfall 1 + 2 + 4 + 6 + 9)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 963-1018 — test_player_caps.py analog + _fake_caps_pad fixture + signal-emission assertion)
    .claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md (Rule 2 — bus-thread → queued-Signal mandatory)
    tests/test_player_tag.py (Player-fixture + mocked-bus + qtbot.waitSignal idiom — read lines 1-80)
    musicstreamer/player.py (Signal declaration + queued-connection wiring — read lines 260-410)
  </read_first>
  <behavior>
    - test_caps_persists_rate_and_bit_depth: build Player via make_player(qtbot) (mocked Gst pipeline pattern from test_player_tag.py); set player._caps_armed_for_stream_id = 42; build _fake_caps_pad(96000, "S24LE"); call player._on_caps_negotiated(pad, None); assert qtbot.waitSignal(player.audio_caps_detected, timeout=1000) fires with args [42, 96000, 24].
    - test_caps_emitted_as_queued_signal: assert the audio_caps_detected Signal is declared with the (int, int, int) signature and the Player connects it (or MainWindow does) with Qt.ConnectionType.QueuedConnection — Pattern 1: introspect via player.metaObject() or via grep-style assertion on player.py source ("Qt.ConnectionType.QueuedConnection" appearing near audio_caps_detected.connect(). Acceptable assertion: import inspect; src = inspect.getsource(musicstreamer.player); assert "audio_caps_detected" in src and "QueuedConnection" in src (defense-in-depth grep). PROHIBITED: a `grep -c == 0` assertion that counts comment lines; use grep -v '^[[:space:]]*#' filter.
    - test_caps_disarm_after_emit: after _on_caps_negotiated emits, player._caps_armed_for_stream_id is reset to 0 (Pitfall 6 one-shot guard; per-URL idempotency).
    - test_caps_ignores_unknown_format: _fake_caps_pad(96000, "GIBBERISH") → handler returns WITHOUT emitting audio_caps_detected (bit_depth_from_format → 0 → early-return per Pattern 1 line 302-303).
    - test_caps_ignores_zero_rate: _fake_caps_pad(0, "S24LE") → no emit.
    - test_caps_no_double_emit_for_same_stream: arm once with stream_id=42, call handler twice with same pad; assert audio_caps_detected fired exactly once (Pitfall 6 — disarm after first emit).
    - test_main_thread_slot_repo_update_ordering: this test belongs in test_main_window.py (Plan 70-05), NOT here — leave a `# NOTE: Pitfall 4 / Phase 50 D-04 covered by test_main_window.py::test_audio_caps_persists_before_ui_refresh in Plan 70-05` comment so the wave-merge grep finds the link.
    - All tests RED until Plan 70-04 ships the Player audio_caps_detected Signal + _on_caps_negotiated handler.
  </behavior>
  <action>
    Mirror tests/test_player_tag.py shape verbatim for the fixture and the qtbot.waitSignal idiom. Add module-top `from unittest.mock import MagicMock, patch` and `import pytest`. Define a module-level `_fake_caps_pad(rate, format_str) -> MagicMock` helper per PATTERNS.md lines 982-996. Define `make_player(qtbot)` per PATTERNS.md lines 967-980 (re-use test_player_tag.py's pattern; do NOT import a private fixture from test_player_tag.py — duplicate it for module isolation).

    Each test function starts with `player = make_player(qtbot)` followed by the scenario-specific setup. All assertions reference identifiers that do not yet exist on Player (audio_caps_detected, _on_caps_negotiated, _caps_armed_for_stream_id) so collection succeeds but execution fails with AttributeError. The "queued-signal" introspection test uses `inspect.getsource(musicstreamer.player)` + grep-style substring assertion on `"QueuedConnection"` near `"audio_caps_detected"` — this directly tests the qt-glib-bus-threading.md Rule 2 invariant at the source-level.

    Filter comment-only lines when grepping the player source: use `lines = [l for l in src.splitlines() if not l.lstrip().startswith("#")]; joined = "\n".join(lines); assert "audio_caps_detected" in joined and "QueuedConnection" in joined`. NEVER count comment text.

    Do NOT write to Player or repo or pipeline within these tests; the handler is a pure-emitter (Pitfall 2). Slot-level behavior (repo.update_stream call) belongs to Plan 70-05 MainWindow tests.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_player_caps.py -x --co -q 2>&1 | grep -E "test_caps_" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_player_caps.py is collected by pytest (collection succeeds — no SyntaxError, no module-import failures).
    - At least six test functions named test_caps_persists_rate_and_bit_depth, test_caps_emitted_as_queued_signal, test_caps_disarm_after_emit, test_caps_ignores_unknown_format, test_caps_ignores_zero_rate, test_caps_no_double_emit_for_same_stream are listed by `pytest --co`.
    - `uv run --with pytest --with pytest-qt pytest tests/test_player_caps.py -x` exits non-zero (every test fails — AttributeError on audio_caps_detected / _on_caps_negotiated / _caps_armed_for_stream_id).
    - Grep-style threading assertion uses `lines = [l for l in src.splitlines() if not l.lstrip().startswith("#")]` to strip comments before substring check (Nyquist hygiene per planner critical-rules).
  </acceptance_criteria>
  <done>tests/test_player_caps.py collected, ≥6 RED tests, all fail with AttributeError pinned to identifiers Plan 70-04 must add.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend 8 existing test modules with RED stubs (repo / stream_ordering / settings_export / station_filter_proxy / station_star_delegate / now_playing_panel / station_list_panel / edit_station_dialog)</name>
  <files>tests/test_repo.py, tests/test_stream_ordering.py, tests/test_settings_export.py, tests/test_station_filter_proxy.py, tests/test_station_star_delegate.py, tests/test_now_playing_panel.py, tests/test_station_list_panel.py, tests/test_edit_station_dialog.py</files>
  <read_first>
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 905-940 — full Phase Requirements → Test Map)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-VALIDATION.md (lines 38-65 + 69-82 — per-task validation map + Wave 0 requirements)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 1022-1235 — test analog mapping per file)
    tests/test_repo.py (lines 515-595 — Phase 47.2 bitrate_kbps hydrate + idempotent ALTER analog)
    tests/test_stream_ordering.py (lines 14-180 — _s helper + GBS regression test)
    tests/test_settings_export.py (lines 600-720 — Phase 47.3 round-trip + missing-key forward-compat analog)
    tests/test_station_filter_proxy.py (lines 200-275 — Phase 68 set_live_only + Pitfall 7 invalidate-guard analog)
    tests/test_station_star_delegate.py (existing file — read lines 1-60 to learn paint-assertion idiom)
    tests/test_now_playing_panel.py (search for _live_badge tests)
    tests/test_station_list_panel.py (search for _live_chip tests)
    tests/test_edit_station_dialog.py (search for _COL_BITRATE column snapshot tests)
  </read_first>
  <behavior>
    Each existing test module gains a small block of RED stubs that reference identifiers Plans 70-01..70-10 will introduce. ALL tests RED at this point.

    - tests/test_repo.py: add test_sample_rate_hz_hydrated_from_row + test_bit_depth_hydrated_from_row (mirror test_bitrate_kbps_hydrated_from_row at lines 529-541); add test_db_init_idempotent_for_sample_rate_hz (simulates pre-70 schema without the two columns, runs db_init twice, asserts no raise + default-0 hydration — mirror test_bitrate_kbps_migration_adds_column at lines 544-579). Use kwargs sample_rate_hz=96000, bit_depth=24 on insert_stream — currently RED because Repo.insert_stream signature lacks those kwargs (Plan 70-02 fixes).
    - tests/test_stream_ordering.py: extend the _s helper signature with sample_rate_hz: int = 0, bit_depth: int = 0 kwargs; add test_hires_flac_outranks_cd_flac (mirror test_quality_tier_beats_codec_rank shape — two FLAC streams, one 44/16 one 96/24, assert 96/24 sorts first). Sentinel kwarg means existing GBS test still passes (rate/depth default 0 → no sort change).
    - tests/test_settings_export.py: add test_export_import_roundtrip_preserves_sample_rate_hz_and_bit_depth (seed sample_rate_hz=96000, bit_depth=24, export ZIP, re-import into fresh repo, assert columns round-tripped); add test_commit_import_forward_compat_missing_quality_keys (payload omits both keys, assert default 0 — Phase 47.3 P-2 idiom forward-compat lock); add test_station_to_dict_emits_quality_keys.
    - tests/test_station_filter_proxy.py: add test_set_hi_res_only_with_quality_map_filters_stations (set_quality_map({1: "hires", 2: "lossless", 3: ""}) + set_hi_res_only(True) → only station 1 visible); add test_set_quality_map_no_invalidate_when_chip_off (Pitfall 7 mirror — verify invalidate is NOT called when _hi_res_only=False; verify it IS called when _hi_res_only=True after a set_quality_map call); add test_clear_all_clears_hi_res_only.
    - tests/test_station_star_delegate.py: add test_paints_hires_pill_for_hires_station (Station with FLAC 96/24 stream → delegate paint calls drawText with "HI-RES"); test_paints_lossless_pill_for_cd_flac_station (FLAC 44/16 → "LOSSLESS"); test_no_pill_for_lossy_station (MP3 only → no pill paint); test_no_pill_for_provider_row (Provider rows skipped — UI-SPEC Component Inventory item 2 "Provider-row safety"); test_sizehint_grows_for_pill (Station-row sizeHint width > pre-Phase 70 baseline).
    - tests/test_now_playing_panel.py: add test_quality_badge_visible_for_hires_stream (bind a station with one FLAC 96/24 stream → panel._quality_badge.isVisible() True + .text() == "HI-RES"); test_quality_badge_hidden_for_lossy_stream (MP3 only → _quality_badge hidden); test_quality_badge_text_matches_tier (Lossless variant → "LOSSLESS"); test_quality_badge_tooltip_when_caps_known ("Hi-Res — 96 kHz / 24-bit"); test_quality_badge_tooltip_cold_start ("Lossless — playback caps not yet detected" — D-03 fallback); test_picker_label_appends_tier_suffix (combo item shows "FLAC 1411 — HI-RES" for hi-res stream); test_picker_label_no_suffix_for_lossy_stream.
    - tests/test_station_list_panel.py: add test_hi_res_chip_hidden_when_no_hi_res_streams (F-02 initial state); test_hi_res_chip_visible_after_update_quality_map_with_hires (flip on any "hires" entry); test_set_hi_res_chip_visible_unchecks_when_hiding (parallel to set_live_chip_visible at lines 574-587); test_hi_res_chip_toggle_calls_proxy_set_hi_res_only.
    - tests/test_edit_station_dialog.py: add test_audio_quality_column_present_and_read_only (_COL_AUDIO_QUALITY = 5 exists; cell has no ItemIsEditable flag); test_audio_quality_cell_shows_prose_label (FLAC 96/24 → "Hi-Res"; FLAC 44/16 → "Lossless"; MP3 → ""); test_audio_quality_header_tooltip (UI-SPEC OD-8 string match: "Auto-detected from playback. Hi-Res ≥ 48 kHz or ≥ 24-bit on a lossless codec.").

    All extensions RED until later waves ship the producing code. Existing tests in each module remain GREEN (do not modify them — only append).
  </behavior>
  <action>
    For each of the 8 test modules, append (do not rewrite) a Phase 70 block of RED stubs. Use this section-header marker so future readers can find the boundary: `# --- Phase 70 / HRES-01 ---`. Each appended test follows the existing module's import style + fixture conventions; do not introduce new imports unnecessarily. The _s helper extension in test_stream_ordering.py needs to live alongside the existing _s definition (line 9-12 area) — add kwargs without renaming.

    For test_station_star_delegate.py: paint-assertion idiom uses pytest-qt's `qtbot` to render the delegate into a QPixmap and pixel-scan a known region, OR monkey-patch `painter.drawText` to capture call_args. PATTERNS.md does not lock the mechanism — match whatever the existing file uses (likely call-capture via MagicMock if existing tests use it; pixel-scan if not). Match the file's pre-existing style.

    For test_settings_export.py: the round-trip + missing-key tests follow the exact shape at lines 626-720 verbatim (only swap bitrate_kbps for sample_rate_hz / bit_depth on each line). The Phase 47.3 idiom `int(stream.get("KEY", 0) or 0)` produces a default-0 result for the missing-key case.

    All tests reference identifiers that do NOT yet exist on the production code (Repo kwargs, StationStream fields, Panel attributes, Delegate paint helpers, Proxy methods, Dialog column constants). Collection succeeds (no SyntaxError) but execution fails for each appended test with AttributeError / TypeError / AssertionError. RED status is the contract.

    Do NOT add `pytest.fail("Wave 1 implements")` placeholders — let the natural ImportError/AttributeError be the failure. Phase 62-00 idiom: "ImportError IS the RED state, no pytest.fail placeholders" (STATE.md decision).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_repo.py tests/test_stream_ordering.py tests/test_settings_export.py tests/test_station_filter_proxy.py tests/test_station_star_delegate.py tests/test_now_playing_panel.py tests/test_station_list_panel.py tests/test_edit_station_dialog.py --co -q 2>&1 | grep -cE "test_(sample_rate_hz|bit_depth|hires_flac|export_import_roundtrip_preserves_sample_rate|set_hi_res_only|set_quality_map_no_invalidate_when_chip_off|paints_hires_pill|quality_badge|picker_label_appends|hi_res_chip|audio_quality_column)"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test modules collect without SyntaxError.
    - At least 25 new test functions appear in `pytest --co` output across the 8 modules (the new tests enumerated under <behavior>).
    - Running each new test individually exits non-zero (RED).
    - Existing tests in each module remain GREEN — `uv run --with pytest --with pytest-qt pytest tests/test_repo.py tests/test_stream_ordering.py tests/test_settings_export.py tests/test_station_filter_proxy.py tests/test_station_star_delegate.py -k "not (sample_rate_hz or bit_depth or hires_flac or set_hi_res_only or set_quality_map_no_invalidate or paints_hires_pill or quality_badge or picker_label or hi_res_chip or audio_quality)" -x` exits zero.
    - test_gbs_flac_ordering still passes (regression guard).
    - Each new test references identifiers that map to a CONTEXT decision or UI-SPEC component listed in 70-VALIDATION.md Per-Task Verification Map.
  </acceptance_criteria>
  <done>8 test modules extended with Phase 70 RED stubs; all stubs collected and RED; existing tests in each module still GREEN; test_gbs_flac_ordering regression intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest test discovery → module import | Test files import from production modules; Wave 0 leaves musicstreamer/hi_res.py with no executable body so imports succeed at module-level but symbol imports raise ImportError (intended RED state). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-W0-01 | Tampering | musicstreamer/hi_res.py docstring | accept | Module docstring is not a security boundary; rationale: Wave 0 ships zero executable code. |
| T-70-W0-02 | Information disclosure | tests/test_player_caps.py | accept | Test fixtures use synthetic caps payloads; no real network or filesystem I/O; no secrets in fixtures. |

Reference: 70-RESEARCH.md `## Security Domain` lists Phase 70 threats as LOW (closed-enum tier strings, Qt.PlainText lock, parameterized SQL); none materialize in Wave 0 since no production code is added.
</threat_model>

<verification>
- All RED stubs collected without error: `uv run --with pytest --with pytest-qt pytest tests/test_hi_res.py tests/test_player_caps.py tests/test_repo.py tests/test_stream_ordering.py tests/test_settings_export.py tests/test_station_filter_proxy.py tests/test_station_star_delegate.py tests/test_now_playing_panel.py tests/test_station_list_panel.py tests/test_edit_station_dialog.py --co -q` succeeds.
- All NEW tests fail (RED): `uv run --with pytest --with pytest-qt pytest tests/test_hi_res.py tests/test_player_caps.py -x` exits non-zero.
- All existing tests still pass: `uv run --with pytest --with pytest-qt pytest -x --deselect tests/test_hi_res.py --deselect tests/test_player_caps.py -k "not (sample_rate_hz or bit_depth or hires_flac or set_hi_res_only or set_quality_map_no_invalidate or paints_hires_pill or quality_badge or picker_label or hi_res_chip or audio_quality)"` exits zero.
- musicstreamer/hi_res.py: `python -c "import musicstreamer.hi_res"` succeeds; `python -c "from musicstreamer.hi_res import classify_tier"` raises ImportError.
</verification>

<success_criteria>
Wave 0 RED contract complete when:
1. musicstreamer/hi_res.py exists with docstring only (no executable body).
2. tests/test_hi_res.py and tests/test_player_caps.py NEW files exist and are collected by pytest.
3. ≥6 new tests in tests/test_player_caps.py covering T-06 + threading invariant + one-shot disarm + ignore-unknown-format + ignore-zero-rate + no-double-emit.
4. ≥5 new tests in tests/test_hi_res.py covering T-01 truth table + DS-02 mapping + best_tier_for_station + label constants.
5. ≥25 new tests appended across the 8 existing test modules covering T-02..T-05 + chip + dialog + proxy + picker + delegate + main-window-fan-out hooks.
6. Every new test fails (RED) when executed in isolation.
7. Every existing test still passes (no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-00-SUMMARY.md`
</output>
