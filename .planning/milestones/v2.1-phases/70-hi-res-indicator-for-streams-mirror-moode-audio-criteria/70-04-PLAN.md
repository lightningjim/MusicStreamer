---
phase: 70
plan: 04
type: execute
wave: 2
depends_on: [70-00, 70-01, 70-02]
files_modified:
  - musicstreamer/player.py
autonomous: true
requirements: [HRES-01]
tags: [player, gstreamer, caps, signal, threading, tdd-green]

must_haves:
  truths:
    - "Player declares audio_caps_detected = Signal(int, int, int) (stream_id, rate_hz, bit_depth) as a class-level attribute"
    - "Player connects an audio-sink pad's notify::caps once per _set_uri (one-shot per URL via _caps_armed_for_stream_id one-shot guard)"
    - "Streaming-thread caps handler _on_caps_negotiated reads pad.get_current_caps(), parses rate + format, emits audio_caps_detected via the queued Signal; never touches Qt widgets, never calls repo.update_stream, never mutates self._pipeline.set_property"
    - "Pattern 1b sync-read in main-thread _on_playbin_state_changed: when PLAYING is reached on main, attempt a synchronous get_current_caps() read; if non-None, emit audio_caps_detected directly; if None, the streaming-thread notify::caps handler covers the async path"
    - "Disarm-after-emit one-shot semantics: each _set_uri arms a single stream_id; first successful caps read disarms, preventing duplicate writes for the same URL (Pitfall 6)"
    - "Phase 43.1 Pitfall 2 honored: bus-loop / streaming thread MAY ONLY emit signals — qt-glib-bus-threading.md Rule 2 enforced"
    - "All Wave 0 tests/test_player_caps.py RED tests turn GREEN"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "audio_caps_detected Signal + _on_caps_negotiated handler + _arm_caps_watch_for_current_stream wiring inside _set_uri / _on_playbin_state_changed"
      contains: "audio_caps_detected = Signal(int, int, int)"
  key_links:
    - from: "Player.audio_caps_detected"
      to: "MainWindow._on_audio_caps_detected"
      via: "Qt.ConnectionType.QueuedConnection (wired in Plan 70-05 at main_window.py:351 sibling)"
      pattern: "audio_caps_detected"
    - from: "Player._on_caps_negotiated (streaming thread)"
      to: "Player.audio_caps_detected.emit"
      via: "queued Signal emission ONLY — Phase 43.1 Pitfall 2 invariant"
      pattern: "audio_caps_detected\\.emit"
---

<objective>
Wave 2 GREEN — wire GStreamer audio-sink-pad caps detection into Player and expose a queued Signal carrying `(stream_id, rate_hz, bit_depth)` to MainWindow. Plan 70-05 consumes the Signal and persists via `repo.update_stream`.

Purpose: DS-01 — runtime GStreamer caps drive the per-stream rate/depth cache. RESEARCH Pattern 1b (combined sync-read in `_on_playbin_state_changed` + async `notify::caps` on the audio pad) catches both common cases:
- Caps already negotiated when PLAYING transition fires (most HTTP audio streams) → main-thread sync-read path.
- Caps stabilize after PLAYING (HLS / Icecast re-negotiation) → streaming-thread notify::caps handler fires later.

Threading invariant: bus-loop / streaming thread handlers MAY ONLY emit signals. Direct repo writes, widget updates, or pipeline property writes from those threads silently fail per Phase 43.1 cross-OS regression (qt-glib-bus-threading.md Rule 2). The handler is a pure emitter; the persistence + UI dispatch happens on main thread in Plan 70-05.

Output: tests/test_player_caps.py turns GREEN end-to-end; player.py grows by ~50-80 lines (Signal decl + one-shot guard fields + _arm_caps_watch_for_current_stream + _on_caps_negotiated + main-thread sync-read hook in `_on_playbin_state_changed`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-00-SUMMARY.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-01-SUMMARY.md
@.claude/skills/spike-findings-musicstreamer/SKILL.md
@.claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md

<interfaces>
<!-- Player declarations + wiring sites to mirror. -->

From musicstreamer/player.py (lines 265-273 — Signal declaration block, Phase 62 baseline):
```
_playbin_playing_state_reached = Signal()    # bus-loop -> main
_underrun_cycle_opened    = Signal()
_underrun_cycle_closed    = Signal(object)
underrun_recovery_started = Signal()
```

From musicstreamer/player.py (lines 381-401 — queued-connection wiring inside __init__):
```
self._playbin_playing_state_reached.connect(
    self._on_playbin_state_changed, Qt.ConnectionType.QueuedConnection
)
```

From musicstreamer/player.py (lines 674-707 — bus-handler emit-only pattern, Phase 43.1 + 62):
```
def _on_gst_tag(self, bus, msg) -> None: ...
def _on_gst_buffering(self, bus, msg) -> None: ...
```

From musicstreamer/player.py (lines 736-746 — main-thread state-changed slot, Phase 57 / WIN-03):
```
def _on_playbin_state_changed(self) -> None: ...
```

From musicstreamer/hi_res.py (Plan 70-01):
- bit_depth_from_format(format_str: str) -> int  # DS-02 GstAudioFormat → bit-depth mapping
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare audio_caps_detected Signal + caps-watch state on Player + queued-connection wire</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    musicstreamer/player.py (lines 260-410 — Signal declarations + __init__ + queued-connection wiring)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DS-01)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 214-314 Pattern 1; lines 642-650 Pitfall 2; lines 712-724 Pitfall 9 Option A)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 264-307 — Signal declaration pattern + queued-connection wiring)
    .claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md (Rule 2 — bus-thread-to-Qt threading invariant)
    tests/test_player_caps.py (Wave 0 RED contract for the expected attribute names)
  </read_first>
  <behavior>
    - Declare class-level Signal `audio_caps_detected = Signal(int, int, int)` adjacent to the existing _playbin_playing_state_reached / underrun_recovery_started Signals (Phase 62 block at lines 265-273).
    - Declare instance state in Player.__init__:
      - `self._caps_pad = None` — cached audio-pad reference so we can disconnect on next _set_uri.
      - `self._caps_handler_id: int = 0` — GObject handler-id returned by pad.connect; 0 when not connected.
      - `self._caps_armed_for_stream_id: int = 0` — per-URL one-shot guard; 0 when disarmed.
    - Per Phase 43.1 Pitfall 2 + RESEARCH Pitfall 9 Option A — Player does NOT connect audio_caps_detected to its own slot. MainWindow (Plan 70-05) wires it to `MainWindow._on_audio_caps_detected` with Qt.ConnectionType.QueuedConnection. The connection chain crosses thread boundaries automatically because the receiver (MainWindow) lives on the main thread.
    - No QTimer.singleShot from non-Qt threads. No direct repo writes from Player.
  </behavior>
  <action>
    1) In the Signal declaration block (currently at player.py:265-273), add `audio_caps_detected = Signal(int, int, int)` with a one-line comment `# Phase 70 / DS-01: streaming/bus thread → main: persist sample_rate_hz / bit_depth for the playing stream`.

    2) In Player.__init__, immediately after the existing bus-handler-attachment / queued-Signal wiring block (around player.py:381-401), initialize the three new instance fields: `self._caps_pad = None`, `self._caps_handler_id = 0`, `self._caps_armed_for_stream_id = 0`. Add a one-line comment referencing 70-CONTEXT.md DS-01 + qt-glib-bus-threading.md Rule 2.

    3) Do NOT add a self-connect call (`audio_caps_detected.connect(self._on_audio_caps_detected, Qt.ConnectionType.QueuedConnection)`) — Player does not own the repo (Pitfall 9). The slot lives on MainWindow (Plan 70-05). The queued-connection wiring is purely on the receiver side.

    Per planner critical-rules: NO fenced code blocks in <action>. The Signal declaration + state-init lines are tiny — sourced from PATTERNS.md lines 264-307 + RESEARCH Pattern 1 lines 232-240.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_player_caps.py::test_caps_emitted_as_queued_signal -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from musicstreamer.player import Player; print(hasattr(Player, 'audio_caps_detected'))"` prints "True".
    - `Player.audio_caps_detected` is a class-level PySide6 Signal with 3 int args.
    - Player.__init__ initializes `_caps_pad`, `_caps_handler_id`, `_caps_armed_for_stream_id` (verify via test_player_caps.py make_player + getattr assertions).
    - Grep gate (Signal decl): `grep -v '^[[:space:]]*#' musicstreamer/player.py | grep -c 'audio_caps_detected = Signal(int, int, int)'` == 1.
    - Grep gate (no self-connect): `grep -v '^[[:space:]]*#' musicstreamer/player.py | grep -c 'self.audio_caps_detected.connect'` == 0 — MainWindow owns the connection.
    - tests/test_player_caps.py::test_caps_emitted_as_queued_signal turns GREEN (introspects player.py source for "audio_caps_detected" + "QueuedConnection" co-occurrence; QueuedConnection appears in Player.__init__ for OTHER Signals — receiver-side connect lands in Plan 70-05).
    - Reading the test_player_caps.py grep-style assertion: it strips comments first; the substring search for `"audio_caps_detected"` matches the Signal declaration. The substring search for `"QueuedConnection"` matches the existing Phase 62 wiring in Player. Both substrings co-occur in the (non-comment) source post-Task-1.
  </acceptance_criteria>
  <done>audio_caps_detected Signal declared on Player; three state fields initialized in __init__; receiver-side QueuedConnection wiring deferred to Plan 70-05; test_caps_emitted_as_queued_signal GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _on_caps_negotiated (streaming-thread handler) + _arm_caps_watch_for_current_stream + Pattern 1b sync-read hook in _on_playbin_state_changed</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    musicstreamer/player.py (lines 540-746 — _set_uri + _on_playbin_state_changed + bus-handler region)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DS-01, DS-02, DS-03)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 214-314 Pattern 1 + Pattern 1b verbatim handler code; lines 632-700 Pitfalls 1, 2, 4, 6)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 308-346 — bus-handler emit-only + main-thread state-changed slot)
    .claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md (Rule 2 — handler MUST only emit; pitfall is silent message drop on non-QThread)
    musicstreamer/hi_res.py (Plan 70-01 — bit_depth_from_format)
    tests/test_player_caps.py (Wave 0 RED contract — _fake_caps_pad fixture)
  </read_first>
  <behavior>
    - Add `_arm_caps_watch_for_current_stream(self) -> None` method. Behavior:
      1. Disconnect prior pad watch if any (`self._caps_pad.disconnect(self._caps_handler_id)` inside try/except for stale-pad safety) — clears the previous URL's listener before arming a fresh one.
      2. Reset `self._caps_pad = None`, `self._caps_handler_id = 0`.
      3. Return early if `self._current_stream is None`.
      4. Capture the stream id: `self._caps_armed_for_stream_id = self._current_stream.id` (Pitfall 6 — pair the future emit with the URL that triggered the arm; failover-safe).
      5. Fetch the audio pad: `pad = self._pipeline.emit("get-audio-pad", 0)`. If pad is None, return (no audio stream yet — notify::caps will fire later via subsequent state-changed cycles).
      6. Cache `self._caps_pad = pad`; connect `self._caps_handler_id = pad.connect("notify::caps", self._on_caps_negotiated)`.
      7. Perform an immediate synchronous read by calling `self._on_caps_negotiated(pad, None)` directly. If caps are already negotiated, this emits immediately and disarms. If not, the GObject handler we just connected will fire when caps stabilize.
    - Add `_on_caps_negotiated(self, pad, _pspec) -> None` streaming-thread handler. Behavior:
      1. Return immediately if `not self._caps_armed_for_stream_id` (already emitted for this URL — Pitfall 6 one-shot).
      2. Fetch `caps = pad.get_current_caps()`; return if None or empty.
      3. Extract `structure = caps.get_structure(0)`.
      4. Parse rate via the dual-path defensive idiom (RESEARCH Pattern 1 lines 290-295): try `s.get_int("rate")` for the (bool, int) tuple binding; fall back to `int(s["rate"])` if get_int unavailable; final fallback rate=0.
      5. Parse format string via `s.get_string("format")` with dict-access fallback (RESEARCH lines 296-299).
      6. `depth = bit_depth_from_format(fmt or "")` — import from musicstreamer.hi_res.
      7. Return without emitting if `rate <= 0 or depth <= 0` (Pitfall 1 — caps not yet stable).
      8. Capture sid = `self._caps_armed_for_stream_id`; disarm `self._caps_armed_for_stream_id = 0` (one-shot per URL).
      9. `self.audio_caps_detected.emit(sid, rate, depth)` — queued cross-thread to MainWindow's main-thread slot.
      10. Return. Do NOT touch Qt widgets, do NOT call repo, do NOT mutate self._pipeline.
    - Hook `_arm_caps_watch_for_current_stream` in TWO places per RESEARCH Pattern 1b:
      - At the end of `_set_uri` (after `self._pipeline.set_state(Gst.State.PLAYING)`) — installs the async notify::caps watch.
      - Inside main-thread `_on_playbin_state_changed` (line 736-746) — the existing post-PLAYING queued-Signal slot is the natural place for the synchronous one-shot read. Pattern 1b: call `_arm_caps_watch_for_current_stream` here too (idempotent: it disconnects any prior watch first). This catches streams whose caps are already stable at PLAYING (most HTTP audio).
  </behavior>
  <action>
    1) Add `from musicstreamer.hi_res import bit_depth_from_format` to player.py module imports (place near the other musicstreamer-internal imports — e.g., near the `from musicstreamer.url_helpers import aa_normalize_stream_url` line). Single-symbol import keeps player.py purity — hi_res is pure-Python so this does not pull Qt/GStreamer into player.py beyond what is already there.

    2) Add the two new methods `_arm_caps_watch_for_current_stream(self) -> None` and `_on_caps_negotiated(self, pad, _pspec) -> None` to the Player class. Place them adjacent to the existing bus-handler region (after `_on_gst_buffering`, before or after `_on_playbin_state_changed`). Method bodies follow the behavior spec above; source the exact body from RESEARCH Pattern 1 lines 255-307.

    3) In `_set_uri` (around player.py:540-580 — read the function to find the exact insertion point): after the existing `self._pipeline.set_state(Gst.State.PLAYING)` call, add `self._arm_caps_watch_for_current_stream()`. This installs the async notify::caps watch on the new pipeline lifecycle.

    4) In `_on_playbin_state_changed` (line 736-746): after the existing Phase 57 volume re-apply body, add `self._arm_caps_watch_for_current_stream()`. This is the Pattern 1b main-thread synchronous read path — fires post-PLAYING when caps are typically already negotiated.

    5) Add a module-top comment block near the new methods documenting the Phase 43.1 Pitfall 2 invariant: handler runs on the streaming thread, MAY ONLY emit Signals, NEVER touch Qt widgets / repo / pipeline.

    6) Do NOT add `QTimer.singleShot(0, ...)` anywhere in the new code (Pitfall 2 anti-pattern). Do NOT call `self._pipeline.set_property` from the new handler (Pitfall 2 anti-pattern).

    Per planner critical-rules: NO fenced code blocks inside this <action>. The full method bodies live in RESEARCH Pattern 1 (lines 255-307) and PATTERNS.md lines 308-346 — executor copies from there with surgical edits to match the player.py call-site style (`self._pipeline` instead of `pipeline` etc.).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_player_caps.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests in tests/test_player_caps.py turn GREEN (test_caps_persists_rate_and_bit_depth, test_caps_emitted_as_queued_signal, test_caps_disarm_after_emit, test_caps_ignores_unknown_format, test_caps_ignores_zero_rate, test_caps_no_double_emit_for_same_stream).
    - Grep gate (Pitfall 2 — no bare QTimer.singleShot from caps handler): the function bodies of `_on_caps_negotiated` and `_arm_caps_watch_for_current_stream` MUST NOT contain `QTimer.singleShot`. Verify by reading the source of those two methods via `inspect.getsource` in a test, OR by line-range grep: `grep -A 30 'def _on_caps_negotiated' musicstreamer/player.py | grep -c 'QTimer.singleShot'` == 0; same for `_arm_caps_watch_for_current_stream`.
    - Grep gate (Pitfall 2 — no Qt widget touch from caps handler): `grep -A 30 'def _on_caps_negotiated' musicstreamer/player.py | grep -cE '\.setVisible|\.setText|\.setStyleSheet'` == 0.
    - Grep gate (Pitfall 2 — no pipeline mutation): `grep -A 30 'def _on_caps_negotiated' musicstreamer/player.py | grep -cE 'set_property|set_state'` == 0.
    - Grep gate (Pitfall 6 — disarm-after-emit): `grep -A 30 'def _on_caps_negotiated' musicstreamer/player.py | grep -c '_caps_armed_for_stream_id = 0'` ≥ 1.
    - bit_depth_from_format imported from musicstreamer.hi_res — `grep -c 'from musicstreamer.hi_res import bit_depth_from_format' musicstreamer/player.py` == 1.
    - No regression in existing Player tests: `uv run --with pytest --with pytest-qt pytest tests/test_player.py tests/test_player_tag.py tests/test_player_buffering.py -x` exits 0.
    - Specifically test_player_buffering.py + test_player_tag.py still pass — caps wiring does not perturb other bus handlers.
    - Pre-Plan 70-05 expectation: audio_caps_detected emits BUT no receiver is connected yet on MainWindow (Plan 70-05 wires that). Test fixtures in test_player_caps.py use qtbot.waitSignal which subscribes its own listener for the test duration — that's how the tests are GREEN absent MainWindow wiring.
  </acceptance_criteria>
  <done>Player.audio_caps_detected emits queued Signals from a streaming-thread caps handler that obeys Pitfall 2 invariants; Pattern 1b dual-path (sync-on-main + async-on-streaming) covers both common and HLS re-negotiation cases; all 6 tests/test_player_caps.py tests GREEN; no regression in adjacent Player tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GStreamer streaming thread → main thread (Player.audio_caps_detected queued Signal) | Streaming thread is non-QThread (no Qt event loop); MUST cross via queued Signal per qt-glib-bus-threading.md Rule 2. |
| Audio pad's notify::caps → Player._on_caps_negotiated | GObject signal subscription; pad lifetime is bound to the current pipeline (recreated per _set_uri). Disconnect on _set_uri prevents stale handler firing against a destroyed pad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-09 | Tampering | Caps payload (format string + rate int) | mitigate | bit_depth_from_format returns 0 for unknown format strings (closed allowlist); rate is int-coerced; both run defensive `int(... or 0)` guards. The downstream classifier (Plan 70-01 classify_tier) is also closed-enum and None-safe. |
| T-70-10 | Tampering / DoS | Streaming-thread handler reaches into Qt objects | mitigate | Handler ONLY calls `self.audio_caps_detected.emit(...)` and returns. Verified by line-range grep gates rejecting setVisible/setText/setStyleSheet/set_property/set_state inside the handler body. |
| T-70-11 | Repudiation | Caps emit not logged | accept | Phase 70 does not add logging for caps. The DB write in Plan 70-05 is observable via repo state inspection; the Wave 0 test_caps_persists_rate_and_bit_depth pins the contract. |
| T-70-12 | Information disclosure | Stream rate/depth surfaced | accept | Audio format metadata; not PII. |

ASVS L1 mapping: V5 Input Validation — closed-allowlist on the format string + int-coercion on the rate; defensive `if rate <= 0 or depth <= 0: return` guards reject malformed/incomplete caps.
</threat_model>

<verification>
- `uv run --with pytest --with pytest-qt pytest tests/test_player_caps.py -x` exits 0 (all 6 Wave 0 RED stubs GREEN).
- `uv run --with pytest --with pytest-qt pytest tests/test_player.py tests/test_player_tag.py tests/test_player_buffering.py -x` exits 0 (no regression in adjacent Player tests).
- Full Wave 1+2 quick suite: `uv run --with pytest --with pytest-qt pytest tests/test_hi_res.py tests/test_repo.py tests/test_stream_ordering.py tests/test_player_caps.py -x` exits 0.
- Pitfall 2 invariants verified by 4 grep gates on the new methods' line ranges.
- Pitfall 6 invariant verified by grep gate ensuring _caps_armed_for_stream_id is reset to 0 inside _on_caps_negotiated.
</verification>

<success_criteria>
1. Player.audio_caps_detected Signal declared at class scope; instance state initialized in __init__.
2. _arm_caps_watch_for_current_stream + _on_caps_negotiated methods implemented per RESEARCH Pattern 1+1b.
3. _set_uri and _on_playbin_state_changed both call _arm_caps_watch_for_current_stream (dual-path coverage).
4. Pitfall 2 (no Qt-touch from streaming thread) + Pitfall 6 (one-shot disarm) + Pitfall 1 (return on None caps) all honored.
5. All Wave 0 tests/test_player_caps.py tests GREEN.
6. No regression in Player tests; no QTimer.singleShot added to caps handler.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-04-SUMMARY.md`
</output>
