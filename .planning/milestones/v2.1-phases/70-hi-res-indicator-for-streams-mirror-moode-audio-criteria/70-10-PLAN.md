---
phase: 70
plan: 10
type: execute
wave: 5
depends_on: [70-02]
files_modified:
  - musicstreamer/settings_export.py
autonomous: true
requirements: [HRES-01]
tags: [settings-export, zip-roundtrip, forward-compat, tdd-green]

must_haves:
  truths:
    - "_station_to_dict adds two keys after bitrate_kbps: sample_rate_hz: s.sample_rate_hz and bit_depth: s.bit_depth"
    - "_insert_station INSERT SQL grows by two columns + two placeholders + two value-tuple entries; each value uses int(stream.get('sample_rate_hz'|'bit_depth', 0) or 0) forward-compat idiom (Phase 47.3 P-2)"
    - "_replace_station mirrors the same two-line extension verbatim"
    - "Pre-Phase-70 ZIPs missing the keys default both columns to 0 (forward-compat lock)"
    - "All Wave 0 tests/test_settings_export.py for sample_rate_hz / bit_depth round-trip + missing-key forward-compat turn GREEN"
  artifacts:
    - path: "musicstreamer/settings_export.py"
      provides: "sample_rate_hz + bit_depth ZIP round-trip + forward-compat default"
      contains: "sample_rate_hz"
  key_links:
    - from: "_station_to_dict"
      to: "_insert_station / _replace_station SQL"
      via: "round-trip dict → ZIP → import → SQL re-insert preserves sample_rate_hz / bit_depth"
      pattern: "sample_rate_hz"
---

<objective>
Wave 5 GREEN — settings_export.py ZIP round-trip preserves `sample_rate_hz` + `bit_depth`. Forward-compat for pre-Phase-70 ZIPs via the Phase 47.3 idiom `int(stream.get("KEY", 0) or 0)`.

Purpose: DS-04 — settings ZIPs carry the cache between machines (Linux ↔ Windows) so a user who has built a hi-res cache on one device sees it light up immediately on the other after import. Old ZIPs without the keys default both columns to 0 (cache rebuilds on next replay per DS-05).

Output: tests/test_settings_export.py Wave 0 RED stubs turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-02-SUMMARY.md
@musicstreamer/settings_export.py
@.planning/phases/47-stream-quality-by-codec-rank/47-03-PLAN.md

<interfaces>
<!-- Phase 47.3 verbatim forward-compat idiom — the analog applied twice for Phase 70. -->

From musicstreamer/settings_export.py (Phase 47.3 baseline, lines 118-130 _station_to_dict):
```
"streams": [
    {
        "url": s.url,
        "label": s.label,
        "quality": s.quality,
        "position": s.position,
        "stream_type": s.stream_type,
        "codec": s.codec,
        "bitrate_kbps": s.bitrate_kbps,
    }
    for s in station.streams
],
```

From musicstreamer/settings_export.py _insert_station (~line 405-420) and _replace_station (~line 461-477):
```
"INSERT INTO station_streams"
"(station_id, url, label, quality, position, stream_type, codec, bitrate_kbps) "
"VALUES (?,?,?,?,?,?,?,?)",
(
    station_id,
    stream.get("url", ""),
    ...
    int(stream.get("bitrate_kbps", 0) or 0),  # P-2 forward-compat + defense
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _station_to_dict with sample_rate_hz + bit_depth keys + extend _insert_station + _replace_station SQL with two new columns / placeholders / value-tuple entries (forward-compat idiom)</name>
  <files>musicstreamer/settings_export.py</files>
  <read_first>
    musicstreamer/settings_export.py (lines 100-130 — _station_to_dict; lines 390-477 — _insert_station + _replace_station)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DS-04)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 424-466 Pattern 4 verbatim)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 349-411 — settings_export modify section)
    .planning/phases/47-stream-quality-by-codec-rank/47-03-PLAN.md (Phase 47.3 forward-compat idiom precedent)
    tests/test_settings_export.py (Wave 0 RED stubs — read to confirm expected behavior)
  </read_first>
  <behavior>
    - `_station_to_dict`: append two keys to the streams dict-comprehension AFTER `bitrate_kbps`:
      - `"sample_rate_hz": s.sample_rate_hz`
      - `"bit_depth": s.bit_depth`
    - `_insert_station`: extend the INSERT SQL column list to include `sample_rate_hz, bit_depth` after `bitrate_kbps`; extend the VALUES placeholder count by `,?,?`; extend the value tuple by:
      - `int(stream.get("sample_rate_hz", 0) or 0),  # Phase 70 forward-compat`
      - `int(stream.get("bit_depth", 0) or 0),  # Phase 70 forward-compat`
    - `_replace_station`: mirror the same three extensions verbatim (column list + placeholders + value tuple). The Phase 47.3 idiom `int(x or 0)` neutralizes missing key + None + empty string + malformed value in one expression.
    - No other functions or invariants changed.
  </behavior>
  <action>
    1) Find `_station_to_dict` at settings_export.py:~108-130. In the streams dict-comprehension, append two new keys AFTER `"bitrate_kbps": s.bitrate_kbps,`:
       - `"sample_rate_hz": s.sample_rate_hz,`
       - `"bit_depth": s.bit_depth,`

    2) Find `_insert_station` at settings_export.py:~390-420. In the INSERT SQL string, extend the column list to read `(station_id, url, label, quality, position, stream_type, codec, bitrate_kbps, sample_rate_hz, bit_depth)` and the VALUES placeholder string to `(?,?,?,?,?,?,?,?,?,?)`. In the value tuple, after `int(stream.get("bitrate_kbps", 0) or 0),`, append:
       - `int(stream.get("sample_rate_hz", 0) or 0),`
       - `int(stream.get("bit_depth", 0) or 0),`

    3) Find `_replace_station` at settings_export.py:~461-477. Apply the SAME three extensions verbatim.

    4) Per planner critical-rules: NO fenced code blocks in <action>. Source the verbatim extension from RESEARCH Pattern 4 (lines 446-462) and PATTERNS.md lines 391-411.

    5) Do NOT alter unrelated import logic, ZIP-validation logic, mode-handling logic, or schema-version semantics. The forward-compat idiom is the entire change — two added keys in the to_dict, two added columns + placeholders + value-tuple entries in EACH of the two SQL functions.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_settings_export.py -k "sample_rate_hz or bit_depth or quality_keys" -x</automated>
  </verify>
  <acceptance_criteria>
    - `_station_to_dict` output for a station with sample_rate_hz=96000, bit_depth=24 contains those two keys in the per-stream dict.
    - `_insert_station` INSERT SQL column list contains "sample_rate_hz, bit_depth"; placeholders count is 10 question marks.
    - `_replace_station` INSERT SQL identical extension.
    - Round-trip test: seed sample_rate_hz=96000, bit_depth=24 → export ZIP → re-import into fresh repo → repo.list_streams returns those values. Wave 0 RED stub test_export_import_roundtrip_preserves_sample_rate_hz_and_bit_depth turns GREEN.
    - Forward-compat test: ZIP payload omits both keys → import succeeds with both columns defaulting to 0. Wave 0 RED stub test_commit_import_forward_compat_missing_quality_keys turns GREEN.
    - test_station_to_dict_emits_quality_keys GREEN.
    - No regression: `uv run --with pytest pytest tests/test_settings_export.py -x` exits 0 (all existing tests + new Wave 0 stubs GREEN).
    - Grep gate: `grep -v '^[[:space:]]*#' musicstreamer/settings_export.py | grep -c 'sample_rate_hz'` ≥ 4 (1 in _station_to_dict + 1 in each of _insert_station / _replace_station SQL column lists + 1 in each of their value-tuple `int(stream.get(...))` lines = 5 mentions total minimum; conservative gate at 4).
    - Same for bit_depth: ≥ 4 mentions.
    - SQL parameterization preserved: no string interpolation introduced.
  </acceptance_criteria>
  <done>_station_to_dict carries sample_rate_hz + bit_depth keys; _insert_station + _replace_station SQL extended with two columns + placeholders + forward-compat-idiom value-tuple entries; all Wave 0 settings-export RED stubs GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZIP payload → _insert_station / _replace_station | Untrusted ZIP content; values flow through `int(... or 0)` defensive coercion before parameterized SQL binding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-28 | Tampering | ZIP payload with malformed sample_rate_hz / bit_depth | mitigate | int(stream.get("KEY", 0) or 0) neutralizes missing key + None + empty string + malformed string + boolean False; final int() coerces to numeric. SQL is parameterized. |
| T-70-29 | Tampering | SQL injection via column-list addition | mitigate | Column names are static literals in the source; only values are bound via `?` placeholders. |
| T-70-30 | Information disclosure | Audio metadata in ZIPs | accept | ZIP file is user-driven export; sample_rate_hz / bit_depth is non-sensitive audio metadata. |

ASVS L1 mapping: V5 Input Validation — defensive int-coercion idiom + parameterized SQL.
</threat_model>

<verification>
- `uv run --with pytest pytest tests/test_settings_export.py -x` exits 0.
- Cross-OS contract: pre-Phase-70 ZIPs missing both keys still import cleanly (forward-compat lock).
- Round-trip preservation: hi-res cache survives Linux → ZIP → Windows → import cycle (per Phase 42 Linux↔Windows UAT shape).
- Grep gates as enumerated under acceptance_criteria hold.
</verification>

<success_criteria>
1. _station_to_dict emits sample_rate_hz + bit_depth keys.
2. _insert_station + _replace_station SQL extended with two columns + placeholders + value-tuple entries.
3. Phase 47.3 forward-compat idiom `int(x or 0)` applied to BOTH new value-tuple entries in BOTH functions.
4. All Wave 0 settings-export RED stubs GREEN.
5. No regression in existing settings_export tests.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-10-SUMMARY.md`
</output>
