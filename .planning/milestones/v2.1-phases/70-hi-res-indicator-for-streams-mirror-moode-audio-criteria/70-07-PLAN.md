---
phase: 70
plan: 07
type: execute
wave: 3
depends_on: [70-01, 70-02]
files_modified:
  - musicstreamer/ui_qt/station_star_delegate.py
autonomous: true
requirements: [HRES-01]
tags: [tree-row, delegate, paint, qpainter, sizehint, tdd-green]

must_haves:
  truths:
    - "station_star_delegate.paint() renders an additional pill BEFORE the star icon, only for station rows (provider rows skipped) — UI-SPEC §Component Inventory item 2"
    - "Pill geometry: text via QFontMetrics(option.font).horizontalAdvance + horizontal padding 6px each side + vertical padding 4px each side + corner radius 8px + pill-to-star gap 8px"
    - "Pill text from TIER_LABEL_BADGE — 'HI-RES' or 'LOSSLESS'; tier from best_tier_for_station(station)"
    - "Selection-state color swap: when QStyle.State_Selected, pill fill = palette(HighlightedText), text = palette(Highlight); unselected, pill fill = palette(Highlight), text = palette(HighlightedText). UI-SPEC §Color OD-3 lock."
    - "Antialiasing ON, Qt.NoPen border, QPainter.drawRoundedRect + drawText with Qt.AlignCenter"
    - "sizeHint grows by pill_w + pill_to_star_gap for station rows; provider rows + height unchanged"
    - "No QSS on delegate (QSS does not flow into QPainter); pill rendered via QPainter primitives only"
    - "All Wave 0 tests/test_station_star_delegate.py for paint + sizeHint turn GREEN"
  artifacts:
    - path: "musicstreamer/ui_qt/station_star_delegate.py"
      provides: "paint() + sizeHint() extension rendering tier pill before star"
      contains: "best_tier_for_station"
  key_links:
    - from: "station_star_delegate.paint"
      to: "musicstreamer/hi_res.py best_tier_for_station + TIER_LABEL_BADGE"
      via: "tier = best_tier_for_station(station); label = TIER_LABEL_BADGE[tier]"
      pattern: "best_tier_for_station"
---

<objective>
Wave 3 GREEN — paint a tier pill BEFORE the star icon in the station-tree row. Tier derived from `best_tier_for_station(station)` (Hi-Res > Lossless > nothing across station.streams). Single combined delegate (extend `station_star_delegate.py`) per RESEARCH.md recommendation + UI-SPEC Component Inventory item 2.

Purpose: DP-01 surface 2 (tree row badge) + DP-02 (best-tier-across-streams) + DP-03 (right side, before star). The pill is a curation hint at library-browsing time; it surfaces capability ("this station CAN deliver Hi-Res").

Output: tests/test_station_star_delegate.py Wave 0 RED stubs turn GREEN — paint emits "HI-RES" / "LOSSLESS" text for the right station types, omits pill for lossy stations, omits pill for provider rows; sizeHint grows for station rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-01-SUMMARY.md
@.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-02-SUMMARY.md
@musicstreamer/ui_qt/station_star_delegate.py

<interfaces>
<!-- Phase 54 BUG-05 multi-pixmap-painting delegate — the analog we extend. -->

From musicstreamer/ui_qt/station_star_delegate.py (lines 23-32 — geometry constants; lines 55-99 — paint + sizeHint):
```
_STAR_SIZE = 20
_STAR_MARGIN = 4
_PROVIDER_TREE_MIN_ROW_HEIGHT = 32

def _star_rect(row_rect): ...

class StationStarDelegate(QStyledItemDelegate):
    def paint(self, painter, option, index): ...    # current Phase 54 body
    def sizeHint(self, option, index): ...           # current geometry hint
```

From musicstreamer/hi_res.py (Plan 70-01):
- best_tier_for_station(station) -> str   # Hi-Res > Lossless > "" across station.streams
- TIER_LABEL_BADGE: dict[str, str] = {"hires": "HI-RES", "lossless": "LOSSLESS", "": ""}

UI-SPEC pill geometry constants (Phase 70):
- _PILL_PADDING_X = 6
- _PILL_PADDING_Y = 4
- _PILL_TO_STAR_GAP = 8
- _PILL_RADIUS = 8
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend paint() to render tier pill BEFORE star + extend sizeHint() to reserve width for pill</name>
  <files>musicstreamer/ui_qt/station_star_delegate.py</files>
  <read_first>
    musicstreamer/ui_qt/station_star_delegate.py (lines 1-113 — full file)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-UI-SPEC.md (lines 96-152 — Color OD-3 selection-state swap, delegate paint setup; lines 231-247 — Component Inventory item 2 pill geometry; lines 305-316 — Layout Decisions)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-CONTEXT.md (DP-02, DP-03, DP-07)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-PATTERNS.md (lines 506-600 — station_star_delegate paint + sizeHint extension pattern verbatim)
    .planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-RESEARCH.md (lines 525-590 Pattern 6 — tree delegate)
    musicstreamer/hi_res.py (Plan 70-01 — best_tier_for_station + TIER_LABEL_BADGE)
  </read_first>
  <behavior>
    - Add four module-level geometry constants near existing _STAR_SIZE / _STAR_MARGIN: _PILL_PADDING_X = 6, _PILL_PADDING_Y = 4, _PILL_TO_STAR_GAP = 8, _PILL_RADIUS = 8 (UI-SPEC §Spacing Scale lock).
    - Add `_pill_rect(row_rect: QRect, pill_width: int, pill_height: int) -> QRect` helper: pill is right-anchored, immediately LEFT of the star with _PILL_TO_STAR_GAP between pill right edge and star left edge. Vertically centered in the row.
    - In `paint(self, painter, option, index)`, AFTER `super().paint(painter, option, index)` and BEFORE the existing star icon paint (around station_star_delegate.py:55-79):
      1. Read `station = index.data(Qt.UserRole)`. If not a Station instance (provider row), return early after super().paint — no pill, no star (provider-row safety; matches existing behavior).
      2. Compute `tier = best_tier_for_station(station)` (import from musicstreamer.hi_res).
      3. If tier is non-empty:
         - Build bold font from option.font; compute text_w + pill_w + pill_h via QFontMetrics.
         - Compute pill_rect via the _pill_rect helper.
         - Selection-state color swap per UI-SPEC OD-3:
           - `if option.state & QStyle.State_Selected: pill_fill = option.palette.color(QPalette.HighlightedText); pill_text = option.palette.color(QPalette.Highlight)` ELSE swap (default: pill_fill = Highlight, pill_text = HighlightedText).
         - painter.save() → setRenderHint(Antialiasing) → setPen(Qt.NoPen) → setBrush(pill_fill) → drawRoundedRect(pill_rect, _PILL_RADIUS, _PILL_RADIUS) → setFont(bold_font) → setPen(pill_text) → drawText(pill_rect, Qt.AlignCenter, TIER_LABEL_BADGE[tier]) → painter.restore().
      4. After the pill paint (or skip if no tier), proceed with the existing star icon paint at the existing _star_rect.
    - In `sizeHint(self, option, index)`, grow station-row width by `pill_w_worst_case + _PILL_TO_STAR_GAP`. "LOSSLESS" is the longer label (~78 px at 9pt bold per UI-SPEC §Spacing Scale sample math). Reserve a fixed worst-case 80 px so sizeHint is deterministic (does NOT depend on QFontMetrics — the option.font may not be available reliably at sizeHint time on every platform; using a constant matches existing _STAR_SIZE+_STAR_MARGIN idiom). Provider rows unchanged. Height unchanged (32 px floor already exceeds pill ~22 px).
  </behavior>
  <action>
    1) Add `from musicstreamer.hi_res import best_tier_for_station, TIER_LABEL_BADGE` to module imports.

    2) Add the four geometry constants near the existing `_STAR_SIZE = 20` / `_STAR_MARGIN = 4` constants block (top of file).

    3) Add the `_pill_rect(row_rect, pill_width, pill_height)` helper adjacent to the existing `_star_rect` helper. Geometry per UI-SPEC OD-1: right-anchored, immediately LEFT of star, vertically centered.

    4) Extend `paint()`: after `super().paint(painter, option, index)`, before the existing star paint, insert the pill-paint block from PATTERNS.md lines 558-585 verbatim. Apply UI-SPEC OD-3 selection-state color swap from UI-SPEC lines 102-114. Use QPainter primitives ONLY (no QSS — UI-SPEC §Color "Delegate paint setup" line 141 lock).

    5) Extend `sizeHint()`: for station rows (existing branch where `isinstance(station, Station)`), grow returned QSize.width() by `80 + _PILL_TO_STAR_GAP` (the worst-case "LOSSLESS" pill width + the gap). Match the existing _STAR_SIZE + _STAR_MARGIN add idiom.

    6) Provider rows: explicit early-return after super().paint() so no pill is painted. Mirrors existing `# provider row -- no star` logic at line 71-72.

    7) Antialiasing MUST be enabled for the rounded-rect to render crisply: `painter.setRenderHint(QPainter.Antialiasing, True)` per UI-SPEC §Color Delegate paint setup.

    8) Per planner critical-rules: NO fenced code blocks in <action>. Source the verbatim paint body from PATTERNS.md lines 555-590 + UI-SPEC lines 122-140.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_station_star_delegate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_station_star_delegate.py Wave 0 RED stubs all turn GREEN: test_paints_hires_pill_for_hires_station, test_paints_lossless_pill_for_cd_flac_station, test_no_pill_for_lossy_station, test_no_pill_for_provider_row, test_sizehint_grows_for_pill.
    - Provider rows do NOT trigger pill paint (early-return after super().paint() when index.data(Qt.UserRole) is not a Station).
    - sizeHint for station rows returns a width ≥ pre-Phase-70 width + 80 + _PILL_TO_STAR_GAP.
    - Grep gate (no QSS in delegate paint): `grep -v '^[[:space:]]*#' musicstreamer/ui_qt/station_star_delegate.py | grep -c 'setStyleSheet'` == 0 (delegate paints via QPainter primitives only).
    - Grep gate (selection-state swap): `grep -v '^[[:space:]]*#' musicstreamer/ui_qt/station_star_delegate.py | grep -c 'QStyle.State_Selected'` ≥ 1.
    - Grep gate (antialiasing): `grep -v '^[[:space:]]*#' musicstreamer/ui_qt/station_star_delegate.py | grep -c 'QPainter.Antialiasing'` ≥ 1.
    - Grep gate (best_tier_for_station): `grep -c 'from musicstreamer.hi_res import' musicstreamer/ui_qt/station_star_delegate.py` == 1.
    - Geometry constants defined at module scope: `grep -c '^_PILL_PADDING_X = 6\|^_PILL_PADDING_Y = 4\|^_PILL_TO_STAR_GAP = 8\|^_PILL_RADIUS = 8' musicstreamer/ui_qt/station_star_delegate.py` == 4.
    - No regression: `uv run --with pytest --with pytest-qt pytest tests/test_station_list_panel.py tests/test_now_playing_panel.py -x` exits 0 (delegate is consumed by station-list panel + tree views).
  </acceptance_criteria>
  <done>paint() renders tier pill before star with selection-state-aware color swap; sizeHint reserves worst-case pill width; provider rows safe; no QSS on delegate; all Wave 0 delegate RED tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| best_tier_for_station return → TIER_LABEL_BADGE lookup | Closed-enum 3-key dict; KeyError-loud on injection (closed-allowlist invariant). |
| painter.drawText with tier label | Plain-text Qt drawing primitive; no markup interpretation; safe by Qt design. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-20 | Tampering | Pill text from best_tier_for_station | mitigate | Closed-enum lookup via TIER_LABEL_BADGE; KeyError-loud on out-of-enum values. |
| T-70-21 | Spoofing | Pill on selected row could be invisible if no contrast swap | mitigate | UI-SPEC OD-3 selection-state color swap (pill fill ↔ text colors); both colors come from the same theme-managed palette pair. |
| T-70-22 | Information disclosure | Pill exposes station's hi-res capability | accept | Same data is on the now-playing badge + EditStationDialog; the pill is a curation hint. |

ASVS L1 mapping: V5 Input Validation — closed-enum tier lookup; QPainter.drawText is plain-text by design.
</threat_model>

<verification>
- `uv run --with pytest --with pytest-qt pytest tests/test_station_star_delegate.py -x` exits 0.
- `uv run --with pytest --with pytest-qt pytest tests/test_station_list_panel.py tests/test_now_playing_panel.py -x` exits 0 (no consumer regression).
- Grep gates as enumerated under acceptance_criteria all hold.
</verification>

<success_criteria>
1. paint() renders tier pill BEFORE the star icon for station rows; provider rows safe.
2. Selection-state color swap (UI-SPEC OD-3) honored — pill stays visible against selected-row Highlight background.
3. sizeHint reserves worst-case pill width on station rows.
4. Delegate paint uses QPainter primitives only (no setStyleSheet); antialiasing ON.
5. All Wave 0 delegate RED tests GREEN; no regression in tree-view consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/70-hi-res-indicator-for-streams-mirror-moode-audio-criteria/70-07-SUMMARY.md`
</output>
