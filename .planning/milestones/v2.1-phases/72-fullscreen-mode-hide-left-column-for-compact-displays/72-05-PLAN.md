---
phase: 72
plan: 05
type: execute
wave: 4
depends_on:
  - 72-04
files_modified:
  - tests/test_phase72_integration.py
  - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md
autonomous: false
requirements:
  - LAYOUT-01
user_setup: []
must_haves:
  truths:
    - "End-to-end integration test exercises the full compact+peek lifecycle: launch → toggle → peek → click station → close peek → toggle off → repeat"
    - "Manual UAT script is on disk and walks the user through visual / Wayland-specific verifications that automated tests cannot cover"
    - "User has confirmed visually that (a) icon flip is intelligible, (b) hover-peek timing feels right, (c) bottom-bar overlap is resolved on the small/secondary display, (d) Wayland behavior is clean, (e) ToastOverlay z-order is correct"
  artifacts:
    - path: "tests/test_phase72_integration.py"
      provides: "End-to-end test combining toggle, shortcut, peek, station-click, multi-cycle"
      contains: "def test_full_compact_peek_lifecycle"
    - path: ".planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md"
      provides: "Manual UAT script per VALIDATION.md §Manual-Only Verifications"
      contains: "Wayland"
  key_links:
    - from: "tests/test_phase72_integration.py"
      to: "all 4 prior Phase 72 test files + production code"
      via: "MainWindow + qtbot end-to-end exercise"
      pattern: "MainWindow"
---

<objective>
Final wave: end-to-end integration test exercising the full compact+peek lifecycle, plus
a Manual UAT script for the visual / Wayland-specific verifications that automated tests
cannot cover. Includes a checkpoint pause for the user to run UAT.

Purpose: VALIDATION.md §Manual-Only Verifications lists 5 manual-only checks (icon flip
intelligibility, hover-peek feel, overlay z-order vs toasts, bottom-bar overlap fix on
small display, Wayland GNOME Shell behavior). This plan captures those as a UAT script
and gates the phase on the user signing off.

Output: One end-to-end automated test, one UAT script document, one user-action checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-CONTEXT.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-RESEARCH.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-VALIDATION.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-03-SUMMARY.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-04-SUMMARY.md

<interfaces>
<!-- All test fixtures + production code now shipped by Plans 01–04 -->

```python
# Available to integration test:
from PySide6.QtCore import Qt, QEvent, QPoint
from PySide6.QtGui import QMouseEvent
from musicstreamer.ui_qt.main_window import MainWindow
from musicstreamer.ui_qt.station_list_peek_overlay import StationListPeekOverlay

# From Plan 03:
mw._compact_shortcut          # QShortcut(Ctrl+B)
mw._on_compact_toggle         # central slot
mw._splitter_sizes_before_compact

# From Plan 04:
mw._peek_overlay              # StationListPeekOverlay | None (lazy)
mw._peek_dwell_timer          # QTimer | None (lazy)
mw.eventFilter(obj, event)    # MouseMove dispatch

# From Plan 02:
mw.now_playing.compact_mode_toggle_btn
mw.now_playing.compact_mode_toggled  # Signal(bool)
mw.now_playing.set_compact_button_icon(checked)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration test exercising full compact+peek lifecycle</name>
  <files>tests/test_phase72_integration.py</files>
  <read_first>
    - tests/test_phase72_compact_toggle.py (test patterns + fixture imports)
    - tests/test_phase72_peek_overlay.py (peek-test patterns + mouse simulation idioms)
    - tests/test_main_window_integration.py (FakeRepo + window fixture)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md (§Interaction Contract — full sequence diagram)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-VALIDATION.md (§Per-Task Verification Map — dimensions list)
  </read_first>
  <action>
    Create tests/test_phase72_integration.py with three end-to-end tests:

    test_full_compact_peek_lifecycle(qtbot, fake_player, fake_repo): step-by-step:
    (1) Construct MainWindow at default size [1200, 800]; assert initial state (button unchecked, panel visible, peek None).
    (2) Set splitter sizes explicitly [400, 800].
    (3) Click compact button → assert station_panel hidden, splitter snapshot captured [400, 800], button icon flipped, tooltip = "Show stations (Ctrl+B)".
    (4) Synthesize MouseMove at QPoint(2, 100) on centralWidget; qtbot.wait(300) → assert peek overlay shown.
    (5) Synthesize click on a station inside the peeked panel → assert station_activated signal fired AND peek still visible.
    (6) Synthesize QEvent.Leave on the peek overlay → assert peek hidden, station_panel reparented to splitter index 0 and hidden (compact still active).
    (7) Repeat MouseMove at (2, 100) + wait → peek re-opens (proves lifecycle is repeatable).
    (8) Toggle compact OFF via Ctrl+B (mw._compact_shortcut.activated.emit()) → assert peek closed, station_panel visible, splitter sizes ≈ [400, 800] (round-trip).
    (9) Final assertions: button unchecked, panel visible, peek hidden, _splitter_sizes_before_compact == None (Pitfall 5).

    test_multiple_toggle_cycles_preserve_sizes(qtbot, fake_player, fake_repo): toggle ON-OFF-ON-OFF-ON-OFF (3 full cycles) starting from sizes [350, 850]; after each OFF, assert sizes ≈ [350, 850].

    test_no_compact_setting_written_after_full_lifecycle(qtbot, fake_player, fake_repo): run a full lifecycle as in test 1; after completion, assert no key containing "compact" appears in fake_repo._settings (final D-09 lock at integration level).
  </action>
  <verify>
    <automated>pytest tests/test_phase72_integration.py tests/test_phase72_*.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new end-to-end tests pass GREEN.
    - All prior Phase 72 tests still pass (no regression).
    - Full test suite: `pytest tests -x` exits 0.
    - Source assertion: `grep -c "def test_full_compact_peek_lifecycle" tests/test_phase72_integration.py` returns 1.
    - Source assertion: `grep -c "_splitter_sizes_before_compact" tests/test_phase72_integration.py` returns ≥ 2 (snapshot verified at multiple steps).
    - Lambda gate: `grep -c "lambda" tests/test_phase72_integration.py` returns 0.
  </acceptance_criteria>
  <done>
    End-to-end test green; the full compact+peek+restore lifecycle is regression-locked at integration level.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Manual UAT script per VALIDATION.md §Manual-Only Verifications</name>
  <files>.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md</files>
  <read_first>
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-VALIDATION.md (§Manual-Only Verifications — 5 items)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md (§Interaction Contract — exact expected behaviors for each visual check)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-CONTEXT.md (§domain — bottom-bar overlap is the actual bug)
  </read_first>
  <action>
    Create .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md with five numbered tests, each in the form:

    ```
    ## UAT-NN: <title>

    **Maps to:** VALIDATION.md §Manual-Only Verifications row N + UI-SPEC §<section>
    **Requirement covered:** <D-NN ID>

    ### Steps
    1. <step>
    2. <step>
    ...

    ### Expected
    - <observable outcome>

    ### Pass criteria
    [ ] <observable check>

    ### Notes / observations
    (leave blank for user)
    ```

    The five UAT items:
    - UAT-01: Icon flip visual correctness (D-05). Steps: launch app on Wayland; observe initial icon on compact-toggle button + tooltip; press Ctrl+B; observe icon changes + tooltip changes. Pass if both icon and tooltip clearly communicate next action.
    - UAT-02: Hover-peek feel/timing (D-13). Steps: enter compact mode; deliberately move cursor to left edge of window; observe whether peek opens "after a noticeable but not laggy delay"; deliberately drag cursor rapidly past the left edge (without dwelling) — confirm peek does NOT open. Pass if 280ms feels natural and zone-exit cancels the dwell.
    - UAT-03: Overlay z-order vs ToastOverlay. Steps: enter compact mode; trigger a toast (e.g., star a track or activate any toast-producing action); while toast is visible, hover-peek. Confirm toast renders ABOVE peek (toast > peek > now-playing). Pass if no interaction trap (no toast hidden behind peek; no peek hidden behind toast in a way that blocks input).
    - UAT-04: Bottom-bar overlap fix verified (phase goal — the actual bug). Steps: drag window onto secondary/small display; resize to ~560–700px wide; observe bottom-bar control overlap; toggle compact ON; confirm controls now have room and are all fully clickable. Pass if overlap is resolved at narrow widths.
    - UAT-05: Wayland on GNOME Shell behavior. Steps: confirm running under Wayland (`echo $XDG_SESSION_TYPE` → wayland); exercise full lifecycle (toggle ON, hover-peek, click station, mouse-leave, toggle OFF). Confirm no Wayland-specific glitch (no flicker, no compositor freeze, no DPR=1.0 anomaly). Pass if behavior matches X11 expectation under Wayland.

    Each UAT row has a [ ] pass checkbox the user ticks when running. Top of file: instructions on how to run (`python -m musicstreamer` or the project's standard launch command — verify with `grep -r "if __name__" musicstreamer/__main__.py` or similar). Bottom of file: a pass/fail summary block with the exact line `**Overall:** PASS` (or `**Overall:** FAIL`) that the Task 3 checkpoint verify-grep will detect. Initially write the line with `**Overall:** PENDING` so the file shipped by this task does not falsely satisfy the Task 3 gate before the user has actually run UAT.
  </action>
  <verify>
    <automated>test -f .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md && grep -c "^## UAT-" .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md</automated>
  </verify>
  <acceptance_criteria>
    - File 72-UAT-SCRIPT.md exists at the phase directory.
    - Contains 5 numbered UAT items (`grep -c "^## UAT-" 72-UAT-SCRIPT.md` returns 5).
    - Each UAT item has a Pass criteria checkbox (`grep -c "^\[ \]" 72-UAT-SCRIPT.md` returns ≥ 5).
    - File references Wayland explicitly (per memory: deployment target is Wayland, never X11).
    - Negative gate: `grep -ci "X11" 72-UAT-SCRIPT.md` returns 0 (no X11 references — phase memory + RESEARCH §Phase Requirements note).
  </acceptance_criteria>
  <done>
    UAT script ready for the user to execute. Five numbered tests cover all five VALIDATION.md manual-only items.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User executes 72-UAT-SCRIPT.md and signs off</name>
  <files>.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md (user-edited)</files>
  <action>Pause for the user to run the UAT script created in Task 2 against a live Wayland session of the app. Do not proceed until the user explicitly resumes via the resume-signal text.</action>
  <verify>
    <automated>grep -E "^\*\*Overall:\*\* PASS" .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md</automated>
  </verify>
  <done>User has marked the UAT script as overall PASS (or has filed specific failures requiring a follow-up gap-closure plan).</done>
  <what-built>
    All compact-mode + hover-peek functionality from Plans 02-04. The end-to-end automated test (Task 1) and UAT script (Task 2) are now in place. The user must now run the app under Wayland and execute the five UAT items.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md` in your editor.
    2. Launch MusicStreamer normally (whatever your usual launch command is — typically `python -m musicstreamer` or your packaged launch).
    3. Execute UAT-01 through UAT-05 in order. Tick the `[ ]` checkbox next to each Pass criteria as you confirm it.
    4. If UAT-04 (bottom-bar overlap) requires the small/secondary display, physically move the window there to verify.
    5. Take notes in the "Notes / observations" sections if anything feels off (timing, visual quirks, animation desire, etc.) — these go into Phase 72 deferred polish if you want them.
    6. At the bottom of the file, mark overall PASS or FAIL.

    If FAIL — note which UAT item(s) failed and why, then return here with the issues so we can spin a Phase 72.1 gap-closure plan.

    If PASS — type "approved" or describe any nice-to-haves you spotted (those go to deferred polish; not a blocker).
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 UAT items pass, OR describe specific failures so a gap-closure plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Same as Plans 01–04 — UI-state-only. UAT runs against the user's real Wayland session but introduces no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-07 | (n/a) | UAT execution | accept | Manual test under user's Wayland session — no new code, no new I/O. |
</threat_model>

<verification>
After all three tasks complete:
- `pytest tests/test_phase72_integration.py -x -v` exits 0
- `pytest tests -x` exits 0 (full suite green)
- `.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UAT-SCRIPT.md` exists with 5 UAT items
- User has signed off on UAT script (Task 3 checkpoint cleared)
</verification>

<success_criteria>
- End-to-end test green (full compact+peek lifecycle locked)
- UAT script on disk with 5 numbered items, all referencing Wayland explicitly, none referencing X11
- User has run UAT and approved (or filed specific failures for a follow-up gap-closure plan)
</success_criteria>

<output>
After completion, create `.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-05-SUMMARY.md`:
- Final test count for Phase 72 (across all 5 plans)
- UAT result (PASS / FAIL — link to user's marked-up UAT-SCRIPT.md if available)
- Any deferred polish items the user surfaced during UAT (animation, alternate dwell, etc.) — log to .planning/STATE.md Deferred Items if any.
- Confirmation: Phase 72 SC met (compact mode hides left column, bottom-bar overlap resolved on small display, hover-peek behaves per spec, session-only persistence holds).
</output>
