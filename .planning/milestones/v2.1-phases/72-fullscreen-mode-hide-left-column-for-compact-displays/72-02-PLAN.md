---
phase: 72
plan: 02
type: execute
wave: 1
depends_on:
  - 72-01
files_modified:
  - musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg
  - musicstreamer/ui_qt/icons/sidebar-hide-symbolic.svg
  - musicstreamer/ui_qt/icons.qrc
  - musicstreamer/ui_qt/icons_rc.py
  - musicstreamer/ui_qt/now_playing_panel.py
  - tests/test_phase72_now_playing_panel.py
autonomous: true
requirements:
  - LAYOUT-01
must_haves:
  truths:
    - "NowPlayingPanel exposes a QToolButton (compact_mode_toggle_btn) at the far right of the control row immediately before controls.addStretch(1)"
    - "Button is checkable, 28x28 with 20x20 icon, matches star_btn / eq_toggle_btn precedent"
    - "Button click emits compact_mode_toggled(bool) signal — bound-method connect, no lambda"
    - "Button icon and tooltip flip between sidebar-show-symbolic + 'Show stations (Ctrl+B)' and sidebar-hide-symbolic + 'Hide stations (Ctrl+B)' per checked state"
    - "Two new sidebar SVGs (16x16 viewBox, fill='currentColor') are registered in icons.qrc and bundled via icons_rc.py"
    - "No repo.set_setting call introduced for compact-mode key inside NowPlayingPanel (D-09 session-only)"
    - "D-01: Compact toggle activation surface is a QToolButton on NowPlayingPanel (NOT a hamburger menu entry) — covered by compact_mode_toggle_btn presence + addAction grep gate."
    - "D-04: Button location is the far-right of the control row, AFTER volume_slider and BEFORE controls.addStretch(1) per UI-SPEC line 514 — covered by source-order assertion at now_playing_panel.py line ~514."
    - "D-05: Icon flips between sidebar-show-symbolic.svg (compact ON) and sidebar-hide-symbolic.svg (compact OFF), with matching tooltip text — covered by test_compact_button_icon_flips_per_state."
  artifacts:
    - path: "musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg"
      provides: "SVG glyph for compact-mode-checked state (left panel hidden — about to show)"
      contains: "fill=\"currentColor\""
    - path: "musicstreamer/ui_qt/icons/sidebar-hide-symbolic.svg"
      provides: "SVG glyph for compact-mode-unchecked state (left panel visible — about to hide)"
      contains: "fill=\"currentColor\""
    - path: "musicstreamer/ui_qt/icons.qrc"
      provides: "Resource manifest with new aliases sidebar-show-symbolic.svg and sidebar-hide-symbolic.svg"
      contains: "sidebar-show-symbolic.svg"
    - path: "musicstreamer/ui_qt/icons_rc.py"
      provides: "Regenerated pyside6-rcc bundle containing the two new SVG resources"
      contains: "sidebar"
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "compact_mode_toggle_btn QToolButton + compact_mode_toggled Signal(bool) + set_compact_button_icon(checked) helper"
      contains: "compact_mode_toggled = Signal(bool)"
    - path: "tests/test_phase72_now_playing_panel.py"
      provides: "Unit tests: button presence at correct control-row position, icon-flip per state, signal emission"
      contains: "def test_compact_button_icon_flips_per_state"
  key_links:
    - from: "musicstreamer/ui_qt/now_playing_panel.py compact_mode_toggle_btn.toggled"
      to: "self._on_compact_btn_toggled bound method"
      via: ".connect(self._on_compact_btn_toggled)"
      pattern: "toggled\\.connect\\(self\\._on_compact_btn_toggled\\)"
    - from: "self._on_compact_btn_toggled(checked)"
      to: "self.compact_mode_toggled.emit(checked)"
      via: "signal re-emit (QA-05 bound method, no lambda)"
      pattern: "compact_mode_toggled\\.emit"
---

<objective>
Build the icon assets, signal, and button on NowPlayingPanel. This plan is gated by
Plan 72-01 (Wave 0 spike tests for A1/A2) — the spike must pass before any production
code lands so a reparent-safety regression cannot waste this plan's button work.
Plan 03 will then depend on this plan's compact_mode_toggle_btn + compact_mode_toggled signal.

Purpose: D-01 / D-04 / D-05 / UI-SPEC Interaction Contract — surface the compact-mode
toggle on the always-visible right pane (now-playing). NO hamburger menu entry (D-01).
The button is the single source of truth for compact state (Pattern 5 / QA-05).

Output: Two new SVG icons, an updated icons.qrc with two new aliases, a regenerated
icons_rc.py, a modified now_playing_panel.py with a new QToolButton + Signal(bool) +
icon-flip helper, and unit tests pinning button placement, icon flip, and signal emission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-CONTEXT.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-RESEARCH.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-PATTERNS.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md
@.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing project conventions the executor consumes directly -->

icon family conventions (from grepped SVGs — authoritative over UI-SPEC's 24x24 mention):
- viewBox: 0 0 16 16
- width="16" height="16"
- single <path d="..." fill="currentColor"/>
- monochrome symbolic style (no inline <style>, no fill-opacity if currentColor used)

Existing checkable QToolButton precedent from now_playing_panel.py (lines 479-507):

```python
# star_btn:
self.star_btn = QToolButton(self)
self.star_btn.setIconSize(QSize(20, 20))
self.star_btn.setFixedSize(28, 28)
self.star_btn.setCheckable(True)
self.star_btn.setEnabled(False)
self.star_btn.setIcon(QIcon.fromTheme("non-starred-symbolic", QIcon(":/icons/non-starred-symbolic.svg")))
self.star_btn.clicked.connect(self._on_star_clicked)
controls.addWidget(self.star_btn)

# eq_toggle_btn: identical shape — 28x28 / 20x20 / setCheckable(True) / setToolTip / clicked.connect.
```

Insertion point (verified by Read of now_playing_panel.py:509-515):
- Line 513: `controls.addWidget(self.volume_slider)`
- Line 515: `controls.addStretch(1)`
- NEW: insert `controls.addWidget(self.compact_mode_toggle_btn)` AT LINE 514 (between volume_slider and addStretch).

Existing outbound Signal declarations on NowPlayingPanel class scope (lines 224-269):

```python
track_starred = Signal(str, str, str, bool)
edit_requested = Signal(object)
stopped_by_user = Signal()
sibling_activated = Signal(object)
similar_activated = Signal(object)
# Phase 72 NEW: compact_mode_toggled = Signal(bool)
```

icons.qrc current entries (lines 4-15):
```xml
<file alias="open-menu-symbolic.svg">icons/open-menu-symbolic.svg</file>
<file alias="audio-x-generic-symbolic.svg">icons/audio-x-generic-symbolic.svg</file>
<file alias="media-playback-start-symbolic.svg">icons/media-playback-start-symbolic.svg</file>
... etc ...
<file alias="multimedia-equalizer-symbolic.svg">icons/multimedia-equalizer-symbolic.svg</file>
```

document-edit-symbolic.svg (full file — exact template):
```xml
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16">
  <path d="..." fill="currentColor"/>
</svg>
```
</interfaces>

<planner_decisions>
## Decisions resolved here (per planning_context guidance)

1. **Icon viewBox = 16x16** (NOT 24x24 from UI-SPEC §File Inventory). Matches the existing 12-icon family. UI-SPEC §Spacing scales QIcon to 20x20 regardless of viewBox, so visual parity holds. Task 1 acceptance criterion locks this with a grep gate.

2. **SVG fill = `currentColor`** (not hardcoded hex). Matches document-edit-symbolic.svg modern convention. Lets palette-driven theming (Phase 66) color the icon automatically.

3. **Button insertion point** = AFTER `controls.addWidget(self.volume_slider)` at line 513 and BEFORE `controls.addStretch(1)` at line 515. Per UI-SPEC §Interaction Contract (authoritative over CONTEXT D-04's stale pre-EQ/pre-volume description).

4. **Bound-method connect** (QA-05). No lambdas anywhere in this plan.
</planner_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create two sidebar SVG icons + register in icons.qrc + regenerate icons_rc.py</name>
  <files>musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg, musicstreamer/ui_qt/icons/sidebar-hide-symbolic.svg, musicstreamer/ui_qt/icons.qrc, musicstreamer/ui_qt/icons_rc.py</files>
  <read_first>
    - musicstreamer/ui_qt/icons/document-edit-symbolic.svg (exact template — 16x16 viewBox + currentColor)
    - musicstreamer/ui_qt/icons/non-starred-symbolic.svg (alt template — confirm 16x16 family)
    - musicstreamer/ui_qt/icons.qrc (full file — alias registration pattern)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md (§Interaction Contract §Glyph design — chevron-on-panel shape spec)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-PATTERNS.md (§4 icons section — 16x16 vs 24x24 reconciliation)
  </read_first>
  <action>
    Create musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg with `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16">` wrapper and a single `<path>` element with `fill="currentColor"`. Glyph: a rectangle outline on the left side of the canvas (representing the hidden left panel) plus a right-pointing chevron/arrow indicating "reveal left panel." Visual mass parity with document-edit-symbolic and multimedia-equalizer-symbolic (similar stroke weight, similar canvas occupancy). Hand-craft the path data or compose via two paths in a single <path d="..."/> if needed.

    Create musicstreamer/ui_qt/icons/sidebar-hide-symbolic.svg with the same 16x16 wrapper. Glyph: visible left-panel rectangle plus a LEFT-pointing chevron indicating "hide left panel / collapse."

    Modify musicstreamer/ui_qt/icons.qrc: insert two new `<file alias="…">icons/…</file>` lines inside the existing `<qresource prefix="/icons">` block, before the closing `</qresource>`. Aliases: `sidebar-show-symbolic.svg` and `sidebar-hide-symbolic.svg`. Match the existing alias→path pattern verbatim.

    Regenerate musicstreamer/ui_qt/icons_rc.py via `pyside6-rcc musicstreamer/ui_qt/icons.qrc -o musicstreamer/ui_qt/icons_rc.py` (working directory = repo root). Do NOT hand-edit icons_rc.py. Per Phase 36 / D-24, this is the standard project build step.
  </action>
  <verify>
    <automated>python -c "from musicstreamer.ui_qt import icons_rc; from PySide6.QtCore import QFile; assert QFile(':/icons/sidebar-show-symbolic.svg').exists(); assert QFile(':/icons/sidebar-hide-symbolic.svg').exists()" && grep -c 'sidebar-show-symbolic.svg' musicstreamer/ui_qt/icons.qrc && grep -c 'sidebar-hide-symbolic.svg' musicstreamer/ui_qt/icons.qrc</automated>
  </verify>
  <acceptance_criteria>
    - Both SVG files exist with `viewBox="0 0 16 16"` and `fill="currentColor"`. grep gate: `grep -c 'viewBox="0 0 16 16"' musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg` and same for sidebar-hide-symbolic.svg both return 1.
    - grep gate: `grep -c 'fill="currentColor"' musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg` and same for sidebar-hide-symbolic.svg both return 1.
    - Negative gate: `grep -E "fill=\"#" musicstreamer/ui_qt/icons/sidebar-show-symbolic.svg` returns no matches (no hardcoded hex).
    - icons.qrc contains both new aliases: `grep -c 'sidebar-show-symbolic.svg' musicstreamer/ui_qt/icons.qrc` returns 2 (alias + path), same for sidebar-hide-symbolic.svg.
    - icons_rc.py contains both resources: `grep -c sidebar musicstreamer/ui_qt/icons_rc.py` returns ≥ 2 (one per SVG).
    - Python side-effect import succeeds: `python -c "from musicstreamer.ui_qt import icons_rc"` exits 0.
    - QFile resolution succeeds at runtime: `python -c "from musicstreamer.ui_qt import icons_rc; from PySide6.QtCore import QFile; assert QFile(':/icons/sidebar-show-symbolic.svg').exists()"` exits 0.
  </acceptance_criteria>
  <done>
    Two new SVGs in icons/, registered in icons.qrc with alias entries, bundled into icons_rc.py via pyside6-rcc. Both resolvable at `:/icons/sidebar-{show,hide}-symbolic.svg` runtime paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compact_mode_toggle_btn QToolButton + compact_mode_toggled Signal + set_compact_button_icon helper to NowPlayingPanel</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py, tests/test_phase72_now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (lines 224-269 for Signal declarations site; lines 479-515 for control-row buttons + insertion point at line 514)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-PATTERNS.md (§Pattern Assignments §2 NowPlayingPanel)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-UI-SPEC.md (§Interaction Contract §Compact-toggle button visual states + §Copywriting Contract)
    - .planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-RESEARCH.md (§Pattern 5 single-source-of-truth + §Pitfall 4 lambda ban)
    - tests/test_now_playing_panel.py (lines 1-50 imports + FakePlayer/FakeRepo fixtures; lines 298-306 test_play_pause_icon_toggle as analog)
    - .planning/codebase/CONVENTIONS.md (Qt Patterns section — Signal naming, bound-method connects)
  </read_first>
  <behavior>
    Tests (write FIRST, RED before adding button):
    - test_compact_mode_toggle_button_present_at_far_right(qtbot): construct NowPlayingPanel, assert hasattr(panel, "compact_mode_toggle_btn"), assert it is a QToolButton, assert it appears in the `controls` layout AFTER volume_slider and BEFORE the trailing stretch. Verify position by walking layout.itemAt(i).widget() until volume_slider is found, then asserting the very next non-stretch item is compact_mode_toggle_btn.
    - test_compact_button_size_and_icon_size(qtbot): assert panel.compact_mode_toggle_btn.size() == QSize(28, 28) (after qtbot.addWidget exposes it) or that the fixed-size property is (28, 28); iconSize() == QSize(20, 20).
    - test_compact_button_is_checkable(qtbot): assert panel.compact_mode_toggle_btn.isCheckable() is True; initial isChecked() is False.
    - test_compact_button_initial_tooltip(qtbot): assert panel.compact_mode_toggle_btn.toolTip() == "Hide stations (Ctrl+B)".
    - test_compact_button_icon_flips_per_state(qtbot): call panel.set_compact_button_icon(True), assert toolTip() == "Show stations (Ctrl+B)" and the icon cache key / pixmap differs from initial. Call set_compact_button_icon(False), assert toolTip() reverts.
    - test_compact_mode_toggled_signal_emits_on_click(qtbot): qtbot.waitSignal(panel.compact_mode_toggled) around panel.compact_mode_toggle_btn.toggle(); assert spy receives True. Toggle again, assert spy receives False.
    - test_compact_button_no_repo_setting_write(qtbot): use FakeRepo with empty _settings dict; toggle the button ON and OFF; assert no key containing "compact" is written to fake_repo._settings (D-09 session-only invariant, panel-level half).
    - test_no_lambda_in_compact_connect(): import inspect, get source of NowPlayingPanel; find the line containing `compact_mode_toggle_btn.toggled.connect`; assert "lambda" is not in that line (QA-05 / Pitfall 4 / mirrors test_buffer_percent_bound_method_connect_no_lambda).

    Implementation (after RED tests fail correctly):
    - Add at class scope alongside other outbound signals: `compact_mode_toggled = Signal(bool)` with a docstring comment "# Phase 72 / LAYOUT-01: emitted when compact-mode toggle button is clicked. Payload = new checked state. Session-only — no repo write side-effect.".
    - In __init__ between `controls.addWidget(self.volume_slider)` (line 513) and `controls.addStretch(1)` (line 515), insert the new button block (six lines: construct QToolButton + setIconSize + setFixedSize + setCheckable + setIcon via QIcon.fromTheme fallback chain + setToolTip + toggled.connect + controls.addWidget). Use the same pattern as star_btn / eq_toggle_btn at lines 479-507. Initial icon = sidebar-hide-symbolic (panel currently visible, button says "Hide stations (Ctrl+B)").
    - Add bound-method slot `_on_compact_btn_toggled(self, checked: bool) -> None` that calls `self.compact_mode_toggled.emit(checked)`. Place adjacent to other bound-method slots (e.g., near _on_star_clicked at line ~ 549 — verify exact line via read).
    - Add public method `set_compact_button_icon(self, checked: bool) -> None` that flips the icon and tooltip per UI-SPEC §Interaction Contract — checked True → sidebar-show-symbolic icon + "Show stations (Ctrl+B)" tooltip; checked False → sidebar-hide-symbolic icon + "Hide stations (Ctrl+B)" tooltip. MainWindow will call this from its toggle slot (Plan 03).
    - Do NOT add any `self._repo.set_setting(...)` call inside NowPlayingPanel for compact mode. D-09 session-only invariant — confirmed by negative test above.
  </behavior>
  <action>
    Create tests/test_phase72_now_playing_panel.py with 8 RED tests listed above; use fixture pattern from tests/test_now_playing_panel.py:1-50 (FakePlayer, FakeRepo({"volume": "80"})). Run tests once to confirm RED. Then modify musicstreamer/ui_qt/now_playing_panel.py: (a) add `compact_mode_toggled = Signal(bool)` at class-scope adjacent to existing Signal declarations (~lines 224-269), (b) insert the new QToolButton block at line 514 (between volume_slider and addStretch), (c) add `_on_compact_btn_toggled` and `set_compact_button_icon` bound methods. Verify all 8 tests now pass GREEN. Icon load via `QIcon.fromTheme("sidebar-hide-symbolic", QIcon(":/icons/sidebar-hide-symbolic.svg"))` mirrors Shared Pattern 3 / star_btn pattern. Per QA-05, NO lambdas: connect via `self.compact_mode_toggle_btn.toggled.connect(self._on_compact_btn_toggled)`.
  </action>
  <verify>
    <automated>pytest tests/test_phase72_now_playing_panel.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 8 new tests pass GREEN.
    - Source assertion: `grep -n "compact_mode_toggled = Signal(bool)" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 1 hit at class scope.
    - Source assertion: `grep -n "compact_mode_toggle_btn = QToolButton" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 1 hit.
    - Source-order assertion: line number of `compact_mode_toggle_btn` button-add (`controls.addWidget(self.compact_mode_toggle_btn)`) is greater than the line of `controls.addWidget(self.volume_slider)` AND less than the line of `controls.addStretch(1)`. Verify with `awk '/controls.addWidget\(self.volume_slider\)/{a=NR} /controls.addWidget\(self.compact_mode_toggle_btn\)/{b=NR} /controls.addStretch\(1\)/{c=NR} END {exit !(a<b && b<c)}' musicstreamer/ui_qt/now_playing_panel.py`.
    - Lambda gate: `grep "compact_mode_toggle_btn.toggled.connect" musicstreamer/ui_qt/now_playing_panel.py | grep -c lambda` returns 0.
    - Negative gate (D-09 in panel): `grep -E "set_setting.*compact" musicstreamer/ui_qt/now_playing_panel.py` returns no matches.
    - Negative gate (no hamburger entry per D-01): `grep -E "_act_compact|_act.*sidebar" musicstreamer/ui_qt/main_window.py` returns no matches (D-01: no hamburger menu entry; we only modify now_playing_panel.py in this plan but assert the hamburger remains untouched).
    - Full panel suite still passes: `pytest tests/test_now_playing_panel.py -x` exits 0.
  </acceptance_criteria>
  <done>
    NowPlayingPanel exposes `compact_mode_toggle_btn` at the correct control-row position with checkable state and icon-flip helper. `compact_mode_toggled` Signal(bool) fires on toggle. NO repo writes from the panel. All 8 new tests + the existing now_playing_panel test suite pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI-state-only — no I/O, no new auth, no new external surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02 | (n/a) | NowPlayingPanel button + signal | accept | No new attack surface — checkable QToolButton emitting bool to MainWindow within-process. Tooltip is plain text. SVG assets are project-local. |

Per RESEARCH §Security Domain: V5 input validation applies trivially (button input is constrained to bool). No new attack surface introduced.
</threat_model>

<verification>
After both tasks complete:
- `pytest tests/test_phase72_now_playing_panel.py tests/test_now_playing_panel.py -x -v` exits 0
- `pytest tests/` full suite still green
- `python -c "from musicstreamer.ui_qt import now_playing_panel; from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; assert hasattr(NowPlayingPanel, 'compact_mode_toggled')"` exits 0
- Negative source assertion: `grep -E "lambda" musicstreamer/ui_qt/now_playing_panel.py | grep -c "compact"` returns 0
</verification>

<success_criteria>
- Two new SVG icons exist, register in icons.qrc, bundled in icons_rc.py, resolvable at runtime.
- NowPlayingPanel exposes compact_mode_toggle_btn at far right of control row (line 514 between volume_slider and addStretch).
- compact_mode_toggled = Signal(bool) declared at class scope, fires on toggle.
- set_compact_button_icon(checked) flips icon + tooltip per UI-SPEC §Interaction Contract.
- All 8 new unit tests + full now_playing_panel suite pass.
- No lambdas (QA-05); no repo writes for compact-mode (D-09 panel-level half).
</success_criteria>

<output>
After completion, create `.planning/phases/72-fullscreen-mode-hide-left-column-for-compact-displays/72-02-SUMMARY.md`:
- Final button line number in now_playing_panel.py
- Glyph-design decisions for the two SVGs (chevron direction, stroke weight)
- Any deviations from PATTERNS §Pattern Assignments §2
</output>
