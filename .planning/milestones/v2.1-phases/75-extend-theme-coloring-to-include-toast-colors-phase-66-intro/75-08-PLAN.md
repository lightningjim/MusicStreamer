---
phase: 75
plan: 08
type: execute
wave: 3
depends_on: [01, 03, 04]
files_modified:
  - tests/test_theme_picker_dialog.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "Tile click on vaporwave sets QApplication.property('theme_name') to 'vaporwave'"
    - "Tile click on system sets QApplication.property('theme_name') to 'system'"
    - "Tile click on vaporwave retints a live ToastOverlay via PaletteChange dispatch — toast.styleSheet() contains rgba(249, 214, 240, 220)"
  artifacts:
    - path: "tests/test_theme_picker_dialog.py"
      provides: "Property-mirror assertion + integration test for live-preview toast retint"
      contains: "theme_name"
  key_links:
    - from: "qtbot.mouseClick(dlg._tiles['vaporwave'], Qt.LeftButton)"
      to: "ToastOverlay.styleSheet() contains rgba(249, 214, 240, 220)"
      via: "PLAN-04 setProperty + Qt synchronous PaletteChange + PLAN-03 changeEvent → _rebuild_stylesheet"
      pattern: "rgba\\(249, 214, 240, 220\\)"
---

<objective>
Extend `tests/test_theme_picker_dialog.py` with three new tests that exercise the PLAN-04 setProperty mirror site and the end-to-end live-preview retint flow:

1. Tile click on vaporwave → `qapp.property("theme_name") == "vaporwave"` (PLAN-04 mechanism lock)
2. Tile click on system → `qapp.property("theme_name") == "system"` (mechanism extends to system branch)
3. Tile click on vaporwave with a live ToastOverlay parented under a test QWidget → toast's stylesheet contains the vaporwave ToolTipBase rgb tuple (end-to-end integration: PLAN-01 preset + PLAN-03 toast retint + PLAN-04 picker mirror, all wired together)

Purpose: This is the only integration test in Phase 75 that exercises the picker→toast retint chain through real widgets. UI-SPEC §Test surface row at 75-UI-SPEC.md:314 (`Tile click on vaporwave retints toast: ToolTipBase of running palette == #f9d6f0`).

Output: `tests/test_theme_picker_dialog.py` with three new tests added. All picker tests must run green: `pytest tests/test_theme_picker_dialog.py -x`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@tests/test_theme_picker_dialog.py
@musicstreamer/ui_qt/theme_picker_dialog.py
@musicstreamer/ui_qt/toast.py
@musicstreamer/theme.py

<interfaces>
Existing tests/test_theme_picker_dialog.py fixtures (lines 23-47):
- FakeRepo class (line 23) — in-memory dict-backed repo
- repo fixture (line 39) → FakeRepo()
- dialog fixture (line 44) — instantiates ThemePickerDialog

Existing analog `test_tile_click_applies_palette` (lines 75-80):
- def test_tile_click_applies_palette(qtbot, repo, qapp):
-     dlg = ThemePickerDialog(repo)
-     qtbot.addWidget(dlg)
-     qtbot.mouseClick(dlg._tiles["vaporwave"], Qt.LeftButton)
-     assert qapp.palette().color(QPalette.ColorRole.Window).name().lower() == "#efe5ff"

ToastOverlay parent construction (tests/test_toast_overlay.py:19-26 pattern):
- parent = QWidget(); parent.resize(800, 600); qtbot.addWidget(parent); parent.show(); qtbot.waitExposed(parent)
- toast = ToastOverlay(parent)

Cross-test pollution: pytest-qt `qapp` is session-scoped. Each new test that depends on `qapp.property("theme_name")` MUST set it explicitly at the start, OR rely on the picker tile-click setting it (which is what these tests verify).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add property-mirror tests for vaporwave + system tile clicks</name>
  <files>tests/test_theme_picker_dialog.py</files>
  <read_first>
    - tests/test_theme_picker_dialog.py lines 75-80 (test_tile_click_applies_palette analog)
    - tests/test_theme_picker_dialog.py lines 23-47 (FakeRepo + repo + dialog fixtures)
    - musicstreamer/ui_qt/theme_picker_dialog.py lines 260-285 (_on_tile_clicked after PLAN-04)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §tests/test_theme_picker_dialog.py (lines 536-571)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Test surface (line 314)
  </read_first>
  <action>
    Add two new tests to `tests/test_theme_picker_dialog.py`. Place them adjacent to the existing `test_tile_click_applies_palette` at lines 75-80.

    (a) `test_tile_click_sets_theme_name_property(qtbot, repo, qapp)`:
      - `dlg = ThemePickerDialog(repo)`; `qtbot.addWidget(dlg)`
      - `qtbot.mouseClick(dlg._tiles["vaporwave"], Qt.LeftButton)`
      - `assert qapp.property("theme_name") == "vaporwave"`
      - Docstring: `"""Phase 75 PLAN-04: tile click mirrors apply_theme_palette by setting QApplication property."""`

    (b) `test_tile_click_system_sets_theme_name_property(qtbot, repo, qapp)`:
      - `dlg = ThemePickerDialog(repo)`; `qtbot.addWidget(dlg)`
      - `qtbot.mouseClick(dlg._tiles["system"], Qt.LeftButton)`
      - `assert qapp.property("theme_name") == "system"`
      - Docstring: `"""Phase 75 PLAN-04: tile click sets property even for system branch (covers Linux+system early return invariant)."""`

    Reuse the existing `FakeRepo`, `repo` (fixture line 39), `qapp` (pytest-qt builtin), and `qtbot` fixtures verbatim. No new fixtures.

    DO NOT touch the existing `test_tile_click_applies_palette` test at lines 75-80 — it verifies palette mutation, this plan adds property-mirror coverage on top of (not instead of) that.

    DO NOT introduce a cleanup fixture for the qapp property. Each new test sets the property via the picker tile click; tests run in isolation by virtue of always clicking a tile before asserting.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; pytest tests/test_theme_picker_dialog.py::test_tile_click_sets_theme_name_property tests/test_theme_picker_dialog.py::test_tile_click_system_sets_theme_name_property -x -v 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test_tile_click_sets_theme_name_property` passes — `qapp.property("theme_name") == "vaporwave"` after vaporwave tile click
    - `test_tile_click_system_sets_theme_name_property` passes — `qapp.property("theme_name") == "system"` after system tile click
    - All 17 pre-existing picker tests still pass
    - Source grep: `grep -c 'test_tile_click_sets_theme_name_property' tests/test_theme_picker_dialog.py` returns 1
    - Source grep: `grep -c 'test_tile_click_system_sets_theme_name_property' tests/test_theme_picker_dialog.py` returns 1
  </acceptance_criteria>
  <done>Two new property-mirror tests pass for vaporwave + system; existing tests untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add end-to-end integration test — tile click retints live ToastOverlay</name>
  <files>tests/test_theme_picker_dialog.py</files>
  <read_first>
    - tests/test_theme_picker_dialog.py (current state after Task 1)
    - tests/test_toast_overlay.py lines 19-26 (parent_widget fixture — ToastOverlay parent construction pattern)
    - musicstreamer/ui_qt/toast.py (final state after PLAN-03 — _rebuild_stylesheet + changeEvent)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §tests/test_theme_picker_dialog.py §test_tile_click_retints_toast_overlay (lines 549-565)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Test surface (line 314 — retint integration)
  </read_first>
  <action>
    Add one new integration test to `tests/test_theme_picker_dialog.py`. Place it adjacent to the two tests added in Task 1.

    `test_tile_click_retints_toast_overlay(qtbot, repo, qapp)`:
      - Imports at the top of the test body (or hoisted to file-level imports): `from musicstreamer.ui_qt.toast import ToastOverlay`; `from PySide6.QtWidgets import QWidget`. If file already imports QWidget, do not re-import.
      - Construct a parent widget following the test_toast_overlay.py:19-26 pattern: `parent = QWidget()`; `parent.resize(800, 600)`; `qtbot.addWidget(parent)`; `parent.show()`; `qtbot.waitExposed(parent)`
      - Construct a toast under that parent: `toast = ToastOverlay(parent)`. At this moment, ToastOverlay has whatever stylesheet matched the qapp's property state at construction time.
      - Construct picker: `dlg = ThemePickerDialog(repo)`; `qtbot.addWidget(dlg)`
      - Click the vaporwave tile: `qtbot.mouseClick(dlg._tiles["vaporwave"], Qt.LeftButton)`
      - Read toast styleSheet AFTER the click: `qss = toast.styleSheet()`. Qt dispatches `PaletteChange` synchronously inside `setPalette()` during `_on_tile_clicked`, so by the time the click handler returns, ToastOverlay's `changeEvent(PaletteChange)` has fired and `_rebuild_stylesheet` has rebuilt the QSS for vaporwave.
      - Assert UI-SPEC vaporwave LOCKED rgb: `assert "rgba(249, 214, 240, 220)" in qss, f"toast did not retint to vaporwave: {qss!r}"`
      - Docstring: `"""Phase 75 end-to-end: vaporwave tile click → live ToastOverlay rebuilds QSS via PaletteChange dispatch. Locks the full PLAN-01 + PLAN-03 + PLAN-04 chain."""`

    Reuse `FakeRepo`, `repo`, `qapp`, `qtbot` fixtures verbatim. The test does NOT need `parent_widget` (that fixture lives in test_toast_overlay.py and is not shared via conftest); construct the parent inline.

    The test does NOT depend on the toast becoming visible; the stylesheet is rebuilt regardless of widget visibility (changeEvent fires on hidden widgets too). No `qtbot.waitUntil` or animation handling needed.

    If the toast's stylesheet at construction time is already "vaporwave" (because a prior test left the qapp property set to "vaporwave"), the test still passes — the assertion is about post-click state, not pre/post-delta. For maximum determinism, you MAY add `qapp.setProperty("theme_name", "system"); qapp.setPalette(QPalette())` at the very top of the test before any construction, to force a known starting state — but this is optional defense-in-depth, not required for the assertion to hold.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; pytest tests/test_theme_picker_dialog.py -x -v 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test_tile_click_retints_toast_overlay` passes — vaporwave tile click results in toast.styleSheet() containing `"rgba(249, 214, 240, 220)"`
    - All 17 pre-existing picker tests still pass
    - The two property-mirror tests from Task 1 still pass
    - Source grep: `grep -c 'test_tile_click_retints_toast_overlay' tests/test_theme_picker_dialog.py` returns 1
    - Source grep: `grep -c 'rgba(249, 214, 240, 220)' tests/test_theme_picker_dialog.py` returns at least 1
    - `pytest tests/test_theme_picker_dialog.py -x` returns 0
    - Full Phase 75 test suite green: `pytest tests/test_toast_overlay.py tests/test_theme.py tests/test_theme_editor_dialog.py tests/test_theme_picker_dialog.py -x` returns 0
  </acceptance_criteria>
  <done>End-to-end integration test passes — vaporwave tile click retints live ToastOverlay via the PaletteChange dispatch chain. Full 4-file Phase 75 test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only changes consuming the full PLAN-01 → PLAN-03 → PLAN-04 chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-10 | Tampering (V5 — N/A) | Test files only | accept | Tests are read-only consumers of the picker, toast, and theme contracts. V5 mitigation for theme_custom JSON lives upstream (see PLAN-01 threat model); test isolation handled via FakeRepo dict per-instance. |
</threat_model>

<verification>
- `pytest tests/test_theme_picker_dialog.py -x` returns 0
- Full Phase 75 test suite green: `pytest tests/test_toast_overlay.py tests/test_theme.py tests/test_theme_editor_dialog.py tests/test_theme_picker_dialog.py -x` returns 0
- UI-SPEC §Test surface picker rows (lines 314) green
</verification>

<success_criteria>
- Two property-mirror tests + one end-to-end retint integration test pass.
- Full 4-file Phase 75 test suite green.
- UI-SPEC manual-UAT Wayland live-flip step (75-VALIDATION.md §Manual-Only Verifications) remains the only outstanding verification path — covered by /gsd-verify-work, not this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-08-SUMMARY.md`.
</output>
