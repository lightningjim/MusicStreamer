---
phase: 75
plan: 07
type: execute
wave: 3
depends_on: [01, 05]
files_modified:
  - tests/test_theme_editor_dialog.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "test_editor_shows_9_color_rows retrofitted to 11 rows; ToolTipBase + ToolTipText keys present in _rows"
    - "ROLE_LABELS['ToolTipBase'] and ROLE_LABELS['ToolTipText'] strings asserted ('Toast background', 'Toast text')"
    - "Save round-trip includes both new keys in theme_custom JSON"
    - "Reset to source preset restores both new keys to UI-SPEC vaporwave LOCKED values"
    - "Cancel snapshot restores ToolTipBase role in QApplication.palette()"
  artifacts:
    - path: "tests/test_theme_editor_dialog.py"
      provides: "11-row coverage; Save/Reset/Cancel round-trip for new keys; ROLE_LABELS lock"
      contains: "ToolTipBase"
  key_links:
    - from: "PLAN-01 EDITABLE_ROLES (11 entries)"
      to: "_rows dict in ThemeEditorDialog"
      via: "iteration at theme_editor_dialog.py:161-167 → _rows[role_name] population"
      pattern: "_rows\\[.ToolTipBase.\\]"
---

<objective>
Extend `tests/test_theme_editor_dialog.py` to cover the 11-row editor layout introduced by PLAN-01 (EDITABLE_ROLES growth) + PLAN-05 (ROLE_LABELS additions). Specifically: retrofit the 9-row count test to 11; add ROLE_LABELS string assertion; add Save round-trip assertion for new keys; add Reset assertion for new keys; add Cancel snapshot-restore assertion for ToolTipBase role in the running palette.

Purpose: Lock the contract that the Custom-theme editor grows correctly (CONTEXT.md D-05 / UI-SPEC §Test surface lines 311-315), Save persists the new keys to `theme_custom` JSON (D-14), Reset reverts them to source-preset values (D-14), and Cancel snapshot-restore covers them (D-14).

Output: `tests/test_theme_editor_dialog.py` extended additively (one test renamed/retrofitted, the rest are new). All editor tests must run green: `pytest tests/test_theme_editor_dialog.py -x`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@tests/test_theme_editor_dialog.py
@musicstreamer/ui_qt/theme_editor_dialog.py
@musicstreamer/theme.py

<interfaces>
Existing tests/test_theme_editor_dialog.py fixtures (lines 25-79):
- FakeRepo (lines 25-33) — in-memory dict-backed repo with get_setting/set_setting
- repo fixture (line 59) → returns FakeRepo()
- dialog fixture (line 64) — instantiates ThemeEditorDialog with source_preset="vaporwave"
- stub_color_dialog fixture (lines 70-79) — monkeypatches QColorDialog.getColor to return a pre-set QColor stored in a dict (mutate via `stub_color_dialog["color"] = QColor("#abcdef")` before clicking a swatch)

Existing test_editor_shows_9_color_rows (lines 86-90) — RETROFIT to 11:
- def test_editor_shows_9_color_rows(dialog):
-     assert set(dialog._rows.keys()) == set(EDITABLE_ROLES)
-     assert len(dialog._rows) == 9
-     assert "Highlight" not in dialog._rows

Existing Save round-trip pattern (lines 174-184):
- Set stub_color_dialog["color"] = QColor("#abcdef")
- Click Window row swatch
- Call dlg._on_save()
- Load saved JSON via json.loads(repo.get_setting("theme_custom", ""))
- Assert content
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retrofit 9-row test → 11 rows; add ROLE_LABELS Toast-string assertion</name>
  <files>tests/test_theme_editor_dialog.py</files>
  <read_first>
    - tests/test_theme_editor_dialog.py lines 86-90 (test_editor_shows_9_color_rows)
    - tests/test_theme_editor_dialog.py lines 22-79 (fixtures + FakeRepo + stub_color_dialog)
    - musicstreamer/ui_qt/theme_editor_dialog.py lines 37-47 (ROLE_LABELS — after PLAN-05)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §tests/test_theme_editor_dialog.py (lines 446-527)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Test surface (lines 311, 315)
  </read_first>
  <action>
    Edit `tests/test_theme_editor_dialog.py`.

    (a) Retrofit `test_editor_shows_9_color_rows` at lines 86-90. Rename function to `test_editor_shows_11_color_rows`. Update the docstring to read `"""dlg._rows has exactly 11 keys matching EDITABLE_ROLES (Phase 75 D-05)."""`. Change `assert len(dialog._rows) == 9` to `assert len(dialog._rows) == 11`. Keep `assert set(dialog._rows.keys()) == set(EDITABLE_ROLES)` and `assert "Highlight" not in dialog._rows` verbatim. Add two new assertions at the end of the body: `assert "ToolTipBase" in dialog._rows` AND `assert "ToolTipText" in dialog._rows`.

    (b) Add new test `test_role_labels_include_toast_pair()` (no fixtures needed — pure module-level dict assertion). Import `ROLE_LABELS`: add `from musicstreamer.ui_qt.theme_editor_dialog import ROLE_LABELS` at the top of the file IF NOT already imported (check existing imports first; if absent, add). Body: `assert ROLE_LABELS["ToolTipBase"] == "Toast background"` AND `assert ROLE_LABELS["ToolTipText"] == "Toast text"`. Docstring: `"""UI-SPEC §Copywriting Contract — locked Toast labels for new rows."""`.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; pytest tests/test_theme_editor_dialog.py::test_editor_shows_11_color_rows tests/test_theme_editor_dialog.py::test_role_labels_include_toast_pair -x -v 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test_editor_shows_11_color_rows` passes (renamed from `_9_color_rows`); asserts `len(_rows) == 11`, set equality with EDITABLE_ROLES, and presence of both new keys
    - `test_role_labels_include_toast_pair` passes; asserts both new labels verbatim
    - Source grep: `grep -c 'test_editor_shows_9_color_rows' tests/test_theme_editor_dialog.py` returns 0
    - Source grep: `grep -c 'test_editor_shows_11_color_rows' tests/test_theme_editor_dialog.py` returns 1
    - Source grep: `grep -c '"Toast background"' tests/test_theme_editor_dialog.py` returns at least 1
    - Source grep: `grep -c '"Toast text"' tests/test_theme_editor_dialog.py` returns at least 1
  </acceptance_criteria>
  <done>Editor row count retrofitted to 11 with explicit ToolTipBase/ToolTipText presence assertions; ROLE_LABELS Toast-string lock test added; both green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Save / Reset / Cancel coverage for ToolTipBase + ToolTipText keys</name>
  <files>tests/test_theme_editor_dialog.py</files>
  <read_first>
    - tests/test_theme_editor_dialog.py lines 142-184 (test_reset_reverts_to_source_preset, test_save_persists_theme_custom_json patterns)
    - tests/test_theme_editor_dialog.py lines 214-229 (test_cancel_restores_snapshot pattern)
    - tests/test_theme_editor_dialog.py lines 70-79 (stub_color_dialog fixture)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §tests/test_theme_editor_dialog.py (lines 487-527)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Test surface (lines 311-313)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-14
  </read_first>
  <action>
    Add three new tests to `tests/test_theme_editor_dialog.py`. Place each adjacent to its analog (Save next to test_save_persists_theme_custom_json at line 174, Reset next to test_reset_reverts_to_source_preset at line 142, Cancel next to test_cancel_restores_snapshot at line 214).

    (a) `test_save_persists_toast_keys_when_user_edits_them(qtbot, repo, qapp, stub_color_dialog)`:
      - Set `stub_color_dialog["color"] = QColor("#abc123")` (any 6-char hex distinct from preset locked values)
      - Construct `dlg = ThemeEditorDialog(repo, source_preset="vaporwave")`; `qtbot.addWidget(dlg)`
      - `qtbot.mouseClick(dlg._rows["ToolTipBase"]._swatch_btn, Qt.LeftButton)` — opens the (stubbed) color dialog, returns #abc123, fires _on_role_color_changed
      - `dlg._on_save()`
      - `saved = json.loads(repo.get_setting("theme_custom", ""))`
      - `assert saved["ToolTipBase"] == "#abc123"`
      - Docstring: `"""Phase 75 D-14: editing the ToolTipBase row → Save → JSON contains the new hex."""`

    (b) `test_reset_restores_toast_rows_to_source_preset(qtbot, repo, qapp, stub_color_dialog)`:
      - `stub_color_dialog["color"] = QColor("#000000")` (any value distinct from UI-SPEC vaporwave locked pair)
      - Construct `dlg = ThemeEditorDialog(repo, source_preset="vaporwave")`; `qtbot.addWidget(dlg)`
      - Click both new rows: `qtbot.mouseClick(dlg._rows["ToolTipBase"]._swatch_btn, Qt.LeftButton)`; `qtbot.mouseClick(dlg._rows["ToolTipText"]._swatch_btn, Qt.LeftButton)` (both now #000000)
      - `dlg._on_reset()`
      - Assert UI-SPEC vaporwave LOCKED pair restored: `assert dlg._role_hex_dict["ToolTipBase"] == "#f9d6f0"` AND `assert dlg._role_hex_dict["ToolTipText"] == "#3a2845"`
      - Docstring: `"""Phase 75 D-14: Reset reverts ToolTipBase + ToolTipText to source-preset hex (vaporwave UI-SPEC LOCKED)."""`

    (c) `test_cancel_restores_toast_roles_in_palette(qtbot, repo, qapp)`:
      - Import at top of file if not present: `from PySide6.QtGui import QPalette`
      - Capture original palette role: `original_bg = qapp.palette().color(QPalette.ColorRole.ToolTipBase)`
      - Construct `dlg = ThemeEditorDialog(repo, source_preset="vaporwave")`; `qtbot.addWidget(dlg)`
      - Directly invoke the slot: `dlg._on_role_color_changed("ToolTipBase", "#000000")` (bypass stubbed dialog; faster + more direct than qtbot.mouseClick)
      - Assert palette mutated: `assert qapp.palette().color(QPalette.ColorRole.ToolTipBase).name().lower() == "#000000"`
      - `dlg.reject()`
      - Assert palette restored: `assert qapp.palette().color(QPalette.ColorRole.ToolTipBase) == original_bg`
      - Docstring: `"""Phase 75 D-14 + Phase 66 D-12: reject() snapshot-restore covers new ToolTipBase role in QApplication.palette()."""`

    Reuse existing `FakeRepo`, `repo`, `stub_color_dialog` fixtures verbatim. DO NOT introduce new fixtures. DO NOT delete any existing test. The existing `test_save_persists_theme_custom_json` at lines 174-184 ALREADY iterates `EDITABLE_ROLES` to assert each role lands in the JSON — once PLAN-01 grew that tuple to 11, this test implicitly asserts the new keys are present in the JSON dump. Test (a) adds explicit defense-in-depth.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; pytest tests/test_theme_editor_dialog.py -x -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test_save_persists_toast_keys_when_user_edits_them` passes — `saved["ToolTipBase"] == "#abc123"` after clicking the ToolTipBase swatch and saving
    - `test_reset_restores_toast_rows_to_source_preset` passes — both new keys revert to UI-SPEC vaporwave locked values (`#f9d6f0`, `#3a2845`) after Reset
    - `test_cancel_restores_toast_roles_in_palette` passes — palette ToolTipBase mutates to `#000000` then restores to original via `dlg.reject()`
    - Source grep: `grep -c 'test_save_persists_toast_keys_when_user_edits_them' tests/test_theme_editor_dialog.py` returns 1
    - Source grep: `grep -c 'test_reset_restores_toast_rows_to_source_preset' tests/test_theme_editor_dialog.py` returns 1
    - Source grep: `grep -c 'test_cancel_restores_toast_roles_in_palette' tests/test_theme_editor_dialog.py` returns 1
    - All pre-existing editor tests still pass; the existing `test_save_persists_theme_custom_json` continues to pass (its `for role in EDITABLE_ROLES` loop auto-covers the new keys now that EDITABLE_ROLES has 11 entries)
    - `pytest tests/test_theme_editor_dialog.py -x` returns 0
  </acceptance_criteria>
  <done>Save round-trips ToolTipBase to JSON, Reset reverts both new keys to UI-SPEC vaporwave LOCKED values, Cancel snapshot restores ToolTipBase role. All editor tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only changes consuming PLAN-01 + PLAN-05 contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-09 | Tampering (V5 — N/A) | Test files only | accept | Tests are read-only consumers of EDITABLE_ROLES, ROLE_LABELS, ThemeEditorDialog, and JSON round-trip. V5 mitigation for theme_custom JSON lives upstream in `theme.py:179-186` (no Phase 75 change needed; see PLAN-01 threat model). |
</threat_model>

<verification>
- `pytest tests/test_theme_editor_dialog.py -x` returns 0
- UI-SPEC §Test surface rows for the editor (lines 310-315) all green
- No deletion of existing tests; one rename (9_color → 11_color)
</verification>

<success_criteria>
- All tests in `tests/test_theme_editor_dialog.py` pass after retrofit + 4 new tests.
- 11-row layout, ROLE_LABELS Toast strings, Save round-trip, Reset restore, Cancel snapshot — all locked.
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-07-SUMMARY.md`.
</output>
