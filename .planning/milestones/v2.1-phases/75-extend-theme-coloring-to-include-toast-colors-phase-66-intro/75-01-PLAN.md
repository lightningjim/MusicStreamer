---
phase: 75
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/theme.py
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "All 6 non-system THEME_PRESETS declare locked ToolTipBase + ToolTipText hex (UI-SPEC §Color)"
    - "system preset entry remains {} (Phase 66 D-23 sentinel)"
    - "EDITABLE_ROLES has exactly 11 entries with ToolTipBase, ToolTipText appended last"
    - "apply_theme_palette sets QApplication property 'theme_name' before any branch logic"
    - "theme.py docstring reflects 11 roles, not 9"
  artifacts:
    - path: "musicstreamer/theme.py"
      provides: "THEME_PRESETS with ToolTipBase/ToolTipText pairs, EDITABLE_ROLES of length 11, app.setProperty('theme_name', theme_name) inside apply_theme_palette"
      contains: "#f9d6f0"
  key_links:
    - from: "musicstreamer/theme.py::apply_theme_palette"
      to: "QApplication.setProperty('theme_name', theme_name)"
      via: "single line inserted after repo.get_setting('theme', 'system')"
      pattern: "app\\.setProperty\\(\"theme_name\""
---

<objective>
Land the Phase 75 theme.py foundation: append `ToolTipBase` + `ToolTipText` hex pairs (UI-SPEC LOCKED) to all 6 non-system presets, append `"ToolTipBase"` + `"ToolTipText"` to `EDITABLE_ROLES`, add a single `app.setProperty("theme_name", theme_name)` call inside `apply_theme_palette` (D-10 path b, RESEARCH §1), and update the module docstring "9 roles" → "11 roles".

Purpose: Wave 1 foundation — every downstream consumer (toast.py, theme_picker_dialog.py, theme_editor_dialog.py, all 4 test files) reads from these structures. No widget code changes here. Pure additive — first 9 EDITABLE_ROLES entries and existing preset keys are immutable per UI-SPEC §Visual Invariance Locks.

Output: `musicstreamer/theme.py` with 12 new hex literals, 2 new tuple entries, 1 new setProperty line, and docstring update. THEME-02 lives downstream in PLAN-02; this plan references it forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md
@.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md
@musicstreamer/theme.py

<interfaces>
<!-- Existing structures the executor must preserve verbatim (UI-SPEC §Visual Invariance Locks): -->
<!-- THEME_PRESETS["system"] == {} (Phase 66 D-23 sentinel) -->
<!-- THEME_PRESETS["vaporwave"] / "overrun" / "gbs" / "gbs_after_dark" / "dark" / "light" — existing 10 keys per preset unchanged -->
<!-- EDITABLE_ROLES first 9 entries (Window → Link) unchanged and in current order -->
<!-- build_palette_from_dict signature unchanged — it already iterates arbitrary {role_name: hex} dicts via getattr(QPalette.ColorRole, role_name, None) at theme.py:182-184 -->

UI-SPEC §Color LOCKED pairs (75-UI-SPEC.md:81-89):
- vaporwave: ToolTipBase #f9d6f0, ToolTipText #3a2845
- overrun: ToolTipBase #1a0a18, ToolTipText #ffe8f4
- gbs: ToolTipBase #2d5a2a, ToolTipText #f0f5e8
- gbs_after_dark: ToolTipBase #d5e8d3, ToolTipText #0a1a0d
- dark: ToolTipBase #181820, ToolTipText #f0f0f0
- light: ToolTipBase #2a2a32, ToolTipText #f5f5f5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append UI-SPEC locked ToolTipBase/ToolTipText hex to all 6 non-system presets</name>
  <files>musicstreamer/theme.py</files>
  <read_first>
    - musicstreamer/theme.py (entire file — preset dict shapes at lines 34-127)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-UI-SPEC.md §Color (lines 75-90) — LOCKED hex pairs table
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §THEME_PRESETS extension pattern (lines 27-58)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-08 (per-preset coverage)
  </read_first>
  <action>
    Edit musicstreamer/theme.py THEME_PRESETS dict (lines 34-127). For each of the 6 non-system preset entries (vaporwave, overrun, gbs, gbs_after_dark, dark, light), append two new key-value pairs AFTER the existing Link entry: "ToolTipBase" and "ToolTipText" with the UI-SPEC LOCKED hex values listed in &lt;interfaces&gt; above. Preserve uppercase-hex convention for gbs (matches existing Phase 66 D-05 verbatim brand sampling — keys "ToolTipBase": "#2d5a2a" and "ToolTipText": "#f0f5e8" stay lowercase per UI-SPEC §Color table; do not invent new case rules). DO NOT touch the system entry (stays {} per Phase 66 D-23). DO NOT reorder any existing keys. DO NOT change Highlight, Link, or any other existing hex value. Format matches surrounding entries: 4-space indent, comma-terminated, trailing comma after ToolTipText. After insertion, each non-system preset dict has exactly 12 keys (10 existing + 2 new).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -c "from musicstreamer.theme import THEME_PRESETS; expected = {'vaporwave': ('#f9d6f0', '#3a2845'), 'overrun': ('#1a0a18', '#ffe8f4'), 'gbs': ('#2d5a2a', '#f0f5e8'), 'gbs_after_dark': ('#d5e8d3', '#0a1a0d'), 'dark': ('#181820', '#f0f0f0'), 'light': ('#2a2a32', '#f5f5f5')}; [(_ for _ in ()).throw(AssertionError(f'{tid}: bg={THEME_PRESETS[tid].get(\"ToolTipBase\")} fg={THEME_PRESETS[tid].get(\"ToolTipText\")} expected {pair}')) for tid, pair in expected.items() if (THEME_PRESETS[tid].get('ToolTipBase'), THEME_PRESETS[tid].get('ToolTipText')) != pair]; assert THEME_PRESETS['system'] == {}, 'system must stay empty'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `THEME_PRESETS["vaporwave"]["ToolTipBase"] == "#f9d6f0"` AND `THEME_PRESETS["vaporwave"]["ToolTipText"] == "#3a2845"`
    - `THEME_PRESETS["overrun"]["ToolTipBase"] == "#1a0a18"` AND `THEME_PRESETS["overrun"]["ToolTipText"] == "#ffe8f4"`
    - `THEME_PRESETS["gbs"]["ToolTipBase"] == "#2d5a2a"` AND `THEME_PRESETS["gbs"]["ToolTipText"] == "#f0f5e8"`
    - `THEME_PRESETS["gbs_after_dark"]["ToolTipBase"] == "#d5e8d3"` AND `THEME_PRESETS["gbs_after_dark"]["ToolTipText"] == "#0a1a0d"`
    - `THEME_PRESETS["dark"]["ToolTipBase"] == "#181820"` AND `THEME_PRESETS["dark"]["ToolTipText"] == "#f0f0f0"`
    - `THEME_PRESETS["light"]["ToolTipBase"] == "#2a2a32"` AND `THEME_PRESETS["light"]["ToolTipText"] == "#f5f5f5"`
    - `THEME_PRESETS["system"] == {}` (Phase 66 D-23 sentinel preserved)
    - Each non-system preset dict has exactly 12 keys (no removal of existing 10)
    - Source grep: `grep -c '"ToolTipBase":' musicstreamer/theme.py` returns 6
    - Source grep: `grep -c '"ToolTipText":' musicstreamer/theme.py` returns 6
  </acceptance_criteria>
  <done>All 6 non-system presets contain the locked ToolTipBase + ToolTipText hex pairs from UI-SPEC §Color; system preset still {}; existing 10 keys per preset unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append "ToolTipBase" + "ToolTipText" to EDITABLE_ROLES and add app.setProperty inside apply_theme_palette</name>
  <files>musicstreamer/theme.py</files>
  <read_first>
    - musicstreamer/theme.py lines 154-164 (EDITABLE_ROLES tuple)
    - musicstreamer/theme.py lines 189-231 (apply_theme_palette function)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §EDITABLE_ROLES extension pattern + §apply_theme_palette setProperty pattern (lines 60-88)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-RESEARCH.md §1 Specific File:Line Answers (lines 256-295)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md D-05, D-10 path (b)
  </read_first>
  <action>
    Two edits in musicstreamer/theme.py:

    (a) EDITABLE_ROLES tuple at lines 154-164: append `"ToolTipBase"` then `"ToolTipText"` as the last two entries AFTER `"Link"`. Preserve order of existing 9 entries (Window → WindowText → Base → AlternateBase → Text → Button → ButtonText → HighlightedText → Link). Final tuple length: 11. Maintain trailing-comma style.

    (b) apply_theme_palette at line 206 (function body): insert a single `app.setProperty("theme_name", theme_name)` call IMMEDIATELY AFTER `theme_name = repo.get_setting("theme", "system")` and BEFORE the `if theme_name == "system":` branch. Placement rationale: property must hold the active theme name even on the Linux+system early-return at line 214 (D-10 path b, RESEARCH §1). Pass `theme_name` (the local variable) as the value — never a literal.

    DO NOT change the function signature. DO NOT touch any branch logic. DO NOT add new imports — `QApplication` is only forward-typed via TYPE_CHECKING and `setProperty` is a QObject method available on the passed-in `app`.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -c "from musicstreamer.theme import EDITABLE_ROLES; assert len(EDITABLE_ROLES) == 11, f'got {len(EDITABLE_ROLES)}'; assert EDITABLE_ROLES[-2:] == ('ToolTipBase', 'ToolTipText'), f'got tail {EDITABLE_ROLES[-2:]}'; assert EDITABLE_ROLES[:9] == ('Window', 'WindowText', 'Base', 'AlternateBase', 'Text', 'Button', 'ButtonText', 'HighlightedText', 'Link'), f'head changed: {EDITABLE_ROLES[:9]}'; print('OK')" &amp;&amp; grep -cE 'app\.setProperty\("theme_name"' musicstreamer/theme.py | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `len(EDITABLE_ROLES) == 11`
    - `EDITABLE_ROLES[-2:] == ("ToolTipBase", "ToolTipText")`
    - `EDITABLE_ROLES[:9]` matches the existing 9-tuple order verbatim (Window, WindowText, Base, AlternateBase, Text, Button, ButtonText, HighlightedText, Link)
    - Source grep: `grep -cE 'app\.setProperty\("theme_name"' musicstreamer/theme.py` returns 1
    - The `app.setProperty("theme_name", theme_name)` call appears after `theme_name = repo.get_setting(...)` and before the `if theme_name == "system":` branch (manual inspection — line ordering preserved)
    - `pytest tests/test_theme.py::test_apply_theme_palette_uses_repo_setting -x` still passes (regression — existing test unaffected by additive setProperty)
  </acceptance_criteria>
  <done>EDITABLE_ROLES length is 11 with ToolTipBase/ToolTipText appended last; apply_theme_palette sets QApplication property "theme_name" once before any branch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update theme.py module docstring "9 roles" → "11 roles"</name>
  <files>musicstreamer/theme.py</files>
  <read_first>
    - musicstreamer/theme.py lines 1-19 (module docstring)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-PATTERNS.md §Docstring update pattern (lines 90-101)
    - .planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-CONTEXT.md Claude's Discretion item ("9 roles" → "11 roles" recommended YES)
  </read_first>
  <action>
    Edit the module-level docstring in musicstreamer/theme.py at line 9. Change the line `- Theme owns 9 QPalette primary roles (Window, WindowText, Base, AlternateBase,` and its continuation `  Text, Button, ButtonText, HighlightedText, Link) plus a Highlight baseline.` to read `- Theme owns 11 QPalette primary roles (Window, WindowText, Base, AlternateBase,` and continuation `  Text, Button, ButtonText, HighlightedText, Link, ToolTipBase, ToolTipText)` then `  plus a Highlight baseline.` Documentation only — no behavioral change. Preserve existing comment style, indentation (2 spaces under bullet), and line-break shape.
  </action>
  <verify>
    <automated>grep -c 'Theme owns 11 QPalette primary roles' /home/kcreasey/OneDrive/Projects/MusicStreamer/musicstreamer/theme.py | grep -q '^1$' &amp;&amp; (grep -v '^#' /home/kcreasey/OneDrive/Projects/MusicStreamer/musicstreamer/theme.py | grep -c 'Theme owns 9 QPalette' | grep -q '^0$') &amp;&amp; grep -q 'ToolTipBase, ToolTipText' /home/kcreasey/OneDrive/Projects/MusicStreamer/musicstreamer/theme.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Theme owns 11 QPalette primary roles' musicstreamer/theme.py` returns 1
    - `grep -v '^#' musicstreamer/theme.py | grep -c 'Theme owns 9 QPalette'` returns 0 (no stale claim survives)
    - Docstring lists `ToolTipBase, ToolTipText` after `Link`
    - `python -c "import musicstreamer.theme"` imports without error
  </acceptance_criteria>
  <done>Module docstring reflects 11 roles with ToolTipBase/ToolTipText named explicitly; no stale "9 roles" claim survives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `theme_custom` JSON → palette construction | Tampered or hand-edited JSON values flow through `build_palette_from_dict` at `theme.py:179-186`. New keys ToolTipBase/ToolTipText cross this boundary identically to existing 9 keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering (V5 Input Validation) | `theme.py:179-186` build_palette_from_dict | accept | Existing `_is_valid_hex` regex from `musicstreamer/accent_utils.py` already validates every hex via `theme.py:180`; new ToolTipBase/ToolTipText keys flow through this same validator. No new validation code needed (RESEARCH §Security Domain). |
| T-75-02 | Tampering (unknown role name injection) | `theme.py:182-184` | accept | `getattr(QPalette.ColorRole, role_name, None)` returns None for unknown names; loop continues. ToolTipBase and ToolTipText are valid `QPalette.ColorRole` enum members (verified at `musicstreamer/__main__.py:88-89`); they pass the getattr check naturally (RESEARCH §Risk 3). |
</threat_model>

<verification>
After all tasks:
- `pytest tests/test_theme.py -x` — existing tests still pass (additive change). One test (`test_all_presets_cover_9_roles` at line 202) will continue to pass because the loop iterates EDITABLE_ROLES; PLAN-06 retrofits the assertion name + length expectation later.
- `python -c "from musicstreamer.theme import THEME_PRESETS, EDITABLE_ROLES; print(len(EDITABLE_ROLES), THEME_PRESETS['vaporwave']['ToolTipBase'])"` outputs `11 #f9d6f0`.
- Source grep gates from each task's acceptance_criteria.
</verification>

<success_criteria>
- All 6 non-system presets contain `ToolTipBase` + `ToolTipText` keys matching UI-SPEC §Color LOCKED pairs.
- system preset is still `{}` (Phase 66 D-23 invariant).
- EDITABLE_ROLES has exactly 11 entries with the new keys last.
- `apply_theme_palette` sets `app.setProperty("theme_name", theme_name)` exactly once, before any branch.
- Module docstring reflects 11 roles.
- All pre-existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/75-extend-theme-coloring-to-include-toast-colors-phase-66-intro/75-01-SUMMARY.md`.
</output>
