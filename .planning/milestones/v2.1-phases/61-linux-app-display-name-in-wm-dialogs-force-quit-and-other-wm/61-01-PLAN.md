---
phase: 61
slug: linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
autonomous: false
requirements: [BUG-08]

must_haves:
  truths:
    - "61-DIAGNOSTIC-LOG.md exists with a PRE-FIX section capturing Kyle's X11 rig BEFORE state"
    - "Session type recorded (XDG_SESSION_TYPE) — gate confirms x11 (or Kyle has logged into 'GNOME on Xorg' before continuing)"
    - "WM_CLASS / _GTK_APPLICATION_ID / _KDE_NET_WM_DESKTOP_FILE captured for the running MusicStreamer window"
    - "ls of ~/.local/share/applications/ + ~/.local/share/icons/hicolor/*/apps/ recorded — confirms the (predicted) absence of any installed org.lightningjim.MusicStreamer.desktop and the (predicted) presence of stale org.example.MusicStreamer.desktop"
    - "Install marker absence confirmed (~/.local/share/musicstreamer/.desktop-installed-v1)"
    - "MPRIS bus name readout (busctl --user list | grep musicstreamer) recorded — baseline for D-04 unchanged-bus-name verification"
  artifacts:
    - path: ".planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md"
      provides: "PRE-FIX diagnostic readback for D-14 and BEFORE/AFTER delta artifact"
      contains: "## PRE-FIX"
  key_links:
    - from: "Plan 61-01 (this) PRE-FIX section"
      to: "Plan 61-04 POST-FIX section (appended later)"
      via: "Same file 61-DIAGNOSTIC-LOG.md, section headers"
      pattern: "## PRE-FIX -> ## POST-FIX in same .md file"
---

<objective>
Capture the BEFORE state of Kyle's X11 rig per D-14 (diagnose-first, Phase 56 pattern) into `61-DIAGNOSTIC-LOG.md`. No code changes. This plan is human-driven: Kyle runs the diagnostic command set on his X11 rig and pastes the output into the log file. Output is the artifact; subsequent plans reference it for the BEFORE/AFTER delta.

Purpose: Lock in concrete BEFORE readouts so Plan 04's UAT can show a clean delta and the SUMMARY documents the bug's actual on-rig manifestation (placeholder app_id in `_GTK_APPLICATION_ID`, missing renamed `.desktop`, etc.). Per D-15, the fix ships regardless of what the diagnostic finds — but the artifact is required for D-14 + D-16 sign-off.

Output: `.planning/phases/61-.../61-DIAGNOSTIC-LOG.md` with a complete `## PRE-FIX` section. The `## POST-FIX` section is appended in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-CONTEXT.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-RESEARCH.md
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kyle (operator) -> shell command output | Operator runs read-only diagnostic commands; no untrusted input flows back into the codebase |
| Diagnostic log file -> repo | Pasted command output written to `.planning/` (gitignored per project memory); no runtime code consumes the log |

## STRIDE Threat Register

Per RESEARCH.md § Security Domain — this plan ships NO code, only diagnostic readouts. Threat surface is limited to the act of running enumerated commands. All threats from RESEARCH.md § Security Domain (path traversal, symlink race, subprocess injection, marker race, marker info-disclosure) apply to Plans 02/03 (code-bearing), not this plan.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Information Disclosure | Diagnostic log file | accept | Log lives under `.planning/` which is gitignored project-wide (commit `0c58595` post-cookie-leak scrub). No secrets are captured by the command set (no env-var dumps, no token files read, only window-property + filesystem-listing output). Kyle reviews before pasting. |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Kyle runs the BEFORE diagnostic on his X11 rig and pastes output into 61-DIAGNOSTIC-LOG.md</name>
  <files>
    .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
  </files>
  <read_first>
    - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-CONTEXT.md (D-14 diagnostic intent)
    - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-RESEARCH.md (Example 6 — full diagnostic command set, lines ~772–855; Pitfall 8 — Wayland-vs-X11 caveat)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-03-DIAGNOSTIC-LOG.md (Phase 56 artifact convention — header style, command-then-output blocks)
  </read_first>
  <what-built>
    Nothing yet — this task IS the diagnostic capture step. Code change ships in Plans 02/03.
  </what-built>
  <how-to-verify>
    Kyle runs the following commands on his X11 rig (per RESEARCH.md Example 6) and pastes output verbatim into a NEW file at `.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md`. The file MUST start with this exact frontmatter+header skeleton (Claude creates the skeleton; Kyle fills in the command outputs):

    ```markdown
    ---
    phase: 61
    plan: 01
    captured_by: Kyle
    captured_on: <YYYY-MM-DD>
    rig: <e.g., Linux X11 DPR=1.0 dev rig>
    ---

    # Phase 61 — Linux WM Display-Name Diagnostic Log

    Per D-14 (diagnose-first, Phase 56 pattern). PRE-FIX captured before Plans 02/03 ship the rename + self-install. POST-FIX appended by Plan 04 after the code change is in and `uv run musicstreamer` has been launched once to fire the self-install routine.

    ## PRE-FIX

    Captured: <YYYY-MM-DD HH:MM>

    ### Step 1: Session type
    ```
    $ echo "XDG_SESSION_TYPE=$XDG_SESSION_TYPE"
    <PASTE OUTPUT>
    ```
    Note: gate is `x11`. If output is `wayland`, log out, select "GNOME on Xorg" at the login screen, log back in, re-run from Step 1. Per RESEARCH.md Pitfall 8.

    ### Step 2: GNOME Shell version
    ```
    $ gnome-shell --version
    <PASTE OUTPUT>
    ```

    ### Step 3: Running window WM_CLASS + GTK/KDE app-id properties
    Launch `uv run musicstreamer` in another terminal, wait for window, then:
    ```
    $ WID=$(xdotool search --name MusicStreamer | head -1)
    $ echo "Window ID: $WID"
    $ xprop -id "$WID" WM_CLASS WM_NAME _NET_WM_NAME _GTK_APPLICATION_ID _KDE_NET_WM_DESKTOP_FILE
    <PASTE OUTPUT>
    ```
    PRE-FIX expectation per RESEARCH.md Example 6:
    - WM_CLASS = "musicstreamer", "MusicStreamer"
    - _GTK_APPLICATION_ID = "org.example.MusicStreamer"  ← THE BUG
    - _KDE_NET_WM_DESKTOP_FILE = "org.example.MusicStreamer"

    ### Step 4: Installed .desktop files in user XDG path
    ```
    $ ls -la ~/.local/share/applications/ 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```
    Per RESEARCH.md Pitfall 2, expected to show a stale `org.example.MusicStreamer.desktop` and (predicted) absence of `org.lightningjim.MusicStreamer.desktop`. Note both for Kyle's awareness — D-11 declines stale-file cleanup, so the duplicate will appear AFTER the fix; Kyle can `rm` manually if desired.

    ### Step 5: Installed .desktop files in system XDG path
    ```
    $ ls -la /usr/share/applications/ 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```

    ### Step 6: Installed icons
    ```
    $ ls -la ~/.local/share/icons/hicolor/*/apps/ 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```
    Per RESEARCH.md Runtime State Inventory, icons may already be present in 64/128/256 buckets from a prior manual install. The Plan 03 install routine is idempotent and will skip if the 256x256 icon already exists.

    ### Step 7: Install marker file (predicted absent)
    ```
    $ ls -la ~/.local/share/musicstreamer/.desktop-installed-v1 2>/dev/null
    $ echo "exit=$?"
    <PASTE OUTPUT>
    ```
    Expect "No such file or directory" / non-zero exit — Plan 03 hasn't run yet.

    ### Step 8: MPRIS bus name baseline (D-04 unchanged check)
    With app running:
    ```
    $ busctl --user list 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```
    Expect: `org.mpris.MediaPlayer2.musicstreamer` present (D-04: bus name is the MPRIS spec convention, NOT the reverse-DNS app_id; Plan 02 does NOT rename it).

    ### Step 9: Repo grep for org.example literal (drift baseline)
    From repo root:
    ```
    $ cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -rn "org\.example" --include="*.py" --include="Makefile" --include="*.desktop" . | grep -v "\.planning/" | grep -v "\.git/"
    <PASTE OUTPUT>
    ```
    Expected per RESEARCH.md Open Question #10:
    - musicstreamer/__main__.py:144
    - musicstreamer/constants.py:17
    - musicstreamer/media_keys/mpris2.py:104
    - Makefile lines 5/6/32/43
    - org.example.MusicStreamer.desktop (the file itself; renamed by Plan 02)

    Five drift sites. After Plan 02, this grep MUST return empty.

    ### Notes / observations
    <Kyle adds any rig-specific notes here, e.g., "had to log out of Wayland and choose GNOME on Xorg first", or screenshots of the current Activities thumbnail showing the placeholder name>
    ```

    The executor (Claude) creates the skeleton via `Write`, then sits at this checkpoint waiting for Kyle to fill in command outputs. Kyle's signal that he's done is "approved" or "diagnostic captured".
  </how-to-verify>
  <verify>
    <automated>grep -q "^## PRE-FIX" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
    - grep -q "^## PRE-FIX" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
    - grep -q "XDG_SESSION_TYPE" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
    - grep -q "_GTK_APPLICATION_ID" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
    - grep -q "busctl" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
    - grep -q "Step 9" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
    - File contains output for all 9 numbered steps (Kyle pasted command output, not just the placeholders)
  </acceptance_criteria>
  <action>
    Create the diagnostic log skeleton at `.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md` using the exact template above (Write tool). Then surface a checkpoint:

    > **HUMAN ACTION REQUIRED — Phase 61 Plan 01 diagnostic capture**
    >
    > Kyle, please run the 9 numbered command blocks in the file
    > `.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md`
    > on your X11 rig and paste the verbatim output where each `<PASTE OUTPUT>` placeholder is.
    >
    > If `XDG_SESSION_TYPE` is `wayland`, log out, choose "GNOME on Xorg" at the login screen, log back in, then re-run from Step 1 (per RESEARCH.md Pitfall 8 — `xprop` does not work on native Wayland windows).
    >
    > When done, save the file and reply "approved" to continue.

    Do NOT proceed to Plans 02–04 until Kyle confirms.
  </action>
  <resume-signal>Type "approved" once 61-DIAGNOSTIC-LOG.md PRE-FIX section is complete with command output pasted in. Or describe issues if Kyle hits a blocker (e.g., can't switch off Wayland, app fails to launch).</resume-signal>
  <done>
    61-DIAGNOSTIC-LOG.md exists with `## PRE-FIX` section populated by Kyle's verbatim command output for all 9 numbered steps. No code changes shipped. Subsequent plans (02/03) can now proceed.
  </done>
</task>

</tasks>

<verification>
Single-task plan; verification is the task's acceptance_criteria. The diagnostic log file is the artifact. Plans 02/03 do not depend on this plan's output for code (they ship regardless per D-15) — they depend on it for the SUMMARY's BEFORE/AFTER delta narrative.
</verification>

<success_criteria>
- `61-DIAGNOSTIC-LOG.md` exists with `## PRE-FIX` section
- All 9 numbered diagnostic steps have output pasted in (no remaining `<PASTE OUTPUT>` placeholders)
- Session type confirmed as `x11` (or Kyle's note explains the X11/Wayland accommodation he made)
- Kyle has typed "approved" so subsequent plans can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-01-SUMMARY.md` summarizing:
- Captured BEFORE state (1-2 paragraphs paraphrasing the diagnostic readouts)
- Confirmed/refuted predictions (was `_GTK_APPLICATION_ID` actually `org.example.MusicStreamer`? was the install marker absent? were stale `.desktop` files present? was the org.example grep yield exactly the 5 expected drift sites?)
- Notes for Plan 02 (any drift site found that wasn't in RESEARCH.md Open Question #10)
- Notes for Plan 04 UAT (any rig-specific quirks Kyle hit, e.g., Wayland-to-X11 switch, app launch friction)
</output>
