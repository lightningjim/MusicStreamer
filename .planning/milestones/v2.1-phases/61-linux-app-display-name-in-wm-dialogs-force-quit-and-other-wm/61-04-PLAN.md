---
phase: 61
slug: linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm
plan: 04
type: execute
wave: 4
depends_on: ["61-01", "61-02", "61-03"]
files_modified:
  - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
autonomous: false
requirements: [BUG-08]

must_haves:
  truths:
    - "Kyle launches `uv run musicstreamer` once after Plans 02+03 land — first-launch self-install fires"
    - "GNOME force-quit dialog reads 'MusicStreamer' on Kyle's Wayland (GNOME Shell) rig (D-16 gate; SC #1)"
    - "Activities/Alt-Tab show 'MusicStreamer' (SC #2)"
    - "_GTK_APPLICATION_ID and _KDE_NET_WM_DESKTOP_FILE on the running window read 'org.lightningjim.MusicStreamer' (POST-FIX)"
    - "MPRIS bus name remains 'org.mpris.MediaPlayer2.musicstreamer' (D-04 unchanged; SC #3 amended)"
    - "Install marker present at ~/.local/share/musicstreamer/.desktop-installed-v1 — confirms self-install ran (RESEARCH §Pitfall 3 layered verification)"
    - "New .desktop file present at ~/.local/share/applications/org.lightningjim.MusicStreamer.desktop"
    - "61-DIAGNOSTIC-LOG.md has both ## PRE-FIX (from Plan 01) and ## POST-FIX (this plan) sections, capturing the BEFORE/AFTER delta"
  artifacts:
    - path: ".planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md"
      provides: "Complete BEFORE/AFTER UAT artifact with Kyle's Wayland-rig sign-off on D-16"
      contains: "## POST-FIX"
  key_links:
    - from: "Plan 02+03 code changes"
      to: "Force-quit dialog string 'MusicStreamer' on Kyle's rig"
      via: "GNOME ShellWindowTracker -> _GTK_APPLICATION_ID -> ~/.local/share/applications/org.lightningjim.MusicStreamer.desktop -> Name= field (RESEARCH §Pattern 1)"
      pattern: "_GTK_APPLICATION_ID -> .desktop -> Name="
---

<objective>
Run the on-rig UAT per D-14 + D-16 — the BUG-08 phase gate. Kyle launches the freshly-built app once (`uv run musicstreamer`) so Plan 03's self-install routine fires. Then he re-runs Plan 01's diagnostic command set and pastes the POST-FIX output into `61-DIAGNOSTIC-LOG.md`. He provokes the GNOME force-quit dialog (per RESEARCH §Example 6 step 9) and confirms it reads "MusicStreamer", appending the result to the same log file. Activities + Alt-Tab consistency check + MPRIS bus-name unchanged check round out the four success criteria.

Purpose: The phase is gated on this UAT. Plan 02 + 03 ship code that should structurally close BUG-08, but only the on-rig test can verify the GNOME Shell rendering. Per D-15, the code change ships regardless of what Plan 01's PRE-FIX diagnostic showed; this plan confirms the fix took effect.

Output: `61-DIAGNOSTIC-LOG.md` with a complete `## POST-FIX` section + Kyle's signoff line ("UAT PASSED — BUG-08 closed" or a description of what failed). If anything is broken, return to gap-closure mode (re-plan with `/gsd-plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-CONTEXT.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-RESEARCH.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-VALIDATION.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-01-SUMMARY.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-02-SUMMARY.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-03-SUMMARY.md
@.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
</context>

<threat_model>
## Trust Boundaries

Same as Plan 01 — read-only diagnostic + manual UAT. No code shipped. The UAT does run a freshly-built app from Kyle's checkout, but the binary is the same that the test suite green-lit in Plans 02/03; UAT is observation-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | Information Disclosure | UAT log readouts | accept | Same as T-61-01-01: `.planning/` is gitignored; readouts contain window properties + filesystem listings only; no secrets. Kyle reviews paste content. |
| T-61-04-02 | Tampering | False-positive UAT pass via Pitfall 3 (old .desktop coincidentally satisfies the lookup) | mitigate | RESEARCH §Pitfall 3: layered verification. Kyle MUST capture (a) the GNOME force-quit dialog text reads "MusicStreamer" — captured via screenshot since `xprop` does not work on native Wayland windows and `gdbus Eval` is locked on GNOME 50.1, AND (b) install marker is present at `~/.local/share/musicstreamer/.desktop-installed-v1` with the expected content, AND (c) the renamed `.desktop` file is present at `~/.local/share/applications/org.lightningjim.MusicStreamer.desktop`. Pasted into the POST-FIX section explicitly so a coincidental match via the old org.example file cannot disguise a missing self-install. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Kyle launches the rebuilt app once on his Wayland (GNOME Shell) rig, runs the POST-FIX diagnostic, provokes the force-quit dialog, and signs off in 61-DIAGNOSTIC-LOG.md</name>
  <files>
    .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md
  </files>
  <read_first>
    - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md (existing PRE-FIX section from Plan 01 — keep it intact, append below)
    - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-RESEARCH.md (Example 6 — full diagnostic command set, lines ~772–855; the same commands as PRE-FIX, with different EXPECTED outputs)
    - .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-RESEARCH.md (Pitfall 3 — layered verification: marker + .desktop file + _GTK_APPLICATION_ID must ALL be correct, not just the dialog string)
  </read_first>
  <what-built>
    Plans 02 and 03 shipped code:
    - constants.APP_ID literal renamed to `org.lightningjim.MusicStreamer`
    - `__main__.py` reads `constants.APP_ID` via `setDesktopFileName` and `_set_windows_aumid` default; adds `setApplicationDisplayName("MusicStreamer")`
    - `mpris2._MprisRootAdaptor.DesktopEntry` returns `constants.APP_ID`
    - Bundled `.desktop` renamed + relocated to `packaging/linux/org.lightningjim.MusicStreamer.desktop`
    - `Makefile` drift sites fixed
    - New `musicstreamer/desktop_install.py` ships first-launch self-install, called from `_run_gui` between `Gst.init(None)` and `migration.run_migration()`
    - `tests/test_constants_drift.py` (4 tests) + `tests/test_desktop_install.py` (6 tests) all green
    - Old `tests/test_aumid_string_parity.py` deleted (subsumed by drift guard)

    Now Kyle confirms the GNOME Shell rendering takes effect on his rig.
  </what-built>
  <how-to-verify>
    Kyle does the following on his Wayland (GNOME Shell) rig (in order). The executor (Claude) appends a POST-FIX skeleton to `61-DIAGNOSTIC-LOG.md` (preserving the existing PRE-FIX section), then waits for Kyle's confirmation.

    ## A. Launch once to fire the self-install routine

    ```
    cd /home/kcreasey/OneDrive/Projects/MusicStreamer
    uv run musicstreamer
    ```

    Wait for the main window. The `desktop_install.ensure_installed()` call ran during `_run_gui` startup; on first launch it should have:
    - Created `~/.local/share/applications/org.lightningjim.MusicStreamer.desktop` (if not already there from a hand-install)
    - Created/skipped `~/.local/share/icons/hicolor/256x256/apps/org.lightningjim.MusicStreamer.png` (RESEARCH §Runtime State Inventory notes Kyle's rig already has this)
    - Best-effort run `update-desktop-database` + `gtk-update-icon-cache`
    - Written marker `~/.local/share/musicstreamer/.desktop-installed-v1`

    Leave the app running for the steps below. (Or kill it and relaunch — the install is one-shot, the marker prevents re-install. Either way works.)

    ## B. Re-run the diagnostic command set (POST-FIX)

    Run all 9 numbered command blocks from Plan 01 again. Paste the verbatim output into the new POST-FIX section of `61-DIAGNOSTIC-LOG.md`. Compare each block's output to the PRE-FIX section to surface the BEFORE/AFTER delta.

    Per RESEARCH §Example 6, EXPECTED POST-FIX deltas:

    | Step | PRE-FIX (Plan 01) | POST-FIX (this plan) |
    |------|-------------------|----------------------|
    | 1 — XDG_SESSION_TYPE | wayland | wayland (unchanged) |
    | 2 — gnome-shell --version | GNOME Shell 50.1 | same |
    | 3 — gdbus app_id readback (`(false, '')` on GNOME 50.1 due to `Eval` lockout) — fall back to **force-quit dialog screenshot** as the binding signal | dialog read "org.example.MusicStreamer" (Plan 01 BEFORE shot) | dialog reads **"MusicStreamer"** ← FIX VERIFIED. If `gdbus Eval` happens to be enabled, JSON shows `gtk_app_id: "org.lightningjim.MusicStreamer"`. |
    | 4 — ~/.local/share/applications/ | shows org.example.MusicStreamer.desktop only | shows BOTH org.example.* AND **org.lightningjim.MusicStreamer.desktop** (D-11 additive) |
    | 6 — installed icons | already 64/128/256 buckets (Kyle's prior manual install) | same; 256x256 install was idempotent (no clobber per `if not icon_dst.exists()` guard) |
    | 7 — install marker | absent | **PRESENT** with content `desktop install v1 complete; app_id=org.lightningjim.MusicStreamer` |
    | 8 — busctl | `org.mpris.MediaPlayer2.musicstreamer` (if app running) | **same** (D-04 unchanged — SC #3 amended) |
    | 9 — repo grep `org.example` | 5 hits + 6 worktree hits (worktrees pruned during Plan 01) | **0 hits** (all drift sites resolved by Plan 02) |

    ## C. Provoke and verify the force-quit dialog (SC #1 — D-16 gate)

    From a second terminal, with the app running:

    ```bash
    kill -STOP $(pgrep -f musicstreamer)
    ```

    Click anywhere on the MusicStreamer window. After ~5s GNOME pops the "Application is not responding" dialog with Wait/Force Close.

    **Capture the dialog title-bar / app-name string.** Take a screenshot if convenient. The dialog should read **"MusicStreamer"** (or "MusicStreamer is not responding") — NOT "org.example.MusicStreamer", NOT a generic shell label.

    Resume the app (so it can be cleanly closed):
    ```bash
    kill -CONT $(pgrep -f musicstreamer)
    ```

    Close the app cleanly via the window's X button.

    Append to POST-FIX:
    ```
    ### Step 9 (force-quit dialog UAT — SC #1):
    Dialog text: "<verbatim string from the dialog>"
    Expected: "MusicStreamer" (D-16 gate)
    Result: PASS / FAIL
    Screenshot: <path or note>
    ```

    ## D. Activities / Alt-Tab consistency (SC #2)

    Relaunch the app: `uv run musicstreamer`.

    With the window present:
    - Press the Super key to open Activities. Hover the MusicStreamer thumbnail. Confirm tooltip / overlay reads "MusicStreamer".
    - Alt-Tab through windows. Confirm the switcher entry reads "MusicStreamer".

    Append:
    ```
    ### Step 10 (Activities + Alt-Tab UAT — SC #2):
    Activities tile name: "<verbatim>"
    Alt-Tab switcher name: "<verbatim>"
    Expected: "MusicStreamer" in both
    Result: PASS / FAIL
    Screenshot: <path or note>
    ```

    ## E. Layered verification (Pitfall 3 false-positive guard)

    Confirm THREE conditions all hold:
    1. Force-quit dialog text reads "MusicStreamer" (Step 9 screenshot above) — on Wayland this is the user-facing binding signal
    2. Install marker exists: `test -f ~/.local/share/musicstreamer/.desktop-installed-v1 && echo PRESENT`
    3. New `.desktop` file exists: `test -f ~/.local/share/applications/org.lightningjim.MusicStreamer.desktop && echo PRESENT`

    If any one is missing, the dialog could be reading the OLD `.desktop` (`org.example.MusicStreamer.desktop`) coincidentally because both files declare `Name=MusicStreamer`. RESEARCH §Pitfall 3 calls this out specifically — all three must be PRESENT for a real pass.

    Append:
    ```
    ### Step 11 (Pitfall 3 layered verification):
    force-quit dialog reads "MusicStreamer": PASS / FAIL
    install marker present: PASS / FAIL
    new .desktop file present: PASS / FAIL
    All three required for true PASS.
    ```

    ## F. SC #4 (X11 note)

    Per CONTEXT.md (amended) and project memory, Kyle's deployment is Wayland — X11 is NOT a deployment target and NOT a UAT gate. Kyle records a one-line memo for completeness:

    ```
    ### Step 12 (SC #4 — X11 memo):
    XDG_SESSION_TYPE during UAT: wayland (expected)
    X11 behavior: <not tested — out of scope per CONTEXT.md amendment>
    ```

    ## G. Sign-off

    Final line of the POST-FIX section:
    ```
    ### Sign-off
    UAT date: <YYYY-MM-DD>
    Operator: Kyle
    Status: PASSED  (or FAILED — describe issue and route to /gsd-plan-phase --gaps)
    BUG-08: closed (or still-open)
    ```

    ## Skeleton the executor writes to the file BEFORE handing off

    The executor uses `Edit` to APPEND (not overwrite) this skeleton at the bottom of `61-DIAGNOSTIC-LOG.md`, preserving the Plan 01 PRE-FIX section verbatim:

    ```markdown

    ---

    ## POST-FIX

    Captured: <YYYY-MM-DD HH:MM>
    After Plans 02+03 shipped (commits: `<sha-02>`, `<sha-03>`).

    ### Step 1 (POST-FIX): Session type
    ```
    $ echo "XDG_SESSION_TYPE=$XDG_SESSION_TYPE"
    <PASTE OUTPUT>
    ```

    ### Step 2 (POST-FIX): GNOME Shell version
    ```
    $ gnome-shell --version
    <PASTE OUTPUT>
    ```

    ### Step 3 (POST-FIX): Window app_id readback (Wayland-native via gdbus)
    ```
    $ gdbus call --session \
        --dest org.gnome.Shell \
        --object-path /org/gnome/Shell \
        --method org.gnome.Shell.Eval \
        'JSON.stringify(global.get_window_actors().map(a => a.meta_window).filter(w => w.title && w.title.includes("MusicStreamer")).map(w => ({title: w.title, wm_class: w.get_wm_class(), gtk_app_id: w.get_gtk_application_id()})))'
    <PASTE OUTPUT>
    ```
    Expected (if Eval enabled): `gtk_app_id: "org.lightningjim.MusicStreamer"` (changed from PRE-FIX placeholder). On GNOME 50.1 Eval is locked → returns `(false, '')` → fall back to the force-quit screenshot in Step 9 as the binding signal. The screenshot is what actually validates SC #1; gdbus is a quantitative bonus when available.

    ### Step 4 (POST-FIX): User XDG applications
    ```
    $ ls -la ~/.local/share/applications/ 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```
    Expected: BOTH org.example.MusicStreamer.desktop AND org.lightningjim.MusicStreamer.desktop present (D-11 additive).

    ### Step 5 (POST-FIX): System XDG applications
    ```
    $ ls -la /usr/share/applications/ 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```

    ### Step 6 (POST-FIX): Installed icons
    ```
    $ ls -la ~/.local/share/icons/hicolor/*/apps/ 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```

    ### Step 7 (POST-FIX): Install marker (must be present)
    ```
    $ ls -la ~/.local/share/musicstreamer/.desktop-installed-v1 2>/dev/null
    $ cat ~/.local/share/musicstreamer/.desktop-installed-v1 2>/dev/null
    <PASTE OUTPUT>
    ```
    Expected: file exists; content is `desktop install v1 complete; app_id=org.lightningjim.MusicStreamer`.

    ### Step 8 (POST-FIX): MPRIS bus name (D-04 unchanged)
    ```
    $ busctl --user list 2>/dev/null | grep -i music
    <PASTE OUTPUT>
    ```
    Expected: org.mpris.MediaPlayer2.musicstreamer (unchanged from PRE-FIX).

    ### Step 9 (POST-FIX): Force-quit dialog UAT — SC #1
    Steps:
    1. Launch `uv run musicstreamer`. Wait for window.
    2. From second terminal: `kill -STOP $(pgrep -f musicstreamer)`
    3. Click on the window. After ~5s the dialog appears.
    4. Capture the dialog string + screenshot.
    5. `kill -CONT $(pgrep -f musicstreamer)` and close cleanly.

    Dialog text: <verbatim>
    Expected: "MusicStreamer" (D-16 gate)
    Result: PASS / FAIL
    Screenshot: <path or note>

    ### Step 10 (POST-FIX): Activities + Alt-Tab — SC #2
    Activities tile name: <verbatim>
    Alt-Tab switcher name: <verbatim>
    Expected: "MusicStreamer"
    Result: PASS / FAIL
    Screenshot: <path or note>

    ### Step 11 (POST-FIX): Layered verification (Pitfall 3)
    force-quit dialog reads "MusicStreamer": PASS / FAIL
    install marker present: PASS / FAIL
    new .desktop file present: PASS / FAIL

    ### Step 12 (POST-FIX): SC #4 — X11 memo
    XDG_SESSION_TYPE during UAT: wayland (expected)
    X11 behavior: <not tested — out of scope per CONTEXT.md amendment>

    ### Step 13 (POST-FIX): Repo drift grep (Plan 02 sanity)
    ```
    $ grep -rn "org\.example" --include="*.py" --include="Makefile" --include="*.desktop" . 2>/dev/null | grep -v "\.planning/" | grep -v "\.git/"
    $ echo "exit=$?"
    <PASTE OUTPUT>
    ```
    Expected: ZERO hits (Plan 02 closed all drift sites).

    ### Sign-off
    UAT date: <YYYY-MM-DD>
    Operator: Kyle
    Status: <PASSED | FAILED>
    BUG-08: <closed | still-open>
    Notes: <e.g., "duplicate Activities entry due to D-11 declined cleanup; Kyle removed manually" or "Test 9 dialog showed correct name, screenshot at...">
    ```

    The executor surfaces the checkpoint:

    > **HUMAN ACTION REQUIRED — Phase 61 Plan 04 UAT**
    >
    > Kyle, please:
    > 1. `uv run musicstreamer` once to fire the first-launch self-install.
    > 2. Run the 13 numbered command blocks in the new POST-FIX section of
    >    `.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md`,
    >    pasting verbatim output where each `<PASTE OUTPUT>` placeholder is.
    > 3. Provoke the force-quit dialog (Step 9) and confirm it reads "MusicStreamer".
    > 4. Confirm Activities + Alt-Tab consistency (Step 10).
    > 5. Verify the layered Pitfall 3 conditions (Step 11) — all three must be PRESENT.
    > 6. Sign off at the bottom.
    >
    > When done, save the file. Reply "approved" if BUG-08 is closed, or describe what failed for `/gsd-plan-phase --gaps` to pick up.

    Do NOT mark the phase complete until Kyle approves.
  </how-to-verify>
  <verify>
    <automated>grep -q "^## POST-FIX" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md && grep -q "^### Sign-off" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## PRE-FIX" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md` succeeds (Plan 01 section preserved)
    - `grep -q "^## POST-FIX" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md` succeeds (this plan's append)
    - `grep -q "^### Sign-off" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md` succeeds
    - `grep -q "Status: PASSED" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md` succeeds (gate; if FAILED, route to gap closure)
    - `grep -q "BUG-08: closed" .planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-DIAGNOSTIC-LOG.md` succeeds
    - All 13 numbered POST-FIX step headers are present (`Step 1 (POST-FIX)` through `Step 13 (POST-FIX)`); none has remaining `<PASTE OUTPUT>` placeholder text
    - Step 9 (force-quit) records dialog text containing "MusicStreamer" (D-16 gate verbatim)
    - Step 11 (layered) records all three conditions as PASS
  </acceptance_criteria>
  <action>
    Use `Edit` (NOT Write — preserve the Plan 01 PRE-FIX section) to APPEND the POST-FIX skeleton to `.planning/phases/61-.../61-DIAGNOSTIC-LOG.md`. Then sit at the checkpoint and wait for Kyle to fill in command outputs + sign off.

    If Kyle reports FAILED status, document the specific failure mode in the Sign-off Notes line, then return control to the user with a recommendation to run `/gsd-plan-phase --gaps` to author Plan 05 closure.
  </action>
  <resume-signal>Type "approved" once 61-DIAGNOSTIC-LOG.md POST-FIX section is complete with PASSED sign-off, OR describe the failure (e.g., "force-quit still shows org.example", "marker missing despite running app once") so the gap closure plan can pick it up.</resume-signal>
  <done>
    `61-DIAGNOSTIC-LOG.md` has both `## PRE-FIX` (from Plan 01) and `## POST-FIX` (from this plan) sections, with all 13 POST-FIX numbered steps populated by Kyle's verbatim output. The Sign-off line reads "Status: PASSED" / "BUG-08: closed". The phase is ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<verification>
- `61-DIAGNOSTIC-LOG.md` contains both `## PRE-FIX` and `## POST-FIX` sections (must_haves §"61-DIAGNOSTIC-LOG.md has both BEFORE and AFTER sections")
- Step 9 dialog string contains "MusicStreamer" verbatim (SC #1 gate)
- Step 10 Activities/Alt-Tab strings both read "MusicStreamer" (SC #2)
- Step 11 layered verification — all three conditions PASS (Pitfall 3 false-positive guard)
- Step 13 repo drift grep returns ZERO hits (Plan 02 closure verification)
- Sign-off line reads "Status: PASSED" / "BUG-08: closed"
</verification>

<success_criteria>
- All four ROADMAP success criteria attested:
  - SC #1: Force-quit dialog shows "MusicStreamer" (Step 9)
  - SC #2: Activities/Alt-Tab show "MusicStreamer" (Step 10)
  - SC #3 (amended): App ID migrated to `org.lightningjim.MusicStreamer`; D-Bus interface names + MPRIS bus name unchanged (Step 8 busctl + Step 13 grep)
  - SC #4: Wayland confirmed (the actual deployment surface); X11 memoed as out-of-scope per CONTEXT.md amendment (Step 12)
- Pitfall 3 layered verification (Step 11) prevents the "old .desktop coincidentally satisfies the lookup" false positive
- Kyle's signoff line is present
- BUG-08 closed (or, if FAILED, the failure mode is documented for `/gsd-plan-phase --gaps`)
</success_criteria>

<output>
After completion, create `.planning/phases/61-linux-app-display-name-in-wm-dialogs-force-quit-and-other-wm/61-04-SUMMARY.md` summarizing:
- POST-FIX delta vs PRE-FIX (the four BEFORE/AFTER changes that confirm the fix)
- Force-quit dialog readback (Step 9)
- Activities/Alt-Tab readback (Step 10)
- Layered verification results (Step 11)
- Whether Kyle removed the duplicate `org.example.MusicStreamer.desktop` from Activities (D-11 declined cleanup as a phase responsibility, but Kyle may do it manually)
- Sign-off + BUG-08 status (closed/still-open)
- Phase 61 close-out: ready for `/gsd-verify-work` and `/gsd-complete-phase`
</output>
