---
phase: 76
plan: 05
type: execute
wave: 3
depends_on:
  - 76-01
  - 76-02
  - 76-03
  - 76-04
files_modified: []
autonomous: false
requirements:
  - GBS-AUTH-01

must_haves:
  truths:
    - "User launches the app, opens AccountsDialog, clicks `[Connect to GBS.FM…]`, completes a real gbs.fm login in the QtWebEngine subprocess, and observes the dialog flip to `Connected` with cookies file written to `~/.local/share/musicstreamer/gbs-cookies.txt`."
    - "After connect, `os.stat(~/.local/share/musicstreamer/gbs-cookies.txt).st_mode & 0o777 == 0o600`."
    - "User clicks `[Disconnect]`, confirms in the dialog, observes the cookies file removed AND status flips to `Not connected`."
    - "User clicks `[Connect to GBS.FM…]` again, waits 120 seconds without logging in, observes the category-aware failure dialog with `LoginTimeout` copy."
    - "User clicks `[Import cookies file…]` in the not-connected state and the existing `CookieImportDialog` (Phase 60 File/Paste tabs) opens unchanged."
    - "`paths.oauth_log_path()` contains JSON-line entries for the GBS subprocess events with `provider=\"gbs\"` (NOT `\"twitch\"`)."
    - "An existing gbs-cookies.txt from Phase 60 vintage continues to work after the upgrade (additive feature — D-03 confirms)."
  artifacts: []
  key_links: []
---

<objective>
Manually verify Phase 76's end-to-end behavior on a live Linux Wayland session. Runs after all autonomous plans (76-01 through 76-04) have landed and the full test suite is green. This plan documents the manual checkpoints; the user actually performs the gbs.fm login.

Purpose: Goal-backward must-haves #1, #4, #5 cannot be verified by unit tests alone — they require a real QtWebEngine subprocess + a real gbs.fm Django session. Plan 76-05 is the closure gate for GBS-AUTH-01.

Output: A signed `76-05-SUMMARY.md` documenting each verification step's pass/fail status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-CONTEXT.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-VALIDATION.md

# Upstream plans landed before this:
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-01-SUMMARY.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-02-SUMMARY.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-03-SUMMARY.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-04-SUMMARY.md
</context>

<interfaces>
<!-- The user-facing surface this plan verifies. -->

App launch command (Linux dev workflow): `python -m musicstreamer` (or whatever the project's launch entry point is — confirm via `pyproject.toml [project.scripts]`).

Paths referenced during verification:

- `~/.local/share/musicstreamer/gbs-cookies.txt` (the cookies file path returned by `musicstreamer.paths.gbs_cookies_path()`).
- `~/.local/share/musicstreamer/oauth.log` (the OAuthLogger path returned by `musicstreamer.paths.oauth_log_path()`).

Pre-test cleanup (BEFORE step 1 of step #1 below): if the user has an existing `gbs-cookies.txt` they want to preserve, copy it aside first. Step #7 (existing-user invariant) restores and re-tests it.

Verification environment:

- Linux Wayland session (GNOME Shell), DPR=1.0 — per project memory `project_deployment_target.md`. Verify via `echo $XDG_SESSION_TYPE` should return `wayland`.
- Live network access to `https://gbs.fm/accounts/login/`.
- Valid gbs.fm credentials (username + password). Plan 76-05 does NOT require the dev-fixture API key.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live happy-path login flow</name>
  <what-built>
    Plan 76-01 + 76-03 have shipped:
    - `oauth_helper.py --mode gbs` subprocess loads `https://gbs.fm/accounts/login/` in QtWebEngine, auto-completes on sessionid + csrftoken cookies.
    - `AccountsDialog` connect button text is now `"Connect to GBS.FM…"`; clicking it launches the subprocess.
    - On success the cookies file is written to `~/.local/share/musicstreamer/gbs-cookies.txt` with `0o600` permissions.
    - Status flips to `Connected` (or `Connected (cookies)` — verify which one Plan 76-03 picked).
    - Secondary `[Import cookies file…]` button visible only when not connected.
  </what-built>
  <how-to-verify>
    Pre-step: if you have an existing `~/.local/share/musicstreamer/gbs-cookies.txt`, copy it aside: `cp ~/.local/share/musicstreamer/gbs-cookies.txt /tmp/gbs-cookies.bak`. Then remove the canonical copy so the dialog starts in the "Not connected" state: `rm ~/.local/share/musicstreamer/gbs-cookies.txt`.

    Step 1: Launch the app. Expected: app opens; the AccountsDialog is reachable via the hamburger menu or whatever the existing UX is.

    Step 2: Open Accounts dialog. Expected: GBS.FM GroupBox shows status `"Not connected"`, primary button `"Connect to GBS.FM…"`, secondary `[Import cookies file…]` button visible.

    Step 3: Click `[Connect to GBS.FM…]`. Expected: a new QtWebEngine subprocess window opens with title `"GBS.FM Login"` (or similar), displaying `https://gbs.fm/accounts/login/`.

    Step 4: Enter your gbs.fm credentials in the form and submit. Expected: after the form submits (Django redirects to `/playlist` or `/` after login), the subprocess window auto-closes within a few seconds (because both `sessionid` and `csrftoken` cookies are now observed on the `gbs.fm` domain).

    Step 5: Back in AccountsDialog: status flips to `"Connected"` (or `"Connected (cookies)"` — note which one). Primary button changes to `"Disconnect"`. Secondary `[Import cookies file…]` button is hidden.

    Step 6: Verify on disk: `ls -la ~/.local/share/musicstreamer/gbs-cookies.txt` shows mode `-rw-------` (0o600). `head -1 ~/.local/share/musicstreamer/gbs-cookies.txt` outputs `# Netscape HTTP Cookie File`. `grep -E "(sessionid|csrftoken)" ~/.local/share/musicstreamer/gbs-cookies.txt` returns ≥ 2 lines.

    Step 7: Verify the OAuthLogger: `grep -E "provider.*gbs.*Success" ~/.local/share/musicstreamer/oauth.log` returns a JSON line with `provider="gbs"` and `category="Success"` (the timestamp should match the login you just completed).

    Step 8: Optional — exercise a real gbs.fm API call to confirm cookies work end-to-end. From the app, navigate to the GBS.FM search dialog (or whatever entry point the GBS active-playlist polling uses) and confirm vote / playlist features work. (This re-verifies the Phase 60 cookies-auth path is intact.)

    Report: paste the `ls -la` output for the cookies file, the `head -1` output, and the OAuthLogger JSON line into the resume message. Note which status label text Plan 76-03 picked (`"Connected"` vs `"Connected (cookies)"`).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass. Otherwise describe which step failed and what was observed.</resume-signal>
  <files>none (manual verification task — no source files modified)</files>
  <action>Follow each numbered step in the &lt;how-to-verify&gt; block above exactly. Report the requested observations in your resume message. Do not paste real cookie / session values into the resume message — only line counts, permission bits, category strings, and timestamps.</action>
  <verify>
    <human-check>User confirms each step's expected outcome matches actual observation per &lt;how-to-verify&gt;.</human-check>
  </verify>
  <done>User types "approved" in the resume signal AND the required artifacts (cookies file with 0o600, OAuthLogger entry with provider="gbs", etc.) are observable on disk per the step's instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Disconnect flow + secondary-button reachability</name>
  <what-built>
    - Plan 76-03's disconnect branch: `_on_gbs_action_clicked()` when connected shows confirmation dialog; on Yes, removes the cookies file with broader-OSError tolerance (Phase 60 HIGH 2 fix preserved).
    - Plan 76-03's secondary button: `[Import cookies file…]` opens the existing `CookieImportDialog` (Phase 60 D-04 ladder #3 unchanged), reachable in the not-connected state.
  </what-built>
  <how-to-verify>
    Pre-state: Task 1 completed; cookies file exists; status shows `Connected`.

    Step 1: Click `[Disconnect]`. Expected: a `QMessageBox.question` dialog appears with title `"Disconnect GBS.FM?"` and body text containing `"This will delete your saved GBS.FM cookies"` (verbatim Phase 60 copy preserved).

    Step 2: Click `Yes`. Expected: dialog closes. Status flips to `"Not connected"`. Primary button text reverts to `"Connect to GBS.FM…"`. Secondary `[Import cookies file…]` button becomes visible again.

    Step 3: Verify on disk: `ls ~/.local/share/musicstreamer/gbs-cookies.txt` returns "No such file or directory" (file deleted).

    Step 4: Test the No-path: re-establish cookies (repeat Task 1 steps 1-5 or restore from `/tmp/gbs-cookies.bak`). Click `[Disconnect]` again; click `No`. Expected: dialog closes; status stays `Connected`; cookies file still on disk; no toast fired.

    Step 5: With the dialog back in not-connected state (delete the cookies file again to set up), click `[Import cookies file…]`. Expected: the existing `CookieImportDialog` opens with the GBS.FM-flavored UI — title bar contains `"GBS.FM"`, the File tab + Paste tab are both present, no `[Connect with GBS.FM Login…]` button inside the import dialog (that button is only in the parent AccountsDialog).

    Step 6: Cancel out of the `CookieImportDialog` (Cancel button or window close). Expected: no state change in AccountsDialog; status still `Not connected`.

    Report: confirm steps 1-6 all pass. Note any UI deviations from the expected behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 steps pass. Otherwise describe the deviation.</resume-signal>
  <files>none (manual verification task — no source files modified)</files>
  <action>Follow each numbered step in the &lt;how-to-verify&gt; block above exactly. Report the requested observations in your resume message. Do not paste real cookie / session values into the resume message — only line counts, permission bits, category strings, and timestamps.</action>
  <verify>
    <human-check>User confirms each step's expected outcome matches actual observation per &lt;how-to-verify&gt;.</human-check>
  </verify>
  <done>User types "approved" in the resume signal AND the required artifacts (cookies file with 0o600, OAuthLogger entry with provider="gbs", etc.) are observable on disk per the step's instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 120s timeout failure path + category-aware dialog</name>
  <what-built>
    - Plan 76-01's `_GbsLoginWindow._TIMEOUT_MS = 120_000` and the `_on_timeout` callback emit `{"category": "LoginTimeout", "detail": "120s", "provider": "gbs"}` on stderr.
    - Plan 76-03's `_on_gbs_login_finished` failure path delegates to the extracted `_classify_and_show_failure(provider="gbs", ...)`.
    - The existing Phase 999.3 `_CATEGORY_LABELS` shows `"LoginTimeout"` → `"Login took too long (2 min)"` (provider-agnostic).
  </what-built>
  <how-to-verify>
    Pre-state: AccountsDialog in `Not connected` state (delete cookies file if needed).

    Step 1: Click `[Connect to GBS.FM…]`. Expected: subprocess window opens with the gbs.fm login form.

    Step 2: Do NOT log in. Let the subprocess sit idle.

    Step 3: Wait 120 seconds (2 minutes). Expected: at approximately the 2-minute mark, the subprocess window auto-closes.

    Step 4: AccountsDialog shows a category-aware failure dialog with text containing `"Login took too long (2 min)"` (the `_CATEGORY_LABELS["LoginTimeout"]` value) and a `[Retry]` button.

    Step 5: Verify the OAuthLogger: `tail -3 ~/.local/share/musicstreamer/oauth.log | grep -c "LoginTimeout.*gbs"` returns ≥ 1 — confirms the JSON event has `category="LoginTimeout"` AND `provider="gbs"` (NOT `"twitch"`).

    Step 6: Click `[Retry]` in the failure dialog. Expected: a new subprocess starts (the existing `_launch_oauth_subprocess` retry hook works for GBS too via the shared `_classify_and_show_failure` helper). Either complete login this time (auto-close + Connected) or just cancel out of the new subprocess window.

    Step 7 (optional — slow): instead of waiting 120s, you can simulate the WindowClosedBeforeLogin path. Click `[Connect to GBS.FM…]`, then immediately close the subprocess window (X button or Alt+F4). Expected: failure dialog appears with `"Login window was closed before completing"` copy. OAuthLogger gets a `WindowClosedBeforeLogin` event with `provider="gbs"`.

    Report: confirm timeout fires at ~2 min; failure dialog text matches `_CATEGORY_LABELS["LoginTimeout"]`; OAuthLogger entry has `provider="gbs"`.
  </how-to-verify>
  <resume-signal>Type "approved" if steps 1-6 pass (step 7 is optional). Otherwise describe what diverged.</resume-signal>
  <files>none (manual verification task — no source files modified)</files>
  <action>Follow each numbered step in the &lt;how-to-verify&gt; block above exactly. Report the requested observations in your resume message. Do not paste real cookie / session values into the resume message — only line counts, permission bits, category strings, and timestamps.</action>
  <verify>
    <human-check>User confirms each step's expected outcome matches actual observation per &lt;how-to-verify&gt;.</human-check>
  </verify>
  <done>User types "approved" in the resume signal AND the required artifacts (cookies file with 0o600, OAuthLogger entry with provider="gbs", etc.) are observable on disk per the step's instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Existing-user invariant (additive feature)</name>
  <what-built>
    D-03 confirmed: existing GBS users with a working `gbs-cookies.txt` are not migrated, not invalidated. The new login subprocess is purely additive — they keep playing voting/etc. as before.
  </what-built>
  <how-to-verify>
    Step 1: If you have a `/tmp/gbs-cookies.bak` backup from Task 1's pre-step, restore it: `cp /tmp/gbs-cookies.bak ~/.local/share/musicstreamer/gbs-cookies.txt && chmod 0600 ~/.local/share/musicstreamer/gbs-cookies.txt`. If you don't have a backup, complete a fresh login via Task 1 and stop there.

    Step 2: Restart the app (so the upgraded code re-reads the cookies file from disk). Open AccountsDialog. Expected: GBS.FM section shows status `Connected` (the pre-Phase-76 cookies are valid; no migration prompt, no re-auth required).

    Step 3: Without re-logging-in, attempt a real gbs.fm action (e.g. trigger a search or open the now-playing panel that polls `/ajax`). Expected: GBS features continue to work — the existing Phase 60 cookies-auth path through `gbs_api._open_with_cookies` is unchanged.

    Step 4: Confirm that NO new SQLite settings key `gbs_api_token` was introduced (D-03 invariant): from a Python REPL in the project, run `from musicstreamer.db import Repo; r = Repo(<db_path>); print(r.get_setting("gbs_api_token", "<DEFAULT>"))` and expect the literal `<DEFAULT>` to be returned (key was never created). Alternative: `sqlite3 ~/.local/share/musicstreamer/musicstreamer.sqlite3 "SELECT key FROM settings WHERE key = 'gbs_api_token';"` returns 0 rows. (The `musicstreamer.sqlite3` filename is the canonical DB path per project memory `reference_musicstreamer_db_schema.md`.)

    Report: confirm pre-Phase-76 cookies still authenticate; no `gbs_api_token` key in SQLite.
  </how-to-verify>
  <resume-signal>Type "approved" if steps 1-4 pass and no migration is required for existing cookies users. Otherwise describe what regressed.</resume-signal>
  <files>none (manual verification task — no source files modified)</files>
  <action>Follow each numbered step in the &lt;how-to-verify&gt; block above exactly. Report the requested observations in your resume message. Do not paste real cookie / session values into the resume message — only line counts, permission bits, category strings, and timestamps.</action>
  <verify>
    <human-check>User confirms each step's expected outcome matches actual observation per &lt;how-to-verify&gt;.</human-check>
  </verify>
  <done>User types "approved" in the resume signal AND the required artifacts (cookies file with 0o600, OAuthLogger entry with provider="gbs", etc.) are observable on disk per the step's instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Full test suite + RETROSPECTIVE.md candidate notes</name>
  <what-built>
    All 4 autonomous plans (76-01..76-04) have landed. Final regression sweep confirms nothing else broke.
  </what-built>
  <how-to-verify>
    Step 1: From the project root, run `python -m pytest -x -q --ignore=tests/test_main_window.py 2>&1 | tail -30`. Expected: exit code 0 (full suite green). Document any pre-existing failures (per `deferred-items.md` — Phase 62 FakePlayer drift, MPRIS DBus, etc.) and confirm they are NOT caused by Phase 76.

    Step 2: Specifically confirm the Phase 76 test files all pass: `python -m pytest tests/test_oauth_helper_gbs.py tests/test_oauth_helper_twitch.py tests/test_accounts_dialog.py::TestAccountsDialogGBS tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished -x -q`. Expected: exit code 0.

    Step 3: Source-grep gates for the anti-pitfalls (T-76-D4, RESEARCH §_emit_event Provider Field): from project root, all should return 0:

    ```
    grep -A 12 "^def _emit_event" musicstreamer/oauth_helper.py | grep -c '"provider": "twitch"'
    grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -cE '"provider":\s*"twitch"'
    grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -E "netscape_text.*\.strip\(\)" | wc -l
    grep -c "Import GBS.FM Cookies..." tests/test_accounts_dialog.py
    ```

    Step 4: Note any candidate items for RETROSPECTIVE.md:
    - Did the `_classify_and_show_failure` extraction reveal any structural debt worth flagging?
    - Did the `_emit_event` provider hardcode regression suggest a broader source-grep CI gate?
    - Did the subprocess flow reveal any QtWebEngine pitfalls on Wayland not covered by the spike-findings skill?

    Step 5: Update `.planning/REQUIREMENTS.md` to mark GBS-AUTH-01 as `Complete` once steps 1-3 all pass. (Add the row to the Traceability table if not already there.)

    Report: paste the `pytest -x -q` final line, the four grep-gate counts (all should be 0), and any retrospective candidate notes.
  </how-to-verify>
  <resume-signal>Type "approved" if all gates pass and REQUIREMENTS.md is updated. Otherwise describe what's still red.</resume-signal>
  <files>none (manual verification task — no source files modified)</files>
  <action>Follow each numbered step in the &lt;how-to-verify&gt; block above exactly. Report the requested observations in your resume message. Do not paste real cookie / session values into the resume message — only line counts, permission bits, category strings, and timestamps.</action>
  <verify>
    <human-check>User confirms each step's expected outcome matches actual observation per &lt;how-to-verify&gt;.</human-check>
  </verify>
  <done>User types "approved" in the resume signal AND the required artifacts (cookies file with 0o600, OAuthLogger entry with provider="gbs", etc.) are observable on disk per the step's instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → live gbs.fm | Real Django session; HTTPS to `gbs.fm`. User enters real credentials in the subprocess WebView. |
| Subprocess WebView → user's cookies file | `_validate_gbs_cookies` gates the write; `0o600` chmod immediately after. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-M1 | Information Disclosure | User's real session cookies leaked via screenshot / paste into PR | mitigate | Plan 76-05 explicitly asks the user NOT to paste actual cookie values into the resume message — only line counts, permission bits, and category strings. |
| T-76-M2 | Tampering | Existing `gbs-cookies.txt` accidentally clobbered during testing | mitigate | Task 1 pre-step explicitly asks to back up to `/tmp/gbs-cookies.bak` before starting. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No new package installs. |
</threat_model>

<verification>
Manual verification — see each task's `<how-to-verify>` block.

Final gate (Task 5):
```bash
python -m pytest -x -q --ignore=tests/test_main_window.py
# AND four anti-pitfall greps return 0
```
</verification>

<success_criteria>
- [ ] Task 1: live happy-path login works; cookies file written with 0o600; OAuthLogger has provider="gbs".
- [ ] Task 2: disconnect-Yes deletes file; disconnect-No is no-op; secondary import button opens existing CookieImportDialog.
- [ ] Task 3: 120s timeout fires; category-aware failure dialog shows `"Login took too long (2 min)"`; OAuthLogger entry has `provider="gbs"`.
- [ ] Task 4: pre-Phase-76 cookies still work; no `gbs_api_token` SQLite key introduced (D-03 invariant).
- [ ] Task 5: full test suite green; 4 anti-pitfall grep gates all return 0; REQUIREMENTS.md GBS-AUTH-01 marked Complete.
- [ ] No actual cookie/session values pasted into resume messages or commits.
</success_criteria>

<output>
Create `.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-05-SUMMARY.md` documenting each Task's outcome (pass / deviation note). Mark `GBS-AUTH-01` as Complete in REQUIREMENTS.md if all Tasks passed.
</output>
