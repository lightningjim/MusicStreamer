---
phase: 76
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_oauth_helper_gbs.py
autonomous: true
requirements:
  - GBS-AUTH-01

must_haves:
  truths:
    - "Failing-first stubs exist for every behavior Plan 76-01 ships: `--mode gbs` argparse, `_GBS_LOGIN_URL` constant, `_GBS_TRIGGER_COOKIES` constant, `_cookie_domain_matches_gbs` (6 cases), `_GbsLoginWindow` constructor + cookie-trigger + Netscape-flush + timeout + closeEvent, `_emit_event` provider-gbs regression."
    - "Tests use a real `_fake_cookie` MagicMock helper that matches `QNetworkCookie.name() returns bytes, .value() returns bytes, .domain() returns str` semantics (mirror tests/test_oauth_helper_twitch.py:73-80)."
    - "Tests defer all `musicstreamer.oauth_helper` symbol imports to inside the test functions (matching existing tests/test_oauth_helper_twitch.py shape) so collection does not fail before Plan 76-01 lands."
    - "Failing tests turn GREEN once Plan 76-01 tasks 1-4 are merged — they are not `pytest.fail` placeholders, they are real fixture-mirroring contracts."
  artifacts:
    - path: "tests/test_oauth_helper_gbs.py"
      provides: "RED test contract for Plan 76-01 deliverables"
      contains: "def test_gbs_"
  key_links:
    - from: "tests/test_oauth_helper_gbs.py"
      to: "musicstreamer/oauth_helper.py symbols (Plan 76-01 output)"
      via: "deferred imports inside each test function"
      pattern: "from musicstreamer.oauth_helper import"
---

<objective>
Create `tests/test_oauth_helper_gbs.py` with RED test stubs for every behavior contract Plan 76-01 ships. Tests are real fixture-mirroring contracts (NOT `pytest.fail` placeholders) so they automatically turn GREEN as Plan 76-01 tasks 1-4 merge. Mirror the existing `tests/test_oauth_helper_twitch.py` shape verbatim — same module-level structure, same `_fake_cookie` MagicMock helper, same deferred-import discipline.

Purpose: Wave 0 contract pins for the Plan 76-01 subprocess work. Per project memory `feedback_gstreamer_mock_blind_spot.md`: prefer real fixture contracts (real cookie domain strings, real Netscape format expectations, real JSON-line schema) over loose mocks that pass through any signal.

Output: A new `tests/test_oauth_helper_gbs.py` file with ≥ 13 tests covering the entire `_GbsLoginWindow` + helpers + `--mode gbs` argparse surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-CONTEXT.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md

# Mirror source for the test file shape:
@tests/test_oauth_helper_twitch.py
</context>

<interfaces>
<!-- Verbatim shapes the new test file mirrors. -->

From `tests/test_oauth_helper_twitch.py:1-15` (header + imports — Phase 76 file uses the same shape with one-line title swap):

```python
"""Unit tests for the GBS.FM cookie-harvest helper (Phase 76).

The full QWebEngine flow cannot be unit-tested headlessly (it requires a live
gbs.fm login), so these tests cover the pure-Python helpers:
- Cookie domain matching
- Trigger-cookie set logic (sessionid + csrftoken)
- Netscape flush format
- Structured stderr event schema with provider="gbs"
- Constants wiring
"""
from __future__ import annotations

import json
from unittest.mock import MagicMock
```

From `tests/test_oauth_helper_twitch.py:73-80` — `_fake_cookie` helper (Phase 76 reuses verbatim, no changes):

```python
def _fake_cookie(name: str, value: str, domain: str):
    """Construct a MagicMock that quacks like a QNetworkCookie for the narrow
    surface _on_cookie_added touches."""
    c = MagicMock()
    c.name.return_value = name.encode("utf-8")
    c.value.return_value = value.encode("utf-8")
    c.domain.return_value = domain
    return c
```

Symbols expected to exist after Plan 76-01 lands (these are the imports the new test file performs INSIDE each test function, NOT at module load — matching the existing Twitch test file's discipline):

- `musicstreamer.oauth_helper._PROVIDER`
- `musicstreamer.oauth_helper._GBS_LOGIN_URL`
- `musicstreamer.oauth_helper._GBS_TRIGGER_COOKIES`
- `musicstreamer.oauth_helper._cookie_domain_matches_gbs`
- `musicstreamer.oauth_helper._GbsLoginWindow`
- `musicstreamer.oauth_helper._emit_event`

Existing validator the round-trip test calls (`tests/test_oauth_helper_gbs.py::test_gbs_flush_produces_valid_netscape` exercises this):

- `musicstreamer.gbs_api._validate_gbs_cookies(text: str) -> bool` at `gbs_api.py:116-141` — accepts a Netscape-format string containing sessionid + csrftoken on a gbs.fm domain.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_oauth_helper_gbs.py with constants + domain-match + _emit_event provider tests</name>
  <files>tests/test_oauth_helper_gbs.py</files>
  <read_first>
    - tests/test_oauth_helper_twitch.py (entire file, 154 lines — shape Phase 76 mirrors verbatim with provider="gbs" substitution)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md (§File 3 Excerpts 3A-3D at lines ~860-998 — verbatim `_fake_cookie` helper + 6 domain-match adapted tests + provider-regression test + URL/trigger constant tests)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§Test Strategy lines 762-805 Group A test table)
  </read_first>
  <behavior>
    - Test infrastructure: `_fake_cookie` helper produces a MagicMock with `.name()` returning bytes, `.value()` returning bytes, `.domain()` returning str.
    - Constant tests:
      - `test_gbs_login_url_constant` — `_GBS_LOGIN_URL == "https://gbs.fm/accounts/login/"`.
      - `test_gbs_trigger_cookies_constant` — `_GBS_TRIGGER_COOKIES == frozenset({"sessionid", "csrftoken"})`.
    - Domain-match tests (6 cases, mirror PATTERNS.md Excerpt 3B adapted):
      - `test_cookie_domain_matches_gbs_dot` — True for `.gbs.fm`.
      - `test_cookie_domain_matches_gbs_www` — True for `www.gbs.fm`.
      - `test_cookie_domain_matches_gbs_bare` — True for `gbs.fm`.
      - `test_cookie_domain_matches_gbs_subdomain` — True for `sub.gbs.fm`.
      - `test_cookie_domain_rejects_lookalike_gbs` — False for `fakegbs.fm`.
      - `test_cookie_domain_rejects_subdomain_attack` — False for `gbs.fm.evil.com`.
    - Provider-regression test:
      - `test_gbs_emits_provider_gbs_field` — `monkeypatch.setattr(oauth_helper, "_PROVIDER", "gbs")` then call `_emit_event("Success", detail="")`; captured stderr JSON line has `event["provider"] == "gbs"` AND `event["category"] == "Success"`.
    - Twitch contract preservation:
      - `test_emit_event_default_provider_is_twitch` — without monkeypatching `_PROVIDER`, call `_emit_event("Success")`; captured stderr has `event["provider"] == "twitch"` (default fall-back invariant).
  </behavior>
  <action>
    Create `tests/test_oauth_helper_gbs.py` mirroring `tests/test_oauth_helper_twitch.py` shape verbatim. Per project memory `feedback_mirror_decisions_cite_source.md`: this is a clone of `tests/test_oauth_helper_twitch.py` (the entire file structure) — NOT a paraphrase. Cite that file path in the commit message.

    File structure:

    1. Module docstring (mirror lines 1-9 of Twitch file, swap "Twitch" → "GBS.FM", swap "twitch.tv login" → "gbs.fm login", swap test domain list to "sessionid + csrftoken").
    2. `from __future__ import annotations` (line 10 of Twitch file).
    3. `import json` + `from unittest.mock import MagicMock` (lines 12-13).
    4. `_fake_cookie(name, value, domain)` helper VERBATIM from `tests/test_oauth_helper_twitch.py:73-80` (mirror PATTERNS.md Excerpt 3A).
    5. Test functions — for each, defer the import of `musicstreamer.oauth_helper` symbols to INSIDE the function body (mirror Twitch file's pattern at lines 21-22, 50-51, 73-74, etc.).

    Test list for THIS task (others in Task 2):

    - `test_gbs_login_url_constant` — assert `_GBS_LOGIN_URL == "https://gbs.fm/accounts/login/"`.
    - `test_gbs_trigger_cookies_constant` — assert `_GBS_TRIGGER_COOKIES == frozenset({"sessionid", "csrftoken"})`.
    - `test_cookie_domain_matches_gbs_dot` — mirror PATTERNS Excerpt 3B adapted test 1.
    - `test_cookie_domain_matches_gbs_www` — adapted test 2.
    - `test_cookie_domain_matches_gbs_bare` — adapted test 3.
    - `test_cookie_domain_matches_gbs_subdomain` — assert True for `sub.gbs.fm` (subdomain via `.endswith(".gbs.fm")`).
    - `test_cookie_domain_rejects_lookalike_gbs` — adapted test 4 (`fakegbs.fm`).
    - `test_cookie_domain_rejects_subdomain_attack` — adapted test 5 (`gbs.fm.evil.com`).
    - `test_gbs_emits_provider_gbs_field` — PATTERNS Excerpt 3C adapted (monkeypatch `_PROVIDER="gbs"`, call `_emit_event("Success")`, assert stderr JSON has `provider="gbs"`).
    - `test_emit_event_default_provider_is_twitch` — without monkeypatching, call `_emit_event("Success")`; capture stderr JSON; assert `event["provider"] == "twitch"` (Twitch invariant preservation guard).

    All tests use `capsys` fixture for stderr capture (mirror `test_emit_event_writes_json_line_to_stderr` at Twitch line 20).

    Do NOT use `pytest.fail` placeholders. Each test runs the assertion against the imported symbol. Tests are RED until Plan 76-01 tasks 1, 2, 4 land (which then makes them GREEN).
  </action>
  <acceptance_criteria>
    - Source assertion: `test -f tests/test_oauth_helper_gbs.py` succeeds.
    - Source assertion: `grep -c "^def test_" tests/test_oauth_helper_gbs.py` returns ≥ 10 (the 10 tests listed above).
    - Source assertion: `grep -c "^def _fake_cookie" tests/test_oauth_helper_gbs.py` returns 1.
    - Source assertion: `grep -E "from musicstreamer\.oauth_helper import" tests/test_oauth_helper_gbs.py | head -1` shows the import is INSIDE a function body (not at module level — confirmed by line position > 1).
    - Source assertion: no `pytest.fail(` calls in the file: `grep -c "pytest.fail" tests/test_oauth_helper_gbs.py` returns 0.
    - Source assertion: `grep -c "monkeypatch" tests/test_oauth_helper_gbs.py` returns ≥ 1 (for `test_gbs_emits_provider_gbs_field`).
    - Behavior: collection succeeds — `python -m pytest tests/test_oauth_helper_gbs.py --collect-only -q` lists ≥ 10 tests, exits 0 even if Plan 76-01 hasn't merged (because imports are deferred).
    - Behavior: AFTER Plan 76-01 Task 1 + Task 2 + Task 4 merge, `python -m pytest tests/test_oauth_helper_gbs.py -x -q -k "constant or domain or emits_provider"` exits 0 (all constant + domain + provider tests pass).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_oauth_helper_gbs.py --collect-only -q</automated>
  </verify>
  <done>Test file created with ≥ 10 deferred-import tests covering constants, domain match, and provider-field regression; no `pytest.fail` placeholders; commit message cites `tests/test_oauth_helper_twitch.py` as the clone source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _GbsLoginWindow construction + cookie-trigger + Netscape-flush + argparse tests</name>
  <files>tests/test_oauth_helper_gbs.py</files>
  <read_first>
    - tests/test_oauth_helper_gbs.py (the file you just created in Task 1 — extend it)
    - tests/test_oauth_helper_twitch.py:118-154 (URL constant and argparse tests — adapted patterns)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md (§File 3 Excerpts 3D + 3E — login URL test + argparse test patterns)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§Test Strategy lines 762-805 — Group A test table; remaining tests `test_gbs_login_window_loads_login_url`, `test_gbs_trigger_fires_on_both_cookies`, `test_gbs_flush_produces_valid_netscape`, `test_gbs_timeout_emits_login_timeout`, `test_gbs_window_closed_before_login`, `test_argparse_accepts_mode_gbs`)
    - musicstreamer/gbs_api.py:116-141 (the existing `_validate_gbs_cookies` that `test_gbs_flush_produces_valid_netscape` calls)
  </read_first>
  <behavior>
    - `test_argparse_accepts_mode_gbs` — invoking the helper module's argparse with `--mode gbs` does not raise SystemExit(2). Approach: directly construct an `argparse.ArgumentParser` mirroring `main()`'s definition with `choices=["twitch","google","gbs"]` and call `parser.parse_args(["--mode","gbs"])`; assert `ns.mode == "gbs"`. (Subprocess invocation is heavier — the local-parser approach matches the granularity of Twitch's `test_oauth_helper_uses_same_login_url` at `tests/test_oauth_helper_twitch.py:118-125`.)
    - `test_gbs_login_window_constructor_smoke` — `_GbsLoginWindow` is importable + `_TIMEOUT_MS == 120_000`. Full construction is GUI-heavy and unit-tested via mocked components in subsequent tests.
    - `test_gbs_trigger_fires_on_both_cookies` — construct `_GbsLoginWindow` with mocked `QWebEngineView` / `QApplication` / `QTimer`; manually invoke `window._on_cookie_added(_fake_cookie("sessionid", "v1", ".gbs.fm"))` — assert `window._finished == False` and stdout still empty (only one of two trigger names). Then invoke `window._on_cookie_added(_fake_cookie("csrftoken", "v2", ".gbs.fm"))` — assert `window._finished == True` and captured stdout starts with `"# Netscape HTTP Cookie File"`.
    - `test_gbs_trigger_does_not_fire_on_only_sessionid` — only sessionid observed → `_finished == False`, no stdout.
    - `test_gbs_trigger_does_not_fire_on_only_csrftoken` — only csrftoken observed → `_finished == False`, no stdout.
    - `test_gbs_trigger_ignores_non_gbs_domain` — inject `_fake_cookie("sessionid", "v", "fakegbs.fm")` and `_fake_cookie("csrftoken", "v", "fakegbs.fm")` — `_finished == False`, no stdout (domain gate rejects before storage).
    - `test_gbs_flush_produces_valid_netscape` — round-trip: trigger flush with both cookies, capture stdout, call `gbs_api._validate_gbs_cookies(captured_stdout)`, assert returns True.
    - `test_gbs_flush_deduplicates_repeated_cookies` — inject the same (domain, name) twice (e.g. sessionid on `.gbs.fm` with two different values) plus csrftoken; resulting Netscape dump has only ONE line for sessionid (the last value wins) plus the `# Netscape HTTP Cookie File` header + one csrftoken line. Total ≥ 3 lines, sessionid appears exactly once in the cookie-data lines.
    - `test_gbs_timeout_emits_login_timeout` — invoke `window._on_timeout()` directly; captured stderr has JSON event with `category="LoginTimeout"`, `detail="120s"`, `provider="gbs"` (with `_PROVIDER` monkeypatched to `"gbs"`).
    - `test_gbs_window_closed_before_login` — invoke `window.closeEvent(MagicMock())` directly (NOT via Qt event loop); captured stderr has JSON event with `category="WindowClosedBeforeLogin"`, `provider="gbs"`.
    - `test_gbs_collects_all_gbs_cookies_not_just_triggers` — inject sessionid + csrftoken + a third gbs.fm cookie (e.g. `messages`); resulting Netscape dump contains all THREE cookies' names in the output (forward-compat for Django auxiliary cookies per RESEARCH.md lines 368-374).
  </behavior>
  <action>
    Extend `tests/test_oauth_helper_gbs.py` with the additional tests listed above. For tests that need to construct `_GbsLoginWindow` without bringing up a real Qt event loop:

    - Use `unittest.mock.patch` to patch `musicstreamer.oauth_helper.QWebEngineView`, `musicstreamer.oauth_helper.QApplication.quit`, `musicstreamer.oauth_helper.QApplication.exit`, and `musicstreamer.oauth_helper.QTimer` (so `singleShot` is a no-op and we can manually call `_on_timeout` / `_flush_cookies` / `closeEvent`).
    - Construct an instance via `_GbsLoginWindow.__new__(_GbsLoginWindow)` then manually initialize the attributes the methods touch (`_finished=False`, `_cookies=[]`, `_observed_names=set()`). This avoids the full `__init__` GUI plumbing while still exercising the pure-Python state-machine paths. Alternative: use `MagicMock(spec=_GbsLoginWindow)` and reach into the actual method via `_GbsLoginWindow._on_cookie_added(mock_instance, fake_cookie)` — pick whichever is cleaner per executor judgment. Both are acceptable per RESEARCH.md note "The full QWebEngine flow cannot be unit-tested headlessly".

    For `_emit_event` provider assertions: `monkeypatch.setattr(oauth_helper, "_PROVIDER", "gbs")` BEFORE invoking the method under test (Task 1 already exercised this pattern; reuse).

    For `test_argparse_accepts_mode_gbs`: construct a local `argparse.ArgumentParser` mirroring the choices list and parse `["--mode", "gbs"]`. Alternative: import `musicstreamer.oauth_helper.main` and `monkeypatch.setattr(sys, "argv", ["oauth_helper", "--mode", "gbs"])` — but that invokes `QApplication(sys.argv)` which is heavy. Stick to the local-parser approach.

    Do NOT add tests that require a live `gbs.fm` HTTP call — those are manual UAT only (Plan 76-05).

    Per project memory `feedback_mirror_decisions_cite_source.md`: each test that mirrors a Twitch counterpart cites the source line range in a `# Mirror: tests/test_oauth_helper_twitch.py:LL-LL` comment above the test function definition.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "^def test_" tests/test_oauth_helper_gbs.py` returns ≥ 21 (10 from Task 1 + 11 from Task 2 — exact count may be ≥ this depending on splitting).
    - Source assertion: `grep -c "test_gbs_trigger" tests/test_oauth_helper_gbs.py` returns ≥ 3.
    - Source assertion: `grep -c "test_gbs_flush" tests/test_oauth_helper_gbs.py` returns ≥ 2.
    - Source assertion: `grep -c "test_gbs_timeout\|test_gbs_window_closed" tests/test_oauth_helper_gbs.py` returns ≥ 2.
    - Source assertion: `grep -c "test_argparse_accepts_mode_gbs" tests/test_oauth_helper_gbs.py` returns 1.
    - Source assertion: `grep -c "_validate_gbs_cookies" tests/test_oauth_helper_gbs.py` returns ≥ 1 (the round-trip test calls it).
    - Source assertion: `grep -c "# Mirror: tests/test_oauth_helper_twitch.py:" tests/test_oauth_helper_gbs.py` returns ≥ 5 (cite source for at least the cloned test shapes).
    - Behavior: collection still passes — `python -m pytest tests/test_oauth_helper_gbs.py --collect-only -q` lists all tests, exits 0.
    - Behavior: After Plan 76-01 fully lands, `python -m pytest tests/test_oauth_helper_gbs.py -x -q` exits 0 (all tests GREEN).
    - Behavior: `tests/test_oauth_helper_twitch.py` still passes — `python -m pytest tests/test_oauth_helper_twitch.py -x -q` exits 0 (no cross-test interference).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_oauth_helper_gbs.py --collect-only -q && python -m pytest tests/test_oauth_helper_twitch.py -x -q</automated>
  </verify>
  <done>All Group A tests from RESEARCH.md §Test Strategy lines 762-805 present in `tests/test_oauth_helper_gbs.py`; tests are RED before Plan 76-01 merges, GREEN after; collection succeeds standalone; Twitch tests still pass; commit message cites RESEARCH.md §Test Strategy + `tests/test_oauth_helper_twitch.py` (specific line ranges per test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → production code under test | Tests run inside the project's pytest harness with full repo access. No external network calls. No real cookie values used (all `_fake_cookie` mocks use synthetic data like `"v1"`, `"abc"`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-T1 | Information Disclosure | Real session cookies accidentally landed in test fixtures | mitigate | All cookie values in this plan are synthetic short strings (`"v1"`, `"abc"`, `"val"`). No `gbs_api.cookies_path()` read, no dev-fixture file read. Source-grep `grep -E "[a-f0-9]{20,}" tests/test_oauth_helper_gbs.py` returns 0 matches (no hex blobs). |
| T-76-T2 | Tampering | Module-level imports cause collection failure before Plan 76-01 merges | mitigate | All `from musicstreamer.oauth_helper import ...` statements deferred inside test functions (mirror Twitch test discipline). Collection-only run validates this. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (test file uses existing pytest + unittest.mock). |
</threat_model>

<verification>
After Task 1 and Task 2 land:

```bash
# Collection must always succeed (deferred imports):
python -m pytest tests/test_oauth_helper_gbs.py --collect-only -q

# Cross-test independence:
python -m pytest tests/test_oauth_helper_twitch.py -x -q   # Must still pass

# After Plan 76-01 merges, full pass:
python -m pytest tests/test_oauth_helper_gbs.py -x -q

# Synthetic-cookie hygiene:
test "$(grep -E '[a-f0-9]{20,}' tests/test_oauth_helper_gbs.py | wc -l)" -eq 0
```
</verification>

<success_criteria>
- [ ] `tests/test_oauth_helper_gbs.py` exists with ≥ 21 tests total.
- [ ] All `from musicstreamer.oauth_helper import ...` imports are inside test function bodies.
- [ ] No `pytest.fail` placeholders.
- [ ] Synthetic cookie values only — no real session blobs.
- [ ] Existing `tests/test_oauth_helper_twitch.py` still passes after this plan lands.
- [ ] Collection succeeds standalone (RED state acceptable before Plan 76-01).
- [ ] All tests GREEN after Plan 76-01 fully merges.
- [ ] Commit messages cite specific Twitch-test line ranges per cloned shape and PATTERNS.md / RESEARCH.md source ranges.
</success_criteria>

<output>
Create `.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-02-SUMMARY.md` when done.
</output>
