---
phase: 76
plan: 03
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - musicstreamer/ui_qt/accounts_dialog.py
autonomous: true
requirements:
  - GBS-AUTH-01

must_haves:
  truths:
    - "Clicking the primary GBS button when NOT connected launches `python -m musicstreamer.oauth_helper --mode gbs` as a QProcess subprocess (NOT opens CookieImportDialog)."
    - "Clicking the primary GBS button when connected shows a confirmation dialog; on Yes deletes `paths.gbs_cookies_path()` with broader-OSError tolerance (mirror Phase 60 HIGH 2 fix)."
    - "A secondary `[Import cookies file…]` button is visible in not-connected state and opens the existing `CookieImportDialog` with the GBS-FM parameterization unchanged."
    - "On subprocess success (exit=0 + Netscape stdout + Success category): `_validate_gbs_cookies` gates the write; cookies file written to `paths.gbs_cookies_path()` with `os.chmod(0o600)`; toast fires; status flips to `Connected` (or `Connected (cookies)` per planner discretion); OAuthLogger event recorded with `provider=\"gbs\"`."
    - "On subprocess failure (any non-Success category OR exit≠0): category-aware failure dialog appears via the shared `_classify_and_show_failure` helper with `provider=\"gbs\"` parameter."
    - "Existing Twitch `_on_oauth_finished` path still passes through the same extracted `_classify_and_show_failure` helper with `provider=\"twitch\"` — no Twitch regression."
  artifacts:
    - path: "musicstreamer/ui_qt/accounts_dialog.py"
      provides: "_on_gbs_action_clicked rewrite + _launch_gbs_login_subprocess + _on_gbs_login_finished + _on_gbs_import_clicked + extracted _classify_and_show_failure helper + secondary import button + 2-state status enumeration"
      contains: "def _launch_gbs_login_subprocess"
  key_links:
    - from: "AccountsDialog._on_gbs_action_clicked (connect branch)"
      to: "AccountsDialog._launch_gbs_login_subprocess"
      via: "direct method call"
      pattern: "self._launch_gbs_login_subprocess()"
    - from: "AccountsDialog._launch_gbs_login_subprocess"
      to: "QProcess.start with sys.executable + ['-m','musicstreamer.oauth_helper','--mode','gbs']"
      via: "QProcess subprocess invocation"
      pattern: "musicstreamer.oauth_helper.*--mode.*gbs"
    - from: "AccountsDialog._on_gbs_login_finished (success branch)"
      to: "paths.gbs_cookies_path() write + os.chmod(0o600)"
      via: "validated Netscape stdout"
      pattern: "os.chmod.*0o600"
    - from: "AccountsDialog._on_gbs_login_finished (failure branch)"
      to: "AccountsDialog._classify_and_show_failure(provider='gbs', ...)"
      via: "extracted helper shared with _on_oauth_finished"
      pattern: "_classify_and_show_failure"
---

<objective>
Rewrite the GBS connect/disconnect flow in `musicstreamer/ui_qt/accounts_dialog.py` to use the subprocess login pattern shipped by Plan 76-01. Connect now launches `oauth_helper --mode gbs`; disconnect clears the cookies file only (no token half — D-03 collapsed). Add a secondary `[Import cookies file…]` button that preserves D-14 reachability of the existing File/Paste tabs. Extract the failure-classification logic from `_on_oauth_finished` into a shared `_classify_and_show_failure(provider, ...)` helper so both Twitch and GBS finished-handlers reuse identical Phase 999.3 category-dialog plumbing.

Purpose: GBS-AUTH-01 — wires the Plan 76-01 subprocess into the `AccountsDialog` so the user can actually trigger it. Honors D-03 collapsed scope: no inline token row, no SQLite `gbs_api_token` key, no `_is_gbs_token_saved` predicate, no 4-state status enumeration, no AuthContext changes.

Output: Modified `musicstreamer/ui_qt/accounts_dialog.py` with the new methods, the secondary import button, and the extracted `_classify_and_show_failure` helper used by BOTH Twitch and GBS finished-handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-CONTEXT.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md

# Files this plan reads / mirrors / modifies:
@musicstreamer/ui_qt/accounts_dialog.py
@musicstreamer/ui_qt/cookie_import_dialog.py
@musicstreamer/oauth_helper.py
@musicstreamer/gbs_api.py
@musicstreamer/paths.py

# Plan 76-01 produced the subprocess this plan invokes — its SUMMARY documents the contract:
@.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-01-SUMMARY.md
</context>

<interfaces>
<!-- Verbatim contracts cited from PATTERNS.md and the actual source file. -->

Current GBS surface in `musicstreamer/ui_qt/accounts_dialog.py` (verified via grep — line numbers match):

- Lines 105-115: GBS GroupBox construction (`self._gbs_box`, `self._gbs_status_label`, `self._gbs_action_btn`). Plan 76-03 ADDS a sibling `self._gbs_import_btn` (per RESEARCH §AccountsDialog Layout lines 597-599).
- Line 84 (current): `self._oauth_proc: QProcess | None = None`. Plan 76-03 adds `self._gbs_login_proc: QProcess | None = None` adjacent.
- Lines 173-175: `_is_gbs_connected()` — KEEP unchanged.
- Lines 186-192: GBS branch of `_update_status()` — currently 2-state. Plan 76-03 KEEPS 2-state (D-15 4-state is moot under D-03) but adds visibility toggle for `self._gbs_import_btn` (hidden when connected).
- Line 192: current button text is `"Import GBS.FM Cookies..."` — Plan 76-03 changes to `"Connect to GBS.FM…"` (matches the subprocess-launch semantics).
- Lines 298-330: current `_on_gbs_action_clicked` — Plan 76-03 REWRITES the connect branch to call `_launch_gbs_login_subprocess()` instead of opening `CookieImportDialog`. The disconnect branch stays verbatim (delete cookies file with broader-OSError tolerance — Phase 60 HIGH 2 fix at lines 312-316).
- Lines 332-341: `_launch_oauth_subprocess` (Twitch) — Plan 76-03 CLONES (not parameterizes — per RESEARCH line 671 explicit recommendation, the two output contracts differ).
- Lines 361-458: `_on_oauth_finished` (Twitch) — Plan 76-03 MIRRORS but: (a) replaces `token` with `netscape_text` (NO `.strip()` — preserves Netscape leading newlines per RESEARCH line 709); (b) gates the success write through `gbs_api._validate_gbs_cookies(netscape_text)`; (c) writes to `paths.gbs_cookies_path()` instead of `paths.twitch_token_path()`; (d) uses `provider="gbs"` in OAuthLogger events; (e) failure path delegates to the new extracted `_classify_and_show_failure` helper.
- Line 464+ (current `_show_failure_dialog`): Plan 76-03 does NOT modify `_show_failure_dialog` itself — the extraction is at the LAYER ABOVE it: the failure-classification + logger.log_event + `_show_failure_dialog(category, detail)` chain currently inline at lines 424-458 of `_on_oauth_finished` is what gets extracted to `_classify_and_show_failure(provider, exit_code, output, last_event)`.

Verbatim adapted excerpts from PATTERNS.md:

- §File 2 Excerpt 2B adapted (rewritten `_on_gbs_action_clicked` + new `_on_gbs_import_clicked`) at lines 498-538.
- §File 2 Excerpt 2C adapted (`_launch_gbs_login_subprocess`) at lines 562-575.
- §File 2 Excerpt 2D adapted (`_on_gbs_login_finished` with extracted-helper hook) at lines 688-753.
- §File 2 Excerpt 2E inline (`_write_cookies` 0o600 pattern — 6 lines inlined into success branch).
- §File 2 Excerpt 2F + 2G (PROVIDER-AGNOSTIC `_CATEGORY_LABELS` + `_get_oauth_logger` — REUSE unchanged).

Existing AccountsDialog support surface (Plan 76-03 reuses unchanged):

- `accounts_dialog.py:46-51` — `_CATEGORY_LABELS` dict mapping `"LoginTimeout"`/`"WindowClosedBeforeLogin"`/`"InvalidTokenResponse"`/`"SubprocessCrash"` → human-readable strings. Provider-agnostic by design. NO CHANGES.
- `accounts_dialog.py:220-237` — `_get_oauth_logger` — lazy-init `OAuthLogger`. NO CHANGES.
- `accounts_dialog.py:464+` — `_show_failure_dialog(category, detail)` — Phase 999.3 category-aware dialog with inline Retry. NO CHANGES (still called by the extracted helper).

Existing modules this plan imports (already imported at top of file):

- `os`, `sys`, `json` (stdlib).
- `QProcess`, `QMessageBox`, `QPushButton`, `QLabel`, `QVBoxLayout`, `Qt`, `QGroupBox`, etc. (PySide6).
- `musicstreamer.paths` (for `gbs_cookies_path`, `oauth_log_path`, etc.).
- `musicstreamer.oauth_log.OAuthLogger`.

NEW (deferred) imports inside `_on_gbs_login_finished`:

- `from musicstreamer.gbs_api import _validate_gbs_cookies` (deferred to avoid module-load cost; matches the deferred-import pattern Phase 60 `_on_gbs_action_clicked` already uses at lines 318-319).

NEW (deferred) imports inside `_on_gbs_import_clicked`:

- `from musicstreamer.ui_qt.cookie_import_dialog import CookieImportDialog`
- `from musicstreamer import gbs_api`

(These exact deferred imports are what Phase 60's current `_on_gbs_action_clicked` connect branch uses — see lines 318-330. Plan 76-03 lifts them verbatim into `_on_gbs_import_clicked`.)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _classify_and_show_failure helper and rewire _on_oauth_finished</name>
  <files>musicstreamer/ui_qt/accounts_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/accounts_dialog.py:361-458 (the full Twitch `_on_oauth_finished` method — Plan 76-03 extracts lines 424-458 into a shared helper, then rewires the Twitch caller to invoke the helper with `provider="twitch"`)
    - musicstreamer/ui_qt/accounts_dialog.py:46-51 (`_CATEGORY_LABELS` — provider-agnostic, REUSE unchanged)
    - musicstreamer/ui_qt/accounts_dialog.py:464+ (`_show_failure_dialog` — the helper's terminal step, REUSE unchanged)
    - musicstreamer/ui_qt/accounts_dialog.py:220-237 (`_get_oauth_logger` — REUSE unchanged)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md (§File 2 Summary Table row 9 — extract `_classify_and_show_failure(provider, exit_code, output, last_event)` helper used by BOTH `_on_oauth_finished` and `_on_gbs_login_finished`)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§`_on_gbs_login_finished` lines 741-746 — recommended helper signature)
    - tests/test_accounts_dialog.py:196-251 (existing `TestAccountsDialogOAuthFinished` class — Plan 76-03's refactor MUST leave these tests green; they are the regression guard)
    - tests/test_accounts_dialog.py:254-381 (existing `TestAccountsDialogStderrParsing` class — also a regression guard for the existing failure-path)
  </read_first>
  <behavior>
    - Test 1: `tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished` all pass after extraction (the existing Twitch finished-handler tests are the regression guard).
    - Test 2: `tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing` all pass after extraction.
    - Test 3: `tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText` all pass.
    - Test 4: `tests/test_accounts_dialog.py::TestAccountsDialogRetry` all pass (the inline Retry from the failure dialog still works).
    - Test 5: `_on_oauth_finished` no longer contains the full failure-classification inline block; that logic now lives in `_classify_and_show_failure`. Source-grep: the synthetic event construction with `category: "InvalidTokenResponse" + detail: "empty_stdout"` and `category: "SubprocessCrash" + detail: f"exit={exit_code}"` appears in `_classify_and_show_failure`, NOT inline in `_on_oauth_finished`.
  </behavior>
  <action>
    Extract the failure-classification + logger.log_event + `_show_failure_dialog` chain from `_on_oauth_finished` at `accounts_dialog.py:424-458` into a new method `_classify_and_show_failure(self, provider: str, exit_code: int, output: str, last_event: dict | None) -> None`. Per project memory `feedback_mirror_decisions_cite_source.md`: this extraction mirrors PATTERNS.md File 2 Summary Table row 9 + RESEARCH §`_on_gbs_login_finished` lines 741-746 verbatim — cite both in the commit message.

    The extracted helper preserves the existing classification precedence (per PATTERNS.md Excerpt 2D lines 650-657 + current source at accounts_dialog.py:424-458):

    1. If `exit_code == 0 and not output`: synthesize `last_event = {"ts": ..., "category": "InvalidTokenResponse", "detail": "empty_stdout", "provider": provider}` (replaces existing inline synthetic; `provider` is the parameter, NOT hardcoded `"twitch"`).
    2. Elif `last_event is None`: synthesize `last_event = {"ts": 0.0, "category": "SubprocessCrash", "detail": f"exit={exit_code}", "provider": provider}`.
    3. Otherwise: use `last_event` as-is.
    4. Call `self._get_oauth_logger()`; if non-None, try `logger.log_event(last_event)` (suppress `Exception` per existing line 444 pattern).
    5. Call `self._update_status()`.
    6. Coerce `category = str(last_event.get("category", "SubprocessCrash"))` and `detail = str(last_event.get("detail", ""))` (T-999.3-05 invariant from existing line 681).
    7. Call `self._show_failure_dialog(category, detail)`.

    Rewire `_on_oauth_finished` at the call site that previously contained lines 424-458: replace the inline block with a single call `self._classify_and_show_failure(provider="twitch", exit_code=exit_code, output=token, last_event=last_event)`. The Twitch path passes `output=token` (the variable name in `_on_oauth_finished`).

    Do NOT change `_show_failure_dialog`, `_CATEGORY_LABELS`, `_get_oauth_logger`, or anything outside this extraction. The helper is `private` (underscore prefix) per CONVENTIONS.md.

    Do NOT change `_on_oauth_finished`'s success path (lines 392-422 area). The extraction is failure-path only.

    Do NOT change anything inside the stderr parsing block (lines 369-390 area) — that stays inline in `_on_oauth_finished`. (RESEARCH line 846 mentions a potential `_parse_oauth_stderr` extraction as an OPTIONAL planner-discretion refactor — Plan 76-03 SKIPS this optional refactor to minimize Twitch-regression risk. Plan 76-03 Task 3's GBS finished-handler will inline its own stderr parse mirroring the Twitch shape.)

    Place `_classify_and_show_failure` adjacent to `_show_failure_dialog` so related helpers cluster (PATTERNS.md convention — group related methods).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -nE "^\s+def _classify_and_show_failure\(" musicstreamer/ui_qt/accounts_dialog.py` returns exactly 1 match.
    - Source assertion: signature includes `provider: str` parameter — `grep -A 5 "def _classify_and_show_failure" musicstreamer/ui_qt/accounts_dialog.py | grep -c "provider: str"` returns 1.
    - Source assertion: `_on_oauth_finished` body no longer contains the inline synthetic-event construction for `InvalidTokenResponse` / `SubprocessCrash` — `grep -A 100 "^\s\+def _on_oauth_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -c "category.*InvalidTokenResponse.*empty_stdout"` returns 0 (that code now lives in `_classify_and_show_failure`).
    - Source assertion: `_on_oauth_finished` calls `_classify_and_show_failure` — `grep -A 100 "^\s\+def _on_oauth_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -c "_classify_and_show_failure"` returns ≥ 1.
    - Source assertion: the synthetic `provider` field in `_classify_and_show_failure` is the PARAMETER, not hardcoded — `grep -A 40 "def _classify_and_show_failure" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "\"provider\":\s*provider"` returns ≥ 1 AND `grep -A 40 "def _classify_and_show_failure" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "\"provider\":\s*\"twitch\""` returns 0.
    - Behavior: ALL existing Twitch tests pass: `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText tests/test_accounts_dialog.py::TestAccountsDialogRetry -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText tests/test_accounts_dialog.py::TestAccountsDialogRetry -x -q</automated>
  </verify>
  <done>`_classify_and_show_failure(provider, exit_code, output, last_event)` extracted from `_on_oauth_finished` lines 424-458; Twitch call site rewired to call the helper with `provider="twitch"`; all existing Twitch tests still green; commit message cites `accounts_dialog.py:424-458` and PATTERNS.md File 2 Summary Table row 9.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _gbs_import_btn + _on_gbs_import_clicked + rewrite _on_gbs_action_clicked</name>
  <files>musicstreamer/ui_qt/accounts_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/accounts_dialog.py:105-115 (current GBS GroupBox construction — Plan 76-03 ADDS `self._gbs_import_btn` as a sibling)
    - musicstreamer/ui_qt/accounts_dialog.py:84 (current `self._oauth_proc: QProcess | None = None` — Plan 76-03 adds `self._gbs_login_proc: QProcess | None = None` adjacent)
    - musicstreamer/ui_qt/accounts_dialog.py:177-214 (current `_update_status()` — Plan 76-03 modifies the GBS branch at lines 186-192 only)
    - musicstreamer/ui_qt/accounts_dialog.py:298-330 (current `_on_gbs_action_clicked` — Plan 76-03 REWRITES the connect branch lines 318-330; the disconnect branch lines 300-317 stays verbatim)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md (§File 2 Excerpt 2B adapted at lines ~498-538 — verbatim `_on_gbs_action_clicked` + `_on_gbs_import_clicked`)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§GBS Group Construction lines 576-621 — verbatim GBS group construction + 2-state status enumeration)
  </read_first>
  <behavior>
    - Test 1: `AccountsDialog.__init__` creates `self._gbs_import_btn` as a `QPushButton` instance with text `"Import cookies file…"` (U+2026 ellipsis); `clicked.connect` bound to `self._on_gbs_import_clicked` (bound-method per QA-05, NO lambda).
    - Test 2: `AccountsDialog.__init__` creates `self._gbs_login_proc` initialized to None.
    - Test 3: When `_is_gbs_connected()` returns True, `_update_status` sets `self._gbs_status_label.setText("Connected")` (or `"Connected (cookies)"` per planner discretion — RESEARCH line 620 net-zero cost), `self._gbs_action_btn.setText("Disconnect")`, AND `self._gbs_import_btn.setVisible(False)`.
    - Test 4: When `_is_gbs_connected()` returns False, `_update_status` sets `self._gbs_status_label.setText("Not connected")`, `self._gbs_action_btn.setText("Connect to GBS.FM…")` (U+2026), AND `self._gbs_import_btn.setVisible(True)`.
    - Test 5: Clicking `self._gbs_action_btn` when NOT connected calls `self._launch_gbs_login_subprocess()` (the new method introduced in Task 3). Test placeholder OK if Task 3 hasn't merged — the test fails on `AttributeError` until Task 3 lands.
    - Test 6: Clicking `self._gbs_action_btn` when connected shows the existing confirmation dialog with the verbatim Phase 60 copy ("This will delete your saved GBS.FM cookies. You will need to reconnect to vote, view the active playlist, or submit songs."); on Yes, `os.remove(paths.gbs_cookies_path())` is called with broader-OSError tolerance; on No, the file is not removed.
    - Test 7: Clicking `self._gbs_import_btn` opens a `CookieImportDialog` with kwargs `target_label="GBS.FM"`, `cookies_path=paths.gbs_cookies_path`, `validator=gbs_api._validate_gbs_cookies`, `oauth_mode=None` (mirror Phase 60 D-04 ladder #3 invocation verbatim).
  </behavior>
  <action>
    Make four coordinated changes to `musicstreamer/ui_qt/accounts_dialog.py`:

    **Change A — `__init__` additions:**

    - Adjacent to `self._oauth_proc: QProcess | None = None` at line 84, ADD `self._gbs_login_proc: QProcess | None = None`. (PATTERNS.md File 2 Summary Table row 1.)

    - Inside the GBS GroupBox layout block (currently lines 105-115; immediately after `gbs_layout.addWidget(self._gbs_action_btn)` at line 115), ADD per PATTERNS.md Excerpt 2B + RESEARCH §GBS Group Construction lines 592-599 (verbatim):
      - `self._gbs_import_btn = QPushButton("Import cookies file…", self)` (U+2026 — single ellipsis character, NOT three dots).
      - `self._gbs_import_btn.clicked.connect(self._on_gbs_import_clicked)` (QA-05 bound-method — NO lambda).
      - `gbs_layout.addWidget(self._gbs_import_btn)`.

    **Change B — `_update_status` GBS branch rewrite (replaces lines 186-192):**

    Per PATTERNS.md File 2 Summary Table row 4 + RESEARCH §Status Label lines 602-616 (verbatim):

    - When `self._is_gbs_connected()`:
      - `self._gbs_status_label.setText("Connected")` (planner discretion: optionally `"Connected (cookies)"` for forward-compat with future re-attempt — RESEARCH line 620 says net-zero cost; either is acceptable, executor picks one and documents in commit message).
      - `self._gbs_action_btn.setText("Disconnect")`.
      - `self._gbs_import_btn.setVisible(False)`.
    - Else:
      - `self._gbs_status_label.setText("Not connected")`.
      - `self._gbs_action_btn.setText("Connect to GBS.FM…")` (U+2026).
      - `self._gbs_import_btn.setVisible(True)`.

    Remove the current line 192 text `"Import GBS.FM Cookies..."` — it's replaced by `"Connect to GBS.FM…"` (the action semantic flipped from "import" to "launch subprocess").

    **Change C — `_on_gbs_action_clicked` rewrite (replaces lines 298-330):**

    Per PATTERNS.md Excerpt 2B adapted (RESEARCH §`_on_gbs_action_clicked` Rewrite lines 626-647 verbatim):

    - If `self._is_gbs_connected()`:
      - Show `QMessageBox.question` with title `"Disconnect GBS.FM?"` and the existing Phase 60 body text (KEEP verbatim — PATTERNS.md Excerpt 2B "Disconnect copy reverts to cookies-only").
      - On `Yes`: `try: os.remove(paths.gbs_cookies_path()) except OSError: pass` (broader-OSError tolerance — Phase 60 HIGH 2 fix at lines 312-316 stays).
      - Call `self._update_status()`.
    - Else: call `self._launch_gbs_login_subprocess()` (the method Task 3 introduces).

    Remove the current connect branch (lines 318-330) which opens `CookieImportDialog` with `oauth_mode=None` — that flow MOVES to the new `_on_gbs_import_clicked` method (Change D below) so the existing File/Paste tabs stay reachable via the secondary button.

    **Change D — New method `_on_gbs_import_clicked`:**

    Place adjacent to (after) `_on_gbs_action_clicked`. Per PATTERNS.md Excerpt 2B adapted lines 520-537 (verbatim):

    - Deferred imports inside the function body (matches Phase 60 pattern at current `_on_gbs_action_clicked` lines 318-319):
      - `from musicstreamer.ui_qt.cookie_import_dialog import CookieImportDialog`
      - `from musicstreamer import gbs_api`
    - Construct `dlg = CookieImportDialog(self._toast_callback, parent=self, target_label="GBS.FM", cookies_path=paths.gbs_cookies_path, validator=gbs_api._validate_gbs_cookies, oauth_mode=None)`.
    - Call `dlg.exec()`.
    - Call `self._update_status()`.
    - Docstring: `"""Phase 76 D-14: secondary path — open the existing File/Paste tabs."""`

    Per project memory `feedback_mirror_decisions_cite_source.md`: cite `accounts_dialog.py:318-330` (where this code currently lives) and RESEARCH §`_on_gbs_action_clicked` Rewrite lines 626-666 in the commit message — this is a verbatim MOVE, not a paraphrase.

    Per QA-05: every new `.clicked.connect` uses a bound method (`self._on_xxx`), never a lambda.

    Per T-40-04: `self._gbs_import_btn` does NOT call `.setTextFormat()` because it's a QPushButton not a QLabel; T-40-04 applies only to QLabel.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -nE "self\._gbs_login_proc: QProcess \| None = None" musicstreamer/ui_qt/accounts_dialog.py` returns exactly 1 match.
    - Source assertion: `grep -nE "self\._gbs_import_btn = QPushButton" musicstreamer/ui_qt/accounts_dialog.py` returns exactly 1 match.
    - Source assertion: `grep -E "self\._gbs_import_btn\.clicked\.connect" musicstreamer/ui_qt/accounts_dialog.py | grep -c "lambda"` returns 0 (QA-05 invariant — no lambdas).
    - Source assertion: button text uses single U+2026 ellipsis — `grep -c "Import cookies file…" musicstreamer/ui_qt/accounts_dialog.py` returns ≥ 1; `grep -c "Import cookies file\.\.\." musicstreamer/ui_qt/accounts_dialog.py` returns 0 (no three-dot variant).
    - Source assertion: `grep -nE "^\s+def _on_gbs_import_clicked\(" musicstreamer/ui_qt/accounts_dialog.py` returns exactly 1 match.
    - Source assertion: `grep -A 15 "def _on_gbs_import_clicked" musicstreamer/ui_qt/accounts_dialog.py | grep -c "CookieImportDialog"` returns ≥ 1.
    - Source assertion: `grep -A 15 "def _on_gbs_import_clicked" musicstreamer/ui_qt/accounts_dialog.py | grep -c "oauth_mode=None"` returns 1.
    - Source assertion: `_on_gbs_action_clicked` no longer opens `CookieImportDialog` — `grep -A 40 "def _on_gbs_action_clicked" musicstreamer/ui_qt/accounts_dialog.py | grep -c "CookieImportDialog"` returns 0.
    - Source assertion: `_on_gbs_action_clicked` calls `_launch_gbs_login_subprocess` — `grep -A 40 "def _on_gbs_action_clicked" musicstreamer/ui_qt/accounts_dialog.py | grep -c "_launch_gbs_login_subprocess"` returns ≥ 1.
    - Source assertion: disconnect path preserves broader-OSError tolerance — `grep -A 40 "def _on_gbs_action_clicked" musicstreamer/ui_qt/accounts_dialog.py | grep -c "except OSError:"` returns ≥ 1.
    - Source assertion: `_update_status` GBS branch toggles `_gbs_import_btn` visibility — `grep -A 40 "_is_gbs_connected" musicstreamer/ui_qt/accounts_dialog.py | grep -c "_gbs_import_btn.setVisible"` returns ≥ 2 (one for each branch).
    - Source assertion: button text `"Connect to GBS.FM…"` present — `grep -c "Connect to GBS.FM…" musicstreamer/ui_qt/accounts_dialog.py` returns ≥ 1.
    - Source assertion: old text `"Import GBS.FM Cookies..."` removed — `grep -c "Import GBS.FM Cookies..." musicstreamer/ui_qt/accounts_dialog.py` returns 0.
    - Behavior: Plan 76-04's RED stubs for `test_gbs_action_launches_subprocess_when_not_connected`, `test_gbs_import_button_opens_cookieimportdialog`, `test_gbs_status_shows_connected_when_cookies_present`, `test_gbs_status_shows_not_connected_when_no_cookies`, `test_gbs_disconnect_clears_cookies_with_yes`, `test_gbs_disconnect_no_op_with_no` partially turn GREEN (the launch-subprocess test still waits on Task 3 for the method to exist).
    - Behavior: existing tests still pass — `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogGBS -x -q` may fail on tests that check for the old `"Import GBS.FM Cookies..."` text or the old connect-branch behavior; Plan 76-04 will update those tests. ACCEPT this temporary RED state for tests directly asserting against the OLD GBS connect-branch text/behavior; report which tests broke in the task summary.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from musicstreamer.ui_qt import accounts_dialog; import ast, inspect; src = inspect.getsource(accounts_dialog); assert 'def _on_gbs_import_clicked' in src; assert '_gbs_import_btn' in src; assert '_gbs_login_proc' in src; assert 'Connect to GBS.FM' in src; print('OK')"</automated>
  </verify>
  <done>`_gbs_import_btn` constructed in GBS group; `_on_gbs_import_clicked` opens existing `CookieImportDialog` verbatim; `_on_gbs_action_clicked` connect branch delegates to `_launch_gbs_login_subprocess`; `_update_status` toggles import-button visibility; disconnect path retains broader-OSError tolerance; commit message cites `accounts_dialog.py:298-330` and RESEARCH §`_on_gbs_action_clicked` Rewrite lines 626-666.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add _launch_gbs_login_subprocess and _on_gbs_login_finished</name>
  <files>musicstreamer/ui_qt/accounts_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/accounts_dialog.py:332-341 (verbatim `_launch_oauth_subprocess` — the Twitch clone source)
    - musicstreamer/ui_qt/accounts_dialog.py:361-458 (verbatim `_on_oauth_finished` — the Twitch mirror source for `_on_gbs_login_finished`)
    - musicstreamer/ui_qt/accounts_dialog.py (Task 1's new `_classify_and_show_failure` helper — Plan 76-03 calls it with `provider="gbs"`)
    - musicstreamer/ui_qt/cookie_import_dialog.py:333-342 (`_write_cookies` 0o600 pattern — inlined in `_on_gbs_login_finished` success path)
    - musicstreamer/gbs_api.py:116-141 (`_validate_gbs_cookies` — the validator gating the success-path write; do NOT modify)
    - musicstreamer/paths.py:54-60 (`gbs_cookies_path` — the destination path)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-PATTERNS.md (§File 2 Excerpt 2C adapted at lines 562-575 + Excerpt 2D adapted at lines 688-753)
    - .planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-RESEARCH.md (§`_launch_gbs_login_subprocess` lines 669-686 + §`_on_gbs_login_finished` lines 688-753)
  </read_first>
  <behavior>
    - Test 1: `_launch_gbs_login_subprocess()` constructs a `QProcess(self)`, connects `finished` to `self._on_gbs_login_finished` (bound method, no lambda), and calls `proc.start(sys.executable, ["-m", "musicstreamer.oauth_helper", "--mode", "gbs"])`. The resulting QProcess is held on `self._gbs_login_proc`.
    - Test 2: `_launch_gbs_login_subprocess()` calls `self._update_status()` (so a "connecting" state can surface if the planner extended `_update_status` to handle in-flight state; under D-03 minimal scope it's effectively a no-op refresh).
    - Test 3: `_on_gbs_login_finished(exit_code=0, exit_status=Normal)` with stdout containing a valid Netscape dump (sessionid + csrftoken + gbs.fm domain) and stderr containing a `{"category": "Success", "provider": "gbs"}` JSON line: writes the Netscape text to `paths.gbs_cookies_path()`, applies `os.chmod(0o600)`, calls `self._toast_callback("GBS.FM logged in.")` (or planner-equivalent), and calls `self._update_status()`. The OAuthLogger event recorded has `provider="gbs"`, `category="Success"`.
    - Test 4: `_on_gbs_login_finished(exit_code=1, exit_status=Normal)` with stderr containing `{"category": "LoginTimeout", "detail": "120s", "provider": "gbs"}` JSON line: does NOT write the cookies file; calls `self._classify_and_show_failure(provider="gbs", exit_code=1, output="", last_event={..."LoginTimeout"...})` which shows the category-aware failure dialog.
    - Test 5: `_on_gbs_login_finished(exit_code=0, ...)` with stdout = `"garbage text"` (does NOT pass `_validate_gbs_cookies`): does NOT write the cookies file; routes through `_classify_and_show_failure(provider="gbs", ...)` with an `InvalidTokenResponse` synthetic event (since the success precondition failed).
    - Test 6: `self._gbs_login_proc` is reset to None at the start of `_on_gbs_login_finished` (mirror line 367 pattern from `_on_oauth_finished`).
    - Test 7: `_on_gbs_login_finished` does NOT call `.strip()` on the Netscape stdout (RESEARCH line 709 anti-pitfall — preserves leading newlines).
    - Test 8: After `_on_gbs_login_finished` success path, the existing Twitch tests still pass (the helper-extraction from Task 1 + Twitch flow is unaffected).
  </behavior>
  <action>
    Add TWO new methods to `musicstreamer/ui_qt/accounts_dialog.py`:

    **Method 1 — `_launch_gbs_login_subprocess(self) -> None`** (mirror `_launch_oauth_subprocess` at lines 332-341):

    Per PATTERNS.md Excerpt 2C adapted (verbatim from RESEARCH §`_launch_gbs_login_subprocess` lines 673-686):

    - Docstring: `"""Phase 76 D-09: launch oauth_helper --mode gbs. Mirror _launch_oauth_subprocess shape but route the finished signal to _on_gbs_login_finished (which validates Netscape stdout)."""`
    - `self._gbs_login_proc = QProcess(self)`.
    - `self._gbs_login_proc.finished.connect(self._on_gbs_login_finished)` (QA-05 bound-method — NO lambda).
    - `self._gbs_login_proc.start(sys.executable, ["-m", "musicstreamer.oauth_helper", "--mode", "gbs"])`.
    - `self._update_status()`.

    Place adjacent to (after) `_launch_oauth_subprocess` at line 341.

    Per RESEARCH line 671: CLONE, do NOT parameterize `_launch_oauth_subprocess` by mode. Rationale (cite in commit): the two output contracts differ — Twitch returns a raw token, GBS returns a Netscape dump; downstream processing diverges in the finished-handler.

    **Method 2 — `_on_gbs_login_finished(self, exit_code: int, exit_status: QProcess.ExitStatus) -> None`** (mirror `_on_oauth_finished` at lines 361-458):

    Per PATTERNS.md Excerpt 2D adapted (verbatim from RESEARCH §`_on_gbs_login_finished` lines 688-753):

    - Docstring: `"""Mirror _on_oauth_finished but for the Netscape-stdout contract."""`
    - `proc = self._gbs_login_proc; self._gbs_login_proc = None` (mirror line 367 reset pattern).
    - Stderr parse block — mirror lines 369-390 of `_on_oauth_finished` INLINE (do NOT extract a helper per Plan 76-03 Task 1's explicit decision to leave this inline to minimize regression risk). Extract `last_event: dict | None` from the stderr JSON lines, keep last valid event.
    - Stdout read — `netscape_text: str = ""`; if `proc is not None`: `try: netscape_text = proc.readAllStandardOutput().data().decode("utf-8", errors="replace") except Exception: netscape_text = ""`. **CRITICAL: do NOT `.strip()`** — Netscape format preserves leading newlines per RESEARCH line 709 anti-pitfall.
    - Deferred import: `from musicstreamer.gbs_api import _validate_gbs_cookies`.
    - Success precondition: `success_category = last_event is None or last_event.get("category") == "Success"`; full success guard: `exit_code == 0 and netscape_text and success_category and _validate_gbs_cookies(netscape_text)`.
    - On success: `cookies_path = paths.gbs_cookies_path()`; `os.makedirs(os.path.dirname(cookies_path), exist_ok=True)`; open the file with `"w"` mode and `encoding="utf-8"`, write `netscape_text`; `os.chmod(cookies_path, 0o600)` (T-40-03 / Phase 999.7 invariant — inline the `cookie_import_dialog.py:333-342` pattern verbatim).
    - OAuthLogger event on success: `logger = self._get_oauth_logger()`; if non-None, try `logger.log_event({"ts": (last_event or {}).get("ts", 0.0), "category": "Success", "detail": "", "provider": "gbs"})` (suppress `Exception` per existing pattern). `provider="gbs"` NOT hardcoded `"twitch"`.
    - Toast: `self._toast_callback("GBS.FM logged in.")` (or `"GBS.FM connected."` — planner picks, document choice in commit).
    - `self._update_status()`.
    - Return.
    - Failure path: call `self._classify_and_show_failure(provider="gbs", exit_code=exit_code, output=netscape_text, last_event=last_event)`. The `output` parameter doubles as the empty/garbage trigger for the `InvalidTokenResponse empty_stdout` classification — passing `netscape_text` (which is empty string on read failure) means the existing classification precedence in `_classify_and_show_failure` works unchanged.

    Place `_on_gbs_login_finished` adjacent to (after) `_on_oauth_finished` (which ends at line 458).

    Per QA-05: bound-method connection (`finished.connect(self._on_gbs_login_finished)`); NO lambda.

    Per project memory `feedback_mirror_decisions_cite_source.md`: cite `accounts_dialog.py:332-341` (Method 1 source) AND `accounts_dialog.py:361-458` (Method 2 source) AND RESEARCH §`_on_gbs_login_finished` lines 688-753 in the commit message. This is a verbatim clone with the three documented substitutions (output variable, validator gate, provider field).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -nE "^\s+def _launch_gbs_login_subprocess\(" musicstreamer/ui_qt/accounts_dialog.py` returns exactly 1 match.
    - Source assertion: `grep -A 10 "def _launch_gbs_login_subprocess" musicstreamer/ui_qt/accounts_dialog.py | grep -c "musicstreamer.oauth_helper"` returns 1.
    - Source assertion: `grep -A 10 "def _launch_gbs_login_subprocess" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "\"--mode\",\s*\"gbs\""` returns 1.
    - Source assertion: `grep -A 10 "def _launch_gbs_login_subprocess" musicstreamer/ui_qt/accounts_dialog.py | grep -c "lambda"` returns 0 (QA-05).
    - Source assertion: `grep -nE "^\s+def _on_gbs_login_finished\(" musicstreamer/ui_qt/accounts_dialog.py` returns exactly 1 match.
    - Source assertion: `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -c "_validate_gbs_cookies"` returns ≥ 1.
    - Source assertion: `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -c "os.chmod"` returns ≥ 1.
    - Source assertion: `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "0o600"` returns ≥ 1.
    - Source assertion: `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "gbs_cookies_path"` returns ≥ 1.
    - Source assertion: NO `.strip()` on the Netscape stdout — `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -E "netscape_text.*\.strip\(\)"` returns 0.
    - Source assertion: `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -c "_classify_and_show_failure"` returns ≥ 1.
    - Source assertion: provider field is "gbs" — `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "\"provider\":\s*\"gbs\""` returns ≥ 1.
    - Source assertion: provider field is NOT hardcoded "twitch" — `grep -A 60 "def _on_gbs_login_finished" musicstreamer/ui_qt/accounts_dialog.py | grep -cE "\"provider\":\s*\"twitch\""` returns 0.
    - Behavior: Plan 76-04's RED stubs for `test_gbs_action_launches_subprocess_when_not_connected`, `test_gbs_login_finished_writes_cookies_on_success`, `test_gbs_login_finished_failure_dialog_for_each_category`, `test_gbs_login_finished_invalidates_bad_netscape` turn GREEN.
    - Behavior: Twitch tests still pass — `python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText tests/test_accounts_dialog.py::TestAccountsDialogRetry -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText tests/test_accounts_dialog.py::TestAccountsDialogRetry -x -q</automated>
  </verify>
  <done>`_launch_gbs_login_subprocess` clones `_launch_oauth_subprocess` with `--mode gbs`; `_on_gbs_login_finished` mirrors `_on_oauth_finished` with the three documented substitutions (netscape_text NO-strip, `_validate_gbs_cookies` gate, `provider="gbs"`); success path writes cookies file with 0o600; failure path delegates to `_classify_and_show_failure`; Twitch tests still green; commit message cites `accounts_dialog.py:332-341` + `:361-458` + RESEARCH §`_on_gbs_login_finished` lines 688-753.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess stdout (Netscape dump) → cookies file | Untrusted text from QProcess. `_validate_gbs_cookies` gates the write; bad input never touches disk. |
| subprocess stderr (JSON lines) → AccountsDialog state | Per-line JSON parsing with `json.JSONDecodeError` swallow (T-999.3-05 invariant from existing `_on_oauth_finished`). Categories coerced to `str` before UI consumption. |
| `paths.gbs_cookies_path()` → on-disk file | `os.chmod(0o600)` after every write (T-40-03 / Phase 999.7 invariant). |
| `os.remove(gbs_cookies_path())` → file system | Broader-OSError tolerance (`except OSError: pass`) — Phase 60 HIGH 2 fix preserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-D1 | Tampering | Garbage stdout written as cookies file (bypassing validation) | mitigate | `_validate_gbs_cookies(netscape_text)` is the gate before `open(cookies_path, "w")`. Plan 76-04 `test_gbs_login_finished_invalidates_bad_netscape` regression test. |
| T-76-D2 | Information Disclosure | Cookies file readable by other users on multi-user systems | mitigate | `os.chmod(cookies_path, 0o600)` applied IMMEDIATELY after write (T-40-03 / Phase 999.7 invariant from `cookie_import_dialog.py:340`). |
| T-76-D3 | Tampering | Twitch flow regresses when failure-classification helper is extracted | mitigate | Task 1 acceptance gate runs ALL existing Twitch finished-handler tests; commit blocked on regression. `provider="twitch"` parameter still flows through synthetic events. |
| T-76-D4 | Spoofing | `_on_gbs_login_finished` synthetic-event constructor uses `provider="twitch"` (copy-paste oversight) | mitigate | Task 3 acceptance gate `grep -E "\"provider\":\s*\"twitch\"" inside _on_gbs_login_finished` returns 0. Plan 76-04 includes a guard test. |
| T-76-D5 | Denial of Service | Phase 60 `os.remove(gbs_cookies_path())` fails on `PermissionError` / `IsADirectoryError` and crashes the disconnect flow | mitigate | Broader-OSError tolerance (`except OSError: pass`) preserved verbatim from Phase 60 HIGH 2 fix at lines 312-316. |
| T-76-D6 | Information Disclosure | Cookie/token value leaked into OAuthLogger event via `detail` field | mitigate | OAuthLogger event constructed with `detail: ""` on success (line by line — never user-derived). Phase 999.3 invariant from existing `_on_oauth_finished:417` reaffirmed in `_on_gbs_login_finished`. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
After all 3 tasks land:

```bash
# Twitch regression guard (Task 1 + Task 3):
python -m pytest tests/test_accounts_dialog.py::TestAccountsDialogOAuthFinished \
    tests/test_accounts_dialog.py::TestAccountsDialogStderrParsing \
    tests/test_accounts_dialog.py::TestAccountsDialogFailureDialogPlainText \
    tests/test_accounts_dialog.py::TestAccountsDialogRetry -x -q

# GBS new-method import smoke (Tasks 2 + 3):
python -c "
from musicstreamer.ui_qt.accounts_dialog import AccountsDialog
assert hasattr(AccountsDialog, '_launch_gbs_login_subprocess')
assert hasattr(AccountsDialog, '_on_gbs_login_finished')
assert hasattr(AccountsDialog, '_on_gbs_import_clicked')
assert hasattr(AccountsDialog, '_classify_and_show_failure')
print('OK')
"

# Anti-pitfall grep gates:
test "$(grep -A 60 'def _on_gbs_login_finished' musicstreamer/ui_qt/accounts_dialog.py | grep -cE '\"provider\":\s*\"twitch\"')" -eq 0
test "$(grep -A 60 'def _on_gbs_login_finished' musicstreamer/ui_qt/accounts_dialog.py | grep -E 'netscape_text.*\.strip\(\)' | wc -l)" -eq 0
test "$(grep -A 40 'def _on_gbs_action_clicked' musicstreamer/ui_qt/accounts_dialog.py | grep -c 'CookieImportDialog')" -eq 0
```
</verification>

<success_criteria>
- [ ] `_classify_and_show_failure(provider, exit_code, output, last_event)` extracted; Twitch finished-handler tests still pass.
- [ ] `_gbs_login_proc` attribute initialized in `__init__`; `_gbs_import_btn` constructed in GBS group with bound-method click handler.
- [ ] `_update_status` GBS branch is 2-state (`Connected` / `Not connected`) with `_gbs_import_btn` visibility toggle.
- [ ] `_on_gbs_action_clicked` connect branch calls `_launch_gbs_login_subprocess`; disconnect branch deletes cookies file with broader-OSError tolerance.
- [ ] `_on_gbs_import_clicked` opens the existing `CookieImportDialog` with verbatim GBS-FM parameterization.
- [ ] `_launch_gbs_login_subprocess` invokes `python -m musicstreamer.oauth_helper --mode gbs` via `QProcess.start(sys.executable, [...])`.
- [ ] `_on_gbs_login_finished` validates Netscape stdout, writes cookies file with `0o600`, logs `provider="gbs"`, toasts, refreshes status; failure path delegates to `_classify_and_show_failure(provider="gbs", ...)`.
- [ ] NO `.strip()` on Netscape stdout (RESEARCH anti-pitfall).
- [ ] NO `"provider": "twitch"` hardcodes inside `_on_gbs_login_finished` (T-76-D4 mitigation).
- [ ] All existing Twitch tests still pass.
- [ ] Commit messages cite the specific source line ranges per task.
</success_criteria>

<output>
Create `.planning/phases/76-gbs-fm-authentication-support-both-pre-existing-api-token-an/76-03-SUMMARY.md` when done.
</output>
