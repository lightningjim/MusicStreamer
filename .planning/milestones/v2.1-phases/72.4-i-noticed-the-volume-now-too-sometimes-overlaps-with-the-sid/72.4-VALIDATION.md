---
phase: 72.4
slug: i-noticed-the-volume-now-too-sometimes-overlaps-with-the-sid
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-19
---

# Phase 72.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: see `72.4-RESEARCH.md` §Validation Architecture (Nyquist Dimension 8).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-qt (existing) |
| **Config file** | `pytest.ini` / `pyproject.toml` (existing) |
| **Quick run command** | `pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q` |
| **Full suite command** | `pytest tests/ -x -q` |
| **Estimated runtime** | ~15s (focused) / ~3min (full) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q`
- **After every plan wave:** Run `pytest tests/test_now_playing_panel.py tests/test_phase72_1_stream_picker_reflow.py tests/test_phase72_3_*.py tests/test_phase72_4_*.py -x -q` to assert the resizeEvent triple-call (picker + volume-cluster + art-tier) does not regress.
- **Before `/gsd:verify-work`:** Full suite must be green.
- **Max feedback latency:** 30 seconds for the focused file.

---

## Per-Task Verification Map

> Populated by the planner from RESEARCH §Test Envelope (16 tests) when PLAN.md files are written.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 72.4-01-01 | 01 | 0 | LAYOUT-04 | — | N/A (layout-only) | unit (TDD-RED) | `pytest tests/test_phase72_4_volume_cluster_reflow.py -x -q` | ❌ W0 | ⬜ pending |
| 72.4-02-01 | 02 | 1 | LAYOUT-04 | — | N/A | unit | `pytest tests/test_phase72_4_volume_cluster_reflow.py::TestThreshold -x -q` | ❌ W0 | ⬜ pending |
| 72.4-02-02 | 02 | 1 | LAYOUT-04 | — | N/A | unit | `pytest tests/test_phase72_4_volume_cluster_reflow.py::TestSizePolicyRoundTrip -x -q` | ❌ W0 | ⬜ pending |
| 72.4-03-01 | 03 | 2 | LAYOUT-04 | — | N/A | unit | `pytest tests/test_phase72_4_volume_cluster_reflow.py::TestSixStateTruthTable -x -q` | ❌ W0 | ⬜ pending |
| 72.4-03-02 | 03 | 2 | LAYOUT-04 | — | N/A | unit | `pytest tests/test_phase72_4_volume_cluster_reflow.py::TestIdempotency -x -q` | ❌ W0 | ⬜ pending |
| 72.4-03-03 | 03 | 2 | LAYOUT-04 | — | N/A | unit | `pytest tests/test_phase72_4_volume_cluster_reflow.py::TestSignalSurvival -x -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Planner expands this table to one row per task. Wave 0 (TDD-RED) lands the failing test scaffold first; Waves 1-2 turn rows green.*

---

## Wave 0 Requirements

- [ ] `tests/test_phase72_4_volume_cluster_reflow.py` — TDD-RED scaffold covering all 16 envelope cases from RESEARCH §Test Envelope. Asserts fail initially because `_reflow_volume_cluster` / `_move_volume_cluster_to` / `_set_volume_size_policy` / `_volume_cluster_row1_index` do not yet exist.
- [ ] `.planning/REQUIREMENTS.md` — register `LAYOUT-04` row in the LAYOUT section and the Traceability table (exact text in RESEARCH §`<phase_requirements>`). Coverage summary bumps 62→63 total, 41→42 pending.

*Existing pytest-qt fixtures (`qtbot`, `now_playing_panel` factory if present) cover the framework needs — no new conftest plumbing required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Volume slider drag continuity across width-driven reparent | LAYOUT-04 | Slider drag is a Qt-platform-level interaction; programmatic sliderPressed/Moved/Released triplet under pytest-qt does NOT fully exercise the OS-level mouse-grab-during-resize path (Wayland compositor coalescing). | (1) Launch app on lower display. (2) Resize main window so right pane is ~600px (very-narrow). (3) Start dragging volume slider thumb. (4) While dragging, resize panel wider past threshold — slider should remain grabbed and emit `valueChanged`. (5) Resize narrow again — same. (6) Release. Volume value should be the final position; no Python tracebacks. |
| Visual no-overlap at 560px right-pane width (the reproducer) | LAYOUT-04 | Pixel-perfect "no overlap" assertion across font-rendering and Qt-style backends is fragile in headless test environments. | (1) Launch app, sidebar expanded (compact OFF). (2) Drag splitter or resize main window until right pane is at its 560px minimum. (3) Visually verify: volume slider does not visually overlap compact-toggle button; both fully visible on row 3 (multi-stream) or row 2 (single-stream); transport+actions cluster on row 1 fits without truncation. (4) Repeat on the user's small lower display (the reported reproducer surface). |
| Compact-toggle icon flip across reparent | LAYOUT-04 | Visual confirmation that the icon (`sidebar-hide-symbolic` ↔ `sidebar-show-symbolic`) renders correctly in both row positions. | (1) Toggle compact ON, then resize to very-narrow. Verify the toggle's `sidebar-show-symbolic` icon renders correctly on its wrap row. (2) Toggle compact OFF, verify icon flips back to `sidebar-hide-symbolic` and tooltip updates ("Hide stations (Ctrl+B)"). |
| No flicker on splitter-drag through thresholds | LAYOUT-04 | Flicker budget (≤16ms reparent per UI-SPEC) is hard to measure in pytest-qt; visual confirmation on the Wayland deployment is the load-bearing check. | (1) Drag the main-window splitter slowly across both reflow thresholds (picker threshold and volume-cluster threshold). (2) Watch for visual flicker, layout-reshuffle artifacts, or transient overlapping. Should be smooth in both directions. (3) Resize same boundary back and forth several times rapidly. (4) No accumulated layout corruption; no "ghost" widgets at old positions. |

---

## Validation Sign-Off

- [ ] All 16 envelope cases from RESEARCH §Test Envelope mapped to a task or a manual-only row
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (auto-satisfied; every task in this phase touches the volume-cluster test file)
- [ ] Wave 0 covers TDD-RED scaffold + LAYOUT-04 REQUIREMENTS.md row
- [ ] No watch-mode flags (`-x -q`, no `--ff` outside debugging)
- [ ] Feedback latency < 30s for focused file
- [ ] `nyquist_compliant: true` set in frontmatter once the planner finalizes the per-task map

**Approval:** pending (set by planner during Plan-Phase Wave 0)
