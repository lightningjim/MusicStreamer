---
phase: 60.3
slug: fix-for-icy-label-gap-in-60-2-context-deferred-ideas
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-07
---

# Phase 60.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Detailed test contract is defined in `60.3-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (with pytest-qt for Qt slot wiring) |
| **Config file** | `pyproject.toml` (existing repo config) |
| **Quick run command** | `pytest tests/test_now_playing_panel.py -k "gbs" -x -q` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~10–15 seconds (quick), ~60 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run quick command (filtered to GBS tests)
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 60.3-01-* | 01 (source flag + lifecycle) | 1 | GBS-01 family | — / Pitfall T-40-04 (plain-text invariant) | Source-flag transitions reset on bind/unbind; default `None` outside GBS | unit | `pytest tests/test_now_playing_panel.py -k "gbs_label_source" -x -q` | ✅ existing | ⬜ pending |
| 60.3-02-* | 02 (/ajax authoritative writer) | 2 | GBS-01 family | — | `_on_gbs_playlist_ready` writes `state["icy_title"]` to `icy_label`; flips source flag to `'ajax'`; star+cover-art enable | unit | `pytest tests/test_now_playing_panel.py -k "ajax_stamps_label" -x -q` | ✅ existing | ⬜ pending |
| 60.3-03-* | 03 (kick + no-downgrade + auth-expired) | 2 | GBS-01 family | — / Pitfall (token-discard) | Track-change ICY tag kicks `_on_gbs_poll_tick` when worker idle; skips when in-flight; bare ICY does not overwrite `'ajax'`-stamped label; auth-expired flips source to `'icy'` | unit | `pytest tests/test_now_playing_panel.py -k "kick_or_no_downgrade or auth_expired" -x -q` | ✅ existing | ⬜ pending |

*Plan-level test IDs (60.3-T-01 .. 60.3-T-08) are defined in `60.3-RESEARCH.md § Validation Architecture`. The planner maps each test to a task and copies the assertion contract verbatim into the task's `<acceptance_criteria>`.*

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- No new test infrastructure required. `tests/test_now_playing_panel.py` already covers the GBS cluster (existing tests around line 1005, 1170 — see RESEARCH.md). Inline `state` dict pattern is the model; no `mock_gbs_api` fixture extension needed for 60.3.

*All test files exist. pytest-qt is already a project dependency.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real-stream label upgrade timing | GBS-01 family | Live `/ajax` round-trip + ICY tag emission cannot be deterministically reproduced in unit tests | (1) Run app on Linux Wayland; (2) bind a GBS.FM station while logged in; (3) observe label briefly shows bare ICY title, then upgrades to `Artist - Title` within 1–2s; (4) on track change, label refreshes within 1s. Capture screen recording or screenshot pair (before/after upgrade) for the verify-work record. |
| Bridge-window star/cover-art gating | GBS-01 family | Visible UI state during the sub-second bridge window is hard to assert in unit tests | (1) Bind a GBS.FM station; (2) before /ajax responds, visually confirm the star button is disabled and no cover art request fires (check network log); (3) after /ajax stamps, confirm star enables and cover-art lookup runs against the full `Artist - Title` string. |
| Auth-expired fallback | GBS-01 family | Requires real cookies-expired state | (1) Invalidate yt-cookies.txt or log out of GBS.FM; (2) bind a GBS.FM station; (3) confirm bare ICY title displays via `on_title_changed` and star/cover-art remain enabled. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (none required)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
