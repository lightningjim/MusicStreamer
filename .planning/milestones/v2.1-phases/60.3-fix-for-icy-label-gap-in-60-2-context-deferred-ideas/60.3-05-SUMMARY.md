---
phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
plan: 05
subsystem: ui
tags: [pyqt6, gbs.fm, auth-expired, bridge-gate, gap-closure, cr-02, wr-02, in-01, verification-gap-2]

# Dependency graph
requires:
  - phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
    plan: 04
    provides: "on_title_changed body with restructured `suppress_local_writes` guard + kick split (Plan 05 modifies the SAME function's `in_bridge_window` cover-art predicate); _on_gbs_playlist_ready track-change branch with `_gbs_label_source = None` reset (Plan 05 leaves this untouched)."
  - phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
    plan: 01
    provides: "_gbs_label_source tri-state flag scaffolding — Plan 05 adds the sibling `_gbs_ajax_disabled: bool` immediately after, mirroring Plan 01's placement convention."
provides:
  - "_gbs_ajax_disabled: bool panel-local flag declared in __init__ (init: False). Distinct from _is_gbs_logged_in() (which checks cookies-file existence per Phase 60 D-04 ladder #3) — preserves that signal while encoding 'session is dead even though file persists'."
  - "Bridge predicate hardening at TWO sites: _update_star_enabled (star-button gate) + on_title_changed (cover-art bridge gate). Both gain `and not self._gbs_ajax_disabled` so the gate relaxes after auth_expired even though cookies persist on disk."
  - "Lifecycle resets at THREE sites: bind_station (fresh-station entry), _refresh_gbs_visibility should_show=False branch (leaving GBS context), and the should_show=True branch is DELIBERATELY untouched (WR-02: same-station re-entry preserves the flag)."
  - "_on_gbs_playlist_error('auth_expired') sets _gbs_ajax_disabled = True alongside the existing _gbs_label_source = 'icy' flip."
  - "test_gbs_auth_expired_relaxes_bridge_gate now asserts on _gbs_ajax_disabled is True AND star_btn.isEnabled() is True (closes IN-01: the 13-line apology comment is gone)."
  - "Three new lifecycle invariant tests: test_gbs_refresh_gbs_visibility_preserves_ajax_disabled_on_rebind (WR-02 preservation), test_gbs_bind_station_clears_ajax_disabled_on_fresh_station (fresh-station reset), test_gbs_refresh_gbs_visibility_clears_ajax_disabled_on_leaving_gbs (leaving-GBS reset)."
  - "Closes CR-02 + WR-02 + IN-01 (60.3-REVIEW.md) and Verification Gap 2 (60.3-VERIFICATION.md). D-08 contract per CONTEXT.md line 48 is now satisfied for the auth-expired-mid-runtime path (was only satisfied for logged-out-from-start)."
affects:
  - "Plan 06 (CR-03 favourites refresh in _apply_gbs_icy_label + CR-04 _make_gbs_station factory fix): independent — Plan 05 ships three more BLOCKER #1 explicit overrides (count rises from 9 -> 14)."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Panel-local boolean flag distinct from disk-state oracle: Phase 60 D-04 says cookie-file-existence answers 'do we have credentials?' Plan 05 adds _gbs_ajax_disabled to answer 'did we last hear back that /ajax doesn't work?' — bridge gate is the AND of both. Cleaner than inverse approaches (e.g., deleting the cookies file on auth_expired) because it preserves the D-04 invariant and keeps the two questions independently observable in tests."
    - "Asymmetric lifecycle reset: bind_station + _refresh_gbs_visibility should_show=False both reset the flag, but the should_show=True branch is DELIBERATELY untouched — preserving the auth_expired state across same-station re-entries (e.g., user clicks play/pause on the SAME station). This is the WR-02 invariant. Three tests pin it: one for preservation, two for the two reset paths."

key-files:
  created: []
  modified:
    - "musicstreamer/ui_qt/now_playing_panel.py — five sub-changes. (1) Line 446: declare `self._gbs_ajax_disabled: bool = False` immediately after _gbs_label_source. (2) Line 524: reset to False in bind_station after `self.star_btn.setChecked(False)`. (3) Line 595: add `and not self._gbs_ajax_disabled` to in_bridge_window predicate in on_title_changed. (4) Line 709: add same conjunct to _update_star_enabled bridge predicate; docstring expanded to document both D-08 paths (logged-out-from-start vs. auth-expired-mid-runtime). (5) Line 1007: reset to False in _refresh_gbs_visibility should_show=False branch (NOT should_show=True). (6) Line 1136: set to True in _on_gbs_playlist_error auth_expired branch alongside `_gbs_label_source = 'icy'`."
    - "tests/test_now_playing_panel.py — replace the 13-line apology comment block in test_gbs_auth_expired_relaxes_bridge_gate (was lines 1643-1655) with `assert panel._gbs_ajax_disabled is True` and `assert panel.star_btn.isEnabled() is True`. Insert three new lifecycle tests after test_gbs_logged_out_icy_writes_normally (before the GBS-01d vote-control banner at line 1683)."

key-decisions:
  - "Option (b) `_gbs_ajax_disabled` flag chosen over Option (a) cookie deletion. Cookie deletion would invalidate Phase 60 D-04 (cookie-file-existence as login-state ladder #3 signal) and conflate two distinct states: 'no credentials on disk' vs. 'credentials on disk but server says they're stale'. Option (b) keeps the two questions independently observable; _is_gbs_logged_in() body is unchanged, no os.remove() added anywhere."
  - "Asymmetric reset semantics in _refresh_gbs_visibility: only should_show=False resets the flag. Same-station re-entry (e.g., the path through _on_play_pause_clicked -> bind_station(self._station) -> _refresh_gbs_visibility with the same Station object) keeps should_show=True and must NOT clobber the auth_expired state. Test_gbs_refresh_gbs_visibility_preserves_ajax_disabled_on_rebind exercises this directly."
  - "bind_station does the fresh-station reset, not _refresh_gbs_visibility. Rationale: bind_station is the entry point for a NEW station (per the docstring — 'Attach a Station and reset the panel for playback of that station'). _refresh_gbs_visibility runs on every play/pause cycle for the SAME station too — putting the reset there would silently break WR-02. Test_gbs_bind_station_clears_ajax_disabled_on_fresh_station pins this: bind a different GBS station -> flag clears."
  - "Inline conjunct (`and not self._gbs_ajax_disabled`) at both bridge sites, not a helper method `_ajax_possible()`. Rationale: matches Plan 03's predicate shape (each conjunct visible inline), keeps the diff readable against the prior REVIEW snippets, and lets a future maintainer scan a single function for the full bridge logic without jumping to a helper. The flag has exactly four writers and two readers — a method wrapper would obscure that asymmetry."
  - "Three new lifecycle tests, not one combined test. The three resets/preservations are independent state machines; one combined test would mask a regression in any single transition. With three tests: a future change that wipes the flag in should_show=True would break test (1) only; a change that fails to clear on fresh-bind would break test (2) only; a change that fails to clear on leaving GBS would break test (3) only. The error message tells the maintainer exactly which invariant fired."

requirements-completed: []  # Phase 60.3 has no formal REQ-IDs; acceptance == CONTEXT.md decisions D-NN. CR-02 / WR-02 / IN-01 / Verification Gap 2 all closed.

# Metrics
duration: ~5min
completed: 2026-05-07
---

# Phase 60.3 Plan 05: D-08 Auth-Expired Bridge Gate Fix (CR-02 + WR-02 + IN-01 / Verification Gap 2) Summary

**Closes CR-02, WR-02, IN-01, and Verification Gap 2: introduce a panel-local `_gbs_ajax_disabled` boolean that distinguishes 'cookies-on-disk' (Phase 60 D-04 invariant) from '/ajax-is-impossible' (auth_expired received). Both bridge predicates (`_update_star_enabled` and `on_title_changed`'s `in_bridge_window`) gain a `not self._gbs_ajax_disabled` conjunct so star + cover-art come alive when /ajax dies mid-runtime — exactly the D-08 contract. Asymmetric lifecycle resets (bind_station + leaving-GBS clear; same-station re-entry preserves) close WR-02. Test cleanup replaces the IN-01 apology comment with real assertions.**

## What Changed

### Code (musicstreamer/ui_qt/now_playing_panel.py)

| Site | Change | Line |
|------|--------|------|
| `__init__` | Declare `self._gbs_ajax_disabled: bool = False` adjacent to `_gbs_label_source` | 446 |
| `bind_station` | Reset to `False` after `star_btn.setChecked(False)` (fresh-station entry) | 524 |
| `on_title_changed` `in_bridge_window` | Add `and not self._gbs_ajax_disabled` conjunct (cover-art bridge) | 595 |
| `_update_star_enabled` | Add same conjunct; docstring expanded to document both D-08 paths | 709 |
| `_refresh_gbs_visibility` (`should_show=False`) | Reset to `False` (leaving-GBS-context) — should_show=True branch DELIBERATELY untouched | 1007 |
| `_on_gbs_playlist_error('auth_expired')` | Set to `True` alongside the existing `_gbs_label_source = 'icy'` flip | 1136 |

### Tests (tests/test_now_playing_panel.py)

- **`test_gbs_auth_expired_relaxes_bridge_gate`** — 13-line apology comment block (`# D-08 gate relaxation: ...` through `# (No assertion on star_btn here — see test_gbs_logged_out_icy_writes_normally.)`) replaced with two assertions: `panel._gbs_ajax_disabled is True` and `panel.star_btn.isEnabled() is True`. **Closes IN-01.**
- **NEW `test_gbs_refresh_gbs_visibility_preserves_ajax_disabled_on_rebind`** — same-station rebind sequence: bind GBS -> auth_expired sets flag -> call `_refresh_gbs_visibility()` again -> flag MUST survive -> on_title_changed -> star MUST stay enabled. **Closes WR-02 (preservation invariant).**
- **NEW `test_gbs_bind_station_clears_ajax_disabled_on_fresh_station`** — bind Station X -> auth_expired -> bind Station Y -> flag clears. **Closes WR-02 (fresh-station reset).**
- **NEW `test_gbs_refresh_gbs_visibility_clears_ajax_disabled_on_leaving_gbs`** — bind GBS -> auth_expired -> bind non-GBS station -> flag clears via the `should_show=False` branch. **Closes WR-02 (leaving-GBS reset).**

## Phase 60 D-04 Invariant Preserved

`_is_gbs_logged_in()` body is **unchanged**. No `os.remove(cookies)` or `os.unlink(cookies)` added anywhere. The cookies file persists on disk through auth_expired exactly as Phase 60 D-04 requires; only `_gbs_ajax_disabled` flips. This was the whole point of Option (b) over Option (a) (cookie deletion) — the two questions ('cookies on disk?' and 'does /ajax work?') are now independently observable.

## Test Counts

| Suite | Before Plan 05 | After Plan 05 | Delta |
|-------|----------------|---------------|-------|
| Full panel suite | 89 passed | **92 passed** | +3 |
| GBS cluster (`-k gbs`) | 37 passed | **40 passed** | +3 |
| GBS+ICY cluster (`-k "gbs or icy"`) | 44 passed | **47 passed** | +3 |

## Deviations from Plan

**None.** All five sub-changes in Task 1 and both sub-changes in Task 2 landed verbatim per the plan body. The flag is declared, set, reset, and read at exactly the six sites specified; the three new tests use the WR-02-preservation, fresh-station-reset, and leaving-GBS-reset shapes from the plan body verbatim.

One **non-issue worth flagging**: the plan's acceptance grep `awk '/def bind_station/,/def [^ ]/' ... | grep -c "self._gbs_ajax_disabled = False"` returns 0 due to a self-matching range pattern (the awk end-pattern matches the same `def bind_station` line). The actual code is correct: `self._gbs_ajax_disabled = False` is present at line 524, inside `bind_station` (lines 504-541). Verified via direct line-number lookup. Same caveat applies to the `_on_gbs_playlist_error` awk grep — substantive content is right, the awk pattern is buggy. Not a Plan 05 defect.

## Cross-Reference

- **Plan 04 closed:** Verification Gap 1 (D-05 no-downgrade kick split + track-change reset) + REVIEW CR-01.
- **Plan 05 closes:** Verification Gap 2 (D-08 auth-expired bridge gate) + REVIEW CR-02 + WR-02 + IN-01.
- **Plan 06 (Wave 3, pending):** REVIEW CR-03 (favourites refresh in `_apply_gbs_icy_label`) + CR-04 (`_make_gbs_station` factory fix). Independent of Plan 05's code paths.

Both VERIFICATION.md BLOCKER gaps are now resolved. The two remaining REVIEW items (CR-03, CR-04) are scoped to Plan 06.

## Self-Check: PASSED

**Files claimed exist:**
- `musicstreamer/ui_qt/now_playing_panel.py` — modified (verified via `git diff HEAD~2 HEAD`)
- `tests/test_now_playing_panel.py` — modified (verified via `git diff HEAD~1 HEAD`)
- `.planning/phases/60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas/60.3-05-SUMMARY.md` — created (this file)

**Commits claimed exist:**
- `5928bfb` (`fix(60.3-05): add _gbs_ajax_disabled flag to fix CR-02 auth_expired bridge gate`) — confirmed via `git log --oneline | grep 5928bfb`
- `d923665` (`test(60.3-05): close IN-01 + add three WR-02 lifecycle invariant tests`) — confirmed via `git log --oneline | grep d923665`

**Test results match claim:** `uv run pytest tests/test_now_playing_panel.py -x -q` → 92 passed.
