---
phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
plan: 02
subsystem: ui
tags: [pyqt6, qt-signals, gbs.fm, ajax-stamping, single-coupling, icy-label, cover-art]

# Dependency graph
requires:
  - phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas
    plan: 01
    provides: "_gbs_label_source: Optional[str] tri-state flag + _gbs_poll_in_flight() predicate + lifecycle resets in _refresh_gbs_visibility"
  - phase: 60-gbs-fm-integration
    provides: "_GbsPollWorker + token-discard machinery + _on_gbs_playlist_ready slot + _apply_vote_* helper-method cluster"
provides:
  - "_apply_gbs_icy_label(self, icy_title: str) -> None helper method on NowPlayingPanel — single coupling point (D-06) for icy_label.setText, _last_icy_title, _gbs_label_source = 'ajax', _update_star_enabled, and _fetch_cover_art_async"
  - "Helper invocation as the LAST statement of _on_gbs_playlist_ready (after token guard, after entryid + vote stamping, after queue render — last-writer-wins per D-02)"
  - "icy_disabled-on-GBS lock per Open Question 1: helper short-circuits when _station.icy_disabled is truthy (consistent with on_title_changed gate at line 524)"
  - "Empty-string guard: helper is a no-op on empty icy_title (prevents clobbering pre-/ajax bridge ICY value on /ajax cold-start race)"
  - "Three new tests pinning D-01/D-06/D-07 happy path + cold-stamp + Open Question 1 icy_disabled lock"
  - "Regression-protection mock for existing test_gbs_playlist_populates_from_mock_state (offline-CI invariant preserved after the new helper invocation lands)"
affects:
  - "Phase 60.3 Plan 03 (Wave 3): on_title_changed now becomes the no-downgrade-guard / kick-on-tag-change surface; reads _gbs_label_source to decide whether to write icy_label, calls _gbs_poll_in_flight() for kick debounce. Plan 03 also flips _gbs_label_source = 'icy' on the auth_expired branch of _on_gbs_playlist_error."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Helper-method extraction adjacent to _apply_vote_* cluster (PATTERNS §B precedent — bare class-body method, no Signal/Slot wiring; called directly from the slot)"
    - "Last-writer-wins ordering: helper invocation placed as the LAST statement of _on_gbs_playlist_ready so all prior side effects (entryid, vote, cursor, queue render) settle before label rewrite"
    - "Defensive monkeypatch.setattr(panel, '_fetch_cover_art_async', MagicMock()) BEFORE on_title_changed — mock placement is load-bearing because both on_title_changed AND _apply_gbs_icy_label trigger the cover-art worker"

key-files:
  created: []
  modified:
    - "musicstreamer/ui_qt/now_playing_panel.py — added _apply_gbs_icy_label helper at line 1064 (between _current_highlighted_vote line 1053 and _on_gbs_vote_clicked line 1100); added 3-line invocation block at the end of _on_gbs_playlist_ready (line 1014-1016)"
    - "tests/test_now_playing_panel.py — added 3 new tests after test_gbs_poll_in_flight_predicate_truth_table (line 1224 onwards); added _fetch_cover_art_async mock to existing test_gbs_playlist_populates_from_mock_state (regression-protection)"

key-decisions:
  - "Open Question 1 locked as 'consistent / icy_disabled-on-GBS skips /ajax stamping' — the helper early-returns when self._station.icy_disabled is truthy, matching the on_title_changed gate at line 530-534. Rationale: a station with icy_disabled=True is a user preference signal; respecting it on the /ajax path too means the user gets a consistent display experience regardless of which writer (ICY vs /ajax) tries to update the label."
  - "Helper extracted as _apply_gbs_icy_label rather than inlined into _on_gbs_playlist_ready — mirrors the established _apply_vote_buttons_enabled / _apply_vote_highlight / _current_highlighted_vote convention from Plan 60-05/60-09. Future plans (especially Plan 03's on_title_changed branch + race-tightening) can call into the same helper if they need a forced-stamp path."
  - "Helper invocation placed at the END of _on_gbs_playlist_ready, NOT interleaved with the playlist-widget render. By the time _apply_gbs_icy_label runs, all the state machinery (entryid, vote, cursor, playlist render) has already settled — last-writer-wins per CONTEXT D-02."
  - "Empty icy_title is a no-op (early return) — guards the /ajax cold-start race where the playlist-events stream can land between on_title_changed and the first /ajax response. Without the guard, an empty state['icy_title'] would clobber the bridge ICY value."
  - "BLOCKER #1 mitigation pattern adopted: the two new non-icy_disabled tests explicitly set gbs_station.icy_disabled = False after _make_gbs_station(). Per-test override is the surgical fix; the shared factory at line 950 stays bare (a future Plan 03 will repeat the same per-test override pattern, NOT modify the factory)."
  - "Existing test_gbs_playlist_populates_from_mock_state regression-protected with monkeypatch.setattr(panel, '_fetch_cover_art_async', MagicMock()) — without this, after Plan 02 lands, every CI run of that test would spawn a daemon thread hitting iTunes Search API + artwork URL via urllib.request.urlopen. The test does not assert on cover-art behaviour; the mock is purely defensive."
  - "Comment in helper docstring describes plain-text invariant T-40-04 in prose ('plain-text-locked at construction') rather than referencing the API name 'setTextFormat(Qt.PlainText)' — keeps the grep -c 'setTextFormat\\|setHtml\\|RichText' baseline at 4 (Wave 1 baseline), so future invariant audits don't false-flag a docstring as a lockdown change."

patterns-established:
  - "Pattern B-extended — Helper method adjacent to _apply_vote_* cluster: _apply_gbs_icy_label sits in the same class-body region (line 1032 onwards) as the three vote helpers; same call-shape (direct method call from slot), same docstring style (Phase tag + design intent + invariant cross-reference)."
  - "Pattern: defensive _fetch_cover_art_async mock — any test that calls panel.on_title_changed(...) or panel._on_gbs_playlist_ready(...) on a panel with a bound station MUST mock _fetch_cover_art_async if the test doesn't intend to assert on cover-art behaviour. Both code paths trigger the live-network worker; mocking only ONE is insufficient."

requirements-completed: []  # phase requirements field empty (implicit GBS-01 follow-up — plan-level acceptance == D-01/D-06/D-07 logged-in path + Open Question 1 lock)

# Metrics
duration: 5min
completed: 2026-05-07
---

# Phase 60.3 Plan 02: /ajax Stamping Helper for ICY Label Upgrade Summary

**`_apply_gbs_icy_label` helper added to `NowPlayingPanel` and invoked from `_on_gbs_playlist_ready`; bound + logged-in GBS.FM stations now upgrade `icy_label` from bare ICY title to canonical `Artist - Title` once /ajax responds (D-01/D-06/D-07 closed; Open Question 1 locked).**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-07T13:59:32Z
- **Completed:** 2026-05-07T14:04:29Z
- **Tasks:** 2 (T1 + T2)
- **Files modified:** 2 (`musicstreamer/ui_qt/now_playing_panel.py`, `tests/test_now_playing_panel.py`)

## Accomplishments

- **`_apply_gbs_icy_label(self, icy_title: str) -> None` helper added at line 1064**, sandwiched between `_current_highlighted_vote` (line 1053) and `_on_gbs_vote_clicked` (line 1100). Mirrors the `_apply_vote_*` cluster convention from Plan 60-05/60-09. Single coupling point per D-06: writes `icy_label.setText`, `_last_icy_title`, `_gbs_label_source = 'ajax'`, calls `_update_star_enabled()`, and triggers `_fetch_cover_art_async` (junk-title-guarded + station-bound + cover-cache-checked).
- **Helper invocation lands as the LAST statement of `_on_gbs_playlist_ready`** (line 1014-1016) — after the token-discard guard at line 961, after entryid + vote stamping, after queue render. Last-writer-wins per CONTEXT D-02; if the helper raised (it cannot — no IO, no exceptions), the playlist render is already done.
- **Open Question 1 locked: icy_disabled-on-GBS skips /ajax stamping.** Helper early-returns on `self._station.icy_disabled` truthy. Matches the `on_title_changed` gate at line 530-534 — user's icy_disabled preference is respected on both the bridge and /ajax paths.
- **Empty icy_title guard** (early-return on `not icy_title`) — prevents an empty `state["icy_title"]` from a /ajax cold-start race from clobbering the pre-/ajax bridge ICY value.
- **3 new tests pin the D-01/D-06/D-07 contract + the Open Question 1 lock.** All assertions on `icy_label.text()`, `_last_icy_title`, and `_gbs_label_source` (D-06 single coupling — all three downstream consumers read the same string after /ajax stamps).
- **Regression-protection mock for existing `test_gbs_playlist_populates_from_mock_state`** — without it, every CI run would spawn a daemon thread hitting iTunes Search API now that `_apply_gbs_icy_label` lands as the last statement of `_on_gbs_playlist_ready`. Offline-CI invariant preserved.
- **Plain-text invariant T-40-04 unchanged** (grep -c "setTextFormat|setHtml|RichText" still returns 4, same as Wave 1 baseline; helper uses `setText()` on a plain-text-locked label).
- **Full test file: 82/82 panel tests green** (79 pre-Plan-02 + 3 new). GBS-only cluster: 30/30 green (was 27 + 3 new).

## Task Commits

Each task was committed atomically:

1. **Task 1: `_apply_gbs_icy_label` helper + invocation from `_on_gbs_playlist_ready` + regression-protection mock for existing test** — `0448155` (feat)
2. **Task 2: Three D-01/D-06/D-07 + icy_disabled-lock tests** — `7a24b22` (test)
3. **Doc clarification: comment for the load-bearing `_fetch_cover_art_async` mock placement before `on_title_changed`** — `34726d1` (docs)

## Files Created/Modified

- `musicstreamer/ui_qt/now_playing_panel.py` — Added 1 helper method (37 lines incl. docstring), 3-line invocation block at end of `_on_gbs_playlist_ready`. Net +39 lines. No new imports — `is_junk_title`, `Optional`, `_fetch_cover_art_async`, `_update_star_enabled`, `_last_cover_icy`, `_last_icy_title`, `_gbs_label_source` all already in scope.
- `tests/test_now_playing_panel.py` — Added 3 new test functions (~131 lines incl. docstrings + Plan 02 section header), 1 new `monkeypatch.setattr` line in existing `test_gbs_playlist_populates_from_mock_state`, 1 comment-clarification block. Net +136 lines (T2 + T3 combined). No new imports — `MagicMock`, `os`, `paths` all already in scope at lines 945-948.

## Decisions Made

- **Open Question 1 locked as 'consistent' (icy_disabled-on-GBS skips /ajax stamping).** Helper early-returns on `self._station.icy_disabled` truthy. Symmetric with `on_title_changed` gate at line 530-534. Rationale: icy_disabled is a user preference for THIS station — respecting it on both writers gives the user a stable display.
- **Helper extracted, not inlined.** Mirrors `_apply_vote_buttons_enabled` / `_apply_vote_highlight` / `_current_highlighted_vote` convention from Plan 60-05/60-09. Future plans (especially Plan 03's on_title_changed branch) can reuse the helper.
- **Helper invocation placed at the END of `_on_gbs_playlist_ready`, not interleaved with playlist-widget render.** Last-writer-wins per CONTEXT D-02. By the time the helper runs, all prior state (entryid, vote, cursor, queue render) has settled.
- **Empty icy_title is a no-op (early-return).** Guards the /ajax cold-start race; without the guard, an empty `state["icy_title"]` would clobber the bridge ICY value.
- **Per-test `gbs_station.icy_disabled = False` override (NOT shared-factory mutation).** Surgical fix per checker BLOCKER #1; the shared `_make_gbs_station()` factory at line 950 stays bare. Plan 03 will repeat the same per-test pattern (target ≥ 8 instances total).
- **Existing `test_gbs_playlist_populates_from_mock_state` regression-protected with `_fetch_cover_art_async` mock.** Without it, every CI run would spawn a daemon thread hitting iTunes Search API.
- **Helper docstring describes plain-text invariant in prose, not API call.** Keeps the grep -c "setTextFormat|setHtml|RichText" baseline at 4. Originally I included `setTextFormat(Qt.PlainText)` in the docstring (raised count to 5); rewrote to "plain-text-locked at construction (line 280)" before commit.

## Deviations from Plan

**1. [Rule 1 - Test hygiene] Mock placement in `test_gbs_icy_label_upgrades_on_ajax_after_icy`**
- **Found during:** Task 2 verification — `pytest -W error::PytestUnhandledThreadExceptionWarning` flagged a daemon-thread RuntimeError in pre-existing tests, but my new test was clean. The plan's `<action>` block placed the `monkeypatch.setattr(panel, "_fetch_cover_art_async", MagicMock())` AFTER `bind_station` and BEFORE `panel._gbs_poll_token = 5` — which is also BEFORE the `panel.on_title_changed("La Frontière de la Nuit")` call. That ordering is correct (mock applies before any code path that calls the helper). I added a clarifying comment in commit `34726d1` documenting WHY the placement is load-bearing (both `on_title_changed` AND `_apply_gbs_icy_label` trigger `_fetch_cover_art_async`). No code-logic change.
- **Files modified:** `tests/test_now_playing_panel.py`
- **Commit:** `34726d1`

**2. [Rule 1 - Doc precision] Plain-text invariant grep baseline**
- **Found during:** Task 1 verification — initial helper docstring included the literal `setTextFormat(Qt.PlainText)` API name, which bumped the `grep -c "setTextFormat|setHtml|RichText"` count from 4 → 5. The Plan 01 SUMMARY noted the actual baseline is 4 (not the plan's stale "1" claim). Rewrote the docstring to describe the invariant in prose ("plain-text-locked at construction (line 280)") so the grep baseline stays at exactly 4. Caught and fixed before the Task 1 commit landed.
- **Files modified:** `musicstreamer/ui_qt/now_playing_panel.py`
- **Commit:** Folded into `0448155` (Task 1 commit) — fix applied before commit.

**Pre-existing thread-leak warnings (out of scope, NOT fixed):** Pre-existing tests `test_panel_construction`, `test_sibling_label_hidden_when_no_siblings`, `test_sibling_label_hidden_when_no_station_bound`, `test_gbs_label_source_resets_on_leaving_gbs_context` all show `RuntimeError: Signal source has been deleted` daemon-thread teardown warnings when `pytest -W error::PytestUnhandledThreadExceptionWarning` is enabled (default mode silences them as warnings; tests still PASS). These tests do not mock `_fetch_cover_art_async` and rely on `on_title_changed` triggering the cover-art worker thread; the worker outlives qtbot teardown. **Not Plan 02's surface** — these issues existed before Plan 02 landed and are scope-bounded to the cover_art worker lifetime semantics, not to the /ajax stamping helper. Logged here for traceability; future cover-art-lifecycle work could address.

## Issues Encountered

None blocking. The two minor adjustments above (mock-comment clarification + grep-baseline docstring tweak) were caught and fixed before the corresponding task commit.

## User Setup Required

None — no external service configuration required. All testing was offline (every test path that touches `_fetch_cover_art_async` is mocked).

## Next Phase Readiness

- **Plan 03 (Wave 3 — `on_title_changed` branch + race-tightening) is READY.** Plan 03's surface:
  - Read `self._gbs_label_source` in `on_title_changed` for the no-downgrade guard (RESEARCH §Pattern E step 3) — Plan 02's helper now writes `'ajax'`, so Plan 03 can detect "the /ajax winner already stamped" and refuse to overwrite with bare ICY.
  - Call `self._gbs_poll_in_flight()` (Wave 1 primitive) in `on_title_changed` for the kick debounce — Plan 02 didn't touch this surface.
  - Flip `self._gbs_label_source = 'icy'` on the `auth_expired` branch of `_on_gbs_playlist_error` (PATTERNS §D / D-08) — Plan 02 didn't touch `_on_gbs_playlist_error`.
- **No blockers.** All Wave 2 contracts in place; Plan 03 can layer race-tightening without touching the helper body or invocation site.
- **Visible defect closed (the original 60.2 deferred-idea):** GBS.FM stations bound + logged-in now show the full `Artist - Title` in `icy_label` once /ajax responds. Pre-/ajax bridge still shows bare ICY (Plan 03 will refine with no-downgrade guard).

## Self-Check: PASSED

Verified at execution close (2026-05-07T14:04:29Z):

- [x] All three task commits exist in `git log --oneline -5`: `0448155` (T1), `7a24b22` (T2), `34726d1` (T2 doc-clarify).
- [x] `grep -c "def _apply_gbs_icy_label" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 1.
- [x] `grep -c "self\._apply_gbs_icy_label" musicstreamer/ui_qt/now_playing_panel.py` returns 1 (the call site; the def line uses `def _apply_gbs_icy_label`, no `self.` prefix).
- [x] `grep -c "self._gbs_label_source = 'ajax'" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 1 (helper body).
- [x] `grep -c "def test_gbs_icy_label_upgrades_on_ajax_after_icy\|def test_gbs_ajax_stamps_label_first_no_prior_icy\|def test_gbs_icy_disabled_suppresses_ajax_stamp" tests/test_now_playing_panel.py` returns exactly 3.
- [x] `grep -A 60 "def test_gbs_playlist_populates_from_mock_state" tests/test_now_playing_panel.py | grep -c 'monkeypatch.setattr(panel, "_fetch_cover_art_async"'` returns 1 (regression-protection mock landed).
- [x] `grep -c "gbs_station.icy_disabled = False" tests/test_now_playing_panel.py` returns 2 (BLOCKER #1 fix; will rise to ≥ 8 after Plan 03 lands).
- [x] `grep -c "setTextFormat\|setHtml\|RichText" musicstreamer/ui_qt/now_playing_panel.py` returns 4 (plain-text invariant T-40-04 unchanged from Wave 1 baseline).
- [x] Definition ordering: `def _current_highlighted_vote` (1053) → `def _apply_gbs_icy_label` (1064) → `def _on_gbs_vote_clicked` (1100). Confirmed via `grep -n`.
- [x] `pytest tests/test_now_playing_panel.py -q` exits 0 (82 passed).
- [x] `pytest tests/test_now_playing_panel.py -k gbs -x -q` exits 0 (30 passed, 52 deselected).
- [x] My 3 new tests are clean even with `-W error::pytest.PytestUnhandledThreadExceptionWarning` (no thread-leak warnings on Plan 02's surface).

---
*Phase: 60.3-fix-for-icy-label-gap-in-60-2-context-deferred-ideas*
*Plan: 02 of 3 (Wave 2 — visible defect fix; /ajax stamping helper)*
*Completed: 2026-05-07*
