---
phase: 72.3
plan: 03
type: execute
wave: 2
depends_on: ["72.3-02"]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: true
requirements: [LAYOUT-03]
tags: [layout, pyside6, ui-qt, refactor, resizeevent, tdd-green, layout-03, phase-72-3]
must_haves:
  truths:
    - "The three pixmap render call sites at lines 1462-1470 (_set_cover_pixmap), 1493-1495 (_show_station_logo), and 1497-1499 (_show_station_logo_in_cover_slot) ALL read the central tier integer (via `n = self._current_art_tier or 180`) instead of hard-coded QSize(180, 180) / QSize(160, 160) literals."
    - "_set_cover_pixmap also writes `self._last_cover_path = path` at the tail (after successfully calling `self.cover_label.setPixmap(scaled)`) — Pattern 4 path tracking."
    - "_show_station_logo_in_cover_slot writes `self._last_cover_path = None` at the tail — Pattern 4 fallback-state tracking (the cover slot is showing the station logo, not a real cover)."
    - "The existing Phase 72.1 resizeEvent override at line 1149 is EXTENDED (NOT duplicated) with a SINGLE new bound-method call `self._apply_art_tier()` placed AFTER `super().resizeEvent(event)` AND AFTER `self._reflow_stream_picker_row()`. The docstring on the override is updated to mention Phase 72.3 / LAYOUT-03 alongside the existing Phase 72.1 / LAYOUT-02 reference. Exactly ONE resizeEvent override on NowPlayingPanel after this plan."
    - "All 13 tests in tests/test_phase72_3_responsive_art.py turn GREEN."
    - "No legacy hard-coded `QSize(180, 180)` or `QSize(160, 160)` literals survive in now_playing_panel.py at the three refactored render sites. The only surviving 180,180 literals are the two setFixedSize calls in __init__ — the medium-tier defaults at construction."
    - "No SQLite write (D-09 inheritance). No lambda. No new imports. No edits to _theme.py, MainWindow, Repo, Player, _load_scaled_pixmap, or any other file."
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "Three render-site refactors reading central tier + _last_cover_path mutation in cover slots + extended Phase 72.1 resizeEvent override calling _apply_art_tier"
      contains: "self._apply_art_tier()"
  key_links:
    - from: "musicstreamer/ui_qt/now_playing_panel.py::resizeEvent"
      to: "musicstreamer/ui_qt/now_playing_panel.py::_apply_art_tier"
      via: "self._apply_art_tier() bound-method call AFTER self._reflow_stream_picker_row()"
      pattern: "self\\._apply_art_tier\\(\\)"
    - from: "musicstreamer/ui_qt/now_playing_panel.py::_set_cover_pixmap"
      to: "self._last_cover_path = path"
      via: "tail assignment after setPixmap"
      pattern: "self\\._last_cover_path = path"
    - from: "musicstreamer/ui_qt/now_playing_panel.py::_show_station_logo_in_cover_slot"
      to: "self._last_cover_path = None"
      via: "tail assignment after setPixmap"
      pattern: "self\\._last_cover_path = None"
---

<objective>
Wave 2 GREEN closure — refactor the three pixmap render call sites to read the central tier (D-03), wire `_apply_art_tier` into the existing Phase 72.1 `resizeEvent` override (D-06), and add the `_last_cover_path` mutation in both cover slots (Pattern 4).

This plan completes the LAYOUT-03 implementation. Four edits land in a single file:

1. `_set_cover_pixmap` (line 1462-1470) — replace `QSize(160, 160)` with `QSize(n, n)` reading `n = self._current_art_tier or 180`; add `self._last_cover_path = path` at the tail.
2. `_show_station_logo` (line 1493-1495) — replace `QSize(180, 180)` with `QSize(n, n)` reading the same central tier.
3. `_show_station_logo_in_cover_slot` (line 1497-1499) — replace `QSize(160, 160)` with `QSize(n, n)`; add `self._last_cover_path = None` at the tail.
4. `resizeEvent` (line 1149-1159) — extend with one new line `self._apply_art_tier()` placed AFTER the existing `self._reflow_stream_picker_row()` call; update the docstring to reference both phases.

After Plan 03: all 13 Plan 01 RED tests turn GREEN. The phase ships.

Output: One file modified (`now_playing_panel.py`). Four edits totaling ~10 line changes. No new methods, no new imports, no other file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-CONTEXT.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-01-SUMMARY.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-02-SUMMARY.md

<interfaces>
<!-- Lives in musicstreamer/ui_qt/now_playing_panel.py. Confirmed at HEAD on main 2026-05-19; Plan 02 confirmed all helpers callable. -->
- def _current_art_tier_size(self) -> int  (Plan 02 — pure helper returning 140/180/240)
- def _apply_art_tier(self) -> None  (Plan 02 — cached-diff orchestrator; calls setFixedSize + re-render)
- self._current_art_tier: Optional[int]  (Plan 02 — sentinel attribute, None at construction)
- self._last_cover_path: Optional[str]  (Plan 02 — sentinel attribute, None at construction)
- def resizeEvent(self, event)  (Phase 72.1 — currently calls super then self._reflow_stream_picker_row; Plan 03 extends)
- def _set_cover_pixmap(self, path: str) -> None  (existing — currently hard-codes QSize(160, 160); Plan 03 refactors + adds path tracking)
- def _show_station_logo(self) -> None  (existing — currently hard-codes QSize(180, 180); Plan 03 refactors)
- def _show_station_logo_in_cover_slot(self) -> None  (existing — currently hard-codes QSize(160, 160); Plan 03 refactors + clears path tracking)
- def _load_scaled_pixmap(path: Optional[str], size: QSize) -> QPixmap  (module-level helper at line 201 — signature unchanged)
</interfaces>

@musicstreamer/ui_qt/now_playing_panel.py
@tests/test_phase72_3_responsive_art.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor _set_cover_pixmap / _show_station_logo / _show_station_logo_in_cover_slot to read central tier and track cover path</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py lines 1462-1500 (the three render call sites — confirm current bodies match the literals targeted by this refactor)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §E (verbatim refactored body for _set_cover_pixmap — two material additions: n = self._current_art_tier or 180 reads the central tier with medium fallback; self._last_cover_path = path at the tail enables Pattern 4 replay)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §F (verbatim refactored body for _show_station_logo — single line swap reading the central tier)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §G (verbatim refactored body for _show_station_logo_in_cover_slot — central tier read AND self._last_cover_path = None at the tail)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md §Pattern 4 (re-render bound pixmaps after tier change — branch logic depends on path tracking)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md Pitfall 2 (cover-fetch-token semantics — within-station tier change is invariant-safe, no new mitigation needed) and Pitfall 3 (_last_cover_path going stale — null-pixmap fallback at line 1465 handles it; no new code needed)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md §Per-Render-Site Source-of-Truth Mapping (the canonical mapping table from old QSize literals to the new QSize(N, N) central-tier read)
  </read_first>
  <action>
    Three edits inside `musicstreamer/ui_qt/now_playing_panel.py`:

    (1) `_set_cover_pixmap` (lines 1462-1470) — refactor to PATTERNS.md §E verbatim. The new body:
    - Builds the QPixmap from path as before.
    - On null pixmap, calls `self._show_station_logo_in_cover_slot()` and returns (existing behavior — Pitfall 3 covers stale-temp-file fallback).
    - Reads the central tier: `n = self._current_art_tier or 180` (the medium-tier fallback for the brief window between __init__ and the first resizeEvent where _current_art_tier is still None).
    - Scales: `scaled = pix.scaled(QSize(n, n), Qt.KeepAspectRatio, Qt.SmoothTransformation)`.
    - Sets the pixmap: `self.cover_label.setPixmap(scaled)`.
    - Tail assignment: `self._last_cover_path = path` — Pattern 4 tracking. MUST be after the successful setPixmap to avoid recording a path that never rendered.
    - Add an inline comment citing Phase 72.3 / LAYOUT-03 / D-03 + Pattern 4.

    (2) `_show_station_logo` (lines 1493-1495) — refactor to PATTERNS.md §F verbatim:
    - Resolve path as before: `path = self._station.station_art_path if self._station else None`.
    - Read central tier: `n = self._current_art_tier or 180`.
    - Set pixmap: `self.logo_label.setPixmap(_load_scaled_pixmap(path, QSize(n, n)))`.
    - Inline comment citing D-03.

    (3) `_show_station_logo_in_cover_slot` (lines 1497-1499) — refactor to PATTERNS.md §G verbatim:
    - Resolve path as before.
    - Read central tier: `n = self._current_art_tier or 180`.
    - Set pixmap: `self.cover_label.setPixmap(_load_scaled_pixmap(path, QSize(n, n)))`.
    - Tail assignment: `self._last_cover_path = None` — Pattern 4 fallback-state tracking. Signals to _apply_art_tier on the next tier change that the cover slot is showing the logo fallback, not a real cover, so the fallback path is replayed on tier change rather than _set_cover_pixmap being called with a stale _last_cover_path.

    All edits are surgical — keep method signatures unchanged, keep docstrings if present, do not restructure the methods otherwise.

    No new imports needed (QSize, Qt, QPixmap, Optional already imported in this module).
  </action>
  <verify>
    <automated>grep -cE 'QSize\(160, 160\)|QSize\(180, 180\)' musicstreamer/ui_qt/now_playing_panel.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'QSize\(160, 160\)|QSize\(180, 180\)' musicstreamer/ui_qt/now_playing_panel.py` returns 0 (all three call sites refactored; no surviving QSize literals).
    - `grep -c "self._current_art_tier or 180" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 3 (one per refactored render site).
    - `grep -c "QSize(n, n)" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 3.
    - `grep -c "self._last_cover_path = path" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 1 (only in _set_cover_pixmap tail).
    - `grep -c "self._last_cover_path = None" musicstreamer/ui_qt/now_playing_panel.py` returns at least 2 (one is the __init__ sentinel from Plan 02; one is the new tail line in _show_station_logo_in_cover_slot).
    - `grep -c "setFixedSize(180, 180)" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 2 (the two setFixedSize defaults in __init__ — Plan 02's logo + cover defaults at the medium tier — unchanged by this plan).
    - `grep -c "setFixedSize(160, 160)" musicstreamer/ui_qt/now_playing_panel.py` returns 0 (Plan 02 already removed; this plan re-asserts the invariant).
    - `uv run python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; print('OK')"` prints `OK` (no syntax errors).
    - `uv run pytest tests/test_phase72_3_responsive_art.py::test_cover_art_ready_after_resize_matches_tier -x` exits 0 (test 9 turns GREEN — _set_cover_pixmap reads the current tier so a 240-tier panel resizing then receiving a cover scales to 240x240).
    - No imports added (`git diff musicstreamer/ui_qt/now_playing_panel.py | grep -cE '^\+from |^\+import '` equals 0).
  </acceptance_criteria>
  <done>The three render call sites read the central tier. Cover-path tracking lives at both cover entry points. The Pattern 4 branch in _apply_art_tier (Plan 02) can now distinguish "replay real cover at new tier" vs "replay fallback at new tier" because both render sites correctly mutate `_last_cover_path`. Task 2 wires the resizeEvent extension that drives tier changes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Phase 72.1 resizeEvent override with self._apply_art_tier() and update docstring</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py lines 1149-1159 (the existing Phase 72.1 resizeEvent override — confirm current body has super().resizeEvent(event) on its own line followed by self._reflow_stream_picker_row() on its own line, both within the override body)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §A (verbatim final-state of the extended resizeEvent override after this plan — the docstring covers BOTH phases, super() stays first, _reflow_stream_picker_row stays before _apply_art_tier)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md Pattern S-2 (super().resizeEvent MUST be called first per Qt convention — base implementation invalidates layout and updates internal geometry)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md Pitfall 1 (setFixedSize on child labels does NOT re-fire resizeEvent on the panel — verified by Phase 72.1 production; the cached-diff guard absorbs any spurious re-entry)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md §Co-existence With Phase 72.1's resizeEvent (truth table: exactly ONE resizeEvent override on NowPlayingPanel; ordering is super -> _reflow_stream_picker_row -> _apply_art_tier; both helpers are bound methods, no lambdas)
  </read_first>
  <action>
    One surgical edit to `musicstreamer/ui_qt/now_playing_panel.py`:

    Extend the existing `resizeEvent` override at lines 1149-1159 with EXACTLY ONE additional line: `self._apply_art_tier()`, placed immediately AFTER the existing `self._reflow_stream_picker_row()` line. Update the docstring to mention Phase 72.3 / LAYOUT-03 alongside the existing Phase 72.1 / LAYOUT-02 reference — use the docstring shape from PATTERNS.md §A "Final state (after Phase 72.3 edit)" excerpt, which already mentions both phases, the mandatory `super()` first ordering, and the idempotence/no-recursion invariants for both helpers.

    Do NOT add a second `resizeEvent` override. Do NOT call `self.resize(...)` or `self.setGeometry(...)` from inside the override (would cause infinite recursion per Qt docs — already verified Pitfall 9 of 72.1-RESEARCH). Do NOT introduce any conditional gating (the cached-diff guard inside `_apply_art_tier` is the sole short-circuit; `_apply_art_tier` is safe to call on every resize event).

    Final body shape (PATTERNS.md §A verbatim — see read_first):
    - Docstring describing both phases (Phase 72.1 LAYOUT-02 stream-picker reparent on narrow widths; Phase 72.3 LAYOUT-03 logo/cover tier retarget); notes super's resizeEvent is mandatory first (Pitfall 9, 72.1-RESEARCH); notes both helpers are idempotent and safe to call when state hasn't changed; notes neither helper calls self.resize / setGeometry (Qt docs: infinite recursion).
    - Body, in order: `super().resizeEvent(event)` then `self._reflow_stream_picker_row()` then `self._apply_art_tier()`.
    - No lambdas, no conditionals, no try/except.

    Net file change: +1 line of code (the new `self._apply_art_tier()` call) plus updated docstring (~3 line growth for the cross-phase context).
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase72_3_responsive_art.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def resizeEvent" musicstreamer/ui_qt/now_playing_panel.py` returns exactly 1 (single override on NowPlayingPanel, not duplicated).
    - `grep -c "self._apply_art_tier()" musicstreamer/ui_qt/now_playing_panel.py` returns at least 2 (one call inside resizeEvent + one or more inside _apply_art_tier itself or _show_station_logo wiring — but the resizeEvent occurrence is what proves the wiring).
    - The line containing `self._apply_art_tier()` inside resizeEvent appears AFTER the line containing `self._reflow_stream_picker_row()` (compare line numbers via `grep -n "self._reflow_stream_picker_row()\|self._apply_art_tier()" musicstreamer/ui_qt/now_playing_panel.py`; the resizeEvent body has _reflow before _apply).
    - The line containing `super().resizeEvent(event)` is BEFORE both helper calls (in source order).
    - The resizeEvent docstring contains both `72.1` AND `72.3` substrings.
    - No `lambda` token on any line of the resizeEvent body (`awk '/def resizeEvent/,/^    def [^_]/' musicstreamer/ui_qt/now_playing_panel.py | grep -c "lambda"` equals 0).
    - No `self.resize(` and no `self.setGeometry(` calls inside the resizeEvent body.
    - `uv run pytest tests/test_phase72_3_responsive_art.py -x` exits 0 — all 13 tests GREEN.
    - `uv run pytest tests/ -x -k "phase72 or now_playing"` continues to exit 0 (Phase 72 / 72.1 / 72.2 tests still pass — no resizeEvent regression).
    - `uv run pytest tests/ -x` exits 0 (full suite green) — phase gate ready for `/gsd:verify-work`.
  </acceptance_criteria>
  <done>resizeEvent now drives both Phase 72.1 picker reflow AND Phase 72.3 tier retargeting. Every panel resize event recomputes the tier and, on tier change, mutates both labels + replays the bound pixmaps through the central tier. All 13 Plan 01 tests GREEN. Phase 72.3 ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qt platform -> NowPlayingPanel.resizeEvent | Platform-dispatched resize events; no remote injection surface. The override now drives two helpers (picker reflow + tier retarget) but both are pure widget-state mutations. |
| Repo (SQLite) -> station_art_path consumed by _set_cover_pixmap / _show_station_logo | Repo-supplied paths validated by Phase 47.x station-edit flow; existing _load_scaled_pixmap null-fallback handles invalid/missing paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.3-01 | Information Disclosure (residual V12) | _set_cover_pixmap / _show_station_logo on station_art_path | accept-inherited | Phase 72.3 introduces no new file-resolution path. The refactored render sites still call _load_scaled_pixmap (line 213 helper, unchanged) which handles null pixmap via :/icons/audio-x-generic-symbolic.svg fallback. Per RESEARCH §Security Domain: "this phase introduces no new threat surface." |
| T-72.3-03 | Denial of Service | resizeEvent firing _apply_art_tier on every resize | mitigate | The early-return guard `if new_tier == self._current_art_tier: return` inside _apply_art_tier (Plan 02) absorbs continuous-drag thrash. setFixedSize on child labels does NOT recurse on the parent panel (Pitfall 1, verified by Phase 72.1 production). No timer / debounce introduced. |
| T-72.3-04 | Tampering | Stale _last_cover_path pointing to a deleted temp file | accept | Pitfall 3 — the existing _set_cover_pixmap null-pixmap branch (line 1465 fallback) handles deleted temp files by routing to _show_station_logo_in_cover_slot, which resets _last_cover_path = None. Graceful degradation: a user briefly sees a real cover and then sees the logo at the new tier. No code change needed. |

Phase 72.3 introduces no new threat surface beyond the inherited V12 residual. All mitigations live in existing code paths.
</threat_model>

<verification>
- `git diff --stat musicstreamer/ui_qt/now_playing_panel.py` shows only this one file modified by Plan 03.
- `uv run pytest tests/test_phase72_3_responsive_art.py -x` exits 0 — all 13 tests GREEN.
- `uv run pytest tests/ -x` exits 0 — full suite green.
- `grep -cE 'QSize\(160, 160\)|QSize\(180, 180\)' musicstreamer/ui_qt/now_playing_panel.py` equals 0 (all three render sites refactored).
- `grep -c "def resizeEvent" musicstreamer/ui_qt/now_playing_panel.py` equals 1 (no duplication).
- `grep -c "self._apply_art_tier()" musicstreamer/ui_qt/now_playing_panel.py` is at least 2 (one call inside resizeEvent + the _apply_art_tier method body itself does NOT recursively call self._apply_art_tier, so this count being >= 2 means resizeEvent + some other site — typically the count is exactly 1 from resizeEvent; criterion holds as "at least 1" but >=2 surfaces if any future site also invokes; relax to `>= 1` if interpretation differs — final goal is that resizeEvent contains the call).
- No new imports in the diff (`git diff musicstreamer/ui_qt/now_playing_panel.py | grep -cE '^\+from |^\+import '` equals 0).
- No edits to any file other than `musicstreamer/ui_qt/now_playing_panel.py`.
</verification>

<success_criteria>
- All five legacy literals (lines 339, 745, 1468, 1495, 1499) are sourced from the central tier mechanism: lines 339 and 745 stay at the 180,180 medium-tier defaults; lines 1468, 1495, 1499 read `n = self._current_art_tier or 180`.
- The Phase 72.1 resizeEvent override at line 1149 is extended (not duplicated) with `self._apply_art_tier()` AFTER `self._reflow_stream_picker_row()`.
- `_set_cover_pixmap` writes `self._last_cover_path = path` at the tail; `_show_station_logo_in_cover_slot` writes `self._last_cover_path = None` at the tail.
- YouTube 16:9 letterboxing is preserved at every tier (test_youtube_16_9_letterbox_at_every_tier GREEN).
- All 13 tests in tests/test_phase72_3_responsive_art.py turn GREEN.
- Full suite `uv run pytest tests/ -x` exits 0 (no Phase 72.x regression).
- No new repo.set_setting call (D-09); no new lambda; no new imports; no edits outside now_playing_panel.py.
</success_criteria>

<output>
Create `.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-03-SUMMARY.md` when done. Include:
- Final test status: all 13 Plan 01 tests GREEN; full suite GREEN.
- The exact resizeEvent body after the edit (paste).
- Confirmation that no QSize(160, 160) or QSize(180, 180) literals survive at the three render sites (paste grep output).
- Phase 72.3 verification gate ready: announce readiness for `/gsd:verify-work 72.3`.
</output>
