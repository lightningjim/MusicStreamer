---
phase: 72.3
plan: 02
type: execute
wave: 1
depends_on: ["72.3-01"]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
autonomous: true
requirements: [LAYOUT-03]
tags: [layout, pyside6, ui-qt, helpers, tdd-green, layout-03, phase-72-3]
must_haves:
  truths:
    - "NowPlayingPanel.__init__ declares two new sentinel attributes immediately after `self._cached_row1_min: Optional[int] = None` at line 297: `self._current_art_tier: Optional[int] = None` and `self._last_cover_path: Optional[str] = None`. Initializing BEFORE the setMinimumWidth / setLayout calls is load-bearing (Pitfall 4)."
    - "The literal `self.cover_label.setFixedSize(160, 160)` at line 745 is changed to `self.cover_label.setFixedSize(180, 180)` — the medium-tier default, the deliberate D-05 +20px bump."
    - "The literal `self.logo_label.setFixedSize(180, 180)` at line 339 stays at `180, 180` literally (already the medium-tier default per UI-SPEC Initial-Bind Contract); the banner comment on the section header is updated to reference Phase 72.3 / LAYOUT-03 tier scheme."
    - "Two new bound methods are added to NowPlayingPanel under a Phase 72.3 banner block immediately after the Phase 72.1 helper section banner (~line 1224): `_current_art_tier_size(self) -> int` (pure width-to-tier classifier returning 140/180/240 from `self.width()` against thresholds 700 and 1100) and `_apply_art_tier(self) -> None` (cached-diff orchestrator)."
    - "_current_art_tier_size returns 140 when self.width() < 700, returns 180 when 700 <= width < 1100, returns 240 when width >= 1100. No hidden caching, no side effects — pure compute."
    - "_apply_art_tier short-circuits when computed tier equals self._current_art_tier (Pitfall 8 re-entrancy guard), otherwise: (a) writes self._current_art_tier = new_tier, (b) calls self.logo_label.setFixedSize(new_tier, new_tier) AND self.cover_label.setFixedSize(new_tier, new_tier), (c) re-renders logo via self._show_station_logo(), (d) re-renders cover slot via branch on self._last_cover_path — if not None call self._set_cover_pixmap(self._last_cover_path), else call self._show_station_logo_in_cover_slot()."
    - "Plan 02 lands the helpers but does NOT wire them into resizeEvent yet (Plan 03 owns that), and does NOT modify the three render call sites (_set_cover_pixmap, _show_station_logo, _show_station_logo_in_cover_slot). Tests 1-3, 6, 8, 11 of Plan 01 turn GREEN after Plan 02 (label sizes flip on Plan 03 resizeEvent wiring; helpers are callable on their own and the cover default bump means cover==logo==180 at default width)."
    - "No lambda on any new line (QA-05). No SQLite write (D-09 no-persistence). No edits to _theme.py, MainWindow, Repo, Player, _load_scaled_pixmap, or any other file."
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "_current_art_tier_size (pure helper) + _apply_art_tier (orchestrator) + _current_art_tier / _last_cover_path sentinels in __init__ + cover_label 160->180 default bump"
      contains: "def _current_art_tier_size"
  key_links:
    - from: "musicstreamer/ui_qt/now_playing_panel.py (_apply_art_tier)"
      to: "musicstreamer/ui_qt/now_playing_panel.py (_show_station_logo / _show_station_logo_in_cover_slot / _set_cover_pixmap)"
      via: "self.<method>() direct call (bound method, no lambda)"
      pattern: "self\\._show_station_logo|self\\._show_station_logo_in_cover_slot|self\\._set_cover_pixmap"
    - from: "musicstreamer/ui_qt/now_playing_panel.py (__init__)"
      to: "self._current_art_tier sentinel attribute"
      via: "attribute initialization before layout"
      pattern: "self\\._current_art_tier: Optional\\[int\\] = None"
---

<objective>
Wave 1 GREEN tier — helpers, sentinels, and the cover_label 160 -> 180 default bump.

This plan lands the **pure compute** and the **orchestrator** without yet wiring them into the existing Phase 72.1 `resizeEvent` override and without yet refactoring the three render call sites. The helpers exist as bound methods; the sentinels exist on `self`; the cover_label constructs at 180x180 (matching the logo and locking D-05 at the medium-tier default).

After Plan 02: tests 1, 2, 3, 6, 8, 11 of Plan 01 turn GREEN (they rely on `_current_art_tier` reads and equal-size assertions at default widths). Tests 4 and 5 (threshold tests on `_current_art_tier_size()`) ALSO turn GREEN because the pure helper is callable on its own. Tests 7, 9, 10 stay RED until Plan 03 refactors the render call sites and wires `_apply_art_tier` into `resizeEvent`. Tests 12 and 13 stay GREEN.

Purpose: Single-source-of-truth tier computation as a bound method, cached integer state on `self`, and the user-visible default-state change (cover bumps to 180 at construction).

Output: One file modified (`now_playing_panel.py`). Three additions: two sentinel attributes in `__init__`, two new bound methods (with banner comment), and one literal swap (`(160, 160)` -> `(180, 180)` on the cover_label setFixedSize line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-CONTEXT.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md
@.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-01-SUMMARY.md

<interfaces>
<!-- Key existing methods _apply_art_tier will call. Confirmed at HEAD on main 2026-05-19. -->
From musicstreamer/ui_qt/now_playing_panel.py:

- Line 201-213: def _load_scaled_pixmap(path: Optional[str], size: QSize) -> QPixmap  (module-level helper, signature unchanged)
- Line 297:     self._cached_row1_min: Optional[int] = None  (existing Phase 72.1 cache sentinel — new sentinels go IMMEDIATELY after this line)
- Line 326:     self.setMinimumWidth(560)  (must NOT fire resizeEvent before sentinels are initialized — Pitfall 4)
- Line 338-341: self.logo_label = QLabel(self); self.logo_label.setFixedSize(180, 180); self.logo_label.setAlignment(Qt.AlignCenter); self.logo_label.setScaledContents(False)  (logo: STAYS at 180,180 literal — UI-SPEC Initial-Bind Contract)
- Line 743-745: self.cover_label = QLabel(self); self.cover_label.setFixedSize(160, 160); self.cover_label.setAlignment(Qt.AlignCenter)  (cover: 160,160 -> 180,180 — D-05 default bump)
- Line 1149-1159: def resizeEvent(self, event) -- existing Phase 72.1 override (Plan 03 extends; Plan 02 does NOT touch)
- Line 1224-1232: Phase 72.1 helper banner block (Plan 02 adds a new Phase 72.3 banner IMMEDIATELY after, or alongside — see Task 2 action)
- Line 1462-1470: def _set_cover_pixmap(self, path: str)  (Plan 03 refactors; Plan 02 does NOT touch)
- Line 1493-1495: def _show_station_logo(self)  (Plan 03 refactors; Plan 02 calls it from _apply_art_tier and relies on the FUTURE refactored body — which still works pre-refactor because the unrefactored body just renders at fixed 180x180 which equals the medium tier; the pixmap dims will be wrong at tier 140 / 240 until Plan 03 lands, but no AttributeError)
- Line 1497-1499: def _show_station_logo_in_cover_slot(self)  (same — Plan 03 refactors; Plan 02 calls it pre-refactor)

From PySide6:
- QLabel.setFixedSize(int, int) -> None  (synchronous; calls updateGeometry internally)
- QWidget.width() -> int  (returns current widget width in device pixels)
</interfaces>

@musicstreamer/ui_qt/now_playing_panel.py
@tests/test_phase72_3_responsive_art.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _current_art_tier + _last_cover_path sentinels in __init__ and bump cover_label default to 180</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py lines 290-345 (existing `__init__` head — confirm `self._cached_row1_min` line and surrounding banner comments; locate the logo banner comment at line 336 and the `setFixedSize(180, 180)` at line 339)
    - musicstreamer/ui_qt/now_playing_panel.py lines 740-748 (existing cover_label setup — the literal to change is at line 745)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §B (verbatim sentinel-init excerpt; the new attribute lines are quoted there exactly — match the comment voice)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §C (logo banner comment update — `# Left column: 180x180 station logo` -> tier-aware variant; the literal `180, 180` STAYS — only the comment changes)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §D (cover bump — the literal `160, 160` -> `180, 180` AND the banner comment at line 743)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md §Initial-Bind Contract (rationale for the medium-tier default at construction)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md Pitfall 4 (Optional[int] sentinel initialization order — MUST precede setMinimumWidth/setLayout)
  </read_first>
  <action>
    Three edits to `musicstreamer/ui_qt/now_playing_panel.py`:

    (1) **Insert two sentinel attribute lines** immediately after `self._cached_row1_min: Optional[int] = None` (line 297). Mirror the Phase 72.1 comment voice and the Optional[T] = None idiom from Pattern S-4. The block is two attribute assignments preceded by a multi-line comment in the same voice as the existing Phase 72.1 cache comment. Both attributes get the Optional[...] type annotation. The comment cites 72.3-RESEARCH Pitfall 4 (initialization order) and CONTEXT D-02 (single-tier source of truth). After this edit, the four lines that previously sat at 295-297 (the Phase 72.1 banner comment + cache attribute) are followed by an analogous Phase 72.3 banner comment + two new attributes. Net file growth ~7 lines.

    (2) **Update the logo banner comment** at line 336 from `# Left column: 180x180 station logo` to a tier-aware form that references Phase 72.3 / LAYOUT-03 and notes 180 is the medium-tier default per UI-SPEC Initial-Bind Contract. The `self.logo_label.setFixedSize(180, 180)` literal at line 339 STAYS — `180, 180` is the medium default and is what the panel renders at construction before the first resizeEvent corrects.

    (3) **Update the cover section banner comment** at line 743 (currently `# Right column: 160x160 cover art slot`) to a tier-aware form citing D-05 (equal at every tier) and Phase 72.3 / LAYOUT-03. **Change the literal at line 745** from `self.cover_label.setFixedSize(160, 160)` to `self.cover_label.setFixedSize(180, 180)`. This is the user-visible default-state change — at the medium-tier default, the cover now matches the logo at 180x180 (D-05 enforcement).

    All edits are pure additions or one-literal swap; no method bodies are restructured. No new imports. No changes to `Optional` import (already imported at the top of the file).

    Source quotes from PATTERNS.md §B / §C / §D are the exact text to mirror — do not invent alternative wording for the comments.
  </action>
  <verify>
    <automated>grep -c "self._current_art_tier: Optional\[int\] = None" musicstreamer/ui_qt/now_playing_panel.py  # must be 1; AND: grep -c "self._last_cover_path: Optional\[str\] = None" musicstreamer/ui_qt/now_playing_panel.py  # must be 1; AND: grep -c "setFixedSize(160, 160)" musicstreamer/ui_qt/now_playing_panel.py  # must be 0; AND: grep -nE "setFixedSize\(180, 180\)" musicstreamer/ui_qt/now_playing_panel.py  # must show TWO lines (logo at ~339 + cover at ~745 — both 180,180 now)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "self._current_art_tier: Optional\[int\] = None" musicstreamer/ui_qt/now_playing_panel.py` equals 1.
    - `grep -c "self._last_cover_path: Optional\[str\] = None" musicstreamer/ui_qt/now_playing_panel.py` equals 1.
    - Both sentinel attribute lines appear BEFORE `self.setMinimumWidth(560)` (compare line numbers via `grep -n` — both `_current_art_tier:` and `_last_cover_path:` line numbers must be < the line number of `setMinimumWidth(560)`). Pitfall 4 mitigation.
    - `grep -c "setFixedSize(160, 160)" musicstreamer/ui_qt/now_playing_panel.py` equals 0 (cover literal swapped).
    - `grep -nE "setFixedSize\(180, 180\)" musicstreamer/ui_qt/now_playing_panel.py` shows EXACTLY 2 occurrences (logo around line 339 + cover around line 745).
    - The logo banner comment at line 336 mentions either "tier" or "Phase 72.3" or "LAYOUT-03" (was previously just `# Left column: 180x180 station logo`).
    - The cover banner comment near line 743 mentions either "tier" or "Phase 72.3" or "LAYOUT-03" or "D-05".
    - `uv run python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; print('import OK')"` prints `import OK` (no syntax errors).
    - No new imports were added (`git diff musicstreamer/ui_qt/now_playing_panel.py | grep -cE "^\+from |^\+import "` equals 0).
  </acceptance_criteria>
  <done>Sentinels exist on the panel; cover_label constructs at 180x180; logo_label stays at 180x180; banner comments are tier-aware. Helpers in Task 2 can now read self._current_art_tier and self._last_cover_path without AttributeError.</done>
</task>

<task type="auto">
  <name>Task 2: Add _current_art_tier_size (pure helper) and _apply_art_tier (orchestrator) bound methods</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py lines 1220-1240 (the existing Phase 72.1 helper banner block — confirm where the banner sits and what shape the comment block uses; the new Phase 72.3 banner mirrors it)
    - musicstreamer/ui_qt/now_playing_panel.py lines 1312-1360 (Phase 72.1 `_move_stream_picker_to` / `_reflow_stream_picker_row` — exemplars of the cached-diff idempotence pattern S-1)
    - musicstreamer/ui_qt/now_playing_panel.py lines 1462-1500 (existing `_set_cover_pixmap`, `_show_station_logo`, `_show_station_logo_in_cover_slot` — _apply_art_tier calls these by name; do NOT modify them in this plan)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §H (verbatim `_current_art_tier_size` body including the banner block, the thresholds 700/1100, the docstring with the truth table, and the narrow-tier rationale for width < 560)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-PATTERNS.md §I (verbatim `_apply_art_tier` body including the early-return guard, the cache write, the dual setFixedSize calls, the logo re-render, and the branch on self._last_cover_path)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md §Threshold Derivation (locks 700 and 1100 — geometric justification against the 560-px floor)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-RESEARCH.md Pitfall 1, Pitfall 8 (re-entrancy guard MUST be first; setFixedSize on child labels does not recurse on the parent panel)
    - .planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-UI-SPEC.md §Tier State Truth Table + §Co-existence With Phase 72.1's resizeEvent (the orchestrator MUST be safe to call from resizeEvent AFTER _reflow_stream_picker_row — Plan 03 owns the resizeEvent wiring)
  </read_first>
  <action>
    Append two new bound methods to the `NowPlayingPanel` class. Placement: immediately after the existing Phase 72.1 helper section, ending where the `_reflow_stream_picker_row` family ends. Add a new banner block following Pattern S-6 (use `# ----` separators and a multi-line block citing Phase 72.3 / LAYOUT-03 + RESEARCH §Patterns 2/3/4 + UI-SPEC §Tier State Truth Table).

    **Method 1 — `_current_art_tier_size(self) -> int`** (PATTERNS.md §H, verbatim where possible):
    - Body reads `w = self.width()` ONCE.
    - Returns 140 when `w < 700`, returns 180 when `w < 1100` (else-if), returns 240 otherwise.
    - Docstring describes the truth table inline (width < 700 -> 140; 700 <= width < 1100 -> 180; width >= 1100 -> 240) AND notes that width < 560 is the panel's setMinimumWidth floor (in production the width is never below 560, but the narrow tier is the safe default during initial layout passes).
    - No side effects, no cache writes, no setFixedSize, no setPixmap. Pure compute. Testable in isolation.

    **Method 2 — `_apply_art_tier(self) -> None`** (PATTERNS.md §I, verbatim where possible):
    - First statement: `new_tier = self._current_art_tier_size()`.
    - Re-entrancy guard MUST be the second statement: `if new_tier == self._current_art_tier: return` — load-bearing per Pitfall 8.
    - On change: write the cache (`self._current_art_tier = new_tier`), then call `self.logo_label.setFixedSize(new_tier, new_tier)` AND `self.cover_label.setFixedSize(new_tier, new_tier)` (D-02 single source of truth; D-05 equal at every tier).
    - Re-render the logo: `self._show_station_logo()` (the bound method handles the fallback when self._station is None, per Pitfall 5).
    - Re-render the cover slot — Pattern 4 branch on self._last_cover_path: if not None, call `self._set_cover_pixmap(self._last_cover_path)` (replay real cover at new tier); else, call `self._show_station_logo_in_cover_slot()` (replay fallback at new tier).
    - Docstring cites CONTEXT D-02, D-05, D-Claude-Discretion; cites Pattern 4 from 72.3-RESEARCH (lines 319-365); notes idempotence and that setFixedSize on a child label does NOT recursively fire resizeEvent on the panel (Pitfall 1, verified by Phase 72.1 production behavior).
    - NO lambda anywhere in the body. NO `self.resize(...)` / NO `self.setGeometry(...)` (would cause infinite recursion per Qt docs).
    - NO call to `super()` (this is not an override).
    - NO repo.set_setting (D-09 no-persistence inheritance from Phase 72).

    Both methods are bound (defined inside the `class NowPlayingPanel(QWidget):` body, take `self` as first parameter, use `self.<attr>` for state access). The new methods are NOT yet called from anywhere (Plan 03 wires `_apply_art_tier` into `resizeEvent`). They are reachable as `panel._current_art_tier_size()` and `panel._apply_art_tier()` from tests.

    The new banner block at the start of the Phase 72.3 helper section is structured like the Phase 72.1 banner at lines 1224-1232 — same `# ----` separators, same multi-line context block, cross-references to 72.3-RESEARCH and 72.3-UI-SPEC. Pattern S-6 from PATTERNS.md.

    Net file growth: ~50-60 lines (two methods plus banner plus blank-line padding to match existing class formatting).
  </action>
  <verify>
    <automated>grep -c "def _current_art_tier_size" musicstreamer/ui_qt/now_playing_panel.py  # must be 1; AND: grep -c "def _apply_art_tier" musicstreamer/ui_qt/now_playing_panel.py  # must be 1; AND: uv run pytest tests/test_phase72_3_responsive_art.py::test_narrow_to_medium_threshold tests/test_phase72_3_responsive_art.py::test_medium_to_wide_threshold tests/test_phase72_3_responsive_art.py::test_no_lambda_in_tier_wiring -x  # the two threshold tests and the no-lambda test must turn GREEN (helpers callable; no lambda on tier-related lines)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _current_art_tier_size" musicstreamer/ui_qt/now_playing_panel.py` equals 1.
    - `grep -c "def _apply_art_tier" musicstreamer/ui_qt/now_playing_panel.py` equals 1.
    - Both methods are defined INSIDE the `class NowPlayingPanel(QWidget):` body (assert by `grep -n "class NowPlayingPanel"` line < both `def _current_art_tier_size` and `def _apply_art_tier` line numbers, AND no other `class ` declaration appears between them).
    - `_current_art_tier_size` body contains the three return values 140, 180, 240 AS literal integer returns (`grep -cE "return 140|return 180|return 240" musicstreamer/ui_qt/now_playing_panel.py >= 3`).
    - `_current_art_tier_size` body references the two thresholds 700 and 1100 as integer literals (`grep -cE "< 700|< 1100" musicstreamer/ui_qt/now_playing_panel.py >= 2`).
    - `_apply_art_tier` body contains the early-return guard `if new_tier == self._current_art_tier:` followed by `return` (grep for the guard string).
    - `_apply_art_tier` body calls all four bound methods by name: `self.logo_label.setFixedSize`, `self.cover_label.setFixedSize`, `self._show_station_logo`, AND a branch with `self._set_cover_pixmap` + `self._show_station_logo_in_cover_slot`.
    - `_apply_art_tier` body contains zero `lambda` tokens (`awk '/def _apply_art_tier/,/^    def [^_]/' musicstreamer/ui_qt/now_playing_panel.py | grep -c "lambda" == 0`).
    - `_apply_art_tier` body contains zero `self.resize(` calls and zero `self.setGeometry(` calls (would cause recursion).
    - `_apply_art_tier` body contains zero `repo.set_setting` and zero `_repo.set_setting` calls.
    - `uv run pytest tests/test_phase72_3_responsive_art.py::test_narrow_to_medium_threshold -x` exits 0 (GREEN — the pure helper is callable).
    - `uv run pytest tests/test_phase72_3_responsive_art.py::test_medium_to_wide_threshold -x` exits 0 (GREEN).
    - `uv run pytest tests/test_phase72_3_responsive_art.py::test_no_lambda_in_tier_wiring -x` exits 0 (GREEN — invariant maintained).
    - `uv run pytest tests/test_phase72_3_responsive_art.py::test_logo_equals_cover_at_every_tier -x` exits 0 (GREEN — cover is now 180 default so at width 800 both labels are 180x180; at widths 600 and 1200 the labels are STILL at their construction default until Plan 03 wires resizeEvent, but the test only asserts logo==cover at each width, which is true given both are 180,180 at construction).
    - `uv run pytest tests/test_phase72_3_responsive_art.py -x 2>&1 | tail -10` shows the remaining failures are tests that require Plan 03's resizeEvent wiring (e.g., test_narrow_tier_at_floor still fails because resizeEvent does not yet call _apply_art_tier, so the labels remain at the 180 default).
    - Full suite outside Phase 72.3: `uv run pytest tests/ -x -k "not phase72_3"` continues to exit 0 (no collateral damage; the new helpers are unreached by other tests).
  </acceptance_criteria>
  <done>NowPlayingPanel has the two new bound methods and the two sentinel attributes. Helpers are callable from tests; threshold and no-lambda assertions are GREEN. Plan 03 owns the resizeEvent wiring and the three render-site refactors that complete the LAYOUT-03 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qt platform -> NowPlayingPanel.resizeEvent | (Plan 03 wires the override extension; Plan 02 lands helpers only.) Platform-dispatched, no remote injection surface. |
| Repo (SQLite) -> station_art_path | _apply_art_tier reads station_art_path indirectly via _show_station_logo; existing repo validation chain is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.3-01 | Information Disclosure (residual V12) | _show_station_logo path resolution | accept-inherited | Phase 72.3 introduces no new file-resolution path; _load_scaled_pixmap null-fallback handles missing/invalid paths. The orchestrator delegates to existing render methods. |
| T-72.3-03 | Denial of Service | _apply_art_tier called from resizeEvent | mitigate | The early-return guard `if new_tier == self._current_art_tier: return` (Pitfall 8) prevents pixmap-thrash during continuous drag within a tier band. setFixedSize on child labels does NOT recurse on the parent panel (verified by Phase 72.1 production). No timer or debounce introduced. |

Plan 02 introduces no new security-sensitive surface. All mitigations inherit from existing code paths.
</threat_model>

<verification>
- `git diff --stat musicstreamer/ui_qt/now_playing_panel.py` shows additions only (no other files modified by Plan 02).
- `uv run python -c "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel; print('OK')"` returns `OK`.
- `uv run pytest tests/test_phase72_3_responsive_art.py -x 2>&1 | tail -15` shows the threshold + no-lambda + no-repo-set-setting + logo-equals-cover + within-band-noop tests as GREEN; remaining failures are tier-resize-driven tests that need Plan 03's resizeEvent wiring.
- `uv run pytest tests/ -x -k "not phase72_3"` continues to exit 0.
- No imports added (`git diff musicstreamer/ui_qt/now_playing_panel.py | grep -cE "^\+from |^\+import " == 0`).
</verification>

<success_criteria>
- Two sentinel attributes initialized in `__init__` before setMinimumWidth/setLayout.
- Logo banner comment is tier-aware; logo literal stays at 180,180.
- Cover banner comment is tier-aware; cover literal swapped 160,160 -> 180,180 (D-05 default bump).
- Two new bound methods added inside `NowPlayingPanel`: `_current_art_tier_size` (pure compute returning 140/180/240) and `_apply_art_tier` (cached-diff orchestrator with Pattern 4 branch on _last_cover_path).
- All Plan 02 acceptance criteria GREEN; threshold + idempotence + no-lambda + no-repo-set-setting tests pass.
- No edits to any file outside `musicstreamer/ui_qt/now_playing_panel.py`.
- No new imports.
</success_criteria>

<output>
Create `.planning/phases/72.3-make-station-tile-and-album-art-responsive-to-window-screen-/72.3-02-SUMMARY.md` when done. Include:
- Diff summary (lines added, no removals beyond the 160,160 -> 180,180 literal).
- Test status: list which Plan 01 tests turned GREEN at this wave; which stayed RED waiting for Plan 03.
- Note any Pitfall that surfaced (e.g., AttributeError during initial layout that required hasattr guard — should NOT happen if sentinels are placed before line 326, but log if observed).
</output>
