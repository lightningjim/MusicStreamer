---
phase: 60.1
plan: 04
type: execute
wave: 3
depends_on: [60.1-01, 60.1-02, 60.1-03]
files_modified:
  - .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md
autonomous: false
requirements: [GBS-01e]
tags: [phase60.1, gbs-fm, drill-down, verification, uat, manual]
must_haves:
  truths:
    - "Full project test suite is GREEN — `uv run pytest --no-header` exits 0 with no failed tests and the count includes Phase 60.1's new tests"
    - "All 4 ROADMAP success criteria for Phase 60.1 are observable: SC1 (multi-word artist click drills correctly OR documented server-side limitation), SC2 (multi-word album click drills correctly OR documented), SC3 (single-word click path no regression), SC4 (D-07 deletes + new tests cover new contract)"
    - "60.1-UAT.md exists in the phase folder, signed off by the human"
    - "Manual UAT executed on the Linux Wayland deployment target: live click on /artist/<id> with real GBS.FM cookies returns parseable rows; live click on /album/<id> works; Back button restores; multi-word artist link works (or documented server-side limitation per Plan 02 SUMMARY)"
  artifacts:
    - path: ".planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md"
      provides: "Manual UAT log signed off by human; records SC1-SC4 outcomes + any deviations"
      contains: "SC1"
  key_links:
    - from: "60.1-UAT.md"
      to: "ROADMAP Phase 60.1 Success Criteria"
      via: "explicit SC1/SC2/SC3/SC4 references with PASS/FAIL/N-A markers"
      pattern: "SC1.*Multi-word artist click"
---

<objective>
Wave 3 (verification): Run the full project test suite, then conduct manual UAT against the running app on the Linux Wayland deployment target. Sign off Phase 60.1's four ROADMAP success criteria, record any deviations, and gate the phase for `/gsd-verify-work`.

Purpose: VALIDATION.md flags two manual-only verifications: (a) visual layout of drill-down view (Back button placement, label clarity), and (b) live drill-down against a real GBS.FM artist/album page (network + auth dependent). Plans 01-03 cannot validate these via pytest. Plan 04 is the human gate.

Output: One signed UAT log (`60.1-UAT.md`) with SC1-SC4 outcomes + a clean full-suite test pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-CONTEXT.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-RESEARCH.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UI-SPEC.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-VALIDATION.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-01-SUMMARY.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-02-SUMMARY.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite + write UAT log scaffold</name>
  <files>.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md</files>
  <read_first>
    - .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-VALIDATION.md (manual-only verifications + SC mapping)
    - .planning/ROADMAP.md (Phase 60.1 entry — SC1-SC4 verbatim)
    - .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-02-SUMMARY.md (multi-word triage outcome — informs whether SC1/SC2 are "drill works" or "documented server-side limitation")
    - .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-03-SUMMARY.md (Plan 03 commit hashes + UI-SPEC compliance map)
  </read_first>
  <action>
    Step 1 — Run the full project test suite and capture the output:

    ```bash
    uv run pytest --no-header 2>&1 | tail -30 > /tmp/60.1-fullsuite.log
    cat /tmp/60.1-fullsuite.log
    ```

    Confirm: NO failed tests. The output should end with something like `XX passed in N.NNs`.

    If ANY test fails, STOP — do not proceed to UAT. The failure must be triaged (Plan 02 or Plan 03 regression) and a remediation commit landed before this plan can continue. Failures are NOT acceptable to wave through into UAT.

    Step 2 — Write the UAT scaffold using the Write tool. Path: `.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md`. Content:

    ```markdown
    # Phase 60.1: GBS.FM Search Artist/Album Drill-Down — UAT Log

    **Phase:** 60.1
    **Requirement:** GBS-01e (refines)
    **Date:** {fill in YYYY-MM-DD at sign-off}
    **Tester:** {fill in name at sign-off}
    **Deployment target:** Linux Wayland, DPR=1.0 (per project memory)

    ---

    ## Test-Suite Pre-Gate

    Phase gate per VALIDATION.md: full suite must be green before manual UAT.

    ```
    {paste output of `uv run pytest --no-header 2>&1 | tail -10`}
    ```

    Status: [ ] PASS  [ ] FAIL

    ---

    ## ROADMAP Success Criteria

    ### SC1: Multi-word artist click opens artist's catalog

    From ROADMAP: "Clicking an Artist link with a multi-word name (e.g. 'Foo Fighters', 'Pearl Jam') opens that artist's catalog (the user can see the artist's songs/albums) — does NOT just re-run a base song search."

    Test steps:
    1. Launch the app: `uv run python -m musicstreamer`
    2. Open Search GBS.FM dialog (hamburger → Search GBS.FM…)
    3. Enter a multi-word query: `foo fighters` (or `pearl jam`)
    4. Press Enter; observe the search results table populates
    5. **Branch**:
       - **If multi-word fix landed (Pitfall 1 hypothesis 2/3/5)**: Artist: panel populates with multi-word artist names. Click one of them.
       - **If hypothesis 1 (server omits panels)**: Per Plan 02 SUMMARY, multi-word queries don't surface Artist:/Album: panels server-side. SC1 is then validated indirectly: search for a SINGLE-word query (e.g., `pearl`) that yields a multi-word artist in the panel (e.g., `Pearl Jam`), then click it.
    6. Confirm: drill-down view loads — table replaced with that artist's catalog (multiple rows), breadcrumb reads `Viewing artist: {name}`, Back button visible, Artist:/Album: panels still visible.
    7. Confirm: this is NOT a free-text search (the previous Phase 60-11 D-11a Shape 4 behavior would have re-searched and shown song results matching the artist name as substring — SC1 explicitly rejects that).

    Result: [ ] PASS  [ ] FAIL  [ ] N/A (documented server-side limitation per Plan 02 SUMMARY)

    Notes:
    {observations, screenshots if relevant, any deviations}

    ### SC2: Multi-word album click opens album's catalog

    From ROADMAP: "Clicking an Album link with a multi-word name behaves the same way for the album's catalog."

    Test steps:
    1. Continue from SC1 (or fresh search). Find a multi-word album in the Album: panel (typical: any album with multiple words, e.g. `Brotherhood of the Snake`).
    2. Click the album entry.
    3. Confirm: drill-down view loads — table replaced with that album's track listing, breadcrumb reads `Viewing album: {name}`.
    4. Confirm: per-row Add! buttons function (clicking one submits the song to the playlist; toast confirms).

    Result: [ ] PASS  [ ] FAIL  [ ] N/A (documented server-side limitation per Plan 02 SUMMARY)

    Notes:
    {observations}

    ### SC3: Single-word click path no regression

    From ROADMAP: "The single-word click path that already works in Phase 60-11 continues to work — no regression on the simple case."

    Test steps:
    1. Search a single-word query: `test` (matches the Phase 60-11 baseline fixture).
    2. In the Artist: panel, click any single-word artist (e.g., `Testament`).
    3. Confirm: drill-down view loads — `Viewing artist: Testament` breadcrumb; rows show Testament's catalog.
    4. Click Back; confirm search results restore (page label `Page 1 of N`, prev/next pagination buttons restored).
    5. Click any album in the Album: panel; confirm album drill-down works.

    Result: [ ] PASS  [ ] FAIL

    Notes:
    {observations}

    ### SC4: Existing tests updated; new tests cover new contract

    From ROADMAP: "Existing Phase 60 tests (`test_artist_click_kicks_free_text_search`, `test_album_click_kicks_free_text_search`) are either updated to match the new contract or marked superseded; new tests cover multi-word and drill-down paths."

    Verification commands:
    ```bash
    grep -c "^def test_artist_click_kicks_free_text_search\|^def test_album_click_kicks_free_text_search" tests/test_gbs_search_dialog.py
    # Expect: 0  (deleted per D-07)

    uv run pytest tests/test_gbs_search_dialog.py -k "drill_down or back_button or pre_drill or click_drills_down" --no-header 2>&1 | tail -3
    # Expect: 7 passed (Plan 60.1-01's RED → GREEN)

    uv run pytest tests/test_gbs_api.py -k "multiword or artist_page_parses or album_page_parses or album_parser_rejects or fetch_artist_songs or fetch_album_songs" --no-header 2>&1 | tail -3
    # Expect: 6 passed (Plan 60.1-01's parser RED → GREEN)
    ```

    Result: [ ] PASS  [ ] FAIL

    Notes:
    {paste output of the three commands}

    ---

    ## Additional Manual Validations (UI-SPEC contract)

    ### Visual layout (UI-SPEC Layout Deltas 1-5)

    - [ ] Delta 1: During drill-down, the Page N of M label is hidden and the breadcrumb label `Viewing artist/album: {name}` appears in its slot in the top row
    - [ ] Delta 2: During drill-down, Prev/Next pagination buttons are hidden and `← Back to search` button appears in their place (left-aligned)
    - [ ] Delta 3: Search input + Artist:/Album: panels remain visible during drill-down (clicking another panel entry triggers a new drill, breadcrumb updates)
    - [ ] Delta 4: Results table is swapped in place (no flicker of empty state mid-fetch)
    - [ ] Delta 5: Back button restores the EXACT pre-drill state (results, page label, panels, pagination button enabled flags)

    ### Pitfall mitigations (live verification)

    - [ ] Pitfall 8: Click artist A → drill loads → click artist B (without Back) → drilled view replaces; click Back → search results restore (NOT artist A's drilled view)
    - [ ] Pitfall 9: Click an artist → during the brief fetch window, observe that the Back button is GREYED/DISABLED. After fetch completes, Back is enabled.
    - [ ] Pitfall 10: Drill into an artist → without clicking Back, type a new query and press Enter → drill chrome resets, new search runs, no flash of old results.

    ### Auth-expired path (manual)

    - [ ] To test: temporarily move/rename the GBS.FM cookies file (`paths.gbs_cookies_path()`), restart the app, attempt a drill-down → toast "GBS.FM session expired — reconnect via Accounts" appears, login gate re-locks. (Optional — only if convenient.)

    ---

    ## Sign-Off

    - [ ] All 4 ROADMAP success criteria met (or N/A documented per Plan 02 SUMMARY)
    - [ ] Full test suite green
    - [ ] Visual layout matches UI-SPEC Layout Deltas 1-5
    - [ ] Pitfalls 8/9/10 verified live

    **UAT signed off:** {date} by {tester}

    **Outcome:** [ ] PASS — Phase 60.1 ready for `/gsd-complete-phase`
                 [ ] FAIL — open Phase 60.1.1 / Phase 60.2 to address gaps

    ---

    ## Deviations / Open Issues

    {list any observed deviations from UI-SPEC, anti-patterns, or behaviors that
    need a follow-up phase. Common categories:
    - Server-side multi-word limitation (hypothesis 1) — already documented in Plan 02 SUMMARY
    - Pagination-within-drill needed for prolific artists — deferred per Deferred Ideas
    - Album parser brittleness — deferred per Deferred Ideas (Phase 60.2 fallback)}
    ```

    Step 3 — Stage the new file: `git add .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md`. Do NOT commit yet — Task 2's checkpoint will commit after the human signs off.

    Notes for executor:
    - The UAT scaffold uses placeholders (`{...}` style) that the human fills in during Task 2.
    - The "Test-Suite Pre-Gate" section captures the actual test output before Task 2's checkpoint pause.
    - DO NOT skip running the full test suite. Even if `--no-header` shows 100% green for the dialog tests, OTHER tests in the project may have been broken by Plan 02/03 changes. Full-suite green is non-negotiable.
  </action>
  <verify>
    <automated>uv run pytest --no-header 2>&amp;1 | tail -3 | grep -qvE "failed" &amp;&amp; test -f .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md &amp;&amp; grep -q "SC1: Multi-word artist click" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md &amp;&amp; grep -q "SC2: Multi-word album click" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md &amp;&amp; grep -q "SC3: Single-word click path no regression" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md &amp;&amp; grep -q "SC4: Existing tests updated" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Full project test suite is green: `uv run pytest --no-header 2>&1 | tail -3` does NOT contain `failed` (zero failures)
    - The output contains a passed-count line (e.g., `XXX passed in N.NNs`)
    - File `.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md` exists
    - File contains all 4 SC headers verbatim: `grep -c "^### SC[1-4]:" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md` MUST print exactly `4`
    - File contains the "Test-Suite Pre-Gate" section (placeholder for the test output)
    - File contains "Sign-Off" section with checklist + tester/date placeholders
    - File contains the three UI-SPEC Delta-mapping checklist items + the three Pitfall (8/9/10) live-verification checklist items
    - File is staged via `git add` but NOT yet committed (Task 2's checkpoint commits after human sign-off)
  </acceptance_criteria>
  <done>
    - Full test suite green
    - 60.1-UAT.md scaffold authored with SC1-SC4 + UI-SPEC Delta checks + Pitfall live-verifications + sign-off section
    - Test-suite output captured in the UAT log's pre-gate section
    - File staged for Task 2's checkpoint commit
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT execution + sign-off + commit</name>
  <files>.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md</files>
  <what-built>
    Plans 60.1-01 through 60.1-03 have shipped:
    - Plan 01 (Wave 0): captured multi-word fixture + 13 RED tests
    - Plan 02 (Wave 1): _ArtistPageParser + _AlbumPageParser + fetch_artist_songs + fetch_album_songs + multi-word fix + Pitfall 7 docstring fix
    - Plan 03 (Wave 2): _GbsArtistWorker + _GbsAlbumWorker + drill-down click handlers + drilled/back-clicked slots + snapshot/restore + D-07 deletes

    Task 1 of this plan ran the full test suite (green) and authored the 60.1-UAT.md scaffold.

    The HUMAN now needs to:
    1. Run the app on Linux Wayland with their dev GBS.FM cookies
    2. Execute the manual UAT script in 60.1-UAT.md (SC1-SC4 + UI-SPEC Delta checks + Pitfall live-verifications + auth-expired path if convenient)
    3. Fill in the placeholder fields ({date}, {tester}, observations, paste test-suite output) using the Edit tool
    4. Mark each checkbox as [x] PASS, [x] FAIL, or [x] N/A as appropriate
    5. Type "approved" to resume — the executor commits the signed UAT log
  </what-built>
  <how-to-verify>
    Run the app:
    ```bash
    cd /home/kcreasey/OneDrive/Projects/MusicStreamer
    uv run python -m musicstreamer
    ```

    Open the Search GBS.FM dialog (hamburger menu → Search GBS.FM…) and execute the test steps in `.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md`. The UAT log is the script — follow its sections in order:

    1. **Test-Suite Pre-Gate**: paste the output Task 1 captured (or re-run `uv run pytest --no-header 2>&1 | tail -10`)
    2. **SC1**: Multi-word artist drill (foo fighters / pearl jam — SEE Plan 02 SUMMARY for which case applies; if hypothesis 1 holds, validate via single-word query yielding multi-word artist name)
    3. **SC2**: Multi-word album drill (or single-word query yielding multi-word album)
    4. **SC3**: Single-word baseline (Testament, etc.) — drill-in + Back round-trip
    5. **SC4**: Run the three pytest commands and paste their output
    6. **UI-SPEC Layout Deltas 1-5**: visually inspect each delta during a drill-in
    7. **Pitfalls 8/9/10**: live verification (click artist A → click artist B → Back returns to search; observe Back disabled during fetch; new search while drilled exits drill chrome)
    8. **Auth-expired path** (optional): rename cookies file → restart → drill → expect toast

    For each section, mark [x] in the appropriate checkbox AND fill in the Notes field with concrete observations. Use the Edit tool to update 60.1-UAT.md.

    At the end, fill in:
    - "UAT signed off: {date} by {tester}" → e.g., `UAT signed off: 2026-05-06 by Kyle`
    - "Outcome: [x] PASS" or [x] FAIL with deviation notes

    If FAIL: do NOT type "approved". Instead describe the failure in the resume signal so the orchestrator can spawn a 60.1-05-PLAN gap-closure plan.
  </how-to-verify>
  <action>
    The HUMAN executes the UAT and edits 60.1-UAT.md to sign off. The EXECUTOR (Claude) does NOT modify the UAT log — only the human can fill in the test results and outcomes.

    After the human signs the UAT (types "approved"):

    1. Verify the UAT log has been edited (no remaining `{...}` placeholder strings in the SC sections):
       ```bash
       grep -c "^Result: \[ \]" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md
       # MUST print 0 — every Result line must have an [x] mark, not [ ]
       ```

    2. Verify the Outcome section is filled:
       ```bash
       grep -E "Outcome:.*\[x\] PASS|Outcome:.*\[x\] FAIL" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md
       # MUST match exactly one PASS or FAIL line
       ```

    3. Stage and commit the signed UAT log:
       ```bash
       git add .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md
       git commit -m "docs(60.1-04): UAT sign-off — Phase 60.1 ${OUTCOME}

Phase 60.1 / GBS-01e UAT (Wave 3):

- 60.1-UAT.md: human-signed UAT log
  * SC1 (multi-word artist drill): ${SC1_RESULT}
  * SC2 (multi-word album drill): ${SC2_RESULT}
  * SC3 (single-word click no regression): ${SC3_RESULT}
  * SC4 (D-07 deletes + new tests): ${SC4_RESULT}
  * UI-SPEC Layout Deltas 1-5: ${DELTAS_OUTCOME}
  * Pitfalls 8/9/10 live verified: ${PITFALLS_OUTCOME}
- Tester: ${TESTER}
- Date: ${DATE}

${IF PASS: 'Phase 60.1 closed; ready for /gsd-complete-phase.'}
${IF FAIL: 'Open Phase 60.1.1 / Phase 60.2 for gap closure (see 60.1-UAT.md Deviations section).'}"
       ```
       The commit message variables (`${OUTCOME}`, `${SC1_RESULT}`, etc.) are placeholders the executor REPLACES with actual values read from the signed UAT log. Use the Read tool to read 60.1-UAT.md, extract the actual outcomes, and substitute.

    Notes for executor:
    - Do NOT auto-mark any checkbox in the UAT log. Only the human fills in PASS/FAIL.
    - If the human types "blocked: <reason>" instead of "approved", DO NOT commit. Instead surface the failure to the orchestrator for follow-up planning.
    - The auth-expired manual test is OPTIONAL — if the human skips it, the corresponding checkbox stays [ ] and the Outcome can still be PASS.
    - The checkpoint commit message must reference Phase 60.1 closure or gap-closure intent based on the actual sign-off outcome.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^Result: \[ \]' .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md)" = "0" ] &amp;&amp; grep -qE "Outcome:.*\[x\] (PASS|FAIL)" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md &amp;&amp; git log -1 --pretty=%s | grep -q "60.1-04"</automated>
  </verify>
  <acceptance_criteria>
    - Every "Result:" line in 60.1-UAT.md is filled in (no `[ ]` checkboxes remain on any "Result:" line; PASS/FAIL/N-A all marked [x]): `grep -cE "^Result: \[ \]" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md` MUST print exactly `0`
    - The "Outcome:" line is marked PASS or FAIL: `grep -cE "Outcome:.*\[x\] (PASS|FAIL)" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md` MUST print exactly `1`
    - Tester name + date filled in (no `{tester}` or `{date}` placeholder strings remain in the Sign-Off section): `grep -cE "(\\{tester\\}|\\{date\\}|\\{fill in)" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md` MUST print `0`
    - All 4 SC sections have non-empty Notes (not just `{observations}`): `grep -c "^Notes:" .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md` confirms presence; manual visual confirmation that each Notes section has actual observations (impossible to grep — relies on human discipline)
    - Wave 3 commit landed: `git log -1 --pretty=%s | grep -q "60.1-04"` exits 0
    - Test-Suite Pre-Gate section contains real pytest output (not the placeholder text)
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all SC1-SC4 PASS (or are documented N/A per Plan 02 SUMMARY), UI-SPEC Layout Deltas 1-5 visible in the running app, and Pitfalls 8/9/10 verified live. Executor commits the signed UAT log.

    Type "blocked: <description>" if any SC fails or a UI-SPEC delta is not observable. Executor will NOT commit and the orchestrator will spawn Phase 60.1's gap-closure plan (60.1-05) covering the specific failure.
  </resume-signal>
  <done>
    - Manual UAT executed end-to-end on Linux Wayland with real GBS.FM cookies
    - 60.1-UAT.md fully filled in (no placeholders, all checkboxes marked, tester+date set)
    - Outcome (PASS or FAIL) recorded
    - Wave 3 commit landed with the signed UAT log
    - Phase 60.1 ready for `/gsd-complete-phase` (if PASS) OR for gap-closure planning (if FAIL)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live network → app → user observation | The UAT runs against live gbs.fm with the user's dev cookies. The captured fixtures from Plans 01-03 already validated parser correctness; UAT validates end-to-end UX + any production-only failure modes that test mocks can't surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.1-16 | Information Disclosure (UAT log captures live data) | 60.1-UAT.md | accept | The UAT log records artist/album names from the live app. These are public catalog entries (gbs.fm artist names like "Testament", "Foo Fighters") — no sensitive data. The human filling in the log SHOULD avoid pasting raw HTML or stack traces that could contain session cookies. Mitigation: the UAT scaffold's "Notes" sections instruct only observation-text, not raw payloads. |
| T-60.1-17 | Repudiation | UAT sign-off | mitigate | The commit message names the tester + date. Git history provides the audit trail. The signed UAT log itself contains tester+date in the Sign-Off section. Phase closure cannot proceed without this attestation. |

</threat_model>

<verification>
After this plan ships:

1. Full project test suite is green (Task 1 gate).
2. 60.1-UAT.md exists with all 4 SC sections filled in by the human.
3. Outcome marked PASS or FAIL.
4. Wave 3 commit landed.

If PASS: Phase 60.1 is ready for `/gsd-complete-phase`. The roadmap is updated with the Phase 60.1 close-out + plan list.

If FAIL: The orchestrator triages the deviations recorded in 60.1-UAT.md and either:
- Spawns a Phase 60.1.1 gap-closure plan (small, targeted fix)
- Opens Phase 60.2 for larger restructuring (e.g., album parser too brittle → use external browser)
- Updates CONTEXT.md / ROADMAP entries to relax the failed SC (e.g., document multi-word server-side limitation)
</verification>

<success_criteria>
- Full project test suite green before manual UAT
- 60.1-UAT.md created, scaffolded, signed off by human
- All 4 SC outcomes recorded (PASS/FAIL/N-A)
- UI-SPEC Layout Deltas 1-5 + Pitfalls 8/9/10 live-verified
- Single Wave 3 commit lands the signed UAT log
- Phase 60.1 reaches a known terminal state (PASS = ready to close, FAIL = ready to plan gap closure)
</success_criteria>

<output>
This plan does not require its own SUMMARY.md — the UAT log itself is the artifact. The orchestrator's `/gsd-complete-phase` step will roll up the four plan SUMMARYs (60.1-01, 60.1-02, 60.1-03) plus the UAT log into the Phase 60.1 closeout.

If the UAT outcome is FAIL, the orchestrator captures the deviations and decides whether to spawn 60.1-05 (gap closure) or open Phase 60.2.
</output>
