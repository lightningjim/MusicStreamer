---
phase: 60.1
slug: gbs-fm-search-artist-album-drill-down
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-06
---

# Phase 60.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-qt (qtbot) |
| **Config file** | `pyproject.toml` / `pytest.ini` |
| **Quick run command** | `uv run pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py -x` |
| **Full suite command** | `uv run pytest` |
| **Estimated runtime** | ~quick: 5–10s · full: ~60s |

---

## Sampling Rate

- **After every task commit:** Run quick command (parser + dialog tests for the file modified)
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~10s for the quick path

---

## Per-Task Verification Map

> Stub. Planner expands this map during PLAN.md authoring; each PLAN task slot resolves to one row keyed by Task ID. Required columns are reproduced below — populate when plans land.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 60.1-01-XX | 01 | 0 | GBS-01e | — | RED test fails before fix; multi-word fixture committed | unit | `uv run pytest tests/test_gbs_api.py::test_search_returns_artist_links_multiword -x` | ❌ W0 | ⬜ pending |
| 60.1-02-XX | 02 | 1 | GBS-01e | — | _ArtistPageParser extracts ≥1 song from artist_4803.html | unit | `uv run pytest tests/test_gbs_api.py::test_artist_page_parses -x` | ❌ W0 | ⬜ pending |
| 60.1-03-XX | 03 | 1 | GBS-01e | — | _AlbumPageParser locks onto unclassed table inside `<div class="playlist">` | unit | `uv run pytest tests/test_gbs_api.py::test_album_page_parses -x` | ❌ W0 | ⬜ pending |
| 60.1-04-XX | 04 | 2 | GBS-01e | — | Drill-down click invokes worker, render replaces results, Back restores prior search state | qt-integration | `uv run pytest tests/test_gbs_search_dialog.py -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/fixtures/gbs/search_multiword_p1.html` — captured multi-word search response (D-03 RED-first)
- [ ] `tests/test_gbs_api.py` — RED test for multi-word search-response parsing (`test_search_returns_artist_links_multiword`)
- [ ] `tests/test_gbs_api.py` — RED tests for `_ArtistPageParser` and `_AlbumPageParser` against existing fixtures `artist_4803.html` / `album_1488.html`
- [ ] `tests/test_gbs_search_dialog.py` — RED tests for drill-down click → worker → render, plus Back-button round-trip and snapshot-guard (no overwrite on second drill)
- [ ] D-07 deletions: remove `test_artist_click_kicks_free_text_search` and `test_album_click_kicks_free_text_search` in the same commit that lands the drill-down implementation (NOT in Wave 0 — these are the GREEN-side replacement contract)

*Existing infrastructure (qtbot, urlopen monkeypatch via `_urlopen_factory`) covers all phase needs — no new conftest fixtures required per RESEARCH.md.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual layout of drill-down view (Back button placement, label clarity, song-row rendering inside dialog) | GBS-01e (UI-SPEC contract) | Pixel-level visual inspection beyond automated `text()` / `count()` assertions | Launch dev build, run Phase 60-11 search for "Foo Fighters", click an Artist link, confirm Back button appears, click Back, confirm search state restored |
| Live drill-down against a real GBS.FM artist/album page (network + auth dependent) | GBS-01e | Requires live cookie context and external service availability | Smoke-test post-merge: actual click on `/artist/<id>` for a real multi-word artist returns parseable rows |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < ~10s for quick path
- [ ] `nyquist_compliant: true` set in frontmatter (planner flips when per-task map is fully populated)

**Approval:** pending
