---
phase: 77-test-infrastructure-stabilization-fix-pre-existing-test-doub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_fake_player.py
  - tests/test_fake_player_signal_parity.py
  - tests/test_fake_player_no_inline.py
autonomous: true
requirements: [INFRA-01]
tags: [test-infrastructure, drift-guard, pyside6, fakeplayer]

must_haves:
  truths:
    - "D-07: Shared FakePlayer module exists at tests/_fake_player.py and declares every Signal currently on musicstreamer.player.Player with matching argument arity"
    - "D-08, D-16: Drift-guard test fails if Player gains a new Signal without parity update on shared FakePlayer (name AND arity)"
    - "D-17: Drift-guard test fails if any test file outside tests/_fake_player.py declares a class _?FakePlayer(QObject) inline"
    - "All three new files run green under `uv run pytest tests/_fake_player.py tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py -x` BEFORE any FakePlayer migrations land (Wave 2 work)"
  artifacts:
    - path: "tests/_fake_player.py"
      provides: "Canonical FakePlayer(QObject) test double — module-level class with all 18 Player signals and method stubs"
      min_lines: 80
      contains: "class FakePlayer(QObject)"
    - path: "tests/test_fake_player_signal_parity.py"
      provides: "D-16 drift-guard — name + arity parity between Player and FakePlayer"
      min_lines: 50
      contains: "test_fake_player_mirrors_every_player_signal"
    - path: "tests/test_fake_player_no_inline.py"
      provides: "D-17 drift-guard — only _fake_player.py may declare FakePlayer(QObject)"
      min_lines: 25
      contains: "test_no_inline_fake_player_subclass_in_tests"
  key_links:
    - from: "tests/test_fake_player_signal_parity.py"
      to: "musicstreamer/player.py:241-282"
      via: "regex source-parse of `^\\s*([a-zA-Z_][a-zA-Z0-9_]*)\\s*=\\s*Signal\\(([^)]*)\\)`"
      pattern: "_grep_signal_decls"
    - from: "tests/test_fake_player_signal_parity.py"
      to: "tests/_fake_player.py"
      via: "regex source-parse of identical pattern"
      pattern: "_grep_signal_decls"
    - from: "tests/test_fake_player_no_inline.py"
      to: "tests/test_*.py + tests/ui_qt/test_*.py"
      via: "rglob + regex search for inline FakePlayer(QObject) class definition"
      pattern: "class\\s+_?FakePlayer\\s*\\(QObject\\)"
---

<objective>
Ship the canonical shared `tests/_fake_player.py` module + two drift-guard tests as Wave 1 foundation. Every subsequent plan in Phase 77 depends on the shared module existing (Plan 77-02 migrates 11 sites to import from it) and on the drift-guards being installed BEFORE migration so any regression during migration trips the guard immediately.

Purpose: per CONTEXT.md `<specifics>`, the drift-guards are the actual user-facing value of Phase 77 — they are what stops the 10+ phase recurrence of the same FakePlayer-drift failure logged across phases 51, 54, 55, 60.4, 61, 65, 66, 68, 71, 72, 72.1, 73. In-place migrations (Wave 2) are merely the price of admission to install the guards.

Output:
- `tests/_fake_player.py` — shared FakePlayer(QObject) declaring all 18 Player signals with production-correct arity + method stubs covering the API surface MainWindow / EditStationDialog / NowPlayingPanel test consumers touch
- `tests/test_fake_player_signal_parity.py` — D-16 drift-guard with two assertion functions (name parity via `Player.__dict__` walk + isinstance(Signal); arity parity via regex source-parse of both files)
- `tests/test_fake_player_no_inline.py` — D-17 source-grep drift-guard
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-CONTEXT.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-RESEARCH.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-PATTERNS.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-VALIDATION.md

<interfaces>
<!-- Production Player signal declarations — single source of truth for the 18 signals tests/_fake_player.py mirrors.
     Re-derive at execution time via `grep -nE "^\s*[a-zA-Z_][a-zA-Z0-9_]*\s*=\s*Signal\(" musicstreamer/player.py` to confirm
     no signals were added between planning (2026-05-17) and execution. -->

From musicstreamer/player.py:241-282 (18 Player-specific signals; the only ones tests/_fake_player.py must mirror):

```
title_changed              = Signal(str)
failover                   = Signal(object)
offline                    = Signal(str)
twitch_resolved            = Signal(str)
youtube_resolved           = Signal(str)
youtube_resolution_failed  = Signal(str)
playback_error             = Signal(str)
cookies_cleared            = Signal(str)
elapsed_updated            = Signal(int)
buffer_percent             = Signal(int)
_cancel_timers_requested   = Signal()
_error_recovery_requested  = Signal()
_try_next_stream_requested = Signal()
_playbin_playing_state_reached = Signal()
_underrun_cycle_opened     = Signal()
_underrun_cycle_closed     = Signal(object)
underrun_recovery_started  = Signal()
audio_caps_detected        = Signal(int, int, int)
```

Note: production-correct arity for `audio_caps_detected` is `Signal(int, int, int)` per `player.py:282` (NOT `Signal(object)` — the gbs/soma sites currently have the drifted single-arg form, which Plan 77-02 auto-fixes by importing the shared module).

From tests/test_main_window_integration.py:31-84 (seed body — most complete FakePlayer in the tree; has 10 of the 18 signals + method stubs):

```
class FakePlayer(QObject):
    title_changed / failover / offline / playback_error / cookies_cleared / elapsed_updated
    buffer_percent / underrun_recovery_started / audio_caps_detected = Signal(int, int, int)

    Methods: __init__(records play_calls/pause_calls/stop_calls/volume), set_volume(value),
             play(station, **kwargs), pause(), stop(), restore_eq_from_settings(repo),
             set_eq_enabled(enabled), set_eq_profile(profile), set_eq_preamp(db),
             shutdown_underrun_tracker()
```

The 8 signals the seed is MISSING are the Player-internal ones from `player.py:244-276`:
- `twitch_resolved`, `youtube_resolved`, `youtube_resolution_failed` (worker-thread → main)
- `_cancel_timers_requested`, `_error_recovery_requested`, `_try_next_stream_requested` (bus-loop / worker → main)
- `_playbin_playing_state_reached` (Phase 57 / WIN-03 D-12 volume re-apply)
- `_underrun_cycle_opened`, `_underrun_cycle_closed` (Phase 62 cycle Signals)

MainWindow does not connect to these — they are intra-Player marshaling. But D-16's parity test enumerates `Player.__dict__` and the test would fail unless they are declared on FakePlayer too. Declaring them as unused Signals on FakePlayer is safe — Qt does not raise on connecting-zero or never-emitting.

From tests/test_yt_dlp_opts_drift.py:1-44 (Phase 79) — source-grep drift-guard precedent for D-17:

```python
"""Phase 79 / BUG-11 drift-guard: both yt-dlp call sites must use the shared
`yt_dlp_opts.build_js_runtimes(` helper..."""
from __future__ import annotations
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent / "musicstreamer"
# ... rglob "*.py" + count check ...
```

From tests/test_constants_drift.py:48-60 (Phase 61) — rglob + ban-list precedent for D-17:

```python
def test_no_org_example_literal_remains_in_python_sources():
    pkg_root = Path(__file__).parent.parent / "musicstreamer"
    needle = "org.example.MusicStreamer"
    hits = []
    for py in pkg_root.rglob("*.py"):
        try:
            text = py.read_text(encoding="utf-8")
        except UnicodeDecodeError:
            continue
        if needle in text:
            hits.append(str(py.relative_to(pkg_root.parent)))
    assert not hits, f"Phase 61 left placeholder behind in: {hits}"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/_fake_player.py — shared FakePlayer(QObject) declaring all 18 Player signals + method stubs</name>
  <files>tests/_fake_player.py</files>
  <read_first>
    - musicstreamer/player.py:1-90 (imports + module-level Signal block context)
    - musicstreamer/player.py:241-282 (canonical 18-signal block — this is the source of truth)
    - tests/test_main_window_integration.py:1-90 (seed body to derive from; 10/18 signals + method stub pattern)
    - tests/conftest.py:90-220 (underscore-prefix shared-double convention: _FakeStation/_FakeStream/_FakeRepo)
    - tests/test_main_window_gbs.py:25-50 (DRIFT site — shows the wrong `audio_caps_detected = Signal(object)` arity to NOT replicate)
    - 77-RESEARCH.md L350-371 (verified canonical 18-signal list with production arities)
    - 77-PATTERNS.md L62-157 (full file layout: imports, class header, signal block in production declaration order, method stubs)
  </read_first>
  <behavior>
    - Test 1 (covered by Task 2's test_fake_player_mirrors_every_player_signal): introspecting `musicstreamer.player.Player.__dict__` and `tests._fake_player.FakePlayer.__dict__` for Signal-typed attributes produces identical name sets
    - Test 2 (covered by Task 2's test_fake_player_signal_arity_matches_player): regex source-parse of `musicstreamer/player.py` and `tests/_fake_player.py` for `NAME = Signal(ARGS)` produces matching ARGS string per name
    - Test 3 (manual smoke): `from tests._fake_player import FakePlayer; fp = FakePlayer(); fp.play_calls == []; fp.set_volume(0.5)` works without TypeError
  </behavior>
  <action>
    Create `tests/_fake_player.py` as a new module-level shared test double mirroring the existing `tests/conftest.py` `_FakeStation`/`_FakeStream`/`_FakeRepo` convention (module-level classes — NOT pytest fixtures — opt-in via direct import).

    Module structure (top-to-bottom):

    1. Module docstring referencing Phase 77 D-07/D-08/D-16/D-17 and naming `musicstreamer/player.py:241-282` as the canonical Signal source of truth. State that this module's Signal block MUST be re-grepped from production at the start of any future maintenance session (Rule 1 — never assume the production Signal block is unchanged).

    2. Imports: `from __future__ import annotations`; stdlib `from typing import Optional`; PySide6 `from PySide6.QtCore import QObject, Signal`; project model `from musicstreamer.models import Station` (mirrors the seed body's `Station` type-hint on `play_calls`).

    3. Class definition: `class FakePlayer(QObject):` — public (NOT underscore-prefixed) because D-09's migration target is `from tests._fake_player import FakePlayer` (consumed name is `FakePlayer`). Docstring cites the 18-Player-signal invariant + D-16/D-17 drift-guard names.

    4. Signal block — declare all 18 Player signals in production declaration order (verified from `player.py:241-282`):
       - Public Signals (10): `title_changed`, `failover`, `offline`, `twitch_resolved`, `youtube_resolved`, `youtube_resolution_failed`, `playback_error`, `cookies_cleared`, `elapsed_updated`, `buffer_percent`
       - Underscore-prefixed internal Signals (7): `_cancel_timers_requested`, `_error_recovery_requested`, `_try_next_stream_requested`, `_playbin_playing_state_reached`, `_underrun_cycle_opened`, `_underrun_cycle_closed`, `underrun_recovery_started`
       - Caps Signal (1): `audio_caps_detected = Signal(int, int, int)` — production-correct arity per `player.py:282`; NEVER `Signal(object)` (the gbs/soma drift form)
       - Total: 18 Signals. Each declaration's `Signal(...)` argument list MUST EXACTLY MATCH the production declaration's argument list (D-16 arity drift-guard enforces this verbatim, so any whitespace difference inside the parens fails the parity test).

    5. `__init__` body mirrors the seed (`tests/test_main_window_integration.py:44-50`): `super().__init__()` + four call-tracking attributes (`play_calls: list[Station] = []`, `pause_calls: int = 0`, `stop_calls: int = 0`, `volume: Optional[float] = None`).

    6. Method stubs (from seed `tests/test_main_window_integration.py:51-84`):
       - `set_volume(value: float) -> None` — assigns `self.volume = value`
       - `play(station: Station, **kwargs) -> None` — appends station to `self.play_calls`
       - `pause() -> None` — increments `self.pause_calls`
       - `stop() -> None` — increments `self.stop_calls`
       - `restore_eq_from_settings(repo) -> None` — no-op pass (called from `MainWindow.__init__` per Phase 47.2)
       - `set_eq_enabled(enabled: bool) -> None` — no-op pass
       - `set_eq_profile(profile) -> None` — no-op pass
       - `set_eq_preamp(db: float) -> None` — no-op pass
       - `shutdown_underrun_tracker() -> None` — no-op pass (called from `MainWindow.closeEvent` per Phase 62 D-03)

    Do NOT add a `.calls` log attribute analogous to `tests/test_equalizer_dialog.py:42-51` — that file's FakePlayer stays inline per CONTEXT D-09 Option A (it is a non-QObject EQ-only double, not a MainWindow consumer). The drift-guard D-17 regex `class\s+_?FakePlayer\s*\(QObject\)` excludes that file by design.

    Do NOT subclass any other class; do NOT add `node_runtime` / Phase 79 kwargs (out of scope for Phase 77 per CONTEXT D-03).

    File ends with a trailing newline; ~95 lines total. Format consistent with `tests/conftest.py` style (4-space indent, no trailing whitespace).
  </action>
  <verify>
    <automated>uv run python -c "from tests._fake_player import FakePlayer; from PySide6.QtCore import Signal; sigs = {n for n,v in FakePlayer.__dict__.items() if isinstance(v, Signal)}; assert len(sigs) == 18, f'expected 18 signals, got {len(sigs)}: {sorted(sigs)}'; fp = FakePlayer(); fp.set_volume(0.5); fp.play(object()); assert fp.volume == 0.5 and len(fp.play_calls) == 1; print('FakePlayer OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/_fake_player.py` exists
    - Source contains exactly one declaration `class FakePlayer(QObject):` (grep `-c "^class FakePlayer(QObject):" tests/_fake_player.py` returns 1)
    - Source contains exactly 18 lines matching `^\s*[a-zA-Z_][a-zA-Z0-9_]*\s*=\s*Signal\(` (verify via `grep -cE "^\s*[a-zA-Z_][a-zA-Z0-9_]*\s*=\s*Signal\(" tests/_fake_player.py` returns 18)
    - Source contains the literal `audio_caps_detected = Signal(int, int, int)` (NOT `Signal(object)`) — verify via `grep -F "audio_caps_detected = Signal(int, int, int)" tests/_fake_player.py` returns 1 line
    - `uv run python -c "from tests._fake_player import FakePlayer; FakePlayer()"` exits 0 (module imports + class instantiates without TypeError)
    - All 18 signal names from `musicstreamer/player.py:241-282` Signal block are declared as class-level attributes on FakePlayer (drift-guard Task 2 enforces this rigorously; this acceptance criterion is the smoke check)
    - Method stubs `set_volume`, `play`, `pause`, `stop`, `restore_eq_from_settings`, `set_eq_enabled`, `set_eq_profile`, `set_eq_preamp`, `shutdown_underrun_tracker` all exist as instance methods (verify via `grep -cE "^    def (set_volume|play|pause|stop|restore_eq_from_settings|set_eq_enabled|set_eq_profile|set_eq_preamp|shutdown_underrun_tracker)\(" tests/_fake_player.py` returns 9)
  </acceptance_criteria>
  <done>tests/_fake_player.py exists; instantiates cleanly; declares 18 signals with production-correct arity; method stub set matches the seed body from test_main_window_integration.py:44-84. Wave 2 (Plan 77-02) can now switch every inline FakePlayer site to `from tests._fake_player import FakePlayer` and have it Just Work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_fake_player_signal_parity.py — D-16 drift-guard (name + arity parity)</name>
  <files>tests/test_fake_player_signal_parity.py</files>
  <read_first>
    - tests/_fake_player.py (created in Task 1 — confirm 18-signal block exists)
    - musicstreamer/player.py:241-282 (production Signal block — source of truth for the parity check)
    - tests/test_yt_dlp_opts_drift.py:1-44 (Phase 79 precedent — single-file source-grep drift-guard)
    - tests/test_constants_drift.py:48-99 (Phase 61 precedent — rglob + literal-search ban-pattern)
    - 77-RESEARCH.md L375-435 (verified D-16 drift-guard body draft — reference directly, do not re-derive)
    - 77-RESEARCH.md L300-322 (Pitfall 3 + Pitfall 4 — why `Player.__dict__` not `dir(Player)`, why arity matters)
  </read_first>
  <behavior>
    - Behavior 1: When `tests/_fake_player.py` mirrors all 18 Player signals (Task 1 deliverable) the test passes — `_signal_names(Player) - _signal_names(FakePlayer) == set()`
    - Behavior 2: If a Player signal is dropped from `tests/_fake_player.py`, `test_fake_player_mirrors_every_player_signal` fails with a message naming the missing signal(s) and pointing at `tests/_fake_player.py` as the fix location
    - Behavior 3: If `tests/_fake_player.py` declares a signal with different argument list than production (e.g. `Signal(object)` vs `Signal(int, int, int)`), `test_fake_player_signal_arity_matches_player` fails with a message naming the signal and both argument lists
    - Behavior 4: `dir(Player)` inherited Signals (`destroyed`, `objectNameChanged`) do NOT appear in the parity required set — using `Player.__dict__` instead of `dir(Player)` filters them out automatically (verified probe: 18 via `__dict__` vs 20 via `dir`)
  </behavior>
  <action>
    Create `tests/test_fake_player_signal_parity.py` per the verified draft at `77-RESEARCH.md L375-435`. Do NOT re-derive — reference that draft and apply verbatim.

    Module structure:

    1. Module docstring naming D-16 + the recurrence pattern: 10+ phases logged the same drift since Phase 62 shipped; this guard fires the moment a new Player Signal lands without parity update.

    2. Imports: `from __future__ import annotations`, `import re`, `from pathlib import Path`, `from PySide6.QtCore import Signal`, `from musicstreamer.player import Player`, `from tests import _fake_player as fp_mod`.

    3. Module-level `ROOT = Path(__file__).resolve().parent.parent` (resolves to project root — `parent` is `tests/`, second `parent` is repo root).

    4. Module-level helper `_signal_names(cls)` returning the set comprehension `{name for name, value in cls.__dict__.items() if isinstance(value, Signal)}`. Critical detail: use `cls.__dict__`, NOT `dir(cls)` (Pitfall 3 — `dir` returns inherited QObject signals `destroyed` + `objectNameChanged` which FakePlayer should NOT mirror).

    5. Module-level helper `_grep_signal_decls(path: Path) -> dict[str, str]`: reads `path.read_text(encoding="utf-8")`, applies the regex pattern `re.compile(r'^\s*([a-zA-Z_][a-zA-Z0-9_]*)\s*=\s*Signal\(([^)]*)\)', re.M)`, returns `{m.group(1): m.group(2).strip() for m in pattern.finditer(text)}`.

    6. Test function `test_fake_player_mirrors_every_player_signal()`: computes `player_sigs = _signal_names(Player)` and `fake_sigs = _signal_names(fp_mod.FakePlayer)`, asserts `not (player_sigs - fake_sigs)` with a multi-line failure message naming `sorted(player_sigs - fake_sigs)` and pointing at `tests/_fake_player.py` as the fix location with the literal hint `Add \`<name> = Signal(...)\` to tests/_fake_player.py mirroring the declaration in musicstreamer/player.py`.

    7. Test function `test_fake_player_signal_arity_matches_player()`: computes `player_decls = _grep_signal_decls(ROOT / "musicstreamer" / "player.py")` and `fake_decls = _grep_signal_decls(ROOT / "tests" / "_fake_player.py")`. For each `name, player_args` in `player_decls.items()` where `name` is also in `fake_decls`, append a mismatch entry to a list if `player_args != fake_decls[name]`. Assert `not mismatches` with a multi-line failure message that names each mismatch as `{name}: Player has \`Signal({player_args})\`, FakePlayer has \`Signal({fake_decls[name]})\``.

    Critical implementation notes (cite RESEARCH.md):
    - Name-parity test uses `__dict__` not `dir()` (Pitfall 3)
    - Arity-parity test uses regex source-parse not metaobject introspection (Pitfall 4 — Shiboken metaobject inspection is fragile + version-dependent)
    - Both tests target `tests/_fake_player.py` specifically; the file path is hardcoded in `_grep_signal_decls` call site (not parameterized) because that is the canonical name

    File is ~50-60 lines total. No fixtures, no parametrize. Two standalone test functions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fake_player_signal_parity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_fake_player_signal_parity.py` exists
    - Contains exactly 2 test functions named `test_fake_player_mirrors_every_player_signal` and `test_fake_player_signal_arity_matches_player` (verify via `grep -cE "^def test_" tests/test_fake_player_signal_parity.py` returns 2)
    - Contains the literal call `Player.__dict__` somewhere in source (Pitfall 3 — verify via `grep -F "Player.__dict__" tests/test_fake_player_signal_parity.py` returns ≥ 1 line; alternatively the equivalent via `cls.__dict__` inside `_signal_names`)
    - Does NOT contain the literal `dir(Player)` (verify via `grep -F "dir(Player)" tests/test_fake_player_signal_parity.py` returns 0)
    - Contains the regex literal `r'^\s*([a-zA-Z_][a-zA-Z0-9_]*)\s*=\s*Signal\(([^)]*)\)'` exactly (verify via `grep -F "Signal\\\\(([^)]*)\\\\)" tests/test_fake_player_signal_parity.py` returns ≥ 1 line OR similar; intent is the arity-capturing pattern is present)
    - `uv run pytest tests/test_fake_player_signal_parity.py -x` exits 0 — proves the parity test passes against the Task 1 deliverable
    - Manual drift-injection check (does NOT need to be committed): temporarily remove one signal from `tests/_fake_player.py`, run the test, confirm it FAILS with a message naming that signal. Restore. Repeat for arity (change `Signal(int)` to `Signal(object)` for any signal, run test, confirm arity failure mentions both arg lists). This is a one-shot human sanity-check during execution; do not leave any modifications behind.
  </acceptance_criteria>
  <done>Both drift-guard tests exist; pass against Task 1 deliverable; would fail clearly with diagnostic messages if a future Player signal regression lands. This is the safety net that catches Phase 78+ work the way Phase 77 wishes Phases 62 through 73 had had.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_fake_player_no_inline.py — D-17 source-grep drift-guard</name>
  <files>tests/test_fake_player_no_inline.py</files>
  <read_first>
    - tests/_fake_player.py (created in Task 1)
    - tests/test_constants_drift.py:48-60 (Phase 61 precedent — rglob + ban-pattern shape that this file mirrors exactly)
    - tests/test_yt_dlp_opts_drift.py:1-44 (Phase 79 precedent)
    - 77-RESEARCH.md L440-469 (verified D-17 drift-guard body draft)
    - 77-PATTERNS.md L200-225 (rglob-must-include-subdirectory requirement: `tests/ui_qt/test_main_window_node_indicator.py:35` lives in a subdirectory)
    - Run `grep -rnE "^\s*class\s+_?FakePlayer\s*\(QObject\)" tests/` before writing the test to baseline the current 11 offenders (Plan 77-02 deliverables) — at execution time this number must be EXACTLY 11 (the current inline sites) since 77-02 has not run yet
  </read_first>
  <behavior>
    - Behavior 1: When run during Wave 1 (before Plan 77-02 migrations), the test FAILS with a message naming all 11 current inline-FakePlayer(QObject) sites. This is expected and proves the regex is correctly tuned.
    - Behavior 2: After Plan 77-02 migrates all 11 sites and removes their inline declarations, the test PASSES (zero offenders).
    - Behavior 3: If any future test file declares `class FakePlayer(QObject):` or `class _FakePlayer(QObject):` inline, the test FAILS naming that file.
    - Behavior 4: `tests/test_equalizer_dialog.py:40` (`class FakePlayer:` WITHOUT `(QObject)`) does NOT match the regex — verified by inspection. The non-QObject EQ-only double stays inline per CONTEXT D-09 Option A.
    - Behavior 5: Only `tests/_fake_player.py` is allowed to declare `class FakePlayer(QObject):` — confirmed by the `ALLOWED` set.

    IMPORTANT: this test will be RED until Plan 77-02 lands. Plan 77-02 IS the work that turns this test GREEN. The Wave 1 commit of this file therefore ships RED on purpose. Per the orchestrator's quality gate, that is acceptable here — the test is meaningful (it produces a precise actionable failure list for Plan 77-02). To prevent CI from blocking Wave 1 commit alone, this test does NOT need to pass at Wave 1 close; it MUST pass at Plan 77-02 close + Phase 77 close (Plan 77-06 full-suite gate).
  </behavior>
  <action>
    Create `tests/test_fake_player_no_inline.py` per the verified draft at `77-RESEARCH.md L440-469`. Mirror the `tests/test_constants_drift.py:48-60` shape (rglob + literal/regex search + ban-list + descriptive failure message).

    Module structure:

    1. Module docstring naming D-17 + the recurrence shape: only `tests/_fake_player.py` may declare `FakePlayer(QObject)`. Any test file that re-introduces an inline FakePlayer(QObject) class fails this guard immediately. Cite `tests/test_yt_dlp_opts_drift.py` as the same-project source-grep precedent. **The docstring MUST include this prominent known-RED notice in its first paragraph (verbatim or equivalent):** `KNOWN RED until Plan 77-02 lands. This test ships intentionally failing at Wave 1 close — its purpose is to enumerate the 11 inline-FakePlayer(QObject) sites that Plan 77-02 will migrate. Plan 77-06's full-suite phase gate is the first commit that requires this test GREEN. If you see this test RED during Wave 1, do NOT retry/revise — Wave 1's success criterion is the baseline-offender-count == 11 assertion in the orchestrator's <automated> command, NOT pytest exit 0 on this specific file.`

    2. Imports: `from __future__ import annotations`, `import re`, `from pathlib import Path`.

    3. Module-level constants:
       - `ROOT = Path(__file__).resolve().parent.parent / "tests"` — directory whose `*.py` files are scanned (NOT just `tests/test_*.py`; the rglob must include `tests/ui_qt/test_main_window_node_indicator.py`)
       - `FAKE_PLAYER_RE = re.compile(r'^\s*class\s+_?FakePlayer\s*\(QObject\)', re.M)` — matches `class FakePlayer(QObject)`, `class _FakePlayer(QObject)`, and indented variants but NOT `class FakePlayer:` (the test_equalizer_dialog case)
       - `ALLOWED = {"_fake_player.py"}` — single-file allow-list

    4. Single test function `test_no_inline_fake_player_subclass_in_tests()`:
       - Iterate `ROOT.rglob("*.py")` (NOT `glob` — must descend into `tests/ui_qt/`)
       - Skip if `py.name in ALLOWED`
       - Read with `try/except UnicodeDecodeError: continue` (Phase 61 precedent — be tolerant of any binary-encoded test fixture)
       - If `FAKE_PLAYER_RE.search(text)`, append `str(py.relative_to(ROOT.parent))` to `offenders` list
       - Assert `not offenders` with multi-line failure message naming each offender path and including the literal fix hint `Each offender below should \`from tests._fake_player import FakePlayer\` and delete its local copy`

    File is ~25-35 lines total. One test function. No fixtures.

    Acceptance note on the temporarily-RED state: at Wave 1 close, this test is expected to be RED with 11 offenders. That is by design and proves the regex is correctly scoped. Plan 77-02 closes the offenders. Plan 77-06 full-suite gate then includes this test running GREEN.
  </action>
  <verify>
    <automated>uv run python -c "import re; from pathlib import Path; ROOT = Path('tests'); FAKE_PLAYER_RE = re.compile(r'^\s*class\s+_?FakePlayer\s*\(QObject\)', re.M); ALLOWED = {'_fake_player.py'}; offenders = [str(p) for p in ROOT.rglob('*.py') if p.name not in ALLOWED and FAKE_PLAYER_RE.search(p.read_text(encoding='utf-8'))]; print(f'Wave 1 baseline offender count: {len(offenders)}'); assert len(offenders) == 11, f'expected 11 inline FakePlayer(QObject) sites, found {len(offenders)}: {offenders}'"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_fake_player_no_inline.py` exists
    - Contains the regex literal `r'^\s*class\s+_?FakePlayer\s*\(QObject\)'` (verify via `grep -F "class\\\\s+_?FakePlayer" tests/test_fake_player_no_inline.py` returns ≥ 1 line)
    - Contains the `ALLOWED = {"_fake_player.py"}` allow-list (verify via `grep -F '"_fake_player.py"' tests/test_fake_player_no_inline.py` returns ≥ 1 line)
    - Uses `rglob` (NOT `glob`) to descend into `tests/ui_qt/` (verify via `grep -F "rglob" tests/test_fake_player_no_inline.py` returns ≥ 1 line)
    - Contains exactly 1 test function `def test_no_inline_fake_player_subclass_in_tests` (verify via `grep -cE "^def test_" tests/test_fake_player_no_inline.py` returns 1)
    - When run at Wave 1 close, baseline offender count is exactly 11 (verified by the inline `python -c` command above which acts as a Wave 1 sanity check). This proves the regex correctly identifies all 11 known inline sites without false positives.
    - The test itself is allowed to be RED at Wave 1 close. Plan 77-02's success criterion is that this test turns GREEN after migration; Plan 77-06's full-suite gate verifies it stays GREEN.
  </acceptance_criteria>
  <done>tests/test_fake_player_no_inline.py exists; regex identifies exactly 11 inline-FakePlayer(QObject) offenders at Wave 1 baseline; will turn GREEN when Plan 77-02 migrates all 11. The test_equalizer_dialog.py non-QObject FakePlayer is correctly excluded by the regex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → file system | tests/_fake_player.py and the two drift-guard tests read project source files via `Path.read_text` — bounded to repo-relative paths |
| test process → PySide6 internals | drift-guard introspects `Player.__dict__` via `isinstance(v, Signal)` — read-only metaclass inspection, no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-S | Spoofing | `_grep_signal_decls` regex parse of `tests/_fake_player.py` | accept | Test infrastructure only; no untrusted input ever reaches this regex (the file is repo-controlled and committed before the drift-guard runs). |
| T-77-01-T | Tampering | Future test author adds inline FakePlayer(QObject) bypassing the shared module | mitigate | D-17 grep gate at `tests/test_fake_player_no_inline.py` fails the suite immediately on any new inline declaration. |
| T-77-01-T2 | Tampering | Future Player signal change without parity update | mitigate | D-16 source-introspection at `tests/test_fake_player_signal_parity.py` fails on name OR arity drift. |
| T-77-01-I | Information Disclosure | Drift-guard reads no secrets; only public test/source files | accept | n/a — no PII or credentials involved. |
| T-77-01-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 77 ships ZERO new dependencies — entire stack already pinned in `pyproject.toml` (pytest 9, pytest-qt 4, PySide6 6.11). No legitimacy gate needed. Package Legitimacy Audit table not required per `77-RESEARCH.md L70-82`. |
</threat_model>

<verification>
After all three tasks complete:

1. `uv run python -c "from tests._fake_player import FakePlayer; FakePlayer()"` exits 0 (Task 1 smoke)
2. `uv run pytest tests/test_fake_player_signal_parity.py -x` exits 0 (Task 2 — parity holds against Task 1 deliverable)
3. The inline drift-guard regex baseline reports exactly 11 offenders (Task 3 — the regex is correctly scoped; turning GREEN is Plan 77-02's job).
4. Optional one-shot human sanity check (do not commit): temporarily remove a signal from `tests/_fake_player.py` and confirm `test_fake_player_mirrors_every_player_signal` fails with a clear diagnostic naming the missing signal. Restore.
</verification>

<success_criteria>
- [ ] `tests/_fake_player.py` exists with 18 signals (production-correct arities) and 9 method stubs
- [ ] `tests/test_fake_player_signal_parity.py` exists; both `test_fake_player_mirrors_every_player_signal` and `test_fake_player_signal_arity_matches_player` pass
- [ ] `tests/test_fake_player_no_inline.py` exists; regex identifies exactly 11 baseline offenders at Wave 1 close (RED — by design until Plan 77-02 closes)
- [ ] Zero new third-party deps; entire delta is three new files under `tests/`
- [ ] No production-code changes (`musicstreamer/` is byte-identical post-Plan-77-01)
</success_criteria>

<output>
Create `.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-01-SUMMARY.md` when done. Record: count of signals declared (must be 18), count of method stubs (must be 9), Wave 1 baseline offender count for the no-inline guard (must be 11), and any deltas from the RESEARCH.md L350-371 canonical signal list (must be zero — if non-zero, halt and report which signal drifted between planning and execution).
</output>
