---
phase: 77-test-infrastructure-stabilization-fix-pre-existing-test-doub
plan: 06
type: execute
wave: 4
depends_on: ["77-01", "77-02", "77-03", "77-04", "77-05"]
files_modified:
  - .planning/PROJECT.md
autonomous: false
requirements: [INFRA-01]
tags: [test-infrastructure, phase-gate, full-suite-green, deferred-items-closure]

must_haves:
  truths:
    - "`uv run pytest tests/` exits 0 with no xfail / skip masking the six clusters from CONTEXT.md"
    - "Drift-guard tests `tests/test_fake_player_signal_parity.py` and `tests/test_fake_player_no_inline.py` both run GREEN as part of the full-suite run"
    - "MPRIS2 tests in `tests/test_media_keys_mpris2.py` pass without `RuntimeError: registerService(...) failed: name already taken` collisions"
    - "Cluster 3 cross-file teardown crashes are gone (test_main_window_integration → test_now_playing_panel; test_phase72_now_playing_panel → test_phase72_assumptions; test_main_window_underrun mid-suite; test_yt_scan_passes_through mid-suite)"
    - "Cluster 4 `_aa_quality` orphan + Cluster 5a/5b station_list_panel drifts + Cluster 6 Twitch streamlink-API drift all closed"
    - ".planning/PROJECT.md test-count line is updated to reflect the new pass rate (CONTEXT.md `<canonical_refs>` line 53)"
    - "deferred-items.md backlog accumulated across phases 51, 54, 55, 60.4, 61, 65, 66, 68, 71, 72, 72.1, 73 is closed by this phase"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated `Tests: N passing, M pre-existing failures` line reflecting Phase 77 outcome"
      contains: "Tests:"
  key_links:
    - from: "`uv run pytest tests/` exit 0"
      to: "All 6 CONTEXT clusters closed"
      via: "Cumulative deliverables from plans 77-01 through 77-05"
      pattern: "exit code 0 on full suite"
---

<objective>
Verify the cumulative outcome of Plans 77-01 through 77-05: `uv run pytest tests/` exits 0 across the full suite with no `xfail` / `skip` masking the six named clusters from CONTEXT.md. Update `.planning/PROJECT.md`'s `Tests:` line (CONTEXT line 53 mentions `399 passing, 1 pre-existing failure` — that count is widely-known to be undercounted per Phase 71 audit; this plan refreshes it to the real number).

This is the phase gate. Plans 77-01..77-05 individually verify their per-file deliverables; Plan 77-06's job is the FULL-SUITE rollup + the documentation update that records the new pass rate.

Purpose: close the deferred-items.md backlog accumulated across 12 phases. After this plan ships, future phases stop logging the same six cluster failures into their deferred-items.md files — they either pass or they reveal a genuinely-new failure (Phase 77 D-03 boundary).

Output:
- `.planning/PROJECT.md` `Tests:` line refreshed
- A single checkpoint:human-verify task confirming the user observed `uv run pytest tests/` exit 0 (or accepting any newly-discovered failures as Phase-77-D-03-deferred)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-CONTEXT.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-RESEARCH.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-VALIDATION.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-01-SUMMARY.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-02-SUMMARY.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-03-SUMMARY.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-04-SUMMARY.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-05-SUMMARY.md

<interfaces>
<!-- Current PROJECT.md test line (CONTEXT.md <canonical_refs> line 53 cites this) -->

From .planning/PROJECT.md (the line to update — actual content may vary; grep for `Tests:` to find current state):

```
Tests: 399 passing, 1 pre-existing failure
```

This number was 2026-04 era. Phase 71 audit revealed the true number was 35+ failures + 18+ setup errors (per CONTEXT line 53). Phase 77 closes those.

After Plan 77-05 lands, the new line should read (approximate — confirm by running pytest):

```
Tests: <N> passing
```

(Drop the `pre-existing failure` clause if zero failures remain. If 1-2 newly-discovered failures slip in per CONTEXT D-03 "7th cluster goes into <deferred>", record them with phase ref.)

Capture the exact pass count by running `uv run pytest tests/ --tb=no -q 2>&1 | tail -5` at this plan's execution. The pytest summary line at tail will show e.g., `399 passed in 45.32s` or `405 passed, 0 failed`.

<!-- The six clusters from CONTEXT.md that must be closed by this phase's end -->

| Cluster | Deliverable plan | Verification command |
|---------|------------------|----------------------|
| 1. FakePlayer drift | 77-01 + 77-02 | `uv run pytest tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py -x` exits 0 |
| 2. MPRIS2 DBus collision | 77-03 | `uv run pytest tests/test_media_keys_mpris2.py -x` exits 0 |
| 3. Qt teardown crashes (4 reproducers) | 77-05 | All 4 sequential pair runs exit 0; full-suite reaches `test_yt_scan_passes_through` without abort |
| 4. `_aa_quality` orphan | 77-04 Task 1 | `grep -F "_aa_quality" tests/` returns 0 lines |
| 5a. recent-3-vs-5 | 77-04 Task 3 | `uv run pytest tests/test_station_list_panel.py::test_refresh_recent_updates_list -x` exits 0 |
| 5b. isVisibleTo | 77-04 Task 3 | `uv run pytest tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode -x` exits 0 |
| 6. set_plugin_option | 77-04 Task 2 | `uv run pytest tests/test_twitch_auth.py -x` exits 0 |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full-suite green run + capture metrics</name>
  <files></files>
  <read_first>
    - All 5 prior plans' SUMMARY.md files (77-01 through 77-05) — confirm each individually verified its deliverable
    - .planning/PROJECT.md (find the `Tests:` line — may be at line ~53 per CONTEXT reference)
  </read_first>
  <behavior>
    - Behavior 1: `uv run pytest tests/ --tb=short` exits 0 — full suite green
    - Behavior 2: Each of the six clusters' per-cluster verification commands exits 0 (see table in <interfaces> above)
    - Behavior 3: No `xfail` or `skip` markers were added to mask any of the six cluster failures (the boundary check)
    - Behavior 4: The new test files from Plan 77-01 (`tests/_fake_player.py`, `tests/test_fake_player_signal_parity.py`, `tests/test_fake_player_no_inline.py`) are committed and run as part of the full suite
  </behavior>
  <action>
    1. Run the full suite, capturing both stdout and exit code:

       ```bash
       uv run pytest tests/ --tb=short 2>&1 | tee /tmp/77-06-fullsuite.log
       echo "Exit: $?"
       ```

       Inspect the tail for the pytest summary line (e.g., `XXX passed in YY.Ys` or `XXX passed, YY failed`). If failed > 0, the failure list MUST be one of:
       - A newly-discovered "7th cluster" failure per CONTEXT D-03 (record in 77-06-SUMMARY as deferred to a follow-up phase; do NOT auto-merge xfail)
       - A regression caused by one of plans 77-01..77-05 (return to that plan for fix; do NOT proceed)

    2. Run each per-cluster verification command from the <interfaces> table above to confirm each cluster is independently closed. Capture exit code per command. Any non-zero exit means that cluster's plan needs a fix-up commit.

    3. Source-grep verification that no `xfail` / `skip` markers were added to mask the six clusters during plans 77-01..77-05. Specifically:

       ```bash
       grep -rE "(@pytest\.mark\.xfail|@pytest\.mark\.skip|pytest\.skip\()" tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py tests/test_media_keys_mpris2.py tests/test_import_dialog_qt.py tests/test_twitch_auth.py tests/test_station_list_panel.py tests/test_main_window_underrun.py tests/test_main_window_integration.py tests/test_now_playing_panel.py tests/test_phase72_now_playing_panel.py tests/test_phase72_assumptions.py 2>/dev/null
       ```

       This command's output must NOT contain any of:
       - Any of the six cluster's test names
       - Any FakePlayer drift-guard test
       - Any MPRIS2 test

       If the command returns hits that include cluster-specific test names, those tests were silently masked — return to the relevant plan for fix and re-run this gate.

    4. Capture the final pass/fail metric for SUMMARY: extract the pytest summary line from `/tmp/77-06-fullsuite.log` and record verbatim (e.g., `405 passed in 47.21s`). Also capture the change vs CONTEXT line 53's prior `399 passing, 1 pre-existing failure`.

    Do NOT modify any test or production file in this task. If a regression is found, return to the originating plan (77-01..77-05) for fix. This plan is a verification gate, not a fix surface.
  </action>
  <verify>
    <automated>uv run pytest tests/ --tb=short 2>&1 | tail -10 && uv run pytest tests/ --tb=no -q 2>&1 | grep -E "passed|failed" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ --tb=short` exit code is 0 (full suite green)
    - Each per-cluster verification command from the <interfaces> table exits 0:
      - `uv run pytest tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py -x` exits 0
      - `uv run pytest tests/test_media_keys_mpris2.py -x` exits 0
      - `uv run pytest tests/test_main_window_integration.py tests/test_now_playing_panel.py -x` exits 0 (cross-file pair A)
      - `uv run pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_assumptions.py -x` exits 0 (cross-file pair B)
      - `uv run pytest tests/test_station_list_panel.py::test_refresh_recent_updates_list tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode -x` exits 0
      - `uv run pytest tests/test_twitch_auth.py -x` exits 0
    - `grep -F "_aa_quality" tests/` returns 0 lines
    - The xfail/skip grep command above returns no hits naming the six clusters' test functions
    - Pytest summary line captured verbatim for SUMMARY recording
  </acceptance_criteria>
  <done>Full suite runs green; each of the six clusters is independently verified closed; no xfail/skip masking detected; final pass count recorded for the next task to write into PROJECT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/PROJECT.md Tests: line to reflect Phase 77 outcome</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (find the `Tests:` line — likely near the top metadata section around line 50-60)
    - Task 1 output: the captured pytest summary line from `/tmp/77-06-fullsuite.log`
  </read_first>
  <behavior>
    - Behavior 1: `.planning/PROJECT.md` `Tests:` line reads (approximately) `Tests: <N> passing` where N matches the captured count from Task 1
    - Behavior 2: The `1 pre-existing failure` clause is removed (or updated to the actual count of any new D-03-deferred items)
    - Behavior 3: A one-line annotation references Phase 77 closure
  </behavior>
  <action>
    Open `.planning/PROJECT.md`. Search for the line beginning with `Tests:` (CONTEXT.md `<canonical_refs>` mentions this is around line 53, but the exact location may have shifted — use grep).

    Replace:
    - OLD: `Tests: 399 passing, 1 pre-existing failure` (or whatever current line reads)
    - NEW: `Tests: <N> passing` where N is the count from Task 1's verification (e.g., 405, 410 — depends on the precise post-Plan-77 number)

    Add a one-line annotation either inline or as an immediately-adjacent line referencing the closure: `(Phase 77 closed deferred-items.md backlog across phases 51, 54, 55, 60.4, 61, 65, 66, 68, 71, 72, 72.1, 73 — 2026-05-17)`.

    If Task 1 discovered a new "7th cluster" failure that was D-03-deferred (CONTEXT D-03 boundary), record the count: `Tests: <N> passing, 1 D-03-deferred (see 77-06-SUMMARY.md)`.

    Do NOT modify any other line in PROJECT.md. Do NOT update the version field (pyproject.toml + PROJECT.md version are auto-bumped per VER-01 / Phase 63 mechanism, not by Phase 77).
  </action>
  <verify>
    <automated>grep -E "^Tests:" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains a `Tests:` line with the new pass count from Task 1 (verify via `grep -E "^Tests: [0-9]+ passing" .planning/PROJECT.md` returns ≥ 1 line)
    - The old literal `Tests: 399 passing, 1 pre-existing failure` is gone (verify via `grep -F "Tests: 399 passing, 1 pre-existing failure" .planning/PROJECT.md` returns 0)
    - Phase 77 annotation present near the Tests: line (verify via `grep -F "Phase 77" .planning/PROJECT.md` returns ≥ 1 line — though Phase 77 may also appear elsewhere; the requirement is at least one ref in proximity to Tests:)
  </acceptance_criteria>
  <done>PROJECT.md Tests: line refreshed to reflect post-Phase-77 pass count. Annotation links to the closed deferred-items backlog. Documentation is now consistent with the actual test state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User confirms Phase 77 gate is green</name>
  <files>(verification-only task — no file modifications)</files>
  <verify>
    <human-check>User runs the four verification commands listed under `<how-to-verify>` and confirms each produces the expected output. Approval string from `<resume-signal>` is recorded in 77-06-SUMMARY.md.</human-check>
  </verify>
  <done>User typed "approved" (or describes the issue found). If approved, Phase 77 is closed. If issue reported, route to the originating plan (77-01..77-05) for fix-up commit and re-run this checkpoint.</done>
  <action>
    Present the Phase 77 close-out summary to the user (everything in `<what-built>` below). Run no automated changes — this is purely a verification gate. Wait on the user's resume-signal before marking Phase 77 closed. If user reports any failure, route to the responsible plan (77-01..77-05) for fix-up commit rather than masking via xfail.
  </action>
  <what-built>
    All 6 CONTEXT clusters closed across Plans 77-01 through 77-05:
    - Cluster 1 (FakePlayer drift): shared tests/_fake_player.py + 2 drift-guards (signal parity + no-inline)
    - Cluster 2 (MPRIS2 collision): unique_mpris_service_name fixture
    - Cluster 3 (Qt teardown crashes): block_real_network fixture + worker.wait drain
    - Cluster 4 (_aa_quality orphan): 2 test functions deleted
    - Cluster 5a (recent 3-vs-5): test asserts against min(5, len(...)) matching production
    - Cluster 5b (isVisibleTo): replaced with _stack.currentIndex() check
    - Cluster 6 (set_plugin_option): test asserts production-correct set_option API with MagicMock(spec=Streamlink) drift-guard

    Documentation: .planning/PROJECT.md `Tests:` line refreshed.
    Production code: byte-identical (musicstreamer/ unchanged across all 6 plans).
    Drift-guards installed: tests/test_fake_player_signal_parity.py and tests/test_fake_player_no_inline.py run as part of every future pytest invocation.
  </what-built>
  <how-to-verify>
    1. From the project root, run the full suite:
       ```
       uv run pytest tests/
       ```
       Expected: exit code 0. The pytest summary line should match the count recorded in 77-06-SUMMARY.md (Task 1).

    2. Run the drift-guard tests in isolation to confirm they're wired up:
       ```
       uv run pytest tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py -v
       ```
       Expected: both files pass with 3 passing tests total (2 parity + 1 no-inline).

    3. Sanity-check the production code is byte-identical:
       ```
       git diff musicstreamer/
       ```
       Expected: empty output (zero production-code changes across Phase 77).

    4. Confirm zero `xfail` / `skip` markers mask the six clusters:
       ```
       grep -rE "(@pytest\.mark\.xfail|@pytest\.mark\.skip|pytest\.skip\()" tests/test_fake_player_signal_parity.py tests/test_fake_player_no_inline.py tests/test_media_keys_mpris2.py tests/test_import_dialog_qt.py tests/test_twitch_auth.py tests/test_station_list_panel.py
       ```
       Expected: no output, OR output that does NOT name any of the cluster-specific tests.

    5. If any of the above commands produces unexpected output, do NOT approve. Report the discrepancy so the responsible plan can be re-opened.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 77 closed, or describe the issue you found (e.g., "test_X is still failing", "_aa_quality reference still present in tests/Y") so a fix-up commit can land.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → full pytest suite | The phase gate verifies the cumulative result of 5 prior plans. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-06-R | Repudiation | A test that fails for a new reason (unrelated to the 6 clusters) gets silently swept under "D-03-deferred" without proper investigation | mitigate | Task 1 explicitly grep-checks for xfail/skip masking and explicitly records any new-cluster failure in 77-06-SUMMARY.md so it's visible to the next phase. CONTEXT D-03 boundary is enforced: don't silently expand scope. |
| T-77-06-T | Tampering | PROJECT.md is edited beyond the Tests: line | accept | Task 2 acceptance criteria requires the literal-grep delta — anything more drastic fails the gate. |
| T-77-06-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies for this verification plan. |
</threat_model>

<verification>
1. `uv run pytest tests/ --tb=short` exits 0 (Task 1 main gate)
2. All 6 per-cluster verification commands exit 0 (Task 1 cluster-level checks)
3. `grep -E "^Tests:" .planning/PROJECT.md` returns the new pass count (Task 2)
4. User has confirmed via the checkpoint:human-verify task (Task 3)
</verification>

<success_criteria>
- [ ] `uv run pytest tests/` exits 0 (full suite green)
- [ ] Per-cluster verification commands all exit 0
- [ ] No xfail/skip markers mask the six clusters
- [ ] `.planning/PROJECT.md` Tests: line refreshed
- [ ] User-verified via checkpoint
- [ ] Zero production code change (musicstreamer/ byte-identical across all of Phase 77)
- [ ] Zero new dependencies across all of Phase 77
- [ ] Drift-guards (tests/test_fake_player_signal_parity.py, tests/test_fake_player_no_inline.py) installed and passing
</success_criteria>

<output>
Create `.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-06-SUMMARY.md`. Record:
- Final pytest summary line verbatim
- Per-cluster verification command exit codes (table)
- Pre-Phase-77 baseline failure list (from CONTEXT.md `<canonical_refs>` line 53 quote: "Tests: 399 passing, 1 pre-existing failure" — note this was undercounted)
- Post-Phase-77 actual count from Task 1
- Confirmation `git diff musicstreamer/` is empty
- Any D-03-deferred new-cluster failures (should be zero — but if non-zero, record them with phase reference for the next planner)
- User approval string from Task 3 checkpoint
</output>
