---
phase: 77-test-infrastructure-stabilization-fix-pre-existing-test-doub
plan: 04
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - tests/test_import_dialog_qt.py
  - tests/test_twitch_auth.py
  - tests/test_station_list_panel.py
autonomous: true
requirements: [INFRA-01]
tags: [test-infrastructure, test-impl-drift, cleanup]

must_haves:
  truths:
    - "`_aa_quality` orphan assertions are deleted from tests/test_import_dialog_qt.py (Phase 56 widget removal closure)"
    - "tests/test_twitch_auth.py asserts the production-correct streamlink API `session.set_option('twitch-api-header', ...)` — NOT the deleted `set_plugin_option` (D-05 INVERTED per RESEARCH §Summary 1)"
    - "tests/test_twitch_auth.py uses `MagicMock(spec=Streamlink)` so reintroducing the removed `set_plugin_option` API raises AttributeError at test time (drift-guard via spec)"
    - "tests/test_twitch_auth.py module docstring at lines 1-9 no longer references the deleted streamlink 5.x API"
    - "tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode no longer uses `isVisibleTo(panel)` against an unshown widget (D-15 REVISED)"
    - "tests/test_station_list_panel.py::test_refresh_recent_updates_list asserts against `min(5, len(repo._recent))` matching production `list_recently_played(5)` (D-06)"
    - "Production code (musicstreamer/) is byte-identical — all changes are test-side per CONTEXT D-04, D-05 (revised), D-06, D-15 (revised)"
  artifacts:
    - path: "tests/test_import_dialog_qt.py"
      provides: "Import dialog tests with `_aa_quality` orphan assertions deleted"
    - path: "tests/test_twitch_auth.py"
      provides: "Twitch test asserting the migrated streamlink API + MagicMock(spec=Streamlink) drift-guard + updated module docstring"
      contains: "session.set_option"
    - path: "tests/test_station_list_panel.py"
      provides: "Station list panel tests with isVisibleTo→currentIndex swap + recent-count fix"
      contains: "min(5, len(repo._recent))"
  key_links:
    - from: "tests/test_twitch_auth.py"
      to: "musicstreamer/player.py:1156 (session.set_option('twitch-api-header', ...))"
      via: "MagicMock.set_option.assert_called_once_with assertion"
      pattern: "set_option.*twitch-api-header"
    - from: "tests/test_station_list_panel.py"
      to: "musicstreamer/ui_qt/station_list_panel.py:492 (list_recently_played(5))"
      via: "rowCount assertion mirrors production limit"
      pattern: "min\\(5,"
---

<objective>
Close four test↔impl drift items in a single coordinated plan: (a) delete `_aa_quality` orphan assertions in `tests/test_import_dialog_qt.py` (Cluster 4 — Phase 56 widget removal carry-over); (b) rewrite `tests/test_twitch_auth.py` to assert the production-correct `session.set_option("twitch-api-header", ...)` API (Cluster 6 — D-05 INVERTED per RESEARCH §Summary 1; `set_plugin_option` was removed in streamlink 6.0); (c) update `tests/test_station_list_panel.py::test_filter_strip_hidden_in_favorites_mode` to use `panel._stack.currentIndex()` instead of `isVisibleTo(panel)` (Cluster 5b — D-15 REVISED; the failure is unshown-top-level, not offscreen-platform); (d) update `tests/test_station_list_panel.py::test_refresh_recent_updates_list` to assert against `min(5, len(repo._recent))` matching production `list_recently_played(5)` (Cluster 5a — D-06).

Purpose: each of these four drifts has been logged across multiple phases' deferred-items.md (51, 56, 65, 66, 68, 71, 72, 73) without resolution because the fixes seemed too small to justify their own phases. Phase 77 closes them in a single commit set. Production code is unchanged — every fix is test-side per the corresponding CONTEXT decision.

Output: 3 modified test files. Zero production code changes. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/TESTING.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-CONTEXT.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-RESEARCH.md
@.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-PATTERNS.md

<interfaces>
<!-- Cluster 4: _aa_quality orphan locations in tests/test_import_dialog_qt.py -->

From tests/test_import_dialog_qt.py (current state at lines 141-154):

```
def test_audioaddict_tab_widgets(dialog):       # line 141
    assert dialog._aa_key is not None
    assert dialog._aa_quality is not None       # line 143 — ORPHAN (Phase 56 commit 414e236 removed this attribute)
    assert dialog._aa_import_btn is not None

def test_audioaddict_quality_combo(dialog):     # line 152
    items = [dialog._aa_quality.itemText(i) for i in range(dialog._aa_quality.count())]
    assert items == ["hi", "med", "low"]
```

CONTEXT D-04 prescribes deletion of BOTH functions wholesale. The remaining `_aa_key` and `_aa_import_btn` checks in test_audioaddict_tab_widgets are valid but the mixed-orphan function goes per D-04. If a future phase wants to re-introduce the AA Quality dropdown UX, file a new feature phase.

<!-- Cluster 6: Twitch streamlink API drift; D-05 REVISED per RESEARCH §Summary 1 -->

From musicstreamer/player.py:1156 (PRODUCTION, already migrated):

```
session.set_option("twitch-api-header", [...])
```

streamlink 8.3.0 installed (verified via `python -c "import streamlink; print(streamlink.__version__)"`). `Streamlink.set_plugin_option()` was removed in streamlink 6.0.0 (PR #5033, 2023-07-20). The Phase 31 docstring at tests/test_twitch_auth.py:1-9 references the removed API and is 2 years stale.

From tests/test_twitch_auth.py (current shape — lines 1-9 docstring + lines 68-86 first test):

```
"""...session.set_plugin_option('twitch', 'api-header', ...) — scoped to..."""   # line 5: STALE
...
def test_play_twitch_sets_plugin_option_when_token_present(...):
    ...
    session.set_plugin_option.assert_called_once_with(
        "twitch", "api-header", [("Authorization", "OAuth abc123")]
    )                                                                                # line 84-86: REWRITE
```

Plus two sibling tests at lines 89-103 (`test_play_twitch_no_header_when_token_absent` etc.) that have `session.set_plugin_option.assert_not_called()` — these must also flip to `session.set_option.assert_not_called()` with `set_plugin_option` removed from spec.

Locations of `set_plugin_option` references in tests/test_twitch_auth.py:
- Line 5 (module docstring)
- Line 70 (in-function docstring)
- Line 84 (assert_called_once_with — Cluster 6 main test)
- Line 90 (in-function docstring sibling)
- Line 103 (assert_not_called sibling 1)
- Line 121 (assert_not_called sibling 2)

All 6 occurrences must flip to `set_option` semantics. Use `MagicMock(spec=Streamlink)` (NEW import) so any future regression that re-introduces `set_plugin_option` raises AttributeError immediately.

From RESEARCH.md L555-578 (verified rewrite draft for the main test — apply this verbatim):

```python
def test_play_twitch_sets_option_when_token_present(qtbot, tmp_path, monkeypatch):
    """When the token file exists and has content, the worker calls
    session.set_option('twitch-api-header', [...])."""
    from streamlink.session import Streamlink  # NEW: import for spec=
    from musicstreamer import paths
    monkeypatch.setattr(paths, "_root_override", str(tmp_path))
    token_file = tmp_path / "twitch-token.txt"
    token_file.write_text("abc123")

    session = MagicMock(spec=Streamlink)
    session.streams.return_value = {"best": _FakeStream("https://ok.m3u8")}

    p = make_player(qtbot)
    with patch("streamlink.session.Streamlink", return_value=session):
        with qtbot.waitSignal(p.twitch_resolved, timeout=2000):
            p._twitch_resolve_worker("https://www.twitch.tv/testchannel")

    session.set_option.assert_called_once_with(
        "twitch-api-header", [("Authorization", "OAuth abc123")]
    )
```

<!-- Cluster 5a + 5b: tests/test_station_list_panel.py -->

From tests/test_station_list_panel.py (current state):

Line 43 (`FakeRepo.list_recently_played` default kwarg — STAYS at 3, matches the seed data of 3 recent stations):
```
def list_recently_played(self, n: int = 3) -> list[Station]:
    return list(self._recent[:n])
```

Production at musicstreamer/ui_qt/station_list_panel.py:492 calls `self._repo.list_recently_played(5)` — so test must follow.

Line 208 (`test_recently_played_populated` — _sample_repo has 3 recent stations; list_recently_played(5) returns min(5,3)=3; assertion `== 3` is correct AS-IS; recommendation: change to `min(5, len(repo._recent))` for robustness OR leave as-is since it's not in CONTEXT.md's cluster list):
```
def test_recently_played_populated(qtbot):
    panel = StationListPanel(_sample_repo())
    qtbot.addWidget(panel)
    assert panel.recent_view.model().rowCount() == 3  # OK as-is
```

Line 317-333 (`test_filter_strip_hidden_in_favorites_mode` — D-15 REVISED: replace isVisibleTo with stack-page check):
```
def test_filter_strip_hidden_in_favorites_mode(qtbot):
    ...
    panel = StationListPanel(_sample_repo_with_favorites())
    qtbot.addWidget(panel)

    assert panel._stack.currentIndex() == 0
    assert panel._search_box.isVisibleTo(panel), "search box should be visible in Stations mode"   # line 326 — DELETE

    panel._favorites_btn.click()
    assert panel._stack.currentIndex() == 1
    assert not panel._search_box.isVisibleTo(panel), "search box should not be visible in Favorites mode"   # line 332 — DELETE
```

Per RESEARCH §Pitfall 2: `isVisibleTo()` returns False on widgets whose top-level was never `show()`-n. The test never calls `panel.show()`. The fix is to delete the two `isVisibleTo` lines — the `currentIndex()` checks at lines 325 and 330 already cover the semantic (page 0 active == search box visible-when-shown).

Line 504-520 (`test_refresh_recent_updates_list` — D-06: production calls list_recently_played(5)):
```
def test_refresh_recent_updates_list(qtbot):
    repo = _sample_repo()    # _recent has 3 stations
    panel = StationListPanel(repo)
    qtbot.addWidget(panel)

    new_top = make_station(99, "New Top Station", "TestFM")
    repo._recent = [new_top] + repo._recent    # _recent now has 4 stations

    panel.refresh_recent()    # production calls list_recently_played(5) → returns min(5, 4) = 4

    assert panel.recent_view.model().rowCount() == 3   # line 517 — WRONG (will be 4)
    top_station = panel.recent_view.model().index(0, 0).data(Qt.UserRole)
    ...
```

Fix per RESEARCH.md L523-541: change line 517 to `assert panel.recent_view.model().rowCount() == min(5, len(repo._recent))`. The remaining assertions on `top_station` (lines 518-520) are correct.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete _aa_quality orphan assertions (Cluster 4 — D-04)</name>
  <files>tests/test_import_dialog_qt.py</files>
  <read_first>
    - tests/test_import_dialog_qt.py:135-160 (the two orphan functions + surrounding context to confirm safe deletion)
    - musicstreamer/ui_qt/import_dialog.py (grep `_aa_quality` — confirm ZERO matches in production; this is the safety check that proves the widget is genuinely deleted, not just renamed)
    - 77-CONTEXT.md D-04 (locked direction: test follows impl)
    - 77-RESEARCH.md L511-518 (deletion specification)
  </read_first>
  <behavior>
    - Behavior 1: `tests/test_import_dialog_qt.py` no longer contains either `test_audioaddict_tab_widgets` or `test_audioaddict_quality_combo` function
    - Behavior 2: `grep -F "_aa_quality" tests/test_import_dialog_qt.py` returns 0 matches
    - Behavior 3: All other tests in `tests/test_import_dialog_qt.py` (including `test_audioaddict_invalid_key_shows_error`, `test_audioaddict_invalid_key_error_text`, etc.) still pass
    - Behavior 4: Production `musicstreamer/ui_qt/import_dialog.py` is unchanged
  </behavior>
  <action>
    Open `tests/test_import_dialog_qt.py`. Locate `def test_audioaddict_tab_widgets(dialog):` at line ~141 — delete the entire function (function header through final `assert dialog._aa_import_btn is not None`, ~4 lines). Locate `def test_audioaddict_quality_combo(dialog):` at line ~152 — delete the entire function (function header through `assert items == ["hi", "med", "low"]`, ~3 lines). Total deletion: ~7-10 lines including any blank lines between the functions.

    Per CONTEXT D-04, delete both functions wholesale. Do NOT preserve the `_aa_key` and `_aa_import_btn` checks elsewhere — if those checks are wanted, file a follow-up phase. Phase 77's job is to close the orphan, not to preserve partial coverage. The remaining `test_audioaddict_invalid_key_shows_error`, `test_audioaddict_invalid_key_error_text`, `test_audioaddict_import_progress`, `test_inputs_disabled_during_import`, and `test_aa_import_emits_imported_and_skipped` already exercise the AA tab — coverage is fine without these two.

    Do NOT touch any other test in the file. Do NOT modify the `dialog` fixture definition. Do NOT touch production code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_import_dialog_qt.py -x --deselect tests/test_import_dialog_qt.py::test_yt_scan_passes_through</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "_aa_quality" tests/test_import_dialog_qt.py` returns 0 lines (orphan attribute references gone)
    - `grep -cE "^def test_audioaddict_tab_widgets" tests/test_import_dialog_qt.py` returns 0 (function deleted)
    - `grep -cE "^def test_audioaddict_quality_combo" tests/test_import_dialog_qt.py` returns 0 (function deleted)
    - `git diff musicstreamer/ui_qt/import_dialog.py` returns empty (production unchanged)
    - `uv run pytest tests/test_import_dialog_qt.py -x --deselect tests/test_import_dialog_qt.py::test_yt_scan_passes_through` exits 0 (the deselect avoids the Cluster 3 Qt-teardown crash which Plan 77-05 handles separately)
  </acceptance_criteria>
  <done>Two orphan _aa_quality test functions deleted; remaining import-dialog tests still pass; production code unchanged. The `test_yt_scan_passes_through` crash is out of scope for this plan (Plan 77-05 handles it).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite tests/test_twitch_auth.py to assert production-correct streamlink API (Cluster 6 — D-05 REVISED)</name>
  <files>tests/test_twitch_auth.py</files>
  <read_first>
    - tests/test_twitch_auth.py (full file — confirm all 6 `set_plugin_option` references and the docstring shape)
    - musicstreamer/player.py:1140-1167 (production `_twitch_resolve_worker` body — `session.set_option("twitch-api-header", [...])` at line 1156)
    - Run `uv run python -c "from streamlink.session import Streamlink; s = Streamlink(); print('set_plugin_option:', hasattr(s, 'set_plugin_option')); print('set_option:', hasattr(s, 'set_option'))"` BEFORE making changes — confirms `set_plugin_option=False, set_option=True` on the installed streamlink 8.3.0 (this is the safety check that proves D-05's inversion is correct)
    - 77-CONTEXT.md D-05 REVISED + 77-RESEARCH.md L555-578 (verified rewrite draft)
    - 77-RESEARCH.md L276-286 (Pitfall 1 — why `MagicMock(spec=Streamlink)` is essential)
  </read_first>
  <behavior>
    - Behavior 1: Module docstring at lines 1-9 no longer references `set_plugin_option` — it describes the migrated `set_option("twitch-api-header", ...)` API
    - Behavior 2: First test (currently `test_play_twitch_sets_plugin_option_when_token_present` at line 68) is renamed to `test_play_twitch_sets_option_when_token_present` and asserts `session.set_option.assert_called_once_with("twitch-api-header", [("Authorization", "OAuth abc123")])`
    - Behavior 3: All 3 tests that mock streamlink use `MagicMock(spec=Streamlink)` so any future code that tries to call the removed `session.set_plugin_option(...)` raises AttributeError at test time
    - Behavior 4: Sibling tests at lines ~89-103 and ~106-121 (`test_play_twitch_no_header_when_token_absent` etc.) flip their `session.set_plugin_option.assert_not_called()` to `session.set_option.assert_not_called()`
    - Behavior 5: All in-function docstrings that mention `set_plugin_option` are updated to mention `set_option` consistently
    - Behavior 6: Production `musicstreamer/player.py` is unchanged
  </behavior>
  <action>
    Open `tests/test_twitch_auth.py`. Apply the following coordinated edits in a single pass:

    1. Module docstring (lines 1-9): Replace the `session.set_plugin_option('twitch', 'api-header', ...) — scoped to the twitch plugin only (Pitfall 6)` paragraph with a current-API description. Suggested text:

       ```
       """Tests for Twitch token-header threading through _twitch_resolve_worker.

       Production at `musicstreamer/player.py:1156` calls
       `session.set_option("twitch-api-header", [("Authorization", "OAuth <token>")])`
       on the streamlink session when a non-empty `twitch-token.txt` exists in
       paths.twitch_token_path().

       Phase 77 D-05 (REVISED) note: streamlink removed `Streamlink.set_plugin_option`
       in version 6.0.0 (PR #5033, 2023-07-20). Project pins `streamlink>=8.3`. These
       tests use `MagicMock(spec=Streamlink)` so any future regression that
       reintroduces the removed `set_plugin_option` API raises AttributeError at
       test time — drift-guard via spec.
       """
       ```

    2. Add `from streamlink.session import Streamlink` to the imports section near the top of the file (after the existing `from unittest.mock import ...` import).

    3. First test (line ~68, currently `def test_play_twitch_sets_plugin_option_when_token_present`): rename function to `test_play_twitch_sets_option_when_token_present`. Replace the body per the verified RESEARCH.md L555-578 draft:
       - Update in-function docstring to: `"""When the token file exists and has content, the worker calls session.set_option('twitch-api-header', [...])."""`
       - Replace `session = MagicMock()` with `session = MagicMock(spec=Streamlink)` (the spec= drift-guard)
       - Replace the assertion:
         - OLD: `session.set_plugin_option.assert_called_once_with("twitch", "api-header", [("Authorization", "OAuth abc123")])`
         - NEW: `session.set_option.assert_called_once_with("twitch-api-header", [("Authorization", "OAuth abc123")])`

    4. Sibling test at line ~89 (`test_play_twitch_no_header_when_token_absent`):
       - In-function docstring at line 90: change `set_plugin_option` → `set_option`
       - At line 103: change `session.set_plugin_option.assert_not_called()` → `session.set_option.assert_not_called()`
       - Also `session = MagicMock()` → `session = MagicMock(spec=Streamlink)` (consistent spec= drift-guard)

    5. Second sibling test (around line 106-121, the one with line 121 `set_plugin_option.assert_not_called()`):
       - Apply the same docstring + assertion + spec= edits

    Do NOT change the test fixture (`make_player(qtbot)`, the `tmp_path` + `paths._root_override` pattern). Do NOT touch production code. Do NOT modify the FakeStream / FakeStreams helpers — they were never tied to the API drift.

    After edits, total `set_plugin_option` references in the file MUST be 0 (verify via `grep -c set_plugin_option tests/test_twitch_auth.py` returns 0). Total `set_option` references should be 3+ (one per test that originally asserted plugin-option behavior).
  </action>
  <verify>
    <automated>uv run pytest tests/test_twitch_auth.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "set_plugin_option" tests/test_twitch_auth.py` returns 0 (all 6 references removed)
    - `grep -c "set_option" tests/test_twitch_auth.py` returns ≥ 3 (each previously-`set_plugin_option` reference now flipped)
    - `grep -F "MagicMock(spec=Streamlink)" tests/test_twitch_auth.py` returns ≥ 3 lines (spec= drift-guard wired into the 3 streamlink-mocking tests)
    - `grep -F "from streamlink.session import Streamlink" tests/test_twitch_auth.py` returns ≥ 1 line (the new import)
    - `grep -cE "^def test_play_twitch_sets_option_when_token_present" tests/test_twitch_auth.py` returns 1 (function renamed)
    - `grep -cE "^def test_play_twitch_sets_plugin_option_when_token_present" tests/test_twitch_auth.py` returns 0 (old name gone)
    - Module docstring no longer references `set_plugin_option` — verify via `head -10 tests/test_twitch_auth.py | grep -F "set_plugin_option"` returns 0 lines
    - `git diff musicstreamer/player.py` returns empty (production unchanged)
    - `uv run pytest tests/test_twitch_auth.py -x` exits 0
  </acceptance_criteria>
  <done>Twitch auth tests assert the production-correct `set_option("twitch-api-header", ...)` API. `MagicMock(spec=Streamlink)` drift-guard installed so reintroducing the removed `set_plugin_option` raises AttributeError. Module docstring + 3 tests + 6 in-source references all updated consistently. Production code byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix tests/test_station_list_panel.py — isVisibleTo→currentIndex swap (D-15) + recent count limit (D-06)</name>
  <files>tests/test_station_list_panel.py</files>
  <read_first>
    - tests/test_station_list_panel.py:315-345 (test_filter_strip_hidden_in_favorites_mode — lines 318-333 currently fail at line 326 with `isVisibleTo` returning False)
    - tests/test_station_list_panel.py:500-525 (test_refresh_recent_updates_list — line 517 assertion `rowCount() == 3` is wrong post-mutation)
    - tests/test_station_list_panel.py:34-70 (FakeRepo + _sample_repo — confirms _recent default has 3 stations)
    - musicstreamer/ui_qt/station_list_panel.py:485-498 (`_populate_recent` calls `self._repo.list_recently_played(5)` — production correct, do not change)
    - 77-CONTEXT.md D-06 + D-15 REVISED
    - 77-RESEARCH.md L290-298 (Pitfall 2 — isVisibleTo unshown-widget gotcha)
    - 77-RESEARCH.md L490-541 (verified rewrite drafts)
  </read_first>
  <behavior>
    - Behavior 1: `test_filter_strip_hidden_in_favorites_mode` no longer asserts on `isVisibleTo(panel)`. The two `isVisibleTo` lines at L326 and L332 are removed. The remaining `_stack.currentIndex()` checks at L325 and L330 are sufficient (page 0 active ⇒ search box visible-when-shown).
    - Behavior 2: `test_refresh_recent_updates_list` asserts `rowCount() == min(5, len(repo._recent))` matching production `list_recently_played(5)` — robust to seed-data changes
    - Behavior 3: Both tests pass under `QT_QPA_PLATFORM=offscreen` without panel.show() / qtbot.waitExposed() calls
    - Behavior 4: Other tests in tests/test_station_list_panel.py are unchanged (in particular `test_recently_played_populated` at line 208 stays as-is — its assertion `rowCount() == 3` is correct because _sample_repo's _recent has 3 stations and min(5, 3) = 3)
    - Behavior 5: Production `musicstreamer/ui_qt/station_list_panel.py` is unchanged
  </behavior>
  <action>
    Open `tests/test_station_list_panel.py`. Apply two targeted edits:

    Edit 1 (D-15) — lines 326 and 332:
    - DELETE line 326: `assert panel._search_box.isVisibleTo(panel), "search box should be visible in Stations mode"`
    - DELETE line 332: `assert not panel._search_box.isVisibleTo(panel), "search box should not be visible in Favorites mode"`

    The two surrounding `_stack.currentIndex()` checks at lines 325 and 330 stay. After deletion the test reads:
    ```
    def test_filter_strip_hidden_in_favorites_mode(qtbot):
        ...
        panel = StationListPanel(_sample_repo_with_favorites())
        qtbot.addWidget(panel)

        # In Stations mode, search box is on page 0 (semantic visibility check)
        assert panel._stack.currentIndex() == 0

        # Switch to Favorites
        panel._favorites_btn.click()
        assert panel._stack.currentIndex() == 1
    ```

    Optional: add a one-line comment above each `currentIndex()` assertion documenting the D-15 rationale (`# D-15: page-index check replaces isVisibleTo (panel never shown under offscreen)` or similar). Keep the comment brief and pointing back to the decision.

    Edit 2 (D-06) — line 517:
    - OLD: `assert panel.recent_view.model().rowCount() == 3`
    - NEW: `assert panel.recent_view.model().rowCount() == min(5, len(repo._recent))`

    Optional: add a one-line comment above documenting D-06 (`# D-06: production calls list_recently_played(5); robust against seed-data changes`).

    Do NOT touch line 208 (`test_recently_played_populated`'s `assert ... rowCount() == 3`) — that assertion is CORRECT for the unmodified _sample_repo (3 recent stations, min(5, 3) = 3). Per CONTEXT D-03 ("Pre-existing failures NOT in the six-cluster list stay deferred"), do not change tests not explicitly in scope.

    Do NOT change `tests/test_station_list_panel.py:43` (`def list_recently_played(self, n: int = 3)` default) — the kwarg default is part of FakeRepo's signature, and production always passes the explicit `5`. Changing the default would touch out-of-scope tests.

    Do NOT touch production code (`musicstreamer/ui_qt/station_list_panel.py:492` stays at `list_recently_played(5)`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_station_list_panel.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "isVisibleTo" tests/test_station_list_panel.py` returns 0 lines (both isVisibleTo assertions deleted)
    - `grep -F "min(5, len(repo._recent))" tests/test_station_list_panel.py` returns ≥ 1 line (the new recent-count assertion)
    - `grep -cE "^def test_filter_strip_hidden_in_favorites_mode" tests/test_station_list_panel.py` returns 1 (function still exists, just slimmer)
    - `grep -cE "^def test_refresh_recent_updates_list" tests/test_station_list_panel.py` returns 1 (function still exists)
    - Line 208 (`test_recently_played_populated` assertion) is UNCHANGED — verify via `grep -F "assert panel.recent_view.model().rowCount() == 3" tests/test_station_list_panel.py` returns 1 line (the unmodified test_recently_played_populated assertion; the test_refresh_recent_updates_list assertion that USED to be `== 3` is now `== min(...)` so this grep returns 1 not 2)
    - `git diff musicstreamer/ui_qt/station_list_panel.py` returns empty (production unchanged)
    - `uv run pytest tests/test_station_list_panel.py -x` exits 0
  </acceptance_criteria>
  <done>D-15 + D-06 fixes land in a single commit. test_filter_strip_hidden_in_favorites_mode passes without panel.show() or qtbot.waitExposed(). test_refresh_recent_updates_list passes against production's `list_recently_played(5)`. Production code byte-identical. test_recently_played_populated (out of scope per D-03) untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → mocked streamlink Session | `MagicMock(spec=Streamlink)` constrains the mock surface to the real Streamlink class. Removed methods (e.g., `set_plugin_option`) raise AttributeError on access — drift-guard via spec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-T | Tampering | Future regression re-introduces `set_plugin_option` call in production | mitigate | `MagicMock(spec=Streamlink)` raises AttributeError on `session.set_plugin_option(...)` because the real class no longer has that method. Test fails immediately. RESEARCH §Pitfall 1. |
| T-77-04-T2 | Tampering | _aa_quality re-introduced inadvertently | accept | Phase 56 deliberately removed it. If re-introduced, the new feature phase MUST also add new tests; the orphan deletion in this plan is not a drift-guard, just cleanup. |
| T-77-04-T3 | Tampering | Production changes list_recently_played(5) limit to something else | mitigate | `min(5, len(...))` test assertion would still pass for the old limit but fail loudly for a higher limit. For a stricter drift-guard, future maintainer can pin the literal `5` — but D-06 LOCKED `min(5, ...)` for seed-data robustness. |
| T-77-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies — `streamlink` already pinned in `pyproject.toml`. |
</threat_model>

<verification>
1. `uv run pytest tests/test_import_dialog_qt.py -x --deselect tests/test_import_dialog_qt.py::test_yt_scan_passes_through` exits 0 (Task 1)
2. `uv run pytest tests/test_twitch_auth.py -x` exits 0 (Task 2)
3. `uv run pytest tests/test_station_list_panel.py -x` exits 0 (Task 3)
4. `git diff musicstreamer/` returns empty (zero production code change across all 3 tasks)
</verification>

<success_criteria>
- [ ] `_aa_quality` orphan assertions deleted; production `import_dialog.py` unchanged
- [ ] tests/test_twitch_auth.py asserts `session.set_option("twitch-api-header", ...)` with MagicMock(spec=Streamlink) drift-guard; module docstring + 3 tests + 6 source references all updated
- [ ] tests/test_station_list_panel.py L326/L332 isVisibleTo deletions + L517 `min(5, ...)` assertion landed
- [ ] All 3 affected test files run green
- [ ] Zero production code change (musicstreamer/ byte-identical)
- [ ] Zero new dependencies
</success_criteria>

<output>
Create `.planning/phases/77-test-infrastructure-stabilization-fix-pre-existing-test-doub/77-04-SUMMARY.md`. Record: count of test functions deleted (must be 2 — both _aa_quality orphans), count of test functions renamed (must be 1 — `set_plugin_option`→`set_option` Twitch test), count of source `set_plugin_option` references replaced (must be 6), count of `isVisibleTo` lines removed (must be 2), confirmation that `git diff musicstreamer/` returns empty, and the streamlink probe baseline (`hasattr(Streamlink(), 'set_plugin_option') == False, hasattr(Streamlink(), 'set_option') == True`).
</output>
