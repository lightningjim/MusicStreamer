---
phase: 66
plan: 04
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
  - 66-03
files_modified:
  - musicstreamer/ui_qt/main_window.py
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - THEME-01
nyquist_compliant: true
tags:
  - pyside6
  - menu
  - integration
  - uat

must_haves:
  truths:
    - "Hamburger menu shows new 'Theme' action immediately ABOVE existing 'Accent Color' action (UI-SPEC anchor: main_window.py:188)"
    - "Clicking 'Theme' opens ThemePickerDialog as a modal child of MainWindow"
    - "After Apply on Theme picker, the chosen theme palette is in effect across all panels (station list, now playing, hamburger menu) and survives app restart (theme setting persists in SQLite)"
    - "Layered Highlight contract preserved: setting accent_color = #ff77ff THEN switching theme to Overrun keeps Highlight = #ff77ff (NOT Overrun's #ff2dd1 baseline)"
    - "Custom theme persists across restart: Save Custom palette → quit app → relaunch → theme='custom' active and Custom palette renders identically"
    - "Settings export/import ZIP round-trip carries theme + theme_custom keys (existing settings_export.py covers all string-keyed settings additively — no Plan 04 change needed)"
    - "ROADMAP.md Phase 66 entry has concrete Goal text (replacing the [To be planned] placeholder), updated plans count = 4, and the 4 plan checklist filled with brief objectives"
  artifacts:
    - path: "musicstreamer/ui_qt/main_window.py"
      provides: "Theme hamburger menu action + _open_theme_dialog slot (lazy import)"
      contains: "act_theme = self._menu.addAction(\"Theme\")"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 66 entry with concrete Goal + Requirements: THEME-01 + Plans: 4 plans + 4-row plan checklist"
      contains: "**Plans:** 4 plans"
  key_links:
    - from: "musicstreamer/ui_qt/main_window.py:_open_theme_dialog"
      to: "musicstreamer/ui_qt/theme_picker_dialog.py:ThemePickerDialog"
      via: "lazy import + .exec() with parent=self"
      pattern: "from musicstreamer\\.ui_qt\\.theme_picker_dialog import ThemePickerDialog"
    - from: ".planning/ROADMAP.md §Phase 66"
      to: ".planning/REQUIREMENTS.md THEME-01"
      via: "Requirements field references THEME-01"
      pattern: "THEME-01"

threat_model:
  trust_boundaries:
    - "user → hamburger menu (action click)"
  threats:
    - id: "T-66-13"
      category: "Information Disclosure"
      component: "Theme menu action ordering near Accounts (which contains credentials surface)"
      disposition: "accept"
      mitigation: "Theme action sits in Settings group ABOVE Accent Color (peer entries) — same group as Accounts (which Phase 53 already secured). No new credential surface introduced; theme is appearance-only. Misclick on Theme cannot leak any data — picker shows tile palettes only."
---

<objective>
Wire the hamburger menu "Theme" action to ThemePickerDialog (Plan 02), update ROADMAP.md Phase 66 from placeholder to a fully-fleshed entry, and run a manual UAT checkpoint covering: visual mood validation, Layered Highlight contract preservation, Custom theme persistence across restart, and Settings export/import round-trip.

Purpose: ship the user-visible affordance and validate end-to-end that Plans 01-03 integrate correctly with the running app on Linux Wayland (DPR=1.0).

Output: 2-line addition to main_window.py menu construction + new `_open_theme_dialog` slot + ROADMAP.md Phase 66 update + manual UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-CONTEXT.md
@.planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-PATTERNS.md
@.planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-UI-SPEC.md
@.planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-VALIDATION.md
@musicstreamer/ui_qt/main_window.py
@musicstreamer/ui_qt/theme_picker_dialog.py
@musicstreamer/ui_qt/theme_editor_dialog.py
@musicstreamer/theme.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Theme" hamburger menu action + _open_theme_dialog slot</name>

  <read_first>
    - musicstreamer/ui_qt/main_window.py lines 163-200 (hamburger menu construction; current state — Theme action is INSERTED at line 188 above existing act_accent)
    - musicstreamer/ui_qt/main_window.py lines 776-797 (existing dialog-opener slots — pattern for `_open_theme_dialog`; `_open_equalizer_dialog` at line 793 uses lazy import — Plan 04 mirrors that)
    - musicstreamer/ui_qt/main_window.py lines 241-245 (existing accent_color restore — UNCHANGED; this layered Highlight invariant is what UAT validates)
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-PATTERNS.md §"musicstreamer/ui_qt/main_window.py" lines 600-650 — exact insertion site + slot
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-CONTEXT.md D-15 (anchor location)
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-UI-SPEC.md §"Copywriting Contract" line 199 (locked label "Theme" — Title Case, no ellipsis)
  </read_first>

  <files>musicstreamer/ui_qt/main_window.py</files>

  <behavior>
    Two atomic edits to `main_window.py`:
    1. Menu construction: insert two lines (`act_theme = self._menu.addAction("Theme")` + `act_theme.triggered.connect(self._open_theme_dialog)`) IMMEDIATELY ABOVE the existing `act_accent = self._menu.addAction("Accent Color")` at line 188 (CONTEXT.md D-15)
    2. Slot definition: add a new `_open_theme_dialog` method ABOVE the existing `_open_accent_dialog` slot at line 776, using the lazy-import pattern from `_open_equalizer_dialog` (line 793)

    DO NOT touch the existing `act_accent`, `_open_accent_dialog`, accent_color startup restore (line 241-245), or the existing import block.
  </behavior>

  <action>
    **Edit A — Menu construction (insert at line 188, BEFORE existing act_accent).**

    Find this block in `musicstreamer/ui_qt/main_window.py` (lines 185-189):
    ```python
            self._menu.addSeparator()

            # Group 2: Settings dialogs (D-16, D-17, D-18)
            act_accent = self._menu.addAction("Accent Color")
            act_accent.triggered.connect(self._open_accent_dialog)
    ```

    Replace with (insert two new lines after the comment, before act_accent):
    ```python
            self._menu.addSeparator()

            # Group 2: Settings dialogs (D-16, D-17, D-18)
            # Phase 66 D-15 / THEME-01: Theme picker — peer above Accent Color.
            act_theme = self._menu.addAction("Theme")
            act_theme.triggered.connect(self._open_theme_dialog)  # QA-05 bound method

            act_accent = self._menu.addAction("Accent Color")
            act_accent.triggered.connect(self._open_accent_dialog)
    ```

    **Edit B — Slot definition (insert ABOVE existing `_open_accent_dialog` at line 776).**

    Find this block at lines 776-779:
    ```python
        def _open_accent_dialog(self) -> None:
            """D-16: Open AccentColorDialog from hamburger menu."""
            dlg = AccentColorDialog(self._repo, parent=self)
            dlg.exec()
    ```

    Insert above it:
    ```python
        def _open_theme_dialog(self) -> None:
            """Phase 66 D-15 / THEME-01: Open ThemePickerDialog from hamburger menu.

            Lazy import matches _open_equalizer_dialog precedent (line 793).
            The dialog handles its own snapshot/restore on Cancel; Apply persists
            `theme` setting. The existing accent_color restore at line 241-245
            continues to layer on top (Phase 59 D-02 contract preserved).
            """
            from musicstreamer.ui_qt.theme_picker_dialog import ThemePickerDialog
            dlg = ThemePickerDialog(self._repo, parent=self)
            dlg.exec()

        def _open_accent_dialog(self) -> None:
            """D-16: Open AccentColorDialog from hamburger menu."""
            dlg = AccentColorDialog(self._repo, parent=self)
            dlg.exec()
    ```

    DO NOT add `from musicstreamer.ui_qt.theme_picker_dialog import ThemePickerDialog` at the module top (line 53 area where `AccentColorDialog` is imported). The lazy-import inside the slot keeps `main_window.py` import graph small (matches `_open_equalizer_dialog` precedent — newer convention).

    DO NOT modify lines 241-245 (the existing accent_color startup restore). Phase 59 D-02 contract preserved.
  </action>

  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -c "import ast; ast.parse(open('musicstreamer/ui_qt/main_window.py').read()); print('OK')"</automated>

    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -c 'addAction("Theme")' musicstreamer/ui_qt/main_window.py</automated>

    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -c 'def _open_theme_dialog' musicstreamer/ui_qt/main_window.py</automated>

    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && pytest tests/ -x -q 2>&1 | tail -15</automated>

    Expected:
    - AST parse outputs `OK`
    - `addAction("Theme")` count = 1
    - `_open_theme_dialog` definition count = 1
    - Full pytest suite green (Plan 01 + Plan 02 + Plan 03 + all pre-existing tests pass; no regressions)
  </verify>

  <done>
    - `musicstreamer/ui_qt/main_window.py` parses as valid Python
    - `grep -c 'addAction("Theme")' musicstreamer/ui_qt/main_window.py` returns `1`
    - `grep -c 'def _open_theme_dialog' musicstreamer/ui_qt/main_window.py` returns `1`
    - The new `act_theme = self._menu.addAction("Theme")` line is positioned IMMEDIATELY ABOVE `act_accent = self._menu.addAction("Accent Color")` (verified by line-number grep: `grep -n 'addAction("Theme")' main_window.py` < `grep -n 'addAction("Accent Color")' main_window.py`)
    - `from musicstreamer.ui_qt.theme_picker_dialog` appears INSIDE `_open_theme_dialog` (lazy import) and NOT at module top
    - Full test suite green: `pytest tests/ -x -q` passes
    - Existing accent_color restore at line 241-245 is unchanged (verify by `grep -n 'accent_color' musicstreamer/ui_qt/main_window.py` shows the original line content unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 66 entry</name>

  <read_first>
    - .planning/ROADMAP.md lines 509-528 (Phase 65 — closest fully-fleshed analog block)
    - .planning/ROADMAP.md lines 529-537 (Phase 66 — current placeholder block to replace)
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-PATTERNS.md §".planning/ROADMAP.md" lines 681-708 — exact format
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-CONTEXT.md (entire file — Goal text drawn from "user-visible promise" in §specifics line 207)
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <behavior>
    Replace the Phase 66 placeholder block with concrete content. Match Phase 65's voice and field order verbatim. Keep `Plans:` checklist format consistent with other completed phases.
  </behavior>

  <action>
    Find this block in `.planning/ROADMAP.md` (lines 529-537):

    ```markdown
    ### Phase 66: Color Themes — preset and custom color schemes (Vaporwave pastel, Overrun neon+black)

    **Goal:** [To be planned]
    **Requirements**: TBD
    **Depends on:** Phase 65
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 66 to break down)
    ```

    Replace with:

    ```markdown
    ### Phase 66: Color Themes — preset and custom color schemes (Vaporwave pastel, Overrun neon+black)

    **Goal:** Add a "Theme" entry to the hamburger menu (immediately above "Accent Color") that opens a modal picker with 8 tiles: System default, Vaporwave (light pastel), Overrun (dark neon), GBS.FM (light, brand-sampled), GBS.FM After Dark (dark, brand-derived), Dark, Light, and a single user-editable Custom slot. Picking a theme retints the entire app palette (9 primary QPalette roles); the existing `accent_color` setting (Phase 59) continues to override Highlight on top whenever non-empty. Custom palette is duplicate-and-edit only via a "Customize…" button on the picker, persisted as JSON in a new `theme_custom` SQLite key. No new dependencies; pure PySide6 QPalette + QColorDialog. The hardcoded QSS tokens (`ERROR_COLOR_HEX`, `WARNING_COLOR_HEX`, `STATION_ICON_SIZE`) survive across all themes — they are theme-independent UI tokens.
    **Requirements**: THEME-01
    **Depends on:** Phase 65
    **Plans:** 4 plans

    Plans:
    - [ ] 66-01-PLAN.md — `theme.py` core + startup wire (apply_theme_palette before MainWindow) + THEME-01 in REQUIREMENTS.md
    - [ ] 66-02-PLAN.md — `theme_picker_dialog.py` (4×2 tile grid, Customize…/Apply/Cancel, _save_committed flag for editor stacking)
    - [ ] 66-03-PLAN.md — `theme_editor_dialog.py` (9-row Custom palette editor, per-row QColorDialog, Save/Reset/Cancel)
    - [ ] 66-04-PLAN.md — Hamburger menu wire (above Accent Color) + ROADMAP update + manual UAT (mood validation, layered Highlight, Custom persistence, export/import round-trip)
    ```

    DO NOT touch any other lines (Phase 65 above, Phase 67 below, or any other section).
  </action>

  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -c '\*\*Plans:\*\* 4 plans' .planning/ROADMAP.md</automated>

    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -c '66-01-PLAN.md' .planning/ROADMAP.md && grep -c '66-02-PLAN.md' .planning/ROADMAP.md && grep -c '66-03-PLAN.md' .planning/ROADMAP.md && grep -c '66-04-PLAN.md' .planning/ROADMAP.md</automated>

    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -A2 '### Phase 66' .planning/ROADMAP.md | grep -c 'To be planned'</automated>

    Expected:
    - `**Plans:** 4 plans` count = 1
    - Each plan filename appears at least once
    - `[To be planned]` count = 0 (placeholder fully replaced)
  </verify>

  <done>
    - ROADMAP.md Phase 66 block has concrete Goal text (no `[To be planned]`)
    - `**Requirements**: THEME-01` line exists
    - `**Plans:** 4 plans` line exists
    - All 4 plan checkboxes exist with one-sentence brief objectives
    - Phase 65 (above) and Phase 67 (below) blocks unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): UAT — End-to-end Theme integration</name>
  <files>—</files>

  <read_first>
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-VALIDATION.md §"Manual-Only Verifications" — the 6-row table this UAT executes
    - .planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-CONTEXT.md D-02 + D-22 + D-23 — layered Highlight contract + persistence shape
    - .planning/phases/59-visual-accent-color-picker/59-CONTEXT.md (entire) — accent contract that must NOT regress under theme switch
    - musicstreamer/ui_qt/main_window.py (after Task 1) — confirm Theme action wires to ThemePickerDialog
  </read_first>

  <what-built>
    Full Phase 66 stack is now live: `theme.py` core, ThemePickerDialog, ThemeEditorDialog, hamburger menu wire-up, ROADMAP updated, REQUIREMENTS updated. The app boots with `theme='system'` by default (visually identical to pre-Phase-66) and the user can now switch themes from the hamburger menu.

    This UAT validates 6 dimensions on Linux Wayland (DPR=1.0):
    1. Visual mood — does each preset feel right?
    2. WCAG contrast in practice — are all 6 presets readable?
    3. Live preview perceptual smoothness — no flicker?
    4. Layered Highlight contract — does accent_color survive theme switch?
    5. Custom theme persistence across restart
    6. Settings export/import round-trip carries Custom

    Every UAT step explicitly references the underlying contract being validated.
  </what-built>

  <action>
    Start with a clean app launch:
    ```bash
    cd /home/kcreasey/OneDrive/Projects/MusicStreamer
    python -m musicstreamer
    ```

    **UAT Check 1 — Theme menu action exists:**
    1. Open hamburger menu (≡ icon, top-left).
    2. Verify "Theme" action is visible IMMEDIATELY ABOVE "Accent Color".
    3. Verify "Theme" has no ellipsis (it opens a dialog directly — UI-SPEC §Copywriting line 199).

    **UAT Check 2 — Visual mood matches branding (THEME-01 / VALIDATION.md §Manual-Only):**
    1. Click "Theme" → picker opens with 8 tiles in 4×2 grid (System default, Vaporwave, Overrun, GBS.FM, GBS.FM After Dark, Dark, Light, Custom).
    2. Verify Custom tile is disabled with italic "Click Customize…" hint inside (no stripe).
    3. Click each enabled tile in sequence. Confirm:
       - **Vaporwave** feels lavender / pink / soft pastel
       - **Overrun** feels dark neon / hot magenta / electric cyan
       - **GBS.FM** matches the brand site (sage Window, kelly green Highlight, mint Base — visually identical to https://gbs.fm)
       - **GBS.FM After Dark** feels like GBS.FM transposed to a dark surface (mint text on near-black, brand kelly Highlight)
       - **Dark** feels neutral utility — gray Window, neutral blue Highlight (no theme-defined highlight)
       - **Light** feels neutral utility — light gray Window, neutral blue Highlight
    4. Click "Cancel" — confirm the picker closes and the original palette is restored.

    **UAT Check 3 — Apply persists theme (THEME-01):**
    1. Open Theme picker → click "Vaporwave" → click "Apply".
    2. Confirm the dialog closes and the entire app (station list, now playing panel, hamburger menu) is now Vaporwave.
    3. Quit app (`Ctrl+Q` or X).
    4. Relaunch (`python -m musicstreamer`).
    5. Verify Vaporwave is STILL the active theme (theme setting persisted).

    **UAT Check 4 — Layered Highlight contract (THEME-01 + ACCENT-02 — UI-SPEC §State Machine + RESEARCH Pitfall 2):**
    1. With Vaporwave active, open hamburger → Accent Color → set accent to bright red `#ff0000`. Apply.
    2. Verify station-list selection rectangle is now RED (accent override).
    3. Open Theme picker → click "Overrun" → click "Apply". Picker closes.
    4. Verify Highlight (selection rectangle, default-button border) is STILL RED `#ff0000` — NOT Overrun's pink magenta `#ff2dd1`. **This is the load-bearing contract.**
    5. Open Accent Color → click "Reset". Confirm accent cleared.
    6. Verify Highlight reverts to Overrun's `#ff2dd1` magenta baseline (theme's own Highlight value).

    **UAT Check 5 — Custom theme creation + persistence:**
    1. Open Theme picker → click "Vaporwave" (so Customize will use Vaporwave as source). Then click "Customize…".
    2. Editor opens titled "Customize Theme" with 9 single-column rows: Window background, Window text, Base, Alternating row, Body text, Button background, Button text, Selected text, Hyperlink. (NO Highlight row — owned by accent path per D-08.)
    3. Click the swatch for "Window background" → QColorDialog opens with title "Choose Window background color". Pick a distinctive color (e.g., `#005f5f` teal). Click OK.
    4. Verify the editor dialog itself (and the main window behind it) retints to teal Window. The hex display label updates to `#005f5f`.
    5. Click "Reset". Verify the row reverts to Vaporwave's original `#efe5ff` and the dialog stays open.
    6. Re-pick the teal `#005f5f` for Window. Click "Save".
    7. Editor closes. Picker is still visible — Custom tile is now ENABLED with a 3-color stripe (showing teal Window, the rest from Vaporwave). Custom tile is the active tile.
    8. Click "Apply" on the picker (or Cancel — `_save_committed` flag should preserve the saved palette either way per UI-SPEC Pitfall 1).
    9. Quit and relaunch app.
    10. Verify Custom is the active theme on relaunch — Window is teal, other roles inherit Vaporwave (per the saved JSON).

    **UAT Check 6 — Settings export/import round-trip carries Custom (VALIDATION.md §Manual-Only):**
    1. With Custom theme active (from Check 5), open hamburger → Export Settings. Save the ZIP somewhere (e.g., `/tmp/ms-export.zip`).
    2. Open the data DB directly (verify only):
       ```bash
       sqlite3 ~/.local/share/musicstreamer/musicstreamer.db "SELECT key, value FROM settings WHERE key IN ('theme', 'theme_custom');"
       ```
       Confirm `theme=custom` and `theme_custom={...JSON with Window: #005f5f...}`.
    3. Wipe the keys to simulate a fresh import:
       ```bash
       sqlite3 ~/.local/share/musicstreamer/musicstreamer.db "DELETE FROM settings WHERE key IN ('theme', 'theme_custom');"
       ```
    4. Restart app. Verify it now boots with System default theme (no theme setting).
    5. Open hamburger → Import Settings → pick the exported ZIP → confirm import.
    6. Restart app. Verify Custom theme is restored — Window is teal again. **This proves theme + theme_custom flow through the existing settings_export.py round-trip without any Plan 04 change** (additive SQLite keys).

    **UAT Check 7 — Defensive fallback (RESEARCH Pitfall 4 — corrupt JSON):**
    1. Quit app.
    2. Corrupt the theme_custom value:
       ```bash
       sqlite3 ~/.local/share/musicstreamer/musicstreamer.db "UPDATE settings SET value='{not valid json' WHERE key='theme_custom';"
       ```
    3. Relaunch app. Verify it boots with NO crash, NO toast, NO migration prompt — silently falls back to System default visual (RESEARCH Q12 + Pitfall 4 + UI-SPEC §Copywriting "Error toast for corrupt theme_custom JSON: none — silent fallback").
    4. Open Theme picker. Verify Custom tile is now DISABLED (corrupt JSON → tile disabled per Plan 02 `_refresh_custom_tile_enabled`).
    5. Click Cancel.
    6. Restore a valid theme_custom (or click Customize and Save again) to clean up.

    **If any check fails, describe the issue. Reference the underlying contract (D-XX, RESEARCH Q#, UI-SPEC section) so the planner can target gap closure if needed.**
  </action>

  <verify>
    Manual UAT — human runs `python -m musicstreamer` and walks the 7 checks. Per-check automated artifacts:
    - Check 1 menu: `grep -c 'addAction("Theme")' musicstreamer/ui_qt/main_window.py` outputs `1` (Task 1 already verified)
    - Checks 4-5 contract: `pytest tests/test_theme.py tests/test_theme_picker_dialog.py tests/test_theme_editor_dialog.py tests/test_accent_color_dialog.py tests/test_accent_provider.py -x -q` passes (full Phase 66 + Phase 59 regression)
    - Checks 6-7 are pure manual (export ZIP round-trip + corrupt-JSON fallback) — no automated equivalent
  </verify>

  <done>
    All 7 UAT checks pass; user types "approved" or "approved with notes". Per check (1-7), document outcome in 66-04-SUMMARY.md. Any failing check is logged with the underlying contract reference (D-XX / RESEARCH Q# / UI-SPEC section) and routed to /gsd-plan-phase 66 --gaps for closure planning.
  </done>

  <resume-signal>Type "approved" if all 7 UAT checks pass, or describe failing checks with contract references.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → hamburger menu (action click) | Theme action is appearance-only; no credential surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-13 | Information Disclosure | Theme action sits adjacent to Accounts entry in Settings group | accept | Theme picker has zero credential surface; misclick on Theme cannot leak data — picker shows tile palettes only. Group placement (above Accent Color) follows existing pattern (Accent Color is also peer to Accounts in Phase 59 / `main_window.py:188-192`); no new boundary introduced. |
</threat_model>

<verification>
- `python -m musicstreamer` launches without errors
- Hamburger menu shows "Theme" above "Accent Color" (Task 1 verify grep gates)
- All Phase 66 + pre-existing test suites green: `pytest tests/ -x -q`
- ROADMAP.md Phase 66 has concrete Goal + 4 plans listed (Task 2 verify grep gates)
- Manual UAT 7 checks all pass (Task 3 checkpoint sign-off)
</verification>

<success_criteria>
- THEME-01 fully shipped: hamburger menu → Theme action → tile-grid picker → tile click = live preview → Apply persists → restart preserves
- Layered Highlight contract verified end-to-end on Linux Wayland (UAT Check 4)
- Custom theme creation flow works: Customize… opens editor → 9 rows → swatch click → QColorDialog → Save → picker shows Custom enabled → restart preserves Custom (UAT Check 5)
- Settings export/import ZIP carries theme + theme_custom keys without any Plan 04 code change (UAT Check 6)
- Corrupt theme_custom JSON does NOT crash app and Custom tile is auto-disabled (UAT Check 7)
- ROADMAP.md placeholder fully replaced with concrete Phase 66 metadata
- Phase 59 ACCENT-02 contract verified intact via UAT Check 4 step 4 (red accent survives Overrun theme switch)
</success_criteria>

<output>
After completion, create `.planning/phases/66-color-themes-preset-and-custom-color-schemes-vaporwave-paste/66-04-SUMMARY.md` documenting UAT outcomes per check (1-7), any issues observed, and any deviations from the plan.
</output>
