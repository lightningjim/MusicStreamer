---
phase: 80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe
plan: 03
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - tests/test_db_fk_invariants.py
autonomous: true
requirements: [BUG-10]
requirements_addressed: [BUG-10]

must_haves:
  truths:
    - "A regression test opens a connection through the production factory, inserts a station + its streams, DELETEs the station, and asserts zero child rows remain."
    - "Two symmetric tests cover `station_siblings` cascade — one deletes the `a_id` station, one deletes the `b_id` station; both assert the join row is gone."
    - "A negative-proof test opens raw `sqlite3.connect(\":memory:\")` WITHOUT setting the PRAGMA and proves the cascade does NOT fire (orphan child row survives)."
    - "A sweep test manufactures an orphan via the OFF→DELETE→ON sequence, calls `sweep_orphans(con)`, and asserts the orphan is gone."
    - "Two drift-guard tests cover the WARN-on-OFF behavior and the once-per-session throttle, using `_reset_pragma_drift_sentinel_for_tests()` for isolation."
    - "DB filename is `musicstreamer.sqlite3`; tests use `:memory:` or temp-file dbs (never the real user DB)."
    - "D-13: positive cascade test for `station_streams` exists, exercising the production `db_connect()` end-to-end (not an open-coded fixture)."
    - "D-14: two symmetric tests cover `station_siblings` cascade — one per FK column (`a_id` and `b_id` directions)."
    - "D-15: negative-proof test opens raw `sqlite3.connect(\":memory:\")` WITHOUT setting the PRAGMA and asserts the child row SURVIVES the parent DELETE — names the cause-effect explicitly in the test name (`test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing`)."
    - "D-16: one `sweep_orphans` happy-path test manufactures an orphan via OFF→DELETE→ON sequence, calls `sweep_orphans(con)`, and asserts the orphan is gone."
  artifacts:
    - path: tests/test_db_fk_invariants.py
      provides: "7 unit tests covering D-13/D-14/D-15/D-16 + drift-guard log + sentinel-throttle; per-test autouse fixture resets _pragma_drift_logged"
      contains: "def test_delete_station_cascades_station_streams"
    - path: tests/test_db_fk_invariants.py
      provides: "Negative-proof test naming the cause-effect explicitly"
      contains: "def test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing"
    - path: tests/test_db_fk_invariants.py
      provides: "sweep_orphans happy-path test"
      contains: "def test_sweep_orphans_removes_orphan_streams_and_siblings"
  key_links:
    - from: "tests/test_db_fk_invariants.py fixtures"
      to: "musicstreamer.repo.db_connect via paths.db_path monkeypatch"
      via: "Pitfall 2 — patch BEFORE calling db_connect()"
      pattern: "monkeypatch.setattr.*paths"
    - from: "tests/test_db_fk_invariants.py per-test autouse fixture"
      to: "musicstreamer.repo._reset_pragma_drift_sentinel_for_tests"
      via: "function-scoped autouse fixture calling the reset helper"
      pattern: "_reset_pragma_drift_sentinel_for_tests"
---

<objective>
Create `tests/test_db_fk_invariants.py` — a new test file with seven unit tests + a Strategy-A fixture that routes through `musicstreamer.repo.db_connect()` (via `paths.db_path` monkeypatch) so the tests exercise the production factory function end-to-end (Pitfall 1 — fixtures that open-code the connection don't catch drift in `db_connect()` itself).

Seven tests:
1. `test_delete_station_cascades_station_streams` — D-13 positive cascade.
2. `test_delete_station_cascades_station_siblings_a_id` — D-14 symmetry coverage.
3. `test_delete_station_cascades_station_siblings_b_id` — D-14 symmetry coverage.
4. `test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing` — D-15 negative proof (uses raw `sqlite3.connect(":memory:")`, Strategy B; the verbose test name IS the documentation).
5. `test_sweep_orphans_removes_orphan_streams_and_siblings` — D-16 sweep happy path (OFF→DELETE→ON manufacturing recipe).
6. `test_drift_guard_warns_when_pragma_reads_off` — covers D-10 (planner-discretion add; locks the WARN-on-OFF surface).
7. `test_drift_guard_logs_at_most_once_per_session` — covers D-11 (planner-discretion add; locks the sentinel throttle).

Plus a per-test autouse fixture that calls `_reset_pragma_drift_sentinel_for_tests()` so the sentinel is re-armed between tests (Pitfall 3).

Purpose: Lock the existing PRAGMA invariant + the new `sweep_orphans` + the new drift-guard into regression tests. Future drift in any direction (PRAGMA dropped, schema cascade flag flipped, sweep function broken, sentinel stops throttling) fails CI loudly.

Output: One new file `tests/test_db_fk_invariants.py` ships seven green tests; the existing `tests/test_repo.py::test_cascade_delete` and `tests/test_station_siblings.py::test_cascade_on_station_delete` are left in place (Open Questions 1 + 2 in RESEARCH — duplication is cheap; coverage is the goal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-01-SUMMARY.md
@musicstreamer/repo.py
@tests/test_repo.py
@tests/test_station_siblings.py
@tests/conftest.py

<interfaces>
<!-- Key existing test patterns extracted from current source. -->

From musicstreamer/repo.py (post-Plan-80-01 surface — verify Plan 80-01 SUMMARY landed these symbols):
- `db_connect() -> sqlite3.Connection` — opens connection on `paths.db_path()`, sets `PRAGMA foreign_keys = ON`, runs drift-guard read-back, returns con.
- `db_init(con: sqlite3.Connection)` — schema (CREATE TABLE IF NOT EXISTS ... with FK CASCADE clauses for `station_streams` and `station_siblings`).
- `sweep_orphans(con: sqlite3.Connection) -> None` — two parameterless DELETEs, conditional INFO log, `con.commit()`.
- `_reset_pragma_drift_sentinel_for_tests() -> None` — sets module-level `_pragma_drift_logged = False`.
- `_log = logging.getLogger(__name__)` — module logger named `musicstreamer.repo`.
- `_pragma_drift_logged: bool` — module-level throttle sentinel.

From tests/test_repo.py:7-13 (CAUTIONARY analog — Pitfall 1 says do NOT reuse this fixture; it bypasses db_connect):
```python
@pytest.fixture
def repo(tmp_path):
    con = sqlite3.connect(str(tmp_path / "test.db"))
    con.row_factory = sqlite3.Row
    con.execute("PRAGMA foreign_keys = ON;")
    db_init(con)
    return Repo(con)
```

From tests/test_repo.py:584-594 (POSITIVE analog — mirror this shape for D-13):
```python
def test_cascade_delete(repo):
    sid = repo.insert_station("Test FM", "http://test.fm/stream", "", "")
    streams_before = repo.list_streams(sid)
    assert len(streams_before) == 1
    repo.delete_station(sid)
    rows = repo.con.execute("SELECT * FROM station_streams WHERE station_id=?", (sid,)).fetchall()
    assert len(rows) == 0
```

From tests/test_station_siblings.py:102-113 (sibling cascade analog — mirror for D-14, both directions):
```python
def test_cascade_on_station_delete(repo):
    repo.con.execute("INSERT INTO stations(id, name, tags) VALUES (1, 'A', '')")
    repo.con.execute("INSERT INTO stations(id, name, tags) VALUES (2, 'B', '')")
    repo.con.execute("INSERT INTO station_siblings(a_id, b_id) VALUES (1, 2)")
    repo.con.commit()
    repo.con.execute("DELETE FROM stations WHERE id = 2")
    repo.con.commit()
    count = repo.con.execute("SELECT COUNT(*) AS c FROM station_siblings").fetchone()["c"]
    assert count == 0
```

Memory `reference_musicstreamer_db_schema.md`: DB filename is `musicstreamer.sqlite3` (NOT `library.db`); `stations.provider_id` FK to `providers.id`. Tests use `:memory:` or temp-file dbs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_db_fk_invariants.py with Strategy-A fixture + 7 tests covering D-13/D-14/D-15/D-16 + drift-guard log + throttle</name>
  <files>tests/test_db_fk_invariants.py</files>
  <read_first>
    - musicstreamer/repo.py (verify Plan 80-01 landed all five new symbols: `_log`, `_pragma_drift_logged`, `_reset_pragma_drift_sentinel_for_tests`, hardened `db_connect`, `sweep_orphans`)
    - tests/test_repo.py (lines 7-13 cautionary fixture; lines 584-594 positive-cascade analog; lines 620-624 `_make_bare_con` helper)
    - tests/test_station_siblings.py (lines 102-113 sibling cascade analog — `b_id` direction; this plan adds the `a_id` symmetry counterpart)
    - tests/conftest.py (autouse fixtures and Qt offscreen platform setup — verify nothing this new file does conflicts; the new tests do NOT need Qt — pure stdlib `sqlite3` + `logging` + pytest `caplog`)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md (Code Examples — "Negative PRAGMA-off test — target shape" at lines 491-524; Fixture strategy A vs B sections; Pitfalls 1, 2, 3, 4, 6 — all of which apply to this test file)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (Analogs 1-5 in the test_db_fk_invariants section + the Strategy-A fixture target shape at lines 270-294)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-VALIDATION.md (Per-Task Verification Map — the 7 test names map exactly to the entries in this validation table)
  </read_first>
  <behavior>
    Seven tests in `tests/test_db_fk_invariants.py`, all GREEN after this task lands:

    1. `test_delete_station_cascades_station_streams` (D-13): given a connection from `db_connect()` (Strategy A) with schema from `db_init()`, insert a station + its stream via `Repo.insert_station`, verify the stream row exists, call `Repo.delete_station(sid)`, assert `SELECT COUNT(*) FROM station_streams WHERE station_id=?` for that station is 0.

    2. `test_delete_station_cascades_station_siblings_a_id` (D-14a): insert stations `id=1, id=2`, insert sibling row `(a_id=1, b_id=2)`, DELETE station id=1 (the `a_id` direction), assert `SELECT COUNT(*) FROM station_siblings` is 0.

    3. `test_delete_station_cascades_station_siblings_b_id` (D-14b): same setup, DELETE station id=2 (the `b_id` direction), assert `SELECT COUNT(*) FROM station_siblings` is 0.

    4. `test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing` (D-15): open raw `sqlite3.connect(":memory:")` WITHOUT setting `PRAGMA foreign_keys`, executescript a minimal stations + station_streams schema with the CASCADE clause, insert station + child, DELETE parent, assert child SURVIVES (`COUNT(*) == 1`).

    5. `test_sweep_orphans_removes_orphan_streams_and_siblings` (D-16): use Strategy-A fixture, then `con.execute("PRAGMA foreign_keys = OFF")` mid-test, insert station + stream + sibling pair, DELETE station via raw SQL (cascade does NOT fire because PRAGMA OFF), re-enable PRAGMA, call `sweep_orphans(con)`, assert orphan stream AND orphan sibling rows are gone.

    6. `test_drift_guard_warns_when_pragma_reads_off` (D-10 closure): force the PRAGMA read-back to return `0` (recommended approach: monkeypatch `sqlite3.Connection.execute` for the `"PRAGMA foreign_keys"` query OR monkeypatch the entire `db_connect` body's read-back, OR — simplest — open a raw connection, set the sentinel back to False via the helper, then construct a fake execute path; pick whichever leaves Plan 80-01 unmodified). Use `caplog.set_level(logging.WARNING, logger="musicstreamer.repo")`. Call `db_connect()`. Assert exactly one log record with `record.levelno == logging.WARNING` and `"PRAGMA foreign_keys is OFF after SET" in record.message`.

    7. `test_drift_guard_logs_at_most_once_per_session` (D-11 closure): under the same forced-OFF condition as test 6, call `db_connect()` THREE times in a row. Assert exactly one matching WARN record in `caplog.records` (the sentinel throttle worked).

    Plus an autouse function-scoped fixture that calls `musicstreamer.repo._reset_pragma_drift_sentinel_for_tests()` BEFORE every test in this file (Pitfall 3 — prevents cross-test sentinel leakage).
  </behavior>
  <action>
    Create the new file `tests/test_db_fk_invariants.py`.

    File header: module docstring citing Phase 80 / BUG-10 D-13..D-16 + the two planner-discretion drift-guard tests; imports (`pytest`, `sqlite3`, `logging`, and from `musicstreamer.repo` import the surface from Plan 80-01: `db_connect`, `db_init`, `sweep_orphans`, `_reset_pragma_drift_sentinel_for_tests`, plus `Repo`). Also import `from musicstreamer import paths as _paths` (used by the monkeypatch in Strategy A).

    Define a per-test autouse fixture (function-scoped) that calls `_reset_pragma_drift_sentinel_for_tests()` so each test starts with the sentinel re-armed:
    - Decorator: `@pytest.fixture(autouse=True)`
    - Body: call the helper; `yield`; done.

    Define a `db_con` fixture (function-scoped, NOT module-scoped — each test gets a fresh DB):
    - Takes `monkeypatch` and `tmp_path` parameters.
    - Calls `monkeypatch.setattr(_paths, "db_path", lambda: str(tmp_path / "musicstreamer.sqlite3"))` BEFORE the first `db_connect()` call (Pitfall 2 — `paths.db_path()` is read at call time inside `db_connect()` body).
    - Calls `con = db_connect()` then `db_init(con)`.
    - Yields `con`.
    - On teardown calls `con.close()`.
    - File-level constant should declare the DB filename matches project convention (`musicstreamer.sqlite3` per memory `reference_musicstreamer_db_schema`); do NOT use `library.db` or `test.db`.

    Define a `repo` fixture that takes `db_con` and returns `Repo(db_con)` — convenience for the cascade tests that use `Repo.insert_station` / `Repo.delete_station`.

    Implement the seven tests per the `<behavior>` section above. Specific implementation notes:

    - Test 1 (D-13): mirror `tests/test_repo.py:584-594` exactly — `insert_station("Test FM", "http://test.fm/stream", "", "")`, verify `len(repo.list_streams(sid)) == 1`, `repo.delete_station(sid)`, assert `con.execute("SELECT COUNT(*) FROM station_streams WHERE station_id=?", (sid,)).fetchone()[0] == 0`. Use the `repo` fixture (built on Strategy-A `db_con`).

    - Tests 2, 3 (D-14a, D-14b): mirror `tests/test_station_siblings.py:102-113` — raw SQL inserts via `db_con.execute(...)` for stations id=1, id=2 and the sibling row `(a_id=1, b_id=2)` (respecting the `CHECK(a_id < b_id)` constraint). `db_con.commit()` after inserts. Then for test 2: `DELETE FROM stations WHERE id = 1`; for test 3: `DELETE FROM stations WHERE id = 2`. Commit. Assert `SELECT COUNT(*) FROM station_siblings` returns 0.

    - Test 4 (D-15): lift the verified shape from RESEARCH.md "Negative PRAGMA-off test — target shape" (lines 491-524). NO PRAGMA SET on the raw `:memory:` connection. `executescript` the minimal schema. Insert station + child. DELETE parent. Assert `SELECT COUNT(*) FROM station_streams` is 1 (child SURVIVES — cascade did NOT fire because PRAGMA OFF). Failure assertion message names the cause-effect explicitly so a future maintainer reading a CI failure understands the invariant.

    - Test 5 (D-16): use `db_con` (Strategy A, production factory path). Then `db_con.execute("PRAGMA foreign_keys = OFF")` mid-test. Insert one station via `db_con.execute("INSERT INTO stations(id, name, tags) VALUES (10, 'Orphaned', '')")`, insert one station_stream via `db_con.execute("INSERT INTO station_streams(station_id, url) VALUES (10, 'http://x.y/stream')")`, insert one second station id=20 and a sibling `(a_id=10, b_id=20)` (respect CHECK). Commit. Now `DELETE FROM stations WHERE id = 10` (cascade does NOT fire — PRAGMA is OFF). Commit. `db_con.execute("PRAGMA foreign_keys = ON")`. Call `sweep_orphans(db_con)`. Assert `SELECT COUNT(*) FROM station_streams WHERE station_id = 10` is 0 AND `SELECT COUNT(*) FROM station_siblings WHERE a_id = 10 OR b_id = 10` is 0.

    - Test 6 (D-10): the goal is to assert that the WARN line fires when the PRAGMA read-back returns 0. Simplest implementation: monkeypatch `sqlite3.Connection.execute` to return a fetchone-returning-0 result ONLY for the `"PRAGMA foreign_keys"` read (and pass-through for everything else). Alternative: monkeypatch `musicstreamer.repo.sqlite3.connect` to return a wrapped connection whose `execute("PRAGMA foreign_keys")` returns `[(0,)]`. Use `caplog.set_level(logging.WARNING, logger="musicstreamer.repo")`. Reset the sentinel inside the test body (the autouse fixture already did this, but be explicit if the test does any earlier `db_connect()` call). Call `db_connect()`. Inspect `caplog.records`: assert exactly one record where `record.name == "musicstreamer.repo"`, `record.levelno == logging.WARNING`, and `"PRAGMA foreign_keys is OFF after SET" in record.getMessage()`.

    - Test 7 (D-11): same setup as test 6 (forced PRAGMA-OFF read-back). After resetting the sentinel via the autouse fixture, call `db_connect()` three times in succession. Assert that `caplog.records` contains exactly one record matching the WARN signature — proving the sentinel throttled the second and third calls.

    Hard-coded WARN-message substring used in tests 6 and 7: `PRAGMA foreign_keys is OFF after SET` (no em-dash needed in the substring assert — partial match is sufficient and avoids encoding ambiguity).

    Verify after writing: `uv run pytest tests/test_db_fk_invariants.py -x` exits 0 and the eight test functions (one autouse fixture + seven tests) all show as collected and passed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db_fk_invariants.py -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_delete_station_cascades_station_streams -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_delete_station_cascades_station_siblings_a_id -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_delete_station_cascades_station_siblings_b_id -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_sweep_orphans_removes_orphan_streams_and_siblings -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_drift_guard_warns_when_pragma_reads_off -x</automated>
    <automated>uv run pytest tests/test_db_fk_invariants.py::test_drift_guard_logs_at_most_once_per_session -x</automated>
    <source-grep>grep -c "^def test_" tests/test_db_fk_invariants.py | grep -q "^7$"</source-grep>
    <source-grep>grep -c "_reset_pragma_drift_sentinel_for_tests" tests/test_db_fk_invariants.py | grep -q "^[1-9]"</source-grep>
  </verify>
  <acceptance_criteria>
    - `tests/test_db_fk_invariants.py` exists and contains exactly 7 functions starting with `def test_` (grep matches exactly 7).
    - All 7 named test commands above exit 0 individually (and the omnibus `uv run pytest tests/test_db_fk_invariants.py -x` exits 0).
    - `tests/test_db_fk_invariants.py` contains at least one reference to `_reset_pragma_drift_sentinel_for_tests` (the autouse fixture calls it).
    - The fixture `db_con` calls `monkeypatch.setattr(...)` against `paths.db_path` BEFORE the first `db_connect()` call (Pitfall 2 invariant) — verified by `grep -c "monkeypatch.setattr" tests/test_db_fk_invariants.py >= 1` and source-ordering: in the fixture body, the `setattr` line appears BEFORE the `db_connect()` line.
    - The negative-proof test `test_pragma_off_leaks_orphans_proving_pragma_is_load_bearing` opens a raw `sqlite3.connect(":memory:")` (D-12 allows this in tests/ — the gate in Plan 80-04 explicitly excludes `tests/`).
    - The two drift-guard tests (6 and 7) use `caplog` and assert on log records emitted by the `musicstreamer.repo` logger.
    - Pre-existing tests still pass: `uv run pytest tests/test_repo.py tests/test_station_siblings.py -x` exits 0 — the existing `test_cascade_delete` and `test_cascade_on_station_delete` are left in place (RESEARCH Open Questions 1 + 2 recommendation).
    - Wider regression net: `uv run pytest tests/test_db_fk_invariants.py tests/test_repo.py tests/test_station_siblings.py -x` exits 0.
  </acceptance_criteria>
  <done>
    `tests/test_db_fk_invariants.py` exists with 7 green tests + per-test autouse sentinel-reset fixture; covers D-13/D-14/D-15/D-16 and the two planner-discretion drift-guard tests; pre-existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process file system | Tests use `tmp_path` (pytest-managed; isolated per test) and `:memory:` SQLite — never touch the user's real DB. |
| Per-test sentinel state | The module-level `_pragma_drift_logged` boolean is process-scoped; the autouse fixture's `_reset_pragma_drift_sentinel_for_tests()` call is the only mitigation against cross-test bleed (Pitfall 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-08 | Tampering | `paths.db_path` monkeypatch ordering | mitigate | Fixture sets `monkeypatch.setattr` BEFORE the first `db_connect()` call (Pitfall 2). Acceptance criteria asserts source-ordering of the two lines inside the fixture. |
| T-80-09 | Tampering | Module-level sentinel cross-test leak | mitigate | Autouse function-scoped fixture calls `_reset_pragma_drift_sentinel_for_tests()` before each test (Pitfall 3). |
| T-80-10 | Information disclosure | Test touches real user DB | mitigate | All fixtures use `tmp_path` (pytest-managed) or `:memory:` connections. Never references `paths.db_path()` directly without monkeypatch in scope. |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs zero new packages. All test infrastructure (`pytest`, `caplog`) is pre-existing. |
</threat_model>

<verification>
- `uv run pytest tests/test_db_fk_invariants.py -x` exits 0.
- `uv run pytest tests/test_db_fk_invariants.py tests/test_repo.py tests/test_station_siblings.py -x` exits 0 (wider regression net).
- No test in this new file references `~/.local/share/musicstreamer` or any production-path component — `grep -c "share/musicstreamer\\|local/share" tests/test_db_fk_invariants.py` outputs `0`.
- The 7 test names exactly match the entries in 80-VALIDATION.md's Per-Task Verification Map.
</verification>

<success_criteria>
- All 7 tests green individually and as a file.
- Cross-test isolation works (running the file twice in the same session still passes).
- Sentinel-reset autouse fixture is wired and active.
- Strategy-A fixture (production factory path) used for tests 1, 2, 3, 5, 6, 7; Strategy-B (raw `:memory:` no PRAGMA) used for test 4 only.
- No regression in pre-existing test files.
</success_criteria>

<output>
Create `.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-03-SUMMARY.md` when done.
</output>
