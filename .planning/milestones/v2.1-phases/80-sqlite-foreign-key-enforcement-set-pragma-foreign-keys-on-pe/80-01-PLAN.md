---
phase: 80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/repo.py
autonomous: true
requirements: [BUG-10]
requirements_addressed: [BUG-10]

must_haves:
  truths:
    - "`con` returned by `db_connect()` is a connection on which `PRAGMA foreign_keys` reads back as ON."
    - "A drift-guard log line at WARN emits at most once per session when `PRAGMA foreign_keys` reads OFF after the SET in `db_connect()`."
    - "`db_connect()` has a function-level docstring stating that the PRAGMA is load-bearing and that anyone needing a SQLite connection MUST go through this function."
    - "Source of truth for FK cascade clauses is `musicstreamer/repo.py::db_init` (D-05) — unchanged by this plan."
    - "Throttle sentinel is module-level `_pragma_drift_logged: bool` in `musicstreamer/repo.py`; test isolation goes through `_reset_pragma_drift_sentinel_for_tests()`."
    - "D-01: `sweep_orphans` deletes orphan rows and emits a single INFO log line only when at least one rowcount > 0 (silent when both are 0)."
    - "D-02: `sweep_orphans(con)` is a separate function from `db_init(con)`, living adjacent to it in `musicstreamer/repo.py`."
    - "D-04: N=0 case is silent — no log line emitted when both table rowcounts are 0."
    - "D-05: only the two real FK-cascade child tables (`station_streams`, `station_siblings`) are swept; matches the existing `db_init()` schema CASCADE clauses."
    - "D-06: `favorites` is NOT swept (no FK; v1.3 FAVES design persists track titles after station delete) — `sweep_orphans` MUST NOT contain `DELETE FROM favorites`."
    - "D-07: `station_siblings` is swept via a single DELETE statement handling both FK columns (`WHERE a_id NOT IN ... OR b_id NOT IN ...`) — one atomic statement, one rowcount, one log entry."
    - "D-08: `stations.provider_id` orphans (ON DELETE SET NULL, not CASCADE) are NOT swept — out of scope by design."
    - "D-09: defense in depth — runtime drift-guard (this plan's `db_connect()` read-back) and source-grep gate (Plan 80-04) are complementary."
    - "D-10: drift-guard log level is WARN (not INFO, not ERROR) when PRAGMA reads OFF after the SET."
    - "D-11: drift-guard is throttled to at most one WARN per session via the module-level `_pragma_drift_logged: bool` sentinel."
    - "D-17: docstring placement is function-level on `db_connect()` only; NO module-top docstring is added to `repo.py`."
  artifacts:
    - path: musicstreamer/repo.py
      provides: "Module logger `_log`, drift-guard sentinel + reset helper, hardened `db_connect()` with docstring + read-back, new `sweep_orphans(con)` function"
      contains: "_log = logging.getLogger"
    - path: musicstreamer/repo.py
      provides: "sweep_orphans function entry"
      contains: "def sweep_orphans("
    - path: musicstreamer/repo.py
      provides: "Test-only sentinel reset helper"
      contains: "def _reset_pragma_drift_sentinel_for_tests"
  key_links:
    - from: "musicstreamer/repo.py::db_connect"
      to: "module-level _pragma_drift_logged sentinel"
      via: "global declaration + read-and-set inside db_connect"
      pattern: "global _pragma_drift_logged"
    - from: "musicstreamer/repo.py::sweep_orphans"
      to: "stations / station_streams / station_siblings tables"
      via: "two parameterless DELETE ... WHERE NOT IN (SELECT id FROM stations) statements"
      pattern: "DELETE FROM station_streams"
---

<objective>
Land all `musicstreamer/repo.py` edits for Phase 80 / BUG-10 in a single atomic plan: introduce a module-level logger, add the once-per-session PRAGMA drift-guard sentinel + test-only reset helper, append a function-level docstring + read-back drift-guard to `db_connect()` (the existing `PRAGMA foreign_keys = ON;` line is already present at line 20 and MUST NOT be duplicated), and add a new `sweep_orphans(con)` function adjacent to `db_init()`.

Purpose: This is the "tightening" half of Phase 80. Every other plan in the phase (wiring in `__main__.py`, regression tests, source-grep gate) consumes the surface this plan creates. Logger, sentinel, helper, hardened factory, and sweep function must land together so Wave 2 has a stable target.

Output: `musicstreamer/repo.py` carries all five additions; existing `db_connect()` body and `db_init()` schema bodies remain functionally unchanged (no PRAGMA duplicated, no FK cascade clause edited).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md
@.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md
@musicstreamer/repo.py
@musicstreamer/player.py

<interfaces>
<!-- Key existing repo.py shape the executor needs. Extracted from current source. -->

From musicstreamer/repo.py (verified lines 1-21, current state — DO NOT add a duplicate PRAGMA):
```python
import sqlite3
from typing import Optional, List

from musicstreamer.models import Station, Provider, Favorite, StationStream
from musicstreamer import paths


VALID_COVER_ART_SOURCES: frozenset[str] = frozenset({"auto", "itunes_only", "mb_only"})


def db_connect() -> sqlite3.Connection:
    con = sqlite3.connect(paths.db_path())
    con.row_factory = sqlite3.Row
    con.execute("PRAGMA foreign_keys = ON;")
    return con
```

From musicstreamer/repo.py db_init body (verified — schema is the SOURCE OF TRUTH for FK cascades per D-05; this plan reads but does NOT edit):
- station_streams: `FOREIGN KEY(station_id) REFERENCES stations(id) ON DELETE CASCADE`
- station_siblings: `FOREIGN KEY(a_id) REFERENCES stations(id) ON DELETE CASCADE` AND `FOREIGN KEY(b_id) REFERENCES stations(id) ON DELETE CASCADE`

From musicstreamer/player.py:79-81 (Phase 62 logger-intro analog — mirror the comment style):
```python
# Phase 62 / BUG-09: module logger (first logger in player.py).
_log = logging.getLogger(__name__)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Introduce module logger + drift-guard sentinel + test-reset helper in repo.py</name>
  <files>musicstreamer/repo.py</files>
  <read_first>
    - musicstreamer/repo.py (entire file — verify current shape; especially lines 1-21 for the existing import block and `db_connect()` body, and the `db_init` schema body which MUST NOT be modified by this plan)
    - musicstreamer/player.py (lines 79-81 — the Phase 62 logger-introduction analog this task mirrors verbatim in shape and comment convention)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (Analog 1 — module-level `_log` introduction, Analog 4 — test-only helper convention)
  </read_first>
  <behavior>
    - After this task, `musicstreamer.repo` exposes `_log` as a `logging.Logger` instance whose name equals the module name (`musicstreamer.repo`).
    - After this task, `musicstreamer.repo._pragma_drift_logged` exists as a module-level `bool` initialized to `False`.
    - After this task, calling `musicstreamer.repo._reset_pragma_drift_sentinel_for_tests()` from any test sets `_pragma_drift_logged` back to `False` (uses `global` to rebind module-level state).
    - All other module behavior is unchanged: `db_connect()`, `db_init()`, `Repo.*` methods continue to behave identically. No PRAGMA edits, no schema edits in this task.
  </behavior>
  <action>
    Add `import logging` to the standard-library import block at the top of `musicstreamer/repo.py` (immediately above or below the existing `import sqlite3` on line 1 — either order is fine; pick the order that keeps stdlib imports contiguous and alphabetized). Do NOT add a module-top docstring (D-17 — function-level docstrings only is the project convention).

    After the `from musicstreamer import paths` line (currently line 5) and before the existing `VALID_COVER_ART_SOURCES` block (currently line 14), add the module-level logger declaration `_log = logging.getLogger(__name__)`. Place a one- or two-line comment above it in the exact Phase-X-rationale style established by `musicstreamer/player.py:79-81`: cite `Phase 80 / BUG-10`, note this is the first logger in `repo.py`, and note that it is surfaced at INFO via `__main__.py` per-logger `setLevel` (the per-logger escalation itself lands in Plan 80-02 — reference it forward).

    Immediately below the `_log` declaration, add the drift-guard throttle sentinel `_pragma_drift_logged: bool = False` with a comment citing Phase 80 / BUG-10 / D-11 and noting it is reset for tests via `_reset_pragma_drift_sentinel_for_tests()` (cross-reference Pitfall 3 from RESEARCH.md).

    Add the test-only reset helper `_reset_pragma_drift_sentinel_for_tests() -> None` adjacent to the sentinel declaration (or grouped with `db_connect()`; pick the placement that keeps the sentinel + helper visibly paired). Body: `global _pragma_drift_logged` then `_pragma_drift_logged = False`. Function-level docstring states this is a test-only helper that re-arms the WARN throttle, cites Pitfall 3, and explains the `_for_tests` suffix is intentional grep-discoverability convention. Return type is `None`.

    Do NOT modify `db_connect()` or `db_init()` bodies in this task — those edits land in Tasks 2 and 3 of this same plan. Do NOT add a module-top docstring (D-17).

    Run `uv run pytest tests/test_repo.py -x` after the edit to confirm no existing test regressed (the logger introduction must be transparent — no Repo method behavior changed).
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py tests/test_station_siblings.py -x</automated>
    <source-grep>grep -c "^_log = logging.getLogger(__name__)$" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "^_pragma_drift_logged: bool = False$" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "^def _reset_pragma_drift_sentinel_for_tests" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "^import logging$" musicstreamer/repo.py | grep -q "^1$"</source-grep>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/repo.py` contains the line `_log = logging.getLogger(__name__)` at module scope (grep matches exactly one occurrence).
    - `musicstreamer/repo.py` contains the line `_pragma_drift_logged: bool = False` at module scope (grep matches exactly one occurrence).
    - `musicstreamer/repo.py` contains a function `_reset_pragma_drift_sentinel_for_tests` (grep matches exactly one `def` for this name).
    - `musicstreamer/repo.py` contains `import logging` at module scope (exactly one occurrence).
    - `uv run pytest tests/test_repo.py tests/test_station_siblings.py -x` exits 0 — no regressions in pre-existing repo tests.
    - `musicstreamer/repo.py` does NOT contain a module-top docstring (D-17 — `head -n 1` is `import logging` or `import sqlite3`, NOT a triple-quoted string).
  </acceptance_criteria>
  <done>
    Module logger, drift-guard sentinel, and test-reset helper are all present at module scope of `musicstreamer/repo.py`; pre-existing tests still pass; `db_connect()` and `db_init()` bodies are untouched by this task.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Harden db_connect() with function docstring + post-SET drift-guard read-back</name>
  <files>musicstreamer/repo.py</files>
  <read_first>
    - musicstreamer/repo.py (current `db_connect()` body at lines 17-21 — confirm the existing `PRAGMA foreign_keys = ON;` line is present and MUST NOT be duplicated)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md (decisions D-09, D-10, D-11, D-17 — function-docstring placement; WARN level; once-per-session throttle)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md (Code Examples section, "`db_connect()` — target shape after Phase 80" — the verified target shape with docstring + global declaration + read-back block; Pitfall 1 — fixture-that-bypasses-factory; Pitfall 6 — PRAGMA returns int `0`/`1` not bool; Pitfall 7 — `sqlite3.Row` supports `__getitem__(int)`)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (Analog 2 — `db_connect()` shape; explicit instruction to keep all four existing lines and append)
  </read_first>
  <behavior>
    - `db_connect()` continues to return a `sqlite3.Connection` configured with `row_factory = sqlite3.Row` and `PRAGMA foreign_keys = ON;` set.
    - `db_connect()` has a function-level docstring stating that the PRAGMA is load-bearing FK-enforcement for the entire app, must not be removed, and that anyone needing a SQLite connection MUST go through this function. Docstring contains the substring `load-bearing` so a future grep gate (if added) can pin it.
    - After the existing `con.execute("PRAGMA foreign_keys = ON;")` line, `db_connect()` reads the PRAGMA back via `con.execute("PRAGMA foreign_keys").fetchone()[0]`. If the read returns `0` AND `_pragma_drift_logged` is `False`, the function emits `_log.warning("PRAGMA foreign_keys is OFF after SET — drift detected")` and then sets `_pragma_drift_logged = True`. The sentinel is set to `True` on first check regardless of whether the WARN fires (so a transient drift logs once-per-session and a never-drift skips the read-back entirely on subsequent calls).
    - Across multiple calls to `db_connect()` in the same session, the WARN line emits AT MOST ONCE (D-11 throttle invariant).
  </behavior>
  <action>
    Modify `musicstreamer/repo.py::db_connect` per the verified target shape in RESEARCH.md Code Examples. Append a function-level docstring as the first statement of the function body — the docstring must state explicitly that the `PRAGMA foreign_keys = ON;` line is load-bearing, that every `ON DELETE CASCADE` in `db_init()`'s schema only fires when the PRAGMA is set on the connection issuing the parent DELETE, that SQLite defaults the PRAGMA to OFF per connection (so removing the line silently leaks orphans — cite BUG-10 and Phase 74 F-07-03), and that anyone needing a SQLite connection MUST go through this function (cite the source-grep gate that Plan 80-04 lands at `tests/test_db_connect_is_sole_connection_factory.py`). The docstring MUST contain the literal substring `load-bearing` (downstream tests grep for it).

    Add a `global _pragma_drift_logged` declaration as the first executable statement after the docstring.

    Keep the existing four lines of `db_connect()` body verbatim: `con = sqlite3.connect(paths.db_path())`, `con.row_factory = sqlite3.Row`, `con.execute("PRAGMA foreign_keys = ON;")`, `return con`. Do NOT add a duplicate `PRAGMA foreign_keys = ON;` (anti-pattern per `.continue-here.md` cited in CONTEXT.md `<domain>`).

    Between the `con.execute("PRAGMA foreign_keys = ON;")` line and the `return con` line, insert the read-back block: `if not _pragma_drift_logged:` then check `con.execute("PRAGMA foreign_keys").fetchone()[0] == 0` (use `== 0` truthy comparison per Pitfall 6 — NOT `is False`). If the read returns `0`, emit `_log.warning("PRAGMA foreign_keys is OFF after SET — drift detected")`. Set `_pragma_drift_logged = True` after the check (whether or not the WARN fired) — this is the throttle: once we've checked and flipped the sentinel, subsequent calls skip the read entirely.

    The exact WARN message string is `PRAGMA foreign_keys is OFF after SET — drift detected` (U+2014 em-dash; CONTEXT specifics). This wording is grep-discoverable.

    Do NOT create `sweep_orphans` in this task — that lands in Task 3. Do NOT modify `__main__.py` — that lands in Plan 80-02. Do NOT add a module-top docstring (D-17).
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py tests/test_station_siblings.py -x</automated>
    <source-grep>grep -c "load-bearing" musicstreamer/repo.py | grep -q "^[1-9]"</source-grep>
    <source-grep>grep -c "PRAGMA foreign_keys is OFF after SET" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "global _pragma_drift_logged" musicstreamer/repo.py | grep -q "^[1-9]"</source-grep>
    <source-grep>grep -c "PRAGMA foreign_keys = ON;" musicstreamer/repo.py | grep -q "^1$"</source-grep>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/repo.py` contains the literal substring `load-bearing` in the `db_connect()` docstring (grep matches at least once).
    - `musicstreamer/repo.py` contains the literal WARN message `PRAGMA foreign_keys is OFF after SET — drift detected` (em-dash) exactly once (grep matches exactly one occurrence — the literal lives inside `db_connect()`'s body, nowhere else).
    - `musicstreamer/repo.py` contains the substring `global _pragma_drift_logged` at least once (inside `db_connect()` plus the helper from Task 1 = 2 hits is also acceptable).
    - `musicstreamer/repo.py` contains the EXACT literal `PRAGMA foreign_keys = ON;` exactly once — NO duplicate of the existing SET line was introduced (anti-pattern guard).
    - `uv run pytest tests/test_repo.py tests/test_station_siblings.py -x` exits 0 — no regression in pre-existing tests; the drift-guard read-back is a no-op when the PRAGMA is correctly ON.
    - Manual smoke (executor self-check): `python -c "from musicstreamer.repo import db_connect; c = db_connect(); print(c.execute('PRAGMA foreign_keys').fetchone()[0])"` prints `1`.
  </acceptance_criteria>
  <done>
    `db_connect()` carries a function-level docstring containing the `load-bearing` invariant statement; a post-SET read-back block emits the WARN line throttled by `_pragma_drift_logged`; the existing PRAGMA SET line is unchanged and not duplicated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add sweep_orphans(con) function adjacent to db_init</name>
  <files>musicstreamer/repo.py</files>
  <read_first>
    - musicstreamer/repo.py (current `db_init()` body — confirm `station_streams` declares `FOREIGN KEY(station_id) REFERENCES stations(id) ON DELETE CASCADE` and `station_siblings` declares matching cascades on `a_id` and `b_id`; the table names hard-coded in `sweep_orphans` MUST match the names in `db_init`)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-CONTEXT.md (decisions D-01 delete + INFO-log when N>0; D-02 separate function in repo.py; D-04 silent on N=0; D-05 only `station_streams` and `station_siblings`; D-06 favorites excluded; D-07 single DELETE handling both FK columns; D-08 provider_id excluded)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-RESEARCH.md (Code Examples section, "`sweep_orphans(con)` — target shape" — verified two-statement shape with `cur.rowcount` capture; conditional INFO log format `_log.info("sweep_orphans: station_streams=%d station_siblings=%d", n1, n2)`)
    - .planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-PATTERNS.md (Analog 3 — placement adjacent to `db_init`; mirror the `con.commit()` pattern; two `con.execute` calls NOT `executescript` because rowcount per statement is needed)
  </read_first>
  <behavior>
    - After this task, `from musicstreamer.repo import sweep_orphans` succeeds and `sweep_orphans` is a callable taking `con: sqlite3.Connection` and returning `None`.
    - `sweep_orphans(con)` executes exactly two `DELETE` statements against `con`:
      1. `DELETE FROM station_streams WHERE station_id NOT IN (SELECT id FROM stations)`
      2. `DELETE FROM station_siblings WHERE a_id NOT IN (SELECT id FROM stations) OR b_id NOT IN (SELECT id FROM stations)`
    - When BOTH statements return `rowcount == 0`, the function emits NO log line and returns `None` (D-04 silent on N=0 invariant).
    - When EITHER `rowcount > 0`, the function emits exactly one `_log.info("sweep_orphans: station_streams=%d station_siblings=%d", n_streams, n_siblings)` line with the two integer rowcounts as `%d` format args (integers from `cursor.rowcount`; no string interpolation, no user-controlled data).
    - `con.commit()` is called once at the end of the function (after the optional log line), persisting both DELETEs atomically with the existing transaction.
    - The function does NOT touch `favorites` (D-06) or `stations.provider_id` (D-08).
    - The function does NOT call `con.close()` — caller owns the connection lifecycle.
  </behavior>
  <action>
    Add a new top-level function `sweep_orphans(con: sqlite3.Connection) -> None` to `musicstreamer/repo.py`, placed adjacent to `db_init()` (either immediately before or immediately after `db_init` — pick whichever keeps the schema-lifecycle functions visibly grouped; CONTEXT D-02 says NOT inside `db_init`).

    Function-level docstring (first statement) explains: the function deletes orphan rows in FK-child tables and logs per-table counts only when at least one table had `rowcount > 0`; cite D-01 / D-02 / D-04 / D-05 / D-06 / D-07 / D-08 explicitly; mention that it heals orphans left by manual `sqlite3`-shell DELETEs that bypassed `db_connect()` (cite Phase 74 F-07-03 Synphaera ghosts as the motivating incident); state that `favorites` is intentionally excluded (no FK; persists track titles by design) and `stations.provider_id` is out of scope (`ON DELETE SET NULL`, not `CASCADE`).

    Body — two `con.execute(...)` calls (NOT a single `executescript` — `executescript` does not return per-statement rowcount):
    1. `cur1 = con.execute("DELETE FROM station_streams WHERE station_id NOT IN (SELECT id FROM stations)")`
    2. `cur2 = con.execute("DELETE FROM station_siblings WHERE a_id NOT IN (SELECT id FROM stations) OR b_id NOT IN (SELECT id FROM stations)")`

    After both DELETEs, capture rowcounts and conditionally log: `if cur1.rowcount > 0 or cur2.rowcount > 0:` then `_log.info("sweep_orphans: station_streams=%d station_siblings=%d", cur1.rowcount, cur2.rowcount)`. The format string is exactly `sweep_orphans: station_streams=%d station_siblings=%d` (grep-discoverable; matches Phase 62 cycle_close shape).

    End with `con.commit()`. Return `None` (implicit; function annotated `-> None`).

    Hard-code the two table names (`station_streams`, `station_siblings`) — NO `PRAGMA foreign_key_list(...)` introspection (CONTEXT "too clever" anti-pattern). NO `favorites` DELETE (D-06 anti-pattern). NO `user_version` gate (D-03 anti-pattern). NO settings flag.

    Do NOT modify `__main__.py` in this task — the call-site insertion is Plan 80-02. Do NOT create any test files — those are Plans 80-03 and 80-04.

    After the edit, run `uv run pytest tests/test_repo.py tests/test_station_siblings.py -x` to confirm no existing test regressed (the new function is unreferenced by existing code; only the new tests in Plan 80-03 will call it).
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo.py tests/test_station_siblings.py -x</automated>
    <source-grep>grep -c "^def sweep_orphans(con: sqlite3.Connection) -> None:$" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "DELETE FROM station_streams WHERE station_id NOT IN" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "DELETE FROM station_siblings WHERE a_id NOT IN" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "sweep_orphans: station_streams=%d station_siblings=%d" musicstreamer/repo.py | grep -q "^1$"</source-grep>
    <source-grep>grep -c "DELETE FROM favorites" musicstreamer/repo.py | grep -q "^0$"</source-grep>
    <import-check>python -c "from musicstreamer.repo import sweep_orphans; assert callable(sweep_orphans)"</import-check>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/repo.py` contains the function-definition line `def sweep_orphans(con: sqlite3.Connection) -> None:` (grep matches exactly one occurrence).
    - `musicstreamer/repo.py` contains the literal SQL substring `DELETE FROM station_streams WHERE station_id NOT IN` exactly once.
    - `musicstreamer/repo.py` contains the literal SQL substring `DELETE FROM station_siblings WHERE a_id NOT IN` exactly once.
    - `musicstreamer/repo.py` contains the literal log format `sweep_orphans: station_streams=%d station_siblings=%d` exactly once.
    - `musicstreamer/repo.py` contains ZERO occurrences of `DELETE FROM favorites` (D-06 anti-pattern guard).
    - `python -c "from musicstreamer.repo import sweep_orphans; assert callable(sweep_orphans)"` exits 0.
    - `uv run pytest tests/test_repo.py tests/test_station_siblings.py -x` exits 0 — no regression in pre-existing tests.
  </acceptance_criteria>
  <done>
    `sweep_orphans(con)` is a callable top-level function in `musicstreamer/repo.py`; it executes exactly the two specified DELETEs, emits the conditional INFO log only when N>0, and commits at the end; existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sqlite3 connection inside one process | Single-trust-zone; no IPC or network. Per-connection PRAGMA state is the integrity boundary. |
| `db_connect()` → all production callers | Implicit contract that every connection has PRAGMA ON; this plan adds a runtime read-back to detect drift from inside the trust zone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | `db_connect()` body | mitigate | Function-level docstring stating the PRAGMA is load-bearing; runtime read-back logs WARN if a future refactor silently drops the SET. Source-grep gate (Plan 80-04) additionally blocks bypass via fresh `sqlite3.connect(` callsites. |
| T-80-02 | Tampering | `sweep_orphans(con)` SQL | accept | All table identifiers (`station_streams`, `station_siblings`) and the parent table reference (`stations`) are hard-coded literals — no dynamic identifier interpolation. No injection vector. |
| T-80-03 | Repudiation / Tampering | `sweep_orphans(con)` destructive DELETE | accept | The two DELETEs are mathematically restricted to rows whose parent station is gone (`NOT IN (SELECT id FROM stations)`). By FK-contract those rows are already unreachable from any user-facing query; sweep is a no-op for any reachable data. The conditional INFO line provides observability when N>0. |
| T-80-04 | Information disclosure (log injection) | `_log.info` format args | accept | Format args are integer `cursor.rowcount` values — typed, no user-controlled string. No log-injection vector. |
| T-80-05 | Denial of service | `_pragma_drift_logged` cross-thread race | accept | sqlite3 connections in Python are not thread-shared in this codebase (verified Phase 77). Race is theoretical, not practical. Worst case: WARN line logs twice instead of once on a contrived multi-threaded drift — observability fails OPEN, not CLOSED. |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs zero new packages. Zero new third-party dependencies. |
</threat_model>

<verification>
After all three tasks land:
- `uv run pytest tests/test_repo.py tests/test_station_siblings.py -x` exits 0 (no regression).
- `python -c "from musicstreamer.repo import db_connect, db_init, sweep_orphans, _reset_pragma_drift_sentinel_for_tests, _log, _pragma_drift_logged; print('OK')"` exits 0 — all new module surface is importable.
- `python -c "from musicstreamer.repo import db_connect; c = db_connect(); print(c.execute('PRAGMA foreign_keys').fetchone()[0])"` prints `1` — drift-guard does not fire under normal conditions.
- `grep -c "PRAGMA foreign_keys = ON;" musicstreamer/repo.py` outputs `1` — the original PRAGMA line was not duplicated.
- `head -n 1 musicstreamer/repo.py` outputs `import logging` or `import sqlite3` (NOT a triple-quoted string — D-17 module-top-docstring guard).
</verification>

<success_criteria>
- `musicstreamer/repo.py` ships module logger, drift-guard sentinel, test-reset helper, hardened `db_connect()` (docstring + read-back), and new `sweep_orphans` — all in a single coherent edit.
- All existing repo / station-siblings tests continue to pass.
- No new external dependencies.
- The PRAGMA SET line at line 20 of the original file remains the unique callsite of `PRAGMA foreign_keys = ON;` in `repo.py` (no duplicate).
- The drift-guard WARN message literal `PRAGMA foreign_keys is OFF after SET — drift detected` appears exactly once in `musicstreamer/repo.py` source.
</success_criteria>

<output>
Create `.planning/phases/80-sqlite-foreign-key-enforcement-set-pragma-foreign-keys-on-pe/80-01-SUMMARY.md` when done.
</output>
