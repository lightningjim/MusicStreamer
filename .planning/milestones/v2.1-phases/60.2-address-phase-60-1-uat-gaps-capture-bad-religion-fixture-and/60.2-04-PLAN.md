---
phase: 60.2
plan: 04
type: execute
wave: 3
depends_on:
  - 60.2-03
files_modified:
  - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md
autonomous: false
requirements:
  - GBS-01e
tags:
  - gbs.fm
  - uat
  - verification

must_haves:
  truths:
    - "Full project test suite is GREEN (modulo the 11 pre-existing baseline failures + 1 C-level abort already documented in 60.1-UAT.md)."
    - "Phase 60.2's own scope (test_gbs_api.py + test_gbs_search_dialog.py) is 100% GREEN — Plan 01's 9 NEW tests + all Phase 60.1 baseline tests + QA-05 invariant."
    - "Manual UAT signed by Kyle on Linux Wayland (DPR=1.0) — bad-religion live behavior matches TRIAGE outcome; album section-headers visible in artist drill."
  artifacts:
    - path: ".planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md"
      provides: "Signed UAT log with full-suite results, bad-religion live verification, album-grouping visual verification"
      contains: "UAT signed off"
  key_links:
    - from: "60.2-UAT.md SC1"
      to: "60.2-01-TRIAGE.md Decision"
      via: "live behavior must match the TRIAGE finding (D-05 limitation OR D-06 fix)"
      pattern: "TRIAGE"
    - from: "60.2-UAT.md SC2"
      to: "musicstreamer/ui_qt/gbs_search_dialog.py::_insert_album_section_header"
      via: "live render shows bold album name + song count between groups"
      pattern: "section.header\\|album.*section"
---

<objective>
Wave 3 (Verification + UAT) for Phase 60.2: run the full project test suite as the phase gate, then conduct a manual UAT on Linux Wayland verifying the two SC1+SC2 closures (bad-religion live behavior matches TRIAGE; album section-headers render visually correctly in artist drill-down). Sign off the UAT and create the close-out artifacts.

Purpose: Closes the phase by confirming all of CONTEXT.md D-01..D-11 + RESEARCH Pitfalls 1/2/3/5/6/9 + the GBS-01e SC1 + new album-grouping defect via live exercise on the deployment target rig (Wayland, DPR=1.0). Mirrors Phase 60.1's Wave 3 close-out structure (60.1-04-PLAN signed FAIL UAT). Phase 60.2 must sign PASS to be ready for `/gsd-complete-phase`.

Output:
- `.planning/phases/60.2-.../60.2-UAT.md` — full UAT log with test-suite pre-gate results, two SC outcomes, visual verification of section-headers, bad-religion live exercise, and a sign-off line dated by Kyle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-CONTEXT.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-RESEARCH.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-VALIDATION.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-01-TRIAGE.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-01-SUMMARY.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-02-SUMMARY.md
@.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-03-SUMMARY.md
@.planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite test gate + UAT log scaffolding</name>
  <files>.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md</files>
  <read_first>
    - .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md (the FAIL precedent — copy structure for the PASS attempt)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-VALIDATION.md (test commands + sampling rate + manual-only verifications + 11-baseline-failure accounting)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-01-TRIAGE.md (Decision section — drives SC1 expected behavior)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-01-SUMMARY.md (Plan 01 outcomes — fixtures + RED test list + triage finding)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-02-SUMMARY.md (Plan 02 outcomes — parser changes + bad-religion fix or doc'd limitation)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-03-SUMMARY.md (Plan 03 outcomes — dialog changes; helper signatures)
  </read_first>
  <action>
    Run the test-suite pre-gate per VALIDATION.md:

    1. Phase scope (must be 100% GREEN):
       ```bash
       uv run pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py --no-header -q
       ```
       Expect: all tests pass (~43 tests in test_gbs_api.py + Phase 60.1's 25 + Plan 01's 6 = ~31 in test_gbs_search_dialog.py — exact count from output).

    2. Full project suite (must show only the 11 pre-existing baseline failures + 1 C-level abort already documented in 60.1-UAT.md — ZERO new regressions):
       ```bash
       uv run pytest --no-header --tb=no -q --deselect tests/test_import_dialog_qt.py::test_yt_scan_passes_through
       ```
       Expect: failure count exactly `11` (or whatever 60.1-UAT.md documented + zero new). If MORE than 11 failures, ABORT and investigate before proceeding.

    3. SC4 verification commands (mirroring 60.1-UAT.md format):
       ```bash
       uv run pytest tests/test_gbs_search_dialog.py -k "drill or back_button or pre_drill or section_header or empty_album or clear_table_clears_spans or album_drill_is_flat" --no-header
       uv run pytest tests/test_gbs_api.py -k "artist_page or album_page or fetch_artist or fetch_album or response_shape" --no-header
       uv run pytest tests/test_gbs_search_dialog.py::test_no_self_capturing_lambdas_in_dialog --no-header
       ```
       Capture all outputs verbatim.

    Create `.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` with the following structure (mirror 60.1-UAT.md format):

    ```markdown
    # Phase 60.2: Address Phase 60.1 UAT Gaps — UAT Log

    **Phase:** 60.2
    **Requirement:** GBS-01e (refines)
    **Date:** {today}
    **Tester:** Kyle
    **Deployment target:** Linux Wayland, DPR=1.0 (per project memory)

    ## Test-Suite Pre-Gate

    Phase gate per VALIDATION.md.

    **Phase 60.2's own test suites (must be 100% green):**

    {paste output of step 1 here verbatim}

    **Full project test suite:**

    {paste output of step 2 here verbatim — confirm exactly 11 pre-existing failures + zero new regressions}

    **SC4-style verification commands:**

    {paste outputs of step 3 here verbatim}

    Status: [ ] PASS  [ ] FAIL

    ## ROADMAP Success Criteria

    ### SC1: Bad-religion zero-panel response addressed (per TRIAGE outcome)

    From CONTEXT.md D-04: "Phase 60.2 commits to triage + ship whatever fix the triage warrants."

    TRIAGE outcome: {D-05 OR D-06 — copy from 60.2-01-TRIAGE.md "## Decision"}

    Test steps (live):
    1. Launch the app: `uv run python -m musicstreamer`
    2. Open Search GBS.FM dialog (hamburger → Search GBS.FM…)
    3. Enter the query: `bad religion`
    4. Press Enter; observe the search results.
    5. Branch on TRIAGE:
       - **If D-05 (documented server-side limitation):** confirm the response shows song results but NO Artist:/Album: panels — matching the captured `search_bad_religion_p1.html` shape. The behavior matches the documented limitation in `_ArtistAlbumParser` docstring.
       - **If D-06 (parser fix):** confirm the response NOW shows Artist:/Album: panels populated with at least one Bad Religion entry each — the fix lands in `_ArtistAlbumParser` per TRIAGE.
    6. Capture observed behavior.

    Result: [ ] PASS  [ ] FAIL  [ ] N/A (documented limitation per TRIAGE D-05)

    Notes: _________________________________________

    ### SC2: Album section-headers in artist drill-down

    From CONTEXT.md D-01..D-03: "Render album grouping in the artist drill-down via section-header rows in the existing QTableView. Section-header is f'{album} ({n} songs)'."

    Test steps (live):
    1. Continue from SC1 (or fresh search).
    2. Search for a multi-album artist: `testament` (single-word; matches the captured `artist_4803.html` fixture — 14 albums).
    3. Click the Testament entry in the Artist: panel.
    4. Confirm: drill-down view loads. Verify visually:
       - Bold album-name section-header rows appear between groups (e.g., `Brotherhood of the Snake (8 songs)`, `The New Order (10 songs)`).
       - Headers span the entire table width (4 columns merged).
       - Headers have NO Add! button.
       - Clicking a header does nothing (non-selectable).
       - Clicking an Add! button on a song row still adds that song to the playlist (toast confirms).
    5. Click Back; confirm search results restore (no stale span on the search-results table).
    6. Search a single-album artist (e.g., a less-prolific band) to verify the empty-album D-11 path doesn't appear (Testament has no pre-first-albumTitle rows so D-11 won't manifest there).

    Result: [ ] PASS  [ ] FAIL

    Notes: _________________________________________

    ## Additional Manual Validations (UI-SPEC contract — Phase 60.1 still binding)

    - [ ] Phase 60.1 Layout Deltas 1-5 still PASS (drill chrome shows back button + breadcrumb; search chrome restores on Back)
    - [ ] Phase 60.1 Pitfalls 8/9/10 still PASS (snapshot single-shot; Back disabled during fetch; new search abandons drill)
    - [ ] Pitfall 1+9: drill artist A → click Back → search again → first row of new search has NO stale span (visual confirmation)
    - [ ] Pitfall 3: clicking Add! on a song under a section-header submits the CORRECT song (not a wrong-index neighbor) — verify by toast text matching the song clicked
    - [ ] Pitfall 6: drill into an album (not artist) → flat song list, no section-headers (album drill stays flat)

    ## Sign-Off

    - [ ] All 2 ROADMAP success criteria met (or N/A documented per TRIAGE)
    - [ ] Full test suite green for Phase 60.2's own scope; pre-existing failures elsewhere documented as out-of-scope
    - [ ] Phase 60.1 UI-SPEC + Pitfalls still PASS

    **UAT signed off:** {date} by Kyle

    **Outcome:** [ ] PASS — Phase 60.2 ready for /gsd-complete-phase
                 [ ] FAIL — open Phase 60.3 to address gaps

    ## Deviations / Open Issues

    {Record any pre-existing baseline failures, any deferred items, any post-fix follow-ups discovered during UAT.}
    ```

    Replace `{today}` with the current date (read from environment).
    Replace `{D-05 OR D-06 — copy from 60.2-01-TRIAGE.md "## Decision"}` with the actual decision content from TRIAGE.md.
    Leave the live test-step result checkboxes EMPTY for Kyle to fill in during the human-verify checkpoint (Task 2).

    Run a final sanity check that 60.2-UAT.md is well-formed:
    ```bash
    grep -c "^## " .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md
    ```
    Should return `>=4` (Test-Suite Pre-Gate, ROADMAP Success Criteria, Additional Manual Validations, Sign-Off, Deviations).
  </action>
  <verify>
    <automated>uv run pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py --no-header -q && test -s .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase scope GREEN: `uv run pytest tests/test_gbs_api.py tests/test_gbs_search_dialog.py --no-header -q` exits 0
    - Full suite shows ONLY the 11 pre-existing baselines (zero new regressions): `uv run pytest --no-header --tb=no -q --deselect tests/test_import_dialog_qt.py::test_yt_scan_passes_through 2>&1 | grep -oE '[0-9]+ failed' | head -1` returns `11 failed` (or fewer if any pre-existing baseline coincidentally got fixed; never MORE than 11)
    - UAT log scaffold exists: `test -s .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` exits 0
    - UAT log has all required sections: `grep -c "^## Test-Suite Pre-Gate\|^## ROADMAP Success Criteria\|^### SC1:\|^### SC2:\|^## Additional Manual Validations\|^## Sign-Off\|^## Deviations" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `>=7`
    - UAT log embeds the actual TRIAGE decision: `grep -c "TRIAGE outcome:.*D-0[56]" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `1`
    - UAT log embeds full-suite test output: `grep -c "passed\|failed" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `>=2` (test-suite output captured, not just placeholder text)
  </acceptance_criteria>
  <done>Test gate GREEN; UAT log scaffolded with TRIAGE decision embedded and live-test checkpoints ready for Kyle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT on Linux Wayland — sign SC1 + SC2 + UI-SPEC + Pitfalls</name>
  <files>.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md</files>
  <read_first>
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md (the scaffolded log Task 1 produced — Kyle edits this file)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-01-TRIAGE.md (Decision section — drives SC1 expected behavior)
    - .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-CONTEXT.md (D-01..D-11 — the contract Kyle is verifying)
    - .planning/phases/60.1-gbs-fm-search-artist-album-drill-down/60.1-UAT.md (precedent FAIL UAT — same form Kyle signs)
  </read_first>
  <what-built>
    Phase 60.2 closes Phase 60.1's UAT FAIL gaps:
    - **SC1:** Bad-religion query is now triaged. Per `60.2-01-TRIAGE.md`, the routing was {D-05 documented limitation OR D-06 parser fix} — see the TRIAGE log for the exact decision and surface.
    - **SC2:** Artist drill-down view now inserts bold album section-header rows between album groups (`f"{album} ({n} songs)"`, e.g. `"Brotherhood of the Snake (8 songs)"`), spanning all 4 columns. Empty-album rows render without a header (D-11 — defensive, may not manifest in practice).
    - Pitfall 1+9 mitigation: `_clear_table` calls `clearSpans()` BEFORE `removeRows` so search-after-drill renders cleanly.
    - Pitfall 3 mitigation: Add! button slot uses song-list index, not table-row index — clicking Add! on the Nth song under a section header submits the correct song.
    - Pitfall 6 mitigation: album drill view stays flat — no section headers there.
    - Phase 60.1 invariants preserved: drill chrome (back button + breadcrumb), QA-05 no-lambda, plain-text rendering.

    Task 1 of this plan ran the full test suite (must be GREEN modulo the 11 pre-existing baselines) and authored the `60.2-UAT.md` scaffold with TRIAGE decision embedded.

    The HUMAN now needs to:
    1. Run the app on Linux Wayland with their dev GBS.FM cookies.
    2. Execute the manual UAT script in `60.2-UAT.md` (SC1 + SC2 + Additional Manual Validations).
    3. Fill in the placeholder fields ({date}, observations) using the Edit tool.
    4. Mark each checkbox `[x] PASS`, `[x] FAIL`, or `[x] N/A` as appropriate.
    5. Type "approved" to resume — the executor commits the signed UAT log.
  </what-built>
  <how-to-verify>
    1. Launch the app on the Linux Wayland rig: `uv run python -m musicstreamer` (DPR=1.0).
    2. Hamburger menu → "Search GBS.FM…" — dialog opens with login gate clear (cookies present).
    3. **SC1 verification:** type `bad religion` → press Enter →
       - **If TRIAGE was D-05:** confirm song results render but Artist:/Album: panels are empty/absent. Match the documented behavior in `_ArtistAlbumParser` docstring.
       - **If TRIAGE was D-06:** confirm Artist:/Album: panels show Bad Religion entries (band + albums).
       - Mark the SC1 result PASS or FAIL or N/A in `60.2-UAT.md`.
    4. **SC2 verification:** clear search → type `testament` → press Enter →
       - In the Artist: panel, click `Testament`.
       - Drill-down view loads. **VISUAL CHECK:**
         - Bold album-name section-headers separate song groups (e.g. `Brotherhood of the Snake (8 songs)`, `The New Order (10 songs)`).
         - Headers span all 4 columns visually (one merged cell across the table width).
         - Headers have NO Add! button.
         - Clicking on a header does nothing (cell not selectable).
       - Click Add! on the FIRST song under any section-header. **CORRECTNESS CHECK:** the toast confirmation should name THAT song, not a neighbor (Pitfall 3 mitigation).
       - Click Back to return to search results.
       - **PITFALL 1+9 CHECK:** the search results restore cleanly — first row's columns are NOT merged into one cell (no stale span).
       - Mark the SC2 result PASS or FAIL.
    5. **Pitfall 6 verification:** in the Album: panel of the Testament results, click any album → drill-down view loads → confirm flat song list (NO section headers anywhere). Mark in UAT log.
    6. **Phase 60.1 regression spot-check:** verify back-button + breadcrumb still display correctly during drill (Layout Delta 2 unchanged); verify a fresh search after drilling resets the drill chrome (Pitfall 10).
    7. Open `.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` in an editor. Mark the SC1 + SC2 + Additional Manual Validations checkboxes. Add notes for any anomalies.
    8. Sign the bottom: `**UAT signed off:** {today's date} by Kyle` and select PASS or FAIL.
    9. If PASS: phase is ready for `/gsd-complete-phase`. If FAIL: record gaps and open Phase 60.3 (or 60.2.1 gap-closure depending on severity).
  </how-to-verify>
  <action>
    The HUMAN executes the UAT and edits `60.2-UAT.md` to sign off. The EXECUTOR (Claude) does NOT modify the UAT log content beyond what Task 1 scaffolded — only the human can fill in the live test results and outcomes.

    After the human signs the UAT (types "approved"):

    1. Verify the UAT log has been edited (no remaining `{...}` placeholder strings in the SC sections):
       ```bash
       grep -c "{date}\|{today}\|{D-05 OR D-06" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md
       ```
       Expect `0` (all placeholders filled).

    2. Verify a sign-off line exists with a date and "Kyle":
       ```bash
       grep -cE "UAT signed off:.*by Kyle" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md
       ```
       Expect `1`.

    3. Verify an outcome was selected (PASS or FAIL marked with `[x]`):
       ```bash
       grep -cE "^\*\*Outcome:\*\* \[x\]" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md
       ```
       Expect `1`.

    4. Commit the signed UAT log:
       ```bash
       git add .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md
       git commit -m "docs(60.2-04): UAT sign-off — Phase 60.2 {PASS or FAIL outcome}"
       ```

    If the human typed something other than "approved" (described a failure instead), DO NOT commit — instead surface the failure to the orchestrator so a follow-on plan can be opened.
  </action>
  <verify>
    <automated>grep -cE "UAT signed off:.*by Kyle" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - All placeholder strings filled: `grep -c "{date}\|{today}\|{D-05 OR D-06" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `0`
    - Sign-off line present: `grep -cE "UAT signed off:.*by Kyle" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `1`
    - Outcome marked: `grep -cE "^\*\*Outcome:\*\* \[x\]" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `1`
    - SC1 result marked: `grep -cE "Result: \[x\] (PASS|FAIL|N/A)" .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` returns `>=2` (one for SC1, one for SC2)
    - UAT log committed: `git log -n 5 --oneline -- .planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-UAT.md` shows a commit with `60.2-04` and `UAT sign-off`
  </acceptance_criteria>
  <resume-signal>Type "approved" if SC1+SC2 PASS and 60.2-UAT.md is signed PASS by Kyle; describe the issues if FAIL.</resume-signal>
  <done>UAT signed by Kyle (PASS or FAIL recorded); placeholders filled; commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live GBS.FM `/search` + `/artist/<id>` HTTP responses → running musicstreamer process | The phase changes already mitigate this (parsers + dialog tests cover the data-flow) — UAT exercises the live integration end-to-end |
| Human visual confirmation → UAT log | Manual sign-off; not automatable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.2-11 | Repudiation | UAT sign-off accuracy | mitigate | UAT log has structured PASS/FAIL checkboxes per SC + a single sign-off line; recorded by date in git. Same precedent as 60.1-UAT.md. |
| T-60.2-12 | Information Disclosure | UAT log captures live response shape (e.g., test-suite output, song titles) | accept | Output is not sensitive — song titles and band names are public catalog data; full-suite test output is project-internal. No cookies or session data captured. |
</threat_model>

<verification>
- Phase 60.2 own scope: 100% GREEN test pass.
- Full suite: exactly the 11 pre-existing baselines + 1 deselected C-level abort (no NEW regressions).
- UAT log signed PASS by Kyle.
- Live SC1 behavior matches TRIAGE.md decision.
- Live SC2 shows section-headers visually correct.
- Phase 60.1 UI-SPEC + Pitfalls 8/9/10 still hold.
</verification>

<success_criteria>
- `60.2-UAT.md` exists with full test-suite output captured.
- SC1 marked PASS or N/A (per TRIAGE D-05).
- SC2 marked PASS.
- All Additional Manual Validations checkboxes marked.
- Sign-off line present with today's date and Kyle's name.
- Outcome marked PASS — phase ready for `/gsd-complete-phase`.
</success_criteria>

<output>
After completion, create `.planning/phases/60.2-address-phase-60-1-uat-gaps-capture-bad-religion-fixture-and/60.2-04-SUMMARY.md` summarizing:
1. Test-suite pre-gate outcome (counts: pass / fail / deselect).
2. SC1 result + the TRIAGE branch taken in live verification.
3. SC2 result + visual confirmation of section-headers.
4. UI-SPEC + Pitfalls regression spot-check outcome.
5. Phase outcome (PASS or FAIL) + any deviations or follow-ups for next phase.
</output>
