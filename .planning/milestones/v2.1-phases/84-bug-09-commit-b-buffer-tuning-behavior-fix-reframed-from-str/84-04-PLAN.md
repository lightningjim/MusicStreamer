---
phase: 84
plan: 04
type: execute
wave: 2
depends_on: [84-02, 84-03]
files_modified:
  - .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md
autonomous: true
requirements: [BUG-09]
tags: [verification, closure, ship-plus-monitor, waived-gate]

must_haves:
  truths:
    - "D-13: 84-VERIFICATION.md exists and explicitly states the Phase 78 D-06 'M < N AND median lower' statistical gate is WAIVED for Phase 84 with the harvest-week reframe rationale (12 events / 7 days = insufficient sample for marginal-effect detection)."
    - "D-13: 84-VERIFICATION.md documents the 2-week post-ship monitor plan: compare buffer_underrun event count + long-event magnitudes in ~/.local/share/musicstreamer/buffer-events.log against the harvest-week baseline (12 events / 5 long: 2 YouTube + 3 SomaFM, max magnitude 7389ms — honors D-09 'both clusters' framing correction)."
    - "D-13: 84-VERIFICATION.md defines explicit follow-up trigger thresholds: ≥3 long events (>1s) with min_percent=0 in 2-week window, OR any recovered event >10s, OR ≥1 cause_hint=network event → open follow-up phase for reconnect-on-stall evaluation."
    - "D-13: 84-VERIFICATION.md states BUG-09 SC #3 (behavior side) CLOSES on the Phase 84 ship commit — NOT pending the monitor window. The monitor is forward-looking guidance, not a closure prerequisite."
    - "84-VERIFICATION.md cross-references Phase 78 Commit A 78-VERIFICATION.md as the predecessor closure record and Phase 62 62-VERIFICATION.md for the SC #3 deferral chain."
    - "84-VERIFICATION.md goal-achievement section enumerates the D-10, D-11, D-12 deliverables that closed and links each to evidence (file path + line range; pytest test name + GREEN status)."
  artifacts:
    - path: ".planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md"
      provides: "Phase 84 closure record with waived-gate language + 2-week monitor plan + follow-up trigger thresholds"
      min_lines: 80
      contains: "WAIVED"
  key_links:
    - from: "84-VERIFICATION.md follow-up-trigger section"
      to: "Phase 78 deferred 'reconnect-on-stall' item"
      via: "explicit threshold table → phase opening criterion"
      pattern: "reconnect-on-stall"
---

<objective>
Wave 2 closure: write 84-VERIFICATION.md per D-13. Documents that BUG-09 SC #3 behavior side is CLOSED on the Phase 84 ship commit under a "ship + monitor" reframe (Phase 78 D-06 statistical gate explicitly waived; 12 harvest events insufficient for marginal-effect detection). Defines the 2-week post-ship monitor plan and the follow-up trigger thresholds that would open a follow-up phase for reconnect-on-stall evaluation (Phase 78 deferred item, still parked).

Purpose: Phase 84 ships under a non-statistical closure model — the verification artifact is the closure decision record itself, not a gate to pass. The artifact also seeds the post-ship monitoring practice so a follow-up phase can be opened deterministically if the harvest log shows the picked thresholds are exceeded.
Output: One markdown file in the phase directory. Mirrors Phase 78 VERIFICATION.md shape (frontmatter + Goal Achievement table + Required Artifacts table + human-verification block for the harvest monitor). No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-CONTEXT.md
@.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-RESEARCH.md
@.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-PATTERNS.md

@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VERIFICATION.md
@.planning/phases/62-audio-buffer-underrun-resilience-intermittent-dropouts-stutt/62-VERIFICATION.md
@.planning/phases/62-audio-buffer-underrun-resilience-intermittent-dropouts-stutt/62-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 84-VERIFICATION.md per D-13 (waived gate + monitor plan + trigger thresholds)</name>
  <files>.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VERIFICATION.md (canonical mirror — frontmatter shape + Goal Achievement table + Required Artifacts table + human_verification block for harvest UAT)
    - .planning/phases/62-audio-buffer-underrun-resilience-intermittent-dropouts-stutt/62-VERIFICATION.md (the original BUG-09 closure record where SC #3 was deferred to Phase 78 → Phase 84)
    - .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-CONTEXT.md (D-13 — verbatim waiver + monitor + trigger language)
    - .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-RESEARCH.md (§D-11 Resolution — for the technical reason this phase shipped the fallback shape; needed in the rationale block)
  </read_first>
  <action>
    Create `.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md`. Mirror Phase 78 VERIFICATION.md frontmatter + structure exactly, with these Phase 84-specific contents:

    **Frontmatter:**
    ```yaml
    ---
    phase: 84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str
    verified: {YYYY-MM-DDT00:00:00Z — date of execution}
    status: human_needed
    score: {N}/{N} must-haves verified (Phase 84 ship-side)
    overrides_applied: 0
    closure_model: ship-plus-monitor
    waived_gates:
      - id: phase-78-D-06
        text: "M < N AND median lower"
        rationale: "Harvest week yielded 12 buffer_underrun events / 7 days — insufficient sample for marginal-effect detection. Reframed per Phase 84 D-13."
    human_verification:
      - test: "Post-ship 2-week monitor window (~/.local/share/musicstreamer/buffer-events.log)"
        expected: "Over 2 weeks of normal daily-use after Phase 84 ship commit, the buffer-events.log shows REDUCED long-event count vs the harvest-week baseline (12 events / 5 long) OR holds steady. If thresholds tripped (see Follow-Up Triggers below), open a follow-up phase."
        why_human: "Requires real-world environmental conditions and 2 weeks of accumulated samples. No CI substitute is meaningful — the entire Phase 84 reframe predicate is that statistical sample size is too low for synthetic A/B."
    ---
    ```

    **Body sections (top to bottom):**

    1. **Title line:** `# Phase 84: BUG-09 Commit B — Buffer-Tuning Behavior Fix (ship + monitor closure) Verification Report`

    2. **Phase Goal (verbatim or near-verbatim from ROADMAP entry + CONTEXT.md `<domain>` block):** Ship the deferred Commit B buffer-tuning behavior fix under a "ship + monitor" reframe. D-10 (static bump 30s/20MB) + D-11 (adaptive growth 30→60→120 with stage-and-apply fallback) + D-12 (always-visible stats row + Signal wire) + D-13 (this VERIFICATION.md). Closes BUG-09 SC #3 (behavior side) on the ship commit.

    3. **`## Closure Model — Waived Statistical Gate`** — explicit section title with `WAIVED` in the heading or first sentence. Body:
       - Phase 78 D-06 originally specified an `M < N AND median lower` statistical gate (M = post-fix long-event count over comparable window; N = pre-fix baseline count from harvest week).
       - That gate is **WAIVED** for Phase 84. Rationale: 12 buffer_underrun events / 7 days is insufficient sample for marginal-effect detection at any reasonable confidence level. The harvest data is DIRECTIONAL (5 long events split 2 YouTube + 3 SomaFM, worst case 7.4s; 7 brief recoveries) — sufficient to lock the D-10 magnitudes (30s covers worst case with 3× headroom) and the D-11 schedule shape (30→60→120 doubling) but NOT to detect "did the fix reduce long-event count by X%" via post-ship A/B.
       - Closure is "ship + monitor". The deliverables (D-10 / D-11 / D-12) are technically complete on the ship commit; the monitor plan below is forward-looking guidance, NOT a closure prerequisite.

    4. **`## Goal Achievement`** — table of must-haves verified, mirroring Phase 78 VERIFICATION.md Goal Achievement table. Merge truths from all 3 prior plans' (84-01 / 84-02 / 84-03) `must_haves.truths` arrays. For each truth, columns: `#`, `Truth`, `Status`, `Evidence`. Evidence cites the specific file path + line range AND the pytest test name that GREEN-locks the behavior. Example row:
       ```
       | 1 | BUFFER_DURATION_S == 30 and BUFFER_SIZE_BYTES == 20 * 1024 * 1024 in musicstreamer/constants.py (D-10) | VERIFIED | `musicstreamer/constants.py:54-56`; tests/test_player_buffer.py::test_player_constants_match_documented_values PASSED |
       ```
       Target: at least 12 rows covering D-10 (2 truths), D-11 (8 truths spanning Signal, state fields, helpers, both apply sites, baseline reset, no spurious emit, dash-form property name, flags|0x100 preservation, __main__.py untouched), D-12 (3 truths spanning Signal wire, slot, stats row).

    5. **`## Required Artifacts`** — table mirroring Phase 78. Rows for each of the 4 production files modified + 2 NEW test files + 2 edited test files + 1 FakePlayer parity edit. Status = VERIFIED + brief Details.

    6. **`## Monitor Plan (Post-Ship — 2 Weeks)`** — body:
       - Window: from the Phase 84 ship commit date forward 14 days.
       - Source: `~/.local/share/musicstreamer/buffer-events.log` (Phase 78 RotatingFileHandler at 1MB × 3 backups; harvest infra unchanged).
       - Baseline reference (harvest-week, from 84-CONTEXT.md `<data-summary>`):
         - 12 total `buffer_underrun` events / 7 days (~1.7/day)
         - 5 long (>1s): 6683ms YT, 1356ms SomaFM, 5474ms SomaFM (`cause_hint=network`), 7389ms YT, 2446ms SomaFM
         - 7 brief (<200ms): 6 SomaFM + 1 GBS.FM
         - Cluster split: 2 YouTube / 3 SomaFM among long events
       - Monitor procedure: at end of 2-week window, count `buffer_underrun` lines in current log (+ backups), compare against baseline.

    7. **`## Follow-Up Triggers`** — table of thresholds that, if hit, open a follow-up phase for reconnect-on-stall evaluation (Phase 78 deferred item still parked). Verbatim from CONTEXT.md D-13:
       | Trigger | Threshold | Action |
       |---|---|---|
       | Long-event recurrence | ≥3 long events (>1s) with `min_percent=0` in 2-week window | Open follow-up phase: reconnect-on-stall evaluation |
       | Very-long recovery | Any `recovered` event >10s | Open follow-up phase: reconnect-on-stall evaluation |
       | Network cause-hint | ≥1 `cause_hint=network` event | Open follow-up phase: reconnect-on-stall evaluation |

    8. **`## SC #3 Closure (BUG-09)`** — explicit closure statement:
       - SC #3 behavior side CLOSED on Phase 84 ship commit.
       - SC #3 logging side (file sink + live counter) ALREADY CLOSED by Phase 78 Commit A (2026-05-17).
       - Full BUG-09 SC #3 chain: Phase 62 (instrumentation) → Phase 78 Commit A (harvest infra) → Phase 84 (behavior fix). All three plans now closed.
       - The monitor window above is NOT a re-opening of SC #3; it is forward-looking guidance for OPENING a NEW follow-up phase if needed.

    9. **`## Cross-Phase References`** — short link block:
       - Phase 62 VERIFICATION.md — original BUG-09 closure where SC #3 was deferred.
       - Phase 78 VERIFICATION.md — Commit A closure (harvest infra shipped 2026-05-17).
       - 84-CONTEXT.md `<data-summary>` — corrected harvest data (12 events; supersedes ROADMAP "11 events" entry — ROADMAP not amended per user choice).
       - 84-RESEARCH.md §D-11 Resolution — playbin3 mid-session-write FALSIFIED; documents why the fallback shape was MANDATORY (not optional).

    Tone: factual, succinct, citation-heavy. Match Phase 78 VERIFICATION.md's tone exactly (it is the structural template).

    DO NOT include code changes in this plan.
    DO NOT mark the closure as `verified: true` if any Wave 1 plan still has open RED tests — the executor must check Plan 02 and Plan 03 SUMMARYs first; if either reports incomplete tests, set `status: blocked-on-wave-1` instead of `human_needed` and document the gap.
    DO NOT invent monitor thresholds — they MUST come verbatim from CONTEXT D-13.
    DO NOT close BUG-09 SC #3 unless Goal Achievement table is fully VERIFIED.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer &amp;&amp; test -f .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md &amp;&amp; grep -cE "WAIVED|waived" .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md &amp;&amp; grep -cE "ship-plus-monitor|ship \+ monitor" .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md &amp;&amp; wc -l .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md` exits 0.
    - `wc -l 84-VERIFICATION.md` returns at least 80 lines.
    - `grep -cE "WAIVED|waived" 84-VERIFICATION.md` returns ≥ 2 (one in frontmatter waived_gates section, one in body "Closure Model" section title or first sentence).
    - `grep -cE "phase-78-D-06|Phase 78 D-06" 84-VERIFICATION.md` returns ≥ 1 (waived-gate identifier).
    - `grep -cE "ship.plus.monitor|ship \+ monitor|ship-plus-monitor" 84-VERIFICATION.md` returns ≥ 1 (closure model named).
    - `grep -cE "reconnect.on.stall" 84-VERIFICATION.md` returns ≥ 1 (follow-up trigger section names the deferred item).
    - `grep -cE "min_percent=0" 84-VERIFICATION.md` returns ≥ 1 (verbatim threshold language from CONTEXT D-13).
    - `grep -cE "cause_hint=network" 84-VERIFICATION.md` returns ≥ 1 (verbatim threshold language from CONTEXT D-13).
    - `grep -cE ">10s|> 10s|10 seconds" 84-VERIFICATION.md` returns ≥ 1 (very-long recovery threshold).
    - `grep -cE "12 events|12 .*buffer_underrun" 84-VERIFICATION.md` returns ≥ 1 (corrected harvest count, not the ROADMAP's "11").
    - `grep -cE "BUG-09" 84-VERIFICATION.md` returns ≥ 3 (closure statement section + cross-refs + frontmatter context).
    - `grep -cE "SC #3" 84-VERIFICATION.md` returns ≥ 2 (closure statement + Phase 78 chain reference).
    - Frontmatter `status:` is `human_needed` (matching Phase 78 precedent — monitor window IS human-verification-discharged) AND `closure_model: ship-plus-monitor`.
    - Goal Achievement table has at least 12 rows (covering D-10, D-11, D-12).
    - Cross-Phase References section explicitly links to 78-VERIFICATION.md AND 62-VERIFICATION.md (use relative path: `.planning/phases/78-.../78-VERIFICATION.md`).
  </acceptance_criteria>
  <done>
    - 84-VERIFICATION.md created with waived-gate frontmatter + body, 2-week monitor plan, explicit follow-up trigger thresholds (≥3 long events with min_percent=0, OR any recovered >10s, OR ≥1 cause_hint=network), SC #3 closure statement, cross-phase references.
    - Goal Achievement table cites file paths + pytest test names for each of D-10 / D-11 / D-12 truth.
    - Status `human_needed` (monitor window is the human-verifier step); `closure_model: ship-plus-monitor`.
    - No code changes in this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 84 ship commit → BUG-09 SC #3 closure record | VERIFICATION.md is the durable closure record; once committed, downstream phases (e.g. any follow-up reconnect-on-stall phase) read it as the predecessor closure artifact. |
| Monitor-window harvest data → follow-up phase trigger | The thresholds documented here are the predicate for OPENING a future phase. Wrong thresholds = wrong gating; thresholds MUST come verbatim from CONTEXT D-13 (no invention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-11 | Repudiation | Future audit asks "why was the D-06 gate waived?" and finds no documented rationale | mitigate | Frontmatter `waived_gates` block + body "Closure Model" section both explicitly cite the rationale (12 events / 7 days insufficient). Cross-references to 78-VERIFICATION.md + 62-VERIFICATION.md provide the full deferral chain so the waiver is contextualized. |
| T-84-12 | Tampering | A future planner accidentally re-opens BUG-09 SC #3 because the closure statement is ambiguous | mitigate | "SC #3 Closure" section is an explicit standalone section, NOT a frontmatter-only flag. It states the closure date AND that the monitor window is forward-looking guidance (not re-opening). The grep-gate acceptance criteria require `SC #3` to appear ≥ 2 times — disambiguity by repetition. |

No information-disclosure or DoS concerns — this plan writes a markdown file in the repo.
</threat_model>

<verification>
- `test -f .planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md` succeeds.
- File has all 9 body sections enumerated in Task 1 action.
- All grep gates in Task 1 acceptance_criteria PASS.
- Cross-phase references resolve (the referenced 78-VERIFICATION.md + 62-VERIFICATION.md files exist on disk).
- ROADMAP.md is NOT amended in this plan (user choice per CONTEXT — historical record of reframe stands; the corrected harvest count lives in 84-CONTEXT.md `<data-summary>` only).
</verification>

<success_criteria>
- One new markdown file at the canonical path: `.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-VERIFICATION.md`.
- File implements the D-13 four-part contract: (1) waived-gate statement with rationale; (2) 2-week monitor plan; (3) explicit follow-up trigger thresholds (3 thresholds, verbatim from CONTEXT); (4) SC #3 closes on ship commit (NOT pending monitor).
- Frontmatter shape matches Phase 78 precedent for tooling compatibility (`gsd:verify-work` reads it).
- BUG-09 SC #3 chain (Phase 62 → 78 Commit A → 84) is documented as fully closed.
- No code changes; no other file modified.
</success_criteria>

<output>
After writing 84-VERIFICATION.md, create `.planning/phases/84-bug-09-commit-b-buffer-tuning-behavior-fix-reframed-from-str/84-04-SUMMARY.md` summarizing: VERIFICATION.md filename + line count + section list; status field value (human_needed vs blocked-on-wave-1); BUG-09 SC #3 closure confirmation; pointer to the harvest log path the user will monitor (`~/.local/share/musicstreamer/buffer-events.log`); pointer to the 3 follow-up trigger thresholds.
</output>
