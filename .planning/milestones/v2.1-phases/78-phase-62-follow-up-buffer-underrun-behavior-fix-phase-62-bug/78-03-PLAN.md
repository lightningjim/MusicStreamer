---
phase: 78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug
plan: 03
type: execute
wave: 2
depends_on:
  - 78-02
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
  - musicstreamer/ui_qt/main_window.py
  - tests/test_now_playing_panel.py
  - tests/test_main_window_underrun.py
autonomous: true
requirements:
  - BUG-09
must_haves:
  truths:
    - "D-08: `NowPlayingPanel._build_stats_widget` produces a `QFormLayout` with an `Underruns` row (Commit A part of D-08) positioned AFTER the existing `Buffer` progressbar row and BEFORE `wrapper.setVisible(False)`; the `Buffer config: Xs (adapted)` row is explicitly Commit B work and is NOT added by this plan."
    - "The `Underruns` row has default value text `0` (mirrors Player counter initial state from Plan 78-02)."
    - "Both new row widgets use `_MutedLabel` (Phase 47.1 D-10 theme-flip safety)."
    - "`NowPlayingPanel.set_underrun_count(count: int)` updates `self._underrun_count_label` text to `str(int(count))`."
    - "`MainWindow.__init__` connects `self._player.underrun_count_changed` → `self.now_playing.set_underrun_count` via bound method (QA-05 — no lambda) and DEFAULT connection type (no `Qt.ConnectionType.QueuedConnection` argument — both ends are main-thread)."
    - "Emitting `underrun_count_changed.emit(N)` from a FakePlayer fixture updates the `_underrun_count_label` text to `str(N)` in an integration test."
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "Underruns stats-for-nerds row + set_underrun_count(int) slot"
      contains: "set_underrun_count"
    - path: "musicstreamer/ui_qt/main_window.py"
      provides: "Player.underrun_count_changed → NowPlayingPanel.set_underrun_count wire"
      contains: "underrun_count_changed.connect"
    - path: "tests/test_now_playing_panel.py"
      provides: "Underruns row presence + set_underrun_count slot unit tests (additive)"
      contains: "test_underrun_count_row_present"
    - path: "tests/test_main_window_underrun.py"
      provides: "End-to-end Signal → label integration test (additive)"
      contains: "test_count_changed_updates_stats_row"
  key_links:
    - from: "musicstreamer/ui_qt/main_window.py::MainWindow.__init__"
      to: "musicstreamer/ui_qt/now_playing_panel.py::NowPlayingPanel.set_underrun_count"
      via: "self._player.underrun_count_changed.connect(self.now_playing.set_underrun_count) — bound method, DirectConnection (default)"
      pattern: "underrun_count_changed\\.connect\\(self\\.now_playing\\.set_underrun_count\\)"
    - from: "musicstreamer/ui_qt/now_playing_panel.py::set_underrun_count"
      to: "musicstreamer/ui_qt/now_playing_panel.py::_build_stats_widget (self._underrun_count_label)"
      via: "self._underrun_count_label.setText(str(int(count)))"
      pattern: "_underrun_count_label\\.setText"
---

<objective>
Ship the UI half of Phase 78 Commit A: add an `Underruns` row to `NowPlayingPanel._build_stats_widget` (after the existing `Buffer` row), expose a `set_underrun_count(int)` slot, and wire `Player.underrun_count_changed` → `NowPlayingPanel.set_underrun_count` in `MainWindow.__init__` (sibling to the existing `buffer_percent` wire).

Purpose: make the cumulative cycle count from Plan 78-02 observable live in the stats-for-nerds panel without grepping `~/.local/share/musicstreamer/buffer-events.log`.
Output: 2 small UI edits + 2 small test additions (unit + integration).

This plan depends on Plan 78-02 (Wave 1) because it consumes the new `underrun_count_changed = Signal(int)` declared on Player.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-CONTEXT.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-RESEARCH.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md
@.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-02-PLAN.md
@musicstreamer/player.py
@musicstreamer/ui_qt/main_window.py
@musicstreamer/ui_qt/now_playing_panel.py
@tests/_fake_player.py
@tests/test_now_playing_panel.py
@tests/test_main_window_underrun.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From musicstreamer/player.py (after Plan 78-02):
- `underrun_count_changed = Signal(int)` -- class-level, emitted from `_on_underrun_cycle_closed` on the main thread.
- `self._underrun_event_count: int = 0` -- counter initialized in `__init__`.

From musicstreamer/ui_qt/now_playing_panel.py (existing — read-only context):
- `_MutedLabel` is a project-internal QLabel subclass that re-applies muted palette on theme flips (Phase 47.1 D-10). Constructor: `_MutedLabel(text: str, parent: QWidget)`. Reads same as `QLabel.setText(...)`.
- `_build_stats_widget(self) -> QWidget:` at line 2451-2482 builds a `QWidget` with a `QFormLayout`, a `Buffer` row (label `_MutedLabel("Buffer")` + value `QHBoxLayout(QProgressBar + _MutedLabel("0%"))`), and ends with `wrapper.setVisible(False)`. INSERT POINT: between `form.addRow(buffer_row_label, value_row)` at line 2478 and `wrapper.setVisible(False)` at line 2481.
- `def set_buffer_percent(self, percent: int) -> None:` at line 946-949 is the sibling-slot template: updates `self.buffer_bar.setValue(int(percent))` and `self.buffer_pct_label.setText(f"{int(percent)}%")`. The new `set_underrun_count(int)` slot lands IMMEDIATELY AFTER this method (line ~950) using the same shape.
- `set_stats_visible(self, visible: bool)` at line 951 is the hamburger-toggle wrapper-visibility slot. The new `Underruns` row inherits the wrapper's visibility — NO new visibility code is needed; the toggle that already governs `Buffer` also governs `Underruns`.

From musicstreamer/ui_qt/main_window.py (existing — read-only context):
- Lines 378-393: Player→panel wiring block. Existing pattern: `self._player.buffer_percent.connect(self.now_playing.set_buffer_percent)` at line 381 (DirectConnection / no QueuedConnection / bound method / QA-05). The Phase 62 `underrun_recovery_started.connect(..., Qt.ConnectionType.QueuedConnection)` at lines 391-393 uses QueuedConnection only because that signal originates from various contexts and is defensive — the new `underrun_count_changed` connection sits at the same block but uses the DirectConnection idiom (matching `buffer_percent`).
- INSERT POINT: one line below `self._player.buffer_percent.connect(self.now_playing.set_buffer_percent)` at line 381, ABOVE the toast-notification block at line 383.

From tests/test_main_window_underrun.py (existing test patterns):
- `block_real_network` fixture autouse autouse pattern at lines 32-42 — pulls in `qtbot`, `fake_player`, `fake_repo`, `monkeypatch`, `block_real_network` and constructs `MainWindow(fake_player, fake_repo)` with `qtbot.addWidget(w)`. The new test in this file follows the same fixture pattern.
- `fake_player` fixture: returns a `FakePlayer()` from `tests/_fake_player.py`. After Plan 78-02 Task 2 lands, `fake_player.underrun_count_changed` is callable as `fake_player.underrun_count_changed.emit(3)`.
- `test_main_module_sets_player_logger_to_info` at lines 109-133: source-grep drift-guard for Pitfall 5 — DO NOT MODIFY; must remain green.

From tests/test_now_playing_panel.py (existing test patterns):
- `test_stats_widget_always_constructed(qtbot)` at lines 612-624 is the analog construction test.
- `test_set_buffer_percent_updates_both(qtbot)` at lines 657-663 is the analog `set_X` slot test — `NowPlayingPanel(FakePlayer(), FakeRepo({"volume": "80"}))` constructor pattern.
- The file has a top-level autouse fixture that pulls in `block_real_network`; new tests inherit it without explicit injection.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Underruns row + set_underrun_count slot to NowPlayingPanel + unit tests</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py, tests/test_now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py lines 2451-2482 (full `_build_stats_widget` body — confirm the existing `Buffer` row construction at lines 2460-2478 and the trailing `wrapper.setVisible(False)` at line 2481)
    - musicstreamer/ui_qt/now_playing_panel.py lines 940-960 (existing `set_buffer_percent` slot at lines 946-949 and `set_stats_visible` at line 951 — the new `set_underrun_count` slot lands between them or immediately after `set_buffer_percent`, matching the analog shape)
    - musicstreamer/ui_qt/now_playing_panel.py — also locate the `_MutedLabel` class definition (use grep to find its line) and confirm its constructor signature (text: str, parent: QWidget)
    - tests/test_now_playing_panel.py lines 1-50 (autouse fixtures including `block_real_network`)
    - tests/test_now_playing_panel.py lines 600-680 (existing Phase 47.1 stats-widget tests for shape and assertion style)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md (§ now_playing_panel.py section + §S-4 + § test_now_playing_panel.py section)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-RESEARCH.md (Pattern 3 + Open Question 2 — two-column shape is the recommendation)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md (B-78A-11)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-CONTEXT.md (D-08 + Discretion bullet on stats row ordering)
  </read_first>
  <action>
    Make TWO edits to `musicstreamer/ui_qt/now_playing_panel.py`:

    (1) **Insert new row in `_build_stats_widget`.** Between `form.addRow(buffer_row_label, value_row)` at line 2478 and `wrapper.setVisible(False)` at line 2481, add THREE statements (preceded by a 2-3 line `Phase 78 / BUG-09 Commit A` comment block stating: "two-column shape mirrors the Buffer row above (Open Q2); _MutedLabel preserves theme-flip readability per D-10; the wrapper-level setVisible(False) below applies to BOTH rows — no per-row visibility code needed"):
        - `underrun_row_label = _MutedLabel("Underruns", wrapper)`
        - `self._underrun_count_label = _MutedLabel("0", wrapper)`
        - `form.addRow(underrun_row_label, self._underrun_count_label)`

      The default text `"0"` mirrors the Player counter initial state from Plan 78-02 (`self._underrun_event_count: int = 0`). Per D-08 + the Discretion bullet, place the row AFTER the existing Buffer row and BEFORE the trailing `setVisible(False)`. The label-column-only widget (`self._underrun_count_label`) is a simple `_MutedLabel("0")` — NO `QProgressBar`, NO container `QHBoxLayout`, NO percentage (the `Buffer` row's two-widget value column was only needed for the bar + percent; `Underruns` is a single integer string).

    (2) **Add `set_underrun_count(int)` slot.** Immediately AFTER `set_buffer_percent` at line 946-949 (and BEFORE `set_stats_visible` at line 951), insert a new method:

        - Method signature: `def set_underrun_count(self, count: int) -> None:`
        - Docstring tagged `Phase 78 / BUG-09 Commit A` describing the slot as the receiver for `Player.underrun_count_changed`.
        - Body: a single statement `self._underrun_count_label.setText(str(int(count)))`. The `int()` coercion is defensive (mirrors `set_buffer_percent`'s `int(percent)` pattern).

    Add TWO new test functions to `tests/test_now_playing_panel.py` in the Phase 47.1 stats-section neighborhood (immediately after the existing `test_set_buffer_percent_updates_both` test):

    - `test_underrun_count_row_present(qtbot)` (B-78A-11): construct `panel = NowPlayingPanel(FakePlayer(), FakeRepo({"volume": "80"}))`, `qtbot.addWidget(panel)`, and assert `panel._underrun_count_label.text() == "0"` (the default value text matches Player's `_underrun_event_count: int = 0` initial state). Match the constructor/fixture pattern from the analog `test_stats_widget_always_constructed` / `test_set_buffer_percent_updates_both`.

    - `test_set_underrun_count_updates_label(qtbot)`: same setup, then `panel.set_underrun_count(5)`; assert `panel._underrun_count_label.text() == "5"`. Then `panel.set_underrun_count(0)`; assert `panel._underrun_count_label.text() == "0"` (slot is idempotent on the default value).

    HARD CONSTRAINTS:
    - DO NOT modify the existing `Buffer` row construction (lines 2460-2478) in any way.
    - DO NOT add any new visibility code; the existing `set_stats_visible` and the `wrapper.setVisible(False)` at line 2481 govern both rows.
    - DO NOT use a `QProgressBar` or any composite value widget for the `Underruns` row — single `_MutedLabel` only (Pitfall 5: silent stats row, no toast).
    - DO NOT plan the `Buffer config:` row (Commit B work — D-08 second bullet — explicitly out of scope per CONTEXT.md `<domain>`).
    - DO NOT change `set_buffer_percent` or `set_stats_visible`.
    - DO NOT introduce any new constants, dependencies, or imports unless `_MutedLabel` is somehow not yet in scope (it already is — used at line 2460 / 2473).
  </action>
  <acceptance_criteria>
    - B-78A-11: `uv run pytest tests/test_now_playing_panel.py::test_underrun_count_row_present -x` exits 0.
    - `uv run pytest tests/test_now_playing_panel.py::test_set_underrun_count_updates_label -x` exits 0.
    - `grep -c "self._underrun_count_label" musicstreamer/ui_qt/now_playing_panel.py` returns >= 2 (declaration in `_build_stats_widget` + setText in `set_underrun_count`).
    - `grep -nE "def set_underrun_count\\(self, count: int\\) -> None:" musicstreamer/ui_qt/now_playing_panel.py` returns the slot definition line, located AFTER `def set_buffer_percent` and BEFORE `def set_stats_visible`.
    - `grep -nE "_MutedLabel\\(['\"]Underruns['\"], wrapper\\)" musicstreamer/ui_qt/now_playing_panel.py` returns 1 match (label widget uses `_MutedLabel` per §S-4 / Phase 47.1 D-10).
    - `grep -nE "_MutedLabel\\(['\"]0['\"], wrapper\\)" musicstreamer/ui_qt/now_playing_panel.py` returns 1 match (default value text is `"0"`).
    - Full panel test file still green: `uv run pytest tests/test_now_playing_panel.py -x` exits 0.
    - Phase 47.1 regression check: existing `test_stats_widget_always_constructed` and `test_set_buffer_percent_updates_both` are still green inside that pytest run.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_now_playing_panel.py -x</automated>
  </verify>
  <done>`NowPlayingPanel._build_stats_widget` now contains an `Underruns` row positioned after the `Buffer` row (default value `"0"`). `set_underrun_count(int)` slot updates the label text. Two new test functions pass; existing Phase 47.1 stats tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Player.underrun_count_changed → NowPlayingPanel.set_underrun_count in MainWindow + integration test</name>
  <files>musicstreamer/ui_qt/main_window.py, tests/test_main_window_underrun.py</files>
  <read_first>
    - musicstreamer/ui_qt/main_window.py lines 370-400 (the Player→panel Signal wiring block, especially the existing `self._player.buffer_percent.connect(self.now_playing.set_buffer_percent)` at line 381 and the Phase 62 `underrun_recovery_started.connect(..., Qt.ConnectionType.QueuedConnection)` block at lines 391-393)
    - musicstreamer/ui_qt/now_playing_panel.py (after Task 1 — confirm `set_underrun_count` is the slot symbol)
    - tests/test_main_window_underrun.py (full file — read the existing fixtures and `test_first_call_shows_toast` at lines 32-42 for the integration template; identify the `block_real_network` fixture usage, the `fake_player`/`fake_repo` fixtures, and the Pitfall-5 drift-guard `test_main_module_sets_player_logger_to_info` at lines 109-133 which MUST remain unmodified)
    - tests/_fake_player.py (after Plan 78-02 Task 2 — confirm `underrun_count_changed = Signal(int)` is on FakePlayer for the integration test to emit)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-PATTERNS.md (§ main_window.py section + §S-3 + § test_main_window_underrun.py section)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-RESEARCH.md (Pattern 2 + Anti-Pattern "Adding underrun_count_changed as a queued connection" + Assumption A3)
    - .planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-VALIDATION.md (B-78A-12)
    - .claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md (Pitfall 2 — DirectConnection is correct here because both emitter (`_on_underrun_cycle_closed` slot — main-thread) and receiver (`NowPlayingPanel.set_underrun_count` — main-thread by QWidget convention) are on the main thread; document this explicitly in the inline comment to dispel "be safe, add QueuedConnection" overreach)
  </read_first>
  <action>
    Make ONE edit to `musicstreamer/ui_qt/main_window.py`: immediately AFTER `self._player.buffer_percent.connect(self.now_playing.set_buffer_percent)` at line 381 and BEFORE the empty line that precedes the toast-notification block at line 383, insert ONE new connect line preceded by a 2-3 line inline comment:

    - Comment lines (tagged `Phase 78 / BUG-09 Commit A`) must state: (a) bound method per QA-05 / §S-3 (no lambda), (b) DirectConnection (default — no `Qt.ConnectionType.QueuedConnection` argument) is correct because emit happens from the main-thread slot `Player._on_underrun_cycle_closed` and receiver `NowPlayingPanel.set_underrun_count` is a `QWidget` slot also on the main thread (Pitfall 2 satisfied; RESEARCH Assumption A3 verified), and (c) this differs from the Phase 62 `underrun_recovery_started` connection 3 lines below which uses QueuedConnection defensively for unrelated reasons.
    - Wire line: `self._player.underrun_count_changed.connect(self.now_playing.set_underrun_count)`.

    Make ONE edit to `tests/test_main_window_underrun.py`: add a new test function `test_count_changed_updates_stats_row` immediately AFTER `test_first_call_shows_toast` at line 42 (and before the next test or the drift-guard at line 109). The test pattern mirrors `test_first_call_shows_toast`:

    - Signature: `def test_count_changed_updates_stats_row(qtbot, fake_player, fake_repo, block_real_network):`
    - Docstring tagged `Phase 78 / BUG-09 Commit A` describing the end-to-end contract: `MainWindow` wires `Player.underrun_count_changed` → `NowPlayingPanel.set_underrun_count` one line below the `buffer_percent` connection at `main_window.py:381`; emitting the FakePlayer signal mimics the post-cycle-close emit from Player.
    - Body:
        - `w = MainWindow(fake_player, fake_repo)`
        - `qtbot.addWidget(w)`
        - `fake_player.underrun_count_changed.emit(3)`
        - `qtbot.wait(50)` (DirectConnection is synchronous, but defensive)
        - `assert w.now_playing._underrun_count_label.text() == "3"`
        - `fake_player.underrun_count_changed.emit(7)`
        - `qtbot.wait(50)`
        - `assert w.now_playing._underrun_count_label.text() == "7"`

    HARD CONSTRAINTS:
    - DO NOT add `Qt.ConnectionType.QueuedConnection` to the new wire (RESEARCH Anti-Pattern + Assumption A3).
    - DO NOT use a lambda or `functools.partial` in `.connect(...)` (QA-05 / §S-3 — bound method only).
    - DO NOT touch `tests/test_main_window_underrun.py::test_main_module_sets_player_logger_to_info` (Pitfall 5 drift-guard at lines 109-133 — MUST remain unchanged and green).
    - DO NOT modify the Phase 62 `underrun_recovery_started` connection at lines 391-393 (its QueuedConnection is correct for its own reasons; do not "harmonize" the two wires).
    - DO NOT add any new fixtures or modify the `fake_player`/`fake_repo`/`block_real_network` fixtures.
  </action>
  <acceptance_criteria>
    - B-78A-12: `uv run pytest tests/test_main_window_underrun.py::test_count_changed_updates_stats_row -x` exits 0.
    - `grep -c "underrun_count_changed.connect" musicstreamer/ui_qt/main_window.py` returns exactly 1.
    - `grep -nE "self\\._player\\.underrun_count_changed\\.connect\\(self\\.now_playing\\.set_underrun_count\\)" musicstreamer/ui_qt/main_window.py` returns one line, BELOW the existing `buffer_percent.connect` line at line 381.
    - `grep -nE "underrun_count_changed.*Qt\\.ConnectionType\\.QueuedConnection" musicstreamer/ui_qt/main_window.py` returns 0 matches (DirectConnection lock; RESEARCH Anti-Pattern).
    - `grep -nE "underrun_count_changed.*lambda" musicstreamer/ui_qt/main_window.py` returns 0 matches (QA-05 / §S-3 — no lambdas).
    - Pitfall-5 drift-guard still green: `uv run pytest tests/test_main_window_underrun.py::test_main_module_sets_player_logger_to_info -x` exits 0.
    - Full integration file green: `uv run pytest tests/test_main_window_underrun.py -x` exits 0.
    - Existing Phase 62 integration tests (`test_first_call_shows_toast` etc.) still green within the same pytest run.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_main_window_underrun.py -x</automated>
  </verify>
  <done>`MainWindow.__init__` wires `Player.underrun_count_changed` → `NowPlayingPanel.set_underrun_count` via a single bound-method DirectConnection sibling to the `buffer_percent` wire. The integration test `test_count_changed_updates_stats_row` emits via FakePlayer and asserts the `_underrun_count_label` text updates end-to-end. Pitfall-5 drift-guard remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player main thread → MainWindow main thread (new wire) | `underrun_count_changed = Signal(int)` emit site is `Player._on_underrun_cycle_closed` (main-thread slot — Pitfall 2 confirmed). Receiver `NowPlayingPanel.set_underrun_count` is a `QWidget` method on the main thread by Qt convention. DirectConnection is correct. |
| count value → label text | `set_underrun_count` coerces via `str(int(count))` — defensive against accidental float/string inputs in tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78A-Pitfall2-NewWire | Tampering (threading) | `underrun_count_changed.connect(...)` | mitigate | Both emitter and receiver on main thread (RESEARCH Assumption A3, source-verified). DirectConnection is the project-idiomatic default for main→main wires (mirrors `buffer_percent.connect(...)`). Inline comment documents the thread-affinity decision explicitly to dispel "be safe, add QueuedConnection" overreach. |
| T-78A-Toast-Regress | Tampering (UX regression) | NowPlayingPanel row visibility | mitigate | Plan 78-03 does NOT add any new toast paths (RESEARCH Pitfall 5 — silent stats row). Existing `Buffering…` toast governance from Phase 62 is untouched. |
| T-78A-Pitfall5-Regress | Tampering (logging config regression) | `__main__.py` | mitigate | Plan 78-03 does NOT modify `__main__.py`. Drift-guard `test_main_module_sets_player_logger_to_info` is exercised as part of Task 2's verification command. |
| T-78A-QA05 | Repudiation (debug-traceability regression) | new connect site | mitigate | Bound-method connect per §S-3 / QA-05 — no lambda. Grep gate in acceptance criteria. |
</threat_model>

<verification>
Run the per-task `<verify>` commands AND the plan-level integration:

```
uv run pytest tests/test_now_playing_panel.py tests/test_main_window_underrun.py tests/test_fake_player_signal_parity.py -q
```

Then run the Phase 78 wave-merge full-suite gate (after Plans 78-01, 78-02, and 78-03 all land):

```
uv run pytest tests/ -q
```

Expected: 0 failures across the full suite. The new wire + UI row + integration test add no regression to Phase 62 / Phase 47.1 / Phase 77 INFRA-01 / Phase 79 BUG-11 tests.
</verification>

<success_criteria>
- B-78A-11 (UI row presence) covered by `tests/test_now_playing_panel.py::test_underrun_count_row_present`.
- B-78A-12 (end-to-end Signal → label) covered by `tests/test_main_window_underrun.py::test_count_changed_updates_stats_row`.
- `NowPlayingPanel._build_stats_widget` has an `Underruns` row positioned after the `Buffer` row.
- `NowPlayingPanel.set_underrun_count(int)` slot exists and updates `self._underrun_count_label`.
- `MainWindow.__init__` connects `Player.underrun_count_changed` → `NowPlayingPanel.set_underrun_count` via bound method, DirectConnection.
- Pitfall-5 drift-guard `test_main_module_sets_player_logger_to_info` remains green.
- Full Phase 78 suite green after wave-merge: `uv run pytest tests/test_buffer_events_log.py tests/test_player_underrun_count.py tests/test_paths.py tests/test_now_playing_panel.py tests/test_main_window_underrun.py tests/test_fake_player_signal_parity.py -q` exits 0.
</success_criteria>

<output>
Create `.planning/phases/78-phase-62-follow-up-buffer-underrun-behavior-fix-phase-62-bug/78-03-SUMMARY.md` when done.
</output>
