---
phase: 72.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_phase72_1_stream_picker_reflow.py
autonomous: true
requirements: [LAYOUT-02]
tags: [layout, pyside6, ui-qt, tdd-red, scaffold, layout-02]
must_haves:
  truths:
    - "tests/test_phase72_1_stream_picker_reflow.py exists with all 10 sample-point/negative tests imported and collected by pytest."
    - "Every test in the new file fails on collection or assertion (TDD-RED) — none can pass before Plan 02/03 land production code."
    - "REQUIREMENTS.md ### Layout (LAYOUT) section gains a LAYOUT-02 row mirroring the Phase 79 / BUG-11 fresh-code precedent."
    - "REQUIREMENTS.md Traceability table gains a row mapping LAYOUT-02 → Phase 72.1, status Pending."
    - "REQUIREMENTS.md coverage rollup numbers update from 58/58 to reflect the new requirement (rolling polish milestone)."
    - "Test fixtures FakePlayer + FakeRepo + _make_panel + _make_multi_stream_station are present in the new test file, ready for Plan 02/03 to call."
    - "Decisions reflected in the test scaffold (test names encode the contract for each): D-01: width-responsive trigger (tests 1+2 sweep wide→narrow). D-02: single-stream stays one-row (tests 3+4). D-03: only picker moves (every test asserts other widgets remain in self.controls). D-04: no FlowLayout (no FlowLayout import permitted in scaffold). D-05: reparent on resize (tests resize the panel, not call internal helpers). D-06: instant reparent (no qtbot.wait, no animation poll). D-07: picker Expanding in row 2 (test 2 asserts horizontalPolicy). D-08: picker Preferred + 140 min in row 1 (test 1 asserts width fits the 140 floor)."
  artifacts:
    - path: "tests/test_phase72_1_stream_picker_reflow.py"
      provides: "TDD-RED scaffold covering LAYOUT-02 sample points (a)-(h) + 2 negative-assertion tests"
      contains: "def test_wide_multi_stream_picker_in_row_1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAYOUT-02 requirement row + traceability row"
      contains: "LAYOUT-02"
  key_links:
    - from: "tests/test_phase72_1_stream_picker_reflow.py"
      to: "musicstreamer/ui_qt/now_playing_panel.py"
      via: "imports NowPlayingPanel; references panel.controls and panel._controls_row2 (both will exist after Plan 02)"
      pattern: "from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel"
    - from: ".planning/REQUIREMENTS.md (LAYOUT-02 row)"
      to: ".planning/REQUIREMENTS.md (Traceability table row)"
      via: "REQ-ID string match"
      pattern: "LAYOUT-02"
---

<objective>
Wave 0 TDD-RED scaffold for Phase 72.1 (multi-stream picker reflow). Lands two artifacts:

1. **REQUIREMENTS.md update** — register `LAYOUT-02` as a fresh requirement code in the `### Layout (LAYOUT)` section (mirrors Phase 79 / BUG-11 follow-up-phase precedent; see 72.1-RESEARCH §Open Question 1 recommendation, finalized here). LAYOUT-01 stays owned by Phase 72; LAYOUT-02 owns the multi-stream overlap closure.
2. **Test file scaffold** — `tests/test_phase72_1_stream_picker_reflow.py` with all 10 tests (8 sample points + 2 negative assertions) in TDD-RED state. Includes copied FakePlayer / FakeRepo / _make_panel from `tests/test_phase72_now_playing_panel.py:34-102` plus a new `_make_multi_stream_station(n: int = 3)` helper that constructs a Station with N StationStream rows wired through FakeRepo.list_streams.

Purpose: Lock the contract that Plan 02 (core reflow) and Plan 03 (extension hooks + negatives) will implement against. Every test must FAIL initially — running `pytest tests/test_phase72_1_stream_picker_reflow.py -x` exits non-zero. This is the RED state; it becomes GREEN as Plans 02 and 03 land. Per Phase 72 Lessons (`72-LEARNINGS.md` §"pytest-qt synthetic events can mask receiver-identity bugs"), tests use direct `panel.resize(W, H)` / `qtbot.waitExposed(panel)` rather than synthetic QResizeEvent injection.

Output: REQUIREMENTS.md gains LAYOUT-02 (rolling polish milestone — coverage line updates). Test file exists with 10 RED tests. Plan 02 can begin immediately upon Plan 01 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-CONTEXT.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-PATTERNS.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-VALIDATION.md
@.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md

<interfaces>
<!-- Key types and fixtures the executor will reuse verbatim from Phase 72 tests. -->
<!-- Do NOT re-derive — copy the established shapes. -->

From musicstreamer/models.py:
- @dataclass Station(id: int, name: str, provider_id: Optional[int], provider_name: Optional[str], tags: str, station_art_path: Optional[str], album_fallback_path: Optional[str], icy_disabled: bool = False, cover_art_source: Literal["auto","itunes_only","mb_only"] = "auto", streams: List[StationStream] = [], last_played_at: Optional[str] = None, is_favorite: bool = False)
- @dataclass StationStream(id: int, station_id: int, url: str, label: str = "", quality: str = "", position: int = 1, stream_type: str = "", codec: str = "", bitrate_kbps: int = 0, sample_rate_hz: int = 0, bit_depth: int = 0)

From musicstreamer/ui_qt/now_playing_panel.py (lines 215, 283):
- class NowPlayingPanel(QWidget)
- def __init__(self, player, repo, parent: Optional[QWidget] = None) -> None
- def bind_station(self, station: Station) -> None  # at line 767; calls _populate_stream_picker(station)
- def _populate_stream_picker(self, station) -> None  # at line 1114; calls self._repo.list_streams(station.id), populates combo, sets visibility per len(streams) > 1
- self.stream_combo: QComboBox  # constructed at line 484 with setMinimumWidth(140), setVisible(False), currentIndexChanged.connect(self._on_stream_selected), controls.addWidget(self.stream_combo) at line 488
- self.controls (local in __init__): QHBoxLayout  # row 1, constructed at line 440, setSpacing(8) at line 441
- # Plan 02 will add: self._controls_row2 (QHBoxLayout, sibling under self.center)
- # Plan 02 will add: def resizeEvent(self, event) override that calls self._reflow_stream_picker_row()
- # Plan 02 will add: def _reflow_stream_picker_row(self) -> None
- # Plan 02 will add: self.controls exposed as instance attribute (currently local)

From tests/test_phase72_now_playing_panel.py:34-102 (verbatim source of FakePlayer/FakeRepo/_make_panel):
- class FakePlayer(QObject) with signals title_changed, failover, offline, playback_error, elapsed_updated, buffer_percent and methods set_volume, stop, pause, play, set_eq_enabled
- class FakeRepo with get_setting/set_setting/is_favorited/add_favorite/remove_favorite/all_stations — needs extension for list_streams (returns Phase 72.1's per-station stream list) and list_streams_lookup dict
- def _make_panel(qtbot, settings: Optional[dict] = None) -> NowPlayingPanel

From tests/test_main_window_integration.py:197:
- def _make_station(name="Test Station", provider="TestFM") -> Station  # returns Station with streams=[]; tests that need streams should extend
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register LAYOUT-02 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — confirm current state of ### Layout section, Traceability table, and Coverage rollup line)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (§Open Question 1 — confirms fresh-code recommendation matching Phase 79 / BUG-11 precedent)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-CONTEXT.md (§canonical_refs — confirms planner may register LAYOUT-02 as new code)
  </read_first>
  <action>
Add three edits to `.planning/REQUIREMENTS.md`:

1. **Insert a new section `### Layout (LAYOUT)`** if not already present. The current file does not include a `### Layout` section (Phase 72 closed BUG-09 era; LAYOUT-01 was not added retroactively). Add the section header after `### Sibling Stations (SIB)` and before `### Cover-Art Sources (ART-MB)`. Use this section body (verbatim — match the surrounding section style with bold `**LAYOUT-NN**` prefix, italic phase reference, plain prose; mirror SIB-01's voice):

   ```
   ### Layout (LAYOUT)

   Width-responsive control-row layout follow-ups (Phase 72 era).

   - [x] **LAYOUT-01**: Compact-mode toggle hides the left column on small/secondary displays; session-only, no SQLite persistence; activated by `Ctrl+B` shortcut and a `compact_mode_toggle_btn` QToolButton on the now-playing pane; left-edge hover-peek overlay survives compact mode. *(Phase 72)*
   - [ ] **LAYOUT-02**: When the now-playing panel narrows below the threshold where row 1 with the stream picker can fit AND the active station has multiple streams, `stream_combo` reparents into a dedicated second row beneath the existing controls row; it returns to row 1 when width allows. Single-stream stations stay one-row at all widths. Picker signals (`currentIndexChanged.connect(self._on_stream_selected)`), model (item text + userData), and `currentIndex` survive each reparent. Trigger is width, not compact-mode (D-01). No SQLite persistence (D-09 inheritance from Phase 72). *(Phase 72.1)*
   ```

   Rationale for fresh code (cite in commit message): Phase 79 / BUG-11 set the precedent that follow-up phases get a fresh code, not an annotation under the parent — keeps Traceability table one-row-per-phase.

2. **Add Traceability rows.** In the `## Traceability` table, after the `SIB-01 | Phase 71 | Complete` row, insert TWO rows (preserve the existing alphabetical-by-section grouping):

   ```
   | LAYOUT-01 | Phase 72 | Complete |
   | LAYOUT-02 | Phase 72.1 | Pending |
   ```

3. **Update the Coverage rollup.** The current line at file bottom reads `v2.1 requirements: 58 total` / `Mapped to phases: 58 ✓` / `Complete: 20` / `Pending: 38 ...`. Update to:
   - `v2.1 requirements: 60 total` (added LAYOUT-01 + LAYOUT-02)
   - `Mapped to phases: 60 ✓`
   - `Complete: 21` (LAYOUT-01 is shipped in Phase 72)
   - `Pending: 39` — append `; LAYOUT-02 — Phase 72.1 stream-picker reflow` to the pending parenthetical list.

   Update the trailing `Pending:` parenthetical so the new entry reads consistently with sibling entries (e.g., `LAYOUT-02 — Phase 72.1 multi-stream picker reflow`).

4. **Add a Last-updated footer line** at the very bottom (mirror existing format used after BUG-11 entry):

   ```
   *Last updated: 2026-05-16 — LAYOUT-01 (Phase 72 compact-mode toggle, marked Complete) + LAYOUT-02 (Phase 72.1 multi-stream stream-picker reflow into row 2 when narrow) added during `/gsd:plan-phase 72.1`. Fresh-code precedent matches Phase 79 / BUG-11 (follow-up phases get their own code, not annotations under the parent).*
   ```

Do NOT remove or reorder any existing rows. Do NOT touch the Future Requirements or Out of Scope sections.
  </action>
  <verify>
    <automated>grep -c "^### Layout (LAYOUT)$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Layout (LAYOUT)$" .planning/REQUIREMENTS.md` == 1
    - `grep -c "^- \[ \] \*\*LAYOUT-02\*\*" .planning/REQUIREMENTS.md` == 1
    - `grep -c "^- \[x\] \*\*LAYOUT-01\*\*" .planning/REQUIREMENTS.md` == 1
    - `grep -c "| LAYOUT-02 | Phase 72.1 | Pending |" .planning/REQUIREMENTS.md` == 1
    - `grep -c "| LAYOUT-01 | Phase 72 | Complete |" .planning/REQUIREMENTS.md` == 1
    - `grep -c "^v2.1 requirements: 60 total$" .planning/REQUIREMENTS.md` >= 1 OR `grep "60 total" .planning/REQUIREMENTS.md` matches Coverage line
    - `grep -c "Last updated: 2026-05-16 — LAYOUT-01.*LAYOUT-02" .planning/REQUIREMENTS.md` >= 1
    - No existing rows removed (file line count grows by ~10-13 lines from the baseline pre-edit; verify via `git diff --stat .planning/REQUIREMENTS.md` shows only additions)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md contains the LAYOUT section with LAYOUT-01 (complete) + LAYOUT-02 (pending), two new Traceability rows, updated coverage numbers, and a Last-updated footer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TDD-RED scaffold tests/test_phase72_1_stream_picker_reflow.py</name>
  <files>tests/test_phase72_1_stream_picker_reflow.py</files>
  <read_first>
    - tests/test_phase72_now_playing_panel.py (lines 1-110 — copy FakePlayer, FakeRepo, _make_panel verbatim; lines 130-179 — layout-walk pattern used by Pattern H in 72.1-PATTERNS.md; lines 290-307 — no-lambda inspect.getsource grep test pattern)
    - tests/test_phase72_compact_toggle.py (lines 95-230 — direct `window.resize(W, H)` round-trip pattern; no `qtbot.resize` / `qtbot.wait` per Pattern I in 72.1-PATTERNS.md)
    - tests/test_phase72_integration.py (lines 75-90 — `qtbot.waitExposed(w)` fixture pattern; mirror for panel-level fixture)
    - tests/test_main_window_integration.py (lines 87-209 — FakeRepo + _make_station; note `list_streams` returns [] by default and must be parameterized for multi-stream tests)
    - musicstreamer/ui_qt/now_playing_panel.py (lines 1114-1135 — _populate_stream_picker calls `self._repo.list_streams(station.id)`; lines 484-488 — stream_combo construction; line 215 — class signature; line 767 — bind_station signature)
    - musicstreamer/models.py (Station + StationStream dataclass signatures — confirm field order for constructor calls)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-VALIDATION.md (Per-Task Verification Map — names the 10 test functions verbatim; do NOT rename)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-RESEARCH.md (§Validation Architecture lines 720-734 — Per-Test Map confirms test-function names verbatim; §Code Examples lines 600-653 — layout-membership assertion helper + round-trip test template)
    - .planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-UI-SPEC.md (§State Truth Table — the four-combination matrix the eight sample-point tests assert)
  </read_first>
  <action>
Create `tests/test_phase72_1_stream_picker_reflow.py` (new file, ~280-320 lines). Structure:

**File header docstring** — describe scope (Phase 72.1 / LAYOUT-02 — stream-picker reflow between row 1 / row 2), TDD-RED state at landing (Wave 0; Plans 02/03 turn each test GREEN), and the test inventory matching `72.1-VALIDATION.md` Per-Task Verification Map verbatim.

**Imports** (ordered per `CONVENTIONS.md`):
- `from __future__ import annotations`
- stdlib: `inspect`, `typing.Any`, `typing.List`, `typing.Optional`
- third-party: `pytest`, `from PySide6.QtCore import QObject, QSize, Signal`, `from PySide6.QtWidgets import QHBoxLayout, QSizePolicy, QToolButton, QWidget`
- project: `from musicstreamer.ui_qt import icons_rc  # noqa: F401`, `from musicstreamer.ui_qt.now_playing_panel import NowPlayingPanel`, `from musicstreamer.models import Station, StationStream`

**Test doubles — copy verbatim from tests/test_phase72_now_playing_panel.py:34-102:**
- `class FakePlayer(QObject)` with the six signals + the six methods listed in <interfaces>. Do NOT modify.
- `class FakeRepo` with the methods at lines 68-96 of the analog, PLUS:
  - extend `__init__` to accept `streams_by_station: Optional[dict[int, list]] = None` and store as `self._streams_by_station = streams_by_station or {}`
  - add `def list_streams(self, station_id: int) -> list: return list(self._streams_by_station.get(station_id, []))`
  - this is the SOLE addition; do NOT touch the existing surface
- `def _make_panel(qtbot, settings: Optional[dict] = None, streams_by_station: Optional[dict] = None) -> NowPlayingPanel`: construct FakePlayer + FakeRepo (with streams_by_station kw passed through), `qtbot.addWidget(panel)`, return panel. Mirrors test_phase72_now_playing_panel.py:99-102 with one new kw.
- `def _make_multi_stream_station(stream_count: int = 3, station_id: int = 1, name: str = "Multi-Stream Test") -> Station`: return a Station with `streams=[StationStream(id=i+1, station_id=station_id, url=f"http://example.test/{i+1}", quality=f"q{i}", position=i+1) for i in range(stream_count)]`. All other Station fields use defaults / None. The returned `station.streams` list AND the streams list passed via `streams_by_station={1: ...}` to `_make_panel` must agree (FakeRepo.list_streams is what `_populate_stream_picker` consumes).
- `def _make_single_stream_station(station_id: int = 1, name: str = "Single-Stream Test") -> Station`: same shape but `stream_count=1`.

**Module-level helper — layout-walk assertion (mirrors 72.1-RESEARCH §Code Examples lines 600-616 + Pattern H from PATTERNS.md):**
- `def _assert_stream_combo_in_layout(panel: NowPlayingPanel, expected_layout: QHBoxLayout, msg: str = "") -> None`: assert `expected_layout.indexOf(panel.stream_combo) >= 0` with a clear failure message; cross-check `panel.stream_combo.parent() is panel`. Use this in every sample-point test.

**No `panel_shown` fixture** — per PATTERNS.md Pattern I (`tests/test_phase72_compact_toggle.py:206-215`), the established repo convention for resize tests is direct `panel.resize(W, H)` followed by an immediate assertion. `qtbot.waitExposed` is NOT used anywhere in the test suite (verified by grep) and is forbidden by PATTERNS.md Pattern I as a first-in-codebase pattern this phase MUST NOT introduce. Qt delivers `resizeEvent` to non-exposed widgets in offscreen pytest-qt runs (verified by Phase 72's existing test suite, which uses the same pattern). If any test in this scaffold turns out to be flaky from missing exposure, that is a fixture concern handled in Plan 03 Task 2 Step 2 — do NOT pre-emptively add a `panel_shown` fixture.

**Ten test functions — ALL must be RED in Wave 0 (no production code exists yet to make them pass).** Names taken verbatim from `72.1-VALIDATION.md` Per-Task Verification Map — DO NOT rename:

1. `def test_wide_multi_stream_picker_in_row_1(qtbot)` — sample (a): build panel via `_make_multi_stream_station(3)`, bind, `panel.resize(1200, 800)`, assert picker in `panel.controls` via `_assert_stream_combo_in_layout`. Also assert `panel.stream_combo.isVisible()` True.

2. `def test_narrow_multi_stream_picker_in_row_2(qtbot)` — sample (b): same setup, `panel.resize(560, 800)`, assert picker in `panel._controls_row2`. Also assert `panel.stream_combo.isVisible()` True and `panel.stream_combo.sizePolicy().horizontalPolicy() == QSizePolicy.Expanding`.

3. `def test_wide_single_stream_picker_hidden_in_row_1(qtbot)` — sample (c): build with single-stream station, `panel.resize(1200, 800)`, assert picker is in `panel.controls` AND `panel.stream_combo.isVisible() is False`.

4. `def test_narrow_single_stream_picker_hidden_in_row_1(qtbot)` — sample (d): same single-stream setup, `panel.resize(560, 800)`, assert picker is in `panel.controls` (NOT row 2 — D-02 says single-stream stays one-row) AND `panel.stream_combo.isVisible() is False`.

5. `def test_signal_survives_round_trip(qtbot)` — sample (e): build multi-stream panel, wide → assert row 1 + `currentIndexChanged` fires correctly; narrow → assert row 2 + same signal still fires; wide again → assert back to row 1 + signal still fires. Use the pattern from 72.1-RESEARCH §Code Examples lines 627-653 verbatim (capture `fired = []`, connect a `fired.append` lambda — note this is a TEST file lambda, NOT a production lambda; QA-05 applies to production connects only).

6. `def test_multi_to_single_mid_narrow_rehomes_picker(qtbot)` — sample (f): build multi-stream panel, narrow → assert row 2; then call `panel._populate_stream_picker(_make_single_stream_station())` (or `panel.bind_station(single_station)` — pick whichever exercises the extension hook Plan 03 will add); assert picker is now back in `panel.controls` AND `panel.stream_combo.isVisible() is False`.

7. `def test_compact_mode_and_picker_wrap_independent(qtbot)` — sample (g): mount panel inside a MainWindow (import `from musicstreamer.ui_qt.main_window import MainWindow` and `from tests.test_main_window_integration import FakePlayer as MWFakePlayer, FakeRepo as MWFakeRepo, _make_station as mw_make_station`; pre-load FakeRepo with a multi-stream Station). Toggle compact (click `window.now_playing.compact_mode_toggle_btn`), resize narrow, assert picker in row 2. Toggle compact off, assert picker still in row 2 (compact toggle is orthogonal to width-driven reparent). NOTE: This test imports the MainWindow integration fixtures — Phase 72.1 leans on Phase 72's compact toggle which is panel-aware via MainWindow wiring.

8. `def test_palette_survives_reparent(qtbot)` — sample (h): build multi-stream panel, snapshot `panel.stream_combo.palette()` color roles at wide, resize narrow, snapshot again, assert color roles `QPalette.Window`, `QPalette.WindowText`, `QPalette.Button`, `QPalette.ButtonText`, `QPalette.Text` are identical (Qt palette inheritance survives setParent per 72.1-RESEARCH §Pattern 2 + Qt docs).

9. `def test_no_repo_set_setting_for_wrap_state(qtbot)` — negative (1) — mirrors `test_phase72_compact_toggle.py:149-165` pattern. Snapshot `fake_repo._settings` keys before construct + resize cycles; assert no key containing `"wrap"`, `"row2"`, `"picker_row"`, `"picker_position"`, or `"reflow"` (case-insensitive) was ever written. Locks D-09 inheritance from Phase 72.

10. `def test_no_lambda_in_reflow_wiring()` — negative (2) — QA-05 lint via `inspect.getsource(NowPlayingPanel)`. Mirror the pattern at `test_phase72_now_playing_panel.py:290-306` lines verbatim. For each line containing `_reflow_stream_picker_row` OR `resizeEvent` OR `_move_stream_picker_to`, assert `"lambda"` is not in the line. Source-grep style — runs without instantiating widgets.

**Critical RED-state behavior:**

Tests 1-8 will fail with `AttributeError: 'NowPlayingPanel' object has no attribute '_controls_row2'` (or similar) — this IS the RED state. Test 9 will likely PASS by accident (no production code is writing any settings yet) — that's fine, it remains a regression net. Test 10 will likely PASS by accident (no `_reflow_stream_picker_row` exists yet to contain a lambda) — that's also fine. Do NOT replace those two with `pytest.fail("Wave 0 stub")` placeholders — they encode the contract for Plans 02/03 and become permanent regression locks.

Tests 1-8 should use `panel.controls` (not the local `controls` variable). Plan 02 MUST therefore expose `self.controls = controls` after constructing the row 1 layout (i.e., promote the local to an instance attribute). Document that expectation inline via a comment near test 1: `# Plan 02 promotes the local 'controls' to self.controls so tests can address it`.

Do NOT add any production-side imports beyond NowPlayingPanel, icons_rc, Station, StationStream. Do NOT define `_controls_row2` or `_reflow_stream_picker_row` — those are Plan 02's deliverables.

**File-level docstring after imports — pin the contract:**
```
# Phase 72.1 / LAYOUT-02 — Wave 0 TDD-RED scaffold
#
# All 10 test functions below MUST FAIL on Plan 01 commit (Wave 0) and turn
# GREEN as Plans 02/03 land production code. The two negative-assertion tests
# (test_no_repo_set_setting_for_wrap_state, test_no_lambda_in_reflow_wiring)
# may pass by accident pre-Plan-02 because no production code exists to trip
# them; they remain in place as permanent regression locks.
#
# Test names match 72.1-VALIDATION.md Per-Task Verification Map verbatim —
# do NOT rename without updating the Validation Map alongside.
```
  </action>
  <verify>
    <automated>pytest tests/test_phase72_1_stream_picker_reflow.py --collect-only -q 2>&1 | grep -c "test_phase72_1_stream_picker_reflow.py::test_"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_phase72_1_stream_picker_reflow.py` exists
    - `pytest tests/test_phase72_1_stream_picker_reflow.py --collect-only -q 2>&1 | grep -c "test_"` >= 10 (all 10 tests collected by pytest)
    - `grep -c "^def test_" tests/test_phase72_1_stream_picker_reflow.py` == 10
    - All 10 test names from 72.1-VALIDATION.md present verbatim: `grep -c "def test_wide_multi_stream_picker_in_row_1\|def test_narrow_multi_stream_picker_in_row_2\|def test_wide_single_stream_picker_hidden_in_row_1\|def test_narrow_single_stream_picker_hidden_in_row_1\|def test_signal_survives_round_trip\|def test_multi_to_single_mid_narrow_rehomes_picker\|def test_compact_mode_and_picker_wrap_independent\|def test_palette_survives_reparent\|def test_no_repo_set_setting_for_wrap_state\|def test_no_lambda_in_reflow_wiring" tests/test_phase72_1_stream_picker_reflow.py` == 10
    - `grep -c "class FakePlayer" tests/test_phase72_1_stream_picker_reflow.py` == 1
    - `grep -c "class FakeRepo" tests/test_phase72_1_stream_picker_reflow.py` == 1
    - `grep -c "def _make_panel" tests/test_phase72_1_stream_picker_reflow.py` == 1
    - `grep -c "def _make_multi_stream_station" tests/test_phase72_1_stream_picker_reflow.py` == 1
    - `grep -c "def _assert_stream_combo_in_layout" tests/test_phase72_1_stream_picker_reflow.py` == 1
    - `grep -c "qtbot.resize\|qtbot.wait(0)" tests/test_phase72_1_stream_picker_reflow.py` == 0 (forbidden patterns per Pattern I + 72.1-RESEARCH Pitfall 7)
    - `pytest tests/test_phase72_1_stream_picker_reflow.py -x` exits non-zero (RED state — at least tests 1-8 must fail; collection itself must succeed)
    - Test 9 (`test_no_repo_set_setting_for_wrap_state`) and test 10 (`test_no_lambda_in_reflow_wiring`) may pass — that's accepted; they encode invariants for Plans 02/03
  </acceptance_criteria>
  <done>
    Test file lands with 10 properly-named test functions (RED), reusable FakePlayer/FakeRepo/_make_panel/_make_multi_stream_station/_make_single_stream_station helpers, and the `_assert_stream_combo_in_layout` walker. No `panel_shown` fixture (per Pattern I — direct `panel.resize(W,H)` is the established convention). Plan 02 can write production code knowing exact test contracts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | Plan 01 lands a test scaffold and a Markdown edit. No new code paths, no new input surface, no network I/O, no persistence writes. |

## STRIDE Threat Register

Per 72.1-RESEARCH §Security Domain (cited as source — terse threat model approved by phase planning context). Layout-only phase; ASVS V2-V13 NOT APPLICABLE.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72.1-01 | Tampering | Untrusted stream metadata rendered as rich text in QComboBox items | accept | NOT INTRODUCED — Phase 72.1 does not change `_populate_stream_picker`'s `addItem(label, userData=s.id)` plain-text rendering (now_playing_panel.py:1133). Existing Qt default treats `label` as plain text. Monitored, not introduced. |
| T-72.1-02 | Tampering | Reflow helper re-entry during resize event handler causing stale state | accept | NOT INTRODUCED in this plan (no production code lands). Plan 02 will mitigate via idempotent `_reflow_stream_picker_row` (Pattern 2 early-return — see 72.1-RESEARCH §Pattern 2). |
| T-72.1-03 | DoS | Resize-event spam from user dragging splitter floods UI thread | accept | NOT INTRODUCED in this plan. Wayland coalesces resize events; per-call cost is ~1µs (cached threshold + comparison + idempotent reparent). Verified by 72.1-RESEARCH §Pattern 5. |

No `high` severity findings — block-on-high gate is trivially satisfied. Plan 01 introduces zero security surface.
</threat_model>

<verification>
**Wave 0 success means:**
1. `pytest tests/test_phase72_1_stream_picker_reflow.py --collect-only -q` collects all 10 tests successfully (no import/syntax errors).
2. `pytest tests/test_phase72_1_stream_picker_reflow.py -x` exits non-zero (RED — at least tests 1-8 fail because `panel._controls_row2` does not exist yet).
3. `grep -c "LAYOUT-02" .planning/REQUIREMENTS.md` >= 4 (section, requirement row, traceability row, footer).
4. Existing test suite untouched: `pytest tests/test_phase72_now_playing_panel.py tests/test_phase72_compact_toggle.py tests/test_phase72_integration.py -x` exit code unchanged from pre-plan baseline (no regressions caused by REQUIREMENTS.md edit or by file creation).

**Sampling:** Per-task acceptance criteria are the verification gates. No additional sampling needed for Wave 0 scaffold.
</verification>

<success_criteria>
- [x] Test file exists at `tests/test_phase72_1_stream_picker_reflow.py` with all 10 test names matching 72.1-VALIDATION.md verbatim.
- [x] Test file IMPORTS clean (`python -c "import tests.test_phase72_1_stream_picker_reflow"` exits 0) — RED state is acceptable, but collection failure is NOT.
- [x] Test file collects clean under pytest.
- [x] At least tests 1-8 fail (RED state — Plan 02 turns them GREEN).
- [x] REQUIREMENTS.md gains `### Layout (LAYOUT)` section with LAYOUT-01 + LAYOUT-02 rows.
- [x] REQUIREMENTS.md Traceability table gains both LAYOUT rows.
- [x] REQUIREMENTS.md coverage rollup numbers updated (58 → 60 total).
- [x] REQUIREMENTS.md gains a 2026-05-16 Last-updated footer line.
- [x] Phase 72 regression suite (`pytest tests/test_phase72_*.py -x`) unchanged from pre-plan baseline.
- [x] No production code edits (`git diff musicstreamer/` empty).
</success_criteria>

<output>
Create `.planning/phases/72.1-stream-selector-dropdown-overlap-persists-in-fullscreen-mode/72.1-01-SUMMARY.md` when done.
</output>
</content>
</invoke>