---
phase: 69
plan: 02
type: execute
wave: 2
depends_on:
  - 69-01
files_modified:
  - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md
autonomous: false
requirements:
  - WIN-05
tags:
  - windows
  - uat
  - aac
  - operator-driven
  - vm
must_haves:
  truths:
    - "Operator captures pre-fix baseline on Win11 VM: both fixture URLs (DI.fm AAC tier + SomaFM HE-AAC) FAIL on the current installer build (R-03 — pre-fix repro REQUIRED before applying the fix). Failure is logged with the exact observed toast text from `_on_playback_error`."
    - "Operator recreates the `musicstreamer-build` conda env from the updated Plan-01 README recipe (or runs `conda env update` to add the missing packages), confirms `conda list -n musicstreamer-build | findstr gst-` shows `gst-libav`, `gst-plugins-base`, `gst-plugins-good`, `gst-plugins-bad`, `gst-plugins-ugly`, and `gstreamer` all at 1.28.x (RESEARCH Pitfall 2 — env↔README axis must be verified manually)."
    - "Operator runs `.\\build.ps1` and observes BUILD_OK with the new step 4b emitting `POST-BUNDLE PLUGIN GUARD OK` before Inno Setup compile (G-01 + G-04 — empirical proof the new exit code 10 path is wired and the bundle contains both required DLLs)."
    - "Operator force-fresh-installs the new installer per Phase 56 D-08 sequence: uninstall existing MusicStreamer → delete `%LOCALAPPDATA%\\Programs\\MusicStreamer` → delete Start Menu LNK → reinstall with the Run checkbox UNCHECKED (preserves `%APPDATA%\\musicstreamer` user data). UAT-LOG records the install attestation."
    - "Operator plays both fixture URLs from the installed binary launched via Start Menu shortcut. Both stations produce audible AAC playback within 10 seconds; ICY title appears in the panel within 30 seconds; no `Playback error` toast fires."
    - "Operator records a Release-Grade Re-attestation block (analog of `56-05-UAT-LOG.md:102–113`) summarizing that all WIN-05 success criteria are PASS on a release-grade install, and a Phase Completion Decision block selecting `ship-phase`."
    - "Operator-driven optional negative test (G-01 exercise): delete `dist/MusicStreamer/_internal/gst_plugins/gstlibav.dll` after a successful build, re-run `build.ps1`, observe `BUILD_FAIL reason=plugin_missing` with `$LASTEXITCODE = 10`. This validates the guard fails the build loudly. UAT-LOG marks this as an optional Pitfall-2 cross-check; mandatory only if PASS attestation for the happy path is ambiguous."
  artifacts:
    - path: ".planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md"
      provides: "Operator attestation that AAC streams play on Windows post-bundle-fix — single canonical record of WIN-05 closure"
      contains: "WIN-05"
      exports: []
  key_links:
    - from: "69-02-UAT-LOG.md PASS attestation"
      to: ".planning/REQUIREMENTS.md WIN-05 row"
      via: "/gsd-complete-phase flips WIN-05 checkbox from [ ] to [x] and Traceability row Pending → Complete"
      pattern: "WIN-05"
    - from: "Operator's conda env"
      to: "Plan 01's updated packaging/windows/README.md conda recipe"
      via: "`conda env update --file ...` or `conda env remove` + recreate"
      pattern: "musicstreamer-build"
    - from: "build.ps1 step 4b (Plan 01)"
      to: "Bundled gst_plugins/ DLLs"
      via: "Invoke-Native python tools/check_bundle_plugins.py --bundle"
      pattern: "POST-BUNDLE PLUGIN GUARD"
---

<objective>
Drive the operator-led UAT on the Win11 VM that proves AAC streams play end-to-end on the installed binary after the Plan 01 bundle fix lands. This is the SHIP gate for WIN-05 (V-03 — UAT gates ship, not phase verify). The plan is NOT autonomous: every task is operator-driven and Claude orchestrates the checkpoint sequence + UAT-LOG.md scaffolding.

Purpose: The Phase 56 F2 finding ("AAC streams don't play on Win11 VM") will be empirically closed by a single-pass force-fresh-install on the Win11 VM (V-01 + V-02 — single-pass installer-only, Phase 56 D-08 sequence). Per R-03, the operator MUST record a pre-fix FAIL baseline against both fixture URLs before applying the conda env update, so the post-fix PASS is meaningful. The fixture URL slots (R-01) are filled by the operator at the start of this plan from their existing library.

Output: A single `69-02-UAT-LOG.md` operator attestation document, structured per the `56-05-UAT-LOG.md` analog, with PASS/FAIL stanzas for pre-fix baseline, build attestation, post-install playback, and Release-Grade Re-attestation. The Phase Completion Decision block at the end signals `ship-phase`, which `/gsd-verify-work 69` consumes before `/gsd-complete-phase` closes WIN-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 69 inputs (mandatory)
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md

# Plan 01 output (this plan consumes the bundle fix shipped there)
@.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-01-PLAN.md

# Smoking-gun evidence
@.planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md

# Project skill (auto-loaded per CLAUDE.md routing)
@.claude/skills/spike-findings-musicstreamer/SKILL.md

<interfaces>
<!-- The operator-facing fixture slots that this plan's first task will fill verbatim into 69-02-UAT-LOG.md.
     Per R-01, the user supplies these from their existing station library; the executor does NOT invent
     fixture URLs. The slots match the verbatim form RESEARCH lines 685–696 reserved for them. -->

FIXTURE_DI_FM_AAC = <PASTE-DI-FM-AAC-TIER-URL-HERE>     # one DI.fm "medium" or "low" preset (codec=AAC per aa_import.py:128)
FIXTURE_SOMA_HE_AAC = <PASTE-SOMA-FM-HE-AAC-URL-HERE>   # one SomaFM HE-AAC stream (codec_tokens include HE-AAC per playlist_parser.py:34)

<!-- Per Pitfall 6 (RESEARCH lines 557–565), the diagnostic gst-inspect commands in UAT-LOG.md MUST use
     the element-name form (bare `avdec_aac`) NOT the plugin-name form (`--plugin avdec_aac` would always
     report "no such plugin" because avdec_aac is an element inside the libav plugin, not a plugin itself).
     CONTEXT-DG-01 has this wrong; the planner corrects it in this plan's UAT-LOG template. -->

Bundled-registry gst-inspect element queries (correct form):
  & "dist\MusicStreamer\_internal\gst-inspect-1.0.exe" avdec_aac     # element query
  & "dist\MusicStreamer\_internal\gst-inspect-1.0.exe" aacparse       # element query
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator supplies two AAC fixture URLs (R-01) and Claude creates 69-02-UAT-LOG.md scaffold</name>
  <files>.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md (verbatim structural analog — top-of-file metadata header lines 1–12, PASS/FAIL stanza shape lines 14–31, Release-Grade Re-attestation block lines 102–113, Phase Completion Decision block lines 117–130)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md (R-01 — operator supplies URLs; V-01 — single-pass force-fresh-install; R-03 — pre-fix repro REQUIRED)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md ("Fixture URLs" section lines 683–696 — constraint set on the URLs operator picks; Pitfall 6 — corrected gst-inspect command form)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md (`69-UAT-LOG.md` section lines 397–463 — exact stanza structure to instantiate)
    - musicstreamer/aa_import.py:128 (`_CODEC_MAP = {"hi": "MP3", "med": "AAC", "low": "AAC"}` — informational: DI.fm tier mapping for fixture pick)
    - musicstreamer/playlist_parser.py:34 (`_CODEC_TOKENS` includes HE-AAC and AAC — informational: SomaFM codec detection)
  </read_first>
  <action>
1. PAUSE for the operator to supply two AAC fixture URLs in the resume-signal form `FIXTURE_DI_FM_AAC=https://...` and `FIXTURE_SOMA_HE_AAC=https://...` (one URL per line). Constraints on the URLs (per R-01 + RESEARCH "Fixture URLs"): one DI.fm AAC-tier station (codec column shows "AAC" in EditStationDialog — maps to DI.fm "medium" or "low" preset per `musicstreamer/aa_import.py:128`); one SomaFM HE-AAC stream (codec tokens include HE-AAC per `musicstreamer/playlist_parser.py:34`). Both URLs MUST currently fail playback on the Win11 VM's existing pre-fix installer build (otherwise R-03's pre-fix baseline is meaningless).

2. After receiving the two URLs, create `.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md` using the `56-05-UAT-LOG.md` structural analog (lines 1–130) with these specific substitutions:

   a. **Top-of-file metadata header** (analog: 56-05-UAT-LOG.md:1–12):
      - Title: `# Phase 69 — UAT Log`
      - `**Started:** <date when operator confirms — leave as `<pending — operator fills at §A start>` until Task 2`
      - `**Path chosen:** Single-pass installer + force-fresh-install (Phase 56 D-08 pattern; CONTEXT V-01 / V-02).`
      - `**Pre-flight requirement (R-01):**` block listing both fixture URLs verbatim from the resume signal — with NO `<PASTE-...>` placeholders remaining. Use the exact form `FIXTURE_DI_FM_AAC = <url>` and `FIXTURE_SOMA_HE_AAC = <url>` (the literal `<url>` is replaced by the operator-supplied URL).

   b. **Four empty PASS/FAIL stanza scaffolds**, each modeled on `56-05-UAT-LOG.md:14–31`. Use these exact `##` headers in order:
      - `## WIN-05 §A — Pre-fix baseline (R-03)` — body has `**Status:** <pending — operator fills>`, placeholder bullets for `**Procedure:**` (steps 1–N), `**Observations:**` (channel name + observed toast text per fixture), and `**ROADMAP SC coverage:** ✗ baseline FAIL (expected pre-fix)`.
      - `## WIN-05 §B — Conda env recreate + build attestation (G-01 / Pitfall 2)` — body has `**Status:** <pending>`, `**Pre-update env audit:**` (placeholder for `conda list -n musicstreamer-build | findstr gst-` output), `**Recreate path:**` (Path 1 clean / Path 2 incremental — operator picks), `**Post-update env audit:**` (placeholder for the same `conda list` post-recreate showing all 5 plugin packages + gstreamer at 1.28.x), `**Build attestation:**` (placeholder for `BUILD_OK step=pyinstaller`, `POST-BUNDLE ASSERTION OK`, **POST-BUNDLE PLUGIN GUARD OK**, `BUILD_OK step=iscc`, and `BUILD_DIAG bundle_size_mb=... dll_count=... installer_size_mb=...` lines copied from `packaging/windows/artifacts/build.log`).
      - `## WIN-05 §C — Force-fresh-install attestation (V-01 / Phase 56 D-08)` — body has `**Status:** <pending>` and the five D-08 sequence steps verbatim from `56-05-UAT-LOG.md:102–113`: (1) uninstall existing MusicStreamer; (2) delete `%LOCALAPPDATA%\Programs\MusicStreamer`; (3) delete Start Menu LNK; (4) reinstall new installer with "Run MusicStreamer" UNCHECKED (preserves `%APPDATA%\musicstreamer`); (5) launch via Start Menu shortcut. Operator confirms each step with a checkbox.
      - `## WIN-05 §D — Post-install playback PASS attestation (R-02)` — body has `**Status:** <pending>` and a sub-block per fixture URL with fields `**Channel:**`, `**Time to first audio:**` (PASS bar: <10s), `**First ICY title displayed:**` (PASS bar: within 30s), `**GStreamer error:**` (PASS bar: NONE).

   c. **Diagnostic command examples block** inside §B for any operator who wants to probe the bundled registry. Use the corrected element-name form per RESEARCH Pitfall 6:
      ```
      # Probe bundled scanner (NOT host conda env)
      $env:GST_REGISTRY = "dist\MusicStreamer\_internal\registry.bin"
      $env:GST_PLUGIN_SCANNER = "dist\MusicStreamer\_internal\gst-plugin-scanner.exe"
      # Element-name query (correct form per RESEARCH Pitfall 6)
      & "dist\MusicStreamer\_internal\gst-inspect-1.0.exe" avdec_aac
      & "dist\MusicStreamer\_internal\gst-inspect-1.0.exe" aacparse
      # Plugin-name query (alternative)
      & "dist\MusicStreamer\_internal\gst-inspect-1.0.exe" --plugin libav
      & "dist\MusicStreamer\_internal\gst-inspect-1.0.exe" --plugin audioparsers
      ```
      The header for this block reads `### Diagnostic commands (optional; operator may use during §B if PLUGIN GUARD output is ambiguous)`. DO NOT use the `--plugin avdec_aac` form (CONTEXT-DG-01 wrote this wrong; the researcher corrected it).

   d. **Optional negative test scaffold**:
      `## Optional negative test — G-01 guard exercise` — body has `**Status:** <pending / not-run>`, instructions to delete `dist\MusicStreamer\_internal\gst_plugins\gstlibav.dll` after a successful build then re-run `.\build.ps1` and expect `BUILD_FAIL reason=plugin_missing` with `$LASTEXITCODE = 10`. Mark this section optional — only mandatory if §D PASS attestation is ambiguous.

   e. **Findings (Surprises during UAT)** section header — analog of `56-05-UAT-LOG.md:72–98`. Empty body; operator fills F1/F2/… stanzas only if anomalies surface.

   f. **Release-Grade Re-attestation block** — analog of `56-05-UAT-LOG.md:102–113`. Empty body with placeholder `<filled after §A–§D all PASS>`. Operator copies the 56-05 force-fresh-install language verbatim and substitutes "WIN-05" for "WIN-01/WIN-02" and "AAC playback" for "DI.fm playback".

   g. **Phase Completion Decision block** — analog of `56-05-UAT-LOG.md:117–130`. Body starts with `**Decision:** <pending until operator fills>` and lists three placeholder bullets for the WIN-05 sub-criteria (one each from §A pre-fix FAIL → §D post-fix PASS, §B BUILD_OK with plugin guard PASS, §C force-fresh-install completed).

3. After file creation, surface the path in the resume signal so the operator knows where to find it and pause for the next checkpoint (Task 2 — operator runs the UAT and fills the stanzas).

Do NOT invent fixture URLs. Do NOT proceed past step 1's pause until both URLs are supplied. If the operator's URLs do not match the codec constraints (DI.fm AAC tier + SomaFM HE-AAC), surface the mismatch and request replacements.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md && grep -q "Phase 69 — UAT Log" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "## WIN-05 §A — Pre-fix baseline" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "## WIN-05 §B — Conda env recreate" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "## WIN-05 §C — Force-fresh-install" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "## WIN-05 §D — Post-install playback" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "## Phase Completion Decision" .planning/phases/69-*/69-02-UAT-LOG.md && ! grep -q "<PASTE-DI-FM-AAC-TIER-URL-HERE>" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "gst-inspect-1.0.exe\" avdec_aac" .planning/phases/69-*/69-02-UAT-LOG.md && ! grep -q "gst-inspect-1.0.exe\" --plugin avdec_aac" .planning/phases/69-*/69-02-UAT-LOG.md && echo "scaffold OK"</automated>
  </verify>
  <what-built>
Two operator actions BEFORE this checkpoint can resume:

1. Operator picks two stream URLs from their existing MusicStreamer library and pastes them into the resume signal:
   - One DI.fm "AAC tier" station (codec column shows "AAC"; e.g. an AudioAddict-imported channel at the "medium" or "low" quality tier).
   - One SomaFM station with an HE-AAC stream variant.
   Both URLs MUST currently FAIL playback on the Win11 VM's existing installer build (otherwise R-03's pre-fix baseline is meaningless).

2. After receiving the URLs, Claude creates the `69-02-UAT-LOG.md` scaffold with:
   - Top-of-file metadata header naming the chosen path as "Single-pass installer + force-fresh-install (Phase 56 D-08 pattern; CONTEXT V-01 / V-02)" and listing the two fixture URLs verbatim under "Pre-flight requirement".
   - Four empty PASS/FAIL stanza scaffolds, named: "WIN-05 §A — Pre-fix baseline (R-03)", "WIN-05 §B — Conda env recreate + build attestation (G-01 / Pitfall 2)", "WIN-05 §C — Force-fresh-install attestation (V-01 / Phase 56 D-08)", "WIN-05 §D — Post-install playback PASS attestation (R-02)".
   - Empty "Release-Grade Re-attestation" block (analog of 56-05-UAT-LOG.md:102–113).
   - Empty "Phase Completion Decision" block (analog of 56-05-UAT-LOG.md:117–130).
   - An "Optional negative test — G-01 guard exercise" stanza (operator deletes `gstlibav.dll` and confirms BUILD_FAIL reason=plugin_missing with `$LASTEXITCODE` = 10).
   - A "Findings (Surprises during UAT)" section header following the F1/F2/F3 idiom of 56-05-UAT-LOG.md:72–98 — empty body, operator fills if anomalies surface.
   - Per RESEARCH Pitfall 6, the gst-inspect diagnostic command examples in §B MUST use the element-name form (`gst-inspect-1.0.exe avdec_aac`) NOT `--plugin avdec_aac` (the latter is the CONTEXT-DG-01 form which the researcher corrected — see Pitfall 6 lines 557–565 for the rationale).
   - The "## Phase Completion Decision" stanza must end with `Decision: <pending until operator fills>` so it's obvious the gate is open.
  </what-built>
  <how-to-verify>
1. Confirm the operator has chosen two URLs that currently fail on the Win11 VM. Sample verification dialogue: "Have both URLs been confirmed to fail playback on the current build before this UAT begins? If yes, what is the observed toast text on the playback failure for each?"

2. Open `.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md` (after Claude creates it from the scaffold). Confirm:
   - The top metadata block names "Single-pass installer + force-fresh-install" path AND lists the two fixture URLs verbatim under "Pre-flight requirement" with no `<PASTE-...>` placeholders remaining.
   - The four §A/§B/§C/§D stanza headers exist as empty placeholders.
   - The §B diagnostic command examples use bare `avdec_aac` and `aacparse` (NOT `--plugin avdec_aac`).
   - The Release-Grade Re-attestation block + Phase Completion Decision block exist as empty placeholders.
   - The "Optional negative test — G-01 guard exercise" section exists.

3. Confirm with the operator that the UAT-LOG scaffold matches expectations and the next checkpoint (build + UAT execution) is ready to begin.
  </how-to-verify>
  <resume-signal>Provide the two fixture URLs in the form `FIXTURE_DI_FM_AAC=https://...` and `FIXTURE_SOMA_HE_AAC=https://...` (one per line). Then Claude will create the UAT-LOG scaffold and pause for the next checkpoint.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md` exists.
    - File contains BOTH fixture URLs verbatim under a "Pre-flight requirement" block (no `<PASTE-...>` placeholders remaining).
    - File contains four empty PASS/FAIL stanza headers: `## WIN-05 §A`, `## WIN-05 §B`, `## WIN-05 §C`, `## WIN-05 §D`.
    - File contains an empty `## Release-Grade Re-attestation` block (analog of 56-05-UAT-LOG.md:102–113).
    - File contains an empty `## Phase Completion Decision` block ending in `Decision: <pending until operator fills>`.
    - File contains the `gst-inspect-1.0.exe avdec_aac` element-name form (NOT `gst-inspect-1.0.exe --plugin avdec_aac`) in the §B diagnostic command examples — verified with `grep -q "gst-inspect-1.0.exe\" avdec_aac" .planning/phases/69-*/69-02-UAT-LOG.md && ! grep -q "gst-inspect-1.0.exe\" --plugin avdec_aac" .planning/phases/69-*/69-02-UAT-LOG.md`.
    - File contains an `## Optional negative test — G-01 guard exercise` stanza with the `delete gstlibav.dll → rebuild → expect exit 10` sequence outlined.
  </acceptance_criteria>
  <done>UAT-LOG.md scaffold exists with both fixture URLs locked, four empty stanza placeholders ready for operator notes, and the Phase 56 D-08 force-fresh-install sequence pre-documented. Checkpoint resumes once operator pastes URLs and Claude confirms the scaffold matches the spec.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator captures pre-fix baseline (R-03), recreates conda env, runs build.ps1, completes UAT, fills 69-02-UAT-LOG.md</name>
  <files>.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md (the scaffold produced by Task 1)
    - .planning/phases/56-windows-di-fm-smtc-start-menu/56-05-UAT-LOG.md (the analog — particularly the "Release-Grade Re-attestation" block at lines 102–113 for the verbatim force-fresh-install language)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-CONTEXT.md (V-01 — Phase 56 D-08 force-fresh-install sequence; R-03 — pre-fix repro REQUIRED; G-01 — exit code 10 expected)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-RESEARCH.md (Pitfall 2 — env may already have spike snapshot plugins, recreate from new README is the only sound path; Runtime State Inventory line 501 — operator action at UAT time)
    - .planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-PATTERNS.md (§A/B/C/D stanza shape; "## Phase Completion Decision" closing block)
  </read_first>
  <action>
PAUSE for the operator to execute the full Win11 UAT sequence end-to-end on the VM and fill `69-02-UAT-LOG.md` with empirical PASS/FAIL evidence. Claude orchestrates the four-stanza sequence by prompting the operator at each phase but does NOT itself touch the Win11 VM. Recommended sequence:

**§A — Pre-fix baseline (R-03).** Prompt the operator: "Before applying any fix, launch the current installed MusicStreamer.exe on the Win11 VM. For each fixture URL (DI.fm AAC + SomaFM HE-AAC) press Play and record (a) station/channel name, (b) the exact observed toast text from `_on_playback_error` (e.g. `Playback error: <text>`), (c) timestamp." Both fixtures MUST FAIL — if either plays, the URL is wrong (request a replacement) and §A re-runs. After operator confirmation, fill §A with `Status: FAIL (baseline)`.

**§B — Conda env recreate + build attestation (G-01 / Pitfall 2).** Prompt operator to (1) capture pre-update env state via `conda list -n musicstreamer-build | findstr gst-` and paste output, (2) choose EITHER Path 1 (`conda env remove -n musicstreamer-build` then re-run the full Plan-01 conda recipe verbatim) or Path 2 (`conda install -n musicstreamer-build -c conda-forge gst-libav gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly` for incremental update), (3) capture post-update env state via the same `conda list | findstr gst-` command (expected: all 5 plugin packages + gstreamer at 1.28.x present), (4) run `cd packaging\windows; .\build.ps1` and capture the build log. Operator pastes into §B the following lines verbatim from `packaging/windows/artifacts/build.log`: `BUILD_OK step=pyinstaller`, `POST-BUNDLE ASSERTION OK -- dist-info singleton`, **`POST-BUNDLE PLUGIN GUARD OK`** (the empirical proof that Plan 01's step 4b ran), `BUILD_OK step=iscc`, `BUILD_DIAG bundle_size_mb=... dll_count=... installer_size_mb=...`. Close §B with `Status: BUILD_OK`. If the build fails at step 4b with `BUILD_FAIL reason=plugin_missing exit code=10`, §B records the failure verbatim and operator returns to fix conda env state before retrying.

**§C — Force-fresh-install attestation (V-01 / Phase 56 D-08).** Prompt operator to follow the verbatim five-step sequence from `56-05-UAT-LOG.md:102–113`: (1) uninstall existing MusicStreamer via Settings → Apps; (2) delete `%LOCALAPPDATA%\Programs\MusicStreamer` directory; (3) delete the Start Menu LNK; (4) run `dist\installer\MusicStreamer-<version>-win64-setup.exe` with "Run MusicStreamer" checkbox UNCHECKED at the end of the wizard (preserves `%APPDATA%\musicstreamer` user data per D-03); (5) launch MusicStreamer via Start Menu shortcut (NOT `python -m musicstreamer`). Close §C with `Status: INSTALL_OK` and confirmed installed binary path.

**§D — Post-install playback PASS attestation (R-02).** Prompt operator to play each of the two fixture URLs from §A again, this time from the freshly-installed binary. For each fixture record: (a) station/channel name, (b) time-to-first-audio (PASS bar: <10s), (c) first ICY title displayed (PASS bar: within 30s), (d) any GStreamer error messages (PASS bar: NONE — the `error -5` or `Playback error` toast that fired in §A must NOT recur). Both fixtures MUST PASS. Close §D with `Status: PASS` per stanza.

**Optional G-01 negative test.** If operator wants belt-and-braces confidence in step 4b's guard, prompt: "Delete `dist\MusicStreamer\_internal\gst_plugins\gstlibav.dll` and re-run `.\build.ps1` — expect `BUILD_FAIL reason=plugin_missing` and `$LASTEXITCODE = 10` after the run. Optional, not blocking." UAT-LOG records PASS/FAIL/skipped.

**Release-Grade Re-attestation.** After §A–§D all PASS, prompt operator to fill the Release-Grade Re-attestation block by copying the `56-05-UAT-LOG.md:102–113` force-fresh-install language verbatim and substituting "WIN-05" for "WIN-01/WIN-02" and "AAC playback" for "DI.fm playback". One sentence per WIN-05 sub-criterion confirms the criterion holds on the release-grade installed binary.

**Phase Completion Decision.** Final operator action: select `Decision: ship-phase` (assuming §A–§D all PASS) or `Decision: revise-and-retry` (otherwise; operator names which stanza failed) and write a one-paragraph rationale citing the specific §A→§D PASS attestations. This is the SHIP gate `/gsd-verify-work 69` consumes before `/gsd-complete-phase 69` flips WIN-05 from Pending to Complete in REQUIREMENTS.md.

Throughout: Claude DOES NOT modify any Win11 VM state; all VM interaction is operator-driven. Claude's job is to (a) prompt at each phase, (b) interpret operator responses, (c) write the empirical evidence into the UAT-LOG stanzas, (d) surface anomalies (e.g. unexpected build.log lines) for operator triage, (e) hold the gate until the operator gives an explicit resume signal.
  </action>
  <verify>
    <automated>grep -q "POST-BUNDLE PLUGIN GUARD OK" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "Status: PASS" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "Status: BUILD_OK" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "Status: FAIL (baseline)" .planning/phases/69-*/69-02-UAT-LOG.md && grep -q "Status: INSTALL_OK" .planning/phases/69-*/69-02-UAT-LOG.md && (grep -q "Decision: ship-phase" .planning/phases/69-*/69-02-UAT-LOG.md || grep -q "Decision: revise-and-retry" .planning/phases/69-*/69-02-UAT-LOG.md) && uv run pytest -x 2>&1 | tail -5</automated>
  </verify>
  <what-built>
The operator executes the full Win11 UAT sequence end-to-end and fills `69-02-UAT-LOG.md` with empirical PASS/FAIL evidence. Recommended sequence (operator follows on Win11 VM):

**§A — Pre-fix baseline (R-03).** Operator launches the current (pre-fix) installed `MusicStreamer.exe` on the Win11 VM. For each fixture URL, presses Play and records: (a) station/channel name, (b) observed toast text (expected: a `Playback error: ...` message from `_on_playback_error`), (c) timestamp. Both fixtures MUST FAIL — if either plays, the URL is wrong (operator picks a different fixture) and §A re-runs. The §A block in UAT-LOG closes with `Status: FAIL (baseline)` per stanza.

**§B — Conda env recreate + build attestation (G-01 / Pitfall 2).** Operator first captures the existing env state (RESEARCH Runtime State Inventory line 503): `conda list -n musicstreamer-build | findstr gst-` and pastes the verbatim output into §B. Then EITHER:
- Path 1 (clean): `conda env remove -n musicstreamer-build`, then re-run the new Plan-01 conda recipe verbatim (`conda create -n musicstreamer-build -c conda-forge python=3.12 pygobject gstreamer=1.28 gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly gst-libav pyinstaller "pyinstaller-hooks-contrib>=2026.2"`).
- Path 2 (incremental): `conda install -n musicstreamer-build -c conda-forge gst-libav gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly` to add the missing packages without env recreate.

§B records which path was taken AND the post-update `conda list -n musicstreamer-build | findstr gst-` output (expected: all five plugin packages + `gstreamer` at 1.28.x). Then operator runs `cd packaging\windows; .\build.ps1` and records:
- `BUILD_OK step=pyinstaller` from the existing step 4 (PyInstaller success).
- `POST-BUNDLE ASSERTION OK -- dist-info singleton: ...` from the existing step 4a.
- `POST-BUNDLE PLUGIN GUARD OK` from the NEW Plan-01 step 4b (the empirical proof that gstlibav.dll AND gstaudioparsers.dll landed in the bundle).
- `BUILD_OK step=iscc` from the existing Inno Setup compile step.
- `BUILD_DIAG bundle_size_mb=... dll_count=... installer_size_mb=...` summary.

§B closes with `Status: BUILD_OK`. If the build fails at step 4b with `BUILD_FAIL reason=plugin_missing exit code=10`, §B records the failure verbatim and operator returns to fix the conda env before retrying.

**§C — Force-fresh-install attestation (V-01 / Phase 56 D-08).** Operator follows the verbatim sequence from `56-05-UAT-LOG.md:102–113`:
1. Uninstall existing MusicStreamer via Settings → Apps → uninstall (or `wmic product where ...`).
2. Delete `%LOCALAPPDATA%\Programs\MusicStreamer` directory.
3. Delete the Start Menu shortcut (LNK) at `%APPDATA%\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk` (or via Start Menu right-click → Uninstall).
4. Run the new installer `dist\installer\MusicStreamer-<version>-win64-setup.exe` with the "Run MusicStreamer" checkbox UNCHECKED at the end of the wizard (preserves `%APPDATA%\musicstreamer` user data — D-03).
5. Launch MusicStreamer via Start Menu shortcut (NOT `python -m musicstreamer` — Start Menu binding required for separate WIN-02 SMTC concern, though SMTC is not the WIN-05 success criterion).

§C closes with `Status: INSTALL_OK` and the installed binary path confirmed (e.g. `%LOCALAPPDATA%\Programs\MusicStreamer\MusicStreamer.exe`).

**§D — Post-install playback PASS attestation (R-02).** Operator plays each of the two fixture URLs from §A again, this time from the freshly-installed binary. For each: records (a) station/channel name, (b) time-to-first-audio (PASS bar: <10s), (c) first ICY title displayed (PASS bar: within 30s), (d) any GStreamer error messages (PASS bar: NONE — the `error -5` symptom or `Playback error` toast that fired in §A must NOT recur). Both fixtures MUST PASS. §D closes with `Status: PASS` per stanza.

**Optional — G-01 guard exercise.** If operator wants belt-and-braces confidence in step 4b, they can: stop MusicStreamer → delete `dist\MusicStreamer\_internal\gst_plugins\gstlibav.dll` → re-run `.\build.ps1` → expect to see `BUILD_FAIL reason=plugin_missing` AND `$LASTEXITCODE = 10` after the run. UAT-LOG marks this PASS/FAIL — optional, not blocking.

**Release-Grade Re-attestation.** After §A–§D all PASS, operator fills the Release-Grade Re-attestation block per `56-05-UAT-LOG.md:102–113` shape (substitute "WIN-05" for "WIN-01/WIN-02"; substitute "AAC playback" for "DI.fm playback"). One sentence confirms each WIN-05 sub-criterion holds on the release-grade installed binary.

**Phase Completion Decision.** Operator selects `Decision: ship-phase` (or `revise-and-retry` if any §A–§D failed) and writes a one-paragraph rationale citing the specific §A→§D PASS attestations. This is the SHIP gate `/gsd-verify-work 69` consumes.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-UAT-LOG.md`. Confirm:
   - §A "Pre-fix baseline" stanza contains both fixture URL station names AND observed toast text AND `Status: FAIL (baseline)`.
   - §B "Conda env recreate + build attestation" stanza contains pre-update `conda list` output, the chosen recreate path (clean vs incremental), post-update `conda list` output showing all 5 plugin packages, AND the literal `POST-BUNDLE PLUGIN GUARD OK` line copied from `build.log` AND `Status: BUILD_OK`.
   - §C "Force-fresh-install attestation" stanza records all five D-08 sequence steps as completed AND `Status: INSTALL_OK`.
   - §D "Post-install playback PASS attestation" stanza contains time-to-first-audio + ICY title evidence for BOTH fixtures AND `Status: PASS` for each.
   - The "Release-Grade Re-attestation" block is filled with a confirmation that all WIN-05 sub-criteria hold on the release-grade install.
   - The "Phase Completion Decision" block specifies `Decision: ship-phase` (or `revise-and-retry`, in which case the resume signal indicates the failure mode).

2. Confirm with the operator: "Did the new `POST-BUNDLE PLUGIN GUARD OK` line appear in `packaging/windows/artifacts/build.log` during the build? If yes, the Plan 01 wiring is empirically confirmed end-to-end."

3. If the operator chose to run the optional G-01 negative test, confirm the §"Optional negative test" stanza records `BUILD_FAIL reason=plugin_missing` + `$LASTEXITCODE = 10` after the deliberate `gstlibav.dll` deletion.

4. Confirm no surprises in the "Findings" section that change the phase scope (analog of 56-05-UAT-LOG.md's F1/F2/F3 — surprises get logged but do not block ship unless they refute the WIN-05 PASS).
  </how-to-verify>
  <resume-signal>
Either:
- `UAT PASS — all stanzas filled, Decision: ship-phase` (Claude proceeds to commit UAT-LOG.md and the planning loop hands off to /gsd-verify-work 69).
- `UAT FAIL — <which stanza>: <what failed>` (Claude proceeds to surface the failure mode; this is the cue for a Phase 69 follow-up plan if the bundle fix is incomplete).
- `UAT BLOCKED — <reason>` (e.g. build VM unavailable; operator returns later).
  </resume-signal>
  <acceptance_criteria>
    - `69-02-UAT-LOG.md` has all four §A/§B/§C/§D stanzas filled with empirical evidence (timestamps, toast text, build.log excerpts, time-to-first-audio).
    - `69-02-UAT-LOG.md` §B contains the literal `POST-BUNDLE PLUGIN GUARD OK` substring (verifiable: `grep -q "POST-BUNDLE PLUGIN GUARD OK" .planning/phases/69-*/69-02-UAT-LOG.md`).
    - `69-02-UAT-LOG.md` §D records `Status: PASS` for BOTH fixture URLs.
    - `69-02-UAT-LOG.md` "Phase Completion Decision" block specifies `Decision: ship-phase` (assuming §A–§D all PASS) or `Decision: revise-and-retry` (otherwise; in which case the operator's resume signal explains).
    - `69-02-UAT-LOG.md` "Release-Grade Re-attestation" block is non-empty and confirms WIN-05 PASS on the installed binary.
    - The full Linux test suite still passes: `uv run pytest -x` exits 0 (no regressions; Plan 02 should not touch Linux code).
  </acceptance_criteria>
  <done>The Win11 UAT empirically attests that AAC streams play on the installed binary post-bundle-fix. WIN-05's SHIP gate is closed with operator-driven evidence; `/gsd-verify-work 69` consumes the UAT-LOG as input for goal-backward verification, and `/gsd-complete-phase 69` flips the WIN-05 checkbox + Traceability status to Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's Win11 VM ↔ conda-forge package repository | Operator pulls untrusted bytes when recreating the conda env. Same boundary as Plan 01 / T-69-01. |
| Public radio stream URL ↔ MusicStreamer.exe | DI.fm and SomaFM AAC stream endpoints. Untrusted-input boundary (network response → souphttpsrc → aacparse → avdec_aac pipeline). |
| Operator-driven `gstlibav.dll` deletion (optional G-01 negative test) ↔ build.ps1 | Operator manually mutates the bundle to exercise the guard. Not a production failure mode; controlled test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Tampering | Operator manually edits `69-02-UAT-LOG.md` to claim PASS without running the actual UAT | accept | Single-operator project; the operator is the only stakeholder. The Phase Completion Decision is signed by the same person who runs the UAT. No multi-party accountability exists in this trust model. If the operator falsely claims PASS, the runtime symptom (AAC streams still failing) would surface immediately on next playback attempt — self-correcting. |
| T-69-05 | Information Disclosure | UAT-LOG.md commits to git, mirrors to QNAP → GitHub, contains operator-named fixture URLs | accept | Same disposition as Plan 01 T-69-03 — URLs are public radio stream endpoints with no credential surface. Operator's project context (MEMORY.md `2026-05-04 cookie-leak scrub`) confirms QNAP→GitHub push mirror is in effect; treat UAT-LOG as effectively public. No PII or auth tokens land here. |
| T-69-06 | Denial of Service | Malicious AAC stream payload exploits libavcodec via avdec_aac → gst-libav | accept | Standard trust model for a personal-use streaming app pulling from operator-curated stations. CVE risk is inherited from `gst-libav` (Plan 01 T-69-01 disposition applies). UAT does not introduce new attack surface beyond what Plan 01 already considers. |
</threat_model>

<verification>
After Task 2 completes:

1. UAT-LOG.md attests `Decision: ship-phase` with all four stanzas PASS.
2. Plan 01's drift-guard pytests + bundle guard remain green on Linux dev.
3. `/gsd-verify-work 69` can run goal-backward verification with UAT-LOG.md as the empirical input.
4. `/gsd-complete-phase 69` flips the WIN-05 row in `.planning/REQUIREMENTS.md` from `[ ]` to `[x]` and the Traceability table from `Pending` to `Complete`.

If UAT fails (one or more §A–§D stanzas FAIL), the operator's resume signal triggers either:
- A Phase 69 follow-up plan addressing the specific failure (e.g. assumption A3 from RESEARCH — conda-forge's gst-libav build disabled AAC for license reasons — would require a follow-up plan switching to a different decoder source).
- A scope expansion to investigate which assumption (A1/A2/A3 from RESEARCH "Assumptions Log") was wrong.
</verification>

<success_criteria>
- `69-02-UAT-LOG.md` exists in the phase directory with all four §A/§B/§C/§D stanzas filled and `Status: PASS` on §D.
- §B records the empirical `POST-BUNDLE PLUGIN GUARD OK` line from `build.log` proving Plan 01's new step 4b ran successfully.
- §D records `Status: PASS` for BOTH fixture URLs on the force-fresh-installed binary (within the 10s time-to-first-audio bar).
- Phase Completion Decision block: `Decision: ship-phase` with one-paragraph rationale.
- Release-Grade Re-attestation block confirms WIN-05 PASS on the installed binary.
- The full Linux test suite still passes (`uv run pytest -x` exits 0).
</success_criteria>

<output>
After completion, create `.planning/phases/69-debug-why-aac-streams-aren-t-playing-in-windows-possibly-mis/69-02-SUMMARY.md` documenting:
- The two fixture URLs that PASS'd (verbatim).
- The conda env recreate path taken (clean vs incremental) AND the post-update `conda list` snapshot.
- The build.ps1 step-4b empirical attestation: `POST-BUNDLE PLUGIN GUARD OK` line + timestamp + verified DLL presence.
- The Phase 56 D-08 force-fresh-install sequence steps confirmed completed.
- §D time-to-first-audio + ICY title evidence per fixture.
- Whether the optional G-01 negative test was run AND its result.
- Any "Findings" surprises (analog of 56-05-UAT-LOG.md F1/F2/F3 idiom).
- Pointer to `/gsd-verify-work 69` as the next step in the SHIP gate, followed by `/gsd-complete-phase 69` to flip WIN-05 to Complete.
</output>
