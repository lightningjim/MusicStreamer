---
phase: 73
plan: 04
type: execute
wave: 4
depends_on: [73-01-PLAN.md, 73-02-PLAN.md, 73-03-PLAN.md]
files_modified:
  - musicstreamer/ui_qt/edit_station_dialog.py
  - musicstreamer/ui_qt/now_playing_panel.py
  - musicstreamer/settings_export.py
  - tests/test_edit_station_dialog.py
  - tests/test_settings_export.py
  - tests/test_now_playing_panel.py
autonomous: true
requirements:
  - ART-MB-10
  - ART-MB-12
tags:
  - edit-station-dialog
  - settings-export
  - now-playing-panel
  - ui-wiring
must_haves:
  truths:
    - "EditStationDialog has a 'Cover art source:' QComboBox with 3 entries (Auto / iTunes only / MusicBrainz only) (D-06)"
    - "Dialog populates the combo from station.cover_art_source on open"
    - "On Save, the dialog passes cover_art_source=<selection> to repo.update_station"
    - "Dirty-detection includes cover_art_source — flipping the combo enables Save"
    - "Settings export ZIP carries cover_art_source on every station; old ZIPs (missing key) default to 'auto' on import (Pitfall 9)"
    - "NowPlayingPanel._fetch_cover_art_async passes self._station.cover_art_source as the source kwarg to fetch_cover_art"
  artifacts:
    - path: "musicstreamer/ui_qt/edit_station_dialog.py"
      provides: "cover_art_source_combo widget + populate + save + dirty-snapshot"
      contains: "cover_art_source_combo"
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "_fetch_cover_art_async forwards source kwarg from self._station"
      contains: "cover_art_source"
    - path: "musicstreamer/settings_export.py"
      provides: "cover_art_source roundtrip on export + import (insert + replace)"
      contains: "cover_art_source"
  key_links:
    - from: "musicstreamer/ui_qt/edit_station_dialog.py cover_art_source_combo"
      to: "musicstreamer/repo.py update_station"
      via: "kwarg cover_art_source on save"
      pattern: "cover_art_source\\s*=\\s*self\\.cover_art_source_combo|cover_art_source\\s*=\\s*cover_art_source"
    - from: "musicstreamer/ui_qt/now_playing_panel.py _fetch_cover_art_async"
      to: "musicstreamer/cover_art.fetch_cover_art"
      via: "source kwarg pass-through from self._station.cover_art_source"
      pattern: "fetch_cover_art\\([^)]*source\\s*=\\s*"
    - from: "musicstreamer/settings_export.py _station_to_dict"
      to: "ZIP settings.json payload"
      via: "dict key 'cover_art_source' alongside 'icy_disabled'"
      pattern: "[\"']cover_art_source[\"']"
---

<objective>
Wire the per-station preference end-to-end through the UI surface: add a QComboBox selector to EditStationDialog (D-06); round-trip the field through settings export/import ZIP (Pitfall 9 forward-compat); and pass `station.cover_art_source` through `_fetch_cover_art_async` to the Plan 03 router. After this plan, the user can flip the source per station via the UI, save persists, the ZIP round-trips, and the actual cover-art fetch honors the selection.

Purpose: this is the visible UX of Phase 73. Plans 01-03 built the data layer + lookup module + router; this plan makes the feature discoverable and persistent. Closes ART-MB-10 (settings export round-trip) and ART-MB-12 (selector reads/writes) — the final two requirements (other than UAT).

Output: EditStationDialog QComboBox selector; settings_export INSERT + REPLACE side; NowPlayingPanel source pass-through; three test files extended with new tests covering the surfaces; all 16 ART-MB requirements GREEN behaviourally (UAT in Plan 05 covers manual-only verifications).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-CONTEXT.md
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-RESEARCH.md
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-PATTERNS.md
@musicstreamer/ui_qt/edit_station_dialog.py
@musicstreamer/ui_qt/now_playing_panel.py
@musicstreamer/settings_export.py
@tests/test_edit_station_dialog.py
@tests/test_settings_export.py

<interfaces>
<!-- Surfaces consumed by Plan 04 (all locked by Plans 01-03). -->

Repo.update_station signature (from Plan 03):
- kwarg `cover_art_source: str = "auto"` AFTER `icy_disabled: bool = False`.

Station dataclass field (from Plan 01):
- `cover_art_source: Literal["auto", "itunes_only", "mb_only"] = "auto"`

cover_art.fetch_cover_art signature (from Plan 03):
- kwarg `source: str = "auto"`; valid values `{'auto','itunes_only','mb_only'}`; unknown values fall back to 'auto' with a WARNING log.

Existing UI sites being extended:

From musicstreamer/ui_qt/edit_station_dialog.py:
- `self.icy_checkbox = QCheckBox("Disable ICY metadata")` — line 403; new combo goes IMMEDIATELY AFTER its `form.addRow` call at line 404.
- `self.icy_checkbox.setChecked(bool(station.icy_disabled))` — line 533; new combo's index-restore goes adjacent.
- `_form_snapshot` return dict — line 595-602; new key `"cover_art_source"` joins `"icy"` line 599.
- `repo.update_station(...)` call — line 1393-1401; new kwarg goes inline.

From musicstreamer/ui_qt/now_playing_panel.py:
- `fetch_cover_art(icy_title, _cb)` — line 1187; widens to `fetch_cover_art(icy_title, _cb, source=source)` with `source = getattr(self._station, "cover_art_source", "auto") if self._station else "auto"`.

From musicstreamer/settings_export.py:
- `_station_to_dict` at line 108-134; new key `"cover_art_source": station.cover_art_source` joins `"icy_disabled"` at line 114.
- `_insert_station` SQL at line 501-512; INSERT column list + tuple extended with `cover_art_source`.
- `_replace_station` SQL at line 559-568; UPDATE column list + tuple extended with `cover_art_source`.
- Forward-compat pattern (Pitfall 9): `data.get("cover_art_source") or "auto"` — matches `int(stream.get('bitrate_kbps', 0) or 0)` precedent at line 528.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EditStationDialog cover-art-source selector + populate + save + dirty-snapshot</name>
  <files>musicstreamer/ui_qt/edit_station_dialog.py, tests/test_edit_station_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/edit_station_dialog.py lines 370-415 (provider_combo + icy_checkbox creation)
    - musicstreamer/ui_qt/edit_station_dialog.py lines 520-545 (_populate — icy_checkbox.setChecked at line 533)
    - musicstreamer/ui_qt/edit_station_dialog.py lines 580-610 (_form_snapshot return dict)
    - musicstreamer/ui_qt/edit_station_dialog.py lines 1370-1410 (save handler — update_station call)
    - tests/test_edit_station_dialog.py lines 230-260 (test_icy_checkbox_maps_to_icy_disabled — copy this shape)
    - .planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-PATTERNS.md §"musicstreamer/ui_qt/edit_station_dialog.py" (full section — exact widget creation + populate + save + snapshot blocks)
  </read_first>
  <behavior>
    - Test (ART-MB-12 read side): dialog opened on a Station with cover_art_source='mb_only' shows the combo at its MB-only index (itemData='mb_only').
    - Test (ART-MB-12 write side): user changes combo to 'auto'; clicks Save (emit accepted); repo.update_station called with kwarg cover_art_source='auto'.
    - Test (D-06 placement smoke): combo exists immediately after icy_checkbox row in the form (visual layout sanity — assert form.rowCount() increases by exactly 1 vs pre-Plan-04 baseline; or assert the combo's parent is the same QFormLayout that holds icy_checkbox).
    - Test (dirty-snapshot): open dialog on Station with cover_art_source='auto'; flip combo to 'mb_only'; assert dialog._is_dirty() returns True (Save button enables).
    - Test (forward-compat for legacy Station): Station instance with cover_art_source default ('auto' via dataclass default) drives the combo to its 'auto' index without raising.
  </behavior>
  <action>
    Follow PATTERNS.md §"musicstreamer/ui_qt/edit_station_dialog.py" verbatim — that section contains the exact widget-creation, populate, read-on-save, and dirty-snapshot blocks. Apply each of these insertions:

    **0. Enumerate all `repo.update_station` callers (Warning 1 lock — gates Plan 03 Task 2 `test_update_station_omitting_cover_art_source_resets_to_auto`):**

    Before touching any code, run:

    ```
    grep -n "repo\.update_station(" musicstreamer/ui_qt/edit_station_dialog.py
    ```

    Enumerate every match. The save-path going through this dialog uses `repo.update_station(...)` to persist; if ANY of those call sites omits the `cover_art_source=` kwarg after Plan 03 lands the field-defaulting-to-auto lock, the field silently regresses to 'auto' on save. Expected call sites include the primary Save/Close action path (line ~1393-1401) plus any duplicate-station / save-and-keep-open / "apply to all in batch" variants if they exist. Every match from the grep MUST be followed within 30 lines by `cover_art_source=` in the same call (the kwarg may be on the same line or on a continuation line, but must appear within the parenthesis group of that call).

    Add this enumeration as a documented step in the SUMMARY for traceability.

    **1. Widget creation** (insert immediately after `form.addRow("ICY metadata:", self.icy_checkbox)` at line 404):
    - Create `self.cover_art_source_combo = QComboBox()` — default `setEditable(False)` (the 3 values are fixed; this differs from `provider_combo` which IS editable).
    - addItem in this exact order (per D-01 + D-05 — 'auto' is default and appears first):
      - `("Auto (iTunes → MusicBrainz fallback)", "auto")`
      - `("iTunes only", "itunes_only")`
      - `("MusicBrainz only", "mb_only")`
    - `form.addRow("Cover art source:", self.cover_art_source_combo)` — placement per D-06 ("close to the existing per-station ICY disable toggle feels natural").

    **2. Populate from Station** (insert immediately after `self.icy_checkbox.setChecked(bool(station.icy_disabled))` at line 533):
    - Loop over `range(self.cover_art_source_combo.count())`; if `itemData(idx) == (station.cover_art_source or "auto")`, `setCurrentIndex(idx)` and break.
    - The `or "auto"` defensive default handles the legacy-Station case (a Station dataclass with cover_art_source field defaulting to 'auto' from Plan 01 — defensive coverage in case future code instantiates a Station without going through repo).

    **3. Read on save** (modify the `repo.update_station(...)` call at line 1393-1401):
    - Before the `repo.update_station` call, read: `cover_art_source = self.cover_art_source_combo.currentData()` (returns "auto" | "itunes_only" | "mb_only" — already the persisted string form per addItem's data role).
    - Pass as keyword arg: append `cover_art_source=cover_art_source` to the update_station call (after `icy_disabled` positional; do NOT convert to positional — Plan 03 explicitly added it as a keyword arg).
    - Per Plan 03's `test_update_station_omitting_cover_art_source_resets_to_auto` lock, this kwarg MUST always be passed — never omit.

    **4. Dirty-snapshot extension** (modify `_form_snapshot` return dict at line 595-602):
    - Add `"cover_art_source": self.cover_art_source_combo.currentData(),  # Phase 73` between the existing `"icy"` and `"tags"` keys. This makes flipping the combo trigger Save-button-enable per the existing dirty detection mechanism.

    **5. New tests** in `tests/test_edit_station_dialog.py` (mirror `test_icy_checkbox_maps_to_icy_disabled` shape at line 237-252):

    - `test_cover_art_source_combo_populates_from_station_mb_only`: build a Station with cover_art_source='mb_only'; open dialog; assert `dialog.cover_art_source_combo.currentData() == "mb_only"`.
    - `test_cover_art_source_combo_populates_from_station_auto_default`: Station built without explicit cover_art_source (uses Plan 01 dataclass default 'auto'); assert combo at 'auto' index.
    - `test_cover_art_source_combo_save_passes_kwarg_to_update_station`: open dialog; setCurrentIndex to the 'mb_only' entry; `dialog.button_box.accepted.emit()` (or whatever the existing save-trigger is in the test idiom); assert `repo.update_station.call_args.kwargs.get("cover_art_source") == "mb_only"`.
    - `test_cover_art_source_combo_dirty_when_flipped`: open on station with default 'auto'; capture baseline; flip combo to 'mb_only'; assert `dialog._is_dirty() is True`.
    - `test_every_update_station_call_passes_cover_art_source_kwarg` (Warning 1 source-grep gate — mirrors ART-MB-15/16 pattern): open `musicstreamer/ui_qt/edit_station_dialog.py` as a string; for every line matching `repo\.update_station\(`, find the closing parenthesis (scan forward up to 30 lines, tracking paren depth) and assert the substring `cover_art_source=` appears within that call's argument span. Fail with a descriptive message that names the line number of any non-compliant call site. This is a defense-in-depth gate against future code that copies an existing `update_station(...)` call without updating the kwarg.

    **6. Station fixture check** (PATTERNS.md notes test_edit_station_dialog.py:20-30 builds Station without cover_art_source — relies on dataclass default): no change needed; default 'auto' from Plan 01 handles it.

    **CRITICAL — Save-on-Enter ordering:** the placement after icy_checkbox keeps the form's tab order natural (URL → provider → tags → ICY → cover_art_source → streams). Do NOT insert the combo earlier in the form.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_edit_station_dialog.py -x -q</automated>
  </verify>
  <done>All five new dialog tests pass (including `test_every_update_station_call_passes_cover_art_source_kwarg` — the Warning 1 source-grep gate); existing dialog tests unaffected; every `repo.update_station(` call site in edit_station_dialog.py is followed within 30 lines by `cover_art_source=` inside the same call; ART-MB-12 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip cover_art_source through settings_export ZIP (export + INSERT + REPLACE) with Pitfall-9 forward-compat</name>
  <files>musicstreamer/settings_export.py, tests/test_settings_export.py</files>
  <read_first>
    - musicstreamer/settings_export.py lines 105-135 (_station_to_dict)
    - musicstreamer/settings_export.py lines 495-575 (_insert_station + _replace_station)
    - tests/test_settings_export.py lines 170-190, 360-420 (existing icy_disabled round-trip assertions — copy shape)
    - .planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-PATTERNS.md §"musicstreamer/settings_export.py" (Export side / Import side INSERT / Import side REPLACE)
  </read_first>
  <behavior>
    - Test (ART-MB-10 export side): _station_to_dict(station with cover_art_source='mb_only') returns dict containing `"cover_art_source": "mb_only"`.
    - Test (ART-MB-10 import INSERT side): payload with `"cover_art_source": "mb_only"` is _insert_station'd; subsequent `repo.get_station` returns `cover_art_source == "mb_only"`.
    - Test (ART-MB-10 import REPLACE side): existing station; payload with new `"cover_art_source": "auto"` → _replace_station; subsequent get_station shows the new value.
    - Test (Pitfall 9 forward-compat): payload WITHOUT the `cover_art_source` key (simulates pre-Phase-73 ZIP) → _insert_station defaults to 'auto'.
    - Test (Pitfall 9 forward-compat REPLACE): payload without key + existing station → _replace_station resets cover_art_source to 'auto' (the import's intent: a pre-73 export does not carry the field, so the imported state is "default").
  </behavior>
  <action>
    Per PATTERNS.md §"musicstreamer/settings_export.py":

    **1. Export side** — extend `_station_to_dict` at line 108-134:
    - Add `"cover_art_source": station.cover_art_source,` between the existing `"icy_disabled"` key (line 114) and the next key in the dict. Keep alphabetical-ish grouping aside; the icy_disabled neighborhood is the right semantic home.

    **2. Import side — _insert_station** at line 498-533:
    - Extend the INSERT SQL column list to: `(name, provider_id, tags, icy_disabled, last_played_at, is_favorite, cover_art_source)` — add `cover_art_source` as the 7th column.
    - Extend the values list with `data.get("cover_art_source") or "auto"` (Pitfall 9 forward-compat — handles missing key AND explicit None).

    **3. Import side — _replace_station** at line 559-568:
    - Extend the UPDATE SET list to include `cover_art_source=?` after `is_favorite=?`.
    - Extend the values tuple with `data.get("cover_art_source") or "auto"` BEFORE the trailing `station_id`.

    **4. New tests** in `tests/test_settings_export.py`:

    - `test_export_payload_contains_cover_art_source` (ART-MB-10 export): build a Station with cover_art_source='mb_only', call _station_to_dict, assert `"cover_art_source" in d and d["cover_art_source"] == "mb_only"`.
    - `test_import_insert_persists_cover_art_source` (ART-MB-10 INSERT): payload with `"cover_art_source": "itunes_only"`; call _insert_station against a fresh repo; assert `repo.get_station(sid).cover_art_source == "itunes_only"`.
    - `test_import_replace_persists_cover_art_source` (ART-MB-10 REPLACE): pre-existing station with cover_art_source='auto'; payload includes one of its stream URLs (so _replace_station matches) + `"cover_art_source": "mb_only"`; call _replace_station; assert read-back is 'mb_only'.
    - `test_import_insert_missing_cover_art_source_defaults_to_auto` (Pitfall 9 forward-compat): payload constructed WITHOUT the `cover_art_source` key (mimics pre-Phase-73 ZIP); _insert_station; assert get_station's cover_art_source == 'auto'.
    - `test_import_replace_missing_cover_art_source_defaults_to_auto` (Pitfall 9 forward-compat REPLACE): pre-existing station with cover_art_source='mb_only'; payload WITHOUT cover_art_source key matching by URL; _replace_station; assert read-back is 'auto' (the missing-key default applies on REPLACE too — pre-73 ZIP imports always reset the field).

    The last test (`replace_missing_cover_art_source_defaults_to_auto`) documents a known behavior: importing a pre-73 ZIP onto a 73+ machine REPLACES the cover_art_source field with 'auto', not preserves the existing 'mb_only' value. This is the same behavior icy_disabled has when imported from a payload without the key — `data.get(key, default)` is applied uniformly. Document this in the SUMMARY.md as expected behavior; the alternative (preserve-on-missing) would require detecting whether the payload is pre-73 vs 73+ which is out of scope for Phase 73's "additive field" design.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_settings_export.py -x -q</automated>
  </verify>
  <done>All 5 new export tests pass; round-trip is symmetric for present-key payloads; missing-key payloads default to 'auto' on both INSERT and REPLACE; ART-MB-10 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pass station.cover_art_source through NowPlayingPanel._fetch_cover_art_async to the router</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py, tests/test_now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py lines 1170-1212 (_fetch_cover_art_async + _on_cover_art_ready)
    - musicstreamer/ui_qt/now_playing_panel.py lines 820-900 (on_title_changed integration site)
    - tests/test_now_playing_panel.py lines 555-590 (test_icy_disabled_suppresses_itunes_call — double-patch idiom for re-exported symbols)
    - .planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-PATTERNS.md §"musicstreamer/ui_qt/now_playing_panel.py" (signature widening — line 1176-1187 only)
  </read_first>
  <behavior>
    - Test: `_fetch_cover_art_async` called when `self._station.cover_art_source == "mb_only"` invokes `fetch_cover_art(..., source="mb_only")`.
    - Test: when `self._station is None` (no station bound) and `_fetch_cover_art_async` is somehow called (defensive), the source kwarg defaults to 'auto' without raising.
    - Test: when self._station has no cover_art_source attribute (legacy in-memory station; should not happen post-Plan-01 but defensive coverage), getattr default 'auto' is used.
  </behavior>
  <action>
    Per PATTERNS.md §"musicstreamer/ui_qt/now_playing_panel.py" — `_fetch_cover_art_async` widening:

    **1. Modify `_fetch_cover_art_async`** at lines 1176-1187 — minimal diff per PATTERNS.md's "no other changes" caveat:
    - Inside the method body (before the `fetch_cover_art(icy_title, _cb)` call), compute:
      ```python
      source = getattr(self._station, "cover_art_source", "auto") if self._station else "auto"
      ```
    - Change the call from `fetch_cover_art(icy_title, _cb)` to `fetch_cover_art(icy_title, _cb, source=source)`.

    Do NOT touch:
    - The `_cover_fetch_token += 1` line — token-guard is independent of source.
    - The `cover_art_ready` Signal payload format (`f"{token}:{path_or_none or ''}"`) — same string shape regardless of source.
    - `_on_cover_art_ready` — UNCHANGED per PATTERNS.md (D-02's "fall through to station-logo placeholder" reuses the existing miss branch).
    - `on_title_changed` (lines 820-896) — UNCHANGED per PATTERNS.md (no signature change needed; source is read inside _fetch_cover_art_async from self._station).

    **2. New tests** in `tests/test_now_playing_panel.py` — use the double-patch idiom from test_icy_disabled_suppresses_itunes_call:

    - `test_fetch_cover_art_async_passes_station_source_kwarg_mb_only`:
      - Build a panel + bind to a Station with cover_art_source='mb_only'.
      - Double-patch `musicstreamer.cover_art.fetch_cover_art` AND `musicstreamer.ui_qt.now_playing_panel.fetch_cover_art` to a MagicMock spy.
      - Call `panel._fetch_cover_art_async("Daft Punk - One More Time")`.
      - Assert `spy.call_args.kwargs.get("source") == "mb_only"` (or 3rd positional if the implementation uses positional — read the implementation to determine, but recommend kwarg for clarity).

    - `test_fetch_cover_art_async_passes_station_source_kwarg_itunes_only`: same shape, source='itunes_only'.

    - `test_fetch_cover_art_async_defaults_to_auto_when_no_station`:
      - Build a panel; do NOT bind a station (self._station is None).
      - Patch the symbol; call `panel._fetch_cover_art_async("Daft Punk - One More Time")`.
      - Assert spy called with `source='auto'`.

    - `test_fetch_cover_art_async_defaults_to_auto_when_station_lacks_attribute` (defensive — guards against in-memory Station built without going through Plan 01's dataclass):
      - Build a panel; bind a `types.SimpleNamespace()` mock object as self._station with NO cover_art_source attribute.
      - Patch + call.
      - Assert spy called with `source='auto'`.

    **3. Sanity check on existing integration:** the `test_icy_disabled_suppresses_itunes_call` at lines 561-581 SHOULD still pass — Plan 04's only change is adding a kwarg, not altering the icy_disabled gate. Run the full `tests/test_now_playing_panel.py` file to confirm no regression.

    **4. Future-touch note for SUMMARY.md:** the `cover_art_ready` Signal(str) payload was considered for widening per RESEARCH Open Question 3, but explicitly KEPT as Signal(str) per the OQ-3 recommendation ("Keep Signal(str) for Phase 73 — matches existing pattern, no race regression"). Document this decision.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_now_playing_panel.py -x -q</automated>
  </verify>
  <done>Four new now_playing_panel tests pass; existing now_playing_panel tests unaffected (including test_icy_disabled_suppresses_itunes_call); source kwarg flows from Station → panel → router on every cover fetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EditStationDialog combo currentData() → repo | TRUSTED: enum-restricted by addItem; only the three documented values can be returned |
| Settings export ZIP → import on another machine | UNTRUSTED: a malicious or corrupted ZIP could carry arbitrary string for cover_art_source |
| Station object → _fetch_cover_art_async source kwarg | INTERNAL: getattr defensive default keeps unexpected shapes from crashing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-14 | Tampering (V5 Input Validation) | settings_export import path | mitigate | Plan 03 router validates source kwarg against {'auto','itunes_only','mb_only'} — unknown values fall back to 'auto' with WARNING log. Defense-in-depth: an attacker-controlled ZIP carrying `cover_art_source: "drop_table_stations"` cannot escape SQL (parameterized query) and cannot escape the router (validation falls back to 'auto'). |
| T-73-15 | Information Disclosure | Settings export ZIP cross-machine | accept | cover_art_source is a per-station UI preference; no PII, no secrets. Carrying it across machines is the explicit feature (ART-MB-10). |
| T-73-16 | DoS | Combo flipping triggers immediate fetch on next ICY | accept | Save persists; next ICY title change drives a fresh fetch under the existing rate gate. No new attack surface. |
</threat_model>

<verification>
- `uv run --with pytest --with pytest-qt pytest tests -x -q` — FULL suite green. All 16 ART-MB requirements satisfied automated-side.
- `grep -c "cover_art_source" musicstreamer/ui_qt/edit_station_dialog.py | grep -v "^0$"` — combo wiring present.
- `grep -c "source=source" musicstreamer/ui_qt/now_playing_panel.py | grep -v "^0$"` — pass-through present.
- `grep -c "cover_art_source" musicstreamer/settings_export.py | grep -v "^0$"` — round-trip present (≥3 occurrences expected: export, INSERT, REPLACE).
</verification>

<success_criteria>
- ART-MB-10 (settings export round-trip) GREEN.
- ART-MB-12 (EditStationDialog selector) GREEN.
- All 16 ART-MB-NN requirements GREEN by automated tests (ART-MB-01..16). UAT (manual visual + cross-machine) covered by Plan 05.
- No existing test regresses.
- The full user-visible feature is live: user opens EditStationDialog, flips source, saves, the next track's cover fetch honors the new source.
</success_criteria>

<output>
After completion, create `.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-04-SUMMARY.md`.

Update `73-VALIDATION.md`: fill Plan="04" Wave="4" for ART-MB-10 and ART-MB-12. Flip Status to ✅ green. Verify that ALL 16 rows now have non-TBD Plan/Wave values and all are ✅.

Set `nyquist_compliant: true` in the VALIDATION.md frontmatter once all 16 are green.
</output>
