---
phase: 73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - musicstreamer/models.py
  - musicstreamer/repo.py
  - tests/test_repo.py
  - tests/fixtures/mb_recording_search_clean_album_hit.json
  - tests/fixtures/mb_recording_search_bootleg_only.json
  - tests/fixtures/mb_recording_search_score_79.json
  - tests/fixtures/mb_recording_search_score_85.json
  - tests/fixtures/mb_recording_search_no_tags.json
  - tests/fixtures/mb_recording_search_503_body.json
  - tests/test_cover_art_mb.py
  - tests/test_cover_art_routing.py
autonomous: true
requirements:
  - ART-MB-11
tags:
  - musicbrainz
  - cover-art
  - sqlite-migration
  - requirements-registration
must_haves:
  truths:
    - "ART-MB-01..16 are registered in REQUIREMENTS.md under '### Cover-Art Sources (ART-MB)' and the v2.1 OoS row 'MusicBrainz cover art fallback' is removed"
    - "Station dataclass exposes cover_art_source field defaulting to 'auto'"
    - "Fresh database created via db_init has a stations.cover_art_source column with DEFAULT 'auto'"
    - "Pre-Phase-73 database missing the column gains it on db_init() without raising; second db_init() call also does not raise (idempotent)"
    - "Existing stations are backfilled with cover_art_source='auto' via the DEFAULT clause"
    - "Wave 0 test scaffolds and 6 MB JSON fixtures exist on disk so Plans 02/03/04 can run RED→GREEN against them"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ART-MB-01..16 family registered + OoS line removed"
      contains: "### Cover-Art Sources (ART-MB)"
    - path: "musicstreamer/models.py"
      provides: "Station.cover_art_source Literal field"
      contains: "cover_art_source"
    - path: "musicstreamer/repo.py"
      provides: "ALTER TABLE migration for cover_art_source"
      contains: "cover_art_source"
    - path: "tests/test_cover_art_mb.py"
      provides: "Wave 0 RED scaffolds for ART-MB-01..06,13,14,15,16"
    - path: "tests/test_cover_art_routing.py"
      provides: "Wave 0 RED scaffold for ART-MB-09"
    - path: "tests/fixtures/mb_recording_search_clean_album_hit.json"
      provides: "Clean Official+Album MB response fixture (Daft Punk shape)"
    - path: "tests/fixtures/mb_recording_search_bootleg_only.json"
      provides: "Hey-Jude-style Bootleg-top-5 fixture (Pitfall 1)"
    - path: "tests/fixtures/mb_recording_search_score_79.json"
      provides: "score=79 reject fixture (D-09)"
    - path: "tests/fixtures/mb_recording_search_score_85.json"
      provides: "score=85 accept fixture (D-09)"
    - path: "tests/fixtures/mb_recording_search_no_tags.json"
      provides: "tags-key-absent fixture (Pitfall 3)"
    - path: "tests/fixtures/mb_recording_search_503_body.json"
      provides: "MB 503 rate-limit body fixture (Pitfall 7)"
  key_links:
    - from: "musicstreamer/repo.py db_init()"
      to: "stations table"
      via: "ALTER TABLE stations ADD COLUMN cover_art_source TEXT NOT NULL DEFAULT 'auto'"
      pattern: "ALTER TABLE stations ADD COLUMN cover_art_source"
    - from: "musicstreamer/models.py"
      to: "Station dataclass"
      via: "cover_art_source: Literal['auto','itunes_only','mb_only'] = 'auto' field declaration"
      pattern: "cover_art_source.*=.*[\"']auto[\"']"
---

<objective>
Land the Phase 73 foundation: register the ART-MB requirement family, remove the now-stale v2.1 OoS line for MusicBrainz cover art, add the `Station.cover_art_source` dataclass field, add an idempotent `ALTER TABLE` migration in `repo.db_init()` (per D-01/D-05 and RESEARCH §"Pitfall 8"), and scaffold every Wave 0 test file + MB JSON fixture so Plans 02/03/04 can run RED→GREEN against deterministic mocks.

Purpose: this is the foundation that every later plan in Phase 73 depends on. The schema migration **is** the data-layer change (no separate "schema push" step — `db_init` runs at app startup, per the planning_context schema_push_requirement). The fixtures are reused by Plans 02 and 03. The requirements registration is the policy artifact that downstream verification gates against.

Output: REQUIREMENTS.md updated (ART-MB family registered, OoS row removed, traceability table extended); `Station.cover_art_source` field; idempotent schema migration; six JSON fixtures; two RED test files; two repo migration tests GREEN; ART-MB-11 satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-CONTEXT.md
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-RESEARCH.md
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-PATTERNS.md
@.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-VALIDATION.md
@musicstreamer/models.py
@musicstreamer/repo.py
@tests/test_repo.py
@tests/test_cover_art.py

<interfaces>
<!-- Key contracts the executor uses or creates. Extracted from codebase. -->

From musicstreamer/models.py (existing Station shape — Plan 01 inserts cover_art_source AFTER icy_disabled, BEFORE streams):
- `Station.icy_disabled: bool = False` — line 35
- `Station.streams: List[StationStream] = field(default_factory=list)` — line 36
- `Station.last_played_at: Optional[str] = None` — line 37
- `Station.is_favorite: bool = False` — line 38

From musicstreamer/repo.py (existing ALTER TABLE idiom — repo.py:78-94 has six examples; the new ALTER MUST land BEFORE the legacy URL-column rebuild block at line 122-179, per RESEARCH Pitfall 8):
- `con.execute("ALTER TABLE stations ADD COLUMN icy_disabled INTEGER NOT NULL DEFAULT 0")` — line 79
- `con.execute("ALTER TABLE stations ADD COLUMN last_played_at TEXT")` — line 85
- `con.execute("ALTER TABLE stations ADD COLUMN is_favorite INTEGER NOT NULL DEFAULT 0")` — line 91

From tests/test_repo.py (test idiom for migration assertion — `db_init` is called twice in the second test to prove idempotence).

From .planning/REQUIREMENTS.md (existing v2.1 structure — line 79 has the OoS row to remove):
- `### Features (FEAT)` — line 36
- `### Sibling Stations (SIB)` — line 47 (new ART-MB heading lands sibling to this)
- `### Versioning Convention (VER)` — line 52
- `| MusicBrainz cover art fallback | iTunes sufficient; revisit if gaps found |` — line 79 (MUST be removed; this phase IS the revisit)
- `| Requirement | Phase | Status |` traceability table — line 91 (extend with 16 new rows)
- `**Coverage:**` block at line 116-121 (HEAD values: `v2.1 requirements: 22 total`, `Mapped to phases: 22 ✓`, `Unmapped: 0 ✓`, `Complete: 20`, `Pending: 2 (...)`; bump to 38/38/0/20/18)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ART-MB-01..16 in REQUIREMENTS.md and remove the now-stale OoS row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file, ~125 lines — read once, edit in-place)
    - .planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-VALIDATION.md (Per-Task Verification Map — exact behavior text for the 16 reqs)
  </read_first>
  <action>
    Add a new top-level heading `### Cover-Art Sources (ART-MB)` immediately after `### Sibling Stations (SIB)` (around line 50). Under it, register all 16 requirement IDs as a checklist (`- [ ] **ART-MB-NN**: <behavior>`). The behavior text MUST match the "Behavior" column in 73-VALIDATION.md verbatim (e.g., "User-Agent header literal on MB API request matches `MusicStreamer/<version> (https://github.com/lightningjim/MusicStreamer)`"). Group them logically: ART-MB-01/02/15 (User-Agent + source-grep), ART-MB-03/16 (rate gate + source-grep), ART-MB-04/05/13 (match acceptance + genre), ART-MB-06 (queue), ART-MB-07/08/09 (routing per source), ART-MB-10 (settings export), ART-MB-11 (schema migration), ART-MB-12 (UI selector), ART-MB-14 (failure path). All start unchecked.

    Remove the row `| MusicBrainz cover art fallback | iTunes sufficient; revisit if gaps found |` from the Out of Scope table (around line 79) — this phase IS the revisit per CONTEXT §specifics.

    Extend the Traceability table (starts at line 91): append 16 rows mapping ART-MB-01..16 → Phase 73 → "Pending" (status will be updated by the orchestrator on phase close, per Phase 71's SIB-01 precedent).

    Bump the Coverage block at lines 116-121. The block uses the following EXACT labels — match them character-for-character when locating lines to edit (these are the real labels in REQUIREMENTS.md HEAD, NOT `Total:` / `Mapped:`):
      - `- v2.1 requirements: <N> total`     ← currently `22 total`; bump to `38 total`
      - `- Mapped to phases: <N> ✓`           ← currently `22 ✓`; bump to `38 ✓`
      - `- Unmapped: 0 ✓`                     ← unchanged
      - `- Complete: <N>`                     ← currently `20`; unchanged (Phase 73 not yet complete)
      - `- Pending: <N> (<parenthetical>)`    ← currently `2 (WIN-02 — SMTC Start Menu shortcut with AUMID; WIN-05 — AAC Win11 UAT)`; bump the count to `18` but DO NOT enumerate the 16 new ART-MB-NN entries in the parenthetical — leave the parenthetical referring only to the original 2 (`WIN-02 — SMTC Start Menu shortcut with AUMID; WIN-05 — AAC Win11 UAT`). The new ART-MB-NN entries are individually visible in their new `### Cover-Art Sources (ART-MB)` section heading, so no need to enumerate them in the Coverage prose.

    Procedure (read-then-add, NOT hardcoded — but the +16 arithmetic is known-precise because HEAD values are 22/22/0/20/2):
      1. Read the four numeric values currently in the block.
      2. Add 16 to `v2.1 requirements`, `Mapped to phases`, and `Pending`.
      3. Leave `Unmapped` (0) and `Complete` (20) unchanged.
      4. Verify self-consistency: `v2.1 requirements == Mapped to phases == Complete + Pending + Unmapped` (the assertion is in the verify block below).
      5. Post-edit values MUST be EXACTLY:
         - `- v2.1 requirements: 38 total`
         - `- Mapped to phases: 38 ✓`
         - `- Unmapped: 0 ✓`
         - `- Complete: 20`
         - `- Pending: 18 (WIN-02 — SMTC Start Menu shortcut with AUMID; WIN-05 — AAC Win11 UAT)`

    Update the `Last updated:` line at the bottom of REQUIREMENTS.md (currently `*Last updated: 2026-05-12 — SIB-01 ...*`) to today's date with the new note. Final line MUST read EXACTLY:

      `*Last updated: 2026-05-13 — ART-MB-01..16 (MusicBrainz album-cover lookup; per-station 3-mode selector + 1 req/sec rate gate + Lucene-escaped recording search + Official+Album+earliest release selection + ZIP round-trip) added for Phase 73.*`

    Do NOT touch any other section; do NOT renumber existing requirements.
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*ART-MB-" .planning/REQUIREMENTS.md | grep -q "^16$" && ! grep -q "MusicBrainz cover art fallback" .planning/REQUIREMENTS.md && grep -q "### Cover-Art Sources (ART-MB)" .planning/REQUIREMENTS.md && python -c "import re; src=open('.planning/REQUIREMENTS.md').read(); tot=int(re.search(r'v2\.1 requirements:\s*(\d+)', src).group(1)); mapped=int(re.search(r'Mapped to phases:\s*(\d+)', src).group(1)); comp=int(re.search(r'Complete:\s*(\d+)', src).group(1)); pend=int(re.search(r'Pending:\s*(\d+)', src).group(1)); assert tot == 38 and mapped == 38 and comp == 20 and pend == 18, f'drift: tot={tot} mapped={mapped} comp={comp} pend={pend}'; print('OK')" && grep -q "^\*Last updated: 2026-05-13 — ART-MB-01\.\.16" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>REQUIREMENTS.md contains exactly 16 unchecked `- [ ] **ART-MB-NN**` rows, the OoS row for MB fallback is gone, the traceability table lists Phase 73 for ART-MB-01..16, the coverage block reads exactly `v2.1 requirements: 38 total` / `Mapped to phases: 38 ✓` / `Unmapped: 0 ✓` / `Complete: 20` / `Pending: 18 (WIN-02 ...; WIN-05 ...)`, and the `Last updated` line reflects today's date with the ART-MB note.</done>
</task>

<task type="auto">
  <name>Task 2: Add Station.cover_art_source field and schema migration in repo.db_init</name>
  <files>musicstreamer/models.py, musicstreamer/repo.py, tests/test_repo.py</files>
  <read_first>
    - musicstreamer/models.py (full file, 49 lines — Plan 01 adds one field)
    - musicstreamer/repo.py lines 1-200 (db_init body + first Station(...) constructor at line 310)
    - musicstreamer/repo.py lines 295-420 (list_stations, get_station, update_station, list_recently_played — four Station(...) sites that must be extended)
    - musicstreamer/repo.py lines 500-540 (list_favorite_stations — fourth Station(...) site)
    - tests/test_repo.py lines 120-160 (test_icy_disabled_* block — copy this shape for cover_art_source migration tests)
  </read_first>
  <action>
    Per D-01/D-05 + PATTERNS.md "models.py (modified)" section, in `musicstreamer/models.py`:
    1. Add `from typing import Literal` to the existing `from typing import Optional, List` import line.
    2. In the `Station` dataclass, insert `cover_art_source: Literal["auto", "itunes_only", "mb_only"] = "auto"  # Phase 73 D-01/D-05` AFTER `icy_disabled: bool = False` (line 35) and BEFORE `streams: List[StationStream] = field(default_factory=list)` (line 36). Keyword default — never positional, to preserve compat with `test_edit_station_dialog.py:252` and similar positional-args callers (per PATTERNS.md "Ordering caveat").

    Per D-05 + RESEARCH §Pitfall 8, in `musicstreamer/repo.py` `db_init()`:
    3. Add an idempotent `ALTER TABLE` block BEFORE the legacy URL-column migration at line 122. Place it right after the existing `is_favorite` ALTER at line 91-94, using the same try/except sqlite3.OperationalError idiom (copy lines 78-82 verbatim and swap to `cover_art_source TEXT NOT NULL DEFAULT 'auto'`). DO NOT add the column to the legacy rebuild's CREATE TABLE stations_new (line 143-156) or its INSERT SELECT (line 158-162) — the rebuild predates this field; the ALTER lands the column on the rebuilt table afterward, which is the established pattern (Pitfall 8 explicitly accepts this ordering).

    Per PATTERNS.md "Row → dataclass mapping" section, extend FOUR Station(...) constructor call sites in `repo.py` to add `cover_art_source=r["cover_art_source"] or "auto",  # Phase 73 — defensive default` adjacent to the `icy_disabled=bool(r["icy_disabled"])` line:
    - `list_stations` at line ~310
    - `get_station` at line ~345
    - `list_recently_played` at line ~405
    - `list_favorite_stations` at line ~516

    Use grep `grep -n "icy_disabled=bool" musicstreamer/repo.py` to locate every site — there should be exactly 4 matches; each gets a `cover_art_source=` line added immediately after.

    Per D-05 + Pitfall 8, in `tests/test_repo.py`, append three new tests inside the existing test class/module mirroring the `test_icy_disabled_*` block at lines 124-154:
    - `test_cover_art_source_default_is_auto`: create a station with `repo.create_station(...)` (or whatever the existing fixture idiom is — read test_repo.py to discover), call `repo.get_station(sid)`, assert `st.cover_art_source == "auto"`.
    - `test_cover_art_source_round_trip`: same idiom as `test_icy_disabled_round_trip` — write `cover_art_source="mb_only"` via direct SQL UPDATE (since `repo.update_station` is not yet extended; Plan 03 lands that), read back via `get_station`, assert `== "mb_only"`.
    - `test_cover_art_source_migration_idempotent` (ART-MB-11): call `db_init(con)` twice on a fresh connection, assert no exception, assert `pragma table_info('stations')` includes a row with name='cover_art_source' AND dflt_value="'auto'".

    The DEFAULT clause MUST be quoted exactly: `DEFAULT 'auto'` (single quotes around the literal). SQLite stores it verbatim — the test assertion compares against the SQL-quoted form `"'auto'"`.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_repo.py::test_cover_art_source_default_is_auto tests/test_repo.py::test_cover_art_source_round_trip tests/test_repo.py::test_cover_art_source_migration_idempotent -x</automated>
  </verify>
  <done>Three new tests in test_repo.py pass. Station dataclass has cover_art_source field (default 'auto'). repo.db_init adds the column idempotently. All four Station(...) constructor sites in repo.py read the new column with a defensive `or "auto"` fallback. No existing test regresses.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Wave 0 test files + 6 MB JSON fixtures so Plans 02/03/04 can run RED→GREEN</name>
  <files>tests/test_cover_art_mb.py, tests/test_cover_art_routing.py, tests/fixtures/mb_recording_search_clean_album_hit.json, tests/fixtures/mb_recording_search_bootleg_only.json, tests/fixtures/mb_recording_search_score_79.json, tests/fixtures/mb_recording_search_score_85.json, tests/fixtures/mb_recording_search_no_tags.json, tests/fixtures/mb_recording_search_503_body.json</files>
  <read_first>
    - tests/test_cover_art.py (full file, ~55 lines — copy unittest + monkeypatch idiom)
    - tests/test_now_playing_panel.py lines 555-590 (test_icy_disabled_suppresses_itunes_call — copy double-patch idiom for routing tests)
    - .planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-RESEARCH.md §"Code Examples" (live-probed MB JSON shape lines 354-391) and §"Validation Architecture" lines 622-642 (16-row map of test names)
    - .planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-PATTERNS.md §"tests/fixtures/mb_recording_search_*.json" and §"tests/test_cover_art_mb.py"
  </read_first>
  <action>
    Per VALIDATION.md "Wave 0 Requirements" + PATTERNS.md "tests/test_cover_art_mb.py (NEW)" + RESEARCH §"Code Examples":

    **Create 6 MB JSON fixture files** under `tests/fixtures/` using the live-probed shape from RESEARCH lines 354-391 as the template. Each fixture is a complete JSON document (`{"created": ..., "count": ..., "offset": 0, "recordings": [...]}`):

    1. `mb_recording_search_clean_album_hit.json` — one recording, score=100, releases[0] has status="Official", release-group.primary-type="Album", date="2007-11-19", releases[0].id="b9ddde22-fca3-4d94-aee9-f964b34166ce" (the live-verified Daft Punk MBID; this is also the CAA-hit MBID Plan 02 will assert against). Include a `tags` array with `[{"count": 5, "name": "house"}, {"count": 1, "name": "dance"}]` so ART-MB-13 has a multi-tag scenario.
    2. `mb_recording_search_bootleg_only.json` — five recordings, ALL with score=100 (Pitfall 1: score is not a quality signal), all `releases[].status="Bootleg"` or `"Promotion"`, no Official+Album release exists. Used by ART-MB-05 to prove the ladder rejects the top-5 and falls through.
    3. `mb_recording_search_score_79.json` — one recording, score=79, otherwise clean Official+Album. Plan 02 must reject this for ART-MB-04.
    4. `mb_recording_search_score_85.json` — one recording, score=85, clean Official+Album, distinct release MBID `aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee`. Plan 02 must accept this for ART-MB-04.
    5. `mb_recording_search_no_tags.json` — one recording, score=100, clean Official+Album, **NO `tags` key whatsoever** in the recording dict (per Pitfall 3 — MB omits the key entirely when no community tag exists). Plan 02 must return `genre=""` for ART-MB-13.
    6. `mb_recording_search_503_body.json` — NOT a recordings response. Body content for the 503 path: `{"error": "Your requests are exceeding the allowable rate limit. Please see https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting for more information."}` (verbatim from MB docs per RESEARCH Pitfall 7). Used by ART-MB-14.

    All fixtures use 2-space indentation; trailing newline; no Python-style comments (pure JSON).

    **Create `tests/test_cover_art_mb.py`** (NEW file, ~150-200 lines) with Wave 0 RED scaffolds for ART-MB-01, 02, 03, 04, 05, 06, 13, 14, 15, 16. Each test is `pytest.skip("Plan 02 — RED scaffold")` or `pytest.fail("...")` placeholder OR (preferred) an importerror-raising stub: write the test body to import `from musicstreamer import cover_art_mb` — since cover_art_mb.py does not exist yet, the import itself produces RED. Tests:
    - `test_mb_request_carries_user_agent` (ART-MB-01) — full body per RESEARCH §"Code Examples" lines 510-523; will pass once Plan 02 lands the module + _USER_AGENT constant.
    - `test_caa_request_carries_user_agent` (ART-MB-02) — mirror ART-MB-01 but assert against the CAA image request.
    - `test_mb_gate_serializes_with_1s_floor` (ART-MB-03) — full body per RESEARCH §"Code Examples" lines 527-540.
    - `test_score_threshold_rejects_below_80_accepts_at_or_above_80` (ART-MB-04) — parametrize over score_79 / score_85 fixtures + a bare-title input ("Just A Title", no `' - '`) which must short-circuit per D-07.
    - `test_release_selection_ladder_picks_official_album_over_bootleg` (ART-MB-05) — load bootleg_only fixture, assert _pick_release_mbid returns None (no Official+Album); load clean_album_hit fixture, assert it returns the Daft Punk MBID.
    - `test_latest_wins_queue_drops_superseded_jobs` (ART-MB-06) — the scaffold MUST monkeypatch `musicstreamer.cover_art_mb._spawn_worker` to a recording stub (a callable that collects each job dict into a list and immediately returns without spawning anything — never spawns real worker threads). The scaffold's import line is `from musicstreamer import cover_art_mb` and the monkeypatch line targets the attribute name `_spawn_worker` exactly (this is the pinned latest-wins seam — Plan 02 Task 1 step 18 implements `_spawn_worker(target, args)` as the sole call site that wraps `threading.Thread`). Submit 5 rapid jobs via `fetch_mb_cover`; assert the recording stub was called at most 2 times (1 in-flight + 1 final) and the queue contains at most 1 pending job at any time.
    - `test_genre_from_tags_picks_highest_count` (ART-MB-13) — load clean_album_hit, assert `_genre_from_tags(...) == "house"`; load no_tags, assert `== ""`.
    - `test_mb_503_falls_through_to_callback_none` (ART-MB-14) — monkeypatch urlopen to raise `urllib.error.HTTPError(..., 503, ...)`; assert callback is invoked with None and no exception propagates.
    - `test_user_agent_string_literals_present` (ART-MB-15) — full body per PATTERNS.md "Source-grep gates" — reads `cover_art_mb.py` source; will RED until Plan 02 lands the file.
    - `test_rate_gate_uses_monotonic` (ART-MB-16) — same shape; asserts `"time.monotonic"` substring (NOT regex matching a comment-only mention; grep the body of `_MbGate.wait_then_mark` specifically — see "grep gate hygiene" in the planner spec: prefer `import ast` walk OR a non-comment grep `re.search(r"^[^#]*time\.monotonic", src, re.MULTILINE)`).

    **Create `tests/test_cover_art_routing.py`** (NEW file, ~80 lines) with Wave 0 RED scaffolds for ART-MB-09:
    - `test_auto_mode_falls_through_to_mb_when_itunes_misses` (ART-MB-09) — monkeypatch both `musicstreamer.cover_art.urllib.request.urlopen` AND `musicstreamer.cover_art_mb.urllib.request.urlopen` (cover_art_mb monkeypatch silently no-ops until Plan 02 — that's fine; the test stays RED until then). Assert iTunes urlopen called once + MB urlopen called once + final callback receives a non-None path (mocked image bytes).

    The Wave 0 RED state for ART-MB-07/08/10/12 is created by the **absence of the `source` keyword on `fetch_cover_art`** and the **absence of `cover_art_source` in EditStationDialog / settings_export** — those tests live in already-existing test files (test_cover_art.py, test_edit_station_dialog.py, test_settings_export.py) and are scaffolded in their respective plans (Plan 03 / Plan 04). Plan 01 does NOT touch those four — leave them for the consumers.

    Every new test must include a docstring referencing the ART-MB requirement ID and the CONTEXT decision it gates (e.g., `"""ART-MB-01: D-18 User-Agent on MB API request — RESEARCH Pitfall 6 header case."""`).
  </action>
  <verify>
    <automated>ls tests/fixtures/mb_recording_search_*.json | wc -l | grep -q "^6$" && python -c "import json; [json.load(open(p)) for p in __import__('glob').glob('tests/fixtures/mb_recording_search_*.json')]" && uv run --with pytest --with pytest-qt pytest tests/test_cover_art_mb.py tests/test_cover_art_routing.py --collect-only -q 2>&1 | grep -E "collected [0-9]+ item" | grep -vE "collected 0 item" && echo OK</automated>
  </verify>
  <done>Six valid JSON fixtures exist under tests/fixtures/; tests/test_cover_art_mb.py collects ≥10 tests (one per ART-MB-01/02/03/04/05/06/13/14/15/16); tests/test_cover_art_routing.py collects ≥1 test for ART-MB-09. The tests are RED (ImportError on `from musicstreamer import cover_art_mb` is acceptable) but they MUST collect — pytest --collect-only succeeds with non-zero count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite schema | Untrusted: a corrupted .db on disk could have a partial earlier migration; db_init must be idempotent across all prior shapes |
| Test fixtures | Trusted: in-repo JSON, but Plans 02/03 will treat them as opaque payloads to mock urlopen → they exercise the parser as if MB had returned them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-08 | Tampering | repo.db_init ALTER TABLE | mitigate | Wrap ALTER in try/except sqlite3.OperationalError per existing idiom (repo.py:79-94) — idempotent across re-runs and across freshly-created DBs (which already have the column from CREATE TABLE on stations? — NO, the CREATE TABLE in db_init lines 23-33 does NOT include cover_art_source; the ALTER block is the SOLE source of the column. This is acceptable: same shape as is_favorite at repo.py:91-94 which also only lives in ALTER, not CREATE.) |
| T-73-09 | Information Disclosure | REQUIREMENTS.md OoS removal | accept | Removing a row that explicitly invited revisit is the documented closure pattern; no information loss (the original v2.1 milestone shape is preserved in git history) |
| T-73-10 | Repudiation | Wave 0 RED test absence claims | mitigate | Source-grep gates ART-MB-15/16 (scaffolded as RED in Task 3) prove later that protocol-required strings actually exist in the source — not just claimed in a comment |
</threat_model>

<verification>
After Plan 01 completes:
- `uv run --with pytest --with pytest-qt pytest tests/test_repo.py -x` — all repo tests pass including 3 new ones
- `uv run --with pytest --with pytest-qt pytest tests/test_cover_art_mb.py tests/test_cover_art_routing.py --collect-only -q` — collects ≥11 tests (Plan 02 + Plan 03 + Plan 04 will turn them GREEN incrementally)
- `grep -c "^- \[ \] \*\*ART-MB-" .planning/REQUIREMENTS.md` returns 16
- `! grep -q "MusicBrainz cover art fallback" .planning/REQUIREMENTS.md` succeeds (OoS row gone)
- `python -c "from musicstreamer.models import Station; s = Station(id=1, name='x', provider_id=None, provider_name=None, tags='', station_art_path=None, album_fallback_path=None); assert s.cover_art_source == 'auto'"` succeeds
- Full suite (`uv run --with pytest --with pytest-qt pytest tests -x --ignore=tests/test_cover_art_mb.py --ignore=tests/test_cover_art_routing.py`) is GREEN — no regression. The two new files are excluded because they intentionally RED-import `cover_art_mb` which Plan 02 has not yet created.
</verification>

<success_criteria>
- 16 ART-MB rows in REQUIREMENTS.md; OoS line for MB fallback removed; Traceability table extended.
- Station.cover_art_source field default 'auto'; idempotent ALTER TABLE migration in repo.db_init.
- 3 new tests in test_repo.py GREEN; test_cover_art_source_migration_idempotent satisfies ART-MB-11.
- 6 valid MB JSON fixtures on disk.
- 2 new test files collect non-zero tests with RED bodies (Plan 02 + Plan 03 + Plan 04 turn them GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-01-SUMMARY.md`.

Also update `.planning/phases/73-musicbrainz-album-cover-lookup-complement-itunes-with-smart-/73-VALIDATION.md`: in the Per-Task Verification Map, fill Plan="01" Wave="1" for ART-MB-11; leave other rows as Plan="TBD" Wave="TBD" — Plans 02/03/04 will fill their own rows.
</output>
</output>
