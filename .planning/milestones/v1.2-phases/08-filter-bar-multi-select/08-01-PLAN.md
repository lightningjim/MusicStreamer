---
phase: 08-filter-bar-multi-select
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - musicstreamer/filter_utils.py
  - tests/test_filter_utils.py
autonomous: true
requirements:
  - BROWSE-02
  - BROWSE-03

must_haves:
  truths:
    - "matches_filter_multi returns True when station provider is in provider_set"
    - "matches_filter_multi returns True when station has any tag in tag_set"
    - "matches_filter_multi returns False when station fails AND composition of provider + tag"
    - "matches_filter_multi returns True for all stations when both sets are empty"
    - "Tag matching is case-insensitive"
  artifacts:
    - path: "musicstreamer/filter_utils.py"
      provides: "matches_filter_multi function"
      exports: ["matches_filter_multi"]
      contains: "def matches_filter_multi"
    - path: "tests/test_filter_utils.py"
      provides: "Unit tests for multi-select filter"
      contains: "test_matches_filter_multi"
  key_links:
    - from: "tests/test_filter_utils.py"
      to: "musicstreamer/filter_utils.py"
      via: "import matches_filter_multi"
      pattern: "from musicstreamer.filter_utils import.*matches_filter_multi"
---

<objective>
Add `matches_filter_multi` to filter_utils.py — a multi-select variant of the existing `matches_filter` that accepts `set[str]` for providers and tags instead of single strings.

Purpose: BROWSE-02 and BROWSE-03 require OR-within-dimension, AND-between-dimensions filter logic. This function is the filter engine Plan 02's UI will call.
Output: `matches_filter_multi` function + comprehensive test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-filter-bar-multi-select/08-CONTEXT.md
@.planning/phases/08-filter-bar-multi-select/08-RESEARCH.md

@musicstreamer/filter_utils.py
@musicstreamer/models.py
@tests/test_filter_utils.py

<interfaces>
From musicstreamer/models.py:
```python
@dataclass
class Station:
    id: int
    name: str
    url: str
    provider_id: Optional[int]
    provider_name: Optional[str]
    tags: str
    station_art_path: Optional[str]
    album_fallback_path: Optional[str]
    icy_disabled: bool = False
    last_played_at: Optional[str] = None
```

From musicstreamer/filter_utils.py:
```python
def normalize_tags(raw: str) -> list[str]: ...
def matches_filter(station, search_text, provider_filter, tag_filter) -> bool: ...
```
</interfaces>
</context>

<tasks>

<feature>
  <name>matches_filter_multi — multi-select filter function</name>
  <files>musicstreamer/filter_utils.py, tests/test_filter_utils.py</files>
  <read_first>
    - musicstreamer/filter_utils.py (current implementation, normalize_tags function)
    - tests/test_filter_utils.py (existing test patterns, make_station helper)
    - musicstreamer/models.py (Station dataclass fields)
  </read_first>
  <behavior>
    - Test: empty provider_set + empty tag_set + empty search -> returns True for any station
    - Test: provider_set={"Soma.FM"} matches station with provider_name="Soma.FM" -> True
    - Test: provider_set={"Soma.FM"} does not match station with provider_name="DI.fm" -> False
    - Test: provider_set={"Soma.FM", "DI.fm"} matches station with provider_name="DI.fm" -> True (OR within providers, D-08)
    - Test: tag_set={"lofi"} matches station with tags="Lofi, Chill" -> True (case-insensitive)
    - Test: tag_set={"lofi"} does not match station with tags="Rock, Blues" -> False
    - Test: tag_set={"lofi", "rock"} matches station with tags="Rock, Blues" -> True (OR within tags, D-09)
    - Test: provider_set={"Soma.FM"} + tag_set={"lofi"} -> station must match BOTH (AND composition, D-10)
    - Test: provider_set={"Soma.FM"} + tag_set={"lofi"} -> station with provider="DI.fm" tags="Lofi" -> False (provider fails)
    - Test: provider_set={"Soma.FM"} + tag_set={"rock"} -> station with provider="Soma.FM" tags="Lofi" -> False (tag fails)
    - Test: search_text="groove" + provider_set={"Soma.FM"} -> station name must contain "groove" AND match provider
    - Test: tag matching with bullet-separated tags ("lofi \u2022 chill") works
    - Test: provider_set with station having provider_name=None -> False
  </behavior>
  <implementation>
    Add `matches_filter_multi` function to `musicstreamer/filter_utils.py` with this exact signature:

    ```python
    def matches_filter_multi(
        station: Station,
        search_text: str,
        provider_set: set[str],
        tag_set: set[str],
    ) -> bool:
    ```

    Logic:
    1. If search_text is truthy: casefold substring match against station.name; return False on miss
    2. If provider_set is non-empty: return False if station.provider_name not in provider_set
    3. If tag_set is non-empty: normalize station.tags via normalize_tags, casefold both sides, return False if no intersection
    4. Return True

    Casefold tag_set at match time: `{t.casefold() for t in tag_set}` intersected with `{t.casefold() for t in normalize_tags(station.tags)}`.

    Keep the existing `matches_filter` function unchanged (backward compatible).
  </implementation>
  <acceptance_criteria>
    - musicstreamer/filter_utils.py contains `def matches_filter_multi(`
    - musicstreamer/filter_utils.py contains `provider_set: set[str]` in the function signature
    - musicstreamer/filter_utils.py contains `tag_set: set[str]` in the function signature
    - tests/test_filter_utils.py contains `from musicstreamer.filter_utils import` with `matches_filter_multi`
    - tests/test_filter_utils.py contains at least 10 test functions matching `test_matches_filter_multi`
    - `python -m pytest tests/test_filter_utils.py -x -q` exits 0
    - Existing `matches_filter` function unchanged (still present, same signature)
    - Existing tests still pass (no regressions)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -m pytest tests/test_filter_utils.py -x -q</automated>
  </verify>
  <done>matches_filter_multi function implemented and tested with all multi-select filter behaviors: OR within providers, OR within tags, AND between dimensions, case-insensitive tag matching, empty sets inactive</done>
</feature>

</tasks>

<verification>
- `python -m pytest tests/test_filter_utils.py -x -q` exits 0
- All existing tests still pass (no regression)
- New function exported from filter_utils module
</verification>

<success_criteria>
- matches_filter_multi accepts set[str] for providers and tags
- OR logic within each dimension verified by tests
- AND logic between dimensions verified by tests
- Case-insensitive tag matching verified by tests
- Empty sets = inactive filter verified by tests
- Existing matches_filter untouched
</success_criteria>

<output>
After completion, create `.planning/phases/08-filter-bar-multi-select/08-01-SUMMARY.md`
</output>
