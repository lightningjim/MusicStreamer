---
phase: 11-ui-polish
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified: [musicstreamer/__main__.py]
autonomous: false
requirements: [UI-03]
gap_closure: true

must_haves:
  truths:
    - "Station logo and cover art in the now-playing panel have visible 5px rounded corners"
  artifacts:
    - path: "musicstreamer/__main__.py"
      provides: ".now-playing-art CSS rule with background-color and overflow"
      contains: "overflow: hidden"
  key_links:
    - from: "_APP_CSS .now-playing-art"
      to: "logo_stack / cover_stack Gtk.Stack widgets"
      via: "CSS class applied to Gtk.Stack containers"
      pattern: "background-color.*transparent.*overflow.*hidden"
---

<objective>
Fix station art border-radius not visually clipping in GTK4.

Purpose: GTK4 requires a painted background node and overflow:hidden for border-radius to clip child Gtk.Image content. The current .now-playing-art rule only sets border-radius, which has no visible effect.
Output: Updated _APP_CSS with working rounded corners on station art.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.2-phases/11-ui-polish/11-01-SUMMARY.md
@.planning/milestones/v1.2-phases/11-ui-polish/11-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .now-playing-art CSS rule for GTK4 border-radius clipping</name>
  <files>musicstreamer/__main__.py</files>
  <action>
In musicstreamer/__main__.py, find the _APP_CSS string and update the .now-playing-art rule (currently around line 30-32) from:

```css
.now-playing-art {
    border-radius: 5px;
}
```

to:

```css
.now-playing-art {
    border-radius: 5px;
    background-color: transparent;
    overflow: hidden;
}
```

GTK4 needs a background node (even transparent) to create the clip mask, and overflow:hidden to actually clip the child Gtk.Image rendering to the rounded rect.

Do not change any other CSS rules or any Python code.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -c "from musicstreamer.__main__ import _APP_CSS; assert 'background-color: transparent' in _APP_CSS; assert 'overflow: hidden' in _APP_CSS; assert 'border-radius: 5px' in _APP_CSS; print('CSS rule verified')"</automated>
  </verify>
  <done>.now-playing-art CSS rule contains border-radius: 5px, background-color: transparent, and overflow: hidden</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify rounded corners visible on station art</name>
  <files>musicstreamer/__main__.py</files>
  <action>Visual verification of the CSS fix.</action>
  <what-built>Fixed station art border-radius clipping by adding background-color: transparent and overflow: hidden to the .now-playing-art CSS rule.</what-built>
  <how-to-verify>
    1. Launch the app: python -m musicstreamer
    2. Play any station that has a logo or cover art
    3. Look at the station art in the now-playing panel at the bottom
    4. Confirm the art image has visible slightly-rounded corners (5px radius)
  </how-to-verify>
  <verify>User confirms rounded corners are visible</verify>
  <done>User approves that station art displays with rounded corners</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- _APP_CSS contains all three properties in .now-playing-art rule
- Visual confirmation that art corners are rounded in the running app
</verification>

<success_criteria>
Station logo and cover art in the now-playing panel display with visible 5px rounded corners, closing UAT test 3.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2-phases/11-ui-polish/11-02-SUMMARY.md`
</output>
