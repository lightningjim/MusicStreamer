---
phase: 89-youtube-channel-avatar-fetch-cover-slot-swap
plan: 04
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
  - tests/test_now_playing_panel.py
autonomous: true
requirements: [ART-AVATAR-06, ART-AVATAR-08]
must_haves:
  truths:
    - "An ICY-disabled YT station with a stored avatar shows a circular-cropped avatar in the cover slot; the station thumbnail shows first then swaps to the avatar (D-05, D-09)"
    - "The circular crop is center-cropped, antialiased, borderless, sized to the current art tier (D-06)"
    - "On panel resize the circular avatar re-renders at the new tier (does not revert to thumbnail)"
    - "When no avatar is stored or the PNG fails to load, the slot stays on the station thumbnail (D-08)"
    - "Cached avatar load from station-bind completes well under 1 second"
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "circular-avatar render path + _last_avatar_path tier-replay + bind-time load"
      contains: "_set_avatar_pixmap_from_path"
  key_links:
    - from: "now_playing_panel.bind_station"
      to: "_set_avatar_pixmap_from_path"
      via: "icy_disabled and station.channel_avatar_path"
      pattern: "_set_avatar_pixmap_from_path"
    - from: "now_playing_panel._apply_art_tier"
      to: "_last_avatar_path"
      via: "elif branch between real-cover and station-logo fallback"
      pattern: "elif self\\._last_avatar_path"
---

<objective>
Add the circular-avatar render path to `now_playing_panel.py` (ART-AVATAR-06/08): a `_make_circular_pixmap` helper, a `_set_avatar_pixmap_from_path` render method with its own `_last_avatar_path` tracked state, an `elif` branch in `_apply_art_tier` for resize replay, and the bind-time avatar load for ICY-disabled YT stations — all without altering the Phase 72.3 `_set_cover_pixmap` or D-08 `_show_station_logo_in_cover_slot` fallback.

Purpose: This is the user-visible payoff — ICY-disabled YT stations (e.g., Lofi Girl) get a circular channel avatar in the cover slot instead of a duplicated square thumbnail, while real covers and the no-avatar fallback keep today's square behavior.
Output: the new render path, tier-replay participation, bind-time load, and Qt unit tests covering swap, fallback, resize replay, and the <1s cached-load budget.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-RESEARCH.md
@.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-PATTERNS.md

<interfaces>
<!-- Verified from codebase. Use directly — no exploration. -->

now_playing_panel.py (verified line numbers):
  _current_art_tier: Optional[int]          # L315
  _last_cover_path: Optional[str] = None    # L316  (add _last_avatar_path alongside)
  cover_art_ready = Signal(str)             # L231 ; wired QueuedConnection L851-853
  bind_station(...)                         # L878-948 ; ICY-disabled handling ~L887;
                                            #   _show_station_logo_in_cover_slot() call ~L901
  _apply_art_tier(...)                      # L2043-2085 ; tail branch L2082:
                                            #     if _last_cover_path is not None: _set_cover_pixmap
                                            #     else: _show_station_logo_in_cover_slot()
  _on_cover_art_ready(payload)              # L2126-2139 ; token-guard template
  _set_cover_pixmap(path)                   # L2141-2156 ; sets _last_cover_path — DO NOT ALTER (Phase 72.3)
  _show_station_logo_in_cover_slot()        # L2197-2204 ; sets _last_cover_path=None — DO NOT ALTER (D-08)

Station (from Plan 01): channel_avatar_path: Optional[str] (path relative to data_dir)
paths.data_dir() resolves the absolute base for the relative avatar path.
Verify QPainter, QPainterPath, QSize imports at top of file before using them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add circular-crop helper + _set_avatar_pixmap_from_path render method</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py, tests/test_now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (top-of-file imports — confirm QPainter/QPainterPath/QSize present; _set_cover_pixmap L2141-2156 to mirror; _show_station_logo_in_cover_slot L2197-2204 fallback; _current_art_tier/_last_cover_path L315-316)
    - 89-PATTERNS.md §`musicstreamer/ui_qt/now_playing_panel.py` (_make_circular_pixmap helper, _set_avatar_pixmap_from_path mirroring _set_cover_pixmap but tracking _last_avatar_path and clipping to circle)
    - 89-RESEARCH.md §Pattern 4 (QPainter antialiased circular clip) + Pattern 3 + Pitfall 8 (QPixmap main-thread only) + Assumptions A1/A4 (verify QPainter.RenderHint.Antialiasing enum path in installed PySide6 6.10.2)
    - tests/test_now_playing_panel.py (existing Qt test harness — QApplication fixture, how panel is constructed in tests, _root_override usage)
  </read_first>
  <behavior>
    - _make_circular_pixmap(src, n) returns an n×n QPixmap with transparent corners (alpha 0 at corner pixel) and opaque center — i.e. a circular crop.
    - _set_avatar_pixmap_from_path(rel_path) with a valid PNG sets cover_label's pixmap and sets _last_avatar_path == rel_path, WITHOUT setting _last_cover_path.
    - _set_avatar_pixmap_from_path(rel_path) with a null/missing PNG falls back via _show_station_logo_in_cover_slot() and leaves _last_avatar_path == None (cleared on failure so the next _apply_art_tier resize does not retry the bad path — self-healing).
    - Loading a small cached PNG completes in well under 1s (assert elapsed < 1.0).
  </behavior>
  <action>
    Add `_last_avatar_path: Optional[str] = None` in __init__ right after `_last_cover_path` (~L316). Add a `_make_circular_pixmap(source, size)` helper (module-level function or private method) per 89-RESEARCH.md Pattern 4: scale with `Qt.KeepAspectRatioByExpanding` + `Qt.SmoothTransformation`, center-crop to exact square (D-07 center-crop), paint into a transparent QPixmap through a `QPainterPath.addEllipse(0,0,size,size)` clip with `QPainter.RenderHint.Antialiasing` (D-06, no border/ring). Verify QPainter/QPainterPath/QSize are imported; add to the import block only if missing. Add `_set_avatar_pixmap_from_path(rel_path)` mirroring `_set_cover_pixmap` but: build abs path via `paths.data_dir()`, `QPixmap(abs)`, on `isNull()` (load failure) FIRST set `self._last_avatar_path = None` (clear the bad path so a subsequent `_apply_art_tier` resize does NOT retry the corrupt PNG — keeps `_last_avatar_path` and `_last_cover_path` mutually exclusive and self-healing), THEN call `_show_station_logo_in_cover_slot()` and return (do not set a success path), else `_make_circular_pixmap(pix, self._current_art_tier or 180)`, set on cover_label, set `self._last_avatar_path = rel_path` (NOT _last_cover_path — D-05 orthogonality). Must run on main thread only (Pitfall 8). Add tests to tests/test_now_playing_panel.py under `-k avatar` for circular alpha, state tracking, null-fallback, and the <1s timing assertion (ART-AVATAR-08).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_now_playing_panel.py -k avatar -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _make_circular_pixmap\|def _set_avatar_pixmap_from_path\|_last_avatar_path" musicstreamer/ui_qt/now_playing_panel.py` shows all three; `_set_cover_pixmap` and `_show_station_logo_in_cover_slot` bodies are unchanged (Phase 72.3 / D-08).
    - `.venv/bin/python -m pytest tests/test_now_playing_panel.py -k avatar -x` passes: circular alpha corners, _last_avatar_path set on success / not on failure, <1s load.
    - A null/corrupt cached PNG fallback leaves `_last_avatar_path == None`, so a subsequent resize/`_apply_art_tier` takes the clean station-thumbnail branch (not the corrupt-avatar branch) and does NOT re-load the bad path — covered by an explicit test in tests/test_now_playing_panel.py.
  </acceptance_criteria>
  <done>Circular render path exists with its own tracked state, antialiased borderless crop, instant cached load, and clean null-fallback; avatar render tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire bind-time avatar load + _apply_art_tier resize replay</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py, tests/test_now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py (bind_station L878-948 — ICY-disabled path ~L887, _show_station_logo_in_cover_slot() call ~L901; _apply_art_tier tail branch L2082; the Task 1 _set_avatar_pixmap_from_path / _last_avatar_path)
    - 89-PATTERNS.md §`now_playing_panel.py` (bind_station ICY-disabled avatar load — reset _last_avatar_path FIRST then conditional load; _apply_art_tier elif insertion between real-cover and fallback)
    - 89-RESEARCH.md §Pattern 3 + Pitfall 4 (reset _last_avatar_path on bind to avoid stale-station bleed) + Pitfall 6 (elif ordering — real cover wins) + D-08 (no-avatar reverts to today's behavior) + D-09 (thumbnail-first then swap, no flicker-to-blank)
  </read_first>
  <behavior>
    - bind_station on an ICY-disabled station WITH channel_avatar_path renders the circular avatar (cover_label shows the circular pixmap; _last_avatar_path set).
    - bind_station on an ICY-disabled station WITHOUT channel_avatar_path leaves the station thumbnail (D-08 — _last_avatar_path stays None).
    - Binding station A (with avatar) then station B (no avatar) does NOT leave A's avatar showing (Pitfall 4 — reset on bind).
    - After _apply_art_tier with only _last_avatar_path set, the circular avatar re-renders (not the thumbnail); with _last_cover_path set the real cover still wins (Pitfall 6 ordering).
  </behavior>
  <action>
    In bind_station, immediately after the existing `_show_station_logo_in_cover_slot()` call (~L901, which shows the thumbnail first per D-09), add `self._last_avatar_path = None` (reset to avoid stale-station bleed — Pitfall 4), then: `if getattr(station, "icy_disabled", False) and getattr(station, "channel_avatar_path", None): self._set_avatar_pixmap_from_path(station.channel_avatar_path)`. The thumbnail already showed first (D-09); the cached PNG load is effectively instant so no async is needed at bind time (ART-AVATAR-08). Do NOT modify _show_station_logo_in_cover_slot itself (D-08). In _apply_art_tier, change the tail branch (L2082) to insert an `elif self._last_avatar_path is not None: self._set_avatar_pixmap_from_path(self._last_avatar_path)` BETWEEN the existing `if self._last_cover_path is not None:` (real cover, keep first — Pitfall 6) and the `else: _show_station_logo_in_cover_slot()`. Add the bind/swap/fallback/stale-reset/resize-replay tests to tests/test_now_playing_panel.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_now_playing_panel.py -k avatar -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "elif self._last_avatar_path is not None" musicstreamer/ui_qt/now_playing_panel.py` matches and sits between the _last_cover_path branch and the _show_station_logo_in_cover_slot fallback.
    - `grep -n "self._last_avatar_path = None" musicstreamer/ui_qt/now_playing_panel.py` shows the bind-time reset.
    - `.venv/bin/python -m pytest tests/test_now_playing_panel.py -k avatar -x` passes: ICY-disabled+avatar swaps, no-avatar falls back, stale-station reset works, resize replays the circle, real cover still wins over avatar.
  </acceptance_criteria>
  <done>ICY-disabled YT stations with a stored avatar show the circular avatar at bind and across resizes; no-avatar and stale-station cases fall back cleanly; precedence (cover > avatar > thumbnail) holds; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached PNG → Qt render | a stored avatar PNG (possibly corrupt/partial) is loaded into the Qt main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-10 | Denial of Service | corrupt/non-image avatar PNG crashing the cover slot (and re-loaded on every resize) | mitigate | _set_avatar_pixmap_from_path checks QPixmap.isNull(), clears _last_avatar_path=None, then falls back to _show_station_logo_in_cover_slot() — render never crashes the Qt main thread (WR-04) and the bad path is not retried on the next _apply_art_tier resize (self-healing) |
| T-89-11 | Denial of Service | QPixmap construction off the main thread | mitigate | bind-time avatar load runs on the main thread (bind_station is always main-thread); no worker touches QPixmap (Pitfall 8) |
| T-89-12 | Information Disclosure | stale station's avatar bleeding into a new station | mitigate | _last_avatar_path reset to None at the top of each bind before the conditional load (Pitfall 4) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_now_playing_panel.py -k avatar -x` green.
- Manual (89-VALIDATION.md): bind a real ICY-disabled YT station with a stored avatar — confirm clean borderless antialiased circle, balanced size vs neighboring square covers.
</verification>

<success_criteria>
- ICY-disabled YT station with stored avatar shows a circular avatar in the cover slot (ART-AVATAR-06).
- Cached load is well under 1s; missing/corrupt avatar reverts to the station thumbnail (ART-AVATAR-08 / D-08).
- Circle re-renders on resize; real covers and the fallback keep their square Phase 72.3 behavior (D-05).
</success_criteria>

<output>
Create `.planning/phases/89-youtube-channel-avatar-fetch-cover-slot-swap/89-04-SUMMARY.md` when done.
</output>
