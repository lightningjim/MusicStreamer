---
phase: 95-yt-url-change-replay-bug-post-edit-stream-exhausted-on-first
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - musicstreamer/player.py
  - tests/_fake_player.py
  - tests/test_player_edit_invalidation.py
autonomous: true
requirements: []
tdd: true
gap_closure: true

must_haves:
  truths:
    - "GAP (UAT Test 1): editing a playing YouTube station's stream URL while audio is live no longer flashes a 'Stream exhausted' toast before the new URL plays — the stale, pre-restart error-recovery from the OLD exhausted stream is suppressed."
    - "A QUEUED _error_recovery_requested delivery that was POSTED (from the bus thread) BEFORE the most recent play()/invalidate_for_edit restart no-ops in _handle_gst_error_recovery instead of advancing the freshly-emptied queue to failover.emit(None)."
    - "HARD CONSTRAINT: a GENUINE current-generation exhaustion (every stream of the currently-playing station truly fails, error posted AFTER the current restart) STILL reaches _try_next_stream() on an empty queue and STILL emits failover.emit(None) → 'Stream exhausted' toast. The guard rejects only stale (pre-restart) recoveries, never current-generation ones."
    - "A metadata-only edit (D-02, URL unchanged) is unaffected: it does not restart, does not bump the recovery generation in a way that suppresses a legitimate current-stream recovery."
    - "_error_recovery_requested carries the recovery generation captured at error-POST time (bus thread), compared against the current _recovery_seq at recovery-RUN time (main thread); the FakePlayer parity drift-guard stays green after the Signal arity change."
    - "BACKWARD COMPAT: every existing direct test caller of _handle_gst_error_recovery() (no-arg) stays green WITHOUT edits because the signature default is the -1 sentinel ('skip staleness check'), NOT 0 — even when play() has already bumped _recovery_seq to ≥1 before the no-arg call."
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "_recovery_seq generation counter (bumped in play() entry, which every restart including invalidate_for_edit delegates through); _error_recovery_requested widened to Signal(int) carrying the post-time stamp; -1-sentinel stale-recovery early-return in _handle_gst_error_recovery"
      contains: "_recovery_seq"
    - path: "tests/_fake_player.py"
      provides: "_error_recovery_requested Signal arity mirrored to Signal(int) (parity-guard requirement)"
      contains: "_error_recovery_requested"
    - path: "tests/test_player_edit_invalidation.py"
      provides: "V11 stale-recovery-suppressed, V12 genuine-current-generation-exhaustion-still-toasts, V13 metadata-only-recovery-unaffected coverage"
      min_lines: 120
  key_links:
    - from: "Player._on_gst_error"
      to: "Player._handle_gst_error_recovery"
      via: "_error_recovery_requested Signal(int) carrying the _recovery_seq captured at post time"
      pattern: "_error_recovery_requested\\.emit\\(self\\._recovery_seq\\)"
    - from: "Player.play / Player.invalidate_for_edit restart"
      to: "Player._handle_gst_error_recovery stale-reject"
      via: "_recovery_seq bump supersedes any earlier-posted recovery"
      pattern: "_recovery_seq \\+= 1"
    - from: "Player._handle_gst_error_recovery"
      to: "early-return on stale generation (sentinel-aware)"
      via: "if recovery_seq != -1 and recovery_seq != self._recovery_seq: return"
      pattern: "!= -1 and recovery_seq != self\\._recovery_seq"
---

<objective>
Close the residual Phase 95 UAT gap: after Plan 01's fix, editing a playing
YouTube station's stream URL correctly replays the new URL on first play, but a
spurious "Stream exhausted" toast still flashes briefly before the new URL plays.

Root cause (from .planning/debug/95-spurious-stream-exhausted-toast.md): a QUEUED
error-recovery from the OLD exhausted YouTube stream — posted via QueuedConnection
from the GStreamer bus thread — runs deferred on the main loop AFTER the
synchronous edit→play() restart has already emptied `_streams_queue` for the new
(still-resolving) URL and reset `_recovery_in_flight=False`. With the coalescing
guard cleared and the queue empty, `_handle_gst_error_recovery` calls
`_try_next_stream()` → `failover.emit(None)` → `show_toast("Stream exhausted")`.
The error-recovery path has NO edit/restart generation guard, unlike the
`_youtube_resolve_seq` guard Plan 01 added to the resolve path.

Purpose: add an edit/restart generation guard to the ERROR-RECOVERY path,
mirroring the existing `_youtube_resolve_seq` / `_preroll_seq` idiom, so a stale
pre-restart recovery no-ops — WITHOUT suppressing a genuine current-generation
exhaustion (which must still toast).

Output: a `_recovery_seq` monotonic counter bumped at the top of `play()` (every
restart, including invalidate_for_edit's `self.play(station)` delegation);
`_error_recovery_requested` widened to `Signal(int)` so the stamp is captured at
error-POST time (bus thread) and checked at recovery-RUN time (main thread); a
sentinel-aware stale early-return in `_handle_gst_error_recovery`; FakePlayer parity
for the widened signal; and unit coverage V11/V12/V13.

Approach mirrors the Plan 01 `_preroll_seq` / `_youtube_resolve_seq` mechanism
exactly — capture the generation where the cross-thread event is POSTED, reject
deliveries whose stamp != current at RUN time. Do NOT suppress the toast globally;
do NOT touch the legitimate `_recovery_in_flight` cascade-coalescing behavior.

Resolved implementation choices (baked into tasks):
- Q1 (dedicated vs reused counter) → dedicated `_recovery_seq`. The existing
  `_youtube_resolve_seq` is resolve-path-specific and is bumped on metadata-only
  edits too (Plan 01 bumps it unconditionally first in invalidate_for_edit); reusing
  it would over-suppress. A dedicated `_recovery_seq` makes the recovery-path
  semantics independent and auditable, matching the one-counter-per-async-path
  precedent (`_preroll_seq` for preroll, `_youtube_resolve_seq` for resolve).
- Q2 (stamp at post-time vs run-time) → POST time. The stamp MUST be captured where
  `_error_recovery_requested` is emitted (`_on_gst_error`, bus thread, player.py:991),
  carried on the Signal, and compared at run time. Capturing at run time would read
  the post-restart seq and let a pre-restart recovery pass the guard — defeating the
  fix. Post-time capture rejects exactly the recoveries posted before the most recent
  restart and accepts genuine current-generation ones.
- Q3 (where to bump) → bump `_recovery_seq` at the TOP of `play()` (covers every
  restart path, including invalidate_for_edit's `self.play(station)` delegation).
  Bump it ONCE at play() entry; invalidate_for_edit needs no redundant bump because
  its restart goes through `play()`. (The D-04/D-05 no-restart branches of
  invalidate_for_edit do NOT bump — they leave the current generation valid so a
  legitimate same-session recovery still toasts.)
- Q4 (signature default — BACKWARD COMPAT) → the default is the `-1` SENTINEL, NOT
  `0`. `_handle_gst_error_recovery(self, recovery_seq: int = -1)` with the guard
  `if recovery_seq != -1 and recovery_seq != self._recovery_seq: return`. Rationale:
  many existing tests call `p.play(station)` first (bumping `_recovery_seq` to ≥1)
  and THEN call `p._handle_gst_error_recovery()` with no arg. A `default=0` guard
  would evaluate `0 != 1` → True → early-return, regressing those green tests. The
  `-1` sentinel means "no explicit stamp → skip the staleness check (treat as current
  generation)", so every no-arg direct caller bypasses the guard unconditionally,
  while only an EXPLICITLY-stamped delivery (the real QueuedConnection emit carrying
  `self._recovery_seq`) is checked. V11/V12 pass EXPLICIT seqs, so they exercise the
  real checked branch (they do not rely on the default).

NOTE on Plan-02 "research": Plan 01's three `## Open Questions` in 95-RESEARCH.md
(:301-312) are Plan-01-scoped and were closed by Plan 01's execution; they are not
re-opened here. The "research" backing THIS gap-closure plan is the status:diagnosed
debug doc `.planning/debug/95-spurious-stream-exhausted-toast.md` (referenced in
<context>), not 95-RESEARCH.md's open-questions block. Those shipped Plan-01 artifacts
are intentionally left untouched.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-yt-url-change-replay-bug-post-edit-stream-exhausted-on-first/95-UAT.md
@.planning/debug/95-spurious-stream-exhausted-toast.md
@.planning/phases/95-yt-url-change-replay-bug-post-edit-stream-exhausted-on-first/95-01-SUMMARY.md

<interfaces>
<!-- Extracted from live code 2026-06-19; use directly, no re-exploration. -->

Player signals + state (musicstreamer/player.py):
- `_error_recovery_requested = Signal()` (:286) — bus-loop → main; connected QueuedConnection to `_handle_gst_error_recovery` (:523-525). MUST be widened to `Signal(int)` to carry the post-time recovery generation.
- `youtube_resolved = Signal(str, bool, int)` (:273) — the Plan 01 precedent for a generation-carrying widened Signal.
- `_preroll_seq: int = 0` (:583) and `_youtube_resolve_seq: int = 0` (:591) — the two existing generation-counter idioms to mirror; rationale comments :576-591. The new `_recovery_seq` declares alongside them.
- `_recovery_in_flight: bool = False` (:567) — the EXISTING per-URL cascade-coalescing guard. Do NOT remove or repurpose it; the new seq guard is orthogonal (one suppresses cross-restart staleness, the other coalesces same-URL cascades).

play() (:682):
- `def play(self, station, on_title=None, preferred_quality="", on_failover=None, on_offline=None)` — entry does `_cancel_timers()` (:685), `_streams_queue = []` (:686), `_recovery_in_flight = False` (:687). The `_recovery_seq += 1` bump goes HERE at the top (right after :687, before the preroll-teardown block at :698) so EVERY restart — including invalidate_for_edit's `self.play(station)` — supersedes earlier-posted recoveries.

_on_gst_error (:983) — runs on the GstBusLoopThread (daemon):
- `def _on_gst_error(self, bus, msg)`: emits `playback_error`, `_tracker.note_error_in_cycle()`, then `self._error_recovery_requested.emit()` (:991). CHANGE: `.emit(self._recovery_seq)` — captures the generation at POST time (cross-thread int read is atomic in CPython, same justification as `_preroll_seq`).

_handle_gst_error_recovery (:993) — runs on the main thread (QueuedConnection slot):
- Current signature `def _handle_gst_error_recovery(self)` — widen to accept the carried stamp: `def _handle_gst_error_recovery(self, recovery_seq: int = -1)`. The stale early-return goes at the TOP, BEFORE the `_recovery_in_flight` coalescing check (:1000): `if recovery_seq != -1 and recovery_seq != self._recovery_seq: return`.
- The `-1` SENTINEL is the backward-compat mechanism. Semantics: a no-arg call (the default `-1`) means "no explicit stamp → current generation → SKIP the staleness check" and ALWAYS falls through, regardless of the current `_recovery_seq` value. Only an EXPLICITLY-passed stamp (the real QueuedConnection emit from `_on_gst_error` carrying `self._recovery_seq`) is compared against the current generation. This preserves EVERY existing direct test caller WITHOUT edits — crucially the many tests that call `p.play(station)` FIRST (bumping `_recovery_seq` to ≥1) and THEN call `p._handle_gst_error_recovery()` with no arg: test_player_failover.py:419-421 (asserts id==2 at :425), :440/442 (asserts id==3 at :445); test_player.py:1075 (asserts mock_try_next.called at :1080-1082), :1155 (asserts advance at :1158), :1530 (asserts mock_try_next.called at :1532). A `default=0` would early-return on those (`0 != 1` → True) and regress them; the `-1` sentinel does not. The new V11/V12 tests pass EXPLICIT seqs so they exercise the real checked branch.
- Reaches `self._try_next_stream()` (:1034) → empty queue → `self.failover.emit(None)` (:1460) → toast. The guard must sit ABOVE this so a stale recovery never reaches _try_next_stream.

invalidate_for_edit (:2001):
- The restart branch calls `self.play(station)` (:2055) — so the `_recovery_seq` bump at play() entry already covers the edit→restart case. The D-04 queue-invalidate (:2078) and D-05 reset (:2059-2060) branches do NOT restart and MUST NOT bump `_recovery_seq` (leaving the current generation valid so a real same-session recovery still toasts).

FakePlayer (tests/_fake_player.py):
- `_error_recovery_requested = Signal()` (:79) — MUST be widened to `Signal(int)` to mirror player.py, or the parity arity guard (tests/test_fake_player_signal_parity.py) goes RED. The parity guard source-parses EVERY `NAME = Signal(ARGS)` in BOTH files, private signals included (it already mirrors `_cancel_timers_requested`/`_try_next_stream_requested`/`_preroll_about_to_finish_requested`).

Test harness (tests/test_player_edit_invalidation.py:30-73):
- `make_player(qtbot)` mocks `Gst.ElementFactory.make`, sets `player._pipeline = MagicMock()`.
- `make_stream(id_, position, quality, url=...)`, `make_station_with_streams(streams)`.
- `_simulate_playing(player, station, playing_stream)` (:67) sets `_current_station_id`/`_current_station_name`/`_current_stream`/`_streams_queue=[playing_stream]`.
- Existing V5 test (:186) is the template for a seq-guard unit test: capture a stale stamp, perform the restart/bump, then deliver the stale stamp and assert the no-op.

Existing recovery callers preserved by the -1 sentinel (NO edits required):
- tests/test_player_failover.py:105-128 `test_gst_error_triggers_failover`, :405-426 `test_recovery_coalesces_cascading_errors`, :429-445 `test_recovery_guard_resets_between_distinct_url_failures` — all call `p.play(station)` then `p._handle_gst_error_recovery()` with no arg. Under the `-1` sentinel the no-arg call skips the staleness check and advances exactly as today. Confirm green during Task 2 regression run; do NOT edit these tests.
- tests/test_player.py:1066/1075/1080 (D-09 preroll), :1133/1155/1158 (D-10 gapless post-handoff), :1524/1530/1532 (CR-01 preroll-then-about-to-finish) — same pattern, preserved by the sentinel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for the recovery-generation guard + FakePlayer parity for widened _error_recovery_requested</name>
  <files>tests/test_player_edit_invalidation.py, tests/_fake_player.py</files>
  <read_first>
    - tests/test_player_edit_invalidation.py (lines 30-73 harness: make_player/make_stream/make_station_with_streams/_simulate_playing; lines 186-215 the V5 seq-guard test — the exact template to mirror for V11)
    - tests/test_player_failover.py (lines 105-128 test_gst_error_triggers_failover — the existing direct `_handle_gst_error_recovery()` call site; lines 164-171 the empty-queue failover(None) assertion shape)
    - tests/_fake_player.py (lines 78-82 the private bus-internal Signal mirror block — `_error_recovery_requested = Signal()` at :79 is the line to widen)
    - tests/test_fake_player_signal_parity.py (lines 31-82 — the regex source-parse arity guard that scans EVERY Signal decl including private ones)
    - musicstreamer/player.py (lines 286 _error_recovery_requested decl; 983-991 _on_gst_error; 993-1035 _handle_gst_error_recovery; 1448-1461 _try_next_stream empty-queue failover; 682-687 play() entry)
  </read_first>
  <behavior>
    Write FAILING tests first (RED). Define the exact contract the implementation must satisfy. Add to tests/test_player_edit_invalidation.py:

    - V11 (stale recovery SUPPRESSED after edit-restart — the gap): put the player into a playing YouTube state on stream id=10 (old url). Capture `stale_recovery_seq = p._recovery_seq` (the stamp a pre-edit bus-thread error would have carried). Set `p._recovery_seq` to a known current value GREATER than the captured stale stamp (emulating the play()-entry bump a real restart performs), then drive the post-restart state directly: set `_streams_queue=[]` and `_recovery_in_flight=False` (the exact freshly-emptied state), and call `p._handle_gst_error_recovery(stale_recovery_seq)` passing the EXPLICIT stale stamp (`stale_recovery_seq < p._recovery_seq`). Assert `failover.emit(None)` is NOT emitted: `patch.object(p, "_try_next_stream", MagicMock())` and `assert_not_called()`. This is the precise reproduction of the queued-stale-recovery race. The explicit stamp is essential — it exercises the real checked branch, NOT the -1 default.

    - V12 (GENUINE current-generation exhaustion STILL toasts — the hard constraint): put the player in a playing state, set `_streams_queue=[]` and `_recovery_in_flight=False`, and call `p._handle_gst_error_recovery(p._recovery_seq)` passing an EXPLICIT seq EQUAL to the current `_recovery_seq` (a real exhaustion error posted after the latest restart). The explicit current-seq arg (NOT the -1 default) is REQUIRED so this genuinely exercises the pass-through branch of the staleness check rather than the sentinel bypass. Assert `_try_next_stream` IS invoked (patch it and assert called) AND, in a sibling assertion driving the real `_try_next_stream` against an empty queue, assert `p.failover` fires with `None` (mirror tests/test_player_failover.py:164-171 using qtbot.waitSignal). The guard must NOT swallow this.

    - V13 (metadata-only edit unaffected): playing stream id=10 url UNCHANGED, quality changed, is_playing=True. Call `p.invalidate_for_edit(updated, is_playing=True)` (no restart per D-02). Then a legitimate current-generation recovery — `p._handle_gst_error_recovery(p._recovery_seq)` with an EXPLICIT current seq and a NON-empty queue — must still advance normally (patch `_set_uri`/`_try_next_stream` as the existing recovery tests do and assert the advance happens). Asserts the metadata-only path did NOT bump `_recovery_seq` in a way that rejects the legitimate recovery.

    Tests reference `_recovery_seq` and a 1-arg `_handle_gst_error_recovery(seq)` that do not exist yet, so they MUST fail/error at first run (RED). Keep V11/V12/V13 self-contained; do NOT modify player.py in this task.
  </behavior>
  <action>
    Append V11/V12/V13 to `tests/test_player_edit_invalidation.py` using the existing `make_player`/`_simulate_playing` harness and the V5 test (:186) as the structural template. For the failover(None) assertions, mirror tests/test_player_failover.py:164-171 (`with qtbot.waitSignal(p.failover, timeout=1000) as blocker: ...; assert blocker.args == [None]`) for the positive case (V12) and use `patch.object(p, "_try_next_stream", MagicMock())` + `assert_not_called()` for the suppression case (V11). Reference `p._recovery_seq` and call `p._handle_gst_error_recovery(<seq>)` with an EXPLICIT arg in all three tests so they pin the 1-arg signature AND exercise the real checked branch (never the -1 default).

    Widen `tests/_fake_player.py` line 79 `_error_recovery_requested = Signal()` → `_error_recovery_requested = Signal(int)` to mirror the player.py change made in Task 2 (parity-guard requirement — the drift-guard makes this self-enforcing within the wave). Add a brief inline comment noting the int carries the recovery generation (Phase 95 / 95-02, mirroring _preroll_about_to_finish_requested).

    NOTE: because `tests/_fake_player.py` is widened here but player.py's `_error_recovery_requested` arity is widened in Task 2, the parity ARITY test will be RED until Task 2 lands — expected within the same wave (identical to how Plan 01 handled the youtube_resolved widening). Do NOT touch player.py in Task 1, so the V11/V12/V13 tests genuinely RED on the missing `_recovery_seq` / 1-arg handler.
    No fenced code in production files; tests may contain test code as usual.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player_edit_invalidation.py -k "v11 or v12 or v13" -x -q 2>&1 | grep -Eq "(error|Error|failed|FAILED|AttributeError|TypeError)" &amp;&amp; echo "RED confirmed (recovery-guard tests fail pre-implementation)"</automated>
  </verify>
  <acceptance_criteria>
    - source: `tests/test_player_edit_invalidation.py` contains V11, V12, V13 tests that reference `_recovery_seq` and call `_handle_gst_error_recovery` with an EXPLICIT seq argument (never the no-arg default).
    - source: `tests/_fake_player.py` line declaring `_error_recovery_requested` reads `Signal(int)`.
    - behavior: V11/V12/V13 FAIL/ERROR before Task 2 implementation (RED) — proven by the verify command matching an error/failure token (missing `_recovery_seq` attr and/or 1-arg handler TypeError).
    - command: `.venv/bin/python -m pytest tests/test_player_edit_invalidation.py -k "v11 or v12 or v13" -x -q` collects successfully and reports failures (not a collection-only crash).
  </acceptance_criteria>
  <done>RED achieved: V11 (stale recovery suppressed), V12 (genuine exhaustion still toasts), V13 (metadata-only unaffected) fail on the missing `_recovery_seq`/1-arg handler; FakePlayer mirrors the planned widened `_error_recovery_requested` arity. Commit: `test(95-02): add failing tests for recovery-generation guard`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — _recovery_seq generation guard on the error-recovery path</name>
  <files>musicstreamer/player.py</files>
  <read_first>
    - musicstreamer/player.py (lines 286 _error_recovery_requested decl; 523-525 QueuedConnection wiring; 567 _recovery_in_flight; 583/591 _preroll_seq/_youtube_resolve_seq decls + 576-591 rationale comments to mirror; 682-707 play() entry; 983-991 _on_gst_error; 993-1035 _handle_gst_error_recovery; 1037-1042 _clear_recovery_guard; 1448-1461 _try_next_stream; 2050-2061 invalidate_for_edit restart branch)
    - tests/test_player_edit_invalidation.py (the V11/V12/V13 RED contract from Task 1)
    - tests/test_player_failover.py (lines 105-128, 405-445 — confirm the no-arg `_handle_gst_error_recovery()` direct calls still pass via the -1 sentinel even though play() bumped `_recovery_seq` to ≥1)
    - .claude/skills/spike-findings-musicstreamer/references/qt-glib-bus-threading.md (Rule 2 — the bus thread must only emit Signals; the recovery stamp is read on the bus thread as an atomic CPython int and carried on the existing queued Signal — no new cross-thread surface)
  </read_first>
  <behavior>
    Make Task 1's V11/V12/V13 pass (GREEN) by implementing exactly the contract:
    - A dedicated `_recovery_seq` bumped at play() entry (every restart, including invalidate_for_edit's delegation) — NOT bumped by the D-04/D-05 no-restart branches.
    - `_error_recovery_requested` widened to `Signal(int)`; `_on_gst_error` stamps `self._recovery_seq` at POST time; `_handle_gst_error_recovery(recovery_seq=-1)` early-returns when `recovery_seq != -1 and recovery_seq != self._recovery_seq` — ABOVE the existing `_recovery_in_flight` coalescing check, so a stale pre-restart recovery never reaches `_try_next_stream`. The `-1` sentinel default makes every no-arg direct caller skip the staleness check (backward compat).
    - The `_recovery_in_flight` cascade-coalescing behavior is untouched; a genuine current-generation exhaustion (explicit stamp == current) still advances and emits failover(None).
    No behavior beyond what the tests assert.
  </behavior>
  <action>
    In `musicstreamer/player.py`:
    1. Declare `self._recovery_seq: int = 0` in `__init__` immediately alongside `self._youtube_resolve_seq` (:591), with a comment referencing the same CPython-atomic-int rationale and pointing at this gap (95-02 / the spurious-toast debug session). State that it is bumped at play() entry to supersede any error-recovery POSTED before a restart, and that the stamp is captured at error-post time in `_on_gst_error` and rejected at run time in `_handle_gst_error_recovery`.
    2. Widen `_error_recovery_requested = Signal()` (:286) → `Signal(int)` (the int carries the recovery generation captured at post time). Keep the QueuedConnection wiring (:523-525) intact — the slot signature change is compatible.
    3. In `play()` (:682), add `self._recovery_seq += 1` at the TOP of the method, right after `self._recovery_in_flight = False` (:687) and BEFORE the preroll-teardown block (:698). Add a one-line comment: any error-recovery posted before this restart is now stale and will no-op at the run-time check. Do NOT add a bump anywhere in invalidate_for_edit — its restart branch already delegates to `play()` (:2055), and its non-restart branches must leave the current generation valid.
    4. In `_on_gst_error` (:983), change `self._error_recovery_requested.emit()` (:991) → `self._error_recovery_requested.emit(self._recovery_seq)`. The read is on the bus-loop thread; an int read is atomic in CPython (same justification as `_preroll_seq` carried by `_preroll_about_to_finish_requested`). Update the adjacent comment to note the stamp is the recovery generation at post time.
    5. In `_handle_gst_error_recovery` (:993), change the signature to `def _handle_gst_error_recovery(self, recovery_seq: int = -1) -> None:` and add a LEADING guard as the very first statement (before the `_recovery_in_flight` check at :1000):
       `if recovery_seq != -1 and recovery_seq != self._recovery_seq: return` — with a comment explaining BOTH clauses: (a) `recovery_seq != -1` — the `-1` sentinel is the default for no-arg direct callers (and any internal call that does not carry a stamp); it means "no explicit generation → treat as current → skip the staleness check", preserving every existing direct test caller even when `play()` has already bumped `_recovery_seq` to ≥1 before the no-arg call; (b) `recovery_seq != self._recovery_seq` — an EXPLICITLY-stamped delivery posted before the most recent play()/invalidate restart is stale (the OLD exhausted stream's error); ignoring it prevents the spurious "Stream exhausted" toast against the freshly-emptied queue, while a genuine current-generation exhaustion (explicit stamp == current) still falls through to _try_next_stream. Do NOT use `0` as the default — many tests call `p.play(station)` (bumping the seq to ≥1) THEN `p._handle_gst_error_recovery()` with no arg, and a `0` default would early-return (`0 != 1`) and regress them.
    Keep the method otherwise byte-for-byte: do NOT alter the `_recovery_in_flight` coalescing, the preroll branch, the twitch branch, or the `_clear_recovery_guard` scheduling. No fenced code / reimplementation.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player_edit_invalidation.py tests/test_fake_player_signal_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - source: `musicstreamer/player.py` declares `self._recovery_seq: int = 0` near `_youtube_resolve_seq`; `play()` contains `self._recovery_seq += 1` at entry.
    - source: `_error_recovery_requested = Signal(int)`; `_on_gst_error` emits `self._error_recovery_requested.emit(self._recovery_seq)`; `_handle_gst_error_recovery(self, recovery_seq: int = -1)` opens with `if recovery_seq != -1 and recovery_seq != self._recovery_seq: return` placed ABOVE the `_recovery_in_flight` check.
    - source: invalidate_for_edit's D-04/D-05 no-restart branches do NOT contain a `_recovery_seq` bump; the restart branch still calls `self.play(station)` (which performs the single bump).
    - command (GREEN, scoped): `.venv/bin/python -m pytest tests/test_player_edit_invalidation.py tests/test_fake_player_signal_parity.py -x -q` passes (V1-V13 green; FakePlayer arity parity for `_error_recovery_requested` restored).
    - command (regression, scoped — UNMODIFIED tests stay green via the -1 sentinel): `.venv/bin/python -m pytest tests/test_player_failover.py tests/test_player.py -q` passes apart from the project's two known pre-existing unrelated failures. In particular, the no-arg direct callers that run AFTER a `play()` bump stay green WITHOUT edits because the `-1` default skips the staleness check: test_player_failover.py:419-421/425 (`test_recovery_coalesces_cascading_errors`), :440/442/445 (`test_recovery_guard_resets_between_distinct_url_failures`), :122 (`test_gst_error_triggers_failover`), and test_player.py:1075/1080 (D-09 preroll), :1155/1158 (D-10 gapless), :1530/1532 (CR-01). No test edits are required — the `-1` sentinel is what preserves them.
  </acceptance_criteria>
  <done>GREEN: the recovery-generation guard suppresses a stale pre-restart recovery (V11), a genuine current-generation exhaustion still toasts (V12), metadata-only edits are unaffected (V13), the FakePlayer parity guard is green, and all pre-existing no-arg recovery callers stay green unmodified via the `-1` sentinel. Commit: `feat(95-02): add _recovery_seq guard to error-recovery path`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GStreamer bus-loop (daemon) thread → Qt main thread | `_on_gst_error` runs on the bus-loop thread and marshals recovery to the main thread via the queued `_error_recovery_requested` Signal. This fix widens that Signal arity (adds an int recovery-generation stamp) but keeps the same queued-Signal boundary. The int is read on the bus thread (atomic in CPython) and carried as the Signal payload — no shared mutable state crosses the boundary, no new cross-thread surface. |
| user → edited stream URL → restart | Unchanged from Plan 01: the restart re-feeds an already-user-controlled URL through the existing `play()`→`_set_uri` path. This plan adds no new input parsing or external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03 | (correctness, not security) | `_recovery_seq` cross-thread int read in `_on_gst_error` (bus thread) / write in `play()` (main thread) | mitigate | CPython-atomic int read/write — identical documented justification to `_preroll_seq` (carried by the queued `_preroll_about_to_finish_requested` Signal) and `_youtube_resolve_seq` (Plan 01). The stamp is captured at post time on the bus thread and compared at run time on the main thread; the only cross-thread transfer is the int Signal payload. No lock needed. |
| T-95-04 | Denial of Service (self-inflicted) | over-suppression hiding a genuine exhaustion | mitigate | Guard rejects ONLY recoveries whose explicit stamp != current `_recovery_seq` (i.e., posted before the latest restart). The `-1` sentinel default never participates in suppression — only explicitly-stamped real deliveries are checked. A genuine current-generation exhaustion's error is posted AFTER the current restart, so its stamp == current and it falls through to `_try_next_stream` → toast. V12 unit test (explicit current seq) enforces this hard constraint; the bump is confined to play() entry (no bump on the D-04/D-05 no-restart branches). |
| T-95-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed or added in this phase (zero new dependencies; slopcheck not applicable). |

No new credentials, secrets, or network endpoints. ASVS L1: no new untrusted-input parsing introduced. No high-severity threats.
</threat_model>

<verification>
Phase gate (scoped — full `tests/` is >600s and has 2 known unrelated pre-existing
failures; run targeted, not whole-suite, per MEMORY [[run-tests-with-venv-python]]
and per the Plan 01 integration-module teardown note):

```
.venv/bin/python -m pytest \
  tests/test_player_edit_invalidation.py \
  tests/test_player_failover.py \
  tests/test_player.py \
  tests/test_fake_player_signal_parity.py -q
```

Must be green apart from the two known unrelated failures. Covers:
- V11 — stale pre-restart recovery suppressed (no spurious "Stream exhausted")
- V12 — genuine current-generation exhaustion STILL emits failover(None) (hard constraint)
- V13 — metadata-only edit leaves a legitimate recovery un-suppressed
- Parity — `_error_recovery_requested` Signal(int) arity mirrored in FakePlayer
- Regression — all pre-existing no-arg recovery callers (test_player_failover.py
  test_gst_error_triggers_failover / test_recovery_coalesces_cascading_errors /
  test_recovery_guard_resets_between_distinct_url_failures; test_player.py D-09 / D-10 /
  CR-01) stay green UNMODIFIED via the `-1` sentinel default

Manual (end-to-end, the original UAT Test 1): play a YouTube station, edit its
stream URL to a different valid YouTube source, save → confirm the new audio starts
and NO "Stream exhausted" toast flashes during the edit→restart transition. Control:
a station whose every stream genuinely fails STILL shows "Stream exhausted" exactly once.
</verification>

<success_criteria>
- The spurious "Stream exhausted" toast no longer flashes when a playing YouTube
  station's URL is edited and saved (UAT Test 1 gap closed).
- A QUEUED error-recovery posted before the most recent play()/invalidate restart
  no-ops in `_handle_gst_error_recovery` (V11).
- A genuine current-generation exhaustion still reaches `failover.emit(None)` and
  still toasts (V12 — hard constraint preserved).
- Metadata-only edits are unaffected; a legitimate same-session recovery still
  advances/toasts (V13).
- `_error_recovery_requested` widened to `Signal(int)`; stamp captured at post time,
  compared at run time; FakePlayer parity green.
- The existing `_recovery_in_flight` cascade-coalescing behavior is unchanged, and
  every pre-existing no-arg direct caller of `_handle_gst_error_recovery()` stays
  green WITHOUT edits via the `-1` sentinel default.
</success_criteria>

<output>
Create `.planning/phases/95-yt-url-change-replay-bug-post-edit-stream-exhausted-on-first/95-02-SUMMARY.md` when done.
</output>
