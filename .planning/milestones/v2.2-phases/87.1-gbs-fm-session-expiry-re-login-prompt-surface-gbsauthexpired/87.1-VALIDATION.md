---
phase: 87.1
slug: gbs-fm-session-expiry-re-login-prompt-surface-gbsauthexpired
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-17
---

# Phase 87.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (PySide6 + pytest-qt) |
| **Config file** | pyproject.toml / pytest.ini |
| **Quick run command** | `.venv/bin/python -m pytest tests/ -k gbs -q` |
| **Full suite command** | `.venv/bin/python -m pytest tests/ -q` |
| **Estimated runtime** | ~30s (scoped `-k gbs`); full suite >600s — scope it |

> NOTE: run tests with `.venv/bin/python` — system `python3` lacks PySide6.QtWidgets and yields false failures. Full suite is slow; scope to GBS during execution.

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest tests/ -k gbs -q`
- **After every plan wave:** Run scoped GBS + relogin suites (`-k "gbs or relogin"`)
- **Before `/gsd:verify-work`:** Scoped GBS suites must be green; spot-check full suite if time allows
- **Max feedback latency:** 60 seconds (scoped)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 87.1-01-xx | 01 | 1 | GBS-AUTH-EXP-02 | T-87.1-01 / — | Single-flight: concurrent expiry launches exactly one `oauth_helper` subprocess | unit | `.venv/bin/python -m pytest tests/test_gbs_relogin_handler.py -q` | ❌ W0 | ⬜ pending |
| 87.1-02-xx | 02 | 2 | GBS-AUTH-EXP-01, GBS-AUTH-EXP-03 | T-87.1-02 / — | Expiry shows inline prompt (not silent); cancel leaves clear inline state, audio/ICY unaffected | unit | `.venv/bin/python -m pytest tests/test_now_playing_gbs_expiry.py -q` | ❌ W0 | ⬜ pending |
| 87.1-03-xx | 03 | 2 | GBS-AUTH-EXP-02 | — | Marquee worker emits `auth_expired`; consumes shared handler | unit | `.venv/bin/python -m pytest tests/test_gbs_marquee.py -k expiry -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Exact task IDs/counts finalized by the planner; this map will be reconciled against PLAN.md frontmatter during execution.

---

## Wave 0 Requirements

- [ ] `tests/test_gbs_relogin_handler.py` — stubs for the shared handler (single-flight, success/fail signals) — GBS-AUTH-EXP-02
- [ ] `tests/test_now_playing_gbs_expiry.py` — stubs for inline prompt + cancel state + playback-unaffected — GBS-AUTH-EXP-01/03
- [ ] `tests/conftest.py` — reuse existing GBS fixtures; add a `GbsAuthExpiredError`-raising fake `gbs_api` if not already present
- [ ] pytest-qt available in `.venv` (verify; do not reinstall if present)

*Framework already exists — Wave 0 adds test stubs + an expiry-simulation fixture only.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real `oauth_helper --mode gbs` window launches from the inline prompt and, on real login, refreshes the playlist | GBS-AUTH-EXP-01 | Spawns a real QtWebEngine login subprocess against live GBS.FM; cannot run headless/automated | With a deliberately expired/cleared GBS cookie, play a GBS station → confirm inline "GBS session expired" prompt → click "Log in again" → complete login → playlist reloads and polling resumes |
| Cancelling the login leaves a clear inline state (no empty/failed load) | GBS-AUTH-EXP-03 | Requires the real login window + user cancel | Trigger expiry, click "Log in again", close the login window without logging in → inline prompt remains, audio + ICY metadata still live |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s (scoped)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
