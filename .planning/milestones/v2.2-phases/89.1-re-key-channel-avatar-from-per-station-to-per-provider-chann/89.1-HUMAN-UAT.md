---
status: partial
phase: 89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann
source: [89.1-VERIFICATION.md]
started: 2026-06-16
updated: 2026-06-16
---

## Current Test

[awaiting human testing]

## Tests

### 1. D-07 reuse-on-open — provider already has an avatar
expected: Opening EditStationDialog on a YouTube station whose provider already has a stored avatar populates the preview immediately from the cached provider avatar; `_avatar_status` shows "Avatar loaded from channel (shared)"; NO `_AvatarFetchWorker` (no network request) is started.
result: [pending]

### 2. D-08 Refresh avatar — full button → worker → shared-effect roundtrip
expected: Clicking "Refresh avatar" on an EditStationDialog with a YouTube URL re-fetches the avatar (bypassing the reuse gate), overwrites the single per-provider PNG, and shows the shared-effect hint "Avatar saved — all {channel} stations updated"; a sibling stream of the same channel then displays the updated avatar.
result: [pending]

### 3. D-01/D-02/D-03 backfill on a real upgraded DB
expected: Launching the app on an existing database with per-station avatar PNGs (`assets/channel-avatars/{station_id}.png`) runs the one-time backfill: provider-keyed `assets/channel-avatars/{provider_id}.png` files are created, `providers.avatar_path` is set (most-recently-updated sibling wins), old `{station_id}.png` orphans are deleted, and no network re-fetch occurs. Avatars still render with zero flicker.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
