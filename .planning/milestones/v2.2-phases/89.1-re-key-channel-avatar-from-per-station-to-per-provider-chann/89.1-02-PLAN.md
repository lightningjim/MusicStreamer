---
phase: 89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann
plan: 02
type: execute
wave: 2
depends_on: ["89.1-01"]
files_modified:
  - musicstreamer/cover_art.py
  - musicstreamer/ui_qt/now_playing_panel.py
  - musicstreamer/ui_qt/edit_station_dialog.py
  - tests/test_cover_art_avatar.py
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "D-05: cover_art._channel_avatar_lookup reads station.provider_avatar_path (not channel_avatar_path), preserving the WR-04 never-raise / no-thread / no-QPixmap contract"
    - "D-04: now_playing_panel.bind_station resolves the avatar via provider_avatar_path only; stations.channel_avatar_path is no longer read"
    - "D-06: resolution order — provider avatar if provider_avatar_path set AND file exists on disk; provider_id NULL → station thumbnail (unchanged); provider_id set but no provider avatar → fall through to existing cover/logo resolution"
    - "D-07: EditStationDialog reuse-on-open — when the provider already has an avatar, the preview populates from it and the network auto-fetch is skipped"
    - "D-08: Refresh avatar bypasses the D-07 reuse gate (via a _force_avatar_refresh flag), re-fetches, overwrites the single per-provider file, and surfaces a shared-effect hint that other channel stations are updated"
    - "EditStationDialog persists via update_provider_avatar_path and the worker writes via assets.write_provider_avatar (keyed by provider_id)"
  artifacts:
    - path: "musicstreamer/cover_art.py"
      provides: "_channel_avatar_lookup reads provider_avatar_path"
      contains: "provider_avatar_path"
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "bind_station provider-avatar resolution with file-existence guard"
      contains: "provider_avatar_path"
    - path: "musicstreamer/ui_qt/edit_station_dialog.py"
      provides: "reuse-on-open gate + force-refresh bypass + provider-keyed persist/write + shared-effect hint"
      contains: "_force_avatar_refresh"
    - path: "tests/test_cover_art_avatar.py"
      provides: "lookup tests re-pointed to provider_avatar_path with spec= + D-06 NULL case"
      contains: "provider_avatar_path"
  key_links:
    - from: "musicstreamer/cover_art.py _channel_avatar_lookup"
      to: "station.provider_avatar_path"
      via: "getattr(station, 'provider_avatar_path', None)"
      pattern: "provider_avatar_path"
    - from: "musicstreamer/ui_qt/edit_station_dialog.py _on_avatar_fetched"
      to: "repo.update_provider_avatar_path"
      via: "update_provider_avatar_path(self._station.provider_id, rel_path)"
      pattern: "update_provider_avatar_path"
    - from: "musicstreamer/ui_qt/edit_station_dialog.py _AvatarFetchWorker.run"
      to: "assets.write_provider_avatar"
      via: "write_provider_avatar(self._provider_id, data)"
      pattern: "write_provider_avatar"
---

<objective>
Rewire the three avatar consumers to read the per-provider avatar produced by Plan 01: the synchronous `cover_art._channel_avatar_lookup`, the `now_playing_panel.bind_station` cover-slot swap, and `EditStationDialog` (reuse-on-open, force-refresh bypass, provider-keyed persist/write, and a shared-effect hint). Re-point the existing `_channel_avatar_lookup` tests to `provider_avatar_path` with `spec=` so MagicMock can no longer silently auto-create the wrong attribute.

Purpose: Complete the per-station → per-provider re-key so all sibling YouTube streams of one channel reuse one fetch and one file. Plan 01 supplied the data (`provider_avatar_path` on the Station model + provider-keyed writer/persist); this plan consumes it.
Output: Three rewired consumer modules + updated lookup tests, honoring D-04/D-05/D-06/D-07/D-08.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-CONTEXT.md
@.planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-RESEARCH.md
@.planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-PATTERNS.md
@.planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-01-PLAN.md

<interfaces>
<!-- Provided by Plan 01 (Wave 1). Use directly. -->

models.Station.provider_avatar_path: Optional[str]   # populated by all four mappers from providers.avatar_path
repo.Repo.update_provider_avatar_path(provider_id: int, path: Optional[str]) -> None
assets.write_provider_avatar(provider_id: int, data: bytes) -> str   # returns data_dir()-relative 'assets/channel-avatars/{provider_id}.png'

<!-- Consumer call sites to rewire (current state, confirmed by grep) -->
cover_art.py:159 def _channel_avatar_lookup(station, callback)   # L179 reads channel_avatar_path → repoint to provider_avatar_path (contract unchanged)
cover_art.py:24 (test) drift-guard: _mb_caa_lookup must appear BEFORE _channel_avatar_lookup — function name unchanged, guard stays green
now_playing_panel.py:909 def bind_station(self, station)          # L932 _show_station_logo_in_cover_slot() then L937-938 reads channel_avatar_path
now_playing_panel.py:2198 _set_avatar_pixmap_from_path(rel)        # signature/behavior unchanged; clears _last_avatar_path on isNull
edit_station_dialog.py:134 class _AvatarFetchWorker               # L175 calls write_channel_avatar(self._station_id, data)
edit_station_dialog.py:1275 def _on_url_timer_timeout             # YouTube branch launches worker — add D-07 gate here
edit_station_dialog.py:1438 def _on_avatar_fetched               # L1464 sets channel_avatar_path; L1468 update_channel_avatar_path
edit_station_dialog.py:1512 def _refresh_avatar_preview          # L1520 reads channel_avatar_path
edit_station_dialog.py:1537 def _on_refresh_avatar_clicked       # calls _on_url_timer_timeout — must bypass D-07 gate
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-point _channel_avatar_lookup + bind_station to provider_avatar_path; update lookup tests (RED→GREEN)</name>
  <files>musicstreamer/cover_art.py, musicstreamer/ui_qt/now_playing_panel.py, tests/test_cover_art_avatar.py</files>
  <read_first>
    - musicstreamer/cover_art.py (_channel_avatar_lookup L159–187 — the field read at L179 and the WR-04/no-thread/no-QPixmap contract in the docstring)
    - musicstreamer/ui_qt/now_playing_panel.py (bind_station L909, the L932 _show_station_logo_in_cover_slot() + L937–938 channel_avatar_path read; _set_avatar_pixmap_from_path L2198–2219)
    - tests/test_cover_art_avatar.py (the five _channel_avatar_lookup tests L77–143 and the L24 drift-guard; the MagicMock(spec=[]) usage at L86)
    - .planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-PATTERNS.md (§"cover_art.py re-point", §"now_playing_panel.py bind_station re-point", §"test_cover_art_avatar.py Update existing tests")
    - .planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-RESEARCH.md (Pitfall 4 file-existence guard, Pitfall 7 test attribute names)
  </read_first>
  <behavior>
    tests/test_cover_art_avatar.py (re-point existing, add one):
    - test_channel_avatar_lookup_empty_path_calls_cb_none: set station.provider_avatar_path = "" (not channel_avatar_path).
    - test_channel_avatar_lookup_existing_path_calls_cb_abs: set station.provider_avatar_path = avatar_rel; cb called with abs path.
    - test_channel_avatar_lookup_missing_file_calls_cb_none: set station.provider_avatar_path = "assets/channel-avatars/99.png"; cb(None).
    - Add spec= to the MagicMock in the repointed tests (use spec=["provider_avatar_path"] or spec=Station) so the mock cannot auto-create channel_avatar_path and silently pass (Pitfall 7).
    - New test_channel_avatar_lookup_null_provider_avatar_calls_cb_none: station.provider_avatar_path = None → cb(None) (D-06 fallback 2).
    - The L24 ART-AVATAR-09 drift-guard stays unchanged and green (function name unchanged).
  </behavior>
  <action>
    In cover_art.py:179 change `rel = getattr(station, "channel_avatar_path", None)` to `getattr(station, "provider_avatar_path", None)` (D-05); update the docstring to say provider_avatar_path. Do NOT alter the try/except, the callback contract, or add any QPixmap/thread — only the field read changes (WR-04 preserved). In now_playing_panel.bind_station, replace the L937–938 block: after the existing L932 _show_station_logo_in_cover_slot() and the `self._last_avatar_path = None` reset, gate on `getattr(station, "icy_disabled", False)`, read `_provider_rel = getattr(station, "provider_avatar_path", None)`, and only call `self._set_avatar_pixmap_from_path(_provider_rel)` when `_provider_rel` is set AND `os.path.isfile(os.path.join(paths.data_dir(), _provider_rel))` is true (Pitfall 4 file-existence guard) — otherwise fall through to the already-shown station logo (D-06). Do NOT read channel_avatar_path anywhere in bind_station (D-04). Update tests/test_cover_art_avatar.py per the behavior block.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cover_art_avatar.py -q</automated>
  </verify>
  <acceptance_criteria>
    - _channel_avatar_lookup reads provider_avatar_path (not channel_avatar_path)
    - now_playing_panel.bind_station contains provider_avatar_path and an os.path.isfile guard; does not read channel_avatar_path
    - tests/test_cover_art_avatar.py mocks use provider_avatar_path with spec= and the new NULL-provider test passes
    - test_mb_caa_runs_before_channel_avatar drift-guard still passes (function name unchanged)
    - .venv/bin/python -m pytest tests/test_cover_art_avatar.py -q exits 0
  </acceptance_criteria>
  <done>Both synchronous readers resolve via provider_avatar_path with the file-existence guard; lookup tests are re-pointed with spec= and GREEN; drift-guard intact.</done>
</task>

<task type="auto">
  <name>Task 2: EditStationDialog — reuse-on-open gate, force-refresh bypass, provider-keyed persist/write, shared-effect hint</name>
  <files>musicstreamer/ui_qt/edit_station_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/edit_station_dialog.py (_AvatarFetchWorker L134–179 incl. __init__ and the L175 write_channel_avatar call; _on_url_timer_timeout L1275 incl. the `youtube.com`/`youtu.be` branch that launches the worker; _on_avatar_fetched L1438 incl. L1464 channel_avatar_path set + L1468 update_channel_avatar_path; _refresh_avatar_preview L1512 incl. L1520 read; _on_refresh_avatar_clicked L1537)
    - .planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-PATTERNS.md (§"edit_station_dialog.py — Re-key + D-07/D-08 changes" — Changes 1–6, the exact gate/bypass shape)
    - .planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-RESEARCH.md (§Finding 7 D-07/D-08)
  </read_first>
  <action>
    Apply the six changes from PATTERNS.md §edit_station_dialog. (1) _AvatarFetchWorker.__init__: add a `provider_id: int | None = None` param and store `self._provider_id` (D-10). (2) _AvatarFetchWorker.run L175: call `_assets.write_provider_avatar(self._provider_id, data)` instead of write_channel_avatar. (3) _on_url_timer_timeout, INSIDE the YouTube URL branch only: before launching the worker, add the D-07 reuse gate — if `getattr(self._station, "provider_avatar_path", None)` is set AND NOT `getattr(self, "_force_avatar_refresh", False)`, set the status text to a shared/loaded message, call `self._refresh_avatar_preview()`, and return (skip the network fetch); otherwise construct _AvatarFetchWorker passing `provider_id=self._station.provider_id`. (4) _on_avatar_fetched L1464–1468: set `self._station.provider_avatar_path = rel_path` (in-memory), refresh preview, set the D-08 shared-effect hint status (e.g. f"Avatar saved — all {provider_name} stations updated", provider_name from station.provider_name fallback "this channel"), and persist via `self._repo.update_provider_avatar_path(self._station.provider_id, rel_path)` (NOT update_channel_avatar_path — D-04 cutover). (5) _refresh_avatar_preview L1520: read `getattr(self._station, "provider_avatar_path", None)`. (6) _on_refresh_avatar_clicked: set `self._force_avatar_refresh = True` before calling `_on_url_timer_timeout()` in a try/finally that resets it to False, so Refresh bypasses the D-07 gate and always re-fetches (D-08). Initialize `self._force_avatar_refresh = False` in __init__ so the getattr default is well-defined. Do not break the existing token/worker lifecycle.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cover_art_avatar.py tests/test_repo.py tests/test_assets_avatar.py -q && .venv/bin/python -c "import ast,sys; src=open('musicstreamer/ui_qt/edit_station_dialog.py').read(); ast.parse(src); assert 'write_provider_avatar' in src and 'update_provider_avatar_path' in src and '_force_avatar_refresh' in src and 'provider_avatar_path' in src, 'missing re-key identifiers'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - edit_station_dialog.py contains write_provider_avatar, update_provider_avatar_path, _force_avatar_refresh, and provider_avatar_path
    - _AvatarFetchWorker passes/accepts provider_id and writes via write_provider_avatar
    - _on_url_timer_timeout skips the fetch when provider_avatar_path is set unless _force_avatar_refresh is True (D-07 reuse / D-08 bypass)
    - _on_avatar_fetched persists via update_provider_avatar_path and sets a shared-effect hint (D-08)
    - No remaining production call to update_channel_avatar_path or write_channel_avatar in this file (grep returns 0 for both, excluding comments)
    - File parses (ast.parse) and the avatar/repo/assets test scope stays green
  </acceptance_criteria>
  <done>EditStationDialog reuses the provider avatar on open, Refresh bypasses the gate and overwrites the single per-provider file with a shared-effect hint, and persistence/writes go through the provider-keyed path (D-04/D-07/D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row (Station.provider_avatar_path) → Qt render | a relative path string from SQLite is resolved against data_dir() and loaded as a QPixmap in the cover slot |
| EditStationDialog worker thread → filesystem/DB | the fetch worker writes a PNG and the main thread persists the provider path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89.1-05 | Tampering / Denial | now_playing_panel.bind_station loading provider_avatar_path | mitigate | Explicit `os.path.isfile(os.path.join(paths.data_dir(), rel))` guard before _set_avatar_pixmap_from_path (Pitfall 4) — a stale/missing path falls through to the station logo instead of leaving a bad _last_avatar_path that retries on resize. Path comes from providers.avatar_path written only by the int-keyed write_provider_avatar (T-89.1-01), no traversal. |
| T-89.1-06 | Denial of Service | _channel_avatar_lookup in the Qt cover-resolver chain | mitigate | WR-04 contract preserved verbatim: wrapped in try/except, never raises into the Qt callback chain, no thread launch, no QPixmap — only the attribute name read changes. |
| T-89.1-07 | Tampering | EditStationDialog persist of provider avatar | mitigate | Persist goes through the dedicated update_provider_avatar_path (single-column UPDATE) from Plan 01 — no broad provider update that could silently reset avatar_path (Pitfall 5). |

All dispositions are mitigate; no high-severity threat — this plan only re-points field reads and the persist/write call sites onto the int-keyed, atomic, guarded path established in Plan 01.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_cover_art_avatar.py tests/test_repo.py tests/test_assets_avatar.py -q` is GREEN.
- grep: cover_art._channel_avatar_lookup and now_playing_panel.bind_station read `provider_avatar_path`, not `channel_avatar_path`.
- grep: edit_station_dialog.py has no production call to update_channel_avatar_path / write_channel_avatar (comments excluded); has write_provider_avatar, update_provider_avatar_path, _force_avatar_refresh.
- Manual (per VALIDATION.md): reuse-on-open populates preview with no fetch; Refresh updates a sibling stream and shows the shared-effect hint; upgraded DB renders avatars from {provider_id}.png with no {station_id}.png orphans.
</verification>

<success_criteria>
- D-05: _channel_avatar_lookup reads provider_avatar_path, contract intact.
- D-04/D-06: bind_station resolves only the provider avatar with a file-existence guard; NULL provider_id → station thumbnail; provider set but no avatar → existing fallback.
- D-07: reuse-on-open skips network fetch when the provider already has an avatar.
- D-08: Refresh bypasses the reuse gate, overwrites the single per-provider file, and surfaces a shared-effect hint.
- Persistence/writes go through update_provider_avatar_path / write_provider_avatar.
</success_criteria>

<output>
Create `.planning/phases/89.1-re-key-channel-avatar-from-per-station-to-per-provider-chann/89.1-02-SUMMARY.md` when done.
</output>
