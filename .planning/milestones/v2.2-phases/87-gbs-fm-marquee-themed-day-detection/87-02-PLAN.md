---
phase: 87-gbs-fm-marquee-themed-day-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - musicstreamer/gbs_marquee.py
  - tests/test_gbs_marquee.py
  - tests/fixtures/gbs_marquee/synthetic-001.txt
  - tests/fixtures/gbs_marquee/synthetic-002.txt
  - tests/fixtures/gbs_marquee/synthetic-003.txt
  - tests/fixtures/gbs_marquee/synthetic-004.txt
  - tests/fixtures/gbs_marquee/synthetic-005.txt
  - tests/fixtures/gbs_marquee/synthetic-006.txt
  - tests/fixtures/gbs_marquee/synthetic-007.txt
  - tests/fixtures/gbs_marquee/MANIFEST.md
autonomous: true
requirements:
  - GBS-MARQ-02
  - GBS-MARQ-07
tags:
  - gbs.fm
  - marquee
  - parser

must_haves:
  truths:
    - "musicstreamer/gbs_marquee.py exists with parse_marquee(raw_text) -> tuple[str, str] returning (first_segment, full_text)"
    - "parse_marquee strips whitespace per segment, defending against both `|` and ` | ` (space-padded) delimiter conventions (Pitfall #6)"
    - "MARQUEE_URL constant is locked at module scope based on Plan 87-01 harvest dissection"
    - "tests/fixtures/gbs_marquee/ contains ≥ 10 marquee samples (real-captured + synthetic) per GBS-MARQ-07 (Pitfall #8 relaxation)"
    - "tests/test_gbs_marquee.py covers parse_marquee for: empty / single-segment / multi-segment / pipe-padded / unicode / whitespace-only segments"
    - "D-13: parse_marquee splits on `|`, returns first segment as the changeable announcement, ignores subsequent segments for banner display (but full_text is preserved for themed-day keyword search per D-12). Whitespace-trimmed per segment per Pitfall #6."
  artifacts:
    - path: "musicstreamer/gbs_marquee.py"
      provides: "parse_marquee + MARQUEE_URL constant (skeleton; Plan 87-03 fills worker)"
      contains: "def parse_marquee"
    - path: "tests/test_gbs_marquee.py"
      provides: "Unit tests for parse_marquee + fixture count"
      contains: "test_parse_marquee_pipe_split"
    - path: "tests/fixtures/gbs_marquee/MANIFEST.md"
      provides: "Extended manifest documenting synthetic samples per Pitfall #8"
      contains: "synthetic"
  key_links:
    - from: "musicstreamer/gbs_marquee.py"
      to: "musicstreamer.gbs_api + musicstreamer.paths"
      via: "import statements (drift-guard target, enforced in Plan 87-06)"
      pattern: "from musicstreamer import"
    - from: "tests/test_gbs_marquee.py::test_fixture_count_ten_or_more"
      to: "tests/fixtures/gbs_marquee/*.{txt,json,html}"
      via: "Path.glob + len() ≥ 10"
      pattern: "fixtures/gbs_marquee"
---

<objective>
Plan 87-02 reverse-engineers the marquee endpoint from Plan 87-01's harvested bytes, locks the URL constant and parser shape, and ships the parser + GBS-MARQ-07 fixture corpus. Per Pitfall #3, the endpoint is most likely embedded in the gbs.fm `/ajax` event stream (~60%) or homepage HTML (~25%); Plan 87-01 captured both as raw bytes so this plan dissects deterministically without touching the live site.

Pitfall #6 mandates per-segment `.strip()` to defend against both `|` and ` | ` (space-padded) delimiter conventions; the parser commits this regardless of which delimiter is observed in the harvest.

Pitfall #8 relaxes GBS-MARQ-07's "10+ real samples" literal — Plan 87-01 likely yielded 1-3 real captures (single themed window); Plan 87-02 adds ≥ 7 synthetic samples (parser robustness variations: short / medium / long, single pipe, multiple pipes, unicode in announcement, empty segments around pipes, leading/trailing whitespace) to satisfy the ≥ 10 fixture count.

Purpose:
- Lock `MARQUEE_URL` + (if needed) `MARQUEE_AJAX_EVENT` constants from harvested evidence so Plan 87-03 can wire the worker fetch path.
- Ship `parse_marquee(raw_text) -> (first_segment, full_text)` as a pure function the worker calls.
- Ship the test harness + fixture corpus that locks the parser against future regressions.

Output:
- `musicstreamer/gbs_marquee.py` (skeleton — constants, parse_marquee, module docstring; the worker class is Plan 87-03's deliverable but the file exists now so drift-guards in Plan 87-06 have a target).
- `tests/test_gbs_marquee.py` (parse_marquee tests + fixture-count test).
- ≥ 7 synthetic marquee fixture files + extended MANIFEST.md noting the synthetic provenance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-CONTEXT.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-VALIDATION.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@musicstreamer/aa_live.py
@musicstreamer/gbs_api.py
@tests/fixtures/gbs_marquee/2026-05-25_homepage.html
@tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json
@tests/fixtures/gbs_marquee/MANIFEST.md

<interfaces>
<!-- aa_live.py is the closest precedent for "pure function parser + urllib helpers in one module, importable without Qt". -->

From musicstreamer/aa_live.py:55-65 (parse-from-text pattern):
```python
_LIVE_ICY_RE = re.compile(r'^\s*LIVE\s*[:\-]\s*(.+?)\s*$', re.IGNORECASE)

def detect_live_from_icy(title: Optional[str]) -> Optional[str]:
    """Return show name if title matches LIVE: or LIVE - prefix, else None."""
    if not title:
        return None
    m = _LIVE_ICY_RE.match(title)
    return m.group(1).strip() if m else None
```

From musicstreamer/aa_live.py:68-108 (pure parser shape — testable without Qt or HTTP):
```python
def _parse_live_map(events: list[dict], *, now: Optional[datetime] = None) -> dict[str, str]:
    """Pure parser: extract {channel_key: show_name} for currently-live events."""
```

From 87-RESEARCH.md §Pitfall #6 (parser sketch):
```python
def parse_marquee(raw_text: str) -> tuple[str, str]:
    """Returns (first_segment, full_text). Whitespace-trim each segment."""
    full = (raw_text or "").strip()
    if not full:
        return ("", "")
    segments = [s.strip() for s in full.split("|") if s.strip()]
    first = segments[0] if segments else ""
    return (first, full)
```

Plan 87-01 SUMMARY.md will indicate which capture (`2026-05-25_homepage.html` or `2026-05-25_ajax_cold.json`) contains the marquee text. Dissect that file to determine: (a) HTML selector vs JSON path vs ajax event name, (b) exact delimiter (`|` vs ` | `), (c) whether the response shape includes additional fields beyond the marquee string.
</interfaces>

<assumptions>
- Plan 87-01's harvest yielded usable bytes for endpoint discovery. If both candidate files lack any string matching the day's themed phrase (per the Plan 87-01 SUMMARY notes), Plan 87-02 still ships the parser + synthetic fixtures — `MARQUEE_URL` is locked to the best inference (default: `GBS_BASE + "/"` homepage, with a TODO comment for Plan 87-03 to probe at runtime if parsing returns empty).
- The marquee delimiter is locked to `|` with per-segment `.strip()` (Pitfall #6) regardless of whether the harvest shows bare `|` or space-padded ` | `. This defends both conventions uniformly.
- Per Pitfall #8, synthetic samples are acceptable filler to reach GBS-MARQ-07's ≥ 10 fixture count. MANIFEST.md flags each synthetic with `provenance = synthetic` so future real captures supersede the synthetic baseline.
- Plan 87-03 fills `GbsMarqueeWorker` into the same `musicstreamer/gbs_marquee.py` module; this plan creates the file with only constants + `parse_marquee` + module docstring so the file path exists for drift-guards in Plan 87-06 to target.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dissect harvest + create gbs_marquee.py skeleton with parse_marquee + MARQUEE_URL</name>
  <files>musicstreamer/gbs_marquee.py, tests/test_gbs_marquee.py</files>
  <read_first>
    - tests/fixtures/gbs_marquee/2026-05-25_homepage.html (raw bytes — dissect for marquee text)
    - tests/fixtures/gbs_marquee/2026-05-25_ajax_cold.json (raw bytes — dissect for marquee event)
    - tests/fixtures/gbs_marquee/MANIFEST.md (Plan 87-01 notes including any "da troops" grep findings)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-01-SUMMARY.md (whether cookies path or anonymous; observed marquee text)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md §Pitfall #3 (endpoint probability ordering, lines 481-505)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md §Pitfall #6 (delimiter ambiguity, parser sketch)
    - musicstreamer/aa_live.py (full file — pattern for pure-parser module shape)
    - musicstreamer/gbs_api.py:49-77 (GBS_BASE constant + timeout constants + USER_AGENT)
    - .planning/codebase/CONVENTIONS.md §Module Design + §Naming Patterns
  </read_first>
  <behavior>
    - parse_marquee(""): returns ("", "")
    - parse_marquee("only one"): returns ("only one", "only one")
    - parse_marquee("first | second | third"): returns ("first", "first | second | third")
    - parse_marquee("first|second"): returns ("first", "first|second") — bare-pipe delimiter
    - parse_marquee("  spaced  |  perpetual  "): returns ("spaced", "spaced  |  perpetual") — per-segment strip; full_text strips outer whitespace but preserves inner
    - parse_marquee("|leading pipe|middle"): returns ("leading pipe", "|leading pipe|middle") — empty leading segment filtered by `if s.strip()`
    - parse_marquee("unicode • 你好 | next"): returns ("unicode • 你好", "unicode • 你好 | next") — unicode-safe
    - Module exports: parse_marquee, MARQUEE_URL (str), GBS_THEMED_DAY_KEYWORDS reference forwarded from constants module (placeholder OK — Plan 87-04 may move definition).
    - Module docstring cites Phase 76 reuse path (gbs_api.load_auth_context + paths.gbs_cookies_path) explicitly so the source-grep drift-guard in Plan 87-06 finds the import markers.
    - tests/test_gbs_marquee.py contains: test_parse_marquee_empty, test_parse_marquee_single_segment, test_parse_marquee_pipe_split, test_parse_marquee_bare_pipe_delimiter, test_parse_marquee_whitespace_padded, test_parse_marquee_leading_empty_segment, test_parse_marquee_unicode, test_fixture_count_ten_or_more.
  </behavior>
  <action>
    Step 1 — Dissect Plan 87-01 captures: search both files for the themed-day phrase (case-insensitive). For example: `grep -in "da troops" tests/fixtures/gbs_marquee/2026-05-25_homepage.html` and the same against the JSON capture. Identify which file (HTML or JSON) contains the marquee text and which element/field carries it.

    Step 2 — Decide MARQUEE_URL: if marquee text is in homepage HTML, set `MARQUEE_URL = f"{GBS_BASE}/"` and document the HTML selector or regex pattern in a module-level constant comment. If marquee text is in the /ajax event stream, set `MARQUEE_URL = f"{GBS_BASE}/ajax?position=0&last_comment=0&last_removal=0&last_add=0&now_playing=0"` and document the event-name field. If neither file contains the trigger phrase (Plan 87-01 SUMMARY may have noted this), set `MARQUEE_URL = f"{GBS_BASE}/"` as the best default and add a `# TODO(87-03): probe at runtime if parse_marquee returns empty` comment — the worker can swap endpoints at runtime if the homepage parse yields nothing.

    Step 3 — Create `musicstreamer/gbs_marquee.py` with: (a) module docstring naming Phase 87 + the Phase 76 cookies-jar reuse path (CITE both `musicstreamer.gbs_api` and `musicstreamer.paths` by name in the docstring AND import them at module top — the drift-guard in Plan 87-06 greps for these imports), (b) `from __future__ import annotations`, (c) imports: `import re`, `from musicstreamer import gbs_api, paths` (even if not yet called — Plan 87-03 wires the fetch path), (d) `MARQUEE_URL: str = ...` constant per Step 2, (e) `MARQUEE_DELIMITER = "|"` constant (Pitfall #6 — the per-segment strip handles both bare and padded variants), (f) `def parse_marquee(raw_text: str) -> tuple[str, str]:` implementing the §Pitfall #6 sketch verbatim, (g) a `# Placeholder for GbsMarqueeWorker — Plan 87-03 fills this` comment marker. DO NOT define the worker class yet — Plan 87-03 owns that scope and same-wave drift-guard (Plan 87-06) needs a stable target.

    Step 4 — Create `tests/test_gbs_marquee.py` covering the behavior block above. Use plain pytest functions (no Qt event loop required for parse_marquee — pure function). The `test_fixture_count_ten_or_more` test must use `pathlib.Path(__file__).resolve().parent / "fixtures" / "gbs_marquee"` then `len(list(d.glob("*.txt")) + list(d.glob("*.json")) + list(d.glob("*.html"))) >= 10`.

    Step 5 — Re-run `uv run --with pytest pytest tests/test_gbs_marquee.py -x` and ensure all 8 tests pass (will fail initially on test_fixture_count_ten_or_more because synthetic fixtures land in Task 2; this task expects the parser tests pass and the fixture-count test pass once Task 2 commits).

    Project conventions reminders: snake_case, type hints on every signature, module docstring, single-underscore prefix for private helpers, modern `X | None` syntax, pure urllib (no requests). No Qt imports in this file.
  </action>
  <acceptance_criteria>
    - `test -f musicstreamer/gbs_marquee.py`
    - `grep -c "^def parse_marquee" musicstreamer/gbs_marquee.py` returns 1
    - `grep -c "^MARQUEE_URL" musicstreamer/gbs_marquee.py` returns 1
    - `grep -c "from musicstreamer import" musicstreamer/gbs_marquee.py` returns ≥ 1 AND that line includes `gbs_api` and `paths` (drift-guard pre-flight for Plan 87-06)
    - `grep -E "QWebEngineProfile|GBS_WEB_PROFILE_NAME|GBS_WEB_STORAGE_PATH|oauth_helper" musicstreamer/gbs_marquee.py` returns no hits
    - `uv run --with pytest pytest tests/test_gbs_marquee.py::test_parse_marquee_pipe_split tests/test_gbs_marquee.py::test_parse_marquee_empty tests/test_gbs_marquee.py::test_parse_marquee_whitespace_padded -x` exits 0
    - tests/test_gbs_marquee.py contains 8 named test functions matching the behavior block
  </acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_gbs_marquee.py::test_parse_marquee_pipe_split tests/test_gbs_marquee.py::test_parse_marquee_empty tests/test_gbs_marquee.py::test_parse_marquee_whitespace_padded tests/test_gbs_marquee.py::test_parse_marquee_unicode -x</automated>
  </verify>
  <done>parse_marquee + MARQUEE_URL + module docstring committed in gbs_marquee.py; 7+ parser tests pass; drift-guard pre-flight (gbs_api + paths imports present, banned identifiers absent) satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Add ≥ 7 synthetic marquee fixtures + extend MANIFEST.md (Pitfall #8 relaxation)</name>
  <files>
    tests/fixtures/gbs_marquee/synthetic-001.txt,
    tests/fixtures/gbs_marquee/synthetic-002.txt,
    tests/fixtures/gbs_marquee/synthetic-003.txt,
    tests/fixtures/gbs_marquee/synthetic-004.txt,
    tests/fixtures/gbs_marquee/synthetic-005.txt,
    tests/fixtures/gbs_marquee/synthetic-006.txt,
    tests/fixtures/gbs_marquee/synthetic-007.txt,
    tests/fixtures/gbs_marquee/MANIFEST.md
  </files>
  <read_first>
    - tests/fixtures/gbs_marquee/MANIFEST.md (Plan 87-01's manifest — extend, don't overwrite)
    - tests/fixtures/gbs_marquee/2026-05-25_homepage.html OR 2026-05-25_ajax_cold.json (the file containing the real marquee text — use as template for synthetic variations)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-RESEARCH.md §Pitfall #8 (10+ literal relaxation, synthetic categories)
  </read_first>
  <action>
    Create 7 synthetic marquee fixture files exercising parser-robustness variations. Each file contains a single line of marquee text (or a JSON snippet matching the real harvest's shape — match whatever Task 1 dissected as the on-the-wire format). Suggested variations (one per file):

    - `synthetic-001.txt`: single segment, no pipes ("Welcome to GBS.FM")
    - `synthetic-002.txt`: bare-pipe delimiter, two segments ("Announcement|Tagline")
    - `synthetic-003.txt`: space-padded delimiter, three segments ("Special event | Tip jar | Schedule")
    - `synthetic-004.txt`: announcement with internal punctuation + pipe-padded perpetuals ("Memorial Day - da troops salute! | Tip jar live | Schedule online")
    - `synthetic-005.txt`: unicode in announcement ("Holiday cheer 🎄 ho ho ho | Tip jar")
    - `synthetic-006.txt`: leading empty segment ("|leading-empty-stripped|Real announcement")
    - `synthetic-007.txt`: leading + trailing whitespace, mixed delimiter spacing ("   Long announcement spans multiple words and pipes |perpetual1 | perpetual2| perpetual3   ")

    Extend `tests/fixtures/gbs_marquee/MANIFEST.md` by appending a new section `## Synthetic Samples (Pitfall #8 — parser robustness)` with a markdown table:

    ```markdown
    | filename | capture_date | sha256 | provenance | notes |
    |----------|--------------|--------|------------|-------|
    | synthetic-001.txt | 2026-05-25 | <hash> | synthetic | single segment, no pipes |
    ...
    ```

    Compute the SHA-256 of each synthetic file as it's written and record in MANIFEST. Do NOT modify the existing Plan 87-01 entries in MANIFEST.md (real-captured rows stay distinct from synthetic rows).

    After commit, `uv run --with pytest pytest tests/test_gbs_marquee.py::test_fixture_count_ten_or_more -x` should pass (Plan 87-01 captures + these 7 synthetic = ≥ 10).
  </action>
  <acceptance_criteria>
    - `ls tests/fixtures/gbs_marquee/synthetic-*.txt | wc -l` returns ≥ 7
    - `ls tests/fixtures/gbs_marquee/ | wc -l` returns ≥ 10 (real + synthetic + MANIFEST.md)
    - `grep -c "## Synthetic Samples" tests/fixtures/gbs_marquee/MANIFEST.md` returns 1
    - `uv run --with pytest pytest tests/test_gbs_marquee.py::test_fixture_count_ten_or_more -x` exits 0
    - For each `synthetic-*.txt`: the SHA-256 recorded in MANIFEST matches `hashlib.sha256(file_bytes).hexdigest()` (spot-check one file)
  </acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_gbs_marquee.py::test_fixture_count_ten_or_more -x</automated>
  </verify>
  <done>7 synthetic marquee fixtures committed; MANIFEST.md extended with synthetic section + SHA-256 per file; fixture-count test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harvested bytes → parser | Operator-controlled text from gbs.fm — passes through parse_marquee unchanged into downstream string handling |
| Parser → consumers | parse_marquee returns plain str; banner widget (Plan 87-05) renders as Qt.TextFormat.PlainText, so HTML/JS injection is defused at the UI layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-01 | Tampering | gbs.fm operator can inject any text into marquee body | mitigate | parse_marquee preserves text verbatim; rendering in Plan 87-05 uses Qt.TextFormat.PlainText (per CONVENTIONS T-40-04) which strips HTML/JS rendering — banner cannot execute injected markup |
| T-87-02-02 | Denial of Service | Pathologically large marquee bytes consume memory in parser | accept | Plan 87-03 enforces 10s/15s urllib timeouts (reuse `gbs_api._TIMEOUT_READ`); parse_marquee is O(n) on input length and exits quickly even on multi-MB input; gbs.fm marquee bodies observed at <2KB |
| T-87-02-03 | Information Disclosure | Marquee body logged to disk via buffer_log (Plan 87-03) leaks marquee text | mitigate | D-18: log lines are structured key=value with NO marquee body — only event names (`gbs.marquee.fetch_failed`, `gbs.marquee.auth_expired`) + status codes. Plan 87-03 enforces |
| T-87-02-04 | Repudiation | Synthetic fixtures look identical to real captures and pollute future regression analysis | mitigate | MANIFEST.md's `provenance` column distinguishes `synthetic` from real-captured rows; synthetic filenames begin with `synthetic-` prefix |
| T-87-02-SC | Tampering | No package installs in this plan | n/a | Plan 87-02 introduces zero new packages; slopcheck N/A |
</threat_model>

<verification>
- `musicstreamer/gbs_marquee.py` exists with module docstring + parse_marquee + MARQUEE_URL constant + imports of `musicstreamer.gbs_api` and `musicstreamer.paths`
- `tests/test_gbs_marquee.py::test_parse_marquee_pipe_split` and ≥ 6 other parse_marquee tests pass
- `tests/fixtures/gbs_marquee/` contains ≥ 10 files (real + synthetic)
- `tests/test_gbs_marquee.py::test_fixture_count_ten_or_more` passes
- `grep -E "QWebEngineProfile|GBS_WEB_PROFILE_NAME|GBS_WEB_STORAGE_PATH|oauth_helper" musicstreamer/gbs_marquee.py` returns no hits (drift-guard pre-flight)
</verification>

<success_criteria>
- Marquee endpoint locked at module scope based on Plan 87-01 evidence (or best-inference default with TODO marker).
- parse_marquee handles all 7 specified edge cases.
- Fixture corpus satisfies GBS-MARQ-07's ≥ 10 count (synthetic + real per Pitfall #8 relaxation).
- gbs_marquee.py is ready for Plan 87-03 to fill `GbsMarqueeWorker` without further structural changes.
</success_criteria>

<output>
Create `.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-02-SUMMARY.md` when done. Record:
- The chosen `MARQUEE_URL` value and rationale (which Plan 87-01 capture contained the marquee text, what selector/JSON path identifies it).
- Whether the observed delimiter was bare `|` or space-padded ` | ` (informs Plan 87-04's keyword-search behavior on full-marquee text).
- Fixture count after this plan ships (`ls tests/fixtures/gbs_marquee/ | wc -l`).
- Any open questions for Plan 87-03 (e.g., "homepage HTML parsing needs an additional regex if the marquee is inside a JS var").
</output>
