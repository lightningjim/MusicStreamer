---
phase: 87-gbs-fm-marquee-themed-day-detection
plan: "07"
type: execute
wave: 1
depends_on:
  - 87-04
files_modified:
  - musicstreamer/gbs_marquee.py
  - musicstreamer/ui_qt/now_playing_panel.py
  - tests/test_gbs_marquee.py
  - tests/fixtures/gbs_marquee/2026-06-15_pride_homepage.html
autonomous: true
gap_closure: true
requirements:
  - GBS-THEME-01
  - GBS-THEME-02
  - GBS-THEME-03
  - GBS-THEME-04
  - GBS-THEME-05
tags:
  - gbs.fm
  - themed-day
  - logo-swap
  - gap-closure
  - thread-safety

must_haves:
  truths:
    - "Themed-day correlation hashes the DYNAMIC #leftmenulogo logo (resolved from homepage CSS), not the static gbs.fm/images/logo_3.png"
    - "extract_leftmenulogo_url(html) returns the off-host URL from `#leftmenulogo {background-image:url('...')}` for BOTH the imgur form and the img.gbs.fm/.../raw form"
    - "The homepage bytes fetched for the marquee are REUSED to resolve the logo URL — no second homepage round-trip"
    - "_fetch_logo_bytes(url) fetches an arbitrary off-site host (i.imgur.com, img.gbs.fm) and keeps the D-18 quiet-failure (WARN log, no toast)"
    - "On a live themed day (Pride fixture), compute_logo_theme reports drift (is_themed=True) so the session override fires"
    - "QPixmap is constructed on the MAIN thread inside set_themed_logo_override — the worker emits raw PNG bytes, never a QPixmap (CR-01 cleared)"
    - "Non-themed days suppress the override: the canonical #leftmenulogo hash is labelled 'canonical' in GBS_LOGO_BASELINE_HASHES"
  artifacts:
    - path: "musicstreamer/gbs_marquee.py"
      provides: "extract_leftmenulogo_url helper + homepage-bytes reuse in _on_tick/_on_first_gbs_bind + url-param _fetch_logo_bytes + raw-bytes emission + re-baselined GBS_LOGO_BASELINE_HASHES"
      contains: "def extract_leftmenulogo_url"
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "set_themed_logo_override decodes raw bytes into QPixmap on the main thread (CR-01)"
      contains: "loadFromData"
    - path: "tests/fixtures/gbs_marquee/2026-06-15_pride_homepage.html"
      provides: "Pride homepage fixture with the img.gbs.fm/.../raw #leftmenulogo form for the resolver regression test"
      contains: "leftmenulogo"
  key_links:
    - from: "GbsMarqueeWorker._on_tick (homepage html)"
      to: "GbsMarqueeWorker._on_first_gbs_bind (logo URL resolution)"
      via: "self._last_homepage_html reuse (single fetch)"
      pattern: "_last_homepage_html"
    - from: "extract_leftmenulogo_url(html)"
      to: "_fetch_logo_bytes(resolved_url)"
      via: "resolved dynamic logo URL drives the byte fetch"
      pattern: "extract_leftmenulogo_url"
    - from: "GbsMarqueeWorker.themed_logo_ready (raw bytes)"
      to: "NowPlayingPanel.set_themed_logo_override (QPixmap built here)"
      via: "Qt.QueuedConnection — decode on main thread (CR-01)"
      pattern: "loadFromData"
---

<objective>
Close the UAT Test 2 gap: binding GBS.FM on a live themed day (Pride Month, today) does NOT swap the Now Playing logo because the themed-day correlator hashes the STATIC `gbs.fm/images/logo_3.png` — an asset that never changes for themed days. The ACTUAL themed logo is referenced dynamically in the homepage CSS as `#leftmenulogo {background-image:url('...')}`, and the URL rotates per theme AND per host (Memorial Day = `https://i.imgur.com/l27hhaY.png`; Pride 2026 = `https://img.gbs.fm/NIgE8/yucEqesu87.png/raw`).

The fix resolves the live `#leftmenulogo` URL from the homepage HTML that is ALREADY fetched for the marquee (single fetch — no second round-trip), fetches THAT off-host URL for the logo bytes, hashes/correlates as today, and re-baselines `GBS_LOGO_BASELINE_HASHES` against the dynamic logo. It also folds in code-review CR-01 (BLOCKER): QPixmap is currently built on the worker QThread; since this plan rewrites the logo fetch/decode path, the worker now emits raw bytes and `set_themed_logo_override` builds the QPixmap on the main thread.

Purpose:
- Add a pure `extract_leftmenulogo_url(html) -> str | None` resolver that parses the `#leftmenulogo` background-image URL (handles imgur AND img.gbs.fm/.../raw forms).
- Reuse the marquee's homepage bytes (cache as `self._last_homepage_html` in `_on_tick`) so `_on_first_gbs_bind` resolves the logo URL without a second fetch.
- Change `_fetch_logo_bytes(url: str)` to take the resolved URL (arbitrary off-site host), preserving D-18 quiet failures.
- Re-baseline `GBS_LOGO_BASELINE_HASHES` so the dynamic `#leftmenulogo` hashes are what the correlator compares against (existing `bd2b83…` entry corresponds to the imgur themed logo, NOT canonical).
- CR-01: worker emits raw PNG bytes via `themed_logo_ready` (already `Signal(object)`); `set_themed_logo_override` decodes to QPixmap on the main thread.
- Secondary: investigate the intermittent `gbs.marquee.fetch_failed url=https://gbs.fm/ error=URLError` — give the anonymous fallback a User-Agent header (the cookie path already sends one via `gbs_api._open_with_cookies`; the anonymous `urlopen` does not).
- Add a regression test that fails today and passes after the fix: parse a fixture homepage → resolve `#leftmenulogo` URL → drift detected.

Output:
- `extract_leftmenulogo_url` + reused homepage bytes + url-param `_fetch_logo_bytes` + raw-bytes emission + re-baselined `GBS_LOGO_BASELINE_HASHES` in `gbs_marquee.py`.
- Main-thread QPixmap decode in `now_playing_panel.py` `set_themed_logo_override`.
- Pride homepage fixture (`2026-06-15_pride_homepage.html`).
- Regression + unit tests in `test_gbs_marquee.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-HUMAN-UAT.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-REVIEW.md
@.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-04-SUMMARY.md
@musicstreamer/gbs_marquee.py
@musicstreamer/gbs_api.py
@musicstreamer/ui_qt/now_playing_panel.py
@tests/fixtures/gbs_marquee/2026-05-25_homepage.html
@tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png
@tests/fixtures/gbs_themed_logos/MANIFEST.md
@tests/test_gbs_marquee.py
@tests/test_gbs_marquee_drift_guard.py

<interfaces>
<!-- Exact current shapes the executor must extend — no exploration needed. -->

From musicstreamer/gbs_marquee.py — the current STATIC fetch (the bug):
```python
def _fetch_logo_bytes() -> bytes | None:
    logo_url = gbs_api.GBS_STATION_METADATA["logo_url"]   # /images/logo_3.png — NEVER changes per theme
    with urllib.request.urlopen(logo_url, timeout=gbs_api._TIMEOUT_READ) as resp:
        return resp.read()
    # ... except → _log.warning("gbs.themed_day.logo_fetch_failed url=%s error=%s", ...)
```

From musicstreamer/gbs_marquee.py — _on_first_gbs_bind currently builds QPixmap on the worker thread (CR-01):
```python
logo_bytes = _fetch_logo_bytes()
result = compute_logo_theme(logo_bytes, self._last_full_marquee_text)
if result.is_themed:
    pix = QPixmap()                       # CR-01: built off the GUI thread
    ok = pix.loadFromData(logo_bytes, "PNG")
    if ok and not pix.isNull():
        self.themed_logo_ready.emit(pix)  # emits a QPixmap
```

From musicstreamer/gbs_marquee.py — _on_tick already fetches the homepage html (REUSE these bytes):
```python
html = _fetch_marquee()
if html is not None:
    plain = extract_noticearea_text(html)
    if plain:
        first, full = parse_marquee(plain)
        self._last_full_marquee_text = full
        self.marquee_ready.emit(first, full)
if not self._themed_day_detected_this_session:
    self._on_first_gbs_bind()
```

From musicstreamer/gbs_marquee.py — signal already accepts object (CR-01 needs NO signal change):
```python
themed_logo_ready = Signal(object)   # was QPixmap, can carry raw bytes
```

From musicstreamer/gbs_marquee.py — the baseline table (re-baseline target):
```python
GBS_LOGO_BASELINE_HASHES: dict[str, str] = {
    "bd2b83fbe2b4bfe9baf8237a8919494e10cc7cf42ad3c42b1fcd605942881be3": "da troops (Memorial Day 2026-05-25)",
}
```
NOTE: `bd2b83…` is the SHA-256 of `tests/fixtures/gbs_themed_logos/2026-05-25_memorial-day_da-troops.png`, fetched from `logo_3.png` during the Memorial Day window — it WAS the themed asset that day. It is a THEMED entry (label != "canonical"), which is correct. No canonical hash was captured (MANIFEST.md "Open canonical-baseline entry"). So leave it as a themed entry and ADD a `# canonical` comment explaining that any not-yet-seen hash drifts per D-12 until a canonical entry accretes.

From musicstreamer/ui_qt/now_playing_panel.py:1129 — slot currently expects a QPixmap (CR-01 changes payload to bytes):
```python
def set_themed_logo_override(self, pixmap: QPixmap | None) -> None:
    if pixmap is None or pixmap.isNull():
        return
    self._themed_logo_override = pixmap
    n = self._current_art_tier or 180
    scaled = pixmap.scaled(QSize(n, n), Qt.AspectRatioMode.KeepAspectRatio, Qt.TransformationMode.SmoothTransformation)
    self.logo_label.setPixmap(scaled)
```
This slot is ALSO called internally with a cached QPixmap from `bind_station` (line ~937) and `_show_station_logo` (line ~2172) for the D-09 re-apply path. Keep those callers working: after CR-01, the worker passes raw bytes but the internal re-apply passes the cached QPixmap. The slot must accept BOTH bytes and QPixmap (decode bytes → QPixmap; pass QPixmap through). Cache the resulting QPixmap in `self._themed_logo_override` (still a QPixmap) so re-apply is unchanged.

From musicstreamer/gbs_api.py — the anonymous urlopen lacks a User-Agent (secondary URLError suspect):
```python
_USER_AGENT = "MusicStreamer/2.0 (gbs_api)"
def _open_with_cookies(url, cookies, timeout=_TIMEOUT_READ):
    req = urllib.request.Request(url, headers={"User-Agent": _USER_AGENT})   # cookie path HAS a UA
    ...
# but gbs_marquee._fetch_marquee's anonymous branch does:  urllib.request.urlopen(MARQUEE_URL, timeout=...)  # NO UA header
```

From tests/fixtures/gbs_marquee/2026-05-25_homepage.html:509 — the canonical selector to parse (imgur form):
```css
#leftmenulogo {background-image:url('https://i.imgur.com/l27hhaY.png');}
```
The Pride live form to also support (build the new fixture around this):
```css
#leftmenulogo {background-image:url('https://img.gbs.fm/NIgE8/yucEqesu87.png/raw');}
```
</interfaces>

<constraints>
- TEST RUNNER: use `.venv/bin/python -m pytest` (system python lacks PySide6.QtWidgets; `.venv` has PySide6 + pytest 9 + pytest-qt). Do NOT use bare `python`/`python3`.
- Phase 87 drift-guards (tests/test_gbs_marquee_drift_guard.py) MUST stay GREEN. In gbs_marquee.py: NO QtWebEngine/oauth_helper/_GbsLoginWindow; NO `repo.set_setting`/`set_station_art`/`.save(`/builtin `open(`; NO `cover_label`/`set_cover`; NO `show_toast`/`libnotify`/`QSystemTrayIcon`; `self.exec_()` must remain in `run()`; `from musicstreamer import gbs_api, paths` must remain. (The `.save(` and `open(` bans are on gbs_marquee.py — the new fixture is a committed file, NOT written by gbs_marquee.py at runtime; write it with the Write tool.)
- D-18 quiet failures preserved: logo-fetch and URL-resolve failures log WARN and return None; NO UI toast, NO exception escaping the worker thread.
- D-09 once-per-session gate preserved: the themed-day routine still fires once per session via `self._themed_day_detected_this_session`.
- GBS-THEME-03: only `logo_label` is touched; `cover_label`/station-list untouched.
- Keep the existing 24 Phase 87 tests in test_gbs_marquee.py green (no regressions). The old `test_compute_logo_theme_hashes_logo_bytes` etc. operate on the da-troops PNG bytes directly and stay valid — `compute_logo_theme` itself is unchanged; only the URL-resolution feeding it changes.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve dynamic #leftmenulogo URL from reused homepage bytes; add Pride fixture + failing regression test</name>
  <files>musicstreamer/gbs_marquee.py, tests/test_gbs_marquee.py, tests/fixtures/gbs_marquee/2026-06-15_pride_homepage.html</files>
  <read_first>
    - musicstreamer/gbs_marquee.py (full — extend _fetch_logo_bytes, _on_tick, _on_first_gbs_bind; add extract_leftmenulogo_url near extract_noticearea_text)
    - tests/fixtures/gbs_marquee/2026-05-25_homepage.html (line 509 — the imgur `#leftmenulogo` selector; copy this file structure for the Pride fixture but swap the leftmenulogo URL to the img.gbs.fm/.../raw form)
    - tests/test_gbs_marquee.py (existing test style — pathlib fixture loading, pure-function tests at top)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-HUMAN-UAT.md (root_cause + missing items — the authoritative spec for this fix)
  </read_first>
  <behavior>
    - `extract_leftmenulogo_url(html: str) -> str | None` is a PURE function (no I/O, no Qt). It searches the homepage HTML for the CSS rule `#leftmenulogo {background-image:url('<URL>')}` and returns `<URL>`, else None.
      - Test: `2026-05-25_homepage.html` → `"https://i.imgur.com/l27hhaY.png"`.
      - Test: Pride fixture → `"https://img.gbs.fm/NIgE8/yucEqesu87.png/raw"` (note the trailing `/raw` and the hashed path — the regex MUST NOT stop at `.png`).
      - Test: html with no `#leftmenulogo` rule → None.
      - Test: tolerate both single and double quotes around the URL, and arbitrary whitespace inside the rule braces (e.g. `#leftmenulogo { background-image: url("...") ; }`).
      - Test: there are OTHER `background-image:url(...)` rules in the same `<style>` block (`#leftmenu`, `#bottomcont`, `#bg1`, `#commentsbox` — see fixture lines 510-527); the function must select the `#leftmenulogo` one specifically, NOT the first url() in the style block.
    - `_fetch_logo_bytes(url: str) -> bytes | None` takes the resolved URL as a parameter (off-site host allowed). Anonymous `urllib.request.urlopen` with `gbs_api._TIMEOUT_READ`. Quiet failures unchanged: log `gbs.themed_day.logo_fetch_failed url=%s error=%s` at WARN (class name only) and return None. The log `url=%s` now records the resolved off-host URL.
    - `_on_tick` caches the fetched homepage HTML on `self._last_homepage_html` (alongside the existing `_last_full_marquee_text`) so `_on_first_gbs_bind` reuses it — NO second `_fetch_marquee()`/homepage round-trip. Initialise `self._last_homepage_html: str = ""` in `__init__`.
    - `_on_first_gbs_bind` flow becomes: resolve `logo_url = extract_leftmenulogo_url(self._last_homepage_html)`; if None → log `gbs.themed_day.logo_url_unresolved` WARN, flip the once-per-session gate (D-17), return; else `logo_bytes = _fetch_logo_bytes(logo_url)`; if None → return (gate already flipped in finally); else `compute_logo_theme(logo_bytes, self._last_full_marquee_text)` exactly as today. The once-per-session gate flip stays in `try/finally` (T-87-04-05).
    - Re-baseline note: `GBS_LOGO_BASELINE_HASHES` keeps the `bd2b83…` entry but its comment is corrected to state it is the DYNAMIC `#leftmenulogo` imgur themed logo (`https://i.imgur.com/l27hhaY.png`), NOT `logo_3.png`. Add a comment that no `canonical` entry exists yet, so any unseen hash drifts per D-12 (this is what makes the live Pride logo fire). Do NOT invent a fake canonical hash.
    - REGRESSION TEST (the gap, must FAIL before the fix, PASS after): given the Pride fixture HTML, `extract_leftmenulogo_url(html)` returns the img.gbs.fm/.../raw URL AND that URL's hash is NOT the baseline themed hash → `compute_logo_theme(<bytes whose hash differs from baseline>, "...")` returns `is_themed=True`. Because the test cannot hit the network, simulate the resolved-URL→bytes step by hashing arbitrary distinct bytes (or by monkeypatching `_fetch_logo_bytes`). The core assertion the gap demands: the resolver returns the off-host Pride URL (proving we read the dynamic logo, not logo_3.png) and drift is detected for a non-baseline hash.
  </behavior>
  <action>
    Step 1 — Create `tests/fixtures/gbs_marquee/2026-06-15_pride_homepage.html` using the Write tool. Base it on `2026-05-25_homepage.html` (same overall structure: a `<p id="noticearea">…<style>…</style></p>` block). Change the marquee body to a Pride-themed line (e.g. `<b>GBS-FM</b>: Happy Pride! | Tune in this weekend`) and, critically, set the style rule to the LIVE Pride form: `#leftmenulogo {background-image:url('https://img.gbs.fm/NIgE8/yucEqesu87.png/raw');}`. Keep the OTHER background-image rules (`#leftmenu`, `#bottomcont`, `#bg1`, `#commentsbox`) present so the resolver is tested against a realistic multi-url style block. Keep the file reasonably small (the head/playlist boilerplate can be trimmed — the resolver only needs the `<style>` block and a noticearea element). Add the fixture to `tests/fixtures/gbs_marquee/MANIFEST.md` with a `capture_date=2026-06-15`, `theme=Pride`, `leftmenulogo_url=https://img.gbs.fm/NIgE8/yucEqesu87.png/raw`, `source=user-reported live URL (87-HUMAN-UAT Test 2)` row.

    Step 2 — In `musicstreamer/gbs_marquee.py`, add a module-level compiled regex and pure helper `extract_leftmenulogo_url(html: str) -> str | None` near `extract_noticearea_text`. Regex must anchor on the `#leftmenulogo` selector and capture the `url('…')` / `url("…")` argument up to the closing quote (do NOT stop at `.png`; the Pride URL ends in `/raw`). Suggested pattern: match `#leftmenulogo` then non-greedily up to `background-image` then `url(` then a quote then capture `[^'"]+` until the matching quote. Use `re.IGNORECASE | re.DOTALL`. Return None on no match.

    Step 3 — Change `_fetch_logo_bytes()` to `_fetch_logo_bytes(url: str)`: replace the `logo_url = gbs_api.GBS_STATION_METADATA["logo_url"]` line with using the `url` parameter. Keep the WARN log + return-None failure handling; the `url=%s` field logs the passed URL.

    Step 4 — In `GbsMarqueeWorker.__init__`, add `self._last_homepage_html: str = ""`. In `_on_tick`, after `html = _fetch_marquee()`, set `self._last_homepage_html = html or ""` (cache for reuse — do this whether or not noticearea parse succeeds, so the logo URL can still resolve even if the marquee text is empty).

    Step 5 — Rewrite `_on_first_gbs_bind` to resolve the dynamic URL from `self._last_homepage_html`:
    - `logo_url = extract_leftmenulogo_url(self._last_homepage_html)`
    - if `logo_url is None`: `_log.warning("gbs.themed_day.logo_url_unresolved")` then return (gate flips in finally).
    - else `logo_bytes = _fetch_logo_bytes(logo_url)`; if None return; else `result = compute_logo_theme(logo_bytes, self._last_full_marquee_text)`.
    - (CR-01 emission change lands in Task 2 — for THIS task, keep whatever emission exists compiling; Task 2 swaps it to raw bytes. To avoid a broken intermediate, it is acceptable to land Steps 5 and the Task-2 emission change together if cleaner — implementer's call, but keep the suite green at each commit.)
    - Correct the `GBS_LOGO_BASELINE_HASHES` comment block per the behavior block (dynamic imgur logo; no canonical yet).
    - Remove the dead `logo_url = gbs_api.GBS_STATION_METADATA["logo_url"]` line at the top of `_on_first_gbs_bind` (IN-01 from 87-REVIEW.md — it is now genuinely dead and replaced by the resolved URL).

    Step 6 — Add tests to `tests/test_gbs_marquee.py`:
    - `test_extract_leftmenulogo_url_imgur_form`: load `2026-05-25_homepage.html`; assert `extract_leftmenulogo_url(html) == "https://i.imgur.com/l27hhaY.png"`.
    - `test_extract_leftmenulogo_url_imggbsfm_raw_form`: load `2026-06-15_pride_homepage.html`; assert `== "https://img.gbs.fm/NIgE8/yucEqesu87.png/raw"`.
    - `test_extract_leftmenulogo_url_absent_returns_none`: html without the rule → None.
    - `test_extract_leftmenulogo_url_selects_correct_rule`: a small inline html string with `#leftmenu {background-image:url('http://other/a.png');}` BEFORE `#leftmenulogo {background-image:url('http://right/b.png');}` → returns `http://right/b.png` (not the earlier `#leftmenu` url).
    - `test_extract_leftmenulogo_url_quote_and_whitespace_tolerant`: `#leftmenulogo { background-image: url("http://x/y.png") ; }` (double quotes, spaces) → `http://x/y.png`.
    - `test_pride_logo_drifts_from_baseline` (THE REGRESSION — fails before fix because today the code reads logo_3.png and never resolves this URL): load the Pride fixture, assert `extract_leftmenulogo_url(html)` is the img.gbs.fm/.../raw URL (proves dynamic resolution), then assert `compute_logo_theme(b"pride-logo-bytes-distinct-from-baseline", "Happy Pride!").is_themed is True` (a hash not in GBS_LOGO_BASELINE_HASHES drifts per D-12). Document in the test docstring that this encodes UAT Test 2.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_gbs_marquee.py -k "leftmenulogo or pride_logo_drifts" -x -q</automated>
  </verify>
  <done>extract_leftmenulogo_url resolves both imgur and img.gbs.fm/.../raw forms from reused homepage bytes; _fetch_logo_bytes takes a url param; _on_first_gbs_bind resolves the dynamic URL; Pride fixture committed; the regression test (and 5 resolver tests) are green; the baseline comment is corrected; drift-guards still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move QPixmap construction to the main thread (CR-01) + add anonymous-fetch User-Agent (secondary URLError)</name>
  <files>musicstreamer/gbs_marquee.py, musicstreamer/ui_qt/now_playing_panel.py, tests/test_gbs_marquee.py</files>
  <read_first>
    - musicstreamer/gbs_marquee.py (Task 1 result — _on_first_gbs_bind emission site; _fetch_marquee anonymous branch)
    - musicstreamer/ui_qt/now_playing_panel.py:1129-1183 (set_themed_logo_override + attach_gbs_marquee_worker — the slot + connection)
    - musicstreamer/ui_qt/now_playing_panel.py:930-945 (bind_station internal re-apply of cached QPixmap — must keep working)
    - musicstreamer/ui_qt/now_playing_panel.py:2156-2173 (_show_station_logo internal re-apply of cached QPixmap — must keep working)
    - musicstreamer/gbs_api.py:77 + 158-172 (_USER_AGENT constant + _open_with_cookies UA header precedent)
    - .planning/phases/87-gbs-fm-marquee-themed-day-detection/87-REVIEW.md §CR-01 (the BLOCKER spec)
  </read_first>
  <behavior>
    - CR-01: the worker NEVER constructs or touches a QPixmap. In `_on_first_gbs_bind`, when `result.is_themed`, emit the RAW `logo_bytes` (the signal is already `Signal(object)` — no declaration change). Remove the `pix = QPixmap(); pix.loadFromData(...)` block and the `from PySide6.QtGui import QPixmap` import IF QPixmap is no longer referenced anywhere in gbs_marquee.py (grep to confirm; if the import is unused, drop it to keep the module GUI-free).
    - `set_themed_logo_override(payload)` (now_playing_panel.py) accepts EITHER raw `bytes` (from the worker — decode to QPixmap on the main thread via `QPixmap().loadFromData(payload, "PNG")`) OR a `QPixmap` (from the internal D-09 re-apply callers). Behavior:
      - if `payload is None`: return (defensive; existing behavior — note WR-04 is out of scope here, do not change the None semantics).
      - if `isinstance(payload, (bytes, bytearray))`: build `pix = QPixmap(); ok = pix.loadFromData(bytes(payload), "PNG")`; if not ok or `pix.isNull()`: return (decode failure → keep canonical logo, T-87-04-01).
      - elif it is a QPixmap: if `payload.isNull()`: return; else `pix = payload`.
      - Cache `self._themed_logo_override = pix` (always a QPixmap) and apply scaled to `self.logo_label` (unchanged scaling logic). The two internal re-apply call sites (`bind_station` ~937, `_show_station_logo` ~2172) pass the cached QPixmap and keep working unchanged.
    - The QImage alternative (QImage.fromData on worker, QPixmap.fromImage on main) is acceptable too, but emitting raw bytes is simplest and keeps the worker fully GUI-free. Pick raw bytes.
    - Secondary (URLError): in `gbs_marquee._fetch_marquee`, the anonymous fallback branch builds a `urllib.request.Request(MARQUEE_URL, headers={"User-Agent": gbs_api._USER_AGENT})` and passes it to `urlopen`, mirroring `_open_with_cookies`. Likewise `_fetch_logo_bytes(url)` builds a Request with the same UA header. Rationale: the cookie path already sends a UA; a bare anonymous `urlopen` sends `Python-urllib/3.x`, which some hosts (incl. CDNs like img.gbs.fm) reject → URLError. This is the low-risk, no-over-engineering hypothesis from UAT secondary item; keep the existing quiet-failure handling.
  </behavior>
  <action>
    Step 1 — In `_on_first_gbs_bind` (gbs_marquee.py), replace the QPixmap construction + `themed_logo_ready.emit(pix)` with `self.themed_logo_ready.emit(logo_bytes)`. Keep the `fallback_unknown_theme` INFO log (`gbs.themed_day.unknown_theme_observed hash=%s`) exactly as today. Remove the now-unused `from PySide6.QtGui import QPixmap` import from gbs_marquee.py if grep confirms QPixmap is referenced nowhere else in the file. Update the inline comment to state the decode happens on the main thread in `set_themed_logo_override` (CR-01).

    Step 2 — In `now_playing_panel.py` `set_themed_logo_override`, change the signature/body to accept bytes-or-QPixmap per the behavior block. Build the QPixmap from bytes on the main thread; pass a QPixmap through unchanged; cache the resulting QPixmap. Keep the scaling + `self.logo_label.setPixmap(scaled)` logic. Update the docstring to note the worker now sends raw bytes (CR-01 cleared) and the slot decodes on the main thread. Do NOT touch the `bind_station` / `_show_station_logo` re-apply call sites — they pass the cached QPixmap, which the slot still accepts.

    Step 3 — In `_fetch_marquee` (anonymous branch) and `_fetch_logo_bytes`, wrap the URL in `urllib.request.Request(url, headers={"User-Agent": gbs_api._USER_AGENT})` before `urlopen`. (gbs_api._USER_AGENT is already imported transitively via `gbs_api`; reference it as `gbs_api._USER_AGENT`.) Leave the cookie path (`gbs_api._open_with_cookies`) untouched — it already sets the header.

    Step 4 — Tests in `tests/test_gbs_marquee.py`:
    - `test_worker_emits_raw_bytes_not_qpixmap`: instantiate `GbsMarqueeWorker`; monkeypatch `_fetch_logo_bytes` to return the da-troops PNG bytes; set `worker._last_homepage_html` to the Pride fixture html and `worker._last_full_marquee_text` to a non-keyword string; call `worker._on_first_gbs_bind()` directly (synchronous, no event loop needed); capture the `themed_logo_ready` emission via a Python list connected to the signal; assert the emitted payload is `bytes` (NOT a QPixmap). (Requires a QApplication for signal machinery — reuse the existing test's QApplication fixture pattern.)
    - `test_set_themed_logo_override_accepts_bytes`: instantiate NowPlayingPanel with a GBS station; call `panel.set_themed_logo_override(<da-troops PNG bytes>)`; assert `panel.logo_label.pixmap()` is set (not null) and `panel._themed_logo_override` is a QPixmap. Then call `panel.set_themed_logo_override(panel._themed_logo_override)` (the QPixmap re-apply path) and assert it still works (no exception, logo_label still set). Confirms the slot handles BOTH payload types.
    - `test_anonymous_marquee_fetch_sends_user_agent` (optional, light): monkeypatch `gbs_api.load_auth_context` to return None and `urllib.request.urlopen` to capture its first arg; call `_fetch_marquee()`; assert the captured arg is a `urllib.request.Request` whose `get_header("User-agent")` equals `gbs_api._USER_AGENT`. Keep it simple; skip if the urlopen monkeypatch proves brittle (document the skip).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_gbs_marquee.py tests/test_gbs_marquee_drift_guard.py -x -q</automated>
  </verify>
  <done>Worker emits raw PNG bytes (no QPixmap off the GUI thread — CR-01 cleared); set_themed_logo_override decodes bytes on the main thread and still accepts the cached QPixmap for D-09 re-apply; anonymous marquee + logo fetches send a User-Agent header; full gbs_marquee test suite + drift-guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homepage HTML → extract_leftmenulogo_url | Untrusted operator-controlled HTML/CSS; the resolver returns a URL string that then drives a network fetch |
| Resolved off-host URL → _fetch_logo_bytes | The themed-logo URL points at arbitrary third-party hosts (i.imgur.com, img.gbs.fm); bytes are fetched anonymously |
| Worker thread → main thread (raw bytes) | themed_logo_ready now emits `bytes` (immutable, trivially thread-safe) across the Qt.QueuedConnection — QPixmap is built ONLY on the main thread |
| Off-host PNG bytes → QPixmap.loadFromData (main thread) | Untrusted PNG bytes parsed by Qt's decoder on the GUI thread; decode failure → canonical logo retained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-07-01 | Tampering | Operator serves a malicious PNG at the resolved #leftmenulogo URL to crash Qt's decoder | mitigate | `QPixmap.loadFromData` returns False on decode failure (now on the main thread); slot returns early and keeps the canonical logo. Decode is on the GUI thread where Qt's decoder is supported (CR-01 fix) |
| T-87-07-02 | Spoofing | Operator points #leftmenulogo at an attacker-controlled host returning tracking/redirect content | accept | The URL is operator-chosen by design (the site's own themed logo); bytes are only decoded as an image, never executed. Anonymous fetch sends no cookies to the off-host (cookies stay on the gbs.fm cookie path). Acceptable per D-18 quiet-failure model |
| T-87-07-03 | Information Disclosure | Fetching an off-host logo URL leaks the user's IP to a third party (imgur/CDN) | accept | Same exposure the user already incurs by loading gbs.fm in any browser; no credentials sent (anonymous urlopen, no cookie jar on the off-host fetch). No PII transmitted |
| T-87-07-04 | Denial of Service | A pathological #leftmenulogo URL (huge file / slow host) stalls the worker | mitigate | Fetch uses `gbs_api._TIMEOUT_READ` (10s); failure is quiet (WARN + None) and the once-per-session gate flips in finally so no retry loop (T-87-04-05 preserved) |
| T-87-07-05 | Tampering | Regex over-matches and resolves the WRONG background-image rule (e.g. #leftmenu) → hashes the wrong asset | mitigate | Resolver anchors specifically on the `#leftmenulogo` selector; `test_extract_leftmenulogo_url_selects_correct_rule` proves it skips an earlier `#leftmenu` url() in the same style block |
| T-87-07-06 | Information Disclosure | Resolved URL or marquee body leaked into a log line | mitigate | Log lines record only the URL (operator-public) and the SHA-256 hash for the unknown-theme path — NO marquee body. Drift-guard greps in 87-06 stay green |
| T-87-07-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages introduced; uses stdlib `urllib`/`re`/`hashlib` and existing PySide6. Slopcheck N/A |
</threat_model>

<verification>
- `extract_leftmenulogo_url` resolves the imgur form, the img.gbs.fm/.../raw form, returns None when absent, selects the correct rule among multiple url() rules, and tolerates quotes/whitespace.
- The Pride fixture's dynamic logo hash drifts from the baseline → `compute_logo_theme` reports `is_themed=True` (regression test green; this is UAT Test 2 in code form).
- Homepage bytes are fetched once and reused (`self._last_homepage_html`) — no second round-trip.
- The worker emits raw `bytes` via `themed_logo_ready`; NO QPixmap is constructed off the GUI thread (CR-01 cleared).
- `set_themed_logo_override` decodes bytes on the main thread AND still accepts a cached QPixmap for the D-09 re-apply path.
- Anonymous marquee + logo fetches send a `User-Agent` header.
- All Phase 87 tests (test_gbs_marquee.py) + drift-guards (test_gbs_marquee_drift_guard.py) green under `.venv/bin/python -m pytest`.
</verification>

<success_criteria>
- Binding GBS.FM on the live Pride day resolves the dynamic `#leftmenulogo` URL (`img.gbs.fm/.../raw`), fetches it, detects drift, and applies the themed logo to `logo_label` for the session (UAT Test 2 passes on re-verify).
- The static `logo_3.png` is no longer the asset the correlator hashes.
- The themed logo still does NOT persist to disk/SQLite (D-05), still targets logo_label only (GBS-THEME-03), still surfaces no toast (GBS-THEME-05 / D-18), still fires once per session (D-09).
- CR-01 is cleared: QPixmap is built only on the main thread; UAT Test 3 (Windows/macOS thread-safety) can be re-verified now that the swap path actually fires.
- The intermittent anonymous `URLError` on `https://gbs.fm/` is mitigated by sending a User-Agent header (or, if it recurs, the WARN log now records it for further diagnosis — no over-engineering).
- Full gbs_marquee test suite + drift-guards green; no regressions to prior Phase 87 tests.
</success_criteria>

<output>
Create `.planning/phases/87-gbs-fm-marquee-themed-day-detection/87-07-SUMMARY.md` when done. Record:
- The exact `extract_leftmenulogo_url` regex used and which forms it was tested against (imgur, img.gbs.fm/.../raw).
- Whether QPixmap was fully removed from gbs_marquee.py (import dropped?) — confirms CR-01 surface is clean for the drift-guard.
- The final shape of `set_themed_logo_override` (bytes-or-QPixmap union) and confirmation the two internal re-apply call sites still pass a QPixmap.
- Whether the User-Agent change is believed to fix the URLError or whether it remains under observation (so a re-UAT can confirm).
- New `GBS_LOGO_BASELINE_HASHES` comment state: still 1 themed entry, no canonical — note this for the baseline-accretion todo (a canonical entry should be captured next non-themed window).
- Confirmation the worker now reuses `self._last_homepage_html` (single fetch) — no second homepage round-trip introduced.
</output>
