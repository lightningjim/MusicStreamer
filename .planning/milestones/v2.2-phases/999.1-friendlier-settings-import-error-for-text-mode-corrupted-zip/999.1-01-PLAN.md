---
phase: 999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/settings_export.py
  - tests/test_settings_export.py
autonomous: true
requirements:
  - REQ-999.1-DETECT
  - REQ-999.1-FALLBACK
user_setup: []

must_haves:
  truths:
    - "A ZIP that starts with PK magic AND contains injected CRLF runs is classified as text-mode corruption (D-01)"
    - "preview_import raises TextModeCorruptionError for a text-mode-mangled backup (D-03 specific tier)"
    - "preview_import raises a generic ValueError with an incomplete/corrupted hint for any other BadZipFile (D-03 generic tier)"
    - "Detection reads only RAW bytes — no decompression of attacker-controlled zip internals"
    - "Detection never auto-repairs (no 0x0D0A -> 0x0A collapse) (D-01)"
  artifacts:
    - path: "musicstreamer/settings_export.py"
      provides: "TextModeCorruptionError class + _is_text_mode_corrupted_zip helper + branched BadZipFile handling"
      contains: "class TextModeCorruptionError"
    - path: "tests/test_settings_export.py"
      provides: "4 new unit tests for detection + raise behavior (no Qt import)"
      contains: "TextModeCorruptionError"
  key_links:
    - from: "musicstreamer/settings_export.py preview_import except zipfile.BadZipFile"
      to: "_is_text_mode_corrupted_zip"
      via: "branch on detection result"
      pattern: "_is_text_mode_corrupted_zip"
---

<objective>
Add text-mode-corruption detection to the settings-import path. Inside the existing
`except zipfile.BadZipFile` block of `preview_import`, fingerprint a CRLF-corrupted
backup (D-01) and raise a typed `TextModeCorruptionError` (D-03 specific tier); for any
other bad zip, raise a generic `ValueError` carrying an incomplete/corrupted hint
(D-03 generic tier). All logic is pure stdlib `zipfile` with no Qt dependency, fully
unit-testable headless.

Purpose: Turn the unhelpful "Not a valid ZIP archive" into an actionable recovery
signal for the Phase 88 / G4 text-mode transfer failure, without touching the export
side or attempting any auto-repair.

Output: A `TextModeCorruptionError` class, a `_is_text_mode_corrupted_zip` helper,
a branched `except` block, and 4 new unit tests appended to the existing test file.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-CONTEXT.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-RESEARCH.md
@.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-VALIDATION.md

<interfaces>
<!-- Existing contract the executor extends. Extracted from codebase 2026-06-19. -->

settings_export.py:246 (current except block to replace, lines 255-258):
  try:
      zf_handle = zipfile.ZipFile(zip_path, "r")
  except zipfile.BadZipFile:
      raise ValueError("Not a valid ZIP archive")

settings_export.py already imports: zipfile (line 20), os (line 16).
preview_import signature: def preview_import(zip_path: str, repo: Repo) -> ImportPreview

tests/test_settings_export.py:
  - fixture `repo(tmp_path)` (line 30) — sqlite-backed Repo
  - helper `_make_import_zip(tmp_path, payload, ...)` (line 112) — writes a VALID
    minimal zip via zipfile.ZIP_DEFLATED with a settings.json member; returns the path
  - existing `test_preview_invalid_zip` (line 302) uses
    `pytest.raises(ValueError, match="Not a valid ZIP archive")` — regex substring
    match, MUST still pass against the new longer D-03 string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TextModeCorruptionError + detection helper + branched BadZipFile handling</name>
  <files>musicstreamer/settings_export.py</files>
  <read_first>
    - musicstreamer/settings_export.py lines 1-27 (module docstring + imports; confirm `zipfile`, `os` already imported)
    - musicstreamer/settings_export.py lines 246-270 (preview_import; the except zipfile.BadZipFile site at 255-258 is the insertion point)
  </read_first>
  <behavior>
    - `_is_text_mode_corrupted_zip(path)` returns True when file bytes start with `b"PK\x03\x04"` AND contain `b"\x0d\x0a"` somewhere
    - Returns False when bytes do not start with `b"PK\x03\x04"` (non-zip / garbage), even if CRLF present
    - Returns False when no `b"\x0d\x0a"` pair is present (e.g. truncated zip)
    - Returns False (never raises) on OSError opening the path
    - `preview_import` on a text-mode-mangled zip raises `TextModeCorruptionError`
    - `preview_import` on any other BadZipFile raises `ValueError` whose message contains "Not a valid ZIP archive" and "incomplete or corrupted"
  </behavior>
  <action>
    Define a new exception `class TextModeCorruptionError(ValueError)` near the top of
    settings_export.py (after imports, before `_coerce_cover_art_source`), with a short
    docstring noting it signals an LF->CRLF text-mode transfer and the recovery is to
    re-copy in binary mode. It subclasses ValueError so existing broad handlers still
    treat it as an import-validation error (per D-03).

    Define module-level helper `_is_text_mode_corrupted_zip(path: str) -> bool` per D-01
    (strict fingerprint). Open the path in binary mode inside a try/except OSError that
    returns False on failure. Read the file bytes and return
    `data[:4] == b"PK\x03\x04" and b"\x0d\x0a" in data`. SECURITY (T-999.1-01): this is a
    RAW-byte scan only — it MUST NOT call zipfile, decompress, or otherwise parse the
    attacker-controlled zip internals; the file is already known-bad. The read is bounded
    by the OS file size and runs only AFTER `zipfile.ZipFile` already opened (and failed
    on) the same file, so no new unbounded-allocation surface is introduced beyond that
    existing open; settings backups are small (<500 KB typical). Do NOT modify or rewrite
    any bytes — detection only, no auto-repair (D-01 forbids 0x0D0A->0x0A collapse).

    Replace the `except zipfile.BadZipFile:` body at lines 255-258. New body: if
    `_is_text_mode_corrupted_zip(zip_path)` is True, `raise TextModeCorruptionError(...)`
    (message text is not user-facing — the UI maps the type to the D-05 wording in Plan
    02; use a short internal message such as the sentinel token). Otherwise
    `raise ValueError("Not a valid ZIP archive — the file may be incomplete or corrupted.")`
    per D-03. Use `raise ... from None` only if it matches surrounding style; otherwise a
    plain raise inside the except is fine. Do not touch the success path or
    `_validate_zip_members` (path-traversal guard stays unchanged and still runs after a
    successful open).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import musicstreamer.settings_export as s; assert issubclass(s.TextModeCorruptionError, ValueError); assert s._is_text_mode_corrupted_zip.__name__"</automated>
  </verify>
  <acceptance_criteria>
    - settings_export.py defines `class TextModeCorruptionError(ValueError)`
    - settings_export.py defines module-level `_is_text_mode_corrupted_zip(path: str) -> bool` that opens in binary mode, returns False on OSError, and returns `data[:4] == b"PK\x03\x04" and b"\x0d\x0a" in data`
    - The detection helper performs NO decompression and NO zipfile parsing (raw bytes only)
    - The `except zipfile.BadZipFile` block raises `TextModeCorruptionError` on detection, else raises `ValueError` whose message contains both "Not a valid ZIP archive" and "incomplete or corrupted"
    - No byte-rewriting / auto-repair code is present
    - `.venv/bin/python -c "import musicstreamer.settings_export"` imports cleanly
  </acceptance_criteria>
  <done>TextModeCorruptionError, _is_text_mode_corrupted_zip, and the branched except block exist; module imports without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 4 unit tests for detection + raise behavior; confirm existing test survives</name>
  <files>tests/test_settings_export.py</files>
  <read_first>
    - tests/test_settings_export.py lines 30-37 (repo fixture)
    - tests/test_settings_export.py lines 112-140 (_make_import_zip helper — builds a valid minimal zip)
    - tests/test_settings_export.py lines 302-318 (existing test_preview_invalid_zip + test_preview_missing_settings_json patterns)
  </read_first>
  <behavior>
    - test_crlf_detection_text_mode_zip (DET-01): build a valid minimal zip via `_make_import_zip`, read its bytes, produce a mangled copy by replacing each lone `b"\x0a"` (an `\x0a` NOT already preceded by `\x0d`) with `b"\x0d\x0a"`, write to a new path; `_is_text_mode_corrupted_zip(mangled)` is True
    - test_crlf_detection_truncated_zip (DET-02): write a buffer that starts with `b"PK\x03\x04"` plus arbitrary bytes that contain NO `b"\x0d\x0a"` pair; `_is_text_mode_corrupted_zip` is False
    - test_crlf_detection_non_zip (DET-03): write bytes that contain `b"\x0d\x0a"` but do NOT start with `b"PK\x03\x04"`; `_is_text_mode_corrupted_zip` is False
    - test_preview_raises_text_mode_error (RAISE-01): feed the mangled text-mode zip to `preview_import`; it raises `TextModeCorruptionError`
    - test_preview_invalid_zip (existing, RAISE-02/03): still passes against the new longer D-03 message via its regex substring match
  </behavior>
  <action>
    Append 4 new test functions to tests/test_settings_export.py. Import
    `TextModeCorruptionError` and `_is_text_mode_corrupted_zip` from
    `musicstreamer.settings_export` (extend the existing import line; do NOT add any Qt
    import — these tests must run headless). For the text-mode fixture, build a valid zip
    with `_make_import_zip(tmp_path, {...minimal valid payload...})`, read the bytes, and
    construct the mangled bytes by walking the buffer and emitting `\x0d\x0a` wherever a
    lone `\x0a` (not already preceded by `\x0d`) appears, mirroring how a text-mode
    transfer rewrites LF; write the result to `tmp_path/"mangled.zip"`. Reuse the existing
    `repo` fixture for the `preview_import` test. Assert the three detection booleans
    (DET-01 True, DET-02 False, DET-03 False) and `pytest.raises(TextModeCorruptionError)`
    for RAISE-01. Do NOT edit `test_preview_invalid_zip` — only confirm it still passes;
    if the D-03 message somehow broke its `match="Not a valid ZIP archive"` regex, fix the
    Plan-01 Task-1 message rather than the test.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_settings_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_settings_export.py adds test_crlf_detection_text_mode_zip, test_crlf_detection_truncated_zip, test_crlf_detection_non_zip, test_preview_raises_text_mode_error
    - None of the new tests import any PySide6 / Qt module
    - The text-mode fixture is a valid zip mangled by LF->CRLF rewrite (lone 0x0A -> 0x0D0A), making `_is_text_mode_corrupted_zip` return True
    - A truncated/garbage fixture and a non-PK-magic fixture both make detection return False
    - `preview_import(mangled)` raises `TextModeCorruptionError`
    - Existing `test_preview_invalid_zip` still passes (regex substring match against the new D-03 string)
    - `.venv/bin/python -m pytest tests/test_settings_export.py -q` exits 0
  </acceptance_criteria>
  <done>All settings_export tests green (39 total: 35 existing + 4 new); existing invalid-zip test unchanged and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file picker -> preview_import | User-supplied `.zip` that already failed `zipfile.ZipFile` open crosses into detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | Denial of Service | `_is_text_mode_corrupted_zip` full-file read | mitigate | Read runs only after `zipfile.ZipFile` already opened/failed on the same path (no new unbounded surface); operates on a small settings backup (<500 KB typical); pure raw-byte scan, no per-member allocation. Residual severity: LOW (a 10 MB read is <50ms on local storage). |
| T-999.1-02 | Tampering | attacker-controlled zip internals | mitigate | Detection touches RAW bytes only (magic compare + `\x0d\x0a` membership). It does NOT decompress, parse central directory, or read members. No deserialization of corrupt content. Residual severity: LOW. |
| T-999.1-03 | Information disclosure / data loss | byte rewriting | accept (by design forbidden) | D-01 forbids auto-repair; the helper never modifies bytes, so no risk of clobbering legitimate binary pairs. No mitigation code needed; absence-of-feature is the control. |

No npm/pip/cargo installs in this plan — no T-999.1-SC supply-chain checkpoint required.
Residual severity across the register: LOW. No high-severity items.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_settings_export.py -q` exits 0 (39 tests)
- `.venv/bin/python -c "import musicstreamer.settings_export as s; assert issubclass(s.TextModeCorruptionError, ValueError)"` succeeds
- No Qt import added to tests/test_settings_export.py (grep finds no PySide6 import)
</verification>

<success_criteria>
- TextModeCorruptionError and _is_text_mode_corrupted_zip exist in settings_export.py
- BadZipFile handling is two-tier per D-03 (typed error vs generic+hint)
- Detection is strict per D-01 (PK magic AND CRLF), raw-bytes-only, no auto-repair
- 4 new headless unit tests pass; existing invalid-zip test still passes
</success_criteria>

<output>
Create `.planning/phases/999.1-friendlier-settings-import-error-for-text-mode-corrupted-zip/999.1-01-SUMMARY.md` when done
</output>
