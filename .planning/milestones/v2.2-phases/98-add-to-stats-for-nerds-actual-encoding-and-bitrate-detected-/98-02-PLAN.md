---
phase: 98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/ui_qt/now_playing_panel.py
  - tests/test_now_playing_stats.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07]
must_haves:
  truths:
    - "D-04: the Stats-for-Nerds panel shows four detected format rows — Encoding, Bitrate, Sample rate, Bit depth"
    - "D-01: Encoding and Bitrate rows show detected AND declared/expected together, always"
    - "D-02: a detected-vs-declared mismatch renders the detected value amber via _StatLabel.set_mismatch; a match renders muted"
    - "D-05: Sample rate and Bit depth are plain _MutedLabel detected-only rows with no mismatch flag"
    - "D-07: unknown detected values render em-dash '—'; rows are always present; no flag when both detected and expected are unknown"
    - "Bitrate mismatch tolerance of 5 kbps suppresses small drift; >5 kbps flags amber"
    - "Pitfall 8: the four new rows carry no per-row setVisible — they inherit set_stats_visible from the wrapper"
  artifacts:
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "_StatLabel amber subclass, four format rows in _build_stats_widget, update_detected_format, update_detected_caps, bind_station reset"
      contains: "_StatLabel"
    - path: "tests/test_now_playing_stats.py"
      provides: "Wave 0 panel rendering tests"
      exports: ["test_encoding_row_shows_detected_and_expected", "test_encoding_mismatch_sets_amber", "test_no_mismatch_flag_when_codec_matches", "test_bitrate_mismatch_tolerance", "test_sample_rate_label_is_muted_not_stat", "test_em_dash_when_codec_unknown", "test_no_mismatch_when_both_unknown", "test_no_per_row_visible_in_build_stats"]
  key_links:
    - from: "now_playing_panel.update_detected_format"
      to: "self._streams (declared codec/bitrate_kbps)"
      via: "next((s for s in self._streams if s.id == stream_id), None)"
      pattern: "self\\._streams"
    - from: "_StatLabel.set_mismatch"
      to: "amber QPalette.WindowText"
      via: "theme-aware _apply_muted_palette override"
      pattern: "set_mismatch"
---

<objective>
Extend the Stats-for-Nerds panel with the four detected-format rows (Encoding, Bitrate,
Sample rate, Bit depth), an amber-on-mismatch `_StatLabel` subclass, the `update_detected_format`
and `update_detected_caps` public methods, and the `bind_station` reset; plus a new unit-test
file covering all panel rendering decisions.

Purpose: This is the consumer/display half of Phase 98 — it turns the detected values (delivered
by Plan 03's wiring) plus the declared Stream.codec/bitrate_kbps into the user-facing
"validate you are playing what you expect" comparison. Implements D-01, D-02, D-04, D-05, D-07.
Output: _StatLabel, four QFormLayout rows, two panel methods, bind_station reset; tests/test_now_playing_stats.py.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-RESEARCH.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-PATTERNS.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration. -->

musicstreamer/models.py StationStream fields: id: int, codec: str = "" (MP3|AAC|OPUS|FLAC|""), bitrate_kbps: int = 0 (0 = unknown).

musicstreamer/ui_qt/now_playing_panel.py:
- _MutedLabel class (~179-201): QLabel subclass; __init__ calls _apply_muted_palette(); _apply_muted_palette sets QPalette.WindowText to QPalette.Disabled/WindowText; changeEvent re-applies on PaletteChange/StyleChange.
- self._streams (~333): list of StationStream; populated by _populate_stream_picker (~1589-1592) from repo.list_streams(station.id). Lookup declared values here by .id — no signal payload for declared values (D-03).
- _build_stats_widget (~3471-3521): QWidget wrapper + QFormLayout; existing rows Buffer (progressbar), Underruns (_MutedLabel), Buf duration (_MutedLabel); ends with wrapper.setVisible(False); returns wrapper.
- set_underrun_count / set_buffer_duration setters (~1171-1203) — pattern for the new public methods.
- bind_station (~966-994) — resets name/icy/star/logo/_last_cover_icy/_last_icy_title on station switch.
- set_stats_visible (~1205-1207) — wrapper-level toggle governing ALL stats rows (Pitfall 8: never per-row).
- Import line (~35): from PySide6.QtGui import ... QPalette, QPixmap — ADD QColor to it.

Existing test analogs (tests/test_now_playing_panel.py): class FakeRepo (~39), FakePlayer, NowPlayingPanel(FakePlayer(), FakeRepo({"volume": "80"})) construction (~152-155), _RepoWithStreams subclass whose list_streams returns a fixed list (~3129-3140), StationStream import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _StatLabel amber subclass + four format rows in _build_stats_widget + bind_station reset</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py — _MutedLabel (~179-201), the QtGui import (~35), _build_stats_widget (~3471-3521), bind_station (~966-994). 98-PATTERNS.md "_MutedLabel class to subclass", "_StatLabel subclass", "_build_stats_widget" + "New rows", "bind_station reset" sections.
    - 98-RESEARCH.md §Pattern 4 (_StatLabel), §Pattern 5 (new rows), Pitfall 6 (amber theme), Pitfall 7 (bind_station stale reset), Pitfall 8 (no per-row setVisible).
  </read_first>
  <behavior>
    - test_sample_rate_label_is_muted_not_stat: panel._sample_rate_label is a plain _MutedLabel (NOT a _StatLabel) and has no set_mismatch attribute (D-05)
    - test_no_per_row_visible_in_build_stats: inspect.getsource(NowPlayingPanel._build_stats_widget) with comment lines stripped contains exactly one "setVisible" (the wrapper.setVisible(False)) (Pitfall 8)
    - panel construction with four format rows present: _encoding_label, _bitrate_label, _sample_rate_label, _bit_depth_label all default to "—"
  </behavior>
  <action>
    Add QColor to the existing PySide6.QtGui import line (~35).
    Add a _StatLabel(_MutedLabel) class immediately after _MutedLabel implementing D-02 amber: class attrs _AMBER_LIGHT = QColor(180, 120, 0) and _AMBER_DARK = QColor(255, 180, 60); __init__ sets self._mismatch = False; set_mismatch(mismatch: bool) no-ops when unchanged else stores and calls self._apply_muted_palette(); override _apply_muted_palette to call super()._apply_muted_palette() when not mismatched, else pick amber by QPalette.Window lightness (<128 → _AMBER_DARK else _AMBER_LIGHT) and set QPalette.WindowText. Do NOT override changeEvent — it is inherited from _MutedLabel and dispatches to the overridden _apply_muted_palette via MRO (98-PATTERNS "changeEvent Theme-Safety").
    In _build_stats_widget, BEFORE the existing Buffer row, add four rows in D-04 order using the exact addRow pattern with NO per-row setVisible (Pitfall 8): Encoding → self._encoding_label = _StatLabel("—", wrapper); Bitrate → self._bitrate_label = _StatLabel("—", wrapper); Sample rate → self._sample_rate_label = _MutedLabel("—", wrapper) (plain, D-05); Bit depth → self._bit_depth_label = _MutedLabel("—", wrapper) (plain, D-05). Row labels are _MutedLabel("Encoding"/"Bitrate"/"Sample rate"/"Bit depth", wrapper). Leave the trailing wrapper.setVisible(False) as the only setVisible in the method.
    In bind_station, add a reset (Pitfall 7) setting all four labels to "—" and clearing mismatch on the two _StatLabel rows: self._encoding_label.set_mismatch(False); self._bitrate_label.set_mismatch(False). Place it alongside the existing name/icy resets.
    update_detected_format / update_detected_caps are added in Task 2 — Task 1 only creates the labels and the reset.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_now_playing_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/python -m pytest tests/test_now_playing_panel.py -x -q still passes (existing panel suite unaffected)
    - grep -c "class _StatLabel" musicstreamer/ui_qt/now_playing_panel.py returns 1
    - grep -c "self._encoding_label\|self._bitrate_label\|self._sample_rate_label\|self._bit_depth_label" finds all four labels created in _build_stats_widget
    - In _build_stats_widget exactly one setVisible call (wrapper.setVisible(False)) — no per-row setVisible (Pitfall 8)
    - _sample_rate_label and _bit_depth_label are _MutedLabel (not _StatLabel)
  </acceptance_criteria>
  <done>_StatLabel subclass with theme-aware amber added; four detected-format rows wired in D-04 order with no per-row visibility; bind_station resets all four to em-dash and clears mismatch; existing panel suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: update_detected_format + update_detected_caps panel methods + Wave 0 stats test file</name>
  <files>musicstreamer/ui_qt/now_playing_panel.py, tests/test_now_playing_stats.py</files>
  <read_first>
    - musicstreamer/ui_qt/now_playing_panel.py — set_underrun_count / set_buffer_duration (~1171-1203), self._streams + _populate_stream_picker (~333, ~1589-1592). 98-PATTERNS.md "New public methods to add" (update_detected_format / update_detected_caps).
    - tests/test_now_playing_panel.py — FakeRepo (~39), FakePlayer, NowPlayingPanel construction (~152-155), _RepoWithStreams list_streams fixed-list subclass (~3129-3140), StationStream import.
    - tests/test_hi_res.py — truth-table style for the bitrate-tolerance/mismatch cases.
    - 98-RESEARCH.md §Pattern 6 (update_detected_format), §Pattern 7 (update_detected_caps), §Finding 5 (5 kbps tolerance), D-01/D-02/D-05/D-07.
  </read_first>
  <behavior>
    - test_encoding_row_shows_detected_and_expected (D-01): declared codec "AAC", detected "AAC" → _encoding_label text contains both detected and "(exp: AAC)"
    - test_encoding_mismatch_sets_amber (D-02): declared "AAC", detected "MP3" → _encoding_label._mismatch is True
    - test_no_mismatch_flag_when_codec_matches (D-02): declared "MP3", detected "MP3" → _encoding_label._mismatch is False
    - test_bitrate_mismatch_tolerance (D-02/Finding 5): declared 320, detected 318 (Δ2 ≤5) → _bitrate_label._mismatch False; declared 320, detected 128 (Δ192 >5) → True
    - test_em_dash_when_codec_unknown (D-07): detected codec "" → _encoding_label text starts with "—"; when declared known, suffix "(exp: ...)" still appended; _mismatch False
    - test_no_mismatch_when_both_unknown (D-07): detected "" and declared "" → text exactly "—", _mismatch False
    - update_detected_caps: rate_hz 48000 → "48 kHz"; bit_depth 24 → "24-bit"; rate_hz 0 → "—"; bit_depth 0 → "—"
  </behavior>
  <action>
    Add update_detected_format(self, stream_id: int, detected_codec: str, detected_bitrate_kbps: int) -> None per 98-PATTERNS "New public methods": look up declared = next((s for s in self._streams if s.id == stream_id), None); declared_codec = (declared.codec or "") if declared else ""; declared_kbps = int(declared.bitrate_kbps or 0) if declared else 0 (D-03 — declared sourced from self._streams, NOT the signal). Encoding row: detected text = detected_codec or "—"; when declared_codec append "  (exp: {declared_codec})"; mismatch_enc = bool(declared_codec) and detected present and detected_codec.upper() != declared_codec.upper() (False when detected unknown — D-07). Bitrate row with _BITRATE_TOLERANCE_KBPS = 5: detected text "{detected_bitrate_kbps} kbps" when >0 else "—"; when declared_kbps>0 append "  (exp: {declared_kbps} kbps)"; mismatch_brate = bool(declared_kbps) and detected>0 and abs(detected - declared_kbps) > 5 (False when detected unknown — D-07). Call setText + set_mismatch on _encoding_label and _bitrate_label. NO repo.update_stream / DB write (RESEARCH Finding 6 — detected values are transient).
    Add update_detected_caps(self, stream_id: int, rate_hz: int, bit_depth: int) -> None (D-05 detected-only): _sample_rate_label.setText(f"{rate_hz / 1000:g} kHz" if rate_hz > 0 else "—"); _bit_depth_label.setText(f"{bit_depth}-bit" if bit_depth > 0 else "—"). stream_id accepted but unused (rows are panel-wide).
    Create tests/test_now_playing_stats.py: module-local _make_panel(qtbot, streams=None) that builds NowPlayingPanel(FakePlayer(), FakeRepo({"volume": "80"})), qtbot.addWidget, and injects panel._streams = streams when provided (use StationStream objects with id/codec/bitrate_kbps). Implement all eight behavior tests plus the two source-grep tests from Task 1's behavior (test_sample_rate_label_is_muted_not_stat, test_no_per_row_visible_in_build_stats) so the full VALIDATION.md panel row set lives in this one file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_now_playing_stats.py tests/test_now_playing_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/python -m pytest tests/test_now_playing_stats.py::test_encoding_row_shows_detected_and_expected tests/test_now_playing_stats.py::test_encoding_mismatch_sets_amber tests/test_now_playing_stats.py::test_no_mismatch_flag_when_codec_matches tests/test_now_playing_stats.py::test_bitrate_mismatch_tolerance tests/test_now_playing_stats.py::test_sample_rate_label_is_muted_not_stat tests/test_now_playing_stats.py::test_em_dash_when_codec_unknown tests/test_now_playing_stats.py::test_no_mismatch_when_both_unknown tests/test_now_playing_stats.py::test_no_per_row_visible_in_build_stats -x -q all pass
    - .venv/bin/python -m pytest tests/test_now_playing_panel.py -x -q still passes
    - grep -c "def update_detected_format\|def update_detected_caps" musicstreamer/ui_qt/now_playing_panel.py returns 2
    - No repo.update_stream call inside update_detected_format (detected values not persisted — Finding 6)
  </acceptance_criteria>
  <done>update_detected_format renders detected+expected with amber mismatch and 5 kbps tolerance, em-dash on unknown, no DB write; update_detected_caps renders detected-only sample-rate/bit-depth; all VALIDATION.md panel tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detected codec/bitrate signal → panel label text | values delivered from the bus-loop thread (via Plan 03) originate from a remote stream's tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Tampering/Injection | detected_codec string rendered into QLabel via setText | mitigate | detected_codec is already a closed-vocabulary value from _normalise_audio_codec (Plan 01); panel renders via QLabel.setText (plain text, not rich text / not a format string) so no markup or format-string injection is possible |
| T-98-05 | Spoofing (misleading display) | amber mismatch flag illegible on a theme | mitigate | _StatLabel picks _AMBER_LIGHT/_AMBER_DARK by QPalette.Window lightness and re-applies via inherited changeEvent on theme flip; manual UAT confirms WCAG legibility on both themes (VALIDATION Manual-Only) |
| T-98-06 | Information disclosure | declared/detected values shown only in a hidden-by-default local panel | accept | Panel is gated behind the existing Stats-for-Nerds toggle (Phase 47.1); no PII; local UI only |
| T-98-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase |
</threat_model>

<verification>
- .venv/bin/python -m pytest tests/test_now_playing_stats.py tests/test_now_playing_panel.py -x -q green
- QLabel.setText used for all detected values (plain text — no rich-text/format-string surface)
- No per-row setVisible in _build_stats_widget (Pitfall 8); rows inherit set_stats_visible
- update_detected_format performs no DB write (Finding 6 — declared values remain source of truth, D-03)
</verification>

<success_criteria>
- Four detected-format rows present (D-04); Encoding/Bitrate show detected+expected (D-01) with amber mismatch (D-02) and 5 kbps tolerance
- Sample rate / Bit depth are plain detected-only _MutedLabel rows (D-05)
- Unknown detected → em-dash, rows always present, no flag when both unknown (D-07)
- _StatLabel amber survives light/dark theme flips
- bind_station resets all four rows on station switch (Pitfall 7)
- All VALIDATION.md panel-row tests pass
</success_criteria>

<output>
Create `.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-02-SUMMARY.md` when done
</output>
