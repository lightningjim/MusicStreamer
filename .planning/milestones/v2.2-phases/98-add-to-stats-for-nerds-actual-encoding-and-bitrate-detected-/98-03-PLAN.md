---
phase: 98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected
plan: 03
type: execute
wave: 2
depends_on: ["98-01", "98-02"]
files_modified:
  - musicstreamer/ui_qt/main_window.py
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "Player.audio_format_detected is connected to a MainWindow slot with QueuedConnection (qt-glib-bus-threading.md Rule 2)"
    - "_on_audio_format_detected delivers detected codec/bitrate to the panel via update_detected_format and never raises"
    - "_on_audio_format_detected does NOT persist detected codec/bitrate to the DB (declared values remain source of truth, D-03)"
    - "The existing _on_audio_caps_detected slot also fans detected sample-rate/bit-depth out to the panel via update_detected_caps (D-04 completes the format block)"
  artifacts:
    - path: "musicstreamer/ui_qt/main_window.py"
      provides: "audio_format_detected QueuedConnection, _on_audio_format_detected slot, update_detected_caps fan-out in _on_audio_caps_detected"
      contains: "_on_audio_format_detected"
  key_links:
    - from: "Player.audio_format_detected (bus-loop thread)"
      to: "MainWindow._on_audio_format_detected (main thread)"
      via: "QueuedConnection"
      pattern: "audio_format_detected\\.connect"
    - from: "MainWindow._on_audio_format_detected"
      to: "now_playing.update_detected_format"
      via: "hasattr-guarded panel call"
      pattern: "update_detected_format"
    - from: "MainWindow._on_audio_caps_detected"
      to: "now_playing.update_detected_caps"
      via: "hasattr-guarded fan-out after _refresh_quality_badge"
      pattern: "update_detected_caps"
---

<objective>
Wire the producer (Plan 01) to the consumer (Plan 02): connect `Player.audio_format_detected`
to a new `MainWindow._on_audio_format_detected` slot with QueuedConnection, deliver detected
codec/bitrate to the panel, and extend the existing `_on_audio_caps_detected` slot to also
fan detected sample-rate/bit-depth out to the panel's new detected-only rows.

Purpose: This is the integration seam that makes the four detected-format rows actually populate
during playback. Without it the signal fires into the void and the sample-rate/bit-depth rows
never fill. Honors D-03 (no DB write for detected codec/bitrate) and completes D-04.
Output: One QueuedConnection, one new never-raise slot, one fan-out line in the existing caps slot.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-RESEARCH.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-PATTERNS.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-01-SUMMARY.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + delivered by Plans 01/02 — use directly. -->

From Plan 01 (musicstreamer/player.py): audio_format_detected = Signal(int, str, int)  # stream_id, codec_norm, bitrate_kbps
From Plan 02 (musicstreamer/ui_qt/now_playing_panel.py):
- update_detected_format(self, stream_id: int, detected_codec: str, detected_bitrate_kbps: int) -> None
- update_detected_caps(self, stream_id: int, rate_hz: int, bit_depth: int) -> None

Existing musicstreamer/ui_qt/main_window.py (verified line numbers):
- caps wiring (~540-545): self._last_quality_payload: dict[int, tuple[int, int]] = {} then self._player.audio_caps_detected.connect(self._on_audio_caps_detected, Qt.ConnectionType.QueuedConnection)
- _on_audio_caps_detected (~701-784): idempotency cache → DB write → cache update → (~773-774) if hasattr(self.now_playing, "_refresh_quality_badge"): self.now_playing._refresh_quality_badge() → except Exception: _log.exception(...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect audio_format_detected + add _on_audio_format_detected slot + fan caps out to update_detected_caps</name>
  <files>musicstreamer/ui_qt/main_window.py</files>
  <read_first>
    - musicstreamer/ui_qt/main_window.py — wiring block (~540-545), _on_audio_caps_detected (~701-784, especially the _refresh_quality_badge fan-out at ~773-774 and the except at ~784). 98-PATTERNS.md "main_window.py — new slot _on_audio_format_detected + connect" (connection wiring, slot pattern, caps-path fan-out, never-raise slot).
    - 98-RESEARCH.md §Pattern 8 (connect with QueuedConnection), §Finding 6 (no DB persist), Open Question 2.
    - 98-01-SUMMARY.md / 98-02-SUMMARY.md — confirm signal arity and panel method names landed as specified.
  </read_first>
  <action>
    After the existing audio_caps_detected.connect(...) block (~544-545), add the new connection: self._player.audio_format_detected.connect(self._on_audio_format_detected, Qt.ConnectionType.QueuedConnection) with a Phase 98 comment (QueuedConnection is mandatory — bus-loop thread → main thread, qt-glib-bus-threading.md Rule 2).
    Add the slot _on_audio_format_detected(self, stream_id: int, codec_norm: str, bitrate_kbps: int) -> None alongside _on_audio_caps_detected. Body: try → if hasattr(self.now_playing, "update_detected_format"): self.now_playing.update_detected_format(stream_id, codec_norm, bitrate_kbps) → except Exception: _log.exception("_on_audio_format_detected: unhandled exception (stream_id=%r)", stream_id). Do NOT add any repo.update_stream / DB write (RESEARCH Finding 6 — detected codec/bitrate are transient; the declared Stream.codec/bitrate_kbps remain the persistent source of truth, D-03).
    In _on_audio_caps_detected, directly after the existing _refresh_quality_badge fan-out (~773-774, still inside the try), add a second hasattr-guarded fan-out: if hasattr(self.now_playing, "update_detected_caps"): self.now_playing.update_detected_caps(stream_id, rate_hz, bit_depth) (RESEARCH §Pattern 7 Option 1 / Open Question 2 — completes D-04 sample-rate/bit-depth rows). Leave the existing DB-write-first invariant and idempotency cache untouched.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player_codec_tag.py tests/test_now_playing_stats.py tests/test_fake_player_signal_parity.py tests/test_now_playing_panel.py tests/test_player_tag.py tests/test_player_caps.py tests/test_main_window_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "audio_format_detected.connect" musicstreamer/ui_qt/main_window.py returns 1
    - The audio_format_detected.connect call includes Qt.ConnectionType.QueuedConnection on the same statement
    - grep -c "def _on_audio_format_detected" musicstreamer/ui_qt/main_window.py returns 1
    - grep -c "update_detected_caps" musicstreamer/ui_qt/main_window.py returns 1 (fan-out in _on_audio_caps_detected)
    - No repo.update_stream call added inside _on_audio_format_detected (Finding 6)
    - .venv/bin/python -m pytest tests/test_main_window_integration.py -x -q passes (MainWindow construction/wiring unaffected)
  </acceptance_criteria>
  <done>audio_format_detected is connected via QueuedConnection to a never-raise slot that delivers to update_detected_format with no DB write; _on_audio_caps_detected also fans detected caps to update_detected_caps; the scoped phase suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player.audio_format_detected (bus-loop thread) → MainWindow slot (main thread) | cross-thread signal delivery of detected values originating from a remote stream's tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07 | Tampering (thread-safety) | audio_format_detected delivered to a Qt widget | mitigate | Connection uses Qt.ConnectionType.QueuedConnection so the slot runs on the Qt main thread; no Qt widget is touched from the bus-loop thread (qt-glib-bus-threading.md Rule 2) |
| T-98-08 | Denial of Service | exception in slot during a tag burst | mitigate | Slot is wrapped in try/except _log.exception (never-raise pattern), so a malformed payload cannot crash the UI thread |
| T-98-09 | Tampering (data integrity) | detected codec/bitrate overwriting declared values | mitigate | Slot performs no repo.update_stream; detected values stay transient (Finding 6), preserving the declared Stream.codec/bitrate_kbps source of truth (D-03) |
| T-98-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase |
</threat_model>

<verification>
- Scoped phase suite green: test_player_codec_tag, test_now_playing_stats, test_fake_player_signal_parity, test_now_playing_panel, test_player_tag, test_player_caps
- audio_format_detected wired with QueuedConnection; slot never raises
- No DB write for detected codec/bitrate (Finding 6 / D-03)
- update_detected_caps fan-out present in _on_audio_caps_detected (D-04 completion)
</verification>

<success_criteria>
- audio_format_detected connected to _on_audio_format_detected via QueuedConnection
- Slot delivers to update_detected_format and never raises; no DB persistence
- _on_audio_caps_detected fans detected caps to update_detected_caps
- Scoped phase suite + test_main_window_integration green
</success_criteria>

<output>
Create `.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-03-SUMMARY.md` when done
</output>
