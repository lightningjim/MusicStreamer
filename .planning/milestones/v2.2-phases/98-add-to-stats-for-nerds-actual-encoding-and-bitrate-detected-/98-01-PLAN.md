---
phase: 98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/player.py
  - tests/_fake_player.py
  - tests/test_player_codec_tag.py
autonomous: true
requirements: [D-03, D-06]
must_haves:
  truths:
    - "D-06: detected codec/bitrate are captured one-shot at preroll and emitted exactly once per stream"
    - "Player exposes audio_format_detected = Signal(int, str, int) and FakePlayer mirrors it with identical arity"
    - "Raw GStreamer TAG_AUDIO_CODEC strings are normalised to the Stream.codec vocabulary (MP3/AAC/FLAC/OPUS/OGG/'')"
    - "Bitrate tags (bps) are converted to kbps with TAG_NOMINAL_BITRATE preferred over TAG_BITRATE"
    - "SomaFM preroll tags are suppressed (no codec emission while _preroll_in_flight)"
  artifacts:
    - path: "musicstreamer/player.py"
      provides: "audio_format_detected signal, _normalise_audio_codec, one-shot codec/bitrate tag block in _on_gst_tag, _codec_tag_armed_for_stream_id guard + arming"
      contains: "audio_format_detected"
    - path: "tests/_fake_player.py"
      provides: "audio_format_detected Signal parity"
      contains: "audio_format_detected"
    - path: "tests/test_player_codec_tag.py"
      provides: "Wave 0 unit tests for codec/bitrate detection seams"
      exports: ["test_normalise_audio_codec", "test_bitrate_bps_to_kbps_conversion", "test_codec_tag_emits_on_first_tag", "test_codec_tag_one_shot_disarm", "test_codec_tag_suppressed_during_preroll"]
  key_links:
    - from: "musicstreamer/player.py _on_gst_tag (bus-loop thread)"
      to: "audio_format_detected.emit"
      via: "one-shot guard + normalise + bps→kbps"
      pattern: "audio_format_detected\\.emit"
    - from: "tests/_fake_player.py"
      to: "tests/test_fake_player_signal_parity.py"
      via: "Signal arity parity"
      pattern: "audio_format_detected = Signal\\(int, str, int\\)"
---

<objective>
Add the GStreamer-side detection of actual audio codec and bitrate to `Player`, emitted as a
new queued `audio_format_detected = Signal(int, str, int)` signal, plus its FakePlayer parity
mirror and a new unit-test file covering the detection seams.

Purpose: This is the producer half of Phase 98 — without a one-shot codec/bitrate snapshot from
the live pipeline there is nothing for the panel to display. Implements D-06 (one-shot at preroll)
and feeds D-03 (declared-vs-detected comparison happens later in the panel).
Output: New signal + normalisation function + one-shot tag block in `_on_gst_tag` + arming guard;
FakePlayer mirror; `tests/test_player_codec_tag.py`.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-RESEARCH.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-PATTERNS.md
@.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration. -->

Existing analog in musicstreamer/player.py:
- Signal block (~line 359-362): audio_caps_detected = Signal(int, int, int)  # stream_id, rate_hz, bit_depth
- __init__ guard (~line 552): self._caps_armed_for_stream_id: int = 0  # 0 = disarmed
- _on_gst_tag handler (~lines 1134-1152): currently does taglist.get_string(Gst.TAG_TITLE) → _cancel_timers_requested.emit() → if not found: return → if self._preroll_in_flight: return → title_changed.emit(...)
- _on_caps_negotiated disarm-before-emit (~lines 1285-1289): sid = self._caps_armed_for_stream_id; self._caps_armed_for_stream_id = 0; self.audio_caps_detected.emit(sid, rate, depth)
- _set_uri arming site (~line 1622): self._arm_caps_watch_for_current_stream()
- _on_playbin_state_changed Pattern 1b arming (~line 1335): self._arm_caps_watch_for_current_stream()

Verified GStreamer API (98-RESEARCH §Finding 2):
- taglist.get_string(Gst.TAG_AUDIO_CODEC) → (found: bool, value: str | None)
- taglist.get_uint(Gst.TAG_NOMINAL_BITRATE) → (found: bool, value: int)  # bps
- taglist.get_uint(Gst.TAG_BITRATE) → (found: bool, value: int)  # bps
- Gst.TAG_AUDIO_CODEC == "audio-codec"; Gst.TAG_NOMINAL_BITRATE == "nominal-bitrate"; Gst.TAG_BITRATE == "bitrate"

tests/_fake_player.py Signal block (~lines 59-90), last caps line:
- audio_caps_detected = Signal(int, int, int)  # stream_id, rate_hz, bit_depth
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audio_format_detected signal + FakePlayer parity + _normalise_audio_codec pure function</name>
  <files>musicstreamer/player.py, tests/_fake_player.py</files>
  <read_first>
    - musicstreamer/player.py — Signal block (~359-362), __init__ guards (~552), module-level helper functions area (top of file). 98-PATTERNS.md "Signal declaration pattern" and "Pure normalisation function" sections.
    - tests/_fake_player.py — Signal block (~59-90). 98-PATTERNS.md "tests/_fake_player.py" section.
    - tests/test_fake_player_signal_parity.py — to understand the drift-guard (grep-based parity check).
    - tests/test_hi_res.py — truth-table test style analog for _normalise_audio_codec.
  </read_first>
  <behavior>
    - test_normalise_audio_codec: "MPEG-1 Layer 3 (MP3)"→"MP3", "MPEG-2 Layer 3 (MP3)"→"MP3", "MPEG-1 Layer 2 (MP2)"→"MP3", "MPEG-4 AAC"→"AAC", "MPEG-2 AAC"→"AAC", "Free Lossless Audio Codec (FLAC)"→"FLAC", "Opus"→"OPUS", "Vorbis"→"OGG", None→"", ""→"", "weird-unknown"→""
    - test_fake_player_signal_parity (existing test): passes — FakePlayer now declares audio_format_detected with arity (int, str, int)
  </behavior>
  <action>
    In musicstreamer/player.py add audio_format_detected = Signal(int, str, int) with an inline comment (stream_id, codec_norm, bitrate_kbps; Phase 98) immediately after the existing audio_caps_detected Signal declaration.
    Add a module-level pure function _normalise_audio_codec(raw: str | None) -> str (NO Qt/GStreamer imports inside it) implementing the case-insensitive substring mapping from 98-PATTERNS.md "Pure normalisation function" (per D-03 vocabulary): empty/None→"", "layer 3"/"layer3"→"MP3", "layer 2"/"mp2"→"MP3", "aac"→"AAC", "flac"→"FLAC", exact "opus"→"OPUS", "vorbis"→"OGG", else "". Order the checks exactly as in PATTERNS so "MPEG-4 AAC" matches AAC and MP2 maps to MP3 family (per assumption A5).
    In tests/_fake_player.py add audio_format_detected = Signal(int, str, int) with a Phase 98 parity comment immediately after the existing audio_caps_detected line — this MUST land in this same task so test_fake_player_signal_parity stays green (Pitfall 3 / Finding 7).
    Do NOT yet touch _on_gst_tag or the __init__ guard — that is Task 2.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_fake_player_signal_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "audio_format_detected = Signal(int, str, int)" musicstreamer/player.py returns 1
    - grep -c "audio_format_detected = Signal(int, str, int)" tests/_fake_player.py returns 1
    - .venv/bin/python -m pytest tests/test_fake_player_signal_parity.py -x -q passes
    - _normalise_audio_codec exists at module level in player.py with no Qt/Gst imports inside it
  </acceptance_criteria>
  <done>audio_format_detected signal present in player.py and _fake_player.py with identical arity; _normalise_audio_codec implemented; signal-parity drift-guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One-shot codec/bitrate block in _on_gst_tag + guard arming + Wave 0 test file</name>
  <files>musicstreamer/player.py, tests/test_player_codec_tag.py</files>
  <read_first>
    - musicstreamer/player.py — _on_gst_tag (~1134-1152), _on_caps_negotiated disarm-before-emit (~1285-1289), _set_uri arming (~1622), _on_playbin_state_changed Pattern 1b (~1335), __init__ guards (~552). 98-PATTERNS.md "_on_gst_tag handler" + "WARNING" refactor + "One-shot disarm-before-emit" + "Guard arm" sections, and the "Critical Sequencing Note".
    - tests/test_player_caps.py (full) — make_player fixture, one-shot guard / disarm / assertNotEmitted patterns.
    - tests/test_player_tag.py (full) — _fake_tag_msg mock-taglist pattern; existing test_on_gst_tag_ignores_missing_title must still pass after the refactor.
    - 98-RESEARCH.md §Pattern 1, §Pattern 2, §Finding 3, §Finding 4, Pitfall 1/2/4/5.
  </read_first>
  <behavior>
    - test_codec_tag_emits_on_first_tag: arm _codec_tag_armed_for_stream_id=42, deliver a tag msg with codec "MPEG-4 AAC" and nominal-bitrate 128000 → emits audio_format_detected(42, "AAC", 128)
    - test_bitrate_bps_to_kbps_conversion: nominal-bitrate 320000 (and bitrate 319000 present) → emits bitrate_kbps 320 (nominal preferred); when only TAG_BITRATE 128000 present → 128
    - test_codec_tag_one_shot_disarm: after first emit _codec_tag_armed_for_stream_id == 0; a second _on_gst_tag call for the same stream does NOT emit (exactly 1 emission)
    - test_codec_tag_suppressed_during_preroll: _preroll_in_flight=True + armed guard → audio_format_detected NOT emitted
    - existing test_on_gst_tag_ignores_missing_title (in test_player_tag.py) still passes
  </behavior>
  <action>
    Add self._codec_tag_armed_for_stream_id: int = 0 (Phase 98 one-shot tag guard; 0 = disarmed) in __init__ after the existing _caps_armed_for_stream_id.
    Refactor _on_gst_tag per 98-PATTERNS "Critical Sequencing Note": rename the title result to found_title; keep _cancel_timers_requested.emit(); MOVE the if self._preroll_in_flight: return guard to BEFORE the title early-return so it covers both paths; insert the NEW codec/bitrate block AFTER the preroll guard; MOVE if not found_title: return to AFTER the new block (so the existing title path stays intact).
    New block (guarded by if self._codec_tag_armed_for_stream_id:): read taglist.get_string(Gst.TAG_AUDIO_CODEC), taglist.get_uint(Gst.TAG_NOMINAL_BITRATE), taglist.get_uint(Gst.TAG_BITRATE); compute bitrate_kbps preferring nominal (nb_bps // 1000) then falling back to bitrate (b_bps // 1000), 0 if neither (Pitfall 4: always // 1000); codec_norm = _normalise_audio_codec(raw_codec if found_codec else None). Only when codec_norm or bitrate_kbps: capture sid = self._codec_tag_armed_for_stream_id, set self._codec_tag_armed_for_stream_id = 0 (disarm BEFORE emit — Pitfall 6), then self.audio_format_detected.emit(sid, codec_norm, bitrate_kbps).
    Arm the guard at the same two sites as the caps guard, NOT inside _arm_caps_watch_for_current_stream (Open Question 1 / D-06): in _set_uri directly after the _arm_caps_watch_for_current_stream() call set self._codec_tag_armed_for_stream_id = self._current_stream.id if self._current_stream else 0; in _on_playbin_state_changed Pattern 1b directly after its _arm_caps_watch_for_current_stream() call set the guard to self._current_stream.id when self._current_stream (Pitfall 5).
    Create tests/test_player_codec_tag.py with a module-local make_player fixture (copy from test_player_caps.py — do NOT import) and the _fake_codec_tag_msg helper that routes get_string/get_uint by argument (98-PATTERNS "_fake_codec_tag_msg" arg-routing variant). Implement the five behavior tests above plus test_normalise_audio_codec (truth-table). Bitrate tolerance and panel rendering tests belong to Plan 02, not here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player_codec_tag.py tests/test_player_tag.py tests/test_player_caps.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/python -m pytest tests/test_player_codec_tag.py::test_normalise_audio_codec tests/test_player_codec_tag.py::test_bitrate_bps_to_kbps_conversion tests/test_player_codec_tag.py::test_codec_tag_emits_on_first_tag tests/test_player_codec_tag.py::test_codec_tag_one_shot_disarm tests/test_player_codec_tag.py::test_codec_tag_suppressed_during_preroll -x -q all pass
    - .venv/bin/python -m pytest tests/test_player_tag.py -x -q still passes (refactor preserved existing title path)
    - grep -c "audio_format_detected.emit" musicstreamer/player.py returns 1
    - In _on_gst_tag the if self._preroll_in_flight: return line precedes the codec block, which precedes if not found_title
  </acceptance_criteria>
  <done>One-shot codec/bitrate detection emits audio_format_detected exactly once per stream, prefers nominal bitrate in kbps, is suppressed during preroll, and arms at both caps-guard sites; all five new tests + the existing tag/caps suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote stream → GStreamer bus → Player | TAG_AUDIO_CODEC / bitrate tag strings originate from a remote audio stream and arrive on the bus-loop thread |
| bus-loop thread → Qt main thread | detected values cross threads via queued signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering/Injection | _normalise_audio_codec consuming remote TAG_AUDIO_CODEC string | mitigate | Function maps to a closed vocabulary (MP3/AAC/FLAC/OPUS/OGG/""); any unrecognised/hostile string returns "" — no arbitrary remote string propagates to the emitted signal or downstream label |
| T-98-02 | Denial of Service | repeated tag messages on the bus | mitigate | One-shot guard (_codec_tag_armed_for_stream_id) disarms before emit, so a tag flood yields at most one emission per stream |
| T-98-03 | Information disclosure | detected codec/bitrate | accept | Read-only diagnostic metadata about the user's own playing stream; no PII, no persistence (RESEARCH Finding 6) |
| T-98-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- .venv/bin/python -m pytest tests/test_player_codec_tag.py tests/test_fake_player_signal_parity.py tests/test_player_tag.py tests/test_player_caps.py -x -q green
- Bus-loop thread handler emits only a queued signal — no Qt widget access in _on_gst_tag (qt-glib-bus-threading.md Rule 2)
- No repo.update_stream / DB write added for detected codec/bitrate (RESEARCH Finding 6 — values stay transient)
</verification>

<success_criteria>
- audio_format_detected = Signal(int, str, int) exists in player.py and _fake_player.py (parity green)
- _normalise_audio_codec maps the full verified truth-table
- _on_gst_tag emits exactly once per stream, nominal-bitrate preferred, kbps units, preroll-suppressed
- Guard armed at _set_uri and _on_playbin_state_changed Pattern 1b (not inside the caps helper)
- All Wave 0 codec-tag tests pass; existing tag/caps suites unaffected
</success_criteria>

<output>
Create `.planning/phases/98-add-to-stats-for-nerds-actual-encoding-and-bitrate-detected-/98-01-SUMMARY.md` when done
</output>
