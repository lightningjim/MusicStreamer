# Phase 88.1: Fix SMTC media overlay absent and dead media keys on bundled Windows build - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-09
**Phase:** 88.1-fix-smtc-media-overlay-absent-and-dead-media-keys-on-bundled
**Areas discussed:** Build-time regression guard, Diagnose-first vs fix-first, Phase done vs VM boundary, Sequencing

---

## Area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Silent-degradation hardening | Make the win32 NoOp fallback loud where SMTC is expected | (folded into guard/logging decisions) |
| Build-time regression guard | Prove SMTC loads in the bundle | ✓ |
| Diagnose-first vs fix-first | Confirm root cause before/with the fix | ✓ |
| Phase done vs VM boundary | What closes 88.1 given VM is held | ✓ |

---

## Build-time regression guard

| Option | Description | Selected |
|--------|-------------|----------|
| Both: static spec test + smoke assertion | Linux pytest asserts spec collects winrt + build.ps1/--smoke asserts real backend constructs | ✓ |
| Runtime smoke assertion only | Only fail the Windows build if backend doesn't construct | |
| Static spec test only | Only assert spec collects winrt in CI | |

**User's choice:** Both layers.
**Notes:** Fits the existing exit-code-guard house style (exit 3/8/9/10) and the
`test_packaging_linux_spec.py` precedent.

---

## Diagnose-first vs fix-first

| Option | Description | Selected |
|--------|-------------|----------|
| Fix-first + add diagnostics | Apply collect_all('winrt') fix now, smoke step confirms | |
| Diagnose-first, then fix | Land logging, diagnostic Windows build confirms failure, then fix | ✓ |

**User's choice:** Diagnose-first, then fix.
**Notes:** Refined by the Sequencing follow-up below — diagnose-first did not mean
withholding the fix; see Sequencing.

---

## Phase done vs VM boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Linux-side now, VM proof in 88-03 | Spec fix + logging + Linux tests close the phase; smoke guard + UAT-3/UAT-7 run in consolidated 88-03 VM session | ✓ |
| Require live VM pass to close | Keep 88.1 open until Windows build + VM confirm | |

**User's choice:** Linux-side now, VM proof deferred to consolidated 88-03 session (Option C).
**Notes:** Executor runs on Linux; cannot run the Windows build or frozen smoke test.

---

## Sequencing (follow-up to reconcile diagnose-first + close-Linux-now)

| Option | Description | Selected |
|--------|-------------|----------|
| Diagnostics + provisional fix together | Land logging AND the leading-hypothesis collect_all('winrt') + winrt-runtime fix + both guards now; diagnostic build validates | ✓ |
| Diagnostics only; fix gated on diagnosis | Land only instrumentation + scaffolding; defer the real fix until the diagnostic build | |

**User's choice:** Diagnostics + provisional fix together.
**Notes:** Diagnosis validates the fix rather than gating it. One phase, fix is
provisional-but-present.

---

## Claude's Discretion

- Exact `collect_all` shape and which winrt sub-distributions to enumerate (pywinrt 3.2.x layout).
- New `build.ps1` exit code number and smoke-assertion mechanism (extend `--smoke` vs new flag).
- Backend-selection log level/wording.
- Whether `runtime_hook.py` needs winrt env setup.

## Deferred Ideas

- G3 (GBS.FM login) — Phase 88.2.
- UI-level "media keys health" indicator — backlog.
- pywinrt runtime self-heal / fallback wheel install — v2.3+.

## Resolved during discussion (no decision needed)

- AUMID labeling (WIN-02 `[Icons]` directive) is already wired in `MusicStreamer.iss` +
  `__main__.py` (UAT-2 passed). G2 is purely the winrt-bundling/session-absent problem;
  no AUMID scope added.
