---
phase: 86-linux-flatpak-build
plan: 03
type: execute
wave: 2
depends_on: ["86-01", "86-02"]
files_modified:
  - tests/test_packaging_spec.py
autonomous: true
requirements: [PKG-LIN-FP-01, PKG-LIN-FP-03, PKG-LIN-FP-04, PKG-LIN-FP-05, PKG-LIN-FP-06, PKG-LIN-FP-08, PKG-LIN-FP-09, PKG-LIN-FP-10]
must_haves:
  truths:
    - "Drift-guards parse the manifest YAML as data (yaml.safe_load), not text grep (D-13)"
    - "An allow-list guard asserts every required finish-arg including the narrow :ro mount"
    - "A deny-list guard asserts ABSENCE of --filesystem=home and broad --socket=session-bus (security-critical)"
    - "Runtime version pins (6.8 / ffmpeg-full 24.08 / node20) are asserted"
    - "python3-modules.yaml existence + validity and FP-10 validators are covered (skip-if-not-installed)"
  artifacts:
    - path: "tests/test_packaging_spec.py"
      provides: "Extended packaging drift-guard suite with Flatpak allow/deny-list + runtime-pin + validator + first-launch tests"
      contains: "test_flatpak_finish_args_deny_list"
  key_links:
    - from: "tests/test_packaging_spec.py"
      to: "io.github.kcreasey.MusicStreamer.yaml"
      via: "yaml.safe_load(_FLATPAK_MANIFEST.read_text())"
      pattern: "yaml\\.safe_load"
    - from: "tests/test_packaging_spec.py"
      to: "musicstreamer.flatpak_first_launch"
      via: "import + monkeypatched detection test"
      pattern: "flatpak_first_launch"
---

<objective>
Extend tests/test_packaging_spec.py with the Flatpak drift-guard suite (D-13/D-15): a YAML-parsed allow-list, the security-critical deny-list, runtime version pins, python3-modules.yaml validity, the FP-10 validator subprocess tests, and the first-launch detection unit. These are the Nyquist automated gates for FP-01/03/04/05/06/08/09/10. Depends on Plan 01 (manifest + artifacts must exist to parse) and Plan 02 (flatpak_first_launch module).

Purpose: Lock the manifest's security posture as data-level assertions. The deny-list is the security-critical half — a forbidden permission hidden in a YAML comment would pass a text grep but fails `yaml.safe_load` parsing (feedback_drift_guard_presence_not_semantics).
Output: New Flatpak section in tests/test_packaging_spec.py, all green against the Plan 01 artifacts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-linux-flatpak-build/86-CONTEXT.md
@.planning/phases/86-linux-flatpak-build/86-RESEARCH.md
@.planning/phases/86-linux-flatpak-build/86-PATTERNS.md
@.planning/phases/86-linux-flatpak-build/86-VALIDATION.md

# Artifacts produced by Plan 01 (parse targets):
@io.github.kcreasey.MusicStreamer.yaml
@python3-modules.yaml

<interfaces>
<!-- Drift-guard fixture shape + new path constants. Copy verbatim from PATTERNS.md lines 211-268. -->

tests/test_packaging_linux_spec.py provides the analog idioms:
  - Path(__file__).resolve().parent.parent / ... constant pattern
  - @pytest.fixture(scope="module") file-read fixture
  - _strip_comments_sh(source) helper for shell negative-assertion gates
  - @pytest.mark.skipif(not shutil.which("...")) subprocess validator idiom

New path constants (PATTERNS.md lines 214-233):
  _FLATPAK_MANIFEST = repo_root / "io.github.kcreasey.MusicStreamer.yaml"
  _PYTHON3_MODULES  = repo_root / "python3-modules.yaml"
  _FLATPAK_DESKTOP  = repo_root / "tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop"
  _FLATPAK_METAINFO = repo_root / "tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml"

musicstreamer/flatpak_first_launch.py (from Plan 02):
  has_unsandboxed_data(), should_offer_import_wizard(), import_offered_flag_path(), write_offered_flag(), _HOST_DB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest allow-list / deny-list / runtime-pin / app-id drift-guards (YAML-parsed)</name>
  <files>tests/test_packaging_spec.py</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`tests/test_packaging_spec.py` (new Flatpak guards block, lines ~211-268) and §Drift-guard Fixture Shape (lines ~497-501) and §Critical Implementation Notes #2 (deny-list is security-critical, parse YAML not text)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Code Examples drift-guard skeleton (lines ~498-534) and Pitfall 10 (parse YAML, not text)
    - tests/test_packaging_linux_spec.py (read the fixture/constant idioms — lines 25-79 — once)
    - tests/test_packaging_spec.py (read the existing structure to append the Flatpak section without disturbing Windows/AppImage guards)
  </read_first>
  <action>
    Append a Flatpak section to tests/test_packaging_spec.py (extend, do not replace — D-13/D-15; the file covers Windows+AppImage+Flatpak). Add the path constants from the interfaces block and a `@pytest.fixture(scope="module") def manifest_data()` that asserts the manifest exists and returns `yaml.safe_load(_FLATPAK_MANIFEST.read_text())`.
    Implement these YAML-parsed (NOT text-grep) tests:
      - test_flatpak_manifest_id: manifest_data["id"] == "io.github.kcreasey.MusicStreamer" (FP-01)
      - test_flatpak_runtime_version_pins: runtime-version == "6.8", base-version == "6.8", add-extensions ffmpeg-full version == "24.08", and node20 present in sdk-extensions (FP-03)
      - test_flatpak_finish_args_allow_list: assert each of --share=network, --socket=pulseaudio, --socket=wayland, --socket=fallback-x11, --own-name=org.mpris.MediaPlayer2.MusicStreamer, --filesystem=~/.local/share/musicstreamer:ro, --env=QTWEBENGINE_DISABLE_SANDBOX=1 in the parsed finish-args list (FP-04 allow + FP-05 + D-01)
      - test_flatpak_finish_args_deny_list (SECURITY-CRITICAL, D-13): assert "--filesystem=home" not in args, "--filesystem=home:rw" not in args, "--socket=session-bus" not in args. Add an explanatory assertion message on each.
      - test_flatpak_qtwebengine_disable_sandbox: "--env=QTWEBENGINE_DISABLE_SANDBOX=1" in finish-args (FP-05)
      - test_flatpak_mpris2_own_name: "--own-name=org.mpris.MediaPlayer2.MusicStreamer" in finish-args (FP-08 static half)
      - test_flatpak_narrow_ro_mount: "--filesystem=~/.local/share/musicstreamer:ro" in finish-args AND no other --filesystem entry exists except that exact :ro path (FP-06 reconciliation of D-01/D-05 — the ONLY filesystem exception)
    The deny-list and narrow-mount tests MUST operate on the parsed list, so a permission placed inside a YAML comment is invisible to yaml.safe_load and correctly stays out of finish-args.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_packaging_spec.py -k "flatpak and (allow or deny or runtime or manifest_id or qtwebengine or mpris2 or ro_mount)" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_packaging_spec.py::test_flatpak_finish_args_deny_list exits 0`
    - `pytest tests/test_packaging_spec.py::test_flatpak_finish_args_allow_list exits 0`
    - `pytest tests/test_packaging_spec.py::test_flatpak_runtime_version_pins exits 0`
    - All guards use `yaml.safe_load` (verified: `grep -c "yaml.safe_load" tests/test_packaging_spec.py` increased)
    - deny-list test would FAIL if `--filesystem=home` were added to finish-args (assertion present on parsed list, not text)
  </acceptance_criteria>
  <done>Allow-list, deny-list, runtime-pin, app-id, QtWebEngine, MPRIS2-name, and narrow-mount drift-guards added; all parse YAML as data and pass against the Plan 01 manifest.</done>
</task>

<task type="auto">
  <name>Task 2: python3-modules.yaml, FP-10 validator, and first-launch detection guards</name>
  <files>tests/test_packaging_spec.py</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Code Examples appstreamcli/desktop-file-validate pytest tests (lines ~558-583) and §Validation Architecture (python3-modules test, lines ~698)
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §python3-modules.yaml drift-guard (lines ~306-313) and §subprocess validator note (line ~270)
    - musicstreamer/flatpak_first_launch.py (from Plan 02 — the functions to unit-test)
  </read_first>
  <action>
    Add to the Flatpak section of tests/test_packaging_spec.py:
      - test_python3_modules_yaml_exists: assert _PYTHON3_MODULES.is_file(); yaml.safe_load is not None; assert "PySide6" not in the text (FP-09 + Pitfall 5 guard) (FP-09)
      - test_appstreamcli_validate_passes: `@pytest.mark.skipif(not shutil.which("appstreamcli"))`; subprocess.run(["appstreamcli","validate","--explain",str(_FLATPAK_METAINFO)]); assert returncode == 0 with stdout/stderr in the message (FP-10)
      - test_desktop_file_validate_passes: `@pytest.mark.skipif(not shutil.which("desktop-file-validate"))`; subprocess.run(["desktop-file-validate",str(_FLATPAK_DESKTOP)]); assert returncode == 0 (FP-10)
      - test_flatpak_desktop_no_playlist_mime: read the .desktop text; assert "x-mpegurl" not present and "x-scpls" not present (PKG-LIN-APP-09 preserved — mirrors the existing test_desktop_file_has_no_playlist_mime_entries model)
      - test_first_launch_detection: import musicstreamer.flatpak_first_launch; monkeypatch _HOST_DB to a tmp file (create it), monkeypatch paths.data_dir to a tmp dir; assert has_unsandboxed_data() True and should_offer_import_wizard() True; call write_offered_flag(); assert should_offer_import_wizard() now False (FP-06 detection + offer-once)
    Use the existing subprocess validator idiom already present in test_packaging_spec.py / test_packaging_linux_spec.py (skip-if-not-installed) so the suite stays green on hosts without the validators while still gating where they exist (D-15 dual-mode; the hard pre-flight gate lives in Plan 04 build.sh + CI).
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_packaging_spec.py -k "flatpak or first_launch or python3_modules or appstreamcli or desktop_file_validate" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_packaging_spec.py::test_python3_modules_yaml_exists exits 0`
    - `pytest tests/test_packaging_spec.py::test_first_launch_detection exits 0`
    - appstreamcli + desktop-file-validate tests pass on this host (both confirmed installed in RESEARCH.md Environment Availability) and are skip-guarded for hosts without them
    - test_flatpak_desktop_no_playlist_mime passes (no x-mpegurl / x-scpls in the Flatpak .desktop)
    - `uv run --with pytest pytest tests/test_packaging_spec.py tests/test_packaging_linux_spec.py -x` (full packaging subset) is green — no regression to Windows/AppImage guards
  </acceptance_criteria>
  <done>python3-modules validity, FP-10 validators (skip-guarded), desktop playlist-MIME guard, and first-launch detection unit all added and green; combined packaging suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest source → runtime sandbox policy | The drift-guard suite is the enforcement boundary that keeps the manifest's sandbox posture from silently regressing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-07 | Tampering | Silent manifest permission drift (e.g. someone adds --filesystem=home later) | mitigate | test_flatpak_finish_args_deny_list parses the manifest with yaml.safe_load and asserts forbidden permissions are absent from the parsed finish-args list — a permission hidden in a comment is invisible to the parser and a real added permission fails the test (D-13, feedback_drift_guard_presence_not_semantics). |
| T-86-08 | Tampering | PySide6 re-introduced into python3-modules.yaml (ABI conflict / supply surface) | mitigate | test_python3_modules_yaml_exists asserts "PySide6" absent (Pitfall 5). |
| T-86-09 | Repudiation | metainfo/.desktop ships invalid and breaks install trust signals | mitigate | FP-10 validator tests (appstreamcli + desktop-file-validate) gate the artifacts; Plan 04 adds the hard build-time pre-flight (D-15). |
</threat_model>

<verification>
- `uv run --with pytest pytest tests/test_packaging_spec.py tests/test_packaging_linux_spec.py -x` green.
- Deny-list and narrow-mount guards demonstrably operate on parsed YAML (not text).
- First-launch detection unit covers detect + offer-once.
</verification>

<success_criteria>
- FP-01/03/04/05/06(detection)/08(static)/09/10 each have a passing automated drift-guard.
- The combined packaging suite is the Nyquist sampling target for every later task commit.
</success_criteria>

<output>
Create `.planning/phases/86-linux-flatpak-build/86-03-SUMMARY.md` when done
</output>
