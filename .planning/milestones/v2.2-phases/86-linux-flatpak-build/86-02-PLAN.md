---
phase: 86-linux-flatpak-build
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/flatpak_first_launch.py
  - musicstreamer/ui_qt/flatpak_import_wizard.py
  - tests/test_flatpak_first_launch.py
autonomous: true
requirements: [PKG-LIN-FP-06]
must_haves:
  truths:
    - "First launch with existing ~/.local/share/musicstreamer/musicstreamer.sqlite3 detects unsandboxed data"
    - "The import wizard offers once; after dismiss a flag prevents re-prompting (offer-once-then-manual — D-03)"
    - "Import reuses the Phase 25 settings-export preview_import + commit_import path (no new import logic); data is copied into the sandbox data dir and the original ~/.local/share/musicstreamer/ is left untouched (D-02)"
    - "Detection uses a LITERAL host path, never paths.data_dir() (which remaps inside the sandbox)"
  artifacts:
    - path: "musicstreamer/flatpak_first_launch.py"
      provides: "Pure detection module (has_unsandboxed_data, should_offer_import_wizard, import_offered_flag_path)"
      min_lines: 30
      exports: ["has_unsandboxed_data", "should_offer_import_wizard", "import_offered_flag_path"]
    - path: "musicstreamer/ui_qt/flatpak_import_wizard.py"
      provides: "Qt wizard reusing SettingsImportDialog patterns + offer-once flag write"
    - path: "tests/test_flatpak_first_launch.py"
      provides: "Unit tests for detection + offer-once logic (monkeypatched host path)"
  key_links:
    - from: "musicstreamer/ui_qt/flatpak_import_wizard.py"
      to: "musicstreamer.settings_export"
      via: "preview_import + commit_import import"
      pattern: "from musicstreamer.settings_export import|settings_export\\.(preview_import|commit_import|build_zip)"
    - from: "musicstreamer/flatpak_first_launch.py"
      to: "musicstreamer.paths"
      via: "paths.data_dir() for the offer-once flag location only"
      pattern: "paths\\.data_dir"
---

<objective>
Implement the first-launch import-wizard detection and UI (FP-06): detect a pre-existing unsandboxed data dir via the narrow `:ro` host mount, offer the import once, and import via the existing Phase 25 settings-export ZIP path. This plan touches only `musicstreamer/` source + its unit test, so it runs in parallel with Plan 01 (no file overlap).

Purpose: Satisfy FP-06's "detects … and offers an in-app import wizard using the existing Phase 25 settings-export ZIP flow (no broad filesystem permission)" — D-01..D-04 locked the narrow `:ro` mount, copy-don't-delete, offer-once, and Phase 25 reuse.
Output: flatpak_first_launch.py (pure detection), flatpak_import_wizard.py (Qt wizard), and unit tests for the detection/offer-once logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-linux-flatpak-build/86-CONTEXT.md
@.planning/phases/86-linux-flatpak-build/86-RESEARCH.md
@.planning/phases/86-linux-flatpak-build/86-PATTERNS.md

<interfaces>
<!-- Verified codebase signatures (grep-confirmed). Use these directly. -->

musicstreamer/settings_export.py:
  def build_zip(repo: Repo, dest_path: str) -> None
  def preview_import(zip_path: str, repo: Repo) -> ImportPreview   # calls _validate_zip_members internally
  def commit_import(preview: ImportPreview, repo: Repo, mode: str) -> None   # re-validates _validate_zip_members (WR-02 TOCTOU)
  class ImportPreview
  def _validate_zip_members(zf: zipfile.ZipFile) -> None   # path-traversal guard — do NOT bypass

musicstreamer/paths.py:
  def data_dir() -> str   # inside sandbox returns ~/.var/app/io.github.kcreasey.MusicStreamer/data/musicstreamer (XDG_DATA_HOME override) — use ONLY for the offer-once flag, NOT for host detection
  def db_path() -> str

musicstreamer/repo.py:
  class Repo, def db_connect()   # (per PATTERNS.md import dialog analog)

musicstreamer/ui_qt/settings_import_dialog.py:
  class SettingsImportDialog   # layout + _ImportCommitWorker(QThread) threading model to copy

Literal host detection constant (RESEARCH.md Pitfall 7 / Code Examples lines 539-556):
  _HOST_DATA_DIR = os.path.expanduser("~/.local/share/musicstreamer")
  _HOST_DB = os.path.join(_HOST_DATA_DIR, "musicstreamer.sqlite3")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure first-launch detection module + offer-once flag</name>
  <files>musicstreamer/flatpak_first_launch.py, tests/test_flatpak_first_launch.py</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`musicstreamer/flatpak_first_launch.py` (pure-module pattern + detection skeleton, lines ~365-412) and §Pure-module No-Qt Pattern (lines ~503-507)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Common Pitfalls Pitfall 7 (XDG_DATA_HOME path confusion) and §Critical Implementation Notes #5
    - musicstreamer/paths.py (the pure-module style + data_dir() — read lines 1-50 once)
  </read_first>
  <behavior>
    - has_unsandboxed_data() returns True iff os.path.isfile(_HOST_DB) where _HOST_DB is the LITERAL ~/.local/share/musicstreamer/musicstreamer.sqlite3
    - import_offered_flag_path() returns a path inside paths.data_dir() ending in .flatpak-import-offered
    - should_offer_import_wizard() returns True iff has_unsandboxed_data() AND the offer flag file does NOT exist
    - should_offer_import_wizard() returns False when the offer flag exists (offer-once, D-03)
    - should_offer_import_wizard() returns False when no host DB exists
  </behavior>
  <action>
    Create musicstreamer/flatpak_first_launch.py as a PURE module (no Qt imports, no import-time side effects, no writes — mirrors paths.py design). Define module-level constants `_HOST_DATA_DIR = os.path.expanduser("~/.local/share/musicstreamer")` and `_HOST_DB = os.path.join(_HOST_DATA_DIR, "musicstreamer.sqlite3")` (LITERAL host path — Pitfall 7: never route host detection through paths.data_dir(), which remaps to the sandbox path). Implement has_unsandboxed_data(), import_offered_flag_path() (joins paths.data_dir() with ".flatpak-import-offered"), and should_offer_import_wizard(). Also add a small write_offered_flag() helper that creates the flag file (used by the wizard on dismiss/complete) — keep the write OUT of the pure detection functions.
    Write tests/test_flatpak_first_launch.py FIRST (RED): monkeypatch `flatpak_first_launch._HOST_DB` to a tmp_path file and monkeypatch paths.data_dir() to a tmp dir; assert the five behaviors above. Then implement to GREEN.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_flatpak_first_launch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_flatpak_first_launch.py -x exits 0`
    - `grep -c "from PySide6" musicstreamer/flatpak_first_launch.py` returns 0 (pure module, no Qt)
    - `grep -c "paths.data_dir\|paths.db_path" musicstreamer/flatpak_first_launch.py` — db host detection does NOT use paths.* (the only paths.data_dir() use is in import_offered_flag_path)
    - should_offer_import_wizard() honors the offer-once flag (test asserts False after flag written)
  </acceptance_criteria>
  <done>Pure detection module implemented with literal host path; offer-once flag logic tested; no Qt imports; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Flatpak import wizard (Qt) reusing Phase 25 settings-export path</name>
  <files>musicstreamer/ui_qt/flatpak_import_wizard.py</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`musicstreamer/ui_qt/flatpak_import_wizard.py` (imports, _ImportCommitWorker threading, signal connection, key additions — lines ~416-473)
    - musicstreamer/ui_qt/settings_import_dialog.py (the exact analog — read the dialog layout lines 82-197 and the _ImportCommitWorker class)
    - musicstreamer/settings_export.py lines 169-340 (build_zip / preview_import / commit_import signatures + _validate_zip_members at lines 73-100)
  </read_first>
  <action>
    Create musicstreamer/ui_qt/flatpak_import_wizard.py. Reuse the SettingsImportDialog layout (mode radio, summary label, detail tree, button box) and the _ImportCommitWorker(QThread) background-commit pattern (commit_done/commit_error signals, Qt.QueuedConnection). Unlike SettingsImportDialog, this wizard builds the ImportPreview itself: open a source Repo against the LITERAL host path (flatpak_first_launch._HOST_DATA_DIR), call settings_export.build_zip to a temp ZIP, then settings_export.preview_import(temp_zip, sandbox_repo) to produce the preview; commit via settings_export.commit_import(preview, sandbox_repo, mode) on a background worker. This reuses the Phase 25 path verbatim (D-04) — do NOT invent new import logic and do NOT call raw ZIP extraction (that would bypass _validate_zip_members, the path-traversal guard — PATTERNS.md §_validate_zip_members Security Re-validation).
    Import is copy-don't-delete (D-02): the host path is read-only (the `:ro` mount enforces this); nothing writes back to the host. On dismiss OR successful completion, call flatpak_first_launch.write_offered_flag() so the wizard is offer-once (D-03). Keep an "Import settings…" entry point invokable manually later (constructor usable independent of first-launch).
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/test_flatpak_first_launch.py -x && python3 -c "import ast; t=ast.parse(open('musicstreamer/ui_qt/flatpak_import_wizard.py').read()); src=open('musicstreamer/ui_qt/flatpak_import_wizard.py').read(); assert 'preview_import' in src and 'commit_import' in src, 'must reuse Phase 25 import path'; assert 'write_offered_flag' in src, 'must set offer-once flag'; print('wizard OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `musicstreamer/ui_qt/flatpak_import_wizard.py` parses (ast.parse succeeds)
    - source references settings_export.preview_import AND settings_export.commit_import (Phase 25 reuse, D-04)
    - source references flatpak_first_launch.write_offered_flag (offer-once on dismiss/complete, D-03)
    - source does NOT call zipfile extractall/extract directly (no _validate_zip_members bypass)
    - `grep -c "_ImportCommitWorker\|QThread" musicstreamer/ui_qt/flatpak_import_wizard.py` > 0 (background-worker pattern reused)
  </acceptance_criteria>
  <done>Wizard reuses Phase 25 preview_import/commit_import on a QThread worker, builds preview from the `:ro` host path, sets the offer-once flag on dismiss/complete, never bypasses the ZIP-member validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host `:ro` mount → sandbox import | Crafted/legacy ZIP and DB content from the host data dir crosses into the sandbox Repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Tampering | Import wizard ZIP path traversal | mitigate | Wizard routes ALL import through settings_export.preview_import + commit_import, which call `_validate_zip_members()` (path-traversal guard, re-validated at commit time for WR-02 TOCTOU). Task 2 acceptance forbids direct zipfile extract calls that would bypass the guard. |
| T-86-05 | Tampering | Host data integrity (copy-don't-delete) | mitigate | D-02: import is copy-only into the sandbox data dir; the host path is mounted `:ro` so no write-back is possible. The original ~/.local/share/musicstreamer/ stays intact. |
| T-86-06 | Spoofing | Offer-once flag bypass / re-detection | accept | Flag lives in sandbox data dir (paths.data_dir()); a user deleting it simply re-triggers the offer — low risk, no privilege gain. |
</threat_model>

<verification>
- tests/test_flatpak_first_launch.py green (detection + offer-once behaviors).
- flatpak_first_launch.py is Qt-free; host detection uses the literal path constant.
- Wizard reuses Phase 25 import path and never bypasses _validate_zip_members.
</verification>

<success_criteria>
- FP-06 detection + offer-once + Phase 25 reuse implemented in source (the functional first-launch UAT — actually offering the wizard inside the installed sandbox — is the human-verified portion, captured in Plan 05 / must_haves SC5).
</success_criteria>

<output>
Create `.planning/phases/86-linux-flatpak-build/86-02-SUMMARY.md` when done
</output>
