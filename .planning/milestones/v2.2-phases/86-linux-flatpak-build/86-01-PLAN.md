---
phase: 86-linux-flatpak-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - io.github.kcreasey.MusicStreamer.yaml
  - python3-modules.yaml
  - tools/linux-flatpak/flatpak-requirements.txt
  - tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop
  - tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml
  - tools/linux-flatpak/README.md
autonomous: true
requirements: [PKG-LIN-FP-01, PKG-LIN-FP-03, PKG-LIN-FP-04, PKG-LIN-FP-05, PKG-LIN-FP-09, PKG-LIN-FP-10]
must_haves:
  truths:
    - "The Flatpak manifest declares app ID io.github.kcreasey.MusicStreamer (authored as YAML with inline comments — D-14)"
    - "The manifest pins KDE Platform/Sdk 6.8, PySide BaseApp 6.8, ffmpeg-full 24.08, node20"
    - "The manifest finish-args contain the exact allow-list (including the single narrow --filesystem=~/.local/share/musicstreamer:ro for first-launch detection — D-01) and NONE of the forbidden permissions"
    - "QTWEBENGINE_DISABLE_SANDBOX=1 appears in finish-args env (verbatim)"
    - "python3-modules.yaml exists, is valid YAML, and excludes PySide6"
    - "metainfo XML passes appstreamcli validate and .desktop passes desktop-file-validate"
    - "README documents the D-06 release-asset procedure (operator gh release upload of the .flatpak alongside the AppImage)"
  artifacts:
    - path: "io.github.kcreasey.MusicStreamer.yaml"
      provides: "Flatpak build manifest (single source of truth for sandbox policy)"
      contains: "id: io.github.kcreasey.MusicStreamer"
    - path: "python3-modules.yaml"
      provides: "flatpak-pip-generator pip dependency manifest (FP-09)"
    - path: "tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop"
      provides: ".desktop entry for GNOME Software/Activities"
    - path: "tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml"
      provides: "AppStream metainfo for store listing + FP-10 validation"
    - path: "tools/linux-flatpak/flatpak-requirements.txt"
      provides: "Curated pip dep list (excludes PySide6) for flatpak-pip-generator"
  key_links:
    - from: "io.github.kcreasey.MusicStreamer.yaml"
      to: "python3-modules.yaml"
      via: "modules[0] include"
      pattern: "python3-modules.yaml"
    - from: "io.github.kcreasey.MusicStreamer.yaml"
      to: "/app/bin/node"
      via: "build-commands install -D from node20 SDK extension"
      pattern: "install -D /usr/lib/sdk/node20/bin/node /app/bin/node"
---

<objective>
Author the Flatpak manifest and all static build artifacts that the drift-guard tests (Plan 03) parse and that the build driver (Plan 04) consumes. This is the Wave 0 foundation: the manifest, the generated pip-deps manifest, the .desktop entry, the AppStream metainfo XML, and the curated requirements file must exist before any test can assert on them.

Purpose: Establish the single source of truth for sandbox policy (FP-01/03/04/05) and the offline build inputs (FP-09) so downstream waves have concrete artifacts to test and build.
Output: io.github.kcreasey.MusicStreamer.yaml, python3-modules.yaml, the desktop/metainfo artifacts, flatpak-requirements.txt, and a local-build README.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-linux-flatpak-build/86-CONTEXT.md
@.planning/phases/86-linux-flatpak-build/86-RESEARCH.md
@.planning/phases/86-linux-flatpak-build/86-PATTERNS.md
@.claude/skills/spike-findings-musicstreamer/references/linux-appimage-bundling.md

<interfaces>
<!-- Locked manifest values (CONTEXT.md D-01..D-14, RESEARCH.md Pattern 1 + Code Examples). Use verbatim. -->

App ID (FP-01): io.github.kcreasey.MusicStreamer
Runtimes (FP-03):
  runtime: org.kde.Platform   runtime-version: '6.8'
  sdk: org.kde.Sdk            (6.8)
  base: io.qt.PySide.BaseApp  base-version: '6.8'
  add-extensions: org.freedesktop.Platform.ffmpeg-full version '24.08', directory lib/ffmpeg, add-ld-path '.'
  sdk-extensions: org.freedesktop.Sdk.Extension.node20
finish-args (FP-04 + D-01 + FP-05) — EXACT allow-list, in this form:
  --share=network
  --socket=pulseaudio
  --socket=wayland
  --socket=fallback-x11
  --own-name=org.mpris.MediaPlayer2.MusicStreamer
  --filesystem=~/.local/share/musicstreamer:ro
  --env=QTWEBENGINE_DISABLE_SANDBOX=1
FORBIDDEN (must NOT appear): --filesystem=home, --filesystem=home:rw, --socket=session-bus
command: musicstreamer

Curated pip deps (RESEARCH.md Pitfall 5 — EXCLUDE PySide6 and system GObject):
  yt-dlp streamlink platformdirs chardet mutagen pillow requests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Flatpak toolchain + runtimes, generate python3-modules.yaml, author flatpak-requirements.txt</name>
  <files>python3-modules.yaml, tools/linux-flatpak/flatpak-requirements.txt</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Standard Stack (python3-modules.yaml Generation block, lines ~136-156) and §Common Pitfalls Pitfall 5 (PySide6 double-install)
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`python3-modules.yaml` (generation command + critical exclusion, lines ~289-313)
    - pyproject.toml (read the [project] dependencies array to confirm the runtime dep set; cross-check against the curated exclusion)
  </read_first>
  <action>
    Install build tooling (Environment Availability table marks these missing): `sudo apt install -y flatpak-builder`; `pip install flatpak-pip-generator`; `flatpak install -y --user flathub org.kde.Platform//6.8 org.kde.Sdk//6.8 io.qt.PySide.BaseApp//6.8 org.freedesktop.Platform.ffmpeg-full//24.08 org.freedesktop.Sdk.Extension.node20`. Empirically resolve Open Question 1 by running `flatpak run --command=bash org.kde.Platform//6.8 -c "cat /usr/manifest.json | grep -i freedesktop"` (or equivalent) and record the internal Freedesktop SDK version in the README — this confirms ffmpeg-full//24.08 and node20 branch alignment (A4/A6).
    Create tools/linux-flatpak/flatpak-requirements.txt listing ONLY: yt-dlp, streamlink, platformdirs, chardet, mutagen, pillow, requests. Do NOT list PySide6 (provided by BaseApp) or system GObject/PyGObject bindings (provided by KDE runtime) — including them triggers Pitfall 5 (ABI conflict). Cross-check this list against pyproject.toml [project] dependencies and document any dep that is intentionally excluded as runtime-provided.
    Generate the pip manifest: `flatpak-pip-generator --runtime='org.kde.Sdk//6.8' --yaml --requirements-file=tools/linux-flatpak/flatpak-requirements.txt --output python3-modules` (Open Question 3: prefer org.kde.Sdk//6.8; if not resolvable locally, fall back to org.freedesktop.Sdk//24.08 and note the fallback in the README). The output python3-modules.yaml lands at repo root and MUST be committed (FP-09). Verify the generated YAML does NOT contain a PySide6 module entry.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('python3-modules.yaml')); assert d is not None; txt=open('python3-modules.yaml').read(); assert 'PySide6' not in txt and 'pyside6' not in txt, 'PySide6 must be excluded (Pitfall 5)'; print('python3-modules.yaml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `flatpak-builder --version` exits 0 (toolchain installed)
    - `flatpak list --user | grep -E "org.kde.Platform|io.qt.PySide.BaseApp|ffmpeg-full|node20"` shows all four installed
    - python3-modules.yaml exists at repo root, parses as YAML, and contains NO PySide6/pyside6 substring
    - tools/linux-flatpak/flatpak-requirements.txt contains exactly the 7 curated deps and no PySide6 line
    - README records the KDE Platform 6.8 internal Freedesktop SDK version (Open Question 1 resolved empirically)
    - README documents the D-06 release-asset procedure: operator runs `gh release upload <tag> MusicStreamer-<version>.flatpak` to attach the CI-built .flatpak to the GitHub release alongside the AppImage (no CI auto-publish, D-07)
  </acceptance_criteria>
  <done>Toolchain + runtimes installed; python3-modules.yaml generated and committed (FP-09), verified PySide6-free; flatpak-requirements.txt curated.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Flatpak YAML manifest with locked sandbox policy and runtime module sequencing</name>
  <files>io.github.kcreasey.MusicStreamer.yaml, tools/linux-flatpak/README.md</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Architecture Patterns Pattern 1 (manifest top-level, lines ~246-302), Pattern 2 (node20 copy, lines ~304-316), Pattern 3 (ffmpeg-full mkdir, lines ~318-328), §Code Examples finish-args (lines ~484-496)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Common Pitfalls 1 (do NOT set BASEAPP_REMOVE_WEBENGINE=1), 2 (mkdir lib/ffmpeg), 3 (node copy)
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`io.github.kcreasey.MusicStreamer.yaml` (lines ~274-286) and §Critical Implementation Notes #4 (node copy belongs in manifest)
    - tools/linux-build/environment.yml (structural analog — a committed YAML build-input parsed in tests)
  </read_first>
  <action>
    Create io.github.kcreasey.MusicStreamer.yaml at repo root (YAML per D-14). Set id, runtime org.kde.Platform / runtime-version '6.8', sdk org.kde.Sdk, base io.qt.PySide.BaseApp / base-version '6.8', command musicstreamer. Declare sdk-extensions: [org.freedesktop.Sdk.Extension.node20]. Declare add-extensions org.freedesktop.Platform.ffmpeg-full with version '24.08', directory lib/ffmpeg, add-ld-path '.'.
    Do NOT set BASEAPP_REMOVE_WEBENGINE=1 anywhere (Pitfall 1 — GBS.FM login per FP-05/SC3 needs QtWebEngineWidgets). cleanup-commands MUST include `mkdir -p ${FLATPAK_DEST}/lib/ffmpeg` (Pitfall 2 — extension mount point) and the BaseApp cleanup `/app/cleanup-BaseApp.sh`.
    finish-args MUST be exactly the allow-list from the interfaces block (per D-01/FP-04/FP-05): --share=network, --socket=pulseaudio, --socket=wayland, --socket=fallback-x11, --own-name=org.mpris.MediaPlayer2.MusicStreamer, --filesystem=~/.local/share/musicstreamer:ro, --env=QTWEBENGINE_DISABLE_SANDBOX=1. Add a YAML inline comment on the :ro mount citing D-01 (APPROVED ADDITION to FP-04) and on QTWEBENGINE_DISABLE_SANDBOX citing the Flathub io.qt.qtwebengine.BaseApp source (cite-the-source discipline). Do NOT add --filesystem=home or --socket=session-bus.
    modules: first entry includes python3-modules.yaml; second entry name musicstreamer, buildsystem simple, build-commands `install -D /usr/lib/sdk/node20/bin/node /app/bin/node` (Pattern 2 — node20 SDK extension is build-time only; binary must be copied to /app/bin to survive at runtime, Pitfall 3) then `pip3 install --no-deps --prefix=/app .`, source type dir path '.'.
    Defensive hedge for Open Question 2 (GStreamer auto-discovery of /app/lib/ffmpeg, ASSUMED A7): add a commented-out `# --env=GST_PLUGIN_PATH=/app/lib/ffmpeg` line in finish-args with a comment that it is a fallback to be uncommented if `gst-inspect-1.0 avdec_aac` fails during UAT (Plan 05). Keep it commented so the deny/allow-list drift-guard sees the clean allow-list.
    Write tools/linux-flatpak/README.md documenting the local build/install commands (flatpak-builder --user --install, flatpak run), the SKIP_SIGN local-iteration note, and the Open Question 1/3 empirical findings from Task 1.
    Also add a short "Publishing the .flatpak as a release asset (D-06)" subsection to the README: the workflow_dispatch CI (Plan 04) produces the signed `MusicStreamer-<version>.flatpak` as a build artifact but does NOT auto-publish it (D-07); to reconcile D-06 (the .flatpak ships as a GitHub release asset alongside the AppImage), the operator manually downloads the CI artifact and runs `gh release upload <tag> MusicStreamer-<version>.flatpak` against the existing release tag (the same tag carrying the AppImage). This is a one-time operator step per release, not a CI step.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('io.github.kcreasey.MusicStreamer.yaml')); a=d['finish-args']; assert d['id']=='io.github.kcreasey.MusicStreamer'; assert d['runtime-version']=='6.8' and d['base-version']=='6.8'; assert '--own-name=org.mpris.MediaPlayer2.MusicStreamer' in a and '--filesystem=~/.local/share/musicstreamer:ro' in a and '--env=QTWEBENGINE_DISABLE_SANDBOX=1' in a; assert '--filesystem=home' not in a and '--socket=session-bus' not in a; assert d['add-extensions']['org.freedesktop.Platform.ffmpeg-full']['version']=='24.08'; print('manifest OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `io.github.kcreasey.MusicStreamer.yaml parses and finish-args contains '--own-name=org.mpris.MediaPlayer2.MusicStreamer'`
    - finish-args contains '--filesystem=~/.local/share/musicstreamer:ro' and '--env=QTWEBENGINE_DISABLE_SANDBOX=1'
    - finish-args contains NEITHER '--filesystem=home' NOR '--socket=session-bus' (parsed as list, not text)
    - `grep -v '^#' io.github.kcreasey.MusicStreamer.yaml | grep -c "BASEAPP_REMOVE_WEBENGINE"` returns 0
    - manifest contains `mkdir -p ${FLATPAK_DEST}/lib/ffmpeg` and `install -D /usr/lib/sdk/node20/bin/node /app/bin/node`
    - add-extensions ffmpeg-full version == '24.08'; sdk-extensions includes node20
  </acceptance_criteria>
  <done>Manifest authored with exact locked sandbox policy, correct module sequencing, node copy + ffmpeg mkdir build steps, and source-cited inline comments; README documents local build and the D-06 `gh release upload` release-asset procedure (reconciling D-06 with D-07's no-auto-publish).</done>
</task>

<task type="auto">
  <name>Task 3: Author .desktop entry and AppStream metainfo XML (FP-10 validator targets)</name>
  <files>tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop, tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml</files>
  <read_first>
    - .planning/phases/86-linux-flatpak-build/86-PATTERNS.md §`tools/linux-flatpak/desktop/...desktop` (full source to copy + Flatpak changes, lines ~317-343) and §metainfo (required fields, lines ~347-361)
    - .planning/phases/86-linux-flatpak-build/86-RESEARCH.md §Open Question 6 (screenshot needed to pass appstreamcli cleanly)
    - tools/linux-build/desktop/org.lightningjim.MusicStreamer.desktop (the verbatim analog to adapt)
  </read_first>
  <action>
    Copy tools/linux-build/desktop/org.lightningjim.MusicStreamer.desktop to tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop. Change Icon to io.github.kcreasey.MusicStreamer (match new app ID). Keep Exec=musicstreamer %U, StartupWMClass=MusicStreamer, the MimeType audio list, and PRESERVE the PKG-LIN-APP-09 comment + intentional absence of audio/x-mpegurl and audio/x-scpls (playlist MIME types stay absent — curated-library identity).
    Create tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml with the minimum fields appstreamcli requires: id io.github.kcreasey.MusicStreamer, name MusicStreamer, summary, description, metadata_license + project_license, launchable type desktop-id pointing at the .desktop, url type homepage (the GitHub mirror), a releases block with at least one release (version matching the current app version, with a date), a content_rating type oars-1.1 block, and at least one screenshot (Open Question 6 — reuse a Phase 85 evidence-bundle Wayland screenshot URL or a placeholder caption) to avoid a validation error. Provider/developer_name as appropriate.
  </action>
  <verify>
    <automated>bash -c 'set -e; appstreamcli validate tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml && desktop-file-validate tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop && echo VALIDATORS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `appstreamcli validate tools/linux-flatpak/metainfo/io.github.kcreasey.MusicStreamer.metainfo.xml` exits 0
    - `desktop-file-validate tools/linux-flatpak/desktop/io.github.kcreasey.MusicStreamer.desktop` exits 0
    - .desktop Icon line == `Icon=io.github.kcreasey.MusicStreamer`
    - .desktop MimeType line contains NEITHER `x-mpegurl` NOR `x-scpls` (PKG-LIN-APP-09 preserved)
    - metainfo `<id>` == io.github.kcreasey.MusicStreamer and contains a `<releases>` block
  </acceptance_criteria>
  <done>.desktop and metainfo XML authored; both pass their respective validators (FP-10); playlist MIME exclusion preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → sandbox | The narrow `:ro` mount of ~/.local/share/musicstreamer is the only host path crossing into the sandbox |
| host session bus → sandbox | xdg-dbus-proxy mediates; only `--own-name` of the MPRIS2 name is granted |
| QtWebEngine subprocess → host | Chromium inner sandbox disabled; outer Flatpak sandbox still constrains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Elevation of Privilege | finish-args filesystem scope | mitigate | Manifest declares ONLY `--filesystem=~/.local/share/musicstreamer:ro`; NEVER `--filesystem=home`. Verified by Plan 03 deny-list drift-guard parsing YAML as data (D-13). |
| T-86-02 | Information Disclosure | finish-args D-Bus scope | mitigate | Manifest uses `--own-name=org.mpris.MediaPlayer2.MusicStreamer` only; NEVER broad `--socket=session-bus`. Verified by Plan 03 deny-list (D-13). |
| T-86-03 | Elevation of Privilege | QtWebEngine subprocess (GBS.FM login) | accept | `QTWEBENGINE_DISABLE_SANDBOX=1` disables Chromium's inner sandbox; the outer Flatpak sandbox still constrains the subprocess (acceptable per FP-05). Verbatim spelling cited from Flathub io.qt.qtwebengine.BaseApp. |
| T-86-SC | Tampering | apt/pip installs (flatpak-builder, flatpak-pip-generator) + Flathub runtime downloads | mitigate | RESEARCH.md Package Legitimacy Audit confirms all are official flatpak-org / Ubuntu packages (no [SUS]/[SLOP]); Flathub runtimes are signed by Flathub. No [ASSUMED] third-party packages requiring a human gate. |
</threat_model>

<verification>
- Manifest, python3-modules.yaml, .desktop, metainfo, flatpak-requirements.txt all exist at their declared paths.
- The three task `<automated>` checks all pass (manifest parse + deny-list, python3-modules PySide6-free, both FP-10 validators).
- `grep -v '^#' io.github.kcreasey.MusicStreamer.yaml | grep -c "filesystem=home"` returns 0.
</verification>

<success_criteria>
- FP-01 (app ID), FP-03 (runtime pins), FP-04 (finish-args allow+deny), FP-05 (QtWebEngine env), FP-09 (python3-modules.yaml), FP-10 (validators pass) all materially satisfied by committed artifacts.
- All artifacts are the parse/build targets that Plans 03 and 04 depend on.
</success_criteria>

<output>
Create `.planning/phases/86-linux-flatpak-build/86-01-SUMMARY.md` when done
</output>
