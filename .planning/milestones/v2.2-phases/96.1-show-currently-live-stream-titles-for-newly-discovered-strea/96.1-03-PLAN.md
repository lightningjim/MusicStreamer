---
phase: 96.1-show-currently-live-stream-titles-for-newly-discovered-strea
plan: 03
type: execute
wave: 3
depends_on:
  - 96.1-01
  - 96.1-02
files_modified:
  - musicstreamer/ui_qt/live_refresh_dialog.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "_LiveRefreshScanWorker.run() resolves blank/Untitled live-entry titles off the UI thread, threading node_runtime into resolve_live_title (D-02 landmine; worker touches no widget)"
    - "A dedicated 'Newly discovered on channel' section renders each live stream's current YouTube title as read-only Qt.PlainText alongside an editable Name field (D-04 / CR-01 / T-39-01)"
    - "Every currently-live stream is listed in the discover section — no dedup hiding of streams already chosen as a REMAP target (D-05)"
    - "Each newly-discovered row offers Add-as-new-station OR Map/merge-onto-an-existing-station (D-06)"
    - "The map target is chosen from a dropdown of this provider's flagged stations; duplicate maps are caught fail-closed by the existing WR-03 guard in apply_refresh (D-07)"
    - "Newly-discovered rows are unchecked by default; nothing applies without an explicit tick (D-08)"
  artifacts:
    - path: "musicstreamer/ui_qt/live_refresh_dialog.py"
      provides: "_DiscoverRowWidget + extended _LiveRefreshScanWorker.run + extended _populate_rows"
      contains: "class _DiscoverRowWidget"
  key_links:
    - from: "musicstreamer/ui_qt/live_refresh_dialog.py:_LiveRefreshScanWorker.run"
      to: "musicstreamer.yt_import.resolve_live_title"
      via: "blank-title resolution loop threading self._node_runtime (D-02)"
      pattern: "resolve_live_title\\(.*node_runtime=self\\._node_runtime"
    - from: "musicstreamer/ui_qt/live_refresh_dialog.py:_DiscoverRowWidget"
      to: "apply_refresh remap path"
      via: "build_staged_change returns action=remap with station_id+stream_id (D-06/D-07)"
      pattern: "\"action\": \"remap\""
    - from: "musicstreamer/ui_qt/live_refresh_dialog.py:_populate_rows"
      to: "self._flagged_stations"
      via: "_DiscoverRowWidget map-dropdown source (D-07)"
      pattern: "_DiscoverRowWidget\\("
---

<objective>
Wire the display/action half of Phase 96.1 into musicstreamer/ui_qt/live_refresh_dialog.py: extend _LiveRefreshScanWorker.run() to resolve blank live-entry titles off the UI thread (D-02), add a _DiscoverRowWidget that renders the resolved title read-only as Qt.PlainText (D-04) with an Add-or-Map action toggle (D-06) whose map target comes from a flagged-station dropdown (D-07), and replace the old ADD section in _populate_rows with a "Newly discovered on channel" section listing every live stream (D-05), unchecked by default (D-08).

This turns the 7 RED dialog test stubs from Plan 01 GREEN. Depends on Plan 02's resolve_live_title / _is_blank_title.

Purpose: this is the display/action dimension — making the real title visible and acting on it (add new station or map/merge onto an existing one).
Output: extended worker run(), new _DiscoverRowWidget class, extended _populate_rows.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-CONTEXT.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-RESEARCH.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md
@.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-02-SUMMARY.md

<interfaces>
<!-- Existing live_refresh_dialog.py symbols. Extracted from source — no exploration needed. -->

_LiveRefreshScanWorker.run() (lines 86-96): calls yt_import.scan_playlist(self._url, toast_callback=self._toast, node_runtime=self._node_runtime); emits finished/error. self._node_runtime already stored in __init__.

_RowWidget (lines 282-428) is the analog. Key fragments to mirror:
  - Qt.PlainText guard (lines 322-331): label.setTextFormat(Qt.PlainText); label.setWordWrap(True)
  - Name edit (lines 338-347): self._name_edit = QLineEdit(); pre-fill via build_row_data
  - QComboBox userData (lines 352-365): self._combo.addItem(text, userData=obj); read via currentData(Qt.UserRole)
  - is_staged (lines 379-380): return self._check.isChecked()
  - build_staged_change add (lines 400-408): {"action":"add","name":...,"scan_result":entry,"provider_name":...}
  - build_staged_change remap primary-stream resolution (lines 414-428): iterate station.streams for position==1, fallback streams[0].id; return {"action":"remap","station_id":sid,"stream_id":primary_stream_id,"scan_result":entry}

_populate_rows (lines 578-620): builds one REMAP _RowWidget per self._flagged_stations, then the ADD section (label lines 604-608 + _RowWidget("add",...) loop lines 611-620). Replace ONLY the ADD section.

_on_apply (lines 626-634): for row in self._row_widgets: station_id = row._station.id if row._station else None; change = row.build_staged_change(station_id=station_id). _DiscoverRowWidget MUST satisfy this duck-type: expose ._station (None add / chosen Station map), is_staged(), build_staged_change(station_id=None).

default_check_state(action) (lines 167-173): returns action == "remap". "discover"/add -> False (D-08).

apply_refresh (lines 176-274) + WR-03 duplicate-target guard (lines 206-222): already handles action="remap" and action="add"; NO change needed (D-07).

From musicstreamer.yt_import (Plan 02): resolve_live_title(url, *, node_runtime=None) -> str; _is_blank_title(title) -> bool.

From musicstreamer.models: Station (has .id, .name, .streams with .id/.position).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _LiveRefreshScanWorker.run() to resolve blank titles off-thread (D-02)</name>
  <read_first>
    - musicstreamer/ui_qt/live_refresh_dialog.py lines 62-96 (_LiveRefreshScanWorker — run() being extended in place; self._node_runtime already stored)
    - musicstreamer/yt_import.py (Plan 02 output: resolve_live_title + _is_blank_title signatures)
    - tests/test_live_refresh_dialog.py (RED stub from Plan 01: test_scan_worker_threads_node_runtime_into_resolve)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md (_LiveRefreshScanWorker.run extension, lines 101-129; "QThread Worker — No Widget Access" shared pattern lines 481-487)
  </read_first>
  <behavior>
    - After scan_playlist returns, for each entry where yt_import._is_blank_title(entry.get("title","")) is True, entry["title"] is set to yt_import.resolve_live_title(entry["url"], node_runtime=self._node_runtime).
    - Entries with a real (non-blank) title are NOT re-resolved (hybrid — D-01).
    - resolve_live_title is called with node_runtime=self._node_runtime (the worker's stored runtime — D-02 landmine).
    - run() emits finished(results) and touches no widget.
  </behavior>
  <action>
    Extend _LiveRefreshScanWorker.run() in place (no new QThread class — D-02): after the existing results = yt_import.scan_playlist(...) call and BEFORE self.finished.emit(results), insert a loop over results that, for each entry where yt_import._is_blank_title(entry.get("title", "")) is True, sets entry["title"] = yt_import.resolve_live_title(entry["url"], node_runtime=self._node_runtime). Keep the existing try/except wrapping; resolution runs sequentially (real-world 1-2 blanks — RESEARCH Pattern 3). Do NOT touch self._status_label or any widget from run() (Pitfall 5); emit Signals only. The existing finished/error signals are sufficient — no new status_update signal required (Claude's discretion per CONTEXT; keep it simple).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -k "threads_node_runtime_into_resolve or scan_worker" -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "resolve_live_title(" musicstreamer/ui_qt/live_refresh_dialog.py` returns >= 1 and the call passes `node_runtime=self._node_runtime` (`grep "resolve_live_title" musicstreamer/ui_qt/live_refresh_dialog.py | grep -c "node_runtime=self._node_runtime"` returns >= 1).
    - Source: run() body references yt_import._is_blank_title (`grep -c "_is_blank_title" musicstreamer/ui_qt/live_refresh_dialog.py` returns >= 1).
    - Behavior: test_scan_worker_threads_node_runtime_into_resolve PASSES; the existing test_scan_worker_forwards_node_runtime and test_scan_worker_uses_qthread still pass.
    - run() contains no widget access (no self._status_label / self._progress inside the worker class).
  </acceptance_criteria>
  <done>The worker resolves blank live-entry titles off-thread, threading node_runtime into resolve_live_title; the worker-threading stub is green and no widget is touched from run().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _DiscoverRowWidget (read-only PlainText title + add/map toggle + flagged dropdown)</name>
  <read_first>
    - musicstreamer/ui_qt/live_refresh_dialog.py lines 282-428 (_RowWidget — the full analog: Qt.PlainText label 322-331, name edit 338-347, QComboBox userData 352-365, is_staged 379-380, build_staged_change add 400-408 + remap primary-stream resolution 414-428)
    - musicstreamer/ui_qt/live_refresh_dialog.py lines 167-173 (default_check_state) and lines 626-634 (_on_apply duck-type the widget must satisfy)
    - tests/test_live_refresh_dialog.py (RED stubs: test_discover_row_title_label_plaintext, test_discover_row_add_staged_change, test_discover_row_map_staged_change, test_discover_row_map_dropdown_from_flagged, test_discover_row_unchecked_by_default)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-RESEARCH.md (Pattern 4 _DiscoverRowWidget; build_staged_change Map Mode code example; Pitfall 4 stream_id)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md (new _DiscoverRowWidget section lines 141-251)
  </read_first>
  <behavior>
    - _DiscoverRowWidget(entry, flagged_stations, provider_name) renders a read-only title label from entry["title"] with textFormat() == Qt.PlainText (D-04 / CR-01 / T-39-01).
    - Exposes an editable Name QLineEdit pre-filled with entry["title"] (add default) (D-04).
    - is_staged() is False immediately after construction (unchecked default — D-08).
    - In add mode, checked: build_staged_change() == {"action":"add","name":<name or title>,"scan_result":entry,"provider_name":provider_name} (D-06 add).
    - In map mode with a chosen Station having a position==1 stream id=10: build_staged_change() == {"action":"remap","station_id":station.id,"stream_id":10,"scan_result":entry} (D-06 map / D-07).
    - The map dropdown items' userData are the flagged Station objects passed at construction (D-07).
    - ._station is None in add mode, the chosen Station in map mode (so _on_apply's station_id derivation works).
  </behavior>
  <action>
    Add class _DiscoverRowWidget(QWidget) modeled on _RowWidget. Constructor signature: __init__(self, entry: dict, flagged_stations: list, provider_name: str, parent=None). Store self._entry = entry, self._provider_name = provider_name, self._station = None (updated when map mode + a station is chosen). Build a QHBoxLayout. Add a QCheckBox self._check unchecked by default (default_check_state("discover") -> False; "discover" is not "remap" so reuse default_check_state, or set unchecked directly — D-08). Render a read-only title QLabel from entry.get("title",""): MUST call setTextFormat(Qt.PlainText) and setWordWrap(True) (CR-01 / T-39-01 — copy the lines 322-331 idiom verbatim). Add an editable QLineEdit self._name_edit pre-filled with entry.get("title",""). Add an action toggle (two QRadioButton in a QButtonGroup — "Add as new station" / "Map onto existing station" — Claude's discretion on exact layout) and a QComboBox self._map_combo populated from flagged_stations with each item's userData set to the Station object (addItem(station.name, userData=station)); when flagged_stations is empty, the map option may be disabled or the combo shows "(no flagged stations)". When the map radio is selected and a station chosen, set self._station = currentData(Qt.UserRole). Implement is_staged() -> self._check.isChecked(). Implement build_staged_change(self, station_id=None) -> Optional[dict]: return None if not staged; if add mode return {"action":"add","name": self._name_edit.text().strip() or self._entry.get("title",""), "scan_result": self._entry, "provider_name": self._provider_name}; if map mode read chosen = self._map_combo.currentData(Qt.UserRole); return None if chosen is None; resolve primary_stream_id by iterating chosen.streams for position==1, fallback chosen.streams[0].id (EXACT mirror of _RowWidget lines 414-428 — Pitfall 4: stream_id MUST be present); return {"action":"remap","station_id": chosen.id, "stream_id": primary_stream_id, "scan_result": self._entry}. Do NOT auto-stage by anchor similarity (anti-pattern); name pre-fill only.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -k "discover_row" -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "class _DiscoverRowWidget" musicstreamer/ui_qt/live_refresh_dialog.py` returns 1.
    - Source: the read-only title label sets PlainText — `grep -c "setTextFormat(Qt.PlainText)" musicstreamer/ui_qt/live_refresh_dialog.py` returns >= 3 (the two existing _RowWidget labels + the new discover title label).
    - Source: map staged change emits remap with stream_id — `grep -c '"action": "remap"' musicstreamer/ui_qt/live_refresh_dialog.py` returns >= 2 (existing _RowWidget + new _DiscoverRowWidget).
    - Behavior: test_discover_row_title_label_plaintext, test_discover_row_add_staged_change, test_discover_row_map_staged_change, test_discover_row_map_dropdown_from_flagged, test_discover_row_unchecked_by_default all PASS.
  </acceptance_criteria>
  <done>_DiscoverRowWidget renders the untrusted title as Qt.PlainText, offers add/map with a flagged-station dropdown, builds the correct add/remap staged changes (stream_id present), and is unchecked by default; the five discover-row stubs are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the ADD section in _populate_rows with the "Newly discovered" section (D-05)</name>
  <read_first>
    - musicstreamer/ui_qt/live_refresh_dialog.py lines 578-620 (_populate_rows — replace ONLY the ADD section at lines 602-620; keep the REMAP loop 591-600 unchanged)
    - musicstreamer/ui_qt/live_refresh_dialog.py lines 509-511 (self._flagged_stations already loaded in __init__) and 626-634 (_on_apply loop)
    - tests/test_live_refresh_dialog.py (RED stub: test_discover_section_no_dedup)
    - .planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-PATTERNS.md (_populate_rows extension lines 254-291; _on_apply compatibility lines 294-307)
  </read_first>
  <behavior>
    - For every entry in scan_results, _populate_rows appends a _DiscoverRowWidget to self._rows_layout AND self._row_widgets (count of discover rows == len(scan_results) — no dedup hiding, D-05).
    - The REMAP rows (one per flagged station) are still built first, unchanged.
    - A discover-section header QLabel ("Newly discovered on channel …") is added when scan_results is non-empty.
    - _on_apply is unchanged and still collects staged changes from _DiscoverRowWidget via the duck-type interface.
  </behavior>
  <action>
    In _populate_rows, replace the ADD section (the add_label QLabel at lines 604-608 and the for-entry _RowWidget("add", ...) loop at lines 611-620) with: a header QLabel "<b>Newly discovered on channel (unchecked by default):</b>" (setWordWrap(True)) added when scan_results is non-empty, then for entry in scan_results: row = _DiscoverRowWidget(entry, self._flagged_stations, self._provider_name, parent=self._rows_container); self._rows_layout.addWidget(row); self._row_widgets.append(row). Do NOT dedup against REMAP-targeted entries (D-05 — incidental double-listing is acceptable). Leave the REMAP loop (lines 591-600), _on_apply (626-634), apply_refresh, and the WR-03 guard untouched — _DiscoverRowWidget's remap output is already covered by the existing guard (D-07). Update the self._row_widgets type hint / class docstring note if needed to reflect that rows may be _RowWidget or _DiscoverRowWidget.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py tests/test_yt_import_library.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "_DiscoverRowWidget(" musicstreamer/ui_qt/live_refresh_dialog.py` returns >= 2 (class def line excluded; one construction in _populate_rows + the class header counts — confirm at least one construction call inside _populate_rows by `grep -n "_DiscoverRowWidget(" musicstreamer/ui_qt/live_refresh_dialog.py`).
    - Source: the old ADD-section _RowWidget("add", ...) loop is gone — `grep -c '_RowWidget(' musicstreamer/ui_qt/live_refresh_dialog.py` no longer counts an "add" construction in _populate_rows (REMAP _RowWidget construction remains).
    - Behavior: test_discover_section_no_dedup PASSES.
    - Full regression: `.venv/bin/python -m pytest tests/test_live_refresh_dialog.py tests/test_yt_import_library.py -q` reports 0 failures (9 baseline + 7 new dialog tests; 26 baseline + 6 new yt_import tests).
  </acceptance_criteria>
  <done>The "Newly discovered on channel" section renders a _DiscoverRowWidget for every live stream with no dedup; both touched test files are fully green (all 13 new tests + baselines).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker thread → main thread | scan results (incl. attacker-controllable titles) marshalled via queued Signal |
| scan-derived title → QLabel | untrusted text rendered in the discover-section row UI |
| _DiscoverRowWidget map change → apply_refresh | a discover row can re-point an existing station's primary stream URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 / CR-01 | Tampering/Spoofing | scan-derived title rendered in _DiscoverRowWidget QLabel (rich-text / remote-resource injection) | mitigate | read-only title label calls setTextFormat(Qt.PlainText) so a title like "<img src=http://attacker/>" cannot load a remote resource (D-04; verified by test_discover_row_title_label_plaintext) |
| T-96.1-05 | Tampering | duplicate map targets / same live URL mapped by multiple rows | mitigate | apply_refresh WR-03 duplicate-target guard (lines 206-222) already fails closed before any mutation; the discover row emits action="remap" which is covered — no new guard needed (D-07) |
| T-96.1-06 | Elevation/Tampering | worker touching a widget from a non-main thread | mitigate | _LiveRefreshScanWorker.run() emits Signals only; the blank-resolution loop touches no QWidget (Pitfall 5) |
| T-96.1-07 | Tampering | unintended apply without explicit user consent | mitigate | discover rows unchecked by default; build_staged_change returns None when unchecked (D-08); empty-apply guard in apply_refresh (line 203) |
| T-96.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: zero external packages) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_live_refresh_dialog.py tests/test_yt_import_library.py -q` — all green (13 new + 35 baseline).
- D-02 landmine: worker threads node_runtime into resolve_live_title (grep node_runtime=self._node_runtime).
- D-04 / CR-01: discover title label is Qt.PlainText (setTextFormat count >= 3).
- D-05: discover-row count == scan-result count (no dedup) — test_discover_section_no_dedup.
- D-07: no new WR-03 guard added; apply_refresh untouched.
</verification>

<success_criteria>
- _LiveRefreshScanWorker.run() resolves blanks off-thread with node_runtime threading (D-02).
- _DiscoverRowWidget: read-only PlainText title + editable name + add/map toggle + flagged dropdown, unchecked default (D-04/D-06/D-07/D-08).
- _populate_rows lists every live stream in the new section, no dedup (D-05).
- WR-03 guard reused unchanged (D-07).
- All 13 Phase-96.1 tests green; no regression in the 35-test baseline.
</success_criteria>

<output>
Create `.planning/phases/96.1-show-currently-live-stream-titles-for-newly-discovered-strea/96.1-03-SUMMARY.md` when done
</output>
