---
phase: 97-resolve-station-url-duplication-between-the-top-level-standa
plan: 04
type: execute
wave: 3
depends_on: ["97-01", "97-02"]
files_modified:
  - musicstreamer/url_helpers.py
  - musicstreamer/aa_live.py
  - musicstreamer/ui_qt/station_filter_proxy.py
  - musicstreamer/ui_qt/now_playing_panel.py
  - musicstreamer/ui_qt/add_sibling_dialog.py
  - tests/test_aa_siblings.py
autonomous: true
requirements: [D-05, D-07]
must_haves:
  truths:
    - "All 5 external metadata/derivation consumers read Station.canonical_url instead of streams[0].url (D-07)"
    - "Playback ordering in player.py (order_streams + preferred_stream_id) is unchanged (D-05)"
    - "Non-D-07 streams[0] reads (live_refresh_dialog, import paths, gbs_api dict) are left untouched"
  artifacts:
    - path: "musicstreamer/url_helpers.py"
      provides: "find_aa_siblings + suggest_similar keyed off canonical_url"
      contains: "canonical_url"
    - path: "musicstreamer/aa_live.py"
      provides: "get_di_channel_key keyed off canonical_url"
      contains: "canonical_url"
    - path: "musicstreamer/ui_qt/station_filter_proxy.py"
      provides: "filterAcceptsRow keyed off canonical_url"
      contains: "canonical_url"
    - path: "musicstreamer/ui_qt/now_playing_panel.py"
      provides: "_refresh_siblings keyed off canonical_url"
      contains: "canonical_url"
  key_links:
    - from: "url_helpers.find_aa_siblings / suggest_similar"
      to: "Station.canonical_url"
      via: "replace st.streams[0].url"
      pattern: "canonical_url"
    - from: "aa_live.get_di_channel_key"
      to: "Station.canonical_url"
      via: "replace streams[0].url"
      pattern: "canonical_url"
---

<objective>
Switch all five D-07 external metadata/derivation consumers from positional
streams[0].url to the single Station.canonical_url accessor (D-07), so AA sibling
detection, the DI channel-key derivation, the station filter proxy, the now-playing
sibling line, and the add-sibling fallback all key off the canonical stream. Playback
(player.py preferred_stream_id / order_streams) is deliberately NOT touched (D-05).

Purpose: Complete the single-source-of-truth goal at the read side — every metadata
consumer uses one canonical URL rule, matching today's "everything read the single
top URL" behavior.
Output: 5 source files repointed + the AA-canonical sibling test (Plan 01) GREEN.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-CONTEXT.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- From Plan 02, now available: -->
Station.canonical_url -> str   # FK lookup -> position-1 fallback -> ""

<!-- D-07 call sites to repoint (97-RESEARCH Full Call-Site Inventory, grep-verified): -->
url_helpers.py:216  find_aa_siblings   cand_url = st.streams[0].url
url_helpers.py:358  suggest_similar    current_first_url=current_station.streams[0].url
station_filter_proxy.py:179  filterAcceptsRow  url = streams[0].url
now_playing_panel.py:2433  _refresh_siblings  current_url = self._station.streams[0].url
aa_live.py:161  get_di_channel_key  url = streams[0].url
add_sibling_dialog.py:286  fallback (RESEARCH Open-Q3: treat as D-07 consumer)

<!-- DO NOT TOUCH (non-D-07 streams[0] reads, 97-RESEARCH): -->
live_refresh_dialog.py:271,455,613 (remap target, not metadata)
now_playing_panel.py:2796,2803 (playback stream, not URL)
aa_import.py:233 / soma_import.py:318 (streams[0].id, not URL)
gbs_api.py:1241 (dict, not Station.streams)
player.py order_streams + preferred_stream_id (D-05 — playback unchanged)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint AA-derivation consumers (url_helpers + aa_live) to canonical_url</name>
  <behavior>
    - tests/test_aa_siblings.py canonical test (Plan 01): sibling detection matches via the canonical stream URL even when streams[0] is a non-AA URL.
    - canonical_url property already covered by Plan 02's test_url_helpers canonical_url test (regression must stay green).
  </behavior>
  <read_first>
    - musicstreamer/url_helpers.py (find_aa_siblings 213-216; suggest_similar 354-358)
    - musicstreamer/aa_live.py (get_di_channel_key 158-167)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (url_helpers.py + aa_live.py replacement bodies)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md (Full Call-Site Inventory; "streams[0] reads that are NOT D-07" — do not touch)
  </read_first>
  <files>musicstreamer/url_helpers.py, musicstreamer/aa_live.py</files>
  <action>
    In url_helpers.find_aa_siblings (line ~216): keep the `if not st.streams: continue` guard, replace `cand_url = st.streams[0].url` with `cand_url = st.canonical_url` (Phase 97 D-07). In suggest_similar (line ~358): replace `current_first_url=current_station.streams[0].url` with `current_first_url=current_station.canonical_url`; preserve the surrounding `if current_station.streams:` guard. In aa_live.get_di_channel_key (line ~161): replace `url = streams[0].url` with `url = station.canonical_url` and adjust the empty guard to `if not url or not _is_aa_url(url): return None` (97-PATTERNS body). Use the Station.canonical_url property (no new import needed — it is an attribute). Do NOT touch any non-D-07 streams[0] site listed in the interfaces block.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_aa_siblings.py tests/test_url_helpers.py -k "canonical or sibling" -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canonical_url" musicstreamer/url_helpers.py` >= 2
    - `grep -c "canonical_url" musicstreamer/aa_live.py` >= 1
    - `grep -c "streams\[0\].url" musicstreamer/url_helpers.py` == 0 AND `grep -c "streams\[0\].url" musicstreamer/aa_live.py` == 0
    - `.venv/bin/python -m pytest tests/test_aa_siblings.py tests/test_url_helpers.py -k "canonical or sibling" -x` exits 0 (AA-canonical sibling test GREEN, no regression)
  </acceptance_criteria>
  <done>AA sibling + DI channel-key derivation key off canonical_url; AA-canonical test green; non-D-07 sites untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint UI consumers (station_filter_proxy + now_playing_panel + add_sibling_dialog) to canonical_url</name>
  <read_first>
    - musicstreamer/ui_qt/station_filter_proxy.py (filterAcceptsRow 177-182)
    - musicstreamer/ui_qt/now_playing_panel.py (_refresh_siblings 2429-2433; confirm lines 2796/2803 are playback streams[0], NOT URL — leave them)
    - musicstreamer/ui_qt/add_sibling_dialog.py (fallback at ~286)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (station_filter_proxy.py + now_playing_panel.py replacement bodies)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md (Open-Q3 — add_sibling_dialog IS a D-07 consumer)
  </read_first>
  <files>musicstreamer/ui_qt/station_filter_proxy.py, musicstreamer/ui_qt/now_playing_panel.py, musicstreamer/ui_qt/add_sibling_dialog.py</files>
  <action>
    station_filter_proxy.filterAcceptsRow (~179): replace `streams = getattr(station,"streams",None) or []; if streams: url = streams[0].url` with `url = station.canonical_url` and gate `if url and _is_aa_url(url):` (97-PATTERNS body). now_playing_panel._refresh_siblings (~2433): replace `current_url = self._station.streams[0].url` with `current_url = self._station.canonical_url`; adjust the early guard so a None station still returns early and an empty canonical_url hides the sibling label (97-PATTERNS body). Leave now_playing_panel lines 2796/2803 (playback streams[0], not URL) untouched. add_sibling_dialog (~286): replace the `station.streams[0].url` fallback with `station.canonical_url` per RESEARCH Open-Q3 (D-07 consistency). Do not alter playback logic anywhere (D-05).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_filter_proxy.py tests/test_now_playing_panel.py tests/test_add_sibling_dialog.py -x --tb=short 2>/dev/null || .venv/bin/python -c "import musicstreamer.ui_qt.station_filter_proxy, musicstreamer.ui_qt.now_playing_panel, musicstreamer.ui_qt.add_sibling_dialog; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canonical_url" musicstreamer/ui_qt/station_filter_proxy.py` >= 1
    - `grep -c "canonical_url" musicstreamer/ui_qt/now_playing_panel.py` >= 1
    - `grep -c "canonical_url" musicstreamer/ui_qt/add_sibling_dialog.py` >= 1
    - now_playing_panel.py still references `streams[0]` for the playback sites (lines ~2796/2803 untouched): `grep -c "streams\[0\]" musicstreamer/ui_qt/now_playing_panel.py` >= 1
    - The verify command exits 0 (targeted tests pass, or modules import cleanly if no dedicated test file exists)
  </acceptance_criteria>
  <done>Filter proxy, now-playing sibling line, and add-sibling fallback key off canonical_url; playback streams[0] reads preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Regression guard — playback unchanged + no stray D-07 streams[0].url reads remain</name>
  <read_first>
    - musicstreamer/player.py (order_streams call + preferred_stream_id usage ~757-786 — confirm NOT modified)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md (Full Call-Site Inventory — the complete list of D-07 vs non-D-07 sites)
  </read_first>
  <files>tests/test_aa_siblings.py</files>
  <action>
    Run the player suite to confirm D-05 (playback) is untouched. Then audit: grep the whole musicstreamer/ tree for `streams[0].url` and confirm every remaining hit is on the explicit NON-D-07 keep-list (live_refresh_dialog.py:271/455/613, gbs_api dict, import paths) — NOT any of the 5 D-07 consumers. If the AA-canonical sibling test in tests/test_aa_siblings.py needs a small assertion tightening to lock that canonical (not positional) URL drives matching, add it here (this is the only test file this plan owns for edits; do not edit dialog/repo tests owned by other plans). Do not modify player.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_player_*.py tests/test_aa_siblings.py tests/test_url_helpers.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only -- musicstreamer/player.py` shows NO changes to player.py in this phase (D-05 playback unchanged)
    - Every `streams[0].url` hit under musicstreamer/ is on the non-D-07 keep-list: none of url_helpers.py, aa_live.py, station_filter_proxy.py, now_playing_panel.py (sibling line), add_sibling_dialog.py contain `streams[0].url`
    - `.venv/bin/python -m pytest tests/test_player_*.py tests/test_aa_siblings.py tests/test_url_helpers.py -x` exits 0
  </acceptance_criteria>
  <done>Playback path provably unchanged; only non-D-07 streams[0].url reads remain; AA-canonical + player + url_helpers suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted Station -> metadata consumers | The canonical URL read from a loaded Station drives AA-slug/channel-key derivation and filter matching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-08 | Input validation | canonical_url feeding _is_aa_url / _aa_slug_from_url | accept | Same parsing already applied to streams[0].url before this phase; the value source changes, the validation does not (97-RESEARCH Security Domain V5). |
| T-97-09 | Integrity (wrong stream selected for playback) | risk of accidentally repointing playback to canonical | mitigate | Task 3 asserts player.py is unmodified (D-05); only metadata consumers are repointed. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate required. |

No new auth/network/secret surface (97-RESEARCH Security Domain). ASVS L1: no high-severity threats — proceed.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_player_*.py tests/test_aa_siblings.py tests/test_url_helpers.py -x --tb=short` — green (playback + AA-canonical + accessor).
- `grep -rn "streams\[0\].url" musicstreamer/` — only non-D-07 keep-list hits remain.
- `git diff --name-only -- musicstreamer/player.py` — empty (D-05).
</verification>

<success_criteria>
- All 5 D-07 metadata consumers key off canonical_url (D-07).
- Playback (preferred_stream_id / order_streams) unchanged (D-05).
- Non-D-07 streams[0] reads preserved.
- AA-canonical sibling test, player suite, url_helpers suite all green.
</success_criteria>

<output>
Create `.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-04-SUMMARY.md` when done
</output>
