---
phase: 97-resolve-station-url-duplication-between-the-top-level-standa
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - musicstreamer/models.py
  - musicstreamer/repo.py
autonomous: true
requirements: [D-04, D-05, D-07]
must_haves:
  truths:
    - "stations.canonical_stream_id column exists, INTEGER, nullable, no default, with ON DELETE SET NULL FK to station_streams(id)"
    - "db_init backfills canonical_stream_id to each station's position-1 stream idempotently"
    - "repo.set_canonical_stream(station_id, stream_id) persists the marker via a dedicated single-column UPDATE (never via update_station)"
    - "Station.canonical_url resolves the FK, falls back to position-1, then to '' "
    - "Every Station(...) builder in repo.py carries canonical_stream_id from the row"
  artifacts:
    - path: "musicstreamer/models.py"
      provides: "Station.canonical_stream_id field + canonical_url property"
      contains: "canonical_stream_id"
    - path: "musicstreamer/repo.py"
      provides: "ALTER + backfill + set_canonical_stream + builder threading"
      contains: "def set_canonical_stream"
  key_links:
    - from: "musicstreamer/repo.py db_init"
      to: "stations.canonical_stream_id"
      via: "ALTER TABLE after the last Phase-96 ALTER (after the legacy URL rebuild block)"
      pattern: "ADD COLUMN canonical_stream_id"
    - from: "musicstreamer/repo.py Station builders"
      to: "Station.canonical_stream_id"
      via: "canonical_stream_id=r[\"canonical_stream_id\"]"
      pattern: "canonical_stream_id=r\\["
---

<objective>
Add the data-layer single source of truth for the canonical (metadata anchor)
stream: the stations.canonical_stream_id FK (D-04), an idempotent ALTER + position-1
backfill, a dedicated set_canonical_stream setter (D-04, Pitfall 1), the
Station.canonical_stream_id field + Station.canonical_url accessor (D-05/D-07), and
threading the column through every Station builder in repo.py.

Purpose: Establish the persisted canonical marker that both the dialog (Plan 03) and
the external metadata consumers (Plan 04) build on. Playback (preferred_stream_id)
is untouched (D-05).
Output: models.py + repo.py changes turning the Plan-01 repo tests and the
canonical_url accessor test GREEN.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-CONTEXT.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing analogs to mirror EXACTLY (from 97-PATTERNS.md, verified). -->

models.py Station fields (mirror preferred_stream_id at line 41):
  preferred_stream_id: Optional[int] = None  # Phase 82 D-01
  canonical_stream_id: Optional[int] = None  # NEW Phase 97 D-04

repo.py set_preferred_stream (814-820) — exact setter shape to copy.
repo.py preferred_stream_id ALTER (282-295) — exact ALTER shape.
repo.py last Phase-96 ALTER block ends ~line 366 — canonical ALTER lands AFTER it.
repo.py legacy URL rebuild block (208-265) — canonical ALTER MUST land AFTER this (Pitfall 2).
repo.py Station builders to update: list_stations (~690-710), get_station (~733-753),
  list_recently_played, list_favorite_stations, list_flagged_stations_for_provider —
  add canonical_stream_id=r["canonical_stream_id"] right after preferred_stream_id=...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Station.canonical_stream_id field + canonical_url property (models.py)</name>
  <behavior>
    - test_canonical_url_resolves_fk_with_position1_fallback (Plan 01): FK match returns that stream's url even when not position 1; None => position-1; stale FK => position-1; no streams => "".
    - Field default is None on a freshly constructed Station.
  </behavior>
  <read_first>
    - musicstreamer/models.py (Station dataclass 28-48; StationStream 13-24 — fields id, url, position; confirm @dataclass has no frozen=True)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (models.py section — exact field line + canonical_url property body)
  </read_first>
  <files>musicstreamer/models.py</files>
  <action>
    Add `canonical_stream_id: Optional[int] = None  # Phase 97 D-04: metadata anchor stream (separate from playback preferred)` immediately AFTER the preferred_stream_id field (line 41) so it mirrors the established per-Phase field pattern. Then add a `@property def canonical_url(self) -> str` at the bottom of the Station dataclass implementing the four-branch resolution from 97-PATTERNS.md: (1) if streams empty return ""; (2) if canonical_stream_id is not None, return the matching stream's url; (3) else sort streams by (position, id) and return first url; (4) return "" if still empty. Use plain docstring per CONVENTIONS.md and cite Phase 97 D-05/D-07. Do NOT make Station frozen. Do NOT add canonical_url to any __init__/repr — it is a computed property.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_url_helpers.py -k canonical_url_resolves -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canonical_stream_id" musicstreamer/models.py` >= 1
    - `grep -c "def canonical_url" musicstreamer/models.py` == 1
    - `.venv/bin/python -m pytest tests/test_url_helpers.py -k canonical_url_resolves -x` exits 0 (the 4-branch accessor test is GREEN)
  </acceptance_criteria>
  <done>Station carries canonical_stream_id (default None) and canonical_url resolves FK -> position-1 -> "".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: repo.py ALTER + idempotent position-1 backfill + set_canonical_stream setter</name>
  <behavior>
    - test_canonical_stream_id_migration_idempotent: db_init twice more does not raise; column INTEGER, nullable, no default.
    - test_canonical_stream_id_backfill_defaults_position1: existing multi-stream station gets canonical = position-1 stream after db_init.
    - test_canonical_stream_id_default_none_on_fresh_station: streamless station => None.
    - test_set_canonical_stream_round_trip: setter persists, visible via list_stations + get_station.
    - test_canonical_stream_id_on_delete_set_null_when_stream_deleted: deleting the canonical stream nulls the FK.
  </behavior>
  <read_first>
    - musicstreamer/repo.py (preferred_stream_id ALTER 282-295; legacy URL rebuild block 208-265; last Phase-96 ALTER block ~336-366; set_preferred_stream 814-820; the Phase-96 setters 1023-1052 incl. the Pitfall-1 docstring; PRAGMA foreign_keys=ON in db_connect)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (repo.py section 1 ALTER+backfill block, section 2 setter)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md (Pitfall 1, Pitfall 2, Pitfall 3 — sweep_orphans needs NO change)
  </read_first>
  <files>musicstreamer/repo.py</files>
  <action>
    In db_init, AFTER the last Phase-96 ALTER block (~line 366) and unambiguously AFTER the legacy URL rebuild block (208-265, Pitfall 2): add the canonical_stream_id ALTER using the try/con.execute("ALTER TABLE stations ADD COLUMN canonical_stream_id INTEGER REFERENCES station_streams(id) ON DELETE SET NULL")/con.commit()/except sqlite3.OperationalError: pass idempotent idiom. Copy the Pitfall-2 explanatory comment block from 97-PATTERNS.md verbatim. Immediately after, add the one-time backfill UPDATE: set canonical_stream_id to the (SELECT id FROM station_streams WHERE station_id = stations.id ORDER BY position ASC, id ASC LIMIT 1) WHERE canonical_stream_id IS NULL AND EXISTS(SELECT 1 FROM station_streams WHERE station_id = stations.id); con.commit(). The NULL guard makes it idempotent and never clobbers a user-set canonical.
    Add def set_canonical_stream(self, station_id: int, stream_id: Optional[int]) -> None near set_preferred_stream (after line 820): a dedicated single-column UPDATE stations SET canonical_stream_id = ? WHERE id = ? + self.con.commit(). Copy the Pitfall-1 docstring verbatim ("Not routed through update_station ..."). Do NOT add canonical_stream_id to update_station (Pitfall 1). Do NOT touch sweep_orphans (Pitfall 3 — ON DELETE SET NULL handles it).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py -k canonical -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN canonical_stream_id" musicstreamer/repo.py` == 1
    - `grep -c "def set_canonical_stream" musicstreamer/repo.py` == 1
    - `grep -v '^#' musicstreamer/repo.py | grep -c "canonical_stream_id.*update_station\|update_station.*canonical_stream_id"` == 0 (setter NOT routed through update_station)
    - `.venv/bin/python -m pytest tests/test_repo.py -k canonical -x` exits 0 (all 5 repo canonical tests GREEN)
  </acceptance_criteria>
  <done>Column + backfill + setter exist, idempotent, FK ON DELETE SET NULL active; all 5 repo canonical tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Thread canonical_stream_id through every Station builder in repo.py</name>
  <read_first>
    - musicstreamer/repo.py (list_stations builder ~690-710; get_station ~733-753; and grep for every `Station(` construction site: list_recently_played, list_favorite_stations, list_flagged_stations_for_provider)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (repo.py section 3 — builder threading; shows preferred_stream_id placement)
  </read_first>
  <files>musicstreamer/repo.py</files>
  <action>
    For EVERY `Station(...)` constructor call in repo.py, add `canonical_stream_id=r["canonical_stream_id"],` immediately after the existing `preferred_stream_id=r["preferred_stream_id"],` line. Find all sites with grep for `Station(` and for `preferred_stream_id=r`. Confirm each SELECT that feeds these builders uses `s.*` or otherwise includes the new column (the preferred_stream_id rows already prove the SELECT carries dynamically-added stations columns; canonical_stream_id rides the same `s.*`). Do not invent new SELECT column lists — match how preferred_stream_id is already read from the row.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py -k "canonical or preferred_stream" -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - The count of `canonical_stream_id=r[` equals the count of `preferred_stream_id=r[` in repo.py: `[ "$(grep -c 'canonical_stream_id=r\[' musicstreamer/repo.py)" = "$(grep -c 'preferred_stream_id=r\[' musicstreamer/repo.py)" ]` is true
    - `.venv/bin/python -m pytest tests/test_repo.py -k "canonical or preferred_stream" -x` exits 0 (round-trip via list_stations AND get_station both pass; no preferred-stream regression)
  </acceptance_criteria>
  <done>canonical_stream_id is populated by all Station builders; round-trip tests via both list_stations and get_station pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> Repo SQL | station_id / stream_id integers reach the SQLite layer via the new setter and backfill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02 | Tampering (SQL injection) | repo.set_canonical_stream / backfill UPDATE | mitigate | All values bound via `?` placeholders (mirrors set_preferred_stream); zero string interpolation of IDs. |
| T-97-03 | Information disclosure / integrity | canonical_stream_id FK referencing a deleted stream | mitigate | ON DELETE SET NULL FK + position-1 fallback in canonical_url ensure a dangling FK can never point at another station's stream; SQLite enforces with PRAGMA foreign_keys=ON (db_connect). |
| T-97-04 | Denial of service | backfill UPDATE on db_init | accept | Single bounded UPDATE guarded by `WHERE canonical_stream_id IS NULL`; runs once, idempotent, local SQLite — negligible. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (97-RESEARCH.md). No legitimacy gate required. |

No new auth, network, or secret surface (97-RESEARCH.md Security Domain). ASVS L1: no high-severity threats — proceed.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_repo.py -k canonical -x --tb=short` — all 5 repo canonical tests GREEN.
- `.venv/bin/python -m pytest tests/test_url_helpers.py -k canonical_url_resolves -x` — accessor GREEN.
- `.venv/bin/python -m pytest tests/test_repo.py -x --tb=short` — no regression in the broader repo suite (preferred_stream, migration, CRUD all still pass).
</verification>

<success_criteria>
- canonical_stream_id column added idempotently with ON DELETE SET NULL.
- Backfill defaults canonical to position-1 per station, idempotent.
- set_canonical_stream is a dedicated setter, never in update_station.
- Station.canonical_url resolves FK -> position-1 -> "".
- All builders carry the column; no preferred_stream_id regression.
</success_criteria>

<output>
Create `.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-02-SUMMARY.md` when done
</output>
