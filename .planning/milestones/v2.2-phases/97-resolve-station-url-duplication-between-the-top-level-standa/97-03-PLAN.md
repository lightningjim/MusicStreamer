---
phase: 97-resolve-station-url-duplication-between-the-top-level-standa
plan: 03
type: execute
wave: 3
depends_on: ["97-01", "97-02"]
files_modified:
  - musicstreamer/ui_qt/edit_station_dialog.py
  - tests/test_edit_station_dialog.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06]
must_haves:
  truths:
    - "The top URL: field (url_edit) and its timer wiring are gone from the dialog (D-01)"
    - "The streams table has a trailing canonical marker column (_COL_CANONICAL=6) with exactly-one-selected, default-first, pinned across reorder/add/remove (D-04)"
    - "Metadata/derivation reads the live (unsaved) canonical row URL cell via _get_canonical_url_live(); cellChanged drives the same debounced refresh url_edit.textChanged used to (D-02)"
    - "An editable primary row auto-creates on empty/new station / dialog open (D-03)"
    - "Save persists canonical_stream_id via repo.set_canonical_stream after reorder_streams, with the canonical-stream-deleted fallback (D-04)"
    - "Canonical-vs-preferred divergence is allowed silently — no warning UI added (D-06)"
  artifacts:
    - path: "musicstreamer/ui_qt/edit_station_dialog.py"
      provides: "canonical marker column, _get_canonical_url_live, cellChanged wiring, save persist, auto-row; url_edit removed"
      contains: "_COL_CANONICAL"
    - path: "tests/test_edit_station_dialog.py"
      provides: "updated ~14 url_edit-referencing tests + 7 Plan-01 canonical tests GREEN"
      contains: "_get_canonical_url_live"
  key_links:
    - from: "streams_table.cellChanged"
      to: "_on_canonical_cell_changed"
      via: "guarded by _populating flag + early-exit unless row==_canonical_row and col==_COL_URL"
      pattern: "cellChanged.connect"
    - from: "_on_save"
      to: "repo.set_canonical_stream"
      via: "after reorder_streams, with stream-deleted fallback to ordered_ids[0]"
      pattern: "set_canonical_stream"
    - from: "15 url_edit.text() sites"
      to: "_get_canonical_url_live()"
      via: "N-to-1 rewire"
      pattern: "_get_canonical_url_live"
---

<objective>
Rewire EditStationDialog to make the streams table the SOLE URL editor: remove the
top url_edit field entirely (D-01), add a pinned canonical marker column (D-04), read
the live canonical cell for all metadata/derivation (D-02), auto-create a primary row
on empty/new stations (D-03), and persist canonical_stream_id on save (D-04). Update
all ~14 existing url_edit-referencing tests and turn the 7 Plan-01 dialog tests GREEN.

Purpose: Eliminate the duplicate-URL UI surface that forces double edits. No
canonical-vs-preferred warning UI (D-06). Playback path is not touched here.
Output: edit_station_dialog.py rewired + test_edit_station_dialog.py updated/GREEN.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-CONTEXT.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md
@.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- From Plan 02 (data layer), now available: -->
Station.canonical_stream_id: Optional[int]
Station.canonical_url -> str
Repo.set_canonical_stream(station_id, stream_id)

<!-- Existing dialog anatomy to modify (97-PATTERNS.md / 97-RESEARCH.md, verified): -->
Column constants 278-283: _COL_URL=0 .. _COL_AUDIO_QUALITY=5  (add _COL_CANONICAL=6 trailing)
streams_table init 544-574: QTableWidget(0, 6) + 6 header labels
_add_stream_row 1102-1137: setItem per column; url_item.setData(Qt.UserRole, stream_id)
_on_move_up/_on_move_down/_swap_rows 1149-1169
url_edit def + _url_timer wiring 431-444 (REMOVE url_edit; KEEP _url_timer, rewire)
_populate streams seeding 650-652 (REMOVE url_edit.setText)
_snapshot_form_state 752-762 ("url" key -> "canonical_url")
_on_save stream-persist tail ~1872-1878 (add set_canonical_stream after reorder_streams)

15 url_edit.text() read sites (97-RESEARCH.md Full Call-Site Inventory):
  754, 832, 1057, 1318, 1334, 1350, 1410, 1526, 1684, 1789, 1916 (+ doc comment 792)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical marker column + _get_canonical_url_live + cellChanged wiring + auto-row; KEEP _url_timer</name>
  <behavior>
    - test_url_edit_widget_does_not_exist: hasattr(dialog,"url_edit") is False.
    - test_canonical_marker_defaults_to_row_0: _canonical_row == 0 on a multi-stream station with no stored canonical.
    - test_metadata_reads_canonical_cell_live: _get_canonical_url_live() returns the live unsaved canonical row URL cell text.
    - test_auto_create_primary_row_on_empty_station: streamless station => rowCount()==1, _canonical_row==0.
    - test_canonical_marker_stays_pinned_after_reorder: after moving canonical content to row 1, _canonical_row==1 and same stream_id stays canonical.
    - test_dirty_state_captures_canonical_url_not_url_edit: _snapshot_form_state has key "canonical_url", not "url".
  </behavior>
  <read_first>
    - musicstreamer/ui_qt/edit_station_dialog.py (column constants 278-283; streams_table init 544-574; _add_stream_row 1102-1137; _on_move_up/_on_move_down/_swap_rows 1149-1169; url_edit def 431-444; _populate 650-652; _snapshot_form_state 752-762)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (edit_station_dialog.py sections 1-7 — exact insert points)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md (Pitfall 4 reorder tracking, Pitfall 5 cellChanged filter, Pitfall 7 dirty-state, Pitfall 8 manual single-select, A1 _populating guard)
  </read_first>
  <files>musicstreamer/ui_qt/edit_station_dialog.py</files>
  <action>
    Add `_COL_CANONICAL = 6` to the column-constant block (trailing per RESEARCH Open-Q1 so existing _COL_URL=0 references stay valid). Bump streams_table to QTableWidget(0, 7), add a 7th header label "Primary", set _COL_CANONICAL resize mode Fixed + width ~50 + header tooltip ("Mark as canonical (metadata anchor) stream. Stays pinned through reordering."). Add instance attrs `self._canonical_row: int = -1` and `self._populating: bool = False`.
    Remove the url_edit QLineEdit, its `form.addRow("URL:", self.url_edit)`, and its `url_edit.textChanged.connect(...)` (D-01). KEEP self._url_timer (the QTimer) and its timeout connection — only its trigger source changes.
    In _add_stream_row, after the existing setItem calls, add a canonical-marker QToolButton (text "★", setCheckable(True), setAutoRaise(True), tooltip), setChecked(True) iff row==self._canonical_row, connect clicked -> self._on_canonical_btn_clicked(row) via a bound lambda capturing r=row, and setCellWidget(row, _COL_CANONICAL, btn). Use MANUAL single-selection (no QButtonGroup, Pitfall 8): add _on_canonical_btn_clicked(row) that unchecks all other rows' canonical buttons, checks this one, sets self._canonical_row=row, and fires the debounced refresh (restart _url_timer, clear logo status, update url-gated controls — mirror the body of the old _on_url_text_changed).
    Add _get_canonical_url_live(self)->str reading streams_table.item(self._canonical_row, _COL_URL).text() with bounds guard returning "" (97-PATTERNS Pattern 3 / RESEARCH).
    Connect streams_table.cellChanged -> _on_canonical_cell_changed(row,col); that handler EARLY-EXITS unless `not self._populating and row==self._canonical_row and col==_COL_URL` (Pitfall 5), else runs the old _on_url_text_changed body. Set self._populating=True around the _populate streams loop and around programmatic _add_stream_row setItem so populate does not spuriously fire the debounce (A1).
    In _populate: remove the `url_edit.setText(streams[0].url)` line; after the streams loop set _canonical_row from station.canonical_stream_id (scan rows matching Qt.UserRole stream_id, default 0); if rowCount()==0 call _add_stream_row() and set _canonical_row=0 (D-03 auto-row). Re-check the row-0 canonical button accordingly.
    In _swap_rows: also swap the _COL_CANONICAL cell WIDGETS (takeItem/setItem do not move setCellWidget widgets — use cellWidget/setCellWidget swap). In _on_move_up/_on_move_down: after the swap, update self._canonical_row to follow the content (if ==row -> new_row and the inverse) per Pitfall 4.
    In _snapshot_form_state: replace `"url": self.url_edit.text()` with `"canonical_url": self._get_canonical_url_live()` (Pitfall 7).
    Add NO warning/guard UI for canonical-vs-preferred divergence (D-06).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_edit_station_dialog.py -k "url_edit_widget or canonical_marker or metadata_reads_canonical or auto_create_primary or dirty_state_captures" -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_COL_CANONICAL" musicstreamer/ui_qt/edit_station_dialog.py` >= 4 (constant + header + add_row + swap)
    - `grep -c "def _get_canonical_url_live" musicstreamer/ui_qt/edit_station_dialog.py` == 1
    - `grep -c "self.url_edit" musicstreamer/ui_qt/edit_station_dialog.py` == 0 (D-01: url_edit fully removed)
    - `grep -c "self._url_timer" musicstreamer/ui_qt/edit_station_dialog.py` >= 2 (timer kept, rewired)
    - `grep -c "cellChanged.connect" musicstreamer/ui_qt/edit_station_dialog.py` >= 1
    - `.venv/bin/python -m pytest tests/test_edit_station_dialog.py -k "url_edit_widget or canonical_marker or metadata_reads_canonical or auto_create_primary or dirty_state_captures" -x` exits 0 (6 of 7 Plan-01 dialog tests GREEN; save test handled in Task 2)
  </acceptance_criteria>
  <done>url_edit gone, canonical column + live accessor + cellChanged + auto-row + reorder-pinning + dirty-state all in place; 6 Plan-01 dialog tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire the 15 url_edit.text() reads to _get_canonical_url_live + persist canonical_stream_id on save</name>
  <behavior>
    - test_save_persists_canonical_stream_id: a save calls repo.set_canonical_stream once with the canonical stream's id; if the canonical stream was deleted, falls back to ordered_ids[0].
    - Metadata reactions (avatar gate, sibling refresh, YT scan toggle, Twitch derive, logo fetch) now read the canonical cell, not url_edit.
  </behavior>
  <read_first>
    - musicstreamer/ui_qt/edit_station_dialog.py (the 15 read sites: 754 already done in Task 1, 832, 1057, 1318, 1334, 1350, 1410, 1526, 1684, 1789, 1916; _on_save tail ~1872-1878; how ordered_ids + Qt.UserRole stream_id are read in the save loop ~1848)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md (Full Call-Site Inventory table; Pitfall 6 prune/ON DELETE resync; _on_save canonical persist example)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (edit_station_dialog.py section 8 _on_save canonical persist)
  </read_first>
  <files>musicstreamer/ui_qt/edit_station_dialog.py</files>
  <action>
    Replace each remaining `self.url_edit.text()` / `self.url_edit.text().strip()` read with `self._get_canonical_url_live()` (apply .strip() where the original did) at sites 832 (_refresh_siblings), 1057 (_on_add_sibling_clicked live_url), 1318 (avatar btn gate — now inside _on_canonical_cell_changed/_on_canonical_btn_clicked path; ensure the gate logic reads the canonical cell), 1334 (_on_live_resync_toggled YT gate), 1350 (_on_url_timer_timeout logo fetch), 1410 (avatar fetch block), 1526 (_on_logo_fetched error path), 1684 (_on_refresh_avatar_clicked), 1789 (_on_save Twitch provider derive — D-07), 1916 (_on_save sync avatar fetch). Update the doc comment at 792. After this, grep for self.url_edit must return 0.
    In _on_save, AFTER `repo.reorder_streams(station.id, ordered_ids)` (and after prune_streams), add the canonical persist block (97-PATTERNS section 8 / RESEARCH): read `_can_item = table.item(self._canonical_row, _COL_URL) if self._canonical_row >= 0 else None`; `_can_stream_id = _can_item.data(Qt.UserRole) if _can_item else None`; if `_can_stream_id not in ordered_ids` set `_can_stream_id = ordered_ids[0] if ordered_ids else None` (Pitfall 6 — canonical stream deleted); call `repo.set_canonical_stream(station.id, _can_stream_id)`. Place it BEFORE the Phase-96 setters block so it sits with the other dedicated setters. Do NOT add canonical_stream_id to update_station (Pitfall 1).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_edit_station_dialog.py -k "save_persists_canonical or save_calls_repo" -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "self.url_edit" musicstreamer/ui_qt/edit_station_dialog.py` == 0 (all 15 reads + defs gone)
    - `grep -c "_get_canonical_url_live" musicstreamer/ui_qt/edit_station_dialog.py` >= 9 (def + the rewired call sites)
    - `grep -c "set_canonical_stream" musicstreamer/ui_qt/edit_station_dialog.py` == 1 (save persist)
    - `.venv/bin/python -m pytest tests/test_edit_station_dialog.py -k "save_persists_canonical or save_calls_repo" -x` exits 0
  </acceptance_criteria>
  <done>Zero url_edit references remain; metadata reads the canonical cell; save persists canonical_stream_id with deleted-stream fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Update the ~14 existing url_edit-referencing tests + full dialog suite green</name>
  <read_first>
    - tests/test_edit_station_dialog.py (the url_edit.setText sites at lines 515, 574, 587, 595, 601, 885, 1956, 1965, 1973, 2123, 2148, 2168, 2256, 2280, 2287 per RESEARCH; test_save_calls_repo_correctly 214-229; test_is_dirty_after_url_edit 885-888)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-RESEARCH.md ("Existing tests that will break and need updating" table)
    - .planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-PATTERNS.md (tests/test_edit_station_dialog.py replacement examples)
  </read_first>
  <files>tests/test_edit_station_dialog.py</files>
  <action>
    Replace every `dialog.url_edit.setText(X)` with a canonical-cell edit: set `dialog.streams_table.item(dialog._canonical_row, _COL_URL).setText(X)` (creating the item via QTableWidgetItem if None, per the 97-PATTERNS replacement of test_is_dirty_after_url_edit). Replace every `dialog.url_edit.text()` read assertion with `dialog._get_canonical_url_live()`. In test_save_calls_repo_correctly add `repo.set_canonical_stream.assert_called_once()` (FakeRepo/MagicMock must expose set_canonical_stream — add it to FakeRepo if the suite uses one). Since _COL_CANONICAL is trailing (=6), _COL_URL stays 0 so literal column-0 reads are UNAFFECTED — do NOT shift them. Ensure FakeRepo (if present) returns a Station carrying canonical_stream_id and that get_station/list_streams stubs are consistent. After edits the WHOLE dialog test file must pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_edit_station_dialog.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "url_edit" tests/test_edit_station_dialog.py` == 0 except inside the drift-guard test name/string (test_url_edit_widget_does_not_exist) — i.e. no `dialog.url_edit.` access remains
    - `.venv/bin/python -m pytest tests/test_edit_station_dialog.py -x` exits 0 (entire dialog suite GREEN, including all 7 Plan-01 canonical tests)
  </acceptance_criteria>
  <done>All existing url_edit tests migrated to canonical-cell edits; full test_edit_station_dialog.py passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes -> canonical URL cell | The URL the user types in the canonical row feeds metadata/derivation (avatar, provider derive, YT scan) live. |
| dialog -> Repo | canonical stream_id (Qt.UserRole int) crosses into repo.set_canonical_stream on save. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05 | Input validation | live canonical URL cell feeding metadata workers | accept | No NEW validation surface — url_helpers already validates AA/YT URL patterns at the worker boundary (97-RESEARCH Security Domain V5); this phase only changes WHERE the same URL is read from. |
| T-97-06 | Tampering | _on_save canonical persist with deleted/stale stream_id | mitigate | Fallback: if canonical stream_id not in ordered_ids, coerce to ordered_ids[0] or None (Pitfall 6); set_canonical_stream binds via `?` and the FK is constrained to this station's streams. |
| T-97-07 | Repudiation/integrity | cellChanged firing during programmatic populate | mitigate | _populating guard + (row==_canonical_row and col==_COL_URL) early-exit prevent spurious metadata fetches (Pitfall 5 / A1). |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate required. |

No new auth/network/secret surface (97-RESEARCH Security Domain). ASVS L1: no high-severity threats — proceed.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_edit_station_dialog.py -x --tb=short` — full dialog suite GREEN (existing + 7 canonical + drift-guard).
- `grep -c "self.url_edit" musicstreamer/ui_qt/edit_station_dialog.py` == 0.
- Manual (per 97-VALIDATION Manual-Only): in the live app, the canonical star single-selects, stays pinned across reorder, and avatar/provider/scan react as the canonical cell is typed.
</verification>

<success_criteria>
- url_edit removed; streams table is the sole URL editor (D-01).
- Canonical marker column: one-selected, default-first, pinned across reorder/add/remove (D-04).
- Metadata reads the live canonical cell; cellChanged drives the debounce (D-02).
- Auto-row on empty/new station (D-03).
- Save persists canonical_stream_id with deleted-stream fallback (D-04).
- No divergence warning UI (D-06).
- Full dialog test suite green.
</success_criteria>

<output>
Create `.planning/phases/97-resolve-station-url-duplication-between-the-top-level-standa/97-03-SUMMARY.md` when done
</output>
