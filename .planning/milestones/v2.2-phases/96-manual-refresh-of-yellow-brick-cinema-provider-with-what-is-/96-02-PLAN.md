---
phase: 96
plan: 02
type: execute
wave: 1
depends_on: ["96-01"]
files_modified:
  - musicstreamer/repo.py
  - musicstreamer/models.py
autonomous: true
requirements: [D-01, D-03, D-04, D-06]
must_haves:
  truths:
    - "Existing DBs gain live_url_syncs_from_channel (DEFAULT 0), live_url_title_anchor (nullable), and providers.channel_scan_url (nullable) idempotently, surviving the legacy URL-column rebuild (D-01/D-03/D-04)"
    - "Station.live_url_syncs_from_channel and Station.live_url_title_anchor round-trip through ALL four Station-building queries (D-01/D-03)"
    - "Repo exposes dedicated single-column setters for the flag, anchor, and provider scan URL, never routed through update_station (D-01/D-03/D-04)"
    - "list_flagged_stations_for_provider returns only stations of that provider with the flag set (D-06)"
  artifacts:
    - path: musicstreamer/repo.py
      provides: "3 additive migrations after line 334 + 3 setters + list_flagged_stations_for_provider + 4 Station-query field additions"
      contains: "def set_live_url_syncs_from_channel"
    - path: musicstreamer/models.py
      provides: "Station.live_url_syncs_from_channel, Station.live_url_title_anchor, Provider.channel_scan_url"
      contains: "live_url_syncs_from_channel"
  key_links:
    - from: musicstreamer/repo.py
      to: musicstreamer/models.py
      via: "Station(...) construction reading r['live_url_syncs_from_channel']"
      pattern: "live_url_syncs_from_channel=bool"
---

<objective>
Lay the Phase 96 data-layer foundation: three additive SQLite migrations (the per-station flag D-01, the title anchor D-03, the per-provider channel scan URL D-04), the corresponding model fields, dedicated single-column setters, and the flagged-station query (D-06). This is the zero-risk prerequisite that unblocks the dialog and edit-flow plans.

Purpose: Without these columns + setters + query, the EditStationDialog flag (Plan 03) and the LiveRefreshDialog apply path (Plan 04) have nothing to persist to or read from. Migration ordering and the four-query update are the two highest-risk landmines (Pitfall 2 + Pitfall 8) and are isolated here.
Output: Migrated schema, model fields, 4 new Repo methods, and 4 updated Station-building queries — Plan 01's repo tests go GREEN.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md

<interfaces>
<!-- Exact source anchors. Extracted from codebase. Use directly — no exploration. -->

repo.py method line anchors:
- db_init additive blocks end ~line 334 (Phase 89A/89.1 avatar ALTERs) — the 3 new Phase 96 ALTERs go IMMEDIATELY AFTER, never before the URL-column rebuild block at ~208-265 (Pitfall 8).
- list_providers() L477-479 — `return [Provider(id=r["id"], name=r["name"]) for r in rows]` ; SELECT is `SELECT id, name FROM providers` — MUST add channel_scan_url to both SELECT and Provider(...).
- list_streams() L490-498 ; update_stream() L564-572 (full-row replace).
- list_stations() L641-673 ; get_station() L682-712 ; list_recently_played() L792-825 ; list_favorite_stations() L911-942 — ALL FOUR build Station(...) and MUST gain the two new fields (Pitfall 2).
- create_station() L675-680 ; delete_station() L714-716 ; insert_station() L886-896 ; station_exists_by_url() L880-884 (do NOT modify; referenced by Plan 04).
- Existing setter analog: update_channel_avatar_path / update_provider_avatar_path L951-976 (the exact shape for the 3 new setters).

Station(...) constructor tail (get_station L702-711) currently ends with:
  channel_avatar_path=r["channel_avatar_path"], provider_avatar_path=r["provider_avatar_path"]
Append: live_url_syncs_from_channel=bool(r["live_url_syncs_from_channel"]), live_url_title_anchor=r["live_url_title_anchor"]

models.py Station dataclass tail (L27-44) ends with provider_avatar_path: Optional[str] = None
models.py Provider dataclass (L5-8): id:int, name:str
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three additive migrations + model fields (D-01/D-03/D-04)</name>
  <files>musicstreamer/repo.py, musicstreamer/models.py</files>
  <read_first>
    - musicstreamer/repo.py around lines 208-265 (the legacy URL-column rebuild block — the ordering hazard) and 313-334 (the Phase 89A/89.1 additive ALTER blocks the new ones must follow)
    - musicstreamer/models.py lines 5-8 (Provider) and 27-44 (Station dataclass tail)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ Additive Column Migrations, lines 220-278; § New Station/Provider Fields, lines 484-529)
  </read_first>
  <behavior>
    - test_live_url_syncs_from_channel_migration_idempotent: stations.live_url_syncs_from_channel INTEGER NOT NULL DEFAULT 0 after db_init x2
    - test_live_url_title_anchor_migration_idempotent: stations.live_url_title_anchor TEXT nullable
    - test_provider_channel_scan_url_migration_idempotent: providers.channel_scan_url TEXT nullable
  </behavior>
  <action>
    In repo.py db_init(), IMMEDIATELY AFTER the last existing additive block (~line 334, the Phase 89.1 providers.avatar_path ALTER), add three idempotent `try: con.execute("ALTER TABLE ...") ; con.commit() except sqlite3.OperationalError: pass` blocks in this exact order, each with a comment citing the decision and the Pitfall-8 ordering rule:
    1. `ALTER TABLE stations ADD COLUMN live_url_syncs_from_channel INTEGER NOT NULL DEFAULT 0` (D-01).
    2. `ALTER TABLE stations ADD COLUMN live_url_title_anchor TEXT` (D-03, nullable, no DEFAULT).
    3. `ALTER TABLE providers ADD COLUMN channel_scan_url TEXT` (D-04, nullable; providers has no rebuild block).
    These MUST land after the URL-column rebuild block — placing them before ~line 265 causes the CREATE TABLE stations_new / INSERT SELECT to silently drop them (Pitfall 8). Do NOT touch the rebuild block.
    In models.py: append to the Station dataclass (after provider_avatar_path) `live_url_syncs_from_channel: bool = False` (D-01) and `live_url_title_anchor: Optional[str] = None` (D-03); append to Provider `channel_scan_url: Optional[str] = None` (D-04). New defaulted fields go after all existing fields (dataclass rule).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py -k "migration_idempotent" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The three new ALTER blocks appear AFTER the Phase 89.1 block (~L334) in db_init source — confirm via `grep -n "live_url_syncs_from_channel\|live_url_title_anchor\|channel_scan_url" musicstreamer/repo.py` showing the ALTERs at line numbers greater than the URL-rebuild block.
    - `test_live_url_syncs_from_channel_migration_idempotent`, `test_live_url_title_anchor_migration_idempotent`, `test_provider_channel_scan_url_migration_idempotent` all PASS.
    - models.py defines all three new fields with defaults; `.venv/bin/python -c "from musicstreamer.models import Station, Provider; Station(0,'n',None,None,'',None,None); Provider(0,'n')"` runs without error.
  </acceptance_criteria>
  <done>Three idempotent migrations placed after the rebuild block; model fields added; migration-idempotency tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Setters, flagged-station query, and four-query field carry (D-01/D-03/D-04/D-06)</name>
  <files>musicstreamer/repo.py</files>
  <read_first>
    - musicstreamer/repo.py lines 951-976 (update_channel_avatar_path / update_provider_avatar_path — the dedicated-setter template, Pitfall 1)
    - musicstreamer/repo.py lines 477-479 (list_providers + Provider construction), 641-673 (list_stations), 682-712 (get_station), 792-825 (list_recently_played), 911-942 (list_favorite_stations)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ New Single-Column Setter Methods 282-343; § list_flagged_stations_for_provider 347-414)
  </read_first>
  <behavior>
    - test_live_url_syncs_from_channel_round_trip / test_live_url_title_anchor_round_trip: setter writes, get_station reads back
    - test_station_live_flag_loaded_from_db: flag visible via BOTH get_station and list_stations
    - test_list_flagged_stations_for_provider: only flagged stations of the provider returned
  </behavior>
  <action>
    Add three dedicated single-column setters following the update_channel_avatar_path shape (each does one UPDATE + commit; NEVER routed through update_station — Pitfall 1):
    - `set_live_url_syncs_from_channel(self, station_id: int, value: bool)` → `UPDATE stations SET live_url_syncs_from_channel = ? WHERE id = ?` with `(int(value), station_id)` (D-01).
    - `set_live_url_title_anchor(self, station_id: int, title: Optional[str])` → cap title at 500 chars before persist (security T-96-03), `UPDATE stations SET live_url_title_anchor = ?` (D-03).
    - `set_provider_channel_scan_url(self, provider_id: int, url: Optional[str])` → `UPDATE providers SET channel_scan_url = ?` (D-04).
    Add `list_flagged_stations_for_provider(self, provider_id: int) -> List[Station]` mirroring list_recently_played's Station-building loop but `WHERE s.provider_id = ? AND s.live_url_syncs_from_channel = 1 ORDER BY s.name COLLATE NOCASE`, including the two new Phase 96 fields in the Station(...) (D-06).
    Update ALL FOUR existing Station-building queries (list_stations, get_station, list_recently_played, list_favorite_stations) to append `live_url_syncs_from_channel=bool(r["live_url_syncs_from_channel"])` and `live_url_title_anchor=r["live_url_title_anchor"]` to each Station(...) — these queries already `SELECT s.*` so no SELECT change is needed, only the constructor (Pitfall 2).
    Update list_providers: change SELECT to include channel_scan_url and pass `channel_scan_url=r["channel_scan_url"]` into Provider(...) (D-04).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_repo.py -k "round_trip or live_flag_loaded or flagged_stations_for_provider" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def set_live_url_syncs_from_channel\|def set_live_url_title_anchor\|def set_provider_channel_scan_url\|def list_flagged_stations_for_provider" musicstreamer/repo.py` returns all four.
    - All four Station(...) call sites carry the two new fields: `grep -c "live_url_syncs_from_channel=bool" musicstreamer/repo.py` returns 5 (four existing queries + list_flagged_stations_for_provider).
    - Tests test_live_url_syncs_from_channel_round_trip, test_live_url_title_anchor_round_trip, test_station_live_flag_loaded_from_db, test_list_flagged_stations_for_provider PASS.
    - No new positional arg added to update_station (grep its signature unchanged).
  </acceptance_criteria>
  <done>Three setters + flagged-station query added; four Station queries + list_providers carry the new fields; all Plan-01 repo round-trip/query tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yt-dlp scan result → DB (title anchor) | Untrusted stream title persisted as live_url_title_anchor |
| user-entered channel URL → DB | channel_scan_url persisted (validated by caller in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03 | Tampering | set_live_url_title_anchor (oversized/malicious title from yt-dlp) | mitigate | Cap title at 500 chars before persist; parameterized SQL (already used) prevents injection |
| T-96-04 | Tampering | update_station bypass for new columns | mitigate | New columns written ONLY via dedicated setters; update_station signature untouched (Pitfall 1) |
| T-96-05 | Information Disclosure | migration drops columns on rebuild | mitigate | ALTERs land AFTER the URL-column rebuild block (Pitfall 8); idempotency tests assert presence |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (96-RESEARCH.md audit); nothing to gate |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_repo.py -x -q` — all Phase 96 repo tests green (migrations + setters + query).
- `.venv/bin/python -m pytest tests/ -k "repo" -q` — no regression in existing repo tests.
- `grep -n "live_url" musicstreamer/repo.py` confirms ALTERs are positioned after line 334.
</verification>

<success_criteria>
- Schema gains all three columns idempotently and survives the legacy rebuild.
- Station + Provider models expose the new fields with safe defaults.
- Flag/anchor/scan-URL writes go only through dedicated setters; flagged-station query scopes to one provider.
- All Plan-01 repo tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-02-SUMMARY.md` when done.
</output>
