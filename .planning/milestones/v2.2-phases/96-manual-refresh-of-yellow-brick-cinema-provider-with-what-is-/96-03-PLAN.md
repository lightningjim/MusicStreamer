---
phase: 96
plan: 03
type: execute
wave: 2
depends_on: ["96-02"]
files_modified:
  - musicstreamer/ui_qt/edit_station_dialog.py
  - musicstreamer/ui_qt/station_tree_model.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04]
must_haves:
  truths:
    - "EditStationDialog shows a 'Re-sync live URL from channel' checkbox enabled ONLY for youtube.com/youtu.be URLs, disabled for twitch.tv and all others (D-02)"
    - "On save, the flag persists via set_live_url_syncs_from_channel, a title anchor is captured via set_live_url_title_anchor, and the validated companion channel URL persists via set_provider_channel_scan_url — never through update_station (D-01/D-03/D-04)"
    - "The provider _TreeNode carries provider_id so the sidebar can scope a channel refresh (D-04)"
  artifacts:
    - path: musicstreamer/ui_qt/edit_station_dialog.py
      provides: "live-resync checkbox + companion channel-URL field + YouTube-only gate + on-save persistence"
      contains: "_live_resync_checkbox"
    - path: musicstreamer/ui_qt/station_tree_model.py
      provides: "_TreeNode.provider_id field populated from first station of each provider group"
      contains: "provider_id"
  key_links:
    - from: musicstreamer/ui_qt/edit_station_dialog.py
      to: musicstreamer/repo.py
      via: "on-save dedicated setters"
      pattern: "set_live_url_syncs_from_channel|set_provider_channel_scan_url"
    - from: musicstreamer/ui_qt/station_tree_model.py
      to: musicstreamer/models.py
      via: "_populate provider node takes st.provider_id"
      pattern: "provider_id=st.provider_id"
---

<objective>
Wire the per-station opt-in surface (D-01/D-02/D-03) in EditStationDialog and the provider-id plumbing in the tree model (D-04). The user can mark a YouTube station as live-URL-churning, supply the channel scan URL once per provider, and the sidebar tree now knows each provider's id so Plan 05 can open the refresh dialog.

Purpose: This is the entry point that makes a station eligible for refresh. The YouTube-ONLY gate (D-02, distinct from the avatar gate which allows Twitch) and the dedicated-setter persistence (Pitfall 1) are the correctness-critical pieces.
Output: A gated checkbox + companion URL field that persist via the Plan-02 setters; _TreeNode.provider_id populated. Plan-01 gating + provider-id tests go GREEN.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md

<interfaces>
<!-- Exact source anchors. Extracted from codebase. -->

edit_station_dialog.py:
- _on_url_text_changed() L1289-1295 — the avatar gate: `is_yt = "youtube.com" in lower or "youtu.be" in lower`; `is_twitch = "twitch.tv" in lower`; `self._refresh_avatar_btn.setEnabled(is_yt or is_twitch)`. The Phase 96 flag gate appends here, enabled on is_yt ONLY (NOT is_twitch — D-02).
- _on_save / stream-update path L1783-1819 — full-row preserve before update_stream; existing dedicated-setter call sites for avatar persist are the model for adding the flag/anchor/scan-url setters.
- is_yt_playlist_url importable from musicstreamer.yt_import (recognizes @Channel/streams + playlist?list=, NOT watch?v= — used to VALIDATE the companion scan URL, NOT to gate the checkbox; Pitfall 7).

station_tree_model.py:
- _TreeNode dataclass L29-36 — fields: kind,label,parent,children,station,provider_name. Append `provider_id: Optional[int] = None`.
- _populate() L209-233 — provider node creation `grp = _TreeNode(kind="provider", label=pname, parent=self._root, provider_name=pname)`. Add `provider_id=st.provider_id` (first station of group sets it; Ungrouped → None per Pitfall 4).

Plan-02 setters now available: repo.set_live_url_syncs_from_channel, repo.set_live_url_title_anchor, repo.set_provider_channel_scan_url; repo.list_streams(id)[0].label.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _TreeNode.provider_id field + populate (D-04)</name>
  <files>musicstreamer/ui_qt/station_tree_model.py</files>
  <read_first>
    - musicstreamer/ui_qt/station_tree_model.py lines 29-36 (_TreeNode) and 209-233 (_populate provider-group creation) and 81-87 (station_for_index internalPointer pattern)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ station_tree_model, lines 533-595)
  </read_first>
  <behavior>
    - test_tree_node_carries_provider_id: provider node's provider_id == first station's provider_id; Ungrouped node's provider_id is None
  </behavior>
  <action>
    Add `provider_id: Optional[int] = None` to the `_TreeNode` dataclass after `provider_name` (D-04). In `_populate()`, add `provider_id=st.provider_id` to the `_TreeNode(kind="provider", ...)` construction so the first station of each provider group sets the group node's provider_id. Ungrouped stations (provider_id=None) therefore yield a provider node with provider_id None — this is the gate Plan 05 checks before opening the refresh dialog (Pitfall 4). Do not change any other tree behavior.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_tree_model.py::test_tree_node_carries_provider_id -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "provider_id" musicstreamer/ui_qt/station_tree_model.py` shows the dataclass field AND the `provider_id=st.provider_id` populate line.
    - test_tree_node_carries_provider_id PASSES.
    - Existing test_station_tree_model.py tests still pass (no regression).
  </acceptance_criteria>
  <done>_TreeNode.provider_id added and populated; provider-id test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live-resync checkbox, companion URL field, YouTube-only gate, on-save persist (D-01/D-02/D-03/D-04)</name>
  <files>musicstreamer/ui_qt/edit_station_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/edit_station_dialog.py lines 1289-1295 (URL gate to extend), the dialog's form-layout widget construction region (where existing QCheckBox widgets are built), and _on_save L1783-1819 (persistence + existing dedicated-setter call sites)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ edit_station_dialog Flag Checkbox + YouTube URL Gate, lines 672-732)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md (Pitfall 7: gate on "youtube.com" in lower, NOT is_yt_playlist_url; Critical Code Path 4: anchor + scan-url derivation)
  </read_first>
  <behavior>
    - test_live_resync_checkbox_gating: enabled for youtube.com, disabled for twitch.tv, disabled+unchecked for other URLs
  </behavior>
  <action>
    Construct two widgets in the dialog form: `_live_resync_checkbox = QCheckBox("Re-sync live URL from channel")` (disabled by default, gated on YouTube URL per D-02) and `_live_resync_channel_url_edit = QLineEdit()` with placeholder `https://youtube.com/@Channel/streams`, hidden by default. Pre-populate the checkbox from `self._station.live_url_syncs_from_channel` and the URL field from the station's provider channel_scan_url when present (the dialog already has provider context; if not directly available, read via repo by provider_id). Wire the checkbox `toggled` to a small `_on_live_resync_toggled` that toggles the URL field visibility.
    Extend `_on_url_text_changed()` AFTER the existing avatar gate: set `_live_resync_checkbox.setEnabled(is_yt)` where is_yt is the existing youtube.com/youtu.be check (D-02 — YouTube ONLY; do NOT include is_twitch); when not is_yt, uncheck the checkbox; set the companion URL field visible only when `is_yt and _live_resync_checkbox.isChecked()`. Use the existing `is_yt` local — do NOT introduce is_yt_playlist_url for the gate (Pitfall 7).
    In `_on_save()`, after the existing stream persistence, add Phase 96 persistence using the Plan-02 dedicated setters (NEVER update_station — Pitfall 1):
    - `repo.set_live_url_syncs_from_channel(station.id, _live_resync_checkbox.isChecked())` (D-01).
    - When checked: capture anchor = `repo.list_streams(station.id)[0].label or station.name` (D-03) and `repo.set_live_url_title_anchor(station.id, anchor)`.
    - When checked AND companion URL non-empty AND `is_yt_playlist_url(channel_url)` is True AND station.provider_id is not None: `repo.set_provider_channel_scan_url(station.provider_id, channel_url)` (D-04, validate before persist — security T-96-06). If the companion URL is non-empty but fails is_yt_playlist_url, surface the existing error/toast affordance and do not persist that URL.
    Per the Claude's-Discretion note in D-04, do NOT add a separate per-station "Refresh now" button here — the provider context menu (Plan 05) is the refresh surface.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_edit_station_dialog.py::test_live_resync_checkbox_gating -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_live_resync_checkbox_gating PASSES (youtube.com enabled, twitch.tv disabled, other disabled+unchecked).
    - `grep -n "set_live_url_syncs_from_channel\|set_live_url_title_anchor\|set_provider_channel_scan_url\|is_yt_playlist_url" musicstreamer/ui_qt/edit_station_dialog.py` shows the three setters in _on_save and is_yt_playlist_url used ONLY to validate the companion URL (not the checkbox gate).
    - The checkbox gate line uses the youtube.com/youtu.be `is_yt` value WITHOUT `or is_twitch`.
    - Existing edit_station_dialog tests still pass.
  </acceptance_criteria>
  <done>Gated checkbox + validated companion URL field persist via dedicated setters; gating test green; no Twitch in the flag gate; no update_station routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → EditStationDialog (channel scan URL field) | User-entered URL stored as providers.channel_scan_url and later passed to yt-dlp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06 | Tampering / SSRF (limited) | companion channel-URL field | mitigate | Validate with is_yt_playlist_url() before set_provider_channel_scan_url; non-YouTube URLs rejected with a user-visible error and never persisted/scanned |
| T-96-07 | Tampering | flag/anchor/scan-url via update_station | mitigate | Persistence uses ONLY the dedicated Plan-02 setters; update_station untouched (Pitfall 1) |
| T-96-08 | Elevation of Privilege | non-YouTube station enabling churn flag | mitigate | Checkbox disabled (and force-unchecked) unless URL is youtube.com/youtu.be (D-02 YouTube-only gate) |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (96-RESEARCH.md audit) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_edit_station_dialog.py tests/test_station_tree_model.py -x -q` green.
- `.venv/bin/python -m pytest tests/test_repo.py -x -q` still green (no data-layer regression).
- Manual: open EditStationDialog on a youtube.com station → checkbox enabled; paste a twitch.tv URL → checkbox disables and unchecks.
</verification>

<success_criteria>
- YouTube-only gated checkbox + companion URL field present and persisted via dedicated setters.
- Title anchor captured on flag-set; channel scan URL validated then stored per-provider.
- _TreeNode.provider_id populated for provider rows.
- Plan-01 gating + provider-id tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-03-SUMMARY.md` when done.
</output>
