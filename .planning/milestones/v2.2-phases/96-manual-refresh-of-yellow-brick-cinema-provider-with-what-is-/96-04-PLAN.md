---
phase: 96
plan: 04
type: execute
wave: 2
depends_on: ["96-02"]
files_modified:
  - musicstreamer/ui_qt/live_refresh_dialog.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - "The channel re-scan runs off the UI thread on a QThread worker and populates the review dialog on the main thread via a queued Signal, threading node_runtime through to scan_playlist (D-09)"
    - "The dialog shows flagged stations side-by-side with currently-live streams; title-anchor only pre-orders suggestions and NEVER auto-applies a URL change (D-05)"
    - "Apply supports remap-in-place (preserving metadata via update_stream), replacement, drop, and add; drops/adds are unchecked by default and an untouched flagged station is never mutated (D-06/D-07/D-08/D-10)"
  artifacts:
    - path: musicstreamer/ui_qt/live_refresh_dialog.py
      provides: "LiveRefreshDialog(QDialog) + _LiveRefreshScanWorker(QThread) + pure apply helper"
      contains: "class _LiveRefreshScanWorker"
  key_links:
    - from: musicstreamer/ui_qt/live_refresh_dialog.py
      to: musicstreamer/yt_import.py
      via: "_LiveRefreshScanWorker.run → scan_playlist(node_runtime=...)"
      pattern: "scan_playlist\\("
    - from: musicstreamer/ui_qt/live_refresh_dialog.py
      to: musicstreamer/repo.py
      via: "apply path → update_stream / delete_station / insert_station + setters"
      pattern: "update_stream|delete_station|insert_station"
---

<objective>
Build the review-and-confirm refresh dialog (D-05..D-10): a NEW `live_refresh_dialog.py` with `_LiveRefreshScanWorker` (mirrors `_YtScanWorker`) that re-scans the channel off the UI thread, a two-panel review UI (flagged stations vs. currently-live streams), and an apply path that remaps in place / replaces / drops / adds with conservative defaults. No automatic title matching — the user maps every time.

Purpose: This is the real refresh surface (D-04 rationale: drop/replace/add have no single owning station). The apply path must update-in-place via update_stream (bypassing station_exists_by_url — Pitfall 6) and preserve all non-URL stream metadata (Pitfall 5).
Output: A self-contained dialog + worker + a pure apply helper unit-testable without a live event loop. Plan-01 D-05..D-10 dialog tests go GREEN.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.6.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md
@.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md

<interfaces>
<!-- Exact source anchors + contracts. Extracted from codebase. -->

import_dialog.py (the template to copy):
- _YtScanWorker L75-101: `finished = Signal(list)`, `error = Signal(str)`, __init__(url, toast_callback=None, *, node_runtime=None, parent=None); run() calls yt_import.scan_playlist(url, toast_callback=..., node_runtime=...) and emits.
- ImportDialog __init__ L176-203; _on_yt_scan_clicked L338-354 (kick-off: set busy, indeterminate progress, construct worker, connect finished/error with Qt.QueuedConnection, start); _on_yt_scan_complete L356-381 (populate list, setData(Qt.UserRole, entry)); _on_yt_scan_error L383-388 (ERROR_COLOR_HEX status).

yt_import.scan_playlist(url, toast_callback=None, node_runtime=None) -> list[dict] with keys {title, url, provider}. Reuse as-is.

repo (Plan-02 + existing):
- list_flagged_stations_for_provider(provider_id) -> list[Station] (flagged only)
- list_streams(station_id) -> list[StationStream] (sorted by position); primary = position==1
- update_stream(stream_id, url, label, quality, position, stream_type, codec, bitrate_kbps=0, sample_rate_hz=0, bit_depth=0) — FULL-ROW replace (Pitfall 5)
- delete_station(station_id); insert_station(name, url, provider_name, tags) -> int
- set_live_url_title_anchor(station_id, title); set_live_url_syncs_from_channel(station_id, bool)
- DO NOT call station_exists_by_url in the apply path (Pitfall 6)

StationStream fields: id, station_id, url, label, quality, position, stream_type, codec, bitrate_kbps, sample_rate_hz, bit_depth.

Constructor contract (consumed by Plan 05):
LiveRefreshDialog(repo, provider_id: int, provider_name: str, channel_scan_url: str, *, node_runtime=None, toast_callback=None, parent=None); emits `refresh_complete = Signal()` after Apply.

Theme + conventions: from musicstreamer.ui_qt._theme import ERROR_COLOR_HEX; cross-thread signals use Qt.QueuedConnection; QListWidgetItem entry data via Qt.UserRole.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _LiveRefreshScanWorker + LiveRefreshDialog scaffold + two-panel review UI (D-05/D-08/D-09)</name>
  <files>musicstreamer/ui_qt/live_refresh_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/import_dialog.py lines 21-48 (imports), 75-101 (_YtScanWorker), 176-203 (dialog ctor), 338-388 (scan kick-off/complete/error)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-PATTERNS.md (§ _LiveRefreshScanWorker 32-93; § LiveRefreshDialog 97-216; § Shared Patterns 736-799)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md (Pitfall 3 node_runtime; D-05 no-auto-apply; D-08 name pre-population)
  </read_first>
  <behavior>
    - test_scan_worker_uses_qthread: _LiveRefreshScanWorker subclasses QThread, has finished/error Signals, __init__ accepts node_runtime kwarg
    - test_suggestions_pre_order_no_auto_apply: anchor-matching live stream ordered first in a row's suggestions; no staged mapping until user picks
    - test_name_field_prepopulation: ADD row name = scan title; REMAP/REPLACE row name = existing station name
  </behavior>
  <action>
    Create NEW musicstreamer/ui_qt/live_refresh_dialog.py.
    Define `_LiveRefreshScanWorker(QThread)` by copying _YtScanWorker verbatim, renaming the class and storing `node_runtime` in __init__ (kwarg, default None); run() calls `yt_import.scan_playlist(self._url, toast_callback=self._toast, node_runtime=self._node_runtime)` and emits finished(list)/error(str). The node_runtime threading is the landmine — keep the kwarg even when NodeRuntime.available is False (Pitfall 3).
    Define `LiveRefreshDialog(QDialog)` with `refresh_complete = Signal()` and the constructor contract from the interfaces block. On open, load `repo.list_flagged_stations_for_provider(provider_id)` for the LEFT panel; kick off `_LiveRefreshScanWorker(channel_scan_url, node_runtime=...)` for the RIGHT panel (currently-live streams), connecting finished/error with Qt.QueuedConnection and showing an indeterminate progress bar while scanning. On empty flagged list, show the "No stations marked for live URL re-sync — enable the flag via Edit Station" message (RESEARCH Open Question 3). On scan error use ERROR_COLOR_HEX status.
    Build the review UI as row-per-flagged-station with a per-row "map to currently-live" QComboBox whose options are the scan results, plus a per-row editable name QLineEdit and explicit Drop/Add affordances. Implement a pure helper `_build_row_suggestions(anchor, scan_results) -> list` that pre-ORDERS scan results by title-anchor similarity (most-similar first) but returns ALL of them and applies NOTHING (D-05 — anchor only hints; no auto-apply). Pre-populate name fields: ADD rows default to the scan result's title; REMAP/REPLACE rows default to the existing station's current name (D-08). Carry each scan entry dict on its widget via Qt.UserRole.
    Keep run() widget-free (threading discipline). Do NOT auto-select or auto-stage any mapping based on anchor match.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -k "scan_worker_uses_qthread or suggestions_pre_order_no_auto_apply or name_field_prepopulation" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class _LiveRefreshScanWorker\|class LiveRefreshDialog\|def _build_row_suggestions\|refresh_complete" musicstreamer/ui_qt/live_refresh_dialog.py` returns all four.
    - `grep -n "scan_playlist" musicstreamer/ui_qt/live_refresh_dialog.py` shows node_runtime passed through.
    - Tests test_scan_worker_uses_qthread, test_suggestions_pre_order_no_auto_apply, test_name_field_prepopulation PASS.
    - _build_row_suggestions returns all scan results (count preserved), anchor-best first, and stages no mapping.
  </acceptance_criteria>
  <done>Worker + dialog scaffold + anchor pre-ordering (no auto-apply) + name pre-population built; D-05/D-08/D-09 worker tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply path — remap/replace/drop/add with conservative defaults (D-06/D-07/D-10)</name>
  <files>musicstreamer/ui_qt/live_refresh_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/edit_station_dialog.py lines 1783-1819 (full-row-preserve before update_stream — the metadata-preservation pattern)
    - musicstreamer/repo.py lines 564-572 (update_stream full-row), 886-896 (insert_station), 714-716 (delete_station), 490-498 (list_streams)
    - .planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-RESEARCH.md (Critical Code Path 1 update-in-place; Pitfall 5 preserve; Pitfall 6 bypass station_exists_by_url; D-10 conservative defaults)
  </read_first>
  <behavior>
    - test_apply_remap_preserves_metadata: REMAP calls update_stream(position-1) with new URL, preserving label/quality/stream_type/codec/bitrate; updates title anchor
    - test_apply_drop_and_add_actions: DROP→delete_station; ADD→insert_station then set_live_url_syncs_from_channel(True)+set_live_url_title_anchor
    - test_conservative_defaults: DROP+ADD unchecked by default; REMAP checked/active by default (apply calls update_stream); untouched flagged station → no update_stream/no delete_station; empty staged-changes list → zero repo mutations (D-10 empty-apply guard)
  </behavior>
  <action>
    Implement a PURE apply helper (e.g. `apply_refresh(repo, staged_changes)` or a static method) that takes the repo and a list of staged change records, so it is unit-testable without a Qt event loop. Behavior:
    - REMAP/REPLACE: fetch `primary = next(s for s in repo.list_streams(station_id) if s.position == 1)`; call `repo.update_stream(primary.id, new_url, primary.label, primary.quality, primary.position, primary.stream_type, primary.codec, bitrate_kbps=primary.bitrate_kbps, sample_rate_hz=primary.sample_rate_hz, bit_depth=primary.bit_depth)` — preserve ALL non-URL fields (Pitfall 5). Then `repo.set_live_url_title_anchor(station_id, new_title_from_scan)` (D-06). NEVER call station_exists_by_url (Pitfall 6). If the user edited the name for this row, also persist the name via the existing station-name update path; otherwise preserve it (D-08).
    - DROP (only when explicitly ticked): `repo.delete_station(station_id)` (D-07).
    - ADD (only when explicitly ticked): `repo.insert_station(name, url, provider_name, tags="")`, then `repo.set_live_url_syncs_from_channel(new_id, True)` and `repo.set_live_url_title_anchor(new_id, title)` so the new station is itself refresh-eligible (D-07).
    - Conservative defaults (D-10): in the UI, DROP and ADD checkboxes default to Qt.Unchecked; REMAP rows are the only auto-active resolution. An unresolved flagged station (no mapping chosen, not dropped) produces NO repo mutation. Before Apply commits, show a summary of staged changes (the review-and-confirm gate — manual UAT verifies the visual).
    Wire the dialog Apply button to collect staged rows → call the pure helper → emit `refresh_complete` → close. Show a toast on success/failure.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -k "apply_remap_preserves_metadata or apply_drop_and_add_actions or conservative_defaults" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The apply helper is callable without instantiating a QApplication/dialog (pure function or static method taking repo + staged changes).
    - `grep -n "station_exists_by_url" musicstreamer/ui_qt/live_refresh_dialog.py` returns NOTHING (Pitfall 6).
    - update_stream is called with all preserved non-URL fields (test asserts call args).
    - Tests test_apply_remap_preserves_metadata, test_apply_drop_and_add_actions, test_conservative_defaults PASS.
    - DROP/ADD default-unchecked is asserted by test_conservative_defaults; untouched flagged station triggers no mutating repo call.
    - The D-10 summary-before-Apply + empty-apply guard is verified by Plan 01 test_conservative_defaults: calling the pure apply helper with an EMPTY staged-changes list produces ZERO repo mutations (no update_stream/insert_station/delete_station) — this task must keep that test GREEN (W4).
  </acceptance_criteria>
  <done>Pure apply helper handles remap/replace/drop/add with metadata preservation, station_exists_by_url bypass, and conservative unchecked defaults; D-06/D-07/D-10 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yt-dlp scan results → dialog/DB | Untrusted titles + URLs returned by scan_playlist, surfaced and (on apply) persisted |
| user tick → destructive action | Drop = station deletion; only fires on explicit tick |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-09 | Elevation of Privilege | drop/delete of a flagged station | mitigate | D-10: DROP defaults Qt.Unchecked; delete_station fires ONLY on explicit tick; unresolved station never deleted |
| T-96-10 | Tampering | oversized/malicious scan title persisted as anchor/new station name | mitigate | set_live_url_title_anchor caps at 500 chars (Plan 02); name fields are user-editable and reviewed before Apply (D-08) |
| T-96-11 | Spoofing | wrong stream mapped to wrong station | mitigate | D-05 fully manual map + review-and-confirm summary; no auto-apply by anchor match |
| T-96-12 | Information Disclosure | metadata loss on remap (silent quality/codec wipe) | mitigate | Full-row preserve before update_stream (Pitfall 5); test asserts preserved fields |
| T-96-13 | Tampering | duplicate-as-new on churned URL | mitigate | Apply path never calls station_exists_by_url; remap is by explicit stream_id (Pitfall 6) |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (96-RESEARCH.md audit) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_live_refresh_dialog.py -x -q` — all six D-05..D-10 tests green.
- `.venv/bin/python -m pytest tests/ -k "live_refresh or repo" -q` — no regression.
- `grep -n "station_exists_by_url\|scan_playlist" musicstreamer/ui_qt/live_refresh_dialog.py` confirms bypass + node_runtime threading.
</verification>

<success_criteria>
- Off-UI-thread scan with node_runtime threaded through; main-thread population via queued Signal.
- Fully manual map (no auto-apply); anchor only pre-orders.
- Remap preserves metadata; replace/drop/add supported; drops/adds conservative-unchecked; untouched stations untouched.
- Plan-01 D-05..D-10 dialog tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/96-manual-refresh-of-yellow-brick-cinema-provider-with-what-is-/96-04-SUMMARY.md` when done.
</output>
