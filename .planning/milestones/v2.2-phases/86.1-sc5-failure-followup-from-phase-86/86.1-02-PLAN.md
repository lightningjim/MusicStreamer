---
phase: 86.1-sc5-failure-followup-from-phase-86
plan: 02
type: execute
wave: 2
depends_on: ["86.1-01"]
files_modified:
  - musicstreamer/__main__.py
  - tests/test_flatpak_import_wizard_wiring.py
autonomous: true
requirements: [PKG-LIN-FP-06]

must_haves:
  truths:
    - "On first launch inside the sandbox with existing host data, the FlatpakImportWizard is offered (appears over the shown main window)"
    - "The wizard offer is gated on BOTH is_sandboxed() AND should_offer_import_wizard() so it never fires on native Linux"
    - "Dismiss/complete writes the offer-once flag; a relaunch does NOT re-offer (offer-once, D-03 — guaranteed by should_offer_import_wizard() returning False once the flag exists)"
    - "The wizard wiring lives only in _run_gui; _run_smoke stays Qt-light and untouched (D-05/D-24)"
    - "The offer fires AFTER window.show() and at the start of the event loop (QTimer.singleShot(0, ...)) so the modal dialog renders over the main window"
  artifacts:
    - path: "musicstreamer/__main__.py"
      provides: "should_offer-gated, is_sandboxed-gated FlatpakImportWizard offer after window.show()"
      contains: "FlatpakImportWizard"
    - path: "tests/test_flatpak_import_wizard_wiring.py"
      provides: "unit coverage proving the offer gate logic (sandboxed+data → offer; native → no offer; flag present → no offer)"
      contains: "should_offer_import_wizard"
  key_links:
    - from: "musicstreamer/__main__.py"
      to: "musicstreamer/flatpak_first_launch.py"
      via: "is_sandboxed() and should_offer_import_wizard() gate in _run_gui"
      pattern: "should_offer_import_wizard"
    - from: "musicstreamer/__main__.py"
      to: "musicstreamer/ui_qt/flatpak_import_wizard.py"
      via: "FlatpakImportWizard instantiation"
      pattern: "FlatpakImportWizard"
---

<objective>
Wire the fully-built `FlatpakImportWizard` into GUI startup so the offered, consent-based, offer-once import is the SOLE path by which unsandboxed host data enters the sandbox. The wizard module exists and is correct, but no code path ever calls `should_offer_import_wizard()` or instantiates it — so the offer never fires (Phase 86 SC5 defect #1).

This plan adds a gated offer in `musicstreamer/__main__._run_gui()`, AFTER `window.show()`, fired via `QTimer.singleShot(0, ...)` so the modal dialog renders over the shown main window. The offer is gated on BOTH `is_sandboxed()` (from Plan 01 — prevents firing on native Linux where host path == sandbox path) AND `should_offer_import_wizard()` (host-data-present + offer-once-flag-absent).

Depends on Plan 01: the migration auto-copy must already be sandbox-skipped, otherwise it preempts this wizard by silently importing host data before the offer can fire.

Purpose: Close Phase 86 SC5 — first launch offers the wizard; dismiss/relaunch does not re-offer; native Linux is unaffected.
Output: gated wizard offer in `_run_gui`, plus unit coverage of the gate logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-linux-flatpak-build/86-VERIFICATION.md

<interfaces>
<!-- Verified against the actual source. The planning brief's wizard signature was stale; THIS is correct. -->

From musicstreamer/ui_qt/flatpak_import_wizard.py — ACTUAL constructor (verify by reading):
- `FlatpakImportWizard(sandbox_db_path: str, toast_callback: Optional[Callable[[str], None]] = None, parent=None)`
  - It reads `flatpak_first_launch._HOST_DATA_DIR` itself for the host source — do NOT pass a host_data_dir argument (the brief's `(host_data_dir, sandbox_db_path, parent)` signature is WRONG).
  - Modal (`setModal(True)`). Builds preview on a background worker on construction.
  - Calls `flatpak_first_launch.write_offered_flag()` on every dismiss/complete/reject path (D-03).
  - Signal: `import_complete = Signal()`.

From musicstreamer/flatpak_first_launch.py:
- `is_sandboxed() -> bool` (added in Plan 01) — gate to prevent native-Linux firing.
- `should_offer_import_wizard() -> bool` — host-data-present AND offer-once-flag-absent.

From musicstreamer/paths.py:
- `db_path() -> str` — the sandbox-remapped DB path to pass as `sandbox_db_path`.

From musicstreamer/ui_qt/main_window.py:
- `MainWindow.show_toast(text: str, duration_ms: int = 3000) -> None` — the bound method to pass as `toast_callback`.

From musicstreamer/__main__.py _run_gui (wiring site):
- MainWindow constructed ~line 249; `window.show()` line 253; `return app.exec()` line 254.
- `_run_smoke` (line 20) must NOT import ui_qt (D-05/D-24) — do not touch it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offer the FlatpakImportWizard from _run_gui, gated and post-show</name>
  <read_first>
    - musicstreamer/__main__.py _run_gui (lines ~175-254): the wiring point; note window.show() at 253 and `return app.exec()` at 254, and that _run_smoke (line 20) must stay ui_qt-free
    - musicstreamer/ui_qt/flatpak_import_wizard.py (the ACTUAL constructor: `FlatpakImportWizard(sandbox_db_path, toast_callback=None, parent=None)` — reads _HOST_DATA_DIR itself; do not pass host_data_dir)
    - musicstreamer/flatpak_first_launch.py (is_sandboxed from Plan 01 + should_offer_import_wizard)
    - musicstreamer/paths.py (db_path for sandbox_db_path argument)
    - musicstreamer/ui_qt/main_window.py show_toast signature (the bound method to pass as toast_callback)
  </read_first>
  <action>
    In `musicstreamer/__main__._run_gui()`, after `window.show()` (line 253) and before `return app.exec()` (line 254), add a gated, deferred wizard offer. Define a local helper (e.g. `_maybe_offer_import`) that checks `flatpak_first_launch.is_sandboxed() and flatpak_first_launch.should_offer_import_wizard()`; only when both are True, construct `FlatpakImportWizard(paths.db_path(), toast_callback=window.show_toast, parent=window)` and call `.exec()` on it (modal — the wizard writes the offer-once flag on every close path itself, so no extra flag handling is needed here). Schedule it with `QTimer.singleShot(0, _maybe_offer_import)` so it runs at the start of the event loop, rendering the modal over the shown main window. Use LAZY imports inside _run_gui for `flatpak_first_launch`, `paths`, `FlatpakImportWizard`, and `QTimer` (consistent with the existing lazy-import style in this function and so _run_smoke stays Qt-light / ui_qt-free per D-05/D-24). Do NOT modify _run_smoke. Do NOT change the run_migration/buffer_log ordering above. Connect nothing to `import_complete` is required for the offer itself, but it is acceptable to leave the signal unconnected (the wizard self-manages the flag and toast).
  </action>
  <verify>
    <automated>python -c "import ast,sys; t=ast.parse(open('musicstreamer/__main__.py').read()); src=open('musicstreamer/__main__.py').read(); assert 'FlatpakImportWizard' in src and 'should_offer_import_wizard' in src and 'is_sandboxed' in src and 'singleShot' in src, 'missing wiring tokens'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - musicstreamer/__main__.py _run_gui contains an `is_sandboxed()`-AND-`should_offer_import_wizard()`-gated `FlatpakImportWizard` instantiation scheduled via `QTimer.singleShot(0, ...)` AFTER `window.show()`
    - The wizard is constructed as `FlatpakImportWizard(paths.db_path(), toast_callback=window.show_toast, parent=window)` (matches the real constructor; no host_data_dir arg)
    - `_run_smoke` is unchanged and contains no `ui_qt` / `FlatpakImportWizard` import (`grep -n "FlatpakImportWizard\|ui_qt" musicstreamer/__main__.py` shows them only within/after _run_gui, never in _run_smoke)
    - `grep -c "should_offer_import_wizard" musicstreamer/__main__.py` is >= 1 and `grep -c "is_sandboxed" musicstreamer/__main__.py` is >= 1
    - `python -m py_compile musicstreamer/__main__.py` exits 0
  </acceptance_criteria>
  <done>_run_gui offers the wizard only when sandboxed AND should_offer is True, deferred via singleShot after window.show(); _run_smoke untouched and Qt-light.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test the wizard-offer gate logic (offer / no-offer cases)</name>
  <read_first>
    - tests/test_flatpak_first_launch.py (monkeypatch patterns for `_HOST_DB`, `_FLATPAK_INFO`, `paths._root_override`)
    - musicstreamer/flatpak_first_launch.py (is_sandboxed, should_offer_import_wizard, write_offered_flag — the composed gate this test exercises)
  </read_first>
  <behavior>
    - Sandboxed + host DB present + no flag → the composed gate (is_sandboxed() and should_offer_import_wizard()) is True (offer fires)
    - NOT sandboxed (no /.flatpak-info) but host DB present → gate is False (no offer on native Linux, even though should_offer alone is True)
    - Sandboxed + host DB present + offer-once flag written → gate is False (offer-once holds across relaunch)
    - Sandboxed + no host DB → gate is False (fresh Flatpak user)
  </behavior>
  <action>
    Create `tests/test_flatpak_import_wizard_wiring.py` exercising the GATE composition that `_run_gui` uses — `flatpak_first_launch.is_sandboxed() and flatpak_first_launch.should_offer_import_wizard()` — WITHOUT constructing the Qt wizard (keep the test Qt-free / headless-CI-safe). Define a tiny local helper mirroring the gate, or assert each conjunct directly. Use the established monkeypatch pattern: monkeypatch `flatpak_first_launch._FLATPAK_INFO` (to a tmp file for sandboxed=True, or a missing path for sandboxed=False), `flatpak_first_launch._HOST_DB` (to a tmp DB file for host-data-present), and `paths._root_override` (a separate sandbox tmp dir for the offer-once flag). Cover the four cases in <behavior>: (1) sandboxed+data+no-flag → gate True; (2) not-sandboxed+data → gate False; (3) sandboxed+data+flag (call `write_offered_flag()`) → gate False; (4) sandboxed+no-data → gate False. Restore monkeypatched globals (monkeypatch fixture handles this automatically).
  </action>
  <verify>
    <automated>python -m pytest tests/test_flatpak_import_wizard_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_flatpak_import_wizard_wiring.py exists and contains four cases matching <behavior>, all monkeypatching `_FLATPAK_INFO`, `_HOST_DB`, and `paths._root_override`
    - The test asserts the not-sandboxed-but-data-present case yields a False gate (proves no native-Linux offer)
    - The test asserts the offer-once case (flag present) yields a False gate
    - The test constructs NO Qt object (no `from PySide6` / no FlatpakImportWizard import) — runs headless
    - `python -m pytest tests/test_flatpak_import_wizard_wiring.py -x -q` exits 0
  </acceptance_criteria>
  <done>Gate logic is proven: offer fires only when sandboxed AND host-data-present AND offer-once-flag-absent; native Linux and post-dismiss relaunch never offer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem (:ro mount) → Flatpak sandbox | Host data enters the sandbox ONLY via the consent-based wizard import. The wizard is the sole sanctioned crossing (migration auto-copy already closed in Plan 01). |
| user consent → data ingestion | The user explicitly chooses merge/replace and confirms; the wizard's copy-don't-delete keeps the host :ro source intact (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.1-04 | Information Disclosure | wizard-offer gate in _run_gui | mitigate | Without this wiring the wizard never fires and (pre-Plan-01) migration silently ingested secrets. With Plan 01 + this gate, host data enters ONLY through the user-consented wizard. Gate requires `is_sandboxed() AND should_offer_import_wizard()`; tested across all four branches. |
| T-86.1-05 | Spoofing / mis-fire | native-Linux false offer | mitigate | On native Linux host path == sandbox path; `is_sandboxed()` conjunct prevents the offer (and the destructive replace-all path) from ever firing outside the sandbox. Explicitly tested (case 2). |
| T-86.1-06 | Repudiation / re-prompt loop | offer-once flag | accept | The wizard writes `write_offered_flag()` on every close path (cancel/X/complete); `should_offer_import_wizard()` then returns False. Offer-once is owned by the wizard + flag, verified at the gate level (case 3); low residual risk. |
| T-86.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase (pure wiring + tests). No legitimacy gate required. |
</threat_model>

<verification>
- `python -m pytest tests/test_flatpak_import_wizard_wiring.py -q` exits 0
- `python -m py_compile musicstreamer/__main__.py` exits 0
- Full suite regression: `python -m pytest -q` exits 0
- `grep -n "FlatpakImportWizard\|should_offer_import_wizard\|is_sandboxed\|singleShot" musicstreamer/__main__.py` shows all four, only in/after _run_gui
- Manual confirmation deferred to in-sandbox UAT (re-run Phase 86 SC5): build Flatpak, fresh launch with host data → wizard offered; dismiss → relaunch → not re-offered; host data intact.
</verification>

<success_criteria>
- First sandboxed launch with host data offers the wizard over the shown main window
- Offer-once holds across relaunch (flag written by wizard, gate returns False)
- Native Linux never offers (is_sandboxed conjunct)
- _run_smoke remains Qt-light and untouched
- Combined with Plan 01: the consent-based wizard is the SOLE path for host data into the sandbox
</success_criteria>

<output>
Create `.planning/phases/86.1-sc5-failure-followup-from-phase-86/86.1-02-SUMMARY.md` when done
</output>
