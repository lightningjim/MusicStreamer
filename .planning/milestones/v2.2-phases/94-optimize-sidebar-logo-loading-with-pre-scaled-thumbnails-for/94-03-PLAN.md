---
phase: 94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for
plan: 03
type: execute
wave: 3
depends_on: ["94-02"]
files_modified:
  - musicstreamer/ui_qt/station_tree_model.py
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "Implements D-03 (async off-UI-thread generate-on-miss: fallback paints immediately, worker runs off-thread, dataChanged repaints the row on landing, same station never double-enqueued) and D-01 (Now Playing full-res 180px path untouched and drift-guarded)"
    - "StationTreeModel.data(DecorationRole) requests an async thumbnail via on_thumb_needed, painting the fallback immediately while the worker runs off the UI thread"
    - "A station is enqueued at most once while a worker is in flight, even under rapid repeated data() calls during fast scroll"
    - "When a thumbnail lands, the stale source-keyed cache entry is evicted, the in-flight guard is cleared, and dataChanged is emitted for the affected row so it repaints with the real logo"
    - "The cross-thread hand-off uses a Signal(int, str, str) with an explicit QueuedConnection so the slot runs on the main thread; no QPixmap is constructed off the UI thread"
    - "now_playing_panel._load_scaled_pixmap (full-res 180px) is never rewired to the sidebar thumbnail"
  artifacts:
    - path: "musicstreamer/ui_qt/station_tree_model.py"
      provides: "_thumb_landing Signal, _in_flight_thumbs set, _request_thumb, _on_thumb_landing slot, index_for_station_id; data() wired with on_thumb_needed"
      contains: "_thumb_landing = Signal(int, str, str)"
  key_links:
    - from: "musicstreamer/ui_qt/station_tree_model.py::data"
      to: "load_station_icon on_thumb_needed=self._request_thumb"
      via: "DecorationRole branch"
      pattern: "on_thumb_needed=self._request_thumb"
    - from: "musicstreamer/ui_qt/station_tree_model.py::_on_thumb_landing"
      to: "QPixmapCache + dataChanged"
      via: "QPixmapCache.remove(station-logo:source) + index_for_station_id + dataChanged.emit"
      pattern: "self.dataChanged.emit"
    - from: "worker daemon thread"
      to: "_on_thumb_landing (main thread)"
      via: "Signal(int,str,str) emitted from callback, Qt.QueuedConnection"
      pattern: "Qt.QueuedConnection"
---

<objective>
Wire the StationTreeModel coordination layer (D-03) and lock the Now Playing drift-guard (D-01). Add the `_thumb_landing = Signal(int, str, str)` cross-thread channel, the `_in_flight_thumbs` dedup set, the `_request_thumb` main-thread enqueue method, the `_on_thumb_landing` main-thread slot (cache evict → repaint), and the `index_for_station_id` O(N) lookup. Wire `data(DecorationRole)` to pass `on_thumb_needed=self._request_thumb` into the Plan-02 `load_station_icon`. This turns the remaining Plan-01 tests in `tests/test_station_thumb_async.py` from RED to GREEN and completes the phase.

Purpose: Coordinate the async generate-on-miss lifecycle so fast scroll paints fallback icons immediately and rows repaint as real 96px logos land, with no duplicate workers and no off-thread QPixmap construction.
Output: The full async pipeline (data → fallback + enqueue → daemon worker → Signal → main-thread slot → cache evict → dataChanged → repaint). Phase complete; full suite must be green.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-CONTEXT.md
@.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-RESEARCH.md
@.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-PATTERNS.md

<interfaces>
<!-- Current station_tree_model.py surface + the Plan-02 _art_paths contract this plan consumes. -->

From musicstreamer/ui_qt/station_tree_model.py (current):
- class _TreeNode (line 27): fields include kind ("root"|"provider"|"station"), label, parent, children (list), station (Optional[Station])
- StationTreeModel.__init__(self, stations, parent=None): self._root = _TreeNode(kind="root", ...); self._populate(stations)
- refresh(self, stations): rebuilds self._root via _populate (invalidates QModelIndex — never store one across async)
- station_for_index(index) -> Optional[Station]
- index(row, col, parent) -> createIndex(row, 0, parent_node.children[row])
- data(index, role): DecorationRole branch (line ~163) currently: `return load_station_icon(node.station)`
- Current imports: `from PySide6.QtCore import QAbstractItemModel, QModelIndex, Qt`; `from PySide6.QtGui import QFont`; `from musicstreamer.ui_qt._art_paths import load_station_icon`

From musicstreamer/ui_qt/_art_paths.py (Plan 02 — consume these):
- load_station_icon(station, size=STATION_ICON_SIZE, on_thumb_needed=None) -> QIcon
    # on_thumb_needed(station_id: int, source_abs_path: str, thumb_abs_path: str)
- _generate_thumb(source_path, thumb_path, station_id, callback) -> None
    # callback(station_id: int, source_path: str, thumb_path: str | None)
- cache key scheme: f"station-logo:{source_abs_path}"

Analog idioms:
- gbs_marquee.py 468-484: Signal declaration + explicit Qt.QueuedConnection connect in __init__
- edit_station_dialog.py 1321-1325: QPixmapCache.remove(f"station-logo:{resolved}")
- station_list_panel.py 476-486: O(N) provider→station walk by station.id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Signal, dedup set, index_for_station_id, _request_thumb, and _on_thumb_landing slot</name>
  <files>musicstreamer/ui_qt/station_tree_model.py</files>
  <read_first>
    - musicstreamer/ui_qt/station_tree_model.py (the file being modified — __init__, _populate, index/createIndex shape lines 117-129, station_for_index, refresh, data DecorationRole ~163)
    - musicstreamer/gbs_marquee.py lines 460-484 (Signal(...) class-level declaration + explicit Qt.QueuedConnection connect in __init__ — the cross-thread idiom)
    - musicstreamer/ui_qt/edit_station_dialog.py lines 1321-1325 (_invalidate_cache_for — exact QPixmapCache.remove key pattern to reuse)
    - musicstreamer/ui_qt/station_list_panel.py lines 476-486 (O(N) provider→station walk by id — analog for index_for_station_id)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-RESEARCH.md (Pitfalls 2/3/4/6; StationTreeModel changes lines 412-490; cache eviction lines 473-490)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-PATTERNS.md (station_tree_model.py section lines 148-279)
  </read_first>
  <behavior>
    - _request_thumb(station_id, source_path, thumb_path): if station_id in _in_flight_thumbs → return (dedup, Pitfall 2). Else add to set and call _generate_thumb(source_path, thumb_path, station_id, callback) where callback emits _thumb_landing (bound .emit, no self-capture) carrying (station_id, source_path, thumb_path-or-None). Always runs on the main thread (called from data()).
    - _on_thumb_landing(station_id, source_path, thumb_path): runs on main thread via QueuedConnection. discard station_id from _in_flight_thumbs. If thumb_path falsy → return (generation failed; fallback stays). Else QPixmapCache.remove(f"station-logo:{source_path}") to evict the stale fallback entry; idx = index_for_station_id(station_id); if idx.isValid() → dataChanged.emit(idx, idx, [Qt.DecorationRole]) (Pitfall 3: re-derive index, never store across async).
    - index_for_station_id(station_id) -> QModelIndex: walk self._root.children (providers) then each provider's .children (stations); return createIndex(child_row, 0, child_node) where child_node.station.id == station_id; else QModelIndex().
    - Signal payload carries source_path so the slot reconstructs the cache key without fragile .thumb.png string surgery.
  </behavior>
  <action>
    Extend imports: `from PySide6.QtCore import QAbstractItemModel, QModelIndex, Qt, Signal`; `from PySide6.QtGui import QFont, QPixmap, QPixmapCache`; and `from musicstreamer.ui_qt._art_paths import load_station_icon, _generate_thumb`.

    Declare at class level (after the class statement, like gbs_marquee): `_thumb_landing = Signal(int, str, str)` with a CR-01 comment noting the worker emits a PNG path, never a QPixmap; the main-thread slot decodes.

    In __init__, after `self._populate(stations)`, add `self._in_flight_thumbs: set[int] = set()` and `self._thumb_landing.connect(self._on_thumb_landing, Qt.QueuedConnection)` (explicit QueuedConnection — matches gbs_marquee precedent; guarantees main-thread slot even when emitted from the daemon thread's callback).

    Add `index_for_station_id(self, station_id) -> QModelIndex` doing the two-level walk over self._root.children → child_node, returning self.createIndex(child_row, 0, child_node) on match, else QModelIndex(). Mirror station_list_panel's walk but on the source node list (no proxy).

    Add `_request_thumb(self, station_id, source_path, thumb_path) -> None`: dedup guard on self._in_flight_thumbs; add the id; bind `emit = self._thumb_landing.emit`; call `_generate_thumb(source_path, thumb_path, station_id, lambda sid, src, path: emit(sid, src, path))`. The lambda normalizes the 3-arg callback into the Signal; when path is None the slot will no-op after discard. (If _generate_thumb's callback can pass None for thumb_path, ensure the Signal's third arg accepts it — declare the Signal third type as `str` but guard: have the lambda pass `path or ""` and treat "" as failure in the slot, OR use `Signal(int, str, object)`. Choose `Signal(int, str, object)` if None must cross; otherwise map None→"" in the lambda and treat falsy in the slot. Pick one and document it inline.)

    Add `_on_thumb_landing(self, station_id, source_path, thumb_path) -> None`: `self._in_flight_thumbs.discard(station_id)`; `if not thumb_path: return`; `QPixmapCache.remove(f"station-logo:{source_path}")`; `idx = self.index_for_station_id(station_id)`; `if idx.isValid(): self.dataChanged.emit(idx, idx, [Qt.DecorationRole])`. This is the only place QPixmapCache is touched from the model, and it runs on the main thread.

    Do NOT wire data() yet (that is Task 2) — but the symbols must exist so the dedup/index/landing tests can resolve. No fenced code; mirror analogs by name.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_thumb_async.py::test_in_flight_dedup tests/test_station_thumb_async.py::test_index_for_station_id_roundtrip tests/test_station_thumb_async.py::test_now_playing_panel_does_not_use_thumb -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '_thumb_landing = Signal\|def index_for_station_id\|def _request_thumb\|def _on_thumb_landing\|self._in_flight_thumbs' musicstreamer/ui_qt/station_tree_model.py` >= 5.
    - Source assertion: `grep -c 'Qt.QueuedConnection' musicstreamer/ui_qt/station_tree_model.py` >= 1 (explicit queued connect).
    - Source assertion: cache key reused — `grep -c 'station-logo:' musicstreamer/ui_qt/station_tree_model.py` >= 1 and matches the source-path key (not thumb path).
    - Behavior: test_in_flight_dedup, test_index_for_station_id_roundtrip, and test_now_playing_panel_does_not_use_thumb pass GREEN.
    - No QPixmap constructed off the main thread: QPixmapCache/QPixmap usage in this file appears only inside _on_thumb_landing (a slot) — `grep -n 'QPixmapCache\|QPixmap(' musicstreamer/ui_qt/station_tree_model.py` shows occurrences only within _on_thumb_landing.
  </acceptance_criteria>
  <done>Signal + dedup set + index_for_station_id + _request_thumb + _on_thumb_landing added with explicit QueuedConnection and source-keyed cache eviction; dedup/index/drift-guard tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire data(DecorationRole) to enqueue async generation and confirm dataChanged repaint</name>
  <files>musicstreamer/ui_qt/station_tree_model.py</files>
  <read_first>
    - musicstreamer/ui_qt/station_tree_model.py (data() DecorationRole branch ~line 163 — the single line to change; plus the Task-1 additions now present)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-RESEARCH.md (Pitfall 4 dataChanged storm — acceptable per D-03; A2 dataChanged→repaint chain)
    - .planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-PATTERNS.md (data() call site modification lines 263-279)
  </read_first>
  <behavior>
    - The DecorationRole branch passes on_thumb_needed=self._request_thumb into load_station_icon so a thumb miss/stale paints fallback immediately and enqueues a worker.
    - When the worker lands, dataChanged(idx, idx, [DecorationRole]) is emitted exactly once for the row (D-03 async repaint), and the in-flight guard is cleared.
  </behavior>
  <action>
    In data(), change the DecorationRole station branch from `return load_station_icon(node.station)` to `return load_station_icon(node.station, on_thumb_needed=self._request_thumb)`. No other change to data(). The fallback-immediate + async-repaint behavior now flows end to end: data → load_station_icon (fallback + on_thumb_needed) → _request_thumb (dedup + _generate_thumb) → daemon worker (QImage scale + atomic write) → callback → _thumb_landing.emit → QueuedConnection → _on_thumb_landing (discard + cache evict + dataChanged) → Qt re-queries data() → load_station_icon now finds the fresh thumb (Plan 02 fast path) → real logo painted.

    Confirm no QPixmap is built off-thread anywhere in the chain (worker is QImage-only per Plan 02). Do not add rate-limiting/semaphore — the dataChanged storm on startup is the agreed D-03 behavior (Pitfall 4); leave as-is.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_station_thumb_async.py tests/test_art_paths.py tests/test_station_icon_integration.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'on_thumb_needed=self._request_thumb' musicstreamer/ui_qt/station_tree_model.py` == 1.
    - Behavior: test_thumb_landing_emits_datachanged passes GREEN (exactly one dataChanged emission for the single row with Qt.DecorationRole in roles after the worker lands).
    - Regression: all of tests/test_station_thumb_async.py, tests/test_art_paths.py, and tests/test_station_icon_integration.py pass (9 new + existing).
    - The DecorationRole branch is the ONLY changed line in data(); display/font/UserRole branches untouched.
  </acceptance_criteria>
  <done>data(DecorationRole) wired with on_thumb_needed=self._request_thumb; full async repaint pipeline GREEN; all phase quick-suite tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon worker thread → main UI thread | The worker's callback emits _thumb_landing across threads. Only a tuple of (int, str, str/None) crosses — never a QPixmap or live Qt paint object. Qt.QueuedConnection marshals the slot onto the main thread (CR-01 compliance). |
| importer-downloaded PNG → render | As in Plans 01/02: the source logo is trusted local content written by the import flow, not user-supplied at render time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06 | Tampering | QPixmap/QPixmapCache constructed off the UI thread | mitigate | All QPixmap + QPixmapCache access is confined to _on_thumb_landing (a slot delivered via Qt.QueuedConnection) — guaranteed main-thread. Worker (Plan 02) is QImage-only. Verified by grep gate in Task 1 acceptance. |
| T-94-07 | Denial of Service | Worker storm during fast scroll (duplicate threads per station) | mitigate | _in_flight_thumbs dedup set guards _request_thumb; a station is enqueued at most once per in-flight cycle. Verified by test_in_flight_dedup. |
| T-94-08 | Tampering | Stale QModelIndex used across async boundary | mitigate | Only station_id (int) crosses the async boundary; index_for_station_id re-derives a fresh QModelIndex in the main-thread slot; dataChanged guarded by idx.isValid(). |
| T-94-09 | Repudiation/Integrity | now_playing full-res path silently rewired to thumbnail | mitigate | D-01 drift-guard test (test_now_playing_panel_does_not_use_thumb) asserts _load_scaled_pixmap never references station_art.thumb; this plan touches only station_tree_model.py, not now_playing_panel.py. |
| T-94-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — zero external dependencies (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- Quick suite GREEN: `.venv/bin/python -m pytest tests/test_art_paths.py tests/test_station_icon_integration.py tests/test_station_thumb_async.py -q 2>&1 | tail -20` — all 9 new tests + existing pass.
- Full suite (per VALIDATION.md phase gate, >600s, run before /gsd:verify-work): `.venv/bin/python -m pytest tests/ -q 2>&1 | tail -30` — only the two known pre-existing unrelated failures may remain (MEMORY.md); no NEW failures introduced.
- `grep -n 'QPixmap' musicstreamer/ui_qt/station_tree_model.py` confirms QPixmap/QPixmapCache usage only inside _on_thumb_landing (main-thread slot).
- All commands use `.venv/bin/python -m pytest` (MEMORY.md).
</verification>

<success_criteria>
- D-03: async generate-on-miss pipeline complete — fallback paints immediately, worker runs off-thread, dataChanged repaints the row once the thumb lands; same station never double-enqueued (dedup). (GREEN: test_thumb_landing_emits_datachanged, test_in_flight_dedup)
- D-01: now_playing full-res 180px path unchanged and drift-guarded. (GREEN: test_now_playing_panel_does_not_use_thumb)
- Cross-thread hand-off uses Signal(int, str, str/object) + explicit Qt.QueuedConnection; no QPixmap off the UI thread.
- Cache eviction reuses the source-path key f"station-logo:{source_path}", consistent with edit_station_dialog invalidation.
- Phase quick suite fully GREEN; full suite shows no new failures.
</success_criteria>

<output>
Create `.planning/phases/94-optimize-sidebar-logo-loading-with-pre-scaled-thumbnails-for/94-03-SUMMARY.md` when done.
</output>
