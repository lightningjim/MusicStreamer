---
phase: 88.3
slug: bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-10
audited: 2026-06-13
---

# Phase 88.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 88.3-RESEARCH.md ## Validation Architecture.
>
> **NOTE (2026-06-13 audit):** This map was rewritten to reflect the **B1 isolated-helper-bundle**
> architecture the phase actually shipped. The original draft assumed the abandoned approach
> (add WebEngine *into* the conda bundle, `--check-webengine` guard). Under B1 the conda bundle
> stays WebEngine-free and a separate `oauth_helper.exe` carries WebEngine — so the G6-T1..T5
> drift-guards were re-pointed to assert the *negative* invariant, and three new test files were
> added. All requirement IDs were preserved.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest |
| **Config file** | pyproject.toml `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run --with pytest pytest tests/test_packaging_webengine_spec.py -x` |
| **Phase suite command** | `uv run --with pytest pytest tests/test_packaging_webengine_spec.py tests/test_packaging_helper_build_spec.py tests/test_oauth_launch_path.py tests/test_launcher_frozen_dispatch.py -q` |
| **Full suite command** | `uv run --with pytest pytest` |
| **Estimated runtime** | ~0.2s (source-text drift guards + monkeypatched launch-path tests; no build invoked) |
| **Last run** | 2026-06-13 — **27 passed**, 1 unrelated GLib deprecation warning |

---

## Sampling Rate

- **After every task commit:** `uv run --with pytest pytest tests/test_packaging_webengine_spec.py -x`
- **After every plan wave:** phase suite (4 files above)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~1 second

---

## Per-Task Verification Map

All requirement IDs retained from the pre-pivot plan; tests re-pointed/added for B1.

### Conda bundle stays WebEngine-free (B1 invariant) — `tests/test_packaging_webengine_spec.py`

| Req | Secure Behavior | Test | Status |
|-----|-----------------|------|--------|
| G6-T1 | conda spec has NO WebEngine hiddenimports | `test_spec_has_no_webengine_hiddenimports` | ✅ green |
| G6-T2 | revert was surgical (QtNetwork + QtSvg retained) | `test_spec_still_has_network_and_svg` | ✅ green |
| G6-T3 | `__main__.py` conda WebEngine guard gone, `--oauth-helper` kept | `test_main_py_has_no_conda_webengine_guard` | ✅ green |
| G6-T4 | build.ps1 conda WebEngine preflight/step-4e gone | `test_build_ps1_has_no_conda_webengine_guard` | ✅ green |
| G6-T5 | build.ps1 88.1 SMTC + 88.2 oauth guards survived revert | `test_build_ps1_keeps_smtc_and_oauth_guards` | ✅ green |

### Frozen launch target + platform UA — `tests/test_oauth_launch_path.py`

| Req | Secure Behavior | Test | Status |
|-----|-----------------|------|--------|
| P03-T1 | frozen launches sibling `oauth_helper.exe`, not self | `test_frozen_launches_sibling_helper_exe` | ✅ green |
| P03-T1 | program ≠ `sys.executable` (no self-re-exec) | `test_frozen_uses_os_path_join_not_self` | ✅ green |
| P03-T1 | dev/Linux module-form contract preserved | `test_source_branch_unchanged` | ✅ green |
| P03-T1 | mode is a call-site list literal, never shell-interpolated (T-40-05) | `test_mode_is_call_site_literal` | ✅ green |
| P03-T2 | Windows UA token on win32 | `test_ua_windows_token_on_win32` | ✅ green |
| P03-T2 | Linux UA token off win32 | `test_ua_linux_token_on_non_win32` | ✅ green |
| P03-T2 | Chrome version tokens present (Kasada) | `test_ua_chrome_version_tokens_present` | ✅ green |
| P03-T2 | `QTWEBENGINE_CHROMIUM_FLAGS` set before WebEngine import | `test_ua_flags_set_before_qtwebengine_import` | ✅ green |

### Frozen/non-frozen dispatch contract — `tests/test_launcher_frozen_dispatch.py`

| Req | Secure Behavior | Test | Status |
|-----|-----------------|------|--------|
| P03 | frozen branch uses exe+argv dispatch | `test_frozen_branch_uses_exe_argv_dispatch` | ✅ green |
| P03 | non-frozen branch uses module form | `test_non_frozen_branch_uses_module_form` | ✅ green |
| P03 | `--oauth-helper` self-test returns 0 | `test_run_oauth_helper_self_test_returns_0` | ✅ green |
| P03 | in-process dispatch rejected | `test_run_oauth_helper_rejects_in_process_dispatch` | ✅ green |

### Isolated helper bundle build — `tests/test_packaging_helper_build_spec.py`

| Req | Secure Behavior | Test | Status |
|-----|-----------------|------|--------|
| P04-T1 | helper requirements exact-pinned (PySide6 6.11.0, pyinstaller 6.19.0, hooks 2026.2) | `test_helper_requirements_pinned` | ✅ green |
| P04-T1 | standalone spec carries WebEngine hiddenimports | `test_helper_spec_has_webengine_hiddenimports` | ✅ green |
| P04-T1 | standalone spec freezes `oauth_helper.py` as `oauth_helper` | `test_helper_spec_freezes_oauth_helper` | ✅ green |
| P04-T1 | spec REPO_ROOT = `parents[1]` | `test_helper_spec_repo_root_two_levels` | ✅ green |
| P04-T2 | build.ps1 builds second artifact from helper spec/reqs | `test_build_ps1_builds_helper` | ✅ green |
| P04-T2 | build.ps1 uses conda-free isolated venv (`-HelperPythonExe`) | `test_build_ps1_helper_isolated_venv` | ✅ green |
| P04-T2 | build.ps1 asserts QtWebEngineProcess.exe + Qt6WebEngineCore.dll (exit 14) | `test_build_ps1_asserts_helper_webengine` | ✅ green |
| P04-T2 | Inno ships helper to `{app}\oauth_helper` (local path) | `test_iss_ships_helper` | ✅ green |
| P04-T2 | README documents conda-free Python 3.12 (no pip-into-conda) | `test_readme_documents_conda_free_python` | ✅ green |
| P04-T2 | README PySide6 version matches requirements pin (drift coupling) | `test_readme_helper_pyside6_version_matches_requirements_pin` | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*(The drift-guard tests are source-text/monkeypatch assertions over the spec / build.ps1 / .iss / launcher — they do not invoke a Windows build.)*

---

## Wave 0 Requirements

- [x] `tests/test_packaging_webengine_spec.py` — re-pointed B1 drift-guards (G6-T1..T5)
- [x] `tests/test_oauth_launch_path.py` — frozen launch-target + platform-UA guards
- [x] `tests/test_launcher_frozen_dispatch.py` — frozen/non-frozen dispatch contract
- [x] `tests/test_packaging_helper_build_spec.py` — isolated helper build/spec/reqs/Inno/README guards

*All test files exist and are green; pytest already configured in pyproject.toml.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Evidence |
|----------|-------------|------------|----------|
| Two-artifact frozen build completes (BUILD_OK) on Win11 VM | Plan 05 Task 1 | Frozen exe is not runnable on Linux; build is Windows-VM-only | 88.3-UAT.md — build `MusicStreamer-2.2.86-win64-setup.exe` v2.2.86 produced |
| GBS.FM QtWebEngine login opens + auth completes from frozen exe | G6-T6 (UAT-10) | Real frozen Windows build on a VM + interactive OAuth | 88.3-UAT.md — PASS (no exit=2, no DLL-load failure, fallback not triggered) |
| Twitch QtWebEngine login opens + Kasada accepts browser | G6-T7 (UAT-10b) | Same frozen-bundle + interactive-auth constraint | 88.3-UAT.md — PASS (required PySide6 6.11.0 Chromium bump to clear Kasada) |
| Google/YouTube QtWebEngine login opens (graceful error path) | G6-T8 (UAT-10c / CR-01) | Same constraint; exercises graceful error path | 88.3-UAT.md — PASS |

*These four are inherently un-automatable on the Linux dev host (frozen Windows artifact +
interactive OAuth). They are the phase's reason-for-being; the 27 automated guards prevent silent
regression of the bundling fix but cannot replace VM UAT. All four are recorded PASS (UAT 4/4,
status: resolved, 2026-06-13). Re-confirming against a fresh artifact requires a VM rebuild + a
re-run of UAT tests 1–4.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or documented manual-only justification
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all automatable references (4 test files, 27 tests green)
- [x] No watch-mode flags
- [x] Feedback latency < 15s (~0.2s actual)
- [x] `nyquist_compliant: true` — zero automatable gaps; manual-only items documented + UAT-verified

**Approval:** validated 2026-06-13 — automatable coverage complete (27/27 green), 4 documented manual-only.

---

## Validation Audit 2026-06-13

| Metric | Count |
|--------|-------|
| Automatable gaps found | 0 |
| Resolved (already covered green) | 27 |
| Escalated to manual-only | 4 (inherent — frozen Windows build + 3 interactive OAuth flows) |

State A audit. Stale pre-B1-pivot Per-Task Map (G6-T1..T5 asserting WebEngine *present*, all
`pending`) replaced with the shipped B1 map: 27 automated tests across 4 files, all green on a
live run. No `gsd-nyquist-auditor` spawn — there were no automatable gaps to fill, only a stale
map to reconcile with reality. The 4 manual-only items cannot be automated on Linux and are
already recorded PASS in 88.3-UAT.md.
