---
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
verified: 2026-06-13T18:30:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 88.3: Bundle QtWebEngine in Frozen Windows Build Verification Report

**Phase Goal:** GBS.FM, Twitch, and Google/YouTube in-app OAuth logins open the QtWebEngine login window from the frozen Windows build instead of crashing the helper subprocess (exit=2). Approach: B1 isolated-helper-bundle -- oauth_helper.py is frozen as its OWN PyInstaller exe from an isolated pip-PySide6 env (no conda, no GStreamer); the conda main exe launches that separate oauth_helper.exe.
**Verified:** 2026-06-13T18:30:00Z
**Status:** PASSED
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Conda main bundle (MusicStreamer.spec) contains NO QtWebEngine hiddenimports (B1 invariant: conda-forge has no PySide6 WebEngine bindings) | VERIFIED | `grep -v '^[[:space:]]*#' MusicStreamer.spec \| grep -c QtWebEngine` returns 0. `PySide6.QtNetwork` and `PySide6.QtSvg` present (surgical revert confirmed). |
| 2 | `musicstreamer/__main__.py` has no conda-process WebEngine guard (`_run_check_webengine`, `--check-webengine`) but retains the `--oauth-helper` arm from Phase 88.2 | VERIFIED | `grep -c '_run_check_webengine\|--check-webengine\|args.check_webengine' __main__.py` returns 0; `grep -c '_run_oauth_helper\|--oauth-helper\|_run_check_mediakeys' __main__.py` returns 10. |
| 3 | `build.ps1` has no conda-side WebEngine preflight or step-4e guard; retains SMTC SMOKE GUARD (exit 11) and OAUTH HELPER GUARD (exit 12) | VERIFIED | No WEBENGINE GUARD, --check-webengine, pyside6_webengine_missing, or SkipWebEngineGuard on non-comment lines. SMTC SMOKE GUARD and OAUTH HELPER GUARD strings confirmed present. |
| 4 | `packaging/windows/oauth_helper_standalone.spec` freezes `musicstreamer/oauth_helper.py` with PySide6.QtWebEngineWidgets + PySide6.QtWebEngineCore hiddenimports; uses `parents[1]` for REPO_ROOT | VERIFIED | Both WebEngine strings present on non-comment lines. `parents[1]` present; `parents[2]` absent. `name="oauth_helper"` in EXE and COLLECT. |
| 5 | `packaging/windows/oauth-helper-requirements.txt` pins PySide6-Essentials==6.11.0, PySide6-Addons==6.11.0, pyinstaller==6.19.0, pyinstaller-hooks-contrib==2026.2 (bumped from 6.10.1 at 88.3-05 UAT for Twitch Kasada) | VERIFIED | All four exact pins confirmed in file. 6.11.0 bump confirmed in both file and test docstring. |
| 6 | `build.ps1` builds the isolated helper from a conda-free Python 3.12 venv, sanitizes PATH of conda/GStreamer for the helper PyInstaller step, asserts QtWebEngineProcess.exe + Qt6WebEngineCore.dll via exit 14, and adds SHA256 ABI guard asserting bundled Qt6Core.dll matches isolated pip venv Qt6Core.dll | VERIFIED | `oauth_helper_standalone.spec`, `oauth-helper-requirements.txt`, `HelperPythonExe`, PATH sanitization block, QtWebEngineProcess.exe + Qt6WebEngineCore.dll assertions, `exit 14`, `14=` header, and `helper_qt6core_abi_mismatch` SHA256 guard all confirmed in build.ps1. |
| 7 | `packaging/windows/MusicStreamer.iss` ships the helper bundle to `{app}\oauth_helper` (local path, Chromium sandbox requirement); `musicstreamer/subprocess_utils.py` frozen branch resolves sibling `oauth_helper.exe` path, not self-re-exec | VERIFIED | ISS: `Source: "..\..\dist\oauth_helper\*"; DestDir: "{app}\oauth_helper"` confirmed. subprocess_utils.py: `Path(sys.executable).parent / "oauth_helper" / "oauth_helper.exe"` present; `grep -v '^[[:space:]]*#' subprocess_utils.py \| grep -c '"--oauth-helper"'` returns 0. |
| 8 | GBS.FM (G6-T6), Twitch (G6-T7), and Google/YouTube (G6-T8) in-app logins open the QtWebEngine login window from the installed two-artifact frozen build; no exit=2, no DLL-load failure; Twitch Kasada accepted | VERIFIED | Recorded in 88.3-UAT.md (status: resolved, 4/4 pass). Two root-cause fixes confirmed committed: `45c3ce7c` (PATH isolation + SHA256 ABI guard), `46f30049` (PySide6 6.11.0 for Kasada). UAT evidence accepted as the human-verification record per the Windows-packaging context (cannot re-run on Linux). |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_packaging_webengine_spec.py` | B1 drift-guards G6-T1..T5 (WebEngine absent from conda artifact) | VERIFIED | 5 tests, all GREEN in live run |
| `tests/test_packaging_helper_build_spec.py` | Drift-guards for isolated helper spec, requirements, build.ps1 step, ISS, README | VERIFIED | 9 tests (pins updated to 6.11.0 at 88.3-05), all GREEN in live run |
| `tests/test_oauth_launch_path.py` | Launch-target and platform-UA drift-guards | VERIFIED | 8 tests, all GREEN in live run |
| `tests/test_launcher_frozen_dispatch.py` | Frozen/non-frozen dispatch contract (updated for B1) | VERIFIED | 4 tests, all GREEN in live run |
| `packaging/windows/oauth_helper_standalone.spec` | Standalone spec freezing oauth_helper.py with WebEngine hiddenimports | VERIFIED | WebEngine hiddenimports present, parents[1], name="oauth_helper" |
| `packaging/windows/oauth-helper-requirements.txt` | Pinned isolated helper env (PySide6 6.11.0, pyinstaller 6.19.0) | VERIFIED | All four exact pins confirmed |
| `packaging/windows/build.ps1` | Second build step, PATH isolation, ABI guard, exit 14, HelperPythonExe | VERIFIED | All features confirmed |
| `packaging/windows/MusicStreamer.iss` | Two-artifact installer shipping helper to {app}\oauth_helper | VERIFIED | Second Source row confirmed |
| `musicstreamer/subprocess_utils.py` | B1 frozen branch targets sibling oauth_helper.exe | VERIFIED | Path resolution confirmed, self-re-exec flag removed |
| `musicstreamer/oauth_helper.py` | Platform-aware _CHROME_UA (Windows NT on win32, Linux elsewhere) | VERIFIED | Both branches present, QTWEBENGINE_CHROMIUM_FLAGS set before QtWebEngine import |
| `.planning/phases/.../88.3-UAT.md` | Recorded UAT evidence for G6-T6/T7/T8 | VERIFIED | status: resolved, 4/4 pass, all three login flows recorded PASS |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `build.ps1` HELPER BUILD step | `oauth_helper_standalone.spec` | Second PyInstaller invocation from isolated venv | VERIFIED | `oauth_helper_standalone.spec` reference confirmed in build.ps1 |
| `build.ps1` HELPER BUILD step | `oauth-helper-requirements.txt` | `pip install -r oauth-helper-requirements.txt` in isolated venv | VERIFIED | Reference confirmed; PATH sanitization + `-HelperPythonExe` confirmed |
| `MusicStreamer.iss` [Files] | `dist\oauth_helper` | Source row installing to `{app}\oauth_helper` | VERIFIED | Second Source row present |
| `musicstreamer/subprocess_utils.py` frozen branch | `{app}\oauth_helper\oauth_helper.exe` | `Path(sys.executable).parent / "oauth_helper" / "oauth_helper.exe"` | VERIFIED | Implementation matches the ISS install path exactly |
| `packaging/windows/build.ps1` | SHA256 ABI guard | Bundled Qt6Core.dll hash == isolated pip venv Qt6Core.dll hash | VERIFIED | `helper_qt6core_abi_mismatch` BUILD_FAIL guard confirmed (commit `45c3ce7c`) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 26 drift-guard tests pass | `.venv/bin/python -m pytest tests/test_packaging_helper_build_spec.py tests/test_packaging_webengine_spec.py tests/test_oauth_launch_path.py tests/test_launcher_frozen_dispatch.py -q` | 26 passed, 1 warning (PyGI deprecation, unrelated) in 0.15s | PASS |
| Conda spec contains no WebEngine hiddenimports | `grep -v '^[[:space:]]*#' packaging/windows/MusicStreamer.spec \| grep -c QtWebEngine` | 0 | PASS |
| `__main__.py` has no webengine guard | `grep -c '_run_check_webengine\|--check-webengine\|args.check_webengine' musicstreamer/__main__.py` | 0 | PASS |
| 88.1/88.2 guards survive | `grep 'SMTC SMOKE GUARD\|OAUTH HELPER GUARD' packaging/windows/build.ps1` | Both strings found | PASS |
| WR-01 compliant (`Write-Error` absent) | `grep -c 'Write-Error' packaging/windows/build.ps1` | 0 | PASS |
| Gap-closure commits exist | `git show --stat 45c3ce7c 46f30049` | Both commits confirmed with correct file changes | PASS |

### Requirements Coverage

The G6-T1..G6-T8 requirement IDs are internal phase identifiers declared in the ROADMAP for phase 88.3. They do not map to the v2.2 `REQUIREMENTS.md` categorical IDs (WIN-, PKG-, etc.) -- the ROADMAP maps phase 88.3 as an inserted sub-phase of Phase 88, which covers VER-02-J (Win11 VM packaging UAT). The G6 sub-requirements are tracked exclusively at the ROADMAP and PLAN level.

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| G6-T1 | conda spec has NO WebEngine hiddenimports (B1 negative gate) | SATISFIED | `test_spec_has_no_webengine_hiddenimports` GREEN; grep confirms 0 matches |
| G6-T2 | conda spec still has PySide6.QtNetwork + PySide6.QtSvg (surgical revert) | SATISFIED | `test_spec_still_has_network_and_svg` GREEN; both strings confirmed |
| G6-T3 | `__main__.py` has no conda-process WebEngine guard; retains `--oauth-helper` | SATISFIED | `test_main_py_has_no_conda_webengine_guard` GREEN; grep confirms |
| G6-T4 | `build.ps1` has no conda-side WebEngine preflight/step-4e guard | SATISFIED | `test_build_ps1_has_no_conda_webengine_guard` GREEN; grep confirms 0 matches on all four patterns |
| G6-T5 | `build.ps1` retains SMTC + OAUTH HELPER guards (no collateral damage) | SATISFIED | `test_build_ps1_keeps_smtc_and_oauth_guards` GREEN; both guard strings confirmed |
| G6-T6 | GBS.FM in-app login opens QtWebEngine window; no exit=2; cookie-fallback not triggered | SATISFIED | 88.3-UAT.md Test 2 PASS (recorded 2026-06-13) |
| G6-T7 | Twitch in-app login opens and is accepted by Kasada (no "Browser not supported") | SATISFIED | 88.3-UAT.md Test 3 PASS; requires PySide6 6.11.0 (commit `46f30049`) |
| G6-T8 | Google/YouTube in-app login opens QtWebEngine window; no exit=2 | SATISFIED | 88.3-UAT.md Test 4 PASS (recorded 2026-06-13) |

### Anti-Patterns Found

No debt markers (TBD, FIXME, XXX) or stub patterns found in any file modified by this phase. All implementations are complete. The phase's TODO items were explicitly resolved: the PATH isolation and Kasada Chromium-version issues surfaced at the VM UAT were root-caused, fixed Linux-side, and re-validated by a VM rebuild within the same UAT session.

### Human Verification Required

None. The VM UAT was performed by the operator and recorded in `88.3-UAT.md` (status: resolved, 4/4 pass, updated 2026-06-13T13:10:00Z). Per the phase context, the recorded operator UAT is accepted as the human-verification evidence for G6-T6/T7/T8 -- no re-running of the Windows build or logins on the Linux host is possible or required.

### Gaps Summary

No gaps. All 8 must-haves are verified. The five automated drift-guard requirements (G6-T1..T5) are confirmed GREEN by a live test run (26/26 tests pass). The three VM UAT requirements (G6-T6..T8) are confirmed by recorded operator UAT in 88.3-UAT.md. The two root-cause defects found during UAT (conda Qt6Core contaminating the helper build PATH; Twitch Kasada rejecting Qt 6.10 Chromium) were both fixed and committed before UAT closure.

---

_Verified: 2026-06-13T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
