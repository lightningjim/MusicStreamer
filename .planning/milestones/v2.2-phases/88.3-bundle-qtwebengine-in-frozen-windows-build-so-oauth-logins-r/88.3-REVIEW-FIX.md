---
phase: 88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r
fixed_at: 2026-06-13T00:00:00Z
review_path: .planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-REVIEW.md
iteration: 1
findings_in_scope: 10
fixed: 10
skipped: 0
status: all_fixed
---

# Phase 88.3: Code Review Fix Report

**Fixed at:** 2026-06-13
**Source review:** .planning/phases/88.3-bundle-qtwebengine-in-frozen-windows-build-so-oauth-logins-r/88.3-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 10 (fix_scope = all: 6 warnings + 4 info)
- Fixed: 10
- Skipped: 0

## Fixed Issues

### WR-01: ABI-coherence guard silently degrades to a no-op

**Files modified:** `packaging/windows/build.ps1`
**Commit:** 9493a080
**Applied fix:** Inverted the guard so an unresolvable `Qt6Core.dll` path is a HARD build failure (`exit 14`, `reason=helper_qt6core_abi_guard_indeterminate`) instead of a yellow-warning skip. The hash-comparison flow that follows is unchanged. A guard whose miss-mode now refuses to ship an unverifiable bundle.

### WR-02: `_run_oauth_helper` non-self-test arm hits a `sys.exit(2)` trap in the conda bundle

**Files modified:** `musicstreamer/__main__.py`, `tests/test_launcher_frozen_dispatch.py`
**Commit:** 74552ba1
**Applied fix:** Replaced the reachable dead-trap non-self-test branch (which did `from musicstreamer.oauth_helper import main` — triggering the WebEngine import that bare `sys.exit(2)`s in the conda bundle) with a clear stderr diagnostic and `return 2`. Updated the docstring to describe the B1 out-of-process contract. The coupled test was rewritten to assert the new rejection contract. **Also resolves IN-02** (the unreachable `return 0` no longer exists). Committed together with WR-06 because the same test file change covers both.

### WR-03: README states the wrong helper PySide6 version (6.10.1 vs pinned 6.11.0)

**Files modified:** `packaging/windows/README.md`
**Commit:** 67e564c6
**Applied fix:** Updated the file-map row from `PySide6 6.10.1` to `PySide6 6.11.0` to match the requirements pin and the 88.3-05 Kasada fix.

### WR-04: README states the wrong install location

**Files modified:** `packaging/windows/README.md`
**Commit:** a87bda40
**Applied fix:** Corrected the "Install behavior" path from `%LOCALAPPDATA%\MusicStreamer` to `%LOCALAPPDATA%\Programs\MusicStreamer`, matching the `.iss` `{userpf}\MusicStreamer` `DefaultDirName`.

### WR-05: `.iss` comment references a nonexistent function/module

**Files modified:** `packaging/windows/MusicStreamer.iss`
**Commit:** 3e625dda
**Applied fix:** Replaced the phantom `_oauth_helper_exe() in musicstreamer/oauth_launcher.py` reference with the real resolver `_make_oauth_launch_args() in musicstreamer/subprocess_utils.py`. Note: the finding also cited `subprocess_utils.py:44`, but that docstring contains no such phantom reference (verified by grep) — its contract description is already correct, so only the `.iss` comment needed correcting.

### WR-06: test mutates global `sys.argv` without restoring it

**Files modified:** `tests/test_launcher_frozen_dispatch.py`
**Commit:** 74552ba1 (committed together with WR-02)
**Applied fix:** The root cause — the `sys.argv = [...]` mutation in `_run_oauth_helper`'s non-self-test arm — was removed entirely by the WR-02 fix, so the leak source no longer exists. The rewritten test additionally adds `monkeypatch.setattr(sys, "argv", list(sys.argv))` as a belt-and-suspenders auto-restore guard.

### IN-01: `importlib.reload(su)` in frozen-dispatch test is unnecessary

**Files modified:** `tests/test_launcher_frozen_dispatch.py`
**Commit:** 18bf1fc1
**Applied fix:** Removed the `importlib.reload(su)` ceremony from both frozen/non-frozen tests and the misleading "reads sys at module scope" comment. The function reads `sys.frozen`/`sys.executable` per call, so the function is now imported directly like the sibling `test_oauth_launch_path.py`.

### IN-02: `_run_oauth_helper` returns unreachable `0` after `_oauth_main()`

**Files modified:** `musicstreamer/__main__.py`
**Commit:** 74552ba1 (resolved as a side effect of WR-02)
**Applied fix:** The WR-02 rewrite removed the `_oauth_main()` call and its trailing unreachable `return 0` entirely. The function now returns `2` after a clear diagnostic, so there is no dead-code return left.

### IN-03: README PySide6 version drift is untested

**Files modified:** `tests/test_packaging_helper_build_spec.py`
**Commit:** ba26ced7
**Applied fix:** Added `test_readme_helper_pyside6_version_matches_requirements_pin`, which extracts the pinned `PySide6*==X.Y.Z` version from `oauth-helper-requirements.txt` and asserts the README mentions no other PySide6 version string. Verified non-vacuous: it captures `6.11.0` from both the README row and the pin. This couples the two sources that WR-03 drifted between.

### IN-04: `_GoogleWindow` success path emits no structured `Success` event

**Files modified:** `musicstreamer/oauth_helper.py`
**Commit:** 43b2b8b6
**Status:** fixed: requires human verification
**Applied fix:** Added `_emit_event("Success", detail="")` before `QApplication.quit()` in `_GoogleWindow._flush_cookies`, matching the Twitch/GBS happy paths so `_parse_oauth_stderr` sees a final category-bearing event. `detail` is empty string (T-76-02). Flagged for human verification because the source file marks the Google path "UNTOUCHED — working as-is," so a maintainer should confirm this added runtime event does not perturb a frozen Google login contract (no existing test asserts Google Success-absence; the only Google test checks class presence).

## Skipped Issues

None — all in-scope findings were fixed.

## Notes

- `tests/test_oauth_launch_path.py::test_ua_linux_token_on_non_win32` fails in this Linux environment because `PySide6.QtWebEngineWidgets` is not installed, so importing `oauth_helper` hits the module-level guard `sys.exit(2)` (oauth_helper.py:86). This failure was confirmed PRE-EXISTING on the base commit `437230ac` and is unrelated to any fix here — none of the fixes touched that test or that import guard.

---

_Fixed: 2026-06-13_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
