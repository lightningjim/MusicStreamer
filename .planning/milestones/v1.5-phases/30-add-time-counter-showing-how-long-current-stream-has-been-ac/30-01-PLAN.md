---
phase: 30-add-time-counter-showing-how-long-current-stream-has-been-ac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/ui/main_window.py
autonomous: true
requirements:
  - TIMER-01
  - TIMER-02
  - TIMER-03
  - TIMER-04
  - TIMER-05
  - TIMER-06

must_haves:
  truths:
    - "Elapsed time label visible and ticking while a stream is playing"
    - "Timer pauses when stream is paused and resumes with correct accumulated time"
    - "Timer resets to 0:00 when switching to a different station"
    - "Timer stays hidden when nothing is playing"
    - "Timer displays M:SS under 1 hour and H:MM:SS at 1 hour+"
  artifacts:
    - path: "musicstreamer/ui/main_window.py"
      provides: "Timer row widget, GLib tick callback, format helper, lifecycle methods"
      contains: "_on_timer_tick"
  key_links:
    - from: "_play_station"
      to: "_start_timer"
      via: "called at end of _play_station"
      pattern: "_start_timer"
    - from: "_toggle_pause"
      to: "_pause_timer/_resume_timer"
      via: "pause branch calls _pause_timer, resume branch saves/restores elapsed"
      pattern: "_pause_timer|_resume_timer|saved_elapsed"
    - from: "_stop"
      to: "_stop_timer"
      via: "called in _stop to hide and reset"
      pattern: "_stop_timer"
---

<objective>
Add an elapsed time counter to the now-playing panel that shows how long the current stream has been actively playing.

Purpose: Give users awareness of how long they have been listening to the current station.
Output: Timer row (icon + label) in now-playing panel with full play/pause/stop lifecycle integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-add-time-counter-showing-how-long-current-stream-has-been-ac/30-CONTEXT.md
@.planning/phases/30-add-time-counter-showing-how-long-current-stream-has-been-ac/30-RESEARCH.md

<interfaces>
<!-- From musicstreamer/ui/main_window.py — now-playing center column layout -->

Center column widget order (lines 114-172):
  center = Gtk.Box(orientation=Gtk.Orientation.VERTICAL, spacing=4)
  center.append(self.title_label)         # line 123
  center.append(self.station_name_label)  # line 130
  center.append(controls_box)             # line 172 — timer row inserts BEFORE this

Key state variables:
  self._current_station  # Station | None — set in _play_station, cleared in _stop
  self._paused           # bool — set True in pause branch, False in resume/_stop

Key methods:
  _play_station(st: Station)  # line 797 — starts playback, sets UI
  _toggle_pause()             # line 731 — pause/resume; resume calls _play_station(self._current_station)
  _stop()                     # line 760 — stops playback, resets all UI

GLib already imported at line 7:
  from gi.repository import Gtk, Adw, Pango, GdkPixbuf, GLib, Gio
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timer widget and helper methods</name>
  <files>musicstreamer/ui/main_window.py</files>
  <read_first>
    musicstreamer/ui/main_window.py (lines 81-185 for panel construction, lines 731-810 for play/pause/stop)
  </read_first>
  <action>
Add the timer row widget and all timer helper methods to MainWindow.

**Widget construction** — insert between `center.append(self.station_name_label)` (line 130) and `controls_box = Gtk.Box(...)` (line 132):

```python
# Elapsed time counter row (TIMER-01, per D-01, D-02)
self.timer_row = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL, spacing=4)
self.timer_row.set_visible(False)

timer_icon = Gtk.Image.new_from_icon_name("timer-symbolic")
timer_icon.add_css_class("dim-label")
timer_icon.set_pixel_size(16)
self.timer_row.append(timer_icon)

self.timer_label = Gtk.Label(label="0:00")
self.timer_label.add_css_class("dim-label")
self.timer_label.set_xalign(0)
self.timer_row.append(self.timer_label)

center.append(self.timer_row)
```

**Instance variables** — add in `__init__` near other state variables (near `_paused`, `_current_station`):

```python
self._elapsed_seconds: int = 0
self._timer_source_id: int | None = None
```

**Helper methods** — add as new methods on MainWindow (group near `_stop`):

1. `_format_elapsed(self, seconds: int) -> str` — per D-07:
   - `h, rem = divmod(seconds, 3600)` then `m, s = divmod(rem, 60)`
   - If `h`: return `f"{h}:{m:02d}:{s:02d}"`
   - Else: return `f"{m}:{s:02d}"`

2. `_update_timer_label(self)` — sets `self.timer_label.set_text(self._format_elapsed(self._elapsed_seconds))`

3. `_on_timer_tick(self) -> bool` — per D-06:
   - Increment `self._elapsed_seconds += 1`
   - Call `self._update_timer_label()`
   - Return `True` (keep ticking; removal via `_stop_timer`)

4. `_start_timer(self)` — per D-05, D-06:
   - Call `self._stop_timer()` first (prevent double source, per RESEARCH pitfall 1)
   - Set `self._elapsed_seconds = 0`
   - Call `self._update_timer_label()`
   - Set `self.timer_row.set_visible(True)`
   - Set `self._timer_source_id = GLib.timeout_add_seconds(1, self._on_timer_tick)`

5. `_pause_timer(self)` — per D-03:
   - If `self._timer_source_id is not None`: call `GLib.source_remove(self._timer_source_id)`, set `self._timer_source_id = None`

6. `_resume_timer(self)` — per D-03:
   - If `self._timer_source_id is None` and `self.timer_row.get_visible()`:
     `self._timer_source_id = GLib.timeout_add_seconds(1, self._on_timer_tick)`

7. `_stop_timer(self)` — per D-05:
   - If `self._timer_source_id is not None`: call `GLib.source_remove(self._timer_source_id)`, set `self._timer_source_id = None`
   - Set `self._elapsed_seconds = 0`
   - Set `self.timer_row.set_visible(False)`

Per D-08: both `timer_icon` and `self.timer_label` use `dim-label` CSS class (already in widget construction above).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -c "
from musicstreamer.ui.main_window import MainWindow
# Verify methods exist
for m in ['_format_elapsed','_update_timer_label','_on_timer_tick','_start_timer','_pause_timer','_resume_timer','_stop_timer']:
    assert hasattr(MainWindow, m), f'Missing method: {m}'
# Verify format logic
mw = type('MW', (), {'_format_elapsed': MainWindow._format_elapsed})()
assert mw._format_elapsed(0) == '0:00'
assert mw._format_elapsed(61) == '1:01'
assert mw._format_elapsed(3661) == '1:01:01'
assert mw._format_elapsed(3600) == '1:00:00'
assert mw._format_elapsed(59) == '0:59'
print('All checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep: `self.timer_row = Gtk.Box` in main_window.py
    - grep: `timer-symbolic` in main_window.py
    - grep: `dim-label` applied to both timer_icon and timer_label
    - grep: `def _format_elapsed` in main_window.py
    - grep: `def _on_timer_tick` in main_window.py
    - grep: `def _start_timer` in main_window.py
    - grep: `def _stop_timer` in main_window.py
    - grep: `GLib.timeout_add_seconds` in main_window.py
    - grep: `GLib.source_remove` in main_window.py
    - grep: `self._elapsed_seconds` in main_window.py
    - grep: `self._timer_source_id` in main_window.py
  </acceptance_criteria>
  <done>Timer row widget exists in center column between station_name_label and controls_box. All 7 helper methods defined. Format helper produces correct M:SS and H:MM:SS output. Widget starts hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Wire timer into play/pause/stop lifecycle</name>
  <files>musicstreamer/ui/main_window.py</files>
  <read_first>
    musicstreamer/ui/main_window.py (lines 731-810 for _toggle_pause, _stop, _play_station)
  </read_first>
  <action>
Wire the timer methods into the three playback state transitions.

**In `_play_station` (line 797)** — add at the END of the method (after all existing UI setup):

```python
self._start_timer()
```

This resets elapsed to 0 and starts ticking. Per D-04, station changes go through `_play_station` so timer resets. Stream failover uses `_on_stream_failover` which does NOT call `_play_station`, so failover does not reset the timer (D-04 satisfied automatically — confirmed in RESEARCH.md assumption A1).

**In `_toggle_pause` (line 731)** — modify BOTH branches:

Pause branch (the `elif` at line 739, after `self.player.pause()`):
```python
self._pause_timer()
```

Resume branch (the `if` at line 732) — replace the existing resume logic to preserve elapsed time. The resume branch calls `self._play_station(self._current_station)` which would reset the timer. Save and restore elapsed seconds around it (per RESEARCH.md recommended approach):

```python
# Resume: replay same station
self._paused = False
self._paused_station = None
self.pause_btn.set_icon_name("media-playback-pause-symbolic")
self.pause_btn.set_tooltip_text("Pause")
saved_elapsed = self._elapsed_seconds  # preserve timer across resume
self._play_station(self._current_station)
self._elapsed_seconds = saved_elapsed  # restore after _play_station reset
self._update_timer_label()
```

**In `_stop` (line 760)** — add near the top, after `self.player.stop()`:

```python
self._stop_timer()
```

This hides the timer row, cancels the GLib source, and resets elapsed to 0 (per D-05).
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && python -c "
import ast, sys
with open('musicstreamer/ui/main_window.py') as f:
    source = f.read()
tree = ast.parse(source)
for node in ast.walk(tree):
    if isinstance(node, ast.FunctionDef):
        body_src = ast.get_source_segment(source, node)
        if node.name == '_play_station':
            assert '_start_timer' in body_src, '_play_station missing _start_timer call'
        elif node.name == '_stop':
            assert '_stop_timer' in body_src, '_stop missing _stop_timer call'
        elif node.name == '_toggle_pause':
            assert '_pause_timer' in body_src, '_toggle_pause missing _pause_timer call'
            assert 'saved_elapsed' in body_src, '_toggle_pause missing saved_elapsed pattern'
print('All lifecycle wiring verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep: `self._start_timer()` in _play_station method
    - grep: `self._stop_timer()` in _stop method
    - grep: `self._pause_timer()` in _toggle_pause method
    - grep: `saved_elapsed = self._elapsed_seconds` in _toggle_pause method
    - grep: `self._elapsed_seconds = saved_elapsed` in _toggle_pause resume branch
    - grep: `self._update_timer_label()` in _toggle_pause resume branch
  </acceptance_criteria>
  <done>Timer starts on play, pauses/resumes correctly preserving elapsed seconds, stops and hides on stop. Resume via _toggle_pause does not reset the counter. Station change resets to 0:00.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This phase adds a local UI timer with no external input, no network calls, no user-supplied data beyond what already exists.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | D (Denial of Service) | GLib timer source leak | mitigate | `_start_timer` always calls `_stop_timer` first; `_stop` calls `_stop_timer`; prevents accumulating orphan sources |
</threat_model>

<verification>
1. Launch app, play a station — timer row appears showing `0:00`, ticks up each second
2. Pause — timer freezes at current value
3. Resume — timer continues from paused value (not reset)
4. Switch to different station — timer resets to `0:00` and starts over
5. Stop playback — timer row disappears
6. Let timer run past 1 hour (or manually verify `_format_elapsed(3661)` returns `1:01:01`)
</verification>

<success_criteria>
- Timer row visible between station name and controls when playing
- Timer ticks up in real time (1 second intervals)
- Pause freezes, resume continues from correct value
- Station change resets to 0:00
- Stop hides timer completely
- Format switches from M:SS to H:MM:SS at 1 hour
</success_criteria>

<output>
After completion, create `.planning/phases/30-add-time-counter-showing-how-long-current-stream-has-been-ac/30-01-SUMMARY.md`
</output>
