---
phase: 43-gstreamer-windows-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-gstreamer-windows-spike/43-spike.spec
  - .planning/phases/43-gstreamer-windows-spike/runtime_hook.py
  - .planning/phases/43-gstreamer-windows-spike/smoke_test.py
  - .planning/phases/43-gstreamer-windows-spike/build.ps1
  - .planning/phases/43-gstreamer-windows-spike/.gitignore
  - .planning/phases/43-gstreamer-windows-spike/README.md
autonomous: true
requirements: [PKG-06]

must_haves:
  truths:
    - "All four build artifacts exist and carry the exact skeletons from 43-RESEARCH.md"
    - "smoke_test.py redacts URL query string (listen key) before any logging — T-43-01 mitigation"
    - "build.ps1 has deterministic SPIKE_OK/SPIKE_FAIL exit-code contract"
    - ".gitignore excludes test_url.txt, artifacts/, build/, dist/ — listen key never enters git"
    - "README.md gives the user a one-paragraph runbook for iteration 1"
  artifacts:
    - path: ".planning/phases/43-gstreamer-windows-spike/43-spike.spec"
      provides: "PyInstaller .spec with Tree() blocks for gio/modules, girepository, glib schemas + runtime_hook.py registration"
      contains: "gio_modules_tree, typelib_tree, runtime_hooks=[\"runtime_hook.py\"]"
    - path: ".planning/phases/43-gstreamer-windows-spike/runtime_hook.py"
      provides: "Env var setup before Gst.init: GIO_EXTRA_MODULES, GI_TYPELIB_PATH, GST_PLUGIN_SCANNER"
      contains: "GIO_EXTRA_MODULES"
    - path: ".planning/phases/43-gstreamer-windows-spike/smoke_test.py"
      provides: "Freestanding CLI smoke test — playbin3 + souphttpsrc HTTPS, exit codes 0/1/2/3, SPIKE_OK/FAIL/DIAG markers, listen-key redaction"
      contains: "SPIKE_OK"
    - path: ".planning/phases/43-gstreamer-windows-spike/build.ps1"
      provides: "PowerShell build driver: pre-flight DLL checks, pip-install deps, pyinstaller, smoke run, tee to artifacts/smoke.log"
      contains: "pyinstaller"
    - path: ".planning/phases/43-gstreamer-windows-spike/.gitignore"
      provides: "Secret + build-artifact exclusions"
      contains: "test_url.txt"
    - path: ".planning/phases/43-gstreamer-windows-spike/README.md"
      provides: "User runbook: install MSIs, populate test_url.txt, run build.ps1, paste smoke.log back"
      min_lines: 10
  key_links:
    - from: "43-spike.spec"
      to: "runtime_hook.py"
      via: "runtime_hooks=[\"runtime_hook.py\"] block in Analysis()"
      pattern: "runtime_hooks"
    - from: "runtime_hook.py"
      to: "dist/spike/_internal/gio/modules/libgiognutls.dll"
      via: "GIO_EXTRA_MODULES env var pointing at bundle-relative path"
      pattern: "GIO_EXTRA_MODULES"
    - from: "smoke_test.py"
      to: "Gio.TlsBackend"
      via: "_assert_tls_backend() checks get_default_database() to prove libgiognutls loaded"
      pattern: "Gio.TlsBackend"
    - from: "build.ps1"
      to: "smoke_test.py"
      via: "Invokes .\\dist\\spike\\spike.exe <test_url> after successful PyInstaller build"
      pattern: "spike.exe"
---

<objective>
Create the four build artifacts (`43-spike.spec`, `runtime_hook.py`, `smoke_test.py`, `build.ps1`) plus `.gitignore` and `README.md` so the user can run a first-iteration build on the Windows 11 VM.

Purpose: Wave 1 is pure skeleton landing — no VM execution, no iteration. All code comes verbatim from `43-RESEARCH.md` skeletons. This closes the Wave 0 gaps listed in `43-VALIDATION.md` so Wave 2 (paste-back iteration) can run.

Output: Six committed files in the phase directory. The user can take them to the Windows VM and run `.\build.ps1` to produce the first paste-back log for Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-gstreamer-windows-spike/43-CONTEXT.md
@.planning/phases/43-gstreamer-windows-spike/43-RESEARCH.md
@.planning/phases/43-gstreamer-windows-spike/43-VALIDATION.md

<interfaces>
<!-- Code-ready skeletons the executor copies verbatim. All live in 43-RESEARCH.md. -->

- 43-RESEARCH.md §"PyInstaller `.spec` Structure — Draft" — full spike.spec body, lines ~309-454
- 43-RESEARCH.md §"Runtime Hook Template" — full runtime_hook.py body, lines ~458-518
- 43-RESEARCH.md §"`build.ps1` Skeleton" — full build.ps1 body, lines ~522-610
- 43-RESEARCH.md §"`smoke_test.py` — Exit-Code Contract" — full smoke_test.py body, lines ~614-777
- 43-RESEARCH.md §"Recommended Project Structure" — phase dir layout + what goes in .gitignore (lines ~213-231)
- 43-RESEARCH.md §"Runtime State Inventory" — confirms .gitignore contents (lines ~297-307)

Executor copies these skeletons verbatim. The only adjustments are:
  1. Absolute paths stay as-shown (C:\spike-gst\...) — build.ps1 param defaults.
  2. Redaction is already in smoke_test.py line ~693 (`url.split("?", 1)[0] + "?<redacted>"`) — preserve exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land spec + runtime_hook (PyInstaller skeleton)</name>
  <files>
    .planning/phases/43-gstreamer-windows-spike/43-spike.spec
    .planning/phases/43-gstreamer-windows-spike/runtime_hook.py
  </files>
  <read_first>
    43-RESEARCH.md §"PyInstaller `.spec` Structure — Draft" (full code block, ~lines 315-454).
    43-RESEARCH.md §"Runtime Hook Template" (full code block, ~lines 464-518).
    43-RESEARCH.md §"Known Failure Modes" #1 and #4 — explains WHY GIO_EXTRA_MODULES and the gst-plugins path matter.
  </read_first>
  <action>
    Copy the `.spec` skeleton verbatim from RESEARCH §"PyInstaller `.spec` Structure — Draft" into `43-spike.spec`. Do NOT improvise structure. Preserve:
    - `GST_ROOT` env-var default at `C:\spike-gst\runtime\1.0\msvc_x86_64` (matches build.ps1 param default, D-01 clean-snapshot path).
    - All three `Tree()` blocks (`gio_modules_tree`, `typelib_tree`, `glib_share_tree`).
    - `extra_binaries` entry for `gst-plugin-scanner.exe` in `libexec/gstreamer-1.0/` (with the older-MSI-layout comment preserved, commented out).
    - `hooksconfig["gstreamer"]` empty dict with the "ITERATION 1: broad" comment block intact (per RESEARCH Pattern 1).
    - `hooksconfig["gi"]` with empty icons/themes/languages.
    - `runtime_hooks=["runtime_hook.py"]`.
    - `upx=False` on both EXE and COLLECT (RESEARCH anti-pattern: UPX breaks GStreamer DLL loading).

    Copy the `runtime_hook.py` skeleton verbatim from RESEARCH §"Runtime Hook Template". Preserve:
    - `_bundle_path()` helper using `sys._MEIPASS`.
    - All three env var sets: `GIO_EXTRA_MODULES`, `GI_TYPELIB_PATH`, `GST_PLUGIN_SCANNER` (scanner guarded by `os.path.isfile`).
    - The `SPIKE_DIAG_RTHOOK` stderr marker at the end — it's how paste-back confirms the rthook actually ran.
    - The "GIO_EXTRA_MODULES vs GIO_MODULE_DIR" comment (documents why additive beats replacing — assumption A4).

    Implements: PKG-06 (per D-11, D-12).
    Traceability: closes 43-VALIDATION.md rows 43-01-01 (.spec exists) and 43-01-02 (runtime_hook.py has GIO_EXTRA_MODULES).
  </action>
  <verify>
    <automated>
    test -f .planning/phases/43-gstreamer-windows-spike/43-spike.spec \
      && grep -q 'Tree(' .planning/phases/43-gstreamer-windows-spike/43-spike.spec \
      && grep -q 'runtime_hooks=\["runtime_hook.py"\]' .planning/phases/43-gstreamer-windows-spike/43-spike.spec \
      && grep -q 'upx=False' .planning/phases/43-gstreamer-windows-spike/43-spike.spec \
      && test -f .planning/phases/43-gstreamer-windows-spike/runtime_hook.py \
      && grep -q 'GIO_EXTRA_MODULES' .planning/phases/43-gstreamer-windows-spike/runtime_hook.py \
      && grep -q 'GI_TYPELIB_PATH' .planning/phases/43-gstreamer-windows-spike/runtime_hook.py \
      && grep -q 'GST_PLUGIN_SCANNER' .planning/phases/43-gstreamer-windows-spike/runtime_hook.py \
      && grep -q 'SPIKE_DIAG_RTHOOK' .planning/phases/43-gstreamer-windows-spike/runtime_hook.py \
      && python -c "import ast; ast.parse(open('.planning/phases/43-gstreamer-windows-spike/runtime_hook.py').read())"
    </automated>
  </verify>
  <done>Both files exist, carry the verbatim RESEARCH skeletons, and `runtime_hook.py` is syntactically valid Python.</done>
</task>

<task type="auto">
  <name>Task 2: Land smoke_test.py + build.ps1 (exit-code contract + build driver)</name>
  <files>
    .planning/phases/43-gstreamer-windows-spike/smoke_test.py
    .planning/phases/43-gstreamer-windows-spike/build.ps1
  </files>
  <read_first>
    43-RESEARCH.md §"`smoke_test.py` — Exit-Code Contract" (full code block, ~lines 616-777).
    43-RESEARCH.md §"`build.ps1` Skeleton" (full code block, ~lines 526-610).
    43-RESEARCH.md §"Known Failure Modes" #1 (TLS check via `Gio.TlsBackend.get_default_database()`) and §"ROADMAP Success-Criteria Mapping" (SC-1/SC-2 pass conditions).
  </read_first>
  <action>
    Copy the `smoke_test.py` skeleton verbatim from RESEARCH §"`smoke_test.py` — Exit-Code Contract" into `smoke_test.py`. Preserve:
    - Exit-code contract in module docstring: 0=ok, 1=setup, 2=runtime, 3=timeout.
    - `_emit()` helper with `flush=True`.
    - `_assert_tls_backend()` using `Gio.TlsBackend.get_default().get_default_database()` (SPIKE_DIAG marker is the SC-2 pass proof per RESEARCH §"ROADMAP Success-Criteria Mapping").
    - URL redaction on line ~693: `redacted = url.split("?", 1)[0] + "?<redacted>" if "?" in url else url` — T-43-01 mitigation. This runs BEFORE any `SPIKE_DIAG url=...` emit.
    - Bus handler watching `Gst.MessageType.TAG` as first-audio proxy (RESEARCH explains why: ICY tags arrive pre-demux proving TLS + HTTP).
    - 8-second timeout window with 5-second post-first-tag hold.
    - `playbin.set_state(Gst.State.NULL)` in `finally`.

    Copy the `build.ps1` skeleton verbatim from RESEARCH §"`build.ps1` Skeleton" into `build.ps1`. Preserve:
    - `#Requires -Version 5.1` header.
    - Param defaults `$GstRoot = "C:\spike-gst\runtime\1.0\msvc_x86_64"` and `$GstDevel = "C:\spike-gst\devel\1.0\msvc_x86_64"` (match D-01 clean-snapshot install paths).
    - `-SkipSmoke` switch (quick-run command per 43-VALIDATION.md).
    - Three pre-flight DLL existence checks: `libgstreamer-1.0-0.dll`, `gst-inspect-1.0.exe`, `libgiognutls.dll` — each emits a `SPIKE_FAIL reason=...` marker and exits with code 1. These are the "clean VM has devel + runtime + ADDLOCAL=ALL" gate per D-04 + assumption A1.
    - Env exports: `GSTREAMER_ROOT`, prepend `$GstRoot\bin` to PATH, clear PYTHONPATH.
    - `Remove-Item -Recurse -Force "build", "dist"` for idempotent rebuilds.
    - pip install of `pyinstaller>=6.11`, `pyinstaller-hooks-contrib`, `pygobject>=3.50`.
    - PyInstaller invocation tee'd to `artifacts\build.log`; smoke run tee'd to `artifacts\smoke.log` (user pastes the latter back).
    - Exit codes 0/1/2/3 matching the smoke_test contract.

    Create the `artifacts/` directory implicitly via `Tee-Object` — do NOT add a `mkdir artifacts` step (tee creates parent, and the dir is gitignored anyway).

    Implements: PKG-06 (per D-09, D-10, D-11).
    Traceability: closes 43-VALIDATION.md rows 43-01-03 (SPIKE_OK/FAIL in smoke), 43-01-04 (pyinstaller + smoke in build.ps1), 43-01-05 (redact pattern in smoke_test.py).
  </action>
  <verify>
    <automated>
    test -f .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && grep -q 'SPIKE_OK' .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && grep -q 'SPIKE_FAIL' .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && grep -q 'SPIKE_DIAG' .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && grep -q '?<redacted>' .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && grep -q 'Gio.TlsBackend' .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && grep -q 'get_default_database' .planning/phases/43-gstreamer-windows-spike/smoke_test.py \
      && python -c "import ast; ast.parse(open('.planning/phases/43-gstreamer-windows-spike/smoke_test.py').read())" \
      && test -f .planning/phases/43-gstreamer-windows-spike/build.ps1 \
      && grep -q 'pyinstaller' .planning/phases/43-gstreamer-windows-spike/build.ps1 \
      && grep -q 'smoke_test.py\|spike.exe' .planning/phases/43-gstreamer-windows-spike/build.ps1 \
      && grep -q 'libgiognutls.dll' .planning/phases/43-gstreamer-windows-spike/build.ps1 \
      && grep -q 'SPIKE_FAIL' .planning/phases/43-gstreamer-windows-spike/build.ps1 \
      && grep -q 'SkipSmoke' .planning/phases/43-gstreamer-windows-spike/build.ps1
    </automated>
  </verify>
  <done>Both files exist with the RESEARCH skeletons verbatim; smoke_test.py parses as valid Python; build.ps1 has all three pre-flight DLL guards.</done>
</task>

<task type="auto">
  <name>Task 3: Land .gitignore + README runbook</name>
  <files>
    .planning/phases/43-gstreamer-windows-spike/.gitignore
    .planning/phases/43-gstreamer-windows-spike/README.md
  </files>
  <read_first>
    43-RESEARCH.md §"Runtime State Inventory" (lines ~297-307) — confirms `test_url.txt`, `artifacts/`, `build/`, `dist/`, `*.spec.log` must be excluded.
    43-RESEARCH.md §"What Evidence Gets Committed" (lines ~962-974) — confirms committed vs gitignored boundary.
    43-VALIDATION.md §"Wave 0 Requirements" — the .gitignore bullet + the optional README bullet.
    43-CONTEXT.md §Phase Boundary — confirms the runbook frames this as throwaway spike, not production.
  </read_first>
  <action>
    Create `.gitignore` with exactly these entries (one per line, no blanks at top, trailing newline):
    ```
    # Phase 43 spike — never commit secrets or regenerable artifacts
    test_url.txt
    artifacts/
    build/
    dist/
    *.spec.log
    __pycache__/
    ```
    The `test_url.txt` line is the T-43-01 mitigation (D-07 listen-key exclusion). `artifacts/` excludes paste-back logs that may contain unredacted URLs. The rest are regenerable PyInstaller output.

    Create `README.md` as a one-paragraph runbook for the user. Use this exact structure (terse-direct per user profile):

    ```markdown
    # Phase 43 Spike Runbook

    Throwaway experiment: validate HTTPS GStreamer playback inside a PyInstaller `--onedir` bundle on a clean Windows 11 VM. Produces `43-SPIKE-FINDINGS.md` + a draft `.spec` for Phase 44 to inherit.

    ## One-time VM setup

    1. Revert the Win11 VM to a clean snapshot (no system GStreamer on PATH — D-01).
    2. Install both MSIs to `C:\spike-gst\`:
       - `gstreamer-1.0-msvc-x86_64-1.24.12.msi` → `INSTALLDIR="C:\spike-gst\runtime"`
       - `gstreamer-1.0-devel-msvc-x86_64-1.24.12.msi` → `INSTALLDIR="C:\spike-gst\devel"`
       - Both with `ADDLOCAL=ALL /qn` (ensures `libgiognutls.dll` and `gst-plugin-scanner.exe` land).
    3. Ensure Python 3.10+ is on PATH (`python --version`).
    4. Copy this phase directory to the VM (or clone the repo on the VM).
    5. Populate `test_url.txt` with one AA HTTPS channel URL from the user's library (contains live listen key — gitignored).

    ## Per-iteration loop

    1. `cd` into this phase directory on the VM.
    2. Run `.\build.ps1` — builds the bundle via PyInstaller, then runs `smoke_test.py` against the test URL.
    3. Paste the full content of `artifacts\smoke.log` back into the chat.
    4. After pasting, state one word: `audible` or `silent` (whether speakers produced sound).
    5. Claude diffs the log, updates `43-spike.spec` or `runtime_hook.py`, commits, and you re-run step 2.

    ## Pass conditions

    Spike passes when a single iteration produces:
    - `SPIKE_OK audio_sample_received=True` in `smoke.log`
    - `SPIKE_DIAG ... has_default_database=True` in `smoke.log` (proves `libgiognutls.dll` loaded)
    - You reply `audible` (optional — `silent` with `SPIKE_OK` still counts, logs a VM audio note)

    Then Claude runs `/gsd-spike-wrap-up` to persist findings. ≤5 iteration budget before scope revisit.

    ## Never commit

    `test_url.txt`, `artifacts/`, `build/`, `dist/` — all in `.gitignore`.
    ```

    Implements: PKG-06 (per D-09 paste-back model, D-11 deliverables list).
    Traceability: closes 43-VALIDATION.md Wave 0 bullets for `.gitignore` and optional README.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/43-gstreamer-windows-spike/.gitignore \
      && grep -q '^test_url.txt$' .planning/phases/43-gstreamer-windows-spike/.gitignore \
      && grep -q '^artifacts/$' .planning/phases/43-gstreamer-windows-spike/.gitignore \
      && grep -q '^build/$' .planning/phases/43-gstreamer-windows-spike/.gitignore \
      && grep -q '^dist/$' .planning/phases/43-gstreamer-windows-spike/.gitignore \
      && test -f .planning/phases/43-gstreamer-windows-spike/README.md \
      && grep -q 'build.ps1' .planning/phases/43-gstreamer-windows-spike/README.md \
      && grep -q 'test_url.txt' .planning/phases/43-gstreamer-windows-spike/README.md \
      && grep -q 'audible\|silent' .planning/phases/43-gstreamer-windows-spike/README.md \
      && [ "$(wc -l < .planning/phases/43-gstreamer-windows-spike/README.md)" -ge 10 ]
    </automated>
  </verify>
  <done>.gitignore blocks the five secret/artifact paths; README.md covers one-time VM setup, per-iteration loop, pass conditions, and gitignore boundary in one read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's AA listen key → paste-back logs | Listen key lives in `test_url.txt` on the VM and appears in URLs the smoke test logs. Any committed log or chat paste risks leaking it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | `smoke_test.py` logging + git history | mitigate | `smoke_test.py` redacts URL query string (everything after `?`) via `url.split("?", 1)[0] + "?<redacted>"` before any `_emit("SPIKE_DIAG", url=...)` call. `.gitignore` excludes `test_url.txt` and `artifacts/` so raw listen keys never enter git. Wave 3's `43-SPIKE-FINDINGS.md` embeds only `SPIKE_OK`/`SPIKE_DIAG` lines (already redacted), not raw `artifacts/smoke.log` contents. Verified by Task 2 acceptance grep for `?<redacted>` and Task 3 acceptance grep for `test_url.txt` in `.gitignore`. |
</threat_model>

<verification>
- All six files exist at `.planning/phases/43-gstreamer-windows-spike/`.
- `43-spike.spec` has `Tree()`, `runtime_hooks=["runtime_hook.py"]`, `upx=False`.
- `runtime_hook.py` sets `GIO_EXTRA_MODULES`, `GI_TYPELIB_PATH`, `GST_PLUGIN_SCANNER`.
- `smoke_test.py` has SPIKE_OK/FAIL/DIAG markers, the `?<redacted>` redaction, and `Gio.TlsBackend.get_default_database()` check.
- `build.ps1` has pre-flight checks for all three critical DLLs, pip-installs deps, runs PyInstaller, runs the smoke.
- `.gitignore` excludes `test_url.txt`, `artifacts/`, `build/`, `dist/`.
- `README.md` documents the one-time VM setup + per-iteration paste-back loop.
- Both Python files parse as valid AST.
</verification>

<success_criteria>
Plan is complete when the Wave 0 gap list in `43-VALIDATION.md` is closed: all four build artifacts plus `.gitignore` and `README.md` committed. Wave 2 (paste-back iteration) can begin on the user's next VM boot.
</success_criteria>

<output>
After completion, create `.planning/phases/43-gstreamer-windows-spike/43-01-SUMMARY.md` with: files created, bytes landed, verbatim-vs-improvised note (everything should be verbatim from RESEARCH), any skeleton-level assumptions worth flagging for Wave 2 (e.g., which PyGObject version pip resolves to is unresolved until iteration 1 paste-back — A7).
</output>
