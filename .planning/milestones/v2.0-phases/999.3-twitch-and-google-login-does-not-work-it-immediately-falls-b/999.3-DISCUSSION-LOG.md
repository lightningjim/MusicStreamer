# Phase 999.3: Twitch Login Fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 999.3-twitch-and-google-login-does-not-work-it-immediately-falls-b
**Areas discussed:** Twitch client_id, Failure UX

---

## Scope Narrowing (user clarification)

Backlog entry scoped the phase to both Twitch AND Google/YouTube OAuth failures. User confirmed at the start of discussion that YouTube/Google login now works — something was fixed between backlog entry and today. Phase narrowed to **Twitch only**, with the specific failure mode being "can't complete login capture" (browser login succeeds, token extraction fails).

---

## Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Diagnostic strategy | Instrument first vs jump to fixes | |
| Auth flow redesign | Implicit vs code vs device code | |
| Twitch client_id | Keep piggyback vs register our own | ✓ |
| Failure UX | Generic vs detailed error surface | ✓ |

User selected Failure UX + Twitch client_id. Diagnostic + auth-flow-redesign not explicitly selected, but the client_id discussion organically surfaced auth-flow decisions (loopback + PKCE) — captured under D-03/D-04.

---

## Twitch client_id

### Q1: How should we handle the Twitch client_id?

| Option | Description | Selected |
|--------|-------------|----------|
| Register our own | dev.twitch.tv app, our own client_id + redirect_uri | ✓ |
| Keep piggybacking | Reuse Twitch's web client_id (kimne78...) | |
| You decide | Claude picks | |

**User's choice:** Register our own

### Q2: If registering our own, how is the client_id distributed?

| Option | Description | Selected |
|--------|-------------|----------|
| Embedded in code | Hardcoded constant, public client pattern | ✓ |
| User provides | Kyle pastes client_id in settings | |
| Skip for now | N/A | |

**User's choice:** Embedded in code

### Q3: Redirect URI?

| Option | Description | Selected |
|--------|-------------|----------|
| http://localhost | No port, keep urlChanged inspection pattern | |
| http://localhost:PORT | Fixed port + real HTTP listener in subprocess | ✓ |
| You decide | Researcher picks | |

**User's choice:** http://localhost:PORT

### Q4: Who registers the Twitch app?

| Option | Description | Selected |
|--------|-------------|----------|
| You (Kyle) | Kyle registers on dev.twitch.tv | ✓ |
| Claude does it | N/A — requires Twitch account login | |

**User's choice:** Kyle

### Q5: OAuth flow type?

| Option | Description | Selected |
|--------|-------------|----------|
| Authorization code | Code flow with client_secret | |
| Auth code + PKCE | Code flow with PKCE, no secret | ✓ |
| Keep implicit (token) | response_type=token, fragment capture | |
| You decide | Researcher picks | |

**User's choice:** Auth code + PKCE
**Notes:** Researcher must verify Twitch currently supports PKCE; D-06 documents the fallback path if not.

---

## Failure UX

### Q1: What should the user see on failure?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep generic | "Try again." No detail | |
| Category + detail | Category + raw error text | ✓ |
| Category only | Friendly category, no raw text | |

**User's choice:** Category + detail

### Q2: Where should the detail go beyond the dialog?

| Option | Description | Selected |
|--------|-------------|----------|
| stderr only | Subprocess logs to stderr, parent forwards | |
| Persistent log file | ~/.local/share/musicstreamer/oauth.log | ✓ |
| No extra logging | Dialog message only | |

**User's choice:** Persistent log file

### Q3: Retry action in the failure dialog?

| Option | Description | Selected |
|--------|-------------|----------|
| Retry button | Inline retry without closing AccountsDialog | ✓ |
| Close only | Current behavior | |

**User's choice:** Retry button

### Q4: What goes in the oauth log?

| Option | Description | Selected |
|--------|-------------|----------|
| Failure events | Every failed attempt | ✓ |
| Success events | Every successful connect | ✓ |
| Redirect URLs | Final redirect URL (scrubbed) | |

**User's choice:** Failure events + Success events
**Notes:** Redirect URLs not logged — avoids token-fragment leakage concerns.

---

## Claude's Discretion

- Fixed port number for loopback listener
- Log rotation strategy (size vs count vs truncation)
- Log format (JSON lines preferred but not mandated)
- HTTP listener implementation (stdlib `http.server`, no new deps)
- HTML response served after redirect
- Subprocess lifecycle (exit after first redirect vs serve until timeout)
- Timeout duration (suggested 120s)

## Deferred Ideas

- Diagnostic instrumentation as a separate investigation phase (if PKCE + loopback redesign doesn't fix the issue)
- Device code flow (kept as fallback only)
- Auth code without PKCE (kept as fallback only if Twitch doesn't support PKCE)
