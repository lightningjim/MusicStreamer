---
phase: 999.7
plan: 04
type: execute
wave: 3
depends_on: ["999.7-02", "999.7-03"]
files_modified:
  - .planning/PROJECT.md
autonomous: false
requirements: [regression-guard, UAT]
tags: [regression, uat, decisions-log]

must_haves:
  truths:
    - "Full pytest suite is green — no regression from Plans 01-03"
    - "UAT on real YouTube playback confirms canonical cookies.txt is byte-identical before/after playback"
    - "UAT confirms no /tmp/ms-cookies-* leftovers after playback"
    - "UAT confirms corruption recovery toast fires and file is cleared when cookies.txt is manually corrupted"
    - "PROJECT.md Key Decisions table carries a new entry documenting the FIX-02 restoration on the library-API path"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entry for Phase 999.7 (FIX-02 restored on yt-dlp library API)"
      contains: "Phase 999.7"
  key_links:
    - from: "Phase 999.7"
      to: "Plan 35-06 regression window"
      via: "PROJECT.md Key Decisions row explaining the regression introduction + restoration"
      pattern: "FIX-02|Phase 999\\.7"
---

<objective>
Close out Phase 999.7 with a full-suite regression guard, human UAT on the real YouTube playback path, and a PROJECT.md Key Decisions entry documenting the fix.

Purpose: Plans 01-03 land the code and unit-test coverage. This plan validates that nothing else in the codebase regressed, confirms the fix works on real yt-dlp (not just the FakeYDL mock), and writes the decision log so future phases do not re-introduce the same regression when touching cookie I/O.

Output:
- Full `uv run --with pytest pytest` suite green.
- Human UAT sign-off on 3 scenarios (playback round-trip, corruption recovery, no tempfile leaks).
- PROJECT.md Key Decisions entry added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02/999.7-CONTEXT.md
@.planning/phases/999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02/999.7-RESEARCH.md
@.planning/phases/999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02/999.7-01-SUMMARY.md
@.planning/phases/999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02/999.7-02-SUMMARY.md
@.planning/phases/999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02/999.7-03-SUMMARY.md

@musicstreamer/cookie_utils.py
@musicstreamer/player.py
@musicstreamer/yt_import.py
@musicstreamer/ui_qt/main_window.py
@musicstreamer/ui_qt/import_dialog.py
@tests/test_cookies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression suite + targeted grep guards</name>
  <files>(none — read-only verification)</files>
  <read_first>
    musicstreamer/cookie_utils.py
    musicstreamer/player.py
    musicstreamer/yt_import.py
    musicstreamer/ui_qt/main_window.py
    musicstreamer/ui_qt/import_dialog.py
    tests/test_cookies.py
  </read_first>
  <action>
Run the full pytest suite. Capture the result. If anything outside `tests/test_cookies.py` fails that was green on `main` before this phase started, investigate and fix before moving on.

Full-suite command:

    cd /home/kcreasey/OneDrive/Projects/MusicStreamer
    uv run --with pytest pytest --tb=short

Then run these grep guards to confirm the architecture invariants (each listed with its expected result):

1. Helper module exists:  `ls -la musicstreamer/cookie_utils.py`  → file present
2. No direct-canonical assignment left:  `grep -rn 'opts\["cookiefile"\] = paths.cookies_path()' musicstreamer/`  → zero matches
3. Old yt_import pattern gone:  `grep -rn 'opts\["cookiefile"\] = cookies\b' musicstreamer/`  → zero matches
4. Exactly two temp-copy call sites:  `grep -rn 'with cookie_utils.temp_cookies_copy' musicstreamer/`  → exactly 2 matches (player.py + yt_import.py)
5. cookies_cleared Signal is class-level:  `grep -n 'self.cookies_cleared = Signal' musicstreamer/player.py`  → zero matches (Pitfall 4 guard)
6. Class-level Signal declared:  `grep -n 'cookies_cleared .*Signal(str)' musicstreamer/player.py`  → exactly 1 match at class scope
7. MainWindow wiring present:  `grep -n 'cookies_cleared.connect' musicstreamer/ui_qt/main_window.py`  → exactly 1 match connecting to self.show_toast
8. ImportDialog forwards callback:  `grep -n 'toast_callback=self._toast' musicstreamer/ui_qt/import_dialog.py`  → exactly 1 match at the _YtScanWorker construction site

If any grep returns an unexpected count, inspect the diff from Plans 01-03 and fix before continuing.

No commit needed for this task — it is a read-only guard.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && uv run --with pytest pytest --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --with pytest pytest` full suite exits 0 (no regressions)
    - `grep -rn 'opts\["cookiefile"\] = paths.cookies_path()' musicstreamer/` returns zero matches
    - `grep -rn 'opts\["cookiefile"\] = cookies\b' musicstreamer/` returns zero matches
    - `grep -c 'with cookie_utils.temp_cookies_copy' musicstreamer/player.py` returns 1
    - `grep -c 'with cookie_utils.temp_cookies_copy' musicstreamer/yt_import.py` returns 1
    - `grep -c 'self.cookies_cleared = Signal' musicstreamer/player.py` returns 0
    - `grep -cE 'cookies_cleared .*Signal\(str\)' musicstreamer/player.py` returns 1
    - `grep -c 'cookies_cleared.connect' musicstreamer/ui_qt/main_window.py` returns 1
    - `grep -c 'toast_callback=self._toast' musicstreamer/ui_qt/import_dialog.py` returns 1
  </acceptance_criteria>
  <done>
    Full suite green. All invariant grep guards pass. Architecture matches the planned shape.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — real yt-dlp playback + corruption recovery + tempfile leak check</name>
  <what-built>
    - `musicstreamer/cookie_utils.py`: shared corruption detector + temp-copy context manager
    - `musicstreamer/yt_import.py::scan_playlist`: routes cookies through temp_cookies_copy + auto-recovers corrupted files + accepts toast_callback
    - `musicstreamer/player.py::_youtube_resolve_worker`: same contract + new `cookies_cleared = Signal(str)`
    - `musicstreamer/ui_qt/main_window.py`: `cookies_cleared -> show_toast` wiring
    - `musicstreamer/ui_qt/import_dialog.py`: `_YtScanWorker` forwards `toast_callback=self._toast`

    End-to-end: FIX-02 protection is restored on the yt-dlp library API path. Canonical `cookies.txt` stays byte-identical across any yt-dlp call. Corrupted cookies.txt (from any previous unprotected run) is auto-cleared with a toast on next YouTube use.
  </what-built>
  <how-to-verify>
    **Scenario A — FIX-02 round-trip (canonical cookies survive real playback):**
    1. Re-import YouTube cookies via Google login (Accounts menu). Confirm `~/.local/share/musicstreamer/cookies.txt` exists and starts with `# Netscape HTTP Cookie File`.
    2. Record the canonical hash: `sha256sum ~/.local/share/musicstreamer/cookies.txt`.
    3. Play a YouTube live stream (e.g. LoFi Girl) in MusicStreamer. Let it play for ~10 seconds.
    4. Record the hash again: `sha256sum ~/.local/share/musicstreamer/cookies.txt`.
    5. **Expected:** both hashes match exactly. File is unchanged.
    6. Confirm the file's first line is still `# Netscape HTTP Cookie File` (NOT `# This file is generated by yt-dlp.`).

    **Scenario B — no tempfile leaks:**
    7. `ls /tmp/ms-cookies-* 2>/dev/null` — **expected:** no such files.
    8. If any leaked, note which scenario (playback vs. import) produced them and file a follow-up.

    **Scenario C — corruption recovery toast (playback path):**
    9. With the app closed, manually corrupt the file:
       `printf '# This file is generated by yt-dlp.  Do not edit.\n' > ~/.local/share/musicstreamer/cookies.txt`
    10. Re-launch MusicStreamer.
    11. Play any YouTube station.
    12. **Expected:** a toast "YouTube cookies cleared — re-import via Accounts menu." appears briefly.
    13. `ls ~/.local/share/musicstreamer/cookies.txt` — **expected:** file is gone (auto-cleared).
    14. Playback should still proceed (no cookies) — it will likely fail for age-gated content but should succeed for public streams; either outcome is acceptable.

    **Scenario D — import path corruption (recommended):**
    15. Repeat step 9 to re-corrupt the file.
    16. Open Import dialog -> YouTube tab -> paste a playlist URL -> Scan.
    17. **Expected:** same toast fires during the scan.
    18. File is gone after scan.

    Reply with `approved` if all four scenarios pass, or describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failures observed.</resume-signal>
  <acceptance_criteria>
    - Scenario A: canonical cookies.txt sha256 is identical before and after playback
    - Scenario A: file's first line is `# Netscape HTTP Cookie File` after playback (NOT yt-dlp marker)
    - Scenario B: no `/tmp/ms-cookies-*` files leaked
    - Scenario C: toast fires on corrupted file + playback and file is deleted
    - Scenario D: toast fires on corrupted file + scan_playlist and file is deleted
  </acceptance_criteria>
  <done>
    User confirms all UAT scenarios pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md Key Decisions with Phase 999.7 entry</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    .planning/PROJECT.md
  </read_first>
  <action>
**Step 1 — Append a row to the Key Decisions table.**

The Key Decisions table header appears around line 147 of `.planning/PROJECT.md` (starts with `| Decision | Rationale | Outcome |`). The last row currently ends with the `Dropped mpv subprocess fallback for YouTube (Plan 35-06)` entry (around line 193).

Insert this new row at the END of the Key Decisions table, immediately before the `## Constraints` heading that follows:

    | Phase 999.7: FIX-02 restored on yt-dlp library API path | Plan 35-06 replaced the mpv subprocess with `yt_dlp.YoutubeDL` and dropped the v1.5 Phase 23 temp-copy wrapper. yt-dlp's `YoutubeDL.__exit__` calls `save_cookies()` whenever `cookiefile` is set, clobbering the canonical file with its own serialization. Restored via shared `musicstreamer/cookie_utils.py` (`is_cookie_file_corrupted` predicate + `temp_cookies_copy()` @contextmanager with `mkstemp` + `copy2` + unlink-in-finally). Both yt-dlp read sites (`player._youtube_resolve_worker` + `yt_import.scan_playlist`) route cookies through the ctxmgr. Corrupted files auto-clear with a toast. Cross-platform: uses `mkstemp` over `NamedTemporaryFile` for Windows compat (v2.0). | ✓ Good — v2.0 Phase 999.7 |

**Step 2 — Append one sentence to the "YouTube Cookies" state paragraph.**

Around line 47 of PROJECT.md, find the bullet that begins with `- **YouTube Cookies:**` and currently ends with `both yt-dlp and mpv use --cookies when file exists`. Append this sentence to the same bullet (no newline; it stays one bullet):

    **Phase 999.7:** yt-dlp library invocations route cookies through a per-call temp copy via `musicstreamer/cookie_utils.temp_cookies_copy()` so the canonical file is never rewritten; corrupted files (yt-dlp marker header) are auto-cleared with a toast at the read site.

Do NOT touch anything else in PROJECT.md. Do NOT bump phase counts or milestone status — those are `/gsd:transition` concerns.

Commit with message: `docs(999.7-04): log Phase 999.7 FIX-02 restoration in PROJECT.md Key Decisions`
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && grep -c 'Phase 999.7: FIX-02 restored' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 999.7: FIX-02 restored' .planning/PROJECT.md` returns 1
    - `grep -c 'temp_cookies_copy' .planning/PROJECT.md` returns at least 1
    - `.planning/PROJECT.md` Key Decisions table has exactly one new row referencing Phase 999.7
    - `.planning/PROJECT.md` YouTube Cookies state bullet references Phase 999.7
    - No other lines in PROJECT.md are modified (verify via `git diff .planning/PROJECT.md` showing only additions)
  </acceptance_criteria>
  <done>
    PROJECT.md carries the Phase 999.7 decision log entry and an updated state paragraph. Commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tester -> running app | Human drives UAT scenarios via keyboard + shell |
| App -> canonical cookies.txt | Read-only during playback (after Plans 01-03); write only via oauth_helper / cookie_import_dialog (out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.7-10 | T (Tampering) — ASVS V5 | UAT cannot detect a partial regression that the unit tests missed | mitigate | UAT Scenario A asserts byte-level file equality with sha256sum — stronger than the FakeYDL simulation because it exercises the real yt-dlp save_cookies() code path |
| T-999.7-11 | I (Information Disclosure) | Tempfile leaks accumulate in /tmp during failed runs | mitigate | UAT Scenario B asserts zero `/tmp/ms-cookies-*` leaks after happy-path playback; Pitfall 4 (process crash during yt-dlp) remains accepted per v1.5 T-23-03 disposition |
| T-999.7-12 | R (Repudiation) | Future phases re-introduce the direct-canonical pattern | mitigate | PROJECT.md Key Decisions row (Task 3) explicitly documents the regression mechanism + restoration, so `/gsd-plan-phase` context for future phases surfaces this constraint |
</threat_model>

<verification>
1. `uv run --with pytest pytest --tb=short` — full suite exits 0.
2. Human confirmation of all four UAT scenarios (A/B/C/D).
3. `git diff .planning/PROJECT.md` — shows only additive changes (one new table row + one appended sentence).
</verification>

<success_criteria>
- Full pytest suite is green.
- Canonical cookies.txt is provably byte-identical across a real yt-dlp playback round-trip.
- No tempfile leaks in `/tmp/ms-cookies-*` after happy-path playback.
- Corruption recovery toast fires and file is cleared on both read sites (player path AND import path).
- PROJECT.md Key Decisions table records Phase 999.7 so future planners do not reintroduce the regression.
</success_criteria>

<output>
After completion, create `.planning/phases/999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02/999.7-04-SUMMARY.md`
</output>
