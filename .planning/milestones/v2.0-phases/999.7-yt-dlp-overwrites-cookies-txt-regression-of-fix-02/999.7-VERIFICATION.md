---
phase: 999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02
verified: 2026-04-24T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---

# Phase 999.7: yt-dlp Overwrites cookies.txt — FIX-02 Regression Verification

**Phase Goal:** Restore v1.5 FIX-02 temp-copy protection so yt-dlp library invocations no longer overwrite the user's imported `~/.local/share/musicstreamer/cookies.txt`; add corruption auto-recovery for files already corrupted by the pre-fix bug.

**Verified:** 2026-04-24
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `cookie_utils.py` exists with `is_cookie_file_corrupted` + `temp_cookies_copy` @contextmanager | ✓ VERIFIED | `musicstreamer/cookie_utils.py:33,52` — both functions defined; `@contextlib.contextmanager` decorator present; marker `"generated by yt-dlp"` matches yt-dlp header (line 30); `mkstemp` + `copy2` + `unlink` in `finally` (lines 70–83) |
| 2 | `_youtube_resolve_worker` routes cookies through `temp_cookies_copy` with correct nesting | ✓ VERIFIED | `musicstreamer/player.py:498` `with cookie_utils.temp_cookies_copy() as cookiefile:` is textually ABOVE line 502 `with yt_dlp.YoutubeDL(opts) as ydl:` — Pitfall 1 nesting correct |
| 3 | `scan_playlist` routes cookies through `temp_cookies_copy` with same nesting | ✓ VERIFIED | `musicstreamer/yt_import.py:73` outer; `:77` `with yt_dlp.YoutubeDL(opts) as ydl:` inner — correct |
| 4 | `Player.cookies_cleared = Signal(str)` at CLASS scope, wired to `MainWindow.show_toast` | ✓ VERIFIED | `musicstreamer/player.py:69` — declared at class scope alongside other Signals (verified block is outside `__init__`, which begins at line 81); `main_window.py:224` `self._player.cookies_cleared.connect(self.show_toast)` |
| 5 | `scan_playlist` accepts `toast_callback` + `_YtScanWorker` forwards `self._toast` | ✓ VERIFIED | `yt_import.py:41` signature `toast_callback: Optional[Callable[[str], None]] = None`; `import_dialog.py:91` `yt_import.scan_playlist(self._url, toast_callback=self._toast)` |
| 6 | Full pytest suite — only the 10 pre-existing failures remain; no new regressions | ✓ VERIFIED | `10 failed, 764 passed` — failures are exactly MPRIS2 (8) + SMTC (1) + station_list_panel (1), matching baseline |
| 7 | `tests/test_cookies.py` has 17 passing tests covering temp-copy, byte-equality, copy-failure, corruption detection, auto-clear at both sites | ✓ VERIFIED | `grep -cE '^def test_' tests/test_cookies.py` → 17; `pytest tests/test_cookies.py` → `17 passed in 0.25s`. New tests include `test_ytdlp_uses_temp_cookie_copy`, `test_canonical_cookies_unchanged_by_ytdlp`, `test_copy_failure_omits_cookiefile`, `test_corruption_marker_detected`, `test_clean_netscape_not_corrupted`, `test_scan_playlist_auto_clears_corrupted`, `test_youtube_resolve_auto_clears_corrupted`, `test_temp_cookie_unlinked_after_call`, `test_youtube_resolve_uses_temp_cookie_copy` |
| 8 | PROJECT.md Key Decisions row documents FIX-02 restoration | ✓ VERIFIED | `.planning/PROJECT.md:194` — dedicated row "Phase 999.7: FIX-02 restored on yt-dlp library API path" with full rationale; YouTube Cookies description at line 47 also updated |
| 9 | No mpv retry-without-cookies logic resurrected | ✓ VERIFIED | `grep -n "retry\|without.cookies\|without_cookies"` in `player.py` and `yt_import.py` → no matches |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---------|----------|--------|---------|
| `musicstreamer/cookie_utils.py` | New shared helper module | ✓ VERIFIED | 84 lines; both public functions implemented with correct signatures and @contextmanager |
| `musicstreamer/player.py` | `_youtube_resolve_worker` + `cookies_cleared` Signal | ✓ VERIFIED | Signal at class scope (L69); corruption check (L477); temp-copy wrap (L498–506) |
| `musicstreamer/yt_import.py` | `scan_playlist` with `toast_callback` + temp-copy | ✓ VERIFIED | Signature updated (L39–42); corruption check (L57–60); temp-copy wrap (L73–83) |
| `musicstreamer/ui_qt/main_window.py` | `cookies_cleared` → `show_toast` wiring | ✓ VERIFIED | L224 connect |
| `musicstreamer/ui_qt/import_dialog.py` | `_YtScanWorker` forwards `toast_callback` | ✓ VERIFIED | L82 ctor param; L87 store; L91 forward; L342 construct with `toast_callback=self._toast` |
| `tests/test_cookies.py` | 17 passing tests (8 pre-existing + 9 new) | ✓ VERIFIED | All pass in 0.25s |
| `.planning/PROJECT.md` | Key Decisions row for FIX-02 restoration | ✓ VERIFIED | Row added at L194 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `player._youtube_resolve_worker` | canonical cookies.txt (read-only) | `cookie_utils.temp_cookies_copy()` context manager | ✓ WIRED | yt-dlp receives tempfile path; canonical file never rewritten |
| `yt_import.scan_playlist` | canonical cookies.txt (read-only) | `cookie_utils.temp_cookies_copy()` context manager | ✓ WIRED | Same pattern |
| `Player.cookies_cleared` Signal | `MainWindow.show_toast` slot | Qt auto-connect (queued, cross-thread) | ✓ WIRED | main_window.py:224 |
| `yt_import.scan_playlist` toast | `MainWindow.show_toast` | `ImportDialog._toast` → `_YtScanWorker._toast` → `scan_playlist(toast_callback=...)` | ✓ WIRED | Chain complete through import_dialog.py L87, L91, L342, L541 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `temp_cookies_copy` yield | `tmp_path` | `tempfile.mkstemp` + `shutil.copy2(canonical, tmp)` | ✓ real per-call tempfile with canonical contents | ✓ FLOWING |
| `is_cookie_file_corrupted` return | head read | `open(path).read(512)` + marker scan | ✓ true scan of first 4 non-blank lines | ✓ FLOWING |
| `cookies_cleared` Signal payload | toast text | Emitted with literal string from worker thread | ✓ real user-visible toast | ✓ FLOWING (UAT Scenario C confirmed) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 17 cookies tests pass | `pytest tests/test_cookies.py -q` | `17 passed in 0.25s` | ✓ PASS |
| Full suite has only baseline failures | `pytest -q` | `10 failed, 764 passed` — all 10 match pre-existing baseline | ✓ PASS |
| No retry-without-cookies resurrected | grep over player.py + yt_import.py | 0 matches | ✓ PASS |
| UAT Scenario A (byte-identical) | Human sha256sum comparison | hash `5f229d…c4bb` stable across multiple playback attempts | ✓ PASS (human-verified) |
| UAT Scenario B (no leftover tempfiles) | `/tmp/ms-cookies-*` | none | ✓ PASS (human-verified) |
| UAT Scenario C (corruption auto-clear + toast) | Human injected marker file | toast fires, file auto-clears | ✓ PASS (human-verified) |

### Requirements Coverage

Backlog-item phase (999.7); no formal `requirements:` field in the plan frontmatter maps to REQUIREMENTS.md IDs. Phase goal is fully satisfied per truths 1–9.

### Anti-Patterns Found

None. No TODO/FIXME/placeholder markers introduced. No stub returns, no empty handlers, no hardcoded-empty props. Only a single best-effort `pass` in `cookie_utils.py:83` (`except OSError: pass` inside the finally block for tempfile unlink) — this is intentional best-effort cleanup documented in the docstring.

### Human Verification Required

None — UAT was already completed on 2026-04-24 (Scenarios A, B, C passed; D skipped as duplicate mechanism of C). Side finding "No video formats found" is a separate yt-dlp extractor issue filed as Phase 999.9, not in-scope for 999.7.

### Gaps Summary

No gaps. Phase delivers exactly what was promised:
- Canonical cookies.txt survives yt-dlp invocations byte-identical (FIX-02 restored on library-API path).
- Both read sites (`player._youtube_resolve_worker` and `yt_import.scan_playlist`) route through the same shared `cookie_utils.temp_cookies_copy()` helper.
- Context-manager nesting is correct (temp-copy outer, yt-dlp inner) — Pitfall 1 satisfied.
- Corruption auto-recovery works at both sites with a user-visible toast.
- `cookies_cleared` is a proper class-scope Qt Signal, correctly connected to `show_toast`.
- Cross-platform-safe tempfile handling (`mkstemp` over `NamedTemporaryFile`) for v2.0 Windows port.
- 9 new tests added (17 total in `test_cookies.py`), all passing; no new regressions in the full suite.
- PROJECT.md Key Decisions updated with full rationale.

---

_Verified: 2026-04-24_
_Verifier: Claude (gsd-verifier)_
