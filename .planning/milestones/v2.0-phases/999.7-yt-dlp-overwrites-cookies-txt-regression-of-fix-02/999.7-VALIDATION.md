---
phase: 999.7
slug: yt-dlp-overwrites-cookies-txt-regression-of-fix-02
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-24
---

# Phase 999.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-qt (existing, QA-01) |
| **Config file** | pyproject.toml |
| **Quick run command** | `uv run --with pytest pytest tests/test_cookies.py -x` |
| **Full suite command** | `uv run --with pytest pytest` |
| **Estimated runtime** | ~5 seconds (cookies subset) / ~30 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `uv run --with pytest pytest tests/test_cookies.py -x`
- **After every plan wave:** Run `uv run --with pytest pytest tests/test_cookies.py tests/test_player.py -x`
- **Before `/gsd-verify-work`:** Full suite must be green (`uv run --with pytest pytest`)
- **Max feedback latency:** ~5 seconds (cookies subset)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.7-01-01 | 01 | 0 | FIX-02-a,b,c,d,e / NEW-a,b,c,d | T-999.7-01 | Wave 0 stub: `cookie_utils.py` with signatures + failing tests | unit | `uv run --with pytest pytest tests/test_cookies.py -x` | ❌ W0 | ⬜ pending |
| 999.7-01-02 | 01 | 1 | NEW-a,b | T-999.7-01 | `is_cookie_file_corrupted(path)` returns True only for yt-dlp marker | unit | `uv run --with pytest pytest tests/test_cookies.py::test_corruption_marker_detected tests/test_cookies.py::test_clean_netscape_not_corrupted -x` | ❌ W0 | ⬜ pending |
| 999.7-01-03 | 01 | 1 | FIX-02-a,b,d,e | T-999.7-01 | `temp_cookies_copy()` context manager: mkstemp+copy2+unlink, OSError fallback to yield None | unit | `uv run --with pytest pytest tests/test_cookies.py::test_copy_failure_omits_cookiefile tests/test_cookies.py::test_temp_cookie_unlinked_after_call -x` | ❌ W0 | ⬜ pending |
| 999.7-02-01 | 02 | 2 | FIX-02-a,b / NEW-c | T-999.7-01 | `scan_playlist` routes cookies via `temp_cookies_copy()`; corruption → clear + toast | unit | `uv run --with pytest pytest tests/test_cookies.py::test_ytdlp_uses_temp_cookie_copy tests/test_cookies.py::test_canonical_cookies_unchanged_by_ytdlp tests/test_cookies.py::test_scan_playlist_auto_clears_corrupted -x` | ❌ W0 | ⬜ pending |
| 999.7-03-01 | 03 | 2 | FIX-02-c / NEW-d | T-999.7-01 | `_youtube_resolve_worker` routes via `temp_cookies_copy()`; corruption → clear + `cookies_cleared` signal | unit | `uv run --with pytest pytest tests/test_cookies.py::test_youtube_resolve_uses_temp_cookie_copy tests/test_cookies.py::test_youtube_resolve_auto_clears_corrupted -x` | ❌ W0 | ⬜ pending |
| 999.7-04-01 | 04 | 3 | regression guard | — | All existing cookie / player / import tests still pass | unit | `uv run --with pytest pytest tests/test_cookies.py tests/test_player.py tests/test_yt_import.py` | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `musicstreamer/cookie_utils.py` — Wave 0 stub module with `is_cookie_file_corrupted(path: str) -> bool` and `temp_cookies_copy() -> Iterator[Optional[str]]` signatures, body raising `NotImplementedError`. Enables Wave 1 RED tests to import by name.
- [ ] `tests/test_cookies.py` — 9 new failing tests written RED:
  - 5 temp-copy / fallback / byte-unchanged tests (FIX-02-a..e)
  - 4 corruption-detection and auto-clear tests (NEW-a..d)
- [ ] `tests/conftest.py` — no changes; existing `tmp_path` + `_root_override` monkeypatch pattern is sufficient.

No framework installation needed — pytest + pytest-qt already configured (QA-01 complete).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end cookies survive real YT playback | FIX-02 | Byte-level equality is asserted automatically via FakeYDL, but a live yt-dlp run validates the real extraction path | 1. Re-import cookies via Google login (AccountsDialog). 2. `sha256sum ~/.local/share/musicstreamer/cookies.txt` → record. 3. Play a YouTube live stream (LoFi Girl). 4. `sha256sum` again → same hash. 5. `ls /tmp/ms-cookies-*` → no leftovers. |
| Corruption recovery toast fires on real UI | NEW-c/NEW-d | Toast signal wiring is automated, but visual confirmation is human-only | 1. `echo "# This file is generated by yt-dlp." > ~/.local/share/musicstreamer/cookies.txt`. 2. Play any YouTube station. 3. Expect toast "YouTube cookies cleared — re-import via Accounts menu." 4. `ls ~/.local/share/musicstreamer/cookies.txt` → gone. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (`cookie_utils.py`, 9 new tests)
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter (after planner review)

**Approval:** approved 2026-04-24
