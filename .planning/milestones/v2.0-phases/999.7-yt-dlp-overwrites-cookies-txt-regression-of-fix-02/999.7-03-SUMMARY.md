---
phase: 999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02
plan: 03
subsystem: player
tags: [cookies, yt-dlp, player, qt-signal, main-window, tempfile, tdd]

# Dependency graph
requires:
  - phase: 999.7
    provides: cookie_utils.temp_cookies_copy + is_cookie_file_corrupted (Plan 01)
provides:
  - Player.cookies_cleared Signal at class scope (Pitfall 4 compliant)
  - _youtube_resolve_worker routes cookies through temp_cookies_copy ctxmgr
  - Corruption auto-clear + cookies_cleared toast on player-side yt-dlp invocation
  - MainWindow wires cookies_cleared → show_toast alongside existing playback_error route
affects: [999.7-04, v2.0-windows-port]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-thread advisory Signal from bare daemon threading.Thread (non-QThread) via class-level Qt Signal + AutoConnection (auto-queued)"
    - "Nested context managers for save-side-effect isolation: outer temp-copy ctxmgr, inner yt_dlp.YoutubeDL ctxmgr — save_cookies() on __exit__ writes to temp path, then ctxmgr finally unlinks it"

key-files:
  created: []
  modified:
    - musicstreamer/player.py
    - musicstreamer/ui_qt/main_window.py
    - tests/test_cookies.py
    - tests/test_main_window_integration.py
    - tests/test_main_window_media_keys.py
    - tests/test_ui_qt_scaffold.py

key-decisions:
  - "Direct cookies_cleared → show_toast connection (no _on_cookies_cleared slot) — emitter formats full user-facing string, show_toast's default duration_ms=3000 wires natively"
  - "FakePlayer test doubles in 3 MainWindow test modules extended with cookies_cleared Signal — fixture contract follows Player's Signal surface exactly (Rule 3 blocking fix)"

patterns-established:
  - "Player cross-thread advisory toast: class-level Signal(str), emitted from worker thread, auto-queued to MainWindow.show_toast — mirrors playback_error posture from the bus-loop thread"
  - "Cookie-file protection call sites follow canonical pattern: corruption check + auto-clear + advisory emit (outside ctxmgr) → temp_cookies_copy outer ctxmgr → yt_dlp.YoutubeDL inner ctxmgr → post-call resolved-URL extraction"

requirements-completed: [FIX-02-c, FIX-02-d, FIX-02-e, NEW-d]

# Metrics
duration: ~5min
completed: 2026-04-24
---

# Phase 999.7 Plan 03: Player-side FIX-02 Restoration Summary

**Player._youtube_resolve_worker now routes cookies through cookie_utils.temp_cookies_copy with corruption auto-clear + cookies_cleared Signal wired to MainWindow.show_toast — yt-dlp's save_cookies() side effect can no longer overwrite canonical cookies.txt on the player path.**

## Performance

- **Duration:** ~5 min
- **Completed:** 2026-04-24T19:41:49Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- `Player.cookies_cleared = Signal(str)` declared at class scope alongside `playback_error` (Pitfall 4 satisfied)
- `_youtube_resolve_worker` rewritten: corruption check → auto-clear + emit → yt_dlp.YoutubeDL nested inside temp_cookies_copy (Pitfall 1 nesting order satisfied)
- Canonical `cookies.txt` is never passed to yt-dlp — old `opts["cookiefile"] = cookies` pattern fully removed
- MainWindow wires `self._player.cookies_cleared.connect(self.show_toast)` in the same block as `playback_error` — Qt AutoConnection auto-queues cross-thread emission from the bare daemon worker
- Pre-existing `test_youtube_resolve_passes_cookiefile_when_present` updated to match new temp-path contract (keeps EJS assertion as regression guard)
- All 4 Plan-03-scope tests green: `test_youtube_resolve_uses_temp_cookie_copy`, `test_youtube_resolve_auto_clears_corrupted`, plus the 2 re-validated pre-existing player cookie tests
- Full cookie suite green (17 tests), player suite green (12), MainWindow + scaffold suites green (52) — 92 tests in the regression bundle pass with zero failures

## Task Commits

1. **Task 1: GREEN — Add Player.cookies_cleared Signal + rewrite _youtube_resolve_worker** — `0cb4a70` (feat)
2. **Task 2: Wire cookies_cleared → show_toast + update pre-existing player cookie test + fix FakePlayer fixtures** — `09c1e27` (feat)

_Both tasks were TDD-GREEN steps — RED tests shipped in Plan 01 (0cb4a70 consumes them)._

## Files Created/Modified
- `musicstreamer/player.py` — added `cookies_cleared` class-level Signal, `constants` + `cookie_utils` imports, rewrote `_youtube_resolve_worker` with corruption check + nested ctxmgrs
- `musicstreamer/ui_qt/main_window.py` — added `self._player.cookies_cleared.connect(self.show_toast)` after `playback_error.connect` line
- `tests/test_cookies.py` — updated `test_youtube_resolve_passes_cookiefile_when_present` to assert temp-path contract (cf != canonical, cf startswith gettempdir), preserved EJS regression guard
- `tests/test_main_window_integration.py`, `tests/test_main_window_media_keys.py`, `tests/test_ui_qt_scaffold.py` — added `cookies_cleared = Signal(str)` to FakePlayer test doubles so MainWindow wiring succeeds under fixture

## Decisions Made
- **Direct connect to `show_toast`, not via a new `_on_cookies_cleared` slot** — the emitter formats the full user-facing message ("YouTube cookies cleared — re-import via Accounts menu.") with no prefix transformation, and `show_toast(text, duration_ms=3000)` has a default 2nd arg that PySide6 wires correctly from `Signal(str)`. Mirrors plan's Step 1 guidance.
- **Fixture parity over test-double refactor** — FakePlayer doubles in 3 MainWindow test modules gained the new Signal rather than introducing a shared base class (kept diff minimal and each fixture self-documenting). Handled as Rule 3 deviation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `cookies_cleared` Signal to 3 FakePlayer test doubles**
- **Found during:** Task 2 verification (main_window-keyed tests)
- **Issue:** The new `self._player.cookies_cleared.connect(self.show_toast)` wiring in `MainWindow.__init__` raised `AttributeError: 'FakePlayer'/'_FakePlayer' object has no attribute 'cookies_cleared'` under 3 test modules that use FakePlayer doubles (test_main_window_integration.py, test_main_window_media_keys.py, test_ui_qt_scaffold.py). The PLAN acceptance criteria explicitly guard against main_window regressions (`pytest tests/ -k 'main_window' -x` must exit 0), so these fixtures had to be patched to match the expanded Player Signal surface.
- **Fix:** Added `cookies_cleared = Signal(str)  # Phase 999.7` to all 3 FakePlayer class bodies, placed adjacent to `playback_error` to mirror real-Player ordering.
- **Files modified:** tests/test_main_window_integration.py, tests/test_main_window_media_keys.py, tests/test_ui_qt_scaffold.py
- **Verification:** `uv run --with pytest pytest tests/ -k 'main_window'` → 52 passed; `uv run --with pytest pytest tests/test_ui_qt_scaffold.py` → 5 passed
- **Committed in:** 09c1e27 (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to satisfy plan's own regression-guard acceptance criteria — not scope creep. Diff remains disjoint from Plan 999.7-02's yt_import/import_dialog files.

## Issues Encountered
None — both tasks executed cleanly. TDD tests from Plan 01 flipped GREEN on first run after Task 1 edits landed.

## TDD Gate Compliance
- Plan 01 shipped the RED `test(...)` commit (7 failing tests locked the contract).
- Plan 03 supplies the GREEN gate on the player path: commit `0cb4a70` (feat) and `09c1e27` (feat).
- Refactor gate not required — minimal-diff implementation needs no cleanup pass.

## User Setup Required
None — no external service configuration needed.

## Next Phase Readiness
- Plan 999.7-04 (regression-guard + UAT) is unblocked. All 17 cookie tests + 12 player tests + 52 main_window/scaffold tests + 8 yt_import_library tests pass → the full targeted regression bundle (92 tests) is green.
- UAT spec in 999.7-04 can proceed: manual YouTube live playback with pre-seeded cookies.txt should leave canonical bytes unchanged; manually corrupted cookies.txt (yt-dlp marker header) should auto-clear + display the toast on first playback attempt.

## Self-Check: PASSED

**Files verified to exist:**
- FOUND: musicstreamer/player.py (modified — grep confirms `cookies_cleared = Signal(str)` at line 69, `with cookie_utils.temp_cookies_copy` at 498, `with yt_dlp.YoutubeDL(opts) as ydl` at 502)
- FOUND: musicstreamer/ui_qt/main_window.py (modified — grep confirms `cookies_cleared.connect(self.show_toast)` at line 224)
- FOUND: tests/test_cookies.py (updated pre-existing test to temp-path contract)
- FOUND: tests/test_main_window_integration.py, tests/test_main_window_media_keys.py, tests/test_ui_qt_scaffold.py (FakePlayer fixtures extended)

**Commits verified in git log:**
- FOUND: 0cb4a70 (Task 1 — Player signal + worker rewrite)
- FOUND: 09c1e27 (Task 2 — MainWindow wiring + fixture updates)

**Grep guards:**
- `grep -c 'self.cookies_cleared = Signal' musicstreamer/player.py` → 0 (Pitfall 4 satisfied)
- `grep -c 'with cookie_utils.temp_cookies_copy' musicstreamer/player.py` → 1
- `grep -c 'with yt_dlp.YoutubeDL(opts) as ydl' musicstreamer/player.py` → 1
- `grep -c 'opts\["cookiefile"\] = cookies' musicstreamer/player.py` → 0 (old pattern removed)
- `grep -c 'opts\["cookiefile"\] = paths.cookies_path' musicstreamer/player.py` → 0
- Nesting order: temp_cookies_copy at line 498 < yt_dlp.YoutubeDL at line 502 (correct)

---
*Phase: 999.7-yt-dlp-overwrites-cookies-txt-regression-of-fix-02*
*Completed: 2026-04-24*
