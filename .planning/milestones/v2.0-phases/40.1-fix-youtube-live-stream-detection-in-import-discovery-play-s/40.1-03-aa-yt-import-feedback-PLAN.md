---
phase: 40.1
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - musicstreamer/ui_qt/import_dialog.py
  - tests/test_import_dialog_qt.py
autonomous: true
requirements:
  - REGRESSION-AA-YT-FEEDBACK  # D-07, D-08, D-09, D-10
tags: [qt, import, audioaddict, youtube, feedback, ui]

must_haves:
  truths:
    - "_AaImportWorker.finished emits both imported and skipped counts"
    - "_YtImportWorker.finished emits both imported and skipped counts"
    - "_on_aa_import_complete shows context-sensitive summary as both inline label and toast"
    - "_on_yt_import_complete shows matching context-sensitive summary (parity)"
    - "When M==0 and N>0, label reads 'Imported N new' (no skip clause)"
    - "When N==0 and M>0, label reads 'All M already in library' (no imported framing)"
    - "When N>0 and M>0, label reads 'Imported N new, M skipped (already in library)'"
  artifacts:
    - path: "musicstreamer/ui_qt/import_dialog.py"
      provides: "Signal(int,int) workers + context-sensitive summary formatter + inline labels"
      contains: "_format_import_summary"
    - path: "tests/test_import_dialog_qt.py"
      provides: "Regression tests for AA/YT signal signature and wording"
      contains: "test_aa_import_emits_imported_and_skipped"
  key_links:
    - from: "_AaImportWorker.run"
      to: "_on_aa_import_complete"
      via: "Signal(int, int)"
      pattern: "finished = Signal\\(int, int\\)"
    - from: "_YtImportWorker.run"
      to: "_on_yt_import_complete"
      via: "Signal(int, int)"
      pattern: "finished = Signal\\(int, int\\)"
    - from: "_on_aa_import_complete"
      to: "_format_import_summary"
      via: "inline label + toast"
      pattern: "_format_import_summary\\(imported, skipped\\)"
---

<objective>
Change `_AaImportWorker.finished` and `_YtImportWorker.finished` from `Signal(int)` to `Signal(int, int)` so both import pipelines surface `(imported, skipped)`. Update `_on_aa_import_complete` and `_on_yt_import_complete` to render a context-sensitive summary per D-10 as BOTH a persistent inline label in the dialog and a toast (D-07, D-09). `aa_import.import_stations_multi()` and `yt_import.import_stations()` already return `(imported, skipped)` tuples — this plan only threads the second value through the Qt layer.

Purpose: Replace the misleading "Imported 0 channels" wording when everything dedupes; achieve parity between AA and YT import feedback (UI-07).
Output: Updated workers + handlers + regression tests covering all three wording branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md
@.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md
@musicstreamer/ui_qt/import_dialog.py
@musicstreamer/aa_import.py
@musicstreamer/yt_import.py
@tests/test_import_dialog_qt.py

<interfaces>
Backend already returns tuples (verified in RESEARCH.md):
- `aa_import.import_stations_multi(...)` → `(imported: int, skipped: int)` [aa_import.py line ~248]
- `yt_import.import_stations(...)` → `(imported: int, skipped: int)` [yt_import.py line ~107]

Current (buggy) worker definitions in musicstreamer/ui_qt/import_dialog.py:
```python
class _AaImportWorker(QThread):
    finished = Signal(int)  # BUG — drops skipped
    def run(self):
        ...
        result = aa_import.import_stations_multi(...)
        count = result[0] if isinstance(result, tuple) else result
        self.finished.emit(count)

class _YtImportWorker(QThread):
    finished = Signal(int)  # BUG — drops skipped
    def run(self):
        ...
        result = yt_import.import_stations(...)
        count = result[0] if isinstance(result, tuple) else result
        self.finished.emit(count)
```

Current handler signatures (to be updated):
- `_on_aa_import_complete(self, count: int)` at line ~425
- `_on_yt_import_complete(self, count: int)` at line ~347

Existing inline status labels already present in dialog:
- `self._aa_status: QLabel` — used for AA progress text
- `self._yt_status: QLabel` — used for YT progress text
(Reuse these; do NOT add new labels.)

Toast callback (Phase 39 pattern):
- `self._toast(msg)` — shows a transient ToastOverlay message

Existing test at tests/test_import_dialog_qt.py around line 117 asserts:
  `toast_cb.assert_called_once_with("Imported 1 stations")`
This assertion will break after the fix and MUST be updated to the new wording.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regression tests for (imported, skipped) signals and wording</name>
  <files>tests/test_import_dialog_qt.py</files>
  <read_first>
    - tests/test_import_dialog_qt.py (existing fixtures, ImportDialog construction pattern, current test at line ~117)
    - musicstreamer/ui_qt/import_dialog.py (_AaImportWorker, _YtImportWorker, _on_aa_import_complete, _on_yt_import_complete)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md (Fix 3, Pattern 1, Pitfall 2)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md (D-07 through D-10)
  </read_first>
  <behavior>
    Add these test functions (monkeypatch backend + invoke handler directly or drive worker with qtbot.waitSignal):

    test_format_import_summary_imported_only:
      - Call `_format_import_summary(5, 0)` → "Imported 5 new"

    test_format_import_summary_skipped_only:
      - Call `_format_import_summary(0, 12)` → "All 12 already in library"

    test_format_import_summary_mixed:
      - Call `_format_import_summary(3, 2)` → "Imported 3 new, 2 skipped (already in library)"

    test_aa_import_emits_imported_and_skipped:
      - Monkeypatch `aa_import.import_stations_multi` to return `(3, 7)`
      - Build ImportDialog with qtbot, drive the AA import code path (set key, trigger import)
      - Use `qtbot.waitSignal(worker.finished, timeout=3000)` OR directly invoke `_on_aa_import_complete(3, 7)` after patching
      - Assert `dialog._aa_status.text() == "Imported 3 new, 7 skipped (already in library)"`
      - Assert `dialog._aa_status.isVisible() is True`
      - Assert toast callback was called with the same string

    test_aa_import_all_deduped:
      - Monkeypatch backend to return `(0, 12)`
      - Drive import, assert `dialog._aa_status.text() == "All 12 already in library"`
      - Assert toast received same string
      - Assert string does NOT contain "Imported 0"

    test_yt_import_emits_imported_and_skipped:
      - Monkeypatch `yt_import.import_stations` to return `(4, 1)`
      - Drive YT import path, assert `dialog._yt_status.text() == "Imported 4 new, 1 skipped (already in library)"`
      - Assert toast called with same string

    test_yt_import_new_only_no_skip_clause:
      - Monkeypatch backend to return `(5, 0)`
      - Assert `dialog._yt_status.text() == "Imported 5 new"`
      - Assert text does NOT contain "skipped"

    Also update existing `test_youtube_import_calls_import_stations` (around line 117) — replace
      `toast_cb.assert_called_once_with("Imported 1 stations")`
    with the new expected wording for whatever (imported, skipped) the backend mock returns.
    If that test's mock returns a bare int, convert it to `(1, 0)` tuple form and expect `"Imported 1 new"`.
  </behavior>
  <action>
    Open tests/test_import_dialog_qt.py. Import `_format_import_summary` from `musicstreamer.ui_qt.import_dialog` (it does not yet exist — that's fine, Task 2 creates it; these tests are written first, will initially fail import).

    Add the seven test functions listed in <behavior>. Use the existing qtbot fixture and ImportDialog construction pattern already in the file. For worker-driven tests, either:
      (a) monkeypatch the backend and use `qtbot.waitSignal(dialog._aa_import_worker.finished, timeout=3000)` after triggering import, OR
      (b) directly call `dialog._on_aa_import_complete(3, 7)` after patching `dialog._toast` with a Mock.

    Use `unittest.mock.MagicMock` for the toast callback capture.

    Update the existing `test_youtube_import_calls_import_stations` (around line 117):
      - Ensure backend mock returns a 2-tuple (e.g. `(1, 0)`)
      - Replace the old `toast_cb.assert_called_once_with("Imported 1 stations")` assertion with `toast_cb.assert_called_once_with("Imported 1 new")`

    Run the new tests — they MUST fail against HEAD (either import error for `_format_import_summary` or signal/wording mismatch).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_import_dialog_qt.py -x -q</automated>
    Expected before Task 2: multiple FAILs. After Task 2: all PASS.
  </verify>
  <done>Seven new test functions added; existing test_youtube_import_calls_import_stations assertion updated; tests fail against HEAD.</done>
  <acceptance_criteria>
    - `grep -n "def test_format_import_summary_imported_only" tests/test_import_dialog_qt.py` matches
    - `grep -n "def test_format_import_summary_skipped_only" tests/test_import_dialog_qt.py` matches
    - `grep -n "def test_format_import_summary_mixed" tests/test_import_dialog_qt.py` matches
    - `grep -n "def test_aa_import_emits_imported_and_skipped" tests/test_import_dialog_qt.py` matches
    - `grep -n "def test_aa_import_all_deduped" tests/test_import_dialog_qt.py` matches
    - `grep -n "def test_yt_import_emits_imported_and_skipped" tests/test_import_dialog_qt.py` matches
    - `grep -n "def test_yt_import_new_only_no_skip_clause" tests/test_import_dialog_qt.py` matches
    - `grep -n "Imported 1 stations" tests/test_import_dialog_qt.py` returns no matches (old assertion gone)
    - `grep -n "All 12 already in library" tests/test_import_dialog_qt.py` matches
    - Running the test file against HEAD exits non-zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Worker signals + _format_import_summary + handler updates</name>
  <files>musicstreamer/ui_qt/import_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/import_dialog.py (entire file — need to locate workers and both handlers)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md (D-07..D-10)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md (Fix 3, Pattern 1, "Context-Sensitive Import Summary" code block, Pitfall 2)
  </read_first>
  <action>
    In musicstreamer/ui_qt/import_dialog.py:

    1. Add a module-level helper near the top (after imports, before the first class):
       ```python
       def _format_import_summary(imported: int, skipped: int) -> str:
           if imported > 0 and skipped == 0:
               return f"Imported {imported} new"
           if imported == 0 and skipped > 0:
               return f"All {skipped} already in library"
           return f"Imported {imported} new, {skipped} skipped (already in library)"
       ```

    2. In `class _AaImportWorker(QThread)`:
       - Change `finished = Signal(int)` to `finished = Signal(int, int)`
       - In `run()`, replace the current emit block with:
         ```python
         result = aa_import.import_stations_multi(...)  # keep existing call
         if isinstance(result, tuple):
             imported, skipped = int(result[0]), int(result[1])
         else:
             imported, skipped = int(result), 0
         self.finished.emit(imported, skipped)
         ```

    3. In `class _YtImportWorker(QThread)`:
       - Change `finished = Signal(int)` to `finished = Signal(int, int)`
       - Apply the same tuple-unpack + `self.finished.emit(imported, skipped)` pattern. Keep the existing `yt_import.import_stations(...)` call unchanged.

    4. Update `_on_aa_import_complete`:
       - Change signature from `def _on_aa_import_complete(self, count: int) -> None:` to `def _on_aa_import_complete(self, imported: int, skipped: int) -> None:`
       - Body:
         ```python
         msg = _format_import_summary(imported, skipped)
         self._aa_status.setText(msg)
         self._aa_status.setVisible(True)
         self._toast(msg)
         self.import_complete.emit()
         ```
       - Remove any prior `"Imported {count} channels"` or similar wording.

    5. Update `_on_yt_import_complete` identically (D-09 parity):
       - Signature: `def _on_yt_import_complete(self, imported: int, skipped: int) -> None:`
       - Body uses `self._yt_status` (the existing YT status QLabel) and `self._toast(msg)`.

    6. If either handler is wired via `worker.finished.connect(self._on_aa_import_complete)` or `self._on_yt_import_complete`, the connection already works after the signature change (PySide6 auto-splats positional signal args). No connect-line changes required.

    7. Ensure the inline status labels `self._aa_status` and `self._yt_status` are already constructed; if for any reason they are absent, add them as `QLabel("", parent)` placed directly below the per-tab progress bar (Claude's discretion position per CONTEXT.md) and include them in the tab layout. Call `.setVisible(False)` at construction.

    8. Do NOT introduce new dependencies, new signals beyond the two type changes, or touch any code outside import_dialog.py.
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_import_dialog_qt.py -x -q</automated>
  </verify>
  <done>All seven new tests + updated existing test pass; full tests/test_import_dialog_qt.py suite green.</done>
  <acceptance_criteria>
    - `grep -n "def _format_import_summary" musicstreamer/ui_qt/import_dialog.py` matches exactly once
    - `grep -n "finished = Signal(int, int)" musicstreamer/ui_qt/import_dialog.py` has at least 2 matches (AA + YT workers)
    - `grep -nE "finished = Signal\\(int\\)\\s*$" musicstreamer/ui_qt/import_dialog.py` returns no matches (no old single-int signatures left on worker classes)
    - `grep -n "def _on_aa_import_complete(self, imported: int, skipped: int)" musicstreamer/ui_qt/import_dialog.py` matches
    - `grep -n "def _on_yt_import_complete(self, imported: int, skipped: int)" musicstreamer/ui_qt/import_dialog.py` matches
    - `grep -n "All {skipped} already in library" musicstreamer/ui_qt/import_dialog.py` matches
    - `grep -n "Imported {imported} new, {skipped} skipped" musicstreamer/ui_qt/import_dialog.py` matches
    - `grep -n "Imported.*channels" musicstreamer/ui_qt/import_dialog.py` returns no matches (old wording gone)
    - pytest tests/test_import_dialog_qt.py exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → worker → UI | Integer counts cross from import backend through Qt signal to QLabel/toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-FB-01 | Tampering | Import count integers | accept | Values originate in local backend code (`aa_import`, `yt_import`); coerced via `int(...)` before emit; rendered via `QLabel.setText` (plain text, no rich-text injection path). |
| T-40.1-FB-02 | Denial of Service | Signal type mismatch on connect | mitigate | Both signal signatures AND handler signatures updated atomically in Task 2; regression test asserts emission works end-to-end (per Pitfall 2). |
</threat_model>

<verification>
- All new + updated tests pass
- Full tests/test_import_dialog_qt.py suite green
- Manual: re-import same AA key twice → second run shows "All N already in library"; YT playlist with mixed new/dup shows both counts
</verification>

<success_criteria>
- `_AaImportWorker.finished` and `_YtImportWorker.finished` are both `Signal(int, int)`
- Both handlers render context-sensitive wording per D-10
- Inline label (`self._aa_status` / `self._yt_status`) and toast both receive the message (D-07, D-09)
- Test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-03-SUMMARY.md` documenting: signal signature change, wording rules, test count delta.
</output>
