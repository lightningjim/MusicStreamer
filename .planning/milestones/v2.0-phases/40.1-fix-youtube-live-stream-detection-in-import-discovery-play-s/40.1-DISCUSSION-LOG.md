# Phase 40.1: Bug-Fix Sweep - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-13
**Phase:** 40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s
**Areas discussed:** Discovery icons, AA import summary, Station logos (folded from Phase 45), ICY disable (folded from Phase 45)

Note: YT import fix had no gray area — bug fix with one obvious correct answer. User selected only Discovery icons + AA summary initially, then mid-discussion folded all Phase 45 scope (logos + ICY disable) into this phase.

---

## Discovery Icons

### Toggle behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Single toggle button | One button per row swaps between play/stop icon | ✓ |
| Play + Stop columns | Two separate icon buttons per row | |

**User's choice:** Single toggle button (Recommended)
**Notes:** Matches existing text-toggle behavior, less column width.

### Icon source

| Option | Description | Selected |
|--------|-------------|----------|
| Theme icons | QIcon.fromTheme + bundled SVG fallback | ✓ |
| Unicode symbols | ▶ / ■ as button text | |
| Custom SVGs only | Custom assets in icons.qrc | |

**User's choice:** Theme icons (Recommended)
**Notes:** Matches Phase 36 pattern.

---

## AA Import Summary

### Feedback format

| Option | Description | Selected |
|--------|-------------|----------|
| Detailed summary | Toast + inline label: "N new, M skipped (already in library)" | ✓ |
| Toast only with counts | Toast: "N new, M skipped" | |
| You decide | Claude picks | |

**User's choice:** Detailed summary (Recommended)

### Apply to YouTube too?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, both imports | YT import also gets the same summary | ✓ |
| AA only | Only change AA feedback | |

**User's choice:** Yes, both imports (Recommended)
**Notes:** Worker signal types must change from Signal(int) to Signal(int, int).

---

## Phase 45 Fold-In Decision

**User input:** "I am also wanting to move everything from phase 45 to here, too"
**Outcome:** Folded entirety of Phase 45 (station logos + ICY disable regression) into Phase 40.1 scope. Original Phase 40.1 had items 1–3; final phase has items 1–5.

---

## Station Logos (folded)

### Logo input mode in EditStationDialog

| Option | Description | Selected |
|--------|-------------|----------|
| Both | File picker + auto-fetch on URL paste | ✓ |
| File upload only | Just file picker | |
| Auto-fetch only | Only re-fetch from URL | |

**User's choice:** Both (Recommended)
**Notes:** Matches v1.5 EditStation behavior. User reported: "none of them have the option to upload nor import (considering import was both for YT and AudioAddict before) so I can't make that work."

---

## ICY Disable Toggle (folded)

### Suppression scope

| Option | Description | Selected |
|--------|-------------|----------|
| Track title + cover art | Full ICY ignore — station name + station logo only | ✓ |
| Cover art only | ICY title still updates, cover art suppressed | |

**User's choice:** Track title + cover art (Recommended)
**Notes:** User reported: "The itunes album cover pull (or whatever API we are using) still shows regardless of if 'Disable ICY' is enabled or not."

---

## Claude's Discretion

- Exact icon SVG paths
- Inline label position in ImportDialog
- EditStationDialog logo preview size
- "Clear logo" button inclusion
- Test fixture details

## Deferred Ideas

- Diagnostic logging in yt_import.scan_playlist
- Logo cropping/resizing UI
