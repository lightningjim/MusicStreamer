---
phase: 40.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicstreamer/ui_qt/import_dialog.py
  - tests/test_import_dialog_qt.py
autonomous: true
requirements:
  - REGRESSION-YT-SCAN  # D-01, D-02
tags: [qt, import, youtube, regression]

must_haves:
  truths:
    - "scan_playlist() results pass through _YtScanWorker to _on_yt_scan_complete unchanged"
    - "A YT playlist containing live streams shows non-empty checklist in UI"
    - "Entries lacking an 'is_live' key still reach the handler"
  artifacts:
    - path: "musicstreamer/ui_qt/import_dialog.py"
      provides: "_YtScanWorker.run() with no post-filter"
      contains: "self.finished.emit(results)"
    - path: "tests/test_import_dialog_qt.py"
      provides: "Regression test for scan pass-through"
      contains: "test_yt_scan_passes_through"
  key_links:
    - from: "_YtScanWorker.run"
      to: "_on_yt_scan_complete"
      via: "finished signal"
      pattern: "finished\\.emit\\(results\\)"
---

<objective>
Fix the double-filter bug in `_YtScanWorker.run()` that drops every YouTube live stream result. `yt_import.scan_playlist()` already filters via `_entry_is_live()` and returns dicts without an `is_live` key, so the dialog's secondary `[e for e in results if e.get("is_live") is True]` comprehension always yields `[]`.

Purpose: Restore YouTube playlist import functionality (UI-07 regression).
Output: Pass-through worker + regression test covering the exact failure mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md
@.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md
@musicstreamer/ui_qt/import_dialog.py
@musicstreamer/yt_import.py
@tests/test_import_dialog_qt.py

<interfaces>
From musicstreamer/yt_import.py — scan_playlist() returns:
```python
# list of dicts with keys: "title", "url", "provider"
# Pre-filtered via _entry_is_live(); no "is_live" key in output dicts.
```

Current buggy code at musicstreamer/ui_qt/import_dialog.py around line 64:
```python
class _YtScanWorker(QThread):
    finished = Signal(list)
    error = Signal(str)
    def run(self):
        try:
            results = yt_import.scan_playlist(self._url)
            live = [e for e in results if e.get("is_live") is True]  # BUG — drops all
            self.finished.emit(live)
        except Exception as exc:
            self.error.emit(str(exc))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression test for scan_playlist pass-through</name>
  <files>tests/test_import_dialog_qt.py</files>
  <read_first>
    - tests/test_import_dialog_qt.py (existing test patterns, imports, fixtures)
    - musicstreamer/ui_qt/import_dialog.py (for _YtScanWorker, _on_yt_scan_complete signatures)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md (Fix 1 section)
  </read_first>
  <behavior>
    - Test name: test_yt_scan_passes_through
    - Monkeypatch musicstreamer.yt_import.scan_playlist to return:
        [{"title": "Stream A", "url": "https://youtube.com/watch?v=a", "provider": "youtube"},
         {"title": "Stream B", "url": "https://youtube.com/watch?v=b", "provider": "youtube"}]
      (NO "is_live" key — mirrors real scan_playlist output)
    - Instantiate ImportDialog with qtbot, trigger the YT scan path on a dummy URL
    - Wait for _on_yt_scan_complete (use qtbot.waitSignal on the worker's finished signal OR the resulting checklist population)
    - Assert the results QListWidget contains 2 items with the monkeypatched titles
    - This test MUST fail against current buggy code (live=[] drops both entries)
  </behavior>
  <action>
    Append a new test function `test_yt_scan_passes_through(qtbot, monkeypatch)` to tests/test_import_dialog_qt.py.
    Mirror the existing qtbot ImportDialog construction pattern already in the file.
    Use `monkeypatch.setattr("musicstreamer.yt_import.scan_playlist", lambda url: [...])` with two dicts that have ONLY keys "title", "url", "provider" — deliberately omit "is_live".
    Set the YT URL field, click Scan (or call the internal scan trigger directly), use `qtbot.waitUntil` to wait until the results list widget has 2 rows (timeout 3000ms).
    Assert: `assert dialog._yt_results.count() == 2` and verify titles present.
    Run the test and confirm it FAILS against HEAD (git stash any code fix first if overlapping).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_import_dialog_qt.py::test_yt_scan_passes_through -x -q</automated>
    Expected before Task 2: FAIL (0 results instead of 2). After Task 2: PASS.
  </verify>
  <done>Test file updated, new test named test_yt_scan_passes_through exists, test FAILS against unfixed code.</done>
  <acceptance_criteria>
    - grep `def test_yt_scan_passes_through` in tests/test_import_dialog_qt.py returns a match
    - Test uses monkeypatch and dicts without "is_live" key (grep absence of `"is_live"` inside the new test body)
    - Running the test against HEAD (pre-fix) fails with assertion about result count
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove double-filter in _YtScanWorker.run</name>
  <files>musicstreamer/ui_qt/import_dialog.py</files>
  <read_first>
    - musicstreamer/ui_qt/import_dialog.py (find _YtScanWorker class and run() method)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-CONTEXT.md (D-01)
    - .planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-RESEARCH.md (Fix 1 — exact before/after code)
  </read_first>
  <action>
    In musicstreamer/ui_qt/import_dialog.py, locate `class _YtScanWorker(QThread)`.
    In its `run()` method, delete this exact line:
      `live = [e for e in results if e.get("is_live") is True]`
    And change:
      `self.finished.emit(live)`
    to:
      `self.finished.emit(results)`

    Final method body must read:
    ```python
    def run(self):
        try:
            results = yt_import.scan_playlist(self._url)
            self.finished.emit(results)
        except Exception as exc:
            self.error.emit(str(exc))
    ```

    No other changes to the file. Per D-01. No diagnostic logging (deferred).
  </action>
  <verify>
    <automated>uv run --with pytest --with pytest-qt pytest tests/test_import_dialog_qt.py::test_yt_scan_passes_through tests/test_import_dialog_qt.py -x -q</automated>
  </verify>
  <done>test_yt_scan_passes_through passes; existing YT scan tests still pass; file no longer contains the `is_live` filter.</done>
  <acceptance_criteria>
    - `grep -n "is_live" musicstreamer/ui_qt/import_dialog.py` returns no matches in _YtScanWorker.run() body
    - `grep -n "self.finished.emit(results)" musicstreamer/ui_qt/import_dialog.py` matches inside _YtScanWorker.run()
    - `grep -n "live = \[e for e in results" musicstreamer/ui_qt/import_dialog.py` returns no matches
    - pytest test_yt_scan_passes_through exits 0
    - Full tests/test_import_dialog_qt.py suite exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yt-dlp → worker | Untrusted playlist metadata crosses into the dialog worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-YT-01 | Tampering | _YtScanWorker result payload | accept | Results are plain strings displayed via QListWidget default item text; no HTML/QTextDocument rendering; no rich-text injection path. Existing iTunes cover art path unchanged. |
</threat_model>

<verification>
- Regression test `test_yt_scan_passes_through` passes
- Full test_import_dialog.py suite passes (no existing tests regressed)
- Manual: scan playlist `https://www.youtube.com/playlist?list=PL6NdkXsPL07Il2hEQGcLI4dg_LTg7xA2L` shows ≥1 live entry in checklist
</verification>

<success_criteria>
- `_YtScanWorker.run()` contains `self.finished.emit(results)` and no `is_live` filter
- Regression test exists and passes
- `pytest tests/test_import_dialog_qt.py -x -q` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-fix-youtube-live-stream-detection-in-import-discovery-play-s/40.1-01-SUMMARY.md` documenting: bug, fix, test added, test count delta.
</output>
