# Phase 999.8: Twitch stations play silent — likely Phase 47.2 EQ regression - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 999.8-twitch-stations-play-silent-likely-phase-47-2-eq-regression
**Areas discussed:** Scope, Regression guard / testing

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Diagnostic confirmation | Whether to include a formal diagnostic task (toggle EQ off) before writing fix. | |
| Fix shape | Dynamic plug/unplug vs. bin-wrapper with audioconvert vs. both. | |
| Scope — Twitch only or audit all stream types | Narrow fix vs. full-matrix audit. | ✓ |
| Regression guard / testing | What catches this class of bug next time. | ✓ |

**User's choice:** Scope + Regression. Other two locked as Claude's discretion.

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Twitch only — narrow fix | Fix the symptom; format-level remedy benefits others implicitly. | |
| Audit all stream types | 5 stream types × EQ states matrix with captured caps evidence. | ✓ |
| Twitch + light smoke test | Fix Twitch, smoke-test others. | |

**User's choice:** Audit all stream types.

### Audit matrix dimensions (multi-select — all chosen)

| Option | Description | Selected |
|--------|-------------|----------|
| Stream types × EQ states | 5 × 3 = 15 cells. | ✓ |
| Preamp edge cases | Extreme preamp values per stream type. | ✓ |
| Profile band-count variance | 1 / 5 / 10 / 20+ bands per stream type. | ✓ |
| Format-caps inspection | Capture EQ input/output caps per cell as grep-able evidence. | ✓ |

**User's choice:** All four dimensions.

---

## Regression guard / testing (multi-select)

| Option | Description | Selected |
|--------|-------------|----------|
| Unit test: audio-filter caps negotiation | Pytest with synthetic appsrc + explicit caps, asserts bypass passthrough. | ✓ |
| Integration test: one gst-launch per stream type | Live-URL invocations; CI or manual. | ✓ |
| Manual UAT checklist on EQ-adjacent PRs | Human gate in CLAUDE.md. | |
| Runtime assertion / logging | Pad-probe logs silent-but-elapsed. | |

**User's choice:** Unit test + integration (local-only manual).

### Integration test runtime

| Option | Description | Selected |
|--------|-------------|----------|
| Local-only manual command | `scripts/eq_audit.sh` run on demand; not in pytest. | ✓ |
| Pytest with slow marker | `@pytest.mark.slow`, excluded from default. | |
| Both — script + test | Redundancy. | |

**User's choice:** Local-only manual command. Keeps CI fast; run the harness before release.

---

## Claude's Discretion

- **Diagnostic confirmation:** Phase 1 task = reproduce silent playback and toggle EQ off to confirm hypothesis. If confirmed → proceed. If NOT confirmed → stop, widen research (playbin3 sink / buffering / HLS segment boundary interactions). Hard gate.
- **Fix shape:** On confirmed hypothesis, wrap `equalizer-nbands` in `GstBin(audioconvert ! equalizer-nbands ! audioconvert)` and set the bin as `playbin3.audio-filter`. Add `audioresample` only if caps evidence shows rate mismatch. Do NOT switch to dynamic plug/unplug (too invasive, race-prone during state transitions).
- **Scope-creep redirection:** Auditing other playbin3 slots (video-sink, text-sink) rejected — not in the audit matrix user approved. If something else latently silent-fails, Phase 999.x will surface it separately.

## Deferred Ideas

- Runtime pad-probe logging (instrumentation overhead).
- Manual UAT checklist in CLAUDE.md (single-user project, no reviewer).
- CI integration of `scripts/eq_audit.sh` (user chose local-only).
- Dynamic plug/unplug audio-filter (superseded by bin-wrapper).
- Auditing non-audio playbin3 slots.
