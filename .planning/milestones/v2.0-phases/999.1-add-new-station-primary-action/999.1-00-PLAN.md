---
phase: 999.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_edit_station_dialog.py
  - tests/test_station_list_panel.py
  - tests/test_main_window_integration.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, SAVE-CLEANUP]
tags: [qt, pyside6, testing, wave0, tdd]
must_haves:
  truths:
    - "15 new test stubs exist in the three test files, all currently failing (RED)"
    - "Stubs use existing fixture patterns from their host files"
    - "No production code is modified by this plan"
    - "pytest --collect-only lists all 15 new test names"
  artifacts:
    - path: "tests/test_edit_station_dialog.py"
      provides: "8 new is_new-mode test stubs + SAVE-CLEANUP test"
      contains: "def test_is_new_mode_constructs_and_populates"
    - path: "tests/test_station_list_panel.py"
      provides: "3 new \"+\" button + select_station test stubs"
      contains: "def test_new_station_button_exists_and_right_aligned"
    - path: "tests/test_main_window_integration.py"
      provides: "4 new menu/save/select integration test stubs"
      contains: "def test_hamburger_menu_has_new_station_first_in_group1"
  key_links:
    - from: "tests/test_edit_station_dialog.py stubs"
      to: "EditStationDialog.is_new kwarg"
      via: "constructor call with is_new=True"
      pattern: "is_new=True"
    - from: "tests/test_station_list_panel.py stubs"
      to: "StationListPanel.new_station_requested / select_station"
      via: "signal spy + method call"
      pattern: "new_station_requested|select_station"
---

<objective>
Wave 0: Create 15 failing test stubs covering every D-01..D-08 decision plus SAVE-CLEANUP. No production code changes. This plan unblocks Plans 01/02/03 by pre-committing the test contracts each subsequent task must satisfy.

Purpose: Satisfy Nyquist sampling — every task in Plans 01/02/03 cites a specific pytest name as its `<automated>` verify command. Those test functions must exist (even if RED) at the moment those tasks run.

Output: Three modified test files containing 15 new `def test_...` functions, each asserting the target behavior but currently failing (ImportError / AttributeError / AssertionError expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-add-new-station-primary-action/999.1-CONTEXT.md
@.planning/phases/999.1-add-new-station-primary-action/999.1-RESEARCH.md
@.planning/phases/999.1-add-new-station-primary-action/999.1-VALIDATION.md
@tests/test_edit_station_dialog.py
@tests/test_station_list_panel.py
@tests/test_main_window_integration.py

<interfaces>
<!-- Key types/APIs executor needs. All VERIFIED in codebase. -->

From musicstreamer/ui_qt/edit_station_dialog.py:190 (current constructor):
```python
class EditStationDialog(QDialog):
    station_saved = Signal()
    station_deleted = Signal(int)
    def __init__(self, station: Station, player, repo, parent=None) -> None: ...
```
After Plan 01 the constructor gains `is_new: bool = False` — Wave 0 tests must instantiate with `is_new=True`.

From musicstreamer/ui_qt/station_list_panel.py:86-90 (current signals):
```python
class StationListPanel(QWidget):
    station_activated = Signal(Station)
    station_favorited = Signal(Station, bool)
    edit_requested = Signal(Station)
    def __init__(self, repo, parent: QWidget | None = None) -> None: ...
```
After Plan 02 the class gains `new_station_requested = Signal()` and `select_station(station_id: int) -> None` method.

From musicstreamer/models.py:
```python
@dataclass
class Station:
    id: int; name: str; provider_id: Optional[int]; provider_name: Optional[str]
    tags: str; station_art_path: Optional[str]; album_fallback_path: Optional[str]
    icy_disabled: bool; last_played_at: Optional[str] = None
    is_favorite: bool = False; streams: list[StationStream] = field(default_factory=list)
```

From musicstreamer/repo.py:253-258:
```python
def create_station(self) -> int:
    cur = self.con.execute("INSERT INTO stations(name) VALUES ('New Station')")
    self.con.commit()
    return int(cur.lastrowid)
```

Existing test fixtures (tests/test_edit_station_dialog.py:17-50):
- `station` — a Station with id=1, name="Test FM", provider_id=1
- `repo` — MagicMock with list_providers/list_streams/ensure_provider preset
- `player` — MagicMock
- `dialog` — constructs `EditStationDialog(station, player, repo)`

The test file for main_window uses `FakePlayer(QObject)` with title_changed, failover, offline, playback_error, elapsed_updated, buffer_percent signals.
</interfaces>
</context>

<pitfalls>
- All tests in this plan are EXPECTED TO FAIL — do NOT run production-code fixes. Wave 0 is pure test-authoring.
- Do NOT modify the existing `station` / `repo` / `player` / `dialog` fixtures. Add NEW fixtures (e.g. `new_station_fixture`, `new_dialog`) alongside them so existing tests are unaffected.
- `test_reject_in_edit_mode_does_not_delete` (D-04c) is a regression guard — it must prove the DEFAULT (`is_new` absent/False) path does NOT call `repo.delete_station`.
- Test assertions must use specific fixture state — e.g. `repo.delete_station.assert_called_once_with(station_id)` not `assert repo.delete_station.called`.
- For MagicMock `repo.delete_station`, the test doubles in test_edit_station_dialog.py already mock `repo`. For station_list_panel tests, FakeRepo has no `delete_station` — not needed; panel never deletes.
- For main_window integration tests, FakeRepo must grow `create_station() -> int`, `get_station(id) -> Station`, `delete_station(id)` if tests invoke them. Stub these on FakeRepo in the test file alongside the new tests, returning reasonable placeholder values (e.g., `create_station` returns the next integer id from an internal counter).
- pytest-qt signal spy pattern: `from pytestqt.qtbot import QtBot; qtbot.waitSignal(panel.new_station_requested, timeout=500)` OR use `QSignalSpy`. Either works; match existing test file style.
- `test_new_mode_provider_combo_blank` (D-08): after constructing with `is_new=True`, assert `dialog.provider_combo.currentText() == ""`. The fixture station MUST have `provider_name=None`; create a new `fresh_station` fixture rather than mutating existing one.
</pitfalls>

<tasks>

<task type="auto" tdd="false">
  <name>Task 00-01: Add 15 failing test stubs across three test files</name>
  <files>tests/test_edit_station_dialog.py, tests/test_station_list_panel.py, tests/test_main_window_integration.py</files>
  <read_first>
    - tests/test_edit_station_dialog.py (existing fixtures, lines 1-80)
    - tests/test_station_list_panel.py (FakeRepo + panel construction patterns)
    - tests/test_main_window_integration.py (FakePlayer + FakeRepo patterns)
    - musicstreamer/ui_qt/edit_station_dialog.py:184-210 (current constructor)
    - musicstreamer/ui_qt/station_list_panel.py:83-92 (current signals)
    - .planning/phases/999.1-add-new-station-primary-action/999.1-VALIDATION.md (per-task verification map — source of truth for every test name)
  </read_first>
  <behavior>
    In tests/test_edit_station_dialog.py, append these 8 test functions (all currently failing with TypeError on unknown `is_new` kwarg or AttributeError on `_is_new`):

    1. `test_is_new_mode_constructs_and_populates(qtbot, station, player, repo)` — construct `EditStationDialog(station, player, repo, is_new=True)`; assert `dialog._is_new is True` and `dialog.name_edit.text() == station.name` (placeholder "New Station" from repo.create_station).
    2. `test_is_new_mode_pre_adds_blank_stream_row(qtbot, station, player, repo)` — add a fresh fixture `fresh_station` with `streams=[]` and `provider_name=None`; construct with `is_new=True`; assert `dialog.streams_table.rowCount() == 1` AND row 0 URL cell is empty string.
    3. `test_reject_in_new_mode_deletes_placeholder(qtbot, station, player, repo)` — construct `is_new=True` with `station.id=42`; call `dialog.reject()`; assert `repo.delete_station.assert_called_once_with(42)`.
    4. `test_close_in_new_mode_deletes_placeholder(qtbot, station, player, repo)` — construct `is_new=True`, id=42; call `dialog.close()`; assert `repo.delete_station.assert_called_once_with(42)`.
    5. `test_reject_in_edit_mode_does_not_delete(qtbot, station, player, repo)` — construct with DEFAULT (no is_new); call `dialog.reject()`; assert `repo.delete_station.call_count == 0`. This is the D-04c regression guard.
    6. `test_new_mode_empty_name_blocks_save(qtbot, station, player, repo, monkeypatch)` — construct `is_new=True`; set `dialog.name_edit.setText("")`; monkeypatch `QMessageBox.warning` to record calls; invoke `dialog._on_save()`; assert `repo.update_station.call_count == 0` AND `QMessageBox.warning` was called once.
    7. `test_new_mode_provider_combo_blank(qtbot, player, repo)` — use a `fresh_station` fixture with `provider_name=None` and `provider_id=None`; construct `is_new=True`; assert `dialog.provider_combo.currentText() == ""`.
    8. `test_save_clears_is_new_flag_to_prevent_delete_on_close(qtbot, station, player, repo)` — construct `is_new=True`, id=42; set name="X" so save succeeds; call `dialog._on_save()`; then call `dialog.reject()`; assert `repo.delete_station.call_count == 0` (flag was flipped false in _on_save).

    In tests/test_station_list_panel.py, append these 3 test functions:

    9. `test_new_station_button_exists_and_right_aligned(qtbot)` — construct panel with FakeRepo; assert `hasattr(panel, "_new_station_btn")`, button is a QToolButton, `button.toolTip() == "New Station"`; verify the button's parent layout contains an `addStretch` before it (right-alignment).
    10. `test_new_station_button_emits_signal(qtbot)` — construct panel; use `qtbot.waitSignal(panel.new_station_requested, timeout=500)`; programmatically click `panel._new_station_btn` via `qtbot.mouseClick(panel._new_station_btn, Qt.LeftButton)`; assert the signal fired.
    11. `test_select_station_by_id_sets_current_index(qtbot)` — construct panel with FakeRepo containing 2 stations (id=1, id=2); call `panel.select_station(2)`; assert `panel.tree.currentIndex().isValid()` is True AND the station at that proxy index has id==2 (use `panel._proxy.mapToSource(panel.tree.currentIndex())` then `panel.model.station_for_index(...)`).

    In tests/test_main_window_integration.py, append these 4 test functions. Extend FakeRepo in this file with `create_station(self) -> int` (returns next id from an internal counter starting at 100), `get_station(self, sid) -> Station` (returns a fresh placeholder Station(id=sid, name="New Station", provider_id=None, provider_name=None, tags="", station_art_path=None, album_fallback_path=None, icy_disabled=False, streams=[])), `delete_station(self, sid)` (no-op tracking list).

    12. `test_hamburger_menu_has_new_station_first_in_group1(qtbot)` — construct MainWindow; iterate `mw._menu.actions()`; find "New Station" action; assert its index is less than the "Discover Stations" action index AND less than the "Import Stations" action index.
    13. `test_new_station_menu_creates_placeholder_and_opens_dialog(qtbot, monkeypatch)` — monkeypatch `EditStationDialog.exec` to return `QDialog.Rejected` immediately; construct MainWindow; trigger the "New Station" action via `action.trigger()`; assert FakeRepo recorded one call to `create_station()` AND (because dialog rejected) one call to `delete_station(new_id)`.
    14. `test_new_station_save_refreshes_and_selects(qtbot, monkeypatch)` — monkeypatch `EditStationDialog.exec` to emit `self.station_saved` then return `QDialog.Accepted`; also monkeypatch `StationListPanel.select_station` with a recording MagicMock; trigger "New Station"; assert `StationListPanel.select_station` called with the new id AND `station_panel.refresh_model` called (spy on it).
    15. `test_new_station_save_does_not_auto_play(qtbot, monkeypatch)` — monkeypatch `EditStationDialog.exec` to emit `station_saved` then accept; spy on `MainWindow._sync_now_playing_station` and on `player.play`; trigger "New Station"; assert neither spy was called with the new station (new station must never auto-play per D-07).

    Use `@pytest.fixture()` scope for new fixtures. Keep fresh fixture names distinct (`fresh_station`, not reusing `station`).

    Every new test MUST be collectible (parseable) even if its assertions fail. Test functions that depend on kwargs/methods that do not yet exist will raise at runtime but parse fine.
  </behavior>
  <action>
    Open `tests/test_edit_station_dialog.py` and append the 8 functions listed in behavior above at the end of the file. Add a new `fresh_station` fixture (provider_name=None, provider_id=None, streams=[], tags="") near the existing `station` fixture.

    Open `tests/test_station_list_panel.py` and append the 3 functions. Extend the existing FakeRepo by adding an inner field counter for station ids if tests need it — OR use an existing list with two pre-populated stations (id=1, id=2). Use `qtbot.waitSignal` for signal testing.

    Open `tests/test_main_window_integration.py` and append the 4 functions. Extend `FakeRepo` class definition by adding `create_station`, `get_station`, `delete_station` methods (with instance-level tracking lists/counters so tests can assert call order).

    Do NOT modify `musicstreamer/` production code. Do NOT modify existing test functions or fixtures.

    Naming: match the exact function names in VALIDATION.md's Per-Task Verification Map (table row "Test ID" column maps to `pytest ... -x` commands). These names are locked and referenced by Plans 01/02/03.
  </action>
  <verify>
    <automated>pytest tests/test_edit_station_dialog.py tests/test_station_list_panel.py tests/test_main_window_integration.py --collect-only -q 2>&1 | grep -E "test_is_new_mode_constructs_and_populates|test_is_new_mode_pre_adds_blank_stream_row|test_reject_in_new_mode_deletes_placeholder|test_close_in_new_mode_deletes_placeholder|test_reject_in_edit_mode_does_not_delete|test_new_mode_empty_name_blocks_save|test_new_mode_provider_combo_blank|test_save_clears_is_new_flag_to_prevent_delete_on_close|test_new_station_button_exists_and_right_aligned|test_new_station_button_emits_signal|test_select_station_by_id_sets_current_index|test_hamburger_menu_has_new_station_first_in_group1|test_new_station_menu_creates_placeholder_and_opens_dialog|test_new_station_save_refreshes_and_selects|test_new_station_save_does_not_auto_play" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` lists all 15 new test names (the grep above returns `15`).
    - Full test file still parses: `pytest tests/test_edit_station_dialog.py tests/test_station_list_panel.py tests/test_main_window_integration.py --collect-only` exits 0.
    - `grep -c "def test_" tests/test_edit_station_dialog.py` increased by exactly 8 from its pre-plan baseline.
    - `grep -c "def test_" tests/test_station_list_panel.py` increased by exactly 3.
    - `grep -c "def test_" tests/test_main_window_integration.py` increased by exactly 4.
    - `git diff --stat musicstreamer/` returns empty (no production code touched).
  </acceptance_criteria>
  <done>All 15 stubs collected by pytest; production code untouched; tests are expected-RED and ready for Plans 01/02/03 to turn green.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_edit_station_dialog.py tests/test_station_list_panel.py tests/test_main_window_integration.py --collect-only -q` — should list 15 new test names alongside existing tests.

Run `pytest tests/test_edit_station_dialog.py tests/test_station_list_panel.py tests/test_main_window_integration.py -x` — most new tests will FAIL (by design). Existing tests must still PASS. If any existing test breaks, that indicates accidental fixture modification and must be reverted.
</verification>

<success_criteria>
- 15 new test functions exist with names matching VALIDATION.md's per-task map exactly
- No production code in `musicstreamer/` was modified
- Existing test suite still green (no accidental fixture breakage)
- `pytest --collect-only` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-add-new-station-primary-action/999.1-00-SUMMARY.md` listing the 15 test names, which D-ID each maps to, and their current red/green state.
</output>
</output>
