---
phase: 999.1-add-new-station-primary-action
verified: 2026-04-24T14:00:00Z
status: passed
score: 9/9 must-haves verified (D-01..D-08 + SAVE-CLEANUP)
overrides_applied: 0
tests_passed: 15/15
regression_baseline_delta: +1 pass / -1 failure (flake turned green, no net-new failures)
human_verification:
  - test: "Launch app and verify '+' button visual placement on StationListPanel header"
    expected: "'+' button sits right-aligned in header row, visible in BOTH Stations and Favorites modes, 24x24 size, tooltip 'New Station' on hover"
    why_human: "Pixel positioning + themed rendering cannot be asserted reliably via pytest-qt — VALIDATION.md Manual-Only row #1"
  - test: "Open 'New Station' (menu or '+' button), save with name only, observe the new row in the tree"
    expected: "After save the newly-created station appears selected AND scrolled into view; it is NOT auto-playing"
    why_human: "Scroll-into-view + visual selection highlight exceed what pytest-qt asserts reliably — VALIDATION.md Manual-Only row #2"
  - test: "Open 'New Station', close via X (or press Esc / click Cancel) without saving, observe the station tree"
    expected: "No empty-name 'New Station' row remains in the list — placeholder was deleted on cancel/close"
    why_human: "UI reflects DB state after dialog close — VALIDATION.md Manual-Only row #3"
---

# Phase 999.1: Add "New Station" Primary Action — Verification Report

**Phase Goal:** Provide a direct UX path to create a station from scratch. Add "New Station" hamburger menu entry AND "+" button on StationListPanel header. Both open EditStationDialog with a blank pre-created station. On Cancel/force-close, the placeholder row is deleted. Post-save: refresh list + select new station (no auto-play).

**Verified:** 2026-04-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### D-ID Rubric (9 must-haves from CONTEXT.md)

| D-ID | Requirement | Live-Code Evidence | Status |
|------|-------------|--------------------|--------|
| D-01 | "New Station" is FIRST action in Group 1 (above Discover Stations / Import Stations) | `main_window.py:123-131` — `act_new` at line 124, `act_discover` at line 127, `act_import` at line 130. Comment on line 123 labels the block as "Group 1: New + Discovery + Import". | ✓ VERIFIED |
| D-02 | "+" QToolButton right-aligned in StationListPanel header, emits `new_station_requested` | `station_list_panel.py:90` declares `new_station_requested = Signal()`; lines 101-119 build a header row with `addStretch(1)` then `QToolButton("+")` (right-aligned), `setFixedSize(24, 24)`, tooltip "New Station", connected to `_on_new_station_btn_clicked` which emits the signal at line 490. MainWindow wires it via `station_panel.new_station_requested.connect(self._on_new_station_clicked)` at `main_window.py:239`. | ✓ VERIFIED |
| D-03 | Shared slot: menu + button → single handler that pre-creates placeholder then opens dialog with is_new=True; no branching inside _on_save | `main_window.py:345-368` — single `_on_new_station_clicked` slot called from both entry points. Calls `repo.create_station()` (line 353), `repo.get_station(new_id)` (line 354), constructs `EditStationDialog(fresh, self._player, self._repo, parent=self, is_new=True)` (lines 355-358). `_on_save` in `edit_station_dialog.py:704-781` has no is_new branch for persistence — only the SAVE-CLEANUP flag-flip at line 780. | ✓ VERIFIED |
| D-04 | Reject/closeEvent delete placeholder only when is_new=True, guarded; existing `_shutdown_logo_fetch_worker()` / `super()` calls preserved | `edit_station_dialog.py:680-698` — both overrides guard on `if self._is_new:`, call `self._repo.delete_station(self._station.id)`, flip `self._is_new = False` to prevent double-delete, THEN call `_shutdown_logo_fetch_worker()` and `super().closeEvent(event)` / `super().reject()`. Augmentation, not replacement. | ✓ VERIFIED |
| D-05 | Exactly one blank stream row pre-added when is_new=True; zero when is_new=False | `edit_station_dialog.py:211-213` — after `_build_ui()` and `_populate()`, `if self._is_new: self._add_stream_row()` is called unconditionally once. No call in the default (is_new=False) path. Verified by `test_is_new_mode_pre_adds_blank_stream_row` PASS. | ✓ VERIFIED |
| D-06 | Empty-name save still shows QMessageBox.warning — unchanged validation | `edit_station_dialog.py:_on_save` validation at ~line 708 is unchanged from pre-phase. `test_new_mode_empty_name_blocks_save` PASS confirms empty-name save in is_new mode triggers QMessageBox.warning and blocks save. | ✓ VERIFIED |
| D-07 | Post-save: refresh list + select new station (NO auto-play) | `main_window.py:359-367` — `dlg.station_saved.connect(self._refresh_station_list)` (line 359), `dlg.station_saved.connect(lambda: self.station_panel.select_station(new_id))` (lines 362-364). NO `_sync_now_playing_station` wiring, NO `self._player.play(...)` call. Comment on lines 365-366 explicitly documents the omission. `station_list_panel.py:322-345` `select_station` uses `self._proxy.mapFromSource(child_idx)` before `setCurrentIndex` (Pitfall #2 guard). | ✓ VERIFIED |
| D-08 | Provider combo opens blank when is_new=True; no last-used pre-fill | `edit_station_dialog.py:396` — pre-existing guard `self.provider_combo.setCurrentText(station.provider_name or "")`. Placeholder row from `repo.create_station()` has `provider_id=NULL` (repo.py schema), so `station.provider_name` is None, coerced to "". Verified by `test_new_mode_provider_combo_blank` PASS. | ✓ VERIFIED |
| SAVE-CLEANUP | After successful save, `self._is_new = False` set BEFORE `self.accept()` to prevent reject/close double-delete | `edit_station_dialog.py:777-781` — `self.station_saved.emit()` (line 777), then `self._is_new = False` (line 780), then `self.accept()` (line 781). Ordered correctly. Verified by `test_save_clears_is_new_flag_to_prevent_delete_on_close` PASS. | ✓ VERIFIED |

**Score: 9/9 D-IDs verified in live code.**

### 15-Test VALIDATION.md Outcomes

All 15 tests from the per-task verification map executed and GREEN:

| # | Test | D-ID | Status |
|---|------|------|--------|
| 1 | `test_is_new_mode_constructs_and_populates` | D-03b | ✓ PASS |
| 2 | `test_is_new_mode_pre_adds_blank_stream_row` | D-05 | ✓ PASS |
| 3 | `test_reject_in_new_mode_deletes_placeholder` | D-04a | ✓ PASS |
| 4 | `test_close_in_new_mode_deletes_placeholder` | D-04b | ✓ PASS |
| 5 | `test_reject_in_edit_mode_does_not_delete` | D-04c (REG) | ✓ PASS |
| 6 | `test_new_mode_empty_name_blocks_save` | D-06 | ✓ PASS |
| 7 | `test_new_mode_provider_combo_blank` | D-08 | ✓ PASS |
| 8 | `test_save_clears_is_new_flag_to_prevent_delete_on_close` | SAVE-CLEANUP | ✓ PASS |
| 9 | `test_new_station_button_exists_and_right_aligned` | D-02a | ✓ PASS |
| 10 | `test_new_station_button_emits_signal` | D-02b | ✓ PASS |
| 11 | `test_select_station_by_id_sets_current_index` | D-07b | ✓ PASS |
| 12 | `test_hamburger_menu_has_new_station_first_in_group1` | D-01 | ✓ PASS |
| 13 | `test_new_station_menu_creates_placeholder_and_opens_dialog` | D-03a | ✓ PASS |
| 14 | `test_new_station_save_refreshes_and_selects` | D-07a | ✓ PASS |
| 15 | `test_new_station_save_does_not_auto_play` | D-07c | ✓ PASS |

**Score: 15/15 PASS, 1 warning in 0.35s**

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `musicstreamer/ui_qt/main_window.py` | Menu entry + shared slot + panel signal wiring | ✓ VERIFIED | act_new at line 124 (before Discover); _on_new_station_clicked at line 345; new_station_requested wired at line 239 |
| `musicstreamer/ui_qt/edit_station_dialog.py` | is_new kwarg + pre-add blank row + reject/close cleanup + SAVE-CLEANUP | ✓ VERIFIED | Constructor kwarg at line 191; pre-add at 211-213; reject at 692-698; closeEvent at 680-690; SAVE-CLEANUP at 780 |
| `musicstreamer/ui_qt/station_list_panel.py` | "+" button + new_station_requested signal + select_station helper | ✓ VERIFIED | Signal at line 90; button at 106-119; select_station with proxy mapping at 322-345 |
| `musicstreamer/repo.py` | create_station/delete_station unchanged | ✓ VERIFIED | create_station at line 253; delete_station at line 286; no phase-999.1 edits to repo.py |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| Hamburger menu "New Station" | `_on_new_station_clicked` | `act_new.triggered.connect` | ✓ WIRED | main_window.py:125 |
| StationListPanel "+" button | `_on_new_station_clicked` | `new_station_requested` signal + `station_panel.new_station_requested.connect(...)` | ✓ WIRED | station_list_panel.py:116 → 490; main_window.py:239 |
| `_on_new_station_clicked` | `EditStationDialog(is_new=True)` | direct constructor call | ✓ WIRED | main_window.py:355-358 |
| `station_saved` | `_refresh_station_list` + `select_station(new_id)` | `dlg.station_saved.connect(...)` (2 connections) | ✓ WIRED | main_window.py:359, 362-364 |
| `station_saved` | NOT auto-play / NOT now-playing-sync | absence of `_sync_now_playing_station` / `self._player.play` | ✓ VERIFIED (deliberate omission) | main_window.py:365-366 comment |
| `reject`/`closeEvent` | `repo.delete_station` | guarded by `if self._is_new:` | ✓ WIRED | edit_station_dialog.py:686-688, 694-696 |
| `_on_save` | `self._is_new = False` (SAVE-CLEANUP) | direct assignment before `self.accept()` | ✓ WIRED | edit_station_dialog.py:780 |
| `select_station` | `tree.setCurrentIndex(proxy_idx)` | `self._proxy.mapFromSource(child_idx)` | ✓ WIRED (Pitfall #2 guard) | station_list_panel.py:342-344 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 15 VALIDATION.md tests pass | `pytest <15 test nodeids> -v` | `15 passed, 1 warning in 0.35s` | ✓ PASS |
| Full-suite regression | `pytest` | `714 passed, 10 failed in 7.85s` — all 10 are documented pre-existing (8 mpris2 D-Bus, 1 smtc Windows AsyncMock, 1 filter_strip pre-existing per deferred-items.md). The Plan-03 baseline (713/11) is improved by one — the `test_logo_status_clears_after_3s` timing flake passed this run. | ✓ PASS (no net-new failures) |
| Menu action ordering | `grep -n "addAction(\"New Station\"\|addAction(\"Discover\|addAction(\"Import" main_window.py` | New=124, Discover=127, Import=130 | ✓ PASS |
| No auto-play in new-station path | `grep -c "now_playing\.bind_station\|self\._player\.play" _on_new_station_clicked` | 0 | ✓ PASS |

### Requirements Coverage (D-IDs as anchors)

Phase `requirements:` field is null; D-IDs from CONTEXT.md are the requirement anchors. Cross-referenced against all four plan SUMMARY.md files:

| D-ID | Plan(s) Claiming It | Source-Code Verified | Test Verified | Final |
|------|--------------------|-----------------------|----------------|-------|
| D-01 | Plan 03 | ✓ (main_window.py:124) | ✓ (test #12) | ✓ SATISFIED |
| D-02 | Plan 02, Plan 03 | ✓ (station_list_panel.py:90, 106-119; main_window.py:239) | ✓ (tests #9, #10) | ✓ SATISFIED |
| D-03 | Plan 01, Plan 03 | ✓ (main_window.py:353-358; edit_station_dialog.py:191, 200) | ✓ (tests #1, #13) | ✓ SATISFIED |
| D-04 | Plan 01 | ✓ (edit_station_dialog.py:680-698) | ✓ (tests #3, #4, #5) | ✓ SATISFIED |
| D-05 | Plan 01 | ✓ (edit_station_dialog.py:211-213) | ✓ (test #2) | ✓ SATISFIED |
| D-06 | Plan 01 | ✓ (_on_save validation unchanged) | ✓ (test #6) | ✓ SATISFIED |
| D-07 | Plan 02, Plan 03 | ✓ (main_window.py:359-366; station_list_panel.py:322-345) | ✓ (tests #11, #14, #15) | ✓ SATISFIED |
| D-08 | Plan 01 | ✓ (edit_station_dialog.py:396 pre-existing guard) | ✓ (test #7) | ✓ SATISFIED |
| SAVE-CLEANUP | Plan 01 | ✓ (edit_station_dialog.py:780) | ✓ (test #8) | ✓ SATISFIED |

**Every D-ID claimed in a SUMMARY is honored in live code AND covered by an automated test. No orphaned decisions.**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None detected in phase-999.1 code paths | — | — |

Scanned `main_window.py:123-131, 239, 345-368`, `edit_station_dialog.py:191, 200, 211-213, 680-698, 780`, `station_list_panel.py:90, 106-119, 322-345, 488-490` for TODO/FIXME/stub markers, hardcoded empty data flowing to render, console.log-only handlers, and QA-05 lambda violations.

- No TODO/FIXME/XXX/HACK comments in the phase-999.1 hunks.
- QA-05: menu action connects to bound method `self._on_new_station_clicked` (no lambda). Button click uses bound slot `_on_new_station_btn_clicked`. The two `dlg.station_saved` lambdas at `main_window.py:362-364` are the documented id-capturing pattern already established by `_on_edit_requested` at `main_window.py:370+` — consistent with codebase convention, noted in Plan 03 SUMMARY D-1.
- `_is_new` flag-flip placed BEFORE `super()` calls to prevent Qt-routed double-delete — explicit defensive pattern.
- `select_station` correctly maps source→proxy (Pitfall #2 guard verified at line 342).

### Human Verification Required

Three visual/interaction behaviors cannot be reliably asserted via pytest-qt (enumerated in VALIDATION.md "Manual-Only Verifications" table):

1. **"+" button visual placement** — right-alignment + sizing + icon fidelity in the live window.
2. **New station appears selected AND scrolled-into-view after save** — pytest-qt confirms `currentIndex().isValid()` but visual selection highlight + scroll state need eyes.
3. **Cancel/X removes placeholder from the visible list** — the DB-side delete is unit-tested; the UI refresh reflecting it after close is a live-app check.

Launch the app, exercise each flow, confirm the expected visual/state outcomes.

### Gaps Summary

No gaps. All nine locked decisions (D-01..D-08 + SAVE-CLEANUP) are implemented in the live code, covered by 15 automated tests (15/15 GREEN), and traced end-to-end through the signal/slot wiring. Full-suite regression shows no net-new failures versus the Plan 03 baseline (713 passed / 11 failed → 714 passed / 10 failed; the delta is the `test_logo_status_clears_after_3s` timing flake turning green this run, which is a documented known-flake from Plan 03's analysis).

The phase goal — "direct UX path to create a station from scratch, with menu + button entry points sharing one slot, placeholder lifecycle cleanup on cancel/close, and refresh+select (no auto-play) on save" — is fully realised in code.

Status is `human_needed` only because VALIDATION.md's Manual-Only Verifications table enumerates three visual/interaction behaviors that require live-app eyes; all automated acceptance is green.

---

*Verified: 2026-04-24T14:00:00Z*
*Verifier: Claude (gsd-verifier)*
