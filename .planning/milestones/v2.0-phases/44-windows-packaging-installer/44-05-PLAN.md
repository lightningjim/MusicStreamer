---
phase: 44
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md
  - .planning/phases/44-windows-packaging-installer/44-UAT.md
autonomous: false
requirements: [QA-03, QA-05, PKG-01, PKG-02, PKG-04, RUNTIME-01]
must_haves:
  truths:
    - "QA-05 widget-lifetime audit document exists with the inventory table populated from grep, dialog launch sites confirmed, callback flow audit recorded, and UAT-log regression check performed"
    - "44-UAT.md exists in the format established by 43.1-UAT.md, with D-20 8-item playback checklist + D-21 8-item installer round-trip checklist (includes UAT-21-1.5 AppId brace-form acceptance per checker issue 7) + sign-off rows"
    - "Win11 VM UAT executed: build.ps1 produces dist/installer/MusicStreamer-2.0.0-win64-setup.exe, installer round-trip succeeds, all 8 playback items pass (or DI.fm HTTPS waived per D-15), all 7 installer items pass"
    - "AUMID/SMTC binding verified: SMTC overlay shows 'MusicStreamer' (not 'Unknown app') when launched via Start Menu shortcut"
    - "Settings export Linux↔Windows round-trip preserves stations/streams/favorites/tags/logos (SC-6)"
    - "Single-instance behavior verified: second launch raises existing window, no duplicate process"
  artifacts:
    - path: ".planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md"
      provides: "QA-05 widget-lifetime audit (subclass inventory, dialog launch sites, callback flow, UAT-log regression check)"
      min_lines: 50
    - path: ".planning/phases/44-windows-packaging-installer/44-UAT.md"
      provides: "Phase 44 UAT execution record with D-20 + D-21 checklists + sign-off"
      min_lines: 80
  key_links:
    - from: "44-UAT.md D-21 item 7"
      to: "musicstreamer/__main__.py::_set_windows_aumid + packaging/windows/MusicStreamer.iss [Icons] AppUserModelID"
      via: "shortcut-launched process inherits matching AUMID; SMTC binds to display name"
      pattern: "MusicStreamer.*Start Menu"
    - from: "44-UAT.md D-21 items 4-5"
      to: "musicstreamer/settings_export.py + Phase 42 round-trip behavior"
      via: "ZIP export+move+import preserves stations/streams/favorites/tags/logos via platformdirs.user_data_dir"
      pattern: "Linux.*Windows.*round-trip"
    - from: "44-QA05-AUDIT.md"
      to: "musicstreamer/ui_qt/*.py + musicstreamer/player.py callback flows"
      via: "grep sweep over QWidget subclasses + parent= confirmation + UAT-log regression check"
      pattern: "RuntimeError.*Internal C\\+\\+ object already deleted"
---

<objective>
Three deliverables in one plan: (1) write the QA-05 widget-lifetime audit document
(autonomous grep sweep + doc); (2) create the 44-UAT.md execution record template;
(3) execute the Windows UAT on the Win11 VM (human checkpoint — 8 playback items + 7
installer items + sign-off).

Purpose: closes QA-03 (Windows smoke test on clean VM), QA-05 (widget lifetime audit), and
verifies SC-1 through SC-6 of the phase goal. Last plan before Phase 44 ships.

Output: 2 doc files committed; UAT executed and signed off on the live VM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/44-windows-packaging-installer/44-CONTEXT.md
@.planning/phases/44-windows-packaging-installer/44-RESEARCH.md
@.planning/phases/44-windows-packaging-installer/44-PATTERNS.md
@.planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md
@.planning/phases/44-windows-packaging-installer/44-01-SUMMARY.md
@.planning/phases/44-windows-packaging-installer/44-02-SUMMARY.md
@.planning/phases/44-windows-packaging-installer/44-03-SUMMARY.md
@.planning/phases/44-windows-packaging-installer/44-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 44-QA05-AUDIT.md (widget lifetime audit document)</name>
  <files>.planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md</files>
  <read_first>
    - .planning/phases/44-windows-packaging-installer/44-RESEARCH.md (lines 1290-1370 — full QA-05 audit template)
    - .planning/phases/44-windows-packaging-installer/44-CONTEXT.md (D-23 — document-only audit + spot fixes scope)
    - musicstreamer/ui_qt/main_window.py (dialog launch sites — lines around 540-565)
  </read_first>
  <action>
    Create .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md per RESEARCH §QA-05 Widget Lifetime Audit Template (lines 1294-1367) — copy the structure verbatim, then populate each table with actual grep output.

    (a) Frontmatter: `audit_date`, `scope`.

    (b) "Subclass Inventory" table — populate from:
        ```
        grep -E "class.*\((QWidget|QDialog|QMainWindow|QObject).*\)" musicstreamer/ui_qt/*.py
        ```
        For each match: record class name, file, whether `parent=` is in __init__ signature (read each constructor), lifetime owner, and a checkbox column. Mark all as `☐ OK` if `parent=` is properly passed to super().__init__ (this is expected — Phase 37+ established the convention; D-23 says clean audit expected).

    (c) "Dialog Launch Sites" table — populate from:
        ```
        grep -n "Dialog(.*parent" musicstreamer/ui_qt/main_window.py
        ```
        And similar dialog construction calls. For each: record the call site, whether `parent=self` is present, checkbox.

    (d) "Callback Flow Audit (Player → UI)" — list all signal connections in main_window.py from grep:
        ```
        grep -n "self._player\..*\.connect" musicstreamer/ui_qt/main_window.py
        ```
        For each: record signal, handler, whether handler is bound method (NOT a self-capturing lambda — QA-05 rule), checkbox.

    (e) "UAT Log Regression Check" — run:
        ```
        grep -rn "RuntimeError.*Internal C\+\+ object already deleted" .planning/phases/ || echo "NONE"
        ```
        Paste the result into the doc. Expected: NONE.

    (f) "Spot Fixes" — empty checklist (D-23: "Fix any specific findings inline"). If grep surfaces a real issue, document the fix here AND fix it in code (file the fix as a separate commit). Expected: none required per D-23.

    (g) Sign-off section per RESEARCH lines 1362-1367.

    The document is the deliverable; no code changes expected unless grep surfaces a real lifetime issue.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && test -f .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md && grep -q 'Subclass Inventory' .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md && grep -q 'Dialog Launch Sites' .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md && grep -q 'Callback Flow Audit' .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md && grep -q 'UAT Log Regression Check' .planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md && grep -rn 'RuntimeError.*Internal C++ object already deleted' .planning/phases/ 2>/dev/null | grep -v 44-QA05-AUDIT.md | head -1 && echo "audit doc OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md` exists
    - Contains all 4 required sections: Subclass Inventory, Dialog Launch Sites, Callback Flow Audit, UAT Log Regression Check
    - Subclass Inventory table has ≥ 10 rows (covers all dialogs/widgets in musicstreamer/ui_qt/)
    - Dialog Launch Sites table has ≥ 6 rows (Edit/Discovery/Import/Accent/Cookie/Accounts/Equalizer/SettingsImport)
    - Callback Flow Audit lists at least player.title_changed, player.failover, player.offline, player.playback_error
    - UAT Log Regression Check: `grep -rn "RuntimeError.*Internal C\+\+ object already deleted" .planning/phases/` (excluding the audit doc itself) returns NONE — confirmed in the audit doc body
    - Sign-off section present
  </acceptance_criteria>
  <done>QA-05 audit doc complete; clean grep result documented; no spot fixes required (or fixes filed as separate commit if needed).</done>
</task>

<task type="auto">
  <name>Task 2: Create 44-UAT.md template ready for VM execution</name>
  <files>.planning/phases/44-windows-packaging-installer/44-UAT.md</files>
  <read_first>
    - .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md (canonical UAT format to mirror)
    - .planning/phases/44-windows-packaging-installer/44-RESEARCH.md (lines 1218-1288 — full UAT template)
    - .planning/phases/44-windows-packaging-installer/44-CONTEXT.md (D-19, D-20, D-21 — UAT scope)
  </read_first>
  <action>
    Create .planning/phases/44-windows-packaging-installer/44-UAT.md by copying RESEARCH §Smoke Test / UAT Structure template (lines 1222-1288) verbatim with frontmatter:

    ```
    ---
    phase: 44
    plan: 05
    status: in-progress
    created: 2026-04-25
    updated: 2026-04-25
    ---
    ```

    (Update `created` and `updated` to today's date when writing.)

    Required sections:
      (a) Title: "Phase 44 — Windows UAT (Packaging + Installer)"
      (b) Environment Snapshot (Win11 version, Python version, Conda env, Inno Setup version, Node.js presence, pip list filter)
      (c) Build Artifacts table (PyInstaller bundle path/size, installer EXE path/size, build.log + iscc.log presence)
      (d) D-20 Playback Checklist — 8 rows mapping to D-20 items 1-8 (SomaFM HTTPS, HLS, DI.fm HTTP, YT w/ Node, YT w/o Node 3-prong warning, Twitch OAuth, failover, SMTC media keys)
      (e) D-21 Installer/Round-Trip Checklist — 8 rows (fresh install, [NEW] AppId 3-brace acceptance, uninstall, re-install, Linux→Win settings ZIP, Win→Linux settings ZIP, single-instance raise, AUMID/SMTC display name). The new row UAT-21-1.5 per checker issue 7 reads: "iscc MusicStreamer.iss accepts the 3-brace `AppId={{GUID}` form (info-level pattern_compliance check). Pass = iscc compiles cleanly. Fail = switch to 4-brace `AppId={{{{GUID}}` form per RESEARCH §Pitfall 4, recompile, document iscc version + accepted literal in Notes column."
      (f) Sign-Off section (4 items: D-20 pass, D-21 pass, no traceback entries in logs, QA-05 audit reviewed)

    All checkboxes start `☐` (unchecked) — they fill in during the human checkpoint task.

    Each row in D-20/D-21 must include: # column, Behavior, Requirement, Method (instructions for the user), Pass/Fail checkbox, Notes column.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/MusicStreamer && test -f .planning/phases/44-windows-packaging-installer/44-UAT.md && grep -q '^## D-20' .planning/phases/44-windows-packaging-installer/44-UAT.md && grep -q '^## D-21' .planning/phases/44-windows-packaging-installer/44-UAT.md && grep -q 'SomaFM' .planning/phases/44-windows-packaging-installer/44-UAT.md && grep -q 'AUMID' .planning/phases/44-windows-packaging-installer/44-UAT.md && grep -q 'Linux→Windows' .planning/phases/44-windows-packaging-installer/44-UAT.md && grep -c '☐' .planning/phases/44-windows-packaging-installer/44-UAT.md | awk '$1 >= 16 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/44-windows-packaging-installer/44-UAT.md` exists with frontmatter (phase: 44, plan: 05, status: in-progress)
    - Contains D-20 Playback Checklist section with ≥ 8 rows (one per D-20 item)
    - Contains D-21 Installer/Round-Trip Checklist section with ≥ 8 rows (7 original + UAT-21-1.5 AppId brace acceptance row per checker issue 7)
    - Mentions SomaFM, HLS, DI.fm, YouTube, Node.js, Twitch, SMTC, failover (D-20 items)
    - Mentions install/uninstall, Linux→Windows + Windows→Linux export, single-instance, AUMID (D-21 items)
    - Contains ≥ 16 unchecked ☐ checkboxes (8 D-20 + 8 D-21 including UAT-21-1.5)
    - Sign-Off section present at end
    - Environment Snapshot section has slots for Windows version, Python version, Conda env, Inno Setup version, Node.js
  </acceptance_criteria>
  <done>44-UAT.md template ready for human execution on Win11 VM; all checklist rows present and unchecked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute Phase 44 UAT on Win11 VM</name>
  <what-built>
    - Plans 01-04 produced: tests, modules, __main__.py wiring, MainWindow integration, packaging/windows/ artifacts (.spec, runtime_hook.py, build.ps1, MusicStreamer.iss, EULA.txt, README.md, MusicStreamer.ico)
    - Plan 05 Tasks 1-2 produced: 44-QA05-AUDIT.md (autonomous grep audit) + 44-UAT.md (empty UAT execution template ready to fill in)
    - On the Win11 VM with conda env musicstreamer-build (Python 3.12, PyGObject, GStreamer 1.28, PyInstaller ≥6.19, hooks-contrib ≥2026.2 per D-18) + Inno Setup 6.3+ installed
  </what-built>
  <how-to-verify>
    Execute the UAT on the Win11 VM. Open `.planning/phases/44-windows-packaging-installer/44-UAT.md` for the live checklist; fill in the Environment Snapshot at the top, then run through D-20 (playback) and D-21 (installer) item by item. Replace each ☐ with ✅ (pass) or ❌ (fail with notes), commit the filled-in UAT doc.

    **Build:**

    1. SSH/RDP into the Win11 VM. Activate the conda env:
       ```
       conda activate musicstreamer-build
       cd \path\to\MusicStreamer\packaging\windows
       ```
    2. Run the build:
       ```
       .\build.ps1
       ```
       Expected output:
       - `PKG-03 OK: zero bare subprocess.* calls in musicstreamer/`
       - PyInstaller produces `..\..\dist\MusicStreamer\` (~110 MB, ~126 DLLs, ~184 plugins)
       - Inno Setup produces `..\..\dist\installer\MusicStreamer-2.0.0-win64-setup.exe`
       - `BUILD_OK installer='..\..\dist\installer\MusicStreamer-2.0.0-win64-setup.exe'`
       - `BUILD_DIAG bundle_size_mb=... dll_count=... installer_size_mb=...`

       If build fails: file the failure in 44-UAT.md "Build Artifacts" section with exit code + log excerpt, return for diagnosis. Common failures:
       - `BUILD_FAIL reason=iscc_not_found` → install Inno Setup 6 from https://jrsoftware.org/isdl.php OR set `$env:INNO_SETUP_PATH = "C:\Path\To\iscc.exe"`
       - `BUILD_FAIL reason=version_not_found_in_pyproject` → confirm pyproject.toml line 7 reads `version = "2.0.0"` (Plan 02 should have done this)
       - PyInstaller `ModuleNotFoundError: ... yt_dlp.extractor.foo` → add the specific extractor to .spec hiddenimports + rerun

    **D-20 Playback Checklist (8 items)** — for each, install via the EXE (UAT-21-1 first), launch via Start Menu shortcut, then play the indicated stream and observe the indicated behavior. Mark ✅ in 44-UAT.md row:
       - UAT-20-1: SomaFM Drone Zone HTTPS plays + ICY title appears within 30s
       - UAT-20-2: Any HLS station in your library plays
       - UAT-20-3: A DI.fm channel via HTTP plays (HTTPS expected to fail per D-15 — note in row)
       - UAT-20-4: With Node.js on PATH, a YouTube live stream (LoFi Girl etc.) plays via yt-dlp EJS solver
       - UAT-20-5: Remove Node.js from PATH (`set PATH=...` in a fresh cmd, relaunch app), verify ALL THREE warning surfaces appear: (a) startup QMessageBox dialog, (b) hamburger menu shows "⚠ Node.js: Missing (click to install)", (c) attempt YT play → toast "Install Node.js for YouTube playback". Non-YT streams still work. Restore PATH after.
       - UAT-20-6: Twitch live stream plays via streamlink (requires valid OAuth token in user profile from Phase 32)
       - UAT-20-7: Multi-stream failover — edit a station's primary URL to invalid, play it, verify the next stream in `order_streams()` order picks up
       - UAT-20-8: SMTC overlay shows station + ICY + cover art; hardware media keys (play/pause/stop) work

    **D-21 Installer/Round-Trip Checklist (7 items)** — mark ✅ in 44-UAT.md row:
       - UAT-21-1: Revert VM snapshot to clean Win11; run `MusicStreamer-2.0.0-win64-setup.exe`; verify Start Menu shortcut exists at `%APPDATA%\Microsoft\Windows\Start Menu\Programs\MusicStreamer.lnk`; launch via shortcut succeeds
       - UAT-21-1.5 (NEW per checker issue 7 — AppId 3-brace acceptance): BEFORE running the installer, on the build host (or VM) run `iscc MusicStreamer.iss` and observe whether the compiler accepts the 3-brace `AppId={{914e9cb6-f320-478a-a2c4-e104cd450c88}` form. If iscc emits a syntax error citing AppId or unbalanced braces, switch the literal to the 4-brace form `AppId={{{{914e9cb6-f320-478a-a2c4-e104cd450c88}}` in packaging/windows/MusicStreamer.iss, recompile, then proceed. Document the accepted form in 44-UAT.md row UAT-21-1.5 with the iscc version (`iscc /?` first line) and the literal that compiled. Note: `BUILD_OK installer=...` from build.ps1 already implies iscc accepted whatever form is present, but this row explicitly records the brace-count decision so future maintainers know which form is canonical for this Inno Setup version. (Research left this open as info-level; UAT is the validation moment.)
       - UAT-21-2: Settings → Apps → uninstall MusicStreamer; verify install dir `%LOCALAPPDATA%\MusicStreamer` is removed; verify `%APPDATA%\musicstreamer` (user data) STILL contains musicstreamer.sqlite3 + assets (D-03 invariant)
       - UAT-21-3: Re-run installer (no prior install); succeeds
       - UAT-21-4: On Linux dev box, hamburger menu → Export Settings → save ZIP; copy ZIP to Win11 VM; on Windows installed app, hamburger menu → Import Settings → select ZIP → confirm summary dialog → import; verify all stations, streams, favorites, tags, logos visible AND playable
       - UAT-21-5: Reverse — export from Windows installed app, copy ZIP back to Linux, import; verify same
       - UAT-21-6: With app running, double-click Start Menu shortcut a second time → existing window raises and gets focus (no second window appears, no error dialog)
       - UAT-21-7: SMTC overlay shows "MusicStreamer" (NOT "Unknown app"). Required: launched via Start Menu shortcut. Bare `python -m musicstreamer` from cmd will still show "Unknown app" — this is expected per Phase 43.1 finding (note in row).

    **Final sign-off:**
       - All 8 D-20 items: ✅ (DI.fm HTTPS waiver per D-15 acceptable)
       - All 8 D-21 items: ✅ (including UAT-21-1.5 AppId brace-form acceptance)
       - `dir %APPDATA%\musicstreamer\logs\` shows no new traceback entries from the UAT session
       - 44-QA05-AUDIT.md sign-off section all checked
       - Mark `status: signed-off` in 44-UAT.md frontmatter

    Resume after committing the filled-in 44-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" if all UAT items pass + sign-off committed. If any item fails, describe the failure (item ID + observed behavior + log excerpt) so the failure can be diagnosed and a follow-up plan filed.</resume-signal>
  <files>.planning/phases/44-windows-packaging-installer/44-UAT.md</files>
  <action>HUMAN CHECKPOINT — see <how-to-verify> block above for the full UAT script. Executor: pause here, present the build + checklist instructions to the user, wait for resume signal. Do NOT autonomously execute the build or fill in the checkboxes. The user runs the build on the Win11 VM, walks through D-20 + D-21 items in 44-UAT.md, marks each row, then resumes.</action>
  <verify>
    <automated>grep -q "status: signed-off" .planning/phases/44-windows-packaging-installer/44-UAT.md && ! grep -q "☐" .planning/phases/44-windows-packaging-installer/44-UAT.md</automated>
  </verify>
  <done>44-UAT.md frontmatter has status: signed-off; all ☐ checkboxes replaced with ✅ (or ❌ with explanatory note); user has typed "approved" to resume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build.ps1 → Win11 VM | PowerShell driver invokes PyInstaller, Inno Setup, ripgrep guard with VM-level privileges |
| user → installer EXE | User trusts the .exe to install only the documented files; trust mediated by Windows SmartScreen (unsigned) |
| imported settings ZIP → app | Settings ZIP from another OS is unzipped into platformdirs.user_data_dir; Phase 42 contract handles this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | T (Tampering) | UAT executor signs off without actually testing all items | mitigate | Each row has a Pass/Fail checkbox + Notes column; sign-off requires all 15 ☐ replaced with ✅; reviewer (the user) can audit the doc post-hoc. |
| T-44-05-02 | I (Information Disclosure) | UAT logs leak user data paths or station credentials | accept | Local UAT doc; never published; user owns the file. |
| T-44-05-03 | D (Denial of Service) | Single-instance UAT (item 6) wedges if QLocalServer.listen fails on the VM | mitigate | Plan 02 graceful-degradation: app continues without single-instance guard, logs warning. UAT row would mark FAIL but app still functions. |
| T-44-05-04 | I (Info Disclosure) | Importing Linux settings ZIP includes paths that don't exist on Windows | mitigate | Phase 42 settings_export already handles cross-OS path normalization (logos re-rooted relative). UAT-21-4/5 explicitly tests this. |
</threat_model>

<verification>
Static (Linux dev box, before checkpoint):
- `pytest -q` full suite green
- `python tools/check_subprocess_guard.py` exits 0
- `python tools/check_spec_entry.py` exits 0
- All Plan 01-04 SUMMARY.md files exist
- `.planning/phases/44-windows-packaging-installer/44-QA05-AUDIT.md` exists
- `.planning/phases/44-windows-packaging-installer/44-UAT.md` exists with 16+ ☐ checkboxes (8 D-20 + 8 D-21)

Live (Win11 VM, during checkpoint):
- build.ps1 produces `dist/installer/MusicStreamer-2.0.0-win64-setup.exe`
- `BUILD_DIAG` line in build.log shows bundle_size_mb ≈ 110, dll_count ≈ 126
- All 16 UAT checkboxes (D-20 8 + D-21 8) marked ✅ (DI.fm HTTPS waivable per D-15; UAT-21-1.5 AppId brace check per issue 7)
- 44-UAT.md `status: signed-off` in frontmatter
</verification>

<success_criteria>
- 44-QA05-AUDIT.md document complete with all 4 sections + clean UAT-log grep
- 44-UAT.md template created and then filled in during VM checkpoint
- Win11 VM UAT: all 16 D-20+D-21 items pass including UAT-21-1.5 AppId brace acceptance (or DI.fm HTTPS waived per D-15)
- AUMID/SMTC display name verified ("MusicStreamer" not "Unknown app")
- Settings export Linux↔Windows round-trip preserves all data per SC-6
- Single-instance enforcement verified
- Phase 44 ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/44-windows-packaging-installer/44-05-SUMMARY.md` per the standard SUMMARY template. The SUMMARY should reference the filled-in UAT doc (with all checkboxes resolved) and the QA-05 audit doc as the deliverables proving Phase 44 ships.
</output>
