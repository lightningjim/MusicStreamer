# Phase 999.9: YouTube playback broken — yt-dlp "No video formats found" regression - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 999.9-youtube-playback-broken-no-video-formats-found-regression
**Areas discussed:** Phase shape, Mitigation if upstream

---

## Gray-area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Phase shape | Heavy diagnostic plan vs lightweight investigation pass first | ✓ |
| Mitigation if root cause is upstream | Wait, pin git HEAD, switch player_client, or user-facing toast | ✓ |
| Cookie verification scope | Re-import cookies as diagnostic control vs trust 999.7 verification | |
| Test scope | Automated regression test vs manual UAT only | |

**Notes:** User opted to skip cookie verification (trust 999.7's byte-equality proof) and test scope (single-user pragmatic — manual UAT is the project convention).

---

## Phase shape

| Option | Description | Selected |
|--------|-------------|----------|
| Lightweight diagnostic-first (Recommended) | ONE small plan: terminal probe with `uv run yt-dlp` + `--extractor-args` variants, capture which player_client works, then decide fix scope. Avoid 999.8 trap. | ✓ |
| Lightweight + in-app verification | Above PLUS one in-app verification task driving Player._youtube_resolve_worker. | |
| Full diagnostic + implement + tests | Standard 3-plan phase: diagnostic, implement fix, automated regression test. | |

**User's choice:** Lightweight diagnostic-first
**Notes:** Direct lesson carry-over from Phase 999.8 — full plan-checker pipeline against unconfirmed hypothesis was wasted effort. Researcher still runs (light); planner produces ONE PLAN.md.

---

## Mitigation if root cause is upstream

| Option | Description | Selected |
|--------|-------------|----------|
| Try alternate player_client first, then fall back (Recommended) | Probe ios, web_safari, android. If ANY works → set as default in opts. If NONE works → wait + user-facing toast. | ✓ |
| Pin yt-dlp to git HEAD | Update pyproject.toml to install yt-dlp from git main. Faster fixes propagate but git HEAD breaks more often. | |
| Wait for upstream + toast only | No code change beyond user-facing toast. User updates yt-dlp manually when a fix lands. | |

**User's choice:** Try alternate player_client first, then fall back
**Notes:** Cheap fix if any client works; clean fallback to toast-only if it's truly upstream.

---

## Wrap-up

| Option | Description | Selected |
|--------|-------------|----------|
| Write context (Recommended) | Capture decisions, write CONTEXT.md + DISCUSSION-LOG.md, auto-advance to plan-phase per --chain. | ✓ |
| One more area — cookie verification | Add cookie re-import as diagnostic control. | |
| One more area — test scope | Add automated regression coverage. | |

**User's choice:** Write context

---

## Claude's Discretion

- Reproduction URL — use LoFi Girl or any YouTube live URL the user supplies (D-06).
- Probe order for player_clients — researcher picks based on current yt-dlp issue tracker; probe ALL candidates in one pass for full failure-mode matrix (D-07).

## Deferred Ideas

- Cookie re-import diagnostic — re-open phase if toast-only path is chosen and diagnosis dead-ends.
- Automated regression test for YouTube extraction — could revisit if YouTube regressions become recurring (3+ in 6 months).
- Pin yt-dlp to git HEAD — rejected outright.
- Switch YouTube extraction library entirely — would be a new phase.
- Daemon-thread exception wrapping for `_youtube_resolve_worker` — already covered by scheduled 2026-05-01 sweep agent (`trig_0135THdv9pRxjvFoJVrD5ifh`).
