---
phase: 47.1-stats-for-nerds-buffer-indicator
plan: 01
subsystem: player
tags: [gstreamer, qt-signal, pyside6, bus-handler, buffering]

requires:
  - phase: 35-qt-port
    provides: "Player QObject with class-level Signals and GStreamer bus wiring (message::error + message::tag)"
provides:
  - "Player.buffer_percent Signal(int) — 0-100 GStreamer buffer fill, de-duped"
  - "Player._on_gst_buffering bus-loop-thread handler with defensive tuple/int parse"
  - "Player._last_buffer_percent sentinel with reset hook in _try_next_stream"
  - "tests/test_player_buffering.py — 4 bus-handler unit tests"
affects: [47.1-02 (UI wire-up), future stats-for-nerds rows]

tech-stack:
  added: []
  patterns:
    - "Bus-handler → Qt Signal emission template (mirrors _on_gst_tag)"
    - "Sentinel-based Signal de-dup with per-stream reset"

key-files:
  created:
    - tests/test_player_buffering.py
  modified:
    - musicstreamer/player.py

key-decisions:
  - "Defensive tuple-or-int unpack on msg.parse_buffering() (Pitfall 1) — handles both PyGObject flattened form and formal binding"
  - "Sentinel reset lives in _try_next_stream (after self._current_stream = stream), not in _set_uri — new stream is the semantic boundary, not each URI update"
  - "Handler emits signal only; no QTimer marshalling needed (unlike _on_gst_error) — UI slot handles main-thread affinity via queued connection"

patterns-established:
  - "Signal de-dup via instance sentinel (-1) reset at stream-change boundary"
  - "Defensive PyGObject return-type handling: result[0] if isinstance(result, tuple) else int(result)"

requirements-completed: []

duration: ~6min
completed: 2026-04-18
---

# Phase 47.1 Plan 01: Buffer-Fill Signal Backend Summary

**New Player.buffer_percent Signal(int) sourced from GStreamer message::buffering with defensive parse, sentinel de-dup, and per-stream reset — backend half of Phase 47.1 ready for UI wiring in plan 02.**

## Performance

- **Duration:** ~6 min
- **Completed:** 2026-04-18
- **Tasks:** 2
- **Files modified:** 1 (player.py) + 1 created (test_player_buffering.py)

## Accomplishments
- Added class-level `buffer_percent = Signal(int)` on Player (D-12)
- Subscribed `bus.connect("message::buffering", self._on_gst_buffering)` on the shared playbin3 bus (D-12)
- Implemented `_on_gst_buffering` handler with defensive tuple-or-int parse (Pitfall 1), de-dup against `_last_buffer_percent` (D-14), and Qt-queued cross-thread Signal emission
- Added `_last_buffer_percent` sentinel (-1) in `__init__`, reset in `_try_next_stream` after `self._current_stream = stream` so stream changes don't mask first emit (Pitfall 3)
- New `tests/test_player_buffering.py` with 4 tests covering emit, tuple-return Pitfall 1, de-dup, and reset-on-new-stream

## Task Commits

1. **Task 1: Add buffer_percent Signal + bus subscription + de-dup sentinel to Player** — `bbfeaa5` (feat)
2. **Task 2: Create tests/test_player_buffering.py with 4 bus-handler tests** — `a14dd6b` (test)

## Files Created/Modified
- `musicstreamer/player.py` — 5 anchor-based edits:
  - Line 69: `buffer_percent = Signal(int)` class-level signal
  - Line 94: `bus.connect("message::buffering", self._on_gst_buffering)`
  - Line 142: `self._last_buffer_percent: int = -1` init sentinel
  - Lines 306-319: `_on_gst_buffering` method (inserted between `_on_gst_tag` and `_cancel_timers`)
  - Line 358: `self._last_buffer_percent = -1` reset inside `_try_next_stream`
- `tests/test_player_buffering.py` — 81 lines, 4 tests, mirrors `tests/test_player_tag.py` structure

## Verification Gates

- `grep "buffer_percent *= *Signal(int)" musicstreamer/player.py` — matches
- `grep 'bus.connect("message::buffering"' musicstreamer/player.py` — matches
- `grep "_last_buffer_percent: int = -1" musicstreamer/player.py` — matches
- `grep "_last_buffer_percent = -1  # 47.1 D-14: reset" musicstreamer/player.py` — matches
- `grep "def _on_gst_buffering" musicstreamer/player.py` — matches
- `python -c "from musicstreamer.player import Player; assert Player.buffer_percent is not None"` — OK (type: Signal)
- `python -m pytest tests/test_player_buffering.py -q` — **4 passed, 0 failed**
- `python -m pytest tests/test_player_tag.py tests/test_player_buffering.py tests/test_player_buffer.py -q` — **18 passed, 0 failed**

## Decisions Made

- `StationStream` kwarg signature confirmed against `musicstreamer/models.py`: fields `id, station_id, url, label, quality, position, stream_type, codec, bitrate_kbps` — test 4's instantiation uses the subset {id, station_id, url, codec, quality, label, position, bitrate_kbps}, all valid. No adjustment needed.
- Followed plan exactly; no architectural changes.

## Deviations from Plan

None — plan executed exactly as written. All 5 edits landed via anchor-string matching on first attempt (one edit required including the `: bool` type annotation on `_recovery_in_flight` which was in the live file but omitted in the PLAN's suggested old_string snippet — trivial adjustment, not a deviation).

## Issues Encountered

**Pre-existing environment failures (out of scope):** `tests/test_player_failover.py::test_youtube_resolve_success_sets_uri_and_arms_failover` and `test_youtube_resolve_failure_emits_error_and_advances_queue` fail with `ModuleNotFoundError: No module named 'yt_dlp'`. These tests fail on the plan's base commit (verified by reading the test body — they `import yt_dlp` unconditionally inside the test) and are unrelated to 47.1-01 changes. Logged in `deferred-items.md` under phase directory.

## Next Phase Readiness

- Plan 47.1-02 can now `connect(self._player.buffer_percent, self.now_playing.set_buffer_percent)` in `main_window.py`
- The Signal surface (D-12) and parse/de-dup/reset semantics (D-14) are all live
- No blockers for wave 2 UI work

## Self-Check: PASSED

- FOUND: musicstreamer/player.py (modified, 5 edits visible via grep)
- FOUND: tests/test_player_buffering.py (81 lines, 4 tests)
- FOUND: commit bbfeaa5 (Task 1)
- FOUND: commit a14dd6b (Task 2)

---
*Phase: 47.1-stats-for-nerds-buffer-indicator*
*Completed: 2026-04-18*
