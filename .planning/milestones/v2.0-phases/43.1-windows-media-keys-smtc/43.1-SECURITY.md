# Phase 43.1 Security Audit — Windows Media Keys / SMTC

**Phase:** 43.1 — Windows Media Keys / SMTC
**ASVS Level:** 1
**Audit Date:** 2026-04-24
**Result:** SECURED — 4/4 threats closed

---

## Threat Register Verification

| Threat ID | Category | Component | Disposition | Status | Evidence |
|-----------|----------|-----------|-------------|--------|----------|
| T-43.1-01 | DoS (Availability) | SMTC session leak on abnormal exit | mitigate | CLOSED | `musicstreamer/media_keys/smtc.py:272-301` (`shutdown()`): sets `is_enabled = False` at line 292, `MediaPlayer.close()` at line 297, independent try/except per step (lines 286-299), idempotency guard via `_shutdown_complete` at lines 283-284, 301. Verified by `tests/test_media_keys_smtc.py:530-578` (test_shutdown_detaches_handler_and_closes, test_shutdown_idempotent, test_shutdown_continues_on_partial_failure). |
| T-43.1-02 | Tampering | `_art_cache` migration + `_build_thumbnail_ref` PNG read | mitigate | CLOSED | `musicstreamer/media_keys/_art_cache.py:61-62` (int-only `station_id` TypeError guard, T-41-09 preserved); `_art_cache.py:45-51` (migration scoped to `paths.user_cache_dir()`, OSError swallowed at line 50); `musicstreamer/media_keys/smtc.py:233-270` (thumbnail build wrapped in try/except Exception, returns None at line 270). Verified by `tests/test_art_cache.py:85-93` (test_type_error_guard_preserved, parametrized over `"../evil"`, `"42"`, `3.14`, `None`); `tests/test_art_cache.py:66-82` (test_migration_swallows_errors); `tests/test_media_keys_smtc.py:467-494` (test_thumbnail_failure_clears_to_none). |
| T-43.1-03 | Information Disclosure | ImportError message + factory warning log | mitigate | CLOSED | `musicstreamer/media_keys/smtc.py:46-49` (`_INSTALL_HINT` fixed string — no env/path/stack data); `smtc.py:322-323` (`raise ImportError(_INSTALL_HINT) from exc` — `from exc` is chained context, not emitted at WARNING); `musicstreamer/media_keys/__init__.py:49-50` (factory logs `"Media keys disabled (Windows SMTC unavailable): %s"` with `%s` = exception str, no `exc_info=True`, no stack trace). Verified by `tests/test_media_keys_smtc.py:184-213` (test_create_windows_backend_import_error_on_linux, test_factory_falls_back_to_noop_on_linux) and `tests/test_media_keys_smtc.py:581-606` (test_end_to_end_factory_fallback_on_win32_without_winrt). |
| T-43.1-04 | DoS (Availability) | winrt button callback exception poisons threadpool | mitigate | CLOSED | `musicstreamer/media_keys/smtc.py:141-158` (`_on_button_pressed` body fully wrapped in `try:` / `except Exception as exc:`, logs at `_log.warning("SMTC button handler raised: %s", exc, exc_info=True)` and swallows). Verified by `tests/test_media_keys_smtc.py:302-320` (test_button_handler_swallows_exceptions — forces `args.button` property to raise RuntimeError; handler must not propagate). |

---

## Accepted Risks

None. All four threats in the Phase 43.1 register have `mitigate` disposition and all mitigations are present in committed code.

---

## Unregistered Threat Flags

None. `## Threat Flags` sections in `43.1-02-SUMMARY.md` and `43.1-03-SUMMARY.md` explicitly record "None - no new network endpoints, auth paths, or file access patterns introduced." Sub-plans 01, 04, 05, 06 SUMMARYs either omit the section (no new flags) or align with the registered threats.

---

## Post-Fix Regression Check

Three post-implementation fix commits were reviewed for mitigation regressions:

| Commit | Change | Regression Risk | Verdict |
|--------|--------|-----------------|---------|
| d002c71 | Loop-aware `asyncio.run` in `_build_thumbnail_ref` (lines 248-259) | Could bypass T-43.1-02 outer try/except if indentation moved it outside | SAFE — new code at smtc.py:248-259 remains inside the outer `try:` opened at line 233 and `except Exception as exc:` at line 266 still catches `RuntimeError` from `asyncio.get_running_loop()` or `fut.result(timeout=5.0)`. |
| 027d90a | Pinned winrt deps to `>=3.2,<4` | Could introduce version-specifier parsing gap in T-43.1-03 tests | SAFE — `tests/test_media_keys_smtc.py:150-162` (test_pyproject_has_windows_optional_deps) validates pin format explicitly. |
| c30ec92 | Seeded neutral SMTC placeholder before `is_enabled=True` (smtc.py:118-127) | Could leave session active if exception raised between placeholder seed and shutdown | SAFE — placeholder seed precedes `add_button_pressed` (line 136) and `_shutdown_complete = False` (line 137). `shutdown()` at line 272 still detaches handler via stored `_bp_token`, disables session (line 292), and closes MediaPlayer (line 297). T-43.1-01 mitigation path unchanged. |

---

## Audit Trail

| Date | Auditor | Action |
|------|---------|--------|
| 2026-04-24 | gsd-secure-phase (Claude Opus 4.7) | Verified 4/4 threats closed against committed code. No gaps, no accepted risks, no unregistered flags. Regression check for commits d002c71, 027d90a, c30ec92 passed. |

---

*Phase 43.1 — Windows Media Keys / SMTC | ASVS L1 | threats_open=0*
