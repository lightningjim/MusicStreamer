---
phase: 43.1
plan: 06
type: execute
wave: 5
depends_on: ["43.1-05"]
files_modified:
  - .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md
autonomous: false
requirements: [MEDIA-03, MEDIA-04, MEDIA-05]
user_setup:
  - service: winrt
    why: "SMTC validation requires real Windows OS + winrt wheels + media-session HID; cannot be automated on Linux CI."
    env_vars: []
    dashboard_config:
      - task: "Install winrt extras in the conda-forge env on Win11 VM"
        location: "Win11 VM PowerShell: activate conda env, run `pip install -e \".[windows]\"`"
      - task: "Open Windows media overlay"
        location: "Keyboard shortcut Win+V (or Quick Settings → Media panel)"
must_haves:
  truths:
    - "Fresh Linux-side checkout + Win11 VM pull: `pip install -e \".[windows]\"` inside the conda-forge env succeeds (all four winrt-Windows.* packages + winrt-runtime resolve)."
    - "Launching `python -m musicstreamer` on the Win11 VM does not crash at startup — either the WindowsMediaKeysBackend registers (expected) or falls back to NoOp with a WARNING log (tolerated; must be surfaced in UAT.md so Plan 05 can pivot)."
    - "Playing a SomaFM HTTPS station (verified in Phase 43 spike) produces audio on the VM."
    - "Pressing the keyboard media-play key pauses playback; pressing again resumes (MEDIA-04 + ROADMAP SC #3)."
    - "Pressing the keyboard media-stop key stops playback."
    - "Opening the Windows media overlay (Win+V) shows the MusicStreamer session with station name + ICY track title; cover art thumbnail renders (MEDIA-05 + ROADMAP SC #4)."
    - "Overlay play/pause/stop buttons trigger the app's Player."
    - "On ICY title change (wait 2-5 min or skip to a fresh station), the overlay title updates without restarting the app."
    - "Closing the app normally clears the MusicStreamer SMTC session within 5-30 seconds (T-43.1-01)."
    - "A separate run with `[windows]` extras NOT installed logs the install-hint warning and continues without media keys (D-07)."
    - "A UAT record is committed at `.planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` capturing: pass/fail per item, winrt version installed, conda env path, Python version, and any pitfalls-in-the-wild."
  artifacts:
    - path: ".planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md"
      provides: "Live-validation record signed off by the user with per-item pass/fail + environment snapshot"
      contains: "UAT"
  key_links:
    - from: ".planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md"
      to: "ROADMAP Phase 43.1 success criteria 3, 4, 5 (the manual-only items)"
      via: "UAT signs off SC #3 (media keys control Player), SC #4 (overlay shows metadata), SC #5 (fallback when winrt missing)"
      pattern: "Success Criterion"
---

<objective>
Execute live UAT for Phase 43.1 on the user's Windows 11 VM (the same env used in the Phase 43 GStreamer spike — conda-forge Python 3.12 per `.claude/skills/spike-findings-musicstreamer/SKILL.md`). Validate every manual-only item from `43.1-VALIDATION.md §"Manual-Only Verifications"` and record the results in a new `43.1-UAT.md` file. This is the only path to signing off ROADMAP success criteria #3, #4, and #5 — they are genuinely unreachable from Linux CI.

Purpose: Close the phase. Plans 01-05 deliver automated unit coverage. Plan 06 is the OS-integration verification that `add_button_pressed` actually wires to Windows media keys, that `MusicDisplayProperties.title` actually renders in the overlay, that `file://` rejection (Pitfall #2) was correctly replaced with `InMemoryRandomAccessStream`, that store_async().get() does not deadlock on Qt's STA main thread (Pitfall #3 open question), and that the enum naming assumption (Research Assumption A1 — UPPER_SNAKE_CASE) is correct on the live Python projection.

Output: `43.1-UAT.md` with pass/fail per item + environment snapshot + any pitfalls-in-the-wild, committed to git. Phase 43.1 closes after user sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.1-windows-media-keys-smtc/43.1-CONTEXT.md
@.planning/phases/43.1-windows-media-keys-smtc/43.1-RESEARCH.md
@.planning/phases/43.1-windows-media-keys-smtc/43.1-VALIDATION.md
@.planning/phases/43.1-windows-media-keys-smtc/43.1-05-SUMMARY.md
@.planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md
@.claude/skills/spike-findings-musicstreamer/SKILL.md

<interfaces>
<!-- Environment from Phase 43 spike (authoritative; do not re-derive). -->

- Host: Win11 VM
- Python: conda-forge Python 3.12.13 (same env the Phase 43 spike validated with GStreamer 1.28.2)
- Install command: inside activated conda env: `pip install -e ".[windows]"`
- Test station: SomaFM (HTTPS; Phase 43 spike proved it plays — DI.fm HTTPS is server-side broken, use SomaFM)

<!-- ROADMAP success criteria that only this plan can close. -->

ROADMAP §Phase 43.1:
  3. Pressing Windows media keys (keyboard or on-screen overlay) triggers play/pause/stop on Player; validated manually on user's Windows VM
  4. SMTC overlay shows station name, ICY track title, and cover art pixmap; updates on every `title_changed`
  5. SMTC COM/winrt initialization failure logs a warning and falls back to no-op backend — app startup never blocks on this

<!-- Open questions from RESEARCH.md that this UAT resolves empirically. -->

1. DataWriter.store_async().get() — does it deadlock on Qt's STA main thread? (Pitfall #3)
   Expected: no deadlock (typical 2-5 min ICY cadence + short blocking call). If deadlock occurs → file a follow-up issue and pivot to asyncio.run() in a hotfix.
2. Enum member casing — is it UPPER_SNAKE_CASE? (Assumption A1)
   Diagnostic: `python -c "from winrt.windows.media import SystemMediaTransportControlsButton; print(list(SystemMediaTransportControlsButton))"`
3. `du.thumbnail = None` — does clearing to None work or raise? (Assumption A6)
   Expected: None assignment is tolerated; SMTC falls back to app icon.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install winrt extras on Win11 VM and capture environment snapshot</name>
  <files>N/A — manual checkpoint; Task 3 authors .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md</files>
  <read_first>
    - .claude/skills/spike-findings-musicstreamer/SKILL.md (conda-forge env activation steps)
    - .planning/phases/43-gstreamer-windows-spike/43-SPIKE-FINDINGS.md (the environment Plan 06 inherits)
    - pyproject.toml (confirm [project.optional-dependencies].windows lists the four packages)
  </read_first>
  <what-built>
    Plans 01-05 produced:
    - Renamed cache subdir `mpris-art/ → media-art/` with migration (Plan 01)
    - `[project.optional-dependencies].windows` group in pyproject.toml (Plan 02)
    - `WindowsMediaKeysBackend` with full lifecycle (`__init__`, `_on_button_pressed`, `_apply_playback_state`, `publish_metadata`, `_build_thumbnail_ref`, `shutdown`) implementing CONTEXT D-01 through D-10 (Plans 03-05)
    - 24+ unit tests on Linux with mocked winrt (all green)
  </what-built>
  <action>
    This is a checkpoint:human-action task — Claude cannot run PowerShell or install wheels on the Win11 VM. The user executes the 6 steps below and reports results via `<resume-signal>` so Task 3 can populate the UAT.md file.

    On the Win11 VM (same env used for Phase 43 spike):

    1. Open PowerShell. Activate the conda-forge env used during Phase 43 spike:
       ```powershell
       conda activate musicstreamer-spike  # or whatever name was used in Phase 43
       ```
    2. Pull the latest main branch on the VM (git fetch + merge).
    3. Install winrt extras:
       ```powershell
       pip install -e ".[windows]"
       ```
       Expected: all four `winrt-Windows.*` packages install, plus `winrt-runtime` as transitive dep. Note the exact versions output.
    4. Capture environment snapshot (paste into UAT record below):
       ```powershell
       python --version
       conda info --envs
       pip list | findstr winrt
       ```
    5. Run the enum-casing diagnostic (resolves RESEARCH Open Question 2 / Assumption A1):
       ```powershell
       python -c "from winrt.windows.media import SystemMediaTransportControlsButton; print(list(SystemMediaTransportControlsButton))"
       ```
       Expected: Python lists enum members. Record the EXACT casing observed (`PLAY` vs `Play`). If casing differs from `UPPER_SNAKE_CASE`, STOP and file a follow-up issue — the code in `smtc.py` assumes `PLAY`/`PAUSE`/`STOP` and will need updating before continuing.
    6. Launch the app:
       ```powershell
       python -m musicstreamer
       ```
       Expected: window opens, no crash, no console traceback. If the factory falls back to NoOp with a warning log, that is tolerated but must be recorded — it likely means a winrt subpackage failed to resolve.
  </action>
  <verify>
    <automated>MANUAL — user executes steps on Win11 VM; verification is Task 3 UAT.md containing the captured environment snapshot + enum casing output</automated>
  </verify>
  <resume-signal>After running the 6 steps above, paste the results into the Environment Snapshot section of `43.1-UAT.md` (Task 3 creates that file). Type "env-ok" if enum casing is UPPER_SNAKE_CASE and the app launched without crashing, or "env-blocked" with a description of what failed. If "env-blocked", STOP and file a follow-up plan before continuing.</resume-signal>
  <done>Environment snapshot captured; enum casing verified UPPER_SNAKE_CASE (or casing deviation documented + follow-up filed); app launches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute UAT items (media keys, overlay, shutdown, missing-deps fallback)</name>
  <files>N/A — manual verification; Task 3 records results in .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md</files>
  <read_first>
    - .planning/phases/43.1-windows-media-keys-smtc/43.1-VALIDATION.md lines 83-93 ("Manual-Only Verifications" table — the authoritative UAT list)
    - .planning/phases/43.1-windows-media-keys-smtc/43.1-RESEARCH.md §"Pitfalls" lines 386-447 (what to watch for during live testing)
  </read_first>
  <what-built>
    Task 1 completed environment setup + enum sanity check; the app now runs on the Win11 VM with the Windows SMTC backend wired. This task exercises the live integration.
  </what-built>
  <action>
    This is a checkpoint:human-verify task — Claude cannot press keyboard media keys, inspect the Windows overlay, or observe audio. The user executes each UAT item in order and records results per item; Task 3 formalizes them in UAT.md.

    Execute each of these 7 items in order. Record pass/fail + observed behavior per item in `43.1-UAT.md §"UAT Results"`:

    **UAT-1: Keyboard media keys pause/resume (MEDIA-04 + SC#3)**
    1. In the running app, select a SomaFM HTTPS station (e.g., Groove Salad) from the station list.
    2. Confirm audio is playing.
    3. Press the keyboard media-play key (on most laptops: Fn + F-key with play icon; on desktop keyboards: dedicated play/pause key).
    4. Expected: audio pauses.
    5. Press the same key again.
    6. Expected: audio resumes.
    Record: PASS or FAIL + any observations.

    **UAT-2: Keyboard media-stop key stops playback (MEDIA-04 + SC#3)**
    1. With audio playing, press the keyboard media-stop key (if available — some keyboards do not have this).
    2. Expected: playback stops; NowPlayingPanel shows stopped state.
    Record: PASS / FAIL / N-A (no media-stop key on this keyboard).

    **UAT-3: SMTC overlay shows station + ICY title + cover art (MEDIA-05 + SC#4)**
    1. Start SomaFM playing.
    2. Press Win+V (or open Quick Settings → Media panel).
    3. Expected: overlay shows a MusicStreamer session with:
       - Title field: an ICY track title (initially may be the station name until the first ICY update arrives — that's the MPRIS2 parity behavior, acceptable)
       - Artist field: the station name (e.g., "Groove Salad")
       - Thumbnail: cover art pixmap (may briefly show a default iTunes fetch placeholder before the real cover arrives)
    Record: observed title, artist, thumbnail state.

    **UAT-4: Overlay refreshes on ICY title change (MEDIA-05 + SC#4)**
    1. With overlay open + SomaFM playing, wait 2-5 minutes for the next ICY title update (visible in the app's NowPlayingPanel).
    2. Expected: the overlay title refreshes WITHOUT requiring any user action.
    Alternatively: skip to a different SomaFM station to force an immediate ICY title change.
    Record: observed latency between NowPlayingPanel update and overlay update (should be < 1s).

    **UAT-5: Overlay buttons control Player (MEDIA-04 + SC#3)**
    1. With overlay open, click the pause button in the overlay.
    2. Expected: audio pauses; NowPlayingPanel reflects paused state.
    3. Click play in the overlay.
    4. Expected: audio resumes.
    5. Click stop in the overlay.
    6. Expected: audio stops.
    Record: PASS / FAIL per button.

    **UAT-6: Normal shutdown clears SMTC session (T-43.1-01 / SC#5)**
    1. Close the app via the window X button.
    2. Wait 5-30 seconds.
    3. Open the media overlay (Win+V).
    4. Expected: MusicStreamer session is no longer listed.
    Record: time to session-clear (5-30s window is acceptable per Pitfall #5).

    **UAT-7: Missing-deps fallback (D-07 / SC#5)**
    1. Create a fresh conda env WITHOUT installing `[windows]` extras:
       ```powershell
       conda create -n ms-nowinrt python=3.12 -y
       conda activate ms-nowinrt
       pip install -e .  # NOT .[windows]
       ```
    2. Launch: `python -m musicstreamer`.
    3. Expected: app window opens without crash; warning log in console reads roughly "Media keys disabled (Windows SMTC unavailable): ... pip install -e '.[windows]' ..." .
    4. Expected: playback works (GStreamer independent of winrt) but media keys have no effect.
    Record: PASS / FAIL + exact warning-log text.

    **Pitfall watch list (observe during all tests, record ANY occurrence):**
    - Pitfall #1: If _on_button_pressed never fires → check `command_manager.is_enabled = False` landed. If still broken → file follow-up.
    - Pitfall #3 DEADLOCK: If app freezes on first `publish_metadata` call → `DataWriter.store_async().get()` is deadlocking. Record freeze duration + file follow-up to pivot to `asyncio.run()`.
    - Pitfall #4: If app crashes at close → token mismatch in `remove_button_pressed`. File follow-up.
    - Assumption A6: If `thumbnail = None` raises instead of clearing → observed; record and file follow-up (use a sentinel RandomAccessStreamReference instead).
  </action>
  <verify>
    <automated>MANUAL — user performs each UAT step on Win11 VM; verification is Task 3 UAT.md having a PASS/FAIL verdict per item UAT-1 through UAT-7</automated>
  </verify>
  <resume-signal>After completing all 7 UAT items + watching for pitfalls, type "uat-pass" if ALL items (UAT-1 through UAT-7) passed, OR type "uat-fail: <item-id>" with a list of which UAT items failed and why. On "uat-fail", the orchestrator routes to a gap-closure plan before phase close.</resume-signal>
  <done>All 7 UAT items executed with observations recorded; pitfall watch list complete.</done>
</task>

<task type="auto">
  <name>Task 3: Author 43.1-UAT.md with env snapshot + UAT results + sign-off</name>
  <files>.planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md</files>
  <read_first>
    - .planning/phases/43.1-windows-media-keys-smtc/43.1-VALIDATION.md (authoritative UAT list — mirror its structure)
    - The results captured during Task 1 + Task 2
  </read_first>
  <action>
    Create `.planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` with the structure below. Fill each section from the observations recorded during Task 1 and Task 2. Commit the file with `git add` + `git commit` via normal phase-close flow.

    ```markdown
    ---
    phase: 43.1
    slug: windows-media-keys-smtc
    uat_date: <YYYY-MM-DD>
    uat_host: Win11 VM
    signed_off_by: <user>
    status: <passed|failed|partial>
    ---

    # Phase 43.1 — Windows Media Keys (SMTC) UAT Record

    > Live validation of MEDIA-03 / MEDIA-04 / MEDIA-05 success criteria 3, 4, 5 from ROADMAP §Phase 43.1.

    ## Environment Snapshot

    | Property | Value |
    |----------|-------|
    | Host OS | Windows 11 (build: <run `winver`>) |
    | Python | <paste `python --version`> |
    | Conda env | <paste `conda info --envs` active-env line> |
    | winrt packages | <paste `pip list \| findstr winrt`> |
    | GStreamer bundle | Phase 43 conda-forge GStreamer 1.28.2 (unchanged) |
    | App commit SHA | <git rev-parse HEAD> |

    ### Enum casing sanity check (Research A1)

    Command: `python -c "from winrt.windows.media import SystemMediaTransportControlsButton; print(list(SystemMediaTransportControlsButton))"`

    Output: <paste verbatim>

    Verdict: UPPER_SNAKE_CASE confirmed / casing deviation observed (follow-up: <link>).

    ## UAT Results

    | ID | Success Criterion | Result | Observation |
    |----|-------------------|--------|-------------|
    | UAT-1 | Keyboard media keys pause/resume (SC#3) | PASS/FAIL | <notes> |
    | UAT-2 | Keyboard media-stop key (SC#3) | PASS/FAIL/N-A | <notes> |
    | UAT-3 | Overlay shows station + ICY + art (SC#4) | PASS/FAIL | <notes> |
    | UAT-4 | Overlay refreshes on ICY change (SC#4) | PASS/FAIL | Latency: <ms> |
    | UAT-5 | Overlay buttons control Player (SC#3) | PASS/FAIL | play: , pause: , stop: |
    | UAT-6 | Shutdown clears SMTC session (SC#5 / T-43.1-01) | PASS/FAIL | Clear latency: <s> |
    | UAT-7 | Missing-deps fallback (SC#5 / D-07) | PASS/FAIL | Warning log: <quote> |

    ## Pitfall Observations

    - **Pitfall #1** (command_manager.is_enabled = False): <observed / no issue>
    - **Pitfall #3** (DataWriter.store_async().get() deadlock): <observed / no issue>
    - **Pitfall #4** (event token mismatch at shutdown): <observed / no issue>
    - **Assumption A6** (thumbnail = None): <tolerated / raises — follow-up filed>

    ## Follow-ups Filed

    - <links to any follow-up issues or plans created for pitfalls that surfaced>

    ## Sign-off

    All blocking UAT items pass / failures addressed by gap-closure plan <link>.

    Signed: <user> <date>
    ```

    After filling in, commit via the phase-close hook (execute-plan handles this).
  </action>
  <verify>
    <automated>test -f .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md && grep -c "PASS\|FAIL\|N-A\|N/A" .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` exits 0.
    - `grep -c "UAT-[1-7]" .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` returns >= 7 (all 7 UAT items present in results table).
    - `grep -c "PASS\|FAIL\|N-A\|N/A" .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` returns >= 7 (each item has a verdict).
    - `grep -c "Environment Snapshot\|UAT Results\|Pitfall Observations\|Sign-off" .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` returns 4 (all four required sections present).
    - Frontmatter `status:` is one of `passed`, `failed`, `partial`.
    - File committed to git (not just staged): `git log --oneline -1 .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` returns a commit.
  </acceptance_criteria>
  <done>43.1-UAT.md exists with env snapshot + 7 UAT results + pitfall observations + sign-off; committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Win11 VM → live winrt API surface | UAT exercises real winrt call paths; any deviation from unit-test mocks surfaces here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.1-01 | Denial of Service | Abnormal exit leaves SMTC session | mitigate | UAT-6 validates normal-shutdown session clear within 5-30s. Pitfall #5 (abnormal termination) is observational — if the session lingers beyond 30s after a kill -9, record in UAT and file a follow-up to call `_smtc.is_enabled = False` via a crash handler. Not a blocker for phase close. |
| T-43.1-03 | Information Disclosure | D-07 fallback warning (UAT-7) | mitigate | UAT-7 captures the exact warning log text; verdict PASS requires the log to contain the install hint but no system paths or stack trace. If the log leaks PII-adjacent info, record as FAIL and pivot. |
| T-43.1-04 | Denial of Service | winrt callback exception poisons threadpool | mitigate | Unit-tested in Plan 03 (`test_button_handler_swallows_exceptions`). Not re-tested here — live VM cannot inject exceptions into the winrt threadpool without app modification. Accepted via automated coverage. |
</threat_model>

<verification>
- `ls .planning/phases/43.1-windows-media-keys-smtc/43.1-UAT.md` exists.
- UAT.md contains all 7 items + env snapshot + pitfall observations + sign-off line.
- Git log shows the UAT commit.
- If any UAT item is FAIL, a gap-closure plan was filed before phase close (checked in the orchestrator layer, not here).
</verification>

<success_criteria>
1. Environment snapshot captured (winrt versions, Python, conda env, enum casing confirmed).
2. All 7 UAT items executed with verdicts recorded.
3. Pitfall watch list complete (Pitfall #1, #3, #4, Assumption A6 each have an observation).
4. `43.1-UAT.md` committed to git.
5. User signs off OR failures are tracked by a gap-closure plan.
6. ROADMAP §Phase 43.1 success criteria #3, #4, #5 are now verifiable via this record.
</success_criteria>

<output>
After completion, create `.planning/phases/43.1-windows-media-keys-smtc/43.1-06-SUMMARY.md`. Link to `43.1-UAT.md`.
</output>
</content>
</invoke>